

## v577 -> v578 CYCLE 243 1-VERDICT gap4v2 semantic-A 280-atom GPU re-measure MIDDLE_BAND CROSS-HARNESS-CALIBRATION (verdict_handler 472nd PROT-009 paired commit; 0 HP; 1 MIDDLE_BAND [exp_gap4v2_semantic_A_eval_gpu_v1]; 0 HARD_FAIL; 0 LVH; 0 NEW PP ROWS; PP-401 annotated SECOND-HARNESS A-axis reading + corpus-size-stamping methodology fix accepted; cross-harness calibration finding NOT regression; substrate-quality-first frame held; Portfolio 32+403 UNCHANGED; HONEST 1840->1841 +1; LVH 291->291 +0) (2026-06-12)

### Step 0 honest re-read

Metrics source: REMOTE BRIDGE STALE (is_stale=True; get_metrics returned None for exp_gap4v2_semantic_A_eval_gpu_v1). Verdict file body used as authoritative; Exp-Dev verdict file is methodologically textbook honest -- explicitly REFUSES to claim regression and surfaces the calibration ambiguity transparently.

**exp_gap4v2_semantic_A_eval_gpu_v1 MIDDLE_BAND (HONEST):** per-k F1 sweep {5: 0.2937, 8: 0.2966, 12: 0.2704, 16: 0.2337}; best-k=8 F1=0.2966 in pre-reg MIDDLE band [0.22-0.30]; +0.112 over keyword baseline 0.185; n_A=12; corpus 280-algebra-atom / 1782-total at measurement time; device=cuda confirmed on home GPU; bge-large on CUDA. Verdict file headline "0.297 MIDDLE PRIOR_NOT_CLEANLY_VERIFIABLE" is HONEST -- the file body explicitly states: "I will NOT claim a regression (0.369 -> 0.297). The defensible statement: at the current corpus, semantic-A best-k=8 = 0.2966 (MIDDLE), +0.11 over keyword. Whether breadth backfill helped or hurt vs a clean prior is indeterminate from persisted artifacts." No over-claim. Substrate-quality-first frame held (no LLM comparison). HONEST.

**Critical calibration distinction (per USER prompt + Exp-Dev verdict body): the "prior 0.369 not cleanly verifiable" is a MEASUREMENT-METHODOLOGY artifact, NOT a substrate-capability regression.** Three reasons:
1. The persisted base-anchor metrics.json (02:17) reads 0.0 HARD_FAIL -- that is the BUGGY pre-rebuild_index() run. The 0.369 figure from last session's stdout did NOT persist to any verifiable full-run artifact at that anchor.
2. The persisted Testbed bench_reports use a DIFFERENT harness (top_k=15 fixed, mean_f1=0.356, keyword 0.283, lift +0.073) -- not apples-to-apples with the Exp-Dev best-k {5,8,12,16} sweep + keyword 0.185 baseline.
3. Total corpus size at the prior measurement is not stamped in any persisted artifact, so distractor-density change vs prior cannot be isolated.

**Three concurrent A-axis readings exist across distinct harnesses (1742-1782 atom corpus range):**
- Testbed UNION top_k=5 (Cycle 49, commit a8f0843f, 1742-atom corpus): A_content macro F1 = 0.446 -- AUTHORITATIVE substrate A-axis number, PP-401 annotated baseline.
- Testbed bench fixed top_k=15 (Cycle 49, post-280-atom): mean_f1 = 0.356, keyword 0.283, lift +0.073.
- Exp-Dev GPU best-k sweep (this cycle, 280-algebra-atom / 1782-total): best-k=8 F1 = 0.2966, keyword 0.185, lift +0.112.

The three readings disagree because they MEASURE DIFFERENT THINGS (fusion strategy: UNION vs bge-only fixed-k vs best-k sweep; baseline: bge-description vs Testbed-internal vs keyword 0.185). This is a CROSS-HARNESS CALIBRATION finding, not substrate evidence about A-axis capability. The authoritative substrate A-axis number remains Cycle 49 UNION = 0.446 until a single harness re-runs cleanly before/after a corpus-size-stamped delta.

HONEST: 1840 -> 1841 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v577 -> v578 CYCLE 243)

**(A) exp_gap4v2_semantic_A_eval_gpu_v1 (MIDDLE_BAND -- PP-401 annotation; cross-harness calibration finding; no new PP row):**
PP-401 ANNOTATION (no new PP row): qa_self_knowing gap4v2 semantic-A second-harness reading v578: Exp-Dev best-k sweep on home GPU at 280-algebra-atom / 1782-total corpus = best-k=8 F1 0.2966 (+0.112 over keyword 0.185, MIDDLE pre-reg [0.22-0.30]), n_A=12, n_seeds=1 cycle 243 GPU bench. CROSS-HARNESS CALIBRATION FINDING NOT REGRESSION: three concurrent A-axis readings exist (Testbed UNION top_k=5 = 0.446 authoritative; Testbed bench fixed-k = 0.356; Exp-Dev best-k sweep = 0.297) measuring different fusion strategies and baselines. The prior 0.369 stdout figure was overwritten by buggy 0.0 run; no persisted full-run artifact at consistent harness exists for clean before/after delta. Exp-Dev verdict file is METHODOLOGICALLY TEXTBOOK HONEST -- refuses to claim regression, surfaces three independent root causes of ambiguity (buggy persisted base, different harness in bench_reports, no corpus-size stamping), and recommends process fix (stamp n_total_atoms + n_algebra_atoms into every gap4v2 metrics.json going forward). PROCESS RECOMMENDATION ACCEPTED: corpus-size stamping becomes mandatory for all gap4v2 metrics writes going forward (Exp-Dev volunteered one-line addition). With stamping, batch-2 ingest (commit bdf217c7) gives the first clean incremental delta. UNVERIFIED hypothesis flagged by Exp-Dev (not adopted as finding): distractor-density effect -- more bge-NAME-friendly atoms in the pool = more high-similarity distractors in top-k for questions whose gold is other atoms -> precision drops -> F1 drops. Consistent with the gazetteer sign-flip finding (PP-403) and substrate-aux-features-shrink-with-data principle. CANNOT BE CONFIRMED WITHOUT CORPUS-SIZE-STAMPED BEFORE/AFTER. Authoritative substrate A-axis number remains Cycle 49 Testbed UNION = 0.446 (1742-atom, commit a8f0843f). P-band: 0.43-0.48 EXPLORATORY UNCHANGED (Cycle 49 UNION authoritative; today's reading is calibration-distinct second harness, not a refresh of the band). Cross-ref PP-401 v575 macro F1 0.4658 + v576 UNION_top_k5 A 0.446, PP-403 (substrate-aux-shrink sign-flip mechanism analogue to distractor-density hypothesis), Cycle 49 Testbed close note, Exp-Dev verdict file 2026-06-12 GAP4V2_SEMANTIC_A_280ATOM_REMEASURE_0297.

ANNOTATIONS this cycle:
- PP-401 (qa_self_knowledge): A-axis SECOND-HARNESS reading 0.2966 best-k=8 added; calibration-distinct from Cycle 49 UNION 0.446 (different harness, different fusion strategy, different baseline). Cycle 49 UNION = 0.446 remains AUTHORITATIVE A-axis number. P-band unchanged.
- substrate methodology rule candidate: METRICS WRITES SHOULD STAMP CORPUS-SIZE -- any benchmark that may be re-run as the corpus grows should persist n_total_atoms + n_algebra_atoms (and any other relevant corpus-size scalars) so before/after deltas survive corpus changes. 1st appearance in cap_map -- candidate rule, not yet promoted. Pairs with verify-before-asserting Exp-Dev rule and substrate-as-ground-truth methodology.
- Substrate-product positioning artifact: cross-harness reading divergence (0.446 UNION vs 0.356 bench vs 0.297 best-k-sweep) demonstrates that A-axis capability is MEASURED differently by three valid harnesses -- the authoritative number is the one tied to the production retrieval architecture (UNION HYBRID). Future tracking should fix the harness or stamp it explicitly.
- Distractor-density hypothesis FLAGGED, NOT ADOPTED: more bge-NAME-friendly atoms -> more high-similarity distractors -> precision drops. Mechanism-class consistent with PP-403 aux-features sign-flip and substrate-aux-features-shrink-with-data memory; verify via corpus-size-stamped batch-2 ingest delta in future cycle.
- Exp-Dev verify-before-asserting + honest-self-correct: textbook execution this cycle (refused to claim 0.369->0.297 regression; surfaced three independent ambiguity root causes; flagged unverified mechanism explicitly as hypothesis not finding).

PROT-004/006 NOT APPLICABLE: MIDDLE_BAND in pre-reg band; no closure triggered. Path-to-clean-A-axis-delta rescue sketches (cheapest first):
RESCUE-1 (cheapest/subsumption): accept Exp-Dev's corpus-size stamping proposal -- add n_total_atoms + n_algebra_atoms one-line addition to gap4v2 metrics.json on next run; ~1 line of code. Then batch-2 ingest (commit bdf217c7) gives first clean incremental delta. Inline rescue (Exp-Dev volunteered on its own session cadence).
RESCUE-2: fix the harness for tracking. Designate UNION top_k=5 (production retrieval architecture) as the standing A-axis tracker; deprecate the parallel best-k sweep harness for cap_map tracking purposes. Best-k sweep remains useful for k-sensitivity analysis but not for cap_map A-axis baseline. ~2 lines methodology pin in cap_map.
RESCUE-3: re-run UNION top_k=5 at the current corpus size (1782 atoms total, 280 algebra atoms) to update the authoritative A-axis baseline from the Cycle 49 1742-atom point. Pre-reg A axis 0.44-0.50 (Cycle 49 UNION 0.446 plus or minus breadth/distractor effects). If hits 0.50: HP PASS on A-axis pre-reg. Already in flight per Cycle 49 close note ("UNION + batch 2 (1782 atoms post-ingest) compound bench in flight on remote").
RESCUE-4: re-measure prior 0.369 figure under the same Exp-Dev best-k sweep harness at the prior corpus snapshot (if git history preserves the prior corpus state). Confirms whether 0.369 was the same harness or a hallucinated/misremembered figure.
RESCUE-5: distractor-density mechanism test -- ablate the breadth-50 batch atoms from the pool and re-run the best-k sweep to isolate distractor effect from corpus-density effect. ~10 lines of pool-filter wrapper.

Routing files (written to disk, NOT auto-dispatched):
- strategy_request_to_exp_dev_2026-06-12_gap4v2_corpus_size_stamping.md -- accepts Exp-Dev's RESCUE-1 corpus-size-stamping proposal and asks for the one-line addition on next gap4v2 metrics write. Exp-Dev session will pick on its own 15-min cadence per 4-session architecture.

Cap_map: v577 -> v578 CYCLE 243 (0 HP; 1 MIDDLE_BAND [exp_gap4v2_semantic_A_eval_gpu_v1=PP-401-annotation]; 0 HARD_FAIL; 0 LVH; 0 NEW PP ROWS; PP-401 second-harness A-axis reading annotation 0.2966 best-k=8 at 280-algebra-atom corpus; cross-harness calibration finding NOT regression; corpus-size-stamping methodology fix accepted RESCUE-1 routed to Exp-Dev; distractor-density mechanism FLAGGED-NOT-ADOPTED hypothesis; Cycle 49 UNION 0.446 remains AUTHORITATIVE A-axis baseline; substrate-quality-first frame held; Portfolio 32+403 UNCHANGED; HONEST 1840->1841 +1; LVH 291->291 +0; 472nd PROT-009 paired commit) (2026-06-12)
