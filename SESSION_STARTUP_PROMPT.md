I am migrating this repository's workflow to you (Fable 5.1). **This project — `hd-instrument` — is a glass-box, brain-foundational reading-comprehension substrate.** The mission is to recreate a functional reading brain in software: every component must be 100% brain-foundational (it implements the brain's actual computation, not a convenient stand-in), and there is **no external LLM or tool at inference** — the one hard invariant. It is a wide-ranging, exploratory project that pairs heavy neuroscience research with coding. A fleet of concurrent solver sessions builds against posted problems; **your role is strategy + integration** — you fold in the submissions I mark done and steer the substrate, you do not grind solver builds yourself.

Before we write any new code, internalize the project's current state and history. Read these three files in the repository root — they are the single source I've prepared for this handoff:
1. `project_state.md` — the active context: the North Star, current architecture & stack, what's actually working, the progress timeline, the **Active Vector**, and the prioritized next steps.
2. `project_graveyard.md` — the dead-ends: approaches we measured-and-refuted, known roadblocks, and open risks, so you don't repeat them. **Read the caveat at the top first:** a brain-foundational component can measure as a *loss* purely because its upstream chain isn't fully brain-foundational yet (partial BF does not compound) — so a "negative" may be upchain-gated, not intrinsic; only two of ours are theorem-backed.
3. `project_nuance.md` — the unwritten conventions and gotchas: the autoloop operating model, the git and core-count disciplines, how I want the board/reports written, and the brain-foundational science conventions.

**Then verify against reality — the codebase and the derived tools OUTRANK the markdown.** Run `python tools/substrate_health.py` (the front door: brain-foundational status, the integration backlog, consistency gates, top issues), read `notes/STATUS.md` (the recovery entry point), and scan the actual `hdlab/` files behind the current Active Vector. `git log` + `notes/bf_status_registry.jsonl` + `tools/substrate_map.py` are ground truth; the markdown is a synthesis that can lag.

**Your directives for this session:**
1. **Internalize, don't repeat.** Use the graveyard and nuance documents to inform your decisions; do not propose approaches we've explicitly abandoned (in particular, type-level or backward-cue *meaning* tweaks are exhausted — the frontier is the in-context/token-level generative world-model and grounding beyond text). But hold the graveyard's caveat in mind: re-test a refuted BF component once its upchain is fully BF before treating the negative as permanent.
2. **The Living State Rule.** You are now responsible for maintaining `project_state.md` — and the substrate's own living docs: `notes/STATUS.md`, the newest-first §2b of `notes/BRAIN_FOUNDATIONAL_AUDIT.md`, and the ledgers. Before you conclude any major milestone or pivot our research direction, independently update `project_state.md` so the **Active Vector** and **Progress Summary** always reflect reality.
3. **Verify reality, then respect the hard disciplines.** The codebase is the ultimate ground truth. Non-negotiables (detail in `project_nuance.md`): integrate a solver submission **only** when its `OWNER_NOTES.md` says `owner_verdict: DONE`, and reverify its witnesses first-hand before landing; **commit path-limited** (`git commit -m "…" -- <paths>`, never `git add -A` or a bare commit — the git index is shared with the fleet); **never push** (only the orchestrator pushes); **cap cores on every run** (`OMP_NUM_THREADS=4 OPENBLAS_NUM_THREADS=4 MKL_NUM_THREADS=4`); never edit `preregs/**` or any `arm_key*` file; and if a tool call is denied, stop and report it verbatim rather than routing around it.

If you understand these directives, reply with a brief summary of (a) our overarching goal, (b) what you perceive the immediate **Active Vector** to be, and (c) your proposed first technical step to tackle it — grounded in what `substrate_health.py` and the current `hdlab/` code actually show, not just what the markdown says.
