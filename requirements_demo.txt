# v1 demo backend requirements.
# Install: .venv-demo\Scripts\python.exe -m pip install -r requirements_demo.txt
#
# Note: torch is installed separately with the CUDA-12.1 index URL since pip doesn't
# auto-pick the GPU wheel.

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-dotenv>=1.0
httpx>=0.27

# LLM clients
openai>=1.50
anthropic>=0.40

# Substrate library deps
numpy>=1.26,<3
psutil>=5.9

# Tier-5 PATH A (Pythia-1.4B serving)
# NOTE: transformers 5.x requires torch.float8_e8m0fnu which doesn't exist in torch 2.5.1.
# Pin transformers<5 + tokenizers<0.21 + huggingface-hub<0.30 for torch-2.5 compat on RTX 4060 Ti.
transformers>=4.45,<5
tokenizers<0.21
huggingface-hub<0.30
accelerate>=0.30
sentencepiece>=0.2
protobuf>=4.25

# Encoders for two-stage disambig + retrieval
sentence-transformers>=3.0

# IMPORTANT: pyarrow 24.x has a Windows DLL load conflict that segfaults (0xC0000005)
# when imported through the sentence_transformers -> sklearn -> pandas -> pyarrow chain.
# 2026-06-09 incident: fresh processes started crashing on `import sentence_transformers`
# even though running ones (PubMed ingest) survived. Faulthandler trace shows pyarrow/__init__.py:71
# (pyarrow.lib C-extension load) is the crash site. Pinning <24 forces a stable version.
pyarrow>=19,<24
# datasets 5.0 requires pyarrow.json_ (added in 19); 20.x verified working
datasets>=4.0
pandas>=2.0

# NER (Wikipedia ingest fallback path when Llama-8B unavailable)
spacy>=3.7

# Cross-encoder rerank (cycle 187 setup)
# (already covered by sentence-transformers)

# Optional: dev / testing
pytest>=8.0
