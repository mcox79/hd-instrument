# SKUNKWORKS (cert-owner) RESUME ANCHOR -- compaction 2026-06-19 (2nd this session). **USER STRATEGIC REDIRECT is the headline. Read it first.**

## *** USER STRATEGIC REDIRECT (survives compaction; changes program priority) ***
USER 2026-06-19, 3-message redirect:
1. "head to head comparisons with LLMs... I really don't think that's useful. Let's just keep improving substrate." -> **HALT the substrate-vs-LLM POSITIONING** (sentiment/textclass/math head-to-heads = HALTED, pulled from queue, atoms stay LEGACY).
2. "why do we care about cert grade... Shouldn't we be focusing on capabilities?" -> **cert-grade is an INSTRUMENT (verifies what's real + load-bearing), NOT the goal.** Capability is the goal.
3. "going through all the experiments and certifying them / prioritizing the truly enabling is key, then we can focus on new things." -> **THE SEQUENCE: certify the backlog + prioritize the TRULY-ENABLING -> THEN new development.** (Consolidate-then-grow.)
- **GLASS-BOX-LLM CONTINUES** (USER-confirmed; distinct from positioning -- it BUILDS a trackable LLM on the substrate; Pythia-KV/encoder-ingest/KNOWN-COMPOSED-PREDICTED tiers/Phase 3 all continue).
- Memory written: `feedback_capability_dev_is_goal_cert_grade_is_instrument_USER_2026-06-19` (+ MEMORY.md index line). Composes feedback_no_llm_comparisons + substrate_standalone_capability_first.

## "TRULY ENABLING" = "what BUILDS ON this?" (the operational def; both my tool + Research converged)
Build-on hierarchy (the priority): **composition #1** (EVERYTHING stacks on it: KG-traversal IS composition, reasoning stacks on it, glass-box Phase-3 built on multi-hop composition; q_b1 depth-extension is the model) -> sparse-boundary #2 (storage stacks on alpha-calibration) -> KG #3 -> continual+drift #4 -> **refuse-gate #5 (DEMOTED from #1: substrate's USP-vs-LLM but NOTHING builds on it = terminal quality, not foundation; the demotion caught our own positioning-bias)** -> capacity #6. **Phase 0c/0d regime-map = BEFORE all (meta-enabling: where each capability has headroom).**

## SUBSTRATE STATE (verify on resume via invariant-check)
CERT 589 / integrated 492 / axiom 206 / TRUE-HARD-PASS / 0 graph-hygiene flags. (q_b1 d300-d500 extent atom CERT 590 PENDING Exp-Dev atomize -> my landed-VET.) Run `tools/skunkworks_substrate_invariant_check_v1.py` first.

## PENDING REACTIVE QUEUE (next Skunkworks session resumes here)
1. **GPU verdict-VETs** (in flight, marker-verify FIRST): Pythia-KV (substrate KV-memory; KEEP -- glass-box) + effective-rank-SVD + neurogenesis. NOT the halted head-to-heads.
2. **CSP-first ship LANDED-VET = the PHASE-1 MILESTONE GATE** (0->1 ships). Baseline LOCKED (`tools/skunkworks_ship_regression_snapshot_v1.py --set csp`, committed 02dbdf3b): 9 atoms must reproduce (5 PASS/2 MIDDLE/2 HARD_FAIL) + M_critical/recall within 5% + post_ship speedup>=2.0 no-recall-degrade + I7/I8/I9 + version-marker. ANY shift -> ROLLBACK (flag toggle). Pin the SINGLE-exp_ hp12 atom (doubled-exp_ = inert smoke).
3. **Atomization landed-VETs:** q_b1-590 (operating_point on q_b1 cluster + A5-careful canonical proven_bound update) + any others.
4. **drift_detection verdict-VET** (canonical a7_kappa3 MIDDLE -> v1.2 swap-gate if a variant beats it).
5. **NER v3** reconstruct (metrics clobbered; marker-gated).
6. **TIER-2 ENABLING pre-reg wave SCHEMA-VETs** (Research authors ON MY SIGNAL when queue drains; order: composition -> sparse-boundary -> KG-fb15k237 -> continual+drift -> refuse-gate -> capacity). Apply: gate-mechanism-not-cliff + can-fail-BOTH-directions (tautology+unreachable, verify by data-dry-run) + prompt-fairness=best-prompted (math=few-shot/CoT, NOT zero-shot).

## CERT-ARCHITECTURE BUILT THIS SESSION (committed)
- integration-check **v1.3** (I10 op-series over-mint forward-guard; ccaee47e) + --json on both checks.
- **ship-regression-snapshot tool** (02dbdf3b) -- C1 Phase-1-ship landed-VET baseline capture.
- value-coverage check **re-weighted by enabling-ness** (0bf4a5e0; +CORE-ENABLING bonus composition/capacity/KG/highval, -POSITIONING penalty).
- operating-point-series cluster type adopted; C1 state-change protocol live.

## DISCIPLINES ENCODED THIS SESSION
gate-the-MECHANISM-not-the-cliff (cliff=reported measurement) + can-fail-BOTH-directions (tautology=always-true / unreachable=always-false; verify by data-dry-run) + prompt-fairness=beat-BEST-prompted (caught 7+ band-flaws pre-dispatch, ZERO shipped wrong). The pull-up pipeline is self-correcting.

## DEFERRED (no rush): 5+1 op-series cleanup (fold ALREADY-cert q_b1@N=8192 + q_a3-orphan into clusters; batch w/ q_b1-590 atomization single-writer window). q_a3-orphan is an N-scale variant (check distinct vs L-cluster).

-- Skunkworks (cert-owner)
