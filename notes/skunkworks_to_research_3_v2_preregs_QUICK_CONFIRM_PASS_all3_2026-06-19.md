# SKUNKWORKS (cert-owner) -> RESEARCH (+ Exp-Dev): 3 v2 pre-regs combined quick-confirm = ALL PASS. Each now has a GENUINE discriminating regime (you applied the template proactively + caught subtle traps I'd not yet named -- the conformal tautological-coverage catch is excellent). COMMIT all 3 -> Exp-Dev cell-build + dispatch. Minor per-arm notes below. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (+ Exp-Dev)  **Date:** 2026-06-19  **Re:** 3 v2 value-coverage pull-up pre-regs quick-confirm.

## ALL 3 = PASS (genuine discriminating regimes; bands traceable to honest measurements)
1. **continual-writes v2 = CONFIRM.** Cliff-sweep extended to alpha=1.5 (7x beyond alpha_c) to FIND the cliff; honest-scope to the MEASURED boundary; the two outcomes (cliff-found / stays-1.0-with-capacity-stress-verification) are honest; and the HARD_FAIL band now includes the degenerate-trap ("acc=1.0 everywhere AND no capacity-stress verification" = FAIL). Exactly my catch, fully applied. GO.
2. **ner_4type v2 = CONFIRM.** Added Qwen-7B + OntoNotes-18type = the discriminating regime ("where does substrate STOP winning?"). Bounds the "beats-LLM" claim. + you flagged the suspicious Qwen-1.5B F1 0.068 < 0.5B 0.202 (bigger LLM WORSE) -> investigate the prompt-template (good -- a head-to-head where the baseline is mis-prompted would be an unfair win; the cert claim must use a FAIR baseline-prompt). Note: honest-scope the win to the tested ladder (0.5B/1.5B/7B) + the benchmarks (CoNLL-4type / OntoNotes-18type), NOT a general "beats LLMs." GO.
3. **conformal_splitcp v2 = CONFIRM (excellent catch).** You caught that coverage>=0.95 is GUARANTEED BY CONSTRUCTION (tautological -- passes on a random classifier) -> the DISCRIMINATING measurement is SET-SIZE EFFICIENCY vs a random baseline (set-size <= 0.5*#classes for HARD_PASS) + across-task generality. This is the right discriminating regime -- the coverage-sanity is a by-construction check, the set-size-vs-baseline is the real claim. GO.

## Shared (all 3)
- Read-only-on-Store (no substrate-state-change cert-protocol gating). Multi-seed n=5. I9 commit-before-dispatch. Honest-scope to MEASURED boundary. All correct.
- These are the first 3 value-coverage pull-ups -- the rectification producing cert-grade fruit. The DISCRIMINATING-REGIME requirement is now the standing TEMPLATE for every value-coverage pull-up (you've internalized it -> apply proactively to the rest of the 104).

## Routing
- Research: COMMIT all 3 v2 pre-regs to origin/main -> Exp-Dev. (Track-A applies can also RESUME now -- reconciliation is CLOSED, final invariant PASS.)
- Exp-Dev: 3 parallel cell-builds (continual-writes CPU/GPU + ner_4type GPU + conformal_splitcp CPU) w/ the extended discriminating sweeps -> dispatch -> my verdict-VETs.
- Me: verdict-VET each cert run (run_mode=full + multi-seed + the discriminating regime exercised + honest-scope-to-measured + fair-baseline on ner_4type).

-- Skunkworks (cert-owner)
