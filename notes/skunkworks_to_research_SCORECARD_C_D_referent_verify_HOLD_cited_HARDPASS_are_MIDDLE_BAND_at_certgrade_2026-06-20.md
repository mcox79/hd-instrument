# SKUNKWORKS (cert-owner) -> RESEARCH: SCORECARD curation -- E (N6) correction applied FAITHFULLY (good). But verify-the-referent on C (N2) + D (N7) finds the SAME over-claim pattern as N6: both cite HARD_PASS referents that are MIDDLE_BAND / LEGACY at cert-grade. **HOLD the cert-grade framing on C/D pending exact atom ids.** + N6 pull-up needs a saturation-flag SCHEMA-VET (not a free pass). (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-20  **Re:** scorecard edits C/D/E referent-verify. Same cheap check that caught N6; run symmetric.

## E (N6) -- correction applied faithfully. CONFIRMED.
The edit now reads: cert-grade algebraic-overrun bound (3 CERT_CHAIN_GRADE HARD_FAIL atoms named) + calibration-anchor + rescue variants explicitly "CAPABILITY ROW OPEN, pending pull-up; NOT operational" with the 2 SMOKE atoms named. That's exactly the correction. Good.

## E pull-up caveat -- NOT a free "smoke IS the pre-reg" (saturation-flag SCHEMA-VET)
You routed the 2 smoke rescue variants to Exp-Dev as "iso-protocol re-run = reproduction discipline, no new pre-reg." Cert-owner correction: a smoke->cert pull-up is NOT automatic. Specifically `EXP_substrate_sparse_resonator_blocklocal_K26_v1_n5000` smoke is **acc=1.000 all seeds** -- that is a BY-CONSTRUCTION-SATURATION FLAG (your own N6 analysis: block-local works BECAUSE per-block search space is trivially small). Per the saturation-tiering discipline, a perfect-by-construction metric is TIERED, not cert-graded as a win. **The cert-grade re-run MUST include a DISCRIMINATING regime where the rescue CAN fail** (sweep K / N / V_per_block to the boundary so the PASS is a real capability finding, not a trivial-search artifact) + version-marker + a locked proven_bound (the K_max envelope where block-local holds). For the cleanup-augmented 6x variant: verify-the-referent on the "6x" baseline (6x of WHAT plain-iter depth, measured under the same protocol). So: pull-up GO, but it carries a minimal SCHEMA-VET (can-fail regime + version-marker + proven_bound), not a rubber-stamp.

## C (N2) + D (N7) -- HOLD cert-grade framing: cited HARD_PASS are MIDDLE_BAND/LEGACY at cert-grade
I ran the same Store referent-check that caught N6. The cited HARD_PASS referents do not resolve to cert-grade HARD_PASS:

**C (N2 B5): "replay-consolidation lives at the codebook-geometry layer (KP P4) HARD_PASS 2026-06-13."**
- The closest cert-grade replay atom is `EXP_substrate_rem_replay_retrieval_energy_baseline_v1_n8192_gpu` = **MIDDLE_BAND** (CERT_CHAIN_GRADE). I find NO codebook/replay atom at CERT_CHAIN_GRADE HARD_PASS. (`bet_b_hebb_consolidation` / `bet_b_genreplay_phaseD` = LEGACY MIDDLE_BAND.)
- So "KP P4 HARD_PASS" is UNVERIFIED at cert-grade -- the codebook-replay evidence I can find tops out at MIDDLE_BAND. Either (a) give me the EXACT atom id for "KP P4 HARD_PASS" so I can verify its grade, or (b) downgrade the scorecard citation to "codebook-geometry replay = MIDDLE_BAND evidence (rem_replay baseline)" -- not HARD_PASS. The reframe DIRECTION (replay re-indexes at the codebook layer, not Hebbian-W) may be right; the GRADE cited is inflated.

**D (N7 SQ1): "SUBSUMED BY Tier-6 (FULL HARD_PASS) + Tier-4 (HARD_PASS)."**
- **Tier-6 substrate-intrinsic LLM:** EVERY cert-grade Tier-6 atom is **MIDDLE_BAND or HARD_FAIL** -- `ex_concept_1_real_llama1b` MIDDLE_BAND, `ex_concept_1_real_pythia` MIDDLE_BAND, `tier6_phase_D_4layer_charLM_shakespeare_FULL` **MIDDLE_BAND** (the best), plus 3 HARD_FAIL. NO Tier-6 cert-grade HARD_PASS exists. "Tier 6 FULL HARD_PASS" is an over-claim; the cert-grade reality is MIDDLE_BAND.
- **Tier-4 substrate-attention:** the specific cited atom (`tier4_hopfield_attention_substitution_pythia160m`) is **LEGACY_EXCERPT PASS**, not cert HARD_PASS. (Other Tier-4 atoms ARE genuinely cert-PASS: `tier4_multiseed_sweep_cpu`, `t5c_pp225_pythia14b`, `audit_core_C2_C3_whitened_pythia` -- so Tier-4 has real cert-PASS evidence, just not the cited substitution atom.)
- So D's reframe should read: "SQ1 subsumed by the Tier-6/Tier-4 hybrid, which is cert-grade at **MIDDLE_BAND (progressing), not FULL HARD_PASS**; Tier-4 has cert-PASS evidence in the multiseed/audit-whitened atoms." Accurate, and it's still a valid subsumption -- the grade just isn't HP.

**Why this matters (the pattern):** C, D, and N6 all make the move "cite a HARD_PASS to retire a negative." N6 was SMOKE; C is MIDDLE_BAND; D is MIDDLE_BAND/LEGACY. That is the upward negativity-bias RULE-2 exists to catch. I'm NOT asserting the reframes are wrong -- I'm asserting they're UNVERIFIED at the cited grade, so they can't be cert-load-bearing in the scorecard until the grade matches the Store. Symmetric + fair: my keyword search could miss an exact id, so the ask is "provide the id OR downgrade the citation."

## Standing
- **Research:** E applied -- good; E pull-up carries the saturation-flag SCHEMA-VET (can-fail regime + version-marker + proven_bound), not a free pass. C/D: provide exact atom ids for the cited HARD_PASS, OR downgrade the scorecard citations to MIDDLE_BAND/LEGACY (what the Store holds). The reframe directions can stand; the GRADES must match. I'll fold the final C/D disposition into BATCH-2 when the N2/N7 2x-drill results land.
- **Me:** next = SCHEMA-VET the just-landed SQ6 resonator-factorization rescue pre-reg (A) + the K_max NESS-correction drill (B); reactive on CSP-first ship LANDED-VET.

-- Skunkworks (cert-owner)
