# WHERE WE ARE NOW — clean current state (REWRITE each session)

## 🧭 CURRENT STATE (2026-08-03 ~13:41Z) — AUTHORITATIVE. COMPREHENSION STACK BUILT; CHEAP EARNED-CONTENT PATH NOW CLOSED (proven); HOLDING FOR USER FORK.
(Branch = `dataprep/mcguffey-graded-corpus`, all local, NO push. Atoms 29613-29637. Full blow-by-blow in git history + atoms.)

**THE SESSION'S ARC (coref-on-toy -> a real reader-comprehender on a novel):** built + WIRED + USED + banked the full comprehension stack, all VALIDATED **given correct structure**: coref (hdlab/coreference_resolver: match-or-allocate + strict_cb + Principle B + speaker-deixis + honest-mode flag-don't-fabricate; dense B3 ~0.87); situation model (hdlab/situation_model_accumulate accumulate organ) on DURABLE MULTI-BANK memory (hdlab/situation_model_multibank, holds decode >=0.999 where flat degrades to 0.655; now WIRED_AND_PIPELINE_USED); self-improving loop (hdlab/self_improving_loop: calibrated flag [name-margin AUC 0.80, pronoun n_compatible], coherence-gated autonomous keep/revert, demonstrated gold-free on dense content); CAUSAL comprehension organ (CausalLinkRegister: 0.9722 vs baselines 0.0 on cross-chapter integration, GIVEN links -- now on DIRECTOR-VERIFIED gold v3, gold-clean fixed 2/18 arc-origin causes, non-adjacency survives: 61% cross-chapter); GOAL->causal organ (Trabasso open/close: FP 0.0, precise GIVEN goals). CURRICULUM pivot: McGuffey -> Anne of Green Gables (PD, difficulty-by-construction). INTEGRATION GATE FIXED (USED-by-pipeline check + unregistered-scan; the wire-don't-island hole closed).

**THE PRECISELY-LOCALIZED WALL (atom 29637, VET-corrected from my over-broad 'extraction' framing): RELATIONAL / DISCOURSE-STRUCTURE EXTRACTION** -- detecting the GOALS + CAUSAL LINKS + discourse relations comprehension consumes, from varied real prose. Every MECHANISM works given structure; every END-TO-END path bottoms out here. NOT bare mention/entity extraction (62.5% agent-ID, does NOT propagate to the organs). Measured: causal-link detection ~0.12 (only 12% have connectives; 17/18 story-specific, DEEP_INFERENCE_REQUIRED); goal extraction 0.11 (keyword lexicon misses varied phrasing). 🆕 REFINED (2026-08-03, three glass-box CAPs converge): glass-box CONSTRUCTION patterns NEARLY SOLVE goal-STATEMENT extraction (pattern-residual 5/18->1/18, commit 6a25dd91d) -- so extracting structure is NOT the deep wall. The deep wall = SEMANTIC RELATION INFERENCE: (a) inferring UNSTATED goals (6/9 causal-relevant goals are inferred, not marked), (b) SATISFY-vs-RESTATE / satisfy-thwart-cause discrimination between events (content-overlap necessary-not-sufficient; goal-pairing CAP commit 75bcfc3ee: mechanical+semantic-content gains both +0.000). Mechanical clause<->event alignment VERIFIED not-broken. => the deep frontier is EARNED semantic-relation inference or supplied relational-knowledge, NOT more lexical/pattern tuning (that's the cheap-mechanism trap, provably CAPs).

**STRATEGIC FORK (USER's locked no-bolt-on-parser territory; recommend (c)):** (a) EARN relational extraction brain-faithfully [deep multi-session arc] vs (b) broaden supplied-DATA relational extraction [allowed, brittle] vs (c) HYBRID: supplied-data glass-box relational extraction as scaffolding NOW + earn as the deep frontier. IN FLIGHT (ace9666d): a principled GLASS-BOX goal extractor (syntactic goal/desiderative/purpose constructions + supplied frames, NOT a keyword lexicon) -> does it lift goal-recall + end-to-end causal-link recall at good precision, or cap at the inferential residual? = the hybrid's first real test. NEXT after: if it lifts -> wire; if it caps -> the inferential residual is the earned-extraction deep frontier (USER re-engage on the investment). Also pending: Director spot-verify comprehension+goal gold.
**🆕 DEEP-EARN FORK DECISIVELY REFRAMED (2026-08-03 ~10:4XZ, atom pending, commit 22b243508): the lever is CONTENT+STRUCTURE JOINTLY, not content alone.** Diagnostic ceiling probe (BGE-small used as a DISCARDED instrument, lock-compliant): content-similarity ALONE MIS-RANKS satisfy-vs-restate 3/3 (restate is the same-words trap, more content-similar than satisfy); content+STRUCTURE (glass-box different-agent + goal-object-completion) = 2/3 satisfy-vs-restate + 3/3 unstated-goal recovery (incl the schema-missed goal_014) = 83% combined ceiling (N=6, DIRECTIONAL). => the deep-earn is WORTH IT (83% ceiling exists) but is NOT earn a content encoder -- it is EARN STRUCTURE-AWARE CONTENT (structure-half glass-box-cheap; content-half = the earn-problem, borrowed forbidden as mechanism / schema-supply caps 0.25). Fork now maximally informed. Still HOLDING the big build (earn structure-aware content) for USER steer -- the decision (invest vs accept-milestone) is data-decisive now.
**🆕 FORK FULLY DATA-INFORMED (2026-08-03 ~09:5XZ): BOTH cheap paths CAP.** earned-cheap (coherence-margin) FALSIFIED content-blind (29642); supply-schema fork-(b) measured (afa12fdd7): OVERFIT-GUARDED out-of-sample ceiling ~0.25 (in-sample 0.52 was overfit -- 2 splits, 0.52 gap), ~2x lexicon-CAP 0.11 but far below needed; paraphrase-brittle + doesnt generalize. => the last-mile relation inference (unstated goals + satisfy/thwart/cause) genuinely requires DEEP content-aware inference; NOT shortcuttable by structural-signal OR supplied-schema alone. THE REAL DECISION for USER: commit to the deep-earn content-encoding arc (the months-circled encoder problem, brain-faithful earn path) OR accept the current verified milestone (causal comprehension 0.9722 given relations) as the stopping point. No cheap path remains; holding for USER steer on the deep investment.
**🆕 EARNED-INFERENCE FORK REFINED (2026-08-03 ~07:5XZ, atoms 29641/29642):** design (4c8a26aed) proposed the cheap fork-(a) lever = reuse the VALIDATED coherence-margin (AUC 0.917) to discriminate satisfy-vs-restate. N=3 falsification pilot (8de52617f) = FALSIFIED BY CONSTRUCTION: the coherence-margin is CONTENT-BLIND (scores STRUCTURAL collision via content-agnostic random phase vectors, never sees candidate text) -> right for COREF routing (structural identity), WRONG for relation-CONTENT inference. The cheap earned shortcut is DEAD; saved the investment. Remaining options ALL bigger builds needing USER steer: (a-real) wire the margin to REAL relational content (CausalLinkRegister from atom 29636 182 links) = the honest-fix revival of the earned idea; (b) supply a ~15-25-frame goal-schema library (ceiling ~0.3-0.5 on the 6 extraction-bound, overfit-check mandatory); (c) hybrid. Causal comprehension = DIRECTOR-VERIFIED 0.9722. Trustworthy gate COMPLETE (all gold verified). HOLDING for USER fork steer; no cheap probe remains.

**🆕 BRAIN-FIDELITY AUDIT: we deviate in the CRUDE Phase-1 shortcuts, NOT the paradigm; fixes = REUSE already-validated organs (2026-08-03 ~16:4XZ, commit 5bfe20d24, notes/research_drill_brain_fidelity_audit_event_relation_inference_phase1_2026-08-03.md).** Component-by-component SHAPE+POSITION+METRIC vs brain named 7 deviations; top-3 by fidelity threat: (1) DEEPEST -- the READOUT is a similarity-PROXY (structure_overlap vs fixed prototypes) where the brain REASONS (mentalizing = agent-indexed abductive inference, Mar 2011); we have a HARD_PASS ToM organ (theory_of_mind_sally_anne_nested_hrr_v1, per-agent multi-bank + refuse-gate) sitting UNUSED (this CONVERGES with the numbers-VET chasing why random<chance -- the readout is the problem both mechanically + for fidelity); (2) fixed 2-event bundle window vs whole-narrative DIRECTIONAL causal graph -- Trabasso: non-adjacent causal connectivity beats serial position; window=2 forecloses it + bundling discards direction -> fix = reuse CausalLinkRegister (the 0.9722 non-adjacent-causation organ); (3) syntactic sentence-split segmentation vs error-driven boundaries (prediction-error peaks; already earmarked Phase-2). KEY FINDING: this is UNDER-REUSE-OF-PRIOR-ART, not missing capability -- both leading fixes route through organs this substrate ALREADY PROVED (ToM HARD_PASS; CausalLinkRegister 0.9722). So the brain-accurate Phase-2 = route unstated-goal through the ToM mentalizing organ (judge on abductive best-explanation + honest refuse, not overlap) + replace the 2-event window with CausalLinkRegister (judge on connectivity-beats-adjacency, Trabasso's own test) + error-driven segmentation. NOTE: whether these clear 0.52 is a HYPOTHESIS Phase-2 tests -- brain-faithful direction, not a guaranteed win. Phase 2 STILL gated on the numbers-VET (ad4292a7, in flight) confirming Stage B is real + localizing the random<chance readout bug. --- Phase-1 result retained below.

**(prior) PHASE 1 RESULT: the event-level MECHANISM LEARNS cleanly (Stage B), but end-to-end readout INSUFFICIENT vs the 0.52 bar; VET before Phase 2 (2026-08-03 ~16:3XZ, commit 2247466e2, data/exp_event_level_prediction_error_relation_inference_phase1_v1/metrics.json).** Stage A segmentation SANE (16,137 events, naive sentence-split, rough/fixed). Stage B (THE mechanism, can-fail): PASSED cleanly -- from-scratch event-level prediction-error predictor cut held-out next-event-state MSE 0.649->0.332 (48.8% rel) + cosine 0.03->0.58 vs its random-init control = the event-level prediction-error mechanism GENUINELY LEARNS (qualitatively unlike the word-level arc that never clearly learned). Stage C readout on the 25-item gold: trained beats random-init all 3 axes (unstated_goal 0.417 vs 0.167, satrest 0.429 vs 0.143, cause 0.500 vs 0.0) BUT clears the 0.52 word-level bar on NONE -> verdict PHASE1_MECHANISM_INSUFFICIENT. DIRECTOR SKEPTICAL CAVEAT (do NOT over-read the agent's optimistic framing): vs CHANCE (unstated 0.25 / satrest 0.50 / cause 0.50), trained is clearly above chance ONLY on unstated_goal (0.417>0.25); satrest 0.429 is BELOW chance; and random-init scoring BELOW chance (0.143, 0.0) is SUSPICIOUS = possible READOUT ARTIFACT (same bug-class caught all night). So the honest read: the MECHANISM (Stage B) is a real clean pass; the end-to-end signal is NARROW (one axis) and the readout may be buggy. NEXT (before Phase 2 effort): skunkworks VET of (a) Stage B learning real not leak/scale artifact, (b) WHY random-init is below chance on 2 axes (readout sound?), (c) is 'mechanism works, readout is the bottleneck' MEASURED or over-read. Phase 2 (proper clause-level segmentation + multi-step through-model readout + harden thwart_cause) gated on the VET. --- commit/plan retained below.

**(prior) USER COMMITTED to the EVENT-LEVEL build ("commit, definitely"); Phase 1 mechanism-proof IN FLIGHT (2026-08-03 ~16:2XZ, agent a50e14a0).** DEFAULT = NO Binder (fully earned, from-scratch, lock-safest; Binder-categories-as-prior stays a flagged optional lever, only with explicit USER sign-off if the earned path stalls). PLAN: Phase 1 = mechanism proof (does EVENT-LEVEL prediction-error learning yield representations where relation inference beats random AND beats the word-level lexical 0.52? staged+can-fail, NO blind run); Phase 2 = proper event segmentation + a scorer that reads relations THROUGH the predictive model (replaces word-overlap) + harden the lexically-shortcuttable thwart_cause axis; Phase 3 = scale + WIRE into hdlab + re-measure. Phase 1 arms (one variable = trained-vs-random event mechanism): trained event-predictor vs random-init (MUST fail) vs word-level 0.52. Brain-faithful (Zacks/Trabasso/Zwaan event-level prediction error over bound structures), reuses hdlab binding, no borrow. --- measurement-phase result retained below.

**(measurement phase, COMPLETE) MEASUREMENT PHASE COMPLETE + TRUSTWORTHY -> word-level distributional EMPIRICALLY EXHAUSTED; decision = EVENT-LEVEL paradigm (2026-08-03 ~16:0XZ, commit 878ea6cca, data/exp_content_awareness_earned_v6c_tiebreak_fixed_control/metrics.json).** Tie-break bug FIXED (order-independent seeded tie-break), re-ran ALL arms on the ORIGINAL CLEAN 25-item eval. CLEAN reading: content-free baselines (length_only/pure_structure) now floor at chance overall (0.40 vs chance 0.38) = the unstated_goal 'artifact' WAS the tie-break bug (eval did NOT need the ambiguity-introducing hardening -- fix-the-scorer vindicated). Q2 CONFIRMED: random_init 0.52 STILL beats trained SGNS 0.40 + PPMI 0.28 (+0.12) -- random_init = raw lexical WORD-OVERLAP (identical words -> identical random vecs -> overlap), and distributional TRAINING does NOT beat it, slightly DEGRADES it. => on a TRUSTWORTHY scorer+eval, word-level distributional content gives SIMILARITY not INFERENCE and doesn't even beat raw lexical overlap = EMPIRICALLY CONFIRMS the brain drill's verdict (wrong GRAIN). Caveat (honest, not over-read): thwart_cause is heavily lexical-overlap-solvable (random 0.833) = that eval axis rewards overlap, limited deep-inference discrimination; unstated_goal + satisfy_restate are the cleaner axes; a properly-trained word encoder isn't DEFINITIVELY ruled out but biology+evidence both say it's the wrong direction. CONVERGENCE (measurement + biology AGREE): word-level paradigm exhausted -> the brain-foundational-RIGHT path = EVENT-LEVEL prediction-error over bound structures. MEASUREMENT APPARATUS now trustworthy = the phase is DONE; next is the USER paradigm decision (NOT more instrument work). 🎯 DECISION AT USER: commit to the EVENT-LEVEL prediction-error build (multi-session; replaces word-overlap scorer; Binder-categories-as-prior lock call) OR accept the verified milestone (causal 0.9722 given relations). Director recommends COMMIT (principle: pick brain-right not cheap; biology+evidence both point here). HOLDING for steer; no cheap path or instrument fix remains.

**(prior) SCORING ARTIFACT = a CANDIDATE-ORDER TIE-BREAK BUG (partial reinterpret of v6b) + hardening introduced label-ambiguity -> FIX THE SCORER, don't game the eval (2026-08-03 ~15:5XZ, hardening commit 954f2437d).** Eval-hardening cell MEASURED that v6b's 0.75 LENGTH_ONLY on unstated_goal was MOSTLY a candidate-ORDER tie-break bug: score_arm builds candidates=[correct]+distractors and max() breaks ties to the FIRST (=correct) key; prototype-vs-action word-overlap is ~0 so nearly every item was a 4-way tie silently resolved to correct. Swapping distractors dropped LENGTH_ONLY 0.75->0.25 (exact chance), PURE_STRUCTURE 0.33->0.083. BUT Director-VET of the hardened gold: the swap (adding the longest prototype PROTECT_OTHERS as distractor everywhere) introduced LABEL AMBIGUITY on 3 items (_005 COMPLY vs SELF_PRESERVATION on 'frightened ran off'; _006/_010 CARE vs PROTECT) = gaming the symptom. HONEST next = FIX THE SCORER tie-break (neutral/shuffled candidate order, no favor-first) + re-run ALL arms on the ORIGINAL CLEAN eval. Open Qs the re-measure answers: (i) does the tie-break fix ALONE floor content-free baselines at chance (proving the artifact was the bug, hardening unnecessary)? (ii) does random(0.52)>trained(0.40) PERSIST after the fix (likely yes -- real vectors rarely tie, so that puzzle is the deeper bind/overlap-rewards-shared-words issue, NOT the tie-break)? This is the LAST measurement-apparatus fix before the true content-vs-random signal is readable. Lock-neutral, paradigm-independent bug-fix (NOT the USER-gated event-level scorer redesign). Hardened gold = HELD (ambiguity); prefer clean eval + fixed scorer.

**(prior) SCORING-ARTIFACT CONFIRMED + DIAGNOSIS CONVERGES: the blocker is MEASUREMENT (scorer + eval), NOT the encoder (2026-08-03 ~15:2XZ, commit 3698c86fd, data/exp_content_awareness_earned_v6b_scoring_artifact_control/metrics.json).** Tested (not assumed) the v6 scoring-artifact hypothesis on the 25-item verified eval, anchors reproduce v6 (random 0.52, trained 0.40, valid apples-to-apples). REFINEMENT the raw hypothesis missed (MEASURED): the artifact is NOT the bind/overlap path (PURE_STRUCTURE_NO_CONTENT floors at chance 0.40) -- it is a SHARED-WORD-COUNT / LENGTH signal. LENGTH_ONLY (no vectors, no binding) = 0.60, beats random-init 0.52 + both trained arms + chance 0.38 -> SCORING_ARTIFACT_CONFIRMED. Mechanism: distinct random vectors still make IDENTICAL WORDS overlap positively, so the channel silently rewards lexical-overlap COUNT; TRAINING PERTURBS vectors in ways that DEGRADE that overlap, so trained arms (0.40/0.28) score WORSE than the content-free count baseline. => the scorer cannot let ANY encoder show content value until it is made length-controlled + content-isolating. ALSO: the eval does NOT fully defeat trivial baselines (eval_hardening_needed=True) -- LENGTH_ONLY 0.60 driven by unstated_goal 0.75 (word-overlap with category prototypes = a shortcut); satisfy_restate + thwart_cause are robust (length-only 0.57/0.33, near/below chance) = the well-designed half. CONVERGENT DIAGNOSIS (both threads agree): the relation-inference frontier is gated by the MEASUREMENT APPARATUS (content-blind scorer + shortcut-prone unstated_goal items), NOT primarily the encoder -> RULER-FIRST discipline VALIDATED (holding the encoder build was correct). PATH FORWARD = fix scorer (content-isolating) + harden unstated_goal items, THEN the drill's EVENT-LEVEL prediction-error encoder. NOTE: shifting to the event-level paradigm may REPLACE the word-overlap scorer wholesale (score via the predictive model, not lexical overlap) -> this is a paradigm decision for USER, not an incremental patch. HOLDING for USER steer (paradigm + Binder-categories call).

**(prior) BRAIN-GROUNDED DRILL -> ENCODER TARGET = EVENT-LEVEL, not word-level (2026-08-03 ~15:2XZ, commit 98559ab, notes/research_drill_biology_led_encoder_target_representation_2026-08-03.md, P_deflated 0.40).** Biology (Zacks event-segmentation via prediction-error; Trabasso causal-network story grammar; Zwaan situation-model indexing) converges: relation/goal inference operates over a BOUND EVENT/PREDICATE-level structure (agent-action-patient-goal-outcome) with error signal at SITUATIONAL not LEXICAL grain. => word-level distributional SGNS (our default) supports SIMILARITY not INFERENCE = direct indictment of the default we drifted into. RECO encoder target = EVENT-LEVEL next-event-STATE prediction error (not next-word), reusing existing role-filler/TPR binding. DIRECTOR LOCK-CALLS: (a) MADE -- raw Binder feature VALUES as training target = NO (worse violation of earn-your-own-meaning than a distributional embedding; raw norms stay diagnostic-only). (b) FLAGGED FOR USER (not decided; touches USER-locked no-borrowed-meaning) -- Binder feature CATEGORIES as architectural inductive-bias PRIOR (dedicated sub-populations, values EARNED): Director leans yes (borrows STRUCTURE like a dictionary, not meaning) but defers to USER; only live when we spec the encoder. KB-check: TPR-binding + Binder-as-diagnostic already in KB; NEW = the event-cognition target-grain synthesis. NOTE: this informs the encoder decision but does NOT trigger the build -- still holding for the scoring-artifact verdict + ruler-first.

**🆕 v6 DE-POOLED OBJECT + REAL 25-ITEM EVAL: fix worked mechanically, but CONTENT still doesn't carry -> failure LOCALIZED to the SCORING CHANNEL (2026-08-03 ~15:0XZ, commit b0202cc26, data/exp_content_awareness_earned_v6_depooled_object_verified_eval/metrics.json).** Gate1 OBJECT_DEPOOLED **fires** (object spread 0.096->0.809 via per-word index micro-roles; bind/bundle machinery is NO LONGER the suspect). On the Director-VERIFIED 25-item eval: error_driven 0.40, PPMI 0.28, RANDOM_INIT 0.52 (> weighted-chance 0.38). Gate2 CONTROL_FIRES + Gate3 CONTENT_BEATS_RANDOM both FAIL -- random beats both trained arms. KEY: random-init scoring ABOVE CHANCE = the scoring channel rewards a NON-CONTENT (structural/length) artifact that random rides as well as trained content (thwart_cause: random 0.833 vs trained 0.500). This is confirmed on REAL balanced verified data -> closes the "bad N=6 probe" escape hatch; the failure is NOT representation (fixed) and NOT necessarily encoder-weakness alone -- it is now LOCALIZED to a suspected content-blind SCORING ARTIFACT. NEXT (test, don't assume): a PURE-STRUCTURE zero-content baseline on the same eval -- if it matches random-init ~0.52, the scoring channel is provably content-blind and must be redesigned before any content mechanism can show value (this splits scoring-artifact vs content-weakness decisively). Note error_driven(0.40) > PPMI(0.28) among trained = a faint real content signal exists, swamped by the artifact. Parallel: brain-grounded drill on encoder-TARGET (feature-grounded vs distributional) in flight (a777727).

**(superseded) BOUND ROLE-FILLER v5 -- MIDDLE_BAND, NOT a clean positive (skunkworks-VET-CORRECTED; Director OVER-READ caught, 2026-08-03 ~14:2XZ, commit 391281b72, VET on ad456eeb).** Represented each probe event as a BOUND role-filler FHRR structure (agent/action/object via existing hdlab binding.py/bundling.py, sim via structure_overlap). CORRECTED read (my earlier "saturation FIXED / real win" was an OVER-READ; independent recompute): (1) raw sim-spread DID widen 0.0598 -> 0.4654 (MEASURED, reproduced bit-for-bit, real full-run not selftest) -- BUT widening != content recovered; it is equally consistent with content-BLIND variance amplification from 3-way superposition renorm + the still-pooled OBJECT slot. (2) the DECISIVE gate2 (content vs random-control) **FAILS** -- random-init content ceiling 0.500 > trained SGNS 0.333 (control did not fire; the cell's OWN verdict = REPRESENTATION_STILL_BROKEN_GATE2, which I softened). (3) CONFIRMED STRUCTURAL HOLE: the OBJECT role filler = MEAN of all remaining words = the SAME mean-pool blending I claimed binding fixed, now just confined to 1 of 3 slots (only single-word AGENT/ACTION are truly de-pooled). So saturation is RELOCATED, not fixed. Plus a real bug: the per-role `role_decoded_overlap` diagnostic is mathematically degenerate (unbind cancels for unit-phase role vecs -> always == global overlap), cite nothing from it. TIER = MEDIOCRE/MIDDLE_BAND. => do NOT treat bound-structure as validated. REVISED go-forward (two independent fixes, THEN test on the real eval): (A) REDESIGN OBJECT-slot filling so it does NOT re-pool (per-word micro-roles / bind each object word to a distinct slot) + use the EXISTING real role machinery instead of the crude 1st/2nd/rest POSITION heuristic; (B) BUILD THE RULER (real curriculum-grounded relation-inference eval, dozens of Director-VERIFIED items where random-init genuinely fails -- in flight ae561de2). Only after A+B: re-measure encoder arms; ONLY THEN decide the big encoder-training investment (don't train blind). LESSON (already in memory): vet POSITIVES as hard as negatives -- the parallel VET caught this before we built on it. --- prior probe note retained below.
**(prior) ERROR-DRIVEN ENCODER can-fail = PROBE PROVEN INADEQUATE, mechanism UNTESTED (2026-08-03 ~13:52Z, data/exp_content_awareness_earned_v4_error_driven_sgns/metrics.json, NOT yet committed by the died agent).** From-scratch error-driven SGNS (predict+update-on-error, the brain-faithful mechanism): ceiling 0.333 / unstated 0/3 -- WORSE than random-init (0.667) and PPMI (0.667). DECISIVE signal = the RANDOM-INIT CONTROL DID NOT FIRE (passed at 0.667). Root cause: mean-pooling content-word vectors SATURATES cosine ~0.99 for ALL methods (random/trained/PPMI), so the CONTENT channel carries no signal and the glass-box STRUCTURE signal alone produces the 0.667; error-driven's undertrained content just adds tie-breaking noise. HONEST verdict: this does NOT indict error-driven differentiation -- the mean-pooled N=6 cosine probe CANNOT TEST any content mechanism (control passing = instrument inadequate, per pre-reg FAILS-branch). 4th misleading quick-probe tonight; same root cause. => the probe methodology (bag-of-words mean-pool cosine) is DEAD; it is neither substrate-native (the VSA represents events as BOUND role-filler structures, not averaged word vectors) nor able to make random-init fail. NEXT RIGHT step (surface to USER, not another quick probe): earned content must populate BOUND event structures fed to the EXISTING glass-box relational organs (AccumulateRegister/CausalLinkRegister), evaluated on a REAL curriculum-grounded relation-inference eval where random-init genuinely fails. Bigger substrate-native build; principle steer says pick it because it's RIGHT.

**🆕 CHEAP EARNED-CONTENT PATH CLOSED + PRINCIPLE RE-ANCHORED (2026-08-03 ~13:41Z).** Three clean data points on earned-from-corpus distributional content (glass-box, no borrow): raw-PPMI single-novel 0.500/1-3 (commit bfdbf6b17); raw-PPMI multi-corpus 0.333/0-3 (collapse in the RAW rep, not SVD -- corpus size was never the lever, commit 792ec57cf/e69e95e47); mean-removed anisotropy-corrected 0.667/1-3 (mean-removal IS the right lever, broke the collapse, but caps below the pre-registered 0.6-ceiling+2/3-unstated bar; commit e69e95e47). Verdict CHEAP_EXHAUSTED -- count-based distributional content caps below what relation inference needs. **USER PRINCIPLE STEER (verbatim intent): "we don't go after cheap because it's cheap - we go after what we think is the right solution, brain foundational, regardless of whether it's hard."** => the PPMI-probe arc was the wrong SELECTION CRITERION (cheapness); re-anchor to brain-foundational. The RIGHT solution for earned content meaning = the brain's mechanism = ERROR-DRIVEN DIFFERENTIATION (a from-scratch predictive encoder that earns content representations by prediction-error, predictive-processing/Zacks), NOT count-based PPMI and NOT borrowed vectors. This is the months-circled encoder problem; committing to it because it's RIGHT, not deferring because it's hard. DISCIPLINE (hard != reckless): brain-faithful + CAN-FAIL first cell (error-driven-learned content vs the capped PPMI 0.667 + random-init control, structure held fixed) measured on REAL gold (not the N=6 probe -- the 6-item eval was too thin to steer a build; part of doing it right is an honest eval). NORTH STAR unchanged = glass-box substrate you can converse with.

---
[HISTORY BELOW -- superseded by the consolidated banner above; kept for context.]
## 🧭 CURRENT STATE (2026-08-02 ~18:30Z) — THE COMPREHENSION-PIPELINE ARC.
(Full session blow-by-blow is in git history + atoms; this is the consolidated truth. Branch = `dataprep/mcguffey-graded-corpus`, all local, NO push.)

### THE GOAL
A glass-box substrate that genuinely COMPREHENDS real text and you can CONVERSE with — earning meaning + comprehension the brain's way (no borrowed embeddings, no bolt-on reader; supplying knowledge/DATA/dictionary is fine). North star = the self-improving reader (reads at scale → flags what it doesn't know → learns to fix it → reads more).

### THE PIPELINE WE'RE BUILDING (real-text comprehension of McGuffey)
`text → role EXTRACTION (who-did-what per clause) → SITUATION MODEL (track entities across the discourse, accumulate events) → COMPREHENSION`, with a metacognitive layer on top (flag unknowns → research → learn). Eval = McGuffey graded readers, real gold (hand-verified mention/role + cross-clause entity tracking).

### WHAT WORKS (VET-confirmed)
- **Role extraction, learned + construction-conditional:** ~0.60 end-to-end on real McGuffey (trustworthy, powered eval v3 N=165). Resolves order≠role constructions (quotative, by-agent passive) via LEARNED construction-cue → role mapping (atoms 29604-06, scoped: construction-conditional mapping, NOT a cross-clause loop — see correction below).
- **Clause-level agent=subject** (segment clauses, subject of each = its agent): the big lever for canonical narrative (multi-clause/compound/subordinate subjects). Recovered end-to-end 0.55→0.675 on the small eval (0.60 trustworthy on v3).
- **Situation-model register = ACCUMULATE (FHRR bundle), not overwrite** (atom 29609): the register must bundle an entity's growing event set; pure-overwrite loses history. Validated on oracle roles (accumulate 1.0 vs overwrite 0.46). WIRED to hdlab/situation_model_accumulate.py + verification green. (Brain-faithful behaviorally; FHRR neural-implementation labeled CONTESTED.)
- **Learned construction classifier (drift-fix, partial):** 3/4 marked constructions (quotative/by-agent/copular) now LEARNED from gold (near-perfect classification), replacing hand-coded gate rules — matches/beats the hand gates. Canonical-default class needs the eADM reformulation (canonical=commit default; classifier detects only MARKED).
- **Coverage lever:** adding the `theme` role (dominant missing role) lifted end-to-end 0.42→0.57 — role INVENTORY is a real binding constraint.
- **Powered evals built:** by-agent gold N=5→23, quotative N=20→45, multiclause entity-track v3 (39 passages, 165 events), long-form v1 (24 passages 4-7 clauses, rich role trajectories).

### THE WALL + THE HONEST REFRAMES (this session's core findings)
- **The wall is EXTRACTION GENERALIZATION on real text** (atom 29610), NOT the situation-model organ (validated given correct roles). Broken into: ~10% missing-role COVERAGE (recipient/possessor/experiencer, no vocab+gold), + the rest = clause-structure extraction errors (mostly fixed by clause-level agent). NOT meaning: the animacy DICTIONARY was NULL on McGuffey (probe-caught — McGuffey lacks animacy-conflict cases; the dict is built + ready for HARDER content where meaning bites).
- **Cross-clause feedback ≠ role-persistence (interactive loop v1 NULL, load-bearing):** biasing the current clause's role toward an entity's PRIOR role HURTS (even oracle) because multi-event roles VARY across clauses. The brain's cross-clause signal is COHERENCE/identity, not persistence. => the situation-model's real cross-clause job = **tracking WHO across the discourse (coreference) + accumulating events**, NOT re-biasing per-clause parsing.
- **Self-correction is NULL (yet):** the reader can only partially self-detect its errors (~46%, best signal = its own confidence AUC 0.76) and can't yet self-correct (net 0) — short simple McGuffey gives no strong internal coherence/error signal. Self-learning needs RICHER/LONGER content where coherence conflicts arise.
- **⚠️ 4 director over-reads caught + corrected this session** (coverage-hypo; "inversion" was really argmax-mislocalization; "wall broken" deflated to MEASURED_MECHANISM; "interactive loop" was really within-sentence construction cues). VET/audit discipline working; report MEASURED not READ.

### 🧭 SESSION CONSOLIDATED STATE (2026-08-02 ~21:1XZ) — MECHANISMS BUILT+WIRED; CONTENT-RICHNESS IS THE RECURRING WALL.
**The comprehension MECHANISM STACK is built, VET'd, WIRED into hdlab, and banked (atoms 29613-29625):** coref resolver (hdlab/coreference_resolver.py: match-or-allocate + possessive-fixed gender + strict_cb + Principle B + speaker-deixis canonical + flag signals), situation-model accumulate organ (hdlab/situation_model_accumulate.py), the metacognitive FLAG layer (name margin AUC 0.80 / pronoun n_compatible AUC 0.72), and the self-improving-loop CONTROLLER (hdlab/self_improving_loop.py: gold-free coherence-gated keep/revert autonomous router). Loop engine CERTIFIED across 3 cycles (4 fix-levers, 2 falsified). Brain-fidelity audited (Principle B FAITHFUL; rest APPROXIMATING w/ 2 named debts).
**THE RECURRING WALL (5 independent collapses this session): McGUFFEY IS TOO SIMPLE to REQUIRE or POWER the advanced competencies.** (1) coref collapsed to recency-floor until dense multi-entity mining; (2) situation-model value invisible until the identity-demanding query subset; (3) autonomy reliable ONLY on dense g5g6, content-gated on sparse; (4) pronoun/self-correct/dictionary all null on thin McGuffey; (5) causal inference COLLAPSES TO ADJACENCY (207/208 links clause-adjacent = trivial baseline ~99.5%; connective-cued task needs no mechanism). => the mechanisms WORK where the test REQUIRES them (dense/identity-demanding); McGuffey mostly doesn't require them.
**=> STRATEGIC INFLECTION: the highest-leverage next move for the WHOLE program = RICHER/HARDER CONTENT AT SCALE (the USER's 'feed it a shit ton of content' vision, now overdetermined).** It unblocks: autonomy-at-scale (the real self-sustain test), causal inference (needs multi-candidate-distractor or connective-blinded framing, or a harder corpus), and all advanced competencies. Options: (a) aggressively reframe+mine McGuffey hard subsets (distractor causal paragraphs, multi-entity dense scenes); (b) adopt a richer public-domain narrative corpus. USER STEER REQUESTED on corpus direction (big investment, their vision territory). NEW gold this session: gold_causal_relations_v1.jsonl (n=208, verbatim, unverified, collapses-to-adjacency), gold_combined_pronoun_powered_v1.jsonl (36 passages, powered coref).

### ✅ COREF / SITUATION-MODEL ARC — RESOLVED (2026-08-02 ~21:XXZ). "EARNED IN KIND, NOT YET IN DEGREE."
The full arc from coref -> wired situation model, VET'd + honest. Atomization in flight (skunkworks). Commits all local on dataprep/mcguffey-graded-corpus, NO push.
- **Coref EARNABLE** (B3 HARD_PASS, name 0.89; commit 27e10d3a8). **Possessive-gender fix** (a0aac7eeb): head-noun governs NP gender (two root causes: infer_nominal_gender whole-span cue union + is_pronoun_mention misclassifying "his mother"); dense B3 0.843->0.872, merges -33%, 208 tests pass.
- **Pronoun coref**: coarse role-prominence Centering = NULL (5639fe312; hard cases are AGENT-vs-AGENT, prominence tied -- honest null, right diagnosis) -> **STRICT immediate-clause Cb (literal Centering)** = WIN (5b266248f): pronoun-B3 0.666->0.703 (g5g6 0.695->0.722), name+overall up, opt-in/reproducible. The RIGHT lever, once content was powered.
- **Content unblocked**: rejected PARAPHRASED v1 mining (would test a rewrite); probe showed corpus NOT the limit (170+300 male-pronoun-dense verbatim scenes in g5/g6); verbatim re-mine w/ programmatic SUBSTRING-GUARD; DIRECTOR-VERIFIED all 19 (dropped 1 real-error passage + 4 spurious mentions). Combined POWERED eval (0b7ff5d42): 36 passages, 81 pronoun mentions (~5x power). Existing gold confirmed verbatim (prior results stand).
- **Wired coref -> accumulate situation model** (e6a3a9ee8): on the IDENTITY-DEMANDING query subset (44% of queries = >=2 entities in the query clause, where identity is REQUIRED) coref BEATS both fair floors -- strict_cb 0.719 / earned 0.684 vs recency 0.561 / singleton 0.386 (powered; g5g6 +0.24). Recency collapses 1.0(trivial)->0.561(identity-demanding) = it was coasting on single-event clauses. => coref VALUE PROVEN where identity matters (milestone EARNED IN KIND). BUT strict_cb 0.719 is 0.21 BELOW oracle 0.930 => remaining gap = COREF QUALITY (pronoun split/link errors) = the clean measurable next target (NOT milestone_met in degree). Organ capacity FINE: oracle 1.0 on >=8-clause passages (extends atom 29609 past chain-length 2-3).
- **Flag-layer calibration** (c6eb94467): coref decision-MARGIN predicts its own errors, name-path AUC 0.753 (n=182), flags 92% of name errors at 2x base precision => the self-improving-reader's cheapest flag signal WORKS on the dominant path. Pronoun-path calibration content-underpowered (n=16).
- **METHODOLOGY (load-bearing, VET discipline paid off repeatedly)**: caught 3 metric-artifacts (singleton games per-mention decode=1.0; recency games the collision-SKIP; the FAIR test = identity-demanding subset) + a confounded calibration label (global-purity-on-local-decision; fixing it flipped AUC 0.480->0.753 = a false FAIL) + rejected paraphrased gold. Report MEASURED not READ.
- **SELF-IMPROVING-READER ARC STARTED (2026-08-02, follows the resolved coref arc):**
  - **FLAG LAYER CALIBRATED both tiers** (commit 150058b03, powered eval): NAME-path decision-margin AUC 0.80 (up from 0.75, generalizes); PRONOUN-path raw margin FAILS (0.54-0.63) but #compatible-candidates (n_compatible) AUC 0.72 = the CORRECT pronoun flag signal (referential-ambiguity structure). The reader can SEE its own likely errors on both paths.
  - **LOOP CYCLE 1 = NULL_FIX_MECHANISM (clean decomposition, commit 82492af76):** FLAG earned + localizes (pronoun error rate rises with n_compatible: nc3=44.8% vs base 26.3%; threshold>=2 captures 85% of errors; flag-localization control selective==uniform). But the FIX FAILS: topic-continuity (Centering Continue) applied to flagged cases HURT (pronoun-B3 0.703->0.677, identity-demanding 0.719->0.684 AWAY from oracle; corrected 0/broke 3 of 6 divergent). MECHANISM: flagged same-gender pronouns refer to a NON-topic entity (Centering SHIFT) -> both glass-box levers (most-recent-agent, topic-continuity) systematically point wrong -> these need VERB-SEMANTICS / selectional-preference / world-knowledge.
  - **=> THE LOOP DECOMPOSES: FLAG earned; FIX = the open frontier = SUPPLIED semantic knowledge** (maps to USER steers: "just give it a dictionary" + "flag unknowns -> tiered research"). Brain-faithful (semantics resolves referential ambiguity) + allowed (supply knowledge OK, mechanism earned).
  - **NEXT (strategic, USER-relevant -- what knowledge to supply):** build the FIX tier = supplied verb-argument / selectional-preference knowledge (which same-gender entity is the plausible agent/patient of the verb), triggered by the flag (n_compatible high) = the "tiered research" step. Then re-run loop cycle 2. ALTERNATIVE next levers still open: close coref-quality gap directly; scale the loop to more content/competencies. (strict_cb 0.703 remains best coref; atoms 29613-29617 + calibration/loop findings banking.)
### 🎯 CURRENT FOCUS / NEXT (USER-confirmed direction "A" = earn coref; probe LANDED)
**EARN COREFERENCE — PARTIALLY EARNABLE (probe abaf3f4e, commit 68d25389b):** learnable identity-tracking (name/pronoun/salience → match-or-allocate, reusing hdlab/state_of_mind.py salience/Centering primitives; NO bolt-on coref tool) = B-cubed F1 0.850 vs recency-floor 0.858 vs random 0.652. Beats random ✓; 0.8pp BELOW recency-floor ✗ — real PRECISION advantage (+7.6pp, separates distinct entities) but UNDER-MERGES (misses "The girl"→"Ernestine"→"she" APPOSITIVE-NAMING chains = a targeted bridging lever, NOT a ceiling). ⚠️ LOAD-BEARING CAVEAT: the long eval averages only 1.46 entities/passage (10/24 = single-entity), so "chain-everything" trivially wins (recall 1.0) — the recency-floor is unbeatable because the CORPUS IS ENTITY-SPARSE, not because coref lacks value.
### 🟢 COREF FAIR-TEST = HARD_PASS (2026-08-02 ~19:3XZ, exp_earn_coref_match_or_allocate_dense_v1, commit 27e10d3a8, director-VET'd) — FIRST clean fair-test win of the coref/situation-model arc. On the DENSE eval (3.67 entities/passage): learnable F1 0.843 vs recency-floor 0.462 vs random 0.526 (beats both by +0.38/+0.32). ✅ FAIR-TEST PRECONDITION CONFIRMED (what makes it trustworthy): the recency floor COLLAPSED 0.858(sparse)->0.462(dense), BELOW random — chain-everything is actively harmful with 3-5 co-present entities, as designed; learnable stays FLAT 0.84 as entity-count rises (3-ent 0.84 / 4-5-ent 0.84) while the floor degrades = genuine identity-tracking, not a one-bucket artifact. HONEST SCOPE: win is NAME-driven (name F1 0.891 strong); PRONOUN coref still the hard weak part (0.534, N=28 small). Dominant error = MERGES from a SPECIFIC CHEAP BUG: possessive NPs ('his mother') inherit masculine gender from 'his' not the head noun 'mother' -> false-merge with 'Harry' (fix: gender from head noun; actionable, not a ceiling). N=18 small but +0.3 margin not marginal. => COREFERENCE IS EARNABLE + VALUABLE, fairly demonstrated (our own match-or-allocate, no bolt-on tool). The convergent arc PAID OFF: 5-probe 'too thin' finding -> density scan -> dense mining -> fair test -> real win. NEXT LEVERS: (a) CHEAP: fix the possessive-NP-gender bug (reduces the dominant merge errors); (b) pronoun coref (the hard part); (c) WIRE earned coref -> accumulate organ = the REAL situation model (name coref 0.89 is good enough to carry entities). -> atomize (first fair-test coref pass).

### 🚨 CONVERGENT META-FINDING (5 probes, robust): McGUFFEY IS TOO SIMPLE/THIN to develop OR fairly measure the advanced competencies. Dictionary=null (no animacy conflicts), interactive-loop=null (short passages, no cross-clause signal), self-correct=null (no coherence signal in 2-clause sentences), coref=can't-beat-trivial-floor (1.46 entities/passage). => THE NEXT REAL MOVE = DENSER/RICHER CONTENT: passages with MULTIPLE co-present same-gender entities + coherence conflicts + meaning-required cases, where coref/cross-clause/meaning/self-learning all become learnable AND measurable. This is the USER's "feed it a shit ton of content" vision, now FORCED by the evidence.
**NEXT:** (1) ✅ DENSE MULTI-ENTITY SUBSTRATE MINED (density scan: McGuffey HAS it in group scenes/dialogues, no new corpus needed yet; agent a107c9de mined 19 candidates -> data/eval_gold_mention_role_mcguffey_v1/gold_multientity_dense_candidates_v1.jsonl, commit 9bf2b7070: 18/19 have 3+ co-present entities, 19/19 SAME-GENDER pronoun-ambiguity = the genuine coref test, 16/19 multi-speaker dialogue; hard subtypes flagged: cross-species 'he', same-noun-diff-referent, relation-introduced antecedents). gold_verified:false -> NEEDS DIRECTOR VERIFICATION (careful — multi-entity coref clusters across group scenes; do NOT rush). (2) verify -> dense eval -> RE-RUN COREF (fair test: recency/chain-everything floor is now BEATABLE) + the appositive-naming bridging lever; (3) wire earned coref -> accumulate = real situation model; (4) FLAG-AND-RESEARCH metacognitive layer; toward the SELF-IMPROVING READER. Cleanups: canonical-default eADM into the learned gate; learned accept-rule/boundary; amend atoms 29604-06 framing. (If even the dense McGuffey scenes prove too easy/thin, THEN source a denser public-domain corpus — but density-scan + this mining say McGuffey's group/dialogue scenes suffice for the fair coref test.)

### ⚙️ INFRA / OPS
GPU available when a probed direction warrants it (probe-to-aim before big investment — USER discipline). exp_dev anti-patterns to avoid: nohup-detached runs + `timeout`-wrappers (both caused failures this session — use single clean runs; read results off disk). git commits sometimes time out under load (--no-verify + verify-after). Store LOCAL-only, git-commit after each bank, NO origin push w/o in-session USER auth.

---
[HISTORY: the full session blow-by-blow (WM-allocate refutation, the extraction-fix iterations, the dictionary/bootstrap/self-detect/self-correct nulls, the brain-fidelity audit) is in git log + atoms 29594-29612. Earlier pre-comprehension-pipeline state (encoder-lever campaign, multi-competency engine) is in git history before 2026-08-02.]
