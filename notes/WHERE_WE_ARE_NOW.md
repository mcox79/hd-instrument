# WHERE WE ARE NOW — clean current state (REWRITE each session)

## ▶️ SESSION RESUME (2026-08-02 ~05:1Xz) — LIVE DISPATCHES IN FLIGHT (read this first; the ~05:00Z block below is the settled state it continues)
USER: "do it. brain foundational always ... full auto authorized." Executing the post-compaction next step + a lock-clean parallel thread. TWO hdi_exp_dev agents running LOCAL (box CPU-thrashed by peer PID25552, not mine — do NOT kill):
1. **WM ALLOCATE/RECALL FIX (a09536ec)** — the live thread. Brain-foundational: DG pattern-separation match-or-allocate (novel entity -> distinct empty slot; hdlab/hippocampal_encoder.py DGProjection+CA3) + iterative_cleanup CA3 recall (hdlab/iterative_attractor.py) on the lossy HRR unbind. ONE variable vs the current PEGatedSlotWM; must PRESERVE the brain-faithful overwrite gate (spike_WR>=2.0) + floor + add an allocate-selectivity brain-metric + a no-allocate can-fail control. Target recall 0.34 -> ~0.75. Cell exp_contextual_stream_wm_sor_probe1_v1.py.
2. **EXTRACTION RE-RUN ON POWERED GOLD (a8229f36)** — hardens the milestone's #1 caveat. Director powered the by-agent-passive gold N=5 -> **23** (gold_passive_byagent_verified_v2.jsonl, 18 hand-verified true by-agent passives mined from full McGuffey g1-g6 incl untapped g5/g6; v1 left intact for atom-29606 reproducibility; commit 210be7a1c). Re-running exp_interactive_loop_real_gold_mcguffey_v1 on v2 to confirm the OFF/ON/placebo dissociation HOLDS at N=23 (a drop from the N=5 1.000 is fine iff ON>>OFF and ON>>placebo still hold). ONE variable = gold set size.
NEXT after these land: VET both (positives HARDEST); if WM clears -> wire extraction+WM together -> real McGuffey climb. SCALE still HELD for USER. Gates run at resume: capability audit clean; KB freshness was STALE (catch-up ingest launched).

### 🔻 WM ALLOCATE RESULT (2026-08-02 ~08:2Xz, director-VET'd on units.jsonl disk; metrics.json imminent) = CLEAN CONTROLLED NEGATIVE. Recall by arm/seed: OFF 0.047/0.078 (floor holds); ON_BASE 0.297/0.352 (~0.32); ON_ALLOC 0.117/0.391 (mean 0.254 < base, HIGH VARIANCE, inconsistent SIGN); ON_ALLOC_RAND 0.359/[s1 pending] (the random-allocate CAN-FAIL control BEATS real allocate 0.359>0.117 in s0 = control failed-to-fail). All allocate arms DEGRADE route (0.49-0.56 vs 1.0) + overwrite (spike_WR 1.17-1.43 <2.0). recall_acc_cleanup ~= recall_acc everywhere => CA3 iterative_cleanup does NOTHING either. => THE DG MATCH-OR-ALLOCATE FIX IS REFUTED as the recall lever; NEITHER allocate NOR cleanup moves recall off ~0.32. Brain-fidelity element audit (WHY it failed): right COMPONENT (pattern separation), WRONG PLACE/STAGE — allocation was put in the SAME addressing-softmax competition as content-routing, so novelty OVERRODE routing (route->0.49); in the brain DG separation is at ENCODING and doesn't override retrieval routing. Also the agent's informal pre-run calibration (+0.052) was MISLEADING (didn't hold at full multi-seed w/ the RAND control) = another cheap-probe-misled instance. **Load-bearing artifact caught (kept, real):** probe1's route=1.00/alloc=0.167 were a permutation-symmetry artifact (zero-init slots = exact fixed point); _SlotInitFix (Locatello learned per-slot init) corrects it, but recall is ~0.32 with OR without the fix => the symmetry artifact affected the ROUTE/ALLOC METRICS, not recall accuracy. 🎯 HONEST-LEVER TURN (the real conclusion): recall ~0.32 is UNMOVED by TWO brain-faithful sub-mechanisms, AND this synthetic SOR probe has now yielded {symmetry artifact + refuted allocate + inert cleanup} = signs of an artifact-prone/mis-calibrated instrument. Continuing to tune synthetic-WM sub-mechanisms = the instrument-perfectionism trap (same one that burned the encoder-proxy thread). ⚠️ NO positive control has ever shown ~0.75 is REACHABLE on this exact SOR config => the "recall MIDDLE" gap may not even be real (target-too-hard). HARD NEXT STEP (not the easy allocate-redesign): (a) CHEAP DECISIVE fair-test-of-target = does an ORACLE (oracle-route + oracle-slot) hit ~0.75 on this SOR config? if not, the TASK is mis-calibrated + we STOP the WM-recall chase (honest-lever win); (b) regardless, PIVOT off synthetic-WM tuning toward the REAL frontier = wire the overwrite-brain-faithful WM to the VALIDATED real-text extraction, measure entity-tracking on real multi-clause McGuffey. I OWN this cell now (agent a09536ec stood down after a duplicate-run race I resolved).

### 🟢(honest) EXTRACTION RE-RUN ON POWERED GOLD LANDED + DIRECTOR-VET'd (2026-08-02 ~08:4Xz, exp_interactive_loop_real_gold_mcguffey_v1 --full on v2 gold, disk-verified, elapsed 645s) = MILESTONE HOLDS AT POWER, HONESTLY DEFLATED. N used: byagent N=23 (powered v2), quot N=19. Arms — BY-AGENT: OFF 0.000 / ON 0.739 / PLACEBO 0.217 / NO_TD 0.478 (floor_held=True, placebo_ok=True => CLEAN DISSOCIATION, but ON 0.739 JUST under the strict PASS_BYAGENT_MIN_V2=0.75 => byagent_pass=False). QUOTATIVE: OFF 0.211 / ON 0.895 / PLACEBO 0.316 / NO_TD 0.421 (placebo_collapses=True, not_a_leak=True, STRONG PASS; floor 'not held' = learned position-baseline calibration OFF 0.211>0.15, explicitly NOT a leak). Cell verdict PARTIAL_QUOTATIVE_PASS_BYAGENT_UNDERPOWERED... tagger_gold_nominal=0.960. => the interactive top-down loop STILL resolves BOTH real constructions with clean dissociation at higher power; the by-agent magnitude DEFLATED from the optimistic N=5 headline 1.000 (atom 29606) to a HONEST 0.739 at N=23 (still a large dissociated lift over OFF 0.000 / placebo 0.217, just under the strict bar). This HARDENS the milestone's #1 caveat (N=5 exploratory -> N=23 powered; effect real + dissociated; magnitude honestly ~0.74 not 1.0). => ATOM 29606 should be UPDATED: milestone holds at power, by-agent HARD_PASS@N5 -> CLEAN-DISSOCIATION-MAGNITUDE-0.739@N23 (just under strict bar). Quotative gold also powered to N=45 (v2) for a future harder heldout; this run used the N=19 quot v1 subset. Both runs (WM allocate HARD_FAIL + this) now COMPLETE, 0 procs, no hangs.

### 🧭 STRATEGIC STATE @ 08:4Xz (both night-runs done): (1) WM synthetic SOR thread = allocate REFUTED + cleanup inert + recall stuck ~0.32 + 3 artifacts => INSTRUMENT-PERFECTIONISM signal; PIVOT recommended (surfaced to USER). (2) Extraction layer = VALIDATED + now POWER-HARDENED (real text, both constructions, clean dissociation). (3) RECOMMENDED HARD NEXT STEP (surfaced to USER as a prose fork): wire the overwrite-brain-faithful WM (drop refuted allocate) to the VALIDATED real-text extraction, measure entity-tracking on real multi-clause McGuffey (cheap, numpy/CPU, no scale) = the real comprehension frontier + the honest test of the WM's value. Alt (leant-against): oracle-ceiling fair-test on the synthetic SOR. SCALE still HELD for USER. Awaiting USER steer on the fork; NOT auto-committing the milestone-level wiring build at deep-night after 2 exp_dev races.

### 🔬 ORACLE-WM PROBE seed-0 read (2026-08-02 ~11:4Xz, exp_oracle_wm_real_discourse_entity_track_v1, disk-verified; run was killed by the agent's `timeout` wrapper after 1/3 seeds but seed-0 is decisive). MEASURED (starter gold N=6, current-state query): OFF/random-floor 0.267 | WM 0.800 | shuffled 0.640 | LAST-CLAUSE(no-memory) 0.933 | brain spike_ratio 1.01 (NO role-change selectivity). READ: WM beats the random floor (0.80>0.27 = it binds/routes/recalls) BUT the memoryless last-clause baseline BEATS the WM (0.93>0.80) + the overwrite gate does NOT spike at role-change on real discourse (1.01). => FINDING 2 EMPIRICALLY CONFIRMED: on non-memory-optimized gold (entities recur in the final clause) a trivial last-clause reader dominates, so the WM's memory VALUE is not demonstrable + it even underperforms the trivial reader (mis-updates Dash/Sport; missed the one clean memory case boatman). NOT a fair test yet. More seeds won't fix it. NEXT HARD STEP (now data-justified, not speculative): build MEMORY-OPTIMIZED gold (target entity set early + ABSENT from final clause -> last-clause MUST fail) then re-run -> then the WM's cross-clause maintenance is fairly testable. Also note: brain-metric spike failing again echoes the SOR finding (the overwrite gate's PE-selectivity doesn't transfer to real discourse) = a real open fidelity question. Honest: this is an underpowered-but-informative first arm; the WM is above-floor but below-trivial on THIS gold; the fair test awaits memory-optimized gold. (Probe run died via timeout-wrapper after seed 0; I own it, will re-run on the better gold not relaunch on this one.)

### 🟢 ORGAN QUESTION RESOLVED (2026-08-02 ~13:2Xz, exp_situation_model_accumulate_vs_overwrite_v1, HARD_PASS, commit 36ab29a93, director-VET'd) = Finding 3 confirmed ARCHITECTURALLY. Multi-event entities (N=13): overwrite 0.462 (=chance-adjusted last-only ceiling) / accumulate 1.000 / floor 0.205, gap 0.538 (bar >=0.30). => the situation-model register must ACCUMULATE entity-event bindings (FHRR bundle), with pure-overwrite reserved for genuine STATE-REPLACEMENT — the overwrite-WM was the wrong/too-simple frame (Kintsch C-I / Zwaan integration). HONEST SCOPE (VET'd): CONSTRUCTION PROOF of register-form, NOT a capacity/scale win — accumulate hits 1.0 only because chains are short (2-3 events); it does NOT stress the bundling-capacity ceiling (bites at higher load per the prior synthetic bundle organ's curve). BUILD-ON (not rediscovery) of situation_model_event_bundle_focus_v1 (adds overwrite-vs-accumulate + real gold). Agent's can-fail discipline caught its OWN pre-reg bug (mismatched-unbind = chance noise not deterministic-miss; corrected). NEXT (toward the fork, USER-steer): build the accumulate-based situation-model organ (bundle events + overwrite-on-state-replacement) -> wire to validated extraction -> real McGuffey entity/event tracking; the eval framing (event-history, per Finding 3) + scale remain USER calls. NOTE: all session commits on branch dataprep/mcguffey-graded-corpus, local-only, NO push (per lock).

## 🧭 SESSION LANDING + CURRENT STATE (2026-08-01 ~02:45Z) — AUTHORITATIVE (read this first; older blocks below = history)
**Arc:** built the USER-steered growing-library engine, then proved + banked three load-bearing results, chain-graded + integrated the encoder lever, and are now running a depth campaign on it (USER redirect: prove UNIVERSAL + MAXIMIZE before breadth).

**PROVEN + BANKED (VET-confirmed):**
1. **Competencies COMPOSE** — the role competency (#2) CAUSALLY GATES competitive-coref (#3): ablation +0.19, 3/3 seeds, neutral-baseline-verified no confound. Atom **29594**. (First direct evidence the growing-library thesis holds; the additive-blend metric HID it, the AND-gated+ablation metric surfaced it.)
2. **Encoder = CHAIN-GRADED generalizing representation-quality LEVER for ENTITY-ADDRESSED comprehension** — the certified minimal-unfreeze entity-consistency break lifts stage_ENT 0.72→0.86, competitive-coref abs 0.51→0.65, GENERALIZES across harder-difficulty + held-out + independent harness (drift-controlled, multi-seed, independently recomputed). Atom **29596 (CHAIN_GRADE)**. Honest scope: a proven LEVER, NOT solved comprehension (abs 0.65<0.70).
3. **NO-FREE-LUNCH boundary** — training one skill (who's-who) does NOT automatically lift an untrained orthogonal skill for free (state decode flat, geometry mild negative-transfer). Atom **29597**. HONESTLY SCOPED: this is no-free-lunch, NOT an intrinsic ceiling (state was untrained + near-ceiling); trainability-if-directly-trained tested separately (see depth campaign).
4. **FULLY INTEGRATED** (USER directive): `hdlab/encoder_retrain_persist.py` opt-in loader `load_improved_encoder(seed)` (drop-in EncoderExtractor from persisted certified ckpts data/exp_encoder_retrain_persist_v1/), registered in capability_registry (WIRED, cites 29596), import-graph audit confirms WIRED, ledger updated. Commit **367a42729**. Does NOT change any existing default (opt-in).

**DEPTH CAMPAIGN — RUNNING (USER: prove universal + maximize first):**
- **UNIVERSAL** ("is it just lack of training — train ANY independent skill and it lifts + stacks?"): 1st datapoint (P/placement direct-train) was an UNFAIR/broken null (entity-adjacent confound + underpowered; P lifted MORE from indirect who's-who training +0.086 than its own direct +0.011 = broken) — CORRECTED, NOT banked. CLEAN re-test (hardstate = independent local decode + constructed headroom): at SMOKE reports HYPOTHESIS_REFUTED/NO-TRANSFER with all-5-fairness-gates-claimed-PASS (independence, headroom, verified-optimized training, POSITIVE CONTROL, non-floor metric): hardstate lift +0.010<0.05. ⚠️ PENDING full run + independent VET of the POSITIVE CONTROL (this agent a84b9b3e over-read the null twice tonight — do NOT accept "refuted" until the positive control is VET-confirmed to lift in the SAME full run while hardstate stays flat). IF it holds = the lever is SPECIALIZED (not train-anything-improves); USER hypothesis fairly refuted for this skill.
- **MAXIMIZE** ("how high can the entity-addressed lever go vs a fair oracle?"): abca8fa8, self-test PASSED, early — establishing oracle/ceiling FIRST (is 0.65 near oracle?), then push training-scale + stacking, held-out-not-overfit.

**DEPTH CAMPAIGN RESOLVED (2026-08-01 ~04:10Z):**
- MAXIMIZE = clean SATURATED (9e98652da): entity-addressing lever at 95% of its FAIR oracle (loop 0.760/0.800; coref 3-seed disk-truth 0.563→0.763 — NOT the single-seed 0.51→0.65 I mis-cited/propagated). More encoder-addressing training = no held-out breakout. Remaining headroom = CONTENT-decode lever, NOT addressing.
- UNIVERSAL = SCOPED null (atom 29598, 57835a011): recipe did NOT lift an independent target (+0.033<0.05; fair gates pass; positive control +0.149) => literal "just lack of training" refuted for THIS recipe/target. BUT "intrinsic limit" STRUCK (over-read #3): null is SOFT — weak 0.6% loss-descent on target + possibly-hard target (cross-transferred to ENT +0.055 > its own +0.033) + NO achievable-ceiling measured. Not a clean "specialized" proof. Revival option: a higher-power probe settles specialized-vs-underpowered (NOT running now per don't-rabbit-hole-the-encoder).
- STRATEGIC READ (both point same way): the encoder-addressing lever is real + chain-graded + generalizing + MAXED-on-its-axis => STOP investing here; next comprehension gains = CONTENT-decode lever + skill-specific mechanisms (growing library) + breadth on real text.

**⚠️ PHASE-2 BLOCKED at design gate (2026-08-01 ~04:40Z, disk-verified by a8f51b4a — agent refused to fake it):** THE REAL-TEXT WALL = mention/role EXTRACTION from real syntax, NOT the downstream comprehension we proved.
- Our situation-model stack (loop + situation_reader + coref + composition) requires ORACLE-STRUCTURED input (ENT/S/P/MARK spans + baked-in answers from the synthetic GENERATOR). Real McGuffey prose has NO such scaffolding.
- PRIOR-ARC REDISCOVERY: a prior Track-A v4 hand-rule reader on real McGuffey grade-2 (31 real comp Qs) ALREADY HARD_FAILED here — mention/entity detection broke on real syntax, starving coref+composition (entity-assignment fine ONCE given clean mentions). Motivated the 07-18 "learned parser" pivot.
- 🚨 DIRECTOR OVER-READ CORRECTED: McGuffey clean/*.txt has NO appended comprehension questions — the "g1:17→g4:84" I cited were IN-NARRATIVE dialogue `?`-marks (my grep mis-read). No free question-bank. (Encoder vocab reconfirmed fine.)
- => EVERYTHING PROVEN THIS SESSION (composition 29594, encoder lever 29596) was on ORACLE-STRUCTURED SYNTHETIC input. The wall to REAL text is UPSTREAM (the parse/extraction), unsolved.
**FORK (USER STEER NEEDED — implicates the no-bolt-on-parser LOCK):** how to extract structure from real syntax? We HAVE trained glass-box pos_tagger/arc_parser (parsed all McGuffey in the density audit) — is using them "our mechanism" (OK) or a "bolt-on parser" (forbidden)? Alternatives: LM-surprisal/cloze proxy (sidesteps extraction, weaker=not comprehension); re-source McGuffey WITH questions. Director lean = the extraction/parse IS the hard blocking thing now; solve it (how = USER's parser-constraint call). NOT autonomously committing a constrained build. DIAGNOSTIC DONE (ae383749, 42 real sentences hand-verified): trained parser = PARTIALLY USABLE, NOT a drop-in bridge, but degrades GRACEFULLY (strictly better than the prior hand-rule HARD_FAIL). Mentions ~90%+ (but ~5% catastrophic POS fails, e.g. "The sun shines bright" mis-tagged). Roles via LINEAR-ORDER proxy (pre/post-verb) because arc_parser returns UNLABELED arcs -- systematic failures on quotative-inversion dialogue tags ("said Andrew"->Andrew=PATIENT, every time = a DEFINING McGuffey feature), PP-misattachment (~1/3), subordinate-clause scope-bleed (~30% g3-g4). 🔑 KEY LEVER (already half-built): the trained `arc_labeler_hashed_ud_ewt.json` checkpoint EXISTS but has NO decode/consumer code -- decoding it gives real nsubj/obj/obl labels -> would kill the dominant PP + quotative failure classes. Plus ParseResult.margins (per-arc confidence) to gate low-confidence structure. => extraction gap is BRIDGEABLE with concrete tractable steps (decode the existing labeler + confidence-gate), IF the USER permits using our trained parsers.
🆕 FORK NOW FULLY INFORMED — both sides gathered (earn-path drill a7c29be5, notes/research_earn_structure_extraction_vs_supply_parser_fork_2026-08-01.md): (1) the MIDDLE option "train OUR OWN parser on UD-EWT" is REASONED to still be on the SUPPLIED/bolt-on side (the label categories nsubj/obj/agent/patient are externally-authored; self-hosting changes transparency NOT the earned-vs-supplied line) -- so the fork is really SUPPLY-structure (any parser fit to external labels) vs EARN-structure (self-supervised, no external labels ever). (2) a genuinely constraint-CLEAN EARN path EXISTS + is brain-faithful + efficient: mention+role are ONE staged predict-hold-revise mechanism in the brain, so extend the already-spec'd 07-30 causal-predictive encoder + PE-gate so mention-introduction + role emerge from its OWN prediction-error (Option C, one build serves 'who' + 'who-did-what'); self-supervised structure induction (Manning 2020/StructFormer) is the heavier Option B. (3) COST: the EARN path is a real research program -- gated on BUILDING the 07-30 causal-predictive encoder FIRST (only a spec, not trained), and unsupervised extraction is noisier (P=0.28 on the cheapest test). Path A (parser) is fast but the drill argues it IS the supplied reading-mechanism the lock forbids. Step-0 (do regardless): hand-annotate ~50-100 gold mention/role sentences = eval set for either fork, no GPU. DIRECTOR LEAN: earn it (Path B/C).
✅✅ USER LOCKED (2026-08-01 ~12:50Z): EARN it. + USER REFRAME (correct; Director had OVER-SCOPED to a 15h build): this IS the GRAMMAR-LEARNER pattern (encounter -> LOG + give EXACT answer [MEANING=ASSIGNMENT] -> accumulate encounters -> discovery-gate/proginduction identifies the pattern across them -> GENERALIZES -> new RULE applying to NOVEL instances). FIRST step is CHEAP, NOT the 15h encoder: PLANT a KNOWN rule in synthetic encounters + see if the substrate UNCOVERS + GENERALIZES it. **REORDERED PLAN:** (1) NOW = cheap planted-rule-INDUCTION probe (RUNNING hdi_exp_dev a742de76; reuse proginduction_plugin + CRP/MDL discovery gate + situation-model substrate; measurement-first, CPU-cheap; CAN-FAIL floor = no-generalization/pure-lookup MUST fail on NOVEL instances; report the learning curve accuracy-vs-#encounters). (2) THEN apply the validated mechanism to REAL structural-extraction rules (mention/role from real syntax). (3) 07-30 causal-predictive encoder = LATER SCALING step for real text, NOT the first gate. Then enrichment (discourse/predictive/inference) + discovery gate + persistent-schema frontier. Cleanup: role-competency stale-citation.
🧠 RULE-MEMORIALIZATION (brain-foundational, USER Q 2026-08-01 — load-bearing design decision, steered into probe a742de76): a learned rule is stored the BRAIN's way = a DISTRIBUTED relational structure that FACTORS the rule's STRUCTURE apart from its CONTENT/fillers (entorhinal-hippocampal grid / Tolman-Eichenbaum-Machine; consolidated from exemplars via CLS replay) -- it GENERALIZES to NOVEL instances BY CONSTRUCTION precisely because structure is stored separately from fillers. Substrate-native form = an FHRR STRUCTURAL-BINDING operator in its OWN near-orthogonal subspace (bind the same structure to a NEW filler -> the rule fires). NOT a symbolic program (that is the LEAST brain-faithful option). GLASS-BOX = INSPECTABLE, not SYMBOLIC (you can decode the relation-structure / which subspace / source exemplars / which fillers it fires on). Brain's 3 systems: (a) neocortical consolidated weights = the schema/rule store (CLS/replay), (b) PFC+basal-ganglia = abstract-rule holding + gating, (c) entorhinal structure-content FACTORIZATION = the generalization mechanism (the load-bearing one for our extraction rules). proginduction_plugin = a LEGIBILITY READOUT of the induced rule, NOT the primary store. Can-fail discriminator: pure exemplar-lookup (memorize fillers) MUST fail on novel fillers; storing the factored STRUCTURE generalizes to them. 🆕 BRAIN-FAITHFUL RE-RUN LANDED + DIRECTOR-VET'd (2026-08-01 ~13:15Z, metrics.json ts 13:11Z, commit 76f9e6249): the re-run re-centered the primary mechanism to a structure/content-FACTORED FHRR store (per the steer): one FHRR unit per (atom,bool) slot + one per unordered atom-PAIR joint-state (the interaction channel the planted XOR needs), bind+bundle per instance, CLS-dedup to canonical-per-combo prototypes, apply=nearest-prototype cosine cleanup (reuses hdlab.bundling/atoms unmodified); proginduction demoted to legibility readout. VERDICT still MIDDLE_BAND, mechanism_final=0.750, but CLEANER than pre-steer: rise_ok now TRUE (0.500 flat through N=32, then jumps to 0.750 at N=64 exactly when distinct TRAIN combos covered hit 12/12 = a genuine accumulate-then-generalize curve), floor FLATLINED at 0.500 (can-fail control held, leakage_check_passed). 3/4 held-out-NOVEL combos correct. ⚠️ DIRECTOR OVER-READ CATCH (disk-verified, #4 of session pattern): the agent's verdict_msg claims FHRR + proginduction "land on the IDENTICAL held-out subset" = convergent evidence it's an information-theoretic identifiability tie NOT a defect. FALSE per per_checkpoint disk: at N=256 FHRR misses (T,T,F,F) [pred AGENT/true PATIENT] while proginduction misses (F,F,F,T) [pred AGENT/true PATIENT] — SAME accuracy (0.75), DIFFERENT missed combo. So "not-a-defect identifiability tie" is UNPROVEN; per flat=fix, 0.75 is a genuine partial-generalization shortfall to DIAGNOSE, not accept. HONEST CAVEAT the agent GOT RIGHT (load-bearing): this task has NO separate filler/entity dimension — atoms ARE the structure, so "novel FILLER→rule fires" (the actual entorhinal/TEM structure-content-factorization claim, and the one that maps to real comprehension: known construction on a never-seen entity) was NOT tested here; it collapsed to "novel atom-COMBO→rule fires." NET: mechanism genuinely generalizes above a fair floor (real signal, fair test), but the load-bearing brain-faithful claim is UNTESTED + the 0.75 is a real shortfall. POST-COMPACTION PICKUP → NEXT PROBE: FILLER-GENERALIZATION test — DONE + DIRECTOR-VET'd (2026-08-01 ~13:39Z, commit 9a26c73d2, cell exp_grammar_learner_filler_generalization_v1). RESULT (disk-verified): the structure/content-factored FHRR store generalizes a learned rule to BRAND-NEW filler identities never bound at learn time — mech_novel=0.875 = mech_seen=0.875 (parity_gap=0.0 exact, every checkpoint), while the filler-keyed LOOKUP floor stayed pinned at 0.625 on novel probes (must-fail control fired, ~0.25 gap), positive control passed, arms differ. ⚠️⚠️ HONEST SCOPE — this is a REDISCOVERY, not new capability (the agent flagged it, I verified on disk): exp_role_filler_factorization_compgen_v1.py (Jul 18) ALREADY HARD_PASS'd the identical core claim at HIGHER rigor (5 seeds, learned g_hat via TEM-Hebbian, FACTORED held-out=1.000 vs FLAT 0.003, gap 0.997). And parity_gap=0.0 is EXPECTED by FHRR algebra (exact bind/unbind invertibility), not a surprise. The 0.875<1.0 here is a bundling-capacity ceiling (16 near-orthog vectors→2 prototypes), NOT a factorization failure. IDENTIFIABILITY CHECK (contract item 5): the sibling cell's 0.75 plateau = a STRUCTURAL MDL tie (any boolean function ties its exact negation at ~7e-15 bits, since majority-class-per-bucket is chosen independent of which bool labels the bucket) = a genuine mechanistically-explained identifiability gap, NOT a fixable mechanism weakness. AND independently CONFIRMED my over-read catch (FHRR misses (T,T,F,F), proginduction misses (F,F,F,T) = DIFFERENT combos; prior cell's "identical held-out subset" claim is factually wrong).
🎯 STRATEGIC READ (honest-lever check, the load-bearing takeaway): the EARN-MECHANISM — a structure/content-factored FHRR store that induces a rule from accumulating encounters + generalizes it to NOVEL fillers while a memorization floor fails — is now ROBUSTLY PROVEN (a 5-seed prior HARD_PASS + this session's loop-wired confirmation). Step (1) of the earn plan = VALIDATED. ⚠️ BUT do NOT over-read: BOTH probes ran on ORACLE-CLEAN structure (the rule-relevant atoms are HANDED to the store). The actual WALL — extracting those structure atoms OUT of real McGuffey syntax — is UNTOUCHED. Running more clean synthetic factorization confirmations = the easy-path trap (we'd be re-confirming a Jul-18 result a 3rd time). The HARD blocking thing = step (2): apply the validated mechanism to REAL mention/role extraction. THAT hits the USER-steer FORK (does using our trained pos_tagger/arc_parser as input FEATURES count as "our mechanism" or a forbidden "bolt-on parser"? — the earn-drill argued a parser fit to external labels is on the SUPPLIED side; the clean EARN path = extend the 07-30 causal-predictive encoder, a real build). => NOT autonomously committable past the fork. LOCK-CLEAN autonomous step that serves BOTH forks + needs no parser-decision + no GPU = Step-0: build a hand-verified gold mention/role EVAL set from real McGuffey g1-g4 sentences (supplying DATA is explicitly OK). Dispatched a bounded scaffold agent for that; the fork itself is surfaced for USER steer.
🆕 GOLD-EVAL SCAFFOLD LANDED (2026-08-01 ~14:0Xz, commit 5ab2b3f05, data/eval_gold_mention_role_mcguffey_v1/candidates.jsonl, 60 real McGuffey sentences g1=14/g2=18/g3=14/g4=14; hard-class quotative=22/passive=10/relative=10/PP=10/simple=8; builder tools/build_mention_role_eval_mcguffey.py). These are CANDIDATES (gold_verified:false), labeled by pos_tagger + arc_parser's UNLABELED arcs + a position heuristic — they need director verification to become gold. Complementary to the 2 pre-existing McGuffey gold files (gold_mcguffey_castle_building_svo_v1 / _lccp_argstruct_v1, which EXCLUDE quotatives + lack mention-spans). 🎯🎯 KEY MEASURED DATAPOINT (quantifies the WALL, spot-check 10 sentences, seed 7): the parser+position-heuristic candidates are 9/10 WRONG on the hard classes — ALL 6 quotative-inversion sentences mislabel the postposed speaker (position of verb is not an agent/patient cue under inversion), + expletive-there / shared-subject / mis-attached-passive failures. Since quotative inversion = 37% of real McGuffey and the dominant failure, this CONCRETELY sizes the real-extraction gap AND sharpens the fork: the naive "trained parser as position-features" path (Path A) is NOT usable as-is; the specific fix the banner already flagged = DECODE the trained arc_labeler_hashed_ud_ewt checkpoint (real nsubj/obj labels, no consumer code yet) would attack the dominant quotative class directly. NOT auto-committing that (still the USER parser-lock fork). NEXT (director work, lock-clean, when engaged): verify/correct the 60 candidates into real gold; that gold set then scores whichever fork the USER picks.
✅✅ FORK RESOLVED — USER LOCKED PATH B (2026-08-01 ~14:20Z): "path b is entirely what we should be doing — we need this to earn it and learn. brain foundational." = the self-supervised EARN path, NOT the trained-parser (Path A rejected as supplied-side + measured unusable at 9/10-wrong). USER STEER: do NOT jump straight to the 15h build — run FASTER experiments to explore the design space first. WHAT THE 15h BUILD IS (from spec notes/brain_syntax_to_role_mechanism_and_forward_predictive_encoder_spec_2026-07-30.md, read in full): train a from-scratch causal predictive-coding TinyTransformer (own BPE, no borrowed embeddings) on ~158M tokens = (a) flip attention mask bidirectional->CAUSAL + predict next-latent from LEFT context only (Schrimpf/Goldstein: causal LMs best-fit brain; our bidir encoder INVERTS on passives 0.16-0.18 because it never commits-then-revises), (b) a PE-GATED hold-then-REPLACE h_role state (reuse slot_attention_wm.py PBWM gate at token granularity; same gate spawns mention slots at referent-intro = one organ serves who + who-did-what), (c) a cheap clause-level hierarchy head. ~15-19 GPU-hrs; fair test = the built voice-role probe (0.16->target>=0.70). Spec's OWN honest read: P(hard-pass)=0.22, partial~0.35, 3 confounds (causal halves context; can't separate prediction-vs-incremental-commitment; 158M-token fail is ambiguous hypothesis-wrong-vs-scale) => a 15h run landing in an ambiguous middle band is the bad outcome the USER is right to de-risk. 🪜 DE-RISKING LADDER (faster experiments BEFORE the 15h commit): PROBE 1 (RUNNING hdi_exp_dev a3fbd59c, CPU, no GPU) = validate the PE-gate hold-then-REVISE MECHANISM on SYNTHETIC ORACLE streams: does a PE-gated REPLACE correctly revise a canonical-default role at a disambiguation cue + spawn a mention slot at referent-intro, given a CLEAN signal? Can-fail: no-revise control must INVERT on passives (reproduce 0.16-0.18 on clean data); positive control (oracle PE) must pass. Reuses the VET'd PBWM gate verbatim. GATES Probe 2 (no point on GPU if the gate can't commit-then-revise on clean input). PROBE 2 (small/short causal encoder ~1-3 GPU-hrs, gated on Probe 1) = does a causal objective STOP the inversion (move 0.16 up) + does its PE-stream spike at referent-intros on the new gold set? Turns hypothesis-wrong-vs-scale into a measured scaling signal. PROBE 3 (bundled into Probe 2) = causal-vs-bidir-vs-random matched small budget, isolates causal-mask vs prediction-objective (Antonello&Huth confound). ONLY commit the 15h full run if the small proxy shows the signal moving right.
🧠 USER STEER (2026-08-01 ~14:35Z, applies to the WHOLE ladder incl Probe 2/3): make BRAIN-FOUNDATIONAL a CORE DESIGN INPUT of every probe, not cited motivation. Per COMPONENT-FIDELITY-FIRST + BRAIN-FIDELITY-ELEMENT-AUDIT: judge each mechanism on the BRAIN'S OWN METRIC (does it reproduce the brain's mechanism?), NOT only downstream task-accuracy. For the PE-gate probes, brain-fidelity metrics ADDED alongside the can-fail controls: (1) SHAPE — staged commit-then-REPLACE (canonical default first, held, replaced not blended); (2) TIMING — revision fires AT the disambiguation cue (P600 locus, Osterhout&Holcomb), measure flip-position vs gold cue; (3) FAILURE-SHAPE — the no-revise ablation must reproduce the AGRAMMATISM/TDH DIRECTIONAL INVERSION (below-chance reversal, Grodzinsky), NOT unsystematic chance-noise (wrong failure shape). A PASS must mean "reproduces the brain's staged commit-then-revise (right shape+timing+failure-signature)," not merely "high accuracy." Steered into Probe 1 (a3fbd59c) mid-run; carry into Probe 2/3 design (judge the causal encoder on brain-metric: does it reproduce staged commit-then-revise dynamics + the causal-LM-best-fits-brain/Goldstein surprisal signature, not just role-accuracy up).
✅ PROBE 1 LANDED + DIRECTOR-VET'd (2026-08-01 ~14:45Z, commit c5ccca16, cell exp_role_gate_hold_revise_oracle_probe_v1, disk-verified). HARD_PASS, 15/15 gates, 5 seeds: the PE-gate does commit-then-revise (role oracle_acc_noncanon=1.0, mention balanced_acc=1.0 across indefinite/bare-plural/proper-name = not an a/an detector) in the BRAIN-FAITHFUL shape+timing+failure-signature per the steer — frac_staged=1.0 (staged hold->REPLACE not blend), flip_offset=0.0 (fires exactly AT the cue = P600 locus), pe_at_revision=1.0 (P600 analog), oracle margin 1.18 (decisive) vs blend margin 1.2e-7 (ambiguous), and the NO_GATE ablation reproduces the AGRAMMATISM/TDH DIRECTIONAL INVERSION (acc_noncanon=0.0 while acc_active=1.0 = systematic reversal below the randguess-0.496 chance floor, gap 0.496 — right failure SHAPE, not noise). ⚠️ HONEST SCOPE (agent flagged, I concur): this is CONSTRUCTION-DETERMINED plumbing+fidelity confirmation (oracle PE supplied, replace-candidate content supplied, deterministic vocab => 1.0/0.0 by construction), NOT a scientific surprise. It de-risks the GATE only (the gate isn't the problem + its brain-fidelity holds); it says the 15-19h encoder build is NOT moot, nothing more. The real open question — does a REAL trained causal encoder PRODUCE a usable/timely PE signal on messier real syntax — is Probe 2. => PROBE 1 GATE CLEARED. NEXT = Probe 2a (small causal encoder + the ALREADY-BUILT synthetic voice-role probe exp_syntactic_role_agent_patient_voice_probe_v1: does a causal objective STOP the 0.16-0.18 inversion? bundles the causal-vs-bidir-vs-MLM-vs-random control = Probe 3, isolates causal-mask vs prediction-objective) — needs small GPU, needs NO gold (synthetic probe), USER-endorsed as a faster design-space experiment. Probe 2b (PE-spike-at-referent-intro on REAL gold) needs the 60 candidates verified into gold first (parallel director work). NOTE: all session commits are on branch dataprep/mcguffey-graded-corpus (local-only, consistent, no push).
⏳ PROBE 2a INTERIM (2026-08-01 ~15:0Xz, hdi_exp_dev a867e92d, commit 1fc0d090b — NOT DECISIVE, GPU control arms IN FLIGHT + ALIVE: util 87%, gpu_runner_0 running encoder_latent_pc_arc_v1_lite on overnight_queue): MEASURED so far — ARM_MLM (existing bidir) reproduces the inverted wall cleanly (a->p 0.179, p->a 0.163; within-voice 0.90/0.85 = ENCODER_POSITION_ONLY). ARM_LPC_CAUSAL (a PRIOR 07-30 lite ckpt) = 0.0167/0.0167, MORE inverted — BUT this ckpt had a LITE_COLLAPSE verdict (rep_std 0.0181 < 0.020 anti-collapse floor) => the number is a TRAINING-COLLAPSE ARTIFACT (spec risk #3: undertrained collapse at small proxy scale), NOT a clean read on the causal-mask hypothesis. ⚠️ DO NOT over-read the 0.0167 as 'causal refuted'. The decisive read awaits the BIDIR + RANDOM control arms (retraining now at matched lite budget): if BIDIR ALSO collapses => the lite proxy BUDGET IS TOO SMALL for stable training (size up modestly, still <<15h, re-run) = the proxy is underpowered, not the hypothesis wrong; if BIDIR trains fine + reproduces the ~0.16 wall while CAUSAL uniquely collapses => causal-mask harder to train stably at small scale (needs more budget/tuning before it can be read). Either way the current data CANNOT answer the 15h go/no-go yet. Agent also fixed 3 real bugs (cross-C:/D:-mount relpath crash on --ckpt-path; crash-handler clobbering per-ckpt out_dirs; --lite unreachable via prod runner's zero-arg invocation) — genuine infra fixes, self-tests re-PASS. POST-PICKUP: read data/exp_syntactic_role_agent_patient_voice_probe_v1__ckpt_*_ARM_LPC_BIDIR/RANDOM/metrics.json when they land; VET the collapse-vs-clean disambiguation; then either greenlight a slightly-bigger proxy or read the causal signal.
✅ PROBE 2a LITE RUN COMPLETED + collapse read (2026-08-01 ~15:20Z, DIRECTOR-VERIFIED on remote C:/dev/hd-instrument metrics.json): verdict LITE_COLLAPSE. Decisive disambiguation = ARM_LPC_BIDIR rep_std=0.0248 (ABOVE 0.020 floor, trained CLEAN) while ARM_LPC_CAUSAL rep_std=0.0128 (BELOW floor, COLLAPSED; min_target_std 0.966 healthy => ONLINE-encoder representation collapse, not a target problem), ARM_RANDOM 0.0121 (floor control). => at this cheap budget the CAUSAL objective is TRAINING-UNSTABLE where BIDIR is stable; the causal encoder is DEGENERATE so we CANNOT yet read whether causal de-inverts. HONEST: this is 'causal harder to train stably at small scale', NOT 'causal hypothesis refuted' (cell's own verdict_msg concurs). NEXT HARD STEP dispatched (agent a867e92d resumed): fix the causal collapse with ONE variable (single anti-collapse lever — VICReg-variance-weight OR LR-warmup OR lower-LR), re-train ONLY the CAUSAL arm at the SAME budget (reuse BIDIR/RANDOM ckpts), confirm rep_std clears floor, THEN run the voice-role probe on all 3 -> the decisive causal-vs-bidir DIRECTIONAL-INVERSION comparison. If it still collapses at same budget after the one-knob fix -> recommend the MINIMAL budget bump (still <<15h), no silent escalation. ⚠️ PROCESS NOTE (Director error caught + corrected this tick): the REMOTE repo working copy is C:/dev/hd-instrument, NOT D:/AI/hd-instrument (that is the LOCAL path). I ran several remote filesystem sweeps against D:/AI (empty results) and nearly false-alarmed a 'silent non-completion'; the run had actually completed fine. Remote fs checks MUST use C:/dev/hd-instrument. (proc/GPU/nvidia-smi checks are path-independent and were fine.)
⏳ PROBE 2a ITER-2 (2026-08-01 ~16:12Z, var_coef=2.0 causal-only, DIRECTOR-VET'd on disk) — STILL INCONCLUSIVE: the one-knob anti-collapse fix MOVED causal rep_std 0.0128 -> 0.0180 (monotone improvement) but STILL under the 0.020 floor (BIDIR clean 0.0248, RANDOM 0.0121) -> verdict LITE_COLLAPSE persists (cell calls it 'UNDERTRAINED collapse'). AND the voice-role probe CELL_CRASHED on BIDIR+RANDOM and produced no cross-voice number for CAUSAL -> still NO usable de-inversion comparison. TIME-BOXED FINAL proxy iteration dispatched (agent a867e92d): (1) FIX the probe CELL_CRASHED first (blocking; get real active->passive/passive->active per arm), (2) ONE new variable = MINIMAL BUDGET BUMP (~1.5-2x steps, keep var_coef=2.0 as now-standard, still <<15h) since the cell's own diagnosis is 'undertrained' (more training, not more reg), (3) gate rep_std>=0.020 BEFORE probing, (4) if it STILL collapses after the bump -> STOP + that persistent collapse IS the honest go/no-go signal (causal objective is training-stability-costly at small scale; a clean read needs the fuller build budget). No 4th tweak. GUARD: watching the instrument-perfectionism trap -- this is the last cheap iteration before escalating the read to USER.
✅ PARALLEL (lock-clean, director work while GPU runs): GOLD mention/role verification STARTED. Verified the QUOTATIVE subset -> data/eval_gold_mention_role_mcguffey_v1/gold_quotative_verified_v1.jsonl (20 director-verified rows; 2 truncated fragments excluded). MEASURED: the parser mislabels the SPEAKER-as-agent in 19/20 (95%) -- every 'said/cried/asked X' postposed speaker is dropped to patient/None. 🎯 KEY FINDING (encouraging for Path B): the DOMINANT real-extraction failure (quotative inversion, 37% of McGuffey) is ONE highly-regular construction (speech-verb + postposed subject -> that noun is the speaker/AGENT) = exactly the kind of learnable rule the commit-then-revise / grammar-learner mechanism should acquire. NOT chaos. This gold subset = the eval target for Probe 2b's referent-intro/role test on real text. GOLD VERIFICATION now characterizes the wall precisely (3 subsets verified): QUOTATIVE 19/20 parser-WRONG (95%, postposed speaker->patient), PASSIVE 6/7 WRONG (86%, surface-subj->agent), RELATIVE/COMPLEMENT 8/8 CORRECT (0% error; parser handles canonical-order clauses incl relative-pronoun-as-agent). => the real-text extraction wall is SPECIFICALLY the non-canonical ORDER!=ROLE constructions (quotative inversion + passive) = the exact TDH/commit-then-revise cases the causal encoder targets; NOT a broad parser failure. Files gold_{quotative,passive,relative}_verified_v1.jsonl. NEXT: PP subset (last).
🧠🔬 DRILL LANDED — a BETTER, more brain-foundational training objective (USER-requested 'is there a better way?'; synthesized by Director from 3 lit-scans, parent agent parked; banked notes/research_brain_faithful_collapse_free_predictive_encoder_objective_2026-08-01.md). ROOT CAUSE of our collapse: we predict the encoder's OWN EMA latent (BYOL/I-JEPA self-distillation) -- NO external entropy anchor, anti-collapse is an architectural trick (predictor+stop-grad)+a VICReg variance hinge, EMPIRICALLY FRAGILE at small model/batch (CITED) = exactly why the harder causal arm collapses. BIOLOGY (ESTABLISHED): cortex predicts a REAL external/lower-level signal (predictive coding, Rao&Ballard/Friston) and stays collapse-free STRUCTURALLY via lateral inhibition/DECORRELATION + sparsity -- NEVER a self-copy target. CONVERGENCE (the key): the more brain-faithful choice is ALSO the more collapse-robust one. RANKED FIX (minimal-change): RANK 1 = predict a REAL target (regress the actual next-token embedding, arXiv:1902.11269 -- keeps the d_model head, STILL OOM-safe, drops EMA) = cortical predictive coding, collapse-proof by construction, one-axis change; RANK 2 = drop EMA keep stop-grad (SimSiam: EMA isn't load-bearing) as a cheap control; RANK 3 = swap VICReg's variance hinge for BARLOW-TWINS decorrelation (CITED most small-batch-robust AND most brain-faithful = lateral-inhibition analog). Can-fail: real-target causal clears the 0.020 collapse floor at the SAME cheap budget where self-distillation collapsed. ⚠️ PROXY-LIMIT (iter-3 MEASURED): the fixed voice-role probe reads BIDIR=0.0/0.0 AND RANDOM=0.0/0.0 at lite budget (both fully inverted; full-budget MLM=0.179) => the role-reading CAPABILITY doesn't emerge at cheap budget for ANY arm; the small proxy can answer the COLLAPSE axis but NOT downstream de-inversion (spec risk #3, needs fuller budget). ITER-3 in flight (2x causal steps on the OLD objective, gate rep_std>=0.020) = last old-objective datapoint. 🎯 DIRECTOR RECOMMENDATION TO USER (strategic pivot): stop bumping budget on the collapse-prone self-distillation objective; PIVOT the causal encoder objective to REAL-TARGET prediction (Rank 1) + Barlow decorrelation (Rank 3) -- more brain-faithful AND collapse-robust AND a richer learning signal -- and re-run the proxy on the collapse axis, then it's also the right objective for the 15h build. Awaiting USER nod on the objective pivot (bigger than a knob-tweak). ✅ USER SAID YES -> pivot dispatched (2x2: real+Barlow / real+VICReg / EMA+Barlow / old-baseline; pre-reg rep_std>=0.020 band, commit 60e5bc93d). ⚠️ PIVOT RUN #1 FAILED on GPU (queue status=failed run_index=4; self-test passed but real GPU run died ~3min in at GradScaler init; log C:/dev/.../overnight_queue/encoder_latent_pc_arc_v1_lite.log). Suspected: a runtime bug in the new real-target/Barlow GPU path the tiny CPU self-test didn't exercise + a possible 'd=16 L=1' toy-dims mis-wire. Agent a867e92d resumed to pull the traceback + fix + close the self-test-passes/real-run-crashes gap + re-run. 🚩 CHURN NOTE (honest): this cell has crashed/failed ~4x this session from the agent's own edits (iter-3 **_LPC_COMMON drop, probe arms-differ assert, this GPU fail) -- the OBJECTIVE PIVOT DIRECTION is right (drill-validated), but execution is edit-crufty. Instructed: if it fails a 3rd time STOP -> reassess whether the cell needs a cleaner rebuild rather than another blind re-dispatch.
🚨 RUN-5 LANDED (2026-08-01 ~14:30Z, completed, DIRECTOR-VET'd on disk C:/dev metrics.json) = CLEAN PRE-REGISTERED NEGATIVE: verdict LITE_COLLAPSE_PERSISTS. Per-arm eval rep_std (floor 0.020): REAL_BARLOW=0.0078 (the brain-faithful pivot -- collapsed WORSE than old), REAL_VICREG=0.012, EMA_BARLOW=0.0186, old EMA_VICREG=0.018, BIDIR=0.0248 (only arm above floor), RANDOM=0.012. => the drill's Rank-1+Rank-3 (real-target regress-to-embedding + Barlow) did NOT fix the collapse -- it made it WORSE; 2x2 attribution consistent (real target -> more collapse in BOTH regularizer conditions). The drill's P~0.7 'real target is collapse-robust by construction' is MEASURED-REFUTED at this budget. 🧠 MECHANISM INSIGHT (drill-negative 2x, brain-grounded -- REFINES not just refutes): 'real target has intrinsic entropy' is FALSE when the target is a LEARNED-but-UNDERTRAINED next-token EMBEDDING -- at 6000 steps the embedding table is near-degenerate, so regressing to it is regressing to a low-entropy target -> collapse. The brain's real target = actual SENSORY INPUT (FIXED high entropy regardless of training); the faithful external analog = token IDENTITY (sampled/adaptive softmax, fixed intrinsic entropy), which the drill flagged as the fully-external fallback. So 'predict a real target' stays right, but it must be EXTERNALLY-FIXED (token identity), NOT a learned embedding -- regress-to-embedding was the LEAST-external 'real target' option. ⚠️ PROXY EXHAUSTED (honest-lever + instrument-perfectionism guard): the cheap proxy has now shown the causal arm collapses across EVERY variant tried (EMA/VICReg, +2x budget, real/Barlow, real/VICReg, EMA/Barlow); only BIDIR survives; AND (proxy-limit) role-reading doesn't emerge at lite budget for ANY arm -> de-inversion is UNANSWERABLE cheaply. => STOP cheap-proxy iteration (further tweaks = polishing the instrument off the critical path). The real question (does a causal encoder at ADEQUATE SCALE train stably + de-invert roles?) needs a FULLER-BUDGET run = a USER compute-commitment decision. DIRECTOR RECOMMENDATION TO USER: commit a MID-scale run (not necessarily full 15h) using the REFINED brain-faithful EXTERNAL target (token-identity/sampled-softmax, addressing WHY regress-to-embedding failed), measuring BOTH collapse-at-scale AND de-inversion. Escalated to USER for the budget call. (Caveat to verify if we do re-run: confirm the real-target regression stop-grads the embedding target; but the negative is pre-registered + consistent, treated as trustworthy.) CELL-CHURN honest tally: run-5 itself was clean (the pivot ran to completion, no crash) -- the earlier ~4 crashes were prior iterations; the cell is usable, the NEGATIVE is real not a bug.
🛑🛑 CORRECTION -- RUN-5 NEGATIVE IS RETRACTED (skunkworks adversarial VET aacf685c, DIRECTOR disk-confirmed): the run-5 collapse was NOT a fair test of the hypothesis -- it was a BROKEN EXPERIMENT. The USER was RIGHT ('shouldn't have collapsed if truly brain-foundational'). MEASURED (train_diag, disk-verified): the real-target arms' predictive task NEVER LEARNED -- REAL_BARLOW init_pred_loss 0.420->final 0.419 (FLAT), REAL_VICREG 0.431->0.440 (WORSE); mean_pairwise_cos REAL_BARLOW=0.968 WORSE THAN RANDOM (0.924); vs the arms that DID learn (EMA_BARLOW 0.225->0.199 cos 0.821; old LPC_CAUSAL 0.242->0.108). A trained arm worse-than-random = mis-specified, not collapse-vs-scale. THREE DEFECTS: (1) [ROOT, non-brain-faithful] the 'real target' = online.tok_emb(ids).detach() = the encoder's OWN learned embedding table, AND it's simultaneously Barlow-regularized (line 877) -> a CO-ADAPTING, undertrained, self-referential moving target -> the predictive task is UNLEARNABLE -> no pressure to differentiate -> reps drift together. The 'collapse-proof by construction' comment is DEFEATED (target isn't fixed/real). This CONFIRMS the brain-fidelity gap the USER pointed at: cortex regresses to REAL EXTERNAL sensory input (fixed entropy), NOT a self-generated embedding under simultaneous optimization. (2) [measurement decoupling] Barlow WORKED on its tensor (final_train_rep_std 0.998) but the collapse VERDICT measured a DIFFERENT tensor (L2-normed pooled mention reps) -> 'Barlow failed' is unfair, it never regularized the quantity that collapsed. (3) [2x2 confound] real arms carry an extra tok_emb reg the EMA arm lacks + different loss baselines + the EMA+VICReg cell is the OLD lpc_train path (clause loss + role gate) not the same function -> not one-variable. REFUTED suspects: target IS .detach()'d (not the co-collapse bug); batch fine (~1200/step, Barlow effective). => DO NOT treat 'real-target refuted' or 'fuller budget required' as established -- the hypothesis was NEVER fairly tested. FIX (audit-named, for the corrected re-run): (a) regress to a FIXED/FROZEN EXTERNAL target (frozen embedding snapshot OR token-identity/softmax) so the predictive task is LEARNABLE; (b) apply anti-collapse to the SAME tensor the verdict measures; (c) fix the 2x2 to true one-variable (same function, matched reg); (d) add a beat-RANDOM sanity gate before banking any collapse verdict. Brain-fidelity DRILL (a0934aeb, notes/research_brain_fidelity_audit_collapse_proof_predictive_objective_2026-08-01.md, commit 71d738f68) LANDED + INDEPENDENTLY CONVERGED with the code audit: collapse = TWO fidelity gaps -- (1) target NON-external (learned co-adapting embedding, not external token IDENTITY) [= audit Rank 1], (2) soft Barlow-only has a variance loophole vs hard k-WTA SPARSE CODING. MINIMAL collapse-proof brain-faithful config (one-axis-at-a-time): AXIS 1 = external token-identity SAMPLED-SOFTMAX target; AXIS 2 = TOP-K (k-WTA) sparse latent (the brain's structural always-on anti-collapse soft-Barlow lacks); homeostatic term = soft residual-closer only; PC error-architecture de-prioritized. P_deflated 0.42. => code-audit + brain-drill AGREE: the target must be EXTERNAL (token identity), + add k-WTA sparsity. Corrected experiment dispatches now.
🗄️ SUBSTRATE-CURRENCY (USER flagged 2026-08-01 ~16:15Z; skunkworks audit a313e224, atomize commit 108993d82) — TWO findings: (1) ATOMS now current: tonight's settled results banked = 29599 PE-gate commit-then-revise (HARD_PASS, oracle-fidelity-scoped), 29600 rule-induction (MIDDLE_BAND 0.750 not HARD_PASS — MDL split-identifiability tie, not defect), 29601 filler-gen (confirmatory REDISCOVERY of the Jul-19 compgen HARD_PASS, +0 cert), 29602 gold-wall (agent-inversion=quotative95%+passive86% only), 29603 META (VET-negatives-as-hard-as-positives + externally-fixed-target lesson). Net cert +2. (2) 🚨 director_kb QUERYABLE INDEX DEAD 6 DAYS: continuous-ingest loop crashed 2026-07-26 (WinError 1450 on a monolithic 2.8M-triple scan), not self-healing => ALL 07-27->08-01 notes/preregs/metrics are committed-to-git but NOT in the queryable KB => tonight's prior-work/dedup checks ran on a STALE index (part of why we hit the filler-gen rediscovery). FIX dispatched (hdi_testbed a54d0c96): chunk+resumable ingest to clear the OOM + re-ingest the backlog + a session-start freshness gate (durability=session-read-not-crons). ⚠️ HONESTY DEFLATION (skunkworks, accepted): I over-framed the earn-mechanism as 'proven' — it is MIDDLE_BAND + a rediscovery, real-signal-above-floor but SCOPED, not a clean new HARD_PASS. Going forward: atomize as results settle (not batch).
🚨 RUN-6 LANDED (2026-08-01 ~17:30Z, all 7 arms recorded in units.jsonl then crashed on the LPC re-train TAIL; queue status=failed but arm data intact; DIRECTOR disk-read) = SURPRISING RESULT THAT INVERTS THE DRILL. Per-arm rep_std/cos (floor 0.020, RANDOM 0.012/0.924): EXT_BARLOW (external token-id+Barlow) 0.0002/0.99998 COLLAPSED worse-than-random (but pred_drop 0.31 = DID learn the token task); EXT_VICREG 0.0002/0.99996; EXT_BARLOW_TOPK 0.0001/0.99999 (k-WTA did NOT prevent collapse); EMA_BARLOW 0.038/0.242 HEALTHY; EMA_VICREG 0.038/0.678 healthy; LPC_BIDIR 0.025/0.676. => the EXTERNAL-target arms ALL collapsed, the EMA self-distillation arms are the HEALTHIEST — OPPOSITE of the drill's 'external target is collapse-proof, EMA is fragile.' 🚨 DO NOT CONCLUDE (two interpretations, VET-gated): (A) GENUINE — next-token-IDENTITY prediction (sampled-softmax) optimizes a DIFFERENT quantity than the pooled MENTION rep we measure, so it's free to collapse the pooled rep, whereas EMA-latent prediction directly shapes the latent; if so the drill's target-externality fix was WRONG for our measured quantity + the run-5 retraction's diagnosis was itself an over-read. (B) ARTIFACT — the Barlow reg is ineffective/decoupled/underweighted on the EXT path (log shows reg=1.0000 FLAT while pooled_rep_std->0.0001) = ANOTHER fairness bug, meaning EXT arms never got working anti-collapse = still not a fair test. ADVERSARIAL VET DISPATCHED to settle A-vs-B. 🛑 META (honest, load-bearing): this cell has now produced ARTIFACT/fairness-questionable results across ~6 runs (run-4 transient, run-5 self-referential-target broken, run-6 inverted+reg-flat-suspicious) — the encoder-proxy is CHURNING and each 'result' has needed a fairness catch. => STRONG signal to (per the instrument-perfectionism guard + my earlier 3-strikes rule) PAUSE the encoder-proxy iteration + let the BROAD BRAIN-FIDELITY AUDIT synthesis (pending) redirect, rather than a 7th run. Surface to USER.
🟢 REDIRECT VALIDATED (FIRST POSITIVE) — INTERACTIVE-LOOP FIRST PROBE LANDED + DIRECTOR-VET'd (2026-08-02 ~00:33Z, exp_interactive_extraction_situation_model_loop_probe1_v1, 5 seeds, disk-verified) = MIDDLE_PARTIAL_SIGNAL. (Boot HANG fixed by a hard settling-iteration CAP = bug-fix AND brain-faithful [cortex settles in a few iters]; fresh exp_dev ab2f532c after prior agent aaa2e2e1 park-looped 5x.) MEASURED: FLOOR HELD (feed-forward OFF reproduces the wall: passive=0.152 inverted / active=0.860 / gap=0.707) = valid test. INTERACTIVE ON lifts passives SPECIFICALLY 0.152->0.551 (+0.40), active ~unchanged 0.820 = the directional inversion->resolution signature (brain-metric), not a uniform bump. PLACEBO ok (random-feedback passive 0.383 < interactive 0.551, content beats it +0.168>slack; honest nuance: placebo itself lifts 0.152->0.383 so ~half benefit=connection, ~half=content). PRECISION=LIFT (+0.16 on noisy-cue arena; weakest-link element earned its keep on a fair test). NOT HARD-PASS: on_passive 0.551<0.75, on_gap 0.269>0.15. => REAL MECHANISM, honestly PARTIAL. Per flat=fix-not-ceiling: the brain's interactive topology demonstrably (partially) resolves the exact passive-inversion the feed-forward architecture couldn't => VALIDATES THE REDIRECT; magnitude (0.55 not 0.75) needs strengthening (more settling iters / stronger top-down coupling / more training in this deliberately-cheap probe), NOT a ceiling. Contrast the encoder-proxy churn: THIS has floored-floor + clean-placebo + directional-lift + precision-earns-keep = a legitimately encouraging honest first result. NEXT: strengthen 0.55->higher (one variable), then toward real McGuffey extraction (VET + USER steer before scale). ATOMIZE (settled, load-bearing).
🔬 SETTLING-ITERS STRENGTHENING SWEEP LANDED + VET'd (2026-08-02 ~01:xxZ, sweep_metrics.json, 5 seeds) = NEGATIVE ON THIS LEVER: over iters {3,6,12} on_passive = 0.391/0.212/0.305 and content-margin (interactive - placebo) = 0.113/0.030/-0.001 => MORE settling DISSOLVES the content effect (at iters=12 interactive == placebo, mechanism washed out). The first-probe 0.55 (low cap) is ABOVE every swept point => the mechanism is already at its best with MINIMAL settling; iterating more only hurts. Settling-iters lever EXHAUSTED. ⚠️ FRAGILITY caveat: first-probe on_passive 0.55 vs sweep iters=3 0.39 = the mechanism is VARIABLE run-to-run (the 0.55 was on the high side of a distribution). 🎯 HONEST-LEVER CHECK (instrument-perfectionism guard, USER-flagged all night): the MIDDLE_PARTIAL first probe already VALIDATED the redirect (mechanism real, directional lift, controls clean). Its magnitude being partial + variable on a CHEAP SYNTHETIC probe is EXPECTED (same proxy-limit lesson as the encoder: cheap probes are underpowered). Continuing to sweep synthetic knobs (settling failed; coupling/threshold next) IS the instrument-perfectionism trap. => STOP synthetic knob-tuning. The mechanism's real test is on REAL McGuffey structure/scale, NOT more synthetic tuning. STRATEGIC FORK surfaced to USER: (a) keep tuning synthetic (diminishing returns) vs (b) invest in the real-text interactive-loop test (the actual comprehension frontier; needs the extraction pipeline + USER steer before scale). Director lean = (b) but it's a real investment => USER steer.
🟢🟢 FORK-(b) CHEAP FIRST STEP LANDED + DIRECTOR-VET'd (2026-08-02 ~02:xxZ, exp_interactive_loop_real_gold_mcguffey_v1, commit e0d19d2fa, disk-verified) = THE INTERACTIVE LOOP RESOLVES THE REAL DOMINANT McGUFFEY CONSTRUCTION (quotative inversion). MEASURED (real gold, LOOCV, hard subset): OFF(linear-order) quot-agent 4/19=0.211 (fails, as known) vs ON(top-down) 17/19=0.895 vs PLACEBO(scrambled construction) 7/19=0.368 vs NO_TD(position-only) 7/19=0.368. CLEAN DISSOCIATION: scrambling/removing the top-down construction CONTENT collapses ON (0.895) to the position baseline (0.368) => the lift is real construction CONTENT, NOT capacity, NOT a hand-coded rule (mapping LEARNED via ridge-logreg; the no-bolt-on line held: features supplied, mapping learned). 17/19 vs 7/19 = large clear effect, not small-N noise. => the brain-foundational interactive-loop mechanism TRANSFERS FROM SYNTHETIC TO REAL STRUCTURE, on the dominant real failure class (quotative = 37% of McGuffey). HONEST CAVEATS (scope/power, not validity): (1) N=19 quotative -- small but effect large + dissociation clean; (2) PASSIVE = DEGENERATE, NOT A RESULT (N=6, all-negative labels => every non-OFF arm scores 1.0 trivially; needs more passive gold WITH explicit 'by X' agents to break the base-rate); (3) implemented in NUMPY ridge-logreg (torch import was 11min under concurrent-session CPU thrash) => validates the CORE top-down-construction-mapping claim, NOT the full graded-PE/precision torch machinery; (4) floor 'not held' (OFF 0.211>0.15 bar) = calibration (learned position-baseline fails less hard than the raw parser 0.05), NOT a leak (controls confine construction to ON). SETTLING SWEEP (task1, 8a13fefec) reconfirmed: iteration is NOT the lever (a genuine iterative loop UNDERperformed the one-shot additive top-down 0.55; more settling washes out content) => one-shot additive top-down is the better mechanism. 🎯 NET: fork (b) direction VALIDATED at the cheap level -- the redirect resolves REAL quotative structure. NEXT (still cheap, no scale): expand PASSIVE gold (+by-phrase agents) + more quotative gold for a powered/harder-heldout test; confirm with the full torch loop (once the box isn't CPU-thrashed by the peer session PID 25552); THEN USER steer on scale. ATOMIZE (load-bearing: first real-structure positive).
🟢🟢🟢 BOTH REAL CONSTRUCTIONS NOW RESOLVED + DIRECTOR-VET'd (2026-08-02 ~03:xxZ, commit 9a901e51d, disk-verified): after expanding the passive gold with 5 BY-AGENT passives (d50be6d9a, surface-subj=patient + by-phrase=agent, breaking the all-negative degeneracy), the re-run shows the interactive top-down loop resolves BOTH McGuffey non-canonical constructions where linear order provably fails: QUOTATIVE (N=19) OFF 0.211 / ON 0.895 / placebo 0.368; BY-AGENT PASSIVE (N=5) OFF 0.000 / ON 1.000 / placebo 0.000 (placebo collapses BELOW the position baseline = the strongest possible dissociation: the lift depends entirely on the 'by'-construction content attached to the RIGHT mention). byagent_floor_held=True, byagent_pass=True, mapping LEARNED (ridge-logreg, not bolt-on), verdict HARD_PASS_INTERACTIVE_RESOLVES_QUOTATIVE_AND_BYAGENT. => the brain-foundational interactive redirect RESOLVES THE REAL McGUFFEY ROLE-EXTRACTION WALL (both dominant order!=role constructions) on real text, glass-box, cheap. HONEST CAVEATS (VET'd, scope/power not validity): passive N=5 = EXPLORATORY (one mislabel = 0.2 swing; needs more by-agent gold); quotative N=19 = moderate; numpy ridge-logreg = the CORE top-down-construction-mapping mechanism (the load-bearing claim; settling shown not-the-lever) NOT the full graded-PE/precision torch loop; tagger 0.938 on gold (PTB->McGuffey domain noise); this is the EXTRACTION wall resolved, NOT full comprehension (extraction + situation-model + inference still ahead). SESSION ARC (coherent + brain-foundational): churny feed-forward encoder (PAUSED, wrong frame) -> brain-fidelity audit (brain is INTERACTIVE) -> validated interactive loop (synthetic MIDDLE_PARTIAL) -> resolves BOTH real McGuffey constructions. NEXT (cheap, no-scale): more by-agent + quotative gold for power; FULL torch loop confirmation (once box un-thrashed by peer PID 25552, torch=11min); THEN USER steer on scale. ATOMIZE this (both-constructions-resolved) as the load-bearing real-structure result. [DONE: atoms 29604-29606, commit 391cb9a1b.]
🟡 CONTEXTUAL-STREAM WM / SOR PROBE LANDED + VET'd (2026-08-02 ~04:01Z, exp_contextual_stream_wm_sor_probe1_v1, disk-verified) = MIDDLE + a CLEAN BRAIN-GROUNDED DIAGNOSIS. Floor VERIFIED non-vacuous (numpy echo-state reservoir fails SOR at 0.04-0.09 across 12 hardness settings; OFF 0.094). Content-gated WM ON=0.281 > OFF 0.094, placebo 0.094 + pos-only 0.062 both FAIL => real content-dependent advantage. BUT does NOT solve SOR (0.281<<0.75) AND brain-metric FAILS: spike_ratio_WR=1.03 (write-gate fires ~CONSTANT ~0.47 on WRITE/RECALL/distractor alike: W 0.474 / R 0.459 / D 0.477 = NO content-novelty selectivity). route_consistency=1.00 (content-ROUTING works: right slot). alloc 0.17. => SPECIFIC DIAGNOSIS: routing works, selective OVERWRITE does not -- the boundary_k write-gate is NOT PE-driven, so it never suppresses old writes -> slot blends all writes -> muddy recall. FIX (brain-grounded + reuses a VALIDATED organ): make the write-gate PREDICTION-ERROR-driven -- fire HIGH when incoming content is NEW for that slot (WRITE) vs LOW when it matches (RECALL) = exactly Probe-1's validated PE-gate (Zacks event-segmentation: update-at-discontinuity). This connects the WM layer to the interactive-loop PE-gate. Per flat=fix-not-ceiling + brain=existence-proof: MIDDLE because the overwrite gate isn't brain-faithful yet, NOT a ceiling. DISPATCHED the PE-driven-overwrite-gate fix + SOR re-run. (Honest: this WM/situation-model layer is NOT yet validated -- unlike the extraction layer which is; the brain-metric correctly caught accuracy-up-but-dynamics-flat = MIDDLE.)
🟡->🟢(mechanism) PE-GATE FIX LANDED + VET'd (2026-08-02 ~04:49Z, commit pending, disk-verified): ROOT CAUSE found = SlotAttentionWM.step() DID compute a PE-boundary_k=sigmoid((surprise-theta)/tau) but multiplied it by a LEARNED gate_mod that COLLAPSED to constant ~0.47, washing out PE selectivity. FIX (PEGatedSlotWM subclass, one variable): write = addr_w x boundary_k, drop the gate_mod washout (shared organ else byte-identical) = Probe-1 PE-gate + Zacks update-at-discontinuity. RESULT: spike_ratio_WR 1.03->2.61 (clears >=2.0), brain_metric_ok FLIPPED True, route 1.0, floor held (OFF 0.094), placebo 0.060/pos 0.130 fail. => the overwrite gate is NOW BRAIN-FAITHFUL (selective overwrite works). BUT recall only 0.281->0.336 (still MIDDLE, <<0.75). => overwrite-fix NECESSARY-NOT-SUFFICIENT; the accuracy bottleneck moved DOWNSTREAM to: alloc=0.167 (weak -- novel slot_id not getting clean empty slot => entity collisions) and/or lossy HRR-unbind recall at 6 slots. brain-fidelity element trail: routing OK -> overwrite NOW OK -> ALLOCATE next (+ recall fidelity). CONSOLIDATION CALL (2026-08-02 ~05Z, ~all-night full-auto, USER away, box degraded [peer PID25552 CPU-thrash => torch 11min-import, git timeouts]): the PE-fix is a real mechanism WIN + clean checkpoint; the next WM step (allocate/recall) is a legit continued brain-fidelity-audit but is better done FRESH + when the box isn't thrashed, NOT another 6am dispatch into an 11-min-import environment. Extraction layer = the banked milestone (validated real text); WM layer = overwrite now brain-faithful, recall bottleneck = allocate (identified, next). Holding the drive here for a clean session-arc landing + USER steer. DIRECTOR LESSON: I over-read a broken result as a real negative + escalated a fuller-budget go/no-go on it -- the USER's brain-existence-proof push caught it; VET NEGATIVES as hard as positives (a trained-arm-worse-than-random should have self-flagged).

**PROCESS NOTE (honest):** the depth-test agents have been bug/park-prone (crashes, parking-on-self-test, 2 over-reads of nulls); the load-bearing results (29594/29596/29597 + integration) are all VET-confirmed + banked, the churn is in the exploratory depth tests. Discipline held: every over-read caught + corrected; no unfair null banked.

---
[HISTORY BELOW — earlier same-session blocks, superseded by the banner above]
# (older) WHERE WE ARE NOW — rewritten 2026-07-31 ~18:05Z

## Direction (read in order)
1. GOAL + invariants: `notes/SUBSTRATE_CHARTER_read_first.md`
2. Plan: `notes/THE_PLAN.md`
3. This = live snapshot (tight; rewritten each session — history lives in git + the backup doc + atoms).
4. Self-contained recovery: `notes/director_POST_COMPACTION_BACKUP_2026-07-30_NIGHT.md`

## 🎯 THE GOAL (unchanged)
Glass-box VSA/HDC substrate that genuinely COMPREHENDS, earning meaning the brain's way (from scratch; NO borrowed embeddings/LLM at inference; NO bolt-on reader — comprehend via our OWN learned mechanism; supplying DATA/knowledge is OK). North star = a glass-box substrate you can CONVERSE with (comprehension = UPDATE a world model while reading).

## 🔵 CURRENT STATE UPDATE (2026-07-31 ~21:45Z) — MULTI-COMPETENCY ENGINE built + first result + MEASUREMENT PIVOT (authoritative; supersedes the headline below)
Built the USER-steered GROWING-LIBRARY engine: 2-competency continuous loop (entity #1 + roles #2, fully-modular), cell `experiments/exp_multi_competency_growing_library_v1.py`.
- **POWERED RESULT (4 units, seed 7):** H2 NO-INTERFERENCE = **EXACT PASS** (entity metrics BYTE-IDENTICAL with/without roles => fully-modular architecture PROVEN, the core thesis). Official verdict = **INVALID** — fairness gate check-3 (dissociation) marginally FAILED at full scale: entity-track training leaks +0.055 into name-maintenance vs the ≤0.05 bar (roles not quite cleanly separable in this synthetic stream). Per flat=fix that makes the 2comp-vs-1comp comparison a **BROKEN TEST, not a capability verdict**. H1 additive climb-further margin was only +0.015 (< MDE 0.079) on the WRONG (blended) instrument; roles competency itself DID learn (climb_role 0.045, role headroom healthy 0.255). Fixes: crash `f8be2233f`, build()-perf `58c31b5a9` (bit-identical). Do NOT re-run to chase the 0.08 bar; do NOT override the 0.05 dissoc bar.
- **RESEARCH (5 brain-first, glass-box; banked `6a400863b` + notes A-E):** (A) no-interference recipe = fresh near-orthogonal HD subspace per competency + gated update + replay (SupSup/PackNet, VSA gives orthogonality free). (B) discovery trigger = CRP==MDL match-known-or-allocate-new (= event-segmentation/SEM + Adaptor/Fragment grammars). (C) competency #3 = cross-sentence COREFERENCE (parasitic on #1+#2). (D) comprehension value is COMPOSITIONAL/multiplicative for PARASITIC competencies; the blended additive metric DILUTES value => measure on AND-gated/composition-requiring subsets with ABLATION. (E) recalibrate bar: guessed 0.08 was ~human single-skill CEILING => tiered ≥0.02 floor / 0.03-0.06 strong / 0.08+ cumulative-stretch; additive-with-mild-synergy across INDEPENDENT competencies.
- **RECONCILE D+E:** additive across INDEPENDENT competencies (entity/roles => why this run diluted + order-null), multiplicative/BOTTLENECK across PARASITIC ones (coref) = brain-grounded construction parasitism.
- **NEXT BUILD (decided):** competency #3 = COREFERENCE with Tier0/1/2 items (entity-only / role-competitive / role-reversal) + ABLATION-verified bottleneck metric (with-roles vs roles-ablated on AND-gated items) + tiered bar (E) + per-competency cumulative logging. The AND-gated+ablation metric is a cleaner causal test that SUPERSEDES the additive-blend + dissociation gate. Then Phase-2 CRP/MDL discovery gate; Phase-3 McGuffey (audit DONE b40f9e059: construction-graded g1->g4 passive-jump, coref FRONT-loaded => source coref synthetically not McGuffey).
- **🏆 COMPOSITION DEMONSTRATED — VET-CONFIRMED (skunkworks aea3f0ff, independent recompute + neutral baseline; the additive-blend HID it, AND-gated ablation SURFACED it):** eval-only role-key ablation (frozen encoder, matched dist, n~45-47 Tier-1/seed, 3 seeds) = the certified thematic-role mechanism (#2) CAUSALLY GATES competitive-coref Tier-1 accuracy, mean delta **+0.19** (0.11-0.26, 3/3 seeds), verified vs fairness floor (chance=0.05, shortcut<=0.65) AND independent neutral role-shuffled baseline (0.11-0.23) = NO confound. Tier-0 no-regression directional (underpowered n=7). = FIRST VET-confirmed evidence the growing-library competencies COMPOSE. SCOPE: compositional WIRING, NOT solved coref — absolute Tier-1 ~0.51 is ENCODER-DECODE-bound (stage_ENT~0.73). Coref training-track (STEP 2) DEFERRED: wiring shown, absolute perf is ENCODER-limited => **the encoder decode is the next real lever** (ties to certified break atom 29593). Banking: cell-fold + atomize in flight; corrected my own "below-chance" misread (chance=0.05 not 0.5). Director-recommended NEXT strategic fork (USER steer): (A) attack the ENCODER-DECODE ceiling (lifts absolute everything) vs (B) Phase-2 CRP/MDL discovery gate vs (C) competency #4 passive (McGuffey g1-g4). Lean = (A) encoder is the bottleneck on absolute comprehension.

## 🔬 ENCODER-LEVER CAMPAIGN UPDATE (2026-08-01 ~01:20Z) — SCOPED, real, chain-grade probe in flight
- **Encoder-transfer (fork A) CONFIRMED for coref/entity** (commit 71e3e2ed6; decisive frozen-vs-frozen drift control = 0.000): certified minimal-unfreeze break PERSISTED (ckpts data/exp_encoder_retrain_persist_v1/, wiring gap fixed) and swapped into the coref harness lifts stage_ENT 0.72->0.86, absolute Tier-1 0.51->0.65, role-delta held.
- **Broad all-3-type test => VET verdict SCOPED (atom 29595, commit 9d2d658fd):** the retrained encoder lifts ALL 3 query types (a_name +0.19/recompute +0.277, b_coref +0.15, c_overwrite +0.32) REAL + reproduces across TWO independent harnesses (identical score_extractor path) + all seeds positive. BUT all 3 types are ENTITY-ADDRESSED => honest claim = "shared entity-representation substrate lifts all ENTITY-ADDRESSED comprehension," NOT domain-general (untested outside entity-routing).
- **Two corrections banked:** (1) the "a_name stays flat under retrain" A-TYPE DIAGNOSIS was a STALE SINGLE-SEED lite result (top-3 unfreeze), already superseded by 29593's 3-seed grid -- NOT an artifact; a_name lifts. (2) c_overwrite is NAME-ADDRESSED (name-decode + last-write-wins), not pure WM-gating -- so it benefits from better entity decode (Director's "overwrite orthogonal" prior was WRONG). ⚠️ STALE-CITATION RIPPLE: exp_multi_competency_growing_library_v1's rationale for a dedicated ROLE competency rests on the same stale a_name-flat citation -> RE-EXAMINE that framing (does NOT invalidate the VET-confirmed composition result 29594, but the motivating narrative needs a fix).
- **CHAIN-GRADE CAMPAIGN (USER: explore aggressively, chain-grade if we can).** Bar: confound-ruled-out (DONE - real) + generalizes + mechanism=representation-quality + independent VET. (i) generalization prong a0fe4839 => HARD_PASS/GENERALIZES (exp_encoder_alltype_transfer_stress_v1): broad lift holds across HARDER difficulty (a+0.108/b+0.142/c+0.231), HELD-OUT content (a+0.200/b+0.150/c+0.203), and an INDEPENDENT entity-file harness (a+0.050/b+0.175/c+) -- drift-controlled (frozen2=frozen exactly). => NOT a base_loop-specific artifact; the representation improvement is REAL + generalizes. CAVEAT: all 3 conditions still ENTITY-ADDRESSED. (ii) DECISIVE non-entity-addressed prong a09958fb still building the hard entity-independent probe -- THE crux of domain-general vs entity-scoped. Chain-grade tally for the SCOPED claim: confound-ruled-out YES + generalizes YES; non-entity prong decides scoped-vs-general; then chain-grade VET + atomize.

## 🟢 CURRENT STATE (headline)
We proved the comprehension organs COOPERATE, certified a cheap encoder fix for the hard (entity) half, corrected a load-bearing error (the encoder already reads real English), and — per USER — PIVOTED to the right frame: **BUILD THE CONTINUOUS READ-A-CURRICULUM-AND-LEARN-AS-YOU-GO LOOP** (stop artificial batch probes). The real graded curriculum (McGuffey g1-6) is fetched + prepped + measurement-verified READY. The loop is running (hardened after a silent death). NEXT = prove comprehension CLIMBS as it reads -> feed McGuffey g1->g6 = the substrate reading a real curriculum, grade by grade.

## ✅ PROVEN / BANKED (this session; honest scope)
- **Situation-model loop** (native FHRR binding + content-gated WM overwrite + substrate-native competitive coref) — VET-CONFIRMED + ATOMIZED (atom 29592). On CLEAN inputs = an INTEGRATION proof (the organs cooperate), NOT naturalistic reading.
- **Certified encoder break (entity re-id)** — ATOMIZED chain-grade (atom 29593), triple-VET'd. Minimal-unfreeze (top-1 layer) fine-tune of our OWN v2 encoder w/ the validated objective (cross-mention CONSISTENCY + inter-entity PUSH + VICReg provable anti-collapse) lifts held-out situation-model loop **0.52 -> 0.83** via cross-frame ENTITY re-id. Win = CHEAP minimal-unfreeze (deeper unfreeze OVERFITS). GENERALIZES beyond the template (harder-synthetic + in-vocab-naturalistic both hold; not template-specific).
- **Role half** (who-did-what) = VALID MIDDLE (recipe transfers but the frozen encoder already reads role fairly well = SHALLOW wall, not a deep break). => founding-gap mechanism picture COMPLETE: both halves minimal-unfreeze-addressable (entity strongly, role weakly).
- **Voice reanalysis organ** = scoped positive (cue-triggered role-against-position on a synthetic cue; de-prioritized).

## 🚨 THREE LOAD-BEARING CORRECTIONS (all USER-driven, all improvements)
1. **The encoder ALREADY reads real English (disk-verified).** ckpt model_cfg = vocab=16000, tok_emb (16000,512), ~80M real AI2-ARC tokens, BPE. The "closed ~50-word vocab / OOV-blocked / needs re-pretrain" claim (naturalistic-firstcut 22708831a) was FALSE — it conflated the harness's 20-color TASK vocab with the encoder's real vocab. Director wrongly propagated it to USER twice (disk-verify-before-propagate violated, corrected). REAL bottleneck = the comprehension mechanism's GRIP attenuates on broad real vocab (entity break +0.275 on 20 colors -> +0.074 on 120 real nouns; base reading fine, oracle 0.747). MORE VOCABULARY (same difficulty) does NOT help = immediate saturation (grip extracted from ~12 nouns).
2. **FLAT learning result = BROKEN EXPERIMENT, not a ceiling** [[feedback_flat_learning_result_means_broken_experiment_not_capability_ceiling_2026-07-31]]. A functioning learner on genuinely-new content WILL improve => flat means DIAGNOSE (a) not-actually-learning / (b) no-genuinely-new-content / (c) underpowered. NEVER conclude "intrinsic ceiling" from flat.
3. **STOP artificial batch probes -> BUILD the continuous read-and-learn loop** [[feedback_stop_artificial_batch_probes_build_continuous_read_and_learn_curriculum_loop_2026-07-31]]. Diagnostic phase over. Don't separate encoder/reader or train/test; read the curriculum, learn from each sentence, test on NEW upcoming material, comprehension climbs. => the lever is DIFFICULTY (a real curriculum climbs difficulty), not vocabulary count.

## 🔄 RUNNING NOW
- **The CONTINUOUS read-and-learn loop** (exp_dev a13cf1eb, cell exp_continuous_curriculum_learn_as_you_go_v1.py): read a GRADED stream simple->complex, encoder+reader CO-learn from EACH sentence ONLINE, test-as-you-go on HELD-AHEAD material, comprehension CLIMBS (must beat a FROZEN reads-but-doesn't-learn control, without FORGETTING earlier material via a CLS/replay analog). SILENT-DEATH-CAUGHT + HARDENED (17:31Z): the first run died after the frozen-baseline unit (online-learning arms never ran, 33min stale); relaunched RESUMABLE-PER-STEP with per-step _heartbeat.jsonl + _online_ckpt + partial-after-each-step (now never-silent). Frozen baseline valid+reusable (target-loop 0.388, oracle 0.660, floors collapse). **FIRST-CUT RESULT LANDED (commit 90bd5b43a): THE LOOP CLIMBS -- comprehension 0.388 -> 0.484 (+0.096) MONOTONE across 3 grades, BEATS the frozen no-learning control (0.388), learning verified, stream genuinely graded, NO forgetting (rehearsal replay held).** = first direct evidence the read-and-learn-as-you-go mechanism works. Verdict MIDDLE (blocked by the structural entcons guard + eval-power, NOT a failure to climb -- per flat=fix it's NOT flat). CAVEATS: modest (+0.096), 3 grades, SYNTHETIC graded stream (NOT McGuffey yet), fresh/needs independent VET + a clean-pass (fix the guard). More running (self-resuming driver extends the climb). NEXT: VET the climb hardest -> clean pass -> feed the REAL McGuffey g1->g6 ladder.
- **McGuffey graded curriculum = PREPPED + READY** (branch dataprep/mcguffey-graded-corpus, d97e0a57c; data/corpora/mcguffey_graded/clean/g1..g6.txt ~260K words + manifest.json). MEASURED: genuinely graded (FK grade 1->10 monotone g1->g6; long-word% 2->19; syll/word 1.1->1.44), genuinely-new-content-per-grade (a grade-1-vocab reader's coverage falls 100->61% = each grade real new signal), encoder-compatible (0% true UNK, MILD fragmentation 9->18% — NO archaic wall; answers the USER 1800s-register worry). CAVEAT: tokenizer-covers != encoder-comprehends-archaic-style (only the loop proves it); dated content.

## ➡️ NEXT
1. **Prove the loop CLIMBS** (comprehension on held-ahead rises as it reads, beats frozen 0.388, no forgetting). Per flat=fix: if flat, DIAGNOSE (a)/(b)/(c) the loop (incl "is the archaic register confusing it" -> a reason to try modern), never a ceiling.
2. **Feed it McGuffey g1->g6** = the substrate reading a real graded curriculum, grade by grade (McGuffey ready + measured-usable; the loop is corpus-agnostic).
3. **USER-strategic forks/upgrades:** a MODERN graded corpus (Simple-Wiki / readability-binned / OneStopEnglish) as a register/content upgrade once the loop climbs on McGuffey; full-scale curriculum climb; wire the certified encoder win.

## 🎓 STANDING DISCIPLINES (session-specific, load-bearing)
- **DISK-VERIFY agent claims before propagating** (I violated this on the vocab-size claim — never again). **MEASURED, not my-read** (report the measurement; my strategic reads over-reach). **VET POSITIVES HARDEST** via the trustworthy gate (probes/agents misled repeatedly; I refused 3+ bar-overrides + caught 2 framing errors). **FLAT = fix the experiment, not a ceiling.** **Build the SYSTEM (continuous loop), not artificial batch probes.** **Do the HARD blocking thing.** Heartbeat every turn-end; silent-death sweep every ~30min (it caught the loop death). Store LOCAL-ONLY binary + git-commit-after-bank, NO push w/o in-session auth. Only kill what THIS session spawned.

## 🗂️ KEY ARTIFACTS
- Certified break cell: `experiments/exp_situation_model_assembly_encoder_retrain_scale_v1.py`; atoms 29592/29593 (data/substrate_index/).
- Continuous loop: `experiments/exp_continuous_curriculum_learn_as_you_go_v1.py` (a13cf1eb owns it).
- Curriculum: `data/corpora/mcguffey_graded/` (raw g1-6 + clean/ + manifest.json + grading_report.json).
- Encoder ckpt: `data/exp_scale_meaning_learn_arc_heldout_v2/ckpt_seed_7.pt` (vocab=16000, ~80M ARC tokens).

## 🧭 ARCHITECTURE STEER (USER 2026-07-31, FINAL — the multi-competency ENGINE + its discovery mechanism)
The continuous-loop ENGINE must be a GROWING LIBRARY of construction-competencies (not one objective; one objective SATURATES). USER decisions:
- **AUTO-DISCOVERY (fork #1 decided): the system DISCOVERS new construction types ITSELF** — NOT a fixed predefined list. BUT **start with a PRE-PROGRAMMED seed set that is MUTABLE** (seed predefined constructions + grow/discover on top; the seed is editable, not frozen).
- **Discovery MECHANISM (USER):** use POS knowledge (verbs/nouns/etc) to MATCH each incoming sentence/fragment against KNOWN construction templates/fragments; if it does NOT match a known type => EVALUATE for a NEW type => ALLOCATE a new competency (capacity). (Novelty/mismatch = the learning trigger; brain-faithful surprise->learn.)
- **This IS "our grammar learner" — DISK-VERIFIED we already have the machinery to REUSE/EXTEND:** `hdlab/pos_tagger.py`, `hdlab/arc_parser.py`, `hdlab/situation_reader.py`, `hdlab/coref.py`, `hdlab/scene_segment.py`, `tools/build_construction_gold.py`, **`hdlab/learner/plugins/proginduction_plugin.py` (PROGRAM INDUCTION)**, construction-grammar (CG) atoms + dependency-parse/valency/subcat work (see tools/_skunkworks_atomize_*depparse*, *subcat*, *construction*). fork #2 (separate-params vs MoE) still OPEN — Director lean fully-modular/glass-box; not yet decided.
- **=> NEXT BUILD (the frontier):** evolve the continuous read-and-learn loop's engine into a MUTABLE-seed, AUTO-DISCOVERING multi-competency library: (POS/parser) match-known-construction-or-flag-new -> allocate+learn a new competency (dedicated capacity, no interference) -> the curriculum (McGuffey g1->g6) drives encountering new construction types grade by grade -> comprehension climbs as the library GROWS. Reuse the existing pos_tagger/arc_parser/situation_reader/coref/proginduction machinery for the match+discover step. Entity-identity = certified competency #1. This unifies: the grammar-learner (discovers/parses constructions) + the continuous loop (learns as it reads) + the growing library (accumulated comprehension memory).
