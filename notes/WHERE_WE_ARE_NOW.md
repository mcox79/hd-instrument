# WHERE WE ARE NOW — clean current state (REWRITE each session)

> **🧭🆕🆕 CURRENT FRONTIER (2026-08-08, SUPERSEDES ALL banners below). THE WALL (measured, load-bearing): narrative/relational comprehension bottoms out on GENERALIZABLE WORD-GROUNDING — on modern narrative the achievement-comparison plateaus ~0.60 with surface cues and the hand-lexicon relation channels fire ~0/30 (closed-lexicon starvation). Dialogue construction-typing tier = SOLVED+WIRED (selection_weighted_sharded_typer, cert 225/3). Deep-frontier FORMALIZE done + VET'd (aabff956) + a self-caught over-read corrected (fdefef94a). USER (asleep, full-auto): "do a few drills on different ways to overcome the wall" + "share a detailed plan based on results" + "I think we're converging on how the brain actually does it — since the brain can do it, we can too." TONIGHT'S THREE-DRILL BATTERY = COMPLETE + VET'd (plan+results+decision-tree = notes/tonight_plan_three_ways_over_the_grounding_wall_2026-08-08.md). RESULT = the wall DECOMPOSES: (a) WORD-LEVEL grounding = TRACTABLE [D2 extraction-wall: REAL 0.5->GOLD 1.0, lemmatizer mis-stemming revive->reviv, mechanism intact; D1 coverage un-starves class_relation 1->15/verb_sim 0->14, fresh OOV acc-when-fires 1.0], (b) the goal<->outcome RELATION = THE TRUE RESIDUAL [D1 relational acc flat 0.50->0.53; D3 learner generalizes (gam, compression 1.30) but 0.625~=0.60 plateau AND pairscramble does NOT collapse -> reads outcome-only surface cues, not a relation]. USER intuition supported: brain does vATL word-grounding AND ACC goal<->outcome comparison and COMPOSES them; each single-half escape failed. NEXT BUILD (decided, test-first): (1) FIX lemmatizer mis-stemming; (2) WIRE extended grounding INTO a STRUCTURAL goal-state<->outcome-state comparison (GoalOutcomeRegister + _class_relation over GROUNDED features, not a flat outcome classifier). New sharp can-fail from D3: PAIRSCRAMBLE MUST COLLAPSE. Underpowered caveat: n=16-30 across all three -> author more modern goal-achievement data before any ceiling. D1 cell = exp_definition_composition_grounding_featurebundle_crosspos_v1.py. **➡️ FOLLOW-ON (Thread B structural + Thread C agent-indexed, both report-only VET'd = HARD_FAIL): the relation is NOT captured by any of FOUR mechanisms (D1 grounding / D3 learned / structural / agent-indexed) AND my agent-indexed hypothesis was REFUTED (rescued 0). D1 grounding CONFIRMED (0->10/23 replicated). 🛑 HONEST CHECKPOINT (inconclusive, NOT a ceiling): n=23 underpowered + outcome-state extraction weak (2/23) + EVAL CONSTRUCT-VALIDITY SUSPECT (MET/UNMET set by varying the OUTCOME not the goal -> relation under-exercised). RECOMMENDED NEXT (strategic fork, checkpointed to USER): build a FAIR POWERED RELATION-DIAGNOSTIC eval (same-outcome / different-goal -> different verdict) so outcome-only MUST fall to chance + a correct relation model MUST pairscramble-collapse; prerequisite fix outcome-state extraction. Full detail = tonight_plan doc + backup 2026-08-08 launch block.** Invariants: modern data only; glass-box + earn-not-supply + no-borrow; read-the-code; wire-don't-island. The 2026-08-07/08-04 banners below are HISTORY.**

> **🧭🆕 CURRENT FRONTIER (2026-08-07 — SUPERSEDES the 2026-08-04 banner below; authoritative self-contained state = notes/director_POST_COMPACTION_BACKUP_2026-08-04.md TOP "CURRENT STATE" block). SITUATION-MODEL (#5 / DMN) = the confirmed near-term keystone, and it was BUILT + UNIFIED this session. Phase 1 measured the coverage wall = ~29/31 INTEGRATION-limited (not grounding) -> #5 is the lever. SCOREBOARD (all Director-VET'd witness+cert, cert 220/3, GENERALIZATION-verified on 16 hand-gold held-out items = real capability not overfit): POLARITY typing 11->15 (+4, referent-recurrence did-it-happen keyed on goal TARGET, 7e5ed63fe); OWNER never-typed 18->14 (-4, 2a PATH-UNIFICATION dc111492b + speaker-attribution 78e0c7908). 🔑 2a KEYSTONE = unified the two separate typing engines (owner type_goal_events + polarity congruence) so a detector propagates to BOTH numbers. HELD-OUT GENERALIZATION (fair, taxonomy-remapped): 71% polarity precision / 75% owner precision, ~30-50% coverage = HIGH-PRECISION + COVERAGE-LIMITED + generalizes; coverage is the universal wall. 2 honest HARD-FAILs reverted (2b window-widen; cross-char literal-lexicon Δ+0 -- targets use FREE-INDIRECT-DISCOURSE not literal verbs). Plot-Units VET'd (1 real adopt = cross-char links = ToM). TRACTABLE low-risk eval-tail EXHAUSTED (every remaining lever pre-VET'd: bespoke +1 OR needs grounding). NEXT = a genuine RESEARCH FORK awaiting USER steer: (A) speech-act/particle-marker retry (~+1-2, over-fire area) then FID needs grounding; (B) GROUNDING frontier (the field's deep core + USER's 6yo-foundation pivot, but 5x HARD-FAILed via text-induction + blocked on the earn-vs-supply fork); (C) broaden to a new competency/eval. Do NOT grind bespoke +1 detectors (easy-path). Committed through f7ff8d8be; MEMORY + backup current. The 2026-08-04 banner + CURRENT STATE sections below are HISTORY.**

> **🧭 CURRENT FRONTIER (2026-08-04 LATE, SUPERSEDES the old "coherence-selector" framing below): GOAL-OWNER ATTRIBUTION PIPELINE. The coherence-selection thread was BOUNDED (decode_coherence_margins is role-content-BLIND, not the bottleneck) and the "coref=recency-falsified" premise was CORRECTED (coref is FAITHFUL, Centering-Cb, never broken). A per-component brain-fidelity audit found the REAL load-bearing gap = THEMATIC-ROLE LABELING (Component-3): 3/6 pipeline components faithful (coref, situation-binding, select+abstain); the gap is assigning AGENT/EXPERIENCER/GOAL to real clauses. Sub-problems: 3a in-vocab verb-frames=DONE (0.857); 3b OOV frame-induction=MECHANISM PROVEN via config-only reuse of hdlab/learner (Gleitman syntactic bootstrapping; beats default-AGENT; on sparse real data collapses to a position+animacy proxy=data-starved, MIDDLE); 3c=FRAME-PRIMARY verb-class-conditioned assignment IN FLIGHT (frame wins over position — the fix for the shelved perceptron). BRAIN-FOUNDATIONAL: roles are FRAME-based not positional; goal-owner rides the SUBJECT-EXPERIENCER frame (want/crave/dread) which we HAVE (~0.83-0.86); object-experiencer=deferred affect competency. NEXT after 3c: wire Component-3 -> Component-5 goal-owner SELECTION (design drill a6a131ef in flight). Ops: 30-min self-drive cron d4fd7d9a; AskUserQuestion widget BANNED (prose forks). SELF-CONTAINED RECOVERY ANCHOR = notes/director_POST_COMPACTION_BACKUP_2026-08-04.md (authoritative, current). The OLD banner/sections below predate this pivot — treat the backup as authoritative.**

## CURRENT STATE (2026-08-04 ~11:4XZ) — earlier-today snapshot; see the CURRENT FRONTIER pointer above + bottom "---" sections for the latest.

**🟢🧠 GROUNDED VALENCE READ FROM TEXT = ROUTES_TO_GROUNDED_HARM_KNOWLEDGE — DIRECT VALIDATION of the foundational thesis: supply grounded knowledge, the brain-faithful mechanism reads it (2026-08-04 ~11:4XZ, DISK-VERIFIED 5 seeds, commit ae6be1955; cell exp_grounded_valence_read_from_text_v1.py).** Replaced the frozen blind lexicon with the substrate's OWN grounded appraisal read: supplied general ~6yo physical-harm primitives (verb+patient-affordance+hypothetical-guard; supplying KNOWLEDGE = allowed) accumulated via the VET-confirmed FHRR situation-model organ (AccumulateRegister, atom 29609). MEASURED: true-span HARM recall EARNED 0.571 (4/7) vs FROZEN_LEXICON 0.143 (1/7) = 4x extraction gain — recovers HARM on physical-harm spans the lexicon read NEUTRAL (001 knife-in-breast, 005 bowl-broke, 008 poured-ink, 009 snatched-tear). CONTROLS (brain-fidelity, clean): EARNED_NO_KNOWLEDGE=0.0 (empty the primitive store -> reads everything NEUTRAL = the substrate has ZERO innate harm knowledge of its own -> supplied knowledge IS the lever); floors fail (RANDOM 0.286, SHUFFLED 0.314); 003/007 recency-trap both ABSTAINED (structurally immune — valence read consumes ONLY the candidate span, never the outcome). COMPONENT AUDIT (USER directive): the appraisal MECHANISM is brain-faithful + NOT the bottleneck (FHRR accumulate reproduces evidence count, floors fail, NO_KNOWLEDGE collapses); the gap is MISSING GROUNDED WORLD KNOWLEDGE + AGENT-ATTRIBUTION, not a proxy/bug. valence-ONLY selection did NOT clear chance (0.286) — expected, it's confounded (needs the selector's effect-match/agent-attribution dim, which scored 0.714). PER-SPAN ROUTING of the remaining gaps (each names the grounded knowledge needed): 001/005 SOLVED (canonical physical harm); 008/009 need AGENT-ATTRIBUTION (both candidates read HARM); 003 harm is OUT-OF-SPAN (forging) -> needs cross-span BINDING (which we built, recall 0->3/4); 004 IRONY (spiteful withholding reads NEUTRAL) -> needs irony grounding; 007 COUNTERFACTUAL causation (cordial-in-cellar, no surface harm verb) -> needs counterfactual grounding. 🎯 THE THESIS IS VALIDATED (n=7 directional, canonical-harm): the recurring wall was never the MECHANISMS (brain-faithful, work given structure/knowledge) — it is SUPPLYING THE GROUNDED ~6yo FOUNDATION. With grounded harm knowledge -> 4/7; without -> 0/7. This is exactly the USER pivot ('you can't learn revenge from a book; build the ~6yo grounded foundation'). PHASE TRANSITION: probing has localized the lever precisely; the next phase is BUILDING the grounded foundation at BREADTH (harm ✓ -> irony, counterfactual causation, goal/social) + combining reads via the selector's agent-attribution, on a bigger eval than n=7. NEXT: (A bounded) integrate earned-valence + effect-match + agent-attribution + cross-span binding on the 7 items = the current real-text ceiling with pieces-we-have; OR (B phase) start supplying the grounded foundation at breadth = the USER north star, now evidence-warranted.

**🟡🧠 RE-GROUNDED COHERENCE-SELECTOR = GROUNDED_PARTIAL_LEXICAL_PROXY — honest partial; the gap is now precisely localized to VALENCE/APPRAISAL EXTRACTION from literary text, NOT the selector (2026-08-04 ~11:2XZ, DISK-VERIFIED 5 seeds, commit 3b7fdd984; cell exp_grounded_coherence_selector_v1.py).** Re-earned causal-coherence selection over a GROUNDED representation (fix for the CANNOT_BRIDGE negative). IN-SIM: mechanism SOUND + grounded-faithful — FULL 1.000, floors fail (RANDOM 0.291, SHUFFLED 0.302, recency 0.337), and the conjunction is required (NO_EFF 0.500 / NO_VAL 0.506 = neither valence-dim nor effect-dim alone suffices). TEXT TRANSFER (n=7, DIRECTIONAL): text_full 0.714 headline (beats char-trigram 0.429, gold-recency 0.0, chance 0.5) — BUT VET DEFLATES: the pre-registered diagnostic fired — valence_only 0.514 (≈chance; grounded VALENCE does NOT transfer) while eff_only 0.657 carries the lift, and effect-match = content-LEMMA overlap = a LEXICAL-SIMILARITY PROXY (word-level cousin of the failed char-trigram). Real grounded wins = 2/7 clean (005 "bowl dropped and broke"; 009 HARM+"tear/tore"); 3/7 recency-tiebreak/seed-noise (per-seed 0.429-1.0); 2/7 honest fails (003/007, distractor shares more outcome vocab = the recency-trap). COMPONENT BRAIN-FIDELITY AUDIT (USER directive): (1) resolve_valence_blind CANNOT read appraisal off literary prose — labels true-cause spans NEUTRAL, mislabels spiteful withholding as HELP = the bottleneck; (2) effect-match is a similarity-proxy standing where grounded reasoning should be (inverts on 003/007); (3) the SELECTOR itself is grounded-faithful — the gap is UPSTREAM ENCODING of text into grounded features, specifically VALENCE. => groundedness did NOT fix the bridge in the intended way; it swapped char-overlap for word-overlap (marginal, fragile). CONVERGENCE (the recurring wall, now razor-localized): the mechanisms (appraisal, coherence-selection) are grounded-faithful + work GIVEN structure; the load-bearing gap is EXTRACTING grounded structure — specifically VALENCE/APPRAISAL — from raw literary text (matches the old EXTRACTION_BOTTLENECK: appraisal transferred with ORACLE structure arm_a=1.0 vs arm_b=0.45). ROUTED NEXT (brain-foundational): earn a grounded VALENCE read for these spans via the substrate's OWN learned mechanism (situation-model appraisal accumulation over the candidate's action — NOT the frozen blind lexicon, NOT a bolt-on parser); re-test whether valence_only then clears chance. Keep effect-match as a combinable relational signal; guard with the recency-trap items 003/007 against lexical-overlap gaming. Contamination clean (leak-safe goal-description; item-007 goal_owner leak avoided; no char-trigram/borrowed model).

## CURRENT STATE (2026-08-04 ~04:2XZ) — SUPERSEDED BY BANNER ABOVE.

**🔴🧠 REAL-TEXT TRANSFER OF THE COHERENCE-SELECTOR = CANNOT_BRIDGE_REPRESENTATION_GAP — and the BRAIN-FOUNDATIONAL COMPONENT AUDIT (USER directive: VET negatives incl. component brain-fidelity) re-contextualizes the whole in-sim coherence arc (2026-08-04 ~04:2XZ, DISK-VERIFIED 5 seeds, commit dde1cc76d; cell exp_coherence_selector_text_transfer_v1.py).** First end-to-end-on-real-text test of the sim-earned causal machinery, on 7 Director-verified backward-causal items (oracle structure, isolate selector-transfer from extraction). MEASURED: selector_text_acc 0.429 (~chance), beats NO floor (random 0.457 / positional-recency 0.429 / gold-recency 0.0); sim_sanity 0.837 (M_backward bit-faithful, mechanism INTACT); raw-match control (no learned map) ALSO fails 0.286; contamination clean (caught+fixed an item-007 goal_owner leak). BRAIN-FOUNDATIONAL AUDIT (the load-bearing part) — the negative traces to TWO NON-brain-foundational components: (1) the text ENCODER used was CharTrigramEncoder = SURFACE ORTHOGRAPHY (letter-triples), NOT grounded semantics — the brain never encodes event meaning as letter-strings; the raw-match-also-fails confirms this encoding can't carry causal coherence. (2) DEEPER: the sim-earned M_backward learned to invert the sim's FIXED ARBITRARY PERMUTATION T (synthetic coordinate geometry) — the brain does NOT learn causal coherence as 'invert an arbitrary permutation'; real coherence is a GROUNDED SEMANTIC relation determined by the cause's properties. So the sim's causal grammar T is itself NON-brain-foundational, and v3's 'abstract rule generalizes to novel types' was abstract WITHIN the sim's synthetic grammar, NOT over real causation => on text (no T) it's useless regardless of encoding. HONEST RE-CONTEXTUALIZATION: the in-sim v2 HARD_PASS / v3 MIDDLE_BAND coherence wins were REAL but TIED TO SYNTHETIC GEOMETRY = do NOT transfer. The TELL: the GROUNDED APPRAISAL function earlier DID transfer given structure (grounded appraisal-dimension representation) while the coherence SELECTOR (arbitrary type-vectors) did NOT — GROUNDEDNESS is the difference. Consistency win for the founding pivot (can't learn transferable causal coherence from an arbitrary synthetic grammar, same as can't-learn-revenge-from-a-book). ROUTED NEXT (brain-foundational): RE-EARN the coherence-selector over the GROUNDED representation (grounded appraisal/situation-model features SHARED with the text encoding), NOT arbitrary type-vectors + NOT surface char-trigrams — so the learned coherence operates over grounded-semantic effect/outcome features that DO bridge to grounded text (mirroring why the appraisal function transferred). The sim's causal DYNAMICS must be grounded (effect = a grounded consequence of the cause's properties), sharing the representational substrate with how text events are encoded via the reading organs.

## CURRENT STATE (2026-08-03 ~23:20Z) — SUPERSEDED BY BANNER ABOVE. 🧱 FOUNDATIONAL PIVOT (USER): build the ~6yo GROUNDED FOUNDATION reading builds on. Branch dataprep/mcguffey-graded-corpus, all local NO push.

**🟢🆕 SIM-TO-TEXT TRANSFER = EXTRACTION_BOTTLENECK (2026-08-03 ~23:4XZ, DISK-VERIFIED 5 seeds, data/exp_grounded_appraisal_transfer_to_text_v1/, cell exp_grounded_appraisal_transfer_to_text_v1.py). THE FOUNDATION TRANSFERS — the blocker is now event-extraction, not the grounding.** Fed the SAME earned grounded function (all 5 seeds theta-digest MATCH the sim run f52b435fc62d1388 = genuine reuse, NOT a refit; contamination-clean) two ways: ARM-A oracle-extracted structure (given event-structure, NOT the answer) vs ARM-B real reading-organ extraction. RESULT: ARM-A BEATS ALL BASELINES — CAUSAL(n=4) arm_a 1.000 vs recency 0.000 / chance 0.500; IRONY(n=6) arm_a 0.667 vs surface 0.500 / chance 0.500. ARM-B collapses to chance — CAUSAL 0.450, IRONY 0.500. BENEFICIARY(n=5) = honest capability GAP (no appraisal slot distinguishes beneficiary from patient). => THE GROUNDED FOUNDATION CARRIES THE DISCRIMINATING SIGNAL (transfer works GIVEN clean structure — first evidence the earned foundation is real and usable by the reading layer), and the program's blocker is now PRECISELY LOCALIZED + evidence-forced: EVENT-EXTRACTION from raw text to populate the appraisal slots (the "one clearly-remaining gap" we'd flagged, now proven). HONEST BOUND (VET'd hard): tiny n (causal 4, irony 6); arm_a=1.0 is 4/4 on a small set (but beats recency 0/4 + is the same un-refit function + oracle gives structure-not-answer); irony arm_a 0.667 is the weaker leg (valence transfers less cleanly than causal even given structure); beneficiary untouched. GATE DECISION now RESOLVED (was deferred): WIRE the grounded appraisal function (it demonstrably serves the reading layer given structure) + the NEXT BUILD = event-extraction (which is intertwined with the causal-coherence SELECTION problem below — extracting "who blocked whose goal" from raw text IS causal-attribution selection).
**🔎🆕 MULTI-HOP SCOUR (2026-08-04 ~03:2XZ, USER-prompted, DISK-VERIFIED) — the current selector's 2-hop degradation is ALREADY DIAGNOSED + SOLVED by prior work that got ISLANDED. RECOVER-don't-reinvent.** Explore scour + Director disk-verification of prior multi-hop/chain work: (1) DIAGNOSIS of exactly our mechanism family — `data/exp_multihop_reverse_replay_backward_sweep_v1_self_contained/metrics.json` MIDDLE_BAND: reverse-replay ALONE collapses (B_reverse_only=0.016), one-way reverse adds nothing (C=0.507 vs A_baseline=0.506), the ONLY arm that lifts = BIDIRECTIONAL/MEET-IN-THE-MIDDLE (D_bidir=0.690, +0.184). => our one-directional predecessor-SR selector's 2-hop drop is EXPECTED = a DIRECTIONALITY problem, NOT a substrate capacity limit; the fix = add a FORWARD pass (meet-in-the-middle). (2) BEST deep-chain result in the repo is ISLANDED: VAMP-on-chain forward-backward Expectation Propagation (belief propagation) = acc 1.000 to depth ~200, robust K=5000 + 30% edge-noise (verdicts VAMPCHAIN_RESTORES / VAMPNOISE_ROBUST verified) — lives ONLY in experiments/exp_wave14_*vamp_chain*.py, NOT in hdlab/ (grep confirms), NO registry row. (3) TWO chain organs unused: hdlab/multi_hop.py (MIDDLE_BAND K=2 only; wired-but-orphaned, no registry row) + hdlab/reasoner.py (meet-in-middle, docstring 'supersedes K=2 multi_hop'; imported by NOTHING). (4) LOST: sr_routing_multihop (+0.253, SR-based = on-topic) registry row status=orphaned_source_not_locatable_retired_2026-08-03 (SHELVE) — recover from git history only. HONEST CAVEAT: ALL prior wins are SYNTHETIC KG chains, NOT natural-language causal chains over a situation model — so bidirectional-EP is established IN-SUBSTRATE but its transfer to the causal-situation-model setting is UNTESTED = the real frontier (distinct from the islanding). ROUTED: when we address multi-hop, RECOVER+WIRE (add forward/meet-in-middle to the selector; port VAMP-EP into an hdlab organ on the situation-model substrate; git-recover the +0.253 sr_routing cell), do NOT re-invent an islanded win. Validates the USER's islanding concern.

**🟢 IN-SIM COHERENCE-SELECTOR v2 = HARD_PASS (fixes v1's memorization; generalizes via STRUCTURE) — but in a FAVORABLE regime, honest next test named (2026-08-04 ~02:5XZ, DISK-VERIFIED 5 seeds, commit dbbb429c6; cell exp_coherence_selector_insim_v2.py).** THE FIX (mechanism not difficulty): give every entity a SHARED CONTENT-TYPE (N_TYPES=10 type vectors, same type-vocab in both partitions) + a fixed causal GRAMMAR (type->type bijection); entity = TYPE_VECTOR[type] + 5% bit-flip; M_backward (identical train_sr_transport, unchanged) trained on TRAIN-partition reversed transitions. MEASURED held-out EVAL (novel entities): coherence 1.000 (glassbox margin +0.48, positive all seeds) beats recency 0.000 / random 0.507 / no_replay 0.460 (min_lift 0.49); oracle 1.0; v1->v2 delta 0.263->1.000. ANTI-MEMORIZATION BATTERY PASSES (disk-verified): entity_overlap=0 (novel entities), shuffled-structure control COLLAPSES to 0.270 (structural_lift 0.73, fails-as-required = the win is attributable to structural type-recurrence, NOT a spurious cue), recurring-entity excluded. => GENUINE: the mechanism uses STRUCTURAL type-features not entity identity (fixes v1's memorization); brain-foundational reverse-replay over STRUCTURED content codes validated IN PRINCIPLE. ⚠️ LOAD-BEARING CAVEAT (do NOT over-read HARD_PASS): type_overlap_frac=1.0 — EVAL reused the SAME 10 types, only ENTITIES novel (and entity ~= type + 5% noise), so this is generalization to NOVEL INSTANCES OF KNOWN TYPES under a KNOWN single-hop grammar, NOT to novel relation-TYPES / multi-hop / realistic vocab (agent honestly flagged the favorable regime). THE DISCRIMINATING OPEN QUESTION: did it learn an ABSTRACT coherence RULE (effect<->outcome MATCH, generalizes to novel types) or just MEMORIZE this grammar's transition TABLE (works only for seen types)? v2 showed only the latter's robustness to novel instances. NEXT HARD STEP (routed, the honest can-fail): stress generalization to NOVEL TYPES / unseen transitions + multi-hop grammar + larger vocab — does coherence survive novel STRUCTURE? If yes = abstract transferable coherence competence; if no = limited to memorized grammar (important). Real-text transfer probe still deferred (needs the detective-probe position-confound fix below first). HONEST STATUS: coherence-selection works in-sim in a simple regime = a real step, NOT 'coherence solved'.
**➡️ v3 NOVEL-TYPES STRESS = MIDDLE_BAND, the GOOD answer (2026-08-04 ~03:2XZ, DISK-VERIFIED 5 seeds, cell exp_coherence_selector_novel_types_v3.py, commit pending).** On GENUINELY NOVEL TYPES (held out, type_overlap 0 confirmed per-seed): coherence 0.873 beats all floors (recency 0 / random 0.51 / no_replay 0.49, min_lift 0.37) and the SHUFFLED-STRUCTURE control COLLAPSES to 0.25 => the mechanism learned an ABSTRACT TRANSFERABLE coherence RULE, NOT a memorized grammar table (upgrades v2's favorable-regime caveat — it generalizes to unseen relation-types). MIDDLE_BAND (not HARD_PASS) because 0.873 is solid-but-imperfect on novel types vs 1.0 on known types = 'right mechanism class, underpowered', not a refutation. NOVEL-SCALE (larger vocab) holds 0.80. ⚠️ MULTI-HOP (2-hop) DEGRADES to 0.557 (abstain 44%) = the confirmed real limitation — and the USER-prompted multi-hop scour (above) already DIAGNOSED it (one-way reverse-replay collapses; bidirectional/meet-in-middle is the fix, D=0.690) + found the ISLANDED deep-chain solver (VAMP-EP, depth ~200) to recover. So multi-hop = a KNOWN next step, not a wall. NET (coherence-selector arc): v1 HARD_FAIL (memorization) -> v2 HARD_PASS (known types) -> v3 MIDDLE_BAND (novel types, ABSTRACT RULE confirmed; multi-hop is the routed next frontier via recover-VAMP-EP + add-forward-pass).
**➡️ v4 BIDIRECTIONAL 3-WAY (reverse/forward/bidir), VET'd HARD on disk (2026-08-04 ~04:0XZ, commit 0648ac128, verdict-label HARD_FAIL_NO_MULTIHOP_LIFT but re-read carefully):** DATA is FAIR + correctly implemented (floors fail, oracle 1.0, both SR maps converged, entity+type overlap 0, shuffled-control collapses, meeting-cosine NONZERO 0.22-0.28 = NOT the old v1 zero-cosine artifact). BUT (1) the HARD_FAIL LABEL is INFLATED by a MIS-CALIBRATED gate: 'margin-positive-every-seed' FAILS even at 1-hop where acc=0.947 -> trust the accuracy, not the label. (2) TRUSTWORTHY ACCURACY: 1hop R/F/B ~0.95; 2hop R=0.683 F=0.733 B=0.753 (adding FORWARD helps 2-hop +0.07, real+consistent); 3hop ALL ~0.35 BELOW random 0.50 = genuine collapse (single-application SR doesn't chain to 3). (3) MEETING PREMIUM absent BUT CONFOUNDED: B~=F (+0.02 @2hop, worse @3hop) because M_forward & M_backward are over the SAME permutation T = largely REDUNDANT, so the meeting can't add complementary info by construction -> 'no meeting premium' NOT cleanly established (would need complementary directions or iterative multi-step M composition to test fairly). DIRECTOR STRATEGIC CALL: STOP here on in-sim multi-hop (3 cells + scour + VET deep) = approaching INSTRUMENT-PERFECTIONISM on a synthetic toy; narrative causation is mostly 2-hop (handled ~0.75). Bank: 2-hop works, meeting-question confounded+low-priority, 3-hop a known synthetic limit. TURN TO the load-bearing REAL-TEXT TRANSFER (the deferred inflection). USER pushed the VET (right): I had over-propagated prior verdict strings; the fair controls (random-midpoint; here the redundancy confound + gate mis-calibration) are what matter.

**🧪 DETECTIVE-FICTION TRANSFER PROBE (Sherlock) = 10 honest items but a POSITION CONFOUND found in Director-verification -> FIX BEFORE USE (2026-08-04 ~02:4XZ, draft commit 3f145e5ee).** Mined 10 verbatim cross-span whodunit items from Gutenberg #1661/#834 (data/eval_gold_mention_role_mcguffey_v1/gold_causal_crossspan_detective_v3_DRAFT.jsonl, all gold_verified=false). GOOD: 30/30 substring-guard PASS, recency-wrong 10/10 (measured), 30/30 independent spans (fixed last round's reuse), honest yield (10 not the 20-30 target, did not pad; 9+ stories scanned+rejected). ⚠️ DIRECTOR-VERIFICATION FOUND A CONFOUND (agent self-flagged the symptom): the true_blocker is textually LATER than the distractor in 10/10 items -> a trivial 'pick the later-mentioned candidate' baseline scores 10/10 = NOT fair as a coherence test. DEEPER: the query sits just after the distractor and the true cause is the LATE REVEAL (whodunit climax) -> risks a 'find the stated answer' reading task, not causal-COHERENCE inference. FIX NEEDED before use: position-balance (true-blocker NOT always last), and/or exclude the reveal from the candidate set so coherence must be INFERRED, and add a last-position baseline to the defeat-checks the selector must beat. Detective fiction's genre structure (reveal-at-climax) is itself the reason for the confound — a real finding, flagged not papered over. SECONDARY probe (does NOT gate the v2 selector, which has its own in-sim controls); fix deferred behind the v2 capability.

**🔴 IN-SIM COHERENCE-SELECTOR v1 = HARD_FAIL (memorization, not generalization) — real open problem, honestly diagnosed (2026-08-04 ~02:2XZ, DISK-VERIFIED 5 seeds, commit bdccafc09; cell exp_coherence_selector_insim_v1.py).** Built the reverse-replay/predecessor-SR selector to BOTH guards (M_backward genuinely RETRAINED on reversed transitions not a transpose; emerges from replay over AccumulateRegister, not a bolt-on classifier; self_improving_loop abstain-band as control-flow). MEASURED: TRAIN coherence 1.000 (glassbox margin +0.92) but held-out EVAL 0.263 (margin -0.0006 = chance, abstains 74%), BELOW random 0.507 (min_lift -0.24). Machinery SOUND (SR TD converged 0.022->0.002, oracle control 1.0 both partitions, buffer fidelity 1.0) => NOT an impl-bug; textbook MEMORIZATION signature (perfect in-dist, chance out-of-dist). DIRECTOR VET of the agent's diagnosis (mostly right, sharpened): the generalization AXIS was mis-specified — TRAIN/EVAL used DISJOINT i.i.d.-random symbols with ZERO shared structure, and an SR map generalizes only across shared predictive structure (Dayan/Stachenfeld), so it had nothing to generalize from => the test measured memorization, did NOT test whether reverse-replay coherence GENERALIZES (question UNANSWERED, neither win nor clean negative). ⚠️ SHARPENING (the agent's proposed fix 'recurring entities' would be UNFAIR — memorizer passes): the deeper issue is the mechanism learns ENTITY-SPECIFIC associations, not the transferable coherence RULE. Causal coherence is RELATIONAL — the true antecedent is the one whose EFFECT-FEATURES match the OUTCOME, independent of entity identity. HONEST STATUS: we do NOT yet have a working coherence-selector; first attempt memorized = a real open problem, NOT progress-toward-success. FIX (routed next): make coherence a STRUCTURAL/RELATIONAL computation (candidate-effect <-> outcome content-match) that generalizes to novel entities BY CONSTRUCTION, with reverse-replay over STRUCTURED content states (as hippocampal replay actually is) not arbitrary symbols; test by COMPOSITIONAL generalization (novel entities, SAME causal grammar) with an explicit anti-memorization control (recurring-entity path MUST NOT be how it passes). Text transfer-probe NOT run (honest, out of time-box). Detective-fiction transfer probe mining in parallel (separate agent).

**🧭 EVAL-GROWTH + DIRECTOR-VERIFICATION -> corpus is LIMITED -> STRATEGIC REFRAME: EARN THE SELECTOR IN SIM, TEXT EVAL = TRANSFER PROBE (2026-08-04 ~02:1XZ, draft commit d5c95c34a).** Grew the multi_candidate_causal_attribution DRAFT n=4->8 (gold_grounded_causal_crossspan_v2_DRAFT.jsonl, all gold_verified=false). DIRECTOR-VERIFIED the 4 new items (this gate caught a real error): grapp_mcca_006 (brooch) REJECT — its true_blocker_span mis-points to the raspberry-CORDIAL passage, not the brooch-in-shawl cause; the agent shared one span across 006/007 + wrongly rationalized "explains both" (two DISTINCT AoGG incidents ch14 brooch vs ch16 cordial). 007 (cordial, Marilla's cellar mistake, recency->Anne wrong) ACCEPT; 008 (ink-spill, Alfred Temple true, recency->Tom wrong) ACCEPT; 009 (torn book, Becky true, recency->Tom wrong) ACCEPT (minor: distractor span reused from 008). => ~3 valid new (7 total, fewer independent). DEEPER FINDING: the discriminating eval is CORPUS-LIMITED — mining 3 whole novels hard yields only a handful of genuine cross-span whodunits (most blame scenes are single-agent-obvious). Growing the TEXT eval to POWER a selector will NOT work by mining harder. 🎯 REFRAME (the right resolution, reuses the proven architecture): do NOT power the selector from text — EARN the coherence-SELECTOR (M_backward) in the SIMULATION (unlimited CONTROLLED cross-span causal episodes with true-vs-distractor = SOLVES the drill's Gap 1 training-pipeline + gives a POWERED train/validation set), and use the ~7 Director-verified text items as the TRANSFER PROBE. Mirrors the earned-sim->transfer-to-text pattern that worked all night (earn the hard inference in sim, test transfer on real text). Corpus limitation stops being a blocker + becomes the reason the sim exists. NEXT (revised): build the M_backward coherence-selector IN-SIM (powered, controlled, can-fail: recency/random selectors MUST fail; the selector must discriminate true-vs-distractor where binding brings BOTH), validate in-sim, THEN transfer-probe on the Director-verified text items. (Data hygiene: 006 needs a corrected brooch-in-shawl true_blocker_span before use; 007-009 usable.)

**🟢 CROSS-SPAN CAUSAL BINDING v1 = RECALL WIN, SELECTION IS THE CONFIRMED NEXT GAP (2026-08-04 ~01:5XZ, DISK-VERIFIED 5 seeds, commit a780590a5; cell exp_cross_span_causal_binding_v1.py). Clean MIDDLE-BAND as pre-registered.** Built REAL full-strength cross-span binding (situation-model accumulation of harm events across the passage + hippocampal victim<->goal-object coref; FHRR fidelity margins 0.55-0.95 = real organ work not a stub). MEASURED (theta reused bit-identical, forbidden gold fields NEVER read, positive control arm_a=1.0, floor + random both 0/4): recall_true within 0.0 -> CROSS 0.75 (3/4: mcca_003/004/005 reachable; mcca_001 unreachable = named possessive-PP extraction-shape gap "the young man's breast", NOT a binding failure) -> random 0.0. RECALL IS A GENUINE WIN — cross-span binding makes the true blocker reachable where local extraction structurally reaches nothing (recovers e.g. mcca_005 "Sid's fingers slipped and the bowl dropped and broke" from 82 windowed sentences). ⚠️ HONEST VERDICT DEFLATED FROM THE MECHANICAL ONE: metrics.json verdict string = CROSS_SPAN_BINDING_LIFTS_RECALL_AND_SELECTION (because e2e 0->1/4), but the agent correctly TRACED + DEFLATED the selection half — recall_DISTR ALSO lifts to 0.75 (binding brings in true AND distractor equally = does NOT discriminate); the single e2e win (mcca_003) is a VALENCE-LEXICON COINCIDENCE (true span scored HARM, distractor NA, so only true passed the bridge's HARM gate; the coref gate itself did not discriminate). On genuinely discriminating items the recency-biased bridge FAILS (mcca_005 picks distractor, mcca_004 abstains). The earned grounded theta is NOT the selector here (it only scores the final appraisal Q downstream). => BINDING SUFFICES FOR RECALL; the COHERENCE-SELECTOR (retrained M_backward, Gap 1 of the causal-coherence drill) is the CONFIRMED routed next gap — and it is NOW UNBLOCKED: binding produces the reachable candidate SET the selector needs to select among (earlier M_backward was correctly deferred as working-ahead-of-the-blocker; extraction->binding->selection each unblocked the next). HONEST CAVEATS: n=4 all-cross-span = a PILOT not powered (binomial can't reject random at n=4); the NEW windowed-accumulation code has NO independent gold + WINDOW_LINES=100 was a-priori (construction-risk param, unvalidated); the two REUSED sub-primitives carry the parent n=100 gold (0.60 vs 0.03). 🚧 GATING PARALLEL NEED (agent-flagged, recurring "the ruler is too thin"): GROW the multi_candidate_causal_attribution eval beyond 4 items BEFORE any selector verdict is landable — it is the bottleneck for BOTH this cell and the selector cell. NEXT: (1) grow+Director-verify the discriminating cross-span causal eval (gating); THEN (2) build the M_backward coherence-selector + its training pipeline, validated on the powered eval.

**🟢 ARGUMENT-STRUCTURE / PATIENT EXTRACTION = INFORMATIVE NEGATIVE that RE-LOCALIZES the frontier to CROSS-SPAN CAUSAL BINDING (2026-08-04 ~00:5XZ, DISK-VERIFIED, commit 530c95079; cell exp_argument_structure_patient_extraction_v1.py). Verdict PATIENT_FIX_REJECTED_REGRESSES_CAUSAL.** Built per-candidate PATIENT extraction + patient<->victim coref to un-collapse the bridge's hardcoded patient="VICTIM". MEASURED (theta reused bit-identical f52b435fc62d1388, 16 units): the STRUCTURAL fix WORKS (patients_differ_rate 0.75 = candidates genuinely get DIFFERENT patients; the literal collapse is fixed) and the extractor is SOUND (anti-overfit patient-head recall 0.60 vs shuffled 0.03 on N=100 independent verified argstruct gold gold_mcguffey_lccp_argstruct_v1.json — beats control decisively), BUT correct-differentiation REGRESSES HARDCODED 1/4 -> EXTRACTED_REAL 0/4 (random control 0/4, negative-control PASSES). KEY HONEST READ: the regression is a CORRECTION TOWARD HONESTY, not a capability loss — HARDCODED's lone win (mcca_003) was FOOL'S GOLD (patient="VICTIM" trivially matched the query "VICTIM", a lenient artifact of the collapse, not reasoning); the strict gate correctly removes it. 0/4 is the TRUER floor. WHY: the true blockers in all 4 items are CROSS-SPAN / INFERENTIAL (the harm EVENT and the victim/goal-object are not co-located in the candidate span — e.g. Laurie's forging harms Meg but the cited true span is a later meta-statement); a LOCAL per-span extractor structurally cannot reach them — and neither does the brain locally (causal comprehension = cross-sentence BRIDGING inference, Trabasso/Kintsch, via hippocampal relational retrieval + DMN situation model). So the negative CONFIRMS the brain-foundational picture. Self-test crash (mid-build, wrong assertion on rec-leaked pred_slot) was resolved HONESTLY (rewrote to the true invariant on arm_b_real_attributed, did NOT weaken to pass). 🎯 CONVERGENCE (the payoff): the frontier re-localizes to CROSS-SPAN CAUSAL BINDING (link a harm event to its victim/goal-object across sentences) = the SAME target the causal-coherence drill independently reached (situation-model-level reverse binding + coherence selection). The EXTRACTION line and the SELECTION line MEET at situation-model-level causal binding: REUSE CausalLinkRegister (accumulate per-agent harm events across the passage) + coref (hippocampal victim<->goal-object link) + the coherence selector. KEEP per-candidate patient extraction as the correct STRICTER GATE (removes false positives); PAIR it with cross-span binding to recover the true blockers it can't reach locally. NEXT HARD STEP = cross-span causal binding cell (measurement-first: within-span-only MUST stay 0/4; does situation-model-level accumulation + cross-span victim-coref lift differentiation toward the arm_a ceiling?).

**🟡 APPRAISAL-STRUCTURE EXTRACTION CELL = FIX_HELPS_NARROW_BOTTLENECK_REMAINS (2026-08-04 ~00:1XZ, DISK-VERIFIED, commit 8eaf062fa; cell exp_appraisal_structure_extraction_v1.py).** Diagnosed the 4 causal items' extraction failures + routed by flavor: (a) USED-ABILITY-WRONG=1 (mcca_003 "punished" inflection missed by exact-token match), (b) MISSING-PRIMITIVE=2 (mcca_004 spite-by-omission surface-reads HELP; mcca_005 true cause is unworded accident), (c) MISSING-FACT=1 (mcca_001 "drove the knife to the hilt" zero lexicon coverage). FIX APPLIED = content-blind lemma stemming (affix-strip to shared root, applied identically to HARM/HELP tables + input tokens; no per-item tuning) = brain structure = temporal-lobe lexical access (affix-stripping to a shared lemma entry), a WIRING fix not a new mechanism. MEASURED (theta reused bit-identical f52b435fc62d1388, NOT retrained): correct-differentiation 0/20->5/20, deterministic-correct-diff items base=0 -> fix=1 (mcca_003, all 5 seeds) vs random=0 (can-fail control PASSES on the differentiation metric — random cannot produce a seed-consistent correct-diff); anti-overfit valence n=12 0.60->0.60 (no regression); an alternative physical-violence-harm-word SUPPLY was independently tested + REJECTED (regressed valence 9/12->8/12 + inflated false-positives) = validates picking stemming over lexical supply. Bottleneck REMAINS: the fix deterministically solves 1/4; downstream doesn't clear chance. HONEST: n=4 tiny; the mcca_003 win is partly fortuitous ("punished"=aftermath HARM token landing on the right span); anti-overfit guard weak (broad ~165 gold carries no valence truth). ⚠️ rec-LEAK found: the `rec` line-position feature leaks answer-correlated signal into downstream arm_b accuracy (random scores 0.60) — so the TRANSFER cell's arm_b=0.45 number is rec-inflated + should be orthogonalized before trusted; does NOT threaten the transfer WORKS-given-structure claim (arm_a beat recency 0.000, so arm_a's win is not line-position leak) — if anything real-extraction is MORE degenerate than 0.45 = REINFORCES the extraction bottleneck. ROUTE DECISION (next hard step, NOT more lexical patching): dominant failure = MISSING-PRIMITIVE = per-candidate PATIENT / argument-structure extraction + patient<->goal-object/victim COREFERENCE (the bridge currently hardcodes patient="VICTIM" identically for both candidates = collapses the discriminator). Brain structure = thematic-role/argument-structure assignment (IFG/pTL) + coref (hippocampal, REUSE coreference_resolver) — but needs argument-structure extraction the substrate does NOT yet have (a real dedicated build). Secondary cleanup: orthogonalize the parent cell's rec-feature leak.

**🔬 SELF-DRIVE DIAGNOSTIC (2026-08-03 ~23:49Z, in-thread from arm-B metrics): arm-B's failure is PRIMARILY EXTRACTION, not selection — re-orders the next step.** Across seeds, arm-B extraction DIFFERENTIATED the competing causal candidates in only 5/20 (seed×item) cases; in 15/20 it couldn't tell the candidates apart at all (falls to chance). In the ONE differentiated case (grapp_mcca_005) it attributed to the DISTRACTOR (= the recency/coherence selection error the M_backward drill targets). => EVENT-EXTRACTION is the BINDING constraint (get raw text -> differentiated who-blocked-whose-goal + valence candidates); the causal-coherence SELECTOR (M_backward, drill P~0.35) is DOWNSTREAM/not-yet-binding (only bites once extraction differentiates). Building M_backward first = a selector for candidates extraction can't yet produce = working ahead of the blocker (anti-pattern per do-the-hard-BLOCKING-thing). => NEXT HARD STEP re-ordered: appraisal-structure EXTRACTION from raw text (reuse the 08-02 role-extraction ~0.60 + clause-agent + coref prior art, retargeted to the causal-attribution candidate set + valence), measurement-first (degenerate/random extraction MUST fail), on the same transfer eval; target = lift arm-B differentiation 5/20 upward toward arm-A's given-structure ceiling.

**🟢🆕 FIRST EARNED-SIM CAN-FAIL CELL = MECHANISM_EARNS (2026-08-03 ~23:1XZ, commit 2d5695f61, DISK-VERIFIED 5 seeds).** The USER chose (A) commit to the earned experiential-simulation; this was its first bounded can-fail cell (`experiments/exp_grounded_appraisal_sim_earned_v1.py`, prereg 2026-08-03_grounded_appraisal_sim_earned_v1.md, metrics data/exp_grounded_appraisal_sim_earned_v1/). RESULT (held-out agents, 5-seed means): FULL 1.000 / RANDOM 0.168 / MEMORIZED 0.206 / NO_APPRAISAL 0.239 — all three floors FAIL as required (not construction-determined, not memorization, appraisal non-vacuous). REVENGE EMERGES: revenge_emergence_rate=1.0, targeting_specificity=1.0, bystander_harm=0.0 with NO `retaliate` label (primitives {pursue,withdraw,harm,help}; harm learned toward the true causal blocker from episode reward alone) = guard #1 held. EARNED coherence beats recency: earned_restoration 1.0 vs recency 0.303, glass-box Q(coherent)−Q(recent)=+1.146 positive all seeds = fixes tonight's falsification IN THE SIM. HONEST BOUND (VET'd as hard as the positive): FULL=1.0 is a CEILING-BY-DESIGN on a TOY (8 discrete actions, separable once grounded) — the load-bearing evidence is the FLOOR SEPARATION, not the 1.0; non-triviality corroborated by the 1200-episode smoke stalling at 0.44-0.69/revenge=0 for 3/5 seeds (earning takes real experience). SIM-TO-TEXT TRANSFER = NOT claimed by this cell (the real remaining gap, agent P~0.35); self-extending store NOT built here. = first evidence the earned brain-foundational path WORKS, cheaply — in a toy world. NEXT LOAD-BEARING = the transfer test (design-gated).

**🟢🆕 LEARNING-MECHANISM DRILL (commit 6e9a6c6a8, DISK-VERIFIED): the "error-driven update" is NOT hypothetical — we already own it, HARD_PASS certified.** `data/exp_pfc_gate_cfrpe_trained_v2/metrics.json`=HARD_PASS (gonogo_lift 0.600, dynamics_lift 0.603, reach_tcos_corr −0.079 dynamics-carried, sign_p 0.0000; NARROW: n_fair=2/7 regimes) = a basal-ganglia actor-critic (RPE delta-rule + SR-transport TD(0) + Go/NoGo). REUSE spec for the scale-up: (a) appraisal→action = RE-POINT the certified pfc_gate_cfrpe organ (STATE=appraisal-vec, ACTION={pursue/harm/help/withdraw}, REWARD=goal-restoration), P~0.60; (b) causal-COHERENCE credit — REVISED DOWN by the follow-on drill (notes/research_drill_biology_led_causal_coherence_credit_assignment_2026-08-03.md, disk-verified): NOT a cheap reuse. CausalLinkRegister's 0.9722 is a GIVEN-LINK write-then-read CAPACITY test, NOT a selector (it decodes a WRONG written link with identical fidelity — confirmed in code: add_causal_link binds whatever pair given, query_* decodes what was written; no competitor ranking). And the backward map must be a GENUINELY RETRAINED SR-transport on reversed transitions (train_sr_transport), NOT a free M-transpose. self_improving_loop reused as ARCHITECTURE (abstain-band gate) only. REAL BOTTLENECK = Gap 1: no training-data pipeline for M_backward yet (need an exploration-graph of transitions). EXPLICIT NON-REUSE of the falsified coref `_pick_strict_cb` recency selector stands. Drill designed a 4-item can-fail pilot on the real recency-trap gold + glass-box reach-margin witness. P_deflated 0.35 (was 0.45); (c) sim→text transfer rides the shared appraisal-vec interface, UNVERIFIED P~0.35. K=2 delta-rule composition is HARD_FAIL (`exp_substrate_adaptive_cfrpe_x_k2_compose_v1`) → keep each earned mapping to a SINGLE delta-rule stage. The brain-structure/shared-process REUSE gate keeps predicting right (both hard pieces re-point ONE certified RL organ).

**🧱 THE PIVOT (USER 08-03, the night's real conclusion):** the ~8 'we weren't doing it right' corrections tonight (cheap-distributional -> mean-pool -> scorer-bug -> wrong-grain -> wrong-success-criterion -> context-stripped -> bundle-vs-bind -> situated-structure) are ONE failure, not eight: we tried to comprehend goal/affective/social meaning (revenge, anger, care) from UNGROUNDED text with NO foundation for the words to land on. Reading BUILDS ON a grounded ~6yo conceptual foundation; a book doesn't teach what anger/revenge ARE. USER: "there needs to be a foundational knowledge that all other knowledge can build upon... we should strive for that ~6yo foundation. You can't learn revenge and anger from a book. There must be work on this." => NEW PROGRAM: build the FOUNDATIONAL GROUNDED-KNOWLEDGE LAYER. WHAT (dev-science): Spelke core knowledge (objects/AGENTS/goals/self-other) + emotions grounded in goal-outcomes via appraisal (anger=goal-blocked-by-agent, fear=threat, joy=achieved) + early ToM + harm/help/causality. HOW (no-borrow, not-from-text): supply innate core primitives by hand + GROUND emotion/social/goal structure via a MINIMAL EXPERIENTIAL SIMULATION (agents with goals cooperate/conflict; substrate EARNS blocked-goal->anger->retaliate by EXPERIENCE, glass-box) -> THEN reading maps text onto the grounded foundation via the existing organs. Grounding NECESSARY (not optional) for goal/affect; updates the 07-14 grounding=optional scope. Design research IN FLIGHT (ae3868a2). [[project_build_the_6yo_grounded_foundation_reading_builds_on_USER_2026-08-03]]

**🆕 GROUNDING-PREMISE PROBES (cheap, fair+brain-foundational gated; 2026-08-03 ~21:2XZ):** Phase-0 (commit d76763195) = grounded appraisal-STRUCTURE (target + causal-attribution + valence) beats text-only near-synonym disambiguation but NARROW+ORACLE-ONLY (confused-4 0.75 vs text 0.50, n=4, min margin; scramble control revealed VALENCE is a wash -> lift = target + causal-attribution). Then causal-attribution derived AUTOMATICALLY + TRULY brain-foundationally (commit d3b035e59, verdict BRIDGING_WORKS): built bridging AS a LITERAL RETARGET of coreference_resolver's _pick_strict_cb/Centering backward-search (imported UNMODIFIED) from antecedent-ENTITY to agent-caused antecedent-EVENT matches oracle per-item (0.75 confused / 0.417 full-12), beats recency 0.50 + text 0.25, fairness held (shuffle degrades, no gold-flag leakage, backward-constraint). ⚠️ TRUSTWORTHY-GATE CORRECTION (commit bce466189, Director OVER-READ caught): 'circuit-sharing VALIDATED IN CODE' was OVERSTATED. The coref primitives ARE imported unmodified, BUT the coherence-RANKING (_pick_strict_cb = the actual 'same circuit' piece) is a NO-OP: the entity-linking+valence FILTER surfaces 0-or-1 candidate EVER across all 12 items (1 for item007, 0 for the rest), so the Centering backward-search never runs on competitors. The win is 100% the upstream FILTER, not the ranking. HONEST bounded result = 'entity-linking-gated causal-attribution beats naive recency on ONE item + matches oracle on a corpus with NO multi-candidate cases' -- fair (no label leakage) but NOT validated coref-as-bridging (the coherence-ranking = UNTESTED). To genuinely validate the circuit-reuse hypothesis: need multi-candidate cases (>=2 gate-passing blockers) + test the ranking. Circuit-sharing stays a HYPOTHESIS, not a validated result. HONEST LIMITS (VET-level): n=4; bridging-over-recency win rests on ~ONE discriminating item (010, entity-linking rejects a false-positive) since data lacks multi-antecedent cases (coherence-RANKING untested); item 007 still misses but for a SEPARATE valence/IRONY reason (bridging correctly derives Amy-blocked-Jo, oracle fails 007 identically) = causal-attribution SOLVED, valence is the residual; ALL on GIVEN gold events -> event-EXTRACTION is the separate remaining gap. FRONTIER re-localizes to: (1) VALENCE via intent/irony = mentalizing (mPFC/TPJ) = REUSE the Sally-Anne ToM organ (per the brain-structure+shared-process gate); (2) event-EXTRACTION from raw text. TRUSTWORTHY-GATE VET of the bridging positive IN FLIGHT (what actually carries the win: entity-linking filter vs coherence-ranking; construction-determination check). 

**🆕 VALENCE via MENTALIZING = INTENT_VALENCE_WORKS (cleaner positive than bridging; 2026-08-03 ~21:4XZ, commit 1a39789ea).** Reused the ToM organ: MentalStateAffectRegister = 2nd extension of AccumulateRegister (same bind/unbind/cleanup chain as the HARD_PASS Sally-Anne organ, imported not reimplemented) -- holds 'valence I received from agent X', infers retaliatory/reciprocal stance = mentalizing, not surface-word valence. confused_4: SURFACE 0.75 -> INTENT 1.0; full_12 0.417->0.500 (zero regressions). ITEM 007 (irony, UNFIXED even by oracle before) now fixed FULLY AUTOMATICALLY via the ToM retaliation-register (Amy harmed Jo earlier, strictly-earlier-scoped); ABLATION CONTROL HOLDS = scramble the ToM register -> 007's fix COLLAPSES (the mechanism does the work, not noise -- the internal control bridging LACKED). Agent self-caught+fixed a lookahead-leakage bug (007->008) mid-build. HONEST CAVEATS (same bar as bridging): n=4 confused / full-12 modest +1; item 011 beneficiary (Toto != patient Lion) is ORACLE-only (no auto path in this corpus) -> 007 is the truly-automatic win, 011 oracle-carried; runs on GIVEN events (event-extraction gap still underneath). THROUGH-LINE: both hard appraisal pieces (causal-attribution + valence) = REUSE of proven organs (coref / ToM) -- the brain-structure/shared-process gate keeps predicting right. Both NARROW on 12 items -> the RICHER DISCRIMINATING eval (a0f0373f, building) is the proper validation for BOTH (multi-candidate ranking + genuine irony/beneficiary + auto paths), trustworthy-gated. HOLDING for the eval, then re-test both.

**🆕 PROPER VALIDATION on the RICHER DISCRIMINATING eval = DECISIVE NEGATIVE; the reuse-shortcut FALSIFIED, the design's EARNED piece VINDICATED (2026-08-03 ~21:5XZ, commit e34d54701).** Re-ran the existing bridging + valence mechanisms on the 15-item Director-verified discriminating eval (defeats trivial baselines). RESULTS: (1) CAUSAL-ATTRIBUTION coherence-ranking FALSIFIED -- _pick_strict_cb (coref backward-search) IS recency (argmax position); real spans admit <2 candidates (0/4 faithful); forced to 2 candidates (steelman) it picks most-recent = WRONG 0/4 = identical to recency. So 'coref-as-bridging = same circuit' is WRONG at the mechanism level: pronoun-antecedent selection (recency/salience) != causal-antecedent selection (coherence/explanation); shared hippocampal substrate but DIFFERENT selection SIGNAL. My circuit-sharing claim was too facile. (2) VALENCE-via-ToM falsified AS GENERAL: it's a RETALIATION predictor not an irony detector (genuine 1/3 on real irony; agent caught its OWN spurious-fire over-read). (3) BENEFICIARY = unaddressed gap (oracle-only). Fair (contamination-checked, no gold-label leakage). => the 'grounded appraisal is mostly REUSE of proven organs' narrative is FALSIFIED for hard cases: the organs give recency + retaliation, NOT causal-COHERENCE + general mentalizing. WHAT IT TEACHES (brain): causal-attribution needs a COHERENCE-OF-CAUSAL-EXPLANATION signal (which prior event best EXPLAINS the outcome, Kintsch/Trabasso), NOT salience/recency -- a genuinely harder, EARNED thing. VINDICATES the foundational design's core claim I was shortcutting: the affect/causal DYNAMICS must be EARNED (via the experiential simulation), NOT supplied/reused -- now EVIDENCE-MOTIVATED. DECISION for USER: commit to the earned experiential-simulation build (the design's genuinely-new piece, now evidence-forced; multi-session) vs reconsider. The reuse-shortcut is closed.

**GROUND (unchanged, becomes the READING layer ON TOP of the foundation):** full glass-box comprehension stack built+WIRED+USED (coref / situation_model_accumulate / situation_model_multibank / self_improving_loop / CausalLinkRegister 0.9722 GIVEN links / Trabasso goal->causal). Organs WORK GIVEN structured relations — they just had no grounded meaning underneath.

*(Prior relation-inference arc that led here — the frontier had been localized to the unstated-goal axis; every mechanism (predictor 4x, construction-integration, situated-structure) shuffled ungrounded symbols; that's what surfaced the grounding root. Tonight commits bfdbf6b17..(latest); blow-by-blow in HISTORY below + git.)*

**VERIFIED MILESTONE (the ground):** full glass-box comprehension stack built+WIRED+USED (hdlab: coreference_resolver dense-B3~0.87; situation_model_accumulate on durable situation_model_multibank; self_improving_loop coherence-gated; CausalLinkRegister CAUSAL organ DIRECTOR-VERIFIED **0.9722** cross-chapter non-adjacent GIVEN links; Trabasso goal->causal). = a glass-box reader that tracks a novel's characters in durable memory + reads its non-adjacent causation at 0.9722 GIVEN structured relations. The organs WORK GIVEN relations.

**THE FRONTIER:** infer the UNSTATED relations (goals + satisfy/thwart/cause) FROM RAW TEXT to feed the working organs.

**TONIGHT'S ARC (all VET'd hard, negatives as hard as positives — ~10 over-reads of mine corrected by independent VET):** (1) cheap earned-content CLOSED (PPMI single/multi/mean-removed all cap ~0.667). (2) USER PRINCIPLE: select by brain-foundational-RIGHT not cheap [[feedback_select_by_brain_foundational_right_not_by_cheap_regardless_of_hard_USER_2026-08-03]]. (3) built + Director-VERIFIED a 25-item relation eval. (4) measurement phase COMPLETE+trustworthy: word-level distributional EMPIRICALLY exhausted (random-lexical 0.52 > trained; scorer artifacts all found+fixed: mean-pool saturation / object-slot / candidate-order tie-break). (5) USER COMMITTED to the EVENT-LEVEL paradigm (default NO Binder = fully earned). (6) event-PREDICTOR CLOSED — 4 negatives (MSE mean-reverts / associative loses-to-copy / SR-TD pre-registered FALSIFICATION), VET'd on BOTH gates; the fidelity-gap synthesis found the ROOT = WRONG SUCCESS CRITERION: SR/TD's trained/copy/mean/random clustered within 0.005 = next-event prediction is information-theoretically FLAT for ANYONE; the brain uses CONSTRUCTION->INTEGRATION, not prediction-accuracy. Scale/curriculum hypothesis TESTED+REJECTED. (7) 3 brain-drills (SR / inverse-planning goal-inference / fidelity-gap) CONVERGE on construction->integration over validated organs (pure reuse). (8) construction->integration build = MIDDLE_BAND (honestly deflated): satisfy_restate 0.857 == INTEGRATION_ONLY 0.857 (a STRUCTURAL-marker win, construction added ZERO); unstated_goal 0.25 LOSES to lexical 0.333; used organ-PATTERN on ISOLATED snippets not real organs w/ accumulated context (gate-2 fail).

**FRONTIER LOCALIZED (the night's real product):** the unstated-goal axis needs to disambiguate NEAR-SYNONYM categories (REVENGE_PUNISH vs SELF_DISCIPLINE both contain 'punish') — impossible for prediction(closed)/structural-markers(satisfy only)/lexical/spreading-activation => genuinely needs **EARNED CONTENT MEANING** = the deep-earn content-encoding problem, now EVIDENCE-localized not assumed. Existence-proof (USER-locked): the brain disambiguates goals by understanding meaning -> possible, NOT a ceiling, keep digging [[feedback_brain_is_existence_proof_100pct_achievable_every_shortfall_gets_fair_test_and_brain_fidelity_gates_USER_2026-07-23]].

**🆕 A-vs-B DECIDED + REFRAMED (2026-08-03 ~19:2XZ, commit 15dd0da51 + Director self-audit):** A-vs-B decider = CONTENT_NEEDED (accumulated context did NOT disambiguate near-synonym goals, 0/3 flips) -- BUT the agent's own diagnosis + a USER-prompted self-audit CONVERGE on the real issue: we represented goals as DISCRETE CATEGORY picks over ADDITIVE BUNDLES = the two LEAST brain-faithful choices; bundling DESTROYS the situated STRUCTURE. REFRAME (brain-faithful): a goal = SITUATED STRUCTURE agent->TARGET(self/other)->action->AFFECTIVE VALENCE(harm/help). Near-synonym disambiguation DECOMPOSES: (1) TARGET self-vs-other = STRUCTURAL (binding+coref, cheap, we have it; bundling erased it) + (2) AFFECTIVE VALENCE (REVENGE=harm-toward-other vs CARE/PROTECT=help-toward-other) = the ONLY genuine content residual (ties to affect=stakes anchor) -- NOT generic text-only content-encoding. => the frontier is PRECISELY-relocalized from 'earn all content meaning' to 'situated-structure representation (binding not bundling) + a specific affective-VALENCE signal'. TEST IN FLIGHT (a7a370e2): situated-structure (bind agent-target-action + valence) vs bundle+category, ablated per-component. STRUCTURE_HELPS -> the deep-earn is reframed to affect-valence (tractable); STRUCTURE_INSUFFICIENT -> deeper content need (honest). This directly answers USER 'are we doing this RIGHT?': we were NOT (bundle+category); situated-structure is the fix. Locks in force: brain-foundational always; VET negatives as hard as positives; NO borrowed-embedding encoder; NO bolt-on reader/parser; MEANING=ASSIGNMENT; glass-box; STORE LOCAL-ONLY + git-commit-after-bank, NO push w/o in-session auth. Op note: subagents repeatedly backgrounded runs + Monitor instead of foreground (dropped 4 reports, spawned racing procs) -> Director verifies results ON DISK, ran the SR cell foreground in .venv himself.

---
[HISTORY BELOW -- superseded by the consolidated banner above; kept for context.]
## 🧭 CURRENT STATE (2026-08-02 ~18:30Z) — THE COMPREHENSION-PIPELINE ARC.
(Full session blow-by-blow is in git history + atoms; this is the consolidated truth. Branch = `dataprep/mcguffey-graded-corpus`, all local, NO push.)

### THE GOAL
A glass-box substrate that genuinely COMPREHENDS real text and you can CONVERSE with — earning meaning + comprehension the brain's way (no borrowed embeddings, no bolt-on reader; supplying knowledge/DATA/dictionary is fine). North star = the self-improving reader (reads at scale → flags what it doesn't know → learns to fix it → reads more).

### THE PIPELINE WE'RE BUILDING (real-text comprehension of McGuffey)
`text → role EXTRACTION (who-did-what per clause) → SITUATION MODEL (track entities across the discourse, accumulate events) → COMPREHENSION`, with a metacognitive layer on top (flag unknowns → research → learn). Eval = McGuffey graded readers, real gold (hand-verified mention/role + cross-clause entity tracking).

### WHAT WORKS (VET-confirmed)
- **Role extraction, learned + construction-conditional:** ~0.60 end-to-end on real McGuffey (trustworthy, powered eval v3 N=165). Resolves order≠role constructions (quotative, by-agent passive) via LEARNED construction-cue → role mapping (atoms 29604-06, scoped: construction-conditional mapping, NOT a cross-clause loop — see correction below).
- **Clause-level agent=subject** (segment clauses, subject of each = its agent): the big lever for canonical narrative (multi-clause/compound/subordinate subjects). Recovered end-to-end 0.55→0.675 on the small eval (0.60 trustworthy on v3).
- **Situation-model register = ACCUMULATE (FHRR bundle), not overwrite** (atom 29609): the register must bundle an entity's growing event set; pure-overwrite loses history. Validated on oracle roles (accumulate 1.0 vs overwrite 0.46). WIRED to hdlab/situation_model_accumulate.py + verification green. (Brain-faithful behaviorally; FHRR neural-implementation labeled CONTESTED.)
- **Learned construction classifier (drift-fix, partial):** 3/4 marked constructions (quotative/by-agent/copular) now LEARNED from gold (near-perfect classification), replacing hand-coded gate rules — matches/beats the hand gates. Canonical-default class needs the eADM reformulation (canonical=commit default; classifier detects only MARKED).
- **Coverage lever:** adding the `theme` role (dominant missing role) lifted end-to-end 0.42→0.57 — role INVENTORY is a real binding constraint.
- **Powered evals built:** by-agent gold N=5→23, quotative N=20→45, multiclause entity-track v3 (39 passages, 165 events), long-form v1 (24 passages 4-7 clauses, rich role trajectories).

### THE WALL + THE HONEST REFRAMES (this session's core findings)
- **The wall is EXTRACTION GENERALIZATION on real text** (atom 29610), NOT the situation-model organ (validated given correct roles). Broken into: ~10% missing-role COVERAGE (recipient/possessor/experiencer, no vocab+gold), + the rest = clause-structure extraction errors (mostly fixed by clause-level agent). NOT meaning: the animacy DICTIONARY was NULL on McGuffey (probe-caught — McGuffey lacks animacy-conflict cases; the dict is built + ready for HARDER content where meaning bites).
- **Cross-clause feedback ≠ role-persistence (interactive loop v1 NULL, load-bearing):** biasing the current clause's role toward an entity's PRIOR role HURTS (even oracle) because multi-event roles VARY across clauses. The brain's cross-clause signal is COHERENCE/identity, not persistence. => the situation-model's real cross-clause job = **tracking WHO across the discourse (coreference) + accumulating events**, NOT re-biasing per-clause parsing.
- **Self-correction is NULL (yet):** the reader can only partially self-detect its errors (~46%, best signal = its own confidence AUC 0.76) and can't yet self-correct (net 0) — short simple McGuffey gives no strong internal coherence/error signal. Self-learning needs RICHER/LONGER content where coherence conflicts arise.
- **⚠️ 4 director over-reads caught + corrected this session** (coverage-hypo; "inversion" was really argmax-mislocalization; "wall broken" deflated to MEASURED_MECHANISM; "interactive loop" was really within-sentence construction cues). VET/audit discipline working; report MEASURED not READ.

### 🧭 SESSION CONSOLIDATED STATE (2026-08-02 ~21:1XZ) — MECHANISMS BUILT+WIRED; CONTENT-RICHNESS IS THE RECURRING WALL.
**The comprehension MECHANISM STACK is built, VET'd, WIRED into hdlab, and banked (atoms 29613-29625):** coref resolver (hdlab/coreference_resolver.py: match-or-allocate + possessive-fixed gender + strict_cb + Principle B + speaker-deixis canonical + flag signals), situation-model accumulate organ (hdlab/situation_model_accumulate.py), the metacognitive FLAG layer (name margin AUC 0.80 / pronoun n_compatible AUC 0.72), and the self-improving-loop CONTROLLER (hdlab/self_improving_loop.py: gold-free coherence-gated keep/revert autonomous router). Loop engine CERTIFIED across 3 cycles (4 fix-levers, 2 falsified). Brain-fidelity audited (Principle B FAITHFUL; rest APPROXIMATING w/ 2 named debts).
**THE RECURRING WALL (5 independent collapses this session): McGUFFEY IS TOO SIMPLE to REQUIRE or POWER the advanced competencies.** (1) coref collapsed to recency-floor until dense multi-entity mining; (2) situation-model value invisible until the identity-demanding query subset; (3) autonomy reliable ONLY on dense g5g6, content-gated on sparse; (4) pronoun/self-correct/dictionary all null on thin McGuffey; (5) causal inference COLLAPSES TO ADJACENCY (207/208 links clause-adjacent = trivial baseline ~99.5%; connective-cued task needs no mechanism). => the mechanisms WORK where the test REQUIRES them (dense/identity-demanding); McGuffey mostly doesn't require them.
**=> STRATEGIC INFLECTION: the highest-leverage next move for the WHOLE program = RICHER/HARDER CONTENT AT SCALE (the USER's 'feed it a shit ton of content' vision, now overdetermined).** It unblocks: autonomy-at-scale (the real self-sustain test), causal inference (needs multi-candidate-distractor or connective-blinded framing, or a harder corpus), and all advanced competencies. Options: (a) aggressively reframe+mine McGuffey hard subsets (distractor causal paragraphs, multi-entity dense scenes); (b) adopt a richer public-domain narrative corpus. USER STEER REQUESTED on corpus direction (big investment, their vision territory). NEW gold this session: gold_causal_relations_v1.jsonl (n=208, verbatim, unverified, collapses-to-adjacency), gold_combined_pronoun_powered_v1.jsonl (36 passages, powered coref).

### ✅ COREF / SITUATION-MODEL ARC — RESOLVED (2026-08-02 ~21:XXZ). "EARNED IN KIND, NOT YET IN DEGREE."
The full arc from coref -> wired situation model, VET'd + honest. Atomization in flight (skunkworks). Commits all local on dataprep/mcguffey-graded-corpus, NO push.
- **Coref EARNABLE** (B3 HARD_PASS, name 0.89; commit 27e10d3a8). **Possessive-gender fix** (a0aac7eeb): head-noun governs NP gender (two root causes: infer_nominal_gender whole-span cue union + is_pronoun_mention misclassifying "his mother"); dense B3 0.843->0.872, merges -33%, 208 tests pass.
- **Pronoun coref**: coarse role-prominence Centering = NULL (5639fe312; hard cases are AGENT-vs-AGENT, prominence tied -- honest null, right diagnosis) -> **STRICT immediate-clause Cb (literal Centering)** = WIN (5b266248f): pronoun-B3 0.666->0.703 (g5g6 0.695->0.722), name+overall up, opt-in/reproducible. The RIGHT lever, once content was powered.
- **Content unblocked**: rejected PARAPHRASED v1 mining (would test a rewrite); probe showed corpus NOT the limit (170+300 male-pronoun-dense verbatim scenes in g5/g6); verbatim re-mine w/ programmatic SUBSTRING-GUARD; DIRECTOR-VERIFIED all 19 (dropped 1 real-error passage + 4 spurious mentions). Combined POWERED eval (0b7ff5d42): 36 passages, 81 pronoun mentions (~5x power). Existing gold confirmed verbatim (prior results stand).
- **Wired coref -> accumulate situation model** (e6a3a9ee8): on the IDENTITY-DEMANDING query subset (44% of queries = >=2 entities in the query clause, where identity is REQUIRED) coref BEATS both fair floors -- strict_cb 0.719 / earned 0.684 vs recency 0.561 / singleton 0.386 (powered; g5g6 +0.24). Recency collapses 1.0(trivial)->0.561(identity-demanding) = it was coasting on single-event clauses. => coref VALUE PROVEN where identity matters (milestone EARNED IN KIND). BUT strict_cb 0.719 is 0.21 BELOW oracle 0.930 => remaining gap = COREF QUALITY (pronoun split/link errors) = the clean measurable next target (NOT milestone_met in degree). Organ capacity FINE: oracle 1.0 on >=8-clause passages (extends atom 29609 past chain-length 2-3).
- **Flag-layer calibration** (c6eb94467): coref decision-MARGIN predicts its own errors, name-path AUC 0.753 (n=182), flags 92% of name errors at 2x base precision => the self-improving-reader's cheapest flag signal WORKS on the dominant path. Pronoun-path calibration content-underpowered (n=16).
- **METHODOLOGY (load-bearing, VET discipline paid off repeatedly)**: caught 3 metric-artifacts (singleton games per-mention decode=1.0; recency games the collision-SKIP; the FAIR test = identity-demanding subset) + a confounded calibration label (global-purity-on-local-decision; fixing it flipped AUC 0.480->0.753 = a false FAIL) + rejected paraphrased gold. Report MEASURED not READ.
- **SELF-IMPROVING-READER ARC STARTED (2026-08-02, follows the resolved coref arc):**
  - **FLAG LAYER CALIBRATED both tiers** (commit 150058b03, powered eval): NAME-path decision-margin AUC 0.80 (up from 0.75, generalizes); PRONOUN-path raw margin FAILS (0.54-0.63) but #compatible-candidates (n_compatible) AUC 0.72 = the CORRECT pronoun flag signal (referential-ambiguity structure). The reader can SEE its own likely errors on both paths.
  - **LOOP CYCLE 1 = NULL_FIX_MECHANISM (clean decomposition, commit 82492af76):** FLAG earned + localizes (pronoun error rate rises with n_compatible: nc3=44.8% vs base 26.3%; threshold>=2 captures 85% of errors; flag-localization control selective==uniform). But the FIX FAILS: topic-continuity (Centering Continue) applied to flagged cases HURT (pronoun-B3 0.703->0.677, identity-demanding 0.719->0.684 AWAY from oracle; corrected 0/broke 3 of 6 divergent). MECHANISM: flagged same-gender pronouns refer to a NON-topic entity (Centering SHIFT) -> both glass-box levers (most-recent-agent, topic-continuity) systematically point wrong -> these need VERB-SEMANTICS / selectional-preference / world-knowledge.
  - **=> THE LOOP DECOMPOSES: FLAG earned; FIX = the open frontier = SUPPLIED semantic knowledge** (maps to USER steers: "just give it a dictionary" + "flag unknowns -> tiered research"). Brain-faithful (semantics resolves referential ambiguity) + allowed (supply knowledge OK, mechanism earned).
  - **NEXT (strategic, USER-relevant -- what knowledge to supply):** build the FIX tier = supplied verb-argument / selectional-preference knowledge (which same-gender entity is the plausible agent/patient of the verb), triggered by the flag (n_compatible high) = the "tiered research" step. Then re-run loop cycle 2. ALTERNATIVE next levers still open: close coref-quality gap directly; scale the loop to more content/competencies. (strict_cb 0.703 remains best coref; atoms 29613-29617 + calibration/loop findings banking.)
### 🎯 CURRENT FOCUS / NEXT (USER-confirmed direction "A" = earn coref; probe LANDED)
**EARN COREFERENCE — PARTIALLY EARNABLE (probe abaf3f4e, commit 68d25389b):** learnable identity-tracking (name/pronoun/salience → match-or-allocate, reusing hdlab/state_of_mind.py salience/Centering primitives; NO bolt-on coref tool) = B-cubed F1 0.850 vs recency-floor 0.858 vs random 0.652. Beats random ✓; 0.8pp BELOW recency-floor ✗ — real PRECISION advantage (+7.6pp, separates distinct entities) but UNDER-MERGES (misses "The girl"→"Ernestine"→"she" APPOSITIVE-NAMING chains = a targeted bridging lever, NOT a ceiling). ⚠️ LOAD-BEARING CAVEAT: the long eval averages only 1.46 entities/passage (10/24 = single-entity), so "chain-everything" trivially wins (recall 1.0) — the recency-floor is unbeatable because the CORPUS IS ENTITY-SPARSE, not because coref lacks value.
### 🟢 COREF FAIR-TEST = HARD_PASS (2026-08-02 ~19:3XZ, exp_earn_coref_match_or_allocate_dense_v1, commit 27e10d3a8, director-VET'd) — FIRST clean fair-test win of the coref/situation-model arc. On the DENSE eval (3.67 entities/passage): learnable F1 0.843 vs recency-floor 0.462 vs random 0.526 (beats both by +0.38/+0.32). ✅ FAIR-TEST PRECONDITION CONFIRMED (what makes it trustworthy): the recency floor COLLAPSED 0.858(sparse)->0.462(dense), BELOW random — chain-everything is actively harmful with 3-5 co-present entities, as designed; learnable stays FLAT 0.84 as entity-count rises (3-ent 0.84 / 4-5-ent 0.84) while the floor degrades = genuine identity-tracking, not a one-bucket artifact. HONEST SCOPE: win is NAME-driven (name F1 0.891 strong); PRONOUN coref still the hard weak part (0.534, N=28 small). Dominant error = MERGES from a SPECIFIC CHEAP BUG: possessive NPs ('his mother') inherit masculine gender from 'his' not the head noun 'mother' -> false-merge with 'Harry' (fix: gender from head noun; actionable, not a ceiling). N=18 small but +0.3 margin not marginal. => COREFERENCE IS EARNABLE + VALUABLE, fairly demonstrated (our own match-or-allocate, no bolt-on tool). The convergent arc PAID OFF: 5-probe 'too thin' finding -> density scan -> dense mining -> fair test -> real win. NEXT LEVERS: (a) CHEAP: fix the possessive-NP-gender bug (reduces the dominant merge errors); (b) pronoun coref (the hard part); (c) WIRE earned coref -> accumulate organ = the REAL situation model (name coref 0.89 is good enough to carry entities). -> atomize (first fair-test coref pass).

### 🚨 CONVERGENT META-FINDING (5 probes, robust): McGUFFEY IS TOO SIMPLE/THIN to develop OR fairly measure the advanced competencies. Dictionary=null (no animacy conflicts), interactive-loop=null (short passages, no cross-clause signal), self-correct=null (no coherence signal in 2-clause sentences), coref=can't-beat-trivial-floor (1.46 entities/passage). => THE NEXT REAL MOVE = DENSER/RICHER CONTENT: passages with MULTIPLE co-present same-gender entities + coherence conflicts + meaning-required cases, where coref/cross-clause/meaning/self-learning all become learnable AND measurable. This is the USER's "feed it a shit ton of content" vision, now FORCED by the evidence.
**NEXT:** (1) ✅ DENSE MULTI-ENTITY SUBSTRATE MINED (density scan: McGuffey HAS it in group scenes/dialogues, no new corpus needed yet; agent a107c9de mined 19 candidates -> data/eval_gold_mention_role_mcguffey_v1/gold_multientity_dense_candidates_v1.jsonl, commit 9bf2b7070: 18/19 have 3+ co-present entities, 19/19 SAME-GENDER pronoun-ambiguity = the genuine coref test, 16/19 multi-speaker dialogue; hard subtypes flagged: cross-species 'he', same-noun-diff-referent, relation-introduced antecedents). gold_verified:false -> NEEDS DIRECTOR VERIFICATION (careful — multi-entity coref clusters across group scenes; do NOT rush). (2) verify -> dense eval -> RE-RUN COREF (fair test: recency/chain-everything floor is now BEATABLE) + the appositive-naming bridging lever; (3) wire earned coref -> accumulate = real situation model; (4) FLAG-AND-RESEARCH metacognitive layer; toward the SELF-IMPROVING READER. Cleanups: canonical-default eADM into the learned gate; learned accept-rule/boundary; amend atoms 29604-06 framing. (If even the dense McGuffey scenes prove too easy/thin, THEN source a denser public-domain corpus — but density-scan + this mining say McGuffey's group/dialogue scenes suffice for the fair coref test.)

### ⚙️ INFRA / OPS
GPU available when a probed direction warrants it (probe-to-aim before big investment — USER discipline). exp_dev anti-patterns to avoid: nohup-detached runs + `timeout`-wrappers (both caused failures this session — use single clean runs; read results off disk). git commits sometimes time out under load (--no-verify + verify-after). Store LOCAL-only, git-commit after each bank, NO origin push w/o in-session USER auth.

---
[HISTORY: the full session blow-by-blow (WM-allocate refutation, the extraction-fix iterations, the dictionary/bootstrap/self-detect/self-correct nulls, the brain-fidelity audit) is in git log + atoms 29594-29612. Earlier pre-comprehension-pipeline state (encoder-lever campaign, multi-competency engine) is in git history before 2026-08-02.]


---
**🟢🧱 RULER READY (2026-08-04 ~13:0XZ, commit 6afbdc2a7, DIRECTOR-VERIFIED at spot-check): grounded-comprehension eval v4 = 14 category-labeled real-verbatim items across all 7 grounded-knowledge types** (physical_harm 2, multi_candidate_attribution 2, counterfactual_cause 2, out_of_span_cause 2, beneficiary_vs_patient 2, irony 2, goal_blocking 2). data/eval_gold_mention_role_mcguffey_v1/gold_grounded_comprehension_v4_DRAFT.jsonl (all gold_verified=false). Verbatim 100% substring-guard, zero span reuse, position-varied (6 before-query / 4 after-query of the 10 attribution items — fixes the detective-v3 always-last confound), MEASURED baseline-defeat (recency-wrong 11/12, surface-nonseparating 12/14). 3 items HOLD (Director): grapp_v4_002/004 surface-leak, grapp_v4_010 recency-coincides -> excluded from strict scoring, kept as stretch. Sources: Anne of Green Gables/Tom Sawyer/Wizard of Oz/Little Women (Gutenberg). MERGE: extends the Director-verified richer_v1 (15) + v2 (007/008/009); 006 excluded. => COMBINED categorized RULER ~29 items across 7 categories = the instrument for the foundation-building phase (measure per-category coverage as grounded knowledge is supplied). PHASE track-2 (eval) DONE; track-1 (supply grounded knowledge at breadth) begins with the current-foundation baseline measurement across all categories.

---
**🗺️ FOUNDATION COVERAGE BASELINE (2026-08-04 ~13:1XZ, commit b4a26d00d, DISK-VERIFIED 5 seeds): the current foundation has ONLY harm-knowledge -> covers ONLY direct-harm-act causal attribution (3/3 where applicable), ABSTAINS 71% elsewhere.** Per-category EARNED (harm-read-ONLY, uniform): physical_harm 0.333 (only category above both deterministic floors), multi_candidate 0.25, counterfactual 1.0(n=1 noise), out_of_span 0.0, goal_blocking 0.0. Overall EARNED 0.214 < RANDOM 0.443 — NOT 'worse than random capability': it's 71% abstain (harm-read only commits on direct-harm; sound where it applies, direct_harm_act_cause_coverage=1.0). HONEST DEVIATIONS/CAVEATS: (1) harm-read ONLY — effect-match/agent-attribution/cross-span-binding NOT uniformly combined (out_of_span 0.0 may be an ASSEMBLY gap — binding exists — not purely knowledge). (2) STRUCTURAL BOTTLENECK EXPOSED: per-category ruler n=1-4 = TOO THIN to measure a per-category grounded-competency build with power. FINDING: each non-harm causal type (provocation/bribe/dare/omission/misrepresentation/goal-blocking/irony/counterfactual) needs its OWN grounded causal-schema competency = the multi-category foundation to build (matches comprehension=growing-library-of-competencies). STRATEGIC FORK (needs USER steer): the foundation-build is multi-session AND the per-category ruler is too thin to measure it -> (A) invest in a much richer PER-CATEGORY eval (corpus-limited, big data effort) before building competencies, vs (B) build each grounded competency in a CONTROLLED powered setting + thin real-text transfer-probe (the proven harm-read pattern: supply grounded knowledge -> brain-faithful mechanism reads text), accepting thin per-category real-text signal. Recommend a hybrid: build the next competency (GOAL-DIRECTEDNESS = most brain-foundational, core ~6yo, grounds the appraisal/anger chain) grounded like the harm-read, + grow that category's eval items in parallel.

---
**🧠🔁 SELF-EXTENSION ARCHITECTURE (USER-raised "can't baby-step this; make it grow on its own"; prior-art drill commit-in-note research_self_extending_grounded_knowledge_prior_art_2026-08-04.md).** Prior art gives a proven self-extension PATTERN (NELL 2010-2018 seed->multi-view-extraction->ANTI-DRIFT-constraint-check->promote->repeat; Piaget disequilibrium-triggered accommodation; Oudeyer-Kaplan learning-progress gap-selection) but ZERO published system closes the loop for CAUSAL schema induction (all needed a human at the minting step OR induced temporal/co-occurrence not causal) = a genuine NATIVE-BUILD gap, not a borrow (good — glass-box territory). We already own 2 of the 3 organs (self_improving_loop = coherence-gated coupling-check; situation_model_accumulate = relational store). MISSING: (1) a schema-MINTING operator (propose a NEW role/relation-type, not just re-decode existing); (2) a genuine SECOND INDEPENDENT VIEW to couple against (our coherence-gate checks a candidate against ITSELF = the exact drift vulnerability NELL solved with independent cross-checking). THE CRUX RISK = semantic DRIFT (NELL's hardest lesson); the anti-drift guard = a second independent view (candidate discourse-cue causal-typing) + false-consolidation gate. REFRAME (USER-approved): next build is the SELF-EXTENSION LOOP (detect-gap -> induce-schema -> anti-drift-consolidate -> re-read), NOT hand-built competency #2; goal-directedness = the FIRST TEST CASE of self-extension. GATE FIRST (running, exp_disequilibrium_novelty_signal_test_v1, ~1hr no-new-build): does the coherence-margin our self_improving_loop ALREADY computes distinguish 'needs a NEW causal schema' from 'fits existing' on the 18 causal items (labeled BLIND-to-margin)? HARD_PASS (gap>=0.05, non-overlap IQR) => disequilibrium signal is free, minting=routing action; HARD_FAIL (overlap>50%, MW p>0.10) => build a dedicated novelty/prediction-error detector first. This gates the whole self-extension build.

---
**✅🧠 DISEQUILIBRIUM GATE = HARD_PASS on the brain-faithful predictive-coding residual (2026-08-04 ~14:1XZ, commit a8dbf47, DISK-VERIFIED 30 seeds; cell exp_disequilibrium_novelty_signal_test_v1).** The brain's novelty organ (hdlab/predictive_coding.py, Friston/Rao-Ballard: predict via W = schema-library, residual = prediction-error) SEPARATES needs_new_schema (0.361) from fits_existing (0.089) on 18 blind-labeled causal items: gap 0.272, MW p=6e-4, IQR overlap 0.000 = HARD_PASS, and GRADED by feature-overlap (staged-suicide-over-real-kill lowest needs_new 0.189; pure-counterfactual highest 0.40-0.48) = real signal, not tautology. Residual > coherence-margin (whose separation is a near-tautological 2-filler exact-0 collision). Director-verified the 7 fits / 11 needs_new labels at spot-check (2 borderline flagged: det_009 deception-vs-physical, det_004 instrument-vs-direct). => WE HAVE THE NOVELTY/MINTING TRIGGER: the predictive-coding residual against the schema library — no bespoke novelty metric needed. ⚠️ LOAD-BEARING BOUND (agent-flagged, disk-confirmed in caveats): the residual SCORES novelty GIVEN a causal-feature TYPING, but the typing was DIRECTOR-SUPPLIED, not read from text — same shape as every prior result (mechanism works GIVEN structure; extracting structure from raw text is the wall). ROUTES: the remaining build is NOT a novelty detector — it is the TYPING PROPOSER (read passage -> causal-feature structure = the "second independent view" / note Gap 1), which the residual gates. SELF-EXTENSION LOOP ARCHITECTURE now concrete: typing-proposer (read->causal features) -> predictive-coding residual GATE (HAVE it) -> mint new type -> anti-drift consolidate. The typing-proposer IS the grounded-comprehension reader (harm-read = partial typer) = convergence, not a new front. Current role vocab is thin: {agent/patient/theme/recipient/addressee/speaker}+{CAUSE/EFFECT} — NO distinct causal-role TYPE for bribe/dare/provocation/omission/deception/counterfactual/social (they collapse to generic agent/CAUSE). NEXT: HOLD typing-proposer build for the brain-component MAP (reuse-don't-reinvent — may already own a typer organ, as we owned the novelty organ).

---
**�officeMILESTONE🧠🔁 FIRST SELF-EXTENSION LOOP WORKS (controlled set) + ANTI-DRIFT GUARD VALIDATED (2026-08-04 ~15:5XZ, commit d3f5c3b65, DISK-VERIFIED 8 seeds; cell exp_self_extension_loop_v1).** The full loop runs end-to-end: TYPE passage causal-structure -> predictive_coding residual flags 'schema library can't explain this' -> MINT a new causal-role type (native-new operator) -> a STRUCTURALLY-INDEPENDENT 2nd view (connective/purpose cue, zero lexicon overlap w/ residual) disposes -> self_improving_loop abstain-band consolidates on >=2 confirmations -> re-read. Minted {BLOCK_ACT,GOAL_OBJECT,GOAL_OWNER,GOAL_UNMET} from 8 confirming passages (residual margin 0.36), seeded with NO goal type. CONTROLS (8 seeds, all 1.0, disk-verified): C1 noise-no-mint (0 spurious), C2 redundant-no-mint (harm residual ~0.05<gate), C3 utility-lift (goal-block attribution 0->1.0, residual 0.37->0.20 post-mint, no harm regression), C4 ANTI-DRIFT ABLATION (THE key result): residual-ONLY mints 3 types (real + 2 spurious drift {MOTION,SCENERY,WEATHER}); FULL (with 2nd view) mints ONLY the 1 real type => the 2nd independent view is LOAD-BEARING (NELL anti-drift validated in-substrate). Noise: residual-fire 100% but 2nd-view-cue 0% -> rejected. => FIRST EVIDENCE THE FOUNDATION CAN GROW ITSELF WITHOUT DRIFTING. ⚠️ HONEST BOUNDS (agent-surfaced, VET-confirmed): (1) CONTROLLED synthetic set — the ONE real ruler item mcca_004 (implicit goal-block 'let her take care of herself', no connective) trips residual 0.373 but the cue-based 2nd view does NOT fire -> would NOT mint => real-prose self-extension is BOUNDED by the 2nd-view's recall on naturalistic prose; (2) TYPING is SUPPLIED (glass-box lexical map), not induced from raw text. => CONVERGENCE: novelty/mint/anti-drift/consolidate/re-read ALL WORK; both bounds reduce to the SAME recurring wall = READING CAUSAL STRUCTURE FROM RAW NATURALISTIC PROSE (the typing-proposer + a GROUNDED 2nd view that catches implicit goal-blocking, not surface connectives). The whole self-growing-foundation program has converged to that one problem. NOT wired to hdlab yet (promote after the 2nd view is real-prose-robust). NEXT: the grounded causal-structure reader (typing-proposer + grounded 2nd view for implicit relations) = the standing event-extraction bottleneck, now the single gate to a self-growing foundation.

---
**🏆🧠🔁 REAL-PROSE SELF-EXTENSION WORKS + independent grounded anti-drift (2026-08-04 ~16:1XZ, commit 7f534b7f3, DISK-VERIFIED 8 seeds; cell exp_self_extension_grounded_realprose_v1).** Replaced v1's connective 2nd view (recall gap that failed mcca_004) with a GROUNDED goal-OUTCOME appraisal (reuse AccumulateRegister bit-identical, MET/UNMET valence). CRUX (disk-verified): the two grounded views are MEASURED INDEPENDENT — INDEP_co_fire_noise_zero=1.0, INDEP_co_fire_harm_zero=1.0; on noise (seeded w/ outcome-trap words sank/fell/lost) view1-residual fires 1.0 but view2 fires 0.0 -> co_fire 0.0 (goal-schema requires animate desirer); on harm, view1-struct false-fires 0.30 but view2 rejects -> co_fire 0.0 (view2 load-bearing). REAL PROSE: mcca_004 (implicit 'let her take care of herself', v1 FAILED) now passes both gates -> MINTS; theatre_refusal (Little Women verbatim) co-mints (1 of 9 confirmations); honest boundaries reported (book_burning spite-by-destruction correctly NOT typed by withhold-view; real harm mcca_001/005 residual-low no-mint). Controls all hold (8 seeds): C1 noise-no-mint 1.0, C2 harm-no-mint 1.0, C3 utility gb_attrib 0->0.86 no-harm-regression, C4 ablation drifts (residual-only mints spurious MOTION/SCENERY/WEATHER; full rejects). => THE SELF-EXTENSION MECHANISM (novelty-detect -> mint -> independent-grounded-anti-drift -> consolidate -> re-read) IS VALIDATED END-TO-END ON REAL PROSE. ⚠️ THE SINGLE REMAINING BOUND (crisp): the grounded knowledge + typing that let the views RECOGNIZE the pattern are SUPPLIED (goal-schema lexicons; mcca_004 desire via gold goal-description), NOT induced from raw text. So the MACHINERY that grows the foundation works; the wall is INDUCING THE GROUNDED KNOWLEDGE IT GROWS ON, autonomously from raw text (the standing event-extraction/grounded-reading bottleneck, localized all session). PROGRAM DISTILLED: reader spine + novelty organ + minting + anti-drift ALL WORK; the one frontier = read grounded causal/goal structure from raw naturalistic prose WITHOUT a supplied lexicon. NOT wired to hdlab yet (promote when knowledge-induction advances). Routed sub-steps: extend view-1 to spite-by-destruction subtype; scale real implicit-goal-block items beyond n=6; the DEEP frontier = autonomous grounded-knowledge induction from text.

---
**🧭🧠 INDUCTION FRONTIER SPLITS (2026-08-04 ~16:3XZ, prior-art drill research_autonomous_grounded_knowledge_induction_prior_art_2026-08-04.md) — vindicates the founding pivot at the literature level.** (1) Our MINT operator = Harnad/Cangelosi "symbolic theft" grounding-transfer = VALIDATED, not new. (2) STRUCTURAL/lexical growth (which words instantiate a grounded pattern) = TEXT-INDUCIBLE via glass-box Resnik SELECTIONAL-ASSOCIATION (pure corpus counting, no borrowed model). (3) LOAD-BEARING CAP (Andrews/Vigliocco/Vinson 2009, field-wide unresolved): AFFECTIVE/experiential meaning (harm-feel, valence) is NOT inducible from text distribution — selectional preference recovers 'verbs that take these roles' NOT 'verbs that mean withholding'. => THE SPLIT: affective CORE = an EARNED/grounded SEED (bounded ~6yo core, NOT text-induced) = 'you can't learn revenge from a book' now a literature finding; STRUCTURAL lexicon = INDUCED from text (selectional-association) gated by the independent grounded VIEW for affect. Path to a self-growing foundation = small EARNED affective seed + glass-box selectional-association INDUCER (grows the lexicon from text) + the validated self-extension loop (mint + anti-drift). Blum-Mitchell independence conditions for text-only co-training are never field-verified; our co-fire=0 measurement already matches field-standard. NEXT (running, exp_lexicon_induction_selectional_association_v1): given a TINY seed of grounded withhold-verbs, can selectional-association RECOVER the held-out withhold-verbs from raw corpora (recall@10>=0.3, noise-FP<3) + gate by view-2 affect + does the loop STILL mint goal-blocker with the INDUCED (not supplied) lexicon? HARD_PASS = the supplied lexicon is replaceable by an induced one = the loop becomes truly self-growing. HARD_FAIL = affect-cap confirmed -> fold view-2 into induction-time score (pre-specified fallback). P_deflated 0.35.

---
**🔴 LEXICON-INDUCTION FROM TEXT = HARD_FAIL (agent caught its own FALSE PASS; 2026-08-04 ~16:5XZ, commit a8b65b4cd, DISK-VERIFIED: recovered_target_lemmas=[], affect_gate_degenerate=true).** The pre-registered bands LITERALLY passed (recall@10 0.375, FP 0, loop mints 1.0) but VET-as-hard-as-negative exposed a FALSE PASS on 3 artifacts: (1) affect gate (VIEW-2) INERT — fires ~1.2% on ANY sentence, 1.4% (2/142, both spurious) on withhold sentences = the Andrews/Vigliocco distributional-affect cap; (2) selectional scorer DEGENERATE — negative agent-animacy term (windowing artifact), real targets hide/neglect rank below noise, #1 is a lemmatization artifact; (3) induction NEVER fed the loop — recovered=[]; the mint was VACUOUS (carried by the seed + unpatched OMISSION lexicon, no induced item did work). CORRECTS the prior turn's over-optimism ('architecture fully specified, last question under test'): the induction FAILED. TWO parts: (a) selectional scorer = DEGENERATE/broken-experiment (impl artifacts -> doesn't cleanly refute structural induction; re-testable with a fixed scorer, but structural-only recovers 'verbs taking these roles' not 'verbs meaning withhold' = the cap); (b) THE DEEPER STRUCTURAL FINDING: the grounded reads are INERT AT DISCOURSE SCALE — VIEW-2 works on CURATED adjacent goal-block passages but does NOT fire on naturalistic prose where DESIRE + OUTCOME are DISPERSED ACROSS SENTENCES, not adjacent to the verb. => THE WALL SHARPENED to its deepest form: grounded structure in real prose is DISPERSED across the discourse; the reader must INTEGRATE it via situation-model accumulation, NOT read it locally. This is the recurring event-extraction wall, now precisely = DISCOURSE-SCALE GROUNDED INTEGRATION. ROUTE (brain-foundational): make the grounded appraisal/goal reads operate over the ACCUMULATED situation-model (integrate dispersed desire/action/outcome across sentences via the wired situation_model_accumulate hub + maybe the islanded working_memory multibank), THEN re-test discourse-scale firing + the fold-in induction. HONEST STATUS: the self-extension MACHINERY works (validated); the GROUNDED READING it depends on does NOT yet work at discourse scale on naturalistic prose = the single wall, sharpened. VET discipline WORKED (agent rejected its own false pass, disk-confirmed).

---
**🧠🎯 DEEP BRAIN DRILL adjudicates the discourse-scale wall = REPRESENTATION GAP (c), not wiring (a) or missing-hierarchical-window (b) (2026-08-04 ~18:4XZ, disk-verified; note research_discourse_scale_grounded_integration_brain_drill_2026-08-04.md).** CRUX (disk-verified tools/read_anne_glassbox_v2_honest_ledger.py:428): the LIVE situation-model register role_vocab=["agent","mentioned"] — NO goal/causation/OUTCOME slot; AccumulateRegister KeyErrors on unknown roles. So grounded appraisal has NOTHING DISPERSED TO INTEGRATE even if wired to the accumulated register — the DIMENSIONS aren't represented. Brain: Zwaan event-indexing tracks goal/intentionality/causation/outcome dims across the discourse (DMN situation model); we track neither. FIX = REPRESENTATION EXTENSION (not a new organ, not a hierarchical-window build): add GOAL+OUTCOME_VALENCE dims to the situation-model register, MIRRORING how CausalLinkRegister added CAUSE/EFFECT on the SAME AccumulateRegister organ. Corrects the Director's 'mostly wiring' prior. Encouraging: the organ + the extension PATTERN are both owned. NEXT (running, exp_situation_model_goal_outcome_dimension_v1): accumulate GOAL/ACTION/OUTCOME across the discourse (dispersed, via coref) -> VIEW-2 reads goal-blocking OFF the accumulated register -> does it fire DISCRIMINATIVELY on naturalistic goal-block prose (>=0.5 goal-block, <0.1 controls) vs the 1.4%-flat local-window baseline? Bands: HARD_PASS=wall cracks; MIDDLE=fires-but-recency-confound (routes goal-outcome-binding-across-sentences selector, designed against recency per coref falsification); HARD_FAIL=still flat=deeper. P_deflated 0.40.

---
**🟡✅ SITUATION-MODEL GOAL/OUTCOME EXTENSION = MIDDLE_BAND: representation-gap CRACKS, recency-binding is the remaining piece (2026-08-04 ~19:0XZ, commit fb5b2a188, DISK-VERIFIED 5 seeds; cell exp_situation_model_goal_outcome_dimension_v1).** Built GoalOutcomeRegister(AccumulateRegister) — extended role_vocab [agent,mentioned] -> [GOAL,ACTION_AGAINST,OUTCOME_UNMET,OUTCOME_MET] + appraise(), MIRRORING CausalLinkRegister CAUSE/EFFECT on the SAME organ (reuse bit-identical). MEASURED: TREATMENT (grounded read OFF the accumulated register) fires 0.833 on naturalistic DISPERSED goal-block prose (5/6), 0.0 on ALL controls (goal-met/harm/noise); BASELINE (same read on LOCAL 2-sentence windows) fires 0.0 — dispersed beyond the window. => THE (c) REPRESENTATION-GAP FIX IS VALIDATED: tracking goal/outcome dims across the discourse makes appraisal fire where a local window is FLAT (0.833 vs 0.0 vs the 1.4% baseline, controls clean). ⚠️ REMAINING PIECE (pre-registered MIDDLE outcome): recency_binding_accuracy 0.333 — when COREF must bind the dispersed goal->outcome across sentences, the recency-shaped resolver grabs the recent FOIL (pronoun traps fail; the non-trap passes = metric live). Reproduces the coref recency falsification (_pick_strict_cb 0/4, 2026-08-03) one level up. 🔁 THE RECURRING STRUCTURAL GAP (now 3x): binding/selecting the RIGHT antecedent by COHERENCE not RECENCY has appeared in (1) coref (_pick_strict_cb IS recency, falsified), (2) causal-coherence selection (M_backward v1-v4), (3) now goal-outcome discourse binding. The substrate keeps approximating with RECENCY-shaped selectors that fail the recency-trap. HIGHEST-LEVERAGE next build = ONE general COHERENCE-BASED relational binding/selection organ (hippocampal relational retrieval selecting by explanatory/goal COHERENCE, NOT recency) that serves ALL THREE — build on the prior coherence-selector arc (v2/v3 generalized via STRUCTURAL features). NEXT: the coherence-based dispersed-binding selector, designed against recency. HONEST: goal/outcome DIMS + discourse ACCUMULATION solved; COHERENCE-BINDING is the one recurring gap left.
