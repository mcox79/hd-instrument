# WHERE WE ARE NOW — clean current state (REWRITE each session) — updated 2026-07-29 (compaction-prep, brain-alignment focus)

## Direction (read FIRST, in order)
1. GOAL + invariants + disciplines: `notes/SUBSTRATE_CHARTER_read_first.md`
2. Plan: `notes/THE_PLAN.md`
3. This = live snapshot.
4. **METHODOLOGY: `notes/component_brain_fidelity_ledger.md`** (nail each component brain-faithful, judged on the BRAIN's metric not task-win).
5. **THE ANALYSIS (load-bearing): `notes/brain_foundational_component_analysis.md`** + `notes/drill_language_world_model_framing.md` + `notes/stateful_core_situation_model_build_design.md`.

## 🧠 THE MANDATE (USER, standing + emphasized 2026-07-29)
ALIGN TO THE BRAIN as much as possible to overcome the comprehension barrier — we've been trying to show comprehension for OVER A WEEK. Do the HARD thing, not easy paths (frozen-head shortcuts have failed repeatedly). Nail each component brain-faithful, then assemble. Do NOT lose focus or drift. [[feedback_do_the_hard_blocking_thing_not_easy_paths_easy_bias_disastrous_2026-07-29]]

## 🌙 ALL-NIGHT FULL-AUTO (USER-authorized 2026-07-30)
Drive autonomously all night. CADENCE per major result: (1) honest synthesis; (2) BRAIN-FOUNDATIONAL fidelity check of the components involved + honest LEVER re-eval (is this the right, most-effective path?); (3) update docs + heartbeat; (4) dispatch the next HARD step, never the cheap one. Two easy-path traps to guard: cheap-mechanism AND instrument-perfectionism. If a verdict is negative, go UP to the harder root cause (encoder/representation), not sideways into tractable tinkering. Choose the RIGHT path to project success, not the easy one.

## 🔀 PIVOT (2026-07-30) — CORRECTED by root-cause probe: the BLOCK is WM-DYNAMICS COLLAPSE, NOT the encoder
FIRST read (SUPERSEDED): "WM won't train at scale -> encoder is thin -> encoder-first." The root-cause probe REVERSED this. MEASURED (CPU, ckpt_seed_7 + bistable WM 3a457d414, MES-256, held-out 128): **ENCODER-ALONE linear probe = 0.8125 (the encoder CLEANLY carries the MES signal — NOT thin, NOT the block); bistable-WM slot_mean probe = 0.5469 (chance); slot_mean pairwise-cos = 0.974, var ~80x < encoder; addr_entropy var = EXACTLY 0.0 (addressing does NOT differentiate items).** => VERDICT = WM_DYNAMICS_COLLAPSE: the WM collapses the encoder's good signal to a near-constant vector.
**HONEST CORRECTION / likely OVER-CORRECTION:** the gen-curve showed the SIMPLER mean-pool WM fit @256 (0.985); we then stacked THREE changes at once (per-slot gating + role-query + bistable segmentation), ALL driven by the underpowered 64-item smoke (the audit's gap-C "mean-pool is the bug" was judged on 64 items = too small), and the elaborated WM COLLAPSES. We lost the one-variable discipline and may have broken a working mechanism.
**FREEZE FIX REFUTED (07-30, TRUSTWORTHY gate — the probe was optimistic):** the culprit-isolation probe suggested freeze->0.91, but the trustworthy hardened gate (frozen encoder, WM+judge, 2 seeds, held-out N=800, powered MDE=0.051, significance) = **MES eval 0.500 both seeds, loss 0.70, gap over random-init -0.013 (NOT significant).** The single-seed 0.91 did NOT reproduce; mean-pool-frozen=0.50 too (confirms MES is ORDER-CRITICAL, pooling destroys order). **KEY (BGE known-reader recal): MES BGE whole-sentence = 0.670 vs random 0.556 (+0.114) = HEADROOM -> the MES task IS SOLVABLE (~0.67); the block is OUR encoder+architecture, NOT the task/measurement.** (KD: BGE +0.074 = still too-hard-as-built -> KD needs a better construction.) LESSON: probes keep MISLEADING (optimistic/overfit/non-order-critical); route decisive Qs THROUGH the trustworthy gate, not cheap probes.
**LEVER LOCATED (07-30, trustworthy gate): STREAM-ARCHITECTURE, not encoder.** our-encoder WHOLE-SENTENCE = 0.766 trained / **0.80 random-init** (>= BGE 0.67); clause-mean/WM = 0.50. So the ENCODER IS FINE; the CLAUSE-SPLIT+WM pipeline DESTROYS cross-clause order (each clause-rep is blind to the others before the WM sees it). Fix = feed the WM the CONTEXTUAL token stream (whole-sequence attention preserved), not independent clause pools. latent-PC = NOT the MES lever (encoder fine) -> stays SECONDARY enrichment.
**DEEPER NUANCE (do NOT gloss): random-init whole-sentence = 0.80 >= trained -> MES order is RESERVOIR-decodable (untrained transformer's position-emb + attention).** => MES-as-constructed does NOT test LEARNED comprehension; an explicit WM adds NOTHING over whole-sequence attention for SHORT in-window passages. THE FORK RESOLVED -> design note notes/wm_value_regime_and_contextual_stream_design_2026-07-30.md (Director synthesis of 3 lit-scans): WM non-redundant regime = ONLINE/content-gating (NOT short-offline MES, where attention wins). Fix for the clause-blind bug = SHARED encoder pass over whole stream -> SLICE by event (each slice saw the others) -> feed SEQUENTIALLY to a content-gated entity WM (EntNet/PBWM).
**RESERVOIR-FAILING TASK BUILT + CALIBRATED VALID (db39c1082, measurement-first): "Selective-Overwrite-Recall"** — stream of (slot,filler) events + many distractors, randomized positions, query the MOST-RECENTLY-OVERWRITTEN filler (requires overwrite-with-suppression = content-gating). MEASURED both seeds (chance=0.05): random-init reservoir + ALL naive shortcuts = ~chance (0.04-0.05); content-gated oracle = 1.00. Measurement-first caught + fixed 2 leaks that only surfaced at FULL scale (most-freq bias, present-vocab leak). => FEATURES sufficient (reservoir@last-write=1.0); the ONLY thing separating 0.05 from 1.0 is LEARNED content-gated selection = exactly the WM capability. First unconfoundable proof-of-WM test.
**WM_PROVEN — CONFIRMED (commit 88d050955; skunkworks landed-VET a81301a6 = CONFIRMED, bit-for-bit recompute, can-fail clean, leak-proof generalization, restart-robustness legit train-triggered, mechanism-class = genuine content-gated overwrite). FIRST VET-confirmed positive of the session. TWO MANDATORY CAVEATS: (i) INIT-FRAGILE ~40-50% duds (rough loss surface -> expect worse convergence scaling up; keep restart train-triggered only); (ii) the encoder here is DEGENERATE (each event cleanly carries slot+filler), so the WM did ONLY temporal integration/gating — NOT the NL BINDING problem, NOT our v2 encoder. This is a clean maintenance-dynamics isolation, NOT a comprehension result. Original claim detail:** the SIMPLEST content-gated WM (EntNet-style content-addressed slots + learned overwrite gate; RANDOM-INIT FROZEN encoder so ONLY the WM+readout learned; NO bistable/role-query stack — ONE VARIABLE) LEARNS Selective-Overwrite-Recall: eval 0.844/0.843 (seeds 7/13, train~eval => GENERALIZES) vs random-init-WM floor ~0.05 (5 controls) vs chance 0.05; gap 0.79, z~49. Both seeds (exp_dev did NOT claim from seed-7's lucky 1.0 — added restart-on-final-dud training-robustness for seed-13). Trainability lever: addr_temp=0.3 + lr=1e-2 (temp=0.1 stalls = sharp-from-step-0, the recurring lesson). **SCOPE (honest): this proves the WM MECHANISM learns content-gated overwrite-with-suppression (the situation-model's core maintain/update organ) on a SYNTHETIC slot/filler task with a RANDOM encoder, on a task a reservoir PROVABLY fails — a real mechanism milestone, NOT yet natural-language comprehension.** NL COMPREHENSION ARC (the real binding test): NL-rendered Selective-Overwrite-Recall CALIBRATED NL_RESERVOIR_FAILING_VALID (1e1a49e95): whole-seq-attention reservoir + all pooled readers (incl BGE) = chance, oracle keep-last = 1.0 — the answer exists but only a bind+gate mechanism gets it (MES's reservoir-trap did NOT recur). NL TEST 1 (pooled event-rep) = WM_NL_CANT_LEARN (f82370cd1, clean negative both seeds, STUCK_FLAT). LEVER DIAGNOSIS (measured, precise): block is the WM NOT the encoder — linear probe on frozen v2 POOLED event-rep recovers slot=1.0 AND filler=1.0 (info present+separable), but the single-content-address WM overfits (train 0.96/eval 0.07), can't learn a FILLER-INVARIANT slot-addressing rule from the ENTANGLED pool (fair-shot swept, all chance). = THE ROLE-FILLER BINDING PROBLEM: brain binds SEPARABLE role+filler (Frankland-Greene), a pooled bag-of-tokens destroys that separation.
NL TEST 2 (role-separated / token-level) = STILL_CANT_LEARN (b3e5c0b7f) — and the grounding probe is DECISIVE: the FROZEN v2 MLM encoder produces GLOBALLY-ENTANGLED token reps — slot-token->slot=1.0 BUT slot-token->FILLER=0.996; filler-token->slot=0.993. Self-attention blended slot+filler into EVERY token => NO role-separable substrate for ANY read-path. **=> THE BLOCK IS THE ENCODER (measured), converging on the FOUNDING DIAGNOSIS: a frozen feed-forward MLM/bidirectional encoder yields entangled bag-of-everything reps — fine for pooled tasks (MES 0.80), FATAL for BINDING (which needs role-separable reps, as the brain has). The proven WM works; it's been waiting for reps it CAN bind.**
**VET CORRECTION (skunkworks a1f31e21 = PARTIALLY): the "encoder must change" leap is REFUTED — the encoder-pretraining pivot is PREMATURE. New probes: held-out-filler slot decode = 1.000 (a LINEAR read recovers the filler-INVARIANT slot address for UNSEEN fillers); event->query cross-context slot transfer = 1.000. So the reps DO contain a filler-invariant, cross-context, GENERALIZING slot address — a read-path CAN extract it. THE REAL BLOCK: the slot signal lives in a LOW-VARIANCE SUBSPACE swamped by a large SHARED component; the WM's raw softmax(x·key) on UN-STANDARDIZED reps is dominated by the shared component -> near-identical address for all queries -> STUCK_FLAT. The WM key has the SAME linear form as the probe that succeeds => CONDITIONING/OPTIMIZATION gap, NOT encoder-expressiveness.**
**=> CORRECTED DIRECTION (the post-compaction focus): CHEAP READ-CONDITIONING fixes, NOT encoder pretraining. IN FLIGHT [abd107fa]: (1) WHITEN/standardize reps (remove dominant shared component) before addressing; (2) AUX slot-supervised address loss (CE address-logits->slot id); (3) WARM-START key from the working logistic slot-probe. VERDICT = WM_NL_PROVEN_VIA_READ_CONDITIONING (which fix -> NL binding solved CHEAPLY, encoder pivot NOT needed) / STILL_CANT_ALL_FIXES (THEN encoder-objective pivot is EARNED). WM mechanism PROVEN + encoder reps CONTAIN the binding signal; the gap is how the WM READS them. This is the frontier post-compaction.**
DEAD/DEPRIORITIZED: latent-PC (hung in data-prep ~5h, encoder adequate for MES anyway). 30-min SELF-DRIVE CRON now active (33e3a028: silent-death sweep + heartbeat + brain-check + advance) — created after latent-PC's silent death went unnoticed.
**SECONDARY (reframed): latent-PC encoder run [aa234b47, GPU, ~4-6h, running] is NO LONGER the MES fix (encoder is already adequate for MES at 0.81) — it is encoder-ENRICHMENT for HARDER tasks / KD bridging / generalization headroom + the brain-foundational forward-predictive objective. Valuable, keep running, but NOT the MES blocker.** Soft-floor/thinness may still bind on harder tasks, not on MES.

## 🧵 TWO PARALLEL TRACKS (separate axes, separate measurement — never confound)
- **Track A — WM / situation-model** (the built mechanism): gate-hardening (a53222d) -> RUN the bistable verdict on the hard-line. Judged on the MES/KD gate.
- **Track B — Encoder / representation** (the #1 ceiling): lever ranking (a444ba4c) -> build top lever (latent-PC). Judged on rep-quality (graded geometry / relational probe / random-init baseline).
- RULE: parallel DEVELOPMENT ok; NEVER change encoder + WM then read one gate (attribution death).

## 📉 ENCODER-TRACK VERDICT (3 lit-scans, VET'd, 2026-07-30)
Scale-floor question is genuinely UNRESOLVED in the literature, BUT the closest anchor (BabyLM, 10-100M words ≈ our regime) shows clever objectives/curricula/architecture give REAL but BOUNDED gains — they do NOT close the gap to large-corpus richness. => SOFT FLOOR likely: encoder cleverness makes reps less thin but won't reach brain-level on 158M tokens within invariants. STRATEGIC IMPLICATION (validates USER roadmap): the ceiling-raiser is GROUNDING EXPANSION (adds information beyond the text floor), NOT encoder tricks alone (which extract more from what we have). Ranked levers: (1) latent-PC/JEPA [= our banked forward-predictive fix, OOM-free, top]; (2) multi-timescale hierarchy (Hasson/HM-RNN); (3) slow-feature aux; (4) better data/curriculum (BabyLM lessons); (5) k-WTA DEPRIORITIZED as a richness lever (gain is compactness not sample-efficiency — folk wisdom oversold). Collapse guard across all = VICReg variance-floor; collapse risk is capacity-RATIO-dependent (watch: is 512d over-capacity for 158M?). Full note: notes/encoder_representation_lever_ranking_2026-07-29.md.

## THE GOAL
A glass-box VSA/HDC substrate you can CONVERSE with that genuinely REASONS/COMPREHENDS, earning meaning the brain's way (NO borrowed embeddings/LLM at inference; NO bolt-on reader). Foundation (CSKG, 1.24M edges) is BANKED; the frontier is COMPREHENSION.

## 🧭 THE CONVERGED DIAGNOSIS (the barrier, named precisely)
Our encoder is **FEED-FORWARD + BIDIRECTIONAL (MLM) + STATELESS**, where the brain is **RECURRENT + FORWARD-PREDICTIVE + STATEFUL**. A feed-forward net computes a static function of a fixed window; it CANNOT maintain/update a situation model — so comprehension can't be decoded out of it, because it was never CONSTRUCTED. **THE RECURRING ERROR behind a week of failures: we kept bolting ISOLATED pieces onto a FROZEN feed-forward encoder** (contrastive objectives, slot-memory compressed to scalars, position-bind readouts, loop-extraction) instead of building the coupled maintain-and-update machinery END-TO-END.

## 🌍 THE FRAMING (resolved via deep drill, brain-lit-grounded)
Comprehension = using language to UPDATE/QUERY a pre-existing WORLD MODEL, not building meaning from a blank slate (Spelke, Barsalou/Zwaan/Bergen, Friston/Clark, Lambon-Ralph hub-and-spoke; LLM Othello-GPT / space-time = world models emerge from text-at-scale). CORRECTIONS: "grounded" = RELATIONAL/amodal-hub (= our foundation KB), NOT sensorimotor (contested + failed). It's a TARGET-UPGRADE to the stateful core, not a pivot — framing + mechanism are the same missing organ. OUR THIRD ROUTE (vs LLM emergence-at-scale, vs embodiment we lack): SUPPLY the world model (foundation KB as a PRIOR) + LEARN to update it, glass-box.

## 🔨 THE CURRENT BUILD — coupled stateful core (THE hard build, IN PROGRESS)
`experiments/exp_stateful_core_situation_model_v1.py` + `hdlab/slot_attention_wm.py` (committed d92d52c59, 22df65218). Brain-faithful: K=6 FULL-d-dim entity slots (NO scalar compression), recurrently maintained; LEARNED PE-gated write (PBWM analog); role-general HRR binding (content-key, position-invariant, hdlab/binding); encoder UNFROZEN, trained END-TO-END. Objective = forward-prediction from WM + comprehension-consistency. **ARMS (one variable = the framing test): Arm A blank slots vs Arm B KB-grounded** (slots seeded/keyed by foundation-KB concepts, KB prior encoded through OUR OWN encoder — invariant-checked, not borrowed). Reuses prior blueprint (notes/research_drill_substrate_operand_selection_mwp 2026-06-12 + contentgate v6/v8).
- **STATUS (2026-07-29, RESOLVED): the gradient path is PROVEN SOUND — the weak smoke was UNDERTRAINING, not a wiring bug.** Director ran a tiny-config overfit probe (real TinyTransformer + real SlotAttentionWM + real MES items + judge, Arm A): train_loss 0.692 -> 0.015, train acc 1.000 by step 25 of 400; per-component grad norms g_enc/g_wm/g_judge ALL nonzero and active; slot_mean features distinct (pairwise cosine mean -0.067, norm 1.88, not collapsed). The coupled core (slot-attention WM + PE-gate + HRR-bind + unfrozen encoder -> judge) trains end-to-end correctly. The prior "can't OVERFIT 64 items => GRADIENT/WIRING red flag" read was WRONG: the weak smoke did only ~12 optimizer steps (64 items / batch 32 x 6 epochs) to fine-tune a 25M-param 512d/6L transformer through an 11-step recurrence — far too few. One step-125 grad-norm spike (~18x) also flags the joint fine-tune needs gradient clipping.
- **RE-SMOKE LANDED (264ba7f76, 200 steps/arm, grad-clip, seed 7, 64tr/32ev): training FIX CONFIRMED (MES-A train_loss 0.75->0.042 = memorizes) BUT eval exposes OVERFITTING, not generalization:** MES eval A=0.469/B=0.500 = CHANCE and TIES random-init-core (0.4375) — the exact HARD_FAIL_STRUCTURE_ALONE mode this build must avoid; KD eval A=0.625/B=0.500 (KB grounding HURT, B-A=-0.125, opposite of the framing hypothesis; A's 0.625 on N=32 is within noise, p~0.11). discriminator_fires MES=False, KD=True(via A). So: memorizes-not-generalizes on maintenance; no KB benefit at smoke scale.
- **GPU FULL RUN HELD (do NOT auto-fire): the smoke's own structure-alone control ties the trained core -> firing GPU would be spend-on-unproven.**
- **BRAIN AUDIT LANDED (notes/brain_fidelity_audit_as_built_stateful_core_2026-07-29.md, Director-VET'd code-true): VERDICT = MECHANISM-limited. TOP cause (code-verified, structural/unlearned -> exactly the trained-ties-random-init signature): step() addresses via `slots.mean()` (destroys per-slot identity), a SINGLE global write scalar, and a convex-BLEND update -> NOT PBWM per-stripe gating and NOT even faithful Locatello per-slot slot-attention. Compounded by a single shared role-key (gap B, explains MES-specific floor) + one-shot unprotected KB prior eroded by the blend + gameable kb_consistency (gap D, explains KD B<A). Ranked C > B > D; A(objective)/E(trajectory-metric) secondary.**
- **PER-SLOT GATING (gap C) SHIPPED (ee714c31) + re-smoked (seed 7, 64/32): RESULT = fixed KD/KB but NOT MES.** KD B-A flipped -0.125 -> **+0.156** (KB grounding now HELPS bridging; KD-B=0.5625 > random-init 0.5) = FIRST directional framing positive, exactly the audit's gap-C/D prediction (per-slot gating stopped the mean-pool erosion/gaming of the KB prior). BUT MES A=0.500/B=0.469 STILL ties random-init (0.4375), discriminator_fires[MES]=False. Per-slot gating was necessary + brain-faithful but NOT sufficient for multi-entity MAINTENANCE. (Caveat: N=32, KD +0.156 directional not bankable.)
- **GEN-CURVE COMPLETE — AMBIGUOUS + unstable (Director CORRECTION: my earlier "mechanism-limited" read from the incomplete 64->128 points was PREMATURE):** trained MES eval {64:0.50, 128:0.49, 256:0.985, 512:0.50}; random-init {0.49,0.575,0.67,0.675}; train_loss {0.69,0.44,0.127,0.696}. THREE honest takeaways: (1) MES IS SOLVABLE -- old mean-pool mech hit 0.985 @256 (gap +0.315 over random-init), so NOT mechanism-dead; (2) but UNSTABLE + CONFOUNDED -- epochs shrank with size (35/18/9/4) so 64 & 512 never FIT TRAIN (loss ~0.69), an optimization artifact; the 0.985 is a SINGLE suspicious point (>> calibration reader's ~0.63) w/ random-init ALSO rising to 0.675 -> NOT banked; (3) KEY: a 64-item SMOKE CANNOT test MES generalization (too few items -> both mechs memorize-not-transfer). => the smoke is the WRONG instrument for MES; only a properly-budgeted MULTI-SEED FULL run (>=256 items, train must FIT, loss <~0.15) can judge MES + resolve the seed-luck/instability via 2-seed+5-random-init.
- **GAP B BUILT (4133235d3) + device-placement bug fixed (751e4734c, WM/judge were CPU-pinned -> crashed first cuda run; fix unblocks GPU full too) + ESC-byte bug in full-launch bat fixed (92ae48cfb).**
- **DATA-SUFFICIENT MES GATE (256 items, seeds 7&13, cuda) LANDED = OPTIMIZATION_UNSTABLE: the gap-B mechanism did NOT FIT train @256 (seed7 a_loss=0.699, seed13 a_loss=0.686, both ~chance, fit=False) -> eval~chance (0.51 vs random-init 0.52, gap~0), KD framing 0.0. The gate correctly SEPARATED optimization-failure from mechanism-verdict: we CANNOT judge the mechanism until training reliably fits. (Contrast: old mean-pool path fit @256 at loss 0.127 -> the richer per-slot+role-query mechanism made the loss surface harder to optimize.)**
- **🧭 BRAIN-FOUNDATIONAL CONFIRMATION (whole-architecture, notes/brain_foundational_confirmation_whole_architecture_2026-07-29.md): VERDICT = PARTIAL, NO WRONG-HILL. SEM (Franklin/Gershman/Norman/Zacks 2020) is a near-independent literature PRE-VALIDATION of our whole design (structured entity/event states + PE-driven update). => we are on the RIGHT HILL; keep going, don't course-correct.**
- **🔑 THE ONE GAP = a THREE-WAY CONVERGENCE (SEM + EST + our own code-audit gap C all point at the SAME fix): DISCRETE, BISTABLE EVENT-BOUNDARY SEGMENTATION. EST/SEM: comprehension = HOLD the current model then REPLACE at a prediction-error SPIKE. Our convex-blend write does continuous partial averaging = the ANTI-THESIS of segmentation. So audit gap C is not merely an "engineering defect" -- it IS the missing EST/SEM segmentation mechanism. Fix (reframed): bistable PE-THRESHOLD hold-vs-replace gate (not a free sigmoid blend) + PROTECT the KB-prior slot from ordinary write competition in the SAME change -> resolves gap C AND gap D at once.**
- **🔗 SYNTHESIS with the fit-probe STUCK_FLAT: brain wants BISTABLE/sharp; optimization broke on addr_temp=0.5 too-sharp-too-early. UNIFYING answer = ANNEAL soft->bistable (trainable early, discrete-segmentation late) = exactly the temp-anneal arm the fit-probe sweep is testing NOW. The two threads converge on ONE build. Lower-urgency roadmap (post-fix): Hasson multi-timescale hierarchy; unify symbolic time/causation into the situation model.**
- **=> CURRENT BLOCKER = TRAINING STABILITY, not the mechanism, and NOT fixable by the GPU full run (it'd fail to fit too). FIX IN FLIGHT (aac2e665): instrumented fit-probe (per-epoch loss + grad-norm, identify stuck/oscillating/slow) + minimal lever sweep (LR, warmup+cosine, more steps, gentler role_query init / anneal addr_temp soft->sharp) to reliably FIT 256-item MES (train_loss<0.15 both seeds); per-slot(ee714c31)-vs-gap-B control to isolate if role-query is the culprit. Remote push-free. Mechanism verdict + GPU full BLOCKED on achieving reliable fit.**
- **(historical) GAP B rationale = role-differentiated, entity-focused addressing keys (Frankland-Greene).** Cause of MES floor (audit #2, now evidence-confirmed): a single shared clause-POOLED key can't route WHICH of several tracked entities a clause updates; KD (single-entity) works, MES (multi-entity) floors -- exact match. Fix: learned ROLE-QUERY vectors attend over TOKEN-LEVEL clause reps (parser-free, NO bolt-on reader) to extract an entity-identity addressing key; per-slot gating from ee714c31 retained; ONE VARIABLE (addressing-key source only). New sentinel _smoke_gapb.done. Gap D (KB-protect) + forward-predictive objective staged AFTER MES clears random-init.
- **STAGED NEXT BUILD (design banked, build AFTER per-slot re-smoke confirms the core clears random-init): FORWARD-PREDICTIVE objective (the deepest leg of the founding diagnosis — encoder/objective still not forward-predictive). notes/forward_predictive_objective_from_wm_state_design_2026-07-29.md: predictor head predicts next-clause latent from slots_{t-1} BEFORE step; PE feeds the per-slot gate + own loss; SimSiam stop-grad collapse-avoidance (EMA fallback pre-budgeted); keep MLM encoder + clause-granularity head; OOM-free; one-variable ablation Core vs Core+ForwardPred, HARD_PASS +0.10 MES over Core. Sequenced AFTER per-slot gating (don't stack on a broken core).**
- **CORROBORATION (still in flight): generalization-curve on the OLD mechanism [exp_dev ac81ad7, remote, MES Arm-A 64/128/256/512 vs fixed eval, trained-vs-random-init] = data/diag_stateful_core_gen_curve_v1/. Flat/tied => confirms mechanism-limited (converges w/ audit); climbs => old mech was partly data-limited (still want the brain-faithful rewrite). SYNTHESIS when both land: rewrite re-smoke moves MES off the random-init tie + gen-curve flat => proceed to GPU full run w/ fixed mechanism; else keep digging (gap B/objective).**
- **GPU wiring is ALREADY PREPPED + committed (da345f7a3): --device honored, OOM-class-free confirmed, launch bat + notes/stateful_core_full_gpu_launch_recipe.md staged for verbatim fire IF the fork resolves to data-limited.**
- FULL run (HELD, GPU, --full --n-random-init-seeds 5): verdict signature = Arm B beats Arm A SELECTIVELY on KD (bridging) + beats worst-case random-init-core, both seeds.

## 📏 MEASUREMENT (done, honest)
Both in `experiments/diag_order_critical_comprehension_calib_v1.py`, LOCKED_CONSTRUCTION.json + KD_FRAMING_FINDING.json.
- **MES** (MULTI_ENTITY_STATE distE4/distEv6) = MAINTENANCE test. Validated (BGE +0.19-0.25) but NOT bulletproof (~20% structure-alone: 4/5 random-init seeds fail, seed_101 solves at +0.075). GUARD: mechanism must beat WORST-case (~+0.08) random-init across >=5 seeds.
- **KD** (gen_knowledge_dependent, real CSKG facts) = BRIDGING/framing test. MEASUREMENT-LIMIT INSIGHT: you CANNOT frozen-reader-validate a bridging task (bridging IS the capability; a frozen linear reader can't do the 2-step conditional inference even with the KB fact injected). So KD is judged by the SELECTIVE Arm-B-vs-A delta DIRECTLY (no frozen-reader gate) + random-init-fails guard + provably-solvable-by-construction.

## ✅ BANKED this week (do NOT rebuild) + ❌ what's SPENT
- WINS: learned readout (relational placement is readout-limited, +0.038, WIRE); gated_fusion island cashed in (two-seed HARD_PASS, per-axis gate > z-avg, WIRE). Foundation (29585), scale-encoder (29591), reasoner — banked.
- NULLS (all clean, both seeds): grounding (HARD_FAIL_NO_TRANSFER — sensorimotor is for concrete, not relational); objective axis (relObj + full R3/R4 self-teacher); breadth (data lever refuted, equal-budget); forward-PC-alone v5 (crashed CUDA OOM on full-position logits — causal-LM logits chunking needed; and forward-PC-alone is stateless anyway -> folded into the coupled build as an arm).
- **READOUT EASY-PATH SPENT for comprehension**: cross-boundary VET failed-to-replicate (seed-luck); AttnBilinearReadout HARD_FAIL_STRUCTURE_ALONE (random-init matched). A decoder can't fix a signal the encoder never built -> the fix is UPSTREAM (this build).

## 🛠️ PROCESS FIXES (2026-07-29, durable)
- **FS: fixed the right way** — `git config core.fsmonitor/untrackedCache/fscache true` (git 2.53). git status 2min-timeout -> 0.68s. Non-destructive; store (data/substrate_index/concept/atoms.jsonl + corpora) + tracking design intact. (Root: 42k tracked files incl. intentional data/*/metrics.json; NOT a runaway cron.) Deferred (careful): the metrics-in-git tracking design bloats history but fsmonitor makes it fast, store is intermingled -> don't untrack rashly.
- **SUBSTRATE-SEARCH (USER-relocked)**: knowledge/prior-work -> `tools/director_kb_query.py` / `substrate_query.sh`, NOT grep/find. (git/file plumbing stays filesystem.) It surfaces prior work I'd miss + avoids slow fs scans.
- **BUILD-AGENT STALLS diagnosed**: (1) 2-min tool timeout vs multi-minute experiments -> agents background runs + stall at "started it"; (2) fs slowness (now fixed). FIX = SPLIT WORKFLOW: exp_dev builds+self-tests+COMMITS only (fast), returns the run command; DIRECTOR runs the long smoke/full + reads the result off disk. This WORKED for the stateful-core build. Smokes go REMOTE (USER: no local smokes).
- Remote liveness truth = ckpt-mtime + GPU-util, NOT the heartbeat (fooled the Director 3x).

## STORE / OPS
Local-only; NO origin push / remote-persist without in-session USER auth. Only stop/kill what THIS session spawned. Heartbeat every turn-end. Brain = existence proof; on every negative, the difference vs the brain + iterate (diligently, not defeatist). VET+REPLICATE positives before believing.
