# Reader Chain-Grade Component Dossier (scour + brain-dive, per component)

Living checklist for driving each requisite reader component to its proven ceiling and landing it into ONE chain-gradeable who-did-what demonstration. Method per component (USER 07-23): deep KB-scour of our prior results + brain-mechanism deep-dive, THEN strategize to ceiling. Brain is the existence proof -> every shortfall gets (1) fair-test? (2) exactly-like-the-brain? before accepting any wall (USER 07-23).

## 🧠 FOUNDATIONAL BRAIN-ARCHITECTURE REVIEW (USER-requested 07-24: "deep review of every input+operation vs how the brain does it -- may reveal things we're doing wrong")
BRAIN ARCHITECTURE OF WHO-DID-WHAT (established science, 5 principles): (1) INCREMENTAL + PREDICTIVE (word-by-word, predicts args from verb+event-knowledge; not batch); (2) CONSTRAINT-SATISFACTION IN PARALLEL (word-order+morphology+animacy+verb-bias+plausibility integrated simultaneously, reliability-weighted; NOT modular feed-forward); (3) VERB-ARGUMENT-STRUCTURE DRIVES it (verb projects subcat frame + roles -> predicts/constrains args; verb-centered); (4) BAG-OF-ARGUMENTS then role-mapping (find args first, map to roles by position later; two-step); (5) **GOOD-ENOUGH / NVN HEURISTIC + SELECTIVE REANALYSIS (the crux, Ferreira): brain DEFAULT = fast shallow heuristic (first-noun=agent, second=patient = WORD ORDER) + plausibility; deep structural reanalysis ONLY when a noncanonical cue / implausibility forces it. Humans routinely MISINTERPRET passives/OSV (over-apply NVN).**
HYPOTHESIZED INCORRECTNESSES (pending the input×operation inventory a7b1988a + VET -- caveated, my reads over-reach): (A) we built a UNIFORM FULL-STRUCTURAL FEED-FORWARD pipeline (tag->parse->enum->classify->gate) -> errors CASCADE even on easy sentences (ROLES autopsies kept bottoming out at "upstream POS/parse error killed the item"); brain integrates cues in PARALLEL so weak cues get COMPENSATED not propagated. (B) we treat naive WORD ORDER as a strawman to beat everywhere -- but NVN/order IS the brain's PRIMARY strategy (correct on canonical) -> structure should ONLY add value on NONCANONICAL (passive/OSV) as REANALYSIS. (C) role-assignment = learned ANIMACY-BIASED classifier (autopsy-confirmed) vs brain's order-default + verb-arg-structure + reanalysis (animacy only secondary). (D) the reliability-weighted constraint-integration combiner (built 07-23) may NOT be wired into the reader -> verify. **PROVOCATIVE IMPLICATION: brain-faithful reader = ORDER-HEURISTIC base + STRUCTURAL REANALYSIS triggered by noncanonical cues, cues integrated in PARALLEL -- NOT the uniform structural cascade we've been perfecting.** (passive mechanism a8c13061 = one reanalysis trigger, right instinct; the Q is whether to re-architect the whole reader this way.) Refs: PMC2854821 (incrementality), PMC10847345 + ferreira_2003 (good-enough/NVN), chow2015 (bag-of-arguments).
**AUDIT DONE (inventory a7b1988a + 5 principles; VET-pressure-testing ac0af7b7): CORE FINDING = the reader's MECHANISMS are mostly brain-faithful individually (relabel/gate/abstention/slot-overrides) BUT the ORGANIZING ARCHITECTURE is a FEED-FORWARD EMIT-THEN-FILTER CASCADE, where the brain is PARALLEL constraint-satisfaction + order-heuristic-DEFAULT + selective-reanalysis + verb-driven. 4 divergences: (1) EMIT-THEN-FILTER (enum->attach->label->emit tuples, THEN G1-G4 gate them; all-oracle 0.71 ceiling + flat spurious-FPs ~32 = LIKELY an artifact of over-emit-then-filter -- brain wouldn't emit them); (2) CASCADE not PARALLEL (a POS/parse error DELETES the item downstream = exactly why the ENUM>ROLES>PARSE oracle-ranking exists; brain compensates weak cues); (3) **CONFIRMED: the reliability-weighted PARALLEL-integration combiner (built 07-23) is NOT wired into the consolidated reader -- the one architectural fix we already have is on the SHELF**; (4) REFRAME: tuned on McGuffey (simple SVO where ORDER already suffices) -> most of the +0.069 is the structural machinery REPAIRING OUR OWN cascade errors (POS mistag/candidate-bug/split-drop-relativizer/over-emission) NOT out-comprehending order (naive TIES the reader on canonical, LitBank 85%; only ROLES-relabel +0.010 = genuine non-canonical comprehension). STAGE VERDICTS: parser verb-AGNOSTIC (brain=verb-driven); candidate EXCLUSION-lists (brain=bag-of-arguments, include-all-then-role-map); role-BASE = learned ANIMACY-biased clf (brain=order-default+verb-frame+reanalysis, animacy secondary); regex-split (brain=incremental event-boundary). BRAIN-FAITHFUL REDESIGN = ORDER-HEURISTIC BASE (NVN, the brain's fast path not a strawman) + WIRE-IN the parallel reliability combiner + SELECTIVE REANALYSIS (relabel + passive = triggered modules, not always-on) + VERB-DRIVEN + NO emit-then-filter. RECOMMENDATION (VET-pending ac0af7b7): if the 3 claims hold, PROTOTYPE the brain-faithful architecture + A/B vs the cascade on the HARD-SYNTAX held-out set (where the difference should show), NOT another component patch. Passive-mechanism a8c13061 = a reanalysis module = already consistent with the redesign.**
**AUDIT VET-CORRECTED (ac0af7b7 -- Director OVER-READ, corrected honestly): all 3 claims VERIFY on disk (cascade is un-integrated; ~5/6 of the +0.069 is SELF-REPAIR = slotfix+gate fixing OUR OWN POS/candidate/over-emit errors, only whgap +0.0086 is genuine word-order comprehension; the 0.71 ceiling is ~half over-emit) BUT they justify ONE NARROW LEVER, NOT a re-architecture. CORRECTIONS TO MY READ: (1) the 'parallel combiner sitting on the shelf' was actually TESTED + HARD_FAILED 3x today (earlyjoint/jointreliability/measuredreliability, redundant on the strong base 0.5246==0.5246) = NEGATIVE evidence for parallel-CS, not support -- I omitted this; (2) self-repair is WHAT structural machinery is FOR; the genuine order-beating component (whgap) already works + is small only because object-relatives are RARE in the corpus (DATA, not architecture); (3) half the 0.71 ceiling = gold-COVERAGE (spurious_fp=31: a parallel-CS reader emitting the same well-formed tuple is scored FP identically) + a 0.90-recall composition bound = DATA/gold, NOT architecture. HONEST META: the reader is closer to 'CORRECTLY BUILT FOR ITS DATA' than 'mis-architected'; the review's biggest value = the VET catching MY over-read (VET-before-fact on INTERPRETATION). NOT re-architecting. THE ONE JUSTIFIED LEVER (in flight a0272f18): SUBCAT-RESPECTING SELECTIVE EMISSION (don't bind a patient to an intransitive-verb-instance -- come/sit/struggle/lie + locative-PP) -> closes the 32 subcat_fp ~= HALF the oracle precision gap; brain-faithful VALENCY (distinct from redundant selectional-preference); STRUCTURAL-only (semantic subcat gate HARD_FAILed bal_acc 0.601); builds on the motion-aspectual 0.2-reduction structural partial win.**
**PASSIVE CAPABILITY RESULT (exp_reader_passive_thematic_reanalysis_v1, VET-adjudicating-TIER ad3d0ee7): SYSTEMATIC passive mechanism = brain-faithful thematic reanalysis (detect aux-be+VBN; pre-verbal subject->PATIENT, by-object head->AGENT; BYPASSES the learned classifier for passives = exactly the selective-reanalysis-over-learned-base the architecture review pointed at). RESULT: **12/13 passages fired (up from 2 parse-luck), reader 23/24 (0.958) vs naive 0/24 = margin +23**. P1 McGuffey NON-REGRESSION (0.6423->0.6449; does NOT over-fire on actives). P2 ablation OFF -> 2/4 baseline (the mechanism drives +10 passages, not luck). NEVER-CONFIDENTLY-WRONG (abstains on agentless 'injured' -- no agent to recover). Diagnosis (sharp): passive DETECTION already worked on all 13; 3 DOWNSTREAM failures blocked it (by-obj mislabeled LOCATION; prev_prep adjective-skip bug; subordinate-clause subject routing). POTENTIAL FIRST CHAIN_GRADE (guardrail held all session) -- MUCH stronger than the 4/24 weak directional (29503). VET CRUX (the tier gate): by-construction? (all-passive gold + passive-mechanism = genuine GENERAL capability or tautological?). Director lean = GENUINE (general structural detection, held-out blind-mined, correct brain-faithful operation, naive 0/24 because order genuinely fails on passives) -- but defer to cert-owner (I just over-read the last audit -> extra caution). THIS VINDICATES the brain-faithful SELECTIVE-REANALYSIS direction: structural reanalysis BYPASSING the learned animacy-biased classifier on non-canonical = a REAL capability (23/24), whereas the learned base alone gave parse-luck (2/13).**
**🎯 CHAIN_GRADE EARNED -- SESSION FIRST (VET seq 29504, commit 4c088d685; cells committed 17fe43b6b LOCAL-ONLY). By-construction REFUTED by the DECISIVE test: fresh 11/12 on 12 BRAND-NEW unseen passives (the 1 miss = an independent POS mistag = proof it COMPUTES not echoes). Anti-circular (gold blind-mined before mechanism). Cleared the cert-owner's PRE-REGISTERED bar (>=6-8 independent passages -> 12/13, 11 novels). HONEST SCOPE (in atom): AGENTED passives only (agentless deferred via abstention); EFFECTIVE N~12 independent extractions (23/24 + margin+23 OVERSTATE ~2x -- agent+patient correlated; robust number = 12/13 passages); reversal-rule SUPPLIED, legitimacy from generalization (11/12 fresh). VET FRAMING CORRECTIONS (symmetric): DOWNWARD -- Director-relayed P1 '0.6423->0.6449' does NOT reproduce; actual = 0.5868->0.5868 EXACT TIE (non-regression holds; I relayed an agent number without disk-verifying = a disk-verify-discipline lapse, OWNED); PRECISION -- mechanism DOES fire on McGuffey's own passives (net-neutral tie), 'never fires on actives' too strong; UPWARD -- confirmed CG, not down-graded out of caution. Store LOCAL-ONLY, needs_orchestrator_store_sync=true HELD (no push w/o USER auth). MEANING: the substrate systematically reads who-did-what in agented passives where word-order gets 0/24 + generalizes to unseen = the brain-faithful SELECTIVE-REANALYSIS thesis realized as a certified capability.**

## CHAIN-GRADE RUBRIC (what "achieve this" means -- from hdlab/glass_box_loop.py + schema.py CHAIN_GRADE_CAPABILITY_BREAK + verification test naming)
Components individually earn MEASURED_MECHANISM (a fair, measured oracle-uplift-to-ceiling). The COMPOSED reader earns CHAIN_GRADE only by demonstrating ALL 4 on REAL text:
1. DISCRIMINATOR FIRES ON REAL DATA: a real naive baseline (first-noun-is-agent / positional / freq) FAILS where the structural reader succeeds, with margin.
2. CAUSAL GLASS-BOX: hand-edit a logged parse->enum->role step -> who-did-what output FLIPS (monitor-not-control); deterministic replay reproduces.
3. AUDITABLE: sha256/merkle over trace, tamper-detected.
4. NON-CEILING AT SCALE: holds on more/harder sentences + second genre (not an N=163 artifact).
=> the consolidation + demonstration IS the chain-grade bank; ZERO chain-grade banked to date (guardrail held; ~88 component MM banked). Demo scaffold DONE + wiring VET'd/banked seq 29496 (MEASURED_MECHANISM; ARM-B discriminator proven FAIR/not-nerfed + ARM-C causal-glass-box proven FAITHFUL/not-staged; CHAIN_GRADE HELD -- McGuffey = DEV corpus, train==test, not held-out).
**HELD-OUT CORPUS FOR THE REAL CHAIN_GRADE = LitBank OOD golded harness ALREADY EXISTS**: data/corpora/litbank_coref_conll/*.conll (distinct_from_mcguffey=true); exp_multi_turn_loop_litbank_ood_* reconstructs verbatim passages + has Q&A gold (small ~17-21 items, expandable). PLAN: fold improved components (spurious-gate +0.0236 landed; ROLES-2 / ENUM-WHgap / PARSE in flight) -> re-pass all 4 bars on LitBank held-out = the CHAIN_GRADE bank. (Note the small N limits the SCALE bar -> expand the LitBank gold slice for a proper held-out F1.)
**FINISH RESULT (exp_consolidated_reader_chaingrade_FULL_v1, VET-adjudicating a5089fbc): STEP-1 McGuffey composed HARD_PASS = F1 0.5738->0.6423 (+0.069, faithful composition, no regression; per-component alone-over-base: gate +0.034 > slotfix +0.023 > relabel +0.010 > whgap +0.009) = the DEMONSTRABLY-FINISHED reader. STEP-2 held-out LitBank = 2/4 bars: ARM-A accuracy 0.769 TRANSFERS (absolute); ARM-C glass-box PASSES on LitBank (causal-edit flips + tamper breaks hash + replay); ARM-B DISCRIMINATOR DID NOT FIRE (naive positional 11/13 >= composed 10/13, composed subset of naive); ARM-D N=13 small. HYPOTHESIS (pending VET): ARM-B non-firing = held-out-GOLD ARTIFACT (LitBank svo gold skews clean single-clause SVO where nearest-noun already suffices -> structure has no room; machinery even over-suppressed 1) NOT a reader-transfer failure -> path to CHAIN_GRADE = a HARD-SYNTAX held-out gold (fronted/OSV) where the discriminator CAN fire. CHAIN_GRADE NOT earned yet; the 'demonstrably-finished reader' IS achieved (composed + real-text + glass-box + honest accuracy + 77% held-out transfer). VET adjudicating gold-artifact-vs-real-failure via an independent hard-syntax item count.**
**ADJUDICATED (VET seq 29502, commit cca76a48b): STEP-1 composed reader BANKED MM 29502 (byte-faithful base, +0.0685 over V3, recall-ceiling 0.74->0.79). STEP-2 = GOLD-ARTIFACT CONFIRMED (independent count: 0/13 hard-syntax, ALL canonical SVO -> discriminator VACUOUS; the 10-vs-11 gap = ONE over-correction (watch2 "pulled OUT his watch": G4-v2 correctly drops particle "out" but emission needs a surviving patient -> agent dropped; naive keeps WRONG patient "out" yet scores the agent = scoring quirk rewarding the worse analysis) NOT transfer failure). VERDICT: 'DEMONSTRABLY-FINISHED READER' EARNED (engineering milestone: composed + real held-out prose + glass-box + honest 77%); CHAIN_GRADE correctly UNEARNED (77% shows the reader WORKS on canonical prose, NOT that structure beats order -- naive 85% same slice). KEEP THE TWO CLAIMS SEPARATE. NEXT (in flight a95e0b82): build a HARD-SYNTAX held-out gold (passive/fronted/OSV/distractor, selected BLIND to reader) where naive FAILS -> the real discriminator test. Honest either way: structure generalizes = chain-grade candidate / components McGuffey-overfit = keep-digging autopsy.**
**HARD-SYNTAX RESULT (exp_consolidated_reader_hardsyntax_heldout_v1, VET-adjudicating-TIER a03cabd7): built a genuinely-hard held-out slice = 24 passive/cleft items, 11 novels, mined BLIND to reader. VALIDITY GATE PASSES: naive positional = 0/24 (genuinely hard, unlike the vacuous 0/13). DISCRIMINATOR FIRES: reader 4/24, strictly dominates naive (lost_vs_naive=[]), margin +4 (>= pre-reg 3); glass-box + tamper + replay all True on hard-syntax. BUT MAGNITUDE WEAK: 4/24=17%, solves only 2/13 passages (agent_by 1/9 + patient_preverbal 2/13) -> reader mostly FAILS adult-literary passives. HONEST READ (deflated, NOT over-claimed): structure STRICTLY BEATS ORDER on held-out hard syntax (DIRECTION solid -- naive at floor, reader does what order fundamentally cannot) -- NOT 'reader handles passives well'. Margin rests on N=24 + naive-at-floor -> robustness is a VET question. TIER PENDING (potential FIRST chain-grade; guardrail held all session -> must be bulletproof-or-MM). VET adjudicating: are the 4 hits GENUINE structural passive-reversals or incidental? My lean = MEASURED_MECHANISM (direction-established/magnitude-weak), defer to cert-owner.**
**VET CALL (seq 29503, commit 131e7bfae): MEASURED_MECHANISM, NOT chain-grade -- GUARDRAIL HOLDS (0 CG this session, ~53 MM). VET STRENGTHENED validity (naive EMITS the WRONG order-tuple on 12/13, not floor-by-abstention -> direction REAL) + confirmed the 4 hits are GENUINE passive reversals (reader emits exact-reverse-of-naive; abstains on the other 20 = never-confidently-wrong; lost_vs_naive=[]) BUT = only 2 INDEPENDENT passages -> too narrow for a capability. Cert-owner OVERRODE the cell's CHAIN_GRADE verdict (discriminator-fires != capability-proven; banking 1st CG on 2 successes = the over-reach the guardrail prevents). **HONEST 3-PART LANDING: (1) DEMONSTRABLY-FINISHED READER = EARNED (29502); (2) STRUCTURE-BEATS-ORDER on held-out hard syntax = proven DIRECTIONAL MECHANISM (29503, genuine passive reversal + glass-box, naive provably wrong); (3) CHAIN_GRADE correctly UNEARNED.** NEXT (keep-digging, in flight a8c13061): SYSTEMATIC PASSIVE mechanism (brain-faithful thematic-reanalysis, single-sentence: aux-be+VBN+by -> subject=PATIENT, by-obj=AGENT) -- reader currently fires by PARSE-LUCK (2/13) + abstains on structurally-identical passives -> systematize to >=6 passages = turn direction into a real capability (genuine gain; passives are everywhere) = the honest chain-grade re-attempt.

## COMPONENT MAP (cascade order; audit seq 29494: ENUM +0.053 > ROLES +0.039 > PARSE +0.022; all-oracle 0.7106)

### 1. ENUM / candidate-predicate enumeration  (top lever +0.0527)
- STATUS: recall 0.44->0.60->0.75 banked (29495 enumext_v4, strict Pareto over v3; do/have lever KEEPER, ECM NET-NEGATIVE -> drop). F1 0.5738->0.5882. **WH-GAP FIX (v5 exp_multipred_argstruct_relgap_v5, HARD_PASS, VET'd+BANKED 29498): recall 0.75->0.77, F1 0.5882->0.5969 (+0.0087), precision ROSE, ZERO regression (additive), 2/2 WH-gaps recovered, antescramble-control fires.** Clean WH-gap subset was exactly 2. REMAINING 23 residuals: 11 ROLE (ditransitive/OSV/prep-gov/ECM -> ROLES-3 + G4-fix), ~11 POS/candidate-tagger mistags -> POS is the LAST ENUM lever, 1 ECM.
- BRAIN: (a) event segmentation via prediction-error/uncertainty; (b) verb lexical access AUTO-projects argument structure -> ANY event-denoting item projects a frame (not just finite main verbs). 1/2/3-arg verbs graded.
- SCOUR: 25 residual misses decompose = 14 TRUE-enum (5 VERB_NEVER_ENUMERATED = POS/quote-boundary/coord-VP mistagging; 9 ENUM_BUT_NO_LOCAL = parser mis-attach + WH-relative-gap where split_sentences regex DISCARDS the relativizer token) + 11 that are ROLE failures (fronted/OSV). => ENUM metric tops ~0.86 not 1.0; the 11 belong to ROLES.
- LEVER (next): WH-relative-gap regex fix (cleanest single target) + POS/quote robustness. Frame/trigger-word extension = PROVEN DEAD END (only real-parse integration moved it). Knowledge-integration (5 schemes) all HARD_FAILed scramble control.
- CEILING: single-sentence headroom remains (n_cross_sent_bound=0); situation-model wall NOT reached.

### 2. ROLES / patient assignment  (+0.0391; DRIVE-1/2/3 failed+sharpened -> DRIVE-4 emission-preserving relabel = WIN +0.0102 (26% of ceiling), VET'd+banked 29501; residual UPSTREAM parse/tag-bound = fair characterized ceiling)
- DRIVE-2 (frame-gated, HARD_FAIL F1 0.43) + AUTOPSY (the keep-digging payoff): the 5/6 misses are genuinely TRANSITIVE (need post_core[0]->PATIENT); the ONLY real cost is genuine DITRANSITIVES (give/show/hold-hands). Detecting ditransitivity FROM THE PARSE is broken -- the OOD parser mis-routes verb PARTICLES ("knock castles DOWN"), APPOSITIVES ("boy whose NAME was JOE"), embedded-clause nouns into the post-verbal "core" set (within_fp 6->24). We were trying to DERIVE valency from a noisy parse = doing it WRONG. BRAIN does it by KNOWING the verb's valency = a SUPPLIED LEXICAL FACT (throughline: structure composes, FACTS get supplied).
- RULING (Director, on the escalated fairness Q): supplied subcat-VALENCY (ditransitive-verb set from VerbNet/FrameNet) is ADMISSIBLE -- structural argument-structure (brain-faithful, the brain-dive's "retrieve the verb's argument structure"), DISTINCT from forbidden selectional-PREFERENCE (29491 = semantic patient-FIT at dedup; valency = syntactic ARITY), a SUPPLIED FACT, authorized foundation-build (VerbNet). P2 scramble-the-ditransitive-set-must-degrade adjudicates (no free pass). DRIVE-3 (blunt + supplied-valency): HARD_FAIL 0.5391.
- DRIVE-3 AUTOPSY (refuted the drive-2 premise -- keep-digging payoff #2): blunt post_core[0]->PATIENT FORCE-EMITS a patient on every post-verbal core (n_pred 144->156, +12 mostly-FP -> subcat 35->41 + spurious 33->36 inflation) = the WRONG FP category. **ROLE_ORACLE closes +0.0391 with n_pred~=base + subcat/spurious=base -- it RE-LABELS an existing candidate, does NOT force-EMIT.** Also post_core[0] grabs sub-NP FRAGMENTS (possessive "herbert's", quantifier "plenty of"), not the HEAD noun. Supplied-valency IS a real secondary lever (P2 scramble degraded +0.0313 = earns its keep) but sat on the wrong substrate. DRIVE-4 IN FLIGHT = the 3 named gaps: (1) NP-HEAD selection (rightmost N of the post-verbal chunk, skip possessive-'s/quantifier/det); (2) EMISSION-PRESERVING RE-LABEL (flip post-verbal head AGENT/RECIPIENT->PATIENT ONLY when a pre-verbal subject exists; keep n_pred~=base = the emission check; mirrors ROLE_ORACLE); (3) precise ditransitive lexicon (drop VerbNet benefactive datives e.g. "find"). Mechanism sharpened, NOT walled.
- DRIVE-4 (emission-preserving RE-LABEL, MIDDLE_BAND WIN, VET'd+banked 29501): F1 0.5738->**0.5840 (+0.0102, gap_closed 0.26)**, recall 0.70->0.73, n_pred 144->150 (EMISSION-PRESERVING vs oracle +4 / drive-3 force-emit +12), within_fp 6->5 (corrects not invents), ZERO regression, 3/5 recovered (knock/castles, meet/boys, find/boy). ABLATIONS: relabel-gate vs force-emit **+0.024 (DECISIVE lever)**; NP-head neutral; **lexicon REDUNDANT (P2 scramble 0.0, no-lex 0.0) -> STRUCTURE is the lever, NOT supplied-valency** (vindicates 29491 + throughline; the 'admissible-to-test' ruling was right, the fair test adjudicated redundant). 2 MISSES = UPSTREAM: rub/castle (oblique-PP "rubbed against" -- needs verb-prep frame fact, 1 item fair-boundary) + see/child ("dash" verb POS-mistag -- POS-mechanism-extendable, may resolve in consolidation). ROLES at a FAIR CHARACTERIZED CEILING (+0.0102 structural; residual parse/tag-bound). Pluggable: role_relabel_reassign().
- RESULT (drive-1, exp_reader_role_wordorder_valency_v1, HARD_FAIL, VET-pending): both structural levers net-negative -- FRONTED_OSV 0.5657 (<BASE 0.5738; too imprecise on grade-2 prose: fired ~7x, 1 true OSV recovered, 6 FP), CANONICAL_BLUNT 0.5312 (recovers targets but STEALS patient onto ditransitive/light-verb recipients -> within_fp 6->11). 5/6 ROLE_ORACLE-recoverable = canonical post-verbal ANIMATE-patient mislabels; 1/6 clean-OSV net-neutral. Drive's read = mapped wall (animacy-shaped, fair-control forbids selectional).
- BRAIN-DIRECTIVE CHECK (do NOT accept yet): fair-test YES; brain-fidelity NO -- brain = subcat-FRAME-GATED word-order mapping; drive tried UN-GATED word-order (CANONICAL_BLUNT broke on ditransitives = exactly what frame-gating removes). DRIVE-2 = frame-conditioned canonical mapping (post-verbal->patient ONLY for transitive frames, structurally exclude ditransitive/light-verb) -> should keep the 5/6 transitive fixes w/o the ditransitive steal. If DRIVE-2 fails -> DEEP per-item autopsy (why each of the 5/6 mislabels + how the brain does it + what our mechanism does differently); NEVER accept the wall -- brain-faithful+fail = we're doing it WRONG, keep digging (USER 07-23). Machine-contention lesson: don't overlap the scour swarm + heavy foreground cells (drive-1 timed out 2x at 10min under 6 concurrent scours).
- BRAIN: thematic-role assignment DISSOCIABLE from parse (aphasia double-dissociation: morphosyntax=IFG/frontal, roles=temporo-parietal/angular gyrus). Sequence: parse -> grammatical-role -> retrieve verb ARGUMENT STRUCTURE (subcat, postero-sup-temporal) -> map constituents to roles gated by WORD-ORDER CANONICITY. Parietal REANALYSIS only for non-canonical/passive. Animacy/selectional secondary.
- SCOUR: ROLE_ORACLE substitutes LABEL of already-routed gold-matched candidates (0.5738->0.6129, precision +0.027). v3 dominant regression = AGENT_ROUTING_DROP=8. CONVERGENCE w/ ENUM scour: 11 of the 25 reader residuals = fronted/OSV word order biasing position features toward AGENT = exactly the brain's non-canonical case.
- LEVER: WORD-ORDER-AWARE assignment + verb subcat VALENCY-GATE on which constituents can take patient; do NOT over-assign AGENT on OSV/fronted. Structural, not selectional.
- FALSE-STARTS (do NOT retry): learned-clf replacing argmax labeler (-0.019 fair); dense/early/positional/measured/backoff selectional-knowledge (5x HARD_FAIL, 29491); explicit grammar-marker (0 exercisable material); object-typicality/selectional-coherence/event-outcome-density (HARD_FAIL, do-not-retest).
- ONE vetted untried SECONDARY lead: SIG_COS_VERB_GATED_ONTOLOGY (corr 0.3561, passed design gate in exp_affectedness_change_of_state_patient_selection_design_gate_v1, never integrated). CAVEAT: verb-gated dynamic cosine; must prove distinct from redundant static selectional table before trusting; structural word-order lever FIRST.
- REVERSIBLE/DIALOGUE: NOT walls -- brain does them via reanalysis over a discourse model. Reversible = UN-ATTEMPTED on REAL material (only constructed pairs, learner-null 3x). Dialogue = mixed (dative/arg-position gate HARD_PASS + transfers; generic NP-head gate HARD_FAILs on dialogue/news). Deferred to situation-model phase; pull forward only if required.

### 3. SPURIOUS-PREDICATE SUPPRESSION / reader-logic  (HARD_PASS, VET'd + BANKED seq 29497; exp_reader_structural_precision_gate_v1)
- RESULT: 4-cue structural gate F1 0.5738->**0.5974 (+0.0236)**, precision 0.4861->0.5267, combined FP 68->56 (spurious 33->29, subcat 35->27), recall 0.70->0.69 (1 item L04_03, within floor). G4 gov-prep biggest (-8 FP). All 4 gates fire + degrade-witness; P1 base byte-identical; pluggable (clause_predicate_pass_gated 4 flags, all-off=V3). Brain-faithful structural explaining-away CONFIRMED (semantic gating had HARD_FAILed). VET ANSWER (overfit-vs-general): G1 contraction / G2 verb-class+DOBJ / G3 self-loop = GENERAL, transfer; **G4 gov-prep = OVERFIT-EXPOSED** (carries 8/12 FP; skip-list has dev-specific adjectives + conflates governing-prep with VERB-PARTICLES, e.g. "take UP arms" -> would suppress the true patient). Held-out: +0.0236 persists but ATTENUATES (driven by G4); G1+G2+G3 alone ~clear the -4 bar. => **FIX G4 (particle-vs-governing-prep) for the held-out run.** G4-v2 DONE (HARD_PASS, exp_reader_g4_particle_govprep_split_v1, VET-in-consolidation): parser emits UNLABELED heads (no prt/case deprel) -> use PENN TAGS (particle=RP, prep=IN/TO). E1 POS-based oblique skip {DT,PDT,JJ*,CD,PRP$,POS} DOUBLES FP reduction (-8->-16; word-list was under-inclusive) -> spurious-gate composed F1 0.597->**0.610**, precision +0.026. E2 RP-particle guard = held-out-only insurance (on-slice precision-neutral, OOD-tag noise). Director prediction FALSIFIED (Fix#28): take/blocks is a PARTITIVE "one OF the blocks" not a particle -> not recovered. Ship COMPOSED_V2 (g4_excludes mode='v2'); recall 0.70->0.68 (partitive-skip cost, at floor).
- WHY: spurious_verb_fp FLAT ~31-33 + subcat_fp FLAT ~32-35 across EVERY oracle arm incl ALL_ORACLE -> no input-stage oracle removes spurious emission -> distinct bound = reader's own composition logic. all-oracle 0.7106 is therefore NOT a ceiling, it's this fidelity gap. (within_frame_fp collapses to ~1 under oracle = role/enum artifact, NOT this wall.)
- BRAIN: suppress via (a) EXPLAINING-AWAY (a token a higher structural account already covers -- AUX/particle/PP-object/complement -- is not emitted); (b) PRECISION-WEIGHTING; (c) LATERAL INHIBITION among rival candidates. Structural, not knowledge.
- SCOUR (done) -- FP characterization: spurious_verb_fp = contracted-modals slipping the AUX filter (won't/can't; CODE BUG exp_oracle_mention_upperbound_reader_v1.py:201-203 checks raw surface not lemma) + non-factive/mental-state verbs pulling a complement NP up as object (wished/thought/know) + degenerate self-loops (agent==patient) + adjunct-PP mis-extracted as direct object (told...haste/home) + function-word/particle artifacts. subcat_fp = motion/aspectual intransitives with a PP/locative NP bound as object + cognition/report verbs with sentential complement. CONVERGENCE with brain: structural explaining-away; SEMANTIC gating HARD_FAILed (bal_acc 0.601).
- LEVER (drive in flight): glass-box STRUCTURAL emission/admissibility gate composing 4 vetted cues, each a toggle: (1) contraction-normalize before AUX check [code-bug fix, zero recall risk]; (2) verb-class + direct-object-parse gate [generalize exp_reader_clauseseg_verbclass_filter_v1 clean PASS: no genuine NP object -> suppress]; (3) self-loop guard [exp_reader_cheapfix_selfloop_rolefix_v1, zero regression]; (4) governing-preposition hard-exclusion [prev_prep already in v3 ~L318]. Prefer FIXED structural thresholds over learned weighting (hand cue beat learned 0.2 vs 0.1). P1 base reproduces v3; P2 each gate must-fire; recall-retention floor.
- FALSE-STARTS: semantic/selectional-coherence gate on subcat_fp (HARD_FAIL, no tau, bal_acc 0.601); selectional-knowledge table (redundant 2x); replace role-clf (HARD_FAIL); more enumeration (INCREASES FP); topical-animate injection without verb-class gate (precision below floor).

### 4. PARSE / dependency parse  (+0.0221; fairness PROBED = near-ceiling on reader arcs, NOT the bottleneck; VET'd+banked 29499)
- RESULT (exp_parser_indomain_selftrain_adapt_v1, HARD_PASS-but-deflated): the "0.81 UAS" is ALL-arcs UD-EWT (function/PP/punct); on the ARGUMENT arcs the reader needs (verb->patient head-chain) the parser is already **0.9524 (80/84)**. Unsupervised in-domain self-training: arc-proxy 0.9524->0.9643 (+1 arc), reader +1 item (net_regressed=0), UD-EWT dev intact -- but PLATEAUS (3x data = identical). 0.81 NOT a hard fair ceiling (adaptation moves it where algorithmic levers HARD_FAILed) BUT effect tiny+saturated -> PARSE effectively at its fair ceiling for the reader, NOT the bottleneck (residual misses are enum/role/gate; consistent with audit ENUM>ROLES>PARSE). UNTESTED follow-up: SUPERVISED in-domain gold-TREE fine-tune (blocked -- no McGuffey gold trees exist).
- STATUS: algorithmic levers (search 29458 + valency 29460) HARD_FAILed; domain-adaptation now probed (above) = the fair-test answer.
- BRAIN: morphosyntax dissociable/frontal; feeds role assignment. Classical ~0.90 needs semantic/contextual rep (do not assume meaning-feeds-attachment helps, esp. since selectional proved redundant).
- LEVER (queued): in-domain adaptation/retrain; addresses ENUM bucket-ii parser mis-attachment.

### 5. POS  (last ENUM lever; DRIVE DONE = HARD_PASS +0.0202, VET'd+banked 29501; scour = WIRING/LEXICAL bugs not tagger-accuracy)
- RESULT (exp_multipred_argstruct_enumext_posslot_v5, HARD_PASS, VET'd+BANKED 29500): recall 0.75->**0.80**, F1 0.5882->**0.6084 (+0.0202)**, precision UP 0.4839->0.4908, n_pred 155->163, ZERO regression, V4 byte-identical, scramble collapses. 5/11 recovered (watch/her, lay/it, teach/him, amuse/himself, sell/fish). Pluggable overrides: content_verb_indices_ext_v5 + candidate_indices_slotfix.
- **VET CLOSURE (load-bearing): the 4 in-scope not-recovered are BOTH-enumerated-now (verb+patient present) -> residual is genuinely DOWNSTREAM (role-clf/gate). So neither ENUM (0.44->0.80, maxed) nor PARSE (29499) is the bottleneck -- ROLE-ASSIGNMENT is (ROLES-4 pivotal).** OVERFIT for held-out: Lever-1 ACTION list (hurt/knocks/lay/taught) partially DEV-specific (mechanism general, list post-hoc -> generalize the irregular-verb list for LitBank); Levers 2/3/4 GENERAL.
- TARGET (from WH-gap v5 residual recompute): 5 content-verbs mistagged NN (caught/hurt/knocks/lay/taught -> blocks enumeration entirely) + 6 candidate mistags (one->CD, her->PRP$, another->DT, himself reflexive, fish->JJ, those->DT).
- BRAIN (dive done): noun/verb POS ambiguity resolved by SYNTACTIC CONTEXT -- a token in a finite-verb/predicate slot is a VERB regardless of its noun-frequency (noun-predicting vs verb-predicting contexts; structural verb info available ~immediately/150ms). Prior signal: POS cross-domain drill = OOV/unknown-token is the dominant failure.
- SCOUR REFRAME (key): NOT tagger-accuracy -- WIRING + LEXICAL-LIST bugs in existing code (reader uses NLTK's stock PerceptronTagger, NOT hdlab/pos_tagger.py -> retraining ours = WRONG artifact). LEVERS (each an already-proven in-repo fix): (1) VERB_NEVER (5) = port find_main_verb's ACTION_HINTS override into the enum gate `if not pos.startswith('VB')` (caught already in set; add hurt/knocks/lay/taught); (2) her/those/one (3) = SYNTACTIC-SLOT override BEFORE word-identity exclusion in candidate_indices (her-precedence-bug drops the OBJECT pronoun); (3) himself (1) = add REFLEXIVES set (none exists anywhere); (4) fish->JJ (1) = widen the existing DT+JJ->noun grounded-recovery. Brain: syntactic-slot over word-identity. DRIVE IN FLIGHT (patched-override cell, pluggable, does NOT edit shared ORC/enumext in place).
- Fix#28 FLAG: prior POS numbers (v2_transitions 0.911 / v3_hmm 0.929 / oov_diagnostic 0.946-0.749) are PREREG-PROSE ONLY -- on-disk metrics = UNKNOWN/corpus_load_failed. Do NOT cite as landed.

## SEQUENCING (full-auto, budget-aware)
Drive in cascade order, one at a time, VET each, fold each into the demo scaffold: ROLES (in flight) -> spurious-gate -> ENUM WH-gap -> PARSE domain-adapt -> POS. Demo scaffold (naive-baseline discriminator + causal hand-edit glass-box + scale) built in parallel; components plug in as they reach ceiling. DONE = composed reader clears all 4 chain-grade bars on real text = the bank.
