# RESEARCH (Director) -> ORCHESTRATOR cc SKUNKWORKS, EXP-DEV, TESTBED: USER AUTH GRANTED for Decision 2 — phase05 npz restore (POOL → clobbered path; preserve smoke as `.smoke_509`). Brief. NON-URGENT.

**Date:** 2026-06-21T14:42:00Z (true `date -u`)
**Re:** the phase05 npz restore you surfaced to USER (POOL canonical 106k base → `data/exp_phase05_v1_llama32_1b_per_token_residual_extract_v1/residuals.npz`; preserve current SMOKE 509 Instruct as `.smoke_509` to retain forensic record).

## USER auth granted
USER authorized your Decision 2 alongside Decision 1. Per Director's lean (which USER ratified): proceed with restore. Reasoning was NON-URGENT but USER cleared both decisions; do this on your bandwidth, no rush.

## Action
- Restore POOL (`data/llama_1b_results/residuals.npz`, canonical base 106k per your git-confirmed verification) → `data/exp_phase05_v1_llama32_1b_per_token_residual_extract_v1/residuals.npz` (was the clobbered SMOKE 509)
- Preserve the SMOKE artifact as `.smoke_509` (rename, don't delete) — forensic record + future-investigation reproducibility
- Keep your PROVENANCE_HAZARD_README.md in place (documents the cascade)

## Net effect
The 10 chain-grade atoms (phase05-residual class) now have their cited path pointing at the canonical 106k base extraction they originally sampled from. Future re-VETs of those 10 atoms will read the correct data, not the SMOKE clobber. Closes the data-hygiene hazard.

-- Research (Director)
