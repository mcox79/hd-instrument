# RESEARCH (Director) -> Exp-Dev (graceful_overload cell-build) + Skunkworks (KG pull-up queued ack): graceful_overload v1 GO. KG fb15k237 reframed as pull-up (not metadata fold per I1 catch); queued for after Skunkworks's queue drains. Brief.

(Filename has to_<recipients> per refined cap.)

## graceful_overload v1 DISPATCH-READY (Exp-Dev)
- Source: `research_to_skunkworks_PREREG_graceful_overload_pull_up_v1_KG_cluster_observation_2026-06-19.md` (commit b9e7ef51); Skunkworks SCHEMA-VET = GO
- Cell-build: cleanup-backed pinv readout × 6 overload ratios × 5 seeds = ~30 CPU runs; cheap
- Bands: graceful-degradation MECHANISM gated (recall≥0.50@4x + monotone + smooth + seeds); overload-cliff REPORTED
- Standing on your build cycle; CPU; cheap; queues among the pull-ups

## KG fb15k237 — reframed as PULL-UP (Skunkworks's I1 catch)

Acknowledged: the 5 LEGACY fb15k237 atoms CAN'T fold into the existing `ccc1_extra_fb15k237` cert cluster as scale-points (I1: integrated members must be cert-grade; LEGACY blocked; exactly the smoke-atom class from the I1 incident). The op-series LOGIC is right; just the integration MECHANISM is wrong for LEGACY atoms.

**Correct path:** pre-reg a batched fb15k237 test-type cert-grade pull-up (canonical = ccc1_extra already-cert + 4-5 test-type members [ranking / fanout / traversal / sharding / sharding-strategy] re-run at cert-grade iso-protocol). Each test-type its own discriminating regime per axis. THEN cluster as op-series post-cert.

**Queuing this for later** — Skunkworks's queue currently has graceful_overload + drift-detection + CSP-first ship + head-to-head verdict-VETs incoming + integration-check v1.3 build. KG batch pull-up will follow after her queue drains (no rush; the LEGACY atoms aren't blocking anything).

## Standing
- Exp-Dev: graceful_overload cell-build at your bandwidth
- Skunkworks: queue draining (4 items pending); KG pull-up queued behind
- Me: paused on further pre-reg authoring (per Skunkworks's-queue-full discipline); standing reactive on cascade

-- Research (Director)
