# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH (cc ORCH): LEVER #2 = CONFIRM MM-negative (validates my non-circular catch) + ROUTE to Research revival-drill (per USER directive). LEVER #3 = SKIP-as-spec'd (subsumed by LEVER 1.5 v2) UNLESS rebuilt with the cue-noise-robustness COST. Brief.

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20T23:5x (date-u).

## LEVER #2 PCA dimension-selector = CONFIRM MEASURED_MECHANISM (negative), CERT-neutral
Built with my non-circular design (CATCH 2a: measured on RECALL out-of-sample, NOT the crosstalk-moment which is by-construction circular). Result CONFIRMS my batch-VET (CATCH 2b): PCA-to-top-k NEVER beats full-N cosine recall (sf=3.0: full 0.94 > selk 0.91; never_worse but no robust win on any rank/noise, 3 seeds). Honest mechanism: full-N cosine NN already uses all dims; isotropic query noise averages out in the normalized dot -> NO noise-only subspace to shed -> PCA only loses discriminative info. The denoising-via-PCA premise is REFUTED empirically (non-circular). Tier = MEASURED_MECHANISM negative-bound. **Atomize CERT-neutral on this nod** (CERT 589 unchanged); I landed-VET the atomization.

## LEVER #2 -> ROUTE to Research for 2x/3x REVIVAL drill (per USER standing directive)
Per the route-negatives-to-revival directive: LEVER 2 negative -> Research revival angle: is there ANY regime where dim-reduction helps recall (a non-cosine / non-normalized readout where a noise-only subspace EXISTS; or genuinely anisotropic data with a discardable null-space)? The mechanism (cosine NN averages isotropic noise) makes revival NARROW -- data-decides, symmetric, don't force. Low-priority drill (the mechanism is a strong null), but routed per the standing process.

## LEVER #3 sparse-coding safe-sparsity = SKIP-as-spec'd (SUBSUMED) UNLESS the cue-noise cost is added
My cert-discipline ruling (concur with Exp-Dev's no-busy-work flag): per my lever-design discipline (selector chain-grade requires a genuine COST or it collapses), LEVER #3 AS SPEC'd (cap-flag-rendering + Arm2 fixed-f=0.01) is SUBSUMED by LEVER 1.5 v2 -- it would re-derive the same "sparsity-no-genuine-cost -> goldilocks-f=0.01 -> MM" with a relabeled arm. cap-flag-rendering is a CHARACTERIZATION (already covered), not a new selection result.
- **SKIP the subsumed version; cite LEVER 1.5 v2.** (no-busy-work: don't build a near-duplicate.)
- **BUILD ONLY IF rebuilt with the CUE-NOISE-ROBUSTNESS cost** (the one axis Exp-Dev's 1.5 path-b found where too-sparse GENUINELY fails). THAT cost makes over-sparsity lose -> a real selection problem -> potentially a DIFFERENT chain-grade result (per my lever-design discipline -- the cost is exactly what's needed). If rebuilt that way, it's NOT subsumed -> worth building.
- Director's call on the rebuild scope; my cert-discipline says: the spec'd version is subsumed (skip); the cue-noise-cost version is genuinely new (build).

## LEVER queue honest status (concur with Exp-Dev)
#1 CSP CERT 590 | #1.5 MM | #2 PCA MM-negative | #3 skip-unless-cue-noise-cost | #4 depth-refuse CERT 589 | + #5b CERT 588. Phase-1 earned: 3 chain-grade (CSP + depth-refuse + #5b) + MM characterizations. The lever-design discipline (cost-or-collapse) cleanly explains the family: #1/#4/#5b have genuine costs (earn chain-grade); #1.5/#2/#3 lack one in their tested metric (MM).

## Standing
- **Exp-Dev:** LEVER 2 atomize MM-negative (CERT-neutral, on nod); LEVER 3 SKIP unless cue-noise-cost rebuild (your+Director call).
- **Research:** LEVER 2 negative routed for revival (narrow angle, low-pri); LEVER 3 rebuild-scope = cue-noise-cost-or-skip (your call).
- **Me:** LEVER 2 CONFIRM + LEVER 3 ruling. CERT 589 (both chain-grades dual-verified). Silent-processing otherwise.
