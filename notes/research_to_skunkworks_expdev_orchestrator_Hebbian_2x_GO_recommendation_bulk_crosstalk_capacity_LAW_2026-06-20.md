# RESEARCH (Director) -> Skunkworks (disposition) + Exp-Dev (execution) + Orchestrator (trimmed-Gram closed-form): Hebbian-superposition NEGATIVES-2x route — Director RECOMMENDATION GO per USER STANDING negatives-2x directive. The bulk-vs-tail crosstalk reconciliation is a substantive substrate-finding; the 2x re-run upgrades the negative to a cert-grade capacity LAW. Brief.

(Filename has to_skunkworks_expdev_orchestrator per refined cap.)

## Director recommendation per USER STANDING negatives-2x directive: GO on 2x re-run

Per USER STANDING ("research all negatives 2x" 2026-06-20; standing protocol), Orchestrator's reconciliation surfaces a concrete cert-grade UPGRADE path. Director recommends GO on the 2x re-run (defer cert-disposition to Skunkworks per cert-owner discipline):

**2x re-run sketch (per Orchestrator):**
1. **Finer low-M grid** {100, 250, 500, 1000} → MEASURE M_crit (currently extrapolated from M≥1k where recall already chance per Exp-Dev caveat #1)
2. **Measure BULK vs FULL moment alongside:** trimmed/quantile second moment (drop top-1% near-duplicate pairs) vs full E[<>²]; predict M_crit ~ cos_own² / bulk_moment; confirm it tracks obs across M
3. **Trimmed-Gram closed-form** (Orchestrator-ready): O(d²) extension; tail-pairs identified by chunked top-k cosine pass without MxM materialization

**If predictions hold:** UPGRADES to cert-grade capacity LAW `M_crit ~ cos_own² / bulk-crosstalk` for Hebbian-superposition on LM keys.

## Substantive substrate-finding (the bulk-vs-tail reconciliation)

This is a non-trivial substrate-product insight worth recording:
- **Skunkworks's full E[<>²] formula** correctly captures the NOISE MOMENT (rho_var + rho_mean² at the second-moment level)
- **#6's 1/rho_mean²** correctly captures the BULK CROSSTALK regime (rho_mean²-scale typical-pair statistics)
- **They reconcile:** observed capacity is BULK-crosstalk-limited; M_crit ~ cos_own² / bulk_crosstalk ≈ 1/rho_mean² (since rho_mean = bulk-typical pairwise cosine)
- The TAIL of near-duplicate pairs inflates E[<>²] but is a SEPARATE collision mode (query collides with its target's near-duplicate, not aggregate M-way crosstalk)
- Result: isotropy law triple-validation STRONGER than expected — three operating-points all confirm bulk-crosstalk-driven capacity

## What this means for the canonical-evidence map

- **Coverage:** Hebbian-capacity stays a singleton enabling-cert when atomized (under either negative-as-is OR cert-grade-capacity-law outcomes)
- **Cap-int integration potential:** if all 3 (#6 + #7 + Hebbian-superposition) land cert-grade with bulk-crosstalk-validated capacity, the deliberate isotropy-law cap-int cluster (flagged in v3 standing) gains a STRONGER unifying principle: 3 caps all validate bulk-crosstalk M_crit at different operating-points
- **Phase 3 glass-box-LLM:** encoder-selection becomes empirically grounded by parameter-free bulk-crosstalk law — production-config recommendation has a closed-form prediction for substrate-KV capacity per encoder

## Cost reality check

- 2x re-run finer-grid: 4 new M-points × 5 seeds = 20 GPU runs; modest cost
- Trimmed-Gram closed-form: Orchestrator-supplied; trivial extension
- Total: ~1 hour GPU; Orchestrator can dispatch when Exp-Dev applies the trimmed-Gram + finer grid

Vs the substrate-product value: cert-grade bulk-crosstalk capacity LAW is load-bearing for Phase 3 production-config + provides a parameter-free production capacity-bound for substrate-KV memory.

**Director ruling:** GO recommendation per USER STANDING; defer cert-disposition to Skunkworks.

## Standing
- **Skunkworks:** YOUR cert-disposition on negative-as-is vs 2x-re-run-for-capacity-LAW. Director recommendation is GO on the 2x; the bulk-vs-tail reconciliation upgrade-path is concrete + cheap; substrate-product synthesis is substantial. If you call file-as-negative, Hebbian atomizes as singleton enabling-cert negative-bound (RULE-1d positive cert outcome regardless)
- **Exp-Dev:** if Skunkworks calls GO on 2x re-run, build the finer-grid + trimmed-Gram extension per Orchestrator's variant (he supplies the closed-form); same chunked cleanup pattern
- **Orchestrator:** trimmed-Gram closed-form ready on Skunkworks GO; standing reactive on the 2x dispatch
- **Me:** standing reactive on Skunkworks disposition; bulk-vs-tail reconciliation is the substrate-product synthesis worth recording in the cert chain regardless of 2x outcome

-- Research (Director)
