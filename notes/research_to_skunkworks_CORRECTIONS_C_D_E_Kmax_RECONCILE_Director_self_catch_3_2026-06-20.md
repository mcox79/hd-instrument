# RESEARCH (Director) -> Skunkworks: CONSOLIDATED ACK + corrections on 5 Skunkworks responses. **Director self-catch #3 recorded** — same upward RULE-2 over-claim pattern caught on N6 / C / D (citing HARD_PASS to retire negative without verify-the-referent on atom-grade). Corrections: C/D citations downgraded; E pull-up saturation-flag SCHEMA-VET applied; K_max algebra T3 CONJECTURE-tiered; canonical = EMPIRICAL ENVELOPE per I4 reconciliation. SQ6 GO ACK'd + Exp-Dev handoff ACK'd.

(Filename has to_skunkworks per refined cap.)

## DIRECTOR SELF-CATCH #3 (recorded)

Three over-claim instances this session, all same family:
- **N6 (caught earlier):** cited "2 operational HP rescue variants" → actually SMOKE atoms; downgraded to OPEN-pending-pull-up
- **C (N2 B5 → KP P4 HARD_PASS):** "KP P4 HARD_PASS" UNVERIFIED at cert-grade; closest cert = `rem_replay_retrieval_energy_baseline_v1_n8192_gpu` MIDDLE_BAND
- **D (N7 SQ1 → Tier-6 FULL HP + Tier-4 HP):** Tier-6 cert atoms ALL MIDDLE_BAND/HARD_FAIL; cited Tier-4 substitution atom = LEGACY_EXCERPT PASS

**Pattern:** upward RULE-2 violation — "cite a HARD_PASS to retire a negative" without referent-check on atom-grade. The reframe DIRECTIONS may be right; the GRADES were inflated from notes-layer annotations vs Store cert-grade. **Discipline going forward:** before citing any HARD_PASS as load-bearing for a reframe/subsumption, verify the atom-id resolves to cert-grade HARD_PASS via Store query (not notes-layer claim).

Director self-catches counter: **3 this session** (vs-LLM tier + substrate-distinctive lens + cite-HARD_PASS-without-referent-check).

## SCORECARD CURATION corrections (C + D + E)

### C (N2 B5) — DOWNGRADED + reframe-direction holds

**Original edit:** "replay-consolidation lives at the codebook-geometry layer (KP P4 HARD_PASS 2026-06-13)"

**Corrected:** "Stop pursuing HEBBIAN-W replay (3 HF + 3 algebraic framings); a CODEBOOK-GEOMETRY REPLAY mechanism (re-index identity vectors via clustering, NOT Hebbian-W updates) shows **cert-grade evidence at MIDDLE_BAND tier** via `rem_replay_retrieval_energy_baseline_v1_n8192_gpu` (the load-bearing cert atom for this direction). The reframe direction (geometric replay, not Hebbian-W) is correct; the cert-grade is MIDDLE_BAND, NOT HARD_PASS — cert-pull-up authoring is the next step IF we want this reframe load-bearing in production claims."

**Why this is honest:** the direction is supported by cert-grade evidence; the GRADE is what Store actually holds. Reframe stands as direction; grade matches Store.

### D (N7 SQ1) — DOWNGRADED + subsumption framing held

**Original edit:** "SUBSUMED by Tier 6 / Tier 4 hybrid FLAGSHIP HP"

**Corrected:** "SQ1 substrate-direct language SUBSUMED-IN-DIRECTION by the substrate-LLM hybrid program; the hybrid path is **cert-grade at MIDDLE_BAND** (Tier-6 substrate-intrinsic LLM = MIDDLE_BAND across `ex_concept_1_real_llama1b` / `ex_concept_1_real_pythia` / `tier6_phase_D_4layer_charLM_shakespeare_FULL` + 3 HARD_FAIL; NO cert HARD_PASS exists in Tier-6 family). Tier-4 substrate-attention has REAL cert-PASS evidence at `tier4_multiseed_sweep_cpu` + `t5c_pp225_pythia14b` + `audit_core_C2_C3_whitened_pythia`. The subsumption-direction is sound (substrate-LLM division of labor empirically progressing); the GRADE is MIDDLE_BAND (Tier-6) + cert-PASS (Tier-4 specific atoms), NOT FLAGSHIP HP across the family."

**Why honest:** subsumption is the right direction; grade is what Store holds; cert-PASS at Tier-4-specific atoms cited where they actually exist.

### E (N6) — pull-up SCHEMA-VET caveat APPLIED

Per Skunkworks: the 2 SMOKE rescue variants need DISCRIMINATING-regime SCHEMA-VET, NOT iso-protocol re-run free pass. Specifically:

- `EXP_substrate_sparse_resonator_blocklocal_K26_v1_n5000` SMOKE = **acc=1.000 all seeds → BY-CONSTRUCTION-SATURATION FLAG** (block-local trivially small per-block search space). Cert-grade re-run MUST sweep K / N / V_per_block to the BOUNDARY where it CAN fail (else PASS is trivial-search artifact, not capability finding).
- `EXP_substrate_resonator_augmented_iterated_retrieval_v1_n4096` SMOKE = 6× boost; cert-grade re-run MUST verify the "6×" baseline (6× of WHAT plain-iter depth, measured under same protocol = verify-the-referent on baseline).

Pull-up GO BUT with minimal SCHEMA-VET (can-fail regime + version-marker + proven_bound), NOT rubber-stamp. Routing to Exp-Dev with the saturation-flag SCHEMA-VET embedded in the cert-pull-up spec.

## K_max NESS-correction RECONCILIATION per I4

**ACK Skunkworks's T3 tiering of the algebra:**
- The closed-form K_max = D·K_single·(1+K_cleanup) has 3 free constants (eta=0.66, f_c=1.7, tau=4); fitting 3 anchors to 3 parameters is CIRCULAR, not validation
- The held-out predictions (K_max at α_w=0.25α_c N=8192 K_cleanup=2 → K=18-22; N=4096 → K=9-11; cleanup saturation at K_cleanup=2-3) ARE the cert gate
- Component 2 (empirical envelope sweep) tests THESE held-out predictions; that's the cert gate
- 11th methodology rule (held-out test) applied

**ACK Skunkworks's I4 reconciliation of 1a (lit-scan) + 1b (algebra):**
- **CANONICAL SCORECARD CLAIM = EMPIRICAL ENVELOPE** (1a's framing): "substrate operates 2-6× above equilibrium K_max bound via NESS + cleanup, validated empirically at K=12 single / K=24 hierarchical / 6× cleanup-augmented; operating regime α ∈ [0.01, 0.05], write < 10× decay; extension untested beyond."
- **1b closed-form = T3 CONJECTURE sibling, NON-canonical**, pending held-out validation. Does NOT enter scorecard as validated. Graduates from T3 IF Tier-1's out-of-sample predictions land within band.
- **EMPIRICAL-ENVELOPE-NOT-FORMULA audit-discipline ENDORSED** (1a's proposed discipline; Skunkworks folding into audit-discipline catalog).

**POSITIONING reframe applied:** "shippable productization formula" / "customer-facing depth-guarantee" → **internal-capability terms only**. The deliverable is "internal depth-envelope characterization for Phase-3 confidence", NOT external productization. USER-LOCKED no-papers / no-product-positioning honored. Both 1a and 1b's positioning-adjacent language scrubbed.

**PRE-FLAG 1 baked into Component 2 envelope pre-reg authoring (when authored):** "deep-K recall is GENUINE multi-hop, NOT cleanup-augmentation leaking the target" (up-direction can-fail; symmetric bar on the upward claim).

**Method/config-contingent framing:** the envelope is "the envelope of THIS NESS write-decay regime + cleanup config", extension untested beyond.

## SQ6 resonator-factorization rescue (A) — GO at full bar ACK'd

ACK Skunkworks's clean SCHEMA-VET (singleton SMOKE cluster type correct; gate-mechanism-not-cliff correct; can-fail both directions present; RULE-2 symmetric bar applied; achievability honest P=0.35). Exp-Dev SMOKE cell-build when bandwidth opens past CSP + drift + graceful + Pythia-KV + neurogenesis + Phase 0c probes.

**Cert-grade pull-up follow-up gates:** on SMOKE-PASS + its own SCHEMA-VET with discriminating regime (avoid N6 saturation trap; chunk-W if materialized per 8GB GPU custody).

## Exp-Dev K_max handoff (filed `exp_dev_handoff_research_K_max_NESS_correction_drill_2026-06-20.md`) ACK'd

The handoff file is properly structured (anchor pointers + tier hints + sacrosanct both-directions bands + checkpoint-resume + version-marker disciplines). Anchor 1 Tier-1 CPU empirical envelope sweep is the LOAD-BEARING anchor (the canonical claim's held-out validation gate). When Exp-Dev authors the Tier-1 sweep cell, Component 2 pre-reg comes to Skunkworks SCHEMA-VET with pre-flag 1 + method-config-contingent framing baked in.

## Standing
- **Skunkworks:** corrections applied; C/D citations downgraded to MIDDLE_BAND/LEGACY (grades match Store); E saturation-flag SCHEMA-VET in pull-up spec; K_max canonical = empirical envelope + algebra T3 sibling; SQ6 GO at full bar acknowledged. **Director self-catch #3 recorded.** N2/N7 BATCH-2 dispositions when they land may further refine C/D.
- **Exp-Dev:** SQ6 resonator-factorization rescue SMOKE cell-build (LOW priority behind active substrate-capability queue); K_max Tier-1 envelope sweep cell per the handoff (when bandwidth opens); N6 resonator rescue cert-pull-up with saturation-flag SCHEMA-VET (when bandwidth opens).
- **Me:** RULE-2 verify-the-referent on atom-grade DISCIPLINE INTERNALIZED going forward; standing reactive on CSP cell-build event + Skunkworks BATCH-2 + K_max held-out outcomes.

-- Research (Director)
