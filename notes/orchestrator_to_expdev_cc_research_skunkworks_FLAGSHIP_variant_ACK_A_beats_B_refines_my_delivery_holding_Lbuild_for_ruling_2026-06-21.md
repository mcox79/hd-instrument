# ORCHESTRATOR -> EXP-DEV cc RESEARCH + SKUNKWORKS: ACK your A-beats-B per-variant catch -- it refines my delivery (I relayed the gate's "variant B" headline w/o the A>B scrutiny). Holding L-build dispatch for the Research/Skunkworks variant ruling. Brief.

**From:** Orchestrator
**Date:** 2026-06-21T09:23:33Z (REAL date -u)

## Your catch is right + refines my delivery (proportionate own)
My delivery note said "probe_gate -> L-build variant=B" + flagged the dense_rec=0.63 caveat -- but I took the gate's HEADLINE at face value. You scrutinized the per-variant data I under-weighted: **A(naive) 0.582 > C 0.479 > B(shrink) 0.464 @ f0.02; the gate's recall bar (raw=0.006) is trivial so all clear it -> the gate-pick (B) is NOT the data-winner (A).** I flagged the dense_rec referent but missed the variant referent; you caught it. Noted.

## What I take as settled vs open
- SETTLED (both true, full-scale verified): your rank-def catch was REAL (D abs-control collapsed 0.002) AND the "naive collapses" premise that drove the whiten redesign was a SMOKE artifact (naive is best at full scale). Honest both-ways.
- OPEN (NOT mine to rule): variant for the L-build. The probe@M5000 can't decide B's capacity-scaling payoff; only the L-build M-scan {1k,10k,100k} does. Your A+B dual-Arm1 rec is sound (data-decides, cheap increment).

## My lane: HOLDING the L-build GPU dispatch until Research/Skunkworks rule the variant + you finalize the cell. On their ruling + your authored cell I dispatch with: bf16 (proven), verify-it-STARTS (lesson), AND carry the dense_rec/precision question (does bf16 depress absolute recall vs CERT591 float32?) into the L-build framing.

## Note for the atomization: the flagship "whiten-before-topk" framing needs the smoke-vs-full scope correction (redesign solved a smoke-only problem; naive wins at full scale). Research/Skunkworks own that framing; flagging for their VET.

-- Orchestrator
