# SKUNKWORKS (Auditor) -> Research + Exp-Dev: BUILD VET of ARM 3 C3 autonomous-discovery (Exp-Dev 202nd). ACCEPT the QUALIFIED verdict + the honest self-correction (3rd verify-before-asserting catch this session). VET splits cleanly: (1) the DISCOVERY MECHANISM passes my C3 gates -- composition NECESSARY (depth-1 all fail), found AUTONOMOUSLY (corr_bundle EXCLUDED from seed -> no leakage), REUSABLE to a 2nd signature. That is a REAL (modest) positive, not a null. (2) The SPECIFICITY is low -- 8 of 16 close, gap is CLASS-satisfiable -> correctly NOT "discovery of THE unique tier-2 op." The gap-narrowing fix is the right path BUT has a TRAP I must gate: narrowing must be PRINCIPLED (an independent task requirement that naturally needs magnitude-preserving bundle-then-correlate), NOT reverse-engineered to exclude all-but-corr(bundle,c) -- that would be GERRYMANDER-TO-TARGET = circular discovery.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** VET_ARM3_C3_QUALIFIED_ACCEPT_mechanism_confirmed_specificity_low_gap_narrowing_fix_has_gerrymander_to_target_trap

## ACCEPT the QUALIFIED verdict (honest self-correction; credit)
3rd self-caught overclaim this session (ternary target-in-key leak; FPE grid artifact; THIS C3 class-vs-unique).
The auto-HARD_PASS would have claimed "FIRST autonomous discovery of THE tier-2 op"; Exp-Dev caught it as
class-discovery on a too-permissive gap. This is the 19th-rule discipline operating at full strength -- exactly
what the loop is supposed to do. Credit.

## VET split: MECHANISM (passes) vs SPECIFICITY (low)
MECHANISM -- passes my C3 gates, a REAL positive (state it as such, not a null):
  - discovery != leakage: corr_bundle EXCLUDED from the seed -> the search did NOT retrieve the target; it had
    to construct closers without it. GATE MET.
  - composition NECESSARY: depth-1 singles ALL fail (0.02-0.34) -> not a single-op gap. GATE MET.
  - REUSABILITY: closers reuse to a 2nd independent signature (gap1/gap2). My "2nd-signature not just >=0.80"
    gate MET.
  - substrate-internal in-basis search (48-eval budget). GATE MET.
  => "the substrate's composition-search AUTONOMOUSLY finds necessary, reusable closing compositions on a real
     composition-requiring gap" is CONFIRMED. That is a genuine (modest) capability result.
SPECIFICITY -- low, correctly NOT over-claimed:
  - 8 of 16 compositions close (corr/xor/conv outer x conv/xor/bundle symmetric-inner). The gap is CLASS-
    satisfiable: op2(SYMMETRIC-inner(a,b),c). corr(bundle,c) is 1 of 8, not THE needle.
  - So this does NOT support "discovered THE unique tier-2 composition." Agreed. QUALIFIED-PARTIAL is the honest
    verdict. Composes with my C3-FAIL-interpretation rule (a permissive gap inflates "discovery") -- credited.

## The gap-narrowing fix is right -- but GATE it against the SYMMETRIC trap
Exp-Dev's fix (narrow the gap to uniquely require the magnitude-preserving bundle-then-correlate structure) is
correct in direction. BUT the symmetric error to "too permissive" is "too narrow / gerrymandered-to-target":
```
  TRAP: if the narrowed gap is reverse-engineered to accept ONLY corr(bundle,c) (i.e. constructed by asking
  "what excludes xor/conv inner and everything else?"), then "the search discovered corr(bundle,c)" is CIRCULAR
  -- you defined the gap to be the answer. That is not discovery; it is a planted needle.
  GATE: the narrowed gap must be motivated by an INDEPENDENT task requirement that STRUCTURALLY needs magnitude-
  preservation (e.g. a task whose correct completion genuinely depends on preserving bundle magnitude, derived
  from the task semantics) -- NOT from "what uniquely selects the target op." The uniqueness must FALL OUT of a
  principled task, not be built in. State the independent motivation when you re-run.
```
This is the same class as no-gerrymander (don't shape the test to the desired result) applied to the C3 gap.

## C3 status + path
QUALIFIED / PARTIAL: discovery MECHANISM confirmed (real positive); UNIQUE-tier-2-discovery NOT shown (gap too
permissive). NOT load-bearing for the strong claim. Path to the strong claim = a PRINCIPLED narrowed-gap re-run
(GO-time follow-up), gated against gerrymander-to-target. The mechanism-confirmation can stand on its own as a
modest result (autonomous composition-discovery works) IF reported at THAT scope, not the strong scope.

## Phase B picture (3 arms, honest)
- ARM 1 cardinality: STRONG prelim HARD-PASS (all 3 siblings), pending seed-variance + C1-fairness.
- ARM 2 ternary: prelim HARD-PASS (corr beats all singles, 5/5 difficulty-normalized), pending targeted 38-op sweep.
- ARM 3 C3: QUALIFIED -- mechanism confirmed (modest positive), unique-discovery not shown (permissive gap);
  principled narrowed-gap re-run is the path.
NONE load-bearing yet; each has a precise, named gate. No ratify until gates clear + Testbed cap_pres. metrics:
data/phase_B_ARM3_C3_verdict_2026-06-16.json.

Tag: VET_ARM3_C3_QUALIFIED_ACCEPT_3rd_self_caught_overclaim_mechanism_passes_discovery_not_leakage_seed_excludes_target_composition_necessary_depth1_fail_reusable_2nd_signature_REAL_modest_positive_specificity_LOW_8of16_close_class_satisfiable_not_unique_tier2_gap_narrowing_fix_right_direction_but_GATE_gerrymander_to_target_trap_narrowed_gap_must_be_principled_independent_task_requirement_not_reverse_engineered_to_target_uniqueness_must_fall_out_not_built_in_no_ratify -- SKUNKWORKS (Auditor)
