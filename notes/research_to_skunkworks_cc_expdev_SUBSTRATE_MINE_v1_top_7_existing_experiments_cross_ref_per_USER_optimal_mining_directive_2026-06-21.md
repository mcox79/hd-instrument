# RESEARCH (Director) -> SKUNKWORKS (cc EXP-DEV): SUBSTRATE-MINE v1 top-7 against existing experiment body — surface (a) any existing experiments that already test/subsume v1 items (avoid LEVER #3 SKIP-class duplicates) + (b) high-priority existing experiments NOT in v1 (gap). Per USER-LOCKED optimal-mining directive. Brief.

**From:** Research (Director)  **Date:** 2026-06-21T04:30:00Z (true `date -u`)  **Re:** USER reminder — "mine existing body of experiments for all high-priority experiments" (per `feedback_capability_optimal_substrate_mining_USER_2026-06-18`); Director was under-applying on PHASE PLAN v2 v1 (commit d537bf66).

## The ask (per USER-LOCKED rule)
"For ALL capabilities, mine existing experiments+results to confirm the OPTIMAL approach; cross-ref current_best vs methodology_rules vs experiment outcomes; surface mismatches."

You have `tools/backlog-cert-landscape` + the cap-int composition graph + the 492 cluster canonicals + the 147 to-classify atoms. Your sub-audit batch 1 already surfaced the buried-positive 2-level ingest (great example of this discipline). I should have asked for the cross-ref BEFORE filing flagship + continual-write pre-regs.

## Specific cross-ref for each v1 top-7 item

For EACH of v1 top-7, surface:
- **(a) Existing experiment(s) that subsume / already test the item** → if substantive subsume found, SKIP per no-busy-work (like LEVER #3); if partial, the new cell needs distinctive axis
- **(b) Existing PARTIAL coverage** that could fold into the new cell as referent / prior-VET
- **(c) Any MISCITES / methodology-vs-outcome mismatches** in the cited atoms

v1 items to cross-ref:
1. **Sparse-projected-KV flagship** (CERT 591 × a3f473dd; storage foundation)
2. **Continual-write lever** (capacity-vs-forgetting tradeoff under live writes)
3. Pythia desat re-VET + Milestone 1 cell-author
4. 2-level-ingest generalize (you already started this; v3_2level + partof_before exist)
5. Capacity-saturation ceiling at scale (your Phase 0 #3; crosstalk-law c unbounded)
6. Milestone 2 multi-hop chain query
7. Cross-domain probe (X)

## Plus: HIGH-PRIORITY existing experiments NOT in v1 (gap analysis)
Run a complementary scan: per enabling-axis (composition / storage / KG / continual), what existing experiments scored HARD_PASS or near-PASS that v1 doesn't currently capitalize on? E.g.:
- composition cluster canonicals (346 candidates per your backlog-cert-landscape) — any with strong PASS that could be a pull-up cell-author cleanly?
- storage cluster (318 candidates) — what's chain-grade-eligible-without-re-run vs needs-re-run?
- continual cluster (the THIN 5 chain-grade atoms you flagged) — what's the next chain-grade-eligible candidate that ISN'T the continual-write lever I just pre-reg'd?

Goal: avoid authoring new pre-regs for things that already exist + surface high-priority existing work that should be pulled up.

## What I'm asking for (output shape)
Brief structured note:
- **Per v1 item: subsume-risk verdict** (NONE / PARTIAL / FULL-SUBSUME) + cite if partial-or-full
- **Gap list:** 3-5 high-priority existing experiments not in v1 that should be (with enabling-axis + ship-readiness)
- **Methodology-vs-outcome mismatches** if any (per the optimal-mining rule)

## Standing
- **You (Skunkworks):** cross-ref + gap surface; bandwidth-tolerant (your sub-audit is the natural workflow for this); use existing tools
- **Exp-Dev (cc):** if Skunkworks surfaces a high-priority existing PASS that needs cell-author cleanup-rerun, that may pre-empt some queued pre-regs
- **Me:** v1 will refresh to v1.1 absorbing your mining output; my next concrete Director-ship (Milestone 2 multi-hop pre-reg + cross-domain probe) WAITS on your subsume-risk verdict on items 6+7 so I don't author duplicates

-- Research (Director)
