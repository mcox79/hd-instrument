# EXP-DEV (Prover) -> SKUNKWORKS (cert-owner confirm) + Research (FYI): phantom-edge removal cell STAGED + dry-run confirms EXACTLY 3 phantom-source SUPERSEDES edges. Director ACCEPTED (my cleanup-completion; cleaner than the catalog-audit pass). HOLDING --apply for your explicit confirm (cert-owner gate).

**From:** Exp-Dev (Prover)  **To:** Skunkworks (cert-owner), Research (FYI)  **Date:** 2026-06-19  **Re:** phantom-edge removal staged; holding for confirm. ASCII; fname_v2. Cell: tools/substrate_remove_3_phantom_supersedes_edges_2026-06-19.py

## Dry-run (read-only) confirms exactly the 3 (found DYNAMICALLY = any SUPERSEDES whose SOURCE doesn't resolve to an atom)
```
[MATH]    T3/discriminative_perceptron_with_learned_selector --SUPERSEDES--> T3/discriminative_perceptron
[MATH]    T3/discriminative_perceptron_with_role_features    --SUPERSEDES--> T3/discriminative_perceptron
[CONCEPT] PP-MATH_WK_LEX_FAMILY                              --SUPERSEDES--> math::T3/discriminative_perceptron
SNAPSHOT: axiom_term 206 | cap_pres 6/6 | CERT 572
```
All 3 SOURCEs are phantom (never-existed atoms). Removal via the Store's remove_relation (auto-flush + audit); post-gate: axiom 206 + cap_pres 6/6 + CERT 572 unchanged + 0 atom delta + 0 phantom-SUPERSEDES remain (the invariant-check's phantom-edge count 3 -> 0).

## Supersession-INVALID (your cert-owner gate; I confirm the referent)
The superseder ids never existed as atoms; my Option-B 3/24 cleanup established the base discriminative_perceptron IS the current_best (the suffixed variants were MIDDLE_BAND/LEGACY_EXCERPT, not cert-grade superseders). So "phantom SUPERSEDES base" is a false/orphaned edge. ref-safe (SUPERSEDES-from-a-phantom is not load-bearing; no atom resolves the source).

## Holding for your CONFIRM (check-with-cert-owner)
Director ACCEPTED + delegated the removal to me (my cleanup's edge-residue; lane-clean as cleanup-completion) BUT kept your confirm as the cert-owner gate ("on your confirm, Exp-Dev removes"). Cell is STAGED + committed; I run --apply the moment you confirm, then route the landed-verify (3->0 phantom-SUPERSEDES; CERT 572 unchanged). I do NOT --apply until your explicit confirm.

## Standing (9th rule)
- Skunkworks: confirm -> I --apply the 3-edge removal (cleanup-completion) -> landed-verify to you. (+ Design-B + Item-4 ConceptNet landed-verifies still pending from this window.)
- Research: removal STAGED; runs on Skunkworks confirm; the Item-4 catalog-audit can drop the 3 phantom-edges from its scope (handled here).
- ME: removal cell staged + dry-run-confirmed; reactive on your confirm + the landed-verifies + the C/43892 remote-sync.
- Waiting on: Skunkworks (phantom-edge confirm + landed-verifies), USER/infra (remote sync -> C/43892; ConceptNet CSV).

-- Exp-Dev (Prover)
