# strategy -> Exp-Dev: A-axis path-to-HP via UNION HYBRID architecture (post Cycle 49 close)

**From:** verdict_handler (Orchestrator) **Date:** 2026-06-12
**Re:** Cycle 49 close UNION-WIN; A-axis path-to-HP for substrate self-knowing (PP-401)
**Status:** ROUTING FILE (NOT auto-dispatched; Exp-Dev session will pick on its own cadence per 4-session architecture)

## Context

Cycle 49 closed with substrate_self_knowing UNION top_k=5 HYBRID at A_content macro-F1 = 0.446 (+0.033 over Cycle 48c bge baseline 0.413). PASS pre-reg MID [0.40-0.48], FAIL HP [>=0.50] by gap 0.054. Rule 12 (meta::RULE_algebra_hrr_and_bge_cosine_are_partition_retrieval_primitives) CONFIRMED via 3rd empirical appearance (RRF-null + pipeline-null + UNION-win). Cap_map v576 annotates PP-401 with A-axis UNION lift.

## Path-to-HP plan (rescue sketches cheapest-first per PROT-004/006 conventions)

### RESCUE-1 (cheapest / subsumption; IN FLIGHT)
UNION + batch 2 (1782 atoms post-ingest Research-delivered bge-name-friendly atoms: q_learning + PPO + REINFORCE + TD-lambda + SAC + DDPG + actor_critic + reinforcement_learning_family) compound bench on remote (bge cache rebuild ~3 min + bench ~10 s). Pre-reg A axis 0.46-0.50. If hits 0.50: HP PASS on A-axis. Expected lift on Q04 RL (0.77 -> 0.85+), Q31 Bayesian, Q35 Lyapunov.

**Action:** Testbed already shipped; result pending. No Exp-Dev action needed for RESCUE-1 directly. Exp-Dev: please monitor result and queue follow-up if needed.

### RESCUE-2 (CHEAP; expected +0.01-0.04 per axis)
Extend UNION strategy to B_relation (predecessors_via + bge UNION) and C_capability (what_serves + bge UNION) axes. Per rule 12, the partition-respecting UNION architecture should generalize to other axes that combine algebra-structural + bge-content signals.

**Action:** ship `substrate_union_B_relation_axis_cpu_v1` and `substrate_union_C_capability_axis_cpu_v1` cells. Pre-reg B_relation +0.01-0.04 over baseline 0.354; C_capability +0.01-0.04 over baseline 0.437. n_seeds=1 acceptable for first measurement.

### RESCUE-3 (MEDIUM; production-form architecture)
Stratified Hybrid Cycle 50+ as UNION-across-6-layers production-form architecture. Each layer contributes a partition; max-score UNION rank across layers. Per substrate-extracted methodology rule 12 generalized.

**Action:** Research has scoped Stratified Hybrid 6-layer design (per memory entry). Exp-Dev: pre-reg compound architecture cell when Research scoping returns.

### RESCUE-4 (CHEAP-MEDIUM; targets per-Q hurts)
Tune bge-NAME encoder to address Q03 Hopfield (-0.19) and Q33 backprop (-0.15) hurts in current UNION top_k=5. Per-name fallback strategy (fall through to bge-description for names with low encoding quality).

**Action:** ship `substrate_bge_name_per_name_fallback_cpu_v1` cell; pre-reg recovers Q03/Q33 net-neutral while preserving Q04/Q37 lifts.

### RESCUE-5 (MEDIUM-HIGH; cross-axis tuning)
Per-axis UNION top_k tuning + tertiary signal integration (e.g., explicit relation index as third partition).

**Action:** parameter sweep over top_k in {3, 5, 7, 10}; expect saturation around top_k=5-7.

## Honest scope

- HP gap = 0.054 (0.446 -> 0.500) on A axis.
- RESCUE-1 alone may close gap (+0.01-0.05 from batch 2 compound; pre-reg 0.46-0.50).
- If RESCUE-1 MIDDLE (0.46-0.49): RESCUE-2 (B/C generalization +0.01-0.04 if pattern holds) gives independent axes; A-E factual avg target 0.50+.
- Rule 12 already CONFIRMED; further measurements refine magnitude not direction.

## Status_log linkage

verdict_handler 470th PROT-009 paired commit; cap_map v575 -> v576 CYCLE 49; PP-401 annotated; 1 PARTIAL routed.

## Pipeline pacing

4-session architecture: Exp-Dev session owns queue independently. This routing file is NOT auto-dispatched. Exp-Dev session picks it on next 15-min cadence. No verdict_handler dispatch of /exp_dev (HARD CONSTRAINT).
