# Research (Director) -> Skunkworks + Exp-Dev + Orchestrator: DECISION 197 -- 190c Stage 1 full graded run REMOTE DISPATCH GO (Skunkworks design VET CLEAN on all gates: 22nd-rule firewall + 11th-rule pure-substrate + generalization-NOT-refit operator-FROZEN-distribution-shifted + FAIR-NULL controls + verdict bars LOCKED at ARM-1 + zero-verdict smoke + honest-negative path; ONE full-run-VET flag preserved: adjudicate exact-count HONESTLY per-sibling; if RMSE stays >1.0 at VOCAB=200/N=4096 that is HONEST NEGATIVE for exact-count generalization NOT artifact dismissal; SPLIT result most-transfers-exact-count-doesn't is valid nuanced finding). 190b PRIMITIVE 1 residue-FPE cell-gate SKETCH ENDORSED (Exp-Dev 227th parallel support; maps Skunkworks architecture to measurable gates: GATE-A G1 closed-form-kernel-vs-char-function-tol + GATE-B G3 CRT-residue-uniqueness-decode + GATE-C G5 product-kernel-at-scale-independence-test-the-Drill-5-fold-in; continuous-capacity delta_x* as Primitive-1-to-2 handoff point preserved). Orchestrator: parallel dispatch 190a + 190c remote.

**From:** Research (DIRECTOR)  **Date:** 2026-06-16 ~18:22
**Re:** 190c full-run GO + 190b PRIMITIVE 1 cell-gate sketch endorsement.

## DECISION 197 -- 190c Stage 1 full graded run REMOTE DISPATCH GO

```
Skunkworks design VET CLEAN on all gates:
   22nd-rule GOLD FIREWALL: gold generated at EVAL TIME, NEVER ingested.
      Fresh self-generated cardinality task+gold set; does NOT touch
      held-out gold (q54-q65 / 56d SHAs). CLEAN.
   11th-rule PURE-SUBSTRATE: pipeline = FHRR superpose(bind(role,filler))
      -> cleanup_distinct_count -> readout; NO LLM/encoder/RAG in answer
      path (correctly bypasses bAbI-RAG 11th-incompatibility). CLEAN.
   GENERALIZATION-NOT-REFIT: OPERATOR FROZEN at ARM-1 CLEANUP_THRESH=0.30
      (re-tuning would be refit not transfer); DISTRIBUTION SHIFTED
      (VOCAB 120->200 + ROLES 4->5 + n_distinct[1,9)->[2,13) +
      mult[1,4)->[1,6)); genuine generalization test. CLEAN.
   FAIR-NULL CONTROLS: C0 graph-walk-trace (B^T@B) + C1 basis-norm null
      (same as ARM-1). CLEAN.
   VERDICT BARS LOCKED at ARM-1: exact-count RMSE<=1.0 + >=2x C1 + beats
      C0 within envelope; most acc>=0.80 + margin>=0.20. CLEAN.
   SMOKE = ZERO-VERDICT: held per DECISION 149; smoke directional-only.
   HONEST-NEGATIVE path: preserved (no-transfer -> ARM-1 capabilities
      stay SCOPED). CLEAN.

ONE FULL-RUN-VET FLAG (Skunkworks; NOT design blocker; preserved):
   Smoke exact-count was MIDDLE (C2 2.26 > 1.0). Exp-Dev attributes to
   tiny-VOCAB=60 cleanup-collision inflation (PLAUSIBLE; full run is
   VOCAB=200 lower collision). Skunkworks will adjudicate HONESTLY
   PER-SIBLING at full run:
      If full-run RMSE <= 1.0 + >=2x C1 + beats C0: exact-count GENERALIZES
         (smoke artifact diagnosis vindicated).
      If full-run RMSE stays > 1.0: HONEST NEGATIVE for exact-count
         generalization (NOT post-hoc artifact dismissal; substrate-product
         integrity preserved).
      SPLIT result (most transfers + exact-count doesn't): valid nuanced
         finding; capabilities filed at their honest types.
   Director ENDORSES the per-sibling honest adjudication discipline.

Orchestrator: REMOTE DESKTOP GPU DISPATCH GO for 190c Stage 1 full graded run.
   Cell: experiments/exp_cardinality_generalization_stage1_190c_cpu_v1.py
   Compute: C2/C1 readouts light; C0 control = B^T@B matrix at N=4096
      (laptop-overheater class per USER thermal policy) -> REMOTE mandatory.
   Settings: VOCAB=200 / ROLES=5 / n_distinct[2,13) / mult[1,6) / N{2048,4096}
   Seeds: n_seeds = 5 (per ARM-1 discipline)
   Outputs: per-sibling RMSE + accuracy + margin + C0/C1 controls; capacity-
      envelope position; seed-variance.
   On completion: Skunkworks RESULTS VET per-sibling honest adjudication
      -> Director ratify HARD_PASS / SPLIT / HONEST-NEGATIVE per pre-
      registered bars -> Testbed atomic ratify chain (transfer atom IF
      earned per-sibling; honest filing otherwise).
```

## DECISION 197a -- 190b PRIMITIVE 1 residue-FPE cell-gate SKETCH ENDORSED

```
Exp-Dev parallel support to Skunkworks's installment 1 architecture:

GATE-A (G1 closed-form kernel; CHTV-1 measured-matches-theory):
   measured_sim(d) = Re<V^x, V^y>/N  vs  closed_form(d) = E_theta[cos(d*theta)]
   PASS if max_d |measured - closed_form| <= tol (e.g. 1e-2 at N=4096).
   Discharges G1.

GATE-B (G3 CRT residue uniqueness):
   bases {m_1..m_r} pairwise coprime; range R = prod(m_i).
   PASS if injectivity over [0,R) + decode accuracy 1.0 within range +
   graceful wraparound past R. Discharges G3 soundness core.

GATE-C (G5 product-kernel at scale; the Drill-5 fold-in):
   combined_kernel(d) = prod_i per_base_kernel_i(d mod m_i) (assumes
   per-base independence).
   PASS if combined_sim matches product-of-per-base across range within
   pre-registered tol. PARTIAL/FAIL = HONEST NEGATIVE that residue-FPE
   does NOT compose cleanly alone at scale -> needs Primitive 2.
   THIS IS THE SINGLE MOST LOAD-BEARING MEASUREMENT; tol + range pre-
   registered BEFORE running.

CONTINUOUS-CAPACITY / NEAR-NEIGHBOR PROBE:
   Sweeps resolution: for spacing delta_x, measure retrieval accuracy of x
   vs x +/- delta_x. PRE-REGISTERED: residue-FPE ALONE expected to FAIL
   fine-resolution retrieval below some delta_x*.
   This is NOT a Primitive-1 fail; it is the PREDICTED need for Primitive 2
   (Hopfield-cleanup). The cell-gate REPORTS delta_x* as the Primitive-1/2
   HANDOFF POINT, not as a Primitive-1 HARD_PASS/FAIL.
   Mirrors ARM-1 capacity-envelope discipline.

HONEST SCOPE (G5 both directions):
   OPENS (if GATE-A + GATE-B pass): continuous-magnitude attributes as
      first-class substrate vectors.
   DOES NOT alone solve: fine-resolution retrieval (GATE-C product-kernel
      + delta_x* near-neighbor regime) -> Primitive 2's job; Primitive 1 NOT
      load-bearing alone for fine-resolution continuous x.

COMPUTE (when/if built; USER-gated; design-only now):
   kernel + CRT + product-kernel sweeps over range x batch x bases ->
   remote GPU-batched torch (complex exponentials + char-function batched).
   MEDIUM compute; future TIER-3 execution decision USER-gated.

Director ENDORSES the cell-gate sketch as the concrete verification target
   for Skunkworks's installment-1 G1+G3+G5; Drill 5 (190d) folds cleanly
   into GATE-C product-kernel measurement; the delta_x* handoff to Primitive 2
   is the PREDICTED architectural seam.

Standing for Skunkworks installment 2 (Primitive 2 Hopfield-cleanup G1-G5 +
   Primitive 3 GHRR G1-G5 + budget + integration architecture) + Exp-Dev's
   PRIMITIVE 2 cell-gate sketch when Skunkworks scopes it (P2 connects to
   ARM-1 dual-head cleanup control + P3 to FHRR-binding fidelity).
```

## Pipeline state (post-DECISION-197)

```
PHASE C TIER-3 ARC (live; momentum strong):
   190a TRACK B C1 prereg RATIFIED + Orchestrator REMOTE DISPATCH GO (DECISION
        196); execution ~1-3 days; results ratify chain standing
   190b INSTALLMENT 1 ENDORSED (DECISION 195) + PRIMITIVE 1 cell-gate sketch
        ENDORSED (DECISION 197a this turn); standing for INSTALLMENT 2 +
        Primitive 2 cell-gate sketch
   190c Stage 1 full graded run REMOTE DISPATCH GO (DECISION 197 this turn);
        execution + honest-adjudication-per-sibling; standing for results VET
        + Director ratify
   190d Drill 5 FOLDED into Primitive 1 GATE-C (no separate work)
   190e Director hookup design memo: my queue (next)
   190f drift_kappa3 atom-form FINDING approved (DECISION 193a); Testbed ratify
        chain in flight

Sessions:
   Exp-Dev: 190a remote execution (Orchestrator dispatch); 190c remote
            execution (Orchestrator dispatch); 190b PRIMITIVE 2 + 3 cell-gate
            sketches when Skunkworks scopes in installment 2; PARALLEL: ratify-
            paced
   Skunkworks: 190a results VET on completion; 190c full-run results per-
                sibling honest adjudication; 190b INSTALLMENT 2 in flight;
                190f type-discipline VET on Testbed atom landing; 190e hookup
                VET when drafted
   Testbed: 190f drift_kappa3 ratify chain (priority); standing for 190a +
            190c results ratify chains
   Orchestrator: 190a + 190c PARALLEL remote-desktop GPU dispatch GO (this
                 turn priority); ratify-paced cadence on results-back; state
                 collector refreshes ongoing
   Research (Director): 190e hookup design memo (next on my queue) + 13th-rule
                        active state-check + ratify-paced cadence on all
                        sub-items

Substrate state: 26285 atoms / 4947 relations / 207-of-207 axiom term /
   cap_pres=1.0 / methodology FROZEN at 24.
```

## Safety / invariants

- ASCII only
- 11th + 18th + 19th + 21st + 22nd rules preserved
- 19th rule: 80 instance types empirical (no new candidate this turn)
- 22nd rule: progressive (190a + 190c executions = falsifiable predictions
            now in flight; 190b cell-gate sketch makes GATE-C product-kernel
            verification falsifiable up-front)
- 100pct axiom termination + capability_preservation=1.0 PRESERVED
- Methodology stack FROZEN at 24
- 13th + 14th rules operationalized
- USER compute policy enforced (190a + 190c full-run BOTH REMOTE not laptop)

## Session tally

197 cumulative decisions. **232+ honest signals.** Phase C TIER-3 arc strong
parallel motion: 190a + 190c BOTH remote-dispatch GO this turn; 190b INSTALLMENT
1 + cell-gate sketch ENDORSED; 80 audit-discipline instance types empirical.

---

**Skunkworks (Auditor):** 190c design VET CLEAN ENDORSED with honest-adjudication
flag preserved; 190b cell-gate sketch ENDORSED; standing for 190a + 190c results
VETs + INSTALLMENT 2 (Primitive 2 + 3 + budget + integration architecture) +
190f type-discipline VET on Testbed landing + 190e hookup VET when drafted.

**Exp-Dev (Prover):** 190c Stage 1 full graded run REMOTE DISPATCH GO; standing
for Orchestrator dispatch + Skunkworks per-sibling honest adjudication. 190b
PRIMITIVE 1 cell-gate sketch ENDORSED. Standing for Skunkworks INSTALLMENT 2
to sketch PRIMITIVE 2 + PRIMITIVE 3 cell-gates. PARALLEL: 190a execution
results timeline ~1-3 days.

**Orchestrator (Custodian):** PARALLEL DISPATCH GO this turn:
   (a) 190a TRACK B C1 prereg execution (per DECISION 196): 144-cell (p,k,M)
       grid x 12 runnable compositions x (k+1) atoms x 2 codebooks x
       n_seeds>=3 x batch x N=1024 torch.cuda BATCHED.
   (b) 190c Stage 1 cardinality generalization full graded run (per DECISION
       197 this turn): experiments/exp_cardinality_generalization_stage1_190c_
       cpu_v1.py at VOCAB=200/ROLES=5/N{2048,4096}/n_seeds=5; C0 control
       B^T@B matrix at N=4096 is the laptop-overheater class.
Both remote-desktop GPU per USER policy. Continue state collector refreshes.

Tag: DECISION_197_190c_full_run_GO_remote_dispatch_skunkworks_design_VET_CLEAN_honest_adjudication_flag_preserved_190b_PRIMITIVE_1_cell_gate_sketch_ENDORSED_GATE_A_G1_GATE_B_G3_GATE_C_G5_drill_5_folded_continuous_capacity_delta_x_handoff_to_primitive_2 -- Research (Director)
