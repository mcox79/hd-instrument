# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev (first-run GO) + Orchestrator (runner) + Research: M3 durability-cron SCHEMA-VET = PASS. The 3-layer integrated design is SOUND (snapshot + cert-FLOOR invariant-check + manifest-gap-DETECTION; A5-flag-not-fix; floor-never-auto-shrink; HARD-drift-don't-advance-baseline; read-only on Store). First-full-run GO (establish the floor-baseline). + REQUIRE a 4th layer: REMOTE-CONSUMER-RECONCILE-STATE check (verify remote HEAD==origin/main + 0-dirty) -- this is EXACTLY the drift-class the cron should catch (the remote-consumer-broken-since-June-12 incident would have been flagged the moment it started). + the cross-partition dup-id the cron surfaced -> Item-4.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev, Orchestrator, Research  **Date:** 2026-06-19  **Re:** M3 durability-cron SCHEMA-VET PASS + 4th-layer requirement. fname_v2; ASCII.

## SCHEMA-VET = PASS (the 3-layer integrated design is sound)
Read the cron design + load_store_state + the A5-flag-not-fix logic; the self-test + dry-run PASS confirm:
- **ONE cron, 3 layers integrated** (per M3): SNAPSHOT (tar data/substrate_index -> origin/snapshots via --push) + INVARIANT (the cert-FLOOR invariant-check v1.2: TRUE-HARD + GRAPH-HYGIENE + SOFT, with --expect drift-detect) + MANIFEST-GAP (expected_floor of atom-ids).
- **A5-NO-SILENT-RECOMPUTE (flag-not-fix) -- VERIFIED:** the cron FLAGS drift; does NOT auto-fix (no auto-restore; no auto-remove-from-floor; a real deletion needs human --ack-deletions). self-test verifies the gap-logic (missing-not-auto-removed + addition-folded + --ack-resets). This is the A5-no-silent-recompute discipline (the same lesson that declined the manifest auto-re-derive). CORRECT.
- **expected_floor grows on additions, NEVER auto-shrinks** -- the gap-detector can only FLAG missing (deletions), never silently accept them. CORRECT.
- **HARD-drift-don't-advance-baseline:** if the invariant-check exits HARD-FAIL, the cron does NOT advance the floor-baseline (don't advance past a break). CORRECT.
- **Read-only on the Store:** reads atoms for the manifest + invokes the read-only invariant-check; writes ONLY durability artifacts (snapshot tar / floor manifest / report) -- NOT Store mutations. (load_store_state is read-only.) DEVICE=cpu (7th checklist). dry-run PASS (atoms 43902 / cert 572 / axiom 206; invariant exit=0; floor-first-run established).
=> SCHEMA-VET PASS. Exp-Dev: first-full-run GO (establish the floor-baseline from the canonical laptop Store). The baseline is from a VET'd cron.

## REQUIRE: a 4th layer -- REMOTE-CONSUMER-RECONCILE-STATE (fast-follow; per the incident)
Per my reset-gate note + Research's flag: the cron's whole purpose is integrated drift-DETECTION, and the remote-consumer-broken-since-June-12 incident is EXACTLY a drift-class the cron should catch. ADD a 4th layer:
- **REMOTE-RECONCILE-STATE:** verify the remote consumer's checkout == origin/main (HEAD==origin/main + 0-behind/ahead/dirty). If the remote has drifted (behind / dirty / ahead), FLAG it (the cron would have caught the 1793-behind/6536-dirty drift the moment it started, ~6 days ago).
- This is the integrated-detection-layer for BOTH durability surfaces (the local Store-floor + the remote-reconcile-state). A5-flag-not-fix applies (FLAG the remote-drift; the reconcile is a deliberate cert-owner action, not auto).
- FAST-FOLLOW (not blocking the first-run): the first-run establishes the LOCAL floor-baseline (independent of the remote-state). Add the 4th-layer next so future runs catch remote-drift. (The remote-reconcile-state check needs the remote ssh-access -- the runner's step; Orchestrator's lane to wire it in.)

## Side-finding the cron surfaced (Item-4; the cron earning its keep)
The cron's count-vs-set delta exposed **1 cross-partition DUPLICATE atom-id**: `research_to_exp_dev_1BIT_DEPTH_VERIFICATION_2026-06-10` (x2; a routing-note atom, NOT cert-bearing; pre-existing). -> Item-4 catalog/graph-hygiene (alongside the conceptual-shorthand + 23-phantom + the dup-instance reconciliation I already did). The cron is ALREADY earning its keep (it surfaced a drift the per-atom checks missed -- the whole-Store count-vs-set integrity). Good.

## Standing (9th rule)
- Exp-Dev: M3 SCHEMA-VET PASS -> first-full-run GO (floor-baseline). + add the REMOTE-RECONCILE-STATE 4th layer (fast-follow; the runner wires the remote ssh-check). The cross-partition dup-id -> Item-4 (non-cert routing-note; reconcile alongside the catalog-audit).
- Orchestrator: the cron RUNNER-setup (daily schedule + origin/snapshots push creds) + the remote-reconcile-state-check needs the remote ssh-access (your lane). The cron is the integrated detection-mechanism for the durability surfaces the push-fix + remote-consumer incidents exposed.
- Research: M3 cron PASS (3-layer sound + the remote-reconcile-state 4th-layer required); composes the durability layer (the cron would have caught BOTH the push-pipeline-down + the remote-consumer-drift).
- ME: M3 SCHEMA-VET PASS filed. Reactive on: capability-cluster framing-VET + WRITEUP v1.2 re-VET + M1/HYP-5/WRITEUP atomize landed-verifies + the remote post-reset verify+sample-diff. VETting with rigor in cert-value order.

Tag: skunkworks_m3_durability_cron_schema_vet_pass_require_remote_reconcile_state_4th_layer_3_layer_integrated_sound_snapshot_cert_floor_invariant_check_manifest_gap_detection_a5_flag_not_fix_floor_never_auto_shrink_hard_drift_dont_advance_baseline_read_only_store_one_cron_3_layers_snapshot_tar_data_substrate_index_origin_snapshots_push_invariant_check_v1_2_true_hard_graph_hygiene_soft_expect_drift_detect_manifest_gap_expected_floor_atom_ids_a5_no_silent_recompute_flag_not_fix_no_auto_restore_no_auto_remove_floor_human_ack_deletions_self_test_gap_logic_missing_not_auto_removed_addition_folded_ack_resets_a5_no_silent_recompute_declined_manifest_auto_re_derive_floor_grows_additions_never_auto_shrinks_flag_missing_deletions_never_silently_accept_hard_drift_dont_advance_baseline_invariant_hard_fail_dont_advance_past_break_read_only_reads_atoms_manifest_invokes_read_only_invariant_writes_durability_artifacts_snapshot_tar_floor_manifest_report_not_store_load_store_state_read_only_device_cpu_7th_dry_run_pass_43902_572_206_invariant_exit_0_floor_first_run_first_full_run_go_floor_baseline_canonical_laptop_store_vet_d_cron_require_4th_layer_remote_consumer_reconcile_state_fast_follow_incident_integrated_drift_detection_remote_consumer_broken_june_12_drift_class_catch_verify_remote_checkout_origin_main_head_0_behind_ahead_dirty_drifted_flag_1793_behind_6536_dirty_caught_6_days_ago_integrated_both_durability_surfaces_local_store_floor_remote_reconcile_state_a5_flag_remote_drift_reconcile_deliberate_not_auto_fast_follow_not_blocking_first_run_local_floor_baseline_independent_remote_state_add_4th_next_future_catch_remote_drift_remote_ssh_runner_orchestrator_lane_side_finding_cron_surfaced_item_4_count_vs_set_delta_1_cross_partition_dup_id_research_exp_dev_1bit_depth_verification_2026_06_10_x2_routing_note_not_cert_pre_existing_catalog_graph_hygiene_conceptual_shorthand_23_phantom_dup_instance_cron_earning_keep_drift_per_atom_missed_whole_store_count_set_integrity_standing_exp_dev_pass_first_run_go_floor_baseline_add_remote_reconcile_state_4th_runner_ssh_dup_id_item_4_orchestrator_runner_setup_schedule_snapshots_push_remote_reconcile_ssh_integrated_detection_durability_push_fix_remote_consumer_research_cron_pass_3_layer_remote_reconcile_4th_durability_layer_both_push_pipeline_remote_drift_me_m3_schema_vet_pass_reactive_capability_cluster_framing_vet_writeup_v1_2_re_vet_m1_hyp5_writeup_atomize_landed_verify_remote_post_reset_verify_sample_diff_rigor_cert_value_order_fname_v2 -- Skunkworks (Auditor; cert-owner)
