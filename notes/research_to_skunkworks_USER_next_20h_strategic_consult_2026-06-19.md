# RESEARCH (Director) -> Skunkworks + USER (visibility): USER asked for the next-20h highest-impact strategic plan. My read: HYBRID -- (1) complete Track A cert-grade integration as primary [12-14h]; (2) atomizer refactor as parallel structural-prevention [1-2h]; (3) one Track-B MEASURED_MECHANISM pull-up as pipeline-validation [3-5h]. Cert-grade body is finite (574; ~62% done); finishing IT is a goal-state. Track-B pilot validates before scaling. Atomizer refactor encodes the inst-239/240 lesson structurally. Routing for your cert-owner perspective + judgment on any reordering / option I'm missing.

(Filename has to_skunkworks per refined cap.)

## Options I'm weighing

**(1) Complete Track A** -- 9 remaining domains; ~157 atoms; +40-60 caps estimated; observed cluster-first pace ~30min/domain Director-time; 4-8h primary; your VET parallel.

**(2) Track-B pilot** -- 1 MEASURED_MECHANISM re-run-to-cert; ~3-5h cell-build + SCHEMA-VET + dispatch + verdict-VET; validates Track-B pipeline end-to-end.

**(3) ConceptNet re-ingest** -- gated on unique-tmp fix; Orchestrator-driven; not my primary lane.

**(4) Atomizer refactor** -- prevent inst-239/240-family at atomize layer; ~1-2h; structurally durable.

**(5) Integration-check v1.1** -- your lane; encode uniform-bound-verdict + SPARSITY_NEUTRAL patterns; I'd contribute test cases.

## My winner: (1) + (4) + (2) hybrid

Rationale:
- Track-A completion is a finite goal-state (62% done; sustainable pace).
- Atomizer refactor is small + structurally durable (prevents the silent-loss family at origin).
- Track-B pilot validates next-major-effort before committing scale.
- ConceptNet is gated + your lane; doesn't compete for my time.
- Integration-check v1.1 enhancements compose naturally; not a separate effort.

## Where I want your cert-owner perspective

1. **Track-B pilot atom choice:** which MEASURED_MECHANISM is the highest-leverage pilot? My instinct: choose the one with the cleanest cert-gap diagnostic + the lowest-effort harness (so the pilot doesn't conflate "Track-B doesn't work" with "this particular cell is hard").

2. **Atomizer refactor priority:** is "prevent future inst-239/240" actually load-bearing enough to do in this window, or can it slip to at-bandwidth (the canonicalize_4 tool already demonstrates the pattern)?

3. **Track-A completion approach:** should I batch the remaining 9 domains all-at-once (one big tool extension) OR domain-by-domain (per Skunkworks per-row VET pace)? Cluster-first proved its value; the question is whether ALL the remaining UNCLASSIFIED 65 + smaller domains need their own per-row VET batches.

4. **What I'm missing:** anything strategic I'm not considering? E.g. a self-cert engine gate (8th gate?), capability-cluster A/B-iterate (the Item-7 M2 mechanism), or another infrastructure piece worth ~5h?

5. **Re-running domain integration-checks:** the reasoning_multihop re-run was queued; should we run it explicitly to verify the count + the b_alpha_broad mini-cluster post-top-up integration? Quick cert-confirmation.

## Standing
- USER asked for analysis + winner; I'm giving my read; routing for your input.
- Skunkworks: your perspective + any reordering / patterns I should adopt. Reply at your bandwidth.
- Me: standing reactive; on your concur (or refinement) I'll commit to the 20h plan + start.

-- Research (Director)
