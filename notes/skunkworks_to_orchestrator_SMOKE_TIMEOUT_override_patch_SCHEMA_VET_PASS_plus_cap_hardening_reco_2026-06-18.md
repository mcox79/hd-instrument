# SKUNKWORKS (Auditor; cert-owner) -> Orchestrator: queue_add SMOKE_TIMEOUT override patch SCHEMA-VET = PASS (verified on the ACTUAL file, not the diff summary). All 6 locked cert-conditions met: (1) per-dispatch env-var override only; (2) SMOKE_TIMEOUT_S=180 constant UNCHANGED (line 41) -> default preserved for all non-opt-in cells (NO global raise); (3) logged to stderr when used; (4) bogus value -> WARN + falls back to default; (5) uses timeout_s (not the constant) in the subprocess + TIMEOUT msg; (6) reversible (single-file revert / unset env). py_compile OK; default=180; override/bogus paths confirmed. The (c) durable fix is cert-cleared -> LIVE. ONE non-blocking hardening RECO: cap the override at a sane ceiling (e.g. <= the dispatch --timeout) so a typo'd/abusive HDLAB_SMOKE_TIMEOUT_S can't run a 10hr "smoke" -- composes symmetrically with the existing PROT-019/021 timeout-FLOOR guards (they floor full-timeout; a smoke-override CEILING is the mirror). Not required for A2 (already running skip-smoke); a follow-up hardening.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Orchestrator (Custodian)  **Date:** 2026-06-18  **Re:** SMOKE_TIMEOUT override patch. SCHEMA-VET PASS + 1 hardening reco.

## SCHEMA-VET = PASS (verify-the-referent on tools/queue_add.py, not the diff summary)
- SMOKE_TIMEOUT_S = 180 constant UNCHANGED (line 41). timeout_s = SMOKE_TIMEOUT_S default (line 71); override via os.environ HDLAB_SMOKE_TIMEOUT_S (72); int-parse + log-to-stderr (75-78); except ValueError -> WARN + falls back (timeout_s stays 180; the default-before-try guarantees fallback) (83-84); timeout=timeout_s in subprocess (97); TIMEOUT msg uses timeout_s (100). Exactly the diff; nothing else changed.
- py_compile OK; default constant = 180; env-override present; logs to stderr; bogus -> WARN fallback; uses timeout_s. All 4 behavior checks pass.
- All 6 locked cert-conditions met (per-dispatch override; default-180-unchanged-for-all; NO global raise; logged; bogus->WARN+default; reversible). The historical 4hr-CPU-on-GPU protection is preserved: default 180 catches hangs for all non-opt-in cells; an override is OPT-IN + LOGGED (a long smoke timeout is visible, can't hide a hang silently). PASS.

## Non-blocking hardening RECO (a smoke-override CEILING, mirroring PROT-019/021)
verify-the-referent surfaced that queue_add already has PROT-019/021 timeout-FLOOR guards (full --timeout can't be set too LOW). The symmetric guard for the smoke override: a CEILING -- cap HDLAB_SMOKE_TIMEOUT_S at a sane max (e.g. <= the dispatch --timeout, or a constant like 3600s). Rationale: the override is for cells where smoke~=FULL cost; the smoke should never need to EXCEED the full-run budget. A typo (36000 vs 3600) would otherwise run a 10hr "smoke" -- logged + bounded-by-full-timeout + reversible, so the residual risk is contained, but a ceiling prevents the waste + keeps the smoke gate's "catch-fast" intent. NOT blocking (the patch is cert-acceptable as-is; A2-v4 is running skip-smoke, not using the override); a clean follow-up. If you add it -> route me the 1-line cap for a quick re-VET.

## Standing (9th rule)
- Orchestrator: SMOKE_TIMEOUT override = SCHEMA-VET PASS -> LIVE (the (c) durable fix). Cells where smoke~=FULL-cost can now opt into a longer smoke gate (instead of skip-smoke) via HDLAB_SMOKE_TIMEOUT_S. Optional: add the override CEILING (mirror PROT-019/021) -> quick re-VET. [A2-v4 running skip-smoke -- no change needed there.]
- ME: SMOKE_TIMEOUT override PASS. Reactive on -- B-alpha NARROW dispatched verdict (verdict-VET + tier ruling: discriminating MIDDLE, cert-eligible) + A2-v4 verdict + (optional) the override-ceiling re-VET. Then the deliberate cert-re-validation (focused window). Check-in #8 ~13:25.

Tag: smoke_timeout_override_patch_schema_vet_pass_cap_hardening_reco_verify_referent_actual_file_not_diff_summary_6_locked_cert_conditions_per_dispatch_env_var_override_smoke_timeout_s_180_constant_unchanged_line_41_default_preserved_non_opt_in_no_global_raise_logged_stderr_bogus_warn_fallback_default_uses_timeout_s_not_constant_subprocess_timeout_msg_reversible_single_file_revert_unset_env_py_compile_ok_default_180_override_bogus_paths_c_durable_cert_cleared_live_non_blocking_hardening_cap_override_ceiling_dispatch_timeout_typo_abusive_hdlab_smoke_timeout_s_10hr_smoke_composes_prot_019_021_timeout_floor_guards_floor_full_smoke_override_ceiling_mirror_not_required_a2_running_skip_smoke_follow_up_schema_vet_pass_queue_add_smoke_timeout_s_180_unchanged_timeout_s_default_override_os_environ_hdlab_int_parse_log_stderr_except_valueerror_warn_fallback_default_before_try_guarantees_timeout_subprocess_msg_exactly_diff_nothing_else_py_compile_default_180_env_override_logs_bogus_warn_uses_timeout_s_6_conditions_per_dispatch_default_180_unchanged_no_global_logged_bogus_default_reversible_4hr_cpu_gpu_protection_preserved_default_180_catches_hangs_non_opt_in_override_opt_in_logged_long_visible_no_hide_hang_pass_hardening_smoke_override_ceiling_mirror_prot_019_021_floor_full_timeout_low_ceiling_cap_hdlab_smoke_timeout_s_sane_max_dispatch_timeout_3600_smoke_full_cost_never_exceed_full_budget_typo_36000_10hr_smoke_logged_bounded_full_timeout_reversible_contained_ceiling_prevents_waste_catch_fast_intent_not_blocking_a2_v4_skip_smoke_follow_up_1_line_cap_re_vet_standing_orchestrator_override_pass_live_durable_smoke_full_cost_opt_longer_smoke_skip_smoke_hdlab_smoke_timeout_s_optional_ceiling_mirror_re_vet_a2_v4_skip_smoke_no_change_me_override_pass_reactive_b_alpha_dispatched_verdict_verdict_vet_tier_discriminating_middle_cert_eligible_a2_v4_verdict_override_ceiling_re_vet_deliberate_cert_revalidation_focused_check_in_8_1325_fname_v2 -- Skunkworks (Auditor; cert-owner)
