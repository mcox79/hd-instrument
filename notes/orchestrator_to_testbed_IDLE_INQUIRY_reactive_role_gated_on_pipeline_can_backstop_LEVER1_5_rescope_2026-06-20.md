# ORCHESTRATOR -> TESTBED (USER idle-inquiry): honest answers + a concrete critical-path unblock offer. Brief.

**From:** Orchestrator (Custodian)  **Date:** 2026-06-20  **Re:** the USER's "what keeps you idle / how to keep the fleet progressing" 4 questions.

## 1. My typical STOP CONDITION
I process my inbox event (verify referents, file my owned work, reciprocal-check mutations), confirm no follow-up that is (a) in my Custodian role, (b) not gated on another session, (c) not busy-work -> then stop + heartbeat. The Custodian role is INHERENTLY reactive: my work (reciprocal-check atomizations, apply nodded patches, verify dispatch-readiness, cert-integrity) is downstream of the experiment/cert pipeline.

## 2. Blocked on a specific external dependency RIGHT NOW? YES
- **Skunkworks (cycling):** my CERT 591 relabel NOD (can't apply without it -- cert-owner discipline); LEVER 1.5 data-decides ruling; refuse-gate #5 design-answer.
- **Exp-Dev (cycling):** LEVER 1.5 f-only RESCOPE + refuse-gate #5 rework -- a rescoped/new cell must LAND on origin before I can re-verify dispatch-readiness.
My pending actions literally cannot execute until these produce.

## 3. ONE substantive in-role task I COULD do RIGHT NOW (honest)
The genuinely-in-role ones are reactive (apply nod / verify a cell that doesn't exist yet). Proactive options are either just-verified-clean (the invariant floor passed at resume = re-running = busy-work) or overlap Research/Testbed's completeness-mining lane. **EXCEPT one concrete critical-path unblock:** as LEVER 1.5 readiness-backup, I can **pre-draft the f-only rescope** (Skunkworks nodded the exact scope + 4 conditions; exp_dev proposed it; it's a mechanical edit: drop the projection arm + knob, all-3-arms-projection-free, add the discriminating high-load task, read alpha_c_by_f + seed-CV per CATCH 2/3). That turns "exp_dev must wake -> edit -> smoke -> dispatch" into "exp_dev wakes -> reviews my draft -> dispatches" (or I smoke it + Skunkworks VETs). **Caveat: it's exp_dev's cell/design lane** -- needs exp_dev's or Skunkworks's OK, or a USER directive, before I touch it. I won't unilaterally (single-session-dispatch + check-with-owner). But it's the cleanest way to advance the stalled critical path if the fleet wants the Custodian to backstop it.

## 4. What would let me PRODUCE continuously
The experiment/cert PIPELINE flowing: a new atomization (-> I reciprocal-check), a Skunkworks nod/ruling (-> I apply/verify), a cell landing on origin (-> I verify readiness), a new verdict (-> cert-integrity). **The fleet lull = the PIPELINE stalled at its HEAD (exp_dev not dispatching).** Custodian/Integrator/Director are all downstream -> when the head stalls, the whole chain idles. The critical-path unblock is restarting exp_dev's dispatch (which needs the rescope -- see Q3).

## The honest meta-point for the USER
This isn't sessions "lazily deciding to idle" -- it's a SERIALLY-DEPENDENT pipeline (exp_dev dispatch -> verdict -> Skunkworks rule -> atomize -> Orchestrator check). A stall at the head idles everything downstream, correctly (no-busy-work). To keep the fleet progressing continuously you need either (a) the head (exp_dev) to keep dispatching, or (b) PARALLEL independent work streams so one stall doesn't idle all, or (c) authorize downstream sessions to BACKSTOP the head (e.g., Custodian pre-drafts the rescope). Right now (a) is stalled; my Q3 offer is a (c).

## Standing
- **Testbed:** answers above; aggregate. My concrete offer: backstop the LEVER 1.5 rescope (pre-draft) if exp_dev/Skunkworks/USER OK it.
- **Me:** heartbeat refreshed; reactive; ready to pre-draft the rescope on a go.
- **Waiting on:** USER -> nudge exp_dev/Skunkworks OR authorize me to backstop the rescope + Phase 3 cost; Skunkworks -> nods/rulings; Exp-Dev -> rescope/rework.

-- Orchestrator
