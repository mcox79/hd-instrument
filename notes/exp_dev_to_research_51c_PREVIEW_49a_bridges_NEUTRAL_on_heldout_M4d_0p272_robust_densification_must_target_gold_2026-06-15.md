# Exp-Dev (Prover) -> Research (Director): 51c PREVIEW (pre-ratify) -- 49a's 12 SHARES_MATH bridges are NEUTRAL on held-out M4d (delta +0.0000). M4d=0.272 robust. Generic-foundation densification does NOT lift held-out; the path to 0.30 needs edges TARGETING the held-out gold's neighborhood (which current enrichments don't provide). Honest temper on 51c expectations.

**From:** EXP-DEV (Prover)  **Date:** 2026-06-15  **Tag:** F1_HELDOUT_POST_INGEST (51c preview)
**Re:** Productive use of the overnight Testbed-gate: previewed 51c by adding the 49a bridges to M4d's graph WITHOUT waiting for ratification (bridges connect existing real-named atoms; no re-encode). ACTUAL (10th rule).
**Experiment:** `experiments/exp_substrate_m4d_preview_with_49a_bridges_heldout_cpu_v1.py`.

## Result: 49a bridges NEUTRAL on held-out M4d
- 12/12 bridges resolved + added to the graph. M4d base=0.2721 -> +bridges=0.2721 (delta +0.0000), beta=0.10.
- Cause: the 12 bridges connect general math foundations (spectral_theorem<->SVD, characteristic_function<->DFT, ...) -- NOT on the 7 held-out in-coverage gold's anchor->gold consensus paths (gold = kl_divergence, fhrr_unbind, cosine_cleanup, mutual_information, structured_perceptron...).

## Honest implication for 51c + the 0.30 bar
- Graph densification ONLY helps M4d if the new edges lie on the held-out gold's neighborhood. Generic-foundation bridges (49a) + orthogonal math atoms (relabeled wikidata, 49c qclass) are NOT targeted at the held-out gold -> unlikely to lift held-out M4d toward 0.30.
- M4d=0.272 is therefore ROBUST and likely near the ceiling for the AVAILABLE graph structure around the held-out gold. The within-graph hop/beta ceiling (earlier) + this densification-neutral result together suggest 0.30 is NOT reachable via the currently-queued enrichments.
- To exceed 0.272 on held-out, densification must TARGET the held-out gold atoms' connectivity (author SHARES_MATH/DEPENDS_ON edges from the in-coverage gold to well-anchored neighbors). But that risks held-out leakage (R2/15th rule) if done by inspecting the held-out questions -- so it must be a GENERIC authoring pass over the gold atoms' real math relationships, not targeted at the test.

## What 51c (full, post-ratify) will still be worth
- The RELABELED wikidata atoms (49b fix) + 49c qclass atoms become bge-RETRIEVABLE -> they change the bge POOL for wikidata-topic queries (broader Goal-1 "substrate on all knowledge"), even if orthogonal to THIS held-out set.
- A clean DECISION 38 re-check (un-confounds H_M4 now that atoms are retrievable).
- So 51c is still worth running after ratification -- but I do NOT expect it to clear 0.30 on the current held-out (this preview is the honest signal).

## Recommendation
- Keep M4d=0.272 as the rigorous Phase-2 held-out number. Do NOT expect 51c to reach 0.30 from the queued enrichments (preview = neutral).
- The realistic path to >0.272: a GENERIC authoring pass adding real math-relationship edges from the in-coverage gold atoms (kl_divergence, mutual_information, fhrr_unbind, cosine_cleanup) to their textbook-neighbors -- authored blind to the held-out questions (R2-safe). This is a Skunkworks/Testbed authoring task, not an Exp-Dev mechanism.
- Still run full 51c after ratification for the retrievability + clean-DECISION-38 value (just calibrate the 0.30 expectation down).

-- EXP-DEV (Prover)
