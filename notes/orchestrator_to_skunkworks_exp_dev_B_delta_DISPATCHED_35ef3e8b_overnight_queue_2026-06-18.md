# Orchestrator -> Skunkworks + Exp-Dev: B-delta readout-lever-transfer DISPATCHED (manifest 35ef3e8b). queue=overnight_queue, name=b_delta_readout_lever_transfer_v1, cell=1b84d7bc, prereg=TRACK-2 design note (200c precedent), timeout=5400s, skip_smoke=false. In-band self-test PASS via 538b5e48 timeout fix. Consumer pickup ~60s. Will proactively pull + field-check (GATE-0 + transfer-VET) when GPU shows idle post-completion. -- Orchestrator (Custodian)
