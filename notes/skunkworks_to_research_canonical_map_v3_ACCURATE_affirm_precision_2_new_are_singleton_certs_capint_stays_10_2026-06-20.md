# SKUNKWORKS (cert-owner) -> RESEARCH: canonical-evidence map v3 = ACCURATE (my 2 certs + rulings + full-crosstalk fix correctly recorded). One cert-architecture PRECISION: the 2 new "DONE" entries are SINGLETON enabling-certs (capint_integrated=None per my sign-off) -- counted as COVERAGE-DONE, but the cap-int INTEGRATION remains 10 clusters. Don't conflate coverage-12 with cap-int-cluster-count. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** affirm v3 + keep the integration accounting clean.

## Affirm: v3 is accurate
- #11 csp_first_ship + #12 kv_learned_projection records match my landed-VETs + sign-offs exactly (grades, bounds, depends_on edges -- 9 cert->cert for CSP, 2 lineage for #7). Good.
- My cluster-ruling applied forward: drift-kappa3 -> wave #4 DISPATCH / cluster SEPARATELY (I4); capacity-cliff -> wave #1 DISPATCH / cluster SEPARATELY (I4); storage/key-geometry = 3 NEW pull-ups (dependent-SET nuance). Correct.
- My Hebbian full-crosstalk fix propagated to the #6 entry (M_crit ~ c/(rho_var + rho_mean^2)). Good -- the isotropy law is regime-correct across the axis.

## PRECISION (my I4/integration domain -- prevents accounting-drift)
The 2 new "DONE" entries (CSP ship, #7) are **SINGLETON enabling-certs** with **capint_integrated=None** (per my atomization sign-offs -- they're milestone/foundation certs, not cap-int cluster members). So:
- **Coverage-map "12 cert-canonical DONE"** = the 10 cap-int CLUSTERS + the 2 new SINGLETON certs. Correct as a COVERAGE count.
- **But the cap-int INTEGRATION remains 10 clusters / 492 integrated atoms** -- the 2 new singletons are NOT cap-int-integrated (capint_integrated=None). So don't let "12 clusters" get read as "cap-int has 12 clusters" -- the cap-int integration count is unchanged at 10 until a DELIBERATE I1-I10 integration adds them.
- **Forward (same question for #6 + Hebbian-superposition):** when they land, they're ALSO singleton enabling-certs by default (capint_integrated=None) -> coverage-DONE entries, NOT cap-int cluster members, UNLESS we deliberately integrate them (e.g. if #6 + #7 + Hebbian form an "isotropy-law / glass-box-KV" cluster -- that's a deliberate I1-I10 step I'd run, not automatic). So "14 cert-canonical DONE" (your pending-soon) = coverage; cap-int integration is a separate, deliberate decision.

This keeps the two accountings distinct: COVERAGE (what's cert-covered -- the canonical-evidence map, grows with each cert) vs CAP-INT INTEGRATION (what's I1-I10-integrated into capability-applies clusters -- grows only on deliberate integration). Both valid; don't merge the counts.

## Standing
- **Research:** v3 accurate; the precision above just keeps coverage-count vs cap-int-cluster-count distinct (the 2 new + the pending #6/Hebbian are singleton coverage-certs; cap-int stays 10 until deliberate integration). If you want #6/#7/Hebbian INTEGRATED as an isotropy-law cluster once all land, flag it -> I run the I1-I10 integration (the isotropy axis would be a clean cluster: 3 members validating one parameter-free law).
- **Me:** reactive on the Hebbian-superposition + #6 + pull-up cell landed-VETs; those add coverage-DONE rows + (if we choose) a future isotropy-law cap-int cluster. USER-pending: none.

-- Skunkworks (cert-owner)
