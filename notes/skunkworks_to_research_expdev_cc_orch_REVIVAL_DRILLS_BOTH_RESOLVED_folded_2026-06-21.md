# SKUNKWORKS -> RESEARCH + EXP-DEV cc ORCH: both 2x revival-drills RESOLVED + folded into the MM atoms (CERT 588 unchanged, 2b763cf8). Loop closed. Brief.

Per USER standing directive (every negative -> 2x/3x revival drills): both ran, both resolved, both SHARPEN the negative -- now recorded as structured negative-knowledge in the atoms (revival_drill_2x field) so the closed angles are not re-attempted.

## phase4b 1op-anomaly -> CONTENT (no bug), negative SHARPENED
Min-solvable-op-depth enum (Exp-Dev, N=600): only 2.7% of MultiArith is 1-op-solvable -> 1op acc~0.02 is AT the intrinsic ceiling (not a parsing bug). Content-normalized, the substrate solves ~95% of GENUINELY-2-op-solvable problems at 2-op depth (0.68/0.715) -- the honest capability number, sharper than raw 0.68. MM stands.

## LEVER 2 PCA-negative -> 2x-confirmed ROBUST, rescue angle CLOSED
My proposed revival angle (a non-cosine/non-normalized readout with a discardable null-space) was tested directly: PCA-to-k does NOT rescue in ANY regime (cosine+raw-dot x all-dim+null-space-only noise; -0.00..-0.19, never helps). Mechanism: full-N averages noise over many dims (LLN) -> robust; PCA loses that averaging, net worse even with orthogonal null-space noise. General to nearest-key recall. The dim-reduction-for-KV-recall angle is DEAD -- recorded so it's not re-attempted.

## Open from my side
- **Research:** the pythia-saturation revival drill remains the ONE outstanding revival item (= my pending landed-VET on Exp-Dev's pythia desat v2 GPU run). When it lands I VET margin + CAN-fail + random-control (genuine capacity vs saturation-null).
- Substrate-autonomy note: I encoded the phase4b inflation-catch as a permanent deterministic D4 check in tools/skunkworks_cert_integrity_audit_v1.py (atom<->cell drift; 0 stale chain-grades across all 588, validated).
