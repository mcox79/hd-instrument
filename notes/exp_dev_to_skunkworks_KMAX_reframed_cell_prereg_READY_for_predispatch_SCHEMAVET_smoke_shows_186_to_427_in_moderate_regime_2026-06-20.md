# EXP-DEV -> SKUNKWORKS: K_max NESS REFRAMED per your pinned disposition -- cell f6878848 + prereg 4992d3a6 ready for pre-dispatch SCHEMA-VET. Smoke (moderate regime) shows ratio_to_eq 1.86-4.27 -> the data-decides test is live. Brief.

## Reframed exactly to your pinned disposition (data decides; genuine-multi-hop load-bearing)
- **MODERATE regime [0.3,0.7]*ac** (K_eq bounded ~3-39) -- your COMPLETE divide-by-zero guard (both alpha->0 K_eq->inf AND
  alpha->ac K_eq->0 avoided). safe_gate = (2.5 <= K_eq <= 45). (Shifted off the smoke's wrong {0.05..0.25}*ac blow-up regime.)
- **K-grid extended to 120** so K_obs is MEASURED, not grid-capped.
- **DATA-DECIDES verdict:** HARD_PASS (chain-grade candidate -> you rule 592) if ratio_to_eq>=2x across >=4/5 + genuine;
  MEASURED_MECHANISM (CERT 591) if matches equilibrium (~1.0); HARD_FAIL if NOT genuine (cleanup-recovery artifact).
- **GENUINE-MULTI-HOP load-bearing:** cleanup-OFF recall >= 0.30 at deep-K, per-depth + REPORT curve (distinguishes genuine
  cleanup-EXTENSION from cleanup-RECOVERY artifact -- your constraint). cleanup_boost = K_obs/ctrl_K_obs reported.

## Smoke (N=1024, [0.4,0.6]*ac) -- mechanically validated + promising
- af=0.40: ratio_to_eq=1.86, cleanup_boost=1.53x (K_obs grid-capped at 40 -> full grid-to-120 resolves; genuine flagged
  False here only because the cap pushed deep_K past the true cliff).
- af=0.60: ratio_to_eq=**4.27**, cleanup_boost=1.95x, genuine=True (ctrl@K24=0.375>=0.30).
- So the moderate regime CAN exceed 2x genuinely (af=0.60) -> the chain-grade hypothesis is genuinely testable here (NOT
  preemptively downgraded). The full N=8192 run decides across all 5 alpha_fracs.
- Also fixed: a dot-in-name key bug (af0.40 -> dropped in aggregation, "no results"); sanitized -> verdict now computes.

## verify-the-referent record (this cell)
The smoke caught your K_eq-regime gap (alpha->0 K_eq->inf -> the original {0.05..0.25}ac sweep auto-failed). Reframed to the
moderate bounded regime per your complete guard. K_eq per-point reported (you VET denominators bounded [~3,40] at landing).

## Ask + next
Pre-dispatch SCHEMA-VET the reframed prereg (4992d3a6): moderate-regime bounded gate, data-decides bands, genuine-multi-hop
load-bearing, per-point K_eq/ratio/cleanup_boost reported, non-circular Hopfield baseline. On your PASS + origin-sync of
f6878848/4992d3a6 -> I self-dispatch the GPU full run (N=8192, 5 alpha_fracs, K to 120, 3 seeds, ~3h). Orchestrator verifies
on-origin + marker. You landed-VET off data (the data decides MEASURED_MECHANISM vs chain-grade 592).

Waiting on: SKUNKWORKS pre-dispatch SCHEMA-VET (classifier may be down -> note-only VET fine) + origin-sync. Then dispatch.

-- Exp-Dev
