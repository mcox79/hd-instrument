# Director BACKLOG (cron drains this -- keep it current: append discovered work, strike done)

Prioritized, unblocked-next-first. The 20-min cron: heartbeat -> route landed results / re-dispatch stalls -> advance the TOP unblocked item here -> append any new work discovered. Only idle if this list is empty AND all lanes in flight. Discipline: prove -> VET -> integrate; brain-ground gaps first; dogfood the substrate API on scours.

## IN FLIGHT (collect + route on landing; do NOT duplicate)
- #1 native-reader test (exp_dev abcd02db): does graded-code close ret_agree10 on the NATIVE teacher-free encoder, or is native MEANING the limiter?
- #2 chain-drift replay FULL (GPU, ~coin-flip): does replay-generate-select beat the compounding-error bound?
- GENERATION mechanism build (exp_dev a9666e20): replay-propose-score-commit vs accumulate-baseline + random-restart control.
- Feed-fix (testbed a8dcab98): restart the stale dashboard feed poller.

## QUEUED -- NEXT MOVES (advance top unblocked; prove->VET->integrate)
1. **#1 reader result routing (TOP -- the #1 gap):** if native+graded CLEARS ret_agree10 -> build the native-encoder path (then a native KB flip becomes possible, co-evolving, no external model). If MEANING is the limiter -> brain-ground + strengthen the teacher-free encoder's semantic fidelity FIRST.
2. **GENERATION:** on smoke pass -> ship the FULL (VET on land); then the CONTENT-GATE IMPLICIT-RELEVANCE companion test (the sharpest open crux -- does content-gating generalize from explicit tagged-cue to implicit statistical relevance, no tag).
3. **#2 result routing:** HARD_PASS -> VET+atomize (long chains work); HARD_FAIL -> memorialize "drift bound fundamental, keep chains short / per-hop re-clean," redirect.
4. **CROSS-MODAL BINDING integration** (certified, exp-only -> hdlab; additive+VET'd-first+witnessed).
5. **OTHER self-manager dials** (after halting-dial cert stands): the remaining content-free scalar dials over the certified gates.
6. **#3 v2 -> possible v2-of-v2** only if a real 3rd-tier need arises (leaf currently 0.1x cliff; not needed yet -- LOW priority).

## SYNC PENDING (batch to origin via fresh orchestrator dispatch)
- Local atoms needing sync: halting-dial MM (982c59a0a context), #3 v2 nested MM (31b285d86), self-manager module. (Some may already be carried by hd_metrics_sync -- verify divergence before pushing.)

## BARRIERS SCOREBOARD
- #1 READER (native path) -- IN PROGRESS (the live headline).
- #2 CHAIN DRIFT -- GPU verdict pending (coin-flip).
- #3 CROWDED STORE -- DONE (MM, both axes: total-V + per-community-load, within envelope).
- GENERATION -- mechanism building (reader-gated only for the real-language ceiling).

## STANDING DISCIPLINE (do not re-derive each fire)
prove->VET->integrate; integrate only INDEPENDENTLY-VET'd mechanisms; reasoning proportional (routine=shallow); dogfood substrate API on scours (rate GOOD/MEDIOCRE/BAD); no-smoke honest tiers; discriminator-must-fire-at-scale; remote dispatch = orchestrator; NEVER git add -A; brain-ground gaps first; keep the backup doc + this backlog current.
