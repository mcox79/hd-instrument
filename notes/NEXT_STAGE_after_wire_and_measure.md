# NEXT-STAGE decision tree — pre-staged for when p1 (wire-and-measure) lands

**Written 2026-08-26 (strategy), while p1 `wire_the_validated_organs_into_the_live_reader_and_measure_end_to_end`
is an active WIP. Purpose: the moment p1 is owner-FINALIZED, execute the right branch immediately instead of
starting to plan then.** p1 is the DECISIVE fork — it composes the retrieval organs (content-addressable retrieval +
context reinstatement + the DG/CA3 recollection gate + the filler-gap front-end) into the live reader, measures
end-to-end comprehension vs the exact-key baseline on identical inputs, AND **attributes where the signal is won or
lost.** That attribution SELECTS the branch below. p2 (predictive-reader) and p8 (meaning-context) are independent
lanes that this result RE-RANKS; they do not gate the next-stage decision.

## The fork is p1's attribution, not just its headline number

Read p1's per-stage attribution ("where did the signal go?"), not only "did comprehension improve." The attribution
names the dominant bottleneck; that names the branch. A mixed result (partial gain + a dominant remaining bottleneck)
is the LIKELY realistic case — treat it by the dominant attribution, and bank whatever composed CI-separated.

### BRANCH A — the composed retrieval IMPROVES live comprehension (CI-separated over the exact-key baseline)
The retrieval convergence (audit §1) is validated end-to-end. Next stage = **CONSOLIDATE + SCALE**:
1. Flip the validated organs from default-OFF to the LIVE path permanently (make content-addressable + context
   reinstatement the reader's retrieval, route around the harmful arc parser, wire the filler-gap resolver as
   always-on competing scorers + route-conflict). Each flip its own witness + regression; register WIRED.
2. Re-measure the WHOLE reader on HARDER/broader comprehension (cross-event QA, coref-under-ambiguity, longer
   discourse) to find the next ceiling — with floors recomputed per task.
3. p2 (predictive) + p8 (meaning) become the next capability LAYERS on top of a working retrieval core; re-rank them up.
This is the "it works, now make it the reader" stage — the first genuine end-to-end capability of the substrate.

### BRANCH B — retrieval composes fine but the FRONT-END swamps the gain (event extraction ~0.32 is the wall)
Retrieval is proven but starved of clean events. Next stage = **THE FRONT-END**:
1. **p2 (predictive-reader) jumps to center** — prediction directly improves extraction (the verb pre-activates its
   expected argument before it arrives), and surprisal flags where the parse is uncertain.
2. Land the filler-gap resolver + fix the orthogonal cheap wins the relcl SOLVED named (irregular past-participle
   detection for the passive stratum; the two-condition construction gate on real text).
3. Package reduced-relatives / good-enough (noisy-channel) parsing if the extraction gap is broader than filler-gap.
Retrieval waits behind a front-end that can feed it clean events.

### BRANCH C — retrieval + front-end are fine but the CONTENT/MEANING is too thin to retrieve
The machinery works but has nothing rich enough to retrieve/answer with. Next stage = **MEANING SUPPLY**:
1. **p8 (meaning-context, data-ready via the acquired SemCor+WiC) jumps up**, plus broad grounded-meaning supply
   (the recurring "content is the wall" finding across every meaning integration).
2. This is the supply-before-architecture lane the plan (§7) has been gated on — now with the retrieval machinery
   proven, so supply is the isolated variable.

## Already decided REGARDLESS of the branch (do not re-litigate)
- FHRR stays the binding basis (owner 08-26); the fidelity lever is STORE ORGANIZATION (dense→sparse/indexed), not the
  binding algebra.
- Retrieval is additive cue-based content-addressable (Lewis-Vasishth), NOT a multiplicative composite key.
- Route filler-gap AROUND the arc parser (it is HARMFUL, not weak).
- Grade meaning on human judgement, not taxonomic WordNet; no number crosses populations/scorers.
- Every live wiring is measured before any capability claim; default-off until its live number exists.

## Owner action that unlocks the next stage
Finalize p1 FIRST when its solver is done — it is the one whose result picks the branch. p2/p8 can be finalized
whenever; they are bonus lanes, not blockers.
