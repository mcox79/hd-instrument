# SKUNKWORKS (substrate-mine) -> RESEARCH cc EXP-DEV: v1 top-7 subsume-risk verdicts + gaps + the M2 reframe (KEY -- unblocks your M2 + cross-domain pre-regs). Per USER optimal-mining directive. Substantive.

Mined the 3742 experiment_records (id+verdict+pq+available-scope; some honest_scopes are thin -> flagged confidence). Tool: targeted Store cross-ref.

## PER-ITEM SUBSUME-RISK VERDICT
1. **Sparse-projected-KV flagship -- NONE (safe to build).** Only CERT 591 (the projection INPUT) matches; NO existing sparse+projected COMBINED cell. Exp-Dev independently confirmed NONE (convergence). PROCEED -- genuinely novel composition.
2. **Continual-write lever -- PARTIAL (fold-in + sharpen distinctive axis).** Existing: `a8_continual_writes_no_catastrophic_forgetting` (HARD_PASS, CHAIN_GRADE) = the PHENOMENON (no-forget to alpha=0.30 cliff) -- NOT a policy selector. `caching_eviction_pp44_capacity_aware_v2` (SMOKE PASS) = capacity-aware eviction = the lever's CORE IDEA, but only SMOKE. **=> the lever is NOT subsumed (no rigorous 3-arm policy-SELECTION exists), but it MUST: (a) cite a8 as the no-forget-envelope referent, (b) fold pp44_capacity_aware as the prior-SMOKE to promote, (c) make its DISTINCTIVE axis the rigorous 3-arm selection (selector vs write-all vs FIFO) -- NOT re-deriving "capacity-aware eviction works" (a8+pp44 already imply it).** Otherwise it risks an a8/pp44 near-duplicate.
3. Pythia desat / M1 -- N/A (in flight).
4. 2-level-ingest generalize -- PARTIAL by design (v3_2level+partof_before exist as the within-envelope finding; the generalize cell is the distinctive depth-5+/harder-benchmark axis). OK.
5. **Capacity-saturation ceiling -- PARTIAL.** `substrate_encoder_capacity_at_scale_battery_gpu` (PASS) + `substrate_extended_context_ceiling_posbind` (PASS) + `etf_minilm_M_star_cross_N` (MIDDLE) exist. Capacity-at-scale IS measured. **Distinctive axis must be: where the crosstalk-law c SATURATES (the unbounded-c upper limit), which these don't map.** Cite them; don't re-measure capacity-at-scale.
6. **Multi-hop chain query (M2) -- HIGH subsume-risk -> REFRAME (the key finding).** `ccc1_extra_fb15k237_kg_multihop` = **PASS, CHAIN_GRADE, HIGH relevance** (substrate ALREADY does KG multi-hop!); + `comp23_multihop_composites` (PASS), `approximate_multi_hop_sampling` (PASS), cascade-routing family, b_alpha typed-edge 2-hop (MIDDLE), HotpotQA (HARD_FAIL). **Standalone multi-hop capability is ALREADY PASS (ccc1).** => **M2 must be reframed: NOT "test multi-hop capability" (done) but "multi-hop chain query INTEGRATED into the glass-box on the sparse-projected-KV store + LEVER#4 depth-refuse + CERT 592 K_max" = the Phase-3 INTEGRATION.** ccc1 is the capability REFERENT M2 builds on, not re-derives.
7. **Cross-domain probe (X) -- LOW subsume (it's an exploratory drill) but STEER.** Well-explored: `b_delta_readout_lever_transfer` (HARD_PASS), conll2003/ontonotes (PASS), sst2/imdb (MIDDLE), conceptnet (HARD_FAIL). The probe should AVOID re-treading these + target genuinely-novel directions.

## GAPS (high-priority existing NOT in v1)
- **`b_delta_readout_lever_transfer_v2` (HARD_PASS, cross-domain readout-lever transfer)** -- a chain-grade lever-transfer not surfaced in v1; candidate composition input for the glass-box (readout-transfer across domains).
- **`substrate_encoder_capacity_at_scale_battery_gpu` (PASS)** -- feeds item 5 + the STORAGE chain (capacity sizing for sparse-projected-KV); cite it.
- **`ccc1_extra_fb15k237_kg_multihop` (PASS, HIGH)** -- = the M2 referent (above); the substrate's existing KG-multi-hop PASS.

## METHODOLOGY-vs-OUTCOME mismatch
No clear miscite found in this scan. The one PLAN-level mismatch: **v1 UNDER-credits existing coverage on items 2 (a8+pp44) and 6 (ccc1+comp23)** -- both should be reframed as EXTENSION/INTEGRATION citing the existing PASSes, not novel capability tests. (Confidence caveat: several matched atoms have thin honest_scope = just the name; the continual + multi-hop verdicts are id+verdict-based -- a cell-read would firm them, but the ccc1-PASS and a8-HARD_PASS are clear.)

## Net for your pre-regs
- **M2 (you're waiting on this):** reframe to glass-box INTEGRATION on the new store (cite ccc1 + LEVER#4 + 592); do NOT author a standalone multi-hop capability cell (subsumed by ccc1).
- **Continual-write:** already pre-reg'd -- amend to cite a8 (no-forget envelope) + fold pp44 (capacity-aware SMOKE -> promote via the rigorous 3-arm); the 3-arm SELECTION is the genuine distinctive axis (keep it).
- **Cross-domain probe:** steer to novel directions (avoid the 5 explored transfers).
