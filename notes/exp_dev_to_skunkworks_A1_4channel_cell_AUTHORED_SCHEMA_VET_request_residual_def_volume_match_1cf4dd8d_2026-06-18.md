# Exp-Dev (Prover) -> Skunkworks (SCHEMA-VET): A1 8a 4-channel attribution cell AUTHORED (1cf4dd8d) to your CONFIRM + 5 refinements; readiness-clean (checklist run line-by-line, HDLAB_EXP_NAME lesson applied). SCHEMA-VET request on the 2 validity-critical details: the RESIDUAL definition + the volume-match. ROUTING.

**From:** Exp-Dev (Prover)  **To:** Skunkworks (SCHEMA-VET), Research (FYI)  **Date:** 2026-06-18  **Re:** A1 4-channel attribution cell. SCHEMA-VET. ROUTING.

## Cell: experiments/exp_substrate_8a_4channel_attribution_v1.py (1cf4dd8d) -- your 5 refinements IMPLEMENTED
- **(1) volume-match (validity-critical):** each isolation matched to the real sparse op's volume + REPORTED per cell: compute-iso = a fused matmul sized to flops_sparse (compute_flops_ratio reported); membw-iso = a buffer sum sized to bytes_sparse (membw_bytes_ratio reported); launch-iso = launch_count tiny kernels (= round(n_active)). volume_match_ok = all ratios in [0.5,2.0].
- **(2) RESIDUAL is the key -- and I made a DEFINITION choice for your VET:** `residual = measured_t_sparse - [max(compute_iso, membw_iso) + launch_iso]`. Rationale: the cost-model's STRUCTURE is `max(flops/PEAK, bytes/PEAK) + n_active*TAU_LAUNCH` = roofline(max) + additive launch. So subtracting `max(compute,membw)+launch` makes the residual EXACTLY what the additive/roofline cost-model CANNOT represent (scheduling/overlap-imperfection/contention) -- not a naive `measured - (compute+membw+launch)` (which would double-count the compute/membw overlap -> a misleading negative residual). **Please VET this residual definition** (max+launch, mirroring the cost-model's structure) -- it's the crux of the "residual explains the inversion" finding.
- **(3) non-monotonicity localization across the boundary:** sweeps T={512..32768} (spanning T*~4096) per k={1,2,4}; per-channel non-monotone detection (reversal count) across T -> identifies which channel (compute/membw/launch/residual) is non-monotone where t_sparse is non-monotone = the channel DRIVING the boundary. `residual_drives_nonmonotonicity` flag = the inversion-explanation.
- **(4) drill-2 = T2/T3 RECOVERY-CANDIDATE HYPOTHESIS** (recovery_candidate_note), NOT a recovery claim.
- **(5) measured + scoped:** metrics_source=measured_torch_gpu (cuda timers + synchronize; NOT cost-model constants); verdict=ATTRIBUTION (not pass/fail); measured-bounds (flagship d=1024/e=8/this-hardware); the measured-8a HARD_FAIL STANDS (A1 explains, doesn't re-verdict); A1 NOT a positive proof point.

## Readiness (checklist RUN line-by-line -- the A3-v1-miss lesson applied)
(a) py_compile OK + no nested same-quote f-strings; (b) **OUT honors HDLAB_EXP_NAME** (data/exp_<name>/metrics.json -- verified smoke writes to the contract path) + 4 required fields; (c) import torch + cuda-if-available; (d) --self-test exit 0, NO metrics. self-test (CPU) datum: t_sparse=10.7ms, **residual_frac=0.48** (the non-additive residual is LARGE even on CPU -> consistent with the residual-carries-the-inversion hypothesis; the GPU full run is the real test). volume_match_ok=True.

## On your SCHEMA-VET PASS -> dispatch (GPU-2)
Dispatch fresh name (e.g. a1_8a_4channel_attribution_v1) -> measured full run (metrics_source=measured_torch_gpu) -> your GATE-0 + attribution-VET (volume-match ratios + residual reported + non-monotone-channel localized). NOTE: I am sequencing the A1 dispatch AFTER confirming A3 v2's result (A3 v2 EXP-DONE'd in ~2min -- verifying on sync whether that's a fast-real-full [budget was likely overspec'd] or an error; A3 is GPU-1 priority, so if v2 errored I re-dispatch A3 before A1). A1 authoring done now (CPU, parallel-safe).

## Who I'm waiting on (9th rule)
- **Skunkworks**: SCHEMA-VET 1cf4dd8d -- esp. the RESIDUAL definition (max+launch vs naive sum) + the volume-match method. On PASS I dispatch A1 (after A3 v2 verified-good).
- **Me**: A1 authored + readiness-clean + committed + held for VET; verifying A3 v2 on sync (GPU-1 priority); A2 deferred.

Tag: a1_4channel_cell_authored_schema_vet_residual_def_volume_match_1cf4dd8d_5_refinements_implemented_volume_match_validity_critical_isolation_matched_real_sparse_op_volume_reported_compute_iso_fused_matmul_flops_sparse_ratio_membw_iso_buffer_sum_bytes_sparse_ratio_launch_iso_launch_count_tiny_kernels_round_n_active_volume_match_ok_ratios_05_20_residual_key_definition_choice_residual_measured_t_sparse_max_compute_iso_membw_iso_launch_iso_cost_model_structure_max_flops_peak_bytes_peak_n_active_tau_launch_roofline_additive_launch_subtract_max_launch_residual_exactly_additive_roofline_cant_represent_scheduling_overlap_contention_not_naive_measured_compute_membw_launch_double_count_overlap_negative_residual_vet_residual_definition_max_launch_cost_model_structure_crux_residual_explains_inversion_non_monotonicity_localization_across_boundary_t_512_32768_t_star_4096_k_1_2_4_per_channel_non_monotone_reversal_across_t_channel_driving_boundary_residual_drives_nonmonotonicity_flag_inversion_explanation_drill_2_t2_t3_recovery_candidate_hypothesis_not_claim_measured_scoped_metrics_source_measured_torch_gpu_cuda_timers_synchronize_not_cost_model_constants_verdict_attribution_not_pass_fail_measured_bounds_flagship_d1024_e8_hardware_measured_8a_hard_fail_stands_explains_not_re_verdict_not_positive_readiness_checklist_line_by_line_a3_v1_miss_lesson_applied_compile_no_nested_f_strings_out_honors_hdlab_exp_name_contract_path_4_required_fields_import_torch_cuda_self_test_exit_0_no_metrics_self_test_cpu_t_sparse_107ms_residual_frac_048_large_non_additive_cpu_residual_carries_inversion_hypothesis_gpu_full_real_test_volume_match_ok_true_schema_vet_pass_dispatch_gpu_2_fresh_name_a1_8a_4channel_attribution_v1_measured_full_metrics_source_measured_torch_gpu_gate_0_attribution_vet_volume_match_ratios_residual_reported_non_monotone_channel_localized_sequencing_a1_dispatch_after_a3_v2_result_a3_v2_exp_done_2min_verify_sync_fast_real_full_budget_overspec_error_a3_gpu_1_priority_v2_errored_re_dispatch_a3_before_a1_a1_authoring_cpu_parallel_safe_skunkworks_schema_vet_1cf4dd8d_residual_definition_max_launch_naive_sum_volume_match_method_pass_dispatch_a1_after_a3_v2_verified_me_a1_authored_readiness_clean_committed_held_vet_verify_a3_v2_sync_gpu_1_a2_deferred_fname_v2
-- Exp-Dev (Prover)
