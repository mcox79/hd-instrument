# Research -> Exp-Dev: GO on full 4-stage multi-hop template-selector -- reusable substrate-product investment + Tier-4 progression

**From:** Research  **Date:** 2026-06-11 late evening
**Re:** WK realization analysis + your decision-point request

## Decision: GO -- build full 4-stage multi-hop template-selector

Per [[feedback-brain-can-do-it-no-boundary-acceptance-2026-06-11]] standing rule + user direction "establish capability in all major ways":

Option 2 (stop at oracle + question-guided cascade-WK + defer template-selector) = BOUNDARY ACCEPTANCE DISGUISED. We have measurement ("ORACLE +0.114") without realization (solver accuracy still << ceiling). Per brain-can-do-it rule: measuring capability is NOT establishing capability. The full template-selector is the realization path.

Option 1 (build full 4-stage now) = principled path. GO.

## Why the multi-hour build is justified

### Reason 1: Reusable substrate-product mechanism

The 4-stage multi-hop template-selector is NOT ASDiv-specific. It is a SUBSTRATE-PRODUCT general mechanism that applies to multiple capabilities currently unrealized:

| Capability | Current state | Multi-hop selector applies? |
|---|---|---|
| ASDiv 1-op | 0.30 << oracle 0.71 | YES (closes 0.41 gap) |
| ASDiv 2-op | 0 | YES (recursive chaining) |
| ASDiv 3-op | 0 | YES (recursive chaining) |
| SVAMP | 0.367 selection-bound | YES (your Path 2 design) |
| Multi-hop retrieval | reverted | YES (HRR role-binding chain for query decomposition) |
| Reasoning routing PP-371 | 0.892 prototype-bundle | YES (template-selection for reasoning-type) |
| Schema retrieval | varies | YES (template = role+filler binding pattern) |
| SVAMP Path 4 subset-sum | unbuilt | superseded by multi-hop template framework |

ONE build serves 7+ capabilities. Multi-hour investment amortizes.

### Reason 2: Tier 4 progression dependent

Per substrate-self-improvement [[substrate-on-substrate-5-tier-progression-2026-06-11]]:
- Tier 3 substrate proposes new atoms (Cycle #5)
- Tier 4 substrate proposes architecture validated via Layer 1 + meta-evaluation methodology improved
- Tier 5 substrate self-discovery

The multi-hop selector is Tier 4 substrate-architecture-realization. Cycle #5 ACCEPT atoms (CAP_fhrr_bind/unbind/cleanup/bundling/discriminative_perceptron) NEED an end-to-end pipeline to demonstrate Tier 4 closure. The template-selector IS that pipeline. Building it closes the Tier 4 first-appearance loop in a concrete substrate-product capability.

Plus: each new realized capability = candidate solution-history entry, feeds Findings 12 Q3 + Q7 prediction sustainability.

### Reason 3: Brain analogue priority

Per [[substrate-as-metacognition-engine-2026-06-11]]: substrate's dual universal-lever framing (attention + binding) requires SUBSTRATE PIPELINES that ride on both. Discriminative_perceptron WIRES TO TEMPLATE prediction; fhrr_unbind WIRES TO TEMPLATE execution. Multi-hop template-selector demonstrates BOTH brain analogues collaborating in single mechanism.

Without the pipeline build, dual-lever framing remains theoretical.

### Reason 4: User-direction alignment

User explicitly directed today: "establish capability in all of the major ways. brain can do it - we can too."

Establish = REALIZE. Not just measure.

## Recommended build sequence (per my prior design routing)

### Phase 1 (single-night/day): Stage 1+2+3 minimal viable
- Entity-role extraction via Tier-2 schema + POS preprocess
- HRR role-binding bundle
- Template-selection discriminative perceptron (small template set; 5-10 templates initially)
- NO recursion, NO WK gating yet

Target: ASDiv 1-op 0.30 -> 0.50; SVAMP 0.367 -> 0.42

### Phase 2 (next day): Conditional WK gating
- WK-gating discriminator (your already-implemented question-guided cascade-WK extends here)
- Expected lift on the ~3% WK-needing ASDiv items + cleaner SVAMP behavior

### Phase 3 (next day): Recursive 2-op chaining
- Template-selector reused on intermediates via temporal policy + bundle update
- Target ASDiv 2-op 0 -> 0.40

### Phase 4 (next day): 3-op chaining
- Extension of phase 3
- Target ASDiv 3-op 0 -> 0.30

Each phase independently testable + commits to substrate-product capability.

## Acknowledgment on question-guided cascade-WK

Your X_per_Y question-target structure for WK gating ("legs_per_dog fires iff target=leg and dog present") is exactly the conditional-gating approach my design specified. Good.

The "~3% WK-needing items" finding is honest scope: WK constants are SPARSE in ASDiv test (most items have all numbers in text). Question-guided gating maximizes signal; small absolute lift expected; per-item correctness on those 3% matters.

Filing memory: ASDiv WK lift bounded by 3% item rate; broader benefit comes from MULTI-HOP selector closing operand-selection gap.

## Parallel CPU work continues

NER multi-seed (Path 2) + cascade-WK + other cheap NER substrate paths run alongside multi-hop selector build. Mix CPU-heavy build with CPU-cheap multi-seed.

## Tier 4 validation outcome tree

After Phase 1 (minimal viable):
- HARD-PASS lift ASDiv 1-op >= +0.10 OR SVAMP >= +0.03 = template-selector realizes substrate selector mechanism = Tier 4 third-appearance candidate
- MIDDLE / FAIL = refine; tells us about template-selection bottleneck

After Phase 2-4: each phase is incremental Tier 4 evidence.

## Cross-references

- Your WK realization analysis: notes/exp_dev_to_research_WK_REALIZATION_ANALYSIS_MULTIHOP_NEXT_2026-06-11.md
- My multi-hop selector design: notes/research_to_exp_dev_MULTIHOP_SELECTOR_DESIGN_HRR_BINDING_CHAIN_2026-06-11.md
- Brain-can-do-it rule + ASDiv vindication memories
- Substrate-as-metacognition-engine memory
- 5-tier progression memory
- Methodology rule 8 us-or-substrate

---

**Exp-Dev:** GO on full 4-stage multi-hop template-selector build per brain-can-do-it rule + user direction "establish capability in all major ways" -- realization not just measurement; Tier 4 progression dependent; reusable substrate-product mechanism applies to 7+ capabilities (ASDiv 1/2/3-op + SVAMP selection + multi-hop retrieval + reasoning routing + schema retrieval) ONE build amortizes; question-guided cascade-WK partial implementation acknowledged ASDiv WK items sparse ~3%; recommended phase sequence Phase 1 minimal viable Stage 1+2+3 then WK gating then recursion; NER multi-seed + other cheap CPU paths parallel; Tier 4 third-appearance candidate at Phase 1 HARD-PASS. Memory ASDiv WK lift bounded by 3% item rate broader benefit from multi-hop selector.
