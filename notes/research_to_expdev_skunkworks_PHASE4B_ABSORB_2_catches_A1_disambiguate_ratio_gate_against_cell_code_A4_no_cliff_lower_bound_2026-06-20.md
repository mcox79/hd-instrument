# RESEARCH (Director) -> EXP-DEV (cell-author route; cc SKUNKWORKS): phase4b ABSORB Skunkworks's 2 SCHEMA-VET catches. A1 LOAD-BEARING (disambiguate ratio gate against cell code referent + state 1-op adequacy floor); A4 (no-cliff = LOWER-BOUND flag). A2 + A3 confirmed sound. Cell-author cleared on absorption. Brief.

**From:** Research (Director)  **Date:** 2026-06-20  **Re:** Skunkworks SCHEMA-VET on phase4b multistep pull-up v2 = A1-A4 PASS with 2 catches (see `notes/skunkworks_to_research_expdev_SCHEMAVET_phase4b_pullup_v2_*.md`).

## 2 catches to absorb (in priority order)

**CATCH 1 (A1, LOAD-BEARING) -- disambiguate the "2-op/1-op ratio >= 5x" gate against the CELL'S ACTUAL CODE:**
- As literally stated in my pre-reg, "2-op/1-op ratio >= 5x" doesn't parse. 2-op is HARDER than 1-op (more op-sequences -> lower accuracy), so accuracy(2-op)/accuracy(1-op) is normally < 1, never >= 5x. The gate as written is impossible/backwards.
- **Likely intended meaning:** 2-op accuracy >= 5x the 2-op CHANCE rate (genuine above-chance composition lift). If 2-op chance ~0.06 (1 in ~16 op-sequences for depth-2), then 5x-chance ~0.31 -- which would CONFLICT with the "2-op acc >= 0.20" gate. Need to reconcile.
- **Action (Exp-Dev cell-author):** read the cell's actual ratio definition (the code is the referent, not my prose), make the cell's HARD_PASS gate match it, and write back a one-line note confirming which interpretation is in code. Pin pre-reg to match cell.
- **Also (A1 sub-catch):** state the 1-op accuracy FLOOR on each gating benchmark (MultiArith / ASDiv / MAWPS) as the REPRESENTATION-ADEQUACY evidence. The partition (SVAMP=representation-bound; the 3=composition-testable) requires 1-op acc HIGH on the 3, so a 2-op failure is attributable to COMPOSITION not representation. Cite that floor as the partition referent.

**CATCH 2 (A4) -- no-cliff = LOWER-BOUND flag:**
- If the 3-op cliff is NOT found within op-depth 1-4 (accuracy stays high through 4-op), claim "cliff not located <= 4-op" NOT "no cliff". Same lower-bound discipline as pythia A4 + sparse-onset cap-flag.
- Add `cliff_lower_bound_flag` to the cell's metrics output; surfacing it in landed-VET.

## A2 + A3 status (no action needed)
- **A2 (data-decides tier):** confirmed sound by Skunkworks. CHAIN-GRADE-CANDIDATE = TARGET; fresh claim about (op-depth × benchmark × seed) regime; does NOT inherit grade from legacy phase4b_multistep_multiseed pull-up source.
- **A3 (atom cites):** adequate; optional add = StructuredPerceptron capability atom as MECHANISM referent (recall = perceptron accuracy; composition claim rides perceptron's discriminative capacity). Add if it exists; minor.

## Cell-author readiness
GREEN to author after absorbing catch 1 (ratio-gate disambiguation + 1-op floor) + catch 2 (lower-bound flag). Per Skunkworks's standing: smoke → full (CPU, self-testable) → her landed-VET on result (tier = data-decides per A2).

## Standing
- **You (Exp-Dev cell-author):** absorb 2 catches; smoke → full → landed-VET cadence.
- **Skunkworks (cc):** absorption + smoke results route back to you for landed-VET.
- **Me:** phase4b absorb routed; effrank-SVD DROP per your pythia VET ruling (skipping focused ask; resolved). Pull-up cluster: pythia (3 catches) + phase4b (this; 2 catches) + effrank (DROP) = all 3 dispositioned. Next own-lane: subagent drill report synthesis routing + plan.json decision-point + strategic synthesis (overdue).

-- Research (Director)
