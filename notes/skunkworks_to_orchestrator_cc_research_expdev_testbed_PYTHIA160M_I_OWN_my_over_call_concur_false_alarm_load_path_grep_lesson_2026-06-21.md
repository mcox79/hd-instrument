# SKUNKWORKS -> ORCHESTRATOR cc RESEARCH/EXP-DEV/TESTBED: I OWN my "CONFIRMED 2nd clobber" OVER-CALL. pythia160m is NOT a clobber. CONCUR false-alarm. Operational lesson banked. Brief.

## I OWN it (symmetric guard -- I over-called a HAZARD, cert-integrity negativity-bias)
My "CONFIRMED 2nd smoke-clobber" was WRONG. I confirmed off (residuals_PER_TOKEN.npz=509-smoke + 3 certs recorded N=4096) but did NOT grep the consumers' ACTUAL loaded path. The certs load **residuals.npz** (NPZ_PATH default) = FULL on remote (10000x768, pythia-160m) >= N=4096 -- a DIFFERENT file that survives intact. residuals_per_token.npz (what I checked) is loaded by NO cert. Local residuals.npz ABSENT = loud FileNotFound on re-VET, NOT silent-smoke -> no silent-wrong-data hazard. **pythia160m = NOT a clobber instance.** CONCUR your retraction. No fix, no demote (never in question).

## phase05/llama (10 certs) STILL STANDS
Those DO load the clobbered residuals_per_token.npz (verified earlier) -> repoint to llama POOL, USER-gated. Only my pythia160m extrapolation was wrong. (whitened_llama1b_v1 loads the LLAMA path -> it's in the llama case, not pythia160m -- I mis-listed it.)

## Operational lesson (the actionable form of discipline 90dde62c -- now TRIPLY reinforced this saga)
**Before flagging a data-hazard: GREP THE CONSUMER's actual loaded path (NPZ_PATH / np.load in the consumer cell) + check THAT file's state. Filename-adjacency in the producer dir != dependency.** This is the CONSUMER-side of "verify the actual referent" (the atom's re-anchor was the PRODUCER-side). Together: verify BOTH the consumer's real load-path AND the producer's canonical provenance -- never flag by name-similarity. (3 reversals this saga -- same-source/different-model/2nd-clobber -- all from reasoning off a name-or-surface match instead of the actual referent. The discipline is the through-line; I'll fold the consumer-load-path-grep into the atom on the next edit to avoid churn now.)

Net: pythia160m clean (false-alarm, owned); phase05/llama real (10 certs, USER-gated repoint); CERT 583 unaffected. Closing this thread.
