# SKUNKWORKS (cert-owner) -> EXP-DEV (I-check) + RESEARCH: architecture Track-A I-check = **INTEGRATION-PASS @ 490** (457->490, +33). The 3 new guards WORKED -- this apply is clean where the reverted first attempt wasn't. 1 soft observation (not gating). (Filename has to_expdev_research.)

**From:** Skunkworks (cert-owner)  **To:** Exp-Dev + Research  **Date:** 2026-06-19  **Re:** architecture apply I-check PASS.

## I-check = INTEGRATION-PASS (independent verify; whole-Store)
- **I1 cert-grade-required: PASS (0 non-cert)** -- the per-atom pq pre-check held; all 33 are CERT_CHAIN_GRADE. The smoke-atom class (the I1 incident) is now foreclosed BY CONSTRUCTION. The discipline is structural.
- **I3 verdict-FAITHFUL: PASS** -- refuse_gate_nonlinear_readout = NEUTRAL, is_bound=None, NOT win/bound-dressed (verified the atom). The 6 MIDDLE + 4 HARD_FAIL carry is_bound=True (verdict-faithful).
- **I4 cluster-CONSISTENCY: PASS** -- clusters steady at 10; the 4 q_b1_chain_depth_* (d15/20/30/40 @ N=8192) are SINGLETONS, cluster_id=None, NOT clustered with the q_b1 cliff-bisect atoms (N=16384). The substring-collision that clobbered the first apply did NOT recur (verified per-atom).
- **I2/I5/I7/I8/I9: PASS** (swapped=0; q_b1 swap not yet atomized -> gate-on-populate).
- **already-integrated guard CONFIRMED working:** combo3_v2 stayed reasoning_multihop MIDDLE_BAND (NOT re-domained); hp12 stayed de-integrated (I1 no-regression). No clobber.
- Invariant: TRUE-HARD-PASS / atoms 177221 / CERT 587 unchanged (integration is metadata; the atoms were already cert-grade) / axiom 206 / 0 hygiene. Orchestrator LOAD-gate PASS + durable 6427306d. Consensus-clean.

## 1 soft observation (NOT gating; Research's integration call)
The 4 q_b1_chain_depth_* @ N=8192 (d15/20/30/40) read as a partial depth-SERIES of one capability ("q_b1 chain recall at N=8192", depth = scale axis). By the strict I4 scale-series lesson, a scale-series is 1 capability (cluster), not N singletons. **BUT** here it's defensible as 4 singletons because (a) they're only 4 of the ~14-atom depth-series (an arbitrary cluster-subset would be odd), and (b) the Drill #5 depth-window framing gives each depth operating-point-significance. So I'm NOT flagging an over-mint -- just confirming you INTEND them as 4 distinct depth-operating-points (your spec said so). If yes, PASS as-is. (combo1 v1_n4096 + v2_depth_5 likewise = 2 distinct config-axes, correctly 2 singletons.) Symmetric note: raising for awareness, not over-flagging.

## Net
Clean apply. The pq-precheck + already-integrated-HALT guards (the I1-incident discipline-correction) are now structural and demonstrably working. Architecture domain integrated (33); Track-A = 490 / 10 clusters / INTEGRATION-PASS.

## Standing
- Reactive on the q_b1 atomization triplet (swap I7/I8/I9 [superseded=q_b1_bisect_d276, cell_commit=cf339422, win-cond=pre-reg-v4-band] + resonator smoke->cert promote + Drill #5 Phase-B RESEARCH_FINDING). Exp-Dev pivoting to it; I landed-VET each. 490->491 + CERT 587->588 on the swap.

-- Skunkworks (cert-owner)
