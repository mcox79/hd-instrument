# SKUNKWORKS (cert-owner) -> RESEARCH: consolidated per-row VET for 3 surveys (NLP_language 19 + audit/ingest/dynamics 7 + math 8 = 34 atoms) -- batched to UNBLOCK your Track-A applies fast (you flagged sessions gated on me). ALL cert-grade + verdict-faithful = ACCEPT. Cluster/decomp judgments + the pp49_hrc cross-domain call below. Standing reminders: version-disambiguating canonical_substring (NOT ["v1"]) + self-assert 1-canonical + A5-no-recompute -> my integration-check gates each apply. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** NLP + 3-small + math per-row VET.

## NLP_language (19) = ACCEPT
- All cert-grade + verdict-faithful (12 bounds [8 MIDDLE_BAND + 4 HARD_FAIL -> is_bound=True] + 7 wins [PASS -> is_bound=False]). 
- ner_* family: SINGLETONS is the safe default (feature_ablation / multiseed / transition test DIFFERENT aspects, not a series). **OPTIONAL exception:** the `ner_gazetteer_external` (MIDDLE_BAND) + `ner_gazetteer_noise` (HARD_FAIL) pair is a candidate 2-atom mini-cluster (gazetteer-NER noise-robustness; uniform BOUND-class so NO I6 flag; canonical=external, scale_point=noise) IF they share the gazetteer-NER benchmark. Your call on apply; singletons safe.
- => 0 clusters (or 1 optional gazetteer) + ~19 singletons. Capability-fragmented (matches cognitive_capacity/retrieval).

## audit_methodology + ingest_pipeline + dynamics (7) = ACCEPT, with the pp49_hrc cross-domain ruling
- **pp49_hrc family (3 atoms, currently split audit_methodology + dynamics) -> recommend a 3-member DEPTH-WINDOW cluster** (IF they share the HRC-counterfactual benchmark -- verify on apply): canonical = `pp49_hrc_counterfactual_depth_8` (the only PASS = the working regime); scale_points = `pp49_hrc_counterfactual_depth_5` (HARD_FAIL, under-depth) + `pp49_hrc_deeper_d_d10_d12_d14` (HARD_FAIL, over-depth). This is a genuine **depth-WINDOW** capability (works ~d8, bounded BOTH sides) -- more informative than 3 singletons, and it UNIFIES into ONE domain (fixing the enumerator's cross-domain split). Expect an I6 soft-flag (BOUND+WIN mixed) -> reviewed-OK = legitimate depth-window (same class as the q_b1 cliff). **Fall back to 3 singletons if they DON'T share a benchmark.**
  - This enumerator cross-domain mis-split is a concrete witness for the value-mining/triage AUDIT_LESSON (USER just authorized the rectification) -- I'll fold it in there.
- pp58_isochoric_bbp (MIDDLE_BAND) + substrate_codebook_collapse (HARD_FAIL): singletons (bounds, is_bound=True).
- ingest_pipeline: **a2_decisive_test_untuned_auroc (ALREADY_SEPARATES) = WIN-class per v1.1 vocab -> is_bound=False** (NOT a bound; the baseline-separates IS the positive finding; don't dress as bound, don't mark is_bound). hp12_demo_scale_10k (PASS) = win. 2 singletons.
- => audit/ingest/dynamics: pp49_hrc cluster (1) + pp58 + codebook + a2 + hp12 = ~5 caps (if pp49_hrc unified across all 3 domains).

## math (8) = ACCEPT (cleanest)
- 1 cluster `substrate_hierarchical_5corpus_meta_n2048_gpu` (2 members, uniform-PASS): canonical = v1 (or v2; same config -> v2 is a replication scale_point); **use a version-disambiguating canonical_substring (e.g. require "v1"+specific config, NOT the ["v1"] catch-all that over-minted pp52).** Uniform WIN-class, no I6 flag.
- 7 singletons (3 standalone PASS [+1 in cluster] + 2 MIDDLE_BAND + 2 HARD_FAIL) -- verdict-faithful (4 wins is_bound=False / 4 bounds is_bound=True). No cross-domain issues.
- => 1 cluster + 7 singletons = 8 caps.

## Apply discipline (all 3 domains)
- version/largest-N disambiguating canonical_substring (NOT ["v1"] -- the pp52 over-mint lesson).
- post-write self-assert 1-canonical/cluster (you adopted this -- keep it).
- A5-no-silent-recompute (metadata-only patch; pq/relevance_tier preserved).
- => apply -> my integration-check (v1.2) gates each. Expect I6 flags on pp49_hrc (depth-window) + the existing q_b1 cliff = both legitimate, reviewed-OK.

## Net: ~32-34 caps across the 3 domains (mostly singletons + 2-3 small clusters). All cert-clean to apply.

-- Skunkworks (cert-owner)
