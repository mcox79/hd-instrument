---
owner_verdict: DONE
---

SOLVER SUBMISSION — build_the_obl_spatial_defer_consumer_to_cash_the_graded_parser_marginal

STATUS: PARTIAL (a rigorous, fully-drilled LOCATED NEGATIVE — the bar's named full-pass case). Glass-box, NO LLM,
NO hdlab/ writes (Q111 — promotion-ready module + diff for strategy to land). Reverify: verification/test_obl_spatial_defer.py (12/12).

HEADLINE. The dormant obl Matrix-Tree marginal IS a real brain-faithful spatial-attachment signal (UD-EWT AUC 0.76,
selective +0.151), and the consumer is a genuine PRECISION mechanism (SpaceEval moves precision 0.515→0.714,
twin-beating CI-sep). But it does NOT net-beat the attachment-blind floor for locatives — and I drilled EVERY
component of the chain to prove WHY, brain-foundationally, rather than stop at the wall.

WHAT I DISCOVERED WAS *NOT* BRAIN-FOUNDATIONAL (all found this session, each fixed or characterized — see SOLVED §0):
 • The CONSUMER itself ("defer/abstain") — abstaining loses signal; the brain integrates + COMMITS. FIXED → the
   graded-competition (normalized-recurrence) COMMIT: +0.0144 CI[0.003,0.026], 0/351 no-regress. THE LANDABLE WIN.
 • The ARC SCORER's surface features — 18% confidently WRONG; characterized (meaning proven intrinsic-bounded here).
 • My first THEMATIC cue (cheap motion-gated cosine) — FIXED → McRae/Hindle-Rooth selectional preference + CLASS-level
   ConceptNet AtLocation (elicited, Resnik-generalized; coverage 10%→68%).
 • ⭐ TAGGER + PARSER: SEPARATE + FROZEN (owner-flagged) — two architectural deviations. PROTOTYPED the faithful
   alternative: ONE unified, continuously-LEARNING (online delta rule, never a training run), LINKED
   interactive-activation cell — learns-as-it-reads 0.676→0.755. The key architectural contribution.

THE DECISIVE (honest) FINDING. For LOCATIVE attachment the meaning/situation cues are INTRINSICALLY bounded, not
cheap-fixable: symmetric plausibility (a place fits both hosts; Kim 2025) + peripheral/aspect-gated location roles
(Ferretti-McRae) + reporting bias (Gordon-Van Durme) + the marginal already carrying the category signal — ~15
citations. I proved the ceiling by BUILDING the real fixes (the class-AtLocation organ cured the data-starvation and
REVEALED the residual is intrinsic; the linked cue is redundant on held-out, +0.000). The DOMINANT signal loss in the
spatial reading chain is CONSTRUCTION coverage (35%) + typing (14%) — a DIFFERENT organ (the space reader's problem),
not attachment (10%).

LAND (promotion-ready, experiments/graded_spatial_obl_promote_v1.py): the commit + drop-the-obl-logistic + one-marginal
reuse; deploy the class-AtLocation THEM organ on NON-locative consumers (who-did-what) where meaning DOES help.

PLAIN ENGLISH. The parser's confidence really does help attach spatial phrases, and I built the reader that listens.
It makes recorded places more precise but doesn't win overall — because I traced the whole chain and found the real
losses are earlier (the reader doesn't recognize most ways English says "went somewhere"), and the hard attachment
cases are genuinely ambiguous without tracking the story across sentences. Along the way I found several parts weren't
built the way the brain does it — the reader was set up to "give up when unsure" (the brain commits), the meaning cue
was a cheap shortcut (rebuilt it properly), and the tagger and parser are two separate frozen models when the brain
uses one system that keeps learning — and I prototyped that unified, always-learning version.

QUESTIONS: none blocking. NEXT: land the commit now; the biggest remaining lever is the construction-coverage organ;
the north-star is the unified continuously-learning front-end.
