

## pasted by the owner 2026-09-12 23:08 (raw; strategy reads this)

SUBMISSION — problem: wire_the_mined_directed_causal_store_into_the_live_causal_reasoner_and_measure
(status: REFUTED — a full pass per the bar's located-negative provision; WIP until owner_verdict: DONE. Q111: proof +
proposed diff only, NO hdlab writes — strategy lands.)

ONE-LINE RESULT. The brief's mechanism is refuted with numbers AND its own +0.139 premise is corrected; the store's true
value is located; and the bigger lever past the wall is built, generalized, made few-shot, hardened into an organ, and
its grounding bridge PROVEN on real science concepts.

REFUTATION + PREMISE CORRECTION (exp_causal_store_wire_transfer_v1, witness 9/9, n=5005):
  • Wiring the store into the live causal_reasoner._graph() over the reader's own extracted-from-prose graph transfers
    +0.0002 (the extractor yields 2.2 nodes / 0.0 causal edges on process prose; single-lemma store coverage 1.4% vs
    12.6% concept).
  • The brief's +0.139 was a DENSITY ARTIFACT of the global-shuffle twin (~15x sparser per item, 29 vs 2 edges);
    against the honest density-matched twin (CausalGraph.shuffled) it is +0.0026 [-0.011,0.016], NOT CI-sep.
  • Direction is INERT on the necessity/existence axis by three controls: undirected connectivity 0.759 >= directed
    0.743; a map_only topicality baseline 0.762 BEATS the store; direction-scramble reproduces it. Principled (the
    "any effect" axis is direction-insensitive by construction — reachability collapses to association).

THE STORE'S TRUE VALUE + CORRECT ROLE:
  • Its asset is DIRECTION, CI-sep on a direction-2AFC where association is pinned at chance (BCOPA-CE +0.031, e-CARE
    +0.042; fwd beats REVERSE-direction +0.10) — but SMALL, at the intrinsic ~0.61 text ceiling; coverage buys ~+0.02.
  • Correct role = a testimony-derived directional PRIOR that bootstraps intervention (Harris-Koenig + Gopnik):
    +0.118 head-start at 0 interventions, front-loaded (washes out by ~4) — the reading regime.

THE BIGGER LEVER, BUILT PAST THE WALL (grounded interventional learning; the route text cannot take, CHT/Gopnik):
  • Direction 0.993 (+0.38 over text), cause-selection 0.03->0.98 through the live causal_reasoner; twin losing
    (exp_causal_direction_grounded_intervention_v1, witness 5/5).
  • GENERALIZES: monotone-nonlinear 0.915; non-monotone 0.688->0.930 (mutual-information readout); robust to 4x
    confounding (0.81). FEW-SHOT: active selection reaches 0.90 in ~5 interventions (brain band; Bramley) vs random
    never. ORGAN: GroundedCausalLearner — online/plastic, composes into causal_reasoner — BF_SPIRIT (operation pinned;
    readout+selection our-invention; input a stand-in), witness 5/5.
  • BRIDGE LOOP CLOSED ON REAL SCIENCE CONCEPTS (exp_causal_bridge_science_learn_v1, witness 4/4): simulated
    intervention on a runnable grounded physical model (causal_sign_channel structure) recovers physical-law DIRECTION
    0.977-0.989 on 97 real science concepts, while the text store is at CHANCE 0.510 there (reaction pairs are
    direction-ambiguous in text) — non-circular (structure inferred from interventional samples, ground-truthed to
    physics). Science-slice bridge (witness 4/4): runnable grounded do-sim beats the store on necessity +0.111 CI-sep,
    ties the honest twins (existence axis + ungated sign) — the same lessons, honestly.

PROPOSED hdlab CHANGE (Q111): do NOT wire the store as a necessity prior (measured inert). Wire its proven asset:
sm.causal_direction (default-off directional prior) + board_causal_direction_dimension gated on the density-matched
twin + reverse-direction control. Land GroundedCausalLearner as hdlab/grounded_causal_learner.py once the grounding
bridge feeds real experience -> raise BF_SPIRIT to BF.

FILES: experiments/exp_causal_{store_wire_transfer,direction_grounded_intervention,direction_grounded_generalization,
direction_active_selection,store_prior_plus_intervention,bridge_science_slice,bridge_science_learn}_v1.py +
exp_grounded_causal_learner_organ_v1.py; verification/test_causal_store_wire_transfer.py +
test_causal_direction_grounded_intervention.py + test_grounded_causal_learner_organ.py +
test_causal_bridge_science_slice.py + test_causal_bridge_science_learn.py; notes/problems/<slug>/{SOLVED,
FINDINGS_control_battery,BRIDGE_DESIGN_next_problem}.md. No hdlab/ writes. Ledger malformed/incomplete: 0.
REVERIFY: run the three witnesses in the SOLVED.md reverify line.

NEXT PROBLEM (the frontier, scoped out): read-time concept->model grounding (map arbitrary prose to a runnable model)
= the generative world-model main event, meaning-channel-gated — not a store-wire.
