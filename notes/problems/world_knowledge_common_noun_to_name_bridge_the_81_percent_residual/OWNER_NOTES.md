---
owner_verdict: DONE
---

SUBMISSION — world_knowledge_common_noun_to_name_bridge_the_81_percent_residual
status: PARTIAL (satisfies the bar's located-negative full-pass provision + a CI-separated residual win)
owner_verdict: yours to set · glass-box, NO external LLM · NO hdlab/ written (Q111)
reverify: .venv/Scripts/python.exe verification/test_namebridge_generative.py   (11/11)
      &&  .venv/Scripts/python.exe verification/test_namebridge_worldknowledge.py   (9/9)

WHAT THIS IS. The ~81% name-bridge residual ("the artist"->Zurbaran) resolved brain-foundationally on the
honest de-leaked GUM slice (n=356). Two results:
 (A) a genuine CI-SEPARATED WIN on the document-local residual -- an online GENERATIVE WHO-IS-WHO identity
     file (Heim file-change / Kuperberg generative / CLS episodic / ATL PIN; accrues fine type + role from the
     document's OWN predicates, no static KB) beats the strongest floor +0.072-0.080 CI-sep, twin + within-
     item-asymmetry controlled.
 (B) a rigorous WHOLE-SLICE LOCATED NEGATIVE (the bar's blessed full pass) that refutes the brief's hypothesized
     axis (relational/kinship = only 15% of misses; the residual is document-local COVERAGE) and proves WHY
     static routes stop.

vs THE BRAIN: 66% (0.607 / 0.921). The reader is verified brain-faithful DOWN TO THE EQUATIONS (math audit:
Centering recency beats ACT-R-frequency; graded constraint-integration beats gate/flat -- measured) and
brain-OPTIMAL in selection (97% of the residual conversion loss is irreducible same-type ambiguity = the brain's
own 0.079 ceiling). The ENTIRE remaining gap is knowledge coverage of document-local entities -- not mechanism.

BRAIN-FOUNDATIONAL COMPONENTS BUILT (witnessed, landable):
 - the GENERATIVE WHO-IS-WHO file (the win);
 - the assembled FULL-CHAIN resolver (full_resolve, verified math);
 - the SAFE-KB FAMILIARITY GATE (safe_kb_types, Bruce-Young/IAC) -- a reusable project-wide SAFETY tool with a
   PROVEN safety theorem: an ungated KB is HARMED under noise (0.607->0.399) while the gated KB is PROTECTED
   (0.601); wrap ANY KB lookup in it;
 - ABSTENTION (referential-uncertainty confidence): committed accuracy 0.607->0.753@50% coverage, control-flat.

FLAGGED AS NOT BRAIN-FOUNDATIONAL (for strategy):
 - token-overlap name-clustering (OUR-INVENTION -> replace with Bruce-Young individuation);
 - ungated broad-KB / top-1 entity search (REFUTED -> MUST wrap in safe_kb_types);
 - coarse discourse typing, ACT-R-frequency-for-this-op, generic pattern-completion (measured dead-ends);
 - the sibling problem's gold-coref inheritance leak (measured on the de-leaked floor).

NEXT HIGH-PRIORITY STEPS:
 1. INTEGRATE THE WORLD-MODEL WHEN DONE -- the SOLE remaining accuracy lever (document-local coverage, the 0.315
    gap to the brain), fed THROUGH the safe gate. Spec + benchmark: GENERATIVE_MODEL_PROMPT_from_namebridge.md +
    this doc-local subslice (oracle prize 0.921).
 2. LAND THE BF PIECES NOW (Q111): full-chain resolver + verified math (FIXES A-E) + abstention + safe-KB gate.
 3. NAME-INDIVIDUATION upstream (Bruce-Young recognition replacing token-overlap).

KEY REALIZATION: every mechanism AND equation is now the brain's and selection is brain-optimal, so the located
negative is genuine -- the wall is entity-specific knowledge, and the safe-KB gate is the piece that lets the
world-model supply it cleanly. Artifacts in experiments/ + verification/ (11/11 + 9/9); no hdlab written.
