---
priority: 6
review:
review_text:
---

# PROBLEM: meaning now beats frequency on a FAIR test -- but only OFFLINE, on isolated word pairs, and it has never been wired into the live reader or shown to help real comprehension

**slug:** `the_meaning_win_is_offline_context_free_and_unwired` - **opened:** 2026-08-26 by the strategy session
(packaged from the just-integrated `the_own_metric_may_reward_frequency_not_meaning`, EXCELLENT/re-verified)
**status:** OPEN - **the offline win is first-hand and scaffold-witnessed; the live/context question is untouched**

> **PRIORITY NOTE (the call is the strategy session's):** filed at `1`. This is the PAYOFF of the whole
> meaning line -- for the first time a meaning read-out beats the strongest frequency floor on a FAIR
> metric -- and it is convergent with the goal-bearing line (`organ_abstains`), which needs the same broad
> grounded meaning to type arbitrary goals/outcomes. It is also the wiring the strategy session owns
> (Q111) and has been gated on for weeks. Re-rank against p3/p4/p5 if you judge one higher.

> **If a tool call is denied, STOP and report the exact denial text verbatim. Do not retry a variant,
> and do not silently proceed without the denied step.**

> ## ⚙️ SOLVER OPERATING PROTOCOL (standing -- owner 2026-08-25, strengthened 2026-08-26; in EVERY problem)
> **DO THE RIGHT THING, NOT THE CHEAP OR EASY THING** -- the mission is the most brain-faithful substrate,
> not the fastest green check.
>
> **🧠 THE OPENING MOVE, BEFORE ANY METHOD: how does the BRAIN actually do THIS?** Name the structure /
> circuit and the computation it performs, and try to replicate that OPERATION as exactly as you can. This
> is NOT a tiebreaker you reach for after your tools plateau -- it is the FIRST thing you do. If you have
> not identified the brain's mechanism and attempted to build it, you have not started the real work,
> whatever else you have measured.
>
> **🚀 YOU ARE ENABLED -- AND EXPECTED -- TO EXPLORE FAR AND WIDE FOR THAT MECHANISM.** Go read the
> neuroscience. Cross domains. Propose something that looks NOTHING like this brief or the current substrate.
> You are NOT boxed in -- not by this brief, not by the existing organs, not by the integration points you
> would tie into: if a MORE brain-foundational method conflicts with any of them, SUBMIT that alternative
> solution or DIRECTION instead (say what is incompatible and why yours is more brain-faithful). Exploring
> the brain's true method is the work we most want from you; a bold, well-argued brain-faithful direction --
> even unfinished -- beats a tidy engineering result that never asked the question.
>
> **🧱 A SHARED WALL IS A SIGNAL TO GO DEEPER, NOT A REASON TO STOP.** If several of your angles hit the
> SAME wall, that is strong evidence that NONE of them was the brain's mechanism -- the faithful method is
> probably DIFFERENT IN KIND, not another variation of what you already tried. A wall is a FIDELITY GAP TO
> BUILD ACROSS, never a ceiling. Hitting one is exactly the moment to LEAVE the family of methods you were
> sweeping and ask the biology again.
>
> **⛔ "CONVERGED" HAS A HIGH BAR, AND EXHAUSTING ENGINEERING VARIATIONS DOES NOT MEET IT.** Claim
> convergence ONLY when you have (a) identified how the brain actually performs this computation AND (b)
> replicated that operation as faithfully as you can and tested it, OR shown with a SPECIFIC reason why it
> cannot be replicated here. "I tried several combining / gating / scoring angles and they all plateaued at
> the same wall" is NOT converged -- it is tuning-limited, and it means the brain's mechanism is still
> UN-TRIED. That is a reason to explore harder, not to submit.
>
> **🔁 THE 30-MIN DEEPENING IS HOW YOU FORCE THIS -- IT IS NOT OPTIONAL BUSYWORK.** Run your own cron
> (`CronCreate "13,43 * * * *"`); each fire asks "how does the brain REALLY do this, one level deeper than
> my current mechanism?" -> implement -> test (can-fail, strongest real floor, info-free twin LOSING) ->
> iterate. Its whole purpose is to make you ask the brain question several more times than your own sense of
> "done" would. CANCEL it (`CronDelete`) and submit ONLY when the brain-mechanism bar above is met.
> Declining it because "my angles converged" is precisely the case it exists to catch.
>
> **A rigorous negative is a PASS -- but only if what failed was the brain's actual mechanism, faithfully
> built.** A negative on a family of convenient engineering methods is not a negative on the capability; it
> is a report that you have not yet found how the brain does it.
>
> **📖 REFERENCE THE BRAIN-FOUNDATIONAL AUDIT, AND HELP KEEP IT TRUE.** Before you start, read the entry for the
> system you are touching in `notes/BRAIN_FOUNDATIONAL_AUDIT.md` -- it gives the brain structure, whether the
> brain's equation is PINNED or something we are INVENTING, our current fidelity, and the known deviation, so you
> inherit that instead of re-deriving it. If your work shows a verdict there is WRONG, STALE, or INCOMPLETE, or you
> find a NEW deviation, put a short **AUDIT UPDATE** note in your submission -- the strategy session folds it into
> the audit at integration. The audit is a living, shared map and you help maintain it.

## 1. THE PROBLEM IN PLAIN LANGUAGE

For a long time we thought the "understand a word" step was broken, because on our home test plain
word-counting beat our meaning tools. We just proved that home test was unfair -- it was secretly
rewarding "pick the commonest partner," which is frequency, not meaning. When we make the test fair
(force a choice between the right partner and decoys that appear equally often), **our meaning signal --
the hands-on sensory "feel" of a word -- picks the right partner far above chance and above every
frequency method, on every run.** That is a real, first-of-its-kind win.

**But that win has two limits we have not touched, and they are the whole of this problem:**
1. **It is OFFLINE.** It was measured by handing the meaning tool two words in isolation and asking "are
   these related?" It has never been put back into the live reader that reads passages, and never shown
   to change what the reader actually understands.
2. **It is CONTEXT-FREE.** It judges a word by itself, with no sentence around it. But the brain does not
   do that -- it picks a word's meaning FROM the context it is sitting in ("bank" by a river vs "bank"
   that holds money). Our win happened without any of that.

**So the question is: does this win survive contact with real reading?** Wire the meaning signal into the
live reader, give it the sentence as context the way the brain does, and test whether it makes the reader
understand a passage better than counting does -- or whether the offline win evaporates the moment it has
to work in context.

## 2. WHY THIS ONE

- **It is the stage-2 payoff.** Stage 2 (meaning) has read BROKEN for the whole stretch. This is the
  first credible path to marking it something other than broken -- but only if the win transfers to a
  live task. An offline win on borrowed word-pairs is not a capability until it is wired and helps.
- **It is convergent.** `organ_abstains` (just integrated) showed the goal organ says "I don't know" to
  two-thirds of a bank because it lacks broad grounded meaning; this same grounded read-out is what would
  feed it. One build unblocks two lines.
- **It closes an honesty gap the win itself flagged.** The solver of `the_own_metric` explicitly listed
  "context-conditioned read-out (use the reading sentence, not isolated word pairs)" as the missing,
  fully brain-faithful form -- and noted the cache stores provenance PAIRS, not sentences, so context is
  not yet reachable at read-out. That is the exact thing to build here.

## 3. HOW THE BRAIN DOES THIS (frame + discipline)

**PINNED (do not invent around it):** meaning selection is CONTEXT-DRIVEN. The most-frequent sense/associate
is a PRIOR that context overrides when the sentence demands it (lexical-ambiguity resolution -- Duffy &
Rayner reordered-access; the subordinate-bias effect: a subordinate meaning wins when the context supports
it, against frequency). The grounded meaning itself is cross-modal agreement at the anterior-temporal (ATL)
semantic hub over sensorimotor spokes (Patterson 2007; Lambon Ralph 2017) -- which is the grounded
sensorimotor channel this win rides. A brain-faithful read-out therefore (a) uses the current context to
select, and (b) lets the right partner win even when it is not the most frequent.

**OUR-INVENTION-UNDER-TEST:** exactly HOW context conditions the grounded read-out (which contextual
signal, how it re-weights or re-ranks the grounded candidates), and where in the read loop the sentence
context is retained and handed to the read-out. Copy the brain's OPERATION (context-appropriate selection),
sweep any parameter (window, weighting) rather than adopting a number.

**Corpus confound to hold in mind (owner, repeatedly): the reading corpus (McGuffey-derived) is ~200 years
old.** Context-conditioned selection tested only on archaic prose may under-read; the `the_own_metric`
solver already flagged "test whether a MODERN corpus revives the distributional spoke and the concreteness
crossover." If the context-conditioned read-out looks flat, ask whether corpus modernity is doing the work
before concluding the mechanism failed.

## 4. MEASURED vs INFERRED

**MEASURED (`the_own_metric_may_reward_frequency_not_meaning`, SOLVED, re-verified scaffold-free):** on a
FREQUENCY-CONTROLLED own metric, raw counting reads exactly chance by construction (EXACT-matched pool),
while concreteness-stripped grounded meaning beats the strongest floor (PPMI) over its UPPER bound
CI-separated at every pool size (K=1: 0.741 vs 0.558; K=4, K=9 likewise), info-free twins (shuffled
grounding / random pick) at chance. Concreteness, coverage and pseudoreplication controlled. **This is
OFFLINE (isolated word pairs) and CONTEXT-FREE.** Scope: grounded-covered terms only (~55-75%).
Deeper-fidelity iteration CONVERGED: control-gated distributional weighting does NOT help at this corpus
scale (the distributional reading spoke is worse than grounded on both abstract and concrete terms -- no
crossover to gate); plain concreteness-stripped sensorimotor cosine is the current optimum.

**INFERRED / OPEN (this problem, decisive either way):**
- Whether wiring that grounded read-out into the LIVE reader improves a downstream comprehension task over
  (a) the current live reader and (b) a frequency floor.
- Whether CONTEXT-CONDITIONING (using the reading sentence to select) beats the context-free grounded
  read-out -- i.e., whether the brain-faithful form is worth its extra machinery at this corpus scale.

## 5. ALREADY TRIED / DO NOT RE-RUN

- The frequency-controlled OFFLINE metric itself -- done, re-verified (`the_own_metric`). Do NOT re-run the
  offline word-pair comparison; the OPEN question is the LIVE/context one.
- The read-outs vs counting on the OLD (frequency-unfair) top-1 metric -- done (p2). That metric is RETIRED
  as the arbiter of "is stage 2 broken"; do NOT grade against it.
- Route B `track_all_content_lemmas` (co-occurrence coverage for grounded terms) is ALREADY LANDED in
  `hdlab/reading_grounding_loop.py`, default-OFF -- turn it ON for this work rather than rebuilding it.
- Query `experiment_index.py query "readout"`, `query "comprehension"`, `query "context"`, and check
  `exp_base_first_reader_heldout_context_learn_v1`, the `cortical_read_*` cells, and
  `notes/problems/cortical_read_never_tested_where_it_matters/` before building a downstream harness -- a
  held-out context-reading harness may partly exist to reuse.

## 6. VERIFY BEFORE YOU START (the disk outranks this brief)

- Reproduce the offline win once from `experiments/exp_ownmetric_frequency_controlled_v1.py` /
  `verification/test_ownmetric_frequency_controlled.py` so you trust the base you are extending.
- Read how the live reader currently assigns/uses word meaning: `hdlab/reading_grounding_loop.py`
  (the read loop / `ConceptSpace`, the `track_all_content_lemmas` and separable count store),
  `hdlab/meaning_fusion.py`, `hdlab/grounded_similarity.py`. Confirm on disk whether the reading SENTENCE
  is available at read-out time or only provenance pairs -- that determines whether context-conditioning
  needs a read-loop change (which the strategy session lands, not you).
- Confirm the grounded-covered population you will score on, and recompute EVERY floor on it.

## 7. THE BAR

Pick (or build) a DOWNSTREAM comprehension task whose answer depends on selecting the CONTEXT-APPROPRIATE
meaning -- not the most frequent one (e.g. context-conditioned sense/partner selection where the correct
choice is deliberately NOT the highest-frequency associate; reversible where possible). On that task, on a
grounded-covered, held-out population, with all floors recomputed on that population:

- **A context-conditioned grounded read-out must beat, CI-separated over its UPPER bound:** (a) the
  strongest FREQUENCY floor actually run, (b) the current live reader's meaning assignment, AND (c) the
  CONTEXT-FREE grounded read-out (to prove context earns its keep). **Info-free twin (shuffled grounding /
  scrambled context) LOSING.** Report CI half-width and null p95 beside every margin.
- **DECISIVE EITHER WAY:**
  - Context-conditioned beats context-free beats the floor -> wire the CONTEXT-CONDITIONED read-out
    (propose the hdlab diff; strategy lands it).
  - Context-free already matches context-conditioned and both beat the floor and the live reader -> wire
    the SIMPLER context-free read-out; report that context does not earn its machinery at this corpus scale
    (and flag the McGuffey-age confound as the reason to re-test on a modern corpus).
  - Neither beats the current live reader downstream -> the offline win does NOT transfer to live reading;
    report that as an explicit, honest result (a rigorous negative is a PASS) -- it closes the meaning-line
    wiring rather than leaving it perpetually "re-opened."

## 8. FILES AND ENTRY POINTS

- `hdlab/grounded_similarity.py` (the grounded sensorimotor spoke, `GROUNDED_CAP=0.45` -- do NOT raise it),
  `hdlab/meaning_fusion.py` (the general read-out), `hdlab/reading_grounding_loop.py` (the read loop /
  `ConceptSpace`, `track_all_content_lemmas`, the separable count store) -- the wiring targets.
- `hdlab/context_grounded_valence.py`, `hdlab/gated_fusion.py` -- context-conditioning machinery that may
  already exist; check before building.
- `experiments/exp_ownmetric_frequency_controlled_v1.py` (+ `_v2_deeper_mechanism`),
  `verification/test_ownmetric_frequency_controlled.py` -- the offline base to extend.
- `data/conceptnet_gold_v1` -- the gold neighbours.
- Prove in `experiments/` + `verification/`; propose the hdlab WIRING diff in `SOLVED.md` (the strategy
  session lands it, board Q111). **Do NOT write `hdlab/`.**

## DO NOT QUOTE / DO NOT REDO

- Do NOT quote "counting beats the read-outs / wiring NO" as the meaning-line verdict -- that was the
  frequency-UNFAIR metric, now retired. The current base is "meaning beats frequency on the FAIR offline
  metric; the live/context transfer is untested."
- Do NOT grade the live task against the old top-1-argmax-over-co-occurrents grounding-precision metric.
- Do NOT carry any offline number (0.741 etc.) onto the live task -- different scorer and population; no
  number crosses. Recompute every floor on the live population.
