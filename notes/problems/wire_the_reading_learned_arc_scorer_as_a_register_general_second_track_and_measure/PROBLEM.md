---
priority: 11
slug: wire_the_reading_learned_arc_scorer_as_a_register_general_second_track_and_measure
status: OPEN
review:
review_text:
---

# PROBLEM: the reading-learned (treebank-free, self-taught-from-reading) arc scorer, read as a graded DISTRIBUTION, was MEASURED to MATCH the frozen supervised scorer on the reader's own verb→argument extraction in-domain AND BEAT it out-of-domain (GUM +0.0235 CI-sep, no treebank at inference, twin losing) — because the memorized supervised scorer DEGRADES on unfamiliar register while the self-taught one HOLDS. That register-generality win is currently a cached experiment result, LIVE-UNWIRED. Wire the reading-learned scorer as a reliability-gated SECOND TRACK feeding the SAME `graded_parser.single_root_marginals` (select it when the supervised scorer's marginal reliability drops = the register-shift signature; recall byte-identical when the gate does not fire), persist the cached model as a durable BF asset, add a `board_register_general_extraction_dimension`, and MEASURE whether the in-domain-parity / OOD-win transfers to the LIVE board — OR a rigorous located negative naming why it does not (with a number). Glass-box, NO gold/treebank/LLM at inference.

**slug:** `wire_the_reading_learned_arc_scorer_as_a_register_general_second_track_and_measure` — **opened:** 2026-09-11 by strategy, the #1 NEXT STEP of the owner-DONE `scale_the_reading_learned_arc_scorer…` (INTEGRATED 2026-09-11; the capability WIN was measured but the wire is Q111 = strategy/solver lands). **status:** OPEN. The reading-learned scorer is the brain-foundational ACQUISITION replacement for the parser cluster's supervised root; this problem realizes its measured register-generality lever LIVE. Glass-box, NO external LLM at inference.

> ## SOLVER OPERATING PROTOCOL (standing — owner 2026-08-25/26; in EVERY problem)
> **DO THE RIGHT THING, NOT THE CHEAP OR EASY THING** — the mission is the most brain-faithful substrate, not the fastest green check.
> **THE OPENING MOVE, BEFORE ANY METHOD: how does the BRAIN actually do THIS?** Name the structure / circuit and the computation it performs, and replicate that OPERATION as exactly as you can — the FIRST move, not a tiebreaker.
> **YOU ARE ENABLED — AND EXPECTED — TO EXPLORE FAR AND WIDE.** Read the neuroscience; cross domains; if a MORE brain-foundational structure than this brief names emerges, submit THAT instead (say what is incompatible and why yours is more faithful).
> **A SHARED WALL IS A SIGNAL TO GO DEEPER, NOT A REASON TO STOP.** A wall is a FIDELITY GAP TO BUILD ACROSS, never a ceiling.
> **"CONVERGED" HAS A HIGH BAR.** Claim it only when you have (a) identified how the brain performs this computation AND (b) replicated that operation as faithfully as you can and tested it, OR shown a SPECIFIC reason it cannot be replicated here. Exhausting engineering variations is NOT convergence.
> **EVERY COMPONENT MUST BE 100% BRAIN-FOUNDATIONAL, and spaCy/GLUCOSE/MAVEN + any off-the-shelf parser/dataset/model are NOT brain-foundational — never reach for a convenient/easy tool without careful consideration** (a treebank is admissible ONLY as an OFFLINE analysis/eval asset, NEVER as the inference-time scorer; an external tool AT INFERENCE or a supervised scorer trained on labelled trees at read-time is a DEFECT that BLOCKS the reading-learned track).
> **🧩 ONE BRAIN STRUCTURE = ONE ORGAN WITH ARMS (owner 2026-09-10):** this is a reliability-gated SECOND TRACK/arm of the EXISTING parse (feeds the same `graded_parser.single_root_marginals`), NOT a new parser organ. Consult `BRAIN_STRUCTURE_CONSOLIDATION_AUDIT.md` before adding anything.
> **A rigorous negative is a PASS** — but only if what failed was the brain's actual mechanism, faithfully built.
> **REFERENCE `notes/BRAIN_FOUNDATIONAL_AUDIT.md`** for the systems you touch; put a short **AUDIT UPDATE** in your submission for any verdict you find wrong/stale.

> ## BRAIN-FOUNDATIONAL CHECKLIST (work through IN ORDER; the solution is not done until every box holds)
> 1. **OPEN — how does the BRAIN do THIS?** The brain parses with a self-organized, reading-learned scorer read as a graded posterior under reliability-weighted competition (Lewis-Vasishth keep-alternatives-alive), NOT a frozen argmax rulebook; register generality comes from the distributional acquisition being re-estimable, while a memorized supervised map is brittle off-distribution. Replicate: feed BOTH scorers' arc-score matrices into the SAME Matrix-Tree marginal; read top-k by reliability; prefer the reading-learned track when supervised marginal reliability (entropy/margin) drops. Mark each choice PINNED vs OUR-INVENTION.
> 2. **REUSE + ONE-STRUCTURE-ONE-ORGAN** — REUSE `hdlab/graded_parser.py` (`single_root_marginals`, `graded_competition`, `chu_liu_edmonds` — all brute-verified), the cached reading-learned scorer (`exp_readlearned_scorer_scale_v1` SelfSupEM @ EM round 2 + the 5-family construction bonuses), the `reading_grounding_loop` directional grow-by-reading channel (the acquisition substrate), the construction library. Do NOT re-train or re-mine — REUSE the cached model; PERSIST it as a durable rebuildable asset.
> 3. **GENERALIZE — does it need to, and how does the brain?** The gate must generalize across registers (the point): a marginal-reliability detector selects the reading-learned track on OOD prose, the supervised track in-domain — measured, not hard-coded per corpus.
> 4. **HIT A WALL → GO DEEPER, don't stop.** If the in-domain-parity / OOD-win does NOT transfer to the LIVE board, ask WHY with a number (the live board's extraction dim is in-domain-only? the reliability gate can't separate register shift because the reading-learned posterior is MISCALIBRATED — the submission's located root cause? construction weighting wrong per consumer?) — a numbered located negative is a full pass.
> 5. **OPTIMIZE BY EXACT REPLICATION** — copy the MBR/marginal read + the top-k competition; SWEEP the reliability threshold, the per-consumer construction weights (light {verbarg,coord,npmod} for extraction vs full 5-family for content-UAS — the measured per-consumer tradeoff), the EM round — never adopt a fitted value.
> 6. **PERFORMANCE vs THE BRAIN + FULL-STACK UPSTREAM** — add `board_register_general_extraction_dimension()` reusing the submission's downstream A/B (`exp_readlearned_downstream_ab_v1` / `_final_v1`): score verb→argument extraction on an IN-DOMAIN slice AND an OOD slice, both scorers into the same graded marginal, info-free shuffled-top2 twin LOSING, no treebank at inference. Confirm the recall path is byte-identical when the gate does not fire (default-off unless register-shift).
> 7. **ADJACENT COMPONENTS.** The live default head path is the ARC-EAGER parser (`parser_arceager=True`); this second track feeds `graded_parser` (arc-factored) — enumerate which live consumers read which parse and wire the gate so extraction consumers can select the reading-learned track without disturbing the arc-eager head path. The gain-calibrated MBR decode (land-ready, same submission) is the companion read.
> 8. **COMPLETION BAR.** The reading-learned scorer wired as a reliability-gated second track (default-off / byte-identical unless the register-shift gate fires), the cached model persisted as a durable rebuildable BF asset, a board dim scoring in-domain vs OOD extraction, and a MEASURED number on whether the in-domain-parity + OOD-win (+0.0235 CI-sep) transfers to the LIVE board (twin losing, no treebank) — OR a rigorous located negative naming why (with the number).
>
> **(PHASE DIAGRAM.)** The reliability threshold, per-consumer construction weights, EM round, and marginal temperature are FREE to SWEEP; a wall "at this config" = move the operating point, not a ceiling.
> **(FULL-STACK UPSTREAM.)** Prototype the wire AND confirm every downstream consumer of the parse is byte-identical when the gate is off, and measured when it fires; ACCEPT that a fired gate changes the parse and fix downstream to RECEIVE it.

## 1. THE PROBLEM IN PLAIN LANGUAGE
The reader figures out sentence structure with a rulebook it memorized once from a big hand-labeled dataset. We also built a rulebook that teaches itself just by reading, with no labels — and measured that, when you let it keep its best two guesses instead of committing to one (the way the brain hedges), it TIES the memorized rulebook on ordinary text and BEATS it on text from unfamiliar sources, because the memorized one gets brittle on unfamiliar material while the self-taught one holds up. Right now that win only exists in a saved experiment. This problem wires the self-taught rulebook in as a second option the reader switches to when it notices the text is unfamiliar, saves the self-taught model so a fresh install can rebuild it, adds a scoreboard measure for "unfamiliar-text structure reading," and checks whether the win shows up for real. If it doesn't carry over, say exactly why (with a number) — that's a full answer too.

## 2. WHY THIS ONE — the deep root, under the HARD 100%-BF gate
It realizes a MEASURED, twin-controlled, CI-separated capability win (register-general extraction with NO treebank at inference) that is currently live-unwired — the exact "landed ≠ live" gap the owner flags. It is the brain-foundational ACQUISITION replacement for the parser cluster's supervised root (4 live NOT_BF organs route here), delivered as a reliability-gated arm (anti-fragmentation-clean, byte-identical off). The parser is the shared extraction wall multiple integrated solvers route their residuals to.

## 3. MEASURED vs INFERRED
- **MEASURED (on disk, reverified 94/94):** reading-learned scorer read as top-2 marginal, both scorers into the same `single_root_marginals`, verb→arg extraction: in-domain UD-EWT 0.9356 vs supervised 0.9283 (+0.0073 CI[−0.005,+0.019] = parity); OOD GUM 0.9202 vs 0.8967 (+0.0235 CI[0.015,0.033] CI-SEP); supervised degrades OOD 0.928→0.897, reading-learned holds 0.861→0.839; info-free twin loses (0.44/0.40); NO treebank at inference. The reading-learned posterior is MISCALIBRATED (the adaptive-k readout does not strictly dominate flat top-2 — the located root cause of the residual).
- **INFERRED (to prove or refute WITH A NUMBER):** that a marginal-reliability gate cleanly selects the reading-learned track on register shift; and that the in-domain-parity + OOD-win transfers to the LIVE board's extraction dim (twin losing). Do NOT assume it transfers — the miscalibrated posterior may blunt the gate; a numbered located negative is a full pass.

## 4. ALREADY TRIED / DO NOT REDO
- Do NOT re-train/re-mine the scorer (REUSE the cached SelfSupEM @ EM r2 + constructions) or re-implement `graded_parser` (REUSE `single_root_marginals`/`graded_competition`/`chu_liu_edmonds`).
- Do NOT chase raw UAS (the located negative: text-only caps 0.478; scale/EM/online all NOT the lever — the win is the DISTRIBUTION READ + OOD generality, not point-decode UAS).
- Do NOT reverse the arc-eager head-path consolidation for a decode micro-win; wire the second track as a reliability-gated arm of `graded_parser`.
- Do NOT weight constructions uniformly across consumers (the measured per-consumer tradeoff: light for extraction, full for content-UAS).
- Barred: spaCy / any external LLM / a treebank-trained scorer at inference.

## 5. VERIFY BEFORE YOU START (the disk outranks this brief)
Understand ALL existing organs first: `python tools/substrate_map.py`, skim `hdlab/graded_parser.py`, `hdlab/reading_grounding_loop.py` (directional channel). Read IN FULL: the owner-DONE `scale_the_reading_learned_arc_scorer…/SOLVED.md` (esp §12 the MBR/adaptive-k readout, §14 the per-consumer tradeoff, §16 + NEXT STEPS the wire spec) + its downstream cells (`exp_readlearned_downstream_ab_v1.py`, `exp_readlearned_downstream_final_v1.py`, `exp_readlearned_scorer_scale_v1.py` for the cached model). Read `notes/BRAIN_FOUNDATIONAL_AUDIT.md` §2b (the pri-2/pri-3 parser entries). Grade on MODERN gold (UD-EWT in-domain + GUM OOD; 19c banned). CAP local runs below all cores (`OMP_NUM_THREADS=4 …`); heavy EM/ingest goes remote. Report CI half-width + the shuffled-top2 null.

## 6. THE BAR (can-fail; a rigorous located-negative naming the exact ceiling WITH a number is a full pass)
Wire the reading-learned arc scorer as a reliability-gated SECOND track feeding `graded_parser.single_root_marginals` (default-off / recall byte-identical unless the register-shift gate fires), PERSIST the cached model as a durable rebuildable BF asset, add `board_register_general_extraction_dimension()` (in-domain + OOD, twin-controlled), and DELIVER a MEASURED number: does the in-domain-parity + OOD-win (+0.0235 CI-sep) transfer to the LIVE board's verb→argument extraction, info-free twin LOSING, NO treebank at inference? OR a rigorous LOCATED NEGATIVE naming why not (the miscalibrated posterior / the board dim's register coverage / the gate's separation), with the number. INVARIANT: byte-identical when the gate does not fire; no external tool/LLM/treebank at inference; reliability threshold + construction weights swept not fitted.

## 7. FILES AND ENTRY POINTS
`hdlab/graded_parser.py` (`single_root_marginals`, `graded_competition`, `chu_liu_edmonds` — the shared decode + the wire site); `experiments/exp_readlearned_scorer_scale_v1.py` (the cached SelfSupEM model to persist); `experiments/exp_readlearned_downstream_ab_v1.py` + `exp_readlearned_downstream_final_v1.py` (the downstream A/B = the board-dim source); `hdlab/reading_grounding_loop.py` (the directional grow-by-reading acquisition substrate); `experiments/exp_situation_model_qa_modern_v1.py` (the board — add the dimension); `notes/problems/scale_the_reading_learned_arc_scorer…/SOLVED.md` (the wire spec); `notes/BRAIN_FOUNDATIONAL_AUDIT.md` §2b.

## 8. DO NOT QUOTE / DO NOT REDO
Do NOT quote retired figures (`notes/reference_retired_claims_never_requote.md`). Do NOT re-train the scorer or re-implement graded_parser. Do NOT chase raw UAS (located negative). Do NOT reverse the arc-eager consolidation. Do NOT use spaCy / any external LLM / a treebank-trained scorer at inference. Do NOT weight constructions uniformly across consumers. Do NOT claim "converged" on engineering variations; the bar is a measured live-board transfer number (twin losing) or a numbered located negative.

## STRATEGY ADDENDUM 2026-09-12 — the graded-category INPUT for the joint decode
The owner-DONE pos-tagger solver (integrated 2026-09-12) found the who-was-affected POS loss is a HARD-COMMIT loss: the gold category survives the tagger's top-2 ~0.80 and is thrown away at argmax → Viterbi → parse. The dormant calibrated CRF (`hdlab/crf_tagger.GlassBoxCRF`, `.marginals()`) is the graded-category interface for a JOINT POS-parse decode; as a standalone tagger swap it measured −0.010 (so it is NOT flipped). If the second-track scorer here consumes tag MARGINALS rather than hard tags, measure the joint who-affected number — that is where the +0.07 POS budget lives.
