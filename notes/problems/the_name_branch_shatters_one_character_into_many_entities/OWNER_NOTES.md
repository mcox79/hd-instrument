---
owner_verdict: DONE
---

PARTIAL (downstream premise REFUTED + right fix proven): the_name_branch_shatters_one_character_into_many_entities (SOLVER, opus 4.8). Built the brain-faithful mechanism the brief asked for; the disk refuted the brief's two central claims and I then proposed AND proved the real fix.

Built: a content-addressable complete-or-separate person-node organ (given/surname/title/gender slots; gn-agreement filter; given-name-conflict veto = pattern separation) + a full-span+entity-type loader that enriches the head-token LitBank cache in place (100% alignment, gold→chain 0.9991), so clustering and the downstream are measured on the identical stream.

Cluster quality (LitBank 100 novels, held-out, B-cubed F) — the organ TIES the strong floor, does not beat it: organ 0.785 vs the live head-token floor 0.770 (NOT_SEP; more precise 0.90 vs 0.82 — separates same-surname people the floor over-merges — but trades recall). The brief's own proposed fix, full spans, BACKFIRES (0.705, CI-sep BELOW, over-merge). Info-free twin 0.597 loses CI-sep. Proper-names-only 0.864 vs 0.877 (tie). Root cause the brief missed: the head token IS the surname, so single-token clustering already unifies surname-family forms — the floor is strong, not broken.

Who-did-what (the brief's downstream premise) — REFUTED by decomposition (reproduces the cited gap: live 0.161 ≈ "0.17", oracle-coref 0.56–0.61 ≈ "0.62"): swapping the organ for the incumbent clustering does NOT lift who-did-what (+0.010 NOT_SEP). Perfect pronoun binding on the unchanged head-token clusters recovers the whole gap (HEAD_OPB 0.606, +0.444 CI-sep); better name clustering adds +0.000 given perfect pronouns. A control shows unifying aliases HURTS who-did-what (HEAD_OPB 0.606 > ORACLE 0.562 — the register fan effect). So the who-did-what cap is pronoun→event binding + the register fan effect, NOT name clustering.

Right solution proposed AND PROVEN (in-scope): two adjacent levers, correctly ordered; name clustering is neither. (1) Pronoun→event binding — the dominant lever (+0.444 ceiling), fix = clause-level graded binding + wire the tracked-but-unused clause_role/Centering-Cb topicality (surfaced 3× across problems); diagnosed decisively, build is the adjacent pronoun problem's. (2) The sparse MultiBankRegister — built-but-unwired; I measured it lifts who-did-what +0.0149 CI-sep [0.009,0.022] on the ORACLE config (0.601→0.616, D=1024), modest and masked on the live system until pronoun binding is fixed. Residual to perfect (~0.62 ceiling) is the eval-harness multi-verb-per-clause ambiguity, a definition limit.

Every wall drilled to the bottom (6 research + 2 data drills). Same-surname over-merge ("which Miss Bennet"): rare (112 ties/100 novels, ~80 resolvable); I BUILT + decisively measured the brain's cues — Centering-Cb topicality + the primary-text-verified eldest-daughter convention — with an info-free twin: all null or worse; combined ties its scrambled-role twin; the convention is harmful. Understood: recency is a category error (co-present siblings tie by construction; cue-overload theory); the eldest fact is discourse-internal but needs an entity-fact store we lack (not forbidden knowledge — a missing organ).

Files: experiments/exp_name_entity_clustering_v1.py, experiments/exp_name_clustering_serves_whodidwhat_v1.py (adds prove_register), verification/test_name_entity_clustering.py (9/9 PASS), SOLVED.md. Reverify: .venv/Scripts/python.exe verification/test_name_entity_clustering.py (cluster + serve; torch-free) and ... experiments/exp_name_clustering_serves_whodidwhat_v1.py --prove-register (the FHRR register proof). (Stray throwaway experiments/_probe_align.py — env blocked deletion; safe to remove.)

Proposed hdlab (you land it, Q111) — small + opt-in, NOT a who-did-what fix: land the full-span+entity-type loader (additive, zero-risk) + an opt-in run_person_node_clustering on coreference_resolver.py (default-off, byte-identical off) for high-precision same-surname character separation. Do NOT replace _resolve_name_branch (floor is strong); do NOT invest in name clustering for who-did-what.

AUDIT UPDATE (§2b / COREFERENCE): head-token name branch is a STRONG floor (head=surname); full-span backfires; who-did-what cap = pronoun binding + register fan (multibank fix PROVEN, +0.015); structured person-node slots PINNED for the single-novel timescale; clause_role/Cb topicality tracked-but-unused (3rd measurement).

Ranked adjacencies (the real levers, all in SOLVED.md): (1) entity-keyed FACT/EVENT store — the reader's memory of each character; hdlab/situation_model_multibank.py built-but-UNWIRED; unifies who-did-what + eldest-disambiguation + predicate resolution + ToM; highest leverage. (2) pronoun→event binding at the clause level + wire clause_role/Cb into the graded scorer (serves pronouns and the sister case). (3) nominal/definite-description binding — semantic, ~40% ceiling, hard. Cross-ref the SOLVED wire_entity_tracking_end_to_end_on_running_narrative.

Label is PARTIAL (real build ties the floor + premise refuted + fix proven); flip to REFUTED at integration if preferred. WIP until owner_verdict: DONE.
