---
owner_verdict: DONE
---

SOLVED submission — retrieval_interference_is_similar_competitor_cue_overload_not_event_count (SOLVER, opus 4.8). Status: PARTIAL (WIP until owner_verdict: DONE). hdlab UNTOUCHED (proposed diffs below; strategy lands, Q111). NO external LLM at inference. Reverify: .venv/Scripts/python.exe verification/test_similar_competitor_retrieval.py (16 checks, scaffold-free, recomputes every number from source incl. the self-correction).

THE PROBLEM. Our memory-interference organ models interference as EVENT-COUNT per entity, but real retrieval interference is SIMILAR-COMPETITOR CUE-OVERLOAD. Build the reframed similar-competitor / partial-cue retrieval (a TCM context-reinstatement cue disambiguating feature-matching competitors) and beat the content-only floor on real ambiguous queries.

HEADLINE. The reframe is CONFIRMED, the content×context bar is met — but by the ALREADY-LANDED organ, and BOTH candidate NEW memory-axis cues are RIGOROUS NEGATIVES over the current substrate, which exhausts the memory axis and localizes the residual to the structural/semantic axis (exactly what a memory-route negative should do).

WHAT I MEASURED (real LitBank pronoun coref, held-out TEST docs, n=3,378 ambiguous >=2-candidate queries):
1. REFRAME CONFIRMED. who-did-what event-count proxy: naive recency 0.402 ties content-only 0.398 (event-count is not the driver). Real pronoun coref: content 0.521 < recency 0.606 < subject-recency 0.624; oracle "any cue right" 0.763 -> real complementary headroom.
2. CONTENT×CONTEXT CLEARS THE BAR — VIA THE LANDED ORGAN. graded_coref_pick.graded_antecedent_pick = 0.676, +0.155 CI-sep [+0.138,+0.172] over content-only, beats naive recency, shuffled-context twin LOSES 0.676->0.436. Core thesis holds, but was already delivered by a prior integrated problem.
3. NEGATIVE #1 — the brief's MULTI-TIMESCALE TCM cue. Adding graded_temporal_context as a context cue: -0.001 [-0.009,+0.006] NOT_SEP. The multi-timescale/event-segmented richness adds nothing over the single-timescale ACT-R recency already in the picker.
4. NEGATIVE #2 — MORPHOLOGICAL AGREEMENT (the brief's "feature-MATCHING competitors" content cue). Built reliable gender/number from the FULL LitBank coref chains (gold agreement-compatible 98.4%). A naive comparison looked like +0.040 — but the person-vs-gender decomposition control showed that was the ALREADY-LANDED person pool-cleanup (+0.038); gender's marginal on top is +0.003 [0.001,0.005] NOT_SEP, number +0.004 NOT_SEP (+0.001±0.001 across 6 splits). Rigorous negative — VINDICATES the landed organ's own "gender is a non-lever" note.
5. THE PLATEAU IS INFORMATIONAL, NOT A COMBINATION-RULE LIMIT. Faithful ACT-R Boltzmann retrieval (0.659) barely beats crude additive coord-ascent (0.650); both ~0.10 below the oracle (0.763). A better combination of accessibility cues does not help.
6. THE RESIDUAL IS MEASURABLY STRUCTURAL. Against the current-best substrate (landed+person = 0.714): only 7.9% of golds are unreachable cataphora -> hard ceiling 0.921. Of 966 errors, 72% are STRUCTURAL (gold IS a candidate but no accessibility ranking picks it — Binding/coherence/verb-semantics), 28% unreachable. That is WHY every memory cue plateaus.
7. THE VALIDATED ORGAN IS AN ISLAND. graded_antecedent_pick has ZERO callers in hdlab (grep-verified); the live situation_reader resolves coref via the older hdlab.coref + EventCentralityReader. So the +0.155 win is NOT delivered in read() today -> WIRING is the real lever, not a new cue.

FLOORS (recomputed on the SAME held-out population): content-only frequency 0.521; naive recency 0.606; incumbent hard-tier 0.624; the LANDED picker 0.676 (both new cues had to beat this); the PERSON-filtered picker 0.714 (the correct baseline the agreement cues had to beat). who-did-what event-count proxy content floor 0.398.
CONTROLS: shuffled-context twin loses; shuffled-phi twin loses; PERSON-vs-GENDER decomposition (the control that caught v7's conflation — gender's apparent +0.040 was +0.038 person + 0.003 gender); reliability/cue-overload gate ablation (ON 0.639 < OFF 0.650 — refuted); cue-validity no-fit (0.574 — weights must be fit); oracle 0.763 vs best combiner 0.659 (combination rule not the bottleneck); 6-split robustness; who-did-what proxy recency-ties-content (confirms reframe); residual characterization (ceiling 0.921, 72% structural).

KEY REALIZATIONS. (1) Ask whether the experiment COULD succeed first — the oracle diagnostic proved the cues were complementary before building. (2) The disk outranks the brief's instrument — "content 0.398, recency ties it" is a who-did-what-proxy artifact; on real pronoun coref recency beats content. (3) Read the landed substrate before claiming a build — the picker + person cleanup were already there. (4) A control that decomposes a win by SOURCE beats the win — the person-vs-gender split caught a false +0.040 gender win (it was already-landed person cleanup); the scaffold-free witness's independent recompute is what forced the decomposition. (5) A tight NOT_SEP (-0.001; +0.003) is the cleanest negative — indistinguishable-from-nothing, not a noisy near-miss. (6) A fidelity bug in the ESTIMATE is not a fact about the brain — agreement only became testable once phi came from full coref chains (36%->98% gold-compat) and number used gendered-pronoun->singular; neither changed the verdict.

AUDIT UPDATE (BRAIN_FOUNDATIONAL_AUDIT.md §2b). Retrieval interference = similar-competitor cue-overload, NOT event-count (CONFIRMED). Working context-reinstatement cue = single-timescale recency/ACT-R base-level, already in graded_antecedent_pick; the MULTI-TIMESCALE TCM organ adds nothing for linear-order antecedent retrieval (its value is in the episodic store / belief timeline, not coref). Gender/number agreement is a NON-LEVER over the landed person/animacy cleanup (confirms, not challenges, the landed note). The coref memory axis is at its informational ceiling (~0.68–0.71; oracle 0.76); the residual is structural.

PROPOSED hdlab ACTION (strategy lands). (1) WIRE (#1, verdict-independent): graded_antecedent_pick is an island; measure it vs the reader's current live coref (hdlab.coref + EventCentralityReader) and route read() through it if it wins — this delivers the otherwise-unrealised memory-axis win. (2) do-NOTs: don't wire the multi-timescale TCM organ or a gender/number agreement filter into coref (both measured saturated); keep the event-count organ off the coref path.

WHAT I DID NOT ESTABLISH. No NEW organ beats the current substrate (the memory-axis win is the already-landed picker+person cleanup; my v1–v5 re-derived it and v7 initially over-attributed the person win to gender — corrected). Oracle 0.763 is optimistic (peeks at gold). Agreement's negative is on LitBank fiction with mention-derived phi; a phi-dense or dialogue-heavy corpus could shift it. Cross-corpus generalization untested (no modern coref corpus on disk). EventSegmentedContext is the one untested memory lever (needs an event-boundary detector; not half-tested, to avoid an unfair test).

FILES. experiments/exp_similar_competitor_{pronoun_diagnostic_v1, actr_tcm_combiner_v1/v2/v3/v4_robust, actr_boltzmann_ceiling_v5, vs_landed_gradedpick_v6, agreement_cue_v7, agreement_decomposition_v8}.py; verification/test_similar_competitor_retrieval.py (16 checks); notes/problems/<slug>/SOLVED.md. hdlab UNTOUCHED.

TLDR (plain language). Working out who "she"/"it" means gets hard when several look-alikes compete. I tested the two hoped-for fixes on 9,000 real pronoun cases from 100 novels: a fancier "when did I last see it" clock, and a "match the gender/number" check. Both make no real difference — the fancier clock adds nothing the simple one already had, and gender/number matching adds nothing once the narrator ("I"/"you") is removed from the candidates, which the system already does. Two dead ends measured, not guessed. The genuinely useful findings: (a) the good method that blends what+when is already built but NOT plugged into the live reader — so plugging it in is the real win; (b) by counting, 72% of the remaining errors need sentence grammar/meaning, not memory. I also corrected myself: a gender result looked like a win until a check showed the gain was really the already-built narrator-removal step.

QUESTIONS. None blocking. The one surviving lever (sentence grammar/structure) is its own problem, mapped below.

NEXT STEPS. (1) WIRE the validated picker into the live reader (measure-then-wire). (2) Land the do-NOTs. (3) File the structural/semantic coref successor (Binding + coherence + verb semantics) — measured to own ~72% of the residual and the headroom to the 0.921 ceiling; composes with the extraction front-end.
