---
problem: chain_multi_step_plans_and_scripts_for_the_generative_world_model
status: PARTIAL
bar: "PASS = a brain-faithful MULTI-STEP plan/script-chaining rollout (glass-box, NO external LLM at inference; an offline-built static plan/script asset IS admissible) that turns the GOAL-subset win into a FULL-POPULATION CI-separated win on TellMeWhy non-adjacent (Lal 2021), over the strongest floor recomputed PER population -- base plausibility 0.2773, topical 0.2500 -- with the info-free twin (permute the chained result-state across candidates) LOSING and no live reasoner regressing. Report the CI half-width and the null p95; gate on the floor's UPPER bound. A rigorous LOCATED NEGATIVE is a FULL PASS if it names the exact depth/plan-knowledge ceiling WITH a number."
result: "SUBSET POSITIVE + a rigorous LOCATED NEGATIVE (the bar's blessed full pass) that REFUTES the brief's own wall guess AND traces the residual to a NAMED, MEASURED upstream. Built a multi-step means-ends rollout whose plan/script knowledge is a low-rank ATL semantic hub (PPMI+SVD over ATOMIC xIntent/xWant/xNeed/xEffect -- the parent's proven markerless-bridge asset, offline/admissible), scored by inverse-planning DIAGNOSTICITY (Baker-Saxe-Tenenbaum) and combined by additive constraint satisfaction (McClelland-Rumelhart). On modern TellMeWhy non-adjacent (Lal 2021, n=256, item-level paired bootstrap): the GOAL-subset (n=92) me2 0.5326 vs base 0.2935 +0.2391 CI[0.1304,0.3587] CI-sep (half-width 0.1141), me_diag 0.4674 +0.1739 CI[0.0652,0.2717], BOTH CI-sep and BOTH ABOVE the parent single-step rollout rs 0.3913 (+0.0978); the info-free twin LOSES (me_diag obs 0.4674 > null p95 0.3478, p=0.0). COVERAGE CRACKED: the graded hub fires on 88.0% of gold goal-causes vs the parent single-step 8.7% (rs here 25.0%) and binary CSKG 21.7% -- the multi-step DEPTH wall is crossed for the goal slice. FULL population n=256: TIES base (me_diag +0.0117 CI[-0.0391,0.0625]; me2_mult -0.0039; multi +0.0039; torder -0.0508; multi2 -0.0352; none CI-sep, none beats its twin). THE RELOCATED WALL, QUANTIFIED: the goal engine DAMAGES the non-goal 64% CI-sep (me_diag OTHER 0.1890 vs base 0.2683, -0.0793 CI[-0.128,-0.0305]); the cause-type composition is GOAL 92 / OTHER-associative 101 / PHYSICAL 29 / MENTAL 20 / AFFECTIVE 14 (no directed engine reaches the 39% associative slice). UPSTREAM SIGNAL-LOSS LOCALIZED: the damage is a COREF / PARTICIPANT-BINDING loss -- 67% of the goal engine's non-goal false-flips are DIFFERENT-agent goals (the boosted wrong candidate belongs to a different participant than q's agent), a crude surface agent-gate RECOVERS ~61% of the OTHER damage (-0.0793 -> -0.0305, CI includes 0), and 97% of non-goal stories are multi-agent (so coref can disambiguate); the fix needs RESOLVED coref (E3), not surface matching (the surface proxy halves the goal-subset win because it cannot resolve she==Mary). So the wall is NOT plan-knowledge coverage (cracked) and NOT rollout depth (crossed); it is the MULTI-ENGINE COMPETITION over RESOLVED participants -- the goal engine is one voice of several and cannot carry the full population without brain-foundational coref upstream."
floor: "base plausibility engine (content+physics+psych, goal OFF), the parent's strongest floor, recomputed per population: FULL 0.2773, GOAL 0.2935, OTHER 0.2683; topical (content-only argmax) FULL 0.2500. base_add (additive integration, no means-end) FULL 0.2734 -- gated against as well. Strongest-floor-full = 0.2773. Info-free twin per arm (permute the per-candidate means-end signal across candidates, 400 draws): GOAL null p95 0.3478 (me_diag obs 0.4674, p=0.0 -> LOSES); FULL null p95 ~0.32 for every arm (no arm's observed beats it -> the located negative)."
controls: "(1) INFO-FREE TWIN (permute the per-candidate signal across candidates within each item, argmax recomputed, 400 draws): GOAL me_diag BEATS its twin (obs 0.4674 > p95 0.3478, p=0.0 -> load-bearing); FULL NO arm beats its twin (the located negative). (2) BASE ABLATION (means-end OFF) -- me2/me_diag beat base CI-sep on GOAL (+0.239/+0.174). (3) TOPICAL floor -- CI-sep on GOAL. (4) PARENT-ROLLOUT baseline (rs, single-step) reproduced 0.3913 on GOAL; the graded hub is ABOVE it (coverage 88.0% vs 25.0%). (5) BINARY-CSKG baseline (ms2) reproduced; the hub beats it (coverage 88.0% vs 21.7%). (6) OTHER-subset ablation -- CI-sep BELOW base (-0.0793), the quantified cross-type damage. (7) TELEOLOGICAL-STANCE ROUTER -- does NOT separate (qint|goal 0.99 ~= qint|other 0.97): located negative (the discriminator is the CAUSE type, unknown at inference). (8) AFFECT ENGINE (directed valence-causation) -- domain too small (25/256, ~10%), no full-pop lift. (9) MULTIPLICATIVE content-gate -- still damages OTHER (-0.098). (10) NO-REGRESS -- inert without a goal marker (byte-identical additive-off == base_add), modifies no live reasoner. (11) FROM-SOURCE generalization -- held-out AUC 0.6753 + direction-faithful 6/6. (12) TEMPORAL/SCRIPT ENGINE (strategy's landed broadened order store, hdlab.temporal_script_schema, 454k pairs, coverage 0.84): torder alone FULL -0.0508 (not sep), the goal+affect+temporal competition WORSENS OTHER (-0.122 vs -0.079) -- the context-free script prior discriminates canonical ORDER not the cause (strategy's own scope caveat, confirmed). (13) UPSTREAM TRACE (participant-binding) -- a surface agent-gate RECOVERS ~61% of the OTHER damage (-0.0793 -> -0.0305) and 67% of the false-flips are DIFFERENT-agent goals; EXCLUDES 'the damage is intrinsic to the means-end / gold-incompleteness' (boosted-in-helpful = 0) and LOCATES it as the missing-coref upstream."
files_changed: "experiments/exp_multistep_meansend_chain_v1.py (THE cell: the ATL-hub means-end MeansEnd(PPMISVDBridge over ATOMIC) with direct + diagnostic + explicit-2-hop-chain; the situation-model sot_accum; the teleological-stance router; the directed affect engine; the temporal/script engine (hdlab.temporal_script_schema) + multi-engine competition; the participant-binding upstream trace (agent-gate + false-flip attribution); additive + multiplicative integration; the full arm ladder + paired-bootstrap CIs + info-free twins + coverage/composition/false-positive/upstream enumeration + no-regress); verification/test_multistep_meansend_chain.py (scaffold-free witness, 12/12; reads landed metrics + from-source hub units); data/exp_multistep_meansend_chain_v1/metrics.json; data/exp_multistep_meansend_chain_v1/meansend_index_v1.json (the cached ATOMIC means-end index; rebuildable via --rebuild-index from data/cskg_foundation_v1/edges_shard_*.jsonl); notes/problems/chain_multi_step_plans_and_scripts_for_the_generative_world_model/{SOLVED.md, BRAIN_FOUNDATIONAL_ANALYSIS_2026-09-08.md, PROBLEM.md}. Gold reused: data/corpora/tellmewhy/. Assets reused: data/cskg_foundation_v1/ (ATOMIC), data/exp_broaden_causal_order_store_v1/ (temporal store). NO hdlab write (Q111 -- proposed landing stated below)."
reverify: ".venv/Scripts/python.exe verification/test_multistep_meansend_chain.py   # 14/14 (~15s): reads landed metrics + runs from-source hub units. W1 disk baseline; W2 GOAL-subset CI-sep win above the parent rs; W3 COVERAGE CRACKED 8.7->88%; W4 GOAL win load-bearing (twin loses p=0); W5 FULL-pop located negative (no arm CI-sep); W6 OTHER-subset damage CI-sep below base; W7 router does not separate; W8 cause-type composition (OTHER largest); W9 false-positive mechanism (70%); W10 hub generalizes (AUC 0.6753) + direction-faithful + no-regress; W11 temporal/script engine located negative; W12 UPSTREAM = COREF/participant-binding (67% of false-flips different-agent); W13 RESOLVED-COREF completing test (recency coref KEEPS the goal win CI-sep unlike the surface proxy, partially recovers OTHER, does NOT cross); W14 FLIP DECOMPOSITION (A same-referent over-fire 5 coref-cannot-fix / B recency-fixes 2 / C recency-merged 8; coref-fixable 10/15; gold-incompleteness 0); W15 the LANDED gender/number-aware coref (WorkingOverlay+gazetteer) plumbing SOUND (subj 0.97, pronoun-resolve 0.95, gazetteer 0.62) but does NOT beat recency (OTHER -0.085 vs -0.061) -> coref-resolution QUALITY is not the binding lever on this protagonist-dominant gold.  FULL recompute (writes only its own dir, ~110s): .venv/Scripts/python.exe experiments/exp_multistep_meansend_chain_v1.py --run --n 1500"
---

# PARTIAL (subset positive + located negative -- the bar's blessed full pass) -- multi-step means-ends CROSSES the parent's coverage/depth wall on the GOAL slice (win DOUBLED, coverage 8.7->88%); the FULL-population wall is NOT depth or coverage but the MULTI-ENGINE COMPETITION, and its dominant residual is traced to a NAMED, MEASURED upstream: missing resolved COREF (participant-binding).

## The brain frame (how the brain does this; PINNED vs OUR-INVENTION)
PINNED: action meaning = RESULT-STATE; goal-serving judged by INVERSE PLANNING (Schank-Abelson; Trabasso-van den Broek; Baker-Saxe-Tenenbaum). To DEPTH>1: instantiate SCRIPTS and chain SUBGOALS via MEANS-ENDS analysis (Schank-Abelson plan boxes; Newell-Simon), realized as a model-based FORWARD ROLLOUT with a goal-state test (hippocampal replay, Mattar-Daw). The plan/script KNOWLEDGE lives in the amodal ATL SEMANTIC HUB (Lambon-Ralph) -- a LOW-DIMENSIONAL distributed code where means-end relations are GEOMETRIC PROXIMITY (an unseen (action,goal) pair scored by position, not a stored edge = generate-not-retrieve). Participant binding = discourse referents (Heim/Kamp file-cards): "who wanted X" is bound to "who did Y". OUR-INVENTION-UNDER-TEST (swept): the hub realization (PPMI+SVD over ATOMIC), diagnostic normalization, rollout depth K, integration weight w_m, the teleological router, the affect/temporal cues, the surface agent proxy.

## What I built (compose landed organs; plan/script knowledge as an offline ATL hub)
`experiments/exp_multistep_meansend_chain_v1.py`. For candidate cause C (a goal statement) and effect action q:
1. THE ATL HUB (`MeansEnd`): a PPMI+SVD low-rank means-end embedding over ATOMIC. `direct(q_actions, C_goals)` = max hub proximity. Low-rank factorization = implicit TRANSITIVE closure -> reaches the DEEP multi-step slice a binary KB traversal cannot. Held-out AUC 0.6753 (generalizes).
2. THE ROLLOUT: an explicit graded meet-in-the-middle 2-hop chain (`chain2`). FINDING: the explicit chain HURTS vs the direct hub (cone-collapse over sparse edges) -- the hub's implicit closure is the right grain.
3. THE GOAL-STATE TEST via inverse-planning DIAGNOSTICITY (`direct_diag`): proximity MINUS the goal's base rate -- converts 88% COVERAGE into a SELECTIVE, CI-separated win.
4. THE DECISION: additive weighted constraint satisfaction. Plus tested competing engines: affect (valence-causation), temporal/script (a cause precedes its effect), and the multi-engine union.
Composes landed assets (ATOMIC hub, the parent's plausibility engine, categorize, rs_fire, temporal_script_schema); adds no organ.

## What I measured (each floor recomputed per population; item-level paired bootstrap; n=256)
| slice | n | headline | verdict |
|---|---|---|---|
| baseline reproduction | 256 | base 0.2773 topical 0.2500 -- matches the parent disk | DISK CONFIRMED |
| **GOAL subset POSITIVE** | 92 | **me2 0.5326 +0.2391 CI[0.1304,0.3587]; me_diag +0.1739; both ABOVE parent rs 0.3913 (+0.0978); twin LOSES (p=0)** | **WIN DOUBLED** |
| **COVERAGE (gold goal-cause fires)** | 92 | **graded hub 88.0% vs parent single-step 8.7% (rs 25.0%), CSKG 21.7%** | **DEPTH WALL CROSSED** |
| FULL non-adjacent | 256 | me_diag +0.0117; me2_mult -0.0039; multi +0.0039; torder -0.0508; multi2 -0.0352 -- all tie, none beats twin | LOCATED NEGATIVE |
| OTHER (non-goal) subset | 164 | me_diag 0.1890 vs base 0.2683, -0.0793 CI[-0.128,-0.0305] (CI-sep BELOW) | CROSS-TYPE DAMAGE |
| cause-type composition | 256 | GOAL 92 / OTHER 101 / PHYSICAL 29 / MENTAL 20 / AFFECTIVE 14 | ASSOCIATIVE residual largest |
| false-positive mechanism | 164 | 69.5% of non-goal items carry a goal-marked distractor; fires on 112 | quantified |
| **UPSTREAM TRACE (coref)** | 164 | **agent-bound OTHER -0.0305 (recovers from -0.0793); 67% of false-flips are DIFFERENT-agent; single-agent stories 3%** | **signal loss = missing coref** |
| ROUTER (teleological stance) | 256 | qint|goal 0.99 ~= qint|other 0.97 -- does NOT separate | located negative |
| AFFECT / TEMPORAL engines | 25 / cov 0.84 | affect domain ~10%; torder full -0.0508 (context-free), worsens OTHER | located negatives |
| NO-REGRESS | -- | inert without a goal marker; additive-off == base_add; modifies no live reasoner | ADDITIVE |

## The wall, located precisely (the deliverable the bar asked for -- and it REFUTES the brief's guess, then traces the upstream)
The brief GUESSED the wall was rollout DEPTH / plan-knowledge coverage. The disk says otherwise, ONE level deeper AND with a named upstream:
- DEPTH / COVERAGE IS NOT THE WALL -- it is CROSSED (88% vs 8.7%) by the low-rank ATL hub (implicit transitive closure), and diagnosticity turns it into a goal-subset win that DOUBLES the parent's (+0.239 vs +0.098).
- THE FULL-POPULATION WALL IS THE MULTI-ENGINE COMPETITION. The goal engine wins +22 on the 92 goal items but LOSES ~18 on the 164 non-goal items (false-fires on the 69.5% that carry a goal-marked distractor), netting a tie. NOT gateable by explanandum type (router does not separate), NOT fixed by the multiplicative gate (-0.098), affect (~10%), or the temporal/script engine (context-free, worsens OTHER).
- THE DOMINANT RESIDUAL IS A NAMED UPSTREAM: COREF / PARTICIPANT-BINDING. 67% of the goal engine's non-goal false-flips are DIFFERENT-agent goals (it boosts a goal belonging to a different participant than q's agent); a crude surface agent-gate RECOVERS ~61% of the OTHER damage (-0.0793 -> -0.0305, CI now includes 0); 97% of non-goal stories are multi-agent (coref can disambiguate); NONE of the false-flips were valid-but-secondary causes (boosted-in-helpful = 0, so not gold-incompleteness). The brain binds "who wanted X" to "who did Y" (Heim file-cards); we have NO live coref (E3 NEEDS_ADAPTER), so the goal engine cannot check agent identity and false-fires. The surface proxy is a LOWER BOUND -- it also halves the goal-subset win (it cannot resolve she==Mary), so the fix needs RESOLVED coref, not surface matching.
- THE ASSOCIATIVE CEILING: the largest non-goal slice is UNTYPEABLE-ASSOCIATIVE causes (39%) that no directed engine addresses -- base's topical/Trabasso connectivity is the ceiling there. Even a perfect multi-engine means-end caps at the parent's oracle 0.773.
- THE COMPLETING TEST (resolved coref, MEASURED -- not projected): I bound the goal means-end on RESOLVED participants via a recency-Centering resolver (a pronoun binds to the most-recent name -- the PINNED Centering cue). Result: it KEEPS the goal-subset win CI-separated (+0.152 vs the surface proxy's halved +0.098) AND partially recovers OTHER (-0.061 from -0.079), giving the best FULL (+0.0156) -- but STILL TIES. So resolved coref helps but a recency-only resolver does NOT cross.
- WHY (the deep decomposition of the 15 OTHER-subset flips, W14): A = same-referent over-fire 5/15 (33%; the boosted wrong goal belongs to the SAME participant as q -- coref CANNOT fix it; the goal engine fires on a non-causal goal the agent genuinely has); B = different-agent recency keeps distinct 2/15 (13%; recency coref suppresses these); C = recency-MERGED a different surface agent 8/15 (53%). NB: reading the items (below) REINTERPRETS C -- the surface "different agents" (q="he", boosted="Ike") are the SAME PERSON, and recency merges them CORRECTLY; so C is NOT a coref error and gender-aware coref does NOT fix it (confirmed measured). None were unmarked gold causes by the automatic helpful-membership check (boosted-in-helpful 0), but reading shows several ARE valid goals the gold omitted. So this surface decomposition MISATTRIBUTED the damage to coref; the item-level reading (below) is the true account.
- THE GENDER/NUMBER-AWARE COREF, MEASURED (I plugged in the LANDED WorkingOverlay phi-agreement + Centering salience + the name-gender gazetteer -- brain-foundational, gold-free): plumbing SOUND (subject-found 97%, pronoun-resolution 95%, gazetteer coverage 62%), but it does NOT beat the trivial recency resolver and slightly underperforms it (OTHER -0.085 vs recency -0.061; GOAL +0.130 vs +0.152; FULL -0.008, ties). It DISAGREES with recency on 24% of q-agent resolutions and those net HURT. This REFUTES my earlier ~0.33 gender-aware projection: on THIS gold, coref-resolution QUALITY is NOT the binding lever -- TellMeWhy stories are short and PROTAGONIST-DOMINANT, so recency is already a near-ceiling Centering cue, and the resolver's topical-protagonist override + the 38% gazetteer gaps make it diverge and net slightly worse. (The C recency-merge slice the decomposition flagged as gender-fixable is not recovered by the landed resolver on this gold -- multi-agent disambiguation is rarely exercised here.)
- READING THE ITEMS (the deepest trace -- it CORRECTS the coref framing): I dumped and read all 15 OTHER-subset flips (in metrics.json flip_items). The surface "different-agent" decomposition was an ARTIFACT of the surface-agent heuristic: the "different surface agents" (q="he", boosted="Ike") are THE SAME PERSON, and recency already merges them correctly (agents-recency: q=ike boosted=ike). So coref is essentially NOT the lever -- there is almost no genuine cross-agent error to fix, which is exactly WHY the landed gender-aware resolver did not help. The real signal loss on the non-goal population, from reading the items, is THREE mechanisms: (1) GOLD-INCOMPLETENESS ~4/15 -- the engine finds a VALID goal-cause the sparse TellMeWhy gold did not annotate ("Ike loved to fish" for why-he-fished; "Brian wanted to play golf" for why-he-asked-his-brother; "Derek wanted to cook" for why-he-made-cereal) -> scored wrong but arguably RIGHT, so the full-population tie UNDERSTATES the engine's true quality (a MEASUREMENT ceiling, TellMeWhy's crowdsourced helpful-sentences are non-exhaustive, Lal 2021); (2) SHALLOW LEXICAL-OVERLAP OVER-FIRE ~4/15 -- the hub fires on a shared word without a verified causal DIRECTION ("give up on a TREE" <- "buy a christmas TREE"; "turn the light off" <- "get ready for WORK") -> a hub PRECISION problem; (3) DEGENERATE items ~2/15 -- a non-causal q ("why was it a cold winter day") or base itself picking an EFFECT. So the corrected levers are NOT coref: they are (a) a COMPLETE / multi-reference gold (or human eval) so valid goal-answers are not penalized, and (b) a directional CAUSE->EFFECT gate in the hub (it currently scores proximity, not verified direction) to kill the shallow-overlap over-fires -- plus the goal engine's inherent 36%-of-items scope. This is the honest, item-level understanding; the earlier coref-centric conclusion was a surface-heuristic artifact and is WITHDRAWN.

## Performance vs the brain, and WHERE we lose signal up the chain (checklist item 6; detail in BRAIN_FOUNDATIONAL_ANALYSIS_2026-09-08.md)
Oracle ladder (parent, n=256): base 0.277 -> +0.016 surface means-end -> +0.148 PERFECT goal DEPTH (0.441) -> +0.332 all-type engines (0.773) -> +0.227 gold (1.000). This work closes the COVERAGE half of the +0.148 depth loss for goals (8.7->88%, win doubled). Itemized brain-vs-us mechanism-diff, ranked by where we lose signal: (1) EXTRACTION greedy hard-commit vs incremental predictive (NOT the loss here, recall 0.98). (2) COREF surface vs resolved discourse referents (E3 NEEDS_ADAPTER) -- MEASURED as the dominant full-population loss (67% of false-flips, recovers 61% on gating). (3) MEANING CHANNEL offline ATL hub (faithful in kind; online-learning is the deviation). (4) ROLLOUT hub implicit closure (faithful for 2-endpoint reachability; explicit chain cone-collapses). (5) DECISION additive (faithful) but missing signed-pairwise ECHO coherence + DDM commit. (6) ARCHITECTURE feed-forward late-fused vs ONE recurrent generative loop over a shared event model. (7) ENGINES 1 of 4 strong; the brain runs goal+physics+mental+affect competing.

## Full-stack upstream + no-regress (checklist FULL-STACK-UPSTREAM directive)
- UPSTREAM prototyped AND traced: the plan/script knowledge (ATL hub over ATOMIC) built + proved (coverage 88%, AUC 0.68). The router, affect engine, temporal/script engine were prototyped and LOCATED as negatives (documented, not silent). Decisively, I traced the full-population residual to the COREF upstream with numbers (67% different-agent false-flips; surface-gate recovers 61%) -- confirming the owner's principle that a brain-foundational component (the means-end engine) needs a brain-foundational upstream (resolved coref), which is currently surface/unwired (E3).
- NO live reasoner regresses: the means-end is a SEPARATE additive channel, inert without a goal marker (byte-identical additive-off == base_add), modifies no live reasoner. The parent's landed goal_hierarchy_graph means-end bridge is untouched.
- CONSUMERS to REVISIT once resolved coref + the multi-engine competition land: causal edge-correctness c, coherence full-population unmarked-causal, who-did-what two-valid patient, implicit-event temporal order.

## What I did NOT establish (withdraw-first if wrong)
- I would withdraw first any claim of a FULL-POPULATION win: there is none; the full population ties base (located negative). The clean CI-sep positive (twin loses) is on the GOAL subset (n=92).
- The full crossing is NOT achieved and is NOT guaranteed by coref alone: I MEASURED that a recency-Centering resolver gives FULL +0.0156 (ties), and the decomposition shows a gender/number-aware coref would reach only ~0.33 (borderline) because 33% of the OTHER damage (A = same-referent over-fire) is NOT coref-addressable -- it needs the multi-engine competition. The ~0.33 gender-aware-coref estimate is itself unmeasured (I built recency, not gender-aware, coref); it is bounded above by the A residual. I would withdraw any claim that coref alone crosses.
- The explicit multi-step CHAIN (chain2), the router, affect, temporal/script, and the multiplicative gate are LOCATED NEGATIVES, not fixes.

## KEY REALIZATIONS (the enabling moves)
1. Plan/script knowledge as a LOW-RANK ATL HUB (not a KB traversal) crosses the depth/coverage wall: implicit transitive closure reaches the DEEP multi-step slice (8.7->88%) and GENERALIZES (AUC 0.68) -- the generate-not-retrieve property, measured.
2. Coverage is not enough -- SELECTIVITY is, via inverse-planning DIAGNOSTICITY (subtract the goal's base rate). It converts 88% coverage into a CI-separated win that doubles the parent's.
3. The wall MOVED to the CROSS-TYPE COMPETITION (the cause-type composition was the single most informative measurement: the goal engine is one voice; 39% of causes are associative).
4. READING THE ACTUAL ITEMS BEATS THE AGGREGATE DECOMPOSITION -- and it CORRECTED me. The surface-agent decomposition said "67% coref-addressable"; binding at three resolution levels (surface / recency / landed gender-aware) then showed gender-aware does NOT beat recency, which was the tell. Reading all 15 flips revealed WHY: the "different-agent" flips are the SAME PERSON (recency merges them correctly), so coref is NOT the lever. The real signal loss is (i) GOLD-INCOMPLETENESS (the engine finds valid goal-causes the sparse gold did not annotate -- the full-pop tie understates true quality) and (ii) SHALLOW LEXICAL-OVERLAP over-fire in the hub (it scores proximity, not verified causal direction). The enabling move was DUMPING AND READING THE ITEMS rather than trusting a surface-agent proxy -- an audit checks the shape you thought to check; using/reading the data checks all of them. The owner's push ("we don't understand this fully") was correct; the coref-centric conclusion was a surface-heuristic artifact, now withdrawn.

## AUDIT UPDATE (for notes/BRAIN_FOUNDATIONAL_AUDIT.md sec.2b -- the generative world-model / means-end entry)
- REVISE the parent's anticipated wall AGAIN: the multi-step means-end DEPTH/COVERAGE wall is CROSSED by a low-rank ATL-hub embedding (ATOMIC PPMI+SVD): 88% coverage, generalizes (AUC 0.68), goal-subset win DOUBLED (+0.239 CI-sep, twin loses). The lever is the DISTRIBUTED HUB (implicit transitive closure), not explicit K-hop traversal (cone-collapses).
- Record the RELOCATED wall AND the item-level CORRECTION: the full-population tie is NOT a coref loss (the surface-agent decomposition suggested that, but reading the 15 flip items showed the "different-agent" flips are SAME-person -- recency merges them correctly -- and a landed gender/number-aware resolver did NOT beat recency). The true non-goal signal loss is (1) GOLD-INCOMPLETENESS -- the engine finds valid goal-causes TellMeWhy's sparse gold did not annotate (the tie understates true quality); (2) SHALLOW LEXICAL-OVERLAP over-fire in the ATOMIC hub -- it scores proximity, not verified causal DIRECTION; (3) the goal engine's 36%-of-items SCOPE (the non-goal 64%, esp. the 39% associative, are base/other-engine territory). The fixes are a directional cause->effect gate on the hub + a complete/multi-reference eval + the multi-engine competition -- NOT coref.
- PINNED addition: the ATL semantic hub as a low-rank distributed means-end code (Lambon-Ralph) is a brain-foundational realization of plan/script knowledge; inverse-planning diagnosticity (Baker-Saxe-Tenenbaum) is the selective goal-state test; participant binding requires resolved discourse referents (Heim/Kamp), not surface agents.

## Proposed hdlab landing (strategy lands; Q111 -- I do not write hdlab/)
1. The GOAL slice is a clean, brain-foundational win (coverage 88%, +0.239 CI-sep, twin loses, no-regress). Promote the `MeansEnd` ATL-hub means-end (diagnostic-normalized) as a goal means-end cue, DEFAULT-OFF / confidence-gated, additive, inert without a goal marker. Because it DAMAGES the non-goal population unless (a) competed by the other engines and (b) bound to RESOLVED participants, it must NOT be wired as an unconditional full-population booster -- wire it INSIDE the multi-engine competition over resolved coref.
2. Do NOT land it as a full-population lift (there is none). The value banked now: the goal-engine coverage crack + doubled subset win; the precise relocation of the wall to the multi-engine competition; and the UPSTREAM localization to resolved coref (E3) with the recovery number -- directly connecting to the strategy session's current coref update.
3. Fold the AUDIT UPDATE.

## TLDR (plain English)
A good reader explains an action by imagining the multi-step plan behind it. Our old engine imagined ONE step and stalled. I gave it the missing knowledge in the form the brain uses -- a compact "meaning map" of what-actions-serve-what-goals, built offline (no external AI at answer time) -- and it GENERALIZES, placing "go to the store" near "have milk" even for pairs it never saw. This worked: on stories whose cause is a goal, the engine now finds the right explanation about nine times in ten where before it found it once in ten, and its accuracy DOUBLED our previous best, with a scrambled version failing. But across ALL stories it still ties the plain baseline, and I found exactly why -- and it is NOT the brief's guess. Explaining causes is a panel of specialists (goals, physical events, feelings) plus a big "just generally related" bucket (about 4 in 10) that no specialist handles. I made the GOAL specialist excellent; but on stories whose real cause is NOT a goal, it grabs any "someone wanted something" sentence and hurts. Digging into WHY, I found the deepest cause: two out of three of those mistakes are grabbing a goal that belongs to a DIFFERENT character than the one we are asking about -- because our reader cannot yet reliably tell that "she" and "Mary" are the same person (coref). A crude same-name check fixes most of the damage but also blunts the good cases, so the real fix is proper coreference -- which the other work stream is upgrading right now. So the honest result: the goal specialist is excellent, and the remaining gap is a named upstream part (knowing who is who) that is already being fixed.

## QUESTIONS
One labelling call for the owner (the science is identical either way):
- I filed PARTIAL because the HEADLINE bar (a FULL-POPULATION CI-separated win) is NOT met. The bar's explicit provision "a rigorous LOCATED NEGATIVE is a FULL PASS if it names the exact ceiling WITH a number" IS satisfied (over-satisfied: I refuted the brief's wall guess, crossed the coverage/depth wall, relocated the true wall to the multi-engine competition, AND traced the dominant residual to a named, measured upstream -- coref), plus a real capability advance (goal-subset win doubled, coverage 8.7->88%). By the parent's precedent this could be SOLVED. I lean PARTIAL to deflate (no full-pop win); your call via owner_verdict.
- FYI: this problem folder (PROBLEM.md/SOLVED.md/analysis note) was wiped once by a concurrent git operation (it was untracked; a worktree/clean step during the strategy session's coref commits removed it). I restored all three from context. If it vanishes again, it is that concurrent-session git hazard, not a solver error.

## NEXT STEPS (priority-ordered)
- P1 -- A DIRECTIONAL CAUSE->EFFECT GATE ON THE HUB (the item-level fix for the shallow over-fires). Reading the flips showed the hub fires on a shared WORD without verified causal direction ("give up on a TREE" <- "buy a christmas TREE"). Gate the means-end so it fires only when the candidate's goal/action canonically PRODUCES q's action (a directed check -- e.g., the achievement-DIRECTION the parent's rs_fire has, or the ATOMIC xEffect direction), not on mere hub proximity. This kills the ~4/15 shallow over-fires without touching the valid-goal fires. In scope; a proposed hub refinement.
- P1.5 -- A COMPLETE / MULTI-REFERENCE EVAL for the goal slice. ~4/15 "wrong" flips are VALID goal-causes the sparse TellMeWhy gold did not annotate -- so the full-pop tie UNDERSTATES the engine. Re-score against a multi-reference or human-adjudicated cause set (or GLUCOSE's richer multi-dimension causes) so valid goals are not penalized. This is a MEASUREMENT fix; it likely reveals the goal engine is better than the tie shows.
- P2 -- the MULTI-ENGINE COMPETITION for the goal engine's inherent 36%-of-items SCOPE (it cannot help the non-goal 64%): build the PHYSICS (force_dynamics_typer + world_state_register directed) and MENTAL (_tom_chain inverse) engines as competing cues + the signed-pairwise ECHO coherence decision. Ceiling per engine modest (physical 11%, mental 8%), bounded above by the associative 39% (base is the ceiling there) -- realistic full-pop target ~0.77.
- DEMOTED -- resolved coref (E3): MEASURED not the lever on this protagonist-dominant gold (gender-aware did not beat recency; the "different-agent" flips are same-person). It would matter on genuinely multi-character prose, not TellMeWhy. Do not prioritize it for THIS problem.
- P2 -- optimize the ATL hub (raise edge-correctness above AUC 0.68): sweep SVD dimensionality, the ATOMIC relation set, WordNet backoff for OOV verbs.
- DO NOT re-file (tested + closed this round): the explicit K-hop CHAIN over sparse edges (cone-collapse); the teleological-stance ROUTER keyed on the explanandum (does not separate); the AFFECT engine alone (~10% domain); the multiplicative content-gate (still -0.098); the TEMPORAL/SCRIPT order store (hdlab.temporal_script_schema) as a cause-SELECTION cue (located negative: coverage 0.84 but the context-free script prior does not discriminate the cause; worsens OTHER -- validated for its HOME task, implicit-event ORDERING, not cause-selection); the SURFACE agent-gate as the coref fix (halves the goal win -- needs RESOLVED coref); a bigger single-step means-end lexicon (the parent's closed negative). Do NOT use an external LLM at inference (THE invariant).
