---
owner_verdict: DONE
---

SOLVER SUBMISSION — wire_the_crosstype_definite_name_bridge_into_the_live_reader_and_measure_the_experiencer_lift

STATUS: PARTIAL (a rigorous located negative = a full pass under the bar). Glass-box, no LLM at inference.
NO hdlab/ written (Q111) — measured in experiments/ + verification/, diff proposed for strategy to land.

RESULT (all on the HONEST de-leaked floor; gold-coref inheritance ruled a LEAK by strategy 2026-09-08):
 • The crosstype bridge lifts the affect/goal EXPERIENCER bind through the REAL make_canonicalizer:
   +0.0528 CI[+0.0316,+0.0782] CI-sep at the deployable conf_thr=-3.0 (C1 up-or-flat, C2 up, info-free twin loses).
 • END-TO-END through the whole reader.read() on all 275 modern GUM docs: floor 0.1381 -> 0.1775
   = +0.0394 CI[+0.0202,+0.0646] CI-sep, twin loses. ("Equivalent-path" caveat closed.)
 • FULL 100% brain-foundational chain (cue-based ACT-R clustering -> two-route CLS bridge -> identity bind):
   +0.0874 CI[+0.0325,+0.1484] CI-sep over the honest floor; composes without regression; cue-clustering
   structure-dependent +0.0601 CI-sep. Generalizes across ~10/16 GUM genres.

DECISIVE FINDING: the live _apply_commonnoun_gate INHERITS gold coref ids (88.9% kept on native LitBank) —
it was faking cross-type filing with the answer key (honest 0.15 -> gold-inherited 0.80). Non-brain-foundational
(3-lane research drill, P~0.90: no discourse theory posits given coref). The bridge is the brain-faithful
replacement (Garrod-Sanford role/individual bonding + hippocampal one-shot binding).

THE ONE REAL UPGRADE (both negatives drilled): grow the entity-type KB — the KB route converts reach->win at
89%; the residual is ~entirely COVERAGE (84% no-KB-fact), not mechanism. Canonicalizer + disambiguation both
refuted as bottlenecks. Owned by the world-knowledge posted problem.

PROPOSED hdlab DIFF (strategy lands): (1) swap situation_predict -> cue-based clustering; (2) opt-in
crosstype_bridge flag (default off, byte-identical, reader's own parse, conf_thr=-3.0); (3) DE-LEAK common-noun
gold inheritance using NEGATIVE-INTEGER ids (NOT "CN:" strings, else rc>=0 consumers crash). Part 3 is the
load-bearing fix. Coordinate with the sibling name-bridge wire — land the Doc adapter + de-leak ONCE.

REVERIFY: .venv/Scripts/python.exe verification/test_crosstype_{live_wire (6/6), doall_upgrades (3/3),
fullread_endtoend (3/3), full_chain (4/4)}.py   [all green this session]

PLAIN ENGLISH: our tool for working out "the doctor = Elizabeth" genuinely helps the reader (~5 more characters
in 100 matched right, up to 8 if pushed, nothing else worse, a scrambled version can't fake it). But the reader
was secretly peeking at the human answer key — strip the peek and the tool is what does the real work. Biggest
next win: give the reader a "who is this person" knowledge base.
