---
owner_verdict: DONE
---

SUBMISSION — generate_dont_retrieve_causal_edges_for_unmarked_narrative_causation
Status: PARTIAL (rigorous located negative + validated brain-foundational foundation = full pass per the brief's own bar)
Reverify: .venv/Scripts/python.exe verification/test_causal_antecedent_reader.py  → 67/67 PASS
Deliverable: notes/problems/generate_dont_retrieve_causal_edges_for_unmarked_narrative_causation/SOLVED.md

WHAT THIS DELIVERS
1. A REFUTATION of the benchmark-evaluation frame: every external causal gold (MAVEN-ERE / TellMeWhy /
   GLUCOSE) is a POSITION-ARTIFACT TRAP — a trivial position floor beats every brain-faithful mechanism,
   and the "gold causes" are proven orthogonal to predictive coherence. The brain-foundational measure is
   INTRINSIC surprise-reduction (Kuperberg N400 / Gerstenberg counterfactual necessity), which no external
   gold can confound.
2. A 100%-brain-foundational, glass-box causal reader (NO external LLM, NO training, NO gold at inference):
   perceptron tagger → ONLINE predictive-coding world-model (Rao-Ballard + Rescorla-Wagner) →
   counterfactual-NECESSITY causal reader → intrinsic surprisal validation. Finds genuine causes (removal
   raises the effect's surprisal CI-sep over random) that ESCAPE the position confound.
3. STAGE 1 — the participant-bound SITUATION-MODEL EVENT (agent+patient+affect+goal = situation_reader's
   EventRecord dimensions), each via a landed organ (graded_role_assigner, coreference_resolver,
   affect_lexicon Warriner valence, goal_register). On the sparsity-PROOF intrinsic read, knowing WHO acts
   sharpens prediction vs a CAPACITY-MATCHED shuffle; agent + patient + dense valence + goal-state each add
   real capacity-matched causal info; referential coherence is measured (Trabasso).

RIGOR / HONESTY
- Mandatory position floor + shuffle + capacity-matched controls throughout. One flashy result (a "47x
  composition lift") FAILED its shuffle control and was RETRACTED; composition is a graph-density property,
  NOT agent-specific — the agent value is in the READ, not the chain (consistent with the substrate's prior
  CLUTRR/CERT-585 multihop finding that "composition over good codes is free").
- Reconciled with the landed KGStore n-hop reasoner (CERT-585). Phase-diagram lever noted: sparse↔dense is
  free — densifying affect turned a sparse-negation NULL into a real signal.

FOR THE OWNER
- This is an EXCELLENT FOUNDATION + investigation, NOT a complete delivered capability. Requested: a VERDICT.
- To COMPLETE: (a) land the hdlab wire (Q111, strategy) — the necessity read as an additive EventRecord pass
  (landed != live); (b) re-validate the situation-model read on naturalistic NARRATIVE (simplewiki was used
  for volume); (c) optionally tie the necessity measure to human reading-time / N400.
- Constraints honored: experiments/ + verification/ + notes/problems/ only; hdlab/ untouched (git clean);
  no external LLM/gold/training at any step.
