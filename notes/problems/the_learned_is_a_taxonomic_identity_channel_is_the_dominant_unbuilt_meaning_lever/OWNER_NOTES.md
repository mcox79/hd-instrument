---
owner_verdict: DONE
---

SOLVER SUBMISSION — the_learned_is_a_taxonomic_identity_channel_is_the_dominant_unbuilt_meaning_lever

STATUS: REFUTED (the brief's premise), WITH A VALIDATED CONSTRUCTIVE PIVOT. A rigorous located negative — the
bar's blessed full-pass clause — plus an evidence-backed forward direction. Glass-box, NO WordNet/parser/LLM at
inference, NO hdlab/ writes (Q111 — results + proposed direction; strategy lands). Threads capped at 2. Witness:
.venv/Scripts/python.exe verification/test_learned_isa_taxonomic_channel.py (36/36). READ FIRST in the problem
folder: SOLVED.md (esp. "WHAT THIS NEEDS TO IMPROVE IN CAPABILITY" + "COMPONENT / BF STATUS" + KEY REALIZATIONS).

THE HEADLINE (with numbers, on the live sense-assignment decision, SimLex/SimVerb anchor pool):
- The brief's "+0.488 dominant lever" is a PRE-SEQ ARTIFACT. It was measured vs grounded-distinctive ALONE
  (0.188). Against the CURRENT strongest floor (grounded-distinctive + grown-SEQ, the parent's landed lever),
  BASE = 0.505 (n=94 SimLex-high) / 0.391 (pooled); SEQ ALONE already 0.455. Even the CIRCULAR WordNet taxonomic
  ceiling adds only +0.159 (n=94, NOT CI-sep) / +0.057 (pooled, NOT CI-sep), and oracle{grounded+SEQ}=0.744
  EXCEEDS the WordNet ceiling 0.650 — the co-occurrence channel already banks the taxonomic identity signal
  (Harris). So the taxonomic channel is NOT the dominant unbuilt lever.
- I built the strongest learned is-a mechanism (parser-free Hearst edges -> Roller-Kiela-Nickel PPMI+SVD
  densification -> Collins-Quillian genus-differentia co-hyponym-penalty), SEQ-backfilled to 100% coverage on the
  favorable n=94 population: CAPTURE_vs_BASE = +0.000; additive fusion HURTS; knowledge-shuffled twin not beaten.

PIVOT INVESTIGATION (owner: "do it all, research the walls, how do we overcome this in the substrate?"): SIX
brain-foundational mechanisms all null/neg over the strong base — is-a (+0.000), recurrent competition/divisive-
norm (-0.003), CSLS (-0.018), mutual-inclusion (-0.029), symmetric-coordination (-0.051), referential-equivalence
(diffuse hurts, precision-gated fires 0). A PUBLISHED THEOREM explains it (Scheible & Schulte im Walde: first-order
co-occurrence provably conflates synonym/antonym/co-hyponym) — they are all first-order co-occurrence. The
separator (distinctive-feature differentia) RAISES the oracle (+0.044 CI-sep) but is unroutable + coverage-starved
(Binder-2016 experiential norms: 65 rich dims but 534 words -> ~0 eval pairs).

RESEARCH + PROBES nailed the mechanism and corrected me twice: (1) cosine is NOT blind — it separates syn/cohyp at
AUC 0.677 pairwise; the wall is the MULTI-CANDIDATE ranking under frequency-HUBS (46% of errors), taxonomic
siblings (63% of in-reach), antonyms (18%). (2) I had used coordination BACKWARDS (fused "X and Y" as similarity;
it's a co-hyponym/distinctness detector) — but tested correctly it's coverage-starved too.

THE VALIDATED PIVOT (the constructive result): the brain resolves meaning IN CONTEXT (token-level, N400/predictive),
never as a context-free type lookup. Probe 5 (WiC): TYPE-level AUC = 0.485 (chance BY CONSTRUCTION); context-
conditioning lifts it MONOTONICALLY to 0.582; and the substrate's already-landed WiC cells reach ~0.66-0.68. So the
type-level synonym-ranking this brief posed is the WRONG frame for a text reader; the meaning read should be routed
TOKEN-LEVEL / context-gated (a distinct problem — the WiC cells' territory — so a hand-off, not a within-brief solve).

WHAT THIS NEEDS TO IMPROVE (capability, ranked; SOLVED.md §): (1) route the meaning decision in-context [validated,
#1 lever]; (2) acquire fine PERCEPTUAL/experiential features at full-vocab coverage (the differentia the brain
grounds in ATL spokes; Binder is right-KIND but sparse) — a foundation-asset problem; (3) brain-exact per-query
reliability for the read-out third (parent located-negative; confident-wrong caps it); (4) frequency-hub
suppression, in the in-context frame. NOT worth redoing: any type-level is-a/similarity/contrast channel.

COMPONENT / BF STATUS (SOLVED.md table): grounded spoke PINNED (but coarse -> perceptual-differentia limit);
grown-SEQ PINNED-computation (the dominant signal); fusion BF_SPIRIT; canonicalize BF_SPIRIT but the KEY deviation
is context-FREE type-level vs the brain's token-level; learned-is-a/differentia/equivalence/contrast/competition
all BF_SPIRIT computations but null/coverage-starved here; WordNet conceptual channel NOT_BF at inference (analysis-
only); in-context read BF_SPIRIT and the validated path. Every inference channel: no WordNet/parser/LLM.

FILES: 10 experiment cells (directed_isa_capture[_simlex_bf], isa_headroom_diag, residual_decomposition,
relation_rerank, new_channels, equivalence_channel, contrast_probe, confusion_taxonomy, incontext_probe) +
landed isa_knowledge; witness test_learned_isa_taxonomic_channel.py (36/36); SOLVED.md. NO hdlab/ modified.
HONEST BOUNDS: the in-context probe is a directional grounded-only demo (AUC 0.582, context-carried); the strong
in-context evidence is the landed WiC cells. Nothing beats grounded+SEQ on the type-level decision — that is the
text-only modality limit, rigorously characterized. QUESTIONS: none blocking. Awaiting owner_verdict: DONE.
