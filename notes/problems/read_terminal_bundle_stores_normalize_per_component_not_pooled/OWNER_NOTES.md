---
owner_verdict: DONE
---

PARTIAL — read-terminal norm is a targeted (not substrate-wide) fix; the real gap is the register WRITE path: read_terminal_bundle_stores_normalize_per_component_not_pooled (SOLVER, opus 4.8)

Core result. Measured every enumerated read-terminal bundling.bundle caller on its own validated task. The brief's "switch every read-terminal caller to divnorm" is refuted; the real discriminator is readout-class + load, not read-terminal-vs-rebound. divnorm ≥ per-component only for a direction-sensitive read under overload, largest for the gain-matched serial decode — and the only two callers with both (register + multibank) were already switched by the parent. No other enumerated caller should switch, each MEASURED: typer sup_map HURTS at low load; goal_achievement can't overload (6 attrs); cosine consumers null. Two factual corrections: the map's "no caller re-binds" is false (typer sub-bundle is a re-bound unbind key); "per-component is wrong for the whole set" is false.

Drills (owner-pushed; where the real understanding is). (1) The typer "gain-matched win" (+0.014) was rejected — literature + a 4-arm test showed it's non-brain-faithful per-role equalization, load-fragile; the brain-faithful shared-pool norm is argmax-inert. (2) The PPC magnitude-as-reliability alternative to the LOO weight is refuted (−0.19 CI-sep) — the LOO weight is a brain-defensible learned precision weight. (3) The bipolar sign()→graded fix transfers to real callers but is MODEST (+0.02–0.045, not the synthetic +0.17 — measuring the real caller corrected the overclaim). (4) The CA3 attractor's softmax+L2 is already divisive-norm at retrieval.

The biggest gap found — the register WRITE/encode path (NEW, MEASURED). The whole read-terminal family was mostly null because capacity is set at WRITE, not READ. The flat running-sum write has a hard capacity wall — past it recent-event recovery collapses (0.13 @256 events) and read-time divnorm CANNOT move it (raw==divnorm every load; once summed to saturation the info is destroyed). The brain-faithful fix — an asymmetric continuous leaky/recency write — keeps recent context recoverable at any load (1.0 @256), and on our organ reproduces the measured primate-PFC recency gradient (graded, not discrete slots; W9/W10/W11). Research-grounded: asymmetric recency = MEASURED/PINNED-WEAK (Warden-Miller 2007 + Konecky 2017 primate single-unit — stronger than the read-side divnorm; corrected a citation error: Buschman 2011 is a load effect, not recency). Architecture corrected: symmetric divisive-at-write doesn't extend capacity; CLS/recency-chunked consolidation is the WRONG 2nd-store analogy → use the existing content-addressed HDFactStore under a content/salience gate (reuse the MDL/schema gate), not a new mechanism.

Proposed hdlab change: NONE for the read-terminal callers (register+multibank already divnorm; switching anything else is measured neutral-to-harmful). The write-path fix is a new build problem, now fully scoped.

Deliverables. Witness verification/test_read_terminal_divnorm.py (18/18, scaffold-free on real organs/tasks). Cells: experiments/exp_read_terminal_divnorm_{readout_principle,cosine_family,typer,sign_family,attractor,sign_real_callers,write_path}_v1.py. SOLVED.md (per-caller table, 5 drills, adjacent-component audit, AUDIT UPDATE — three gating conditions replace "read-terminal→divnorm"). FOLLOWON_PROBLEMS_FOR_STRATEGY.md — file-ready briefs for the next problems (ranked; #1 = the register write path, the biggest gap, fix form confirmed + architecture corrected; also the content/salience-gated register→HDFactStore hand-off; sign()→graded at encode_sentence; Kamiński/Kyzar dataset reanalysis). Three research notes persisted. Reverify: .venv/Scripts/python.exe verification/test_read_terminal_divnorm.py. Ledger clean.
