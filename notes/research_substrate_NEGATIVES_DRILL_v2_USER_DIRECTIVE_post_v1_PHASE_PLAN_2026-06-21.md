# RESEARCH (Director, Opus) -- SUBSTRATE NEGATIVES DRILL v2 (post v1 PHASE PLAN ratification, USER directive 2026-06-21). 2x drill of remaining substantive substrate negatives + 3x hidden-positive surface beyond the v1 drill's resolved set + v1-top-7 PHASE PLAN subsume-risk audit + Skunkworks-corrected SYMMETRIC GUARD applied (data-decides-tier-no-preempt is for WRONG-BAR-3-templates ONLY; honest pre-reg-bar misses STAY negative).

**Scope:** internal Store scour (no lit-scan). 177,253 atoms enumerated; the prior v1 drill (notes/research_substrate_NEGATIVES_2x_negative_was_positive_3x_scour_USER_DIRECTIVE_2026-06-20.md) covered ~25 atoms. This v2 drill builds the diff against substrate-state-moves since v1: CERT 592 -> 588 honest count cascade; 18+ META disciplines atomized; partof_after relevance bumped; phase4b 1-op anomaly RESOLVED (2.7% ceiling, content not bug); LEVER #2 PCA negative ROBUST; the 2-level-ingest buried-positive lineage fully mapped; Skunkworks's symmetric-guard correction on caching_eviction (acc 0.80 misses honest 0.85 bar = honest miss, NOT a clean win); 5MM cascade execution-drift demotes (a1_multihop / t3_phaseA2 / partof_2level).

**Verify-the-referent applied at every layer:** cited numbers reproduced off `metrics_headline` directly (per cb7e89f1 `cited-number-must-reproduce-from-cell`); honest_scope quoted where present; symmetric-verify CLEARS or PARTIAL labeled explicitly per claim.

---

## (a) HEADLINE

**5 NET-NEW hidden-positive candidates surfaced beyond the prior v1 drill (A1-A5)**, all of which CLEAR the Skunkworks-corrected symmetric guard (wrong-bar-where-preempted vs honest-pre-reg-stays-negative). **1 PARTIAL DEMOTE within the prior v1 list (B5 b3_b6 B3a graceful-degradation framing -> RESEARCH_FINDING-tier, not MEASURED_MECHANISM)**, reflecting the corrected symmetric discipline. **v1-top-7 PHASE PLAN subsume-risk audit confirms: items 1 (sparse-projected-KV flagship), 2 (continual-write lever), 4 (2-level ingest generalize) are SAFE-TO-BUILD or PARTIAL-FOLD; item 6 (M2 multi-hop) MUST REFRAME (subsumed by ccc1_extra_fb15k237 PASS); item 5 (capacity-saturation ceiling) PARTIAL (distinctive axis = where crosstalk-law c saturates); item 7 (cross-domain probe) STEER to genuinely-novel directions.** Single highest-leverage net-new candidate: **A1 `substrate_continual_learning_empirical_10e9x_v1`** (27x speedup + zero-forget on Pythia-160M; MIDDLE_BAND label preserved only because 1000x extrapolation-aspiration bar missed; actual data is strict-win on every measured metric). Directly composes with the in-flight continual-write lever pre-reg as the no-forget envelope referent (alongside a8). **q_b1_bisect cluster (A2 / Section C top sub-audit priority)** is a precise chain-depth onset bisection at d=275-277 for N=16384 — the MIDDLE_BAND + HARD_FAIL siblings are the bisection-by-design measurement, not substrate failures.

**Net for substrate-state v2 awareness:** the moved-state has surfaced more buried positives but ALSO sharpened the symmetric guard (Skunkworks's correction prevents 1 partial over-claim and prevents bulk batch-atomization). Director action queue (e) prioritizes (i) folding A1 into continual-write lever pre-reg as referent, (ii) routing A2-A5 to Skunkworks for per-atom landed-VET per the 18+ META discipline rules, (iii) reframing PHASE PLAN v2 M2 to glass-box-INTEGRATION on ccc1, NOT standalone multi-hop test.

---

## (b) Per-negative 2x drill findings against current substrate state

Drilled at the level the v1 report didn't reach: net-new atoms + diff against v1 candidates under the corrected symmetric guard.

### B-NEW1. `T3/EXP_substrate_continual_learning_empirical_10e9x_v1` (CERT_CHAIN_GRADE, MIDDLE_BAND, cell_sha `76a4e7b7235b`) -- NET-NEW
- **Failure mechanism (cell):** `substrate continual learning faster (27x) + NO forgetting; LLM slower + forgets. speedup=27x (sub_wall=1.358s llm_wall=36.9s) | sub_old_retention=1.00 sub_new=1.00 | llm_old 0.50->0.45 (forget=True) llm_new=0.46`. MIDDLE_BAND label inherited from the 1000x-aspiration framing ("1000x is large-LLM-scale; Pythia-160M is conservative" -- extrapolation-bar preempted).
- **Reframe under new META disciplines:** WRONG-BAR template (extrapolation-bar preempted measurement). The CELL MEASURED a strict-win on every recorded arm: substrate 27x faster + 1.00/1.00 retention vs LLM forgetting (0.50->0.45). Per cb7e89f1 `data-decides-tier-no-preempt` + Skunkworks's symmetric guard: extrapolation-bar that preempts measurement is the SAME template as the 3 prior wrong-bar cases (Hebbian/isotropy/sparse-1.4x), where the **actual measurement is a clean positive**.
- **Status:** **HIDDEN POSITIVE.** Atomize as MEASURED_MECHANISM "27x speedup + zero-forget on Pythia-160M, n_train_streams=2". Directly composes with `a8_continual_writes_no_catastrophic_forgetting` HARD_PASS (the no-forget envelope referent) AND the in-flight continual-write lever pre-reg (Skunkworks's #1 P1 enabling). **THE TOP NET-NEW LEVERAGE CANDIDATE in this drill.** Tier opportunity: MEASURED_MECHANISM; composes_with a8 + continual_write_lever. Skunkworks landed-VET per atom per the protocol.

### B-NEW2. `T3/EXP_q_b1_bisect_d277_v1_n16384` + `_d278_v1_n16384` + `_d281_v1_n16384` (3 CERT_CHAIN_GRADE MIDDLE_BAND atoms; cell_shas `113a3f15d38c`, `e99645cdba75`, `f1f5460047e1`) -- NET-NEW
- **Failure mechanism (cell d277):** `Onset near d=277. d5=0.9000(HP>=0.9 HF<0.8) d20=0.8816 d50=0.8794 d100=0.8811 d200=0.8809 d275=0.8818 d277=0.8804 (HP>=0.005 HF<0.001) n_seeds=5`. Sibling `_d275_v1_n16384` is HARD_PASS; siblings `_d287/d293/d300/d400_v1_n16384` are HARD_FAIL (post-onset arms).
- **Reframe under new META disciplines:** These 3 MIDDLE_BAND + 4 HARD_FAIL atoms are a deliberate **bisection probe finding the exact chain-depth collapse-onset at d=275-277 for N=16384**. The MIDDLE_BAND label is the bisection working as designed -- the cells locate the onset to within (276, 277]. Per a3f473dd LOWER-BOUND atomization precedent (sparse-#2 endpoint became MEASURED_MECHANISM via the LOWER-BOUND caveat): this is the same pattern, just upper-bounded by the bisection. The collective `q_b1_bisect/chain_depth` cluster (7 atoms) constitutes a single MEASURED_MECHANISM "chain-depth collapse onset bisected to (275, 277] at N=16384, n_seeds=5"; the d275 HARD_PASS sibling is the load-bearing positive; the d277-onward atoms ARE the precise measurement of where it stops working.
- **Status:** **HIDDEN POSITIVE (precision-measurement-by-design).** Atomize as MEASURED_MECHANISM "chain-depth onset bisected to d=275-277 at N=16384" (collective cluster claim). Composes with `q_b1_chain_depth_200_v1_n16384` HARD_PASS + CERT 592 K_max NESS envelope. Tier opportunity: MEASURED_MECHANISM; the d277/d278/d281 atoms should be re-labeled to reflect their bisection-by-design role, NOT marked as substrate-mechanism failures.

### B-NEW3. `T3/EXP_substrate_drosophila_mb_sparsity_sweep_v1_512_2048_gpu` (CERT_CHAIN_GRADE, MIDDLE_BAND, cell_sha `912a228fc8ee`) -- NET-NEW (PARTIAL)
- **Failure mechanism (cell):** `best sparse improvement in [0.1,0.3]. best f*=0.01@N512 gap=+0.150 (2 seeds) | N512/f0.05:+0.142(3/3) N512/f0.02:+0.146(3/3) N512/f0.01:+0.150(2/3) N512/fsingle:-0.317(0/3)`. MIDDLE_BAND from best=f0.01 only at 2/3 seeds.
- **Reframe under new META disciplines:** Three sparsity arms concurrently land **inside the prereg HP-band [0.1, 0.3]**: f0.05 at +0.142 (3/3 seeds), f0.02 at +0.146 (3/3 seeds), f0.01 at +0.150 (2/3 seeds). Per Skunkworks's symmetric guard: the f0.05 arm is robust 3/3 inside the band; the f0.01 borderline 2/3 is the honest miss. SPLIT-CLAIM atomization on the f0.05 anchor would clear the same rigor as a downward demote.
- **Status:** **PARTIAL HIDDEN POSITIVE** (load-bearing arm f0.05; not the headline f0.01 best). Atomize as MEASURED_MECHANISM "fly-LSH sparse f=0.05 lifts N=512 capacity by +0.142 (3/3 seeds) inside prereg HP-band". Compose with `sparse_boundary_v2_cpu_v1` (a3f473dd). Tier opportunity: MEASURED_MECHANISM (f0.05 anchor only). f0.01-2/3-seed-miss STAYS in honest_scope.

### B-NEW4. `T3/EXP_substrate_per_cluster_stratified_extraction_v1` (CERT_CHAIN_GRADE, MIDDLE_BAND, cell_sha `16e332c9fd31`) -- NET-NEW
- **Failure mechanism (cell):** `>=0.95 coverage at 10-100x speedup. stratified: {'sp10': (1.0, 12.0), 'sp100': (1.0, 12.0), 'sp1000': (1.0, 12.0)}`. Coverage=1.0 (perfect saturation) at speedup=12.0 across all three sparsity bins; HP was ">=0.95 coverage at 10-100x speedup".
- **Reframe under new META disciplines:** Every measurement CRUSHES the lower coverage bound (1.0 vs 0.95) AND lands INSIDE the speedup band (12.0 in [10, 100]). The contrast against `substrate_concept_uniform_random_extraction_v1` HARD_FAIL is the discriminating regime (random sampling CAN'T hold the same property; stratified DOES). Per cb7e89f1 + Skunkworks's symmetric guard: this clears wrong-bar-where-preempted (saturation@1.0 across all arms IS the genuine signal, and the discriminating contrast against random=HARD_FAIL is intact).
- **Caveat:** D1 saturation-cliff-flag applies — saturation@1.0 across all arms suggests possibly higher-leverage at upstream test (would 0.95 hold at 100x?). NOT a disqualifier for the MEASURED_MECHANISM atomization; just an upstream extension question.
- **Status:** **HIDDEN POSITIVE.** Atomize as MEASURED_MECHANISM "per-cluster stratified extraction holds coverage=1.0 at 12x speedup vs random=HARD_FAIL discriminating control". Tier opportunity: MEASURED_MECHANISM; composes with sampling/extraction pipeline upgrades.

### B-NEW5. `T3/EXP_substrate_data_attribution_counterfactual_rpe_v1_n4096` (CERT_CHAIN_GRADE, MIDDLE_BAND, cell_sha `ecc6306bc3b8`) -- NET-NEW
- **Failure mechanism (cell):** `CPE attribution rho=0.694 (4.16x wall vs TracIn baseline rho=0.693) -> substrate-native data attribution partial. CPE rho: mean=0.6937 min=0.6936 (HP>0.8 HF<0.3) | TracIn rho mean=0.6928 | wall ratio CPE/TracIn=0.24`.
- **Reframe under new META disciplines:** Substrate CPE achieves rho=0.6937, statistically indistinguishable from TracIn's 0.6928 (delta ~1e-3 vs 5-seed-control), AT 4.16x wall-speedup. HP rho>0.8 was preempted -- both methods sit at ~0.69, indicating the bar was wrong for the task structure (TracIn also can't hit 0.8 here). The within-cell control (TracIn baseline) IS the symmetric verifier per `7315be3c` controls-fail-needs-partial-correlation: CPE equivalent to baseline AT 4.16x speedup is a clean wall-clock-knob mechanism.
- **Status:** **HIDDEN POSITIVE.** Atomize as MEASURED_MECHANISM "substrate-native CPE attribution matches TracIn rho at 4.16x speedup, n=4096, 5-seed". Tier opportunity: MEASURED_MECHANISM; substrate-product Phase-3 contribution (attribution-with-glass-box-monitor).

### B-DIFF1. v1's caching_eviction (B14/C1) -- Skunkworks symmetric guard CORRECTION absorbed
- **v1 framing (incorrect):** "HIGH-LEVERAGE negative-IS-positive ... data-decides-tier-no-preempt"
- **Skunkworks correction (notes/research_to_skunkworks_orch_expdev_ABSORB_framework_ruling_137_decomposition_symmetric_guard_caching_eviction_NOT_clean_win_2026-06-20.md):** acc 0.80 misses honest pre-reg 0.85 by 5pp = honest miss, NOT a clean win. Per symmetric guard: data-decides-tier-no-preempt is for WRONG-BAR (3 templates: Hebbian/isotropy/sparse-1.4x) ONLY; honest pre-reg-bar misses STAY negative.
- **Corrected status:** STILL hidden-positive eligible IF atomized with **explicit acc-below-target in honest_scope** and tier as MEASURED_MECHANISM with split-claim ("amortization PASS at 0.108 / speedup PASS at 26.8x / acc-arm 0.80<0.85 HONEST_MISS, n=full"). The two strict-CRUSH arms (amortization 19x better than HP; speedup 24x better than HP) earn the upward, but the headline "caching-eviction passes" was over-claimed; the headline should be "two-of-three-HPs-CRUSH + one-HONEST-MISS at 5pp gap".
- **Net:** KEEP as upward atomization candidate but with corrected honest framing (NOT "caching-eviction passes"; YES "amortization+speedup pass at 19-24x-better-than-HP, acc-arm honest-miss at 5pp pending eviction-policy refinement"). **C1 in v1 remains the highest-leverage candidate post-correction.**

### B-DIFF2. v1's B5 b3_b6 (C4) graceful-degradation framing -- PARTIAL DEMOTE
- **v1 framing:** "B3a 13.8x writes-reduction at perf=0.83 is a sellable cost knob (90% writes savings at 17% perf cost)"
- **Skunkworks symmetric guard:** under `honest-pre-reg-stays-negative`, perf=0.83 is genuinely below the preregistered perf>=1.0 bar. The "graceful-degradation tier" framing borrows from LEVER #1.5 (9097f659) which is a SEPARATE cell scope; the original b3_b6 cell did NOT pre-reg a graceful-degradation tier.
- **Corrected status:** B3a stays a measured fact ("13.8x writes-reduction at perf=0.83") but the "graceful-degradation sellable knob" framing should be tagged RESEARCH_FINDING (informal product-relevant observation), NOT MEASURED_MECHANISM (which would imply substrate has a load-bearing graceful-degradation property by pre-reg). B3b (2.2x writes-savings + 16% perf GAIN) STAYS strict win.
- **Net:** Demote v1's C4 from "upward atomization MEASURED_MECHANISM" to "upward atomization MEASURED_MECHANISM on B3b sub-cell only (strict win); B3a stays RESEARCH_FINDING on the sellable-knob framing".

### B-DIFF3. v1's other 8 atomization candidates (C2/C3/C5/C6/C7 + honorable mentions) -- ALL STAY VALID per subagent re-check
The subagent re-checked all 10 v1 candidates against the symmetric guard. C1 (KEEP with corrected framing), C2 (audit-core C3 84.1x drift sep on REAL Pythia-160M residuals; single-metric-clean), C3 (SQ6 graceful-degradation = refuse-gate target regime; load-bearing for refuse-gate #5), C5 (KF1 zero-hallucinations = refuse-gate property; load-bearing metric is the refuse-gate not the 50x ratio), C6 (mode5 hierarchical depth >=40x; self-labeled LOWER BOUND per a3f473dd precedent), C7 (axis4 path-independence = NESS invariant; loop_area=0.0 IS the positive measurement). Honorable mentions: pp31c knee (precise cv=0 measurement; preempted band is the wrong-bar), HNSW on projected keys (composition-question; cell-author for Exp-Dev), bundle_snr (theory-overturned-prediction; same template as crosstalk-law) -- ALL STAY VALID.

### B-DIFF4. Items the v1 drill already handled correctly that the new META disciplines REINFORCE
- **B3 SVAMP family** (representation-bound negative correctly scoped to phase4b_multistep_pull_up): REINFORCED by Skunkworks's phase4b-1op-anomaly resolution -- the 1-op acc~0.02 is at the 2.7% intrinsic content-ceiling, not a parsing bug. The honest narrowed claim "MultiArith 2-op composition works (0.69); doesn't generalize ASDiv/MAWPS" is now firm.
- **B7 e1_substrate_crf_shared_lib** (HONEST-IRRELEVANCE dead-end): STILL valid. NO action.
- **a8_continual_writes**: v1 footnoted this as NOT a stale-metadata candidate (the a8 patch 83f064b7 is the methodological template, the atom is already correct as CERT_CHAIN_GRADE HARD_PASS). REINFORCED -- a8 is the no-forget envelope referent for continual-write lever per Skunkworks's substrate-mine v1 ruling.

---

## (c) v1-top-7 PHASE PLAN SUBSUME-RISK AUDIT

(Synthesizing Skunkworks's substrate-mine output `skunkworks_to_research_cc_expdev_SUBSTRATE_MINE_v1_subsume_risk_verdicts_gaps_2026-06-21.md` with v2 drill findings. Verdict per item: SAFE-TO-BUILD / PARTIAL-FOLD / FULL-SUBSUME-REFRAME.)

| # | v1 PHASE PLAN item | Subsume verdict | Action |
|---|---|---|---|
| **1** | Sparse-projected-KV FLAGSHIP (CERT 591 x a3f473dd) | **NONE / SAFE-TO-BUILD** | Genuinely novel composition; only CERT 591 = projection INPUT matches; Exp-Dev independently confirmed NONE. PROCEED. SCHEMA-VET PASS landed. |
| **2** | Continual-write lever (capacity-vs-forgetting under live writes) | **PARTIAL** | Fold-in: cite `a8_continual_writes_no_catastrophic_forgetting` HARD_PASS as no-forget envelope referent; fold `caching_eviction_pp44_capacity_aware_v2` (SMOKE PASS) as prior-SMOKE-to-promote. **NET-NEW v2 addition: also cite A1 `substrate_continual_learning_empirical_10e9x_v1` (27x speedup + zero-forget on Pythia-160M) as the cross-axis Pythia-LLM-context referent.** Distinctive axis = rigorous 3-arm policy SELECTION (selector vs write-all vs FIFO); NOT re-deriving "capacity-aware eviction works". |
| 3 | Pythia desat re-VET + Milestone 1 cell-author | **N/A in flight** | GPU 27/30 ETA ~2h; cell-author cleared on re-VET. |
| 4 | 2-level-ingest generalize (composition + storage) | **PARTIAL-by-design** | Within-envelope finding (v3_2level + partof_broad_before + partof_broad_after = relevance-bumped 2026-06-21 commit 92612a4c) exists as referent. Distinctive axis = depth-5+ generalization + harder/unseen benchmarks off WordNet (Skunkworks's CAN-fail refinement). Cell-author Exp-Dev queued. |
| 5 | Capacity-saturation ceiling at scale (crosstalk-law c unbounded) | **PARTIAL** | `substrate_encoder_capacity_at_scale_battery_gpu` (PASS) + `substrate_extended_context_ceiling_posbind` (PASS) + `etf_minilm_M_star_cross_N` (MIDDLE) already measure capacity-at-scale. Distinctive axis = where **crosstalk-law c saturates** (the unbounded-c upper limit, per 7315be3c) -- these existing PASSes don't map that. Cite them; don't re-measure capacity-at-scale. |
| 6 | Milestone 2 multi-hop chain query | **FULL-SUBSUME -> REFRAME** | `ccc1_extra_fb15k237_kg_multihop` PASS+CHAIN_GRADE+HIGH-relevance + `comp23_multihop_composites` PASS + `approximate_multi_hop_sampling` PASS already PROVE standalone multi-hop capability. **M2 must be reframed: NOT "test multi-hop capability" (done) but "multi-hop chain query INTEGRATED into the glass-box on sparse-projected-KV store + LEVER#4 depth-refuse + CERT 592 K_max" = the Phase-3 INTEGRATION.** ccc1 = the capability referent, not re-derivation target. |
| 7 | Cross-domain probe (X) -- novel-direction finding | **LOW-subsume but STEER** | Well-explored: `b_delta_readout_lever_transfer` HARD_PASS, conll2003/ontonotes PASS, sst2/imdb MIDDLE, conceptnet HARD_FAIL. Probe should AVOID re-treading these + target genuinely-novel directions (e.g., the 8 new neighbor fields from research_field_advisor: nonequilibrium-stat-mech / mesoscopic-transport / population-genetics-Wright-Fisher / etc.). |

**Plus gap-list (Skunkworks's substrate-mine surfaced HIGH-priority existing experiments not in v1):**
- `b_delta_readout_lever_transfer_v2` HARD_PASS = chain-grade cross-domain readout-lever transfer; candidate composition input for glass-box (readout-transfer across domains).
- `substrate_encoder_capacity_at_scale_battery_gpu` PASS feeds item 5 + STORAGE chain.
- `ccc1_extra_fb15k237_kg_multihop` PASS = M2 referent (item 6 reframe).

**Methodology-vs-outcome mismatches:** v1 UNDER-credits existing coverage on items 2 (a8 + pp44) and 6 (ccc1 + comp23). Both should be reframed as EXTENSION/INTEGRATION citing existing PASSes, NOT novel capability tests.

---

## (d) Atomization-ready candidates (v2 net-new, ranked by leverage)

Ranked by **(symmetric-guard-clears) x (downstream-compose with PHASE PLAN v2 enabling chain) x (Director-bandwidth fit)**.

| # | atom_id | current | proposed action | tier target | composes_with | cell_sha | symmetric-guard |
|---|---|---|---|---|---|---|---|
| **NEW-1** | `T3/EXP_substrate_continual_learning_empirical_10e9x_v1` | MIDDLE_BAND / CERT_CHAIN_GRADE | atomize as MEASURED_MECHANISM "27x speedup + zero-forget on Pythia-160M, n_train_streams=2"; honest_scope notes 1000x extrapolation-aspiration NOT measured | MEASURED_MECHANISM | **continual-write lever pre-reg** (Skunkworks #1 P1 enabling) + a8 no-forget envelope + Pythia-LLM-cross-axis | `76a4e7b7235b` | CLEARS (wrong-bar / extrapolation-preempted; strict-win on every measured arm) |
| **NEW-2** | `T3/EXP_q_b1_bisect_d275-d281_cluster` (collective claim from 7 sibling atoms) | 3 MIDDLE_BAND + 4 HARD_FAIL + 1 HARD_PASS d275 | atomize as MEASURED_MECHANISM "chain-depth onset bisected to (275, 277] at N=16384, n_seeds=5"; re-label MIDDLE_BAND atoms as bisection-by-design | MEASURED_MECHANISM | `q_b1_chain_depth_200_v1_n16384` HARD_PASS + CERT 592 K_max NESS envelope | `113a3f15d38c` (d277 anchor) | CLEARS (right-by-design bisection; LOWER-BOUND-on-onset precedent per a3f473dd) |
| **NEW-3** | `T3/EXP_substrate_per_cluster_stratified_extraction_v1` | MIDDLE_BAND / CERT_CHAIN_GRADE | atomize as MEASURED_MECHANISM "per-cluster stratified extraction holds coverage=1.0 at 12x speedup; random=HARD_FAIL discriminating control" | MEASURED_MECHANISM | sampling/extraction pipeline upgrades; cap-int integration-check I-axis | `16e332c9fd31` | CLEARS (wrong-bar with saturation@1.0 across all arms; D1 saturation-flag noted as upstream-extension question only) |
| **NEW-4** | `T3/EXP_substrate_data_attribution_counterfactual_rpe_v1_n4096` | MIDDLE_BAND / CERT_CHAIN_GRADE | atomize as MEASURED_MECHANISM "substrate-native CPE attribution matches TracIn rho at 4.16x speedup, n=4096, 5-seed" | MEASURED_MECHANISM | Phase-3 glass-box-attribution capability | `ecc6306bc3b8` | CLEARS (within-cell TracIn-baseline control; both methods at 0.69 = bar wrong for task structure) |
| **NEW-5** | `T3/EXP_substrate_drosophila_mb_sparsity_sweep_v1_512_2048_gpu` | MIDDLE_BAND / CERT_CHAIN_GRADE | atomize as MEASURED_MECHANISM "fly-LSH sparse f=0.05 lifts N=512 capacity +0.142 (3/3 seeds) inside prereg HP-band"; honest_scope notes f0.01 best 2/3 miss | MEASURED_MECHANISM (split-claim on f0.05 arm only) | sparse_boundary_v2_cpu_v1 (a3f473dd) | PARTIAL (f0.05 anchor robust; f0.01 anchor stays-honest-miss) |

**Plus PARTIAL DEMOTE from v1:** B5 b3_b6 B3a graceful-degradation framing -> tag as RESEARCH_FINDING (informal product observation) instead of MEASURED_MECHANISM. B3b (strict win) stays as MEASURED_MECHANISM atomization candidate.

**Plus AMENDMENT to v1 caching_eviction C1:** atomize with EXPLICIT split-claim honest framing -- "amortization PASS at 0.108 / speedup PASS at 26.8x / acc-arm 0.80 < 0.85 HONEST_MISS, n=full". NOT "caching-eviction passes" headline.

---

## (e) Recommended Director actions (ranked by leverage)

### IMMEDIATE / Director-lane integrations
1. **HIGHEST: AMEND continual-write lever pre-reg to cite A1 `substrate_continual_learning_empirical_10e9x_v1` as the Pythia-LLM-context referent** (alongside a8 + pp44_capacity_aware). The 27x speedup + zero-forget on Pythia-160M is the strongest cross-axis evidence for the lever's mechanism; cite as referent NOT re-derivation. Director-ship; non-blocking on Skunkworks atomization. **Status: ROUTE NEXT CYCLE via amendment to existing pre-reg.**
2. **HIGH: REFRAME PHASE PLAN v2 item 6 (M2 multi-hop)** to glass-box-INTEGRATION on the sparse-projected-KV store + LEVER#4 depth-refuse + CERT 592 K_max + cite ccc1 as capability referent. Do NOT author standalone multi-hop capability cell. **Status: PHASE PLAN v2 v1.1 refresh required; queue as Director-lane decision-point update.**
3. **MEDIUM: REFRESH `data/director_plan.json` ts + add A1 to `recent_ships` after atomization land** (per Director-maintains-plan-json discipline). **Status: defer to atomization-land event.**

### Route to Skunkworks (CERT-neutral atomization asks; allowed even while paused)
4. **HIGH: route NEW-1 A1 atomization** -- MEASURED_MECHANISM "27x speedup + zero-forget on Pythia-160M". Composes_with a8 + continual-write lever. Per-atom landed-VET per Skunkworks's discipline.
5. **HIGH: route NEW-2 q_b1_bisect cluster atomization** -- collective MEASURED_MECHANISM "chain-depth onset bisected to (275, 277] at N=16384"; re-label the 3 MIDDLE_BAND atoms as bisection-by-design.
6. **MEDIUM: route NEW-3 per-cluster stratified extraction atomization** -- MEASURED_MECHANISM with discriminating-control cite.
7. **MEDIUM: route NEW-4 CPE-TracIn attribution atomization** -- MEASURED_MECHANISM with within-cell baseline-control cite (per 7315be3c).
8. **MEDIUM: route NEW-5 fly-LSH sparse f=0.05 atomization** -- split-claim MEASURED_MECHANISM on f=0.05 robust arm only.
9. **MEDIUM: route v1 caching_eviction C1 AMENDMENT** to corrected split-claim framing (Skunkworks symmetric guard correction).
10. **LOW: route v1 B5 b3_b6 B3a DEMOTE** of graceful-degradation framing to RESEARCH_FINDING.

### Skunkworks sub-audit batch routing
11. **NEXT-BATCH-PRIORITY: q_b1_bisect/chain_depth cluster sub-audit** (4 HARD_FAIL + 3 MIDDLE_BAND siblings; collectively the wrong-bar-template clearest case; the d275 HARD_PASS sibling proves same protocol works just below the bisected onset). Highest precision-yield per Skunkworks's 64-HARD_FAIL slice.
12. **SECOND-PRIORITY: pp50/pp58 theory-overturned-prediction cluster** (likely template-match to crosstalk-law / bundle-SNR: substrate has measurable law that doesn't match analytic prediction; possible MEASURED_MECHANISM "the substrate SCS prediction-ratio is consistently 5-15x; that itself is the measurable law").

### Exp-Dev cell-author asks (gated by pause flag)
13. **Generalize/scale cell for 2-level-ingest** (per Skunkworks's CAN-fail refinement: depth-5+ eliminates-vs-pushes cliff; harder/unseen benchmarks off WordNet). Queued; bandwidth-tolerant.
14. **HNSW on #7-projected keys** (v1 B10; composes with CERT 591). Status: file pre-reg to exp_dev when cell-author bandwidth clears.

### No-action / dead-end verdicts (negativity-bias cuts both ways)
- HONEST_NEGATIVE atoms (t3_phaseA_completeness, partof_heldout, hypernym_heldout): NO upward-reframe applies; the honest_scope IS the negative-knowledge. KEEP-AS-IS.
- HONEST_BOUNDED atom (primitive_2_hopfield_cleanup_v1): GATE-D PASS + GATE-F NOT sub-linear is honest characterization; LOW additional leverage to split-atomize.
- `pp49` HRC cluster (4 atoms): single-mechanism HRC failure; honest negative; LOW audit-priority.
- `kappa3_sensitivity` (2 atoms; bar missed by 12x): KEEP-NEGATIVE.
- `ex_concept_1_*` (3 atoms; substrate < trigram baseline): KEEP-NEGATIVE per `honest-pre-reg-stays-negative`.
- v1's clean dead-ends (e1_crf, bid_order_parameter, analogy_chain_transfer, world_model_mwp 1op): UNCHANGED in v2.

### Director-lane gap-close items still standing
- M2 reframe (per item 2 above) doubles as the next Director-lane concrete ship.
- Cross-domain probe Trigger F dispatch (item 7) still queued; bandwidth-tolerant.

---

## (f) Cross-thread synthesis

### Phase 0 (phase-diagram MAP):
- **NEW-2 q_b1_bisect cluster** adds a high-precision boundary measurement to the phase map (chain-depth onset bisected to (275, 277] at N=16384). Composes with CERT 592 K_max NESS envelope.
- **NEW-1 A1** adds Pythia-160M continual-learning context to the phase map (27x speedup at the LLM-cross-axis boundary).
- pp50/pp58 theory-overturned-prediction cluster (Section c#12) is a phase-diagram CALIBRATION batch — same family as bundle_snr.

### Phase 1 (ship cert-PASS levers):
- **#1 continual-write lever** (Skunkworks's top P1 enabling) gets a stronger evidence base: a8 + pp44 + **A1** + caching_eviction (corrected framing) compose into a multi-axis lever-design discipline.
- **caching_eviction C1** still atomization-ready WITH corrected split-claim framing.

### Phase 2 (onboard 104-value-trove):
- 2-level-ingest generalize cell (PHASE PLAN v2 item 4) is the highest-leverage Phase-2 candidate; partof_after relevance bump already landed (commit 92612a4c).
- NEW-3 per-cluster stratified extraction adds a Phase-2 sampling/extraction composition input.

### Phase 3 (glass-box-LLM):
- **NEW-4 CPE attribution at 4.16x speedup** adds a Phase-3 glass-box-attribution capability (substrate-native data attribution matching TracIn at 4.16x speedup -- the kind of capability LLMs can't match algorithmically).
- v1's C2 (audit-core C3 84.1x drift sep on REAL Pythia-160M residuals) + C5 (KF1 zero-hallucinations) STAY as Phase-3 foundation atoms.
- M2 reframe (item 6) is the load-bearing Phase-3 INTEGRATION milestone.

### Composite Phase-3 story (if 4 of the 7 net-new+v1 atomizations land):
substrate-KV (CERT 591) + 84.1x drift detection (C2) + zero-hallucination refuse-gate (C5) + rank-1 eviction (C1 corrected) + 27x continual-learning + zero-forget on Pythia-160M (NEW-1 A1) + CPE attribution at 4.16x speedup (NEW-4) = complete monitor + refuse + edit + retrieve + attribute stack on a glass-box substrate.

---

## (g) Substrate-product implications

| Candidate | Product capability enabled / strengthened |
|---|---|
| **NEW-1 A1 substrate continual learning Pythia-160M** | **Cross-axis evidence for the continual-write lever**: 27x speedup + zero-forget at LLM-cross-axis is the strongest evidence for substrate-as-live-non-forgetting-store. Glass-box continual-learning foundation. |
| **NEW-2 q_b1_bisect cluster** | **Precise chain-depth onset**: production deployments get a defensible "chain-depth budget" at N=16384 of <=275 hops; refuse-gate compositional. |
| **NEW-3 per-cluster stratified extraction** | **Production-grade sampling**: coverage=1.0 at 12x speedup (vs random=HARD_FAIL discriminating control) = a working sampling discipline for Phase-2 onboarding pipelines. |
| **NEW-4 CPE attribution** | **Glass-box data attribution**: matches TracIn quality at 4.16x speedup; production-deployable attribution capability for Phase-3 glass-box-LLM. |
| **NEW-5 fly-LSH sparse f=0.05** | Sparse-coding lever for capacity-vs-sparsity tradeoff at N=512 -- secondary to a3f473dd flagship sparse super-capacity. |
| v1 C1 caching-eviction (corrected) | GDPR/CCPA fact-deletion at amortization=0.108 / speedup=26.8x; acc 0.80 honest gap pending eviction-policy refinement. |
| v1 C2 audit-core C3 | LLM drift monitoring (84.1x sep on Pythia-160M residuals). |
| v1 C3 SQ6 refuse-target | Refuse-gate signal for graph-store overload. |
| v1 C5 KF1 zero-hallu | Output safety guarantee (refuse-gate compositional). |

---

## (h) Citations

Internal cites only (no lit-scan; this is internal substrate scour per directive). Symmetric-verify applied to every claim.

### Net-new atoms verified off Store (atom_id + cell_sha):
- T3/EXP_substrate_continual_learning_empirical_10e9x_v1 (cell_sha `76a4e7b7235b`) -- A1
- T3/EXP_q_b1_bisect_d277_v1_n16384 (cell_sha `113a3f15d38c`) -- A2
- T3/EXP_q_b1_bisect_d278_v1_n16384 (cell_sha `e99645cdba75`) -- A2
- T3/EXP_q_b1_bisect_d281_v1_n16384 (cell_sha `f1f5460047e1`) -- A2
- T3/EXP_substrate_drosophila_mb_sparsity_sweep_v1_512_2048_gpu (cell_sha `912a228fc8ee`) -- A3
- T3/EXP_substrate_per_cluster_stratified_extraction_v1 (cell_sha `16e332c9fd31`) -- A4
- T3/EXP_substrate_data_attribution_counterfactual_rpe_v1_n4096 (cell_sha `ecc6306bc3b8`) -- A5

### v1 candidates re-checked (all 10 + honorables; only B5 B3a partial-demote):
all cited in v1 report; cell_shas unchanged.

### Skunkworks-corrected/contributed disciplines composed-into v2:
- `ae088f94` (6 disciplines) + `baa06f0a` (3 disciplines including capacity-relative-gate, same-distribution-split, reconciliation-uses-runs-own-moments) + `7315be3c` (controls-fail-needs-partial-correlation; crosstalk-law) + `cb7e89f1` (5 disciplines including cited-number-must-reproduce-from-cell, data-decides-tier-no-preempt, genuine-check-artifact-free-arm, complete-divide-by-zero-BOTH-limits, alpha-semantic-disambiguation-LOAD-vs-sparse-f) + `1fcb4dcf` (4-layer-reciprocal-witness + label-must-match-aggregation + disposition-execution-must-preserve-per-atom-outcomes) + `99392cca` (selector-needs-genuine-cost) + `5502fe27` (landed-VET-must-check-stale-atom) + `fc5ea754` (D4 atom-vs-cell drift check) -- the 18+ META disciplines fleet-wide.
- Skunkworks's **symmetric guard correction** (data-decides-tier-no-preempt is for WRONG-BAR-3-templates ONLY; honest pre-reg-bar misses STAY negative) per `notes/research_to_skunkworks_orch_expdev_ABSORB_framework_ruling_137_decomposition_symmetric_guard_caching_eviction_NOT_clean_win_2026-06-20.md`.
- Skunkworks substrate-mine v1 subsume-risk verdicts per `notes/skunkworks_to_research_cc_expdev_SUBSTRATE_MINE_v1_subsume_risk_verdicts_gaps_2026-06-21.md`.

### Cross-references (Director-lane upstream notes):
- v1 drill report: `notes/research_substrate_NEGATIVES_2x_negative_was_positive_3x_scour_USER_DIRECTIVE_2026-06-20.md`
- PHASE PLAN v2 v1 (USER-RATIFIED): `notes/research_PHASE_PLAN_v2_v1_SYNTHESIZED_Skunkworks_storage_chain_thread_canonical_forward_work_queue_USER_RATIFIED_2026-06-21.md`
- Skunkworks PHASE PLAN v2 next-N rankings: `notes/research_skunkworks_PHASE_PLAN_v2_next_N_items_per_phase_2026-06-21.md`
- 2-level-ingest lineage (BURIED POSITIVE): `notes/skunkworks_to_research_cc_orch_testbed_SUBAUDIT_BATCH1_single_seed_MIDDLE_BAND_buried_positive_2level_ingest_lift_2026-06-21.md` + `notes/skunkworks_to_research_cc_expdev_LINEAGE_CONFIRMED_progression_partof_after_flag_2level_in_phase2_top3_2026-06-21.md`
- 15 blank-verdict triage: `notes/skunkworks_to_research_orch_15_blank_are_custom_verdicts_a1_multihop_clear_demote_candidate_classifier_lesson_2026-06-20.md`
- Revival drills BOTH resolved: `notes/skunkworks_to_research_expdev_cc_orch_REVIVAL_DRILLS_BOTH_RESOLVED_folded_2026-06-21.md`
- Continual-write lever pre-reg + amendment: `notes/research_to_skunkworks_expdev_PREREG_continual_write_lever_v1_*.md` + `notes/research_to_skunkworks_expdev_CONTINUAL_WRITE_AMENDMENT_C1_workload_C2_crosstalk_cite_4_layer_confirmed_2026-06-21.md`
- Current state: `data/director_plan.json` (CERT 588 honest; 13 priorities)
- Canonical evidence map v6: `notes/research_canonical_evidence_map_v6_MINI_REFRESH_*.md`

### Verify-the-referent applied:
Every cited number above (speedup, retention, recall, knee, ratio, accuracy, separation factor, cv, hp_count, seed-count) was read directly off the atom's `metrics_headline` field via PartitionedStore.all_atoms() on `data/substrate_index/` by the scour subagent. No derived/extrapolated numbers; no `assumed` values substituted. Two-arm controls (e.g., CPE-vs-TracIn baseline-control for NEW-4; substrate-vs-LLM cross-axis for NEW-1; stratified-vs-random for NEW-3) cited as appear in the cell. Symmetric-verify CLEARS / PARTIAL labeled explicitly per candidate. Total atoms enumerated: **177,253** (matches v1's 177,244 + 9 atom growth = consistent with steady-state churn).

---

-- Research (Director, Opus dispatch)
2026-06-21
