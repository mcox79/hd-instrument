# RESEARCH (Director) -- STRATEGIC SYNTHESIS: per-phase exit criteria + Phase 3 first concrete milestone. USER directive ("is the plan clear so we can implement fully?"). Gap-close item 3. This is the destination-defining document so future "progress" answers stop being snapshots and start being trajectories. Substantive; one Director-lane authoring cycle.

(Filename has no `to_<recipient>` — Director-side substrate-product working artifact.)

## Why this exists (the gap USER named)
Earlier this session USER asked "how much progress have we made?" — my answer was a snapshot (counts by status). USER correctly pushed back: that's not progress, that's a slice in time. The honest gap: the strategic destination (per-phase "done" criteria + Phase 3 first concrete milestone) isn't crisply defined; without that, "progress" can't be honestly measured. This document is the destination definition.

## Anchoring honest scale (per Skunkworks decomposition 2026-06-20)
- Substrate state: **440 PASS-family + 137 non-PASS chain-grade + 15 custom-verdict = 592 chain-grade RESULTS** (not "592 working capabilities"; the firm "working capabilities" floor is ~440 with the PASS-vs-HARD_PASS rigor sub-question flagged for completeness)
- 3 chain-grade ships this session: CERT 590 CSP first-ship / 591 #7 learned-projection / 592 K_max NESS envelope (depth-beyond-equilibrium)
- 3 MEASURED_MECHANISM atomizations this session: Hebbian (substrate-KV = NN); crosstalk-law (isotropy overturned); sparse super-capacity (>=300x@f=0.005 LOWER-BOUND)
- 15 META disciplines atomized; substrate-KV mechanism architecturally SETTLED = NN+#7

## Phase 0 — phase-diagram MAP (where substrate works / where it doesn't / what bounds)

**What it is:** comprehensive characterization of substrate capacity boundaries across the operational regime.

**Current state (honest):** 16 cert-canonical clusters DONE per map v5; 3 chain-grade boundary atoms; 3 MEASURED_MECHANISM characterizations; sparse super-capacity curve measured (onset NOT located); K_max envelope measured (cliff REPORTED). Per Skunkworks 137-decomposition, the under-classified atoms include many proven-bounds that strengthen the phase-diagram once relabeled.

**EXIT CRITERIA (proposal — Director-judgement; Skunkworks vet recommended):**
1. **All major capacity dimensions measured with CAN-fail bounds:** recall depth (K_max NESS ✅), chain depth (CERT 592 ✅), sparse-fraction (a3f473dd partial — onset NOT located; LOWER-BOUND only), encoder rho_mean (key-separability cert atoms ✅), crosstalk-moment (7315906c ✅), capacity vs alpha (CSP/Hebbian region ✅).
2. **All boundary claims have proven-bound status labeled** (per the 137-decomp framework; Skunkworks's sub-audit closes this gap).
3. **Crosstalk-onset boundary located** for sparse super-capacity (currently LOWER-BOUND only; a higher-LOADS follow-up cell would close).
4. **A unified phase-diagram document exists** operationalizing the boundaries — like map v5+ but extended with quantitative cliffs.
5. **No major capacity-cliff observed in active experimentation is uncharacterized** (negative discipline: any cliff observed by Exp-Dev must close with a measured boundary atom).

**Honest progress signal:** items 1+5 are ~80% (most major dimensions measured); item 2 is ~30% (Skunkworks sub-audit in progress); items 3+4 are ~50% (sparse-onset open; document partial). Net: Phase 0 is the MOST mature phase but has the open sparse-onset + the label-honesty close-out before it's done.

## Phase 1 — ship cert-PASS levers (runtime additive flags operationalizing measurements)

**What it is:** ship reversible additive flags that operationalize substrate measurements into production capabilities. Each lever consumes cert atoms as inputs + ships a runtime capability + earns its own cert grade via CAN-fail.

**Current state:** 1 lever shipped (CSP CERT 590); 1 lever cell built (LEVER #1.5 capacity-sweet-spot; SCHEMA-VET pending Skunkworks); 3 named in queue (PCA + sparse_coding + multiplicative_composition) — pre-regs NOT yet authored.

**EXIT CRITERIA (proposal):**
1. **5 levers shipped** (CSP ✅ + capacity-sweet-spot + PCA + sparse-coding + multiplicative-composition), each:
   - Reversible additive flag (C1 protocol)
   - Regression-set with no-recall-degrade gate at p>=0.99 / appropriate seeds OR explicit non-inferiority margin
   - CAN-fail discriminating regime (per cb7e89f1 discipline)
   - 3-arm CAN-fail where applicable (known-bad / measurement-driven / naive-fixed) per Skunkworks R2 on LEVER #1.5
2. **Each lever earns own cert tier from data** (per data-decides-no-preempt; CHAIN-GRADE-CANDIDATE = TARGET only).
3. **Cumulative substrate capacity audit-clean** post-each-ship (Skunkworks's D1/D2/D3 audit pattern; no inflation; honest count moves).
4. **Compose-with map updated per ship** showing which levers compose with which cert atoms.

**Honest progress signal:** item 1 is 1/5 shipped + 1/5 cell-built (vet pending). Items 2+3 active discipline. Item 4 lagging (LEVER #1.5 compose-with not yet authored; PCA + sparse-coding + multi-composition all need pre-regs). Net: ~25% by ship count.

## Phase 2 — onboard 104-value-trove (cap-int cluster canonicals)

**What it is:** ingest the 104 cap-int values into the substrate as cluster canonicals — each value gets cert-graded support OR explicit proven-bound; Track-A enumerator runs clean per the I1-I10 invariants.

**Current state:** 492 cap-int integrated (per memory); 10 cluster canonicals seed-tracked; Track-A capability-applies enumerator running per I1 fix (per-atom pq=CERT_CHAIN_GRADE pre-check before integration).

**EXIT CRITERIA (proposal):**
1. **All 104 values either:**
   - cert-graded with explicit `cert_atom` reference (post 137-decomp relabel for under-classified bounds), OR
   - explicitly marked as proven-bound (HONEST_NEGATIVE or HONEST_BOUNDED custom-verdict per the 15-custom-verdict taxonomy), OR
   - explicitly marked as `to_evidence` (open work item; not yet cert-supported)
2. **Track-A enumerator I1-I10 invariants pass on a clean run** (per Skunkworks I1 fix).
3. **Track-B knowledge_graph cert-claim sequence advanced** (CERT 580 first claim; subsequent claims following).
4. **No 104-value claim is unsupported** (lazy-eval claims that haven't been backed by cert atoms = forbidden per substrate-capability-first discipline).

**Honest progress signal:** integration mechanics LIVE (492 atoms; I1 fix; Track-B started). Value-by-value support audit not yet enumerated. Likely ~60% mature but no per-value status doc exists. Director-lane work to enumerate the 104 with status next-cycle.

## Phase 3 — glass-box LLM (FIRST CONCRETE MILESTONE proposal)

**What it is:** build a trackable LLM on the substrate — substrate-KV memory + retrieval-augmented generation with FULL TRANSPARENCY (every retrieval visible; every fact attributable; every reasoning step trackable).

**Current state:** foundation cert-grade (substrate-KV mechanism SETTLED = NN+#7 projection per CERT 591; depth-beyond-equilibrium CERT 592; sparse super-capacity a3f473dd). NO end-to-end glass-box-LLM cell exists yet.

**FIRST CONCRETE MILESTONE (proposal — the "what's the destination?" answer):**
**`exp_glass_box_LLM_milestone_1_substrate_RAG_v1_gpu_v1`** — a runnable cell that:

1. **LLM:** Pythia-2.8B (FULL; pythia-160m smoke)
2. **Substrate-KV:** N facts stored in substrate via #7 learned projection (CERT 591); recall via NN-argmax (per substrate-KV mechanism CERT 591)
3. **Pipeline:** input prompt → LLM generates draft → at each generation step, query substrate-KV for relevant facts → augment context with retrieved facts → continue generation → final output
4. **Transparency:** every retrieval logged with (query, top-K retrieved fact_ids, similarity scores, decision: include/exclude). Every output token traceable to either parametric-LLM source or substrate-fact source.
5. **CAN-fail discriminating regime:** 
   - Arm 1 (raw LLM baseline): no substrate augmentation; recall on factual tasks = LLM's parametric-only floor
   - Arm 2 (substrate-augmented): with substrate-KV active; recall must measurably exceed raw-LLM
   - Arm 3 (naive RAG baseline): embed-then-retrieve with frozen-bge keys (no #7 projection); recall = naive-RAG ceiling
   - **Discriminating iff** substrate-augmented (Arm 2) beats BOTH (a) raw LLM (Arm 1) AND (b) naive-RAG (Arm 3) by threshold (proposed: 10% absolute factual-recall improvement on a held-out factual benchmark)
6. **HARD_PASS bands:** factual-recall-augmented >= 0.70 (vs raw-LLM ~0.50 baseline) on N=10k held-out facts; transparency-rate = 100% (every output token attributable); composes_with CERT 591 (projection) + CERT 592 (depth-beyond-equilibrium for chain queries) + sparse super-capacity (if encoder is sparse-coded).
7. **Cert target:** CHAIN-GRADE-CANDIDATE (per data-decides-no-preempt — fresh claim about glass-box-LLM end-to-end; does NOT inherit from input atoms).
8. **Discriminating-regime + scope-lock:** Pythia-2.8B, factual-recall-only (not reasoning chains yet — that's a future milestone), N=10k facts. Larger LLMs / multi-hop reasoning / refuse-gate integration = subsequent milestones.

**EXIT CRITERIA for Phase 3 (proposal):**
1. **Milestone 1 (above) shipped at cert tier.** This is "Phase 3 first concrete milestone" — the destination definition.
2. **Subsequent milestones** (post-milestone-1; not load-bearing for "is the plan clear"):
   - Milestone 2: multi-hop substrate-KV chain queries (uses CERT 592 depth-beyond-equilibrium)
   - Milestone 3: sparse-encoded substrate-KV (uses sparse super-capacity a3f473dd)
   - Milestone 4: refuse-gate integration (uses refuse-gate #5 path A; substrate refuses on out-of-envelope queries instead of fabricating)
   - Milestone 5: full glass-box-LLM with chain-recall + sparse + refuse-gate + transparency (production-config)

**Honest progress signal:** foundation cert-grade (CERT 591 + 592 + a3f473dd) — that's the "we can build it" floor. Milestone 1 cell NOT yet authored (this synthesis is the pre-reg seed; needs Skunkworks SCHEMA-VET + Exp-Dev cell-author). Subsequent milestones queued. Net: ~10-15% by ship count but the FOUNDATION is what was load-bearing and that's cert-grade.

## The crystal-clear plan (what USER asked for)

**Now you can answer "how much progress" honestly:**
- **Phase 0:** 80% by capability dimensions / 30% by label-honesty close-out / sparse-onset open
- **Phase 1:** 25% by ship count (1/5); LEVER #1.5 cell built awaiting vet; 3 levers need pre-regs
- **Phase 2:** ~60% integration mechanics live / value-by-value audit not yet enumerated
- **Phase 3:** ~10-15% by ship count BUT foundation cert-grade; Milestone 1 (substrate-RAG v1) is the destination-defining first concrete deliverable

**And the destination-line:** when all 5 Phase 1 levers ship + 104 values have status + Milestone 1 substrate-RAG ships at cert tier + all bounds proven-labeled + sparse-onset located. That's the "we did it" condition.

## What this synthesis DOESN'T do
- It does NOT bypass Skunkworks's vet authority — every milestone gets SCHEMA-VET like any pre-reg.
- It does NOT lock the destination — USER can redirect to a different Phase 3 milestone shape (e.g., reasoning-first rather than retrieval-first; or pose a different glass-box-LLM target entirely).
- It does NOT pre-empt data — every cert tier is data-decides-no-preempt per cb7e89f1.
- It is NOT the substrate-product synthesis (that's separate; v2 commit 1d2be088).

## Standing
- **USER:** the plan is now crystal clear. You can redirect any of the per-phase exit criteria OR the Phase 3 Milestone 1 shape. Otherwise: this is the destination.
- **Skunkworks:** SCHEMA-VET on Milestone 1 framing? (Director-side judgement; cert-discipline implications around the 3-arm CAN-fail + scope-lock + cert-tier target — fits the pattern she vet'd for LEVER #1.5; would benefit from her sign-off before becoming a pre-reg.)
- **Exp-Dev:** when Milestone 1 is SCHEMA-VET-passed, cell-author cadence (smoke pythia-160m → full Pythia-2.8B; GPU dispatch via Orchestrator).
- **Me:** synthesis filed; plan.json updated to add `phase_completion_estimates` field per the above + new priority `phase_3_milestone_1_substrate_RAG`. Next own-lane: route Milestone 1 pre-reg to Skunkworks if USER ratifies the framing.
- **USER-pending:** ratify the strategic synthesis (or redirect); Phase 3 cost/policy brief review.

-- Research (Director)
