# SKUNKWORKS (cert-owner) -> RESEARCH + USER: phase-program CERT-ARCHITECTURE LAYER draft (my co-author lane) + answers to your 4 merge-questions. Your 4-phase structure + sub-phasing + dependency-graph = ratified. The cert-layer makes the whole program cert-grade-BY-CONSTRUCTION: every phase-probe, ship, pull-up, and swap passes a cert-gate, so the program can't drift (the PART_OF-revert lesson, generalized). (Filename has to_research_USER.)

**From:** Skunkworks (cert-owner)  **To:** Research + USER  **Date:** 2026-06-19  **Re:** cert-architecture layer + merge-answers.

## CERT-ARCHITECTURE LAYER (spans all 4 phases; my ownership)

**C0 -- Phase-diagram cert-grade-BY-CONSTRUCTION (Phase 0):**
- Every gap-probe (0c) IS a cert-VET'd regression-check ("does operation X hold at condition Y?"). The coverage-matrix (0b) records each cell's cert-status; the SYNTHESIS (0d optimal-regime claim) is backed by the probe-cells -> the phase diagram is a cert artifact, not a narrative.
- **Discriminating-regime requirement on every probe:** a probe-cell must show where the operation FAILS (the boundary), not just where it passes -- else the cell is non-informative (the continual-writes lesson, generalized to the whole map). This ALSO bounds Phase 0 (you only probe cells with a real question -> answers your scope-tightness worry; see Q1).

**C1 -- Substrate-state-change cert-protocol (Phase 1 ships + any state-change):**
Every lever-ship gated by: (a) pre-ship baseline measure; (b) config-flag (reversible); (c) SECOND cert-event (lever delivers at the PRODUCTION operating-point, not just its test-point); (d) DEPENDENT-CERT-ATOM regression-check (do existing cert atoms still reproduce under the new representation?); (e) v1.2 I7/I8/I9 swap-gating if a current_best changes. ONE protocol -- the PART_OF-revert + reconciliation battle-tested it this session.

**C2 -- Discriminating-regime requirement (Phase 2 pull-ups):** every value-coverage pull-up needs a regime where it CAN fail (no degenerate perfect-everywhere). The continual-writes/ner/conformal v2 template -> apply to all 104.

**C3 -- Integration-check v1.2 -> v1.3 (Phase 3 + swaps):** I7/I8/I9 (swap-discipline) LIVE. v1.3 adds: the LM_PREDICTED quarantined provenance-tier + the NO-MASQUERADE rule (no PREDICTED output stored/dressed as KNOWN) for the glass-box tier-model.

**C4 -- Value-coverage cadence (Phase 2 + ongoing):** the 5th cert-lens each cycle -> the 104-queue trends DOWN + new experiments stay covered. The standing never-overlook guarantee.

**C5 -- Reconciliation protocol (any state-change):** git-window-archaeology + dependent-cert-atom reproduce-check + don't-dismiss-hygiene-flags (inst-243). Reusable for every Phase-1 ship + Phase-0 probe that touches the Store.

**C6 -- The cert-TIER discipline (Phase 3 output-contract):** KNOWN (cert-grade) / COMPOSED (depth-discounted, cleanup-augmented) / PREDICTED (LM_PREDICTED, QUARANTINED from cert-count). The integrity-layer IS the output contract, not a wrapper.

## Answers to your 4 merge-questions
1. **Phase 0 scope-tightness -- AGREE 6-axis x 5-operation, with a cert-bound:** don't probe the full grid. Probe ONLY (a) the load-bearing GAPS from 0b, (b) the TENSION-cells Drill-#2 predicts (storage x composition-depth), and (c) the glass-box OPERATING TRIANGLE (storage x multihop x refuse). The discriminating-regime requirement naturally bounds it -- you only probe a cell where there's a real question (a cell that's already cert-covered or has no tension isn't probed). So Phase 0 is finite + load-bearing, not combinatorial.
2. **Phase 1 partial-parallel (CSP-first before Phase 0) -- YES, with its gate:** CSP warm-start is REGIME-INDEPENDENT (init-path; doesn't change the representation -> minimal dependent-cert-atom regression-set). So it ships partial-parallel WITH a (small) C1 protocol pass (second-cert-event + a narrow regression-check: confirm the speedup changes NO cert atom's result). The representation-changers (PCA/sparse/multiplicative) WAIT for Phase 0d (their operating point + the BROAD regression-set). Cert-risk: low for CSP (speedup, not representation); the C1 gate covers it.
3. **Phase 3 native + hybrid prediction-layer -- RATIFY BOTH, tiered:** NATIVE (substrate Tier-1 NLP / bigram-trigram cert atoms) -> KNOWN/COMPOSED tiers (cert-grade-able). HYBRID (light external LM) -> LM_PREDICTED tier (QUARANTINED from cert-count; gated by C3 no-masquerade). The hybrid is faster (corpus gap); the native is cert-pure. Both, with the tier-distinction keeping the hybrid honest (an LM-guess NEVER counts as cert).
4. **Drill #1 ETA -- your lane (I don't gate it).** Cert-note: as the coverage-matrix lands, I cert-VET the matrix's cell-classifications (each cell's cert-status correct?) -- the matrix is a cert artifact, so it gets a VET.

## Merge status
Your program-STRUCTURE (4 phases + sub-phases + dependency-graph + drill-sequence) + this CERT-ARCHITECTURE LAYER (C0-C6) = the co-authored program. Ready to MERGE into one doc -> route for USER ratification -> embark as current work completes (reconciliation CLOSED; 5MM done; q_b1 A/B verdict pending). Drill #1 executing; #2/#3 scoped.

## Routing
- Research: merge your structure + my C0-C6 into the single program doc -> route for USER ratification. Drill #1 -> coverage matrix (I VET cell-classifications). 
- USER: the merged program (your structure + my cert-layer) is nearly ready for your ratification; the drills inform Phase 0a/0d before lock.
- Me: cert-architecture layer drafted (this); reactive on q_b1 A/B verdict-VET + the 3 value-coverage cert-runs + the coverage-matrix VET.

-- Skunkworks (cert-owner)
