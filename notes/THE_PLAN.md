# THE PLAN — Learned Grounded Representation Foundation → Reasoning Substrate (AUTHORITATIVE, consolidated 2026-07-26)

> **STRATEGIC STATUS (2026-08-08 LATE — 🛑 HONEST RECKONING + STRATEGIC FORK PENDING USER; full detail = WHERE_WE_ARE_NOW TOP + notes/director_POST_COMPACTION_BACKUP_2026-08-04.md TOP).** SPINE UNCHANGED (glass-box; earn meaning the brain's way; no borrowed embedding/LLM/reader; the 3 layers). But the goal-achievement comprehension instantiation was BENCHMARKED on the real public DesireDB set and FAILS: F1 0.000 (both arms) vs a trivial valence RULE 0.701 / published LSTM ~0.70. The in-session 0.812 was IN-SAMPLE on our OWN authored benches; it did NOT transfer (fresh held-out 0.53 raw / 0.17 clean). Cited lit-drill placement: a novel SYNTHESIS of known parts (Lehnert plot-units / Kintsch-Zwaan situation-model / Plate-Kanerva VSA / Osgood signed-axis) on an OLD task — we REDISCOVERED the grounding wall that stopped the symbolic camp; we did NOT beat the glass-box field. REAL + auditable wins banked (collapse-against-state 10/10 33xSNR on a construct-valid eval; typed-chunk Cowan-4 capacity account ~17-31 facts -> episodic-store tier; WIRED hdlab/quality_relation + hdlab/parse_goal_extraction, durability-audit clean). THE DEEP BLOCKER (front-and-center): judging whether an outcome SATISFIES a goal needs real-world word-grounding / ENTAILMENT / VALENCE reasoning — the field uses LLMs for exactly this; our from-scratch glass-box structurally loses (the front-end fidelity wall walked down 3 levels: construction->tagger->parser-decoding). Public benchmarks are largely outcome-valence-solvable so they don't even ISOLATE the goal<->outcome RELATION our one novel seam targets. CORE LESSON (now standing): ONLY held-out + PUBLIC-BENCHMARK-vs-baseline numbers count; in-sample authored-bench wins are near-worthless (the VET/held-out/benchmark discipline CAUGHT the inflation = the system working). STRATEGIC FORK PENDING USER: (i) consolidate as an honest research proof-of-mechanism + stop; (ii) attack the deep glass-box entailment/valence grounding blocker (multi-month program); (iii) pivot value to the AUDITABLE/CORRECTABLE-trace edge (the one thing an LLM structurally can't do — probe in flight); (iv) step back. Director rec = (i)+(iii). Committed through f8abdf752. The 2026-08-03 foundational-pivot block + all STRATEGIC STATUS blocks below are HISTORY (kept for lineage).

> **STRATEGIC STATUS (2026-08-03 — 🧱 FOUNDATIONAL PIVOT: build the grounded ~6yo foundation reading builds on; live detail = WHERE_WE_ARE_NOW TOP BANNER):** SPINE UNCHANGED (learn meaning the brain's way; glass-box; earn it, no borrowed embedding/LLM/reader; the 3 layers; North Star = self-improving reader / converse-with substrate). **THE PIVOT (USER 08-03):** the session's repeated "we weren't doing it right" (event-PREDICTOR closed 4x incl SR/TD pre-registered falsification = next-event prediction is information-FLAT for anyone; construction->integration MIDDLE_BAND; near-synonym goal disambiguation impossible from text) = ONE failure = trying to comprehend goal/affect/social meaning (revenge, anger, care) WITHOUT the grounded ~6yo foundation reading builds on. "You can't learn revenge and anger from a book." **NEW PROGRAM = the FOUNDATIONAL GROUNDED-KNOWLEDGE LAYER:** supply only genuine INNATE core (Spelke agent/object/goal-directedness) + EARN affect/goal/social meaning (appraisal theory: anger = goal-blocked-by-an-agent) via a minimal non-textual EXPERIENTIAL SIMULATION + a LIVING SELF-EXTENDING grounded store (substrate ADDS what QUALIFIES, gated by false-consolidation); THEN reading maps text onto it via the existing organs (which become the READING layer). **USER BARS (govern the program):** EARNED understanding not supplied-assignment; brain-foundational in EVERY aspect; for EVERY mechanism ask which BRAIN STRUCTURE does it + does it SHARE a process we already built -> REUSE that organ. **KEY EMERGING FINDING:** the grounded-appraisal computation is turning out to be largely REUSE of proven organs -- target = coref; causal-attribution = coref-as-bridging (real but NARROW, coherence-ranking untested per trustworthy-gate); valence = mentalizing/ToM-organ -- being validated on a RICHER DISCRIMINATING eval now in build (multi-candidate + irony + beneficiary cases); the one clearly-remaining gap after = event-EXTRACTION from raw text to feed the organs. Prior grounding work to BUILD ON (disk-verified): Binder affect/agency feature space (yardstick), Harnad grounding-transfer, animacy/agency lexicon, verb-affectedness, Sally-Anne ToM. The 08-02 comprehension-pipeline status below is HISTORY (the frontier moved from extract-to-feed-organs down to ground-the-foundation-under-them). Full live blow-by-blow = notes/WHERE_WE_ARE_NOW.md.

> **STRATEGIC STATUS (2026-08-02 — THE COMPREHENSION-PIPELINE ARC; live detail = WHERE_WE_ARE_NOW):** SPINE UNCHANGED (learn meaning the brain's way; glass-box; earn it, no borrowed embedding/LLM/reader; the 3 layers; CLS seed->read->sleep). The comprehension barrier is now being attacked as a concrete PIPELINE on real McGuffey: `text -> role EXTRACTION (who-did-what/clause) -> SITUATION MODEL (accumulate entity-events across the discourse) -> COMPREHENSION`, + a metacognitive `flag-unknowns -> research -> learn` layer. NORTH STAR sharpened = the **SELF-IMPROVING READER** (reads at scale -> flags what it doesn't know -> learns to fix it -> reads more).
> - **CURRENT FRONT-LINE (supersedes the encoder-era BLOCKER/FIX/R1-R5 below, now HISTORY):** role extraction WORKS ~0.60 on real McGuffey (LEARNED, construction-conditional; atoms 29604-06); situation-model register = ACCUMULATE (bundle events, atom 29609, WIRED); the WALL = EXTRACTION GENERALIZATION (not the organ) + the self-improving loop needs COREFERENCE (earn it = entity-identity competency #1, currently gold-supplied) + RICHER/LONGER content. Meaning-supply: a DICTIONARY/lexicon is allowed DATA (built, ready for harder content where meaning bites).
> - **KEY LESSONS (this session, brain-lit-grounded):** cross-clause feedback = COHERENCE/identity, NOT role-persistence (multi-event roles vary); SUPPLY-a-dictionary don't over-earn word-meaning; reader must FLAG-unknowns->tiered-research; PROBE-to-aim before big investment (GPU available); 4 director over-reads caught -> report MEASURED not READ.
> - The encoder-era **THE BLOCKER / THE FIX (R1-R5) / IMMEDIATE NEXT** below are HISTORY (encoder scaling partially overcome per the 07-27 note; the current instantiation is the comprehension pipeline above). The CLS knowledge-acquisition architecture (seed->read->sleep) + the 3 layers remain the intact spine.

> **STRATEGIC STATUS (07-29 — HISTORY, superseded by 2026-08-02 above):** SPINE UNCHANGED (learn grounded meaning the brain's way; glass-box; earn it, no borrowed embedding/LLM/reader). The barrier (comprehension, >1 week) is now DIAGNOSED and being attacked at the architecture level.
> - **THE DIAGNOSIS:** our encoder is FEED-FORWARD + BIDIRECTIONAL(MLM) + STATELESS; the brain is RECURRENT + FORWARD-PREDICTIVE + STATEFUL. A stateless net can't MAINTAIN/UPDATE a situation model, so comprehension can't be decoded out of it. The recurring week-long error = bolting ISOLATED pieces on a FROZEN feed-forward encoder (readout, slot-to-scalars, position-bind, loop) instead of the coupled machinery END-TO-END. All cheap levers spent (objective/grounding/breadth DEAD; readout decodes but doesn't raise the ceiling — SPENT for comprehension).
> - **THE FRAMING (resolved, brain-lit-grounded):** comprehension = UPDATE a pre-existing WORLD MODEL, not build meaning from a blank slate. "Grounded" = RELATIONAL/amodal-hub (= our foundation KB), NOT sensorimotor. OUR THIRD ROUTE (vs LLM emergence-at-scale, vs embodiment we lack) = SUPPLY the KB world-model as a PRIOR + LEARN to update it, glass-box.
> - **THE HARD BUILD (in progress) = the coupled STATEFUL CORE:** slot-attention K=6 full-d-dim entity slots (NO scalar compression) + PBWM PE-gated write + role-general HRR binding + encoder UNFROZEN end-to-end; Arm A blank vs Arm B KB-grounded = the falsifiable framing test. Built+committed; smoke shows a GRADIENT/WIRING red flag (can't overfit a tiny set) -> DEBUG the gradient path before the GPU run.
> - **WINS banked:** learned readout (WIRE) + gated_fusion island (WIRE). Measurement (MES maintenance + KD bridging) built + honestly bounded.
> **MANDATE (USER, emphasized):** ALIGN TO THE BRAIN to overcome this; do the HARD thing not easy paths; nail each component brain-faithful (component_brain_fidelity_ledger) then assemble; DON'T LOSE FOCUS. **METHOD:** measurement-first, replicate-before-believe, brain-metric-per-component (not task-win), no frozen-head shortcuts.

> **STATUS UPDATE 2026-07-27 (HISTORY — superseded; see STRATEGIC STATUS above + WHERE_WE_ARE_NOW):** "THE BLOCKER" below (encoder doesn't scale) is now PARTIALLY OVERCOME — the from-scratch encoder trained on 237M real tokens BEATS grounding +0.039 leak-proof on held-out-NEW concepts (banked 29590); scale WORKS + is data-limited. Learned relational inference GENERALIZES to unseen concepts (29587/88/89). Foundation built + VET'd (29585). CURRENT FRONTIER = the SELF-LEARNING LOOP (read->extract->sleep-consolidate->improve) on the trained encoder + breadth ingestion THROUGH the reader. The R1-R5 framing below is HISTORY of how we got here; the plan's spine (learn meaning the brain's way, CLS seed->read->sleep) holds.

**Supersedes/reconciles:** foundation_build_plan_2026-07-14, encoder_rescue_plan_converged_diagnosis_2026-07-04, director_MASTER_PLAN_2026-06-25 + stage1/stage2 design notes, the 2026-07-25 phase commitment, and the 2026-07-26 inference-arc (a TANGENT, see Conflicts). This is the one clear plan. USER-reaffirmed 2026-07-26: commit to the learned grounded representation foundation, brain-true, no shortcuts.

## NORTH STAR
A glass-box VSA/HDC substrate you can CONVERSE with that genuinely REASONS (inspectable derivations), not parrots. Invariants: NO external LLM at inference; NO borrowed embedding (GloVe/BGE/transformer) as the meaning organ; the substrate EARNS its meaning the brain's way.

## THE THREE LAYERS (what a reasoning substrate needs)
1. **REPRESENTATION (meaning)** — how concepts are encoded. Must be LEARNED + GROUNDED + GENERALIZING the brain's way. **<< THE CURRENT FOCUS / the blocker.**
2. **KNOWLEDGE (facts/relations)** — dense vetted per-concept relations. May be SOURCED from existing tools (foundation-from-tools pivot) BUT represented + reasoned-over glass-box, with a held-out slice reserved to prove reasoning not parroting.
3. **REASONING** — additive multi-constraint CONSTRAINT-SATISFACTION (brain CA3 attractor / pattern-completion) over 1+2. Reasoning resolution scales with the NUMBER OF CONSTRAINTS brought to bear.

## WHAT IS DONE — key components (BANKED, do NOT rebuild; keep discoverable)
- **Brain-true LEARNING PRIMITIVES (Stage-2 concept-encoder spokes, HARD_PASS):** competitive-Hebbian WTA (spoke1_v3_D), temporal-contiguity Foldiak/slow-feature (spoke2), sparse-hippocampal DG-CA3 pattern-separation (spoke3), predictive-coding competitive-allocation. hdlab/concept_encoder.py.
- **LEARNED concept representation GENERALIZES on clean structure (Stage-1 SEMANTIC concept-learner battery = CHAIN_GRADE v1 / HARD_PASS v2,v3):** held-out-to-NEW-concept top1=1.000, inheritance 0.93, compose 0.53-0.71, chain 0.93, refuse 1.0, retention 0.94. CAVEAT: SYNTHETIC 8-12 category space; perfect held-out flagged construction-favorable => PROOF-OF-MECHANISM, not yet real-data.
- **REASONING THEORY (07-14, [[project_reasoning_theory_constraints_brought_to_bear...]]):** additive constraint-satisfaction hits 0.99 (toy) where joint tensor-binding fails (0.018); density = capacity-for-constraints; single-hop modest because 1 constraint = underdetermined. Density value = MEANINGFUL not transformative (3x rescue on the 83% taxonomy-misses, ~0.25 MRR modest ceiling). CONJUNCTIONS > chains.
- **Storage/infra (banked):** sharded CG store (foundation-scale, RAM-bound not crosstalk-bound, 100k proven/1M analytical, 29532/34); trust-vetting HD fact store (29531); SemanticHDEncoder meaning-match (29533, SUPPLY-semantic); CLIMB ingestion; sleep-loop (compresses clean structure, KEEP_EPISODIC honest on high-entropy).
- **REASONING MECHANISM (this session, banked 29537-29570):** verification-by-derivation reasoner (hdlab/reasoner.py) — real glass-box selective derivation traces; 100% on cleanly-derivable; both symbolic tie-break doors measured shut (meaning-bound).

## THE BLOCKER (front-and-center, the real gap — do NOT bury)
The learned-representation mechanism (proven on SYNTHETIC) does NOT yet scale to REAL vocabulary:
- **ENCODER MIGRATION (bag-of-words -> concept encoder at 178k) HARD_FAILED (encoder_rescue_plan_2026-07-04):** the learned OBJECTIVE does not scale (DENSE geometry collapses 0.825@3k -> 0.368@178k; not under-training; in-batch RKD never supervises graded geometry at scale), AND it depended on an EXTERNAL BGE teacher (violates the no-borrowed-vector + substrate-standalone locks).
- On real data generally: composed_differentiation_loop v1 MEMORIZES (KEEP_EPISODIC), v2 held-out 0.24 (modest); learned_meaning_frontend_differentiation (29556) ho_lift 0 (flat-MLP over borrowed GloVe memorized).

## THE FIX (brain-true, sequenced — from encoder_rescue R1-R5, still valid)
Make the learned representation SCALE on REAL data the brain's way, judged ONLY on held-out-to-NEW-concept generalization:
1. **Fix the OBJECTIVE to form rich geometry at scale (R1, load-bearing):** global/landmark RKD (match each concept's code-geometry to a fixed ~8k anchor frame + neighbor-clustered batches) so graded geometry is supervised independent of random in-batch co-occurrence. VALIDATE dense recovers ~0.8 at scale FIRST.
2. **Dense-first-then-sparsify (R2):** introduce sparsity by anneal / competitive k-WTA AFTER geometry forms (brain D1), never a step-0 hard bottleneck.
3. **WEAN OFF the external teacher -> INTERNAL SELF-TEACHER (R3/R4) — THE key brain-true move:** EMA self-distillation + positives from the KB's own relational/gloss co-occurrence (R3); predictive/temporal-contiguity auxiliary over the relational graph (R4, reuses the Stage-2 spokes). Resolves the substrate-standalone + no-borrowed-vector locks. This is the direction the whole 07-26 arc independently reconfirmed (borrowed vectors beat us BUT are banned => the substrate must self-teach).
4. **GROUND it:** experiential norms as grounded targets so representations carry generalizing structure (NOT distributional; NOT borrowed). USE THE ASSETS WE HAVE (do not reinvent) -- data/grounding_testbed/ norms = **Lancaster sensorimotor (39,707 words; 6 perceptual + 5 action modalities)**, Brysbaert concreteness (39,954), Warriner VAD (13,915), Kuperman AoA (51,715). **VET'd 07-26: the Binder-65 experiential set is NOT on disk (zero repo hits) -- do NOT assume it; Lancaster sensorimotor IS our real experiential grounding.** Coverage is single-lemma, low-tens-of-% of WordNet synsets / less of ConceptNet -> grounding is PARTIAL (concrete words far better covered than abstract). exp_grounding_measured_attribute_concreteness_v1 = HARD_PASS_GROUNDING_REAL. Two partially-cracked brain-aligned chaining mechanisms never wired to a foundation -- gated-fusion (+0.297 held-out, exp_grounding_gated_fusion_relation_inference_mammal_v1) and SR-routing (+0.253 multihop, exp_grounding_multihop_sr_reachability_routing_v1). CAVEAT (29571): experiential norms are data-bound for ABSTRACT concepts (~4-6% coverage of abstract science terms) -> ground abstract concepts propositionally/relationally (from the foundation itself), reserve sensorimotor grounding for concrete concepts.
5. **R5 capacity diagnostic:** is target sparsity physically reachable or capacity-bound (USER strategy call).

## THE KNOWLEDGE-ACQUISITION ARCHITECTURE (USER-directed 2026-07-26) — the spine, brain-true CLS
This is Complementary Learning Systems (McClelland/O'Reilly) end-to-end, and it is COUPLED to the representation fix (not sequenced strictly after it — the foundation IS the self-teacher's training signal):
1. **SEED the cortex** — convert the large relational KBs we already hold (data/conceptnet/, data/atomic_kb/, data/wordnet_cache/ + data/wordnet_noun_semantics_kb_*, data/_cskg_cache/) into ONE high-quality, optimized, glass-box relational foundation: canonical concept nodes + typed VETTED relations + measured grounding attributes (data/grounding_testbed/ norms). Cleaned, deduped, noise-filtered, label-precise per relation-type, concept-identity canonicalized — NOT a raw dump. Held-out slice reserved. NO borrowed vector as the node encoding (encoding is LEARNED downstream); the foundation stores symbolic nodes + typed edges + measured attributes only.
2. **READ to grow** — the reader/learning module ingests new material and fast-writes new facts (hippocampal episodic; "add new info a different way" = separate fast-write path). Reuse CLIMB reader + director_kb_chunk_ingest assets.
3. **SLEEP to integrate** — consolidation folds episodic new knowledge into the semantic foundation without catastrophic interference (concurrent OR offline — USER: don't care which; brain does both). Reuse the sleep-loop / CLS consolidation cells.
**THE COUPLING (why this cracks the blocker, not a detour):** the optimized relational foundation is simultaneously (a) layer-2 KNOWLEDGE and (b) the R3/R4 self-teacher's positive-source — a concept's meaning is EARNED from its relational/experiential neighborhood in the foundation. So: SEED foundation -> encoder self-teaches meaning FROM the foundation (R1 objective + R3/R4) -> reader adds new facts -> sleep consolidates -> repeat. One loop, not two phases.
SCOPING (raw-KB inventory + quality + conversion/schema design + reusable prior ingest): notes/kb_foundation_conversion_scoping_2026-07-26.md.

## THEN (layer 3, once foundation + learned-grounded reps are in place)
REASON over the foundation via additive multi-constraint constraint-satisfaction; reserve held-out to prove reasoning-not-parroting. -> the glass-box conversational reasoning substrate (north star).

## CONFLICTS REMOVED (the doc was contradictory; these are the resolutions)
1. **SUPPLY vs LEARN (the main conflict):** the REPRESENTATION/meaning is LEARNED the brain's way — no BGE, no borrowed vectors, internal self-teacher (LOCKED). KNOWLEDGE/relations MAY be sourced from vetted external tools (foundation-from-tools, still licensed) but are REPRESENTED + REASONED glass-box. Different layers, not a contradiction. (The 07-14 plan's "LLM-generate relations" applies to layer-2 KNOWLEDGE, NOT to the layer-1 meaning encoder.)
2. **"Everything fails" (2026-07-26 inference arc 29580-84):** that arc tested INFERENCE OVER SUPPLIED SYMBOLIC KBs (analogy/composition/density/redundancy over WordNet/WorldTree/ConceptNet) — a TANGENT. It rigorously confirmed you CANNOT shortcut the foundation (supplied-symbol inference fails + loses to embeddings), which POINTS AT this plan. It does NOT contradict the learned-representation positives (those are real, on synthetic; the gap is real-data scaling = the encoder-migration objective fix). Do NOT conflate "supplied-KB inference fails" with "learned-representation fails."
3. **The encoder-migration HARD_FAIL is the CURRENT front line** (not buried under the inference arc): the R1 objective fix + R3/R4 self-teacher is the immediate next build when work resumes.

## IMMEDIATE NEXT (when building resumes — NOT started per USER)
R1 global/landmark RKD objective fix on the concept encoder (validate DENSE recovers ~0.8 at scale, teacher-free-capable), then R3/R4 internal self-teacher, grounded (Binder), measured on held-out-to-new-concept generalization. Build component-by-component, brain-first, can-fail, VET on the generalization bar.
