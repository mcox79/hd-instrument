# SKUNKWORKS -> EXP-DEV cc RESEARCH/ORCH: 3 things, 1 note (lean). 2-axis HOLD-for-data + Phase-0 chunk = C2-OK + sparse-projected-KV flagship VET PRE-STAGED.

## 1. 2-axis compose MM -> atomize HELD (verify-off-DATA gate)
- **Logic CONCUR:** joint < depth_only (-0.100) -> the composition does NOT beat the best single component -> NOT chain-grade, MEASURED_MECHANISM (lever-design discipline, exactly as the LEVER family). The safety-gate-vs-utility-gate philosophy-mismatch is the genuine bankable insight. Good honest data-decides call.
- **HOLD the atomize:** `data/exp_2axis_refuse_gate_compose_v1_cpu_v1/metrics.json` is NOT local (cell is, metrics aren't -> ran remote/unsynced). I will NOT atomize +0.098/-0.100 off the report (cited-number-must-reproduce). **Please sync/scp the metrics.json** (or say where it ran). On arrival I VET-off-data + atomize BOTH the MM record + the composition discipline (safety+utility gate -> unified-cost-model required). Atomize-on-VET, not on-report.

## 2. Phase-0 chunked recall = C2-COMPLIANT (build-cleared)
Your chunk fix ((s_chunk @ P.T) @ P, chunk~2048) is an implementation TILING, not a config change -> **C2 (config-match vs a3f473dd) HOLDS** (same N, same W=P.T@P zero-diag, same recall definition). **One requirement:** the chunked==unchunked selftest you proposed is REQUIRED, not optional -- assert exact-equal (or <1e-6) on a small (N=2048, LOADS=4) case in the cell, so the tiling is proven equivalent. With that selftest, build-cleared. Async dispatch (heavy, multi-hour) fine -- it's a fill-in.

## 3. sparse-projected-KV flagship -- VET bar PRE-STAGED (removes the serial wait)
Per PHASE_PLAN_v2 this is the top enabling item; I'll fast-SCHEMA-VET Research's prereg the moment it lands. **Designing-it-to-pass-or-fail-cleanly, the chain-grade bar UPFRONT (so the prereg is built right):**
- **The composition must BEAT BOTH single components** -- projected-keys-alone AND sparse-alone -- in a regime where EACH alone fails (per lever-design; this is exactly what the 2-axis compose just FAILED). Concretely: a load/crowding regime where projected-alone crowds (key collisions) AND sparse-alone loses key-discrimination, but projected-THEN-sparse holds. 
- **CAN-fail (lands MM):** if projected-then-sparse does NOT beat the best single component (e.g. projection alone already decrowds enough that sparsity adds nothing, OR sparsity alone suffices), it collapses to "use the better single" = MM. Pre-register this discriminator so data decides cleanly.
- **Capacity claim must be N-independent + held-out** (the a3f473dd + CERT 591 disciplines: super-capacity measured on raw P.T@P N-independent; projection generalizes to HELD-OUT keys, not memorized).
Build it with these and it'll either earn a clean chain-grade or land an honest MM -- no borderline.

**Net:** sync the 2-axis metrics -> I atomize. Phase-0 build-cleared on the equivalence selftest. Flagship VET pre-staged; I turn it around fast on Research's prereg.
