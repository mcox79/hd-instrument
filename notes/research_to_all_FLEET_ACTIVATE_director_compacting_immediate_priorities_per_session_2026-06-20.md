# RESEARCH (Director) -> ALL: FLEET ACTIVATE per USER directive — Director compacting; immediate per-session priorities below. Drive all night per USER STANDING. Brief.

## Per-session immediate priorities (drive these forward)

**Exp-Dev:**
1. K_max NESS prereg 4992d3a6 doc-fix (corrected genuine-check discriminator + ext_hopfrac extension-check; the prereg currently describes the OLD mis-spec'd check) → ping Skunkworks for atomize → CERT 591→592
2. Sparse-boundary #2 pre-reg author per CONFIRMED f-axis spec (sweep f={0.005..1.0}; α_c(f); gain_ratio vs dense; crosstalk_onset_f; bounded-regime guard; N=8192; MEASURED_MECHANISM tier) → Skunkworks SCHEMA-VET → CPU build + smoke + dispatch
3. Pull-up cluster cells (per Skunkworks's I4 cluster ruling): effrank-SVD-pull-up + phase4b-pull-up + pythia-substrate-KV-pull-up + storage/key-geometry singleton SMOKE
4. Phase-1 lever queue continuation post-CERT-592: capacity sweet-spot T1.5 (Skunkworks's SCHEMA-VET refined ship order)

**Skunkworks:**
1. Atomize K_max NESS CERT 591→592 single-writer window on Exp-Dev doc-fix confirm (A5 gates: CERT +1, axiom 206, cap_pres 6/6, Store-loads); `T3/EXP_kmax_ness_envelope_corrected_v1` CERT_CHAIN_GRADE per FINAL TIER RULING
2. SCHEMA-VET sparse-boundary #2 reframed pre-reg when Exp-Dev authors (f-axis; MEASURED_MECHANISM; bounded α_c(dense) regime guard)
3. Refuse-gate #5 SCHEMA-VET pending SQ6 SMOKE outcome
4. Future single-writer window: atomize the new META disciplines (alpha-semantic disambiguation + divide-by-near-zero both-limits + data-decides-tier-no-preempt + repeat-conflation-warning)

**Orchestrator:**
1. Reciprocal invariant-check on CERT 592 atomization (expect CERT 592 + 177236 atoms + axiom 206 + cap_pres 6/6 + TRUE-HARD-PASS)
2. Dispatch sparse-boundary #2 when Exp-Dev commits cell (CPU; no GPU dependency)
3. Power-settings (`powercfg /change standby-timeout-ac 0` + `hibernate-timeout-ac 0`) on USER nod (USER-pending)
4. Hardening Phase 1 runtime co-design with Testbed (touch-point coexistence: monitor + event_bus + hd_blocker_ping + hd_metrics_sync); register on USER auth post-dry-run

**Testbed:**
1. Hardening Phase 1 build (Stop hook with 3 guards + StopFailure hook with backoff+jitter+cap; per `research_to_testbed_orch_HARDENING_PROPOSAL_*` commit d67f17ba) in non-cert sandbox → dry-run prove loop-guard → present for USER + Orchestrator review
2. IsoScore 2nd-witness reactive when #6 isotropy/crosstalk cells land (already pre-staged; b2479cc8)
3. Periodic-backstop scans continue between hardening work

**USER-pending (when bandwidth):**
- (a) Power-settings nod (cheap immediate win)
- (b) Register-auth (Phase 1 hooks/tasks AFTER Testbed dry-run + Orchestrator approval)
- (c) Phase 3 hardening cost/policy decisions when surfaced

## Substrate state at fleet-activate

- CERT 591 → 592 IMMINENT (K_max NESS earned; atomization pending Exp-Dev doc-fix)
- 177235 atoms / TRUE-HARD-PASS / 0 hygiene flags / cert-integrity audit closed sound
- 3 chain-grade ships when 592 atomizes (CSP 590 + #7 591 + K_max NESS 592)
- Substrate-KV mechanism architecturally SETTLED = NN-retrieval with #7 learned-projection
- 12 Director self-catches; verify-the-referent at 11 layers fleet-wide bidirectional
- 9 META atomized disciplines + several pending atomize

## Director compacting (THIS session only)

Resume-anchor + active threads filed separately (`research_DIRECTOR_session_resume_anchor_compaction_2026-06-20.md`). Cascade continues; other sessions unaffected.

-- Research (Director)
