# Research decisions log 2026-07-04

- 2026-07-04 (UTC) — cortex-2 Phase 2 advisory→enforcement architecture drill delivered → `notes/research_drill_cortex_2_phase_2_advisory_to_enforcement_architecture_2026-07-04.md`. HEADLINE: nonce+null-arm two-part discriminator + per-atom SHADOW/WARN/LIVE flag; MM_TENTATIVE tier; P_deflated=0.42; HARD-FAIL nonce-consumption<0.50 OR match-and-honored-and-effect<0.20.
- 2026-07-04 (UTC) — 2x drill: teacher-free semantic bootstrapping from sparse KB (R3 viability) delivered → `notes/research_drill_teacher_free_semantic_bootstrapping_from_sparse_kb_2026-07-04.md`. HEADLINE: NOT viable now at ~1.6 atoms/entity (below every tested floor across KGE, GNN-SSL, random-walk, self-distillation, and classical distributional semantics literature); classical-distributional threshold (~100-400 atoms/entity, 60-250x current density) is the sharpest quantified gate. Corrected pre-supplied WN18RR degree lead (~2.19 did not check out; actual ~4.24-4.5, mode 2). Verdict: grow-into-it foundational bet, gated behind a 2-stage milestone ladder (Stage 1 ~5-10/entity, Stage 2 ~100-400/entity); reinforces D1-now/R3-later sequencing from the brain-grounded drill. P_deflated(NOW) ranges 0.05-0.25 across 6 methods; P_deflated(AFTER milestone) capped 0.15-0.45.
- research_drill_sparse_code_semantic_fidelity_frontier_2026-07-04.md -- FIDELITY frontier: 2%-active NOT the binding constraint on 0.85; gated by dense objective (R1) not sparsifier (R5). P_deflated(main)=0.55. Load-bearing anchor: Morelli CLIP-SAE 4x/0.78%/FVU0.22 spot-verified.
- 2026-07-04 (UTC) -- brain-grounded CONTINUAL self-improving encoder drill delivered -> `notes/research_drill_brain_grounded_continual_self_improving_encoder_2026-07-04.md`. HEADLINE: R1's fixed-landmark objective already IS the mechanism (Shen et al. 2020 Backward-Compatible Representation Learning / BCT) that lets an embedding model keep improving without invalidating a previously-built index; brain's "stable readout despite drift" requires PERIODIC re-alignment, not passive stability (piriform decoder->chance by day 32 under a static decoder; stable-manifold+realignment, Gallego 2020/Degenhart 2020, is the best-supported bio mechanism). Recommendation NOW=nothing new (R1 already is it) / NEXT=cheap decisive test (free, reuses existing R1 checkpoints -- cross-checkpoint retrieval compatibility probe) then a BCT-style compatibility-loss smoke once R1 clears bar / LATER=R3/R4 self-teacher unchanged, gated on 60-250x corpus density (controlled EMA-with-teacher-present sidesteps that wall definitionally; online-vs-batch cadence choice has no lit support for added benefit at current scale, P_deflated=0.25). HARD-FAIL: cross-checkpoint retrieval <50% of same-checkpoint on DENSE or BLOCK -> pull compatibility work forward to NOW. P_deflated(anchor mechanism)=0.45.
- 2026-07-04 (UTC) -- RANKED-LEVERS drill (DENSE 0.52 MID -> 0.85 target) delivered -> `notes/research_drill_encoder_052_to_085_ranked_levers_2026-07-04.md`. HEADLINE: top lever (NCE/contrastive-term weight schedule, anneal-off once relational loss plateaus) is ALREADY running inside v3b's NCE-ablation arm -- 3 convergent 2025/2026 retrieval-distillation papers (LEAF; "Conventional Contrastive Learning Often Falls Short" arXiv:2505.19274; "Beyond Hard Negatives" arXiv:2604.04734) plus alignment/uniformity theory (Wang & Isola 2020) match our own peak-then-decline loss telemetry almost exactly, including one cited relational-loss COLLAPSE (MRR 0.006->0.307) traced to hard-negative-mining imbalance. Landmark selection method/size (Rank 4) and structural/STE (Rank 6) are deprioritized -- Nystrom leverage-score theory says our 4-8k landmark budget is already past the point smarter selection helps; sparsity already ruled out by the prior fidelity-frontier drill. Student capacity (Rank 5) is a genuine literature gap, gated behind a free in-sample-vs-held-out diagnostic (Rank 3) rather than guessed at. Decision table included: maps each v3b primary-tier x secondary-tier outcome combo to the single next dispatch. P_deflated(top lever)=0.55; full per-rank P table in the note.
- 2026-07-04 (UTC) -- 2x drill: v3b batch-ratio-match HARD_FAIL negative understood -> `notes/research_drill_2x_batch_ratio_match_negative_understanding_2026-07-04.md`. HEADLINE: sweep null result explained by TWO converging causes, both lit-confirmed (4 parallel Sonnet lit-scans): (1) code read confirms near-neighbor mining (`_mine_teacher`) runs ONCE globally, decoupling NCE-term near-neighbor gradient from batch size (ANCE/MoCo/CompRess lineage supports this), while NCE weight was held constant across the whole sweep, so the sweep's own antagonism confound (which itself scales with batch-driven negative count, per Wang&Isola/Wu-IJCAI2022) swamped any true coverage-ratio signal; (2) raw cardinality (V=3k smoke -> V=40k MID) drop (0.825->~0.4) occurred even at MORE generous ratio than FULL's collapse point, pointing at a scale-of-VOCAB ceiling independent of ratio (generalized neural collapse, LIMIT sign-rank paper). Sharpens (does not contradict) the batch/N-ratio memory rule: ratio-matching is necessary but NOT sufficient -- must also neutralize co-active confounds and check for absolute-scale (non-ratio) drivers. Q2 (landmark hypothesis): CONDITIONAL DROP pending one free MID arm (`in_batch`+NCE_ZERO@batch=128, never run) -- current confound-tainted evidence favors `in_batch` at every point; do NOT spend the FULL-scale GPU dispatch on the landmark objective yet. P_deflated(confound explanation)=0.50; P_deflated(cardinality ceiling)=0.45; P_deflated(landmark still adds value)=0.35.

- 2026-07-04T18:36:12Z Cardinality/capacity ceiling for 0.85 reachability -> notes/research_drill_encoder_cardinality_capacity_ceiling_0.85_reachability_2026-07-04.md (P_deflated: reachable-via-structural-lever=0.45, NCE-fix-alone-sufficient=0.20, hard-unfixable-ceiling=0.15-0.20; top lever = rank-aware/anisotropic quantization loss, zero structural cost; cheapest diagnostic = teacher-to-sparsifier direct bypass, zero training)
- 2026-07-04 (UTC) -- METRIC-DEFINITION drill: is the 0.85 goal coarse-cosine or fine-retrieval, and how close are we? -> `notes/research_drill_encoder_target_metric_coarse_cosine_vs_fine_retrieval_2026-07-04.md`. HEADLINE: archaeology (PROGRESS.md + USER-confirmed goals memory + v3e docstring VET finding) confirms the literal 0.85 target IS `hi80_cos` (cosine-to-gold on teacher-cosine>=0.80 pairs) -- NEARLY CLOSED (0.832 final-step, 0.857 best-checkpoint, same run). `ret_agree10` (retrieval-agreement@10) was never formally targeted but is the metric that actually answers "does it return the RIGHT memory" -- WIDE OPEN at 0.21. On-disk zero-training bypass diagnostic (`exp_encoder_teacher_sparsifier_bypass_v1`, true 177,899-concept cache) shows the K=128 code's own ceiling is ~0.478 (not ~1.0, so real code-resolution cost exists) and K=256 raises it to ~0.580 (+~0.10, real but second-order) -- but the trained student's actual 0.211 is under half of the CURRENT K=128 ceiling, so the dominant, more urgent gap is student/objective-bound (same root cause as the DENSE collapse; R1 already targets it), not code-width. Composition/algebra gate (keyed_roundtrip) is passing (1.000) in the same run -- composing with the USER-locked runtime-phase-diagram-regime-switching directive, recommends a two-tier retrieval architecture (coarse sparse/algebra code + a separate fine-rerank mechanism on a shortlist) rather than forcing one code to do both. No exp_dev hand-off filed (interpretive/definitional; feeds the reading of the in-flight R1 result). P_deflated(archaeology)=0.90; P_deflated(dual-attribution)=0.65; P_deflated(two-tier architecture recommendation)=0.50.
