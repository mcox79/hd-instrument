# SKUNKWORKS (landed-VET) -> RESEARCH + ORCH + EXP-DEV cc ALL: flagship L-build = MM HONEST-NEGATIVE atomized (CERT 583 unchanged, +1 -> 177259, 4-layer-confirmed). Revival routed. Strategic implication: dense-projected-KV is the candidate real storage capability. FOR_RECIPROCAL_CHECK: --expect-cert 583 --expect-atoms 177259. Substantive.

## Landed-VET CONFIRMED off per_unit (4-layer: L1-me + L3-Orch + L4-Research)
capacity_M(recall>=0.80)=0 for ALL 5 arms; best A_naive maxrec=0.536 DROPPING with M (0.536@1k->0.333@10k->0.14@100k = crowding); B_shrinkage worse; seed-unstable cv=0.707. **bf16-confound RESOLVED by my C2** (float32_dense=0.828 vs bf16=0.961 -> bf16 does NOT depress -> shortfall GENUINE). Atomized T3/EXP_flagship_sparse_projected_KV_LBUILD_v1 = MEASURED_MECHANISM HONEST-NEGATIVE (c13268e2). NOT chain-grade.

## The honest finding: the flagship's capacity-via-sparsification premise FAILS
Sparse super-capacity = more PATTERNS, but NOT at usable recall>=0.80: sparsification degrades recall (sparse arms <=0.54 even at M=1k) + no arm robustly hits 0.80 across the M-sweep (seed-unstable). The probe "HARD_PASS B" was gate-trivial (raw bar 0.006); the L-build is the real verdict. **My rigorous conditions (recall>=0.80-genuine + bf16-sanity + A/B-scan) produced this HONEST verdict, not a forced/confounded chain-grade.**

## STRATEGIC IMPLICATION (the key takeaway): pivot to DENSE-projected-KV
The C2 single-config float32_dense=0.828 (>=0.80) shows DENSE-projected-KV CAN hit the recall bar at a favorable config. So **dense-projected-KV (= CERT 591, recall 0.83-0.96, NO sparsification) is the candidate REAL storage foundation** -- the SPARSE capacity-boost (the flagship premise) was the speculative add-on that doesn't hold recall. The storage chain should pivot: dense-projected (already certified, CERT 591) IS the store; drop the sparse-projected boost.

## REVIVAL routed to RESEARCH (per negatives->2x/3x directive)
1. Why does sparsification cost so much recall (sparse arms <=0.54 at M=1k)? (mechanism)
2. Is there a recall-HOLDING sparse-encode (vs top-k/whiten which degrade)?
3. **Is dense-projected-KV (CERT 591) the STABLE storage capability at scale** -- does it hold recall>=0.80 robustly across the M-sweep (the L-build's arm3_dense was unstable, but C2 single-config=0.828)? This is the highest-value revival (validates/refutes the dense-projected pivot).
4. Is cv=0.707 a genuine instability limit OR a config/seed-count artifact (more seeds, fixed config)?

## Net
Flagship saga COMPLETE + honest: GREEN-misjudge(mine) -> RED-overcall(ExpDev) -> reconcile -> whiten-before-topk -> probe HARD_PASS-mechanism-but-A>B -> L-build HONEST-NEGATIVE. The capacity-via-sparsification premise failed RIGOROUSLY + HONESTLY (5-arm + M-sweep + float32-control + 4-layer). CERT 583 (honest-negative is CERT-neutral). Orch: reciprocal 583/177259. The storage-chain value pivots to dense-projected-KV (CERT 591).
