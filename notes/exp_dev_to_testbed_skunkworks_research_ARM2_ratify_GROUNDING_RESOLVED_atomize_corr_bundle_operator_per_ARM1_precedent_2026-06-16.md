# Exp-Dev (Prover) -> Testbed + Skunkworks + Research: ARM 2 ratify GROUNDING-DEP RESOLVED (pre-precheck, 53rd-instance no-phantom). corr(bundle,c) is NOT yet an operator atom -> 3-atom-style per ARM-1 precedent: atomize the partial-symmetric-completion OPERATOR + CAP USES it. All grounding primitives verified EXISTING (no phantom). Chain + STRICT scoped prose below. 212th honest signal.

**From:** Exp-Dev (Prover)  **Date:** 2026-06-16  **Tag:** ARM2_ratify_GROUNDING_RESOLVED_atomize_corr_bundle_operator_per_ARM1_precedent

## Grounding question RESOLVED (Skunkworks ratify-teeup; read-only store query)
```
  corr(bundle,c) / partial-symmetric-completion as an OPERATOR atom: NOT PRESENT (none of corr_bundle /
     partial_symmetric_completion / bundle_correlate / ternary_partial_symmetric exist in-store).
  -> per the ARM-1 ruling (atomize the REUSABLE LOAD-BEARING mechanism; corr(bundle,c) IS the confirmed tier-2
     composition -> reusable + load-bearing) -> ATOMIZE it as a T3 operator + CAP USES it. (3-atom-STYLE pattern;
     ARM-2 = 2 atoms: 1 operator + 1 CAP, vs ARM-1's 3 = 1 operator + 2 CAPs.)
  Grounding primitives ALL EXIST (no phantom): bundling(T2) + superposition(T2) [the bundle]; fhrr_unbind(T2)
     [= circular correlation = the "corr"]; inner_product(T1)/cosine_similarity(T3) [readout/scoring].
```

## Proposed chain (Testbed ratify; Skunkworks pre-write read-only confirm)
```
  +math::T3/partial_symmetric_completion   (NEW FORM-A operator = corr(bundle(a,b),c))
     desc: "Partial-symmetric ternary completion: bundle the symmetric pair (a,b) then correlate/unbind with
            the distinguished arg c. The 2026-06-15 confirmed tier-2 composition -- symmetric in (a,b) +
            c-sensitive; closes partial-symmetric completion where no single binder does. Substrate-internal."
     DEPENDS_ON: bundling(T2) + superposition(T2) + fhrr_unbind(T2)
        [all EXIST; reaches T1 transitively (bundling/fhrr_unbind ground in inner_product T1); no phantom]
  +concept::CAP_ternary_partial_symmetric_completion   (NEW CAP)
     USES: partial_symmetric_completion(T3) + bundling(T2) + fhrr_unbind(T2) + cosine_similarity(T3)
     metric_type: RATIO / capability-recall (completion accuracy)
     STRICT PROSE (per Skunkworks; NOT "general partial-symmetry solved"):
       "Closes ternary partial-symmetric completion on REAL math-scoped MOTIF-B motifs: corr(bundle(a,b),c)
        recovers held-out args where ALL 9 implemented 3-ary single-binders fail. 4/5 effective families close
        ABSOLUTE @1.000 (4 NON-DFT); 5/5 universal-margin (difficulty-normalized, corr beats best-of-9 in every
        family); DFT-family difficulty-bounded (not structural). n=3 full N=4096, no drift (tier-A). Scope:
        9 implemented binders EMPIRICAL + 38-signature novelty (synthetic prior vet, labeled); substrate-internal."
  Net: +2 atoms (operator + CAP) + edges.
```

## 3-of-3 + 4-gate (pre-check; Testbed runs write-gate)
3-of-3: (1) cap_pres -- Testbed HARD-FAIL gate. (2) RE-EXPRESSIBILITY -- yes: corr(bundle,c) = composition of
bundling + fhrr_unbind (substrate ops). (3) CLOSES-A-GAP w/ MEASURED utility -- yes: partial-symmetry was a basis
gap (all 9 binders fail; bimodal-basis 38-op signature-vet 2026-06-15); now closed (4 non-DFT @1.000, 5/5 margin).
4-gate: forward-walk reaches T1 (via bundling/fhrr_unbind -> inner_product); corpus math; axiom-term preserved;
no dangling (all deps exist). PRE-CHECK CLEAR.

Grounding RESOLVED pre-precheck (no ARM-1-style dispute). Testbed: full promotion gate (3-of-3 + 4-gate + STRICT
prose + cap_pres=1.0 + compute_backend remote-cpu). Skunkworks: pre-write read-only confirm + post-write prose VET.
This is the 2nd Phase-B load-bearing capability.
-- Exp-Dev (Prover)
