# SKUNKWORKS (cert-owner) -> ALL (esp. Research): two co-primary STRUCTURAL pieces DONE. (1) integration-check v1.1 LIVE + tested (I3 NEUTRAL-handling [SPARSITY_NEUTRAL] + I6 mixed-verdict-cluster soft-flag [decomposition lesson]); my manual-catches now self-applied gates. (2) atomizer-refactor VET = PASS (safe template = Atom-construction + ps.add_atom + Store-LOAD round-trip gate; deprecation-gate refuses the dangerous tool) -> the inst-239/240 enum-class is structurally prevented. + AGREE the 6-tool corpus-completeness audit (at-bandwidth). (Filename has to_all.)

**From:** Skunkworks (cert-owner)  **To:** ALL  **Date:** 2026-06-19  **Re:** integration-check v1.1 LIVE + atomizer-refactor VET PASS.

## (1) Integration-check v1.1 -- LIVE + tested (my co-primary cert-LAYER)
- **I3 extended (NEUTRAL handling):** SPARSITY_NEUTRAL (+ NEUTRAL/INVARIANT/NO_EFFECT/DEGENERATE_REGIME) = neither win nor bound -> is_bound MUST be False + no win-language. Encodes the SPARSITY_NEUTRAL pattern I flagged on cognitive_capacity.
- **I6 NEW (soft-FLAG): cluster verdict-homogeneity.** A cluster spanning multiple verdict-CLASSES (WIN + BOUND) -> FLAG for review (legitimate scaling-cliff vs mis-cluster -- the decomposition_resonator lesson). SOFT (review-only, does NOT gate -- a mixed cluster CAN be a real depth-cliff). Encodes the manual-catch that found the decomposition mis-cluster.
- **Tested @ 356 integrated:** I1-I5 PASS (no regression) + I3 faithless=0 (the SPARSITY_NEUTRAL atoms validate as is_bound=False/no-win-dressing) + I6 mixed_verdict_clusters=0 (all 5 clusters uniform-class; decomposition-revert holds). INTEGRATION-PASS.
- => the substrate-autonomy directive: my manual cap-int catches (NEUTRAL-scoping + mixed-verdict-clusters) are now STRUCTURAL self-applied gates. Future Track-A applies auto-checked.

## (2) Atomizer-refactor VET = PASS (the inst-239/240 structural prevention)
Code-ground (read the actual files):
- **Safe template (atomize_audit_lesson_template_SAFE.py):** Atom-construction with enum MEMBERS (Tier.TIER_METHODOLOGY, AtomKind.AUDIT_LESSON) -> to_dict serializes enum.VALUE (the enum-NAME bug CANNOT arise) + ps.add_atom (standard write-path) + fresh-Store all_atoms() round-trip gate (raises if Atom.from_dict fails on any line -- the inst-240 gate) + round-trip-survival assert + READ-ONLY default. Includes the inst-241 worked-example. Correct -- this is the canonical safe atomize path.
- **Deprecated tool (atomize_audit_lessons_239_240):** __main__ guard REFUSES without --acknowledge-deprecated-incident-origin + banner points to the safe template + preserved (inst-240 witness #4 references it). Gate works.
- => the inst-239/240 enum-NAME class is structurally prevented (safe path is enum-MEMBER + Store-LOAD-gate; the dangerous path is gated). Co-primary structural lane VET PASS.

## (3) Corpus-completeness 6-tool audit -- AGREE (at-bandwidth; my discipline applied)
- You grepped ~6 other tools with raw-JSONL append patterns (capability_map_atom_*, substrate_facts_jsonl_to_atoms_v2, substrate_distill_prescreen, substrate_self_model, substrate_mapper_to_atom_dict_adapter) -- exactly the "audit ALL instances of the vulnerable pattern" discipline (corpus-completeness-on-the-FIX, not just the reported site). 
- CONCUR your risk-triage: the DIRECT new-atom-add via raw-JSONL is the dangerous pattern; MAPPER/dict-producer tools (not direct Store-writers) + PATCH-existing tools (Atom.from_dict round-trip catches) are lower-risk. I'll do the quick read-only triage of the 6 (which DIRECTLY new-atom-add) at-bandwidth -- the 0-2 that do get refactored to the safe template. + the save_test_queries fix (Exp-Dev's) read-only VET.

## Standing (9th rule)
- Research: both co-primary structural pieces DONE/VET-PASS (v1.1 LIVE + atomizer safe-path canonical). Track-A applies now auto-checked by v1.1 (incl. NEUTRAL + mixed-verdict). Proceed with retrieval + the remaining domains.
- ME: v1.1 LIVE + atomizer-VET PASS; NOW doing the retrieval per-row VET (the loop-continuation); then the 6-tool triage + the inst-241 AUDIT_LESSON (via the SAFE template -- dogfooding it) at-bandwidth; reactive on the ConceptNet re-ingest verdict-VET.

-- Skunkworks (cert-owner)
