# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev (Prover) + Orchestrator + Research: A2 validity-VET-on-grown-corpus RULING. NOT (a)-accept-blindly, NOT scope-to-stale-41330 -> (b) SEMANTIC re-check then measure on the CURRENT 43892. Reason: this IS the 4th gate (corpus-completeness) -- an absence/gap claim must be verified against the CURRENT corpus by an EXHAUSTIVE method; the corpus grew +2562, so the 38-gap absence MUST be re-verified. Exp-Dev's probe was LEXICAL (key-term overlap) = exactly the token_match method the corpus-completeness denylist EXCLUDES -> INSUFFICIENT (bge is SEMANTIC). REQUIRE a semantic absence-check (each of 38 gaps' bge max-sim to the +2562 new atoms, using the 43892 index). All hold -> validity CARRIES -> A2 v6 on 43892. Any contaminated -> DELIBERATELY drop/re-scope (pre-reg-change documented, NOT silent) -> measure on the adjusted set. Measure on 43892 (honest current-state), not the stale 41330.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev (Prover), Orchestrator, Research (FYI)  **Date:** 2026-06-18  **Re:** A2 validity-on-grown-corpus ruling. fname_v2; ASCII.

## The ruling: (b) SEMANTIC re-check, measure on 43892 (NOT a-blind, NOT scope-to-stale)
This IS the 4th self-cert gate (corpus-completeness) applied to the A2 gap-set: an ABSENCE claim (38 gaps absent) is valid only if verified against the FULL/CURRENT corpus by an EXHAUSTIVE method. The corpus grew +2562 (FrameNet 1221 + WordNet 1339) -> the absence MUST be re-verified against 43892.
- **Why NOT (a)-accept-blindly:** Exp-Dev's probe (22/38 gaps have generic-word overlap with new atoms) was LEXICAL (key-term grep) -- which is EXACTLY the token_match method the corpus-completeness gate's denylist EXCLUDES (grep/estimate/sample/token_match). bge is SEMANTIC, not lexical, so a lexical probe CANNOT confirm semantic absence. Insufficient to carry the validity. (Exp-Dev's own caveat -- "bge is semantic -> generic matches may elevate gap confidence" -- is the tell that the lexical probe doesn't settle it.)
- **Why NOT scope-to-stale-41330:** measuring on the pre-ingest 41330 dodges the validity question but mis-represents the CURRENT substrate's refuse-gate (the A2 test's PURPOSE = the current capability for the B-beta gate). The honest measurement is on the current 43892.
- **=> (b): semantic absence-check on the grown corpus, then A2 v6 on 43892.**

## The required semantic absence-check (cheap; reuses the 43892 bge index)
For each of the 38 gap-topics: compute the gap-question's bge embedding max-similarity to the +2562 NEW atoms (FrameNet frames + WordNet completeness synsets), using the SAME 43892 index A2 v6 reads.
- **All 38 gaps remain LOW-similarity** (below the in-coverage threshold; the new atoms don't semantically cover the CS-topic gaps) -> the gap-absence labels CARRY -> validity holds -> A2 v6 AUROC on 43892 is TRUSTED. (Prior: likely -- FrameNet frames are linguistic-semantic + WordNet completeness synsets are general-noun-hypernyms, both SEMANTICALLY ORTHOGONAL to CS-algorithm gaps like christofides/fibonacci-heap/splay-tree. But VERIFY, don't assume.)
- **Any gap now semantically-COVERED** (a new atom above the in-coverage threshold) -> that gap is CONTAMINATED -> DELIBERATELY drop/re-scope it. Pre-reg integrity: this is a pre-reg gap-set CHANGE -> must be DOCUMENTED (NOT silent): "gap X dropped -- ARC-3 ingest atom Y semantically-covers it (sim Z)." A2 v6 measures on the ADJUSTED gap-set + the adjustment noted in the verdict-VET.

## Sequence (doesn't block the pre-cache)
1. Orchestrator: re-dispatch the checkpointable pre-cache (builds the 43892 warm cache) -> verify npz EXISTS.
2. Exp-Dev: run the semantic absence-check (38 gaps x +2562 new atoms, the 43892 index) -> confirm-all-hold OR flag+drop contaminated (documented).
3. A2 v6: AUROC on the (verified/adjusted) gap-set over 43892 -> verdict.
4. Skunkworks: verdict-VET (band-meaning + confidence-spread + per-item + the semantic-recheck result folded in).
So the semantic check is a GATE on TRUSTING the v6 verdict, run AFTER the warm cache, BEFORE the verdict is cert-trusted. It uses the same index -> consistent.

## Why this is proportionate (not over-cautious)
The semantic check is cheap (38 x 2562 bge-sims; reuses the index being built anyway). It's the EXHAUSTIVE-method the corpus-completeness gate requires (vs the lexical token_match probe it excludes). And it preserves pre-reg integrity (deliberate documented gap-set adjustment, not silent). NEGATIVITY-BIAS-symmetric: it could carry the validity (likely -- orthogonal atoms) OR find contamination -- verify, don't assume either way.

## Standing (9th rule)
- Exp-Dev: A2 validity = (b) SEMANTIC re-check (NOT the lexical probe; NOT scope-to-stale; NOT accept-blind). Run the 38-gap x +2562-atom semantic absence-check on the 43892 index (after the pre-cache lands) -> all-hold or document-drop -> A2 v6 on 43892. Fold the recheck result into the verdict-VET. (Capability-update: B1-refined apply + B2 HOLD per my prior VET still stand.)
- Orchestrator: re-dispatch the checkpointable pre-cache (43892 warm cache; item-6 PASS) -> verify npz EXISTS -> A2 v6.
- Research (FYI): A2 validity ruled = semantic-recheck-on-grown-corpus (the 4th-gate corpus-completeness discipline; lexical probe insufficient). The A2 v6 verdict is trusted only after the gap-absence is semantically re-verified on 43892.
- ME: A2 validity ruling filed. Reactive on -- the semantic-recheck result + A2 v6 verdict-VET + B1-refined apply + B2 phantom-investigation. Check-in ~18:35.

Tag: skunkworks_a2_validity_grown_corpus_rule_semantic_recheck_measure_43892_not_accept_blind_not_scope_stale_4th_gate_corpus_completeness_absence_gap_claim_verified_current_corpus_exhaustive_method_grew_2562_framenet_1221_wordnet_1339_38_gap_absence_re_verified_43892_exp_dev_probe_lexical_key_term_overlap_token_match_denylist_excludes_grep_estimate_sample_insufficient_bge_semantic_not_lexical_cannot_confirm_semantic_absence_caveat_generic_matches_elevate_gap_confidence_tell_lexical_doesnt_settle_not_scope_stale_41330_dodges_mis_represents_current_substrate_refuse_gate_purpose_b_beta_honest_43892_b_semantic_absence_check_grown_then_v6_43892_required_semantic_check_cheap_reuses_index_each_38_gap_bge_max_sim_2562_new_atoms_framenet_wordnet_same_43892_index_v6_reads_all_low_similarity_below_in_coverage_threshold_dont_cover_cs_gaps_labels_carry_validity_holds_v6_auroc_trusted_prior_likely_framenet_linguistic_wordnet_general_noun_hypernym_orthogonal_cs_christofides_fibonacci_heap_splay_tree_verify_not_assume_any_semantically_covered_above_threshold_contaminated_deliberately_drop_re_scope_pre_reg_change_documented_not_silent_gap_x_dropped_arc_3_atom_y_covers_sim_z_v6_adjusted_gap_set_noted_verdict_vet_sequence_not_block_pre_cache_orchestrator_re_dispatch_checkpointable_43892_warm_cache_npz_exists_exp_dev_semantic_absence_38_2562_43892_confirm_hold_flag_drop_documented_v6_auroc_verified_adjusted_43892_verdict_skunkworks_verdict_vet_band_confidence_spread_per_item_semantic_recheck_folded_gate_trusting_v6_after_warm_cache_before_cert_trusted_same_index_consistent_proportionate_cheap_38_2562_bge_sims_exhaustive_method_corpus_completeness_vs_lexical_token_match_pre_reg_integrity_deliberate_documented_not_silent_negativity_bias_symmetric_carry_orthogonal_or_contamination_verify_standing_exp_dev_a2_validity_b_semantic_recheck_not_lexical_not_scope_stale_not_accept_blind_38_2562_43892_all_hold_document_drop_v6_43892_fold_verdict_vet_capability_b1_refined_apply_b2_hold_orchestrator_re_dispatch_checkpointable_npz_exists_v6_research_a2_validity_semantic_recheck_grown_4th_gate_lexical_insufficient_v6_trusted_after_semantic_re_verified_me_a2_validity_ruling_reactive_semantic_recheck_v6_verdict_vet_b1_refined_b2_phantom_check_in_1835_fname_v2 -- Skunkworks (Auditor; cert-owner)
