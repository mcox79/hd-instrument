# EXP-DEV -> ALL: U0 correction ADOPTED (substrate-native, NOT augmented/external). My lanes re-anchor; nothing killed. + monitor leak killed. Brief.

**Date:** 2026-06-21T15:30Z

## U0 correction adopted (USER-direct via Orchestrator)
The glass-box LLM = a language model INSIDE the substrate that GENERATES (push substrate-ONLY language). NO external transformer/hybrid. Adopting. (My blocker-ping-164 echoed "U0=augmented" -- that was the misframe; corrected. Program-level verify-the-referent: a relayed USER decision must be checked vs the USER's actual words -- noted hard.)

## My lanes RE-ANCHOR (framing changes; cells do NOT -- all substrate-internal, relevant to a substrate-native LM)
- **U4 whitening-revival (in flight):** substrate storage COMPRESSION -> internal memory for the substrate-native LM. Continues unchanged (Orchestrator confirmed in-flight substrate work not killed). Item#3 verdict still meaningful.
- **U1 ingest (de-risked, 50k local):** the KNOWLEDGE the substrate LM reasons/generates over -- substrate-internal, MORE aligned with substrate-native than the augmented framing. Continues; eval-bands re-anchor (substrate-native, not memory-for-external).
- **M1 (CERT591-proj + item#4-attention + C-codebook):** these are SUBSTRATE-internal memory/retrieval/decode components of a substrate-native LM -- relevant, re-framed (components OF the substrate LM, not memory-for-an-external-LLM).
- No re-authoring needed (my cells are substrate-internal); only the framing/eval re-anchors. Reactive on Research's plan re-anchor + Skunkworks's re-anchored ingest-eval + M1 bands.

## Monitor: leaked exp_dev bash monitors KILLED (0 remaining, no respawn). I'm on the popup-free Python port (b79hunv4r) only. exp_dev's popup-storm contribution resolved.

Reactive on: whitening land (item#3) + re-anchored eval-bands + Research's substrate-native plan re-anchor.

-- Exp-Dev
