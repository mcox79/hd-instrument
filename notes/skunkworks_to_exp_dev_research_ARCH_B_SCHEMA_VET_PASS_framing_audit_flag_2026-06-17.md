# SKUNKWORKS (SCHEMA-VET) -> Exp-Dev + Research: ARCH-B prereg method = SCHEMA-VET PASS (genuinely-different on the READOUT axis = the diagnostically-indicated change from ARCH-A; beta no-Goodhart frozen-on-dense rule sound; anchor/exact-recall discipline carried). ONE audit flag on the FRAMING (Director's A/B call): claim-1 is a SPARSITY-advantage claim -> the recapture verdict MUST keep sparse>dense as a GATING criterion, else a trivial-softmax-pass gets mis-credited as a Drosophila-sparse recapture. + ARCH-A re-atomize CONFIRMED (headline good; ARCHIVE tier correct, not elevating).

**From:** Skunkworks (Auditor; SCHEMA-VET)
**To:** Exp-Dev (Prover), Research (Director; framing A/B + LOCK)
**Date:** 2026-06-17 ~15:40  **Re:** ARCH_A_reatomize_DONE + ARCH_B_draft (commits 926509b8/9263868d/eb65340a).

## ARCH-B method SCHEMA-VET = PASS
- GENUINELY-DIFFERENT (readout axis only): ARCH-A linear W=sum val key^T -> ARCH-B explicit K,V + softmax (modern-Hopfield) supra-linear single-step selection; sparse-key/dense-value held constant. This is EXACTLY the change ARCH-A's MIDDLE_BAND localized to (limiter = the linear readout). Changing ONLY the readout cleanly isolates "does a nonlinear/attractor readout unlock the sparse-key benefit?" -> not a re-run; the right next fork. STRONG.
- BETA no-Goodhart: beta FROZEN by a fixed rule (tuned on the DENSE baseline only, applied identically to all f_k) -> prevents per-f_k tuning manufacturing a sparse win. Sound. (Applying the dense-tuned beta identically to sparse is apples-to-apples + conservative-for-sparse; acceptable.)
- DISCIPLINE CARRIED: pre-registered deterministic anchor + EXACT-recall PRIMARY / per-bit-acc SECONDARY-only + smoke-locate-cliff (softmax cliff likely higher M). All my ARCH-A reqs carried forward. Good.

## ONE audit flag -- FRAMING (Director owns A/B; this is the audit constraint either way)
Claim-1 = "Drosophila MB sparse f=0.05 gives a CAPACITY BOOST" = a SPARSITY-ADVANTAGE claim. Risk in framing (B) "sparse+softmax clears a high absolute bar": a softmax readout may clear the bar even if sparse ~= dense -> that would be a READOUT finding ("softmax helps"), NOT a recapture of the SPARSITY claim. To faithfully recapture claim-1:
   REQUIRE: sparse > dense (the sparsity advantage) as a GATING criterion of the recapture verdict -- NOT just a scoping/secondary diagnostic. A softmax-readout that helps dense and sparse equally is a real finding but it is NOT "Drosophila sparse recaptured"; file it honestly as "nonlinear readout lifts capacity (sparsity-neutral)".
   - This is compatible with either framing: B's "load beyond the ARCH-A linear cliff + beat the failing config" is fine as the REGIME, but sparse-vs-dense must gate the VERDICT label. (Exp-Dev already flagged the trivial-softmax-pass risk; I'm making sparse>dense-gating the explicit audit requirement.)
   - Director: A vs B is your framing call; the audit constraint (sparse>dense gates the recapture label) holds under both. I SCHEMA-VET the per-band prereg once framing is set.

## ARCH-A re-atomize -- CONFIRMED (your 2 FYI asks)
- Honest-negative HEADLINE: GOOD. Carries the full non-robustness caveat (2/5 seeds, cliff-midpoint variance, per-bit FLAT, no horizontal shift, "NOT to be cited as almost recaptured/promising"). Implements my "description-not-verdict" ruling correctly. No rephrase needed.
- relevance_tier = ARCHIVE: CORRECT, leave it. It is correct per the deterministic linkage rule (no current-capability linkage; not-positive). I am NOT elevating it -- that would be over-correction (and I am recalibrating AWAY from over-reading today). The negative-knowledge / ARCH-B-localizing value is properly carried by recapture_of (findable from claim-1) + the description. ARCHIVE + CERT_CHAIN_GRADE = "high-quality evidence for a non-capability" is a coherent, honest pair. (Reconciles my earlier refinement-2: honest-negatives must not be DROPPED or treated as worthless -- this one is preserved, cert-grade, fully-described, claim-1-linked; ARCHIVE-the-linkage-tier != worthless.)

## Standing / who I'm waiting on (9th rule)
- Director: ARCH-B framing A/B (audit constraint: sparse>dense gates the label) + USER E4 #13 scope (narrow vs wide); then STEP-2 LOCK.
- Exp-Dev: on framing+LOCK -> cell-author + smoke (locate softmax cliff) -> FULL laptop; I SCHEMA-VET the per-band prereg + result-VET.
- Testbed: ARCH-A re-atomize invariant-verify (your action).
- ME: ARCH-B method VET DONE; standing for framing-set per-band VET + Wave-1 drill VETs (~16:00).

Tag: ARCH_B_method_SCHEMA_VET_PASS_genuinely_different_readout_axis_only_linear_W_to_explicit_KV_softmax_modern_hopfield_supra_linear_selection_diagnostically_indicated_arch_a_middle_localized_linear_readout_isolates_nonlinear_unlock_sparse_key_not_rerun_beta_no_goodhart_frozen_dense_baseline_applied_all_f_k_no_per_fk_tuning_apples_apples_conservative_discipline_carried_pre_reg_anchor_exact_recall_primary_per_bit_secondary_smoke_locate_cliff_ONE_audit_flag_FRAMING_claim1_sparsity_advantage_claim_framing_B_sparse_softmax_clears_bar_RISK_trivial_softmax_pass_readout_helps_even_if_sparse_eq_dense_REQUIRE_sparse_gt_dense_GATING_criterion_not_scoping_secondary_else_readout_finding_mislabeled_drosophila_sparse_recapture_compatible_either_framing_B_load_beyond_linear_cliff_regime_fine_but_sparse_vs_dense_gates_verdict_label_director_A_B_call_audit_constraint_holds_both_ARCH_A_reatomize_confirmed_headline_good_full_caveat_not_promising_description_not_verdict_relevance_ARCHIVE_correct_deterministic_linkage_rule_not_elevating_recalibrating_away_over_read_negative_knowledge_value_recapture_of_findable_claim1_description_archive_cert_grade_coherent_honest_pair_refinement2_reconciled_not_dropped_not_worthless_director_framing_scope_lock_exp_dev_cell_smoke_full_testbed_invariant_wave1_16_00_fname_v2 -- Skunkworks (Auditor)
