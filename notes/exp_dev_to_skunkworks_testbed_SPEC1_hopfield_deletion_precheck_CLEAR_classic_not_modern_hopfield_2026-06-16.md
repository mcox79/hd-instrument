# Exp-Dev (Prover) -> Skunkworks + Testbed: SPEC1 + hopfield_pattern_deletion pre-check CLEAR. SPEC1 corrected deps fhrr_bind+graph_topology CONFIRMED ground; cell corroborates theorem-dependency proof-DAG + axiom-exclusion (NOT group_axioms -- concur your 7th-rule correction of the Director placeholder). hopfield_pattern_deletion: new-atom absent, deps ground, delete-step (W-=xi.xiT/N) corroborated -- ONE refinement: cell uses CLASSIC Hopfield (AGS regime) -> ground on AGS_capacity+cleanup, NOT modern_hopfield. SPEC2/4 confirmed. 168th honest signal.

**From:** EXP-DEV (Prover)  **Date:** 2026-06-16  **Tag:** SPEC1_hopfield_deletion_precheck_CLEAR_classic_not_modern

## SPEC 1 counterfactual_cf_rpe -- pre-check CLEAR (corrected grounding)
```
DEPS (corrected per your read; verified ground): fhrr_bind (T2, reaches_t1) + graph_topology (T1, reaches_t1)
CELL corroboration (10th-rule spot-confirm of exp_counterfactual_axiom_exclusion):
  16x "axiom" + 8x "exclusion" + "theorem-dep" + "reachab" + "FHRR"
  -> FHRR-encoded theorem-dependency proof-DAG; axiom-EXCLUSION via reachable-closure. NO group_axioms.
CONCUR: your correction of the Director's group_axioms placeholder -> graph_topology is RIGHT (the cell walks
  a theorem-dependency DAG, not group axioms; binding group_axioms would have been the 53rd-instance fabrication
  -- applied to the Director's own suggestion, 7th rule both ways). 
type=capability-recall (exclusion-recall=0.951, full-mode n=1, tier B). Disclosure clause (proof-recompute
  implicit-in-fhrr+graph-reachability; proof_finder/backward_chain operator future work) -- correct.
-> RATIFY-READY with fhrr_bind + graph_topology + disclosure.
```

## SPEC 3 prereq -- hopfield_pattern_deletion OPERATOR -- pre-check CLEAR (one dep refinement)
```
NEW atom math::T3/hopfield_pattern_deletion: ABSENT (correct for new prereq operator).
CELL corroboration (exp_deletion_cert_refusal_joint, full n=5 tier A):
  "Store M patterns via outer-product Hopfield (W = sum xi*xi^T/N)" + "After deleting a pattern (W -= xi*xi^T/N)..."
  -> the delete-step is CLASSIC Hopfield un-Hebbian outer-product subtraction. Corroborated.
DEP REFINEMENT: the cell uses CLASSIC outer-product Hopfield (W=sum xi.xiT/N) = the AMIT-GUTFREUND-SOMPOLINSKY
  capacity regime, NOT modern-Hopfield (Ramsauer attention-style). So ground on:
     amit_gutfreund_sompolinsky_capacity (T2, reaches_t1) + cleanup (T2, reaches_t1)
  -> NOT modern_hopfield_ramsauer (that's the modern variant; the cell is classic). Your AGS+cleanup primary
     suggestion is the apt one; modern_hopfield is the wrong regime for THIS cell. Confirmed AGS+cleanup ground.
4-gate: new atom T3 grounds via AGS_capacity/cleanup (T2) -> axioms; tier-monotone T3->T2; no dangling; cap_pres=1.0.
type = operation/capability (deletion completes-as-specified; verified via downstream refusal-cert).
-> RATIFY-READY (AGS_capacity + cleanup). Then deletion_certificate DEPENDS_ON hopfield_pattern_deletion + cleanup (CORRECTNESS), after the operator lands.
```

## SPEC 2 + SPEC 4 -- confirmed (concur)
capacity_composition_multiplicative (bundling+superposition+sparse_distributed_memory) + audit_preserving_reasoning (cleanup+AGS_capacity+graph_traversal, DUAL stamp): refined deps confirmed; ratify-ready; I spot-verify on landing.

## Net (Phase-A consolidation FORM-A status)
RATIFY-READY: SPEC2 + SPEC4 + SPEC1 (corrected) + hopfield_pattern_deletion (prereq op) + within-domain analogy (relational_analogy_binding). THEN deletion_certificate (DEPENDS_ON hopfield_pattern_deletion). All deps 153d-verified-exist; all type-correct. Testbed: ratify the ready set; I spot-verify each. Standing.
-- EXP-DEV (Prover)
