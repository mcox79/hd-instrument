# COMPONENT BRAIN-FIDELITY LEDGER — nail each component brain-faithful, then assemble (living doc)

**USER DIRECTIVE (2026-07-28, full-auto authorized):** stop task-chasing; go component-by-component, make EACH brain-faithful, judge each on the BRAIN's metric (NOT downstream task-win), THEN assemble. Root cause of repeated failures = we let a downstream task-metric judge components whose brain-faithfulness we never verified first ("brain-faithful LOSING = presumed impl-bug until proven structural" — we ignored this). This ledger is the tracking backbone for the FORMALIZE method [[project_formalize_deepbrain_analysis_then_comparison_accurate_duplication_method_2026-07-24]] applied SYSTEMATICALLY.

## THE METHOD (per component, in order)
1. **Deep-brain-analysis** — biology-first map of how the brain does this component (the drills do this).
2. **Comparison** — our impl vs brain, scored on SHAPE + POSITION + METRIC (not just "right function").
3. **Name the gap** — the specific fidelity deficit.
4. **Accurate-duplication** — build a brain-accurate, can-fail cell **judged on the BRAIN's metric** (below), not on whether it wins a downstream task.
5. **VET + iterate** — replicate across seeds; a brain-faithful build that loses the BRAIN metric is a real fail; one that loses only a downstream task but passes the brain metric is KEPT (composition problem, not component problem).
6. **Integration checkpoint** — faithful parts don't auto-compose; each component's fidelity spec includes its interface/handoff; assembly is its own phase with its own criteria.

**JUDGING SHIFT (the core change):** each component has a BRAIN METRIC = how closely it reproduces the brain's mechanism/behavior FOR THAT COMPONENT. That metric gates keep/kill — the downstream held-out-NEW task is only the FINAL assembly metric, not the per-component gate.

**Fidelity = algorithmic-level (Marr L2/L3):** right function + shape + role + metric. NOT neuron-level implementation. **Lurking alternative (hold live):** experience-poverty — if the bottleneck is data/experience richness not mechanism, component-fidelity won't save us; mechanism is what we control, but don't over-attribute every failure to fidelity.

---

## THE LEDGER (status: FAITHFUL / IMPROVING / PARTIAL / UNFAITHFUL / ABSENT)

| # | Component | Brain mechanism | Our impl | Gap (shape/pos/metric) | BRAIN METRIC (the per-component gate) | Status | FORMALIZE next |
|---|---|---|---|---|---|---|---|
| 1 | **Encoder OBJECTIVE** | Forward hierarchical predictive coding (Rao-Ballard/Friston); prediction-error is the learning signal | MLM bidirectional cloze | Wrong learning signal: bidirectional reconstruction, not forward-temporal prediction | Does training produce forward next-input predictivity that IMPROVES with depth/context (PE-driven), vs a static cloze fit? | UNFAITHFUL (but +0.44 forward structure already latent in MLM reps) | **Drill #3** resolves: real untried faithful lever (forward-temporal PC term) vs defer (objective not the bottleneck) |
| 2 | **Encoder ARCHITECTURE** | Sparse, recurrent, columnar cortex; mixed selectivity (Rigotti/Fusi) | Dense transformer (attention loosely brain-like) | Dense not sparse; no recurrence; no columns | Mixed-selectivity + sparse-coding statistics vs cortical population | PARTIAL | Deprioritized (deliberate; mechanism≠task-analog) — revisit only if reps plateau after readout/maintenance |
| 3 | **REPRESENTATION geometry** | Graded distributed semantic space; generalizes to novel | Learned from-scratch code (29591), held-out-NEW generalizes | Modest band (0.56-0.63); under-decoded | Held-out-NEW generalization + graded neighborhood structure | PARTIAL-FAITHFUL (banked) | It's READOUT-limited -> row 4; representation itself may be adequate |
| 4 | **READOUT / decoding** | Learned, plastic, NONLINEAR + attentional decoders on population codes | learned bilinear/probe; AttnBilinearReadout tried + SHELVED | Readout lever SPENT for comprehension | Calibration-first: known reader passes; role-GENERAL order decoding | **SPENT for comprehension (07-29).** relational-placement fix (+0.038) stands; but for COMPREHENSION: cross-boundary VET FAILS_TO_REPLICATE (seed_7 +0.21, seed_13 +0.10 = seed-luck) AND AttnBilinearReadout = HARD_FAIL_STRUCTURE_ALONE (random-init encoder matches ~+0.50 => attention+bilinear capacity, not learned semantics). | DONE. A decoder can't fix comprehension: the encoder doesn't robustly carry the signal + clever decoding is structure-alone. Fix is UPSTREAM (rows 1/6). |
| 5 | **COMPREHENSION / situation-model construction** | Kintsch construction-integration; Zwaan event-indexing; Zacks-Franklin SEM (PE-segmented event model) | Two organs PROVEN in isolation (WM-update row 6; binding row 12), NEVER assembled end-to-end on a multi-sentence pipeline | No cell chains encoder->role-extraction->binding->WM-update->query; every proof to date is a single-mechanism synthetic or single-relation-per-sentence task | Multi-sentence entity-state update + query, using EXPLICIT-role extraction (proven) as interim front-end | **PARTIAL — proven-in-pieces, ASSEMBLY not attempted (2026-07-30 stack review)** | **HIGHEST LEVERAGE NEXT: build the end-to-end assembly cell (see notes/brain_foundational_stack_assessment_2026-07-30.md sec 3 #1) — cheap, both prerequisites already VET-confirmed, will empirically surface the real next wall (likely coreference).** |
| 6 | **WORKING MEMORY / active maintenance** | Persistent-activity / attractor / activity-silent WM (Wang, Stokes, Mongillo); PE/content-gated incremental update (PBWM) | Content-gated overwrite-with-suppression WM (EntNet-style) | Proven on a DEGENERATE encoder (synthetic reservoir-failing task); NL version proven too but only with an addressable role signal (explicit word or supervised address), and does NOT generalize to a held-out ROLE | Content-gated overwrite-with-suppression vs reservoir baseline; item-generalization | **PARTIAL — mechanism VET-CONFIRMED (WM_PROVEN 88d050955/a81301a6; NL read_conditioning af702f3b), bounded to trained roles, not yet coupled to a real (non-degenerate) encoder** | Feed it real extracted (role,filler) tuples from the multi-relation slot-filling capability (row 12) in the assembly cell (row 5); held-out-ROLE generalization stays open/deferred |
| 7 | **GROUNDING** | Sensorimotor for CONCRETE (Barsalou); relational/propositional for ABSTRACT (via semantic net) | Sensorimotor (Lancaster) applied to ALL, incl. abstract science | Mis-applied: sensorimotor to abstract concepts | Concrete -> sensorimotor transfer; abstract -> relational-from-graph transfer | PARTIAL / mis-applied (sensorimotor SHELVED, HARD_FAIL) | Relational grounding for abstract concepts (on-deck; revive when reps/readout settle) |
| 8 | **REASONING** | CA3 attractor / pattern-completion; additive multi-constraint satisfaction | Verification-by-derivation reasoner (hdlab/reasoner.py) | Coverage-bound (only cleanly-derivable) | Constraint-satisfaction resolution scaling with #constraints brought to bear | FAITHFUL (banked 29537-70) | Coverage grows from better reps/readout; no component rebuild needed |
| 9 | **CONSOLIDATION / sleep** | Iterated interleaved SWR replay (CLS); Tse/Morris schema-gating; PE/surprise-budgeted replay | Single averaging op per cycle (mean/Kalman/retrieve-not-avg) | Wrong op-CLASS: once vs iterated; average-all vs selective-replay; ungated vs schema-gated | Interleaved OLD-vs-NEW retention (integrate new WITHOUT catastrophic forgetting) — averaging can't be tested on it | UNFAITHFUL; faithful version (cls_discrete_budget, HARD_PASS synthetic) ISLANDED | **Drill #4** -> wire the certified replay engine + surprise-budget + schema-gate (on-deck) |
| 10 | **LEARNING LOOP (read->extract->consolidate)** | CLS continual learning; hippocampal fast-write + cortical slow-consolidate | Loop plumbing works (sleep fires, controls behave); comprehension-EXTRACT unsolved | The "what to extract from reading" step | Sustained, comprehension-SPECIFIC gain from novel reading (vs scrambled/wrong-concept controls) | PARTIAL (CLS shape right; extract unsolved) | Follows comprehension (rows 5/6); loop v1-v6 all negative until comprehension lands |
| 11 | **FOUNDATION / knowledge** | Distributed semantic memory | Symbolic typed graph (1.24M edges), used as SEED/teacher | Symbolic-supplied, not distributed-native | Coverage + relational correctness as a teacher/target signal | FAITHFUL-for-role (supplied seed; learn rep downstream) | Adequate; no rebuild |
| 12 | **BINDING (role<->filler)** | Contested: synchrony/theta-gamma (Lisman-Jensen, Fries) vs mixed-selectivity/conjunctive coding (Rigotti-Fusi) vs TPR (Smolensky) | Native FHRR/HRR algebraic bind/unbind, zero learned params | Oracle role-key derivation has no mechanistic analog (least-defensible); FHRR-as-synchrony is a HYPOTHESIS not settled brain fact | Novel-filler-to-role, zero-shot novel-role, cross-slot relational, multi-relation composition, ALL on STRUCTURED slot-filling w/ EXPLICIT role words | **PARTIAL — composition MECHANISM VET-CONFIRMED (4x, 2026-07-30 night, see director backup) GIVEN roles; brain-theory status CONTESTED (VET a3b18a72, P~0.35); does not extract roles from syntax (that's row 2/13)** | Learned-TPR role-key replacement CLOSED/deprioritized (probe superposition-saturated, inconclusive); revisit only with a non-saturated probe regime |
| 13 | **COREFERENCE / entity tracking** | Competitive antecedent resolution (multiple candidate referents); N400/ERP discourse-integration (Van Berkum) | Single-antecedent pronoun indirection only (inside row-12's multirelation cell) | No competitive resolution among 2+ plausible antecedents has ever been tested | Accuracy resolving a pronoun/definite-NP among 2+ semantically-plausible candidate antecedents | **ABSENT — untouched (2026-07-30 stack review); named as an explicit gap in the multirelation VET (a9dfce95) but never independently pursued** | **SECOND-highest leverage (see assessment note sec 3 #2)** — build after/alongside the row-5 assembly cell, which will likely hit this wall organically |
| 14 | **DISCOURSE / bridging inference** | Causal/elaborative/bridging inference beyond what's explicit (Graesser constructionist theory) | NONE | Never attempted, not even a negative result exists | Accuracy on inferences requiring cross-sentence causal/elaborative connection not stated in text | **ABSENT — untried, not yet on critical path** | Deferred until row 5 (assembly) + row 13 (coreference) land — presupposes both |

---

## EXECUTION SEQUENCE (by load-bearingness × cost) — REVISED 2026-07-30 (full-stack review, notes/brain_foundational_stack_assessment_2026-07-30.md)
Two organs (WM-update row 6, binding row 12) are now independently VET-confirmed but have NEVER been run together. That is the highest-leverage gap — an ASSEMBLY gap, not an unsolved-mechanism gap.
1. **ASSEMBLY (row 5) — FIRST.** Chain encoder -> explicit-role-word slot-filling extraction (row 12, proven) -> content-gated WM (row 6, proven) -> multi-sentence entity-state update -> query. Cheap (every piece exists); tests the actual north-star op; will empirically surface the real next wall (likely row 13).
2. **COREFERENCE at scale (row 13) — SECOND.** Genuinely untouched, textbook-necessary (Kintsch/SEM); higher leverage right now than further syntax-role work since zero prior work exists to build on.
3. **SYNTACTIC THEMATIC-ROLE / voice-invariance (rows 2/3) — THIRD, RE-SCOPED not abandoned.** Real prerequisite for non-templated text eventually, but recalibrated 2026-07-30: the "wall" was partly a cross-voice geometry-transfer PROBE artifact, not a demonstrated incapacity — finish the one remaining falsifiable test (regime where a positional-template control genuinely floors), then pause new variants until 1/2 land.
4. DISCOURSE / bridging (row 14) — deferred, presupposes 1+2.
5. CONSOLIDATION (row 9) — on-deck, not current bottleneck.
6. GROUNDING-abstract (row 7) — on-deck; relational grounding when reps/readout settle.

## STATUS TALLY (honest, updated 2026-07-30): FAITHFUL 2 (reasoning, foundation-for-role) | IMPROVING 1 (readout) | PARTIAL 7 (architecture, representation, grounding, loop, comprehension/situation-model, working memory, binding) | UNFAITHFUL/CONTESTED 1 (objective) | ABSENT 2 (coreference, discourse). => the pipeline has real proven mechanism-pieces now (binding, WM-update) but they are UNASSEMBLED, and two components (coreference, discourse) remain completely untouched — the USER's original "most of the pipeline is not yet brain-faithful" assessment still holds, but the specific gap has shifted from "missing mechanisms" to "missing assembly + two untouched components."
