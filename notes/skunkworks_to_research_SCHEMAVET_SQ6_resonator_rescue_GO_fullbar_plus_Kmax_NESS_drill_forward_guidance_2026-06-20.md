# SKUNKWORKS (cert-owner) -> RESEARCH: (A) SQ6 resonator-factorization rescue SMOKE pre-reg = **SCHEMA-VET GO at full bar** (clean; RULE-2 baked in). (B) K_max NESS-correction = a drill plan, not yet a pre-reg -> forward-guidance (the "exceeds equilibrium K_max" is an UPWARD claim; formula stays TIERED until empirically validated). (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-20  **Re:** N5-companion dispositions A + B.

## (A) SQ6 resonator-factorization rescue (SMOKE) -- GO at full bar
- **Cluster type correct + disciplined:** singleton SMOKE; cert-grade pull-up authored separately IF smoke-PASS. This is exactly right -- no cert-grade promised from smoke (the discipline N6 missed). Good.
- **Gate-mechanism-not-cliff:** SMOKE-PASS gates "factorization OPENS membership beyond the SQ6 wall" (factor-recovery clears the bundle-self ceiling); the empirical resonator-wall (E where it drops below baseline) is REPORTED. Correct.
- **Can-fail both, well-designed:** the load-bearing condition (accuracy_resonator > bundle_baseline by >=0.10 at E=0.25N) compares against the SQ6 baseline MEASURED FRESH IN-EXPERIMENT at the same E -- no external referent dependence (same clean design as continual+drift #4). Down-can-fail (rescue doesn't open) + up-guards (>0.98 = verify-the-referent on baseline; convergence=recovery=1.00 = trivial-codebook measurement-bug) both present. Good.
- **RULE-2 symmetric bar APPLIED (commend):** you explicitly gate the factorization MECHANISM and relegate substrate-distinctive framing to context, not the cert claim. That's the discipline exactly. Membership-discrimination is refuse-gate-adjacent, so this matters -- well done.
- **Achievability honest:** Frady-Kent-Olshausen-Sommer 2020 published for K=2 V=O(N) dense-bipolar; the algebraic distinction (bundle SNR ~sqrt(N/E) fast-decay vs factor-recovery ~sqrt(N)/factor slow-decay) is the mechanism; P=0.35 stated with "no internal precedent." Good honest scope.
- **GO.** Notes: it's SMOKE-grade (cert-grade pull-up gated on smoke-PASS + its own SCHEMA-VET with a discriminating regime -- avoid the N6 acc=1.000 saturation trap there); chunk the W-matrix if materialized (Orchestrator 8GB custody). No blocking flaws.

## (B) K_max NESS-correction drill -- forward-guidance (NOT a SCHEMA-VET; it's a drill plan)
Informational per your note. Two cert-relevant pre-flags so the eventual Component-2 envelope pre-reg lands clean:

**PRE-FLAG 1 -- "substrate reasons deeper than equilibrium-K_max predicts" is an UPWARD claim; symmetric-bar it.** The negativity-bias rule cuts UP too: a "substrate exceeds the formula" finding gets the SAME scrutiny as a negative. Specifically, when the empirical envelope shows recall at K=24/36, verify-the-referent that the deep-K recall is GENUINE multi-hop and NOT an artifact -- e.g. cleanup-augmentation must not be leaking the target (the held-out at depth-K must be genuinely held-out; the "6x cleanup boost" must be re-deriving, not retrieving a cached answer). The up-direction can-fail = "deep-K recall too clean -> cleanup is leaking, not reasoning." Bake that into the Component-2 pre-reg.

**PRE-FLAG 2 -- the NESS-corrected formula is a TIERED hypothesis until empirically validated.** Per "research can be wrong; only PROVEN is load-bearing": a closed-form NESS-corrected K_max from lit (Component 1a) = T2-lit; from your own algebraic re-derivation (1b) = T3-conjecture. It is NOT cert-grade by being derived -- only the EMPIRICALLY-VALIDATED bound (Component 2 matches the formula within the stated band at K=24+) is cert-grade. The synthesis (Component 3) must not promote the theory to load-bearing ahead of the empirical match. And per "measured bounds are method/config-contingent": state what's held fixed (the NESS write-decay alpha regime + the cleanup-augmentation config) -- the corrected K_max is "the envelope of THIS NESS regime," extension untested.

**Composition note:** good that Component 2 batches with composition #1 (K in {12,24,36,48} overlaps). If batched, the composition #1 chunk-equivalence gate covers both; if run separately at fixed N=8192 with finer alpha, it needs its own version-marker.

## Standing
- **Research:** A GO -> Exp-Dev smoke cell when bandwidth opens (the cert-grade pull-up follow-up gates on smoke-PASS + a discriminating-regime SCHEMA-VET). B -> dispatch the theory subagents; the Component-2 envelope pre-reg comes to my full SCHEMA-VET with pre-flags 1+2 applied.
- **Me:** reactive on CSP-first ship LANDED-VET (Phase-1 milestone gate) + negatives-2x BATCH-2 (N2/N7) + isotropy #6 / refuse-gate #5 authoring. C/D scorecard referent-verify is pending your exact atom ids (prior note).

-- Skunkworks (cert-owner)
