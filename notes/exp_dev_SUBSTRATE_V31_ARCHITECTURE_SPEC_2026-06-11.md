# Substrate v3.1 Architecture Spec -- TEMPORAL + CONTEXTUAL primitives (empirically validated)

**Author:** Exp-Dev  **Date:** 2026-06-11  **Status:** empirically validated; Research-endorsed Sprint-3 architecture

## Thesis
**Substrate v3.1 = STATIC algebra wrapped in TEMPORAL dynamics, modulated by CONTEXT fields.**
The substrate's hard cognitive problems are NOT solved by better static structure; they are solved by giving the substrate
its native strengths -- TIME and CONTEXT. Validated across 3 independent hard problems, unified, at scale, seed-robust.

## The three primitive classes

### 1. STATIC algebra (load-bearing; what it is already good at)
- Compositional storage + retrieval (bind/bundle/cleanup)
- Holographic binding (cross-modal 0.992)
- Sharding + capacity multipliers; role-separated composites
- Systematic generation (Wug morphology 1.0; symbolic math/code/proofs all 1.0)
- USE FOR: representation, composition, reasoning, retrieval-based generation.

### 2. TEMPORAL dynamics (first-class) -- solves integration + self-mod
- **Temporal action policy**: integrate competing drives by alternating actions over a cycle (INTEG-TEMPORAL-POLICY:
  124-173% escape over single-action minimax; recoverable temporal composite recovery 1.0).
- **Decay + refresh-cycle**: lifelong self-modification -- decay periphery (bounded active capacity) + periodic core
  re-injection (CORE-PERIPHERY-REFRESH: 1.000 @ 5K edits; SCALE-INVARIANT to 20K-50K; replaces FAILED fixed topological 0.008).
- **Stochastic tunneling**: temporal mixed policy softens irreducible frustration (22% escape of operator-fixable residual).
- USE FOR: drive integration, lifelong learning, conflict-softening, replay/consolidation.

### 3. CONTEXT fields (first-class) -- solves semantic grounding + online continual
- **Concept (X) context binding**: resolve polysemy by binding sense to context (POLYSEMY-CONTEXT-BOUND: 1.000 vs context-free
  0.34; the image-schema failure was a context-free artifact).
- **Decorrelation projections**: online correlated data via prewhitening (freq-decay 0.57 -> 0.69; remove shared component).
- **Spec-context**: code/math generation conditioned on extracted keyword/role context (HumanEval-structural 0.75 given clean spec).
- USE FOR: sense resolution, online correlated streams, context-conditioned generation.

## UNIFIED demonstration (TEMPORAL-CONTEXTUAL-UNIFIED)
One SubstrateV31, one episode, all three operating TOGETHER: context-binding resolves polysemous perception (1.0) WHILE
decay+refresh retains core memory through 200 edits (1.0) WHILE a temporal policy integrates drives (127% escape). HARD_PASS.

## Validation summary
| claim | result |
|---|---|
| 3-domain meta-pattern | INTEG-TEMPORAL 173% / CORE-REFRESH 1.0 / POLYSEMY-CONTEXT 1.0 |
| seed-robust (n=5) | escape 124%+/-12, refresh 1.0+/-0, polysemy 1.0+/-0 |
| unified (all together) | sense 1.0 / retention 1.0 / escape 127% |
| lifelong scale | core retention scale-invariant to 20K-50K edits |

## Honest residual
- Deep multi-drive conflict ~96% genuinely fundamental even temporally; temporal policy SOFTENS (operator-fixable residual),
  doesn't ELIMINATE. Route conflicts past worst-drive threshold to higher-level cultural-convention fallback.
- Neurogenesis online discovery: needs ADAPTIVE density-aware threshold (a contextual fix) + real-FB15K test (deferred).
- The substrate-vs-LLM boundary stands: LLM = parse-arbitrary-English + statistical fluency; substrate = everything structured.

## Self-improvement implication
The self-improvement loop closes via TEMPORAL substrate-state updates + CONTEXT-FIELD-modulated rules -- NOT fixed
architectural protection. Substrate self-modifies via TIME (refresh) + CONTEXT (binding); integrates via TIME (policy);
communicates via systematic generation (algebra) + CONTEXT (context-bound emission); reasons via composition (algebra).
