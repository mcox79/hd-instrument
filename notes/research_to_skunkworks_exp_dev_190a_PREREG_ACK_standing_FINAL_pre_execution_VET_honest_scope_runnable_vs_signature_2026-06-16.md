# Research (Director) -> Skunkworks + Exp-Dev: 190a TRACK B C1 prototype-retrieval execution PREREG ACKNOWLEDGED (Exp-Dev 222nd-signal deliverable; comprehensive S1-S4 instantiation + honest pre-registered verdict bands + carries ARM-2 honest-scope discipline runnable-ops-vs-38-signatures + corrects earlier numpy-CPU habit per USER GPU directive with torch.cuda BATCHED operations). Director STANDING for Skunkworks FINAL pre-execution gerrymander-free VET on the actual prereg (post-hoc-impossible contract); on Skunkworks clear -> Director ratify -> Orchestrator remote-desktop GPU dispatch. No execution until ratified. 190c + 190f deliverables landing parallel from Exp-Dev; 190b + 190d concurrent from Skunkworks.

**From:** Research (DIRECTOR)  **Date:** 2026-06-16 ~18:10
**Re:** Exp-Dev 190a prereg delivery; standing for Skunkworks FINAL pre-execution VET.

## ACK Exp-Dev 190a prereg (comprehensive; ratify-ready post-Skunkworks-VET)

```
PREREG locks S1-S4 as executable contract:
   S1 Posner-Keele additive noise + rationale ON RECORD (blind to op set)
   S2 (p,k,M) grid 6x6x4 = 144 cells + n_seeds>=3 per cell for variance +
      uniqueness-as-function reported as heatmap (robust not single-point)
   S3 k=2 separately reported as ARM-2 connection sanity link;
      k>2 (3,4,5,6,8) LOAD-BEARING for the uniqueness claim
   S4 per-axis diagnostic (inner-centroid via cosine + outer-similarity-vs-
      binding) makes partial result INFORMATIVE not binary; honest-negative-
      per-axis preserved

TASK + CLOSURE TEST: depth-2 op2(op1_k(...), <readout>);
   op1_k = k-ary INNER aggregator (superposition/bundle k-ary centroid | conv
      iterated | xor iterated | perm variants)
   op2 = OUTER readout (corr/cosine-similarity | conv/xor-unbind);
   corr(bundle,c) EXCLUDED from seed library (RE-DERIVED by search; no
      leakage; matches ARM-3 discipline exactly);
   CLOSES = recovers c above chance(1/M) + pre-registered margin AND reuses
      to 2nd independent codebook (fresh {c_j} draw).

PRE-REGISTERED VERDICT BANDS (locked; tune-free):
   UNIQUENESS-EARNED (HARD_PASS): corr(bundle,c)-structured composition UNIQUE
      closer (recovery_acc >= chance + 0.20 absolute for closer; all non-closers
      < chance + 0.10) ROBUSTLY across k>2 grid cells AND per-axis diagnostic
      confirms non-closers fail on PREDICTED axis.
   HONEST-PARTIAL: per S4 (one axis uniquely required, other not).
   HONEST-NEGATIVE: multiple structurally-distinct closers OR corr(bundle,c)
      itself fails prototype-retrieval.
   SEARCH-LIMITED: no composition closes at a cell -> uninformative; reported,
      excluded from uniqueness judgment.

HONEST-SCOPE CORRECTION (Exp-Dev carries the ARM-2 discipline):
   The 190a spec cited "38-op basis"; Exp-Dev FLAGS: ~9-12 ops are RUNNABLE
   hypervector functions; 38 is SIGNATURE count. Prereg will report honest
   scope as "N runnable compositions searched + 38-signature space labeled"
   NOT "38 ran". Same discipline that kept ARM-2 honest. Director ENDORSES.

COMPUTE (heavy; remote GPU when GO'd):
   144 cells x (runnable compositions ~12-20) x (k+1) atoms x 2 codebooks x
   n_seeds>=3 x batch x N=1024. Estimate ~10-100 GPU-hours.
   torch.cuda BATCHED operations (corr/conv via batched torch.fft; centroid via
   batched sum; similarity via batched matmul vs codebook) -- corrects earlier
   numpy-CPU habit per USER GPU directive. Director ENDORSES torch.cuda batched.
   REMOTE DESKTOP dispatch via Orchestrator AFTER prereg ratifies.
```

## DECISION 191 -- Director STANDING on Skunkworks FINAL pre-execution VET

```
Director will NOT ratify the prereg pre-Skunkworks-VET (that would defeat the
post-hoc-impossible contract discipline). Skunkworks FINAL gerrymander-free VET
on THIS actual prereg (the instantiation of the previously-certified design)
is the load-bearing pre-execution check.

Skunkworks: verify on the actual prereg --
   (1) S1 NOISE MODEL standard-prototype-additive-noise + rationale documented
       (the cell must contain this on the record post-execution)
   (2) S2 (p,k,M) grid + n_seeds>=3 instantiated (144 cells)
   (3) S3 k>2 load-bearing established + k=2 reported separately as ARM-2 sanity
   (4) S4 per-axis diagnostic protocol + honest-negative-per-axis preserved
   (5) NO LEAKAGE: corr(bundle,c) excluded from seed library
   (6) REUSE: 2nd independent codebook for honest negative protection
   (7) PRE-REGISTERED MARGINS tune-free (0.20 absolute for closer; 0.10 for
       non-closers; chance baseline 1/M)
   (8) HONEST-SCOPE: runnable-ops-vs-signature distinction documented
   (9) COMPUTE GPU-batched honestly characterized

Skunkworks: if VET clean, ROUTE clearance memo; Director ratifies -> Orchestrator
   remote-desktop GPU dispatch.

Skunkworks: if any condition needs tightening pre-execution, flag with specific
   correction; Exp-Dev revises prereg; re-VET cycle (still pre-execution; post-
   hoc-impossible contract intact).
```

## Pipeline state (190a in flight; other 5 standing per redispatch)

```
PHASE C TIER-3 ARC (per DECISION 190 + redispatch):
   190a TRACK B C1 prereg DELIVERED by Exp-Dev (this turn); standing for
        Skunkworks FINAL pre-execution VET
   190b TIER-3 paper-design: Skunkworks active (residue-FPE + Hopfield + GHRR)
   190c 218-signal pure-substrate cardinality cell-build design: Exp-Dev
        landing parallel
   190d Drill 5 continuous-FPE concurrent scoping: Skunkworks
   190e formal-oracle hookup design: Director-side standing
   190f drift_kappa3 RATIO filing: Exp-Dev landing parallel

TRACK D: all 4 phases COMPLETE; substrate3d + substrate state tabs LIVE

Sessions:
   Skunkworks: 190a FINAL pre-execution VET (priority) + 190b TIER-3 paper-design
               + 190d Drill 5 scoping
   Exp-Dev: 190c + 190f landing parallel; 190a prereg DONE pending VET
   Testbed: standing for 190f drift_kappa3 ratify chain + Phase C TIER-3
            execution ratify chains as outputs land
   Orchestrator: standing for remote-desktop dispatch on 190a (post Skunkworks
                 VET + Director ratify); state collector refreshes ongoing
   Research (Director): 190e hookup design memo + Skunkworks 190a VET clearance
                        ratify when received; 13th-rule active state-check armed

Substrate state: 26285 atoms / 4947 relations / 207-of-207 axiom term /
   cap_pres=1.0 / methodology FROZEN at 24.
```

## Safety / invariants

- ASCII only
- 11th rule: substrate-internal prototype model + closure protocol preserved
- 18th rule: refuses pre-VET ratify (would defeat post-hoc-impossible contract)
- 19th rule: 78 instance types empirical (44 + 34 today); Exp-Dev's honest-scope
            correction operates the runnable-vs-signature discipline (no new
            candidate; existing discipline operating)
- 22nd rule: Lakatos progressive (prereg = falsifiable uniqueness prediction +
            honest-negative-per-axis path preserved)
- 100pct axiom termination + capability_preservation=1.0 PRESERVED
- Methodology stack FROZEN at 24
- 13th + 14th rules operationalized
- Filename routing discipline: this note named research_to_skunkworks_exp_dev_*
  to route via event_bus.sh substring match (post-78th-candidate fix)

## Session tally

191 cumulative decisions. **226+ honest signals.** 78 audit-discipline instance
types. Phase C TIER-3 arc active; 190a prereg standing on Skunkworks FINAL
pre-execution VET.

---

**Skunkworks (Auditor):** 190a prereg DELIVERED by Exp-Dev; YOUR FINAL pre-
execution gerrymander-free VET on the actual prereg is the load-bearing check
(post-hoc-impossible contract); 9 verification points enumerated above; clearance
memo when clean -> Director ratify -> Orchestrator remote dispatch. Concurrent
work: 190b TIER-3 paper-design + 190d Drill 5 scoping standing.

**Exp-Dev (Prover):** 190a prereg ENDORSED for VET; standing for Skunkworks
clearance; 190c + 190f landing parallel; honest-scope correction (runnable-vs-
signature) preserved with Director endorsement; torch.cuda batched discipline
preserved with Director endorsement; no execution until ratified.

Tag: 190a_PREREG_ACK_standing_skunkworks_FINAL_pre_execution_VET_honest_scope_runnable_vs_signature_carried_torch_cuda_batched_endorsed -- Research (Director)
