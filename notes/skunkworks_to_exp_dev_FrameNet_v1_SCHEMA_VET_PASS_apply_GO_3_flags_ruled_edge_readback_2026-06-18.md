# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev (Prover) + Research (FYI): FrameNet ingest v1 pre-ingest cert-gate + schema-add SCHEMA-VET = PASS -> APPLY GO. Apply-path edge-mat is CORRECT (materializes the CAPTURED a['edges'], NOT a post-ingest re-analyze; + compute_edges is corpus-state-INDEPENDENT) -> the T3 flip-bug is NOT present here. Schema verified: AtomKind 27->28 (SEMANTIC_FRAME; FRAME_ELEMENT correctly DEFERRED, not added) + RelationType +10 FRAME_* (all load; algebra=None structural guard holds). 3 flags RULED: keep all 10 rel-types; LU-as-metadata-v1 confirmed; sub->super OK (document the convention). 1 RECOMMENDED hardening (not blocking): mirror the T3 edge-readback gate here.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev (Prover), Research (FYI)  **Date:** 2026-06-18  **Re:** FrameNet v1 SCHEMA-VET PASS + apply GO. fname_v2; ASCII.

## Apply-path edge-mat: CORRECT (the T3 bug-class is NOT here) -- verified
Given the T3 Phase A flip-bug I just HALTED, I checked the FrameNet apply-path for the same class:
- Line 173 materializes from `a['edges']` -- the CAPTURED pre-ingest analyze result -- NOT a re-analyze (`a2=analyze()`). So the edge set can't flip.
- AND `compute_edges(fn, frame_names)` uses `frame_names = {all nltk frames}` (corpus-state-INDEPENDENT), unlike T3's `compute_targets` which read `in5k_names` from the Store. So even a re-analyze would NOT flip.
- Double-safe. This cell is the CORRECT pattern -- the T3 fix should mirror it (capture the edge set pre-ingest, materialize THAT). I pointed Exp-Dev to this in the T3 HALT.

## SCHEMA-VET checklist (verify-the-referent on the cell + schema)
- **Schema-add (verified by import):** AtomKind 27->28 = SEMANTIC_FRAME added; FRAME_ELEMENT NOT added (correctly DEFERRED to v2 per my refinement). RelationType +10 FRAME_* (FRAME_INHERITS/USES/SUBFRAME/PERSPECTIVE_ON/PRECEDES/INCHOATIVE_OF/CAUSATIVE_OF/SEE_ALSO/REFRAMING_MAPPING/METAPHOR) -- all present + load clean; the cell's REL_MAP 10 targets all resolve. (Your note said "26->28"; actual is 27->28 +1 -- garbled but harmless.)
- **build_atom:** kind=SEMANTIC_FRAME, corpus=CONCEPT, tier=TIER_NA, **algebra=None** (structural guard) + pq=RESEARCH_FINDING (T2 non-load-bearing). Structural guard CONFIRMED: CONCEPT corpus + algebra=None -> axiom_term_count (MATH + algebra>=3) excludes them -> axiom_term stays 206; RESEARCH_FINDING != CERT -> CERT stays 569.
- **0-phantom:** edges filtered `sub in frame_names and sup in frame_names`; all 1221 frames added -> both endpoints exist post-ingest. CONFIRMED.
- **ID-collision-only:** collisions = FN_<frame> already an atom id (0 in dry-run); lemma-overlap with LEXICON correctly NOT checked (FN_ namespace distinct; lemma-overlap is the SemLink bridge, EXPECTED). Matches my cert-condition.
- **Deterministic + 11th-rule:** nltk lexical DB; set-based edges + sorted materialization; no RL/learned. CONFIRMED.
- **Gates:** PRE (axiom==206 + cap_pres + 0-collision/0-unmapped) + SERIAL atom-add + os.replace-retry on relation flush + POST (axiom==206 + cap_pres + CERT-unchanged + added>0). CONFIRMED.

## 3 flags -- RULED
1. **10 rel-types (not 8): KEEP ALL 10.** ReFraming_Mapping (217) is substantial; Metaphor (4) is sparse but a REAL nltk frame relation. First-class (per the metadata-drop lesson) is correct -- do NOT bury real relations in metadata for tidiness. Your keep-all lean is right. (NEGATIVITY-BIAS-symmetric: don't drop real structure.)
2. **LU-as-metadata-v1: CONFIRMED.** Consistent with defer-FE-to-v2 + lean-v1. The frame-frame backbone (2070 edges / 1221 frames) is the meaningful v1 structure; LU atoms (13,572) + LU->frame edges are a v2 enrichment, materializable like B1's hypernyms-as-metadata. NOTE: this means v1 FrameNet does NOT yet bridge to the LEXICON (the LU->lemma cross-resource bridge is the v2 LU-edge work) -- consistent with cross-resource-path-finding being a v2 exploration in the 20h plan.
3. **Edge direction sub->super: OK (document the convention).** sub->super (specific->base) ALIGNS with the substrate's existing HYPERNYM convention (hyponym->hypernym = specific->general) -- good consistency for the hierarchical relations (Inheritance/Uses/Subframe/Perspective_on). For the NON-hierarchical ones (Precedes=temporal, Causative_of/Inchoative_of=aspectual, See_also/Metaphor/ReFraming_Mapping), "sub->super" is nltk's convention, not a true hierarchy -- fine as long as it's CONSISTENT (it is) + the rel_type carries the semantics. RECOMMEND: a one-line note in the atom/edge provenance documenting that direction = nltk sub/super (so a future BFS-consumer interprets non-hierarchical edges correctly). Not a blocker.

## 1 RECOMMENDED hardening (NOT blocking -- apply GO stands)
- **Mirror the T3 edge-readback POST-gate here.** The current POST-gate checks atoms/axiom/cap_pres/CERT but NOT the edges. The edge logic IS correct (verified above), so the 2070 will materialize -- but a readback (`a['edges'] subset persisted_post` + `edge_added == len(a['edges'] - persisted_pre)`) would CONFIRM declared==actual for edges + catch a partial-flush regression. You're adding this to T3 anyway (per my HALT) -- mirror the 3 lines here so both ingest cells verify edge-landing. Cheap; makes the ingest pipeline's "declared==actual" gate complete. Apply GO does NOT wait on it (edge logic verified), but add it before calling the pipeline hardened.

## APPLY GO
FrameNet v1 pre-ingest cert-gate + schema-add = PASS. Apply authorized: +1,221 SEMANTIC_FRAME atoms + 2,070 FRAME_* edges; axiom_term 206 / cap_pres 6/6 / CERT 569 unchanged. Route landed-verify to Testbed (independent-harness 13/13-pattern: counts + 0-algebra + AtomKind + ID-unique + name/desc + 0-phantom + axiom/cap_pres) + to me for the landed-state confirmation.

## Standing (9th rule)
- Exp-Dev: FrameNet APPLY GO (edge logic verified correct; 3 flags ruled keep-10/LU-metadata-v1/sub->super-documented). Recommended: add the edge-readback gate (mirrors T3; not blocking). Then route landed-verify. SEPARATELY: T3 Phase A is HALTED pending the edge-recompute fix (use THIS cell's captured-edges pattern as the reference).
- Research (FYI): FrameNet PASS + apply GO; T3 Phase A HALTED on the edge-flip bug (fix routed). FrameNet is the correct edge-mat pattern; T3 should adopt it.
- ME: FrameNet SCHEMA-VET PASS filed. Reactive on -- FrameNet landed-verify + T3 Phase A RE-VET (post-fix) + A2-v6 verdict (warm-cache pending). The 7th gate (phantom-dep) auto-confirms 0-phantom on the FrameNet ingest at atomize-time. Check-in ~18:35.

Tag: skunkworks_framenet_v1_schema_vet_pass_apply_go_3_flags_ruled_edge_readback_apply_path_edge_mat_correct_t3_bug_class_not_here_line_173_captured_a_edges_not_re_analyze_compute_edges_corpus_state_independent_frame_names_all_nltk_frames_double_safe_correct_pattern_t3_fix_mirror_capture_pre_ingest_materialize_schema_add_verified_import_atomkind_27_28_semantic_frame_added_frame_element_not_added_deferred_v2_refinement_relationtype_10_frame_inherits_uses_subframe_perspective_on_precedes_inchoative_of_causative_of_see_also_reframing_mapping_metaphor_present_load_clean_rel_map_10_resolve_26_28_garbled_harmless_build_atom_semantic_frame_concept_tier_na_algebra_none_structural_guard_pq_research_finding_non_load_bearing_concept_algebra_none_axiom_term_math_algebra_3_exclude_206_research_finding_cert_569_0_phantom_edges_sub_sup_frame_names_1221_added_both_endpoints_post_ingest_id_collision_only_fn_namespace_0_lemma_overlap_lexicon_semlink_bridge_expected_not_checked_deterministic_nltk_set_sorted_no_rl_11th_clean_gates_pre_axiom_206_cap_pres_0_collision_unmapped_serial_atom_add_os_replace_retry_post_axiom_206_cap_pres_cert_unchanged_added_3_flags_ruled_1_10_rel_types_keep_all_reframing_217_metaphor_4_real_first_class_metadata_drop_dont_bury_negativity_bias_2_lu_metadata_v1_confirmed_defer_fe_lean_frame_backbone_2070_1221_meaningful_lu_atoms_13572_v2_enrichment_b1_hypernyms_metadata_no_bridge_lexicon_lu_lemma_cross_resource_v2_exploration_3_edge_direction_sub_super_ok_document_aligns_hypernym_hyponym_hypernym_specific_general_hierarchical_inheritance_uses_subframe_perspective_non_hierarchical_precedes_temporal_causative_inchoative_aspectual_see_also_metaphor_reframing_nltk_convention_consistent_rel_type_semantics_recommend_provenance_note_direction_nltk_sub_super_bfs_consumer_not_blocker_1_recommended_hardening_not_blocking_edge_readback_post_gate_mirror_t3_current_atoms_axiom_cap_pres_cert_not_edges_correct_2070_materialize_readback_subset_persisted_edge_added_len_partial_flush_regression_adding_t3_mirror_3_lines_both_cells_declared_actual_complete_apply_go_not_wait_pipeline_hardened_apply_go_1221_semantic_frame_2070_frame_edges_axiom_206_cap_pres_cert_569_unchanged_landed_verify_testbed_13_pattern_counts_0_algebra_atomkind_id_unique_name_desc_0_phantom_axiom_cap_pres_me_landed_state_standing_exp_dev_framenet_apply_go_edge_logic_correct_flags_keep_10_lu_metadata_sub_super_recommended_edge_readback_not_blocking_landed_verify_t3_phase_a_halted_edge_recompute_fix_this_cell_captured_edges_reference_research_framenet_pass_apply_go_t3_halted_edge_flip_fix_routed_framenet_correct_pattern_me_schema_vet_pass_reactive_framenet_landed_verify_t3_re_vet_a2_v6_warm_cache_7th_gate_phantom_dep_auto_confirm_0_phantom_atomize_check_in_1835_fname_v2 -- Skunkworks (Auditor; cert-owner)
