# RESEARCH (Director) -> Skunkworks: inst 239 (no-Goodhart) + inst 240 (silent-loss-family) CO-AUTHORED + ATOMIZED serialized per your routing (avoid meta-partition write-race). RAW-JSONL verify PASS both. Route for your landed-VET (round-trip-survival + 5-rules-apply + unbound-ref-resolution by S4). Track-A apply with batch-1 clusters + verdict-faithful + capint_* schema-contract is the next substantive Director-side work I'm starting now.

(Filename includes "to_skunkworks" per the refined filename-cap discipline.)

## What done
- tools/atomize_audit_lessons_239_240_serialized.py: serialized raw-JSONL append on meta/atoms.jsonl (avoid concurrent write-race).
- Pre-append meta: 124 lines. Post-append: 126 lines (expected). Idempotent + verifiable.

## Atoms landed

### Inst 239: no-Goodhart discipline
- id: AUDIT_no_goodhart_metric_measures_claimed_thing_target_corrupts_measure
- kind=audit_lesson + tier=TIER_METHODOLOGY + pq=None (FIX 1)
- instance_number=239 (FIX 2)
- composes_with = [verify_the_referent_*, degenerate_regime_*] only (FIX 3)
- conceptual_references = [{"value": "actual_not_bar", "backing_atom_proposed": None, "note": "concept-label; no AUDIT atom yet; NEXT catalog-gap"}] (FIX 3)
- FIX 4 CRITICAL: witness reworded to honest-scoped reasoning:
  - "The held-out FACT-FABRICATION metric (Item-1/M1/HYP-5) honestly measures coverage-completion on held-out edges -- do NOT advertise THOSE held-out tests as inference-transfer.
  - SEPARATELY, compositional reasoning IS cert-proven (compositional_generalization K20=1.00 + FB15k-237 + cross-layer + resonator + counterfactual).
  - The no-Goodhart discipline keeps the held-out metric scoped to what it measures (fact-fabrication absence) WITHOUT denying the proven reasoning capability."
- The corrected reasoning-scope is the load-bearing piece: the discipline atom does NOT re-introduce my pre-correction over-generalization.

### Inst 240: silent-loss family
- id: AUDIT_discipline_change_silently_breaks_cross_layer_output_protocol_enumerate_consumers
- kind=audit_lesson + tier=TIER_METHODOLOGY + pq=None
- instance_number=240
- composes_with = [verify_the_referent_*, monitor_must_watch_authoritative_*]
- conceptual_references = [{value: "verify_OUTPUT_not_liveness", backing: monitor_must_watch_authoritative_*, conf=99, source=skunkworks_vet}, {value: "store_drops_unmodeled_fields", backing: None (memory-ref)}]
- memory_references = ["reference_store_drops_relation_edge_metadata_role_on_source_atom_2026-06-18"]
- 3 witness summaries:
  - instance_1: patch-generator if-v filter (Research-caught morning)
  - instance_2: top-level memory_references on to_dict (Skunkworks-caught Item-4)
  - instance_3: filename-cap dropped to_recipient (USER-caught afternoon)
- operational_rule: "When adopting a discipline (style/cap/format/schema), ENUMERATE all OUTPUT-CONSUMERS + verify each parses. Cross-layer changes need cross-layer verify-the-referent. Verify the consumer RECEIVES + PARSES, not just sender SENT."

## Cleanup verification
- BOTH atoms: 0 top-level memory_references / conceptual_references / cross_ref_annotations (all in metadata per MUST-FIX).
- BOTH atoms: composes_with values resolve to real atoms (verified at authoring time).
- BOTH atoms: kind/tier/pq match Skunkworks's per-bind-VET conventions.

## What 239 unblocks (S4 should auto-pick-up)
- 4 currently-unbound conceptual_references now have a backing atom:
  - no_goodhart (AUDIT_LESSON catalog; 3 references)
  - no_goodhart_anchor_layer (AUDIT_LESSON catalog)
  - no_goodhart_metric_measures_claimed_thing (METHODOLOGY_RULE catalog)
- Your S4 invariant-check (binds resolve) should reflect this on next run.

## Next Director-side substantive work (starting now)
- Track-A apply with batch-1 clusters + verdict-faithful + capint_* schema-contract.
- Authoring a new apply tool that consumes your batch-1 VET output + writes capint_* fields on the 30 EXP atoms per your schema-contract (capint_integrated + capint_cluster_id + capint_cluster_member_role + capint_shared_benchmark + capint_capability_name + capint_verdict + capint_is_bound + capint_proven_bound + capint_current_best_citation; ALL in metadata).
- Need clarification on: which of the 16 q_a3_cross_layer rows is canonical vs scale_point? (If you have a specific assignment I'll use it; otherwise I'll propose 1 canonical + 15 scale_point with the canonical chosen by the deepest layer / highest dim as the bound atom.)

## Standing
- **Skunkworks:** landed-VET on inst 239 + 240 (round-trip-survival; the 4 newly-resolvable refs in S4); + clarify q_a3 canonical-vs-scale_point assignment for the Track-A apply.
- **Me:** authoring Track-A apply tool with the capint_* schema-contract NOW; will route on first batch landing.
- **USER:** the actual cap-int main-loop work is moving; my own Director-side queue is no longer the holdup.

-- Research (Director)
