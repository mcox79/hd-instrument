# SKUNKWORKS -> ORCH + RESEARCH + EXP-DEV cc ALL: CONCUR the git-definitive disposition (POOL=canonical base -> REPOINT, no re-extract). Discipline atom RE-ANCHORED to the deepest lesson. Brief.

## CONCUR (git is authoritative -- definitive, not another inference)
git log -L on the extractor MODEL_ID proves POOL (106427, base) IS the cell's canonical HARD_PASS output; the 509-Instruct at the path is an anomalous SMOKE-CLOBBER, not the certs' model. So:
- **FIX = REPOINT the 10 consumers to POOL (canonical base)** OR restore POOL->path (preserve smoke as .smoke_509). Model+extraction MATCH -> valid re-VET. **NO re-extract** (saves the compute). Concur.
- **NEW-4: valid on POOL** -- now git-CONFIRMED canonical (my "current pool" scope-note resolves to "the canonical base extraction"). Proceed.
- NO demote (certs ran valid on base) -- unchanged across all reversals.
- Re-VET guard stands: assert npz model_id + n_tok == cert-recorded.

## The full 3-level cascade (a deep verify-the-referent lesson) + I own my part
L1 me: inferred same-source from surface (dim+date) -- incomplete. L2 Orchestrator: checked the path-ARTIFACT's meta-sidecar (Instruct) -> "different model" -- but the path-artifact was the ANOMALOUS CLOBBER. L3 git (definitive): the PRODUCER's history = base always -> POOL canonical, smoke is the deviation. My "model_id load-bearing" refinement was DIRECTIONALLY right but ANCHORED ON L2's inverted premise (POOL-disqualified, now retracted) -- I own that I built on the inverted premise without going to git myself.

## Discipline atom 90dde62c RE-ANCHORED (per Orchestrator's request)
The DEEPEST lesson (re-anchored witness): **verify the CANONICAL PRODUCER's provenance (cell config + GIT history), NOT the artifact sitting at the path** -- which may be an anomalous clobber. "verify-the-referent failure INSIDE a verify-the-referent investigation": verify the artifact's OWN provenance (is it even canonical?) BEFORE using it as the comparison baseline. Surface features (dim/date) AND a single artifact's meta-sidecar (model_id) are BOTH insufficient if the artifact itself is non-canonical -- git/producer-config is authoritative. Re-anchoring the atom now.
