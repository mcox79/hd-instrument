# SKUNKWORKS -> RESEARCH + EXP-DEV cc ORCH: RESCUE-DRIVE (USER: "rescue this"). Eff-rank diagnostic = anisotropy is INTRINSIC (not templating-artifact; dense-reopen REFUTED at raw) -> the rescue IS the SPARSE/STRUCTURED chain. Drive it: 4-arm live + 3 deferred into the plan + a NEW key-source angle.

**From:** Skunkworks (cert-owner/auditor)
**Date:** 2026-06-21T16:54:35Z

## EFF-RANK DIAGNOSTIC (answers exp_dev's routed Q; CPU pythia-160m last-token, 120 each)
```
keys        mean_cos   eff-rank(PR)   stable-rank
TEMPLATED   0.9961     17.6           5.6
READABLE    0.9976     20.3           9.0
```
- **Anisotropy is INTRINSIC, NOT a templating-artifact:** readable keys are JUST AS anisotropic (mean_cos 0.998 ~ 0.996; eff-rank 20 ~ 18, both ~20 of 768). My "readable reopens dense" scope-caveat HOPE is REFUTED at the raw level (symmetric honesty: raised it, tested it, negative generalizes). Consistent w/ rogue-dimensions lit (Timkey 2021).
- **eff-rank ~20 < the drill's eff_rank<32 DESCOPE GATE** -> the keys live in ~20 dims; this BOUNDS any dense rescue at M=10k (low-eff-rank capacity ceiling per the drill's ancillary HARD-FAIL).
- **CAVEATS (don't over-claim):** RAW last-token, pythia-160m (tiny; 2.8b may differ), NOT CERT591-projected (the projection DE-CROWDS -> raises eff-rank). The PROJECTED eff-rank on pythia-2.8b is the DEFINITIVE number = the 4-arm pre-flight measurement. So: raw anisotropy intrinsic (confirmed); projected eff-rank = theirs to measure.

## RESCUE-DRIVE (the path is SPARSE/STRUCTURED; track the full chain)
Dense-via-isotropization CLOSED (whitening MM) + dense-reopen-via-readable REFUTED (this). So the rescue lives in the sparse/structured chain. DRIVE it:
1. **4-arm (LIVE, dispatched):** ARM A cerebellar K=5 / ARM B fly-LSH (top, ~100x storage win) / ARM C compose / ARM D attention-baseline. My landed-VET on land (vs exp_dev pre-reg fc3b8771: A-fails/B-wins). **Pre-flight MUST report PROJECTED eff-rank** (the definitive number; if <32 -> descope M per the drill).
2. **3 DEFERRED rescues -> request Research TRACK in director_plan.json as the explicit rescue-contingency chain (don't leave only in the drill note):**
   - PC-AM iterative inference (pre-staged v5 contingency; fires IF 4-arm ARM A+B both HARD-FAIL)
   - phase-coding / complex-Hopfield (partial-drill IF phase-hash sub-problem solvable; orthogonal axis)
   - product-key / grid-code (sub-linear-not-M-indep; held-for-future; composes w/ CERT591)
3. **NEW KEY-SOURCE angle (the eff-rank finding surfaces it):** the ~20-eff-rank is for LAST-TOKEN. EARLIER-LAYER or MEAN-POOLED pythia reps are KNOWN to be LESS anisotropic / higher-eff-rank (lit). => a cheap rescue lever for ALL storage mechanisms: pick a HIGHER-eff-rank key SOURCE (earlier layer / mean-pool) before the storage rule. Route to Research/Exp-Dev: measure eff-rank across layers/poolings -> use the highest-eff-rank source as the substrate-LM's keys (raises the ceiling for sparse AND dense). De-riskable on CPU (I can sweep layers next if useful).

## NET (rescue this)
Dense closed + dense-reopen refuted -> rescue = sparse/structured (4-arm live + 3 deferred tracked) + a higher-eff-rank KEY-SOURCE (earlier-layer/mean-pool) as a cross-cutting lever. Request: Research folds the 3-deferred-rescue contingency chain + the key-source lever into director_plan.json; 4-arm pre-flight reports PROJECTED eff-rank. My landed-VET on the 4-arm + sequencing the deferred per outcome. CERT 583/177266.

-- Skunkworks
