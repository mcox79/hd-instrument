# Unifying synthesis (2026-07-22): the glass-box substrate COMPOSES over given structure; INDUCING structure from raw data is the wall

The session's converged, multi-thread finding, each pillar VET-banked or lit-corroborated. This is the consolidated statement the individual atoms/notes point to.

## THE CLAIM
The glass-box VSA/HDC substrate is a strong **COMPOSITIONAL ENGINE over GIVEN structure**. The frontier — where gradient/opacity may be unavoidable — is **INDUCING the structure from raw data**. "Learned, content-dependent structure" is the single ingredient that unlocks every sub-result; given it, glass-box composition works; the open question is whether it can be *induced* glass-box.

## THE PILLARS (evidence)
1. **Given-structure COMPOSITION works (glass-box).** b1 generative-replay-as-traversal (atom **29445**, MM, VET-clean, no-known-T confirmed) ~DOUBLES autonomous multi-hop keyless: reach@2 0.181->0.319, reach@3 0.138->0.321 (86% of oracle), frac_closed 0.431, content-dep fires 0.107 (78% relational / 22% generic-graph, honest), held-out replicates (pairing+replication, not disjoint-subgraph). Recovers 0.735x of the SR known-T ceiling KEYLESSLY. Strong PARTIAL, misses the 0.5-gap HARD_PASS bar -> NOT chain-grade. Composes the greedy(0.181)/supplied-oracle(0.500)/SR-known-map(0.434) MM anchors.
2. **The STORE's crosstalk is fixed by LEARNED-sparse separation, keyless (atom 29444, MM, VET-clean).** Soft-shard aob dense -0.085 / hard-shard +0.123 / soft-shard +0.093 (76% of ceiling), keyless single-shot recall confirmed. Recovery REQUIRES learned separation -- every fixed-RANDOM-sparse config fails. Narrow basin (learned-center + expansion + modest sparsity; ~76%; digits-null = 40-class-scale phenomenon). Store thread CONVERGED: reasoning store = LEARNED-sparse-separated, keyless, single content-addressable store.
3. **INDUCING structure FAILS for fixed/linear learners.** atom 29440 (linear atomize+sleep reduces analytically to fixed-similarity vote); atom 29443 (real Linzen agreement: head feature REPRESENTABLE [Stage-1=1.0] but a fixed-binding + LINEAR readout learns only a positional/count heuristic, NOT head-tracking -> ceiling = the LEARNER, not the representation; VET-confirmed fair).
4. **The sharding-downsides audit ([[sharding_downsides_disk_verified_audit_2026-07-22]]) shows the SAME split:** fixed/known-map/sequential composition works (SR-reachability = MM known-map; scatter-gather; sequential 2-hop); learned/autonomous/relational was the open frontier (joint cross-shard bind HARD_FAILs; autonomous multi-hop ~36% of oracle -- which b1 then lifted). Cross-shard RELATIONAL reasoning without a known map = the same induction wall.
5. **Literature corroboration ([[literature_scan_leap_learner_binding_must_be_learned_content_dependent_2026-07-22]]):** CG-enabling structure "must be LEARNED from data, not imposed by architecture" and is content-dependent (structured beats scrambled; arXiv:2602.24264) -- but the literature's instances are GRADIENT-TRAINED. The consolidation drill ([[research_glassbox_consolidation_relational_geometry_2026-07-22]]): prototype-sleep = NO-GO (similarity wall, contested biology); generative-replay-traversal = GO for COMPOSITION (b1 confirmed); INDUCTION from raw text may need gradient.

## THE FORK IT DEFINES (b2 = the induction wall; USER-authorized to follow b1)
- The invariant-relevant question: can a GLASS-BOX (inspectable, non-backprop) mechanism INDUCE relational/grammatical structure (the head; the map) from raw data, beating the linear positional baseline (29443)? OR does induction fundamentally require a gradient/opaque component -> a GLASS-BOX-INVARIANT TRADEOFF decision (USER-locked invariant).
- b2 is designed glass-box-FIRST: attack induction with the strongest glass-box inducer; the invariant-trade only escalates to USER IF glass-box induction fails. b1 gives the context: pure glass-box composition doubles the baseline but PLATEAUS below the oracle -- the plateau may BE the induction ceiling.

## INDUCTION ARC RESOLUTION (2026-07-22 late; the fork above, answered)
The glass-box-first attack on induction was run EXHAUSTIVELY and CLOSED airtight, then RESOLVED constructively:
- **FOUR fixed-code glass-box induction attempts on real Linzen agreement head-tracking, ALL HARD_FAIL:** (1) b2 ADIOS/ABL chunking (29446, banked); (2) TGPH 12-symbol prediction (confounded by impoverished lexeme-free input); (3) fair dense real-word prediction (crosstalk-limited, worse than frequency); (4) sparse real-word prediction (the sparse fix WORKED -- prediction beats frequency +0.028 when brain-faithfully sparse -- but the head is NOT in the prediction residual); + the Elman representation-probe (fixed-code rep does NOT encode number; q=M(.)key is linearly equivalent to key per 29440; structure-shuffle drop ~0).
- **CONVERGENT FINDING (5 experiments + 3 independent lit-scans, UNANIMOUS not mixed):** relational structure CANNOT be induced from scratch by local/glass-box rules. Hebbian is provably PCA (Oja); local-error predictive coding either IS backprop (Rosenbaum 2022) or finds poorer/lower-rank reps (Garg-Vempala 2022); every genuinely relational neural code (TEM, VSA/HRR, tensor-product binding) requires the relational SCAFFOLD architecturally PRE-GIVEN (Smolensky; Fodor-Pylyshyn) -- the whole reason VSA exists.
- **THE CONSTRUCTIVE PIVOT (answers the fork WITHOUT surrendering):** we ALREADY HAVE the scaffold -- VSA role-filler binding IS TEM's structure x content factorization. The failed attempts put FIXED RANDOM codes inside it + used Hebbian RECALL (=PCA/surface); they NEVER TRAINED the codes with a predictive objective INSIDE the scaffold. TEM's recipe = pre-given factorized scaffold (we have) + predictive objective + gradient-trained content.
- **RESOLUTION = TEM-on-VSA (USER: "implement like the brain does"):** pre-given VSA scaffold + predictive objective + GRADIENT-trained codes inside it. INVARIANT: gradient@BUILD-time (pivot-authorized foundation; Hebbian=PCA is insufficient) + glass-box@RUNTIME binding/reasoning. This makes the A/B/C fork's option (A) CONCRETE + substrate-native (gradient-train OUR OWN factorized codes, NOT outsource a parser). Arms: TEM-on-VSA vs fixed-random-codes (fail control) vs flat-gradient (does the factorization help?). If HARD_PASS -> first structural-generalization landmark -> hardest VET.

Session store: atoms tail 29447, ~43 MM, ZERO false chain-grade. Key atoms: 29440, 29443, 29444, 29445, 29446, 29447.
