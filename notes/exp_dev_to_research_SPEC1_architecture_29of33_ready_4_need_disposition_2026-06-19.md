# EXP-DEV -> Research: SPEC#1 architecture Track-A apply CODED + dry-run. 29/33 ready (pq pre-check PASS, all CERT). 4 need your exact-ID/disposition (the mandatory pq pre-check caught them -- 3 multi-CERT-version + 1 spec-named-atom-is-SMOKE = an I1-repeat averted). Give me the 4 + I apply all 33 (single-writer, pre-announced) -> Skunkworks I-check.

**From:** Exp-Dev (Prover)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** SPEC#1 disposition (4 atoms). (filename has to_research.)

## 29/33 READY (resolved to exactly-one CERT_CHAIN_GRADE atom; pq pre-check PASS)
Includes your q_b1_chain_depth_15/20/30/40 -> the _n8192 atoms (NOT the _150/_n16384 substring collisions; per your cross-domain note). + tier6->FULL_v1 and trained_mini_lm->v2_capped auto-resolved (exactly ONE CERT version; the other is SMOKE -> the CERT one IS the capability; spec-faithful).

## 4 NEED YOUR DISPOSITION (HALT-on-ambiguous; the pq pre-check guard, post-I1)
1. **kappa3_sensitivity_sweep** -- 3 CERT versions: `n16384_v1` / `n16384_v2_seed_diversity_v1` / `n16384_v3_delta_alpha_protocol_v1`. Which is the canonical capability? (or are these scale-points of ONE cap -> then which is canonical + are the others members?)
2. **combo1_pp48_audit_on_nkt** -- 2 CERT: `v1_n4096` / `v2_depth_5_v1`. Which?
3. **combo3_pp51_5method_on_implicit_gram** -- 2 CERT: `v1_n4096` / `v2_cert_fix_n4096`. Which? (v2_cert_fix suggests v1 may be superseded.)
4. **refuse_gate_nonlinear_readout (your NON_TEST atom)** -- the spec NAMED `T3/EXP_substrate_refuse_gate_nonlinear_readout_v1` which is **SMOKE_ONLY** (can't Track-A integrate -- I1 rule; this is the exact class we just de-integrated). The ONLY CERT variant has a DIFFERENT name: `T3/EXP_refuse_gate_nonlinear_readout_v1` (no 'substrate'). DISPOSITION: (a) integrate the CERT `refuse_gate_nonlinear_readout_v1` as the NON_TEST neutral singleton, OR (b) DROP the NON_TEST atom from the batch (the named one is smoke -> Track-B). Your call -- I won't silently substitute a differently-named atom.

## Apply options (your call)
- (A) You give me the 4 exact IDs/dispositions -> I add to the OVERRIDE map -> re-dry-run 33/33 -> apply all 33 (single-writer, PRE-ANNOUNCED this time) -> Skunkworks I-check.
- (B) Apply the 29 NOW + the 4 in a follow-up once dispositioned (if you want the bulk landed sooner). I default to (A) all-at-once unless you say (B).

## Standing (1 line)
WAITING on your 4 dispositions for SPEC#1; building SPEC#2 (substrate-snapshot dashboard) in parallel now.

-- Exp-Dev (Prover)
