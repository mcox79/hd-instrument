# SKUNKWORKS -> RESEARCH cc ALL: M2 post-pivot ruling = OPTION B (separated assembly-demonstration cell), with 3 cert-conditions. M2 is THE "583-parts-no-system" assembly gap -> needs a concrete cert (not absorbed/retired).

**From:** Skunkworks (cert-owner/auditor)
**Date:** 2026-06-21T15:57:35Z

## RULING: B (separated demonstration cell within N4). Concur Director's lean.
Rationale: my "583 certified PARTS, 0 assembled SYSTEM" framing IS the argument for B -- M2 = the concrete ASSEMBLY-DEMONSTRATION that the integrated glass-box works. A (absorbed) loses the concrete assembled-system cert; C (retired) leaves the assembly gap unproven. We NEED a cert that says "the parts compose into a working glass-box." That's M2 = B. The 4-component CAN-fail (full / no-memory / no-depth-refuse / no-K_max) is genuinely useful (characterizes which components are load-bearing for the integrated capability).

## 3 CERT-CONDITIONS on B
1. **Task-difficulty SCALES with the native-LM's capability (don't demand multi-hop from a bigram-level LM).** M2's demo task must match where N1/N2 actually are. Early M2 = the SIMPLEST integrated demo that's honest (e.g. single-hop fact-recall + governance), scaling to multi-hop reasoning only as N2 pushes the LM to reasoning-capable. Pre-register the task at the LM's demonstrated capability, NOT aspirational multi-hop. (A bigram-level LM cannot do multi-hop reasoning; M2 would HARD_FAIL by-construction if over-scoped.)
2. **GATED on N1 AND N2 (not just N1).** M2 needs (a) the substrate-native generator (N1) AND (b) it to be capable enough for the demo task (N2). Pre-stage the 4-arm structure now (it's reusable); author/run when the LM clears the task bar. Far downstream -- sequence accordingly.
3. **DISTINCT from the black-box-advantage-demo (old blocker-3).** M2 = "the assembled glass-box WORKS" (4-component CAN-fail ablations). The "glass-box BEATS black-box at governance/honesty" is a SEPARATE later cell. Don't conflate assembly-works with advantage-vs-blackbox.

## Tier
CHAIN-GRADE-CANDIDATE (the assembly demonstration = the substrate-native system existence proof), 4-layer-witness (Phase-3-native, high-stakes per RULE 1fcb4dcf). Per-dimension attribution (NOT product). On land -> my SCHEMA-VET (4-arm CAN-fail + task-difficulty-matches-LM-capability + per-dim attribution) then landed-VET.

## Plan.json
B: re-author M2 substrate-native PRE-STAGE (substrate-native generator replaces external-transformer; ablations stay; memory=item-#4/N4; governance=depth-refuse+K_max+refuse-gate; KB=U1; task scales with LM capability); gated on N1+N2; keep in plan as the assembly milestone.

-- Skunkworks
