# SKUNKWORKS (Auditor) -> Research + Exp-Dev: DECISION 170c -- per-distinct-structure gate FORMALIZED into the ternary BUILD VET protocol. Concur convergence (canonical 20 math-scoped at threshold; 58th/59th audit candidates logged). The 20 math-scoped MOTIF-B instances rest on ~10 distinct sym-pair STRUCTURES (5 named clusters + ~5 less-concentrated) -> the graded ternary HARD-PASS must be a PER-CLUSTER + CROSS-CLUSTER result, not a raw 20-instance count (which would mask a concentration-failure).

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** per_distinct_structure_gate_FORMALIZED_5_clusters_cross_cluster_HARD_PASS_concentration_failure

## Per-distinct-structure gate (folded into ternary BUILD VET; per DECISION 170c)
```
  The 20 math-scoped MOTIF-B instances are NOT 20 independent structures -- they rest on ~10
  distinct sym-pair structures, 5 of them dominant (Exp-Dev's clusters):
     (i)   forward_algorithm / backward_algorithm
     (ii)  hilbert_space / inner_product
     (iii) DFT / FFT / circular_convolution cluster
     (iv)  dynamic_programming / viterbi_decoding
     (v)   bayes_rule / conditional_probability
     + ~5 less-concentrated structures
  GATE: graded ternary HARD-PASS reports + requires PER-CLUSTER, not just per-instance:
     [ ] report C2 corr(bundle,c) closure PER DISTINCT STRUCTURE (per-cluster HARD-PASS/FAIL)
     [ ] CROSS-CLUSTER requirement: the partial-symmetric composition must close a MAJORITY of
         the distinct structures (e.g. >=6 of ~10), NOT just the 1-2 dominant clusters. A HARD-PASS
         driven by a single dominant cluster (e.g. cluster (i) = 8 of 20 instances) is NOT a general
         partial-symmetry result -> report it as cluster-specific, not general.
     [ ] CONCENTRATION-FAILURE check: surface if (a) a dominant cluster passes while others fail
         (over-claim risk: "20 motifs pass" masks 1-cluster-only), OR (b) the dominant cluster fails
         while sparse clusters pass (the instance-count drops below threshold on the real structures).
```

## Why (composes with the at-threshold flag)
The HARD claim is ALREADY at-threshold (20 = exactly >=20, zero margin). Combined with concentration (~10 structures, 5 dominant), a raw "20 instances close" stamp could mask: a single cluster carrying the count, or a dominant-cluster failure dropping the effective structure-count below viability. Per-cluster + cross-cluster reporting is the integrity-correct readout: the partial-symmetry capability is GENERAL only if it closes across distinct structures, not concentrated in one family. This composes with the at-threshold-fragility gate (one contested instance/cluster fails the claim) + no-gerrymander (a concentrated result is a soft over-claim of generality).

## Ternary BUILD VET protocol -- now complete (folded gates)
```
  CLEAN-SYMMETRY two-layer scope (RELATES-excluded + math-corpus-scoped; document anchors dropped)
  38-op BIMODAL full-basis equivalence-check (C2 closes where all 38 single binders fail)
  no-gerrymander (real mined motifs) + vector-native (bundle+corr) + sibling sym/asym controls
  run_mode=full n>=3 + compute-backend provenance (near-threshold cross-backend check)
  AT-THRESHOLD fragility (claim require majority-close, not razor 20-count)
  PER-DISTINCT-STRUCTURE + CROSS-CLUSTER (this) -- general only if >=majority of ~10 structures close
  C3 = definitively-tier-2 pure-discoverability (FAIL = search-limited only)
```

## Net
Reconciliation arc CLOSED + the ternary BUILD VET protocol is now complete (per-distinct-structure gate folded). Canonical HARD claim = math-scoped MOTIF-B 20 (at threshold; ~10 structures; per-cluster-reported). Phase B GO Option B proceeds; the graded ternary verdict will be an honest per-cluster result, not a concentration-masked instance count. Standing for the graded BUILD vet (full gate set) on GO + the architectural decisions (USER).

Tag: per_distinct_structure_gate_FORMALIZED_5_clusters_forward_backward_hilbert_inner_DFT_FFT_DP_viterbi_bayes_cross_cluster_majority_close_required_concentration_failure_check_composes_at_threshold_no_gerrymander_ternary_vet_complete -- SKUNKWORKS (Auditor)
