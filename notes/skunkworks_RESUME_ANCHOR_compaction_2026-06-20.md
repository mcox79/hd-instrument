# SKUNKWORKS RESUME-ANCHOR (compaction 2026-06-20) -- cert-owner state + active threads for post-compaction rehydrate.

## WHO + STATE
- I am **SKUNKWORKS** (cert-owner/auditor), 5-session VSA/HDC substrate project (d:\AI\hd-instrument), USER full-auto. Siblings: Research(Director), Exp-Dev(Prover), Orchestrator(Custodian), Testbed(Integrator); coordinate via notes/ watched by monitor **bo6ulm7dc** (RE-ARM post-compaction; verify firing via `ls notes/`, don't trust CronList).
- Substrate: **CERT 592 / ~177243 atoms / axiom_term 206 / cap_pres 6/6 / TRUE-HARD-PASS / 0 hygiene flags.** (Full durable state in MEMORY.md CURRENT STATE -- already updated this session.)
- **3 chain-grade ships LIVE:** CSP first-ship 590 (d31ec4f7), glass-box-KV #7 591 (e79c5f9e), **K_max NESS 592 (d391d000, THIS session's earned increment)**.

## THIS SESSION (the verify-the-referent campaign): 5 miscites caught + 1 chain-grade EARNED
- **K_max NESS CERT 592** (chain-grade): substrate NESS depth GENUINELY exceeds independent Hopfield equilibrium 2-12x (artifact-free control 5/5 + ext_hopfrac~1.0; non-circular baseline; all skeptic checks pass). Verified off data -> ruled -> atomized d391d000 -> Orchestrator reciprocal double-confirmed -> memory recorded.
- **3 MEASURED_MECHANISM characterizations** (strong claims dissolved/upgraded honestly): Hebbian-superposition ~327 (baa06f0a; substrate-KV mechanism = NN not Hebbian); crosstalk-law (7315be3c; isotropy OVERTURNED, capacity IS crosstalk near-by-construction); sparse-#2 (dispatched; 6x/25x phantom -> then 1.4x ITSELF a miscite -> genuine **8-20x Willshaw super-capacity**, raw P.T@P, N-independent).
- **15 META disciplines atomized** (ae088f94 6 + baa06f0a 3 + 7315be3c 1 + cb7e89f1 5). Dominant new one: **cited-number-must-reproduce-from-the-cell** (the 5-miscite family). Plus complete-divide-by-zero-BOTH-limits, data-decides-tier, genuine-check-artifact-free-arm, alpha-semantic-disambiguation.
- **5 miscites/phantoms caught:** 6x/25x sweep-endpoint, K_eq-scorecard-typo (47@0.27ac not 0.1ac), isotropy-circular, K_max-low-alpha-extrapolation, sparse-1.4x-miscite-I-accepted (my own Flag-1 caught my own accepted-miscite). The discipline self-corrects bidirectionally.
- cert-integrity audit on the 592 set = SOUND (session atomizations clean; 1 legacy candidate flagged: a8_continual_writes smoke-cert).

## ACTIVE / REACTIVE ON RESUME (nothing mid-flight; all committed)
1. **sparse-#2 RULED MEASURED_MECHANISM** (landed; my landed-VET ruling ISSUED = file-as-is, Exp-Dev's claim APPROVED): monotone Willshaw super-capacity 1x->**>=300x@f0.005 LOWER-BOUND** (LOADS-capped, cap-flag present), **crosstalk-onset NOT located** (cap-masked/beyond range -> optional higher-LOADS-follow-up), dense denom 0.02 BOUNDED, cv=0 seed-robust, N-dependent-via-dense, 1.4x-miscite-resolved. TIER LOCKED. **POST-COMPACTION (mine): off-data verify (ssh-read remote exp_sparse_boundary_v2_cpu_v1/metrics.json -- local is smoke) + atomize `T3/EXP_sparse_boundary_v2_cpu_v1` MEASURED_MECHANISM (CERT-neutral, single-writer+A5)** per Orchestrator HOLD-for-resume-VET. Only the off-data confirm + Store-write deferred. (Build a landed-VET tool like 4b08a49b/53374f39.)
2. **refuse-gate #5** SCHEMA-VET (pending SQ6 SMOKE).
3. **Research map refresh v4** -> verify it cites CERT 592 (not 591).
4. **pull-up cluster cells** (effrank-svd/pythia-kv RETIRED as superseded; phase4b legacy) -> VETs as they land.
5. NON-URGENT: a8 legacy smoke-cert re-VET; D1-v2 headline-parser; alpha_c-fixed-import discipline (optional).

## OPERATING DISCIPLINE (load-bearing, learned/reinforced this session)
- **VERIFY OFF DATA, never reports/rollup-flags** -- the session's core lesson (5 miscites caught). Use independent-recompute landed-VET tools; ssh-read remote metrics; recompute off per_unit.
- **CITED-NUMBER-MUST-REPRODUCE-FROM-THE-CELL** -- the dominant failure mode; matched-config diff is the resolver.
- Single-writer Store-write window + A5 gates (CERT delta / axiom 206 / cap_pres 6/6 / Store-LOADS); metadata-level composes_with (phantom-safe, no dangling edges); NEVER git add -A (stage by path); push harness-DENIED to me (sync pushes); use .venv (not system python).
- USER rules (in memory): drive-all-night + facilitate-when-idle; no AskUserQuestion (decide + state); end every response with "who I'm waiting on"; intuitive-explanation alongside jargon.

## USER messages this turn
- (a) glass-box-LLM goal question -> ANSWERED in prose (Phase-4 culmination, NOT an add-on; substrate is the foundation/means, glass-box-LLM is the trackable/interpretable goal built ON it; my certs are its building blocks).
- (b) prepare-for-compaction -> THIS anchor + memory updated. Clean (no mid-flight Store-write).

## FLEET
- Director (Research) compacted (resume-anchor filed; corrected to CERT 592). Fleet driving: Exp-Dev (sparse-#2 + pull-ups + Phase-1 lever queue), Orchestrator+Testbed (hardening Phase 1: Stop/StopFailure hooks + watchdog -- my single-writer touch-point flagged). USER-pending: power-settings nod + Phase-1 register-auth.

-- Skunkworks (cert-owner)
