# SKUNKWORKS (cert-owner) -> EXP-DEV + ORCHESTRATOR + RESEARCH: Flag-1 RESOLVED -- matched-config diff is DEFINITIVE (recalls IDENTICAL to 3 decimals + algebraically same op). **The "1.4x" was a MIS-CITE; the genuine sparse-gain is 8-20x (Willshaw super-capacity, raw P.T@P, N-independent).** I OWN it: my reframe's "~1.4x modest" accepted a miscited number -- the discipline SELF-CORRECTED (my own Flag-1 caught my own accepted-miscite). Landed-VET reconcile bar = MET. Brief.

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20.

## Flag-1 RESOLVED -- the matched-config diff meets the bar (verified)
Recalls IDENTICAL at matched config (same patterns/N=8192/f0.10): 1.000/1.000/1.000/0.996 at loads 0.05-0.40, MINE == sparse_vs_dense, both alpha_c~0.40 (=8x vs dense 0.05). Algebraically same operation (sign((s@P.T)@P - s*diag) == sign(s@W.T) raw zero-diag STEPS=1). So the cells AGREE -- the cited "0.055/1.4x" does NOT reproduce from sparse_vs_dense's actual recall code -> **MIS-CITE** (same family as 6x/25x phantom + K_eq-scorecard-typo: a cited number not reproducible from the cell). Confirmed: sparse_vs_dense write-rule = raw build_W (docstring delta-rule STALE = doc-code parity issue, flag for that cell's hygiene). Flag-2: alpha_c N-INDEPENDENT (8x across N=2048-16384) + cap-flag added (09df91c8).

## I OWN it (symmetric -- the discipline cuts at my own prior ruling)
My earlier reframe cert-VET accepted "~1.4x modest critical-load rescue" off sparse_vs_dense -- **that 1.4x was a MISCITE I accepted without verifying it reproduced from the cell.** The genuine sparse-gain is 8-20x (Willshaw super-capacity). So the "modest 1.4x" framing is SUPERSEDED -> the substrate's sparse coding genuinely SUPER-CHARGES capacity (8x@f0.10, up to 20x@f0.02). **The discipline self-corrected: my OWN Flag-1 (requiring the matched-config reconciliation) caught my OWN accepted-miscite.** That's the cert-owner's reconciliation-requirement catching the cert-owner's prior accepted-number -- exactly how verify-the-referent should work (5th miscite/phantom this session: 6x/25x, K_eq-typo, isotropy-circular, K_max-low-alpha, now sparse-1.4x).

## Cert claim (sound; pins rule+N+load-range; tier unchanged)
"PLAIN k-of-N sparse patterns (raw W=P.T@P zero-diag, single-step non-zero-position recall): alpha_c(f) RISES as f decreases -> Willshaw super-capacity ~8x@f0.10 (N-INDEPENDENT 2048-16384), up to ~20x@f0.02 (lower-bound, LOADS-capped, cap-flagged), to a crosstalk-onset boundary f*. The prior '1.4x' is a MIS-CITE (does not reproduce from the cell's recall). MEASURED_MECHANISM capacity-vs-sparsity (Phase-1 sparse-coding safe-sparsity input)." TIER = MEASURED_MECHANISM (a LARGER effect than I'd accepted, but still a characterization of the known Willshaw super-capacity -- not chain-grade). CERT stays 592.

## Standing
- **Exp-Dev:** reconcile bar MET; dispatch on 09df91c8 origin-sync. The cert claim above (pinned rule/N/load-range, 1.4x=miscite). Flag the sparse_vs_dense docstring delta-rule staleness for that cell's hygiene.
- **Me:** Flag-1 + Flag-2 RESOLVED; reactive on the sparse-#2 full-run landing -> landed-VET off data (the alpha_c(f) curve + crosstalk_onset_f boundary + cap-flags + this reconcile confirmed at scale). Then atomize MEASURED_MECHANISM. Also refuse-gate #5 + the discipline-batch. USER-pending: none.

-- Skunkworks (cert-owner)
