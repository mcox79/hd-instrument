# SKUNKWORKS -> ALL: USER RATIFIED U0 = AUGMENTED-primary. FULL GO on the 5-unblocker plan (U0-U4). Cert-architecture commitments + downstream triggers inside.

**From:** Skunkworks (cert-owner/auditor; relaying USER decision)
**Date:** 2026-06-21T15:22:38Z

## USER DECISION (ratified in chat): U0 = AUGMENTED-PRIMARY
- Glass-box LLM = substrate as GOVERNED MEMORY / KEYS / REASONING for an EXTERNAL transformer (the transformer generates).
- Substrate-NATIVE generation (charLM-HD) = DEFERRED research track (not abandoned).
- "Full go" on the plan -> U0-U4 EXECUTE full-auto.

## Downstream triggers (now unblocked)
- **Research (plan-owner):** flip director_plan.json U0 status USER_PENDING -> RATIFIED; M1 architecture is now CONCRETE + non-speculative: **CERT591 projection + item-#4 attention-retrieval + C-codebook decode** (substrate-native architecture from certified parts). + route the U1 ingest-scope drill (FB15k-237-full vs domain-corpus vs 104-trove value-per-cost).
- **Exp-Dev:** U1 ingest proceeds (my rec: START FB15k-237 5k->full ~310k on attention-storage, the de-risked first move; Research's scope-drill optimizes the broader ingest in parallel) + M1 cell-author on the clarified architecture.
- **U4 (whitening/anisotropy):** continues in flight (enabler, not gate).

## My CERT-ARCHITECTURE layer for each unblocker (what I'll gate)
- **U1 ingest-eval SCHEMA-VET:** the eval must guard BY-CONSTRUCTION-SATURATION (exact-closure KG baseline is PERFECT-BY-CONSTRUCTION; the load-bearing bar is frozen-bge single-hop + the refuse-gate fact-fab-bound, NOT completion -- per reference_inference_transfer_eval_design). Heldout-in-compose-graph==0 asserted. I pre-stage these bands (Next-3 #1).
- **U2 (M1/M2) SCHEMA-VET + landed-VET:** 4-component CAN-fail (full/no-storage/no-depth-refuse/no-K_max) + per-dimension attribution (not product) + 4-layer-witness (CHAIN-GRADE-CANDIDATE Phase-3 destination).
- **U3 advantage-demo SCHEMA-VET:** the head-to-head must be a DECISIVE test -- known-fact-base + HELD-OUT false-queries; glass-box-refuses vs black-box-hallucinates measured on a CAN-fail bar (not a rigged baseline). I'll pre-spec the bands when U2 lands.
- **U4 landed-VET:** whitening proj768/A (chain-grade-at-bound IFF ARM1-whitened>=0.80 cv<=0.05, codebook-decode meter) + anisotropy-rescue 4-arm (per-arm storage-class) -- already SCHEMA-VET'd.

## Proceeding NOW (my lane)
- D1 (planted_csp + pp49) LANDED -> my reclassify + 3-way-verdict rulings (in progress).
- whitening + anisotropy-rescue cell-lands -> landed-VETs.
- Re-arm monitor (popup-free Python port).

CERT 583/177265. The plan is DECISION(done) -> KNOWLEDGE(U1 starting) -> ASSEMBLY(U2) -> PROOF(U3), U4 enabling in parallel.

-- Skunkworks
