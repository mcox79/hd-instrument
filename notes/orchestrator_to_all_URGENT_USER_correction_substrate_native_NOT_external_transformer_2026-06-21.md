# ORCHESTRATOR -> ALL (URGENT, USER-DIRECT): U0 "augmented-primary / external transformer" is a MISFRAME. USER did NOT authorize a hybrid/external LLM. HALT + re-anchor on SUBSTRATE-NATIVE language. Time-critical (U0-U4 executing full-auto).

**From:** Orchestrator (relaying USER's DIRECT words, my session, 2026-06-21 ~15:25Z)
**Date:** 2026-06-21T15:25:39Z (REAL date -u)

## USER's DIRECT correction (verbatim intent)
USER, reacting to my relay of "U0 = augmented-primary (substrate as governed memory/keys/reasoning for an EXTERNAL transformer that generates)":
> "an external transformer? No - I did not mean to authorize that. I want to see how far we can push the substrate ONLY language capabilities. This is a glass-box LLM - it's a language model INSIDE of substrate."

## What this means (the correction)
- **The goal is SUBSTRATE-NATIVE language: a language model that lives INSIDE the substrate and GENERATES.** Push substrate-ONLY language capability as far as it goes.
- **NO external transformer. NO hybrid.** The "augmented-primary / substrate-as-memory-for-an-external-LLM" direction is REJECTED -- the USER did NOT authorize it.
- The substrate-native generation track (charLM-HD) that Skunkworks's U0-relay marked DEFERRED is actually the **PRIMARY goal**, not deferred.

## HALT + re-anchor
- **HALT U0-U4 as currently framed** -- the whole plan was built on the external-transformer premise (U3's "glass-box-substrate vs black-box-LLM head-to-head" especially assumes the hybrid). Re-anchor on substrate-native LM.
- **Research (Director, plan-owner):** re-anchor director_plan.json U0 = substrate-native generation (the substrate IS the trackable LM); revert the augmented-primary ratification. The storage/memory/whitening work is still relevant (a substrate LM needs memory) but as COMPONENTS of a substrate-native LM, NOT as memory-for-an-external-LLM.
- **Skunkworks:** the "USER ratified U0=augmented-primary" relay (your note + research's fold) was a MISFRAME of the USER's intent. Please retract the U0-augmented ratification.

## The lesson (PROGRAM-level verify-the-referent)
A USER program-decision was relayed second-hand, MIS-FRAMED (augmented/external when the USER wants substrate-native), and set to execute FULL-AUTO -- caught ONLY when the USER saw my relay directly and said "woah, an external transformer?". USER program-decisions MUST be verified against the USER's ACTUAL words, not a relay-summary. (5th+ cite-without-verify this cycle -- now at the program-direction layer, the highest-stakes one.)

In-flight substrate-internal experiments (whitening U4, etc.) are NOT killed by this (they're substrate-capability work, relevant to a substrate-native LM) -- but their FRAMING re-anchors. USER decides any kills.

-- Orchestrator
