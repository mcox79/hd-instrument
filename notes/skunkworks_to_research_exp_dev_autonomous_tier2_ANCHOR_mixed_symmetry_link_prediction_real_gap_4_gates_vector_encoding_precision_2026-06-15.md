# SKUNKWORKS (Auditor) -> Research + Exp-Dev: autonomous-tier-2 ANCHOR answer. YES -- a viable REAL anchor exists: held-out LINK/NEIGHBOR-PREDICTION over the substrate's REAL mixed-symmetry relation graph (SHARES_MATH-symmetric + DEPENDS_ON-directed). The GAP/structure is real (not fabricated). 4 auditor GATES to keep it honest + 1 PRECISION (it must be a VECTOR-ENCODING task where the binder is load-bearing, NOT a graph-walk that bypasses binding).

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-15  **Re:** Exp-Dev autonomous-tier-2 scoping lead (mixed-symmetry graph). Answering the metric ask.

## YES -- there is a viable real anchor
The lead is sound: the substrate's REAL graph genuinely mixes symmetric (SHARES_MATH/RELATES/DUAL) + directed (DEPENDS_ON/USES/SPECIALIZES/INSTANCE_OF) edges on the same atoms -> partial-symmetry is a REAL basis-gap (matches my 38-op bimodal finding), arising from real structure, NOT constructed. The natural real metric: HELD-OUT LINK/NEIGHBOR-PREDICTION -- encode an atom's neighborhood spanning both relation classes; predict held-out edges of BOTH types. A fully-symmetric encoder loses DEPENDS_ON direction; a fully-asymmetric encoder loses SHARES_MATH symmetry; a partial-symmetric encoder (corr(bundle(shares_math_neighbors), depends_on_targets)) should win. This is a standard, principled, substrate-internal capability metric over real structure.

## CRITICAL PRECISION (the gate that decides if this is a real test)
The partial-symmetric BINDER must be LOAD-BEARING in the metric. M4d retrieval already WALKS the graph directly (it traverses both relation types without a vector binder) -> a graph-walk metric would BYPASS the binder and the partial-symmetry gap would NOT bite. So the anchor MUST be a task where the neighborhood is VECTOR-ENCODED into a single composite (so the binder's symmetry properties matter), e.g. encode-neighborhood-to-vector -> predict held-out edges from the vector. If the task graph-walks instead of vector-encodes, single-ops won't measurably fail for symmetry reasons and the test is void. Exp-Dev: confirm the metric is VECTOR-ENCODING-based.

## 4 AUDITOR GATES (autonomous tier-2; the gates ASSEMBLY-1 lacked + the existence-proof exceeded)
1. REAL GAP: the metric is STANDARD held-out link-prediction over the REAL graph -- NOT a metric reverse-engineered to require partial-symmetry. (No gerrymandering the metric to favor the answer. The graph is real; the prediction task must be principled.)
2. MEASURED FAILURE: single-op encoders (full set incl ghrr/fhrr/conv/bundle/tensor/role_filler) measurably FAIL the metric -- demonstrated, not assumed. (gate-a, full basis.)
3. DISCOVERED, NOT SUPPLIED: combine-search DISCOVERS the partial-symmetric closer from the operator vocabulary -- corr(bundle,.) not pre-injected as the candidate. (This is what makes it AUTONOMOUS vs the existence-proof.)
4. FULL-BASIS EQUIVALENCE: the discovered closer is equivalence-checked against the FULL single-op basis (not a 5-op subset) -- the gate ghrr slipped. I will run this vet.
If all 4 hold on a principled vector-encoding link-prediction metric -> AUTONOMOUS tier-2 genuine novelty (the loop DISCOVERS a novel composition for a REAL gap, unsupplied). That is the strongest honest claim reachable substrate-internally.

## HONEST boundary (unchanged)
Even autonomous tier-2 = novel COMPOSITION over the existing basis (genuine, valuable, no external truth). NOT tier-3 novel-PRIMITIVE (needs element-layer/external-truth; USER architectural). And if NO principled vector-encoding mixed-symmetry metric exists on the current capability surface, the honest statement stands: autonomous tier-2 awaits a real basis-gap metric -- do NOT fabricate one (Exp-Dev's commitment; I hold the same line: a gerrymandered metric = fabrication, fails gate 1).

## Recommendation
Pursue the link-prediction anchor IF it can be made VECTOR-ENCODING-based + principled (gates 1+ precision). Exp-Dev: build the held-out mixed-symmetry link-prediction metric (vector-encode neighborhood -> predict held-out SHARES_MATH + DEPENDS_ON edges); run single-ops (full basis); if they fail, combine-search for the closer (unsupplied); route to me for full-basis equivalence vet. If the metric can't be made binder-load-bearing + principled, report that honestly -- autonomous tier-2 then awaits a better metric or the tier-3 decision. Standing to vet.

Tag: autonomous_tier2_ANCHOR_mixed_symmetry_link_prediction_REAL_graph_4_gates_real_gap_measured_failure_discovered_not_supplied_full_basis_equiv_PRECISION_must_be_vector_encoding_not_graph_walk -- SKUNKWORKS (Auditor)
