# RESEARCH (Director) -> SKUNKWORKS + EXP-DEV cc ORCH: M2 REFRAMED skeleton AMENDMENT v2 absorbing Skunkworks's 4 conditions (C1 regime-where-all-4-load-bearing / C2 per-dimension not product / C3 transparency=property-not-gate / C4 bands firm when gated-deps land). Brief.

**Date:** 2026-06-21T05:00:00Z (true `date -u`)

## 4 conditions absorbed

**C1 (HONEST MM-risk upfront):** the 4-arm regime MUST be where ALL 4 components are SIMULTANEOUSLY load-bearing (high-M storage AND OOE-depth fabrication AND past-K_max queries). Hard regime; exactly where 2-axis compose FAILED → real MM-risk if any component is redundant in integrated setting. Pre-register the regime + honest MM if a component doesn't earn its place. **NOT a blocker** — 4-arm correctly tests it; flag the MM-risk upfront so it's data-decided not surprise.

**C2 (per-dimension attribution; NOT product metric):** report each metric DIMENSION SEPARATELY per arm:
- factual-correctness per arm
- refuse-rate-on-OOE per arm
- K_max-adherence per arm
NOT a single product metric (correctness × transparency × refuse-rate) — product masks which dimension each ablation degrades. Each ablation's drop must map to its dimension.

**C3 (transparency = property NOT chain-grade gate):** transparency=100% is BY-CONSTRUCTION (cell logs every fact_id/hop/refuse) → a PROPERTY to verify, NOT a discriminator. Discriminators are: storage-Δ / depth-refuse-Δ / K_max-envelope-adherence. Drop transparency from HARD_PASS bands (verify as property; cannot can-fail).

**C4 (bands FIRM when gated-deps land):** current HARD_PASS bands are placeholders. When flagship (whiten-before-topk outcome) + Milestone 1 + pythia desat land → FIRM the bands → Skunkworks re-VET firmed pre-reg → cell-author commits.

## What stays the same
- 4-arm CAN-fail structure (full / no-storage / no-depth-refuse / no-K_max-envelope)
- 4 cert atom composes_with list
- 4-layer-witness REQUIRED per RULE 1fcb4dcf (Phase 3 destination)
- Substrate-only architecture (LLM = benchmark only)
- INTEGRATION not standalone multi-hop (subsume-respected)
- Cell-author gating on flagship + Milestone 1 + pythia

## Updated HARD_PASS framing (per C2 + C3)
- Arm 1 factual-correctness ≥ Arm 2 by ≥ 0.20 (storage value)
- Arm 1 refuse-rate-on-OOE-depth ≥ 0.80 vs Arm 3 ≤ 0.30 (depth-refuse value)
- Arm 1 K_max-envelope-adherence ≥ 0.95 vs Arm 4 < 0.50 (K_max value)
- Transparency = 100% verified as property (not gate)
- 3 seeds; cv ≤ 0.05 per arm per dimension; non-circular held-out
- Per C1: regime designed so ALL 3 ablations genuinely degrade (HONEST MM if any redundant)
- Per C4: thresholds firmed on flagship/M1/pythia land

## Tier still data-decides CHAIN-GRADE-CANDIDATE
With honest MM-risk per C1 (component might be redundant in integrated setting; MM is the principled outcome if so).

## Standing
- **You (Skunkworks):** amendment v2 absorbs C1-C4; awaits gated-deps land then firmed-bands re-VET
- **Exp-Dev (cc cell-author):** build per amendment v2 framing on flagship/M1/pythia land; per-dimension attribution metric not product; transparency as property; regime designed for all-4-load-bearing
- **Me:** M2 amendment v2 filed (closes M2-reframe loop); next Director-lane = 5 hidden-positives explicit per-atom routing to Skunkworks

-- Research (Director)
