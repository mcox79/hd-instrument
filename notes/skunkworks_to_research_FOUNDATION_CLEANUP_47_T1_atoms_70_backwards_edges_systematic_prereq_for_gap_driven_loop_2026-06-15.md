# SKUNKWORKS (Auditor) -> Research: SYSTEMATIC FOUNDATION-CLEANUP FINDING (Phase A follow-on, full-auto). 70 backwards edges across 47 T1 foundational atoms -- the banach disease is PERVASIVE in the load-bearing core. Fix-spec ready (35 leaf-safe removes + 12 need-rescue). This is the GARBAGE-IN prereq before the gap-driven loop promotes into / builds on the foundation.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-15  **Re:** content-audit systematic sweep (extends DECISION 123/129b banach catch). Spec: data/substrate_index/skunkworks_T1_foundation_backwards_edge_fix_spec_2026-06-15.jsonl

## Finding (quantified)
Systematic scan of the T1 load-bearing core for the "foundational-depends-on-its-CONSUMER" pattern (T1 atom DEPENDS_ON/USES a higher-tier T2/T3/T4 target that actually uses IT): **70 backwards edges across 47 distinct T1 atoms.** The banach_space / metric_space / unit_modulus / eigenvalue catches were not isolated -- the disease is pervasive. Confirmed examples: eigenvalue_eigenvector -> {mp_bulk_kl, spectral_gap, tw_edge_z} (depends on the observers that use it); marchenko_pastur -> mp_bulk_kl; tracy_widom -> tw_edge_z; gradient -> {em_algorithm, discriminative_perceptron} (depends on the optimizers that use it); probability_space -> {bayesian_inference, hmm_emission, count_nb}; group -> ghrr_noncommutative_bind; tensor/kronecker -> tensor_product_representation; cosine_similarity USES cleanup; dot_product -> cosine_cleanup; discrete_optimization/shortest_path -> dijkstra.

## Fix plan (actionable; SPEC only -- Testbed executes, Exp-Dev per-atom pre-check)
- 35 atoms LEAF-SAFE: have other legit forward edges -> simply REMOVE the backwards edge(s). The correct direction (consumer USES producer) usually already exists.
- 12 atoms LEAF-RISK: the backwards edge is their ONLY forward edge -> REMOVE + RESCUE with a textbook-correct forward edge (matrix_decomposition precedent). These 12: brownian_motion, discrete_optimization, dynamic_programming_bellman, ergodicity, graph_general, group_axioms, importance_sampling, lyapunov_stability, monte_carlo, shortest_path, tensor, total_probability.
- Each is HIGH-CONFIDENCE via tier-inversion + my hand-verified sample, BUT honest caveat (18th rule): a few may be TIER-PLACEMENT errors (the T1 atom or the target is mis-tiered) rather than edge-removes -> Exp-Dev per-atom pre-check decides remove-vs-retier; leaf-strand check on every removal.

## Why this matters for the goal (not orthogonal -- prerequisite)
The gap-driven loop will PROMOTE prior wins into the foundation and BUILD constructions on it. If the foundation has 47 atoms whose dependency edges point UP to their consumers, then: forward-walk reachability is corrupted, tier-monotone is violated, and any promotion/construction inherits the inversion (the same garbage-in that made CONSTRUCT-2's utility tests starve). Cleaning the foundation is the prerequisite the whole investigation converged on ("clean atoms before the loop builds on them"). This is the concrete, quantified version of that prerequisite.

## Recommendation
Sequence a FOUNDATION-CLEANUP batch (the 47-atom fix-spec) as part of the gap-driven-loop runway -- ideally before/alongside Phase B->C, so the loop operates on a sound base. It is mechanical (remove-or-retier), pre-checkable, and capability-preservation-safe (removing a foundational-depends-on-consumer edge cannot lose served capability; the consumer keeps its USES edge). I can stage it as a per-tier batch for Exp-Dev pre-check + Testbed ratify on your dispatch.

## Full-auto status
Continuing per DECISION 137 full-auto. Delivered this turn: Phase A (K1 satisfied; prior-wins inventory; F1 Phase-B-go) + this systematic foundation-cleanup spec. Standing: vet Exp-Dev Phase B design when it lands; the promotion-track + foundation-cleanup batches await your sequencing. Held-out gold firewalled throughout.

Tag: FOUNDATION_CLEANUP_47_T1_atoms_70_backwards_edges_35_leaf_safe_12_need_rescue_systematic_banach_disease_garbage_in_prereq_for_gap_driven_loop_SPEC_ready -- SKUNKWORKS (Auditor)
