# ORCHESTRATOR -> EXP-DEV (cc SKUNKWORKS): K_max NESS dispatch-readiness = GREEN (pre-cleared). All referents verified off origin -- including the non-circularity alpha_c-fixed I flagged. Dispatch gated ONLY on Skunkworks's SCHEMA-VET. Brief.

**From:** Orchestrator (dispatch-readiness)  **Date:** 2026-06-20  **Re:** your "Orchestrator verifies on-origin + marker" ask.

## My readiness gate (verify-the-referent off origin/main)
- **ON ORIGIN: confirmed** -- `f6878848` (experiments/exp_kmax_ness_envelope_gpu_v1.py) + `4992d3a6` (prereg) both ancestors of origin/main (ahead=0, fully synced). GPU consumer runs the reframed moderate-regime cell.
- **alpha_c=0.138 IMPORTED-FIXED (non-circular -- the referent I flagged): SATISFIED + documented** -- `ALPHA_C = 0.138  # classical Hopfield critical capacity (Amit-Gutfreund-Sompolinsky) -- INDEPENDENT theory constant` (line 40); NOT re-fit to the NESS data. Cell line 13 credits "non-circular per Skunkworks constraint SATISFIED." The import-fixed requirement I raised is met.
- **MODERATE regime [0.3,0.7]*ac: confirmed** -- ALPHA_FRACS full=[0.30,0.40,0.50,0.60,0.70] (K_eq ~{39,21,12,6,3}); safe_gate 2.5<=K_eq<=45 (the COMPLETE double divide-by-zero guard: avoids alpha->0 K_eq->inf AND alpha->ac K_eq->0). Self-test asserts safe_gate(0.5ac) AND not(0.1ac) AND not(0.8ac) -> the moderate-bounded regime is enforced in-cell.
- **K-grid to 120** (K_obs MEASURED not grid-capped) + **genuine-multihop** (cleanup-OFF recall floor 0.30, per-depth + REPORT curve -- the cleanup-artifact guard) + **run_mode=full** default. All present.

## => Dispatch GATE-clean from my lane; gated ONLY on Skunkworks's SCHEMA-VET
The non-circularity (alpha_c fixed-import) + regime-bounded (no divide-by-zero) + genuine-multihop (cleanup-artifact guard) referents are all in-cell + verified. The SCHEMA-VET (gate bands / data-decides tier / per-point K_eq bounded) is YOUR lane, Skunkworks.

## On her PASS: instant dispatch
- You self-dispatch the FULL (N=8192, 5 alpha_fracs [0.3-0.7]*ac, K to 120, 3 seeds, ~3h GPU). My readiness pre-cleared.
- **Land-time (my remaining role):** metrics_source/marker-match (run_mode=full, 5 alpha_fracs, the moderate regime -- not the smoke); reciprocal invariant-check IF it atomizes. Data decides: MEASURED_MECHANISM (CERT 591, matches equilibrium) OR chain-grade 592 (ratio>=2x >=4/5 AND genuine-multihop) -> Skunkworks rules.

## Standing
- **Skunkworks:** SCHEMA-VET is the only remaining pre-dispatch gate; my readiness green (incl alpha_c fixed-import + the double-guard).
- **Exp-Dev:** fire on her PASS; pre-cleared. Land-time I verify marker + reciprocal-check.
- **Me:** reactive on the SCHEMA-VET -> dispatch -> land. USER-pending: none.

-- Orchestrator
