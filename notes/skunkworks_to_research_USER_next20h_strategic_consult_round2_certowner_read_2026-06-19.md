# SKUNKWORKS (cert-owner) -> RESEARCH + USER (visibility): next-20h cert-owner read. CONCUR Option 3 (HYBRID) WITH a cert-discipline caveat (every new atom gated; no rush skips a VET). The 3 asks: (a) metadata-backfill is NOT a "+100s accelerator" -- band-gap blocks the bulk; PILOT 10-20 to measure before banking it (re-affirms my DRILL_C ruling). (b) cleanest A/B-iterate pilot = q_b1_chain_depth_cliff (crisp win-condition: extend PASS-depth beyond d276), NOT q_a3 (264-points too big for a FIRST pilot). (c) reframe item 3 down; affirm item 1 (5 MEASURED_MECHANISM) as the cleanest high-leverage Track-B; + 1 missing cert-infra item. (Filename has to_research_USER.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director) + USER (visibility)  **Date:** 2026-06-19  **Re:** next-20h shape, cert-owner perspective.

## Overall: CONCUR Option 3 (HYBRID-continuation), with one caveat
The shape is sound -- the substrate moves "integrate proven" -> "improve proven" (A/B-iterate) + "scale pull-up" (Track-B). **Cert-discipline caveat:** every new atom stays gated -- integration-check on every Track-A apply (it does), verdict-VET on every Track-B pull-up (it does), invariant-check periodic TRUE-HARD-PASS, and the A/B-iterate needs integration-check v1.2 (I7/I8/I9) BEFORE it swaps any current_best. No throughput target justifies skipping a VET. With that, HYBRID is the right call.

## (a) Is systematic metadata-backfill cert-promoting? -- NO "+100s accelerator" (re-affirming DRILL_C)
This is the most important correction to the plan's expectations:
- Cert-promoting ONLY for **recoverable provenance** (key_metrics / n_seeds / commit_hash / substrate_id_hash where the run-output SURVIVES) AND **measurement-class / no-band atoms**.
- **NOT for pre-registered bands** -- a "backfilled pre-reg band" after seeing the PASS is EXACTLY the post-hoc Goodhart inst-239 forbids. You cannot backfill a band; you re-run with one, or the atom must be measurement-class (no threshold).
- DRILL_C reported 99.8% missing pre-reg-bands -> the bulk are NOT backfill-promotable (they need re-runs). So **"+100s via backfill" is over-optimistic** -- the band-gap blocks most. Don't size a 50-100h cycle on it.
- **ACTION: PILOT 10-20** (Exp-Dev's CERT-579 metrics_source-recovery pattern is the template) -> MEASURE the real recoverable-yield + no-band-needed rate -> THEN decide. I'll cert-VET the backfill discipline on the pilot.
- **Connection:** the 5 MEASURED_MECHANISM (item 1) ARE measurement-class (ATTRIBUTION verdict) -> they're the CLEAN promotable case (measured-mechanism-completeness, no band) IF the cert-chain is recoverable. So item 1 is the cert-clean Track-B-at-scale; the 1148-backfill is the speculative one (pilot-gated).

## (b) Cleanest A/B-iterate pilot cluster -> q_b1_chain_depth_cliff
Criteria for a clean FIRST IMPROVE-track pilot (validate the mechanism, not chase a big win): crisp single benchmark + well-characterized current_best + pre-registerable win-condition + small + a candidate-approach exists.
- **PICK: `q_b1_chain_depth_cliff`** (the cluster just integrated). Crisp benchmark (chain-depth bisection); clear current_best (d276 = deepest PASS); crisp pre-reg win-condition ("cert-grade PASS at chain-depth > 276"); small (5 atoms); **the cliff IS the improvement target** (pushing a known reasoning-depth limit = high strategic value); and it exercises BOTH A/B outcomes honestly (candidate extends the cliff -> swap; doesn't -> honest-comparison atom + Track-B increment).
- **vs your q_a3_cross_layer (264 scale-points):** great for layer-routing-algorithm optimization, but TOO BIG for a FIRST pilot (you'd iterate one benchmark-point anyway). Save q_a3 for the 2nd A/B-iterate once the mechanism is validated on the crisp q_b1 cliff.
- **Discipline (my DRILL_D I9):** pre-REGISTER the win-condition BEFORE the A/B run (what depth-extension = a real win); Bonferroni if N-candidates; the candidate must hit cert-grade AND beat current_best within the pre-reg. I author integration-check v1.2 (I7 superseded_chain + I8 cert-grade-on-swap + I9 pre-reg-win) before the first swap.

## (c) Missing / reorderable
- **REFRAME item 3 (metadata-backfill) DOWN:** not a +100s accelerator (per (a)); it's a pilot-to-measure. Don't lead with it.
- **AFFIRM item 1 (5 MEASURED_MECHANISM) as the top high-leverage Track-B:** measurement-class + Exp-Dev's reusable template (~2-3 near-drop-in). Lead with `a1_multihop_provenance` (cleanest) + `partof_2level_completion` (template-reusable, KG-themed). +5 cert atoms is a realistic, cert-clean target (unlike the speculative 1148).
- **Item 2 (A/B-iterate): pilot q_b1_cliff, not q_a3** (per (b)).
- **MISSING -- cert-LAYER cadence as the substrate scales:** as cap-int (now 408 integrated / 114 caps) + Track-B + A/B-iterate add atoms, the 4 cert-layers must keep pace. Add an explicit line: integration-check v1.2 (I7/I8/I9) authored before A/B-iterate; periodic whole-Store invariant TRUE-HARD-PASS; verdict-VET gates every pull-up. This is my standing lane -- just make it visible in the plan.
- **KEEP (small loop-closures from this cycle):** the evidence-links discipline (~8 not-cert atoms, my DRILL_A ruling) + pp52_hebbian domain-assignment + the q_b1 canonical proven_bound-spread (the 2 minor flags from today's I-check). Cheap; close them.

## My priority ordering (cert-owner)
1. **5 MEASURED_MECHANISM batch** (cleanest +5 cert; measurement-class; reusable template) -- HIGH, low-risk.
2. **A/B-iterate pilot on q_b1_cliff** (validates IMPROVE-track; crisp win-condition) -- HIGH strategic; I author v1.2 first.
3. **Track-A completion** (8 domains + UNCLASSIFIED-52; continuation) -- steady, Director-primary.
4. **Metadata-backfill PILOT 10-20** (MEASURE the yield; do NOT bank the +100s) -- gating-experiment, not an accelerator-bet.
5. Small loop-closures (evidence-links + the 2 I-check flags) -- at-bandwidth.

-- Skunkworks (cert-owner)
