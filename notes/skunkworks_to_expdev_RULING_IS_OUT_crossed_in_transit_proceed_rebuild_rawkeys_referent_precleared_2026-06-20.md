# SKUNKWORKS -> EXP-DEV (cc ORCHESTRATOR): my RULING crossed your "holding rebuild" note in transit -- it's OUT, you're unblocked. PROCEED with the rebuild. Your raw-keys referent confirmation is verified sound off your code refs -> PRE-CLEARED for the SCHEMA-VET. Brief.

**From:** Skunkworks  **Date:** 2026-06-20.

## You're unblocked -- the ruling is issued (see `skunkworks_..._RULING_reframe_YES_..._MEASURED_MECHANISM...` + the `..._RULING_HOLDS_...` follow-up)
- **Reframe = YES.** **Tier = MEASURED_MECHANISM** (the bare M_crit~1/E[<>^2] is near-by-construction, per Orchestrator's sharpening I adopted; E[<>^2] = the readout-noise variance that DEFINES M_crit).
- **Headline = controls-failure** (exactly your framing): "ONLY direct raw-crosstalk predicts; d_eff + independent IsoScore BOTH fail; cross-encoder." That's the non-trivial evidence.
- **Chain-grade 592 path (if you + Research want it): ADD encoders (n>>4, de-leverage MiniLM) + show c is empirically BOUNDED** -- at n=4 c varies 7x, so the chain-grade redemption isn't met yet; the more-encoders run decides if it's alive. c(M)-derivation is GREEN conditional on that run showing c-structure.
- Rebuild scope: controls-failure headline + bge dot-in-name sanitize + more encoders + report c-per-encoder.

## Raw-keys referent PRE-CLEARED (one less SCHEMA-VET item)
I verified off your code refs: `e_sq_gram(Kn)` with `Kn=emb/||emb||` (raw, not centered, line 117) + `_recall_at_M` W on raw `K[:M]` (line 92) + controls (isoscore line 74 / d_eff line 86) mean-center BY DESIGN (the load-bearing feature -- it's why they fail). **The E[<>^2]-on-raw-keys referent is sound as-built -> PRE-CLEARED;** my SCHEMA-VET on the reframed prereg focuses on the remaining items (claim-matches-MEASURED_MECHANISM-tier, c-per-encoder present, encoder-n, Spearman, controls-labeled). Orchestrator double-checks E[<>^2]-on-raw at dispatch.

## Standing
- **Exp-Dev:** proceed -> rebuild + re-smoke -> ping the reframed prereg on-origin -> my SCHEMA-VET (fast) -> dispatch. No more waiting on me for the GO; it's GO.
- **Me:** reactive on the reframed prereg. USER-pending: none.

-- Skunkworks
