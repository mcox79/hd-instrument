# Brain drill: is the reasoning readout a genuine (non-construction-determined) chain-grade target? (2026-07-19)

Consolidation of a 4-sub-scan brain drill (parent aebc382d; sub-scans: ad419720 constraint-satisfaction/attractor, a3017ed2 relational/transitive inference, ac45ed2f real-data KG benchmarks, a9292fc7 ordinal/asymmetric encoding). Written by the Director (parent stalled on write). Fired after the chain-grade cell VET (atom 29363) proved compositional generalization is FREE from binding (construction-determined), so the genuine chain-grade must live where capability is LEARNED and the ceiling is honestly < 1.000.

## HEADLINE / VERDICT (deflated ~0.55)
Multi-hop reasoning over a GOOD structural code is ALSO close to "FREE" (a geometric consequence, Bakermans-Whittington-Behrens 2024) -- the SAME construction-determined trap. So the genuine chain-grade is NOT composition-over-clean-structure; it is **LENGTH-GENERALIZATION on real relational reasoning (train short chains, test long), where the "free geometry" BREAKS** (models fall from >0.9 at length 2-4 to 0.3-0.6 at length 10). Recommended target = **CLUTRR** (kinship-chain reasoning, ceiling honestly < 1.000, flat/single-hop = mechanism-grounded must-fail). The substrate lever = **monotonic/ordered content codes + commutative bind** (brain-faithful; no non-commutative neural operator exists).

## SCAN 1 (constraint-satisfaction / attractor) -- a CAUTION
- Resolution scaling sharply with # constraints (our 1->7 = 0.05->0.99) is REAL and BRAIN-IMPLEMENTED (CA3 pattern-completion attractor: abrupt nonlinear regime change with cue overlap; recall ~ sqrt(# converging synapses)) AND a GENERIC MATHEMATICAL law (multiplicative evidence combination: each independent constraint removes a constant FRACTION of hypothesis space -> log-ambiguity linear -> resolution exponential; k-SAT + compressed-sensing sharp phase transitions are the non-neural precedents).
- **CAUTION (echoes tonight's lesson): the constraints->resolution gain is GENERIC MATH, not a learned capability. A chain-grade built purely on "more constraints = better resolution" would ALSO be construction-determined/algebraically free.** The genuine-learning part is elsewhere (learning WHICH constraints / the structure).

## SCAN 2 (relational/transitive inference) -- IS multi-hop LEARNED or free? verdict (c) BOTH, two-stage
- SLOW/experience-dependent = STRUCTURE-LEARNING (acquiring a LOW-CONJUNCTIVITY generalizing relational code). Once it exists, multi-hop composition/transitive generalization is close to "FREE" -- a mathematical consequence of the representation's GEOMETRY (Bakermans 2024 PNAS: transitivity emerges from geometry; GD and ridge converge to the same generalizing solution). **BUT "RICH REGIME" adaptive reps BREAK transitivity -> geometry (not learning rule) gates whether inference is free.** PFC-BG gating: online composition fast + ~4-chunk limited; the META-SKILL of what to hold/chunk is RL-LEARNED + improves with experience.
- **=> the genuine LEARNED/hard parts: (a) structure-learning (the generalizing geometry), (b) gating/chunking meta-skill, (c) STAYING in the low-conjunctivity geometry -- and the test that exposes all three = LENGTH-GENERALIZATION (where free-geometry breaks).**

## SCAN 3 (real-data KG benchmarks) -- the concrete target
- Ceilings honestly < 100% everywhere (FB15k-237 MRR 0.29-0.34; MetaQA 2-hop 89.9% -> 3-hop 62.5%, difficulty ratio 0.286->0.003; ConceptNet-KGC sparse/hard). NO oracle path-follower wins (graphs INCOMPLETE + relations not deterministic compositions) -> no algebraic freebie, unlike the matched-decoder unbind.
- **CLUTRR (RECOMMENDED primary): train kinship chains length 2-4, test zero-shot up to length 10. Graph-relational models >0.9 at 2-4 fall to 0.32-0.62 at length 10; flat LSTM/BERT/bag-of-facts FAIL to length-generalize at all = the natural mechanism-grounded MUST-FAIL baseline. Ceiling < 1.000, degrades with length, free + small (~thousands of stories).**
- Query2Box/BetaE conjunctive queries (SECONDARY): multi-constraint accuracy DECREASES with added constraints (answer sets ~10x larger); box-embeddings beat single-vector GQE by up to 25% = a real flat-vs-structured gap on real data.
- Design = CFQ compound-divergence (hold atoms/primitives seen, vary combinations/lengths); must-fail = single-hop/flat model provably can't chain beyond its hop budget (mechanism-grounded, stronger than statistical).

## SCAN 4 (ordinal/asymmetric encoding) -- the substrate lever, brain-confirmed
- Asymmetry (magnitude, order, rank, roles) is carried by MONOTONIC/ORDINAL CONTENT CODES, combined via an otherwise SYMMETRIC/associative bind. Evidence: analog magnitude / mental number line (Dehaene, Nieder number-neurons, distance/size effects); grid-cell METRIC codes generalized to abstract 2D conceptual spaces (Constantinescu 2016, Bao 2019) + social hierarchy; primacy-gradient / positional serial-order codes (CQ/OSCAR); distinct role populations (Frankland-Greene). **NO evidence for a non-commutative NEURAL binding operator -- every asymmetric-relation subfield localizes asymmetry to the REPRESENTATION being bound, not the binding RULE.** Confirms our memory's ENCODING LEVER + the earlier self-correction (don't break bind commutativity; the brain doesn't).
- NON-TRIVIAL engineering point: it's NOT "just distinct role vectors." The substantive part = codes MONOTONIC/METRIC within a dimension (so "5>3" and "8>3" SHARE structure and generalize/interpolate; vector similarity tracks ordinal distance). Arbitrary distinct role vectors get LABELED asymmetry but NOT the GRADED, distance-sensitive, generalizing structure. **The real lever = ordered/monotonic content codes (thermometer/level/grid-style geometry) for magnitude/rank/ordered relations, superposed via standard commutative bind.**

## => THE CHAIN-GRADE CELL DESIGN (hand task-shape to exp_dev; it acquires CLUTRR + designs params)
GENUINE NON-CONSTRUCTION-DETERMINED CLAIM: **the substrate LEARNS the relational-composition rules from SHORT chains (length 2-4) and LENGTH-GENERALIZES to LONG chains (up to 10) better than a param-matched flat/single-hop baseline that provably cannot chain -- on real CLUTRR data whose ceiling is honestly < 1.000.**
- TASK: CLUTRR kinship-chain reasoning; train length 2-4, test length 2..10; report the LENGTH-GENERALIZATION CURVE (accuracy vs chain length).
- ARMS (ONE variable = the compositional/binding reasoner): ARM-A native-VSA multi-hop reasoner (distinct role codes + commutative bind + MONOTONIC/ordered codes for the asymmetric kinship relations + constraints-brought-to-bear resolution); ARM-B param-matched FLAT/single-hop baseline (no iterative composition) that provably fails at length > budget.
- **CONSTRUCTION-DETERMINED GUARD (the load-bearing gate, per atom 29363): an ORACLE / handed-composition-rules reasoner must NOT already solve length-10 for free -- the kinship COMPOSITION RULES must be LEARNED from the short chains, not handed. If length-10 is oracle-free-solvable, it's construction-determined and NOT a chain-grade. The genuine result = learned rules that length-generalize where a flat baseline collapses.**
- MUST-FAIL: flat/single-hop provably fails at length > hop-budget (mechanism-grounded); + a scrambled-composition control (random composition rules -> length-generalization collapses = proves genuine rule-learning).
- CEILING CHECK: report that ceiling is < 1.000 (CLUTRR is genuinely hard); success = beating flat on the length-generalization slope, NOT hitting 1.000.
- Design-gated can-fail, pre-registered bands both directions, ONE variable, no tune-to-pass. skunkworks-VET on landing (adversarial on the construction-determined guard HARDEST, as with 29363).

## BRAIN-CHECK (mandatory)
Brain does two-stage: slow structure-learning (the generalizing low-conjunctivity geometry) + fast composition. Multi-hop over GOOD structure is free (same trap), so the chain-grade must test the part that ISN'T free = LENGTH-GENERALIZATION (where rich-regime geometry breaks). The encoding lever (monotonic ordered codes + commutative bind) is brain-faithful (number line / grid cells / no non-commutative operator). This is a genuine, non-construction-determined target IF the composition rules are LEARNED (guard above), unlike the factorization's algebraically-free generalization.
