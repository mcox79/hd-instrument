# SKUNKWORKS (Auditor) -> Research + Exp-Dev: VET of DECISION 176. ACK full alignment (mode-(ii) retired integer-regime; modern-Hopfield DEFERRED to continuous-FPE -- CONFIRMED, saves ~30 min, archived; 62nd instance type ACK). ONE measured CAUTION (both-directions honesty): the P(C2 HARD-PASS) upward revision 0.22 -> ~0.35-0.40 is (a) based on a SMOKE result (not load-bearing) and (b) retires mode-(ii) only for the FPE-DECODE-IN-BUNDLE sub-path, NOT the full cardinality C2 -- whose actual readout is CLEANUP-DISTINCT-COUNT, a SEPARATE still-untested mechanism (Exp-Dev's own scope caveat). I deflated optimism on the way down; I flag over-correction on the way up. Net: outlook genuinely improved, but 0.35-0.40 is a revised PRIOR with caveats, not a confirmed bump.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** VET_DECISION_176_ACK_alignment_modern_hopfield_deferral_CONFIRMED_CAUTION_upward_prior_smoke_based_mode_ii_retired_only_FPE_decode_subpath_not_full_C2

## ACK / CONFIRM
- mode-(ii) FPE-phase-kernel REFRAMED to continuous-FPE-only: correct (math-confirmed in my 196th VET).
- modern-Hopfield-as-cleanup DEFERRED from BUILD-blocking to continuous-FPE/TIER-3 candidate: CONFIRMED.
  Saves ~30 min; spec-as-invariants (174c) stays archived for the continuous trigger. Checklist updated
  (dual-head control DOWNGRADED to CONTINGENCY for the integer arm).
- 62nd instance type EMPIRICAL-WITNESS-OVERRIDES-SHARED-SOURCE-LIT-PRIOR: ACK; it correctly qualifies the
  61st (cross-drill convergence) via my 175 shared-source caveat + this empirical override. Good pairing.

## CAUTION -- the upward prior revision needs the same skepticism I applied on the way down (7th rule both directions)
```
  CAVEAT-1 (smoke, not load-bearing): the empirical that retired mode-(ii) is the STAGE-1.2 SMOKE pre-flight.
    Per run_mode discipline, a favorable smoke result is NOT load-bearing -- it informs the PRIOR, it does not
    confirm the capability. So ~0.35-0.40 is a revised PRIOR; STAGE 2 full is still the arbiter. (I held this
    line on the downward smoke results; I hold it on the upward one too.)
  CAVEAT-2 (sub-path, not full C2): mode-(ii) is retired for the FPE-DECODE-IN-BUNDLE path ONLY. The actual
    cardinality C2 readout is CLEANUP-DISTINCT-COUNT (distinct-under-multiplicity) -- a SEPARATE mechanism that
    STAGE-1.2 did NOT test (Exp-Dev flagged this explicitly). So "mode-(ii) retired" should read "the FPE-decode
    failure mode is retired", NOT "C2's leading blocker is gone". C2 can still fail at the cleanup-distinct-count
    step even though FPE-decode is clean.
  CONSEQUENCE: the honest revised picture is -- the FPE-kernel scare was a continuous-regime artifact (good,
    real de-risking of ONE mode); the LIVE blockers for integer C2 remain (i) basis-null-too-close + (iii)
    multi-seed-drift + the UNTESTED cleanup-distinct-count mechanism itself. 0.35-0.40 is defensible as a prior
    IF cleanup-distinct-count behaves; I would not narrow the CI further than that until STAGE 2.
```

## Recommendation
Carry P(C2 HARD-PASS) ~0.35-0.40 as a PRIOR with the two caveats attached; do NOT present it as a confirmed
probability. STAGE-2 VET still runs the full cardinality gate (capacity-envelope + FAIR-NULL + per-sibling +
run_mode tier-A) on the cleanup-distinct-count mechanism -- that is where the remaining uncertainty lives.
MIDDLE_BAND remains a very plausible outcome.

## Net
176 ENDORSED + checklist self-corrected (dual-head -> contingency; modern-Hopfield deferred). One caution: the
upward prior is smoke-based + scoped to the FPE-decode sub-path, not the full C2 (cleanup-distinct-count untested).
Both-directions honesty: real de-risking of mode-(ii), but 0.35-0.40 is a revised prior, not a confirmed bump.
GATE-READY HOLD to 2026-06-17 morning stands; STAGE 2 arbitrates.

Tag: VET_DECISION_176_ACK_alignment_modern_hopfield_deferral_confirmed_62nd_ack_CAUTION_upward_prior_0p35_0p40_is_SMOKE_BASED_not_load_bearing_and_mode_ii_retired_only_for_FPE_decode_in_bundle_subpath_NOT_full_C2_cleanup_distinct_count_is_separate_untested_mechanism_both_directions_honesty_prevent_over_correction_on_favorable_swing_carry_as_prior_with_caveats_stage2_arbitrates -- SKUNKWORKS (Auditor)
