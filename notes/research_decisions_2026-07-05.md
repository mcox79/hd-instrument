- 5x convergence drill (reasoning spec + brain mechanism): regenerative-cleanup-between-hops (digital-repeater analog) is the convergent mechanism across 5 angles; decisive cell exp_cortex_regenerative_cleanup_vs_analog_accumulate_v1 spec'd. See notes/research_5x_drill_reasoning_spec_and_brain_mechanism_2026-07-05.md
- 06:01 UTC 5x convergence drill (GENERATION goal) -> notes/research_5x_drill_generation_spec_and_brain_mechanism_2026-07-05.md (cross-validates decoder_design memo Stage A/B/C; new sparse-block-code resonator candidate surfaced for NO_GO branch)
- 5x convergence drill (CONTINUAL LEARNING goal) -> notes/research_5x_drill_continual_learning_spec_and_brain_mechanism_2026-07-05.md. No-forgetting MVP proven 4x independently (a8, CRISPR, distshift v430, c3 segregated-dual-W); schema-formation (the full goal) never tested -- c3's own landed data shows forgetting fixed (0.678->0.011) while transfer stayed EXACTLY 0.000, confirming forgetting-prevention and abstraction are separate mechanisms across all 5 lit angles. Decisive next cell schema_bundle_structural_transfer_v1 spec'd; hand-off filed notes/exp_dev_handoff_research_5x_drill_continual_learning_schema_formation_2026-07-05.md.
- 5x convergence drill (PERCEPTION/ENCODER goal) -> notes/research_5x_drill_perception_encoder_spec_and_brain_mechanism_2026-07-05.md. DPI "student<=teacher" ceiling is an informal folk bound, not proven (Born-Again/Noisy-Student/DINOv2>CLIP counterexamples); brain bridges perceptual->semantic via temporal-contiguity/slowness learning (converges with prior 07-02 Spoke2 finding), not flat Hebbian/sparse-coding alone. Substrate ALREADY ran the teacher-free perception test (exp_n11_random_indexing_semantic_v1, full text8, MIDDLE_BAND, control-verified, ratio 1.20) 13 days ago with zero follow-up -- 2x-drill-negatives gap. Decisive next cell (re-eval RI/BEAGLE on substrate's own KB through Step-3 dual-gate, realistic target ~0.65 not 0.85) spec'd inline in the deliverable per no-routing-files discipline; Lever-B STE fix on the existing distilled pipeline remains the cheaper near-term ship move.
2026-07-05T08:33:59Z research: 2x-drill on cortex_regenerative_cleanup_vs_analog_accumulate_v1 HARD_FAIL -> notes/research_2x_drill_reasoning_cleanup_negative_why_it_shrank_and_the_fix_2026-07-05.md -- root cause = M_BG sweep not M/N-matched across RUN_MODE (N_TEST 48->150 tripled shared-matrix crosstalk); mechanism (regen cleanup beats analog above threshold) reproduced clean both smoke+full, salvageable via M_BG recalibration table, P_deflated=0.40
- 2026-07-05: 2x drill on schema_bundle_real_corpus_transfer_v1 HARD_FAIL -> notes/research_2x_drill_what_encoding_carries_relational_structure_for_schema_transfer_2026-07-05.md (mechanism proven via synth-positive +0.78; real relations at chance/confounded; lit + PP-275 cross-thread say fix is a TRAINED negative-sampled relation-transform estimator, not the entity encoding per se; proposed next cell schema_relation_transform_estimator_ablation_v1, P_deflated=0.45)
- mechanism+envelope drill on exp_schema_relation_TEM_structural_content_binding_v1 MIDDLE_BAND -> notes/research_mechanism_envelope_frontier_inductive_transfer_off_zero_2026-07-05.md -- mechanism = content/type-conditioning (RESCAL/DistMult/ComplEx bilinear analog) vs GLOBAL's TransE-style averaged transform (proven to degenerate to population marginal on one-to-many); magnitude split confirmed off-disk via ConceptNet corpus count (CapableOf top-100 codebook covers only 4.2% of triples -- data-starved, not just model-starved); SCORER>=TEM reflects continuous-bilinear-vs-hard-discretization gap, not a brain-alignment failure (TEM recurrence never ablated in lit, zero precedent either way); next cell schema_relation_TEM_scorer_scaleup_v1 (M_OP/steps/df sweep + soft-TEM prototype mixture) spec'd inline per no-routing-files discipline, P_deflated=0.42
- frontier drill (COMPREHENSION goal, generation's missing complement) -> notes/research_frontier_drill_comprehension_parse_unknown_structure_2026-07-05.md. Corpus scour confirms blind (frame-unknown) factorization on real correlated fillers has ONLY ever been tried with the DENSE algebra (exp_generation_decoder_roundtrip_v1 real_fullreso_hi=0.000; exp_generation_decoder_gsbc_native_blocklocal_v1 dense_gsbc_fullreso=0.000, both 3-seed) -- never with sparse-block geometry in a genuinely frame-unknown config (the existing blocklocal_gsbc HARD_PASS=1.000/0.86 still carries position via known block-index). 3-strand lit convergence (Helmholtz-machine recognition+generation split / analysis-by-synthesis; block-sparse RIP relaxing correlated-component coherence; FrameNet-style classify-then-decode, near-zero accuracy cost) converges on frame_classify_then_known_decode_v1: cheap CPU matched-filter frame classifier + the already-proven known-frame decoder. HARD-PASS frame-acc>=0.90 & chained parse-acc>=0.75; HARD-FAIL frame-acc<=0.40 or parse-acc<=0.15. P_deflated=0.42 (cap 0.50 honored). Hand-off filed notes/exp_dev_handoff_research_frontier_drill_comprehension_parse_unknown_structure_2026-07-05.md.
- INTEGRATION DESIGN DRILL (end-to-end perception->memory->reasoning->generation) -> notes/research_integration_end_to_end_substrate_loop_2026-07-05.md. Substrate scour: 2 of 3 tested seams are clean or smoke-clean same-algebra (encoder GSBC->store, HARD_PASS smoke via exp_regime_switch_encoder_instore_integration_verify_v1; memory->reasoning, CHAIN_GRADE via exp_deep_reasoning_hub_robustness_v1 cleanup directly over real stored atoms). The 3rd seam (reasoning bipolar-BSC output -> generation input) is UNTESTED end-to-end: dense decoder is algebra-matched to reasoning but collapses 0.000 blind/frame-unknown (cliffs F>=3); new block-local decoder is algebra-matched to the encoder instead and HARD_PASSes but only in known-frame regime, never fed real reasoning output. 2 independent lit-scans (neuro: communication-subspace/thalamic-relay/hippocampal-cortical-dialogue; VSA/HDC: Hersche et al. CNN-to-block-code bridge) both converge: biology and VSA lit use explicit bridge-transforms (not shared code) across mismatched representations, and naive/analytic bridges measurably lose fidelity (16.2pt drop in a directly analogous published system) while co-trained bridges do not. 3-arm CPU decisive test spec'd inline (dense-matched / naive-analytic-bridge / no-reasoning control), no routing file per USER-locked no-ferry discipline. P_deflated=0.35 (naive-bridge HARD-PASS), P_deflated=0.45 (co-trained-bridge path more likely to survive). next-drill candidate: sparse-block-code composition algebra (build reasoning directly in block-local/GSBC geometry, the already-flagged v2 STRATEGIC path).

- research: frontier drill CONTROL/instruction-following (PBWM goal-conditioned op-selection) -> notes/research_frontier_drill_control_instruction_following_pbwm_2026-07-05.md. Key finding: cortex_attention_binding_router_v2 (M1.6) already HARD_PASS cross-seed (CM=1.000) for content-conditioned 4-op routing; exp_pfc_goal_conditioned_gate_v3 (goal-vs-content sharper test) smoke-landed MIDDLE_BAND (+0.06 lift, 0.66 oracle headroom) but FULL never dispatched -- cheapest next move is dispatching that existing pre-registered cell, not designing a new one. P_deflated=0.45.
- 15:41 Brain-component inventory + build-priority ranking -> research_thrust_brain_component_inventory_and_build_priorities_2026-07-05.md. Top build: train existing weak BG-flavored gates (PFC-BG additive, BG-analog conflict-op) with existing cfrpe RPE signal, NOT a new component. Decisive cell exp_pfc_gate_cfrpe_trained_v1 spec'd, P_deflated=0.40.
- research: mechanism+envelope drill on exp_schema_relation_richer_content_vscan_v1 HARD_FAIL (one-to-many entropy ceiling) -> notes/research_reframe_rank_set_prediction_one_to_many_ceiling_2026-07-05.md. Fan-out is real but modest (AtLocation 29.6% multi-valid mean 1.82, CausesDesire 10.4% mean 1.25 @ V=300) and is NOT the dominant driver (subject-identity oracle ceiling 0.81-0.94, far above observed ~0.06-0.09). k-NN nonparametric oracle on the SAME frozen BGE features lands in the SAME 0.05-0.12 band as the trained bilinear/MLP scorers -> content-representation-limited, not under-parameterized. Recomputed the FROZEN scorer's own ranking: Hits@10 rms recovers to +0.26 (AtLocation) / +0.18 (CausesDesire) vs Hits@1 rms +0.06/+0.08 -- near-miss content-neighbor competition, not raw guessing. Reframe cell exp_schema_relation_hitsatk_mrr_reframe_v1 spec'd (filtered Hits@k/MRR per Bordes et al. 2013, HARD-PASS Hits@10>=0.20 AND MRR>=0.15 on >=2 rel x >=2 enc @ V>=300). Preliminary single-seed/bge-only proxy lands in MIDDLE-BAND (Hits@10 clears for AtLocation only; MRR clears neither). P_deflated=0.35 for clean HARD-PASS; ~0.50 (capped) for at-least-MIDDLE genuine recoverable signal.
- research: mechanism+envelope drill on exp_schema_relation_hitsatk_mrr_reframe_v1 MIDDLE_BAND (Hits@10 rms partial-clears, MRR rms 0.108-0.166 misses 0.15 floor) -> notes/research_hubness_popularity_debias_rank1_sharpening_2026-07-05.md. Off-disk recompute (3 seeds x 2 rels, FROZEN score matrix, functions imported verbatim) shows the rank-1-blocking crowding is HUBNESS (object-side top-1-winner Gini=0.87-0.95, textbook Radovanovic et al. 2010 signature) compounded by LABEL-PRIOR bias (corr with train-frequency r=0.42-0.80); residual-decomposition splits the two relations cleanly -- CausesDesire nearly all label-prior (residual Gini collapses 0.95->0.08), AtLocation a genuine mix (residual Gini only drops 0.87->0.18, real geometric hubness survives frequency-removal). logQ/sampled-softmax correction (Yi et al. 2019) does NOT apply (scorer already trains exact full softmax, not sampled) -- matched fix is a training-free post-hoc rescore combining CSLS/local-scaling (hubness term) + post-hoc logit adjustment (Menon et al. 2021, label-prior term), staged before the heavier trained complements (train-time logit-adjustment loss / DPR-ANCE-style hard-negative mining). CA3-style attractor cleanup evaluated on merit and NOT chosen -- classical Hopfield capacity theory argues basin depth scales with reinforcement frequency, so cleanup would likely amplify the measured hub bias rather than fix it. Cell spec'd (not dispatched): exp_schema_relation_hubness_debias_rescore_v1, HARD-PASS MRR rms>=0.15 AND Hits@1 material lift>=0.05 on >=2 rel x >=2 enc @ V>=300, HARD-FAIL MRR lift<=+0.02 all cells, P_deflated=0.38 (HARD-PASS) / 0.50 capped (at-least-partial-recovery).
- research: ranked the 3 "multiples improve capability" candidates from the brain-component inventory drill (ensemble RPE / population-coded BG / 3-tier CLS) against the R-banks-of-N==1-bank-of-R*N decorrelation caveat -> notes/research_multiples_ensemble_rpe_decorrelation_control_cell_spec_2026-07-05.md. Ensemble RPE ranks #1: the just-passed exp_pfc_gate_cfrpe_trained_v1 (gonogo_lift=+0.365) landed MIDDLE_BAND not HARD_PASS for exactly ONE reason -- cross-seed cv=0.187 vs required <0.10 -- and variance-reduction-via-decorrelated-ensembling is the textbook fix (Osband 2016 bootstrapped-DQN / Anschel 2017 Averaged-DQN precedent, bagging-variance formula Var=sigma^2*[rho+(1-rho)/K]). Population-coded BG ranks #2 (real headroom but "96% irreducible" frustration already flags a structural not noise ceiling -- R-bank-trap-shaped). 3-tier CLS ranks #3 (temporal-separation architecture, doesn't map to the decorrelation framework at all, prior evidence weak/mixed, don't re-run naively). Decisive cell exp_pfc_gate_ensemble_cfrpe_v1 spec'd (not dispatched): K=5 bootstrap-resampled SR-transport estimators averaged, vs an EQUAL-COMPUTE-BUDGET single estimator control (the R-bank-trap-avoidance arm) plus a direct mean-pairwise-reach-correlation diagnostic. HARD-PASS requires ensemble beats equal-budget single by >=0.03 AND cv_ratio<=0.60 AND rho<0.70 AND ensemble's own cv<0.10; HARD-FAIL if ensemble<=equal-budget single OR rho>=0.90 OR cv_ratio>=0.95. P_deflated=0.38.
- research: perception/encoder capability scope (USER PRIMARY focus) -> notes/research_encoder_perception_state_buried_win_shipmetric_carrythrough_2026-07-05.md. Full step1b BGE-distillation chain inventoried off-disk (Step1 orthographic CRASHED/ceiling~0.49-0.52 -> v1/v2/v3/v3b/v3c). KEY FINDING: a result already clearing the pre-registered 0.85 bar with algebra intact is buried unpromoted inside a HARD_FAIL cell -- exp_encoder_migration_step1b_v3c_full_paired_rkd_only_dense_recovery_v1 in-batch-RKD-only arm (5-seed FULL): spearman-to-teacher mean 0.886 (0.852-0.897), keyed_roundtrip@J5=1.000 all 5 seeds -- masked because the cells AGGREGATE verdict was driven by a co-located GLOBAL-landmark arm whose algebra broke (keyed@J5 mean 0.143). Proven blocker = training-objective mismatch (InfoNCE tail-corruption at scale + GLOBAL-landmark objective breaking bind/unbind sign structure), NOT teacher-dependence/capacity/sparse-algebra-tradeoff (capacity-ceiling diagnosis superseded -- same-size MLP hits 0.886 once corrupting terms removed). 0.85-native (teacher-free) verdict: NOT realistic at current 970K-item corpus scale (cross-referenced same-day 5x drill); distillation-from-BGE stays the pragmatic path and may be much closer to shipped than tracked. Highest-leverage next cell (spec only): exp_encoder_step2step3_inbatch_rkd_shipmetric_carrythrough_v1 -- carry the buried-win checkpoint through Step2 (full 970K sparse-encode) + Step3 (real gold-verify cosine-to-right-answer, the literal ship metric), near-zero marginal cost. HARD-PASS top1 cosine-to-gold>=0.80 AND composed-query FHRR roundtrip>=0.95 AND ret-agree@10>=0.30; HARD-FAIL cosine-to-gold<0.60 OR roundtrip<0.85. Brain-grounding: distillation-as-bootstrap (community/caregiver analog, not a crutch) + in-batch/local relational-RKD beating global-landmark objective mirrors population-coding local-relative-tuning + contrastive-learning in-batch-negatives precedent; ~2% sparsity target independently grounded in HTM/SDR theory (Ahmad and Hawkins) + measured cortical sparsity (Olshausen and Field). P_deflated=0.42. Hand-off filed notes/exp_dev_handoff_research_encoder_perception_shipmetric_carrythrough_2026-07-05.md.
- research: trained hard-neg-mining redirect spec (rank-1 gap, post-hoc CSLS/logit-adjust rescore MIDDLE) -> notes/research_hardneg_mined_scorer_v1_spec_2026-07-05.md. Off-disk recompute of the LANDED debias-rescore smoke metrics (not just verdict prose) shows the post-hoc win is slot-conditional: JOINT (undertrained/hub-collapsed) gets a genuine non-phantom CSLS lift (CausesDesire REAL Hits@1 +0.467, SHUFFLED +0.0, clears_HP=True) while FROZEN (fully-converged bilinear relation transform on FIXED content, the "full-strength" scorer) is phantom-only (REAL Hits@1 -0.033 to -0.317 as correction strength increases, apparent rms win 100% attributable to a SHUFFLED collapse of -0.567). NEW measured pillar: FROZEN's REAL-vs-SHUFFLED argmax-winner Gini is nearly identical (V300 CausesDesire 0.980 vs 0.971; AtLocation 0.918 vs 0.927) -- direct internal proof the hub concentration lives in the FIXED object-embedding geometry, not in what the relation transform W learns (a linear/bilinear map can't touch it; corroborated by lit: PCA/ICA null-results, Feldbauer and Flexer 2019). 2 lit-scans corrected the naive "hard-neg mining" framing: the scorer already trains exact FULL softmax (every candidate already in the loss every step), so DPR/ANCE-style mining's classical benefit (fixing sampled-softmax exposure gaps) does not transfer; the mechanistically correct lever is a MARGIN-augmented loss (ArcFace/CosFace/L-Softmax family) calibrated to the diagnosis note's own MEASURED z-margin (2.2-2.7 std), with periodic re-mining used for curriculum/target-selection rather than new-negative exposure -- plus a Bayes-consistency caveat (Gordon-Rodriguez et al. 2020: confident preference for popular objects on genuinely ambiguous inputs is CE-optimal, not a bug) that motivates reporting curriculum-by-relation-fanout as a diagnostic, not a gating variable. Cell spec'd (not dispatched): exp_schema_relation_hardneg_mined_scorer_v1 -- margin+periodically-mined-hinge loss (+ cheap LOGIT_ADJUST_LOSS train-time ablation) applied to BOTH FROZEN and JOINT slots (deliberate extension beyond literal "JOINT-only" task wording, justified since FROZEN is the proven post-hoc-immune slot and must get a trained-fix opportunity to actually test the claim), plus a new SHUF_OVERFIT_GUARD anti-phantom control specific to trained (not post-hoc) mechanisms. HARD-PASS REAL Hits@1 lift>=0.05 AND MRR rms>=0.15 on >=2 rel x >=2 enc @ V>=300 with SHUFFLED not lifted >+0.03; HARD-FAIL REAL Hits@1 lift<=+0.02 everywhere. P_deflated(HARD-PASS)=0.33; P_deflated(beats post-hoc's own best non-phantom MIDDLE outcome)=0.58.
- research: content-embedding de-hub JOINT lever probe (generalization rank-1 ceiling x encoder ret-agree@10 gap) -> notes/research_content_dehub_joint_lever_gen_encoder_2026-07-05.md. Off-disk direct measurement of BOTH content spaces (not inference from prior notes): generalization content = BAAI/bge-small-en-v1.5 384d over 9,340 ConceptNet-style schema entities; encoder-teacher content = BGE-large 1024d over 177,899 KB concept names (~75% CN_-prefixed ConceptNet-derived). NOT the same index (different checkpoint/dim/corpus) but 49.4% direct vocabulary overlap (4,613 entities), and among that overlap object-side k-occurrence hub status correlates Spearman rho=0.545 (p~0) across the two independently-trained checkpoints, with the SAME literal superordinate-category words (food/animal/clothing/people/drink) topping both hub lists (16/50 top-hub overlap vs ~0.5/50 chance) -- a genuinely novel, directly-measured cross-model hubness-transfer finding, no prior literature found testing this directly (corroborated qualitatively by KG-embedding degree-persistence lit, Obraczka and Rahm 2021 / Fanourakis et al. 2023: real but method-modulated, matching the moderate-not-total rho). Raw content-geometry Nk10 Gini measured directly: 0.372 (schema, n=9340) / 0.468 (encoder, n=6000 subsample) -- real but well below the 0.87-0.98 the trained scorer compounds it to, meaning content de-hub is necessary-but-partial. Evaluated 3 training-free de-hub candidates by merit (own Nk-Gini reduction numbers): ABTT (Mu and Viswanath 2018, weakest, source paper makes no hubness claim) < ZCA whitening (Su et al. 2021 precedent, strong) <= Local Scaling (Zelnik-Manor and Perona 2004, strongest on schema/tied on encoder, best-precedented via Nielsen/Macocco/Baroni AISTATS 2024's direct SBERT-family result) -- Local Scaling picked as PRIMARY. Crucially this is NOT a repeat of the already-run exp_schema_relation_hubness_debias_rescore_v1 post-hoc score rescore (already shown partially phantom on the FROZEN content-baked locus): this spec applies the transform to the CONTENT INPUT before any downstream fit and retrains from scratch -- both lit-scans confirmed no literature tests 'de-hub before training' vs 'after convergence' in any domain, a genuine first-of-its-kind comparison. Cell spec'd (not dispatched): exp_content_dehub_joint_lever_v1 -- shared local_scaling/zca_whiten/abtt transform module applied independently to each content space, retrain-from-scratch on both harnesses (schema relation-scorer + encoder in-batch-RKD), new student-own-retrieval-Nk-Gini diagnostic (tests distillation-induced hub amplification vs pure inheritance), new synth_cross_domain_shared_hub joint-lever-specific positive control. HARD-PASS lift>=+0.05 on BOTH generalization Hits@1 rms AND encoder ret_agree10 with controls firing; HARD-FAIL either side <=+0.02. Stratified prediction (not gating): content de-hub should help AtLocation (genuine geometric mix, residual Gini 0.175) more than CausesDesire (near-pure label-prior, residual Gini 0.077) per the parent hubness note's own decomposition -- most likely MIDDLE-BAND outcome. P_deflated(joint HARD-PASS)=0.20; P_deflated(at least one side genuine >=+0.05)=0.48; the SHARED-MECHANISM finding itself (rho=0.545) is reported as measured, not predicted.
- research: 5x-drill Angle 5/5 (substrate-native LANGUAGE, the glass-box-LM north star) -> notes/research_substrate_native_language_path_5x_angle5_2026-07-05.md. Verified off-disk (not inferred): the landed native-GSBC block-local decoder's filler codebook traces concept_rows -> data/substrate_index/cached_indices/bge_large_v2_name_177899_54f7cf6a.npz::id_order_json to REAL ingested concept names (ConceptNet CN_ nodes + math-taxonomy T1/T2 atoms), not anonymous synthetic codes. Combined with today's Integration reason-generate HARD_PASS + CG multi-hop KG retrieval, all 3 primitives needed for "retrieve a real fact, compose it, decode it into ordered words" are independently already CG/HARD_PASS -- the missing piece is a ~100-150 line glue script (source real ConceptNet triples, print id_order_json string lookups instead of only index-match accuracy, hand-curate common-word entries to dodge obscure nodes like CN_catoptrophorus_semipalmatus) not new mechanism. Cortex-1 (noise/context/attention facade) and Cortex-2 (atom-consultation over the substrate's OWN build-discipline metadata via a char-trigram sidecar, zero wiring to the concept encoder) both confirmed NOT to gate this capability -- neither touches language. Verdict: narrow grounded-telegraphic-utterance demo is READY NOW as a parallel track (not gated on blocked general-knowledge ingest or parked Cortex-2); full conversational M3/M4 glass-box-LM remains the genuine capstone (Cortex-2 utility unproven, one-to-many ceiling open, morphology/function-words entirely unbuilt). Cell spec'd inline (exp_generation_grounded_fact_utterance_v1, not dispatched per no-routing-files discipline): HARD-PASS exact-ordered match>=0.70 on >=20 curated common-word ConceptNet S/V/O facts + zero garbled decodes + shuffled-fact control collapses; HARD-FAIL <0.30 match or >=20% non-legible decodes. P_deflated=0.45 (capped; mechanism pieces independently proven, risk is glue-script/curation not architecture).
- 5x-drill angle 4 of 5 (ingestion readiness): machinery PARTIAL (real ConceptNet KB 133305 nodes already in canonical store since 06-19, verified Store-load-clean; U1 FB15k-237 2-hop reasoning CHAIN_GRADE 5000-7410x random floor, isolated harness) but LIVE-PATH addressability never verified (qualified-id collision fix built on disk, not confirmed wired into retrieve_cache.py's cache-selection) and N8 ConceptNet-eval pre-registered 06-22 never dispatched. Cheap decisive test spec'd (exp_ingest_knowledge_integration_verify_v1, no dispatch, P_deflated=0.35-0.40). Brain CLS mechanism (Tse et al schema-fast-path) validates verify-small-first-then-fast-integrate strategy. Does NOT need re-encode; independent of (not gated behind) angle-3 integration test's open reasoning->generation seam -> run both in parallel. See notes/research_ingestion_readiness_scoped_pilot_5x_angle4_2026-07-05.md

---
2026-07-05 (5x-drill angle 1/5, capability-optimization ranking): notes/research_capability_optimization_ranking_2026-07-05.md.
Ranked CONTROL > COMPREHENSION > REASONING for near-term optimization (capability-gain x feasibility x glass-box-LLM-value);
flagged PERCEPTION and INTEGRATION as STALE candidates (both already CLOSED same-day per backup doc re-reads, verified
off-disk: integration HARD_FAIL margin=-0.706 is a decisive glass-box-positive; perception ship-gate PASSED via graded
GSBC codes v11/v12). Top pick: in-flight exp_pfc_gate_cfrpe_deeper_regime_v1 smoke already falsifies its own SR-horizon
(gamma) hypothesis (bit-identical gonogo across gamma) and confirms branching-factor (n_ops) as the real depth-6 driver
(3x closure effect) -- independently corroborated by a context-blind lit-scan (Redgrave-Prescott-Gurney BG selection
problem, Hick's law, options/HRL literature predicting hierarchy fixes DEPTH not branching). Next: exp_pfc_gate_branching_
depth_entropy_grid_v1 (P_deflated=0.50), gated hierarchical-options gate follow-on. Comprehension: independent lit-scan
found the disjoint-per-role-vocab test design avoids the dominant real-world difficulty driver (cue-overload/fan-effect
interference, Van Dyke & McElree 2006); shared-vocab retest spec'd, not yet cell-authored. Companion hand-off:
notes/exp_dev_handoff_research_capability_optimization_ranking_2026-07-05.md.
