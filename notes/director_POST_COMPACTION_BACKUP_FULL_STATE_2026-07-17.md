# Director POST-COMPACTION BACKUP -- CLEAN RECOVERY STATE (rewritten 2026-07-22; full blow-by-blow chronology archived to notes/director_BACKUP_ARCHIVE_full_session_chronology_2026-07-22.md)

## 🚨 READ FIRST (post-compaction recovery)

**▶ RESUME HERE (07-25 ~22Z, tail ~29569):** REASONING ARC COMPLETE (all-effort). Reasoning MECHANISM = SOLVED+DEMONSTRATED (verification-by-derivation, 100% clean-derivable, real glass-box traces). Both structural doors MEASURED SHUT (tie-break d=0 / precision 1-of-66). INPUT BRAIN-FIDELITY AUDIT (USER): only 2 inputs non-brain-faithful = MEANING (GloVe+WordNet) + COMPREHENSION (_content_words), and THOSE are the measured wall. **EARNED NEXT DECISION (awaiting USER go): commit the grounded-meaning build = REPLACE the 2 non-faithful inputs -> grounded/earned meaning (grounded-relevant relations HasProperty/UsedFor OR supply-Binder 65 brain-features at entity+decision nodes) + wire the real Kintsch reader (situation_reader). Do NOT auto-start; USER holds go.** Detail = the '🎯 ALL-EFFORT REASONING' + 'REASONING ARC FINAL' + 'INPUT BRAIN-FIDELITY AUDIT' blocks below. In-flight at compaction: precision-test VET (skunkworks a7ec889e); if cert ledger tail < 29570, that VET didn't bank -- the HONEST-NEG finding is already documented (re-dispatch only if you want the atom). Store banked 29560-29569 LOCAL-only, NOTHING pushed.

**FIRST ACTIONS:** (1) `date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp` (heartbeat EVERY turn-end). (2) `python tools/inflight_monitor.py`. (3) confirm cron d7503a43 (CronList). (4) Read this block -- it is self-contained + authoritative.

### 🔑 CURRENT FRONTIER (2026-07-25 ~14:10Z, tail 29559, post-VET — SUPERSEDES older frontier blocks below)
**BOTH quick levers to make the composed reasoner beat baseline are VET-DEAD as-approached (skunkworks abda1634, banked 29558+29559). The VET caught two fake positives BEFORE either was wired — the guardrail working.**
- **29558 — learned-meaning front-end = MEMORIZATION, not learning.** Skunkworks ran the held-out-to-new-concepts test I hadn't: fit ridge on 80% of triples, eval held-out 20% → **0.106/0.107/0.157 vs frozen-GloVe 0.324, BELOW chance (0.171)**. "The generalizable linearly-decodable component of frozen GloVe is ~zero." The +0.15 in-vocab lift is a lookup table over the train set. **Do NOT wire this into reasoner.py — it's a fake.** Thin-meaning wall re-confirmed at real scale.
- **29559 — rule-supply (corpus science-extract) = PROMISCUOUS / STRUCTURAL.** Coverage rose 0.07→0.50 (extraction is NOT the bottleneck) but the depth-3 typed-connectivity GATE is anti-selective (typed never beats untyped-null). Precision-first v2 is a WEAK revival (CSKG-arc convergence). Rule-supply arc CLOSED.
- **BRAIN-FIDELITY ELEMENT AUDIT (mandated, done):** 3 of 5 task elements — and the two that CARRY MEANING (concept meaning + fine-distinction learning) — are similarity/lookup proxies where the brain grounds+differentiates. The reasoner (meet-in-middle chaining, the one brain-faithful element) is starved by thin inputs. **Fix is ARCHITECTURE-LEVEL: replace thin/borrowed meaning with learned/grounded meaning the brain's way. No shortcut survives.**
- **CONVERGENCE:** ARC wall (29555) + memorization (29558) + promiscuity (29559) ALL point at ONE root = thin borrowed meaning. This IS the USER's locked no-shortcuts direction, now proven by elimination.
- **🎯 FRONTIER = the substrate's OWN error-driven differentiation loop (100% brain-centric; NO borrowed encoder).** The brain earns fine meaning by grounded, error-corrected, coarse→fine differentiation (Rogers-McClelland + ATL hub + Barsalou), NOT a pretrained oracle. **HARD RULE (USER 2026-07-25, corrected Director drift): a borrowed pretrained embedding (GloVe OR BGE) is NEVER the runtime/ingestion encoder — that is the exact shortcut the no-shortcuts commitment forbids.** The concept encoder is CENTRAL (encodes concepts for BOTH store-ingestion AND runtime queries), so adopting a borrowed vector would make it the substrate's meaning organ = a real violation. The encoder-migration lineage (step1→v4, distills BGE-large) is therefore NOT the meaning destination; at most its teacher (BGE) is a DIAGNOSTIC (below), never the encoder.
- **BGE = DIAGNOSTIC ONLY (routing probe on WHERE the fine-discrimination signal lives), never adopted.** GloVe (raw co-occurrence) fails held-out (~0.10 < chance 0.171). BGE is contrastively trained on RELATIONAL supervision. Test outcome routes the grounding build: (a) BGE ALSO fails held-out → signal not in text → brain-way learner must draw on grounding/structure beyond text (typed relations, supplied axioms); (b) BGE clears held-out → signal IS in text via relational supervision → our differentiation loop should train on the TYPED relations we already have (WorldTree/store), not raw co-occurrence. Either way the BUILD is the substrate's own differentiation loop; BGE is discarded after the probe.
- **✅ WE ALREADY HAVE NATIVE ENCODERS (USER 07-25: "don't we already have an encoder? we put a lot of work into it" — YES). Build the differentiation loop ON THESE, not from scratch, not BGE:** (a) `hdlab/char_trigram_encoder.py` = native surface/orthographic (VWFA analog; "cat/kitten share no trigrams" — spelling not meaning); (b) `hdlab/concept_encoder.py` = **the brain's own learning mechanism we ALREADY built** — competitive-Hebbian sparse coding + WTA + sign (cat_kitten_cos 0.492) BUT trained only on a SYNTHETIC toy corpus with designer-imposed clusters (its own honesty block); (c) `hdlab/composed_encoder_v3.py` = native dual-stream VWFA + PPMI-co-occurrence + ATL-hub N400 late-combine (semantic stream is native co-occurrence = thin like GloVe). GAP is NOT "no encoder" — it's: native encoders capture surface OR co-occurrence (both thin), and the one that LEARNS the brain's way earned its distinctions only on a TOY, never on real structured knowledge.
- **BGE/native DIAGNOSTIC — LANDED (hdi_exp_dev, cell exp_bge_finemeaning_wall_probe_v1, commit 9bb409a59, VET-PENDING skunkworks a04409dc). MEASURED (1125 fine items, BGE-native distractors, chance 0.171): ZERO-FIT cosine BGE 0.374 / GloVe 0.354 / native char_trigram 0.232 / chance 0.171. Held-out learned-linear: BGE 0.228 vs GloVe 0.128(≈chance) vs shuffled 0.111. Verdict MIDDLE. ROBUST NEGATIVE (over-read-proof): NO text-cosine representation — borrowed OR native — is near solving fine discrimination (best BGE 0.374 vs a usable ~0.7+); similarity != fine meaning is UNIVERSAL even for a strong modern encoder => do NOT chase a better pretrained vector. FAINT positive (VET-pending, NOT leaned on): learned-over-relational-supervised (BGE) held-out 0.228 > chance where raw co-occurrence memorizes => the differentiator is LEARNING FROM RELATIONAL STRUCTURE. Routes the build below.**
- **🧩 THE BUILD = COMPOSE OUR OWN PARTS (USER 07-25 "use our learning system if relevant" — YES, load-bearing): concept_encoder (competitive-Hebbian) = the differentiated sparse HD REPRESENTATION + `hdlab/learner/` (the 29487 Learner: MDL model-selection over plugins estimation/ruleind/gam/proginduction, `core.py mdl_select`, `per_cluster_gate` compression>=1) = the GENERALIZATION ENGINE, run over REAL typed WorldTree relations (episodes=concepts, features=typed relations KINDOF/HABITAT/MADEOF/…, target=fine value). The learner induces FEATURE/RELATION-LEVEL rules (shared, compositional) so a held-out concept encoded from its KNOWN relations inherits fine structure = generalizes not memorizes. compression_ratio>=1 gate = the BUILT-IN generalize-not-memorize guardrail (KEEP_EPISODIC = honest memorization flag — would auto-catch the Step-5 failure). Sleep-loop = the consolidation wrapper (already PROVEN to compress on CLEAN structured data McGuffey 1.174, honestly KEEP_EPISODIC on high-entropy real-news 0.907; WorldTree = curated/clean = favorable regime).**
- **NEXT (the brain-centric, work-preserving frontier):** (1) [DONE, VET'd 29560] diagnostic — fine meaning NOT in text cosine; discard BGE. (2) [LANDED 79083ef02, VET'd+CORRECTED 29561, commit 05834fbc8 — DO_NOT_WIRE] composed differentiation-loop. exp_dev's "frequency-saturated foundation-not-ready" story is WRONG (Director over-read it too). VET off-disk recompute: (a) MFV=0.97 = SCORING ARTIFACT not saturation (gold always candidate index-0 + abstain-returns-index-0 => auto-correct; MFV absent from distractors 91%; TRUE base-rate 0.06; honest forced-choice bar = frozen GloVe 0.57); (b) KEEP_EPISODIC = CE->MDL WIRING BUG not "no rule" (relation-only gam compression 1.076>1 DOES induce a rule; adding CE's ~20 raw sparse-dim tokens/concept collapses it to 0.797<1); (c) density REAL-but-partial (frac_ge2=0.20, ~80% held-out lack a 2nd relation). All held-out accuracy bars except frozen-GloVe CONTAMINATED; compression (on TRAIN) trustworthy. => diagnosis = 2 bugs (Director-owned) + 1 real-partial-density, NOT a foundation megaproject.
- **✅ v2 LANDED + VET'd (76471541d, VET banked 29562 commit 0d9776efd): 3 fixes worked, measurement CLEAN. DECISIVE ROUTING = MECHANISM-CAPPED not density-capped.** Native composition dense-subset (>=2 rel) acc 0.211 < sparse 0.264 < GloVe-dense 0.554 = ANTI-SCALES with density (fires-and-wrong, no abstain) => more tables WON'T help. The "genuine rule" is BASE-RATE-DOMINATED (gam pick 80% identical to MFV; relational lift only +0.039, near-nil on dense) -- Director over-read it as real reasoning; VET corrected. frozen-GloVe 0.568 is GloVe-FLATTERED (freq-matched != confusable; real-wall GloVe 0.13-0.35) but native 0.21-0.26 even on easy distractors => negative robust. PIVOTAL: GloVe extracts property structure on dense subset (0.554) that native encoder does NOT (0.211) => the property IS learnable from meaning; our LABEL-CONDITIONED batch/order-invariant concept_encoder doesn't capture it. promote=DO_NOT_WIRE, HOLD for encoder MECHANISM rebuild.
- **🎯 NEXT BUILD -- RE-SCOPED by look-back a4043dbc (mechanism = BINDING not differentiation-encoder): the differentiation encoder ALREADY EXISTS (Hub in exp_learned_meaning_frontend_differentiation_v1, atom 29556) and is a known in-vocab MIDDLE (fine-lift 0.339, coarse-before-fine 4/4, but ho_lift 0.0 = memorizes, NO held-out generalization). Held-out generalization + the v2 anti-scaling are BOTH the SEPARATE axis = NATIVE BINDING / systematicity (Fodor-Pylyshyn/Smolensky failure of flat nets). FIX = NVSA pattern (Raven's ~88%): item+relation combine ONLY via FIXED substrate-native bind (hdlab.binding.bsc_bind) + SINGLE role-agnostic linear readout -- structurally forbids per-pair memorization. DISPATCHED hdi_exp_dev a4f609aa = exp_native_binding_compositional_generalization_v1 per design-of-record notes/research_native_binding_compositional_generalization_2026-07-25.md. Corrected held-out split (category-correlated relations ONLY, not unlearnable-distinctive -- 29556 conflated them). Arms: FROZEN/FLAT-MLP/native-binding(GloVe-CONTROL, isolates binding)/SHUFFLED/bind-then-MLP + Director-added arm 3n = native-binding w/ NATIVE concept_encoder item vector (fully-native stack). GloVe = diagnostic MEANING-CONTROL only (NOT adopted; bsc_bind is native = mechanism under test). SEPARATES binding-vs-meaning bottleneck: HARD-PASS(GloVe-ctrl) => binding is the fix, remaining gap=meaning; HARD-FAIL => binding alone insufficient (meaning co-bottleneck or Lake-MLC training-regime). Cheap ~1-2hr CPU. P_deflated 0.45.**
- **✅ BINDING CELL LANDED + VET'd (already certified 29557 commit 58c317af2; my re-dispatch a09baa4aa = cert-neutral AMENDMENT 29563). VERDICT: MIDDLE, and Director's "REFUTES binding-as-fix" was an OVER-READ -- VET-CORRECTED:** binding is UNTESTED single-hop, NOT refuted. bind+linear and concat+linear are BOTH linear-in-item at single-hop (bind_over_concat=0.0 is NEAR-STRUCTURAL) -> binding's distinctive power (superpose many bindings + unbind-by-role) was NEVER EXERCISED; the HARD_PASS gate was near-unreachable by construction. Systematicity essentially NULL (+1/9=one held item, noise floor; shuffle-sep 0.111<0.15) on an UNDERPOWERED 4-domain TOY (n=9, single seed). linear-vs-MLP finding CONFOUNDED with hyperparams (not causally proven). arm 3n 0.333 CONSTRUCTION-DETERMINED (planted category), and it REINFORCES: clean category structure IN MEANING -> same bind+linear stack jumps. NET: "MEANING IS THE LEVER" = CONFIRMED + reinforced; binding architecture PARKED-not-dead (licensed test = MULTI-ROLE / deep-composition regime, not single-hop).
- **📉 SCIENCE-GROUNDED SPACE CHARACTERIZED (Explore a642b513) -> GPU-SCALE SCIENCE FOLLOW-UP NOT VIABLE, GPU RESERVE RELEASED: WorldTree PROP-* = 20 CLEAN single-attribute tables (magnetism/conductivity/hardness/opacity/acidity/state-of-matter/warm-cold-blooded, value cardinality 2-4 = clean targets) BUT SHALLOW: mean 1.1-1.2 attrs/concept, median 1, frac_ge2=0.13 science-core (< v2's 0.24). Rich multi-attr vector exists for ~10 canonical MATERIALS (plastic/metal/glass/wood/water...); trainable earn-multi-attr-from->=2-relations set = ~21 concepts (80 single-attr). NOT a dense science Binder. => science follow-up = at most a ~20-material MICRO-BENCHMARK, not a GPU-scale run. ARC context abundant (14.6M sentences) but relations sparse. **CONVERGENT FINDING (hold firm conclusion for Binder mechanism test): every grounded+relational source is THIN at the intersection (WT-rel frac_ge2 0.24 / WT-PROP 0.13 / Binder-WT ~21) -> the wall may be shifting from MECHANISM to the GROUNDED-DATA FOUNDATION itself ('substrate knows almost nothing' concrete). Honest options if Binder test confirms: (A) small clean materials micro-proof; (B) invest in a denser grounded foundation (CSLB 638x2725? richer norms); (C) accept meaning is data-bound not mechanism-bound here.**
- **✅ BINDER MECHANISM TEST LANDED (b2d0d0bf2, VET a706edb7 in flight): CONTEXT-CARRIES, RELATIONS-WEAK.** Source-isolated p@10 retrieval vs Binder gold, held-out, no-leak: context_only(distributional) 0.243 > both 0.204 > relations_only 0.139 >> chance 0.026; relations SHUFFLE-COLLAPSE (0.139->0.042 = REAL structure, weak not memorization); relations DILUTE context (both<context). => the win is CONTEXT->grounded = native-Feature2Vec (distributional, the thin lever); the brain-consistent RELATIONS lever is REAL-but-WEAK. **DIAGNOSIS (VET adjudicating sparsity-vs-fundamental): relations weak because relational DATA is SPARSE (~2/concept on Binder-141; science-PROP ALSO sparse frac_ge2 0.13) -> the wall is the RELATIONAL-GROUNDING DATA FOUNDATION, not the mechanism. Agent's rec 'go to science, dense relations' is WRONG (characterization: science relations sparse too).** CANDIDATE NEXT LEVER (if VET says sparsity-bound): DENSIFY relations by READING ARC_Corpus (14.6M sentences) w/ our reader infra -> more relations/concept -> re-run earn-grounded-from-relations. RISK (load-bearing): text-relation-extraction may reproduce PROMISCUITY (29559 science-extract was promiscuous). Explore acce016b DONE: densify-by-reading-ARC BLOCKED -- text-extractor is PROMISCUOUS (29559, -0.06 selectivity, hub 160) AND emits WRONG types (causal not structural KINDOF/MADEOF/PARTOF/HABITAT). Compute trivial (~5min CPU); QUALITY is the wall. => can't cheaply get clean STRUCTURAL relations from noisy text. **CLEAN ALTERNATIVE = supply DENSE CURATED structure from WordNet (nltk, we have it; density probe: hypernym-chain 8-14 = KINDOF, meronyms = PARTOF/MADEOF, 5-48 sisters -- ~10x the WorldTree ~2; curated NOT promiscuous). DECISIVE TEST DISPATCHED (hdi_exp_dev aa4f9a8f): re-run earn-grounded-from-relations with WordNet-dense structure vs WorldTree-sparse 0.139 vs context 0.243. RESOLVES sparsity-vs-fundamental EMPIRICALLY + tests the north-star supply-structure lever: relations_wordnet>>0.139 & approaches context => SPARSITY-BOUND, supply-clean-structure works; FLAT ~0.139 despite 10x => FUNDAMENTAL, structure-alone-insufficient (grounding needs sensorimotor/Barsalou beyond taxonomy). CPU inline. **🎛️ PHASE-DIAGRAM LEVER CASHED IN (USER 07-25 reminder 'we can move sparse<->dense on command'): the trigger fired (brain-faithful/reasoning phase). Bundling 10x WordNet relations in DENSE codes = SUPERPOSITION CROSSTALK = almost certainly the cause of the predecessor 'relations dilute context' (0.204<0.243) + v2 ANTI-SCALING. So the WordNet cell MUST test the relational bundle under SPARSE codes too (reuse concept_encoder WTA / sparse-block-binding / GSBC) -- the SPARSE version is the FAIR test; a dense-FLAT result would be a coding-regime artifact NOT 'fundamental'. Sent to aa4f9a8f. Sparse distributed codes also more brain-faithful (cortex). This lever likely applies broadly (v2 anti-scaling, binding) if it fixes the WordNet dilution.**
- **🔎 CROSSTALK RE-TEST QUEUE + SPARSE-INFRA SCAN (Explore a4367fcf) -- with an HONEST cold-water caveat: SPARSE-REDUCES-CROSSTALK is NOT yet demonstrated in-substrate (prior GSBC dense-vs-sparse + ARCH-B gates came back SPARSITY-NEUTRAL / dense-not-worse; higher-density m8 even beat m5 on one retrieval; only 1 relative positive = sparse block-binding 0.528>0.423). So sparse-fixes-composition is a LIVE HYPOTHESIS to TEST, not assume.** RANKED reopen candidates: (1) **AGGREGATION RETRIEVER 29537/38 (cleanest + ARC-relevant): bundle 0.766>single 0.706 on ORACLE-clean but bundle 0.298<single 0.342 on REAL-noisy = textbook superposition-dilution -> DISPATCHED sparse re-test hdi_exp_dev a86759886**; (2) Binder-grounded 29564 (both 0.204<context 0.243 dilution inversion -- the aa4f9a8f cell covers this); (3) crosstalk-capacity-law 29532/34 = the THEORY licensing the bet (crosstalk dominates capacity r=0.976), don't rerun; (4) diff_loop_v2 29562 = LOW/NOT-crosstalk (GloVe-beats-us 0.57 vs 0.24 CONTENT gap, sparse won't fix); (5) binding 29563 = NOT crosstalk (binding works, failure was MLP; multi-role sparse binding is a legit EXTENSION not a reopen). SPARSE INFRA: GSBC `gsbc_graded_m5` (hdlab/gsbc_graded_encoder.py, certified block bind/unbind, registry-selectable via kb_encoder_registry -- swap = ~config flag at store, but needs text->dense teacher for raw text) OR ppmi_sparse_encoder (self-contained text-native, uncertified-for-binding). **TWO LIVE SPARSE TESTS now adjudicate the hypothesis on complementary tasks: relational-grounding (aa4f9a8f) + fact-aggregation (a86759886). If BOTH neutral -> sparse is NOT the lever, say so honestly (don't keep pushing). If either rescues -> sparse validated + reopen the queue.**
- **✅ BOTH SPARSE TESTS LANDED -> SPARSE-CODES REFUTED as the composition lever (fair test, 2 tasks, STOP pushing sparse): (1) WordNet relational grounding (2b6d294a0, VET af685ba8 in flight): sparse lowered crosstalk 0.406->0.297, NO retrieval gain; (2) aggregation retriever (7ae97e0ab, VET pending/serialized): sparse lowered fact-interference 0.793->0.664 (~16%), NO accuracy gain (sparse-bundle 0.308~=dense 0.301 < single 0.336); oracle-clean advantage preserved (sparse didn't break the code). BOTH = same DISSOCIATION: sparse genuinely REDUCES crosstalk but crosstalk is NOT the limiter. Consistent w/ prior GSBC/ARCH-B neutral. User's phase-diagram lever is REAL but doesn't fix these walls.**
- **🗺️ THE MAP (VET-CORRECTED 29565 -- Director over-read the 'supply-not-learn' pivot; walked back):**
  - **(A) MEANING axis -- NOT walled (my over-read corrected):** what's PROVEN = hypernym TAXONOMY adds ~0 beyond distributional context; sparse doesn't help; 'dilution' was a fusion artifact. What's NOT proven = 'text can't ground meaning' -- the DISTRIBUTIONAL CONTEXT arm itself reaches 0.245 (>>chance 0.02, >> best relation 0.094) => text-distributional DOES carry substantial grounded signal (modest but real). We only tested hypernym TAXONOMY (wrong type for grounding). SUPPLY-NOT-LEARN pivot = HYPOTHESIS-PENDING, gated behind REVIVAL CRITERIA: (1) grounded-relevant relation TYPES (HasProperty/UsedFor/attribute/event, NOT hypernym taxonomy), (2) multi-seed FULL, (3) learned/gated readout. Honest state: distributional meaning = modest working baseline 0.245; structure/taxonomy doesn't add; grounded-relevant-relation revival UNTESTED but the meaning-LEARNING axis has shown diminishing returns over many experiments.
  - **(B) RETRIEVAL wall -- SOLID (aggregation test + prior 29537/38): for ARC the bottleneck is getting the RIGHT facts (retrieval_bottleneck 593 >> aggregation_fail 233), NOT combining them. Sparse doesn't touch it (crosstalk not the limiter). This is the CLEARER, more ARC-relevant, UNDER-EXPLORED-as-focus lever.**
  - **RECALIBRATED RECOMMENDATION (corrected): meaning-learning is at diminishing returns w/ a modest working baseline (distributional 0.245); the RETRIEVAL axis is the clearer next lever for ARC reasoning. Refocus on RETRIEVAL SELECTION (get right facts) + REASON over it (reasoner exists, coverage-bound), using current distributional meaning as the foundation. HOLD grounded-meaning revival (property-relations OR supply-Binder) as a parallel option, NOT a declared conclusion. Reasoning goal INTACT. Brought to USER (corrected).**
- **✅ RECOMMENDATION VET-CONFIRMED + DRIVING (USER deferred, full-auto): aggregation-sparse VET banked 29566 -- loss tracks fact CONTENT not code (oracle composes fine in BOTH regimes, noisy fails in BOTH) => RETRIEVAL is the wall, VET-sound. REFOCUS = RETRIEVAL SELECTION, which CONVERGES with the #1 brain-gap our deep-brain-analysis already named = VLPFC CONTROLLED RETRIEVAL (select task-relevant facts, resist surface-lures; similarity-retrieval gets wrong facts). Look-back adb361fc running: current retrieval path + where a selection-gate inserts + VLPFC design + reusable gate machinery (clarify_gate/trustworthy-reader ACC-analog 29465/PBWM Go-NoGo) + oracle upper-bound (~0.69-0.76 given-gold). NEXT = design+build the VLPFC-analog retrieval-selection gate; discriminator = selection raises right-facts-retrieved -> moves ARC-Challenge off chance. STORE: banked 29564/65/66 this session (3 MM), LOCAL-only, needs_orchestrator_store_sync (no push w/o USER auth).**
- **🧠 SELECTION-GATE BUILD DISPATCHED (a223e2eb) -- LEARNED CREDIT-ASSIGNMENT, NOT similarity (anti-shortcut, load-bearing): look-back adb361fc found (a) a selection gate ALREADY exists (29541, goal-bias+RIF-suppression) = MIDDLE_BAND (Challenge-lure +0.091, p=0.19 n.s.); (b) a 7x HARD_FAIL lineage of SIMILARITY-scoring selection (29544-50), root-caused 29549 = 'still similarity/margin NOT entailment' = the thin-encoder wall -> ANOTHER cosine gate would fail the same way; (c) the non-shortcut fix = a genuinely NON-similarity signal = LEARNED VALUE gate. BUILD = wire `hdlab/action_selection.py` (BG Go/NoGo PBWM actor, RPE-trainable, certified but only on synthetic nav) to REAL ARC CORRECTNESS at the proven insertion point (PPR pool 29539 -> gate -> UNCHANGED bind+settle combiner). = the credit-assignment gap (Stage 8) wired to ARC for the 1st time. NO-LEAK (train-split correctness, eval HELD-OUT, shuffled-collapse). Ladder: chance 0.25 / sim-baseline 0.29 / sim-gate 0.341 / LEARNED-gate <new> / oracle 0.70. WIN=learned beats 0.341 on held-out toward oracle; HONEST-NEG=~0.34 => encoder wall foundational even for selection. TENSION noted: master-map retired sim-scoring-selection + pivoted to derivation-SEARCH (coverage-blocked); learned-gate = the COMPLEMENTARY untested precision lever, cheaper than un-blocking search. Brain-faithful (VLPFC controlled retrieval + BG Go/NoGo + dopamine RPE).**
- **✅ LEARNED CREDIT-ASSIGNMENT GATE LANDED (98c3126d3, VET ad250f50 in flight) = MIDDLE, the session's most-encouraging result (VET-PENDING, Director over-reads positives -> hold at 'promising not proven'). Held-out ARC-Challenge: learned L=0.4115 vs sim-baseline 0.333 (+7.8pp = ~20% of oracle headroom) vs hand-tuned sim-gate 0.3704 vs oracle 0.7284 vs chance 0.25. GENUINELY GOOD + CONTROLS-CLEAN: up-weights NON-similarity feature (ppr_act 1.88 largest) + learns lure-suppression from reward (lure_align -0.45); structure +2.9pp over learned-cosine (gain is credit-assignment NOT cosine); BOTH must-fail controls collapse (shuffled-reward/graph 0.3045), generalizes, no-leak verified; self-test full 1.0 vs cosine-only 0.225. VET-CORRECTED (banked 29567, Director OVER-READ again): the +7.8pp over raw baseline DECOMPOSES = +3.7pp GENERIC gate (any top-K, = what a tuned sim-gate achieves; the SIGNIFICANT part p=0.02) + **+4.1pp credit-assignment-SPECIFIC increment = the NON-significant part (p=0.19)**. So PROVEN = 'a selection gate > raw sim-pool' (~known, 29541); UNPROVEN = 'learned credit-assignment > TUNED similarity gate'. Gate is LEAK-CLEAN (controls collapse BELOW baseline, answer-agnostic features, generalizes -- that held up) BUT no fixed-structural-selector control arm => learned-value-carries-it NOT isolated on ARC. CEILING caveat: 32pp L->O residual = selection-precision + meaning COMBINED (NOT isolated); 'meaning is THE ceiling' is INFERENTIAL (leans on 7x lineage), not directly measured. Single-seed underpowered. => HONEST STATE: modest leak-clean selection gain; credit-assignment-specific benefit PROMISING-BUT-UNPROVEN (needs multi-split pooled p<0.05 + fixed-structural control arm to establish); retrieval-vs-meaning split still needs ISOLATING. NEXT: finish the capability-maximization decomposition (a115ca61) w/ this correction; the credit-assignment lever needs the control-arm+multi-seed before it's a real win.**
- **🔬 BINDER MECHANISM TEST refinement sent (relations are sparse 141/534): must SEPARATE earn-from-RELATIONS-only (brain-grounded signal) vs earn-from-CONTEXT-only (distributional = native-Feature2Vec, weaker) vs both -- the relations-only arm is the clean brain-consistent test; a context-only win is honest but NOT 'grounding works'.**
- **🔌 REASONER WIRE-UP SEAM (mapped, ready for when earned-meaning lands): `hdlab/reasoner.py` node-identity flows `nodes_for(text)` -> `arc._content_words` -> `self.enc.encode_batch(words)` -> `self.g["map_words"](vecs)` -> per-word NODE SETS -> meet-in-middle derivation + `_combiner_score` (population-vector: max cos of candidate node_reps vs bundled evidence node_reps). MEANING ENTERS AT A SINGLE SEAM = `self.enc` (the word encoder) + the node_rep space. WIRE-UP = swap `self.enc` -> the earned grounded encoder; better fine meaning -> words map to the RIGHT nodes (correct fine content) + better combiner decision = exactly where the thin-meaning wall bites. Comprehension is currently an INTERIM content-word stand-in (flagged _encode_words) -- the earned encoder replaces it. No architectural change needed, just the encoder swap.**
- **🧭 TWO-TRACK CONVERGENCE ON MEANING: (T1 binding, CPU, DONE) architecture-binding is NOT the systematicity fix. (T2 meaning, GPU, acf87ce3, REDIRECTED to BINDER-GROUNDED) = the primary lever. Both v2-VET (meaning thin) AND binding-VET (binding not the fix) point here. **REDIRECT (USER 07-25 "brain-aligned correct meaning?" -- Director web-verified): DON'T train a native DISTRIBUTIONAL encoder (= re-derives GloVe = the thin wall). Instead GROUND in the BINDER brain-based feature space (65 neural-correlated experiential attributes, human-rated 535/786 concepts = Barsalou operationalized, vetted external tool per PIVOT, NOT a borrowed distributional encoder). Supply grounded core + substrate EARNS extension by predicting a new concept's 65-dim grounded features FROM ITS RELATIONS (error-driven; NOT Feature2Vec-from-GloVe which re-injects distributional). Yardstick = beat GloVe 0.554 on v2 property-discrim AND generalize held-out (what distributional+binding both failed). [[project_ground_meaning_in_binder_brain_features_not_distributional_2026-07-25]].** Honest fallback if grounded-meaning ALSO walls = Lake-MLC training-regime-for-compositionality.**
- **REVISED NEXT (post-29561, superseded by v2 result above):** (i) FIX SCORER = randomize gold position + score abstention as chance (add to fairness-gate list); (ii) BRIDGE CE->learner = quantized/clustered CE vocabulary or graded-similarity feature, NOT raw dim-ids; (iii) run on a DENSITY-ADEQUATE clean slice (frac_ge2 high) from the densest vetted source (look-back a9a1b1bd choosing among full WorldTree 1GB / ConceptNet 492M [promiscuity-risk] / textbook_concepts_biology 58M / ARC_Corpus); (iv) clean metric, honest chance ~0.21, real bar frozen-GloVe 0.57; re-test whether relation-only rules (gam 0.35) genuinely beat chance, then whether CE-bridged composition adds. THEN mechanism gap (incremental error-driven encoder for Rogers-McClelland curriculum dynamics). (2) **THE BUILD = run `concept_encoder`'s native competitive-Hebbian error-driven differentiation loop on the REAL typed store (WorldTree/CLIMB structured knowledge) instead of the synthetic corpus**, coarse→fine, measured on the SAME held-out-to-new-concepts yardstick (beat GloVe ~0.10). Milestone = fine distinctions the substrate EARNED on real data that GENERALIZE, then wire that earned-meaning encoder into reasoner node-id/decision → reasoner-beats-baseline-at-scale. This REUSES the brain-native encoder work already done; no borrowed vector, no from-scratch.
- SUPERSEDED shortcuts (both DEAD): "wire Step5 learned-meaning into reasoner" (Step5 = memorization); "distill BGE + wire into reasoner" (borrowed-vector shortcut, retracted per USER). The reasoner (6201c53f6) stands as the brain-faithful chaining element; it's starved until meaning is EARNED the brain's way.

### 🎯 ALL-EFFORT REASONING ATTACK (USER 2026-07-25 "figure out the reasoning thing. all effort") + CAPABILITY MAP (decomposition a115ca61)
**CAPABILITY MAP (ARC-Challenge, measured):** ladder chance 0.25 -> single 0.34 -> sim-gate 0.37 -> learned-gate 0.41 -> ORACLE-given-gold 0.70-0.73. TWO GAPS: (A) RETRIEVAL 0.33->0.73 = 39.5pp (credit-assignment closed ~20%; miss-decomp 47% of Challenge misses are retrieval); (B) REASONING-GIVEN-GOLD 0.73->1.0 = ~27pp (even w/ gold, misses ~27%, dominated by surface-lure/fine-discrim WITHIN gold). COMMON ROOT = thin meaning caps BOTH at reasoner.py self.enc seam. RANKED LEVERS: #1 grounded/earned MEANING (common root, both gaps, HYPOTHESIS-PENDING/hard); #2 retrieval SELECTION learned-gate (HIGH tractability, needs multi-seed+control-arm); #3 precise typed-rule SUPPLY for derivation coverage (RED, text-promiscuous). WALLED (do NOT re-run): sparse-coding, taxonomy/native-distributional encoder, borrowed-pretrained-as-runtime, standalone CI, re-retrieval 2nd-pass, another cosine selection gate.
**THE REASONING ATTACK (verification-by-derivation = THE reasoning operation, master-map): the derivation reasoner is coverage-bound (RED typed_cov 0.07) AND -- KEY -- on the COVERED subset typed 0.229 ~= baseline 0.257 => more rules ALONE won't help; the meaning wall bites at NODE-IDENTITY (entity-linking over thin cosine). REFRAME: derivation reasons over SYMBOLS -> if BOTH (1) precise vetted rules [a77aab2d building] AND (2) precise SYMBOLIC entity-linking (exact/lemma match to rule-entities, NOT thin cosine) -> the derivation connects the RIGHT entities + reasons GENUINELY, SIDESTEPPING the meaning wall. PLAN: (1) rules [running] -> (2) wire w/ SYMBOLIC entity-linking (the fix the 0.229~=0.257 demands) -> (3) run derivation gate (RED->GREEN selective?) + demonstrate correct TRACES beating similarity. CAN-FAIL: rules+symbolic-linking STILL ~baseline => reasoning failure is deeper (isolated cleanly). This is the tractable route to DEMONSTRATED reasoning vs the hard grounded-meaning rebuild. Reuse: reasoner.py meet-in-middle, do-calculus PP-270/307 (unwired) for COUPLEDRELATIONSHIP, CI-settle.**
**✅ REASONING ATTACK RESULTS (all-effort, VET a3b2675f in flight):**
- **STEP 1 (rules, bf515fbc0): 233 science-precise SELECTIVE typed rules -> the reasoner EMITS REAL CORRECT GLASS-BOX derivation traces (weathering->erosion->sediment->sedimentary rejecting distractors; rotation-COUPLEDREL->daytime->'days shorter'; abstains uncovered), distractor-derive 0.063. => VERIFICATION-BY-DERIVATION WORKS; 'SHOW REASONING' DELIVERABLE = DEMONSTRATED (real selective inspectable chains, not similarity).**
- **STEP 2 (front-end upgrade, 064c1509c, full 1172 Challenge held-out): WordNet synonym-bridge raised derive-COVERAGE 0.109->0.189 SELECTIVELY (distractor 0.053->0.089 <0.13 guard, NOT vacuous); lemma~=glove (synonym-bridge is the lever). BUT derived-subset acc 0.243 = BEATS sim-baseline 0.199 yet ~CHANCE absolute -> NEW LIMITER = the DECISION: when gold AND a plausible distractor BOTH derive, the thin-meaning tie-break picks ~chance. Verdict MIDDLE.**
- **🧭 DEFINITIVE CONVERGENCE (the session's answer): the reasoning MECHANISM is SOLVED + DEMONSTRATED (glass-box selective derivations). It is MEANING-BOUND at EVERY stage -- front-end COVERAGE (question->rule-entity bridge) AND the DECISION (pick among derivable) -- which with RETRIEVAL + reasoning-OVER-GOLD = FOUR independent places the SAME root (grounded meaning) is the limiter. DELIVERABLE STATE: 'show reasoning' = YES (demonstrated); 'reason correctly AT SCALE' = NOT YET (meaning-bound). => THE one remaining lever to make reasoning FUNCTIONAL = GROUNDED MEANING (untested revival doors: grounded-relevant relations HasProperty/UsedFor, supply-Binder). Everything routes here. Tractable partial levers first (rule-precision so distractors don't co-derive; better tie-break) but the ceiling is grounded meaning.**
- **🏁 REASONING ATTACK TERMINUS (capstone, VET a65278716 in flight): SYMBOLIC TIE-BREAK on the 66 ties = d=+0.000 (ZERO discrimination; HONEST-NEG). Intent fires 13/66, gold-terminal-rel=asked-rel only 4/66 (rule-graph reaches gold via OFF-TYPE science paths), do-calc 3 ties, chain-quality tied -> picking which VALID derivation ANSWERS the question is a question-relevance MEANING judgment neither cosine NOR symbolic structure can make. gold_only 1.00 preserved. => SYMBOLIC PARTIALS EXHAUSTED. THE COMPLETE ANSWER TO 'FIGURE OUT REASONING': (1) MECHANISM SOLVED+DEMONSTRATED (verification-by-derivation; 100% on clean-derivable; real glass-box selective traces = 'show reasoning' MET); (2) GROUNDED MEANING is the SOLE remaining lever to make it FUNCTIONAL at scale -- BOTH failure modes meaning-bound: COVERAGE (51% gold-unreachable) + DECISION (36% tie, symbolic d=0). Symbolic structure (rules/do-calc/tie-break) CANNOT substitute -- MEASURED, not hoped. THE FORK for USER: commit to the grounded-meaning foundation (untested doors: grounded-relevant relations HasProperty/UsedFor + supply-Binder brain-features at entity/decision nodes) to make the working reasoner functional. This is the Barsalou-grounding the phase named, now justified by measurement as the sole path. FIXED: reasoner chance-calc mean(1/n) not 1/int(mean).**
- **🏁🏁 REASONING ARC FINAL (precision test ec2e1d4f2 HONEST-NEG, VET a7ec889e; corrects my 'SOLE' over-read via 29569->now EARNED): BOTH structural doors MEASURED SHUT. (1) symbolic tie-break d=0 (29569, genuine-tie decision meaning-bound); (2) rule/link PRECISION: of 66 ties only 1 SPURIOUS/prunable, 44 GENUINE co-derivations, tightening SHREDS gold-coverage 92->60 (no precision sweet-spot -- pruning distractor chains prunes GOLD chains equally). => grounded meaning is now the EARNED remaining lever (not premature). Reasoning MECHANISM = SOLVED+DEMONSTRATED (gold_only 26@1.00, real glass-box selective traces = 'show reasoning' MET).**
- **🧠 INPUT BRAIN-FIDELITY AUDIT (USER 07-25 'are all inputs brain-similar+consistent?' -- NO, verified vs code): MECHANISM inputs brain-faithful (verification-by-derivation=PFC backward-chain; PPR retrieval=spreading-activation; learned Go/NoGo gate=BG+dopamine-RPE; CI=Kintsch; do-calculus=Pearl; combiner=SPA). SUPPLIED-consistent: the 233 rules = brain-consistent CONTENT, LLM-sourced-not-learned (PIVOT foundation shortcut). NON-brain-faithful SHORTCUTS = the two that carry MEANING+COMPREHENSION, code-flagged INTERIM: (a) MEANING/encoder = SemanticHDEncoder GloVe+WordNet (BORROWED distributional = the forbidden encoder shortcut); (b) COMPREHENSION = `_content_words` crude stand-in (NOT hdlab/situation_reader). => we ran BRAIN-FAITHFUL reasoning machinery over NON-brain-faithful MEANING+COMPREHENSION inputs, and THOSE are the measured wall. THE GROUNDED-MEANING DECISION IS THEREFORE = REPLACE the 2 non-faithful inputs: GloVe->grounded/earned meaning (Barsalou; untested doors grounded-relevant relations HasProperty/UsedFor + supply-Binder 65 brain-features at entity/decision nodes) + `_content_words`->the real Kintsch reader (situation_reader, built-but-unwired). This is the earned, brain-consistent next commitment.**

### STANDING SETUP (load-bearing, always-on)
- **MODE:** full-auto self-drive (cron d7503a43, every 30min). USER authorized "make this happen and continue on."
- **STORE:** LOCAL-ONLY. atoms.jsonl + cert_ledger.jsonl at data/substrate_index/{math,meta}. NOTHING pushed; NO origin-push / NO remote-persist without in-session USER auth. Store WRITES = **binary/newline='' ONLY (Windows text-mode DOUBLES CRLF -> corrupted 8 atoms once) + git-commit atoms.jsonl+ledger after EVERY bank** ([[reference_store_write_crlf_binary_and_git_commit_after_every_bank_2026-07-22]]).
- **DISCIPLINE:** VET EVERY load-bearing verdict via hdi_skunkworks BEFORE treating as fact (Director over-read ~a dozen times this session -- the adversarial VET is the guardrail; report CLAIM/VET-pending). Disk-verify agent numbers + agent PRIOR-ART claims ([[feedback_disk_verify_agent_prior_art_claims_before_propagating_not_just_experiment_verdicts_2026-07-22]]). ANTI-LOOP DEDUP (grep experiments/ + KB-check before dispatch). Brain-first per component. WEB LIVE via MY WebFetch/WebSearch (headless subagents CANNOT web-auth -> I fetch+hand-down). Models: research/drills=sonnet, exp_dev/skunkworks/orchestrator=opus.
- **🧷 SYSTEM WIRED-STATE (AUTHORITATIVE — USER-requested 2026-07-25; READ THIS before ever claiming anything is/is-not "wired". The docs previously CONFLATED three DISTINCT states -> Director twice wrongly said "nothing is wired". They are SEPARATE:**
  - **(1) RUNS END-TO-END = YES.** "THE CLIMB" pipeline EXISTS and RAN (2026-07-24): SemanticHDEncoder -> hdlab/hd_fact_store.py -> ingest ~100k VETTED triples (ConceptNet + WordNet + ARC_Corpus) -> answer ARC by FACT-RETRIEVAL. Cells: `exp_arc_knowledge_scale_ingest_climb_v1` (CLIMB_PASS: ARC-Easy 0.254->0.373 = +0.119, survives scramble; Challenge 0.247->0.246 FLAT) + `exp_arc_fact_retrieval_semantic_kb_climb_v1` (KB_BELOW_FLOOR: Easy 0.260 < semantic-IR floor 0.302). Design note: notes/the_climb_wire_meaning_ingest_vetted_triples_arc_retrieval_2026-07-24.md. => a wired, meaning-matched, knowledge-ingesting ARC-answering system DOES run.
  - **(2) DOES IT REASON? = NOW YES ON A COVERED SUBSET (as of 2026-07-25), not yet at scale.** The CLIMB retrieval pipeline does NOT reason (similarity; Challenge-flat). But the NEW composed reasoner (hdlab/reasoner.py, built 2026-07-25) EMITS real inspectable DERIVATION TRACES on the ~14% it covers -- incl a CORRECT force->acceleration derivation ([force]-REQUIRES->[energy]-USEDFOR->[object]-COUPLED->[increase]; do(object+)->increase; audit tag; why-not the other 3) = the show-reasoning deliverable no similarity score can produce. HONEST: COVERAGE-BOUND (cov 0.140; covered-subset typed 0.229 ~= similarity baseline 0.257 ~= must-fail controls; BELOW_BANDS 0/4) -- it REASONS on covered Qs but doesn't yet beat retrieval at scale. The ARC arc (29537-29557) diagnosed WHY: retrieval/connectivity != entailment over thin meaning.
  - **(3) PROMOTED into a composed hdlab core = NOW YES (2026-07-25, committed 6201c53f6).** hdlab/reasoner.py (composed entry) + hdlab/typed_rule_parser.py (P1 promoted, 1868 licensed rows) NOW EXIST. Pipeline: comprehend -> typed graph + NegAware node-id -> meet-in-middle derivation SEARCH (P3) -> CI consistency (P4) -> do-calculus routing (certified, byte-faithful) -> decision -> trace. P3/P4 reused-by-import (hdlab promotion noted); comprehension = interim _content_words stand-in (flagged, Stage-2/P6 to adapt situation_reader). Before today these were exp-cells (the audit's "wiring debt"); the reasoner build promoted them. THREE STATES STILL DISTINCT: CLIMB-RUNS-end-to-end (yes) / composed-reasoner-BUILT (now yes) / REASONS-AT-SCALE-beats-baseline (NOT yet, coverage-bound).
  - **THE COMPOSED REASONER = BUILT + WIRED + emits traces. What it needs to beat baseline AT SCALE = the two levers, both VET-pending: (a) COVERAGE = rule-supply (Step 1 science-extract = PROMISCUOUS: cov 0.07->0.50 but selectivity negative -- same wall); (b) SELECTIVITY = fine-meaning (Step 5 learned-linear front-end = POSITIVE: moves the real in-vocab wall +0.15, CI-separated, not memorization). skunkworks VET in flight (abda1634 = Step5+Step1); reasoner-VET queued.**
  - **ANTI-SILOING PROCESS (built this session to prevent recurrence): tools/integration_health.py (tripwire flags exp-cell islands + composed-entry-present) + notes/promotion_backlog.md + promote-at-VET lifecycle. RULE: run integration_health / check the wired state BEFORE asserting anything is/is-not wired.** [[feedback_promotion_lifecycle_process_wire_successes_2026-07-25]], [[feedback_runs_vs_promoted_vs_reasons_distinct_states_check_wired_state_2026-07-25]].**
- **🎯 CURRENT FRONTIER (2026-07-25 ~13:47Z, LATEST — supersedes the '05:22Z tail 29546' block below = valid detailed HISTORY). PHASE = duplicate brain REASONING 100%, NO shortcuts, until DEMONSTRABLE reasoning [[project_full_commitment_duplicate_brain_reasoning_100pct_no_shortcuts_2026-07-25]]. Blueprint = notes/research_master_reasoning_stack_map_and_build_plan_2026-07-25.md (7/8 reasoning stages EXIST; debt = wiring + thin-meaning shortcut; Stage-8 credit-assignment = the one structural gap, done by Director by hand).**
  - **MILESTONE: the COMPOSED REASONER `hdlab/reasoner.py` is BUILT + WIRED + committed (6201c53f6)** (P1 promoted parse_tablestore_typed -> hdlab/typed_rule_parser.py). Pipeline: comprehend(_content_words interim) -> typed graph + NegAware node-id -> meet-in-middle derivation SEARCH -> CI consistency -> do-calculus routing (certified) -> decision -> INSPECTABLE TRACE. EMITS real derivation traces (a CORRECT force->acceleration chain w/ do-calculus audit + why-not-others) = the show-reasoning deliverable. HONEST: BELOW_BANDS (cov 0.140; covered-subset typed 0.229 ~= similarity baseline 0.257 ~= must-fail controls) = COVERAGE-BOUND -> reasons on covered Qs, does NOT yet beat retrieval AT SCALE.
  - **WHAT LIMITS IT = 2 levers + 1 structural gap: (a) COVERAGE -- precise typed rules too sparse (WorldTree 1868 covers ~14%); Step-1 science-extraction raised cov 0.07->0.50 but PROMISCUOUS (noisy, no selectivity). (b) SELECTIVITY/FINE-MEANING -- node-identity + discrimination run over thin GloVe that conflates entailing-vs-similar; Step-5 learned-LINEAR front-end = POSITIVE (moves real in-vocab wall +0.15, CI-separated, not memorization). ROOT of both = thin distributional meaning (the arc-proven wall). (c) STRUCTURAL: no credit-assignment/learning loop (Stage 8, Director-by-hand); comprehension = crude _content_words stand-in (not situation_reader).**
  - **NEXT: wire the POSITIVE learned-meaning lever into the reasoner's node-identity + decision (turn +0.15 into reasoner selectivity) + get PRECISE (non-promiscuous) rule coverage -> reason AT SCALE past baseline; then adapt situation_reader for comprehension + build the credit-assignment loop. Store tail ~29557 (VET banking 29558+ for Step5/Step1). VETs in flight: skunkworks abda1634 (Step5 positive hardest + Step1 promiscuous, banking 29558/29559); reasoner-VET queued.**
  - **📋 SHORT/MEDIUM-TERM PLAN (2026-07-25, USER-reviewed -- SURVIVES COMPACTION). SHORT (days -- make the reasoner BEAT retrieval): (1) finish VETs in flight (Step5 positive VET'd HARDEST -- gates everything below). (2) [HIGHEST LEVERAGE] WIRE the learned-meaning front-end (Step5 +0.15 positive) INTO hdlab/reasoner.py in TWO places: node-identity (connect ENTAILING concepts, not promiscuous) + decision/selection (pick the entailing chain) -- turns the isolated positive into reasoner lift; contingent on Step5 VET confirming. (3) PRECISION-FIRST rule-extraction v2 (drop promiscuous IFTHEN-0.13 patterns, fix arg-windowing, confidence-weight) -> PRECISE coverage; re-run the connectivity gate. (4) MILESTONE = reasoner beats the ~0.36 similarity baseline on a broader covered slice (esp surface-trap chal_lure). (5) adapt hdlab/situation_reader.py for question->claim comprehension (replace crude _content_words stand-in). MEDIUM (weeks -- reasoning at scale + the no-shortcut foundation): (6) GROUNDED/LEARNED MEANING foundation = curriculum-ingestion + error-driven differentiation loop that LEARNS grounded meaning for ARC concepts (learned front-end works IN-VOCAB but doesn't generalize to HELD-OUT -> ingest per-concept), AIMED by the reasoner's residual node-merge confusions; the deep no-shortcut fix (USER mandate). (7) CREDIT-ASSIGNMENT loop (Stage 8 = the ONE structural gap): wire the certified RPE/Go-NoGo actor (exp_pfc_gate_cfrpe_trained) to REAL ARC correctness -> self-improving (retires Director-by-hand credit-assignment). (8) SCALE to the SUCCESS BAR = inspectable derivation traces that beat retrieval on ARC-Challenge at meaningful coverage -> NORTH STAR (glass-box substrate you can CONVERSE with). HONEST joint odds ~0.15 for the full bar; the medium-term meaning-foundation is the real weight. IMMEDIATE NEXT ACTION (on Step5-VET-confirm) = SHORT step (2), wire meaning into the reasoner.**
  - **🔁 COMPACTION-RECOVERY CHECK (post-compaction, do FIRST): (1) heartbeat; (2) read THIS FRONTIER block -- it is self-contained (wired-state + milestone + limiters + plan); (3) `git -C d:/AI/hd-instrument log --oneline -8` + check atoms.jsonl tail: if tail == 29557 -> the Step5(realslice)+Step1(science-extract) VET (skunkworks abda1634) did NOT bank before compaction -> RE-DISPATCH that landed-VET (cells committed 33b86bfc2 / 182b82a5f, on disk); if 29559 -> it landed, fold it; (4) reasoner-VET still QUEUED (hdlab/reasoner.py committed 6201c53f6, verdict BELOW_BANDS / coverage-bound; VET the coverage-bound negative + trace correctness); (5) IMMEDIATE work = SHORT-PLAN step (2) wire Step5 learned-meaning into hdlab/reasoner.py node-identity + decision (on Step5-VET-confirm). ALL LOCAL -- nothing pushed (awaiting USER auth). Running builds do NOT survive compaction -> re-dispatch any not-yet-banked.**
- **🔑 STORE-STATE HISTORY (2026-07-25 ~05:22Z, tail 29546 -- SUPERSEDED by the CURRENT FRONTIER above; valid DETAILED HISTORY of the content-enrichment -> reasoning-pivot arc): atoms.jsonl tail = 29546, LOCAL-ONLY (29510-29546 NOT pushed; last push 485d126d7 @29509). CURRENT FRONTIER = CONTENT-THIN MEANING IS THE VET-CONFIRMED BINDING WALL; the DIRECT lever = richer/grounded CONTENT in the concept rep. The whole ARC arc (29537-29546) converged BY ELIMINATION: combining WORKS given gold (Challenge 0.72 oracle); retrieval REACHES the facts (recall@100 0.69, structure-driven); but SELECTION can't ISOLATE gold from a contains-gold pool, and the STRUCTURE + SCORING axes over thin meaning are now ALL CLOSED -- 29544 fixed brain-faithful signals HARD_FAIL, 29545 LEARNED relevance HARD_FAIL, 29546 RELATIONAL-meaning HARD_FAIL (sel_gold_precision ~0.11-0.19 vs oracle 0.97). GLASSBOX ROOT CAUSE (29546 dam/hydro Q): selection picks nuclear/coal/gas power-plant facts (structurally identical) + misses hydro gold because GloVe/WordNet put hydro~nuclear~coal all close ('power/energy') -- the rep can't represent SPECIFIC content. USER INSTINCT CONFIRMED 3x: the FOUNDATION's MEANING is the wall (least-brain-like piece; 'make everything brain-like' + 'fix the wall' = SAME task). **CONTENT-ENRICHMENT v1 LANDED = HARD_FAIL, but VET-ADJUDICATED CONFOUNDED (door NOT closed; NO atom banked).** v1 (exp_concept_featural_enrichment_v1): B_enriched sel-precision 0.1168 ~= RANDOM 0.1066, both BELOW thin 0.1865; coverage passed, dam planted mechanism fired +0.384 but that only proves bind/bundle MATH (construction-determined toy). SKUNKWORKS VET (a9a5546, independent recompute) CORRECTED both the cell's + my read: concept->property assignment at HEAD-WORD level is CLEAN (hydro->water/hydropower/renewable, nuclear->fission/waste all correct); the confound is in the AGGREGATION layer (build_property_index ~259-284): whole-arg1-clause fillers summed over ALL content words -> generic HUB words dominate (something=270 fillers, water=188, object=150, energy=102, usually=93 near-stopword; 41.3% fillers df=1 so 'distinctive=rarest-df' degenerates). prop_overlap saturates 1.6473 and FAVORED the lure in the renewable-resource Q; enr_cos NEGATIVE everywhere (property mass drowns thin cosine). B~=RANDOM = smoking gun that clean content was never presented in isolation. => clean distinctive-content enrichment is UNTESTED. **FAIR RE-TEST v2 LANDED (exp_concept_featural_enrichment_v2, committed a55bcfc40) = CLEAN, DECONFOUNDED CONTENT_ENRICHMENT_HARD_FAIL.** The 4 fixes TOOK (MEASURED@metrics fix_took: enr_cos_gold_mean +0.7423 POSITIVE where v1 was negative, pos_frac 1.0, learned enr_cos weights now positive -> clean content genuinely presented in isolation). Controls anchor EXACT (A_thin 0.1865, ORACLE 0.9754/0.7119 byte-identical, RANDOM collapses 0.1332). Yet clean content STILL fails: B_enriched 0.1148 <= RANDOM 0.1332 << thin 0.1865; C_combined 0.1773 < thin; end-to-end lift +0.0000, McNemar p=0.838. Honest caveat: clean-vs-random separation lives only in symbolic acr_distinct, not cosine (enr_cos_random_gold 0.7362 ~= clean 0.7423). **SHARPENED DIAGNOSIS (the real finding, VET-pending): the wall is NOT just thin content -- it is that our selection is ANSWER-AGNOSTIC + LINEAR. Enrichment raises fact-vs-choice TOPIC-cosine, but ARC-Challenge lures are near-equally on-topic for the WRONG choice, so answer-agnostic content raises topic-match SYMMETRICALLY across the 4 choices and CANNOT break the gold-vs-lure symmetry via a linear read-out (B can fall BELOW random by tipping toward content-rich lures). Our pipeline selects K facts relevant to the QUESTION, then a combiner scores choices -- selection never conditions on WHICH choice it supports. => REDIRECT = ANSWER-CONDITIONED selection (score facts per-choice by how they bridge question->that-choice; pick the DISCRIMINATING fact), the brain's + standard-QA's way; a pipeline SHAPE change, not another meaning-rep tweak.** **BRAIN-DRILL LANDED (a3c4e650, notes/research_drill_answer_conditioned_selection_biology_2026-07-25.md, KB-confirmed NOVEL): 5 independent literatures (PFC goal-biased competitive retrieval; differential-diagnosis illness-scripts; multi-alternative drift-diffusion/LCA; likelihood-ratio not raw likelihood; Thagard ECHO inhibitory-contrast) all say human MC-reasoning is ANSWER-CONDITIONED, never answer-agnostic: bind Q to EACH candidate, score by CONTRASTIVE differential support competitively normalized across choices. NAMED GAP = SHAPE (per-choice conditioned selection not question-only top-K), PLACE (condition at SELECTION time), METRIC (differential support not raw relevance). SUBSTRATE BONUS (our inference, unconfirmed -> the build TESTS it): VSA bind is inherently conjunctive/nonlinear, so answer-conditioning + nonlinear read-out COLLAPSE into ONE build (Q(x)C_i gives the nonlinearity free). RANKED: build answer-conditioned contrastive selection FIRST; nonlinear read-out subsumed; HOLD perceptual grounding as the deeper lever only if the conditioned selector's RESIDUAL failures prove genuine content-resolution gaps. **ANSWER-CONDITIONED SELECTION BUILD LANDED (exp_arc_selection_answer_conditioned_contrastive_v1, committed e6f715800) = COND_SELECTION_HARD_FAIL.** Answer-conditioned contrastive selection (Q(x)C_i per choice, differential support) LOSES to answer-agnostic (B_CONDITIONED 0.284 vs A_AGNOSTIC 0.3786, primary_lift -0.0946, McNemar p=0.011 WRONG-direction); anchor reproduced, controls valid (exp_dev fixed a vacuous MISCOND control mid-run), contrast_beats_noncontrast +0.041 but whole conditioned branch far below agnostic. MONEY METRIC: gold_in_pool 0.9095 (retrieval reaches gold) but gold_points_correct 0.2443 (~=chance) -- a SINGLE gold fact's conditioned differential points at the correct choice only at chance. **INTERPRETATION FORK RESOLVED by VET (skunkworks abe30c2a, atom 29548, committed local ebc9279a8): DIRECTOR's read CONFIRMED, exp_dev's thin-meaning->perceptual-grounding read REFUTED. (1) gold_points_correct=0.24 is CONFOUNDED -- computed with the SAME near-noise conditioned readout under test (contrast spread 0.0154 ~= 1/sqrt(2048)), NOT an intrinsic meaning probe. (2) ORACLE 0.687 is the CLEAN probe = answer-AGNOSTIC bundle combiner over the thin-meaning GOLD SET -> thin meaning DOES carry the answer signal, as a SET-LEVEL / bundle property (no single fact carries it). (3) Trim to Director's read: disk is SILENT on chain/multi-hop; the working readout is a FLAT BUNDLE superposition -> framing = 'SET-HOLISTIC readout', not 'chain'. **=> RESIDUAL WALL = SELECTION (isolate the gold SET whose value is HOLISTIC/emerges only as a set); a working SET-LEVEL answer-readout ALREADY EXISTS (the bundle combiner, 0.687 on gold). Perceptual-grounding rebuild NOT licensed (must be justified on SELECTION directly if ever). The drill's 'conjunction-for-free' sign-bind = a fixable near-noise readout artifact on GloVe-mean->sign, not a rep wall.** NEXT LEVER = SET-LEVEL / HOLISTIC SELECTION: find the SUBSET of the pool whose bundle discriminates a choice (first approach that does NOT assume per-fact scores -- why the prior 5 failed). SET-LEVEL BUILD LANDED (exp_arc_selection_set_level_holistic_v1, committed f39f53fe4) = SET_LEVEL_HARD_FAIL -- the 7th consecutive selection HARD_FAIL. B_SETLEVEL 0.2922 < A_TOPK 0.3621 (set_lift -0.0699), RND collapses, anchors exact, ORACLE 0.6708. DECISIVE MECHANISM: margin_informative=FALSE (winning bundle-margin on CORRECT 0.101 < on WRONG 0.105), setmargin_points_correct 0.29 ~= chance despite gold_in_pool 0.91 -- from the wide 100-fact pool EVERY choice can greedily assemble an equally self-supporting set; per-choice competition can't favor the correct answer. Design-drill (research a4be5070, notes/research_holistic_set_selection_2026-07-25.md) had CONFIRMED the design (Georgopoulos pop-vector=bundle combiner + Kintsch-CI + Usher-McClelland competitive margin) so the fail is not a botched design. **🔻 MAJOR PIVOT (USER-DIRECTED 2026-07-25 ~07:35Z): STOP MATCHING, START REASONING. USER (decisive, twice): "of COURSE we need to reason - that's the whole goal here." VET CONFIRMED the root cause (skunkworks a65085e7, atom 29549 committed 2df74d1ee): root cause = (B) SIMILARITY != ENTAILMENT at confidence ~0.72 -- the ENTIRE retrieve->select->combine pipeline is similarity all the way down (fetch=similar-to-Q, pick=similar/relevant, combine=bundle-similar-to-choice); it NEVER reasons. Decisive evidence: winning fact-sets were 0/12 built from any gold fact; correctness DECOUPLED from gold-reach (more gold facts present -> NO help, sometimes worse); the topical decoys are REAL relevant facts so relevance-tightening can't remove them. => matching cannot become reasoning by tuning. THE FIX = REASON OVER THE ANSWER: treat each candidate as a CLAIM to DERIVE; chain the TYPED RULE-FACTS (WorldTree IFTHEN/CAUSE/REQUIRES = literal inference rules, currently mis-used as similarity text) as inference steps from the question's givens to the candidate; pick the candidate that actually FOLLOWS. Surface-lure (dam-Q->'solar') is topically similar but NOT derivable -> reasoning rejects it. WE ALREADY HAVE THE PIECES (typed rule-facts; the READER = comprehension->structured claim, built but UNWIRED from ARC; the spreading-activation graph -> becomes an INFERENCE graph; Kintsch-CI/ECHO/conflict-monitor for chain-consistency). This is the NORTH STAR (comprehension+reasoning one substrate) finally wired to ARC. **REASONING DESIGN-DRILL LANDED (research a9997d3a, notes/research_verification_by_derivation_reasoning_pivot_2026-07-25.md, disk-verified, 23 cites): design = VERIFICATION-BY-DERIVATION. Brain verifies a candidate by BACKWARD-chaining-from-answer meeting FORWARD-chaining-from-givens over TYPED/DIRECTIONAL rules (PFC/Tanji-Hoshi + LISA + Gentner systematicity + ACT-R serial gating explain why 'not everything associates': edges typed+directional, rules serial+gated, connectedness=evidence). ONE new mechanism = typed-graph derivation SEARCH (not scoring) -- reuses parse_tablestore_typed() + the PROVEN M3 bidirectional meet-in-middle search (0.62 chain-grade) + CI-settle as consistency check UNCHANGED. Killer must-fail = UNTYPED_SIMILARITY_NULL (swap typed edges for similarity edges -> if lift survives, it was never entailment). HONEST DOMINANT RISK = rule COVERAGE (~1700-2000 causal/conditional WorldTree rows for ~2200 Qs); node-unify inherits the thin-meaning wall (may spuriously merge). P_deflated joint 0.13-0.15. DISK CORRECTION: hdlab/situation_reader.py EXISTS (real narrative-passage situation-model reader; drill's 'not found' was WRONG per my grep) -- but it's PASSAGE-comprehension, needs adaptation for question->claim; the cheap gate does NOT need it (uses _content_words crude extraction). **IN FLIGHT: (a) CHEAP CONNECTIVITY/COVERAGE GATE (exp_dev af5a3045) -- implements the drill's Section 4: build typed graph, depth<=3 fwd/bwd meet-test for all 4 choices on ~80-100 Qs, measure correct-choice coverage + selectivity-gap (correct vs lure) + UNTYPED-null comparison; GREEN(coverage>=0.35 & sel-gap>=0.15 & typed>untyped)->build full reasoner; RED(coverage<0.15 or sel-gap<0.05 both)->rule COVERAGE is the bottleneck, SUPPLY more rules; YELLOW->scoped subset. NO scoring, cheap, decisive. C-falsification banked (atom 29550, committed 2728b1a7f, stale content-rebuild gloss corrected in-atom). **🔌 INTEGRATION AUDIT LANDED (USER-requested, hdi_testbed ab1693ea -> notes/integration_audit_hdlab_wired_vs_islands_2026-07-25.md): CORRECTS the 13% headline. hdlab internally HEALTHY (7/93 dead, B-); the REAL debt = the ARC/reasoning FRONTIER bypasses hdlab (a tower of exp_ cells importing exp_ cells, ~3 hdlab touches) + NO composed substrate entry (4 disjoint roots, none is ARC). Reasoning pieces exp-TRAPPED: parse_tablestore_typed, M3 meet-in-middle 0.62 multihop (weaker K=2 sits unused in hdlab/multi_hop.py), CI/polarity, SemanticHDEncoder; hd_fact_store + char_trigram are the only hdlab pieces reaching ARC; situation_reader well-built with 0 ARC consumers. WIRING PLAN P1 promote typed-parser->hdlab; P2 hdlab.arc_pipeline API; P3 promote M3 multihop; P4 wire CI; P5 = CREATE THE COMPOSED ENTRY hdlab/reasoner.py (reader->typed_rule_parser->fact_store retrieval->multihop derivation->CI consistency->selection); P6 decide reader role; kill 7 dead. **CONVERGENCE: the verification-by-derivation reasoner build IS P5 -- building it promotes P1/P3/P4 as its parts. => build the reasoner AS the integration (not an 8th island), GATED by the connectivity/coverage verdict (af5a3045): GREEN/YELLOW->build+wire; RED->supply rules first. Do NOT big-bang refactor; promote only what the reasoner needs.** **⚙️ ANTI-RECURRENCE PROCESS INSTALLED (USER 07-25): tools/integration_health.py (detector: exp-modules-imported->=3x, bypass count, dead hdlab, composed-entry present; baseline 237/4133/7/ABSENT) + notes/promotion_backlog.md (queue, testbed-owned) + lifecycle PROMOTE-decision at VET (skunkworks emits island-ok vs PROMOTE) + critical-path DoD=WIRED; cadence home = meta_audit (cron is session-only). [[feedback_promotion_lifecycle_process_wire_successes_2026-07-25]].** **🧩 REASONING PLAN STATE (07-25 ~08:40Z, tail 29551): CONNECTIVITY GATE = RED (committed d0febe681), VET-adjudicated (atom 29551, committed dd5e0c7a0): PRIMARILY COVERAGE-BOUND ~0.8 -- WorldTree's ~1868 licensed rows / 1293 edges structurally cannot span ARC content; the 0.070 coverage is a SPURIOUS mega-hub artifact ([food] deg 498 routes all chains); deeper hops REFUTED (flat depth-sweep, dropped). Typed signal faintly real (~4.3x untyped null) but mega-hub-confounded -> revive under clean nodes, don't bank. NEGATION-COLLAPSE = FATAL coverage-independent blocker (encoder drops "not" -> "not exist"~"exist" discards entailment structure); fix = two-layer (encoder keep negation tokens + polarity-gated merges reusing PolarityLexicon). **IN FLIGHT: clean negation-aware node-identity gate RE-RUN (exp_dev a25169d4) -- ONE variable = node-identity (phrase-head normalize + kill mega-hub + preserve negation + polarity-gate merges), same rules/Qs/typed-vs-untyped; DECISIVE: coverage stays ~0 => COVERAGE-BOUND confirmed => RULE-SUPPLY is the next lever (expand licensed corpus beyond WorldTree); coverage rises => node-identity-bound (unlikely). Also builds the negation-aware node-identity prerequisite + re-measures if the typed signal survives clean nodes.** PLAN: (1) [DONE] clean-node ablation = COVERAGE_BOUND_CONFIRMED (cell 99736f579): clean negation-aware node-identity (mega-hub 498->83, negation-merges 1979->0) did NOT rescue coverage (0.060) -> ~1868 WorldTree rules don't span ARC -> RULE-SUPPLY mandated. Secondary: typed-vs-null signal VANISHED under clean nodes BUT underpowered at 6% coverage (7 hits/100) -> DEFER the typed-derivation verdict to post-rule-supply, do NOT over-read as 'reasoning dead'. Byproduct: NegAwareEncoder negation-aware node-identity primitive (sign-flip binding cos(not X,not Y)=cos(X,Y), cos(exist,not exist)~0) = prerequisite, promote-candidate. (2) [DONE] clean-node ablation VET'd (atom 29552); NegAware = keeper on backlog P1b (neg-detector fixed). (3) [DONE, VET-CORRECTED atom 29553 bce30338f] RULE-SOURCE PROBE (exp_arc_derivation_rule_supply_cskg_v1, ba2d1b174) = MIDDLE_BAND / NO-DEMONSTRATED-SELECTIVITY (Director+cell over-read it as PROMISCUOUS-FAIL; VET corrected). ROBUST: coverage lever REAL + FAIR (CSKG 187620 rows lift 0.07->0.56, HIGH conf). CONFOUNDED: the "no selectivity" (correct 0.56 ~= wrong 0.51, typed_gap 0.0516 within noise z=0.89 p=0.37) is PARTLY MANUFACTURED by the induction -- induce_subgraph degree-ranks candidates (SELECTS the deg-571 person hub) + the max_deg_induce=64 hub-guard is DEAD CODE -> mega-hub flattens selectivity mechanically. So CSKG promiscuity is NOT cleanly proven intrinsic. ROBUST (induction-independent): VACUOUS chains (conductor->table-salt via water->cooking->eat->table) = the real support for the science-precise redirect. (4) [DONE] SALVAGE probe (exp_arc_derivation_rule_supply_cskg_depromisc_v1, committed 6d9b71d7b) = NEED_EXTRACTION, DE-CONFOUNDED: agent fixed the dead-code hub-guard (my alert) -> debiased hub-capped induction (max_deg 571->2-4, pos control reproduces parent exactly). Every hub-removing arm COLLAPSES coverage to ~0; no cap gives BOTH cov>=0.30 AND sel-gap>=0.15 (monotone tradeoff). VET-CORRECTED (atom 29554, committed 69e1806b4): the ROBUST claim is the DICHOTOMY -- hubs PRESENT (uncapped anchor keeps water/energy) -> NON-discriminative (gap 0.0516, vacuous bridges); hubs REMOVED -> coverage collapses; EITHER branch fails => off-the-shelf commonsense KBs = WRONG supply (SETTLED across 2 probes, HIGH conf). The SHARPER "CSKG has NO science-causal core / science nodes not causally connected" phrasing OVERREACHES (MEDIUM conf): 2 arms budget-starved (0 frontier, uninformative) + no cap tested in the (16,571) window that would retain a legit science-hub -- revival criterion #1 to earn it. Direction unchanged: need SCIENCE-PRECISE extraction. (5) [IN FLIGHT] VET the NEED_EXTRACTION (skunkworks ab1ea90a, atomize) + DESIGN DRILL (research a56999bb) grounding the STRATEGIC FORK: (A) EXTRACT/curate a big science-precise rule-DB + keep rule-chaining, vs (B) MODEL-BASED reasoning (comprehend via situation_reader -> small situation model -> SIMULATE which answer holds; Johnson-Laird/Barsalou; LESS rule-DB-dependent, sidesteps coverage wall, more brain-faithful), vs (C) HYBRID (comprehend -> few facts -> local model -> simulate). Drill: PART A rank science-precise rule sources (GenericsKB science / WorldTree gold-explanation-chain mining / science-text extraction / LLM-built+KB-vetted) + cheapest to probe; PART B biology verdict on chaining-vs-model-based + recommended next build + cheap can-fail gate. On drill land: likely SURFACE the fork to USER (prose rec) if it's a big pivot, else drive. (6) [DONE] DRILL resolved fork to cheap HYBRID (schema-routing via existing do-op primitive). (7) [DONE] SCHEMA-ROUTING GATE (exp_arc_schema_routing_do_calculus_v1, committed 0b1267582) = HARD_FAIL_ROUTING_NO_LIFT: P1 structure-axis HARD_PASS (schemas match 25-30% >> 6% edge-coverage -- STRUCTURE axis SOLVABLE) but P2 HARD_FAIL (routing HURTS matched-subset -4pp, -20pp on resolved routes; random-schema must-fail confirms right-schema also no help) => the ENTITY-SELECTION / fine-meaning axis is the wall. do-op primitive VALID, not the ARC lever. **🎯 CONVERGENCE (VET-CONFIRMED ~0.75, atom 29555 composes 29544-54, committed 517b43730): ~10 experiments across MATCHING (7 selection fails) AND REASONING (chaining coverage-bound; schema-routing entity-wall) ALL bottom out at the SAME wall. CALIBRATED LABEL (VET): "FINE-GRAINED MEANING/RELEVANCE DISCRIMINATION over thin meaning" -- subsumes entity-binding + schema-discrimination + the selection-arc's selection-by-ENTAILMENT; 'entity-level' slightly NARROWS what 29544-50 measured (entailment-relation, not pure entity identity). do-op primitive EXONERATED. The wall = the substrate can't discriminate the correct fine content from a lexically-similar-but-WRONG alternative over thin GloVe/WordNet. Oracle (given the right facts) = 0.69 -> the COMBINER/REASONING already works; the wall is purely finding/binding the right entity-meaning. USER instinct ('the foundation's MEANING is the wall') now PROVEN from every angle. => STRATEGIC FORK SURFACED TO USER: stop patching thin GloVe/WordNet; INVEST in building grounded/learned ENTITY-MEANING = the project MACRO-GOAL (ingest curriculum -> LEARN content -> grade-scale; meaning=ASSIGNMENT not generalization). ARC arc did its job as a yardstick: precisely diagnosed the foundational need. USER steer on the LARGE meaning-foundation investment still open (surfaced with my lean=learned representations). **SELF-DRIVE TICK 10:44Z: reconciled the 07-18 reader/chain-grade cron with the ARC convergence -- BOTH point at a LEARNED MEANING FRONT-END (NVSA: learned encoder + fixed VSA + glass-box, discriminates fine content). DRIVING the CHEAP first component per full-auto (not the large investment I flagged for steer): brain-drill (research af3956ae) on how the brain LEARNS fine-content discrimination (Rogers-McClelland PDP semantic differentiation / ATL hub / the LEARNING CURVE) -> then a can-fail cell that LEARNS to discriminate content thin GloVe/WordNet conflates, MEASURING THE LEARNING CURVE (flexible/improving) + compositional-generalization probe (the genuine edge). Component-by-component toward chain-grade; brain-drill-per-component.** **[COMPONENT-1 LANDED] learned-differentiation cell (exp_learned_meaning_frontend_differentiation_v1, committed 6aeb4c321) = MIDDLE. WIN: a learned glass-box hub over FROZEN meaning ACQUIRES fine in-vocab discrimination GloVe conflates -- learning curve 0.14->0.75 (iv_lift +0.34), SIGMOIDAL (AIC -23.6, midpoint ~57), COARSE-BEFORE-FINE 4/4 (Rogers-McClelland-faithful), shuffled must-fail holds (sep 0.475), difficulty-on (frozen-fine 0.373). => the flexible/IMPROVING property is DEMONSTRATED for TRAINED content. LIMIT: held-out COMPOSITIONAL probe decays to frozen (ho_lift 0.0, transient peak @exp4 = memorize-then-overfit) -> a FLAT hub does NOT systematically generalize. exp_dev DEVIATED (gated HARD_PASS on held-out -> MIDDLE). VET IN FLIGHT (skunkworks ae104298) adjudicating: (2) in-vocab REAL-differentiation vs memorization + what it buys; (3) held-out STRUCTURAL (need NATIVE BINDING = the roadmap's genuine edge) vs FIXABLE (regularization/early-stop, the @exp4 peak); (4) the DEVIATION sound vs over-downgrade + correct band. COMPONENT-1 VET'd (skunkworks ae104298, atom 29556 proven-bound, committed 296030c0d): MIDDLE CONFIRMED + calibrated. (2) in-vocab = REAL but SCOPED -- the WIN is the meaning axis is LEARNABLE (frozen GloVe carries recoverable item-signal cosine can't use) + first flexible/improving curve on meaning, for TRAINED content; endpoint is memorization-inclusive (do NOT over-claim). (3) held-out failure = STRUCTURAL high-conf (the 'peak' was init-noise at chance @exp2, then monotone decay -> flat associative hub CANNOT systematically bind item x relation; NOT regularization-fixable) -> NEXT = NATIVE BINDING. CORRECTION for next cell: hold out ONLY CATEGORY-CORRELATED relations (distinctive ones are unlearnable-by-design -> current gate over-attributes). (4) the DEVIATION directionally SOUND, MIDDLE = right band (proven-bound), do NOT read as component-1 failed. **NEXT COMPONENT (drill running, research a2eb35d2): NATIVE VSA BINDING for COMPOSITIONAL GENERALIZATION = bind item (x) relation via the fixed VSA algebra + learned read-out (NVSA pattern), test HELD-OUT category-correlated combos (isolate binding); baseline = flat MLP hub (ho_lift 0.0, the thing to beat); HARD-PASS = binding generalizes where flat memorizes. **[COMPONENT-2 LANDED] native_binding_compositional_generalization_v1 (committed 58c317af2) = MIDDLE, reframes the component. FINDINGS (VET-pending, skunkworks aac5e8d4): (1) bind_over_concat=0.0 -- native binding ~= concat+linear on SINGLE-HOP property completion (both linear-in-item), binding NOT the lever HERE; its edge = multi-role/deep composition (untested here, NOT refuted; HARD_PASS band bind>concat+0.10 near-structurally unreachable single-hop). (2) KEY POSITIVE: flat-MLP fails (ho 0.556=frozen, iv 0.467) but a single LINEAR readout over frozen meaning fixes single-hop category-correlated systematicity (bind & concat both 0.778) -> component-1's structural failure = the NONLINEAR HIDDEN LAYER, not the format. So the front-end differentiation fix = LEARNED-LINEAR readout. (3) genuine item-specific systematicity base-rate-limited (bind 7/9 vs base-rate 6/9, sep 0.111<0.15, 9-pair env underpowered). Metric deviation: property-value recovery (concept-recovery ill-posed for category-correlated) -- VET checking soundness. COMPONENT-2 VET'd (skunkworks aac5e8d4, atom 29557 committed 2b76ccc2f): MIDDLE clean; metric deviation SOUND; the linear-readout 'fix' is a clean mechanism-ISOLATION but only +1/9 (noise-floor, NOT a capability win); native binding UNTESTED-not-refuted (single-hop is the wrong regime; needs multi-role). CRITICAL: the REAL thin-meaning wall SURVIVES both components (glass-box hydro->clean still MISSED) -- the toy components were mechanism-exploration, did NOT move the real wall. VET mandate: 'fixing thin meaning is orthogonal+upstream; validate the learned-readout direction BEYOND the 9-pair noise floor on REAL data.' **[IN FLIGHT -- PIVOTAL] REAL-DATA VALIDATION (exp_dev af0335c6): learned-LINEAR readout vs frozen GloVe on REAL WorldTree/ARC fine-content discrimination over MANY concepts (past the +1/N noise floor), measuring the learning curve + whether it MOVES the glass-box wall (hydro->clean + real 29544-46 cases); in-vocab AND held-out reported separately; shuffled must-fail; difficulty-on. HARD-PASS = 'meaning is learnable' SCALES to the real wall (=first REAL move on the wall the whole ARC arc found -> strongly justifies the meaning-foundation phase). HARD-FAIL = toy finding didn't scale -> deeper grounding. (This is now STEP 5 of the master plan.) -> VET.**

**🧠🗺️ PHASE COMMITMENT (USER 2026-07-25) + MASTER BLUEPRINT: duplicate brain reasoning 100%, NO shortcuts, until DEMONSTRABLE reasoning (inspectable derivation trace). [[project_full_commitment_duplicate_brain_reasoning_100pct_no_shortcuts_2026-07-25]]. BLUEPRINT = notes/research_master_reasoning_stack_map_and_build_plan_2026-07-25.md (Opus synthesis of ~10 VET'd drills + ~25 experiments + integration audit). HEADLINE REFRAME: 7/8 reasoning stages ALREADY EXIST as real disk-verified brain-cited components (Kintsch reader; native VSA bind; PPR retrieval + fact store recall@100 0.69; CERTIFIED do-calculus PP-270/307 unwired; Kintsch CI settle; population-vector decision readout 0.687-0.71 given gold) -- the debt is WIRING (NO composed entry point; all trapped in exp-cells) + the thin-meaning FOUNDATION shortcut. ONE structural gap = Stage 8 credit-assignment (done by DIRECTOR BY HAND). 6 named shortcuts, each with a designed replacement. DEPENDENCY-SEQUENCED BUILD: STEP 0 [done] connectivity gate RED. STEP 1 [IN FLIGHT exp_dev a476d66] science-precise rule extraction (ARC_Corpus 14M + WorldTree gold-explanations) -> re-run coverage gate (GREEN cov>=0.35 & sel>=0.15 & typed>null unblocks Step 2; RED->broader source). STEP 2 build hdlab/reasoner.py = compose the reasoner (reader->typed-parser->M3 meet-in-middle search->CI consistency->decision; promotes integration-audit P1/P3/P4) = the FIRST demonstrable reasoning trace (success criterion). STEP 3 do-calculus routing (independent, reuses certified primitive). STEP 4 credit-assignment loop (Stage 8, the real structural gap). STEP 5 [IN FLIGHT af0335c6] grounded-meaning rebuild (deep macro-investment, sharpened by Step 2's residual node-merge confusions -- NOT blind). Joint P(demonstrated reasoning) ~0.13-0.16 (honest). SUCCESS = held-out ARC surface-trap answer + inspectable typed-rule derivation chain + why-the-other-3-fail.** (b) C-probe LANDED (exp_arc_selection_pool_tightness_ablation_v1, committed ec86f573b) = C_FALSIFIED: tightening the pool 100->10 leaves Challenge FLAT-to-declining (0.379->0.366) at ALL sizes; the only gold-retaining tighter pool (POOL_50) lifts nothing; the apparent informativeness-rise is SURVIVORSHIP (only where recall collapses <0.5); topical decoys self-support equally even in a tight pool. => cheap pool-tightness escape CLOSED; B (similarity!=entailment, need REASONING) now confirmed with CERTAINTY (not 0.72). VET+atomize in flight (skunkworks a0f1a0a2). ⚠️ the cell's closing 'richer-CONTENT meaning rebuild is the lever' gloss is STALE/WRONG (refuted by 29547 + 29549); correct conclusion = REASONING/derivation -- flagged to VET, do NOT carry content-rebuild framing forward.** On drill land: build the reasoner -> VET. [superseded step-back text: VET IN FLIGHT (skunkworks a65085e7) adjudicating the ROOT CAUSE across the whole arc: (B) DIRECTOR read = gold set is defined by human EXPLANATORY ENTAILMENT, substrate selects by SIMILARITY, similarity CANNOT reconstruct an entailment-defined target (pool has similar-but-non-entailing support for every choice; oracle 0.671 works only because humans PRE-SELECTED entailing facts -- combiner READS given the set, can't IDENTIFY it); vs (C) POOL-TOO-WIDE / retrieval-granularity (drill's pre-registered contingency, CHEAP fix); vs (A) thin-meaning (partly refuted by content-enrichment v2 29547). VET must DISTINGUISH B (deep: pipeline reframe / entailment primitive) vs C (cheap: tighter retrieval) -- gates the strategic recommendation to USER. HIGHER-ORDER REFRAME on the table: the brain does NOT solve ARC by retrieve-100->select-4->combine; it COMPREHENDS + REASONS (the READER path). The retrieve-select-combine framing may itself be the non-brain-like piece (fails the 'EXACTLY like the brain?' gate). On VET land: synthesize the 7-fail arc + deliver a PROSE strategic recommendation to USER (NOT the widget).** [superseded IN FLIGHT text below was: ANSWER-CONDITIONED CONTRASTIVE SELECTION BUILD (hdi_exp_dev a095fc36) -- over the SAME wide pool + combiner UNCHANGED (ONE variable = answer-agnostic vs answer-conditioned SELECTION), bind Q(x)C_i per choice + score facts by DIFFERENTIAL support (bridges Q->this-choice MORE than rivals), pick the DISCRIMINATING fact per choice; arms A_AGNOSTIC(anchor 0.1865/0.3663)/B_CONDITIONED/MISCONDITIONED(random-choice must-fail)/CONDITIONED-NONCONTRASTIVE(isolate the contrast metric)/ORACLE; PRIMARY = lift Challenge above 0.3663 toward oracle 0.71 + raise sel_gold_precision, glass-box the discriminating-fact picks on surface-trap Qs. (b) skunkworks landed-VET + ATOMIZE the deconfounded content negative (a9256d68). On build land: skunkworks-VET; HARD-PASS=shape gap was the wall, answer-conditioning is the lever; HARD-FAIL=shape hypothesis wrong -> residual pattern says retrieval-cut vs content-resolution gap.]** AUTHORITATIVE DETAIL = notes/research_content_thin_concept_meaning_featural_enrichment_2026-07-25.md (build design) + notes/ingestion_learn_sleep_loop_2026-07-24.md -- READ FIRST.**
  - **🧠 USER DIRECTIVE (2026-07-25, FULL-AUTO authorized): as we attack retrieval/knowledge-meaning, make EVERYTHING AROUND the problem as BRAIN-LIKE as possible (so we can actually achieve it / exceed the brain). Keep docs current. The least-brain-like piece = the disembodied GloVe/WordNet meaning encoder -> brain-faithfulness + the retrieval fix POINT THE SAME WAY (grounded/richer meaning).
  - **🔬 METHOD VALIDATED + FORMALIZED (USER "show it works THEN formalize"): deep-brain-analysis -> comparison -> accurate-duplication (right SHAPE + PLACE + METRIC). Proven by the selection gate (29541). Standing policy; march down the mapped gap-list. [[project_formalize_deepbrain_analysis_then_comparison_accurate_duplication_method_2026-07-24]]. Brain-QA architecture map + gap-list artifact: claude.ai/code/artifact/8bf13969-03ee-4d67-a70c-e98f7851852e (scan notes: research_brain_qa_architecture_completeness / _learning_control_neuromodulation_inventory / _mental_simulation_grounding_causal_models / _combiner_robustness_imperfect_facts / _arc_retrieval_biology_and_design, all 2026-07-24). WIRING GAPS (disk-verified EXIST, unwired to ARC): causal do-operator/counterfactual cells, PFC-gate reward-prediction-error (cfrpe), khop bridging, refuse-gate confidence primitives. DEEP GAP = credit-assignment (Director does it by hand) + grounding.
  - **🎯 TARGET LOCKED (USER 2026-07-24): CRACK ARC-CHALLENGE the BRAIN'S WAY. "the brain can do it, so we can too; we do NOT accept walls others have hit." Prior systems' ~chance-on-Challenge = a wall of THEIR non-brain methods, not the problem (a grade-9 student cracks it). Every miss -> how does the brain do THIS one -> build that step.
  - **🚦 DISCIPLINE RECURRENCE (USER 2026-07-24 "I've seen you do this error like 5 times"): interpretation-ahead-of-evidence -- I keep stamping "hypothesis pending VET" then building plans/pivots/reports on the read AS IF confirmed (the caveat went performative). TWO BEHAVIORAL RULES NOW (see [[feedback_strategic_reads_run_ahead_of_evidence_caveat_interpretation_not_just_verdicts_2026-07-17]] sharpened): (1) in every USER report PHYSICALLY SEPARATE MEASURED (banked/disk-verified) from MY READ (unverified); (2) NO un-VET'd read may drive a downstream ACTION (dispatch/source-pivot/plan-as-decided/conclusion). Only banked atoms + raw disk measurements license action.
  - **THE GOAL (USER, 2026-07-24):** ingest an ordered curriculum -> the substrate LEARNS CONTENT (knows THINGS, grade-school style) -> DESCRIBE its knowledge on a HUMAN GRADE-SCALE (same as a person). Keep OneStopEnglish (data/corpora/onestop) AND build a K-12 curriculum (all textbooks 1-12). Text foundational; image-grounding DEFERRED (real-image recog ~0.15, not ready). Director = IN-THE-LOOP VERIFIER of end-to-end impact.
  - **FOUNDATION BUILT + ALL VET'd (this is the achievement):** (1) HUMAN-SCALE YARDSTICK = ARC grade-3-9 science MC (data/corpora/arc/ARC-V1-Feb2018-2/) + ARC_Corpus.txt 14M sci sentences; **baseline substrate = CHANCE (~zero science = bottom of scale); broad-ingest lexical climb only +0.043 = still at floor (29530 MM)**. (2) **TRUST-VETTING HD FACT-STORE (hdlab/hd_fact_store.py, 29531 MM):** fact=(subj,rel,obj) as ONE role-slot HD bundle w/ SOURCE+TRUST bound IN (glass-box round-trip 1.0); conflict-detect + 4 TRUST-RULES (higher-trust REPLACE / lower DROP / equal-contradiction FLAG / equal-multivalued COMBINE); clean-false-flag 0.0. **USER SOURCE-TRUST DESIGN: trust the curated source, OVERRIDE internal checks, resolve conflicts BY TRUST; does NOT verify factual correctness (student-model trade) -- this REPLACED the failed correctness-auditor (CONDENSER_AUDITOR_USELESS 0.53 false-flag).** (3) STORAGE FOUNDATION-SCALE-OPTIMIZED (29532 MM): SHARDED store (facts separate, not superposed) -> NO crosstalk wall to 1M facts at n_dim>=512 (RAM-bound not crosstalk-bound); O(1) content-hash conflict index byte-equivalent to O(n). (4) MEANING-MATCHING (29533 MM): SemanticHDEncoder (fused GloVe + WordNet-synonyms/hypernyms -> JL 300->2048 HD, hdlab or experiments/exp_semantic_hd_encoder_meaning_match_v1) separates MEANING AUC 0.96 where char-trigram fails 0.0 (scramble-controlled, usa~america); lifts ARC +0.042 MODEST; **SUPPLY-semantic (GloVe supplies meaning, WordNet=glass-box structure/alias) = MM not learned.**
  - **PROGRESS SINCE (all VET'd+banked, LOCAL-ONLY):** capacity-re-check 29534 (foundation-scale on real semantic rep confirmed, single-rep store, no hybrid; the 0.588 anisotropy was a false alarm). READING FLOOR 29535/29536: NO single-item method (lexical/meaning/event) beats ~0.35 on ARC or RACE; meaning NUDGES but noise-level. **AGGREGATION RETRIEVER 29537 (the combine-facts attempt) = HONEST VET'd NEGATIVE:** given GOLD facts a dumb word-overlap baseline (0.786/0.713) BEATS our brain-faithful HD-bundle (0.766/0.696) -- the "cracks it given gold" was LEXICAL LEAKAGE (gold facts authored to share words w/ the answer); on REAL retrieved facts combining HURTS vs single; end-to-end Challenge ~chance. Multi-fact combination does NOT crack ARC.
  - **THE ARC 29538-29542 (all VET'd+banked, LOCAL-ONLY -- each a component tried the brain's way; the wall keeps returning to RETRIEVAL):**
    - **29538 CONTRADICTION cell HARD_FAIL:** added a relation-polarity contradiction signal (WordNet antonyms + negation cues) to the CI settle. It FIRED (30% coverage) but HURT -- the crude lexical detector is ANTI-PRECISE (flags the CORRECT answer as contradicted MORE than wrong ones, because gold facts share words w/ the right answer). Refuted = crude-lexical-contradiction; a PRECISE detector untested (low priority). Combiner-noise + retrieval flagged.
    - **BRAIN-ARCHITECTURE ANALYSIS (USER-requested, 3 scans + comparison):** the ONE genuinely-missing mechanism (3 scans + our experiments CONVERGE) = retrieval CONTROL/SELECTION + competitor SUPPRESSION (Badre-Wagner VLPFC). Comparison verdict: we were building the RIGHT function (retrieval precision) in the WRONG SHAPE (noise-robust-combiner vs a select-gate-BEFORE-reasoning), WRONG PLACE, judged on the WRONG METRIC (recall -- which selection is MEANT to lower). This is the LESSON the method formalizes.
    - **29539 RETRIEVAL SPREADING-ACTIVATION VALIDATED (MM):** PPR spreading over the real WorldTree graph recall@10 0.280->0.380 (+0.10, shuffled-graph control collapses -> real relational structure). But short of bar; recall-capped; end-to-end nudge was a REGIME ARTIFACT (baseline also above chance). Prior synthetic-graph PPR fail was CONFOUNDED, not a refutation.
    - **29540 SEED-COVERAGE = PROVEN NON-LEVER (MM):** doubled seed coverage (0.365->0.545) but recall@10 FLAT -- 8x precision crater, noise-seeds dilute PPR. PRECISION not coverage. Wall = pool NOISE; deeper half = combiner drowns on noise (gold-pool 0.690 vs real 0.355).
    - **29541 SELECTION GATE = METHOD VALIDATED (MM):** the brain-accurate fix -- goal-biased SELECTION + competitor SUPPRESSION BEFORE the unchanged combiner, judged on the ANSWER. Both brain-predicted halves fire + dissociate cleanly (SELECTION broad, decisive on Easy +0.13 p<1e-6, pool-size ruled out; SUPPRESSION lure-specific, real-directional but underpowered p=0.087). Must-fail clean. Chain-grade withheld (Challenge-lure significance unmet). => the METHOD DELIVERS. But VET: the COMBINER (not pool-noise) flagged dominant.
    - **29542 COMBINER ESCALATION MIDDLE_BAND (MM):** confidence-gated bounded escalation (gate->khop-bridge->DISCOUNTED-gap-fill->abstain) wrapping unchanged CI settle. On ACCURACY = WITHIN SEED NOISE (+0.022 swings negative other seeds) -- combiner-robustness is NOT the lever. REAL: (a) discount->CALIBRATION-only (undiscounted = more confident-wrong, but same answers; the brain schema-discount prediction holds as calibration not accuracy); (b) ABSTAIN GATE genuine trustworthiness gain (acc-when-answered 0.42 @ 61% cov vs 0.37, both seeds -- know-when-you-don't-know). khop-bridge = clean NEGATIVE (~random). => RETRIEVAL RECALL is the VET-confirmed dominant wall.
  - **🔑 CONVERGED CONCLUSION:** across combination(29537)/contradiction(29538)/selection(29541)/combiner-robustness(29542) -- ALL point at the SAME wall = RETRIEVAL RECALL (~0.38 of gold central facts). Combining WORKS given gold (Challenge 0.69); retrieval can't REACH gold. The facts ARE in the WorldTree graph (gold explanations drawn from it) -> so likely NOT a content-QUANTITY gap but a MEANING/REACHABILITY gap (thin disembodied GloVe/WordNet meaning can't spread to the right facts). USER instinct (need more foundation -- specifically MEANING/grounding, not raw content) likely right.
  - **29543 MAX-RETRIEVAL DIAGNOSTIC VET'd (MM):** recall@K climbs 0.39@10->0.69@100 on the real graph (shuffled collapses to random floor = structure-driven) => K-BOUND, reachability SOLVED, GRAPH/grounding-bound REFUTED (caveat: reachability-ceiling 1.0 is vacuous on the dense graph; the recall@K CURVE is the real evidence). BUT wide-pool end-to-end is statistically FLAT (E_wide_gate 0.331 ~= E_narrow_gate 0.337 << oracle-on-gold 0.712) -- the pool CONTAINS the gold, the gate can't SELECT it. => wall relocates retrieval->SELECTION-PRECISION; meaning-quality RELOCATED to the selection-relevance stage (not killed). Refutes 29542's "retrieval-recall is the wall" redirect (29542 core stands).
  - **29544 SELECTION HARD_FAIL VET'd (MM-bound):** no fixed brain-faithful selection signal beats the gate (best REL -0.027, coherence 0.265 < random 0.288); sel_gold_precision 0.12 vs oracle 0.97 -> localizes the wall to the relevance/MEANING representation. VET widened the redirect: ruled-out = fixed-signal selection over thin meaning; UNTESTED (cheaper) = LEARNED relevance; DEEPER = grounded meaning.
  - **29545 LEARNED-RELEVANCE ceiling test VET'd (MM) = PHASE-DECIDER, pivot CONFIRMED:** glass-box learned relevance beats all fixed arms + random but only +0.037 (non-sig, closes 9.6% of gap); sel_gold_precision 0.149, TRAIN-set 0.189 vs oracle 0.963. **DECISIVE: even WITH gold labels IN-SAMPLE, a linear readout over thin GloVe/WordNet features can't separate gold from lures (0.189) -> a REPRESENTATION problem, not data/scoring.** Ruled out: fixed(29544)+linear-learned selection over thin features. Untested cheap rung = NONLINEAR learner (bounded LOW by 0.189, skipping as low-payoff). => grounded/RICHER MEANING is the SOUND deep lever. Glass-box: learner rediscovered rr_score dominant + coh NEGATIVE (reproduces 29544 from data).
  - **🚀 NEW PHASE (07-25, USER full-auto + "make everything brain-like"): GROUNDED/RICHER MEANING -- the meaning rep is the confirmed wall. DRILL DELIVERED (notes/research_relational_grounded_meaning_relevance_wall_2026-07-24.md): symmetric cosine is PROVABLY incapable of asymmetric/causal relational structure (RotatE/Tversky/DistMult). CHEAPEST FIX = a WIRING GAP (3rd this session): hd_fact_store.py ALREADY builds a typed role-bind (REL/ARG0/ARG1) for STORAGE but the SELECTION stage never reads it (only flat SemanticHDEncoder cosine). Perceptual grounding ranked LOWER for THIS wall (deferred). IN FLIGHT: relational-meaning selection build (exp_dev a6fa1999) -- wire the typed role-bind into selection so relevance = asymmetric relational-role not word-cosine; 5 arms + SWAPPED-ARGUMENT mechanism sub-test (relevance must FLIP on ARG0<->ARG1 swap = captures asymmetry cosine can't) + random must-fail + oracle. Discriminator = raise sel_gold_precision above the 0.12-0.19 thin-meaning ceiling toward oracle 0.96 + lift the answer. RECOVERY: read metrics -> skunkworks-VET -> HARD_FAIL redirects to multi-hop typed-path / perceptual grounding. Curriculum BREADTH still gated behind fixing meaning.
  - **NEXT SEQUENCE:** (a) contradiction-representation (the deeper lever -- lets combining beat word-overlap + resist lures); (b) RETRIEVAL is ALSO a real wall (~50% of misses = right-facts-not-retrieved; even perfect retrieval won't crack it while combining=word-overlap) -> meaning-retrieval / query-expansion / multi-hop bridging of central facts; (c) METHOD-SELECTION control (try-cheap -> monitor-CONFLICT -> escalate-to-deliberate; we ALREADY have the conflict monitor = trustworthy-reader gate 29465); (d) scale curriculum (WorldTree tablestore 9808 typed science facts LOCAL > ConceptNet; CK-12/OpenStax) toward the full human scale. HONEST GRADE-SCALE: 25%=chance/knows-nothing, per-grade accuracy, ~65%~passing; substrate ~0.30 = below grade everywhere. WorldTree V2.1 sourced LOCAL (gitignored). Image-grounding DEFERRED.
  - **GUARDRAIL (still strict): 2 CHAIN_GRADE->MM down-corrections this session (29522 POS-supply, 29532 storage) -- both construction-determined/supply-not-learned; chain-grade stays reserved for genuine reading/reasoning capability (only arc CG = 29504 selective-reanalysis).** Multi-sentence READER (29506-29520) = DONE + now instrumental; detail = notes/multi_sentence_situation_model_plan_2026-07-24.md.
- **STORE STATE (07-23 history):** atoms.jsonl tail was **29509** (PUSHED to origin/main 485d126d7 w/ USER auth 07-24; SINGLE-SENTENCE READER DONE incl. SESSION-FIRST CHAIN_GRADE 29504 [passive reanalysis]; then MULTI-SENTENCE situation-model phase -- see CURRENT above) (older ledger seq 29494, committed 527b13730, LOCAL-ONLY). 29493 = program-induction (info-bound); **29494 = reader-component ORACLE-ABLATION AUDIT VET'd + banked** (MEASURED_MECHANISM; non-leak PROVEN -- all-oracle precision only 0.587 not ~1.0; ranking clean). ~88 MM banked this session, **ZERO chain-grade** -- guardrail held. Key 29478-29492: 29478 parse-integrated find-leg (recall 0.44->0.60); 29479 scaled seed (+0.199 ISOLATED); 29480 parser-error sleep-consolidation = similarity-vote (learner-null); 29481 condenser-auditor HARD_FAIL; 29482 reversible-role construction-favorable (learner-null 3x); **29483 reader F1 0.5738 (parse-fix+dedup, STRUCTURAL not knowledge)**; 29484 knowledge-gate coverage HF; 29485 rule-induction (control +0.56 / real PP-attach ~similarity); 29486 OOV-backoff revives-but-tie-degenerate; **29487 LEARNER MODULE (model-selection engine + plugin registry)**; 29488 dense-knowledge doesn't transfer; 29489 GAM plugin (extensibility proven); 29490 implicative-neg through module; **29491 KNOWLEDGE FAIRLY REDUNDANT for reader-patient-accuracy + P1/P2 FAIRNESS GUARDRAIL banked**; 29492 supply-sign verb-transfer +0.343 / associative-not-symbolic.
- **📋 COMPACTION HANDOFF (2026-07-23 ~19:50Z -- CURRENT FRONTIER = FINISH + CHAIN-GRADE THE READER; supersedes-in-FRONTIER the 13:15Z foundation-build handoff below [its foundation detail is still valid history]. Store @ 29493, local-only. Full current detail = [[project_foundation_llm_built_kb_vetted_plus_substrate_condenses_knowledge_while_reading_USER_2026-07-23]] -- READ IT.):**
  - **USER DIRECTIVES (07-23 evening, LOAD-BEARING):** (1) **DEMONSTRABLY FINISH what we started -- don't only chase frontiers.** The reader must become ONE runnable CONSOLIDATED who-did-what reader + a DEMONSTRATION (real text -> output + honest accuracy + glass-box), NOT a trail of experiment cells. (2) **~CHAIN-GRADE EACH component** -- drive each to its proven ceiling, IDENTIFY every underperformer (method = component ORACLE-ABLATION audit). (3) **NEXT PHASE (after the reader is demonstrably done) = MULTI-SENTENCE COMPREHENSION via a Kintsch/van-Dijk SITUATION MODEL** -- a persistent structured discourse rep updated per-sentence; the 6 shelved walls (cross-sentence/cross-turn STATE+memory, real-text COREF, reversible/dialogue ROLES, generalizing METACOGNITION, REPLY-generation, perceptual GROUNDING) are all OPERATIONS ON the situation model, not separate fights; HELD until the reader is done.
  - **KEY RESULTS/REFRAMES TODAY (all banked, VET'd, local-only):** reader patient-F1 **0.27->0.57 via STRUCTURE** (parse-integration + a knowledge-independent dedup, NOT knowledge; 29483). **KNOWLEDGE FAIRLY REDUNDANT** for simple-narrative patient-accuracy (29491, brain-shared) -- reached only after 3 flawed tests all CAUGHT (cross-base / saturated-combiner) -> the **P1/P2 FAIRNESS GUARDRAIL banked** (same-base + scramble-must-fire; no integration test can bank a false negative). The **LEARNER MODULE (29487)** = centralized model-selection engine + plugin registry (estimation/rule-induction/GAM/program-induction), proven-extensible zero-core-change (29489), auto-selects by compression = the architecture SPINE. The sleep-consolidation LEARNER is NULL 3x (reduces to a similarity-vote -- the lever is REPRESENTATION+SELECTION, not the linear learner). Grow-thrust wall = INFORMATION not learner-class: supply-sign works across held-out verbs +0.343 (29492) but the unseen feature-combo is UNIDENTIFIABLE from data (29493 -- a genuine symbolic program-inducer ALSO can't fill it; 4 boolean fns fit the seen data; a human uses a SUPPLIED axiom). **THROUGHLINE: structure COMPOSES, FACTS get SUPPLIED -- even compositional rules are supplied facts.**
  - **IN FLIGHT (POST-COMPACTION PICK UP):** (a) component ORACLE-ABLATION AUDIT (ad033cd1e8f8d1887) = the ranked UNDERPERFORMER list + all-oracle ceiling (components-bound vs reader-logic-bound) -- the map for chain-grading; (b) CANDIDATE-RECALL push (ad76a3f836d3eaa36) = LAST reader-improvement piece (recover residual ~30%; report single-sentence-recoverable vs cross-sentence-bound split). RECOVERY: grep experiments/ for newest cells + read data/<anchor>/metrics.json; skunkworks-VET each. THEN: drive each closable underperformer to ceiling -> CONSOLIDATE + DEMONSTRATE the reader. Frontiers HELD.
  - **⚙️ CHAIN-GRADE PLAN + READER COMPONENT MAP (THE executable plan -- run this post-compaction):** FINISH SEQUENCE: (1) FOLD the 2 in-flight results -- oracle-ablation AUDIT (ad033cd1e8f8d1887 = the ranked UNDERPERFORMER list + all-oracle ceiling) + candidate-recall PUSH (anchor `exp_multipred_argstruct_enumext_v4`, data dir exists; recover residual ~30% + report single-sentence-recoverable-vs-cross-sentence-bound split) -> grep experiments/ newest + read data/<anchor>/metrics.json -> skunkworks-VET each. (2) From the audit ranking, DRIVE each CLOSABLE underperforming component to its proven ceiling (one at a time, VET each; accept MAPPED walls honestly -- chain-grade = reach the ceiling, don't fake past a real wall). (3) CONSOLIDATE the scattered cells into ONE runnable who-did-what reader. (4) DEMONSTRATION: real text -> who-did-what output + honest end-to-end accuracy + glass-box/inspectable. **READER COMPONENT MAP (cascade order, drive each to ceiling):** #1 **CANDIDATE-RECALL / predicate-enumeration** = THE binding constraint [recall 0.44->0.70->**0.75** (enumext_v4 push landed, F1 0.5738->**0.5882**, +5 recovered / 0 regressed, MIDDLE_BAND VET-pending); **KEY: the 25 residual misses are ALL single-sentence-recoverable, 0 cross-sentence-bound -> MORE single-sentence enum headroom remains, NOT yet situation-model-gated**; still the top oracle-uplift lever +0.053 -> keep pushing enum]. #2 **DEPENDENCY-PARSE** [UAS ~0.79-0.81 = MAPPED feature-ceiling (search 29458 + valency 29460 both HARD_FAILed); classical ~0.90 needs semantic/contextual rep; HELD -- do NOT assume meaning-feeds-attachment helps until shown, esp. since selectional knowledge proved redundant]. #3 **ROLE/PATIENT ASSIGNMENT** [end-to-end patient-F1 0.5738, precision ~0.49; structural version works; HARD cases (reversible / DIALOGUE role-misassignment) WALLED + shelved]. #4 **POS** [0.906 substrate vs ~0.97 classical -- check closable]. #5 **RELIABILITY-WEIGHTED constraint-integration** [built 07-23; the per-decision cue combiner; follow-effect across every decision]. **🔑 AUDIT VET'd + BANKED seq 29494 (cell exp_reader_component_oracle_ablation_audit_v1; commit 527b13730; NON-LEAK PROVEN -- all-oracle precision only 0.587 not ~1.0, ranking clean+deterministic byte-identical): baseline F1=0.5738; oracle-uplift ranking = ENUM/candidate-recall +0.053 (closable, recall-push in flight) > ROLES/patient-assignment +0.039 (CLOSABLE -- the top NOT-yet-pushed lever, drive this next) > PARSE routing +0.022 (MAPPED-BOUND ~0.81 UAS ceiling, accept). ALL-ORACLE ceiling = 0.7106 (NOT 1.0) -> ~0.29 gap remains even with PERFECT components = the READER'S OWN COMPOSITION LOGIC is ALSO bound (a SEPARATE lever beyond chain-grading components). **VET SHARPENED THE TARGET: the 0.29 gap is ASYMMETRIC = precision-side SPURIOUS-PREDICATE emission -- spurious_verb_fp stays FLAT 31-33 and subcat_fp FLAT 32-35 across ALL oracle arms, so oracles never remove spurious predicates. The reader-logic lever (step 4) is concretely SPURIOUS-PREDICATE SUPPRESSION, not vague gold-matching.** POS not separately oracled (no gold POS treebank for McGuffey). NET: chain-grading components caps end-to-end ~0.71; closing past that needs the reader-logic lever too.** **'DONE' = each component at proven ceiling + the reader-logic gap understood + ONE runnable reader + a demonstration.** Frontiers (situation model / supply-axiom / more plugins / vision) HELD until DONE. **🔑 PER-COMPONENT DOSSIER (scour+brain-dive fused, chain-grade rubric, levers + false-starts for EACH component -- the living checklist, update as each drive lands) = notes/reader_chaingrade_component_dossier_scour_plus_braindive_2026-07-23.md. CHAIN-GRADE = composed reader clears 4 bars on REAL text (naive-baseline-discriminator-fires + causal-hand-edit-flips-output + tamper/replay-audit + non-ceiling-at-scale); demo scaffold in flight; component MMs are the building blocks. USER (07-23): proceed FULL-AUTO, diligently drive EACH requisite component to ceiling; PULL FORWARD a held-frontier piece if REQUIRED to chain-grade (currently n_cross_sent_bound=0 -> none required yet).**
- **📋 COMPACTION HANDOFF (2026-07-23 ~13:15Z -- CURRENT FRONTIER = FOUNDATION BUILD, three-part; self-contained; supersedes the 11:25Z handoff):** USER locked the direction into a **3-part foundation** for the glass-box reader, each part now evidence-graded (all banked, VET'd, local-only). Detail = [[project_foundation_llm_built_kb_vetted_plus_substrate_condenses_knowledge_while_reading_USER_2026-07-23]].
  1. **SEED (knowledge -- "choose"): DECIDED + proven.** Sourcing = **LLM-BUILT + KB-VETTED** (USER-locked 07-23). Rich LLM knowledge moves the walls (selectional 0.48->0.81 / 29471, PP-attach 0.70->0.82 / 29472, integrates in the real reader who-did-what 0.56->0.83 / 29473). Independent-KB rigor (29475): pure VerbNet/WordNet recovers only a FRAGILE ~27% (cite +0.076..+0.093) -> pure ontology TOO SPARSE; so LLM supplies dense knowledge, VerbNet/WordNet are the AUDITOR. Leakage = coverage-not-cheating but NARROWED-not-cleared (the dense 73% is un-auditable by ontology).
  2. **GROW + AUDIT (condenser -- "learn as it reads"): mechanism REAL, scope-limited (29476).** USER req: the seed is NOT frozen -- the substrate must LEARN to CONDENSE knowledge as it reads (abstract-to-class NOT memorize; improve-with-exposure). Works: unseen-noun generalization curve 0.500->0.750 monotone, verbatim-arm pinned 0.500. BUT VET-scoped: 100% CROSS-class (easy axis); supersense granularity CAN'T beat the hard same-class wall by construction; headline +0.25 inflated (real signal = +0.10 shuffle-delta + the curve). -> DIVISION OF LABOR: LLM-seed = fine/dense; condenser = category-level-from-reading + the leakage-INDEPENDENT auditor of the un-vettable 73%. OPEN Q: can fine within-class knowledge be condensed from reading, or is it frequency-bound (= LLM's permanent job)?
  3. **FIND (extraction -- "read the whole sentence"): mapped; BUILD IN FLIGHT.** The reader's real ceiling is candidate-RECALL 0.44 (56/100 gold never generated / 29473); diagnostic: 96% extraction-misses, 68% because the reader processes ONLY THE FIRST VERB per sentence. Heuristic multipred fix HARD_FAILED (29477) BUT the VET showed the subcat-GATE design is SOUND + half the regressions were self-inflicted bugs -> the lever = wire the reader to a REAL parse (transition parser 29451, 0.81 UAS) to enumerate ALL predicates+arg-spans; cheap-heuristic path plausibly ~0.60s + NOT closed (kept on the table). Brain-check: brain parses, doesn't verb-scan (supply-structure, 29455).
  - **VISION: HELD (USER steer "come back later").** Integrated recognize->bind->ground battery banked 29474 (works on real digit pixels e2e 0.813; novel-class WALL confirmed honestly = the vision twin of the text unseen-content gap). Vision-ready SCHEMA locked into the foundation NOW so it bolts on later without re-grounding: (a) concept-ID keys (lemma+sense) not word-strings, (b) shared bidirectional meaning-blackboard, (c) concepts = feature-role bundles. Perceptual grounding of the reading vocab DEFERRED (gated on ingesting images for those concepts).
  - **IN FLIGHT (POST-COMPACTION PICK UP): the FIND-leg build -- exp_dev aaf59003825ac04dc** = parser-integrated multi-predicate reader (wire 29451 into candidate enumeration, restore split_sentences, fix the VerbNet put/hear gate false-negs, keep the subcat-gate; measure recall 0.44->? + F1 + precision + learning curve; parse-scramble + gate-off controls). RECOVERY: grep experiments/ for the newest parser-integrated/multipred reader cell -> read data/<anchor>/metrics.json -> skunkworks-VET (does parse-noise flood candidates / precision collapse?) -> bank.
  - **THE 3 FORKS presented to USER + my recs (self-drive is executing A now):** (A) find-leg = wire-in-the-real-parser [REC, executing] vs cheap-heuristic; (B) condenser ambition = accept division-of-labor [REC] vs invest in fine-from-reading; (C) build order = seed-scaling first [REC] then find-leg. USER said "make it happen + continue" (self-drive) -> driving A; C (scale the LLM+KB-vetted seed tables to reading-vocab coverage, concept-ID keyed, condenser as growth+audit loop) is the next big build; then condenser-as-auditor + (deferred) vision-ingestor for novel content. VET every positive HARDEST -- the Director over-read positives ~a dozen times this session; the adversarial VET caught EVERY one (incl. this run: condenser cross-class over-read + multipred needs-real-parse over-read).
  - **🧠 ARCHITECTURE EXPANSION (USER 07-23): the SLEEP/atomize-consolidate loop = the substrate's UNIVERSAL SKILL/RULE LEARNER.** Same brain mechanism (episodic error encoding -> sleep replay -> generalizable rule extraction; CLS) grows every STRUCTURAL/PROCEDURAL skill from experience; FAILS on per-item FACTS (collapses to similarity-vote, 29440) -- boundary MEASURED by the condenser (got category-rule, not within-class facts). DIVISION: sleep-loop grows SKILLS (parsing/roles/attachment/coref-structure/planning/perception); seed+condenser supply FACTS. PER-CLUSTER GATE (rule where a pattern exists; else keep episodic; the rule-vs-episodic ratio is informative). IN FLIGHT: parser error-consolidation (aa970322). NEXT TARGET (USER-approved, AFTER parser loop proves the mechanism generalizes -- don't replicate unproven): ROLE ASSIGNMENT/SRL (turns the mapped dialogue role-misassignment CEILING from bounded into moving). Detail = [[project_foundation_llm_built_kb_vetted_plus_substrate_condenses_knowledge_while_reading_USER_2026-07-23]].
  - **RESULTS THIS RUN (VET in flight):** SEED-scaling HARD_PASS (scaled LLM+KB-vetted 0.690 vs thin 0.491 at coverage, +0.199 scramble-controlled; auditor 24/24 catch but 18% false-flag; caveat = pseudo-gold labels -> leakage-fingerprint VET a61e5488); FIND-leg parser-integration MIDDLE_BAND (recall 0.44->0.60, F1 0.27->0.45, precision UP, arc-scramble fired; VET+commit a1c1ba76). INFRA: fixed exp_dev inline-local bail bug (backgrounded heavy jobs + returned) -> .claude/agents/exp_dev.md lines 34+48 now mandate foreground-to-completion-or-reduce-scope when remote forbidden.
- **⚠️🔑 MAJOR REFRAME (07-23, banked 29471 MM -- CORRECTS the "everything-mapped/chain-grade-unreachable" conclusion):** the mapped reading CEILINGS were measured KNOWLEDGE-STARVED. The selectional ceiling = KNOWLEDGE-GRANULARITY POVERTY, NOT a mechanism bound: rich LLM-BUILT verb-specific selectional foundation (build-time LLM + glass-box-runtime dict lookup = the USER-AUTHORIZED PIVOT) moves a ceilinged 2AFC chance 0.475 -> 0.814 (+0.339, monotone in richness, controls at chance; leakage-CLEARED -- table errs on general-vs-corpus divergences [find child>find boy] not toward 1.0 = genuine general plausibility; thin had FULL coverage so wall = GRANULARITY not coverage). **=> the OTHER mapped ceilings (PP-attach/roles/relation-recall/WSD) were ALL measured thin-knowledge -> KNOWLEDGE-limited-vs-mechanism-bound is a REOPENED testable question. "The brain reads well because it KNOWS things" was the right challenge; Director over-read ceilings as mechanism-bounds.** SCOPE 29471: isolated 2AFC n=59, MM (deterministic lookup not CG). IN FLIGHT: pivot test 2 (a9c5ec64) = does rich knowledge move PP-ATTACHMENT (2nd ceiling)? NEXT: land the rich foundation in the full integrated reader. The genuine on-text authorized thrust reopening the accuracy frontier. (29465 trustworthy gate; 29466 OOD transfer partial; 29467 CAPSTONE NP-head zero-confident-wrong both corpora; **29468 = correct-to-head COMPLETES it: recovers abstentions as CORRECT answers, coverage restored at zero-halluc** -- all MM.)
- **✅ TRUSTWORTHY READER = COMPLETE + VET-CLEAN (the session's built capability, 29465-29468):** a glass-box reader that is NEVER confidently wrong (halluc 0.000) on BOTH in-domain (McGuffey) AND out-of-domain (LitBank) prose, RECOVERS what it would abstain on (correct-to-head, coverage restored, prec 1.000), via INSPECTABLE + TRANSFERRING signals = coref feeling-of-knowing (in-domain) + coherence/conflict + NP-head structure (the piece that GENERALIZES OOD where the calibrated margin went inert). DEEP LESSON: structural/grammatical signals TRANSFER; calibrated-confidence signals are corpus-specific. This is the glass-box EDGE (inspectable honesty that holds on unseen text) -- the honest answer to the conversational goal, since raw accuracy on natural text is CEILINGED (mapped every angle). SCOPE: MM not universal-CG (n=53+21). **SCOPE CORRECTED (banked 29469 MM, VET refined Director over-read in BOTH directions): the zero-confident-wrong property is NARRATIVE-TESTBED-SPECIFIC, does NOT generalize. Robust evidence = DIALOGUE (Shakespeare, the GOAL-relevant genre): complete-gate halluc 0.125, precision COLLAPSE 0.667->0.000 -- the calibrated margin/conflict signals ANTI-CORRELATE (abstain on all 4 correct, keep the 1 wrong); the wrong = ROLE-MISASSIGNMENT ("mother" for "promise", valid-head-noun error INVISIBLE to NP-head); gold-independent/robust. NP-head fired 0/0/0 on all 3 new genres. NEWS (0.111) FRAGILE (debatable santa/claus gold flips to PASS -- did NOT inflate the negative). EXPOSITORY = upstream extractor collapse (moot).** DEFINITIVE LANDING: the trustworthy reader works on NARRATIVE because narrative rarely stresses the hard operations; DIALOGUE fails via ROLE-MISASSIGNMENT = the MAPPED roles/SRL ceiling (reversible/non-canonical rare-and-hard). Every remaining path converges on MAPPED component ceilings (dialogue-generality needs roles ceiling; accuracy needs relation-recall ceiling -- both bounded). NO clean unfilled lever -> further reader-dispatches = mapped-ceiling-gated (filler). Defensible result = the NARRATIVE trustworthy reader + comprehensive mechanism mapping + deep lessons (structural-signals-transfer-within-error-profile; metacognition-is-corpus-specific; natural-text-rarely-requires-hard-ops). USER-STEER FORK: (1) invest in broader structural-signal library incl role-disambiguation [breaks the mapped roles ceiling = LARGE]; (2) accept the narrative capability + consolidate; (3) raw-accuracy [walled].
- **🏁 SESSION CAPSTONE (banked 29467 MM, HARDEST-VET survived 6/6, NO framing corrections): a glass-box STRUCTURAL NP-head-consistency signal completes the trustworthy reader to ZERO confident-wrong (halluc McGuffey 0.038->0.000, LitBank OOD 0.095->0.000) at ZERO coverage cost (retained 18/24 & 8/13 UNCHANGED, prec 1.00, 0 false-abstain -- dropped EXACTLY the 2 wrong residuals/corpus). It TRANSFERS OOD (byte-identical shared module fires where coref-margin was inert AUC 0.433).** DEEP LESSON: CALIBRATED-margin signals are corpus-SPECIFIC (don't transfer); STRUCTURAL/grammatical signals GENERALIZE. The trustworthy reader = coref feeling-of-knowing (in-domain) + coherence/conflict + NP-head structure (transfers) -> honesty you can inspect that holds on unseen text = the glass-box EDGE a black box can't offer. SCOPE: zero-confident-wrong ON THESE SETS (n=53+21), MM not universal-CG; head==gold in all 4 residuals -> CORRECT-TO-HEAD is the coverage-raising follow-up (turn the abstentions into correct answers). Composes 29465/29466. (29461 clean-register composition; 29462 decisive scale HARD_FAIL; 29463 abstain-gate v1 MM [COREF margin = feeling-of-knowing AUC 0.885; PARSE anti-informative]; 29464 coref-only gate HARD_FAIL [ranking-AUC != gate-ability; parse errors tie at ceiling]; **29465 = TRUSTWORTHY READER GATE MM [the session's key capability]** -- all MM.)
- **🛡️ TRUSTWORTHY READER GATE (banked 29465 MM, HARDEST-VET confirmed w/ 3 honest downward corrections):** glass-box gate = coref salience margin (feeling-of-knowing) OR Kintsch coherence/CONFLICT (union of abstentions) -> **NEAR-zero hallucination 0.038 (0.023 deduped) at usable-but-modest coverage 0.377 (answers ~47% of answerable, keeps 18/24 correct, precision 0.90-0.93)** on real McGuffey text. The substrate can be a reader that is RARELY CONFIDENTLY WRONG (right-or-abstains) via a legible mechanism -- its glass-box EDGE given the MM accuracy ceilings. VET CORRECTIONS: (1) n was padded 53->43 distinct (+12 genuine new); (2) residual = ONE error not two (N6=W18 same "log house" query), mechanism = COMPOUND-NOUN HEAD selection (returns "log" not "house") -> needs an NP-HEAD-consistency signal (NOT verb-frame, NOT role-reversal as Director mis-framed); (3) near-zero != zero (1 confident self-consistent residual survives). SCOPE: MM not CG -- 14-passage QUESTION-widened, NOT passage-diversity-widened. Composes 29462/29463/29464. **NEXT LEVERS (clear): (a) NP-head-consistency signal -> catch the 1 residual -> true zero-hallucination; (b) passage-diversity widening (needs verified verbatim source) -> toward scale-general/CG; (c) the HARD bottleneck = raise relation-extraction recall (~0.38) to lift COVERAGE above ~47% (accuracy, not truthfulness).**
- **🌍 OOD METACOGNITION-TRANSFER TEST (banked 29466 MM, VET-clean 6/6): the trustworthy gate PARTIALLY transfers out-of-domain (LitBank adult prose, frozen McGuffey threshold): stays DIRECTIONALLY truthful (halluc 0.191->0.095, coverage correctly drops 0.81->0.48 = abstains more) BUT the CONFIDENCE SIGNAL does NOT generalize (coref-AUC 0.433 BELOW chance, beat_scramble 0.015 below HP floor = signal INERT OOD; the halluc reduction is STRUCTURAL abstention, not graded discrimination). => the substrate's metacognition is CORPUS-SPECIFIC (calibrated to its tuned error profile; brain-shared -- human metacognition also degrades on unfamiliar material). CONVERGENCE: the OOD residuals (title-split "Lord Henry"->"lord", adjective-grab "fair young man"->"fair") are the SAME NP-HEAD-selection error class as the 29465 in-domain "log house"->"log" residual -> ONE well-localized blind spot. HYPOTHESIS (in flight): a STRUCTURAL NP-head-consistency signal (unlike the corpus-specific margin) should TRANSFER, fixing both in-domain + OOD. Relation-recall dedup = MAPPED/bounded (PATIENT_SELECTION_HURTS, RECALL_ARTIFACT).** (29457 grounding binding win; 29458 parser search-bound; 29459 grounding improving-curve; 29460 parser valency-bound; **29461 = FIRST end-to-end multi-turn conversational loop COMPOSES on clean register (interfaces wire together, salience load-bearing, ZERO-HALLUCINATION preserved across turns) -- MM, scoped clean-register [parse=1.0 by construction], NOT scale-proven** -- all MM.)
- **🎯 DECISIVE SCALE TEST = HARD_FAIL, BANKED 29462 (MM, VET-clean 5/5) -- the NORTH-STAR ANSWER:** the validated components + loop machinery COMPOSE PERFECTLY under ORACLE (perfect parse+coref -> cross-turn QA 1.000, halluc 0.000 = machinery sound, ceiling reachable) BUT at REAL-text scale (14 verbatim McGuffey passages, 31 cross-turn Qs) the true MM component ceilings COMPOUND + COLLAPSE: REAL cross-turn 0.191 < memoryless freq floor 0.381 (gap 0.742, bootstrap p=0.95 doesn't beat floor); memory NOT load-bearing (+0.048 ns). **ZERO-HALLUCINATION INVARIANT BREAKS: REAL halluc 0.419 (confident-wrong, no abstain gate); and ADDING coref/memory INCREASES halluc (0.194->0.419) = confident wrong bindings -> memory is net-NEGATIVE without a truthfulness gate.** Bottleneck LOCALIZED = parse->relation-extraction recall (~0.375), NOT coref. **CONCLUSION: pieces compose (oracle proves it) but MM floors compound at scale; path to "talk to it" = (1) ABSTAIN GATE [restore zero-hallucination -- PRECONDITION for memory to help; the substrate's glass-box edge] + (2) raise relation-extraction recall [accuracy bottleneck].**
- **🔬 IN FLIGHT = ABSTAIN GATE (a7565ebe):** glass-box confidence gate (parse score/coref margin/relation support) -> ABSTAIN when weak, restore zero-hallucination at scale. CORE = precision/coverage: drive halluc->~0 while keeping non-trivial coverage (high precision-on-answered). HARD_PASS=confidence GENUINELY discriminates right-vs-wrong (metacognition edge); HARD_FAIL=must-abstain-on-everything (confidence uninformative). VET on landing.
- **🧩 DECISIVE STRATEGIC FINDING (07-23, dedup-driven):** the conversational-substrate COMPONENTS ARE ALL BUILT + mostly HARD_PASS (on small/isolated tests): multi-turn dialogue STATE (HARD_PASS slot-recall 1.0), COREF Hobbs+Centering (HARD_PASS acc 1.0, abstains on ties), SALIENCE/topicality Centering (HARD_PASS), base reader grounded-relations+coref (HARD_PASS coref-lift 0.714), grounding binding (29457/59 MM), parser (0.81 distributed/semantic ceiling: 2 HARD_FAILs 29458 search + 29460 valency; error = PP 18.5%/punct 18.4%/clausal 13%/coord 10%/core-arg only 8.8% -> ~42% needs SEMANTIC attachment), roles/SRL (mapped, crude-cues-sufficient). **THE GAP TO "TALK TO IT" = END-TO-END INTEGRATION, not more components.** Individual components mostly land MM because natural text rarely REQUIRES the hard operation (recurring wall). The path = ASSEMBLE the validated components into a Kintsch CI (construction-integration) CONSTRAINT-NETWORK conversational loop (parse+ground+roles+coref+salience-memory+multi-turn-state settle jointly -- which also fixes the parser's semantic ceiling: meaning feeds attachment). Integration is BOTH the next step AND the robustness test (components validated on small slices -> do they COMPOSE at scale?). Director recommendation to USER: shift from component-building to integration. Prior partial-integration cells exist (exp_graded_thematic_fit_integrated_reader_gate, exp_base_reader_grounded_relations_coref) -> dedup + build the missing end-to-end loop.
- **🔒 DEFINITIVE (07-23, ~7 dedup sweeps): the reader/conversational RESEARCH space is COMPREHENSIVELY MAPPED -- no new research lever found.** Even the "constraint-based lexicalist / selectional-fit feeds attachment" lever (the one I thought untested) is ALREADY BANKED: atoms `constraint_based_lexicalist_MacDonald_Trueswell_Kintsch`, `SELECTIONAL_COHERENCE_is_a_genuine_CEILING`, `SELECTIONAL_RESTRICTIONS_supply_all_net_lift`; the thematic-fit integrated reader gate = MIDDLE_BAND (didn't help); Kintsch-CI loops exist (compress-and-carry CCL, CPCL reader loops, cortex_integration_end_to_end). Recurring wall = natural text rarely REQUIRES the hard operation + selectional-coherence-is-a-ceiling. **CONCLUSION: dispatching more reader-mechanism cells = re-runs (filler, tick-forbidden). The remaining path to the conversational goal is ENGINEERING = assemble the validated mechanisms into a running end-to-end system (a BUILD needing USER scope: which capability, which corpus, what "done" = a demo the USER can interact with). Uncertain payoff (component MM ceilings may compound). This is a genuine phase-change / build-scope decision surfaced to USER -- NOT a research lever to self-drive.**
- **🗣️ CONVERSATIONAL-SUBSTRATE BUILD (07-23, USER full-auto; direction LOCKED [[project_critical_path_to_conversational_substrate_2026-07-23]]):** goal = a substrate you can CONVERSE with (glass-box). Critical path (load-bearing order): PARSER -> GROUNDING -> roles/SRL -> cross-turn MEMORY -> reply. Audio DEFERRED (USER: not off table, do later unless big unlock). KEY REFRAME: humans read text ~95% because they ALREADY KNOW grammar -> our fair task = SUPPLY structure + READ (works, 29455), not invent-from-scratch. STATUS:
  - **PARSER: global-beam training HARD_FAIL (BANKED 29458 MM earned bound, VET-clean 5/5; global arm genuinely trained ~43k updates, null is trained-but-ties):** global-beam 0.809 ~= local greedy 0.811 (2SE-clean, no lift); beam-hurts control reproduced (0.74) then REMOVED by global training but greedy NOT exceeded -> the ~0.81 ceiling is a FEATURE/REPRESENTATION limit, NOT search/training. DRILLED + brain-checked -> the real lever = LEARNED VERB-VALENCY/subcat features (brain: immediate valency effects MacDonald/Trueswell; Zeman "smart lexicalization far better"; LCCP argstruct already HARD_PASSED). **VALENCY-PARSER IN FLIGHT (aa150b08, inline):** ARM_VALENCY vs ARM_BASE, target 0.81->0.84-0.88, shuffle-frame anti-cheat, learning curve. NOTE: parser+roles CONVERGE (verb-argument knowledge = same lever).
  - **GROUNDING: banked 29457 (MM) -- attention-gated multi-object binding WIN (brain-faithful, VET-clean 6/6 targets):** the Treisman illusory-conjunction problem where VLMs fail -- ATTN 0.815 vs FLAT 0.515 (@M=4 feature-sharing), scramble->0.44 + label-shuffle->0.50 both collapse (attention + learned front-end load-bearing), all 3 FIT components present (parallel maps + serial spotlight + FHRR bind), ONE variable. MM-not-HARD_PASS ONLY because the USER-mandated IMPROVING property (learning curve +0.042<0.06) genuinely missed -- k-NN front-end is sample-efficient (learns fast/flat); "learned-from-data" shown, "improves-with-exposure" not. Declined exp_dev's flip-to-HARD_PASS (curve is not redundant w/ label-shuffle). Free-by-construction novel-conjunction=1.0 reported UN-gated (not the headline). **UPGRADE banked 29459 (MM): the improving-with-exposure gap CLOSED -- incremental running-mean prototype front-end (Posner-Keele) gives a MONOTONIC exposure curve (per-seed REAL rise, min 0.155; fair few-shot start EARNED, above chance) while preserving the binding win (ATTN 0.833 vs FLAT 0.521). VET-clean 6/6.** So grounding = a PROVEN brain-faithful MECHANISM (Treisman FIT binding + Posner-Keele refinement: attention solves illusory conjunctions AND improves with experience). **HONEST CEILING (VET framing): MM not CG -- it runs on a SYNTHETIC rendered-scene toy with IDEALIZED object localization (fixed slots, segmentation ASSUMED). The gap to a REAL grounding capability = object localization/SEGMENTATION on REAL images (currently assumed away) = the honest next grounding frontier, substantial.** Envelope: no crosstalk wall by M=6.
  - **REMOTE-PUSH BLOCKED:** exp_dev keeps routing FULL to remote (needs origin push = NOT authorized w/o in-session USER auth); INLINE-local FULL is the working path for pure-numpy cells (29453/55/56/grounding/parser all ran inline). "Full auto on priorities" = build auth, NOT push auth. Store clean+committed, nothing pushed. **SESSION CULMINATION = the LEARNED-INDUCER ARC below (29450->29455): the buried-subject wall was BEATEN, then a LEARNED accumulator shown to generalize PRODUCTIVELY to unseen depth on real text = the structure x content thesis working, honestly MM.** Synthesis notes: [[unifying_synthesis_composition_works_induction_is_the_wall_2026-07-22]], [[reader_capability_map_and_glassbox_improvement_roadmap_2026-07-22]], [[research_incremental_stack_parsing_paradigm_2026-07-22]].
- **🏆 LEARNED-INDUCER ARC (2026-07-23, the session's real forward push; USER "we don't give up on the inducer"):** The induction "wall" was RE-DIAGNOSED as a WIRING FLAW, not a bound. Every prior induction fail (29448/29449/etc.) collapsed ONLY on buried-subject SELECTION (oracle-select+morph=0.9925; subject-first~0.75; buried~0.63=majority) because the inducers got LOCAL features (a 1-step prev-is-prep boolean) + a LINEAR readout that STRUCTURALLY CANNOT compute a running cumulative embedding-depth. Brain does it via incremental function-word bootstrapping + cue-based retrieval (Lewis-Vasishth; prosodic bootstrapping). FIX = a glass-box sequential INCREMENTAL DEPTH ACCUMULATOR. Results banked this arc:
  - **29452** (MM): rich lexical features (k-means cluster proxy + affix) give NEGLIGIBLE UAS lift (+0.0019) + DON'T touch buried -> buried lever is STRUCTURAL not lexical. Closes roadmap #1's rich-feature tail.
  - **29453** (MM): LEARNED depth accumulator BEATS the buried wall (0.7733 > deterministic 0.7561, +0.221 over shortcut) by DISCOVERING function-word depth deltas from the agreement label alone (of:+0.32, that:+0.06, comma:-0.09). Glass-box, all discriminators fire. DOWN-RATED from the cell's HARD_PASS: it learned per-FW SCALARS (a lookup) that COLLAPSE on FW-holdout (0.6425~majority) -> not generative over novel function words. My "chain-grade" framing was the over-read; VET caught it.
  - **29454** (MM): distributional-SIGNATURE map CANNOT reconstruct the DECISIVE high-freq openers' depth role when they are held out (map_HFholdout 0.6012<majority) -- BUT the VET NARROWED it: the map HOLDS (+0.077) on random/mid-freq FW holdouts; collapse is specific to the top openers. Distribution DOES transfer depth-role generally; only the ~10 decisive openers resist. Points to a HYBRID (scalars for openers as closed-class-items-learned-as-items + signature-map for the rest).
  - **29455** (MM, the LANDMARK): the CORRECTLY-SPECIFIED chain-grade axis = PRODUCTIVITY over structure (train shallow depth<=1, test deep depth>=2), NOT FW-holdout (a brain-check: function words are CLOSED-CLASS; humans learn them as items and don't extrapolate to novel FWs, so FW-holdout was the WRONG bar). RESULT: the learned accumulator trained ONLY on shallow HOLDS on unseen deep (learned_deep 0.7324 == deterministic recursive ceiling 0.7312, TRACKS det at d2-d5+ across ALL 5 seeds, nearest flat 0.30, +0.43 over nearest). HARDEST VET SURVIVED all 7 targets; learning is LOAD-BEARING (fixed-random 0.41, naive-hand-uniform-rule 0.675 << learned 0.732). Honestly MM-not-CG for TWO reasons: (1) the recursion is ARCHITECTURAL (accumulator sums by construction) -- only the CONTENT (FW deltas) is learned, and it rediscovers 29450's mechanism class, NOT "learned recursion from scratch"; (2) cond_d degradation is a KNIFE-EDGE (3/5 seeds individually exceed the 0.08 gate; mean passes by 0.003). Honest claim: **pre-given recursive scaffold + LEARNED FW-increment content -> productive generalization to unseen embedding DEPTH on real text = the structure x content compositional-generalization thesis working, on the productivity axis.** Composes 29450/29453/29454. Does NOT cover the novel-FW axis (29453 bound stands).
  - **29456** (MM, GENUINE BOUND -- the clean-CG-via-autonomous-selection shot, CLOSED honestly): brain-check RESOLVED the learn-the-scaffold fork on EVIDENCE -- **McCoy-Frank-Linzen 2020** (hierarchical bias REQUIRES architectural tree/stack; structure-free learners go LINEAR -- so 29455's architectural scaffold is NECESSARY, not a cheat, and explains TEM's fail) + **Perfors-Tenenbaum 2011** (brain SELECTS hierarchy via model-selection over a hypothesis space). So the clean-CG gap = does the substrate SELECT hierarchical over linear FROM DATA? Ran the CANONICAL McCoy ambiguous-training paradigm (train ONLY where depth-rule & nearest-rule AGREE = subject-first; test on DIVERGE = buried). RESULT HARD_FAIL + VET-confirmed GENUINE BOUND: ARM_ACCUM buried 0.5945 = majority floor (NOT hierarchical); ARM_FLAT 0.4221 = linear pole (reproduces McCoy seq-model); ARM_MIX Perfors-gate drifted LINEAR (gamma -0.996); det ceiling 0.80 reachable. **CRUX (VET fixability probe REFUTED the Occam-prior hypothesis): the POP/DECREMENT (closer) signal is UNIDENTIFIABLE from agree-only data** -- subject is always first-noun so no gradient makes closers negative; learned openers right-signed (of+1.69,that+1.48) but closers WRONG-signed positive; an L1/Occam prior can shrink magnitude but CANNOT FLIP A SIGN (hand-forcing closers -1 -> 0.644; full hand-hier -> 0.792==ceiling). So autonomous hierarchical SELECTION from ambiguous data is a REAL structural bound for the scalar accumulator; the push half is learnable, the pop half needs buried supervision (29453/29455 had it) OR a genuine STACK architecture with pop as an architectural primitive (McCoy). Does NOT overturn 29455/29453/29450.
  - **HONEST CURRENT CONCLUSION (arc 29450-29456):** the inducer BEATS the buried wall (29450 det / 29453 learned) and generalizes PRODUCTIVELY to unseen depth (29455 MM landmark) GIVEN the depth structure (buried supervision or hand rule). It does NOT autonomously DISCOVER the full hierarchical rule from ambiguous data (29456 bound: pop-signal unidentifiable). The structure x content thesis WORKS when structure (incl. the pop primitive) is supplied; autonomous selection of the pop from agree-only data is the earned wall. **BRAIN-DRILL RESOLVED THE ARC (2026-07-23): the pop is BRAIN-FAITHFULLY hard + our bound is the brain's bound.** (1) Children acquire constituent-CLOSURE LATE, WM-gated, from COMPLEX input (right-branching before center-embedding; rel-clauses stabilize ~age6; center-embed comprehension 8-12 tracks digit span) -- so pop genuinely NEEDS complex/buried input, the brain too. (2) The brain gets closure from PROSODY (pauses, final-lengthening, pitch-reset; prosodic bootstrapping, Hawthorne-Gerken 2014) -- an INDEPENDENT perceptual cue, NOT learned from the task. **KEY: prosody is largely ABSENT from TEXT** (punctuation marks some clause boundaries but NOT the PP boundaries causing most buried-agreement errors, e.g. "keys TO THE CABINET are" has no comma). So on TEXT the pop MUST come from complex/buried supervision OR a syntactic parse = "structure supplied" = exactly 29455. **CONCLUSION: 29455 IS the brain-faithful text solution; 29456 is a brain-SHARED bound (autonomous-pop-from-agreement-text is impossible because the brain's pop-cue [prosody] isn't in text -- the brain doesn't do it either). Every remaining autonomous-text-CG path (punctuation-pop, parser-stack) reduces to structure-supplied or needs prosody/tree supervision. ARC RESOLVED, zero false CG.** NEXT (strategic, per honesty guard -- no 5th CG bar): BROADEN the working productive-generalization method (accumulator + structure-supplied) to OTHER reader capabilities per roadmap; OR the PROSODY path is real but needs SPEECH/audio input (ties to platform-may-expand-to-vision/audio [[project_platform_may_expand_to_vision_decision_bidirectional_feedback_relevant_2026-07-17]]). Recommend consolidate + broaden; await USER steer on prosody-audio vs broaden-on-text.
- **🎯 CURRENT DECISION POINT (b1/b2 arc COMPLETE + VET-clean; awaiting USER):** the arc resolved the frontier. b1 (29445): glass-box COMPOSES over given structure (VET-clean). b2 (29446): glass-box CANNOT INDUCE structure from raw data (VET-clean, corroborated). Substrate = COMPOSITION engine, NOT induction engine. **REFRAME (Director, presented to USER): induction-needs-gradient does NOT trade the RUNTIME invariant -- a gradient structure-inducer at FOUNDATION/build time is ALREADY authorized by the PIVOT anchor (foundation=any external tool; runtime=glass-box), producing structure the glass-box substrate composes over at runtime (b1). b1+b2 VALIDATE the pivot architecture.** USER decision (invariant-locked): (A) foundation-inducer [parser/edge-extractor at build] + glass-box-runtime-composition [RECOMMENDED, pivot-authorized, crosses the wall, keeps runtime value-prop]; (B) pure-taught structure only; (C) runtime-autonomous induction [only this trades the invariant; pivot marks optional]. **AWAITING USER (A/B/C).** Data-loss incident (CRLF-write + un-guarded checkout, lost 8 atoms) was CONTAINED + fully recovered + fix banked.

### THE SESSION ARC (clean narrative)

**GOAL:** build a learned, brain-faithful, glass-box in-substrate READER toward a CHAIN-GRADE (compositional generalization = handle held-out combinations where flat architectures fail). Glass-box = inspectable, no external LLM / no learned-opaque operator at inference (USER-LOCKED invariant).

**PHASE 1 -- MAPPED THE GLASS-BOX READER ENVELOPE TO A STRONG-MM CEILING (VET-confirmed at every axis):**
- **Compgen CG = CLOSED at strong-MM**, exhaustively (3 independent re-examinations: difficulty-invariance reframe, shared-vs-role-specific brain-check, geometry-ablation -- all -> MM). The systematicity is "free by construction of the fixed VSA algebra," NOT learned. Brain-checks: decode-coupling is structural (brain also WM-load-degrades); brain uses role-specific populations + geometry. Atoms 29425/27/29/32/33/37.
- **Cross-sentence comprehension = BLOCKED 3x** (UD-EWT + LitBank-identity + LitBank-state-tracking): no real-text task provably requires cross-sentence integration (identity locally-resolvable; state-tracking too rare + needs token-WSD).
- **WSD/sense-assignment = EXHAUSTED at the MFS/FREQUENCY ceiling** (3 nulls: fixed-frame 29434, fixed-supersense 29435, learned-collocation 29436). On natural text, context and frequency are correlated-by-construction so MFS captures ~all recoverable signal.
- **CROSS-DOMAIN FREQUENCY-WALL (deepest Phase-1 finding):** FIXED and LEARNED glass-box features REPEATEDLY don't beat frequency baselines (FB15k237 + reader-gate + 3 WSD). ARCHITECTURAL CAPSTONE: the scour design (fixed-VSA-bind + learned-front-end) is FREE-ALGEBRA-BOUND -> strong-MM ceiling, structurally cannot yield learned-composition CG.

**PHASE 2 -- USER UN-TERMINALED IT (07-22): CHASE THE LEAP.** Key USER corrections/insights ([[feedback_chase_the_leap_learned_glue_is_glassbox_atomize_sleep_puns_testbed_USER_2026-07-22]]):
- Learned gluing is NOT a black box -- it's the EXISTING glass-box atomize-then-sleep-consolidate loop (the one that WORKED for grammar generalization) applied to COMPOSITION. So the leap is pursued WITHIN the glass-box invariant, no tradeoff. (Director had over-reserved, assuming neural MLC.)
- PUNS/double-meaning words = the testbed: they FORCE holding multiple interpretations + valuing the low-frequency reading = break the frequency-wall by construction, AND probe whether the glue genuinely USES meaning.
- "Sentence-hypervector LANDING SPOT in the attractor landscape = plausibility" = the coherence-alarm readout; it read NULL before (settling_fix) because the landscape was never SHAPED by plausibility -- the learned-glue is exactly what would shape it. Landing-spot(readout) + learned-glue(training) = one loop.
- NUANCE (Director, load-bearing): case+sleep GENERALIZED for grammar (rules) but FAILED for word-MEANING (per-item facts). USER bets COMPOSITION is grammar-like; puns adjudicate.

**PHASE 3 -- THE LEAP ARC (current frontier):**
1. **Pun coherence-alarm PROBE (29439, MM, GREEN-LIGHT):** compose a pun under the dominant-WRONG meaning -> incoherence signal FIRES 0.985 vs correct/control 0.000, 19/19, p=3.8e-6, must-fails fire. The frequency-wall-breaking re-search trigger has a real signal. CAVEAT: reduces to selectional-LOOKUP (correct sense's WordNet-lexname carries the verb's required feature) = MM prerequisite, not yet learned-composition. Raw attractor-landing signal NULL (unshaped landscape).
2. **LEAP BUILD -- learned-composition-glue via atomize+sleep (29440, MM, CG REFUTED):** the strongest candidate of the session. It LEARNED verb->feature + generalized to verb-disjoint held-out incl VerbNet-UNCOVERED verbs (0.85 vs lookup 0.0); the SIGN-FLIP fired the LEARNED way (real-beats-random +0.34, OPPOSITE of the free-algebra signature) for the first time; scramble-collapsed (not memorization); 5 of 6 CG gates fired. **BUT the adversarial VET REFUTED it decisively: it BUILT the missing WordNet-similarity-kNN control -> it matches the "learned" arm 70/70 exactly, and PROVED a mechanistic identity -- the linear (Hebbian) atomize+sleep loop reduces ANALYTICALLY to a fixed-WordNet-similarity vote = learns nothing beyond the KB tree = structured-lookup-in-disguise = same free-from-fixed-structure trap. KEY LESSON: the sign-flip discriminator is INSUFFICIENT -- it only proves meaning-similarity is USED (the parameter-free similarity-vote shares it), NOT that a rule was LEARNED. That conflation is where the false CG hid; Director's near-over-read caught by the VET's control.**
3. **BRAIN-CHECK on the refutation (adversarial, guarded my mis-specification pattern):** the "beat-similarity" CG bar is BRAIN-FAITHFUL / VALID (NOT mis-specified) -- humans generalize composition to SEMANTICALLY-DISSIMILAR novel combinations (wug test; Marcus-1999 infant rule-learning where similarity-nets fail EXACTLY like our 29440; Lake-Baroni meaning-free primitives; Gentner relational-shift). => the leap is genuinely UNFINISHED; the brain's mechanism = RELATIONAL RULE-RESOLUTION (glass-box: settling + role-binding + conflict-gate), so fork A is glass-box-feasible (no invariant tradeoff).
4. **FORK-A viability PROBE -- relational-vs-similarity conflict (29441, MM, GENUINE-BUT-NARROW):** on a synthetic conflict task (correct answer dissimilar from training -> surface-similarity fails by construction), a role-binding relational feature C=0.997 BEATS surface-kNN 0.545 + the linear-loop 0.492 (both at chance). **This proves the substrate CAN EXPRESS a beyond-similarity RELATIONAL feature that surface approaches cannot -- a real representational escape from the wall -- AND explains 29440's collapse (its selectional rule was similarity-CO-EXTENSIVE).** BUT three confirmed caveats: (1) the lever is the REPRESENTATION not the LEARNER (kNN over the same relational features TIES at 0.998 -> nothing learned; feature hand-built via unbind-compare); (2) SYNTHETIC/construction-favorable (couldn't build a real conflict corpus -- real selectional rules are similarity-co-extensive); (3) iterative settling DEGRADES it (single-step unbind-compare quadratic is the nonlinearity). => NOT a CG.

### 🎯 THE GENUINE CG BAR (VET-named, precise) + OPEN FORK
A real chain-grade needs a **REAL-TEXT task where (a) the discriminating rule CONFLICTS with surface-similarity** (the nearest-similar item carries the WRONG label -- NOT co-extensive like selectional rules) **AND (b) the substrate LEARNS/discovers the relational feature from data** (weighted by the loop, NOT a hand-built unbind-compare), **judged against a FAIR baseline given the same raw input** -- so any win is attributable to LEARNING, not representation-engineering.
- **DEEP PICTURE (the session's synthesis):** SURFACE features -> similarity-wall (frequency / free-algebra / 29440 all one shape: glass-box linear/algebra/similarity reduces to the structure it's handed). ROLE-BINDING -> RELATIONAL features that genuinely ESCAPE surface-similarity (real lever, 29441). The CG = LEARN the relational feature on REAL rule-not-equal-similarity data. Open Q: does such a real-language phenomenon exist? Selectional rules are similarity-co-extensive -> the conflict likely lives in STRUCTURAL phenomena (syntax / anaphora / binding), not word-meaning.
- **USER FORK (awaiting):** (A) scope + build the real-text relational-conflict LEARNING test (the genuine remaining CG shot, glass-box, brain-faithful); (B) bank the mapped finding (linear/similarity reduces to structure; role-binding expresses beyond-similarity relations but learning-it-on-real-data is the open frontier).
- **FORK-A EXECUTED (2026-07-22): agreement-attraction real-text testbed BUILT + VALIDATED + first fair negative banked (29443, MM, VET-confirmed).** USER-directed fairness reframe = TAUGHT-then-generalize (teaching allowed; only the generalization must be the substrate's own learned weights). Two-stage probe on REAL Linzen 2016 corpus (14,761 items, held-out lexeme pools disjoint): **Stage-1 representability = 1.0** (head-number feature IS expressible via HRR binding at N=2048, given oracle POSITION not label -> fair scaffold; isolates the ceiling on the LEARNER not the representation). **Stage-2 = MIDDLE_BAND fair negative**: a fixed role-binding + **LINEAR** logistic readout learns a positional+function-word heuristic (rank-from-start +5.25, FW_PREP -1.66) that beats surface baselines in aggregate/conflict but does NOT clear head-tracking on the held-out subject-not-first subset (0.580 vs majority 0.627; structure_used=False). VET-confirmed fair (baselines bit-exact, discriminator can-fail + FIRED, one variable, real text; NOT construction-determined). **CRITICAL FRAMING (VET-locked): this used a LINEAR learner, NOT the atomize+sleep loop -> it RE-DERIVES atom 29440's linear->fixed-similarity limit ON REAL TEXT + adds representability=1.0; it does NOT test and does NOT refute the atomize+sleep leap.** The genuine leap-test = a NONLINEAR consolidation learner on this now-validated testbed is STILL PENDING (the existing atomize+sleep replay_cycle is LINEAR/Hebbian = would re-derive 29440; the real leap needs a nonlinear structure-induction learner, overlapping fork-B mechanism-level). DEEP PATTERN now doubly-confirmed on real data (grounding-linear 29438 + agreement-linear 29443): fixed/linear learners fail to INDUCE structure; representation is fine, the learner is the ceiling.
- **🧠 INDUCTION ARC (2026-07-22 late, USER-driven "don't give up"): the frontier = STRUCTURE-INDUCTION from raw data, exhaustively probed, then RESOLVED into the TEM-on-VSA build.** COMPOSITION works (b1 29445 glass-box replay-traversal ~doubles autonomous multi-hop keyless; but DBCA compound-divergence test 29447 = test-inconclusive/free-at-saturation). INDUCTION = the wall. FOUR fixed-code glass-box induction attempts on real Linzen agreement head-tracking, ALL HARD_FAIL (now AIRTIGHT): b2 ADIOS/ABL chunking (29446, banked); TGPH 12-symbol prediction (confounded-impoverished-input); fair dense real-word prediction (crosstalk); sparse real-word prediction (sparse fix WORKED -- prediction beats frequency +0.028 -- but head NOT in prediction residual); Elman representation-probe (fixed-code rep does NOT encode number, q=M⊙key≡key per 29440). **CONVERGENT FINDING (5 experiments + 3 lit-scans, UNANIMOUS not mixed): relational structure CANNOT be induced from scratch by local/glass-box rules (Hebbian=PCA per Oja; local-error PC either IS backprop [Rosenbaum 2022] or finds poorer/lower-rank reps [Garg-Vempala]; TEM/VSA/tensor-binding ALL require the relational SCAFFOLD architecturally PRE-GIVEN [Smolensky, Fodor-Pylyshyn]).** KEY CONSTRUCTIVE PIVOT: we ALREADY HAVE the scaffold (VSA role⊗filler binding = TEM's structure×content factorization); prior attempts put FIXED RANDOM codes inside it + Hebbian recall (=PCA/surface) and NEVER TRAINED the codes predictively. **TEM-on-VSA (the evidence-ENDORSED, brain-faithful build, USER: "implement like the brain does", running afa62031): pre-given VSA scaffold + PREDICTIVE objective + GRADIENT-trained codes INSIDE the scaffold (TEM recipe). INVARIANT: gradient@BUILD-time (pivot-authorized foundation) + glass-box@RUNTIME binding. Arms: TEM-on-VSA vs fixed-random-codes(fail-control) vs flat-gradient(does-scaffold-help). Novel-lexeme+shuffle eval, beat majority 0.6269. HARD_PASS=landmark→hardest VET.** This RESOLVES the A/B/C fork: (A) foundation-inducer is now concrete + substrate-native = gradient-train OUR OWN factorized codes, not outsource a parser. Arc negatives held-unbanked pending TEM-on-VSA (then bank the conclusion). Notes: [[unifying_synthesis_composition_works_induction_is_the_wall_2026-07-22]], [[literature_scan_leap_learner_binding_must_be_learned_content_dependent_2026-07-22]].

### 🎯 CURRENT STATE + NEXT STEPS (2026-07-22 latest -- AUTHORITATIVE; supersedes the arc above; self-contained for compaction recovery)
**THE ARC RESOLVED (all VET-clean/banked):** TEM-on-VSA HARD_FAIL (29448: gradient-trained codes in scaffold still tie majority on buried subjects -> wall is SELECTION not codes) -> attractor-select HARD_FAIL (29449: even nonlinear glass-box attractor selector ties -> wall is deeper than the selection MECHANISM) -> parse route HARD_FAIL (MST global-decode dep-parse: buried subject-id 0.30, decode-independent). **DESIGN AUDIT (USER "we ARE doing something wrong; brains do this") FOUND THE FLAW:** every approach (linear/trained-codes/attractor/parser) DEFAULTS TO A POSITIONAL heuristic (nearest/first noun = the attractor on buried cases) and NONE was given the one cue that separates subject from attractor = SYNTACTIC EMBEDDING DEPTH (the attractor sits inside a PP/clause marked by a function word; the subject is a main-clause noun). FLAWS: post-hoc whole-sentence SELECTION vs the brain's INCREMENTAL function-word-cued role-binding (Lewis-Vasishth/Wagers cue-based retrieval, established); missing the cheap deterministic depth cue; class-imbalance -> learners took the positional shortcut; the "no-parse/induce-it-yourself" fairness bar EXCLUDED the brain's actual method (the brain USES a parse -- composition not induction).
**THE GLASS-BOX LEVER FOUND (the session's positive result):** on the real-word agreement testbed (agreement_word_cache_v1, buried/SNF subset n=6425, majority 0.612), a DETERMINISTIC glass-box function-word-DEPTH rule -- **"subject = the depth-0 noun NEAREST the verb"** (skip PP/clause-embedded attractors, then take the compound head) -- scores **0.759 on BURIED subjects, beating the ENTIRE learned arc (~0.63) AND majority by +0.13-0.15.** (My first-pass "leftmost depth-0 noun" was WRONG at 0.528 -- HARD_FAIL of that specific rule; the correction is head-finality + clause-embedding + nearest-tiebreak. A learner GIVEN the depth feature also improves +0.083 -> depth is real usable signal never fed to any learner.) **CONFIRMED HARD_PASS (exp_agreement_glassbox_depth_rule_confirm_v1, commit ced755bdc, NOT banked yet):** nearest_depth0=0.759 (5 held-out splits) vs nearest_noun 0.551 (+0.208) vs majority 0.596 (+0.163) -- beats the entire arc; **ANTI-CHEAT depth-scramble FIRED: 0.759->0.530 (collapses BELOW nearest_noun) = the lift is genuinely DEPTH not position.** POS-depth arm (our offline tagger) 0.766. This is the FIRST glass-box result to beat the buried-subject wall. It is a DETERMINISTIC rule (glass-box, lexeme-free, no verb-number leak) -> NOT chain-grade (no learning); it PROVES the design-audit (missing cue = depth) + is the foundation for the agreement capability (depth->select->read). **HARDEST-VET SURVIVED -> BANKED atom 29450 (MM, cert_delta 0), composes 29443/29448/29449.** All 5 adversarial targets held (independent recompute): scramble-collapse reproduced (0.531 < nearest-noun 0.551 = lift is DEPTH not verb-adjacency); ZERO verb-number leakage (0/6425 selections change on target-flip); like-for-like margin +0.163 over same-subset held-out majority; POS-depth arm +0.007 = WITHIN NOISE (not over-claimed); framed as CAPABILITY not CG (deterministic rule, no learning). First arc cell to BEAT vs the three (29443/29448/29449) that tied.
**CAPABILITY MAP + ROADMAP (disk-verified, [[reader_capability_map_and_glassbox_improvement_roadmap_2026-07-22]]):** most of our NLP stack is UNDER-BUILT, not at-ceiling, and 5 prior claims were over-stated (disk truth: dep-parse 0.7875 [not 0.85]; POS 0.906 [not 0.97 -- that was a coarser tagset]; chunking 0.926; NER 0.654; WSD 0.569 single-annotator [drops to 0.21 on real text]; slot-fill 1.0 was synthetic). AT-CEILING/done: morphology/number. GENUINELY-HARD (need a different mechanism, NOT features): WSD-beyond-frequency, buried-agreement-without-structure, coref, cross-sentence comprehension.
**NEXT STEPS / BUILD PLAN (do after compaction; roadmap's 3 cross-cutting moves, all glass-box/gradient-free at runtime):**
  1. **CONFIRM + bank nearest-depth0** (in-flight a5f8145e); if it holds, it's the first buried-agreement win -- wire depth->select->read as the agreement capability.
  2. **#1 BUILD DONE (banked 29451, MM, VET-clean):** glass-box arc-eager transition parser (structured-perceptron + dynamic-oracle) lifted UAS **0.79 -> 0.811** (dynamic>static confirms the error-prop fix; learning curve MONOTONE +0.037 = flexible/improving property demonstrated) -- a REAL parser-boost. **BUT the "two birds" hope FAILED: buried subject-id did NOT improve** (learned parser 0.23 vs batch 0.27, n=55 small; and 0.23 << the DETERMINISTIC depth-rule's 0.759). LESSON (banked): learners take the POSITIONAL shortcut on rare class-imbalanced buried cases; explicit function-word cues don't. So **AGREEMENT STAYS on the deterministic depth rule (29450); the parser is a SEPARATE general-UAS infra win** -> points at a HYBRID reader (explicit structural priors + learning), the brain-faithful design. IN-FLIGHT AT COMPACTION (detached, survives -- POST-COMPACTION PICKUP): rich-feature push toward ~0.88 = cell `exp_depparse_transition_richfeat_cpu_v1` (Brown clusters K=32 + affix/word-shape feats), FULL training. RECOVERY: read `data/exp_depparse_transition_richfeat_cpu_v1/metrics.json` -- verdict = UAS(rich) vs base 0.811 vs batch 0.79; if it lifts toward ~0.88 -> skunkworks landed-VET + bank; then roadmap #3 (CRF global decode) OR return to the chain-grade compositional-generalization target (COGS/SCAN held-out role-combos) with the reader components now in hand (depth-rule agreement 29450 + transition parser 29451).
  3. **#2 = SHARED RICH-FEATURE LIBRARY** (Brown clusters, affixes, word-shape, gazetteers) -> lifts POS->0.97, NER->0.88 (its whole -0.23 gap is this), chunking->0.94, feeds the parser.
  4. **#3 = CRF-style GLOBAL DECODE** replacing per-token argmax across POS/chunk/NER/slot.
  BOTTOM LINE: biggest+cheapest wins are STRUCTURAL (parse), not meaning; fund the transition parser first (it also unblocks agreement). Infra note: our own POS/chunk models may be usable offline but nltk downloads FAIL offline (blocked the parser corpus-load earlier) -- fixing offline model availability unlocks foundation-grade structure under the pivot.

### PARALLEL / QUEUED
- **Grounding (29438 -> 29442, MM, LANDED + VET-clean):** the perception-MEANING step is REAL (content-aware grounding pixel-shuffle-SENSITIVE, blind shuffle-invariant; glass-box, no CNN) but gave NO lift through the ADDITIVE bind store (crosstalk bottleneck). **SHARDED-STORE probe (29442) CONFIRMED the crosstalk diagnosis, leakage-clean (skunkworks verified test-time scoring argmaxes over ALL shards; label only in the correctness tally, never routing; independent 5-seed bit-repro):** per-class sharded store recovers aware-over-blind -0.085 -> +0.123 (recovery-delta +0.208), un-stalls the vision thread. **VET DOWNWARD CORRECTIONS to the "STRONG" claim (honest):** (1) MECHANISM = the sharded store is a per-class summed-similarity PROTOTYPE classifier; with bipolar words the word-binding CANCELS in the score numerator, so sharding's dominant effect is crosstalk removal that lifts BOTH arms (raw 0.317->0.823, hog 0.232->0.947) and the +0.123 is the RESIDUAL HOG-shape advantage -- accurate to say "recovers grounding lift," bigger story is "prototype-scoring beats one crosstalk-laden superposition vector." (2) Clean perception-meaning dissociation rests on OLIVETTI (faces) ALONE; DIGITS is directionally-consistent (aob +0.074) but content-use dissociation is WEAK (hog shuffle-sens 0.026, pixel-separable). RECURRING THEME reconfirmed: additive-superposition crosstalk = the capacity limit across grounding + settling + compgen; sharding = the recurring fix. **IMPLICATION for foundation-imagery ingest: the viable store architecture is per-class/per-referent SHARDED (prototype), NOT a single additive-superposition store.**
- **SOFT-SHARDING (29444, MM, LANDED + VET-PASSED, store thread CONVERGED):** the USER-hypothesized fix -- SPARSE pattern-separated codes in ONE content-addressable store (no routing key, no partition, no walls) -- WORKS: aob dense -0.085 / hard-shard +0.123 / **soft-shard +0.093 (76% of the hard-shard ceiling), KEYLESS single-shot recall VET-confirmed in code.** So the discreteness costs (routing problem, cross-shard walls) ARE dissolvable. KEY corroboration of the deep pattern: recovery REQUIRES **LEARNED** separation (train-mean-centering; every fixed-RANDOM-sparse config FAILS, aob<=-0.017) -- content-dependent codes, not just sparse. VET survived the narrow-basin/sweep-luck risk: winner per-seed [+0.125,+0.125,+0.083,+0.100,+0.033] all-positive, ~2.75std>0, rand-vs-center split systematic. HONEST NUANCES (in atom): NARROW BASIN (needs learned-center + E4 expansion + modest f0.20 together, 1/16 configs); 76% not full; near-null on 10-class digits (+0.032, crosstalk scales with n_classes -> a 40-class-scale phenomenon). **STORE-THREAD CONVERGENCE: the reasoning store = a LEARNED-sparse-separated, KEYLESS, single content-addressable store (recovers most of the discrete benefit without any discreteness cost) -- this is the substrate the b1 composition learner runs over.** Sharding-downsides audit ([[sharding_downsides_disk_verified_audit_2026-07-22]]): fixed/known-map/sequential works; learned/autonomous/relational was the open frontier -> soft-sharding now closes the STORE half of it.

### KEY MEMORIES
[[feedback_chase_the_leap_learned_glue_is_glassbox_atomize_sleep_puns_testbed_USER_2026-07-22]], [[feedback_meaning_is_assignment_lookup_not_generalization_USER_2026-07-21]], [[feedback_disk_verify_agent_prior_art_claims_before_propagating_not_just_experiment_verdicts_2026-07-22]], [[reference_store_write_crlf_binary_and_git_commit_after_every_bank_2026-07-22]], [[feedback_dont_over_emphasize_mcguffey_one_testbed_not_the_focus_USER_2026-07-21]]. Full chronology: notes/director_BACKUP_ARCHIVE_full_session_chronology_2026-07-22.md. Key drills (07-22): puns_multiple_interpretation_learned_composition, is_learned_not_algebra_free_CG_bar_coherent, does_brain_composition_beat_semantic_similarity.
