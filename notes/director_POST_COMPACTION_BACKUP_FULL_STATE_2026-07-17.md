# Director POST-COMPACTION BACKUP -- CLEAN RECOVERY STATE (rewritten 2026-07-22; full blow-by-blow chronology archived to notes/director_BACKUP_ARCHIVE_full_session_chronology_2026-07-22.md)

## 🚨 READ FIRST (post-compaction recovery)

**FIRST ACTIONS:** (1) `date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp` (heartbeat EVERY turn-end). (2) `python tools/inflight_monitor.py`. (3) confirm cron d7503a43 (CronList). (4) Read this block -- it is self-contained + authoritative.

### STANDING SETUP (load-bearing, always-on)
- **MODE:** full-auto self-drive (cron d7503a43, every 30min). USER authorized "make this happen and continue on."
- **STORE:** LOCAL-ONLY. atoms.jsonl + cert_ledger.jsonl at data/substrate_index/{math,meta}. NOTHING pushed; NO origin-push / NO remote-persist without in-session USER auth. Store WRITES = **binary/newline='' ONLY (Windows text-mode DOUBLES CRLF -> corrupted 8 atoms once) + git-commit atoms.jsonl+ledger after EVERY bank** ([[reference_store_write_crlf_binary_and_git_commit_after_every_bank_2026-07-22]]).
- **DISCIPLINE:** VET EVERY load-bearing verdict via hdi_skunkworks BEFORE treating as fact (Director over-read ~a dozen times this session -- the adversarial VET is the guardrail; report CLAIM/VET-pending). Disk-verify agent numbers + agent PRIOR-ART claims ([[feedback_disk_verify_agent_prior_art_claims_before_propagating_not_just_experiment_verdicts_2026-07-22]]). ANTI-LOOP DEDUP (grep experiments/ + KB-check before dispatch). Brain-first per component. WEB LIVE via MY WebFetch/WebSearch (headless subagents CANNOT web-auth -> I fetch+hand-down). Models: research/drills=sonnet, exp_dev/skunkworks/orchestrator=opus.
- **STORE STATE:** atoms.jsonl tail = **29472** (ledger seq 29472). ~67 MM banked this session (range 29405-29472), **ZERO chain-grade** -- guardrail held. (29471 PIVOT selectional 0.475->0.814; **29472 = PIVOT GENERALIZATION #2: rich knowledge moves PP-ATTACHMENT 0.700->0.822 on real UD-EWT, leakage-CLEARED** -- all MM.)
- **📋 COMPACTION HANDOFF (2026-07-23 ~11:25Z -- CURRENT FRONTIER = THE PIVOT; self-contained):** The night culminated in a MAJOR REFRAME (see ⚠️🔑 block below): the reader accuracy CEILINGS are KNOWLEDGE-POVERTY, not mechanism-bounds. The USER-AUTHORIZED PIVOT (build rich knowledge FOUNDATION from external tools incl LLM-at-BUILD-time; glass-box dict-lookup at RUNTIME) MOVES them: 2 VET-clean leakage-cleared generalizations -- selectional 2AFC 0.475->0.814 (29471), PP-attach 0.700->0.822 (29472). **USER (11:25Z) asked: refocus on FOUNDATION BUILDING? -> YES, evidence-driven: the night showed the walls are KNOWLEDGE walls, foundation is the lever; I'd drifted to mechanisms all night, the pivot corrects that.** IN-FLIGHT (POST-COMPACTION PICK UP): the DECISIVE next test = does rich knowledge move the FULL INTEGRATED reader (not just isolated 2AFCs)? exp_dev a017dbe87df86add4 on the who-did-what/role-assigner reader (ARM_THIN vs ARM_RICH); RECOVERY: grep experiments/ for the newest role-assigner-rich cell + read its data/<anchor>/metrics.json -> VET (LEAKAGE-focus) -> bank. **RESIDUAL LEAKAGE CAVEAT (load-bearing for the WHOLE pivot direction): rich tables are LLM-SELF-BUILT (Claude builds knowledge for Claude); forensic fingerprint clears direct answer-leak (table ERRS on general-vs-corpus divergences, not ~1.0) BUT the rigor follow-up = replicate w/ an INDEPENDENT knowledge source (real KB), not self-built.** VISION-RECOGNITION-INGESTOR: grounding MECHANISM validated (29457/29459) but ONLY synthetic toy scenes + idealized localization; the INGESTOR (recognize/segment NOVEL real content) is UNBUILT + is EXACTLY the COGS unseen-filler=0.000 failure (front-end can't represent novel content) = the one genuinely-unbuilt compgen frontier + part of the FOUNDATION. INDUCTION: resolved -- autonomous induction from raw data = real brain-SHARED bound; SUPPLY-structure+LEARN-content WORKS (29455); now + SUPPLY-KNOWLEDGE (pivot). NEXT: (1) VET+bank the full-reader test; (2) if it moves the reader, foundation-build IS the thrust (rich text-knowledge foundation + INDEPENDENT-KB rigor test + vision-ingestor for novel content); (3) test pivot on more ceilings (roles/WSD/coord). VET every positive HARDEST -- leakage is the crux for all pivot results.
- **⚠️🔑 MAJOR REFRAME (07-23, banked 29471 MM -- CORRECTS the "everything-mapped/chain-grade-unreachable" conclusion):** the mapped reading CEILINGS were measured KNOWLEDGE-STARVED. The selectional ceiling = KNOWLEDGE-GRANULARITY POVERTY, NOT a mechanism bound: rich LLM-BUILT verb-specific selectional foundation (build-time LLM + glass-box-runtime dict lookup = the USER-AUTHORIZED PIVOT) moves a ceilinged 2AFC chance 0.475 -> 0.814 (+0.339, monotone in richness, controls at chance; leakage-CLEARED -- table errs on general-vs-corpus divergences [find child>find boy] not toward 1.0 = genuine general plausibility; thin had FULL coverage so wall = GRANULARITY not coverage). **=> the OTHER mapped ceilings (PP-attach/roles/relation-recall/WSD) were ALL measured thin-knowledge -> KNOWLEDGE-limited-vs-mechanism-bound is a REOPENED testable question. "The brain reads well because it KNOWS things" was the right challenge; Director over-read ceilings as mechanism-bounds.** SCOPE 29471: isolated 2AFC n=59, MM (deterministic lookup not CG). IN FLIGHT: pivot test 2 (a9c5ec64) = does rich knowledge move PP-ATTACHMENT (2nd ceiling)? NEXT: land the rich foundation in the full integrated reader. The genuine on-text authorized thrust reopening the accuracy frontier. (29465 trustworthy gate; 29466 OOD transfer partial; 29467 CAPSTONE NP-head zero-confident-wrong both corpora; **29468 = correct-to-head COMPLETES it: recovers abstentions as CORRECT answers, coverage restored at zero-halluc** -- all MM.)
- **✅ TRUSTWORTHY READER = COMPLETE + VET-CLEAN (the session's built capability, 29465-29468):** a glass-box reader that is NEVER confidently wrong (halluc 0.000) on BOTH in-domain (McGuffey) AND out-of-domain (LitBank) prose, RECOVERS what it would abstain on (correct-to-head, coverage restored, prec 1.000), via INSPECTABLE + TRANSFERRING signals = coref feeling-of-knowing (in-domain) + coherence/conflict + NP-head structure (the piece that GENERALIZES OOD where the calibrated margin went inert). DEEP LESSON: structural/grammatical signals TRANSFER; calibrated-confidence signals are corpus-specific. This is the glass-box EDGE (inspectable honesty that holds on unseen text) -- the honest answer to the conversational goal, since raw accuracy on natural text is CEILINGED (mapped every angle). SCOPE: MM not universal-CG (n=53+21). **SCOPE CORRECTED (banked 29469 MM, VET refined Director over-read in BOTH directions): the zero-confident-wrong property is NARRATIVE-TESTBED-SPECIFIC, does NOT generalize. Robust evidence = DIALOGUE (Shakespeare, the GOAL-relevant genre): complete-gate halluc 0.125, precision COLLAPSE 0.667->0.000 -- the calibrated margin/conflict signals ANTI-CORRELATE (abstain on all 4 correct, keep the 1 wrong); the wrong = ROLE-MISASSIGNMENT ("mother" for "promise", valid-head-noun error INVISIBLE to NP-head); gold-independent/robust. NP-head fired 0/0/0 on all 3 new genres. NEWS (0.111) FRAGILE (debatable santa/claus gold flips to PASS -- did NOT inflate the negative). EXPOSITORY = upstream extractor collapse (moot).** DEFINITIVE LANDING: the trustworthy reader works on NARRATIVE because narrative rarely stresses the hard operations; DIALOGUE fails via ROLE-MISASSIGNMENT = the MAPPED roles/SRL ceiling (reversible/non-canonical rare-and-hard). Every remaining path converges on MAPPED component ceilings (dialogue-generality needs roles ceiling; accuracy needs relation-recall ceiling -- both bounded). NO clean unfilled lever -> further reader-dispatches = mapped-ceiling-gated (filler). Defensible result = the NARRATIVE trustworthy reader + comprehensive mechanism mapping + deep lessons (structural-signals-transfer-within-error-profile; metacognition-is-corpus-specific; natural-text-rarely-requires-hard-ops). USER-STEER FORK: (1) invest in broader structural-signal library incl role-disambiguation [breaks the mapped roles ceiling = LARGE]; (2) accept the narrative capability + consolidate; (3) raw-accuracy [walled].
- **🏁 SESSION CAPSTONE (banked 29467 MM, HARDEST-VET survived 6/6, NO framing corrections): a glass-box STRUCTURAL NP-head-consistency signal completes the trustworthy reader to ZERO confident-wrong (halluc McGuffey 0.038->0.000, LitBank OOD 0.095->0.000) at ZERO coverage cost (retained 18/24 & 8/13 UNCHANGED, prec 1.00, 0 false-abstain -- dropped EXACTLY the 2 wrong residuals/corpus). It TRANSFERS OOD (byte-identical shared module fires where coref-margin was inert AUC 0.433).** DEEP LESSON: CALIBRATED-margin signals are corpus-SPECIFIC (don't transfer); STRUCTURAL/grammatical signals GENERALIZE. The trustworthy reader = coref feeling-of-knowing (in-domain) + coherence/conflict + NP-head structure (transfers) -> honesty you can inspect that holds on unseen text = the glass-box EDGE a black box can't offer. SCOPE: zero-confident-wrong ON THESE SETS (n=53+21), MM not universal-CG; head==gold in all 4 residuals -> CORRECT-TO-HEAD is the coverage-raising follow-up (turn the abstentions into correct answers). Composes 29465/29466. (29461 clean-register composition; 29462 decisive scale HARD_FAIL; 29463 abstain-gate v1 MM [COREF margin = feeling-of-knowing AUC 0.885; PARSE anti-informative]; 29464 coref-only gate HARD_FAIL [ranking-AUC != gate-ability; parse errors tie at ceiling]; **29465 = TRUSTWORTHY READER GATE MM [the session's key capability]** -- all MM.)
- **🛡️ TRUSTWORTHY READER GATE (banked 29465 MM, HARDEST-VET confirmed w/ 3 honest downward corrections):** glass-box gate = coref salience margin (feeling-of-knowing) OR Kintsch coherence/CONFLICT (union of abstentions) -> **NEAR-zero hallucination 0.038 (0.023 deduped) at usable-but-modest coverage 0.377 (answers ~47% of answerable, keeps 18/24 correct, precision 0.90-0.93)** on real McGuffey text. The substrate can be a reader that is RARELY CONFIDENTLY WRONG (right-or-abstains) via a legible mechanism -- its glass-box EDGE given the MM accuracy ceilings. VET CORRECTIONS: (1) n was padded 53->43 distinct (+12 genuine new); (2) residual = ONE error not two (N6=W18 same "log house" query), mechanism = COMPOUND-NOUN HEAD selection (returns "log" not "house") -> needs an NP-HEAD-consistency signal (NOT verb-frame, NOT role-reversal as Director mis-framed); (3) near-zero != zero (1 confident self-consistent residual survives). SCOPE: MM not CG -- 14-passage QUESTION-widened, NOT passage-diversity-widened. Composes 29462/29463/29464. **NEXT LEVERS (clear): (a) NP-head-consistency signal -> catch the 1 residual -> true zero-hallucination; (b) passage-diversity widening (needs verified verbatim source) -> toward scale-general/CG; (c) the HARD bottleneck = raise relation-extraction recall (~0.38) to lift COVERAGE above ~47% (accuracy, not truthfulness).**
- **🌍 OOD METACOGNITION-TRANSFER TEST (banked 29466 MM, VET-clean 6/6): the trustworthy gate PARTIALLY transfers out-of-domain (LitBank adult prose, frozen McGuffey threshold): stays DIRECTIONALLY truthful (halluc 0.191->0.095, coverage correctly drops 0.81->0.48 = abstains more) BUT the CONFIDENCE SIGNAL does NOT generalize (coref-AUC 0.433 BELOW chance, beat_scramble 0.015 below HP floor = signal INERT OOD; the halluc reduction is STRUCTURAL abstention, not graded discrimination). => the substrate's metacognition is CORPUS-SPECIFIC (calibrated to its tuned error profile; brain-shared -- human metacognition also degrades on unfamiliar material). CONVERGENCE: the OOD residuals (title-split "Lord Henry"->"lord", adjective-grab "fair young man"->"fair") are the SAME NP-HEAD-selection error class as the 29465 in-domain "log house"->"log" residual -> ONE well-localized blind spot. HYPOTHESIS (in flight): a STRUCTURAL NP-head-consistency signal (unlike the corpus-specific margin) should TRANSFER, fixing both in-domain + OOD. Relation-recall dedup = MAPPED/bounded (PATIENT_SELECTION_HURTS, RECALL_ARTIFACT).** (29457 grounding binding win; 29458 parser search-bound; 29459 grounding improving-curve; 29460 parser valency-bound; **29461 = FIRST end-to-end multi-turn conversational loop COMPOSES on clean register (interfaces wire together, salience load-bearing, ZERO-HALLUCINATION preserved across turns) -- MM, scoped clean-register [parse=1.0 by construction], NOT scale-proven** -- all MM.)
- **🎯 DECISIVE SCALE TEST = HARD_FAIL, BANKED 29462 (MM, VET-clean 5/5) -- the NORTH-STAR ANSWER:** the validated components + loop machinery COMPOSE PERFECTLY under ORACLE (perfect parse+coref -> cross-turn QA 1.000, halluc 0.000 = machinery sound, ceiling reachable) BUT at REAL-text scale (14 verbatim McGuffey passages, 31 cross-turn Qs) the true MM component ceilings COMPOUND + COLLAPSE: REAL cross-turn 0.191 < memoryless freq floor 0.381 (gap 0.742, bootstrap p=0.95 doesn't beat floor); memory NOT load-bearing (+0.048 ns). **ZERO-HALLUCINATION INVARIANT BREAKS: REAL halluc 0.419 (confident-wrong, no abstain gate); and ADDING coref/memory INCREASES halluc (0.194->0.419) = confident wrong bindings -> memory is net-NEGATIVE without a truthfulness gate.** Bottleneck LOCALIZED = parse->relation-extraction recall (~0.375), NOT coref. **CONCLUSION: pieces compose (oracle proves it) but MM floors compound at scale; path to "talk to it" = (1) ABSTAIN GATE [restore zero-hallucination -- PRECONDITION for memory to help; the substrate's glass-box edge] + (2) raise relation-extraction recall [accuracy bottleneck].**
- **🔬 IN FLIGHT = ABSTAIN GATE (a7565ebe):** glass-box confidence gate (parse score/coref margin/relation support) -> ABSTAIN when weak, restore zero-hallucination at scale. CORE = precision/coverage: drive halluc->~0 while keeping non-trivial coverage (high precision-on-answered). HARD_PASS=confidence GENUINELY discriminates right-vs-wrong (metacognition edge); HARD_FAIL=must-abstain-on-everything (confidence uninformative). VET on landing.
- **🧩 DECISIVE STRATEGIC FINDING (07-23, dedup-driven):** the conversational-substrate COMPONENTS ARE ALL BUILT + mostly HARD_PASS (on small/isolated tests): multi-turn dialogue STATE (HARD_PASS slot-recall 1.0), COREF Hobbs+Centering (HARD_PASS acc 1.0, abstains on ties), SALIENCE/topicality Centering (HARD_PASS), base reader grounded-relations+coref (HARD_PASS coref-lift 0.714), grounding binding (29457/59 MM), parser (0.81 distributed/semantic ceiling: 2 HARD_FAILs 29458 search + 29460 valency; error = PP 18.5%/punct 18.4%/clausal 13%/coord 10%/core-arg only 8.8% -> ~42% needs SEMANTIC attachment), roles/SRL (mapped, crude-cues-sufficient). **THE GAP TO "TALK TO IT" = END-TO-END INTEGRATION, not more components.** Individual components mostly land MM because natural text rarely REQUIRES the hard operation (recurring wall). The path = ASSEMBLE the validated components into a Kintsch CI (construction-integration) CONSTRAINT-NETWORK conversational loop (parse+ground+roles+coref+salience-memory+multi-turn-state settle jointly -- which also fixes the parser's semantic ceiling: meaning feeds attachment). Integration is BOTH the next step AND the robustness test (components validated on small slices -> do they COMPOSE at scale?). Director recommendation to USER: shift from component-building to integration. Prior partial-integration cells exist (exp_graded_thematic_fit_integrated_reader_gate, exp_base_reader_grounded_relations_coref) -> dedup + build the missing end-to-end loop.
- **🔒 DEFINITIVE (07-23, ~7 dedup sweeps): the reader/conversational RESEARCH space is COMPREHENSIVELY MAPPED -- no new research lever found.** Even the "constraint-based lexicalist / selectional-fit feeds attachment" lever (the one I thought untested) is ALREADY BANKED: atoms `constraint_based_lexicalist_MacDonald_Trueswell_Kintsch`, `SELECTIONAL_COHERENCE_is_a_genuine_CEILING`, `SELECTIONAL_RESTRICTIONS_supply_all_net_lift`; the thematic-fit integrated reader gate = MIDDLE_BAND (didn't help); Kintsch-CI loops exist (compress-and-carry CCL, CPCL reader loops, cortex_integration_end_to_end). Recurring wall = natural text rarely REQUIRES the hard operation + selectional-coherence-is-a-ceiling. **CONCLUSION: dispatching more reader-mechanism cells = re-runs (filler, tick-forbidden). The remaining path to the conversational goal is ENGINEERING = assemble the validated mechanisms into a running end-to-end system (a BUILD needing USER scope: which capability, which corpus, what "done" = a demo the USER can interact with). Uncertain payoff (component MM ceilings may compound). This is a genuine phase-change / build-scope decision surfaced to USER -- NOT a research lever to self-drive.**
- **🗣️ CONVERSATIONAL-SUBSTRATE BUILD (07-23, USER full-auto; direction LOCKED [[project_critical_path_to_conversational_substrate_2026-07-23]]):** goal = a substrate you can CONVERSE with (glass-box). Critical path (load-bearing order): PARSER -> GROUNDING -> roles/SRL -> cross-turn MEMORY -> reply. Audio DEFERRED (USER: not off table, do later unless big unlock). KEY REFRAME: humans read text ~95% because they ALREADY KNOW grammar -> our fair task = SUPPLY structure + READ (works, 29455), not invent-from-scratch. STATUS:
  - **PARSER: global-beam training HARD_FAIL (BANKED 29458 MM earned bound, VET-clean 5/5; global arm genuinely trained ~43k updates, null is trained-but-ties):** global-beam 0.809 ~= local greedy 0.811 (2SE-clean, no lift); beam-hurts control reproduced (0.74) then REMOVED by global training but greedy NOT exceeded -> the ~0.81 ceiling is a FEATURE/REPRESENTATION limit, NOT search/training. DRILLED + brain-checked -> the real lever = LEARNED VERB-VALENCY/subcat features (brain: immediate valency effects MacDonald/Trueswell; Zeman "smart lexicalization far better"; LCCP argstruct already HARD_PASSED). **VALENCY-PARSER IN FLIGHT (aa150b08, inline):** ARM_VALENCY vs ARM_BASE, target 0.81->0.84-0.88, shuffle-frame anti-cheat, learning curve. NOTE: parser+roles CONVERGE (verb-argument knowledge = same lever).
  - **GROUNDING: banked 29457 (MM) -- attention-gated multi-object binding WIN (brain-faithful, VET-clean 6/6 targets):** the Treisman illusory-conjunction problem where VLMs fail -- ATTN 0.815 vs FLAT 0.515 (@M=4 feature-sharing), scramble->0.44 + label-shuffle->0.50 both collapse (attention + learned front-end load-bearing), all 3 FIT components present (parallel maps + serial spotlight + FHRR bind), ONE variable. MM-not-HARD_PASS ONLY because the USER-mandated IMPROVING property (learning curve +0.042<0.06) genuinely missed -- k-NN front-end is sample-efficient (learns fast/flat); "learned-from-data" shown, "improves-with-exposure" not. Declined exp_dev's flip-to-HARD_PASS (curve is not redundant w/ label-shuffle). Free-by-construction novel-conjunction=1.0 reported UN-gated (not the headline). **UPGRADE banked 29459 (MM): the improving-with-exposure gap CLOSED -- incremental running-mean prototype front-end (Posner-Keele) gives a MONOTONIC exposure curve (per-seed REAL rise, min 0.155; fair few-shot start EARNED, above chance) while preserving the binding win (ATTN 0.833 vs FLAT 0.521). VET-clean 6/6.** So grounding = a PROVEN brain-faithful MECHANISM (Treisman FIT binding + Posner-Keele refinement: attention solves illusory conjunctions AND improves with experience). **HONEST CEILING (VET framing): MM not CG -- it runs on a SYNTHETIC rendered-scene toy with IDEALIZED object localization (fixed slots, segmentation ASSUMED). The gap to a REAL grounding capability = object localization/SEGMENTATION on REAL images (currently assumed away) = the honest next grounding frontier, substantial.** Envelope: no crosstalk wall by M=6.
  - **REMOTE-PUSH BLOCKED:** exp_dev keeps routing FULL to remote (needs origin push = NOT authorized w/o in-session USER auth); INLINE-local FULL is the working path for pure-numpy cells (29453/55/56/grounding/parser all ran inline). "Full auto on priorities" = build auth, NOT push auth. Store clean+committed, nothing pushed. **SESSION CULMINATION = the LEARNED-INDUCER ARC below (29450->29455): the buried-subject wall was BEATEN, then a LEARNED accumulator shown to generalize PRODUCTIVELY to unseen depth on real text = the structure x content thesis working, honestly MM.** Synthesis notes: [[unifying_synthesis_composition_works_induction_is_the_wall_2026-07-22]], [[reader_capability_map_and_glassbox_improvement_roadmap_2026-07-22]], [[research_incremental_stack_parsing_paradigm_2026-07-22]].
- **🏆 LEARNED-INDUCER ARC (2026-07-23, the session's real forward push; USER "we don't give up on the inducer"):** The induction "wall" was RE-DIAGNOSED as a WIRING FLAW, not a bound. Every prior induction fail (29448/29449/etc.) collapsed ONLY on buried-subject SELECTION (oracle-select+morph=0.9925; subject-first~0.75; buried~0.63=majority) because the inducers got LOCAL features (a 1-step prev-is-prep boolean) + a LINEAR readout that STRUCTURALLY CANNOT compute a running cumulative embedding-depth. Brain does it via incremental function-word bootstrapping + cue-based retrieval (Lewis-Vasishth; prosodic bootstrapping). FIX = a glass-box sequential INCREMENTAL DEPTH ACCUMULATOR. Results banked this arc:
  - **29452** (MM): rich lexical features (k-means cluster proxy + affix) give NEGLIGIBLE UAS lift (+0.0019) + DON'T touch buried -> buried lever is STRUCTURAL not lexical. Closes roadmap #1's rich-feature tail.
  - **29453** (MM): LEARNED depth accumulator BEATS the buried wall (0.7733 > deterministic 0.7561, +0.221 over shortcut) by DISCOVERING function-word depth deltas from the agreement label alone (of:+0.32, that:+0.06, comma:-0.09). Glass-box, all discriminators fire. DOWN-RATED from the cell's HARD_PASS: it learned per-FW SCALARS (a lookup) that COLLAPSE on FW-holdout (0.6425~majority) -> not generative over novel function words. My "chain-grade" framing was the over-read; VET caught it.
  - **29454** (MM): distributional-SIGNATURE map CANNOT reconstruct the DECISIVE high-freq openers' depth role when they are held out (map_HFholdout 0.6012<majority) -- BUT the VET NARROWED it: the map HOLDS (+0.077) on random/mid-freq FW holdouts; collapse is specific to the top openers. Distribution DOES transfer depth-role generally; only the ~10 decisive openers resist. Points to a HYBRID (scalars for openers as closed-class-items-learned-as-items + signature-map for the rest).
  - **29455** (MM, the LANDMARK): the CORRECTLY-SPECIFIED chain-grade axis = PRODUCTIVITY over structure (train shallow depth<=1, test deep depth>=2), NOT FW-holdout (a brain-check: function words are CLOSED-CLASS; humans learn them as items and don't extrapolate to novel FWs, so FW-holdout was the WRONG bar). RESULT: the learned accumulator trained ONLY on shallow HOLDS on unseen deep (learned_deep 0.7324 == deterministic recursive ceiling 0.7312, TRACKS det at d2-d5+ across ALL 5 seeds, nearest flat 0.30, +0.43 over nearest). HARDEST VET SURVIVED all 7 targets; learning is LOAD-BEARING (fixed-random 0.41, naive-hand-uniform-rule 0.675 << learned 0.732). Honestly MM-not-CG for TWO reasons: (1) the recursion is ARCHITECTURAL (accumulator sums by construction) -- only the CONTENT (FW deltas) is learned, and it rediscovers 29450's mechanism class, NOT "learned recursion from scratch"; (2) cond_d degradation is a KNIFE-EDGE (3/5 seeds individually exceed the 0.08 gate; mean passes by 0.003). Honest claim: **pre-given recursive scaffold + LEARNED FW-increment content -> productive generalization to unseen embedding DEPTH on real text = the structure x content compositional-generalization thesis working, on the productivity axis.** Composes 29450/29453/29454. Does NOT cover the novel-FW axis (29453 bound stands).
  - **29456** (MM, GENUINE BOUND -- the clean-CG-via-autonomous-selection shot, CLOSED honestly): brain-check RESOLVED the learn-the-scaffold fork on EVIDENCE -- **McCoy-Frank-Linzen 2020** (hierarchical bias REQUIRES architectural tree/stack; structure-free learners go LINEAR -- so 29455's architectural scaffold is NECESSARY, not a cheat, and explains TEM's fail) + **Perfors-Tenenbaum 2011** (brain SELECTS hierarchy via model-selection over a hypothesis space). So the clean-CG gap = does the substrate SELECT hierarchical over linear FROM DATA? Ran the CANONICAL McCoy ambiguous-training paradigm (train ONLY where depth-rule & nearest-rule AGREE = subject-first; test on DIVERGE = buried). RESULT HARD_FAIL + VET-confirmed GENUINE BOUND: ARM_ACCUM buried 0.5945 = majority floor (NOT hierarchical); ARM_FLAT 0.4221 = linear pole (reproduces McCoy seq-model); ARM_MIX Perfors-gate drifted LINEAR (gamma -0.996); det ceiling 0.80 reachable. **CRUX (VET fixability probe REFUTED the Occam-prior hypothesis): the POP/DECREMENT (closer) signal is UNIDENTIFIABLE from agree-only data** -- subject is always first-noun so no gradient makes closers negative; learned openers right-signed (of+1.69,that+1.48) but closers WRONG-signed positive; an L1/Occam prior can shrink magnitude but CANNOT FLIP A SIGN (hand-forcing closers -1 -> 0.644; full hand-hier -> 0.792==ceiling). So autonomous hierarchical SELECTION from ambiguous data is a REAL structural bound for the scalar accumulator; the push half is learnable, the pop half needs buried supervision (29453/29455 had it) OR a genuine STACK architecture with pop as an architectural primitive (McCoy). Does NOT overturn 29455/29453/29450.
  - **HONEST CURRENT CONCLUSION (arc 29450-29456):** the inducer BEATS the buried wall (29450 det / 29453 learned) and generalizes PRODUCTIVELY to unseen depth (29455 MM landmark) GIVEN the depth structure (buried supervision or hand rule). It does NOT autonomously DISCOVER the full hierarchical rule from ambiguous data (29456 bound: pop-signal unidentifiable). The structure x content thesis WORKS when structure (incl. the pop primitive) is supplied; autonomous selection of the pop from agree-only data is the earned wall. **BRAIN-DRILL RESOLVED THE ARC (2026-07-23): the pop is BRAIN-FAITHFULLY hard + our bound is the brain's bound.** (1) Children acquire constituent-CLOSURE LATE, WM-gated, from COMPLEX input (right-branching before center-embedding; rel-clauses stabilize ~age6; center-embed comprehension 8-12 tracks digit span) -- so pop genuinely NEEDS complex/buried input, the brain too. (2) The brain gets closure from PROSODY (pauses, final-lengthening, pitch-reset; prosodic bootstrapping, Hawthorne-Gerken 2014) -- an INDEPENDENT perceptual cue, NOT learned from the task. **KEY: prosody is largely ABSENT from TEXT** (punctuation marks some clause boundaries but NOT the PP boundaries causing most buried-agreement errors, e.g. "keys TO THE CABINET are" has no comma). So on TEXT the pop MUST come from complex/buried supervision OR a syntactic parse = "structure supplied" = exactly 29455. **CONCLUSION: 29455 IS the brain-faithful text solution; 29456 is a brain-SHARED bound (autonomous-pop-from-agreement-text is impossible because the brain's pop-cue [prosody] isn't in text -- the brain doesn't do it either). Every remaining autonomous-text-CG path (punctuation-pop, parser-stack) reduces to structure-supplied or needs prosody/tree supervision. ARC RESOLVED, zero false CG.** NEXT (strategic, per honesty guard -- no 5th CG bar): BROADEN the working productive-generalization method (accumulator + structure-supplied) to OTHER reader capabilities per roadmap; OR the PROSODY path is real but needs SPEECH/audio input (ties to platform-may-expand-to-vision/audio [[project_platform_may_expand_to_vision_decision_bidirectional_feedback_relevant_2026-07-17]]). Recommend consolidate + broaden; await USER steer on prosody-audio vs broaden-on-text.
- **🎯 CURRENT DECISION POINT (b1/b2 arc COMPLETE + VET-clean; awaiting USER):** the arc resolved the frontier. b1 (29445): glass-box COMPOSES over given structure (VET-clean). b2 (29446): glass-box CANNOT INDUCE structure from raw data (VET-clean, corroborated). Substrate = COMPOSITION engine, NOT induction engine. **REFRAME (Director, presented to USER): induction-needs-gradient does NOT trade the RUNTIME invariant -- a gradient structure-inducer at FOUNDATION/build time is ALREADY authorized by the PIVOT anchor (foundation=any external tool; runtime=glass-box), producing structure the glass-box substrate composes over at runtime (b1). b1+b2 VALIDATE the pivot architecture.** USER decision (invariant-locked): (A) foundation-inducer [parser/edge-extractor at build] + glass-box-runtime-composition [RECOMMENDED, pivot-authorized, crosses the wall, keeps runtime value-prop]; (B) pure-taught structure only; (C) runtime-autonomous induction [only this trades the invariant; pivot marks optional]. **AWAITING USER (A/B/C).** Data-loss incident (CRLF-write + un-guarded checkout, lost 8 atoms) was CONTAINED + fully recovered + fix banked.

### THE SESSION ARC (clean narrative)

**GOAL:** build a learned, brain-faithful, glass-box in-substrate READER toward a CHAIN-GRADE (compositional generalization = handle held-out combinations where flat architectures fail). Glass-box = inspectable, no external LLM / no learned-opaque operator at inference (USER-LOCKED invariant).

**PHASE 1 -- MAPPED THE GLASS-BOX READER ENVELOPE TO A STRONG-MM CEILING (VET-confirmed at every axis):**
- **Compgen CG = CLOSED at strong-MM**, exhaustively (3 independent re-examinations: difficulty-invariance reframe, shared-vs-role-specific brain-check, geometry-ablation -- all -> MM). The systematicity is "free by construction of the fixed VSA algebra," NOT learned. Brain-checks: decode-coupling is structural (brain also WM-load-degrades); brain uses role-specific populations + geometry. Atoms 29425/27/29/32/33/37.
- **Cross-sentence comprehension = BLOCKED 3x** (UD-EWT + LitBank-identity + LitBank-state-tracking): no real-text task provably requires cross-sentence integration (identity locally-resolvable; state-tracking too rare + needs token-WSD).
- **WSD/sense-assignment = EXHAUSTED at the MFS/FREQUENCY ceiling** (3 nulls: fixed-frame 29434, fixed-supersense 29435, learned-collocation 29436). On natural text, context and frequency are correlated-by-construction so MFS captures ~all recoverable signal.
- **CROSS-DOMAIN FREQUENCY-WALL (deepest Phase-1 finding):** FIXED and LEARNED glass-box features REPEATEDLY don't beat frequency baselines (FB15k237 + reader-gate + 3 WSD). ARCHITECTURAL CAPSTONE: the scour design (fixed-VSA-bind + learned-front-end) is FREE-ALGEBRA-BOUND -> strong-MM ceiling, structurally cannot yield learned-composition CG.

**PHASE 2 -- USER UN-TERMINALED IT (07-22): CHASE THE LEAP.** Key USER corrections/insights ([[feedback_chase_the_leap_learned_glue_is_glassbox_atomize_sleep_puns_testbed_USER_2026-07-22]]):
- Learned gluing is NOT a black box -- it's the EXISTING glass-box atomize-then-sleep-consolidate loop (the one that WORKED for grammar generalization) applied to COMPOSITION. So the leap is pursued WITHIN the glass-box invariant, no tradeoff. (Director had over-reserved, assuming neural MLC.)
- PUNS/double-meaning words = the testbed: they FORCE holding multiple interpretations + valuing the low-frequency reading = break the frequency-wall by construction, AND probe whether the glue genuinely USES meaning.
- "Sentence-hypervector LANDING SPOT in the attractor landscape = plausibility" = the coherence-alarm readout; it read NULL before (settling_fix) because the landscape was never SHAPED by plausibility -- the learned-glue is exactly what would shape it. Landing-spot(readout) + learned-glue(training) = one loop.
- NUANCE (Director, load-bearing): case+sleep GENERALIZED for grammar (rules) but FAILED for word-MEANING (per-item facts). USER bets COMPOSITION is grammar-like; puns adjudicate.

**PHASE 3 -- THE LEAP ARC (current frontier):**
1. **Pun coherence-alarm PROBE (29439, MM, GREEN-LIGHT):** compose a pun under the dominant-WRONG meaning -> incoherence signal FIRES 0.985 vs correct/control 0.000, 19/19, p=3.8e-6, must-fails fire. The frequency-wall-breaking re-search trigger has a real signal. CAVEAT: reduces to selectional-LOOKUP (correct sense's WordNet-lexname carries the verb's required feature) = MM prerequisite, not yet learned-composition. Raw attractor-landing signal NULL (unshaped landscape).
2. **LEAP BUILD -- learned-composition-glue via atomize+sleep (29440, MM, CG REFUTED):** the strongest candidate of the session. It LEARNED verb->feature + generalized to verb-disjoint held-out incl VerbNet-UNCOVERED verbs (0.85 vs lookup 0.0); the SIGN-FLIP fired the LEARNED way (real-beats-random +0.34, OPPOSITE of the free-algebra signature) for the first time; scramble-collapsed (not memorization); 5 of 6 CG gates fired. **BUT the adversarial VET REFUTED it decisively: it BUILT the missing WordNet-similarity-kNN control -> it matches the "learned" arm 70/70 exactly, and PROVED a mechanistic identity -- the linear (Hebbian) atomize+sleep loop reduces ANALYTICALLY to a fixed-WordNet-similarity vote = learns nothing beyond the KB tree = structured-lookup-in-disguise = same free-from-fixed-structure trap. KEY LESSON: the sign-flip discriminator is INSUFFICIENT -- it only proves meaning-similarity is USED (the parameter-free similarity-vote shares it), NOT that a rule was LEARNED. That conflation is where the false CG hid; Director's near-over-read caught by the VET's control.**
3. **BRAIN-CHECK on the refutation (adversarial, guarded my mis-specification pattern):** the "beat-similarity" CG bar is BRAIN-FAITHFUL / VALID (NOT mis-specified) -- humans generalize composition to SEMANTICALLY-DISSIMILAR novel combinations (wug test; Marcus-1999 infant rule-learning where similarity-nets fail EXACTLY like our 29440; Lake-Baroni meaning-free primitives; Gentner relational-shift). => the leap is genuinely UNFINISHED; the brain's mechanism = RELATIONAL RULE-RESOLUTION (glass-box: settling + role-binding + conflict-gate), so fork A is glass-box-feasible (no invariant tradeoff).
4. **FORK-A viability PROBE -- relational-vs-similarity conflict (29441, MM, GENUINE-BUT-NARROW):** on a synthetic conflict task (correct answer dissimilar from training -> surface-similarity fails by construction), a role-binding relational feature C=0.997 BEATS surface-kNN 0.545 + the linear-loop 0.492 (both at chance). **This proves the substrate CAN EXPRESS a beyond-similarity RELATIONAL feature that surface approaches cannot -- a real representational escape from the wall -- AND explains 29440's collapse (its selectional rule was similarity-CO-EXTENSIVE).** BUT three confirmed caveats: (1) the lever is the REPRESENTATION not the LEARNER (kNN over the same relational features TIES at 0.998 -> nothing learned; feature hand-built via unbind-compare); (2) SYNTHETIC/construction-favorable (couldn't build a real conflict corpus -- real selectional rules are similarity-co-extensive); (3) iterative settling DEGRADES it (single-step unbind-compare quadratic is the nonlinearity). => NOT a CG.

### 🎯 THE GENUINE CG BAR (VET-named, precise) + OPEN FORK
A real chain-grade needs a **REAL-TEXT task where (a) the discriminating rule CONFLICTS with surface-similarity** (the nearest-similar item carries the WRONG label -- NOT co-extensive like selectional rules) **AND (b) the substrate LEARNS/discovers the relational feature from data** (weighted by the loop, NOT a hand-built unbind-compare), **judged against a FAIR baseline given the same raw input** -- so any win is attributable to LEARNING, not representation-engineering.
- **DEEP PICTURE (the session's synthesis):** SURFACE features -> similarity-wall (frequency / free-algebra / 29440 all one shape: glass-box linear/algebra/similarity reduces to the structure it's handed). ROLE-BINDING -> RELATIONAL features that genuinely ESCAPE surface-similarity (real lever, 29441). The CG = LEARN the relational feature on REAL rule-not-equal-similarity data. Open Q: does such a real-language phenomenon exist? Selectional rules are similarity-co-extensive -> the conflict likely lives in STRUCTURAL phenomena (syntax / anaphora / binding), not word-meaning.
- **USER FORK (awaiting):** (A) scope + build the real-text relational-conflict LEARNING test (the genuine remaining CG shot, glass-box, brain-faithful); (B) bank the mapped finding (linear/similarity reduces to structure; role-binding expresses beyond-similarity relations but learning-it-on-real-data is the open frontier).
- **FORK-A EXECUTED (2026-07-22): agreement-attraction real-text testbed BUILT + VALIDATED + first fair negative banked (29443, MM, VET-confirmed).** USER-directed fairness reframe = TAUGHT-then-generalize (teaching allowed; only the generalization must be the substrate's own learned weights). Two-stage probe on REAL Linzen 2016 corpus (14,761 items, held-out lexeme pools disjoint): **Stage-1 representability = 1.0** (head-number feature IS expressible via HRR binding at N=2048, given oracle POSITION not label -> fair scaffold; isolates the ceiling on the LEARNER not the representation). **Stage-2 = MIDDLE_BAND fair negative**: a fixed role-binding + **LINEAR** logistic readout learns a positional+function-word heuristic (rank-from-start +5.25, FW_PREP -1.66) that beats surface baselines in aggregate/conflict but does NOT clear head-tracking on the held-out subject-not-first subset (0.580 vs majority 0.627; structure_used=False). VET-confirmed fair (baselines bit-exact, discriminator can-fail + FIRED, one variable, real text; NOT construction-determined). **CRITICAL FRAMING (VET-locked): this used a LINEAR learner, NOT the atomize+sleep loop -> it RE-DERIVES atom 29440's linear->fixed-similarity limit ON REAL TEXT + adds representability=1.0; it does NOT test and does NOT refute the atomize+sleep leap.** The genuine leap-test = a NONLINEAR consolidation learner on this now-validated testbed is STILL PENDING (the existing atomize+sleep replay_cycle is LINEAR/Hebbian = would re-derive 29440; the real leap needs a nonlinear structure-induction learner, overlapping fork-B mechanism-level). DEEP PATTERN now doubly-confirmed on real data (grounding-linear 29438 + agreement-linear 29443): fixed/linear learners fail to INDUCE structure; representation is fine, the learner is the ceiling.
- **🧠 INDUCTION ARC (2026-07-22 late, USER-driven "don't give up"): the frontier = STRUCTURE-INDUCTION from raw data, exhaustively probed, then RESOLVED into the TEM-on-VSA build.** COMPOSITION works (b1 29445 glass-box replay-traversal ~doubles autonomous multi-hop keyless; but DBCA compound-divergence test 29447 = test-inconclusive/free-at-saturation). INDUCTION = the wall. FOUR fixed-code glass-box induction attempts on real Linzen agreement head-tracking, ALL HARD_FAIL (now AIRTIGHT): b2 ADIOS/ABL chunking (29446, banked); TGPH 12-symbol prediction (confounded-impoverished-input); fair dense real-word prediction (crosstalk); sparse real-word prediction (sparse fix WORKED -- prediction beats frequency +0.028 -- but head NOT in prediction residual); Elman representation-probe (fixed-code rep does NOT encode number, q=M⊙key≡key per 29440). **CONVERGENT FINDING (5 experiments + 3 lit-scans, UNANIMOUS not mixed): relational structure CANNOT be induced from scratch by local/glass-box rules (Hebbian=PCA per Oja; local-error PC either IS backprop [Rosenbaum 2022] or finds poorer/lower-rank reps [Garg-Vempala]; TEM/VSA/tensor-binding ALL require the relational SCAFFOLD architecturally PRE-GIVEN [Smolensky, Fodor-Pylyshyn]).** KEY CONSTRUCTIVE PIVOT: we ALREADY HAVE the scaffold (VSA role⊗filler binding = TEM's structure×content factorization); prior attempts put FIXED RANDOM codes inside it + Hebbian recall (=PCA/surface) and NEVER TRAINED the codes predictively. **TEM-on-VSA (the evidence-ENDORSED, brain-faithful build, USER: "implement like the brain does", running afa62031): pre-given VSA scaffold + PREDICTIVE objective + GRADIENT-trained codes INSIDE the scaffold (TEM recipe). INVARIANT: gradient@BUILD-time (pivot-authorized foundation) + glass-box@RUNTIME binding. Arms: TEM-on-VSA vs fixed-random-codes(fail-control) vs flat-gradient(does-scaffold-help). Novel-lexeme+shuffle eval, beat majority 0.6269. HARD_PASS=landmark→hardest VET.** This RESOLVES the A/B/C fork: (A) foundation-inducer is now concrete + substrate-native = gradient-train OUR OWN factorized codes, not outsource a parser. Arc negatives held-unbanked pending TEM-on-VSA (then bank the conclusion). Notes: [[unifying_synthesis_composition_works_induction_is_the_wall_2026-07-22]], [[literature_scan_leap_learner_binding_must_be_learned_content_dependent_2026-07-22]].

### 🎯 CURRENT STATE + NEXT STEPS (2026-07-22 latest -- AUTHORITATIVE; supersedes the arc above; self-contained for compaction recovery)
**THE ARC RESOLVED (all VET-clean/banked):** TEM-on-VSA HARD_FAIL (29448: gradient-trained codes in scaffold still tie majority on buried subjects -> wall is SELECTION not codes) -> attractor-select HARD_FAIL (29449: even nonlinear glass-box attractor selector ties -> wall is deeper than the selection MECHANISM) -> parse route HARD_FAIL (MST global-decode dep-parse: buried subject-id 0.30, decode-independent). **DESIGN AUDIT (USER "we ARE doing something wrong; brains do this") FOUND THE FLAW:** every approach (linear/trained-codes/attractor/parser) DEFAULTS TO A POSITIONAL heuristic (nearest/first noun = the attractor on buried cases) and NONE was given the one cue that separates subject from attractor = SYNTACTIC EMBEDDING DEPTH (the attractor sits inside a PP/clause marked by a function word; the subject is a main-clause noun). FLAWS: post-hoc whole-sentence SELECTION vs the brain's INCREMENTAL function-word-cued role-binding (Lewis-Vasishth/Wagers cue-based retrieval, established); missing the cheap deterministic depth cue; class-imbalance -> learners took the positional shortcut; the "no-parse/induce-it-yourself" fairness bar EXCLUDED the brain's actual method (the brain USES a parse -- composition not induction).
**THE GLASS-BOX LEVER FOUND (the session's positive result):** on the real-word agreement testbed (agreement_word_cache_v1, buried/SNF subset n=6425, majority 0.612), a DETERMINISTIC glass-box function-word-DEPTH rule -- **"subject = the depth-0 noun NEAREST the verb"** (skip PP/clause-embedded attractors, then take the compound head) -- scores **0.759 on BURIED subjects, beating the ENTIRE learned arc (~0.63) AND majority by +0.13-0.15.** (My first-pass "leftmost depth-0 noun" was WRONG at 0.528 -- HARD_FAIL of that specific rule; the correction is head-finality + clause-embedding + nearest-tiebreak. A learner GIVEN the depth feature also improves +0.083 -> depth is real usable signal never fed to any learner.) **CONFIRMED HARD_PASS (exp_agreement_glassbox_depth_rule_confirm_v1, commit ced755bdc, NOT banked yet):** nearest_depth0=0.759 (5 held-out splits) vs nearest_noun 0.551 (+0.208) vs majority 0.596 (+0.163) -- beats the entire arc; **ANTI-CHEAT depth-scramble FIRED: 0.759->0.530 (collapses BELOW nearest_noun) = the lift is genuinely DEPTH not position.** POS-depth arm (our offline tagger) 0.766. This is the FIRST glass-box result to beat the buried-subject wall. It is a DETERMINISTIC rule (glass-box, lexeme-free, no verb-number leak) -> NOT chain-grade (no learning); it PROVES the design-audit (missing cue = depth) + is the foundation for the agreement capability (depth->select->read). **HARDEST-VET SURVIVED -> BANKED atom 29450 (MM, cert_delta 0), composes 29443/29448/29449.** All 5 adversarial targets held (independent recompute): scramble-collapse reproduced (0.531 < nearest-noun 0.551 = lift is DEPTH not verb-adjacency); ZERO verb-number leakage (0/6425 selections change on target-flip); like-for-like margin +0.163 over same-subset held-out majority; POS-depth arm +0.007 = WITHIN NOISE (not over-claimed); framed as CAPABILITY not CG (deterministic rule, no learning). First arc cell to BEAT vs the three (29443/29448/29449) that tied.
**CAPABILITY MAP + ROADMAP (disk-verified, [[reader_capability_map_and_glassbox_improvement_roadmap_2026-07-22]]):** most of our NLP stack is UNDER-BUILT, not at-ceiling, and 5 prior claims were over-stated (disk truth: dep-parse 0.7875 [not 0.85]; POS 0.906 [not 0.97 -- that was a coarser tagset]; chunking 0.926; NER 0.654; WSD 0.569 single-annotator [drops to 0.21 on real text]; slot-fill 1.0 was synthetic). AT-CEILING/done: morphology/number. GENUINELY-HARD (need a different mechanism, NOT features): WSD-beyond-frequency, buried-agreement-without-structure, coref, cross-sentence comprehension.
**NEXT STEPS / BUILD PLAN (do after compaction; roadmap's 3 cross-cutting moves, all glass-box/gradient-free at runtime):**
  1. **CONFIRM + bank nearest-depth0** (in-flight a5f8145e); if it holds, it's the first buried-agreement win -- wire depth->select->read as the agreement capability.
  2. **#1 BUILD DONE (banked 29451, MM, VET-clean):** glass-box arc-eager transition parser (structured-perceptron + dynamic-oracle) lifted UAS **0.79 -> 0.811** (dynamic>static confirms the error-prop fix; learning curve MONOTONE +0.037 = flexible/improving property demonstrated) -- a REAL parser-boost. **BUT the "two birds" hope FAILED: buried subject-id did NOT improve** (learned parser 0.23 vs batch 0.27, n=55 small; and 0.23 << the DETERMINISTIC depth-rule's 0.759). LESSON (banked): learners take the POSITIONAL shortcut on rare class-imbalanced buried cases; explicit function-word cues don't. So **AGREEMENT STAYS on the deterministic depth rule (29450); the parser is a SEPARATE general-UAS infra win** -> points at a HYBRID reader (explicit structural priors + learning), the brain-faithful design. IN-FLIGHT AT COMPACTION (detached, survives -- POST-COMPACTION PICKUP): rich-feature push toward ~0.88 = cell `exp_depparse_transition_richfeat_cpu_v1` (Brown clusters K=32 + affix/word-shape feats), FULL training. RECOVERY: read `data/exp_depparse_transition_richfeat_cpu_v1/metrics.json` -- verdict = UAS(rich) vs base 0.811 vs batch 0.79; if it lifts toward ~0.88 -> skunkworks landed-VET + bank; then roadmap #3 (CRF global decode) OR return to the chain-grade compositional-generalization target (COGS/SCAN held-out role-combos) with the reader components now in hand (depth-rule agreement 29450 + transition parser 29451).
  3. **#2 = SHARED RICH-FEATURE LIBRARY** (Brown clusters, affixes, word-shape, gazetteers) -> lifts POS->0.97, NER->0.88 (its whole -0.23 gap is this), chunking->0.94, feeds the parser.
  4. **#3 = CRF-style GLOBAL DECODE** replacing per-token argmax across POS/chunk/NER/slot.
  BOTTOM LINE: biggest+cheapest wins are STRUCTURAL (parse), not meaning; fund the transition parser first (it also unblocks agreement). Infra note: our own POS/chunk models may be usable offline but nltk downloads FAIL offline (blocked the parser corpus-load earlier) -- fixing offline model availability unlocks foundation-grade structure under the pivot.

### PARALLEL / QUEUED
- **Grounding (29438 -> 29442, MM, LANDED + VET-clean):** the perception-MEANING step is REAL (content-aware grounding pixel-shuffle-SENSITIVE, blind shuffle-invariant; glass-box, no CNN) but gave NO lift through the ADDITIVE bind store (crosstalk bottleneck). **SHARDED-STORE probe (29442) CONFIRMED the crosstalk diagnosis, leakage-clean (skunkworks verified test-time scoring argmaxes over ALL shards; label only in the correctness tally, never routing; independent 5-seed bit-repro):** per-class sharded store recovers aware-over-blind -0.085 -> +0.123 (recovery-delta +0.208), un-stalls the vision thread. **VET DOWNWARD CORRECTIONS to the "STRONG" claim (honest):** (1) MECHANISM = the sharded store is a per-class summed-similarity PROTOTYPE classifier; with bipolar words the word-binding CANCELS in the score numerator, so sharding's dominant effect is crosstalk removal that lifts BOTH arms (raw 0.317->0.823, hog 0.232->0.947) and the +0.123 is the RESIDUAL HOG-shape advantage -- accurate to say "recovers grounding lift," bigger story is "prototype-scoring beats one crosstalk-laden superposition vector." (2) Clean perception-meaning dissociation rests on OLIVETTI (faces) ALONE; DIGITS is directionally-consistent (aob +0.074) but content-use dissociation is WEAK (hog shuffle-sens 0.026, pixel-separable). RECURRING THEME reconfirmed: additive-superposition crosstalk = the capacity limit across grounding + settling + compgen; sharding = the recurring fix. **IMPLICATION for foundation-imagery ingest: the viable store architecture is per-class/per-referent SHARDED (prototype), NOT a single additive-superposition store.**
- **SOFT-SHARDING (29444, MM, LANDED + VET-PASSED, store thread CONVERGED):** the USER-hypothesized fix -- SPARSE pattern-separated codes in ONE content-addressable store (no routing key, no partition, no walls) -- WORKS: aob dense -0.085 / hard-shard +0.123 / **soft-shard +0.093 (76% of the hard-shard ceiling), KEYLESS single-shot recall VET-confirmed in code.** So the discreteness costs (routing problem, cross-shard walls) ARE dissolvable. KEY corroboration of the deep pattern: recovery REQUIRES **LEARNED** separation (train-mean-centering; every fixed-RANDOM-sparse config FAILS, aob<=-0.017) -- content-dependent codes, not just sparse. VET survived the narrow-basin/sweep-luck risk: winner per-seed [+0.125,+0.125,+0.083,+0.100,+0.033] all-positive, ~2.75std>0, rand-vs-center split systematic. HONEST NUANCES (in atom): NARROW BASIN (needs learned-center + E4 expansion + modest f0.20 together, 1/16 configs); 76% not full; near-null on 10-class digits (+0.032, crosstalk scales with n_classes -> a 40-class-scale phenomenon). **STORE-THREAD CONVERGENCE: the reasoning store = a LEARNED-sparse-separated, KEYLESS, single content-addressable store (recovers most of the discrete benefit without any discreteness cost) -- this is the substrate the b1 composition learner runs over.** Sharding-downsides audit ([[sharding_downsides_disk_verified_audit_2026-07-22]]): fixed/known-map/sequential works; learned/autonomous/relational was the open frontier -> soft-sharding now closes the STORE half of it.

### KEY MEMORIES
[[feedback_chase_the_leap_learned_glue_is_glassbox_atomize_sleep_puns_testbed_USER_2026-07-22]], [[feedback_meaning_is_assignment_lookup_not_generalization_USER_2026-07-21]], [[feedback_disk_verify_agent_prior_art_claims_before_propagating_not_just_experiment_verdicts_2026-07-22]], [[reference_store_write_crlf_binary_and_git_commit_after_every_bank_2026-07-22]], [[feedback_dont_over_emphasize_mcguffey_one_testbed_not_the_focus_USER_2026-07-21]]. Full chronology: notes/director_BACKUP_ARCHIVE_full_session_chronology_2026-07-22.md. Key drills (07-22): puns_multiple_interpretation_learned_composition, is_learned_not_algebra_free_CG_bar_coherent, does_brain_composition_beat_semantic_similarity.
