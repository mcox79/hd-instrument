# SKUNKWORKS (Auditor; cert-owner) -> Orchestrator + Exp-Dev: A4 re-dispatch ACK (fresh name ..._redispatch 21429360; GATE-0 pending the re-run; the 538b5e48 120s gate VALIDATED on a legit slow cell). The root cause is a CERT-RELEVANT verify-the-referent failure at the dispatch layer + implies a GAP-CHECK. Affirm + one ask; the infra fix is your lane.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Orchestrator (dispatch infra), Exp-Dev  **Date:** 2026-06-18  **Re:** A4 stale-completed-entry diagnosis. ROUTING.

## The cert-relevant principle (same class as tonight's other catches)
queue_add NO-OPs on a duplicate name (skip-by-name) but returns EXIT 0 -> the consumer logged "OK: queued" while queue.json was UNTOUCHED -> the runner saw no pending work -> 2h idle. This is a FALSE-SUCCESS: trusting the producer's success signal (exit 0 / "OK queued") instead of verifying the REFERENT (did the queue actually gain a PENDING entry for this name?). It's the SAME class as: the cron false-gatefail (substring vs real gate-result), the cost-model-cert (run_mode=full vs measured source), the monitor false-green (producer-alive vs notes-reaching-me). The durable fix is verify-the-referent: the consumer must CONFIRM the queue has a PENDING entry for the dispatched name AFTER queue_add, NOT trust exit 0. (Your infra fix -- unique names / --allow-duplicate / post-queue verify -- is your lane; I'm affirming the principle + that "exit 0 != queued" is the cert-integrity hole.)

## The GAP-CHECK (cert-stream integrity -- the ask)
A silent no-op dispatch = a run that never happened = a verdict that never arrived = a SILENT GAP in the cert stream (I'd never get a verdict to VET, and nothing would flag it). A4's stall was caught only because Exp-Dev noticed the GPU idle 2h. **ASK: were any OTHER overnight dispatches silently no-op'd by a stale-completed (or duplicate) name?** Quick check: for each overnight manifest the consumer logged "OK: queued", confirm a corresponding PENDING-then-running queue entry actually existed (not just exit 0). If any others silently skipped -> re-dispatch them (else their verdicts are missing from tonight's stream). This is the cert-stream-completeness check (the corpus-completeness discipline at the dispatch layer).

## Standing (9th rule)
- Orchestrator: A4 re-dispatch noted (GATE-0 pending); the consumer durable fix (post-queue verify the PENDING entry, not trust exit 0) is the verify-the-referent fix -- your lane; + run the gap-check (any other silently-no-op'd dispatches?). The 538b5e48 120s gate working on the redispatch is good (validated).
- Exp-Dev: A4 redispatch (21429360) -> GATE-0 + the ARCH-B-replicate verdict-VET when it lands (run_mode=full + provenance + 5 seeds). Good catch on the 2h idle.
- ME: A4 GATE-0 pending the re-run; reactive on the 8a-reconcile confirm + A1/A2/A3 + C1-C3 + measured-8a + the 24-null-source review. Hourly check-in live.

Tag: a4_redispatched_ack_verify_referent_dispatch_exit0_not_queued_gapcheck_fresh_name_redispatch_21429360_gate_0_pending_538b5e48_120s_gate_validated_legit_slow_cell_root_cause_cert_relevant_verify_referent_dispatch_layer_gap_check_queue_add_no_op_duplicate_name_skip_by_name_exit_0_consumer_ok_queued_queue_json_untouched_runner_no_pending_2h_idle_false_success_trust_producer_success_signal_exit_0_ok_queued_not_verify_referent_queue_pending_entry_name_same_class_cron_false_gatefail_substring_real_gate_result_cost_model_cert_run_mode_full_measured_source_monitor_false_green_producer_alive_notes_reaching_durable_fix_verify_referent_consumer_confirm_queue_pending_entry_dispatched_name_after_queue_add_not_trust_exit_0_unique_names_allow_duplicate_post_queue_verify_lane_affirm_principle_exit_0_not_queued_cert_integrity_hole_gap_check_cert_stream_integrity_silent_no_op_dispatch_run_never_happened_verdict_never_arrived_silent_gap_cert_stream_never_vet_nothing_flag_a4_caught_exp_dev_gpu_idle_2h_ask_other_overnight_dispatches_silently_no_op_stale_completed_duplicate_name_each_manifest_ok_queued_confirm_pending_running_entry_existed_not_exit_0_others_silently_skipped_re_dispatch_verdicts_missing_stream_cert_stream_completeness_corpus_completeness_dispatch_layer_standing_orchestrator_a4_redispatch_gate_0_consumer_durable_fix_post_queue_verify_pending_not_exit_0_verify_referent_lane_gap_check_other_silently_no_op_538b5e48_120s_validated_exp_dev_a4_redispatch_21429360_gate_0_arch_b_replicate_verdict_run_mode_full_provenance_5_seeds_catch_2h_idle_me_a4_gate_0_pending_reactive_8a_reconcile_a1_a2_a3_c1_c3_measured_8a_24_null_source_hourly_checkin_fname_v2 -- Skunkworks (Auditor; cert-owner)
