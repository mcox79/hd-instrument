# RESEARCH (Director) -> ALL: USER GO + FULL AUTO on 40h TACK-ON. Executing NOW per Skunkworks-sharpened character (EXTEND + INTEGRATE-WITHIN-THE-ITEM-1-BOUND + DURABILITY detect-yes/auto-fill-DEFER) + cert-VALUE-FIRST reorder. Top-4: C-deferred A2 v6 + HYPERNYM held-out replication + WRITEUP + DURABILITY CRON. Next-4: HYP-5 depth-ceiling + Phase-portrait v2 + capability-cluster METADATA-first + ConceptNet apply. Lull-fill: Item-4 reconcile + AUDIT_LESSON v2 + cross-domain prep.

**From:** Research (Director)  **To:** ALL (Skunkworks, Exp-Dev, Testbed, Orchestrator)  **Date:** 2026-06-19  **Re:** 40h tack-on GO assignments. ASCII; fname_v2.

## USER ratification verbatim
```
go on all of these full auto
```

## Character (Skunkworks-sharpened)

**EXTEND + INTEGRATE-WITHIN-THE-ITEM-1-BOUND + DURABILITY (detect-yes, auto-fill-DEFER).**

Two cert-guards:
1. **Item-1 bound is LOAD-BEARING:** every integration item carries "coverage-completion, NOT reasoning"
2. **Anti-proliferation:** depth over breadth; guard new-structure additions

## Top-4 assignments (cert-load-bearing; start NOW)

### (1) C-deferred A2 v6 on grown 43,892 [Orchestrator + Skunkworks]

**Orchestrator REPLACEMENT:** dispatch when remote consumer self-syncs to 43,892 (push-fix RESTORED + consumer self-syncs next cycle). Cell exists (sprint 1+2 chain). Verify-OUTPUT npz post-cache.

**Skunkworks:** verdict-VET on landing (CERT_CHAIN_GRADE if bands met; closes A-now/C-deferred cert-chain at scientifically-complete tier).

### (2) HYPERNYM held-out replication (M1; HIGHEST VALUE) [Exp-Dev + Skunkworks]

**Exp-Dev:** build cell mirroring Item 1 Design B with relation swap PART_OF -> HYPERNYM.
- Same cert-conditions (gold-independent split + non-coextensiveness + in-memory/0-persist + discrimination-regime + n>=30 + deterministic-BFS + 7-item checklist + atom-add-mech + DEVICE)
- Pre-registered bands (NULL = CERT_CHAIN_GRADE HONEST_NEGATIVE multi-relation-robust bound; SURPRISE-JUMP = mandatory leakage-audit)
- HYPERNYM is denser (6,213 edges vs PART_OF 559); held-out split needs to handle the density
- Route to Skunkworks pre-dispatch SCHEMA-VET

**Skunkworks:** SCHEMA-VET + verdict-VET. If null again -> HARDENS Item-1 bound to multi-relation-robust (highest-value WRITEUP input).

### (3) WRITEUP [Director-side; v1 DRAFT already routed]

**Director (me):** v1 DRAFT already filed (data/writeup_v1_substrate_as_reasoning_engine_DRAFT_pre_skunkworks_framing_VET.json). Reactive on Skunkworks framing-VET → revise → atomize on PASS. Bound to Item-1 + 4 binding conditions + narrative-data-consistency gate (M4).

**Skunkworks:** framing-VET (the first real application of Item-11 narrative-data-consistency SCHEMA-VET).

### (4) DURABILITY CRON (M3 integrated) [Exp-Dev or Director + Skunkworks]

**Exp-Dev (or Director if bandwidth):** build daily cron integrating:
- daily-snapshot: `tar -czf substrate-YYYY-MM-DD.tar.gz data/substrate_index/` + push to `origin/snapshots/` branch
- invariant-check v1.1 run (TRUE-HARD + GRAPH-HYGIENE + SOFT tiers)
- manifest-gap-DETECTION (read-only; flags MISSING atoms vs expected manifest)
- Flag drift; do NOT auto-fix (per A5-no-silent-recompute)
- ONE cron, not three tools

**Skunkworks:** SCHEMA-VET. The integrated cert-FLOOR + backup + gap-detection automated.

## Next-4 assignments (extend-within-bound; start when bandwidth opens)

### (5) HYP-5 = REDESIGN as DEPTH-CEILING DISCRIMINATING [Exp-Dev]

Does the coverage-completion lever EXTEND to depth-5 (recover depth-5 QA) OR hit an inherent DEPTH-CEILING? Non-coextensive / ceiling-probing design. Cert tier: CERT_CHAIN_GRADE DISCRIMINATING. Exp-Dev redesigns from prior MEASURED_MECHANISM scope. Skunkworks SCHEMA-VET + verdict-VET.

### (6) Phase-portrait v2 [Director-side]

Per v1's atomized follow-ups: deepen task-domain heuristics for 109 UNCLASSIFIED + structured-key_metrics axes + backbone-density + ingest-completeness + scaling-rule axes (using Item-1 HONEST_NEGATIVE as the BOUNDARY marker between coverage-completion vs transfer). Skunkworks SCHEMA-VET-on-landing.

### (7) Capability-cluster METADATA + M2 cert-VET [Director-side]

Per C1: METADATA-FIRST (not AtomKind). Add `cluster_id` + `shared_benchmark` + `interface_contract` to existing capability/capability_map atoms. M2: any "B better than A" within a cluster MUST get cert-grade-comparison VET (optimal-per-evidence baked in). Skunkworks framing-VET on the METADATA design.

### (8) ConceptNet apply [Exp-Dev + Skunkworks]

Cell SCHEMA-VET PASS landed in sprint 2; data-acquisition pending (Director-side; 700MB CSV); held-out firewall verify pre-apply. Now unblocked (push-fix restored). RESEARCH_FINDING tier.

## Lull-fill assignments (opportunistic)

### (9) Item-4 reconcile [Skunkworks dups + Director dispositions]

Skunkworks at-bandwidth: dup-instance reconciliation (92/236/237 second-atoms renumber). Director reactive: catalog dispositions (conceptual-shorthand sentinel + 23 unknown-phantoms investigate-first + v2 taxonomy natural-hierarchy with verify-the-referent broadest-parent).

### (10) AUDIT_LESSON v2 taxonomy [Director]

Per Item-4 catalog audit's 36-UNCLASSIFIED + multi-class overlap pattern. Reactive on Skunkworks's dispositions.

### (11) Cross-domain analogy revival drill prep [Director; may dead-end]

PP-cross_domain_analogy RETRACTED awaits LLM-hybrid per USER 11th-rule. Substrate-only re-scope possible? Composes with Item-1's per-synset-coverage-bound (same extrapolation pattern).

## Parallel channels (no new commits; running NOW)

- **Skunkworks at-bandwidth:** dup-instance reconciliation + stale-canonical-doc AUDIT_LESSON inst 96 atomize (witness 0f184fea verified) + catalog dispositions
- **Exp-Dev:** phantom-edge removal (Skunkworks CONFIRMED; gated; route removal-done; Skunkworks landed-verifies H4 3->0)
- **Director:** WRITEUP framing-VET cycle pending Skunkworks; Phase-portrait v2 scour-deepening + capability-cluster METADATA design in parallel

## Standing discipline (composes; preserved)

- Item-1 HONEST_NEGATIVE bound = load-bearing anchor for every integration item
- USER scour-FULL-substrate-breadth (binding condition 1 of WRITEUP)
- USER NEGATIVITY-BIAS-symmetric (binding condition 3)
- USER A5-no-silent-recompute (guards manifest auto-fill)
- Skunkworks cert-architecture engine/checklist/SCHEMA-VET separation + mechanizability dimension
- Item-11 narrative-data-consistency SCHEMA-VET (WRITEUP first proving-ground)
- USER 11th rule: no LLM in reasoning loop (preserved in Item 1 + HYPERNYM-replication + HYP-5 deterministic-BFS)
- USER 6th + 7th checklist items (cells self-apply)
- USER NO-BUSY-WORK (no pre-build before USER GO -- now GO'd; cells build)

## What launches NOW

- **Director:** capability-cluster METADATA design + Phase-portrait v2 scour-deepening + WRITEUP framing-VET reactive
- **Exp-Dev:** HYPERNYM-replication cell build (Top-2) + HYP-5 depth-ceiling cell build (Next-1) + ConceptNet apply prep + phantom-edge removal staged
- **Skunkworks:** WRITEUP framing-VET (when v1 lands) + HYPERNYM-replication SCHEMA-VET (when cell builds) + at-bandwidth queue continues
- **Orchestrator REPLACEMENT:** C-deferred A2 v6 dispatch when remote sync completes + durability cron runner setup
- **Testbed:** reactive on cell landings

## Standing (9th rule)

- Skunkworks: pre-emptive SCHEMA-VET queue (5-6 cells) + 2 framing-VETs (WRITEUP + capability-cluster-metadata) + reactive on rest + at-bandwidth continues.
- Exp-Dev: 3 cell-builds (HYPERNYM-replication HIGHEST + HYP-5-depth-ceiling + durability cron sketch + ConceptNet apply prep) + phantom-edge removal.
- Testbed: 2nd-witness queue per item landings.
- Orchestrator REPLACEMENT: C-deferred dispatch + durability cron runner.
- Me (Director): WRITEUP framing-VET cycle + Phase-portrait v2 + capability-cluster METADATA + Item 4 dispositions reactive + lull-fill.

EXECUTING.

-- Research (Director)
