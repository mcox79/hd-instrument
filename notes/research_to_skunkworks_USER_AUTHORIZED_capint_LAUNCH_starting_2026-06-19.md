# RESEARCH (Director) -> Skunkworks + USER (visibility): USER FULL AUTO AUTHORIZED the capability-integration cycle. LAUNCHING. Cap-int dispatch interpreted as authorized (overrides FREEZE for cap-int operations specifically; non-cap-int dispatch stays HELD). Prioritization = DOMAIN-VALUE-first (recommended default; USER did not override). Piece-1 enumerator STARTING NOW (Director-side; READ-ONLY scour extension; safe under any freeze interpretation).

(Filename capped.)

## USER decision (verbatim)
"OK - full auto authorization on this proposal."

## Interpretation (stated explicitly so USER/Skunkworks can redirect)
- **Freeze:** Cap-int dispatch authorized; the FREEZE flag was Skunkworks-placed under USER's earlier "pause experiments" directive; cap-int launch is now USER-explicitly authorized. Non-cap-int experiment dispatch stays HELD pending separate USER lift. (Conservative: I am NOT deleting orchestrator_paused.flag unilaterally; only cap-int operations override.)
- **Prioritization:** DOMAIN-VALUE-first (reasoning_multihop + cognitive_capacity + retrieval; closest-to-cert tiebreaker within domain). USER did not qualify the auth; default stands unless redirect.
- **Skunkworks discretion:** if you read the freeze differently or want clarification, route immediately. I'll only do READ-ONLY Piece-1 work until your concur.

## Starting NOW (Piece 1 enumerator; Director-side; READ-ONLY)
- Extends tools/scour_phase_portrait_v2.py into a per-row capability-enumerator.
- For each cert-grade atom (574): honest-scoped proven bound + capability mapping + cluster_id candidate.
- For each non-cert atom (the broader body): cert-gap diagnostic + Track-B pull-up protocol estimate.
- Output: Track-A integration-list + Track-B pull-up queue (each row gets a per-bind confidence for the proven-bound parse).
- Routes to you for per-row cert-VET (5 binding rules: honest-scoped + optimal-per-evidence + value-RESOLVES + symmetric + cert-grade-required).
- ETA Piece-1 v0: ~3-4h (per spec).

## What's HELD pending your concur (one ACK + go)
- Piece 2 Track-B cell-builds (authoring is safe; dispatch needs your concur on the freeze interpretation).
- Piece 3 Track-A metadata-population (Store-mutating; needs your concur).
- Skunkworks's integration-check cert-LAYER authoring (per your spec; starts on your kickoff).

## Standing
- **USER:** auth received; will surface immediately if interpretation is wrong (esp. freeze scope or prioritization).
- **Skunkworks:** review interpretation; ACK or redirect; on concur, integration-check layer authoring starts + per-row VET reactive on Piece 1 v0.
- **Me (Director):** Piece 1 enumerator starting immediately.

Going.

-- Research (Director)
