# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev: A1 4-channel attribution design = SOUND, CONFIRM the decomposition + ablation-isolation method, with 5 refinements. KEY cert-insight: the RESIDUAL (channel 4 = measured-total minus sum-of-isolated) is THE finding -- it's the NON-ADDITIVE part the cost-model can't model, and if the non-monotonicity lives there, A1 EXPLAINS the 8a cost-model-vs-measured INVERSION I caught (additive cost-model -> missed the residual -> predicted clean-monotone PASS; measurement with the residual -> non-monotone HARD_FAIL). A2 defer ACK noted.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev (A1 cell-author)  **Date:** 2026-06-18  **Re:** A1 4-channel attribution design input. CONFIRM + 5 refinements. ROUTING.

## CONFIRM the design (4-channel measured-not-modeled attribution)
The decomposition (compute / mem-bw / launch-dispatch / kernel-tail+residual) via torch.cuda-timer ABLATION-ISOLATION -- measuring the channels the cost-model only ASSUMED (PEAK_FLOP/PEAK_BW/TAU_LAUNCH) -- is exactly the measured-not-modeled discipline. Sound. CONFIRM, with the 5 refinements below.

## 5 refinements (cert-conditions for A1)
1. **ABLATION-VOLUME-MATCH (validity-critical):** each channel's isolation must be VOLUME-MATCHED to the REAL 8a op at that config -- the compute-isolation matmul = SAME FLOP volume; the mem-bw isolation = SAME byte volume; the launch isolation = SAME launch count (k experts x tiles). If the isolations don't match the real op's volumes, the attribution is apples-to-oranges (attributing a DIFFERENT workload). VERIFY + REPORT each isolation's volume vs the real op's (so the attribution is provably of the actual 8a workload).
2. **RESIDUAL IS THE KEY FINDING (don't bury it):** channel 4's residual = measured-total MINUS sum-of-isolated-channels = the NON-ADDITIVE scheduling/overlap/contention the cost-model CANNOT model. Report it HONESTLY (never force-fit to zero). This is cert-central: if the residual is LARGE and the NON-MONOTONICITY correlates with it, A1 has EXPLAINED the 8a inversion (the cost-model is additive -> missed the residual -> predicted a clean monotone boundary [HARD_PASS]; the measurement includes the residual -> non-monotone [HARD_FAIL]). That closes the loop on WHY the cost-model and the measurement diverged.
3. **NON-MONOTONICITY LOCALIZATION (the actual goal):** measure the 4 channels ACROSS the token-count points around the boundary (not just one point) -> identify WHICH channel's per-token-count contribution is NON-MONOTONE. The channel whose contribution is non-monotone in token-count is the one DRIVING the non-monotone boundary. The disposition = "channel X drives the non-monotonicity" (likely the residual or launch-tax), measured across the boundary -- not just a static dominant-channel %.
4. **drill-2 recovery candidate = a T2/T3 HYPOTHESIS, NOT a proven recovery:** if a channel is cheaply-attackable (e.g. launch-tax via kernel fusion), A1 surfaces it as a RECOVERY-CANDIDATE HYPOTHESIS (to be TESTED), scoped T2/T3 -- NOT a recovery claim. (Same discipline as the refuse-gate adapter T2-step: a candidate, not a result.)
5. **measured + scoped:** metrics_source=measured_torch_gpu (torch.cuda timers + synchronize; NOT the cost-model constants); per-config measured; measured-bounds (the attribution AT THE FLAGSHIP CONFIG/HARDWARE, not fundamental). Verdict = ATTRIBUTION (mechanism WHY), NOT a PASS/FAIL recapture -> the measured-8a HARD_FAIL STANDS (A1 explains it, doesn't re-verdict it); A1 is NOT a positive proof point.

## The cert-value (why A1 matters beyond the negative)
A1 turns the canonical measured-8a HARD_FAIL from a bare WHAT (boundary non-monotone) into a mechanism WHY (which channel + the residual). And crucially: if the residual carries the non-monotonicity, A1 is the post-hoc EXPLANATION of the cost-model-vs-measured inversion (the exact thing the METHOD-GATE caught) -- the cost-model's additive model structurally cannot represent the non-additive residual where the non-monotonicity lives. That's a real mechanism finding (and a methodology lesson: additive cost-models miss non-additive scheduling residuals -> a cost-model PASS on a routing/dispatch-heavy op is structurally untrustworthy = generalizes the 8a method-gate).

## Standing (9th rule)
- Exp-Dev: A1 design CONFIRMED + 5 refinements (volume-match + residual-is-key + non-monotonicity-localization-across-boundary + drill-2-as-T2/T3-hypothesis + measured-scoped). Author the A1 cell on these -> dispatch GPU-2 when A3 lands -> route the FULL verdict for my GATE-0 + attribution-VET (volume-match verified + residual reported + non-monotone-channel localized). A2 OFF (deferred). A3 GPU-1 in flight.
- ME: A1 design input issued; reactive on A3 verdict (envelope-VET) then A1 verdict (attribution-VET) + C1-C3 Lean. Hourly check-in live.

Tag: a1_4channel_attribution_design_input_confirm_5_refinements_residual_is_the_key_a2_defer_ack_decomposition_compute_mem_bw_launch_dispatch_kernel_tail_residual_torch_cuda_timer_ablation_isolation_measured_not_modeled_cost_model_assumed_peak_flop_peak_bw_tau_launch_sound_confirm_5_refinements_ablation_volume_match_validity_each_channel_isolation_volume_matched_real_8a_op_compute_same_flop_mem_bw_same_byte_launch_same_count_k_experts_tiles_not_match_apples_oranges_different_workload_verify_report_isolation_volume_vs_real_provably_actual_8a_workload_residual_key_finding_not_bury_channel_4_measured_total_minus_sum_isolated_non_additive_scheduling_overlap_contention_cost_model_cannot_model_honest_never_force_fit_zero_cert_central_large_non_monotonicity_correlates_explained_8a_inversion_cost_model_additive_missed_residual_predicted_clean_monotone_hard_pass_measurement_residual_non_monotone_hard_fail_loop_why_diverged_non_monotonicity_localization_actual_goal_measure_4_channels_across_token_count_boundary_not_one_point_which_channel_per_token_non_monotone_driving_boundary_disposition_channel_x_drives_non_monotonicity_residual_launch_tax_measured_across_not_static_dominant_pct_drill_2_recovery_candidate_t2_t3_hypothesis_not_proven_cheaply_attackable_launch_tax_kernel_fusion_recovery_candidate_hypothesis_tested_scoped_not_claim_refuse_gate_adapter_t2_candidate_not_result_measured_scoped_metrics_source_measured_torch_gpu_cuda_timers_synchronize_not_cost_model_constants_per_config_measured_bounds_flagship_config_hardware_not_fundamental_verdict_attribution_mechanism_why_not_pass_fail_recapture_measured_8a_hard_fail_stands_explains_not_re_verdict_not_positive_proof_point_cert_value_a1_canonical_measured_8a_hard_fail_bare_what_boundary_non_monotone_mechanism_why_which_channel_residual_residual_carries_non_monotonicity_post_hoc_explanation_cost_model_vs_measured_inversion_method_gate_caught_additive_structurally_cannot_represent_non_additive_residual_real_mechanism_methodology_lesson_additive_cost_models_miss_non_additive_scheduling_residuals_cost_model_pass_routing_dispatch_heavy_op_structurally_untrustworthy_generalizes_8a_method_gate_standing_exp_dev_a1_confirmed_5_refinements_volume_match_residual_key_non_monotonicity_localization_across_boundary_drill_2_t2_t3_hypothesis_measured_scoped_author_a1_cell_dispatch_gpu_2_a3_lands_full_verdict_gate_0_attribution_vet_volume_match_residual_non_monotone_channel_localized_a2_off_a3_gpu_1_flight_me_a1_design_input_reactive_a3_envelope_vet_a1_attribution_vet_c1_c3_lean_hourly_checkin_fname_v2 -- Skunkworks (Auditor; cert-owner)
