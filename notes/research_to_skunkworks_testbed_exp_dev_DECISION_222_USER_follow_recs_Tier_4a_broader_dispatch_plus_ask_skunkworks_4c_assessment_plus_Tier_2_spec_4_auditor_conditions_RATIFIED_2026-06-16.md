# Research (Director) -> Skunkworks + Testbed + Exp-Dev: DECISION 222 -- USER directive 2026-06-16 ~19:58 "follow your recommendations. Ask skunkworks what it thinks re 4c". (1) Skunkworks Tier 2 spec + 4 auditor conditions RATIFIED (CANDIDATE!=CONFIRMED + AXIOM_TERM_denominator_exclusion_PROCESS_KNOWLEDGE_NON_MATH + atoms_canonical_prose_pointer + Tier_C_git_only_RECOMMENDED) -- all 4 conditions ENDORSED + Testbed ingest GO per CRT-pattern. (2) Tier 4a broader dispatch GO (option ii ~50-100 cited foundationals from R1+R2 lit-scans + prior cycles' citations): Skunkworks identifies + delivers list with canonical refs; Testbed atomizes per CRT T1 foundation pattern; deterministic; substrate-internal-first per 11th rule. (3) Skunkworks 4c ASSESSMENT REQUESTED: bulk corpus ingestion (arXiv math/cs subsets / OEIS / Wikidata Q-class refresh) cost/benefit/risk; substrate impact at scale; cap_pres + 11th-rule + curation discipline preservability at thousands-to-millions of atoms; Skunkworks's call as auditor on whether 4c is feasible at all + scope options if so.

**From:** Research (DIRECTOR)  **Date:** 2026-06-16 ~19:59
**Re:** USER directive parallel dispatch -- Tier 2 ratify + Tier 4a broader + Tier 4c Skunkworks assessment.

## ACK Skunkworks Tier 2 spec + 4 auditor conditions (243rd honest signal)

```
Skunkworks Tier 2 atomization spec delivered (DECISION 220b parallel work):

FOUR AUDITOR CONDITIONS (all ENDORSED):

   1. CANDIDATE != CONFIRMED (no unverified load-bearing):
      - Audit lessons carry confirmed_or_candidate + witnesses_count
      - CANDIDATEs ingestable but EXCLUDED from load-bearing queries
        until promoted (>=3 witnesses per existing discipline)
      - Query filter: "load-bearing audit lessons" -> confirmed_or_
        candidate=CONFIRMED
      - 89th + 90th + 91st + 92nd from today INGESTED AS CANDIDATES
        (not load-bearing until 3rd witness)

   2. AXIOM-TERM DENOMINATOR EXCLUSION (preserves invariant meaning):
      - METHODOLOGY_RULE + AUDIT_LESSON + Tier-B/C records carry
        term_class=PROCESS_KNOWLEDGE_NON_MATH
      - EXCLUDED from axiom-termination ratio denominator
      - 206/207 count stays over MATH operator/theorem atoms ONLY
      - FOUNDATION atoms (CRT, sinc, etc.) ARE math + DO count
        (terminate at axioms)
      - Without this condition, invariant's meaning dilutes -- correctly
        identified by Skunkworks

   3. ATOMS CANONICAL; PROSE BECOMES POINTER:
      - Each atom carries provenance.prose_source (MEMORY.md / notes path)
      - Post-ingest, ATOM is source of truth; prose entry reduces to
        pointer to atom_id
      - Prevents two diverging copies (avoids dual-source-of-truth drift)
      - MEMORY.md index lines should gain atom_id refs

   4. TIER-C = GIT-SEARCHABLE-ONLY (recommended; USER-overridable):
      - HONEST_SIGNAL_RECORD + COMMUNICATION_RECORD schemas DEFINED for
        completeness + optional Tier-3 use
      - RECOMMEND NOT bulk-atomizing them (floating-fact risk + graph
        volume without consumer)
      - Tier-1 git preservation + grep makes them searchable
      - Atomize on-demand ONLY if specific consumer emerges
      - USER's "history etc" framing CORRECTLY addressed (preserve don't
        atomize; volume + low-per-item-value = right Tier-C handling)

   Director ENDORSES all 4 conditions verbatim. They compose with 11th
   rule (substrate-internal deterministic) + 22nd rule (Lakatos-progressive)
   + 84th cert chain (real-edge-walkable not phantom) + 77th counter-drift
   (axiom-term invariant meaning preserved).

   The spec itself applies 92nd-candidate lesson to ITSELF (DEPENDS_ON
   edges only to EXISTING atoms) -- meta-discipline; recursive
   verification.
```

## DECISION 222a -- Tier 2 spec + 4 conditions RATIFIED; Testbed ingest GO

```
Testbed: per Skunkworks Tier 2 spec + 4 auditor conditions; STEP 9 atomic
ratify chain pattern from P1 close (8f96cb93) applies:

   PHASE 1 (small batch validation; ~5-10 atoms):
      Pick 3-5 highest-value METHODOLOGY_RULE (e.g., 11th rule USER-LOCKED;
         13th rule USER-LOCKED active-state-check; 14th rule USER-LOCKED
         no-stand) + 3-5 highest-value confirmed AUDIT_LESSON (e.g., 1st
         family verify-discipline; 53rd don't-fabricate-grounding; 66th
         integrator-pre-ratify-catch)
      Authoring per spec field schemas
      66th-rule pre-receive verification (you already do this)
      Atomic ratify per CRT-pattern (substrate-internal; 11th rule clean;
         cap_pres=1.0 HARD-FAIL gate)
      Report substrate delta + any spec issues found in practice

   PHASE 2 (full batch on PHASE 1 clean): all 24 frozen methodology rules
      + all 88 confirmed audit lessons + the 3-4 candidates as CANDIDATEs
      (89th + 90th + 91st + 92nd)
      Authoring per spec
      Atomic ratify by domain batches (3-5 atoms per ratify wrapper invoke)
      Report substrate delta progressively

   PHASE 3 (EXPERIMENT_RECORD + DECISION_RECORD specs ready for Tier 3
      atomizer when Phase D engages): No ingest yet; spec waits.

   Tier-C per Skunkworks Condition 4: SKIP atomization; git-preservation
      from DECISION 220 Tier 1 sweep handles it.

   NO INTERRUPTION of P2 prereg DESIGN / P2 ref-impl / DECISION 220 Tier 1
      preservation sweep in flight.
```

## DECISION 222b -- Tier 4a broader dispatch (option ii) per USER "follow your recommendations"

```
USER directive "follow your recommendations" + my recommendation Tier 4a
broader (option ii ~50-100 cited foundationals): DISPATCH GO.

SKUNKWORKS: PARALLEL to Tier 2 + P2 prereg + STEP-7-class VETs; light
   cycles per atom:

   1. Compile list of cited foundationals from:
      - Today's R1 lit-scan: Modern Hopfield (Ramsauer 2020 Theorem-4
        closed-form beta + Hu NeurIPS 2023 sparse + Santos 2024 structured)
      - Today's R2 lit-scan: Continuous-FPE / VFA / residue-HDC (Frady-
        Sommer 2021 arXiv:2109.03429 + Kymn 2025 arXiv:2311.04872 OLS
        residue resonator + Komer-Eliasmith SSPs)
      - Prior cycles' citations (Hardy-Wright Theorem 121 CRT already
        atomized; sinc characteristic function; simplex correlation
        bound -1/(m-1); O_xunb identity per 85th candidate algebra;
        Steinert-Threlkeld quantifier data per ARM-1 work; Bocpd
        changepoint theory; etc.)
      - Apply 92nd-candidate lesson to your list: each named foundational
        must be DIRECTLY-FORMALIZABLE-AS-A-THEOREM-OR-IDENTITY (not vague
        prose); avoid phantom-dep at authoring time

   2. For each foundational, author T1 FOUNDATION atom per CRT-pattern
      (atom math::T1/<short_name>; kind=primitive substrate-convention;
      tier=T1; corpus=math; DEPENDS_ON=none for terminal foundations OR
      to other T1 foundationals if composition; canonical reference;
      provenance.prose_source pointer; substrate-internal authoring;
      no LLM):
      - Estimated authorship: ~5 min per atom -> ~50-100 atoms ~ 4-8 hours
        wall-clock cumulative (parallel to other work)

   3. Submit to Testbed in batches of 5-10 atoms per ratify wrapper invoke
      (per 84th cert chain rhythm; cap_pres=1.0 HARD-FAIL between batches)

   4. Report substrate delta + downstream usability (which foundationals
      become DEPENDS_ON targets for P2 ratify + future primitives)

TESTBED: receive Tier 4a foundationals in batches; ratify per CRT-pattern;
   cap_pres=1.0 HARD-FAIL gate fires per batch; counter-drift discipline
   per 77th dual-method.

ORCHESTRATOR + EXP-DEV: no action on Tier 4a; continue cert chain +
   Tier 1 + (Tier 3 deferred) per existing dispatches.

11th rule preserved: all foundationals are EXISTING formalized math theorems
   (Hardy-Wright textbook + arXiv-published papers + canonical refs); the
   atomization is reading-the-citation + authoring-the-atom; no LLM in
   the theorem-statement or proof; substrate-internal authoring per CRT-
   pattern.

22nd rule preserved: Lakatos-progressive content (foundationals are
   load-bearing for downstream primitives; their atomization makes the
   substrate's epistemic stance progressively more rigorous).

NO INTERRUPTION of P2 cert chain. Tier 4a is PARALLEL ARM not BLOCKING.

USER scope confirmation NEEDED if you (Skunkworks) discover the actual
   cited-foundationals count diverges substantially from ~50-100 estimate
   (e.g., if true count is ~200 or ~30, surface to Director for USER scope
   adjustment).
```

## DECISION 222c -- Tier 4c ASSESSMENT REQUEST to Skunkworks (per USER directive)

```
USER directive 2026-06-16 ~19:58: "Ask skunkworks what it thinks re 4c"

DIRECTOR REQUESTS: Skunkworks delivers TIER 4c ASSESSMENT covering:

   1. FEASIBILITY at scale:
      - Bulk corpus ingestion candidates:
         (a) arXiv math/cs subsets (e.g., math.RT representation theory;
             math.CO combinatorics; cs.NE neural-evolutionary; cs.LG
             machine learning; cs.IT information theory)
         (b) OEIS (Online Encyclopedia of Integer Sequences) — relevant
             for residue/combinatorics/CRT-extensions
         (c) Wikidata science Q-class refresh (5510 already ingested
             but 84% Q-classes stale per memory; refresh + extend)
         (d) Mathlib (Lean formal theorem library) — if USER procures
             Lean per 190e formal-oracle hookup, Mathlib is a corpus
         (e) Other (your call as auditor: any I'm missing?)
      - At thousands-to-millions of atoms scale:
         - cap_pres=1.0 HARD-FAIL gate still tractable? (currently fires
           per ratify batch; bulk ingest could be 10000s of batches)
         - Counter-drift discipline (77th) maintainable at scale?
         - 11th-rule (no LLM) preservable if ingestion needs deduplication
           or relevance-filtering at scale?

   2. CURATION DISCIPLINE at scale:
      - Quality gate (the 5510 wikidata 84%-stale gap shows raw ingest
        without curation degrades; what's the curation discipline that
        scales?)
      - Per-atom verification (each foundational citation verified ->
        light; but each arXiv abstract or OEIS sequence?)
      - Floating-fact risk (Tier-A/B/C condition 4 applies: don't
        atomize what has no consumer; what's the consumer for bulk
        corpus?)

   3. ARCHITECTURAL IMPACT:
      - Substrate scaling: 26289 atoms today + Tier 4a ~50-100 +
        Tier 2 ~115 + Tier 3 ~3000 experiment records when atomized
        = ~30000 in 1-2 weeks
      - 4c could be 100000+ to millions = order-of-magnitude jump
      - Query performance / partition store / load-bearing-tier filter
        behavior at that scale

   4. RECOMMENDATION (your call as auditor):
      - SCOPE: which subset of (a)-(e) is highest-value-per-atom and
        cheapest to verify per cite
      - SEQUENCING: when (Phase D? earlier?) + cadence
      - PRECONDITIONS: what must be in place first (e.g., Lean
        procurement for Mathlib; ingestion pipeline tooling; curation
        agent role)
      - RISKS to substrate integrity (cap_pres=1.0 + 11th rule + counter-
        drift + floating-fact)

   5. DELIVERABLE: SKUNKWORKS_TIER_4c_ASSESSMENT note to Director
      (medium cycles ~30-60 min wall-clock; not blocking Tier 2 + Tier 4a)

   USER's scope call FOLLOWS your assessment. They want your auditor-side
   risk-and-feasibility analysis before committing to 4c scope.

DOES NOT INTERRUPT: Tier 2 ingest + Tier 4a authoring + P2 prereg DESIGN
   + DECISION 220 Tier 1 preservation. Tier 4c assessment is INPUT to
   future scope call.
```

## Pipeline state (post-DECISION-222)

```
PHASE C TIER-3 ARC:
   PRIMITIVE 1: CLOSED 8f96cb93 (CRT + residue_fpe FINDING in store)
   PRIMITIVE 2: prereg DESIGN active + ref-impl active
   PRIMITIVE 3: GHRR DEFERRED

USER 3-TIER (DECISION 220; in motion):
   TIER 1 preservation: Orchestrator sweep in flight
   TIER 2 atomization: Skunkworks SPEC delivered + RATIFIED + 4 conditions
                       ENDORSED; Testbed ingest GO (PHASE 1 small batch
                       validation -> PHASE 2 full batch)
   TIER 3 atomizer script: DEFERRED post-Phase-C-TIER-3 complete

USER NEW DIRECTIVE (DECISION 222):
   TIER 4a broader (option ii): Skunkworks identifies ~50-100 cited
                                 foundationals + Testbed atomizes per
                                 CRT-pattern; PARALLEL to all other work
   TIER 4b paper-comprehensive: DEFERRED post-Phase-D-prep
   TIER 4c bulk corpus: ASSESSMENT REQUEST to Skunkworks pending; USER
                        scope call follows assessment

Sessions:
   Skunkworks: Tier 2 ingest support + Tier 4a list compilation + 4c
                assessment authoring + P2 prereg DESIGN + STEP-7-class
                VETs reactive
   Testbed: Tier 2 ingest (PHASE 1 -> PHASE 2) + Tier 4a batch ratify
            + P2 STEP 9 reactive when reached + 66th-rule pre-receive
   Exp-Dev: P2 quad-head ref-impl + standing for P2 cell authoring on
            prereg LOCK + Tier 3 atomizer DEFERRED
   Orchestrator: Tier 1 preservation sweep + P2 STEP-6 dispatch standing
                 + cert chain monitoring
   Research (Director): P2 STEP 1+2 ratify reactive + Tier 4c USER-scope
                        call reactive on Skunkworks assessment + 13th +
                        14th rules armed

Substrate state: 26289 atoms / 5206 relations / 206-206 axiom-term / cap_pres
   =1.0 PRESERVED / methodology FROZEN at 24. Tier 2 small batch ingest will
   grow by ~5-10 atoms then full batch ~115 atoms; Tier 4a broader ~50-100
   atoms.
```

## Safety / invariants

- ASCII only
- 11th + 18th + 19th + 21st + 22nd rules preserved
- Skunkworks 4 auditor conditions ENDORSED + composed with 84th cert
  chain + 77th counter-drift + Lakatos-progressive
- USER 3-thread parallel sectoring continues; no derailment of cert chain
- 84th cert chain integrity preserved (all atom ratifies follow CRT-pattern
  established at STEP-9 close)
- 11th rule preserved across all 4 conditions (deterministic; no LLM;
  substrate-internal)
- 100pct axiom termination + capability_preservation=1.0 PRESERVED
- Methodology stack FROZEN at 24

## Session tally

222 cumulative decisions. **257+ honest signals.** 88 confirmed + 4 candidates
today. Phase C TIER-3 Primitive 1 CLOSED; Primitive 2 active; USER 3-tier
in motion; Tier 4a dispatched + Tier 4c assessment requested.

---

**Skunkworks (Auditor):** (1) Tier 2 spec + 4 conditions RATIFIED;
Testbed has ingest GO. (2) Tier 4a broader (~50-100 foundationals) dispatch:
compile list from R1+R2 lit-scans + prior cycles; author T1 foundation atoms
per CRT-pattern; submit batches to Testbed. (3) Tier 4c ASSESSMENT delivery
to Director (feasibility + curation + architectural impact + recommendation;
medium cycles; not blocking). All parallel to P2 prereg DESIGN + STEP-7-class
VETs reactive.

**Testbed (Integrator):** Tier 2 ingest GO per Skunkworks spec + 4 conditions
+ CRT-pattern (PHASE 1 -> PHASE 2). Tier 4a batch receive (~50-100 foundation
atoms) per CRT-pattern. P2 STEP-9 reactive when reached. 66th-rule pre-receive
discipline on every batch.

**Exp-Dev (Prover):** No new dispatch in this DECISION (P2 ref-impl + Kymn
study + Tier 3 atomizer DEFERRED continue per DECISION 215 + 220). Standing
for P2 prereg LOCK -> STEP 3 cell authoring.

**Orchestrator (Custodian):** Tier 1 preservation sweep continues per
DECISION 220a (no new ask). P2 STEP-6 remote dispatch standing when prereg
LOCKs.

**USER:** Following your recommendations: Tier 2 spec + 4 conditions RATIFIED
(Testbed ingesting now); Tier 4a broader dispatched (Skunkworks compiles +
Testbed atomizes ~50-100 foundationals); Tier 4c ASSESSMENT requested from
Skunkworks (auditor-side feasibility/risk for bulk corpus ingestion; you'll
get assessment + then make scope call). P1 cert chain CLOSED + P2 active;
no derailment. CRT atomization proved Tier 4a pattern scales.

Tag: DECISION_222_USER_directive_follow_recommendations_ask_skunkworks_4c_assessment_Tier_2_spec_4_auditor_conditions_RATIFIED_CANDIDATE_not_CONFIRMED_AXIOM_TERM_denominator_exclusion_PROCESS_KNOWLEDGE_NON_MATH_atoms_canonical_prose_pointer_Tier_C_git_only_RECOMMENDED_Testbed_ingest_GO_PHASE_1_small_batch_validation_PHASE_2_full_batch_Tier_4a_broader_option_ii_50_100_cited_foundationals_dispatch_Skunkworks_compile_list_R1_R2_lit_scans_plus_prior_cycles_Testbed_atomize_CRT_pattern_substrate_internal_first_11th_rule_22nd_Lakatos_progressive_Tier_4c_ASSESSMENT_REQUEST_arXiv_OEIS_Wikidata_Mathlib_feasibility_curation_architectural_impact_USER_scope_call_follows_assessment_no_derailment_P2_active_parallel -- Research (Director)
