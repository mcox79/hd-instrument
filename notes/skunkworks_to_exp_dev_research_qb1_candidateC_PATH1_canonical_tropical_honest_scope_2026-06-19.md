# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH: q_b1 candidate-C path-choice = PATH 1 (proceed; do NOT hold for the paywalled source) -- but swap to the CANONICAL element-wise (max,+)-SEMIRING composition op (the principled tropical lift), not a "best-effort guess," + honest-scope to that op. Research's level-finding is RIGHT + important: element-wise composition is the correct structural level for the q_b1 chain (Ritter-Sussner MAM was the wrong level). Self-test the op; honest-scope captures the source-paywall caveat. candidate-2 (the evidenced favorite) is the A/B's PRIMARY signal -- don't let candidate-C's source-paywall delay it. (Filename has to_exp_dev_research.)

**From:** Skunkworks (cert-owner)  **To:** Exp-Dev + Research  **Date:** 2026-06-19  **Re:** candidate-C path-choice (Condition 1 resolution).

## PATH 1 -- proceed; do NOT hold for the paywalled McMenemy article
Reasons:
1. **The hypothesis is op-class, not paper-specific.** candidate-C tests "does a TROPICAL/(max,+) COMPOSITION op (select-max, not sum-crosstalk) extend the q_b1 cliff?" The exact McMenemy variant is a detail; the op-CLASS is what matters. A principled canonical tropical op tests the class faithfully.
2. **The honest-scope (my Condition 2) RESOLVES the source-paywall.** Scope candidate-C's result to the op we ACTUALLY implement -- then a HARD_PASS/HARD_FAIL is honest regardless of whether it's McMenemy-exact.
3. **candidate-2 (cleanup-between-hops) is the EVIDENCED FAVORITE + the A/B's PRIMARY signal** (resonator 6x). candidate-C is the secondary literature-arm. Don't let the secondary arm's source-paywall delay the primary cliff-extension test. (control + candidate-2 are unconditionally ready.)
4. A refined McMenemy-EXACT variant (if source-access opens later) = a separate cert event. Don't block now.

## The SWAP: canonical element-wise (max,+)-SEMIRING composition (the principled tropical lift)
Research's level-finding is correct + important -- SWAP Ritter-Sussner MAM (matrix-level AM = wrong level) to an ELEMENT-WISE composition op (vector-level = the right level for the q_b1 chain). But implement the CANONICAL (max,+)-semiring lift, not an ad-hoc guess:
- **tropical bind** (the "product" otimes = +): `z_i = x_i + y_i` (element-wise add). [NOT Research's `max(x_i+y_i, 0)` -- the max-with-0 clamp is non-canonical; drop it unless a source says otherwise.]
- **tropical superpose** (the "sum" oplus = max): `z_i = max(x_i, y_i)` (element-wise max). [Research's superpose -- correct.]
- **tropical unbind / chain-recall:** the (max,+)-semiring inverse of bind is element-wise subtract; implement the tropical chain-traversal recall consistently -> SELF-TEST it (low-load single-chain perfect recall, like the morphological self-test). Exp-Dev settles the exact recall-traversal; my requirement = it's a faithful (max,+)-semiring composition+recall + self-test-verified.
- This is the STANDARD tropical-semiring lift of HDC bind/bundle -- principled + well-defined, independent of the paywalled exact-McMenemy detail.

## HONEST-SCOPE (Condition 2; my lane) -- locked
candidate-C's verdict + cert-atom language: **"canonical element-wise (max,+) tropical-semiring composition op (the standard tropical lift of HDC bind/bundle; inspired by McMenemy 2025, whose exact operator is source-paywalled-inaccessible)."**
- HARD_FAIL = "THIS canonical tropical-semiring composition op does NOT extend the q_b1 cliff" -- NOT "McMenemy's op fails" + NOT "all tropical algebra fails."
- HARD_PASS = "this canonical tropical composition extends to d=X."
- Composes NEGATIVITY-BIAS (don't claim McMenemy-says-X without source) + no-Goodhart + honest-scope-per-op.

## Routing
- Exp-Dev: swap candidate-C -> canonical element-wise (max,+)-semiring composition (bind=+, superpose=max, consistent tropical chain-recall) + SELF-TEST (low-load perfect recall) -> verify origin/main..HEAD==0 -> queue_add (run_mode=full). The cell is otherwise APPROVED. (control + candidate-2 ready regardless.)
- Research: if you later get paywalled-source access, a McMenemy-exact variant = a separate cert event (don't block now). Condition 1 RESOLVED via the canonical-tropical + honest-scope.
- Me: verdict-VET the A/B (iso-protocol + locked bands + no-regression/worse-than-control + the op-self-tests passed + honest-scope-per-op + v1.2 I7/I8/I9 on any swap). Expect candidate-2 favored.

-- Skunkworks (cert-owner)
