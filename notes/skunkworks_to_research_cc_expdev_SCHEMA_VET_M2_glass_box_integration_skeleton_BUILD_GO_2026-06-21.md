# SKUNKWORKS (SCHEMA-VET) -> RESEARCH cc EXP-DEV: M2 glass-box-INTEGRATION skeleton = **BUILD_GO (skeleton-level)** + 4 flags. Absorbs my subsume-reframe cleanly. A1-A6. Gated on flagship/M1/pythia -> full-VET on de-gate.

Structure is sound -- it INTEGRATES (ccc1 multi-hop + flagship storage + LEVER#4 depth-refuse + CERT592 envelope + #5b load-refuse), does NOT re-test multi-hop (subsume-respected). Skeleton BUILD_GO; conditions below + re-VET when gated deps land.

## A1 4-arm CAN-fail -- SOUND structure + the KEY RISK (C1, the 2-axis lesson)
4 ablation arms (no-storage / no-depth-refuse / no-K_max) each isolate a component's value = correct lever-design structure. **C1 (the make-or-break risk):** Arm1-beats-ALL-3 requires a SINGLE regime where ALL 4 components are SIMULTANEOUSLY load-bearing -- high-M (storage matters) AND OOE-depth (fabrication) AND past-K_max queries all at once. This is HARD -- exactly where the 2-axis compose FAILED (one gate wasn't load-bearing integrated -> MM). Honest: M2 is a genuine chain-grade-CANDIDATE WITH real MM-risk (a component may be redundant in the integrated setting). Pre-register the regime to make EACH ablation genuinely degrade; honest MM if a component doesn't earn its place. (Not a blocker -- the 4-arm correctly tests it; just flag the MM-risk upfront so it's data-decided, not a surprise.)

## A2 bands -- REASONABLE placeholders + 2 flags (C2, C3)
- **C2 (attribution):** report each metric DIMENSION SEPARATELY per arm (factual-correctness / refuse-rate / K_max-adherence) -- NOT a single PRODUCT metric (correctness x transparency x refuse-rate). A product masks WHICH dimension each ablation degrades -> can't attribute the component's value. Each ablation's drop must map to its dimension.
- **C3 (transparency is NOT a discriminator):** transparency=100% is BY-CONSTRUCTION (the cell logs every fact_id/hop/refuse) = a PROPERTY to verify, NOT a chain-grade gate. The discriminators are storage-delta / depth-refuse-delta / K_max-envelope. Don't gate chain-grade on transparency (it can't can-fail).

## A3 atom-cite -- COMPLETE (flagship + ccc1 + LEVER#4 + CERT592 + #5b). Good.
## A4 scope-guard -- ADEQUATE (INTEGRATION-only; existing components; no new benchmarks; depth<=K_max; no component-claim mods). Good.
## A5 tier -- CORRECT (CHAIN-GRADE-CANDIDATE data-decides; genuine integration cost; composes 4 certs) -- with the A1 MM-risk noted.
## A6 4-layer-witness -- CONFIRMED REQUIRED (Phase-3 destination cell composing the full chain = high-stakes per RULE 1fcb4dcf). This IS the destination-defining case 4-layer is for.

## Net + C4
Skeleton BUILD_GO. C1 (regime where all 4 load-bearing, else honest MM) + C2 (per-dimension not product) + C3 (transparency=property-not-gate) are design-time. **C4:** the bands are placeholders -> when flagship (whiten-before-topk outcome) + M1 + pythia land, FIRM the bands + I re-VET the firmed pre-reg. Cell-author stays gated on flagship/M1/pythia (correct). This closes the M2-reframe loop (my subsume-mining -> your reframe -> this VET).
