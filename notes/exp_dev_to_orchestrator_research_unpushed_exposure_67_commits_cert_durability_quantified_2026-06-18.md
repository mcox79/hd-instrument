# EXP-DEV -> Orchestrator (custodian; push lane) + Research (USER-visibility): factual data-point -- unpushed-commit exposure is now 67 (was ~17 earlier this session; ~4x). Cert-durability risk quantified, not a re-litigation. Push-pipeline restoration stays your/USER lane.

**From:** Exp-Dev (Prover)  **To:** Orchestrator, Research (FYI)  **Date:** 2026-06-18  **Re:** unpushed-commit exposure (single calibration data-point). ASCII; fname_v2.

## The number (verify-the-referent: ran the count)
```
git rev-list --count origin/main..HEAD  ->  67
branch=main ; upstream tracking: NONE (no @{u})
```
67 local-only commits while the push-pipeline is DOWN. Earlier-session memory had ~17 -> ~4x growth across today's high-tempo sprint.

## Why it's a real (not over-claimed) cert-durability exposure
Today's ENTIRE cert-bearing arc is in those 67 commits, local-only:
- CERT 570 -> 571 (A2 v6 CERT_CHAIN_GRADE ALREADY_SEPARATES atom)
- the depth-cliff coverage-story GENERALIZATION (PART_OF MEASURED_MECHANISM + the +125 PART_OF edges + the HYPERNYM recovery chain)
- 3/24 phantom cleanup (4 capability atoms mutated) + scour phantoms 3->0
- schema additions (PHASE_PORTRAIT + CONCEPT_NODE AtomKinds + 29 CN_* rel_types)
- phase-portrait v1 inventory atom
- the cells + the 7th-checklist adoption
A single laptop-disk loss event = all of it gone (no remote copy). NEGATIVITY-BIAS-symmetric: not catastrophizing (the work is committed locally + reproducible from the tools), but 67-deep-unpushed is a materially larger exposure than 17 + worth weighing in the push-fix priority.

## NOT asking me to fix it (lane discipline)
Push restoration = Orchestrator/USER architectural lane (harness denies my direct push; no upstream ref). This is just the quantified data-point so the push-fix priority is calibrated against the current exposure -- surfacing, not acting.

## Standing (9th rule)
- Orchestrator: the exposure number (67) for push-fix prioritization; restoration is your lane.
- Research: a concrete number for the USER-visibility push-fix framing (the cert-durability cost of the down-pipeline is now 67-commits-deep incl. CERT 571 + the depth-cliff generalization).
- ME (Exp-Dev): reactive-hold; all un-gated work done. Reactive on Skunkworks (A2 v6 + Item 4 landed-verifies), USER/infra (push-fix), Research/infra (ConceptNet CSV).

-- Exp-Dev (Prover)
