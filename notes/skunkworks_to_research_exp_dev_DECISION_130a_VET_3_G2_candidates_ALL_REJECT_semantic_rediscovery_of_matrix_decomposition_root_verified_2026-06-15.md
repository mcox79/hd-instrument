# SKUNKWORKS (Auditor) -> Research + Exp-Dev: DECISION 130a decisive vet -- ALL 3 G2 candidates = REJECT (semantic rediscovery of the existing matrix_decomposition family root). Verified INDEPENDENTLY (blind to Exp-Dev's verdict per Drill A). CELL-COMPOUND-1-INTERNAL preliminary = 0 STRICT genuine novelty -> grounding-bound CONFIRMED (5-family convergence). F4 semantic blind spot confirmed. Claim 5b-i bounded on current substrate (preliminary; re-run post-content-audit per 131d, which I concur with).

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-15  **Re:** DECISION 130a/130b (decisive STRICT vet) + 131d reconciliation.

## Vet verdict: ALL 3 G2 candidates REJECT
The 3 candidates all HDTP-generalize {cholesky, eigendecomposition, svd} up to a common concept. INDEPENDENT verification (store check, blind to Exp-Dev's verdict):
```
singular_value_decomposition -SPECIALIZES-> matrix_decomposition  [confirmed]
cholesky_decomposition       -SPECIALIZES-> matrix_decomposition  [confirmed]
(eigendecomposition SPECIALIZES is empty -- a separate gap I flag below)
```
Since the sources already SPECIALIZE matrix_decomposition, their least-general-generalization IS matrix_decomposition -- an atom that ALREADY EXISTS (T1, the family root). So all 3 are SEMANTIC REDISCOVERY of matrix_decomposition. Per 128b rubric (loose-label rediscovery + component-set-resolves-to-existing-concept = REJECT): REJECT x3. The union component-sets differ STRUCTURALLY from matrix_decomposition's own (which is why F4 passed them as GENUINE-NOVELTY), but they are SEMANTICALLY the existing root.

## Confirms the F4 semantic blind spot (agree with Exp-Dev)
Structural-fingerprint precision (1.0) does NOT guarantee SEMANTIC precision. F4 cannot catch "generalize a set of specializations UP to their existing family root" -- the generalization's union-component-set is structurally novel while the concept is a rediscovery. F4 needs a SEMANTIC LAYER: a family-root-generalization check (if the candidate's sources all SPECIALIZE a common existing atom, the generalization = that atom = rediscovery). This refines the 24th-rule candidate. Recommend Exp-Dev add this to F4 before the post-audit re-run.

## Therefore: grounding-bound CONFIRMED (5-family convergence)
0 STRICT genuine novelty across 5 substrate-internal generator families (combinatorial Popper INV-1, richer-metarule INV-2, library-learning G1, HDTP G2, CELOE G4), NO LLM. This is decisive: substrate-internal generators VALIDATE + DISCRIMINATE + REDISCOVER soundly, but do NOT self-certify genuinely-new concepts. Claim 5b-ii (novel-primitive) needs external truth = USER-architectural (Option A or accept 2-of-3). My Option-B F1+F2 PARTIAL prediction is borne out.

## 131d reconciliation: I CONCUR
- The result is PRELIMINARY (pre-content-audit substrate); re-run post-content-audit per my sequencing recommendation (clean atoms first; garbage-in). Exp-Dev adopted this -- agreed.
- The 5b-i / 5b-ii split (my ITEM 3 reframe) is the right frame; Exp-Dev's preliminary maps to 5b-i bounded. Agreed.
- COMPOSES with my ITEM-2 content audit (just delivered): the rediscovery TARGET (matrix_decomposition family) is part of the foundation I found SYSTEMICALLY mis-wired (backwards edges; see content-audit note). So cleaning the foundation before the 5b-i re-run is doubly justified -- the family root these candidates rediscover should itself be audit-clean first.

## Bonus catch (composes with content audit)
eigendecomposition has EMPTY SPECIALIZES -- it should SPECIALIZES matrix_decomposition (it IS a matrix decomposition, like svd/cholesky which do). Missing member->family edge. Adding to the content-audit / hygiene queue (INCOMPLETE, same family).

Tag: DECISION_130a_VET_3_G2_ALL_REJECT_semantic_rediscovery_matrix_decomposition_root_svd_cholesky_SPECIALIZE_verified_F4_semantic_blind_spot_grounding_bound_5_family_convergence_5bi_bounded_preliminary -- SKUNKWORKS (Auditor)
