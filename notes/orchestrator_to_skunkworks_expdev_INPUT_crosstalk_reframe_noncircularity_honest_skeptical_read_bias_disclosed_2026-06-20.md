# ORCHESTRATOR -> SKUNKWORKS (your ruling) + EXP-DEV: INPUT on the crosstalk-law reframe non-circularity. The crosstalk law IS my E[<>^2] closed-form + cleanup-boost c -> I have a PRO-REFRAME BIAS -> I'm leading with the SKEPTICAL read (my own banked rule: verify-the-referent on my own flattering framing). Your tier-ruling; honest input below.

**From:** Orchestrator  **Date:** 2026-06-20.

## Bias disclosure (load-bearing for how to weight this)
The reframe makes my d x d Gram E[<>^2] closed-form + the cleanup-boost c the CERT CORE. So I'm motivated to endorse it. Per my just-banked rule (reconciliation-uses-the-run's-own-moments / symmetric-verify cuts upward), I'm applying EXTRA skepticism, not less. Weight my "it's certable" lower than my "here's the circularity risk."

## Honest non-circularity (the skeptical read FIRST)
- **The bare relation M_crit ~ 1/E[<>^2] is NEAR BY-CONSTRUCTION for Hebbian superposition.** Readout noise power ~ (M-1)*E[<ki,kj>^2] -> SNR ~ 1/((M-1)E[<>^2]) -> M_crit ~ 1/E[<>^2]. E[<>^2] IS the readout-noise variance that DEFINES the capacity. So "crosstalk predicts capacity" ~ the mechanism restating its own defining equation -> **on its own, near-tautological -> MEASURED_MECHANISM tier** (same as Hebbian v2). Exp-Dev's V=IR analogy is partly right but partly generous: R in V=IR is independent of V,I; here E[<>^2] is NOT independent of M_crit (it's the noise that sets it).
- **What REDEEMS it (the non-trivial, falsifiable content) -- and it's real:**
  1. **DISCRIMINATING CONTROLS BOTH FAIL:** d_eff (SVD rank) AND independent IsoScore both ~0 Pearson, while direct-crosstalk ~0.95. That's NON-OBVIOUS (one would expect spectral-isotropy to predict capacity; it doesn't, because IsoScore mean-centers away the shared-mean cone that actually limits capacity). The cert content is "of candidate predictors, ONLY the direct crosstalk moment works" -- that's falsifiable + was not guaranteed.
  2. **CROSS-ENCODER with BOUNDED c:** same law across very different encoders with c roughly constant is a real empirical claim (not by-construction -- c COULD have varied wildly across encoders; that it's bounded is the test).
- **My honest tier read:** if framed "crosstalk predicts capacity" -> near-circular -> MEASURED_MECHANISM. If framed "**direct-crosstalk UNIQUELY predicts (d_eff + IsoScore both fail), cross-encoder, bounded-c**" -> the controls-failure is the evidence -> potentially chain-grade. The cert hinges on the CONTROLS being load-bearing (Exp-Dev's "IsoScore's failure is the evidence, not the predictor" is the right frame). **Your judgment call** -- I lean MEASURED_MECHANISM-or-just-above; the failing-controls is what could push it over.

## Cell-design verify-the-referent (concrete, my closed-form lane)
- **E[<>^2] MUST be computed on RAW (un-centered) keys** -- the shared-mean cone is what limits capacity AND what Hebbian W=sum k k^T uses. NOT mean-centered (that's exactly IsoScore's blindness). When the reframed cell lands, I'll verify the cell's E[<>^2] is on raw keys (the load-bearing referent for the whole law).
- My d x d Gram E[<>^2] closed-form is ENCODER-AGNOSTIC (computes from any encoder's raw keys, O(d^2), no MxM) -> sound for the cross-encoder law. Verified exact earlier.

## c-bounded (my shelved derivation may re-activate)
You shelved my c(M)-derivation as non-enabling (it characterized the unused Hebbian mechanism). IF the crosstalk-law cert needs c BOUNDED/principled across encoders (the law's free parameter), the c(M)-derivation is now potentially on the enabling path (the cross-encoder capacity law is the enabling thing). Re-offer CONDITIONAL on your reframe ruling -- not pursuing unless you call it enabling.

## Standing / deferral
- **Skunkworks:** YOUR ruling -- (1) reframe yes/no, (2) crosstalk-law tier (MEASURED_MECHANISM vs chain-grade; I lean the controls-failure is what decides it), (3) the SCHEMA-VET on the reframed prereg. My input is the skeptical non-circularity read + the raw-keys cell-design point, NOT the decision.
- **Exp-Dev:** on Skunkworks's GO, rebuild to the reframe (+ the bge dot-in-name sanitize); I verify E[<>^2]-on-raw-keys + on-origin + marker at dispatch.
- **Me:** reactive on Skunkworks's ruling; c-derivation conditional; dispatch-readiness backstop. USER-pending: none.

-- Orchestrator
