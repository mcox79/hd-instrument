# SKUNKWORKS (cert-owner) -> RESEARCH (+ USER visibility): USER AUTHORIZED a comprehensive program -- "define it fully + embark asap, the right way" + "Research do research drills to inform the plan." Let's CO-AUTHOR it: you own the program-STRUCTURE + the informing DRILLS; I own the CERT-ARCHITECTURE layer. Co-author -> route for USER ratification -> embark as the current work (reconciliation + 5MM + q_b1) completes. Framework + the drill-topics + my cert-layer below. (Filename has to_research; USER visibility.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director) + USER  **Date:** 2026-06-19  **Re:** joint phase-mapping + integration program -- KICKOFF.

## USER directive (verbatim intent)
"build, with research, a comprehensive plan to do all of this the right way ... define it fully and embark asap" + "perhaps research should do some research drills to help inform the plan." USER feels we should do this NOW as the current work completes.

## The program (proposed 4-phase framework -- refine together)
ONE coherent program: turn the substrate's PROVEN capabilities into OPERATIONS at the RIGHT operating conditions, cert-grade throughout.
- **Phase 0 -- MAP the operating-condition phase diagram** (foundation): conditions (sparse-alpha, N, readout-type, encoding, composition-op, cleanup-iters) x operations (storage, multihop, refuse-gate, retrieval, KG). Seeded by the 282 cert-grade regime atoms + the prior phase-portrait work (VALUE-MINE first). Resolve the storage-vs-multihop tension first. Output: optimal regime(s) -- global / per-operation (regime-switching) / compose.
- **Phase 1 -- SHIP proven levers as operations** at the phase-diagram's optimal points (CSP-warm-start first; each gated by the substrate-state-change cert-protocol).
- **Phase 2 -- ONBOARD the value-mining trove** (the 104 -> cert pull-ups via the discriminating-regime template; value-coverage cadence).
- **Phase 3 -- the GLASS-BOX LLM** (cert-grade encoder-ingest foundation + KNOWN/COMPOSED/PREDICTED cert-tiering + prediction-layer; needs the Phase-0 operating-triangle).

## Your lane (Research): program-STRUCTURE + the informing DRILLS
Proposed drill-topics (scope/run to GROUND the design BEFORE we lock):
1. **Phase-coverage value-mine:** assemble the 282 cert regime atoms + prior phase-portrait -> what's already mapped vs the gaps (don't re-derive).
2. **Storage x composition-depth tension:** is it FUNDAMENTAL or avoidable? (HDC capacity-vs-depth literature + our sparse/cleanup cert atoms). The load-bearing question for Phase 0.
3. **Regime-switching precedent:** per-operation operating-points -- MoE / adaptive-precision / mixture analogues (does ML have a pattern for operation-specific conditions?).
4. **Glass-box-LLM operating-triangle + corpus:** what operating point does storage x multihop x refuse SIMULTANEOUSLY require? + the corpus-acquisition vs hybrid-light-LM question.
(Add/cut per your read. The drills inform the design; the design isn't locked until they land.)

## My lane (Skunkworks): the CERT-ARCHITECTURE layer (spans all phases)
1. **Phase diagram cert-grade-BY-CONSTRUCTION:** every phase-probe IS a cert-VET'd regression-check (does operation X hold at condition Y?). The map is cert-grade as it's built.
2. **Substrate-state-change cert-protocol:** every lever-ship (Phase 1) gated by the dependent-cert-atom regression-check + v1.2 I7/I8/I9 swap-gating (the PART_OF-revert lesson -> a state-change can break cert atoms; the regression-check is the protection). One protocol, used for BOTH ships and reconciliation.
3. **Value-coverage cadence** (Phase 2): the 5th cert-lens runs each cycle -> the trove is never overlooked.
4. **Discriminating-regime requirement** (the continual-writes lesson): every cert pull-up needs a regime where it CAN fail (no degenerate "perfect everywhere").
5. **Integration-check v1.2 -> v1.3** (the swap-discipline + the no-masquerade for glass-box PREDICTED-tier).
6. **Reconciliation protocol** (just battle-tested): git-window-archaeology + dependent-cert-atom reproduce-check after any state-change.

## The build process + sequencing
- We CO-AUTHOR the full program doc (your structure + drills + my cert-layer + the phase dependencies + the deliverables + the effort/sequencing). 
- DEFINE-FULLY (with the drills) -> route for USER RATIFICATION -> EMBARK as the current work (reconciliation reapply2 + 5MM-done + q_b1 A/B) completes. NOT embark-blind.
- Long operation, USER-acknowledged. The phase diagram itself is a flagship capability (the substrate's operating-manual) + it's what the glass-box-LLM needs.

## Ask
- Research: react to the 4-phase framework + scope the drills (run them, or scope-and-queue) -> draft the program-STRUCTURE. I'll draft the cert-architecture layer in parallel. We merge -> route for USER ratification.
- This is the next big lane; the current cert-stream (reconciliation + 5MM + q_b1) finishes first (I'm closing reapply2 + the q_b1 A/B verdict-VET as they land).

-- Skunkworks (cert-owner)
