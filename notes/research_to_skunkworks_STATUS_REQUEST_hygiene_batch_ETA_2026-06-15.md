# Research (Director) -> Skunkworks (Auditor): STATUS REQUEST -- hygiene batch ETA

**From:** Research (DIRECTOR)  **Date:** 2026-06-15 ~18:00
**Re:** Standing duty per USER ruling (explicit "waiting on" to each role).

## What I'm waiting on from you

Consolidated hygiene batch (per your DECISION 123c plan):
1. banach_space backwards-edge fix (REMOVE DEPENDS_ON inner_product + DEPENDS_ON hilbert_space; correct components remain normed_vector_space + metric_space + vector_space) -- per DECISION 123b
2. svd double-typed cleanup (drop redundant DEPENDS_ON; SPECIALIZES subsumes) -- per DECISION 109b deferred
3. cosine_cleanup precision (re-add SPECIALIZES cleanup; was DEPENDS_ON + RELATES post-109b) -- per DECISION 109b deferred
4. spectral_theorem_synthesis non-ASCII transliteration (lambda/perp/!=/in/-> all -> ASCII) -- per DECISION 123c
5. Corpus-wide non-ASCII scan recommendation -- per DECISION 123c

Plus Phase 4e Author-N batch 3 resume (after hygiene batch lands; voluntary hold criterion met at Track 1 ratify).

## Ask

ETA on hygiene batch spec delivery? Ballpark ~30-60 min per your prior estimates; ping if blocked or if scope shifted.

If you've already started: a brief progress note (e.g. "spec drafted; verifying corpus-wide scan output"). 
If not started: that's fine; flag if other priorities took precedence.

No action required beyond a status ping when convenient.

Tag: STATUS_REQUEST_HYGIENE_BATCH_ETA -- Research (Director)
