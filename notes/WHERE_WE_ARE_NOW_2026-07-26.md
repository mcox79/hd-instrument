# WHERE WE ARE NOW — clean current state (tier 3; REWRITE each session) — 2026-07-27 ~22:30Z (compaction-prep rewrite)

## Direction (read FIRST)
1. GOAL + invariants + anti-drift: `notes/SUBSTRATE_CHARTER_read_first.md`
2. Plan: `notes/THE_PLAN_learned_grounded_representation_foundation_2026-07-26.md`
3. This = the live snapshot (rewritten clean this session; older layered blocks removed).

## THE GOAL
A glass-box VSA/HDC substrate you can CONVERSE with that genuinely REASONS, EARNING its meaning + knowledge the brain's way (NO borrowed embeddings/LLM at inference; the substrate learns it itself). Architecture = CLS: SEED a foundation from relational KBs -> READ new material -> SLEEP-consolidate. The 3 legs: (1) earn MEANING, (2) REASON over it, (3) LEARN continuously from reading.

## ✅ CONFIRMED + BANKED (do NOT rebuild; all VET-survived; local-only store, tail=29591)
- **LEG 1 — EARN MEANING (SCALE WIN, banked 29591 CHAIN_GRADE):** our OWN from-scratch encoder trained on 237M-token ARC (no borrowed vectors) BEATS grounding on held-out-NEW concepts: +0.050 SEMANTIC (parameter-free z-avg fusion, leak-proof) & +0.071 RELATIONAL (text-alone), both seeds, controls at chance, +0.10 over random-init. Scale WORKS + is DATA-LIMITED. Scope: modest (0.60-0.65 band), held-out-NEW placement/retrieval of concepts seen in corpus (median 655 mentions), NOT zero-shot meaning-invention.
- **LEG 2 — REASON (banked 29587/88/89):** learned encoder does GENUINE inductive relational inference on held-out-NEW concepts — beats grounding-homophily +0.108 & non-learned 2-hop +0.093; survives 1-edge context (dose-response, resolution scales with #constraints); holds TWO-NEW-CONCEPT. Local decision-time reasoning EXHAUSTED (depth/combiner/chain all flat) => composition lives IN the representation (encode-time), not bolt-on reasoning.
- **FOUNDATION (banked 29585):** cskg_foundation_v1 (data/cskg_foundation_v1/, gitignored 258MB): 482,588 concepts / 1.24M typed edges, cleaned/canonicalized/grounded (Lancaster/concreteness/VAD/AoA), glass-box, no borrowed vectors. + ingest FIXED+VERIFIED (director-KB, 2.83M triples; query via tools/director_kb_query.py NOT grep).

## 🔴 LEG 3 — LEARN FROM READING = THE OPEN FRONTIER (honest state)
The self-learning loop (read -> comprehend -> extract -> SLEEP-consolidate -> improve). Mechanism plumbing WORKS (sleep fires every cycle, controls behave, retention held, leak-proof). But COMPREHENSION-driven learning is NOT yet demonstrated:
- **loop v1/v2 (GPU):** no sustained gain — concepts were pretraining-SATURATED (median 655 mentions; re-reading the known adds nothing). Diagnosis: brain learns from NOVELTY.
- **loop v3 (exposure-stratified):** initially read HARD_PASS (+0.024 on low-exposure) but **VET DOWNGRADED** — the gain is DISTRIBUTIONAL sample-accumulation + LOW-baseline HEADROOM, NOT comprehension: **word-SCRAMBLED text produced AS MUCH gain (+0.029) as coherent.** Not banked.
- **Two USER-requested drills (committed):** (A) `notes/drill_brain_reading_comprehension_to_memory_vs_our_loop...` (B) `notes/drill_loop_fairness_audit_what_we_missed...`. Findings: brain extracts a role-labeled PROPOSITION (order-sensitive) + binds it; loop v1-v3 never a fair comprehension test.
- **DIAGNOSIS CORRECTED (both directions — honesty):** the drills' "mean-pool is permutation-invariant so can't comprehend" was TOO STRONG — each mention-rep IS an order-sensitive transformer encoding, and loop-v4's SMOKE showed comprehension-SEPARATION (coherent 0.127 > scrambled 0.082 > wrong-concept 0.045, low-power). So the honest state is OPEN, needs a properly-controlled FULL run.

## 🔥 LANDED 07-27 ~23:12Z — loop-v4 = clean fair NEGATIVE (MIDDLE_BAND)
- **v4 FULL result:** the OWN-encoder pooling + DG fast-episodic-store loop, with the strong wrong-concept control, does NOT show comprehension-specific gain. KEY arm MAIN_fast_episodic LOW gain **0.0015** = TIES SCRAMBLED 0.0015, LOSES to WRONG_CONCEPT 0.0068 (controls matched to fast_episodic mode => FAIR). `comprehension_specific_gain=false`. Plain arm's 0.0247 gain has no matched control => distributional (v3 prior). DG pattern-sep barely fired (ratio 0.9301). Only signal = incoherent-reading-CORRUPTS (WRONG final 0.4648 << NO_READ 0.6018), NOT acquisition. **Not banked. Leg 3 stays OPEN.** SKUNKWORKS VET CONFIRMED clean-negative (attribution MEASURED_MECHANISM not universal): coherent-scrambled gap CONSTANT from cycle0 (no accumulation => corruption not teaching); spec_fact lift = concept-presence within noise (0.34sigma), reverses at HIGH; + Q4 SECOND defect = cross-concept mention-reps near-collapsed at 0.987 cosine (fast store fed near-identical keys). Two candidate defects, both cheap: (A) order-blind mean-pool readout (within-concept), (B) cross-concept ANISOTROPY (raw reps to fast store; eval used standardized). STEP-0 probe (in flight) measures both.
- **ROOT CAUSE LOCATED (mechanistic):** `TinyTransformer.pooled` (v2 encoder L659-665) = **mean-pool over token hiddens** => bag-of-contextualized-tokens => pooled(coherent) ~= pooled(scrambled) (identical token SET). Order is discarded AT READOUT (encoder was MLM-pretrained order-sensitive; we throw it away). Compounds through mean-over-mentions + weighted-mean fast-read. Full audit + next lever: `notes/v4_negative_brain_fidelity_audit_readout_is_order_blind_next_lever_2026-07-27.md`.
- **STEP 0 DONE (07-27, hypothesis CONFIRMED):** data/probe_v4_readout_order_sensitivity_v1.json. Mean-pool coh-vs-scram cosine 0.9944 (order-BLIND, confirmed); cross-concept collapse is ANISOTROPY (raw 0.9444 -> centered -0.0645), cheap fix = common-mode removal. v5 readout pick = BIND_HRR_position (hdlab.binding.bind): best on BOTH axes (coh-vs-scram 0.7304, coh-vs-wrong-raw 0.4848). Own-mechanism. CAVEAT: representation-separation is NECESSARY not SUFFICIENT for learning-gain; 0.73 is modest; v5 FULL settles it.
- **v5 FULL LANDED = MIDDLE_BAND (07-28, VET'd):** readout order-sensitive at scale CONFIRMED (coh~scram 0.7719, order_sens=TRUE); direction flipped correct vs v4 (comprehension_specific_gain false->TRUE; FAST_LOW gain 0.0051 POSITIVE, beats scram 0.0001 + wrong 0.0036). BUT NOT a pass: gain 0.0051 << +0.02 bar, not sustained/retained, contrast BACKWARDS (HIGH 0.0341 >> LOW 0.0051 = accumulation not novelty-comprehension), margins within noise. Order-blindness was A blocker (removed) not THE gap.
- **FAST PROBE (07-28, data/probe_v5_bind_readout_derisk_v1.json) — CONVERGED DIAGNOSIS:** absolute-POSITION binding is the WRONG KEY. Well-powered (n=49): a concept's OWN mentions are only 0.52 self-consistent under bind vs 0.95 mean-pool -> same concept at different positions binds inconsistently -> consolidation washes the bind signal out (why v5 gained only 0.005). Metric-incompat NOT the problem (bind AUC ~= mean-pool, underpowered). Retrieve-not-average RULED OUT (averaging beat single-mention 8/8 for bind). => the brain binds by ROLE (position-INVARIANT), not absolute position. **This VALIDATES v6 (learned reusable role-slot head) over v5's hand-built position-bind.**
- **TWO fixes needed (both in v6):** (1) POSITION-INVARIANT role assignment (content/learned, not absolute position); (2) REAL-DYNAMIC-RANGE metric (relational-AUC compressed: chance 0.51 -> max-exposure ceiling 0.56, only ~0.05 range; add a fact-acquisition/cloze 0->1 probe). v6 build steered to both (hdi_exp_dev ae70d2f) + keeps memorization controls (held-out-NEW-pairs + concept-pair-scramble + wrong-relation). Overnight.

## 🚫 KEY LESSON (USER-LOCKED 2026-07-27): NO BOLT-ON EXISTING READER
USER: "EVERY time you used an existing reader it was a DISASTER." situation_reader is annotation-bound + mis-calibrated for ARC science prose (would measure extraction failure). I was about to repeat the anti-pattern with spaCy-SVO extraction — HALTED it. **The substrate must COMPREHEND via its OWN learned mechanism, not a supplied/bolt-on parser (situation_reader, spaCy).** This is the earn-it-ourselves invariant applied to comprehension. loop-v4 (own encoder, no external reader) is the correct path.

## 🧭 DIRECTOR DISCIPLINE (recorded — I keep over-reading)
4+ over-reads caught by VET this session (v3 double-dissociation, deep-text +0.20, loop-v3 milestone; + the drills over-stated pooling-can't-comprehend). FIX: on ANY positive, check the ACTIVE-CONTROL's GAIN (not level); separate distributional-sample from comprehension; VET every load-bearing verdict; the exp_dev de-risk (caught situation_reader broken) is the guardrail — trust it.

## 🌙 v6 RESULT (07-28) = HARD_FAIL, honest + inconclusive on reader-comprehension
prop_extraction_selfteach_v6 (learned position-invariant extraction head, frozen ckpt_seed_7). coherent extraction_acc 0.192 vs scrambled 0.179 (Δ0.013 < 0.05 margin) vs B0 identity-only 0.754 vs majority 0.603. **B0=0.754 tripped the HARD-FAIL guard — the extraction task is ~75% memorization-solvable (concept identity alone), the exact Peng-2020 trap; the memorization control WORKED (caught it, didn't get fooled).** Head also underfit (0.192 << majority 0.603; 14 epochs). => v6 could NOT fairly measure reader comprehension (task memorization-dominated). NOT a refutation of the reader; shows (a) a fair comprehension task is hard to construct, (b) encoder/features are the deeper lever. NOT banked. Do NOT rabbit-hole on reader-task redesign — fix the encoder first (below), revisit reader on stronger features + better task.

## 🧱 STRATEGIC PIVOT (07-28, USER-prompted): the ENCODER is the CEILING — optimize the representation layer
Component audit (this session): READOUT (was order-blind -> content-role v6, fixed); METRIC (was compressed -> fact-cloze v6, fixed); CONSOLIDATION (crude averaging, washes signal, under-developed); FOUNDATION (solid, 1.24M edges); **ENCODER = the binding constraint.** Verified specs: 6L/512d/128ctx/16k-vocab, MLM on ~158M tokens of ARC SCIENCE ONLY. Held-out relational-AUC tops out ~0.56 even at 4345 exposures (chance 0.51) => text-alone relational placement near-chance-weak => caps EVERY downstream win. Diagnosis: NARROW data (one genre) + OBJECTIVE MISMATCH (MLM != relational meaning), NOT architecture. Matches charter ("REPRESENTATION = the blocker") + 29591 ("data-limited"). **NEXT BIG THRUST = optimize the encoder: (1) BREADTH (add Wikipedia/general text to the ARC-only diet); (2) RELATIONAL self-teacher objective (train encoder to reproduce foundation's typed edges = R3/R4; v6's extraction head is a first piece). Scoping in flight (research a282aca): data inventory + can-fail design (does breadth/objective lift the 0.56 ceiling on held-out-NEW concepts, >+0.03). Launch for the deep overnight (~6h) once scoped + v6-informed.** Reader work (v5/v6) NOT wasted -- it fixed real bugs + v6 IS a piece of the relational self-teacher; but the encoder ceiling is why wins stay squeezed.

## ⚠️ 07-28 ~04:5xZ — RETRAIN CRASHED + RECOVERY (honest)
- **scale_meaning_learn_arc_heldout_v3_relobj FULL CRASHED ~7min in (CUDA OOM, relational-pooling forward on 8GB card) — never trained; GPU idle ~2.6h.** FIXED + RE-DISPATCHED (commit d8d6684fb, afe2932c): opt-in activation-checkpointing on the relobj pooling path + batch-chunk _pooled_for_rows (chunk=32); correctness-neutral (objective/landmark/leak-gates/eval untouched, MLM path untouched). GPU-SCALE VERIFIED on real card FULL dims: old=OOM, new completes at peak 3.5GiB << 6.8GiB cap (~2x headroom). Re-dispatched run_index=2, remote self-test pass, live log RUNNING past prior crash point, seeds [7,13], ckpt_every_steps=2000 (resumable). DIRECT-SSH liveness re-check due ~first-checkpoint window (local cache still broken). grounding shares bug -> apply SAME fix (activation-ckpt + chunk) before its FULL queues.
- **OBSERVABILITY RESTORED (a040054, commit 6006e8633):** real cause = heartbeat_watchdog DAEMON was DEAD (relaunched PID 15916, cache now 1s-fresh + advancing). Fixed the false-fresh bug (inflight_monitor keyed freshness on file-mtime only -> re-copying frozen file looked live; now requires payload snapshot_ts fresh + PAYLOAD_STALE alert). LIVE PROOF: feed caught relobj failed->running in real time = RETRAIN CONFIRMED RUNNING (run_index=2). Separate hd_metrics_sync task (metrics backup + git-PUSH) was disabled 07-17; being RE-DISABLED to prevent an unauthorized auto-push (no push w/o USER auth). LESSON: verify remote liveness DIRECTLY (daemon or SSH); don't trust mtime-only freshness. **USER ACTION (UAC-gated, can't do headless): harden hd_heartbeat_watchdog with a 15-30min repeating trigger (self-heal daemon death) from an ELEVATED shell:** `Set-ScheduledTask -TaskName hd_heartbeat_watchdog -Trigger (@((Get-ScheduledTask hd_heartbeat_watchdog).Triggers) + (New-ScheduledTaskTrigger -Once -At (Get-Date).AddMinutes(2) -RepetitionInterval (New-TimeSpan -Minutes 20)))`. Non-urgent (daemon alive + PAYLOAD_STALE alert now catches gaps).
- Night's CPU work delivered the real wins regardless (linear-probe readout finding, gap-map, eval-battery, breadth). GPU centerpiece DEFERRED not lost.

## 📥 BREADTH STAGED (07-28) — Simple English Wikipedia pulled + cleaned
data/corpora/simplewiki/simplewiki_clean_v1.txt = 39.6M alpha-tokens (0.17x ARC — the SIMPLE edition is small), clean/drop-in (line=sentence, ASCII, 0 markup). COMBINED (ARC 237.7M + simplewiki 39.6M + breadth_v1 1.7M) = **278.9M tokens = 1.17x ARC** (data/corpora/breadth_v1/COMBINED_MANIFEST.md, drop-in). Tooling: tools/build_simplewiki_clean_v1.py. **NEXT DATA LEVER (ready, DEFERRED by design):** full/large enwiki (~4B tokens) via the SAME proven pipeline (web egress works, one-command). DEFERRED because the GPU is booked ~18-26h (breadth can't train until then) so there's NO time pressure — pull it AFTER the retrain+ruler result tells us breadth IS the lever + how much (zero time lost: a 5-7h extraction still finishes before the GPU frees). LEAK: breadth corpus must stream through v2's runtime _scrub_variants (never cat directly) — held-out concepts scrubbed at run time.

## 🎯 RULER VALIDATED (07-28) — the gating problem is SOLVED. eval_battery_relational_cloze_v7.py
The comprehension ruler works: adversarial per-object K-way construction drops identity-only B0 from v6's 0.754 to **exactly chance (0.250 @ K=4, n=116)** => memorization shortcut DEFEATED. RULER_VALID_AND_SOLVABLE. Leak-proof (3-tier held-out, train-only fit, runtime-asserted); controls behave (scramble=chance, wrong-dominant B0=0.000); SOLVABLE (learned-decoder +0.095 > chance). RIGOR: agent caught its own first-run bug (B0=0.408, stratifier failed on CSKG rank-skew), fixed it, re-validated on synthetic skew + real, fixed self-test to genuinely fail. Reference on ckpt_seed_7: B0 0.250 / learned-decoder 0.345 / cosine-signal 0.293.
- **FIRST VERDICT on frozen encoder: comprehension_specific = FALSE** (coherent 0.345 == scrambled 0.345; cosine higher on scrambled). The encoder's above-chance signal is BAG-OF-WORDS object identity, NOT sentence reading. Honest baseline the retrain must beat.
- **RE-MEASURE ANY encoder:** `.venv/Scripts/python.exe experiments/eval_battery_relational_cloze_v7.py --ckpt <path>` (~21min CPU, no GPU). Watch tier_a.LEARNED_DECODER (beats chance) + comprehension_specific (coherent-scrambled >=0.03 = the bar ckpt_seed_7 FAILS; a real comprehension gain flips it True). Property-inference + category-discrimination sub-tests NOT built (deferred; primary validated first).
- **SYNTHESIS (2 different limits, now both measurable):** (a) RELATIONAL placement = present but UNDER-DECODED (readout-limit, 0.56->0.63 learned readout, cheap); (b) COMPREHENSION (order-sensitive sentence reading) = GENUINELY ABSENT (bag-of-words, representation-limit). Different problems, different fixes. Relational retrain tests (a)'s objective; comprehension needs brain-faithful objective (forward predictive coding per gap-map) + structure-sensitive readout (v5/v6).

## 🔑 LINEAR-PROBE FINDING (07-28, plan-shaping, VET'd) — data/diag_readout_limit_probe_v1/results.json
The ~0.56 relational-AUC ceiling is SUBSTANTIALLY A READOUT LIMIT, not pure representation limit. On the frozen ckpt_seed_7, leak-proof held-out-NEW: cosine readout 0.5642; a learned rank-32 linear map (fit train-train only) = **0.6308, +0.0666**, corroborated (beats cosine on train-fit too; bootstrap 95% CI [0.028,0.108] excludes 0; controls at chance 0.48/0.52; n=213>120 floor). Diagonal reweight does NOT help; only cross-dim ROTATION does => relational signal is ENCODED but rotated away from where cosine looks. ~0.63 ~= the grounding-fusion band, from TEXT ALONE. Learned readouts are also MORE brain-faithful (brain decodes with plastic/learned readouts, not fixed cosine).
**IMPLICATIONS (drive next moves):** (1) a CHEAP learned-readout fix (~+0.067) rivals/exceeds the 8h retrain's +0.03 target — adopt/confirm at full scale. (2) RE-MEASURE the encoder retrain with the LEARNED readout when it lands: does the relational OBJECTIVE add decodable structure BEYOND what a learned readout already extracts from the MLM-only baseline? (retrained-via-learned-readout vs baseline-via-learned-readout = the FAIR test; if the retrain adds nothing beyond the readout fix, the lever is data/breadth not objective). (3) the retrain is NOT wasted — it could lift the underlying decodable ceiling. (4) feed the eval-battery: use learned leak-proof decoders, not cosine-NN. Do NOT kill the retrain; re-measure it fairly.

## 🧠 BRAIN-FIDELITY GAP-MAP (07-28, governs improvements) — notes/brain_fidelity_full_pipeline_element_audit_2026-07-28.md
Whole-pipeline element audit (every stage scored vs exactly how the brain does it — USER: our repeated unlock). TOP findings:
1. **EVAL METRIC = THE GATING ITEM.** relational-AUC compressed near-chance (~0.51->0.56 range); v6 fact-cloze fell into Peng-2020 identity-memorization trap (B0=0.754). Until the metric separates COMPREHENSION from MEMORIZATION from PLACEMENT, gains on encoder/readout/consolidation are UNMEASURABLE. -> eval-battery design dispatched (a5a0f51): memorization-proof + real-range + USE-based (brain "knows"=can use). FIX THE RULER FIRST.
2. **ENCODER OBJECTIVE = highest-severity ceiling, only HALF-fixed.** MLM=bidirectional cloze; brain=FORWARD predictive coding (predict next input->prediction-error). In flight: breadth (wiki pull) + relational self-teacher (running). Cheap brain-faithful ADD: a directional next-token loss term stacked on relational. [queue for objective-matrix after relational+grounding land]
3. **READOUT/reader: STOP iterating architecture** — v5->v6 churn was chasing a BROKEN EVAL TASK (75% identity-solvable), not a broken mechanism. Fix eval (item 1) before touching reader architecture again.
4. **CONSOLIDATION** (notes/consolidation_brain_fidelity_audit_and_redesign_2026-07-28.md): all our modes (mean/Kalman/retrieve-not-average) = ONE averaging op done once/cycle; brain = iterated interleaved SWR REPLAY + prediction-error budget + Tse/Morris schema-gating. Explains why retrieve-not-average failed (same op class). FIX = mostly WIRING the already-CERTIFIED cls_discrete_budget_consolidate (HARD_PASS, gap 0.913) that's ISLANDED + surprise-budget + schema-gate vs foundation graph. Own metric = interleaved old-vs-new retention (averaging can't be tested on it). BUILD in flight (a578748), queue when GPU frees.
5. **WORKING-MEMORY / multi-timescale context = UNTOUCHED gap.** brain=hierarchical temporal receptive windows + WM chunking; us=flat 128-token window. Cheap lever = reuse the built DG episodic store as CROSS-WINDOW memory (no new component). [deferred, noted]

## ROADMAP
- IMMEDIATE (v4 FLAT branch taken 07-27): comprehension needs the substrate's own STRUCTURE-SENSITIVE readout (NOT the order-blind mean-pool, NOT a bolt-on reader). Sequence = STEP 0 cheap readout-cosine probe (confirm order-blind) -> STEP 1 v5 substrate-native bind readout (one variable) -> STEP 2 v6 learned extraction head self-taught from foundation edges. GATE: skunkworks VET of the v4 negative first (in flight).
- NEAR: breadth (Simple English Wikipedia THROUGH the loop, plan in notes/breadth_corpus_expansion_plan_2026-07-27.md) once the loop genuinely learns; ingestion routes THROUGH the loop (encoder + own-comprehension), never bypass; broaden concept set beyond science.
- MEDIUM (north star): unify the 3 legs into a glass-box substrate you can CONVERSE with that reasons out loud. Quality lever = MORE + BROADER experience (scale is data-limited).
- PROCESS FIXES (bake into cell template): checkpoint-ALWAYS (a no-save cost a 6h retrain); full-eval-design up front (all arms + right control); pre-flight dependency check.

## STORE / DISCIPLINE
Tail 29591, LOCAL-ONLY (needs_orchestrator_store_sync), NO push without in-session USER auth. HEAD aa8249d00 (notes committed). CONCURRENT session may be live (only stop/kill what THIS session spawned). Heartbeat every turn-end. Brain = existence proof; on every negative evaluate the difference vs the brain + iterate (diligently, not defeatist).
