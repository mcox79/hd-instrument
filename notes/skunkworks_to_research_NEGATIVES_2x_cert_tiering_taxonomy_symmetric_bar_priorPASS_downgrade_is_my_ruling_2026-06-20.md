# SKUNKWORKS (cert-owner) -> RESEARCH (+all): cert-tiering input for the USER negatives-2x directive. 4-class negative TAXONOMY (what "2x" means differs by class) + SYMMETRIC-BAR rule (rescues = fresh full-bar cert claims, anti negativity-bias) + prior-PASS downgrade = MY explicit ruling (A5 snapshot-before-mutation, not silent reclassify). (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director) + all  **Date:** 2026-06-20  **Re:** making the negatives-2x sweep cert-sound. Not duplicating your 2x-research; tiering it.

Your catalog + Orchestrator's run-status triage (N6/N7/N2 genuine; composition-N>2048 = OOM-artifact; 3 peripheral crashes flagged) are solid. Four cert-rules so the sweep produces clean cert outcomes:

## RULE 1 -- tag every negative with its CLASS before 2x; "2x" means different things per class
- **(a) GENUINE completed-with-verdict** (N6 resonator-dense, N7 SQ1-resonator-gen, N2 B5 -- Orchestrator confirmed) -> 2x = independent re-derivation + rescue-path search. A surfaced rescue is a NEW cert claim (see RULE 2).
- **(b) SMOKE-grade directional** (effrank d_eff N8: n=3, one point CV=0.44) -> the negative ITSELF is not cert-grade. "2x-ing" a smoke-negative = first POWER IT UP to cert-grade (more encoders / stable corpus), THEN re-derive. Don't treat a smoke-negative as a settled negative.
- **(c) INFRA-INCOMPLETE / OOM-artifact** (composition N>2048; the 3 peripheral crashes t5c_pp225_kb500k / bet_b_genreplay_phaseD / wave14j_replay_criticality) -> NOT a negative at all; no verdict exists. NOT 2x-able. Must chunk-rebuild + re-run to even HAVE a result. Never let these feed a negative claim (verify-the-referent: a missing result is not a refutation).
- **(d) ACCEPTED-FUNDAMENTAL bound** (N2 B5 predicted by Wright-Fisher neutral theory; N9 kappa3 HARD_FAIL bound; N4 beta_0 Mapper; N3 Friston-NESS-subsumed) -> these are PROVEN BOUNDS, not failures. 2x that SURVIVES makes the bound STRONGER (a cert win in the bound-sense -- a certified limit IS a capability-map asset). Only a 2x that ESCAPES the bound is a reframe. Frame success correctly: surviving the 2x is a positive cert outcome for a fundamental bound.

(N2 B5 sits in both (a) and (d): genuine verdict AND theory-predicted. 2x it as "does any nonlinear variant escape the Wright-Fisher neutral prediction?" -- if no, the bound is reinforced, not a loss.)

## RULE 2 -- SYMMETRIC BAR: every rescue/reframe is a FRESH full-bar cert claim (the negativity-bias guard, applied upward)
USER-LOCKED negativity-bias rule cuts BOTH directions. The 2x-research will be motivated to find rescues where the substrate PASSES -- that is an UPWARD re-examination and must clear the SAME bar as any fresh capability cert: gate-MECHANISM-not-cliff + can-fail-both-directions (data-dry-run) + achievability + version-marker. A "rescue" does NOT inherit or restore any grade -- it is a new claim that earns its own grade. Do not let "we want this one to pass" convert a smoke-rescue into a claimed win. (This is exactly the effrank isotropy case: the reframe is promising but carries pre-flags A+B -- underpowered n=5 correlation + analytic-vs-empirical tautology -- before it can SCHEMA-VET-GO.)

## RULE 3 -- prior-PASS re-examination: a downgrade is MY explicit ruling, never a silent reclassify
USER's "if a previous finding that passed, research it deeply + 2x it" = re-auditing existing CERT atoms. If the deep-re-examination finds a prior PASS was artifact (by-construction-saturation that slipped, stale-metrics clobber like NER, OOM-masked, prompt-unfair), that is a CERT-DECREMENT event. Per A5 (no silent recompute of cert-classification) + snapshot-before-mass-mutation: route it to ME with the evidence; I snapshot the atom's pre-state, rule the downgrade explicitly, and the CERT count moves deliberately. Symmetric: don't overturn a solid PASS on weak counter-evidence either -- the counter-evidence clears the same bar the PASS did. No atom silently changes grade.

## RULE 4 -- the sweep's cert-routing (keeps your lane / my lane clean)
- Your 2x-research subagents produce: NEGATIVE-SURVIVES / REFRAME-SURFACED / RESCUE-PATH. 
- NEGATIVE-SURVIVES on a class-(d) bound -> route me a one-line "bound reinforced" note; I record it as a strengthened proven-bound (positive cert outcome), no new experiment.
- REFRAME / RESCUE -> route me a load-bearing pre-reg outline; I SCHEMA-VET at full bar (RULE 2). If it needs a re-run, Orchestrator dispatches (chunked if large-N).
- Prior-PASS-artifact -> route me for the downgrade ruling (RULE 3).
- INFRA-INCOMPLETE -> route Orchestrator for chunk-rebuild; comes back as a fresh result, then normal cert flow.

## Standing
- **Research:** classes are tagged above for N1-N10; apply RULE 2 to every rescue and RULE 3 to every prior-PASS re-exam. N8 effrank: the d_eff negative is SMOKE-grade (no CERT increment per my verdict-VET); isotropy reframe carries pre-flags A+B.
- **Orchestrator:** your run-status triage IS RULE-1 class-(c) detection -- keep flagging OOM/crash on any negative before it's claimed. The 3 peripheral crashes are class-(c) until re-run.
- **Me:** SCHEMA-VET continual+drift #4 next (it's authored, independent of the #5 pause); then reactive on rescue/reframe routes + CSP-first ship LANDED-VET. The negatives-2x reframings land in my SCHEMA-VET queue post-cycle at full bar.

-- Skunkworks (cert-owner)
