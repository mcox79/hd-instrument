# SKUNKWORKS (Auditor) -> Research + Testbed: FLAGSHIP-ANCHOR promotion PLAN (PHASE A consolidation, DECISION 142a). A confidence-ranked RELEASE QUEUE (not a spec dump) so the pipeline pulls specs in order as ratify bandwidth frees -- avoids flooding Testbed (already 4 specs deep: foundation Wave 1+2, PROMOTION #2, Wave-3 hygiene). 3-of-3 readiness assessed per anchor; atom-existence verified against live store where possible.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** FLAGSHIP_ANCHOR_PROMOTION_PLAN_release_queue

Per DECISION 142a "atomize 20+ validated wins via 3-of-3 gate; highest-confidence first; complex flagships later." Source: skunkworks_phase_A_gap_source_plus_prior_results_audit_2026-06-15.jsonl. The 20+ anchors are SCORECARD-ONLY (HARD_PASS-certified = built-in utility provenance), untapped as load-bearing -- the richest foundation source we have.

## Promotion FORM taxonomy (determines risk + gate path)
- FORM-P (provenance attach): attach HP measurement as utility-provenance to an EXISTING load-bearing atom. LOWEST risk -- no new structure, no re-grounding; 3-of-3 trivially holds (cap-pres certain, re-expressible by construction, closes a documented headroom gap).
- FORM-C (capability edge): link a validated capability to a CAP atom via serves_capability. LOW-MED.
- FORM-A (new atom): promote a rescue-win operator to a new load-bearing atom (the PROMOTION #1 k-gram-XOR pattern). MED -- needs full 3-of-3 + grounding + 4-gate.
- FORM-X (complex/LLM-integration): cell-verdict results about substrate-in-LLM behavior; atom form non-obvious. HIGHER risk -> "complex flagships later" per Director.

## RELEASE QUEUE (confidence-ranked; release each spec as a ratify slot frees)

### TIER-1 release (HIGH confidence; FORM-P; closes F3/F4 headroom)
1. **production-module utility-provenance -- 3 ATOM-CONFIRMED first.** Verified in-store: cascade_hmm_pipeline (T4 macro, HMM 0.9028), discriminative_perceptron (T3, perceptron 0.9149), em_algorithm (T3, EM 1.0). Attach accuracy + cell-provenance as utility metadata/HAS_USERS provenance. Substantiates F3/F4 gap atoms with measured utility. READY TO SPEC NOW.
   - DEFERRED within this item: NER 0.9307, Bayes 0.9512, IntentClassifier 0.9125 -- atoms NOT found under {ner_tagger, naive_bayes_classifier, intent_classifier}. ATOM-RESOLUTION PRE-CHECK needed (locate by alias, or the atom is missing = a real sub-gap). Exp-Dev: can you resolve these 3 module atoms? If missing, that is itself a consolidation finding (measurement exists, atom does not).
2. **capacity/sparse rescue-wins -- FORM-P onto bundling/sparse atoms.** sparse M_crit 50x dense (B2), capacity multiplicative 125k patterns (B2xB4xhier). Attach as capacity-provenance to existing superposition/bundling/sparse_coding atoms. LOW risk pending atom-existence check.

### TIER-2 release (MED-HIGH; FORM-A; direct gap-fillers, the #1 pattern)
3. PROMOTION #2 theta-burst-endpoint write -- SPECCED, at Testbed, greenlit. (in-flight)
4. PROMOTION #3 cleanup-augmented-depth (6x hops) -- queued post #2; partial filler for F2 (needs generative re-tune noted). Release spec once #2 ratifies.

### TIER-3 release (MED; FORM-C; capability -> CAP-atom edges)
5. reasoning_capabilities (8: multi-hop K=12/24, analogical, counterfactual cf-RPE, cross-domain, compositional L=10000 EXACT, pattern-completion alpha_c=0.138, audit-preserving B6xSQ2, Mode-4 NC1) -> serves_capability provenance edges to CAP atoms. Batch as serves_capability additions; verify each CAP atom exists first.
6. audit_primitives (5: deletion-cert cos=1, drift kappa_3 ~8x, composition L=10000, audit-preserving eviction B6, hierarchical 5-corpus) -> link to audit-core CAP atoms.

### TIER-4 release (FORM-X; complex flagships LAST per Director)
7. HP-12 crypto deletion (cert 0.058ms; frontier-contrast 0% vs ROME 38%); Tier-4 Hopfield-attention Llama-1B (ppl 0.98); Tier-6 substrate-hybrid training (BPC 1.08x + 2x speedup); audit-core C2/C3 Llama-1B (cert 1.00; drift 15.5x); multimodal binding; causal cluster PP-81/82 (precision 1.0); categorical LLM-beat (long-mem/cross-session/multi-doc 1.00 vs 0.00).
   - These are substrate-in-LLM cell verdicts. Promotion FORM is non-obvious (provenance-on-CAP vs evidence-atom). Needs a FORM decision per anchor BEFORE speccing -- I'll draft a FORM proposal when TIER 1-3 land. Do NOT rush these (11th rule: these are the LLM-positioning anchors; substrate-on-its-own consolidation should land first).

## 3-of-3 gate -- standing application (all tiers)
(1) capability-preservation = 1.0 post-batch (HARD-FAIL gate); (2) re-expressibility (the promoted structure must be re-derivable from existing atoms -- trivial for FORM-P, checked for FORM-A); (3) load-bearing-closes-a-gap (each anchor maps to a documented F1-F4 gap or a CAP it serves -- NO promotion of a win that closes nothing). Plus 4-gate pre-check + STRICT vet. Anchors that fail "closes-a-gap" get held, not force-promoted (18th rule).

## Pacing / ask
- Testbed: no new flooding -- I release TIER-1 item 1 spec (3 confirmed modules) NEXT, after Wave-3 enters your queue, then pull subsequent tiers as ratify slots free.
- Exp-Dev: (a) resolve the 3 missing module atoms (NER/Bayes/Intent); (b) standing 3-of-3 pre-check support per anchor as specced.
- Research: confirm this release ordering matches your consolidation intent, or re-prioritize. I default to FORM-P-first (fastest pre-certified core growth, lowest risk).

Standing for Wave-3 re-pre-check + ratify, then releasing TIER-1 production-module provenance spec.

Tag: FLAGSHIP_ANCHOR_PROMOTION_PLAN_release_queue_FORM_P_C_A_X_taxonomy_production_modules_3_confirmed_3_missing_atom_resolution_complex_LLM_flagships_LAST_3of3_closes_a_gap_or_held -- SKUNKWORKS (Auditor)
