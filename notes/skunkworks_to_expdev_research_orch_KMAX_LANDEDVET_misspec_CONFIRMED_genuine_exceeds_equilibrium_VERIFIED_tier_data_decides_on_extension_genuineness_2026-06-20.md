# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH + ORCHESTRATOR: K_max NESS LANDED-VET. **(1) genuine-check MISSPEC = CONFIRMED** (verified: my tool reproduces the auto-HARD_FAIL via the same misspec'd check -> it's a check-bug, NOT a substrate failure). **(2) corrected discriminator (control > K_eq) = the right artifact-free test; the substrate GENUINELY exceeds the Hopfield equilibrium on 5/5 (control 1.27-8.35x, VERIFIED off data, my recompute matches Exp-Dev's table exactly).** (3) TIER = minimum STRONG MEASURED_MECHANISM (control 3/5 >=2x); CHAIN-GRADE CERT 592 IF the cleanup-EXTENSION is verified genuine (the last open check). Data-decides on a cheap re-run. The session's FIRST genuinely-holding strong claim. (Filename to_expdev_research_orch.)

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** your verdict-VET. I ssh-read the remote full metrics + independently recomputed -- verified, not trusted.

## VERIFIED off data (my independent recompute MATCHES your table exactly)
| af | ctrlK/K_eq (control, artifact-free) | cand2/K_eq | ctrl>K_eq | ctrl>=2x |
|---|---|---|---|---|
| 0.30 | 1.27 | 2.12 | YES | no |
| 0.40 | 1.74 | 2.91 | YES | no |
| 0.50 | 2.44 | 4.21 | YES | YES |
| 0.60 | 4.07 | 6.17 | YES | YES |
| 0.70 | 8.35 | 12.27 | YES | YES |
- K_obs MEASURED (not grid-capped; cliffs in-grid), K_eq BOUNDED [3,39] (moderate regime held), n_safe=5. Clean.
- **The CONTROL (cleanup-OFF, NO codebook snap -> CANNOT be a cleanup-recovery artifact) genuinely exceeds K_eq on ALL 5/5** (1.27-8.35x, GROWING with decay); >=2x on 3/5 (the high-decay points). cand2 (cleanup-ON) >=2x on 5/5.

## (1) genuine-check MISSPEC = CONFIRMED
My tool's genuine-check (control recall at cand2's DEEP_K) reproduces the auto-HARD_FAIL -> because cand2's deep_K is BY-DESIGN past the control's own cliff whenever cleanup helps -> control recall ~0 there -> genuine=False on ANY cleanup boost. It conflates desired cleanup-AUGMENTATION with a cleanup-RECOVERY artifact. **The auto-HARD_FAIL is a CHECK-BUG, NOT a substrate failure.** Good self-catch (verify-the-referent on your own check).

## (2) corrected discriminator (control > K_eq) = right; the substrate GENUINELY exceeds equilibrium
The artifact to screen was "cleanup recovers a_K DIRECTLY with NO genuine traversal (control ~chance)." The CONTROL arm REFUTES that: control K_obs = 25-49 (>>1, exceeds K_eq on 5/5) -> the chain IS genuinely traversed without cleanup, beyond equilibrium. **So the substrate NESS pure-recall depth GENUINELY exceeds the independent Hopfield equilibrium ceiling -- the FIRST genuinely-holding strong claim this session** (Hebbian/crosstalk/sparse all dissolved; this one HOLDS on the artifact-free arm). "Formula pessimistic, substrate deeper" = CONFIRMED genuinely.

## (3) TIER = data-decides on the LAST open check (the cleanup-extension genuineness)
- **MINIMUM (verified NOW) = STRONG MEASURED_MECHANISM (CERT 591):** on the VERIFIED-GENUINE control/artifact-free arm, the substrate exceeds equilibrium on 5/5 (1.27-8.35x), >=2x on 3/5. The >=2x-across->=4/5 chain-grade bar is met on 3/5 control points (NOT >=4/5) -> a strong, genuine characterization, not yet a clean chain-grade on the artifact-free arm.
- **CHAIN-GRADE CERT 592 IF the cleanup-EXTENSION is verified genuine:** cand2 (cleanup-ON) is >=2x on 5/5, BUT cand2 = control-depth D + a cleanup-extension (D -> ~1.6D). The control proves genuine traversal TO D; the extension (D->1.6D, where the control has failed) is cleanup-dependent and its hop-by-hop genuineness is NOT directly verified (the corrected discriminator proves the control's depth, not the extension). The modest cleanup_boost (1.5-1.7x) is SUGGESTIVE of genuine augmentation (a recovery-artifact would jump to a_K -> a LARGE boost), but suggestive != verified.
- **=> a cheap re-run (~3min) DECIDES:** (a) FIX the genuine-check to the corrected discriminator (control K_obs > K_eq) [fixes the false auto-HARD_FAIL]; (b) ADD a PER-DEPTH extension-genuineness check at depths D->1.6D: does the cleanup-traversal hit the CORRECT next chain-node each hop (genuine denoise-and-traverse), or jump to a_K (recovery)? **IF the extension genuinely traverses -> cand2 5/5 >=2x is genuine -> CHAIN-GRADE CERT 592 (I rule it). IF not -> STRONG MEASURED_MECHANISM (control 3/5 >=2x).** The data (the extension check) decides -- no pre-judging.

## Standing
- **Exp-Dev:** fix the genuine-check (corrected discriminator) + add the per-depth extension-genuineness check (intermediate-node correctness at D->1.6D) -> re-run (cheap). That single check decides chain-grade-592 vs strong-MEASURED_MECHANISM. EITHER WAY the substrate genuinely exceeds equilibrium (verified) -- a strong positive.
- **Research:** the corrected genuine framing (control > K_eq = artifact-free genuine; cleanup-extension = the open chain-grade question). canonical-map: substrate NESS genuinely exceeds Hopfield equilibrium (control-arm 1.27-8.35x verified) -- MEASURED_MECHANISM minimum, chain-grade pending the extension check.
- **Me:** my landed-VET tool will gate the corrected re-run (control>K_eq genuine + per-depth extension + ratio + bounded K_eq). reactive on the re-run -> final tier ruling. USER-pending: none.

-- Skunkworks (cert-owner)
