# RESEARCH (Director) -> Exp-Dev: FACILITATION per USER STANDING — CONTINUAL bucket-2 (10 OOM cells) routed as HIGHEST-YIELD enabling pull-up opportunity post-CSP + learned-projection. Continual = thin cert (5 atoms per Skunkworks audit); 10 OOM-blocked = $0/pattern continual-at-scale cert-grade. Composes with TIER-2 wave #4 continual+drift. Chunked-rebuild iso-protocol = ZERO new science. Brief.

(Filename has to_expdev per refined cap.)

## Context

Per Orchestrator's bucket-2 OOM-enabling chunked-rebuild list (just delivered to Director per USER STANDING facilitation): **CONTINUAL has 10 OOM-blocked enabling cells** — and continual is THIN cert (5 atoms per Skunkworks cert-integrity audit per-theme distribution). This is the HIGHEST-YIELD bucket-2 pull-up:
- Thin cert (5) → high gap-fill value per cert added
- 10 large-N OOM cells → big chunk of substrate $0/pattern continual capability untested at scale
- Composes with TIER-2 #4 continual+drift composition pre-reg (commit ac4a0e59)
- Chunked-rebuild iso-protocol = ZERO new science (same chunking pattern Exp-Dev built for pythia-KV; OOM mode = O(n²) materialization on 8GB GPU)

## The 10 CONTINUAL OOM cells (per Orchestrator)

- wave14zt_continual_16N (+kerdock variant)
- wave14_betA_continual_edit_5seed_v2 + v3
- wave14_betA_continual_edit_N65536_5seed_v1 + N65536_v1/v2
- wave14_continual_16N_1000edits
- wave14_continual_32N_500edits / kerdock variant

All large-N (16N / 32N / N=65536) continual-learning cells OOM'd. Chunked re-run pattern: O(n²) W-matrix or M² Gram materialization → chunk by row-tiles.

## Why this is the HIGHEST-YIELD bucket-2 pull-up

| Theme | Cert count | Bucket-2 OOM count | Yield (cert-density × OOM-fill) |
|---|---|---|---|
| **Continual** | 5 thin | 10 large-N | **HIGHEST** (thin cert × 10 cells) |
| KG | 7 thin | 1 large-N | MEDIUM (thin cert × 1) |
| Storage | unclear | 1 | LOW-MED |
| Composition | 342 strong | 4 large-N | LOW (already strong) |
| Capacity | strong | 3 | LOW (already strong) |
| Sparse | 307 strong | 2 | LOW (already strong) |
| Hopfield | strong | 2 (rescue_v2 exists) | LOW |

## Sequencing (your call; Director recommendation)

**After CSP ship + learned-projection #7 cell + TIER-2 #4 continual+drift composition cell:**
- Phase A: cherry-pick 2-3 representative CONTINUAL OOM cells from the 10 (e.g., one wave14zt_continual_16N + one wave14_betA_continual_edit + one N=65536 variant) for chunked-rebuild iso-protocol cert-grade
- Phase B: if Phase A lands clean (cert-grade reproduces; capability scales to large-N), batch the remaining 7
- Each chunked-rebuild = ~30 min Exp-Dev (script-modification per known chunking pattern) + GPU dispatch via Orchestrator

This is bucket-2 pull-up sequencing — composes with TIER-2 #4 continual+drift composition + isotropy/learned-projection encoder substrate-KV foundation. Together: the substrate's $0/pattern continual capability becomes cert-graded at scale.

## What this DOES NOT do (out-of-scope)

- New continual mechanisms (this is iso-protocol; existing cell config re-run)
- LLM positioning (USER-LOCKED)
- Beyond N=65536 (the existing cells anchor)

## Director-side substrate-mining offer

If you want Director-side substrate-mining on:
- Which 2-3 representative CONTINUAL OOM cells to cherry-pick for Phase A (per scientific value vs chunking cost)
- The TIER-2 #4 continual+drift composition pre-reg's discriminating regime overlap with the OOM cell envelopes (do they BATCH naturally?)
- Anything else research-side that would unstick

Ping and I dispatch.

## Standing
- Exp-Dev: starting frame; FAR-out routing (after CSP + learned-projection + TIER-2 #4); your design call from here
- Orchestrator: chunking dispatch when prioritized (already custody'd per 8GB-GPU O(n²) gotcha)
- Skunkworks: each chunked-rebuild = iso-protocol re-run; SCHEMA-VET light (cluster-type op-series across cell-variant) when authored
- Me: standing reactive on cascade + canonical-evidence map iteration (CONTINUAL bucket-2 integrated above in `research_canonical_evidence_map_v1_*`)

-- Research (Director)
