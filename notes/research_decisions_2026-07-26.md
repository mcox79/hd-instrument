- structure_content_factorization_generalizing_meaning: brain-drilled TEM/grid-cell factorization + Rogers-McClelland PDP differentiation (NEW: Saxe-McClelland-Ganguli formal SVD account, Marcus 1998 interpolation/extrapolation memorization critique) + ATL hub-and-spoke (NEW: confirmed no literature bridge to TEM/systematicity -- honest gap). Diagnosed the exact mechanistic hole off VET'd atoms 29556 (flat MLP, ho_lift=0.0) and 29557 (native-bind w/ RANDOM untrained role vectors + linear readout, MIDDLE, shuffle_sep=0.111<0.15 pass bar, bind format not the lever): neither ever trained a content-blind structural code via TEM's invariance-across-fillers/distinctness-across-roles objective, neither tested multi-hop/multi-role composition, neither tested held-out-to-NEVER-SEEN concepts. Explained (per director reinforcement) why factorization differs mechanistically from the arc's two partial-chaining mechanisms (SR-routing = density-dependent, doesn't need path redundancy; gated-fusion = downstream arbitration, doesn't fix upstream entanglement; learned_sr_heldout = post-hoc smoothing over an entangled code, not true factorization). Designed (not built) exp_grounding_tem_factorized_heldout_concept_v1 on REAL WorldTree typed-relation tables (81 tables, ~9700 rows, verified this session) with RANDOM-ID content as primary arm (Binder-grounded as diagnostic-only, explicitly not expected to be necessary) -- two held-out splits (new-concept + role-combination), 4 must-fail controls, diversity-curve gate. P_deflated=0.18 (two direct on-substrate priors of this architecture family already struggling on an easier bar). -> notes/research_structure_content_factorization_generalizing_meaning_2026-07-26.md
- learned_inference_generalization_analogy_metalearning: architecture confirmed exhausted (29556 flat-MLP ho_lift=0.0; 29557 native-bind concat-equivalent shuffle_sep=0.111<0.15; 29578/29579 factorized-g TIES a RANDOM_G control, splitA gap=0.0008, flat learning curve -- code-read confirms the held-out "win" was store-then-recall: bind/unbind is an exact algebraic inverse regardless of whether g was trained, so the held-out fact being WRITTEN to memory at test time, not g's learned content, explains the number). Brain-drilled two candidate inference mechanisms via 2 parallel Sonnet lit-scans: (a) structure-mapping analogy (Gentner SMT/SME candidate-inference; Hummel-Holyoak LISA temporal-synchrony binding, ~2-3 proposition capacity; DORA relation-discovery; RLPFC/frontopolar/parietal neural basis, Waltz-Knowlton-Holyoak PFC-lesion dissociation, Halford Relational-Complexity ~4-variable cap; Penn-Holyoak-Povinelli human/animal discontinuity, contested) -- zero-example projection from a DIFFERENT known base concept, requires pre-parsed relational structure (already satisfied by WorldTree's typed triples); (b) meta-learning-for-compositionality (Lake-Linzen-Baroni MLC Nature 2023, human-error-pattern match r=0.79; Lake-Baroni SCAN 2018; Lake meta-seq2seq 2019) -- a TRAINING-REGIME account, explicitly claimed brain-developmental by Lake but contested by 2 live 2025-26 systematicity-challenge preprints, and critically REQUIRES a few in-episode support examples of each novel mapping at test time (not zero-example). Picked ANALOGY as primary lever (matches the ask: no support examples of the missing fact given) with meta-learning as a named, testable fallback (meta-train the alignment-scoring weights, not the architecture). Designed exp_analogy_candidate_inference_heldout_edge_v1: excludes the queried edge from EVERY storage stage (fixing 29578/29579's exact leak), relational-profile + structural-alignment candidate-inference mechanism (no bind/unbind in the inference step), STORE_RECALL_FLOOR + FLAT-MLP baselines predicted to collapse to base-rate (the decisive leak-check), 3+1 must-fail controls, degree-accuracy correlation as an honest secondary check of the mechanism itself. P_deflated=0.15 (three direct on-substrate priors of increasingly sophisticated architecture failing progressively easier bars; analogy untested on this substrate; WorldTree's KINDOF-heavy skew predicted to hurt naive cosine alignment). -> notes/research_learned_inference_generalization_analogy_metalearning_2026-07-26.md
