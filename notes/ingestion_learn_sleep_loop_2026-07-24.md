# INGESTION LEARN-SLEEP LOOP -- autonomous curriculum reading (phase start 2026-07-24)

**USER-authorized (2026-07-24):** run an ordered graded curriculum through an AUTONOMOUS read -> flag-unknowns -> condense/fix -> periodic-sleep-generalize -> store loop. Director = IN-THE-LOOP VERIFIER of end-to-end IMPACT (not just "it ran"). Full-auto: get it up + really learning.

## SUCCESS CRITERIA (USER-confirmed)
1. **Reads BETTER over time** -- a rising held-out reading curve as it ingests, NOT flat. (The point.)
2. **Knowledge STORED + queryable + IMPACTFUL downstream** -- measured on HARDER decisions (knowledge proven redundant for SIMPLE accuracy, 29491), not easy reading.
- INTEGRITY watches: flag-unknowns gate FIRES (non-vacuous); SLEEP GENERALIZES a rule (not the known averaging-collapse on per-item facts); knowledge impact at real n (not a 3-item hint).

## CORPUS = OneStopEnglish (modern, replaces anachronistic 19c McGuffey/LitBank)
data/corpora/onestop/ (cloned CC BY-SA). Texts-SeparatedByReadingLevel/{Ele,Int,Adv}-Txt = 189 real modern articles x 3 aligned reading levels + Sentence-Aligned/. => clean learning-curve test: INGEST the Ele version, MEASURE reading on the harder Int/Adv aligned version of the SAME content.

## STATE
- **CYCLE-1 (exp_ingest_learn_sleep_loop_cycle1_v1) VET'd+BANKED 29527 (MM, commit f90d9eb85):** the loop EXISTS + runs END-TO-END with REAL banked components (SituationReader 29518 / ClarifyGate M1.8 / condenser 29476 / learner-MDL 29487), reproduced bit-exact. HONEST STATE:
  - GENUINE: SLEEP generalizes a RULE not averaging (real MDL two-part code; engine independently promoted rule on class-labels [compression 1.174, 3 rules] vs KEEP_EPISODIC on per-item [0.964<1] = averaging-collapse DETECTABLE). Flag surfaces the reader's intrinsic abstain (8 flagged = 7 unread + 1 unfilled role).
  - NOT YET: **reads-better-over-time = NULL by construction** (NO knowledge->reader feedback wire; step_read identical before/after). **Knowledge-impact = n=3 SUGGESTIVE-not-PROVEN** (3/3 ON vs floor OFF, but P=0.125 = coin-flip; NOT the impact criterion met).
  - GAPS (all VET-confirmed): (1) no feedback wire [the null's cause]; (2) gold cell-supplied [transparent, reader never sees it]; (3) grade-unit MISALIGNMENT [condenser mined a 60-sent SMOKE slice, NOT the read passage]; (4) flag->condense not causally wired; (5) reader UNDER-COVERS 2/9 [recall 0.222, misses present/aux/copular has/is/catch/fed/left]; ClarifyGate calibration semi-by-construction (hardcoded 0.2/0.9 proxies, not a real confidence distribution).
- **LOOP-v2 (a772379) IN FLIGHT -- the "really learning" upgrade:** (1) FIX reader coverage via spaCy-POS predicates (29522 win; recall 2/9 -> higher); (2) WIRE knowledge->reader FEEDBACK (condensed selectional knowledge feeds the reader's role decision = the 29471/73 lever, knowledge lifts who-did-what 0.56->0.83); re-point at OneStopEnglish Ele; **LEARNING-CURVE test (ingest Ele -> read harder aligned version -> RISING or flat, HONEST)**; knowledge-impact at n>=20. Rising=scale to autonomous full run; flat=diagnose why the feedback wire didn't lift.

- **CYCLE-2 / v2 (exp_ingest_learn_sleep_loop_cycle2_v1) landed, VET a7371162 in flight (agent-reported VET-PENDING):** COVERAGE FIX = decisive PASS (SpacyEventReader subclass, spaCy clause-head predicates: grade-1 recall 0.222->1.000 [2/9->9/9], F1 0.36->0.67 -- no banked-module edit). KNOWLEDGE-IMPACT at n=48 = PASS (unseen-noun 2AFC ON 0.75 vs OFF 0.50, +0.25, random 0.52 -- the condensed knowledge is GENUINELY GOOD, not the cycle-1 n=3 noise; 375 verbs queryable). BUT **LEARNING CURVE = FLAT/hurt** (acc 0.983->0.837 across ingest fractions, rise -0.146): the feedback-wire ablation shows +knowledge HURTS aggregate (structural 0.983 / conservative-gate 0.837 / naive 0.645) but HELPS the hard slice (nearest!=gold n=31: 0.0->0.258 +0.258). AGENT DIAGNOSIS = who-did-what on graded text is STRUCTURE-SATURATED (spaCy dobj-gold ~= nearest-noun ~98% -> no headroom) = 29491 reconfirmed on modern corpus; knowledge is good, the wall is reading INTEGRATION (needs a role-ambiguous task). SLEEP: averaging-collapse detectable BUT class-rule-induction did NOT cleanly generalize on diverse real-news verbs (compression 0.907<1, weaker than curated McGuffey). **DIRECTOR CIRCULARITY FLAG (in the VET): the 'silver gold' = spaCy dobj + the reader = spaCy -> the 98%-structural + flat-curve MIGHT be spaCy-self-agreement ARTIFACT, not true saturation. VET adjudicates: real-saturation (-> cycle-3 = hard/role-ambiguous reading task, PP-attach 29472) vs circular-artifact (-> cycle-3 = re-measure vs REAL human gold, headroom returns).**

- **CYCLE-2 VET'd+BANKED 29528 (MM, commit d012a7f0a):** coverage-fix CONFIRMED (subclass, git-diff clean, no banked edit). **KNOWLEDGE GENUINELY GOOD CONFIRMED + POWERED: 2AFC ON 0.75/OFF 0.50 +0.25, p=0.00036, 95%CI[0.604,0.864], circularity-FREE.** Flat curve HONEST (empty-knowledge==structural exactly) BUT **CIRCULARITY CONFIRMED: who-did-what gold = spaCy dobj SILVER + reader = spaCy noun-chunks SAME parse -> the 0.9826 structural ceiling is INFLATED vs human gold = a WEAK can-fail by construction.** Feedback-wire: conservative halves harm; hard-slice +0.258. SLEEP honest sub-negative (HF_STRUCTURAL_BOUND: did NOT generalize high-entropy real-news verbs, compression 0.907<1 KEEP_EPISODIC, vs McGuffey 1.174; averaging-collapse still detectable). **VERDICT: the loop LEARNS GOOD KNOWLEDGE (criterion #2 substantially met on knowledge tasks); 'reads better' (#1) is UNPROVEN not disproven -- the who-did-what test was unfair (saturated + circular gold). Need a fair hard-reading test.**
- **CYCLE-3 DISPATCHED (a8301ef2) = the FAIR reads-better test:** a ROLE-AMBIGUOUS knowledge-sensitive reading decision (PP-attach 29472 / reversible-passive / dative -- NOT coref=centering) + a REAL HUMAN non-circular gold (Ratnaparkhi PP-attach / PropBank-SRL / hand-labeled) + the INGEST LEARNING CURVE (does hard-reading rise as knowledge accumulates?). Structure baseline must be BEATABLE (headroom ~0.70 not ~0.98). Rising=criterion#1 met fairly; flat=diagnose coverage-vs-mechanism.

## STRATEGIC REFRAME (Director, VET-grounded 2026-07-24)
The loop's VALUE = the GOOD KNOWLEDGE it builds (proven real, +0.25 p=0.0004), which pays off on HARD decisions + downstream (reasoning/conversation) -- NOT on making SIMPLE reading better (structure-saturated, redundant per 29491). This MATCHES the north star (knowledge's home = harder decisions + conversation). So "really learning" = accumulating good knowledge that helps the hard stuff (happening). "Reads better" shows ONLY on hard reading (cycle-3 tests it fairly). SLEEP-on-real-verbs is weaker (high-entropy) = a lever (more data / better clustering). AUTONOMOUS SCALE-UP gated on cycle-3: if hard-reading rises -> scale; regardless, the knowledge-accumulation itself is the durable value.

- **CYCLE-3 VET'd+BANKED 29529 (MM, commit 5ce5201fb):** the FAIR reads-better test PASSED (partially): on non-circular human-UD PP-attach gold, the ingest loop reads BETTER as it reads more -- +5.0pp on the covered subset (n=141), SCRAMBLE-COLLAPSES to 0.6502<structure => GENUINE reading knowledge not artifact; non-circularity CLEAN (spaCy never parses the test); aggregate rise +0.0157 COVERAGE-limited (31.6%, OneStop-news vs UD-web domain mismatch), NOT mechanism-limited. => reads-better (criterion #1) ANSWERED FAIRLY+PARTIALLY; lever = scale/domain-matched ingest.

## PIVOT -- KNOWLEDGE ON A HUMAN SCALE (USER 2026-07-24, full-auto, user away)
USER expanded the vision (4 messages): (1) KEEP OneStopEnglish (serves a purpose); (2) the goal = LEARN THINGS (actual content/knowledge), like grade school, NOT reads-better (mechanics); (3) ALSO build a K-12 curriculum -- "1st through 12th grade, all the textbooks", actual knowledge; (4) **DESCRIBE the substrate's knowledge on a REAL HUMAN SCALE, same as a human** (e.g. "at grade-X"). => the reads-better axis (cycles 1-3, proven) is instrumental; the MAIN axis = ACQUIRE + RETAIN CONTENT, measured on a human grade-scale. Fits the substrate ("meaning=assignment-lookup": store facts, recall them -- honest watch = does it retain SPECIFICS not gist [sleep-null-on-facts]).
INSTRUMENT + CONTENT (downloaded): **ARC** (data/corpora/arc/) = grade 3-9 science exam MC questions (ARC-Easy 2251 + Challenge 1119, the HUMAN-SCALE yardstick) + **ARC_Corpus.txt (14M science sentences, the ingestable content)**. One resource = both the measure AND K-12-ish science content.
- **CELL a670c80e IN FLIGHT (the foundation):** glass-box substrate-native retrieval MC-QA harness (encode Q+choice HD, score by SUPPORT from the store, pick highest, inspectable evidence) + HONEST BASELINE (empty/OneStop store -> expect ~chance 0.25 = "bottom of the human scale") + FIRST INGEST-AND-CLIMB (ingest ARC science corpus chunk -> re-measure -> does science-knowledge climb above chance?). Human-scale reporting (Easy~gr3-5 / Challenge~gr6-9, % -> grade proxy, honestly labeled). Scramble control. Honest: baseline MUST be ~chance (else leak); climb=discriminator; flat=diagnose.
SEQUENCING (full-auto plan): (A) establish ARC measure + honest baseline + first climb [in flight]; (B) if the climb works -> scale the ingest (more ARC corpus / autonomous) + broaden the CURRICULUM (CK-12/OpenStax K-12 textbooks across subjects) + the MEASURE (RACE reading-comp gr-middle/high, more grades) toward the full 1-12 human scale; (C) report the substrate's grade-level + the climb curve. HONEST EXPECTATION: substrate starts near-zero on the human scale (no world knowledge); deliverable = the SCALE + the honest baseline + the climb, NOT an overnight "grade 8".

## HUMAN-SCALE FOUNDATION RESULT (2026-07-24)
- **ARC baseline+climb cell (exp_arc_knowledge_scale_ingest_climb_v1) landed CLIMB_PASS, VET ab0d6c63 in flight (agent-reported VET-PENDING):** the HUMAN SCALE is now ESTABLISHED + the honest baseline read. HARNESS = glass-box substrate char-trigram HD encode(stem+choice) -> max-cosine over the ingested store -> argmax (no external LLM; supporting sentence inspectable). **BASELINE = CHANCE: empty store 0.252 (Easy 0.254/Challenge 0.247), controls agree, no leak = the substrate has ~ZERO science knowledge = bottom of the human scale (honest).** CLIMB: ingesting science -> Easy 0.254->0.373 (+0.119) MONOTONE; SCRAMBLE control collapses to 0.243 (chance) = the gain is GENUINE text-matching not artifact; Challenge FLAT 0.246 (no multi-step reasoning). **TEST-TARGETING CAVEAT (VET adjudicating the FAIR number): ingestion = per-question top-k IR (answer-agnostic but STEM-targeted) -> store tailored to the test; the BROAD-ingest control = 0.282 (+0.028) = the FAIR 'learned-a-curriculum' number; +0.119 is test-targeted-optimistic.** Human-scale proxy (HONESTLY deflated, uncalibrated anchor): Easy 0.373 ~ "emerging grade 2-3" BUT far below a real grade-3-5 student (0.7-0.9) -> the substrate is LEARNING TO CLIMB, NOT at grade level. LIMITS: char-trigram = LEXICAL-only (no deep semantics: cat/kitten share no trigrams); no reasoning (Challenge flat). => the path UP = the proper vetted HD-SEMANTIC KB (a7d4e4ca building) + reasoning + broad curriculum.
- **KB+TRUST-VETTING foundation (a7d4e4ca) IN FLIGHT:** the HD fact store + provenance/trust + trust-ranked conflict resolution (replace/combine/flag) -- replaces the thin count-tables + the failed auditor. Once landed -> re-point ingestion INTO it (deeper than char-trigram lexical) + re-run ARC on the proper vetted semantic store.

## KB+TRUST-VETTING FOUNDATION -- BUILT (2026-07-24)
- **HD FACT STORE + source-trust ingest-vet (hdlab/hd_fact_store.py, exp_hd_fact_store_source_trust_vet_v1) BUILT + demonstrated PASS, agent-reported VET-PENDING (fact-store VET HELD to serialize behind the ARC VET ab0d6c63 -- never 2 skunkworks banking concurrently):** the USER trust-vetting design REALIZED. Fact (subj,rel,obj) = ONE role-slot HD bundle with SOURCE+TRUST bound IN (reuses EventBundleCodec/RoleSlotSummarizer, genuine); glass-box round-trip 1.000 (provenance+trust recover by unbind). Native (s,r) conflict retrieval (threshold 0.75 in a wide gap, can-fail). **All 4 trust-rules fire correctly (40/40): higher-trust REPLACE / lower-trust DROP / equal-trust functional-contradiction FLAG-unresolved / equal-trust multivalued COMBINE.** 160 trials: detection precision/recall 1.000; resolution 40/40; **CLEAN FALSE-FLAG = 0.000 (120 clean+hard-distractors) -- BEATS the failed condenser-auditor 0.53 (the point).** Deterministic. HONEST: source-trust (trusts curation, does NOT verify factual truth = student-model trade); the 0-false-flag is partly BY-DESIGN (conflict-only check, not correctness-check -- sidesteps what the auditor failed at). STUBBED (honest): cardinality (FUNCTIONAL/MULTIVALUED) + source->trust are HAND-tables (real ingestion needs schema/KB-sourced); no subsumption (crimson<red); status=symbolic-flag not HD; scale = 140 facts, O(n) retrieval not KB-scale-benchmarked.
- => **WE NOW HAVE A VETTED, GLASS-BOX, HD-NATIVE KNOWLEDGE STORE** (replaces the thin JSON count-tables + the failed auditor). NEXT: wire ingestion INTO it (curated triples LLM-built+KB-vetted per the foundation program, flowing through the trust-gate) + share codebooks w/ KGStore + route FLAG'd facts to a review queue + re-run ARC on the PROPER semantic vetted store (vs the char-trigram lexical baseline). Then: scale probe (KB-scale conflict-retrieval batching) before production ingest.

- **ARC VET'd+BANKED 29530 (MM, commit 868ee899f):** baseline=CHANCE confirmed; climb GENUINE (scramble collapses); **NO answer-leak (code-verified: IR index from stem+ALL choices, never answerKey).** FAIR NUMBER PINNED: broad-curriculum climb = **+0.043 Easy (0.254->0.297)** = the honest human-scale number; the +0.119 was TEST-TARGETED optimism (store tailored to test stems; broad store LARGER yet climbs LESS => tailoring not size). DOWNWARD proxy correction: fair 0.297 < 0.30 => "below grade 3 / ~chance" = **substrate STILL essentially AT THE CHANCE FLOOR** (+0.043 real but tiny, 4.8 SE). Challenge flat (no reasoning). Limit = lexical char-trigram + no reasoning. **=> honest human-scale reading: substrate knows ~nothing on the science scale; the current LEXICAL+UNVETTED word-matcher CANNOT lift it off the floor -> the lever = the vetted HD-SEMANTIC KB + a reasoning layer (NOT more retrieval). This VALIDATES the KB architecture direction.**
- **FACT-STORE VET a51c7990 IN FLIGHT** (serialized after ARC bank): verifying the trust-vetting + scrutinizing the perfect scores + confirming the O(n)/capacity UN-OPTIMIZED foundational-scale gap (per USER confidence-check: storage NOT yet optimized -- O(n) linear retrieval bypasses the substrate's partitioned/cleanup/capacity machinery; capacity un-benchmarked).

- **FACT-STORE VET'd+BANKED 29531 (MM proven-bound, commit 7326615d0):** trust-vetting DESIGN validated (round-trip 1.0 genuine reuse; 4 rules 40/40; clean false-flag 0.0). VET SHARPENINGS (3 durable): (a) conflict-detection is CONSTRUCTION-DETERMINED = an EXACT (s,r) dictionary lookup dressed as HD cosine (same-(s,r)=bit-identical bundle cos 1.0 + redundant string-confirm); NO fuzzy/surface-variant tested (USA vs United States) -> a SEMANTIC-representation gap; (b) clean-false-flag-0 is BY-DESIGN (conflict-arbitration NOT correctness-vetting; "beats auditor 0.53" is apples-to-oranges -- it AVOIDS the failure by not attempting correctness-check, trusts curation); (c) foundational-scale blocker = the STORAGE ENGINE (O(n) linear retrieval + un-benchmarked capacity + hand-tables). = right design, PROTOTYPE engine. [Immune-trust design ancestor: "Analog 5 per-source trust scoring / V(D)J negative-selection" -- this BUILD realizes it.]
- **KEY CONVERGENCE:** the O(n)-storage gap (#1) AND the exact-match-conflict gap (#2) + the ARC lexical-floor all point at the SAME deeper need = MATCH BY MEANING not spelling (semantic HD representation). Fix the representation -> scalable retrieval + fuzzy-conflict-catch + ARC climb, all at once.
- **STORAGE-OPTIMIZATION cell DISPATCHED (a4ac04dc, USER-flagged priority):** (1) CAPACITY BENCHMARK (facts-before-crosstalk wall vs n_dim, using the banked capacity theory -> is the fixed store viable for millions or does it need partitioning?); (2) SUB-LINEAR retrieval (re-base onto substrate's proven store.py/cleanup_family/random_indexing, correctness-equivalent to the O(n) prototype). SEMANTIC upgrade kept SEPARATE (next).

- **STORAGE-OPTIMIZATION cell landed PASS, VET a384be70 in flight (agent-reported VET-PENDING):** the storage engine is now FOUNDATION-SCALE-OPTIMIZED on crosstalk+retrieval. KEY: the store is SHARDED (each fact = its OWN bundle, NOT superposed) -> single-fact recovery INDEPENDENT of #facts; only crosstalk = cleanup-codebook argmax margin ~0.375*sqrt(n_dim) (exponential-in-N). **CAPACITY: recovery 1.000 to 1,000,000 facts at n_dim>=512 (N>=1024 detection P/R 1.0) = NO crosstalk wall; capacity is RAM-bound NOT crosstalk-bound.** RETRIEVAL: O(1) content-hash conflict index (sr_key deterministic->bit-identical->perfect hash, ANN would ADD error), BYTE-EQUIVALENT to the O(n) prototype (3960 ops, 0 mismatch), 32x speedup growing unbounded; + codebook cache. Default use_index=False preserves 29531 bit-for-bit. HONEST REMAINING: (1) bit-packed/on-disk fact-vector storage (RAM ceiling ~1GB packed/million = the real millions-scale step); (2) use_index default-flip; (3) SEMANTIC/fuzzy = separate (O(1)-hash is EXACT-only; fuzzy needs ANN/LSH).
- **STORAGE-OPT VET'd+BANKED 29532 (commit 0c5dba8b4):** BOTH cruxes CONFIRMED by independent recompute -- capacity RAM-bound-not-crosstalk (target fact byte-identical after 5000 inserts = sharding proven; wall table reproduced, n_dim>=512 no wall to 1M); O(1) index BYTE-EQUIVALENT (independent 3318+1035 ops, 0 mismatch; perfect hash 0/20000 collisions). Honest-remaining flagged (1M is analytical-probe not a real 1M build; RAM/on-disk unbuilt; semantic/fuzzy separate). **TIER CONTESTED (Director, mirrors 29522 POS): skunkworks banked CHAIN_GRADE; I contest -- the cruxes are CONSTRUCTION-DETERMINED (sharding->per-fact-independence by-design; exact-keys->0-collisions trivially true) + it's a STORAGE-ENGINE optimization not a reading/reasoning capability-CG (only genuine arc CG = 29504). Recommend down-correct to MM; reconsideration a384be70 in flight. Do NOT propagate CHAIN_GRADE.** The WIN (foundation-scale storage) is solid regardless of tier. **TIER RESOLVED: skunkworks CONCEDED both points, down-corrected CHAIN_GRADE->MEASURED_MECHANISM (amend commit dcff1fabf), retracted the mis-attribution. Guardrail held (still ~1 genuine arc CG = 29504). 29532 = MM proven-bound, CERT +1.**
- **SEMANTIC ENCODER ("turn on meaning", USER-emphasized) cell DISPATCHED (a34c3af4, in parallel):** build a SEMANTIC HD encoder = WordNet-structure (glass-box synonyms/hypernyms, USA==United_States alias) + distributional (gensim GloVe/word2vec) -> substrate HD (reuse exp_encoder_word2vec_substrate_bind). PROVE meaning-matching (semantic sim BEATS char-trigram lexical on synonym/related/false-friend). FIRST PAYOFF = re-run ARC w/ semantic -> climb above the +0.043 lexical floor? Honest: correlation-hurts-capacity (measure/whiten); glass-box mix; fuzzy-retrieval=ANN separate.

- **SEMANTIC ENCODER landed PASS/CLIMB_RISING, VET a0de547f in flight (agent-reported VET-PENDING): MEANING-MATCHING WORKS (USER goal DEMONSTRATED).** SemanticHDEncoder = fused(GloVe[w] + 0.5*GloVe[WN-synonyms] + 0.3*GloVe[WN-hypernyms]) -> JL 300->2048 HD (reuses word2vec-bind). **MEANING-MATCH PROOF (HARD_PASS): semantic AUC 0.960/sep 0.507 vs char-trigram AUC 0.000/sep -0.400 (spelling ranks EVERY false-friend ABOVE EVERY synonym = backwards); gap 0.907; SCRAMBLE collapses 0.960->0.665; alias usa~america 0.691 vs 0.012.** ARC CLIMB (payoff): char 0.354 -> semantic 0.396 (+0.042 in-regime n=1000, scramble +0.149) = meaning lifts the science scale, MODESTLY. HONEST: (a) still FAR below grade (thin 25k store + retrieval-not-reasoning + correlated codes); (b) SUPPLY-SEMANTIC -- the MEANING is SUPPLIED (GloVe does the bulk: GloVe-only AUC 0.965, WordNet +0.049 = inspectable structure+alias); substrate ENCODES it (like 29522 POS supply, NOT substrate-learned -> MM not CG); (c) **semantic codes STRONGLY CORRELATED (anisotropy 0.588 vs char 0.122) = correlation-hurts-capacity -> the 29532 1M-capacity (on ~random char codes) MUST be RE-CHECKED on semantic vectors; whitening = a lever, not built.**

- **SEMANTIC VET'd+BANKED 29533 (MM supply-validated, commit 78ba4a8d5):** meaning-match GENUINE (recomputed bit-exact: AUC 0.9604 vs char 0.0, scramble collapses, NOT cherry-picked). SHARPENINGS: (a) the AUC-0.0-char is a DISSOCIATION discriminator (false-friends designed lexically-close) not a generalization benchmark; (b) ARC +0.042 = real-SIGNAL (vs scramble +0.149 ~7sigma) but semantic-BEATS-char is MODEST/marginal (1.94sigma); (c) SUPPLY-semantic confirmed (GloVe supplies ~all meaning [glove-only AUC 0.965], WordNet=glass-box structure/alias not accuracy) = MM not CG; (d) **CRITICAL: 29532's 1M-capacity was on EXACT bipolar keys NOT correlated semantic vectors (anisotropy 0.588) -> MUST re-check capacity on semantic codes (revival criterion).**
- **CAPACITY-RE-CHECK-ON-SEMANTIC cell DISPATCHED (aacccd1d):** capacity on SEMANTIC (correlated) role-fillers vs WHITENED vs random(29532) baseline; where's the crosstalk wall on correlated codes; does whitening restore it? KEY nuance: roles random, only FILLERS semantic -> the hard part = SEMANTIC-CODEBOOK cleanup (similar concepts CLOSE -> harder argmax than orthogonal symbols). Honest: if raw-semantic caps + whitening doesn't fully fix (semantic overlap is intrinsic) -> HYBRID (exact-ID keys for facts + semantic only for FUZZY retrieval). Do NOT claim foundation-scale on semantic until measured.

- **CAPACITY-RE-CHECK VET'd+BANKED 29534 (MM proven-bound, commit a3c595af6):** FOUNDATION CONFIRMED READY on the real semantic rep, WITH honest corrections. **SURVIVED recompute:** no capacity wall on the store's real bipolar GloVe rep, proven to **V=100k on the GENUINE full-fact path** (M3 full-fact recovery 1.0 at n512/V30k -- NOT resting solely on the vacuous isolated arm); can-fail REAL (M7 full-fact 0.113 n64/aniso0, 0.673 n512/aniso0.7, wall aniso~0.699, real rep far below -- not analytically pinned); sharded-not-Hebbian = FAIR mechanism distinction (the residual cleanup-crowding channel IS tested, walls only at high aniso); M5 whitening preserves synonym structure (~83% retained, but non-load-bearing since no wall). **CRACKED (mechanism story wrong, conclusion holds):** (1) "sign() washes out 0.588->0.015" is NOT apples-to-apples -- the store's GloVe-only bipolar codes were NEVER at 0.588 (that was 29533's FUSED GloVe+WordNet continuous encoder, a different object/measure); continuous GloVe-only was already 0.0137 BEFORE sign(). sign() is not the decorrelating hero; the rep was just never that correlated in the pairwise-cleanup sense. (2) V=1M is CONSTRUCTION-DETERMINED (isolated pure-common-mode probe, immune by construction; inherited from 29532 random-code, not re-verified on semantic) -> honest genuine bound = **V=100k**, 1M analytical. (3) mean aniso 0.009 UNDERSTATES local crowding (bipolar off-diag max 0.828, p99.9 0.313 near-synonyms) -- still safe (self=1.0) but not "0.015 everywhere". Amends 29533's "1M MUST be re-checked" caveat (0.588 doesn't apply to the store's bipolar rep) WITHOUT demoting it. => **NO forced exact-key/semantic-fuzzy hybrid; single semantic-rep store IS foundation-scale (100k proven / 1M analytical). FOUNDATION PHASE CLOSED.** [prior VET-PENDING narrative below superseded by this banked result]
- (superseded VET-pending capture) CAPACITY-RE-CHECK LANDED FOUNDATION_READY, VET a7b469ed: the 29532 1M-fact claim SURVIVES on the real semantic rep. MECHANISM = **bipolarization washes the correlation out**: continuous encoder anisotropy 0.588, but the store is BIPOLAR and sign() of GloVe->JL over a broad 100k codebook has anisotropy ~0.015 (the self-test's 0.148 was the top-2000 function words, not broad). Capacity 1.000 (isolated cleanup M2 + full-fact round-trip M3) at all n_dim to V=100k; anisotropy-matched synthetic probe holds 1.000 to 1M. WALL is REAL + can-fail (M7): lives in the NOISY full-fact path at SMALL N (N=64 fails even aniso 0 = 29532 Gate-D; N=128 0.42->0.07 as aniso 0->0.7; N=512 wall ~aniso 0.70) -- the real rep aniso 0.015 sits ~46x below the N=512 wall, and N>=2048 no wall. WHITENING N/A (no wall to fix) but M5 refutes the feared trade-off: ZCA orientation-preserving, synonym sep raw 0.427/whitened 0.392/random 0.002 = whitening keeps fuzzy-match structure. Conflict false-rate negligible (raw 0.0013/whitened 0.0004/random 0.0). => **NO forced exact-key/semantic-fuzzy hybrid; the semantic store IS foundation-scale.** DIRECTOR VET-FOCUS (5 cracks to attack before banking): (1) the 0.588->0.015 anisotropy collapse = apples-to-apples? recompute bipolar aniso independently; (2) recovery-1.000-everywhere -- isolated arm is CONSTRUCTION-DETERMINED (self-score maximal = tautology), only the full-fact NOISY arm carries info -- which arm does "no wall" rest on?; (3) M7 truly telemetry-can-fail not analytically-pinned; (4) M5 whitening-preserves-structure real not weak-by-construction; (5) SCOPE DODGE -- is "banked correlation-hurts doesn't transfer (sharded != Hebbian superposition)" a fair mechanism distinction or a re-label to escape a negative? Prior-art extended (credited): reference_correlation_hurts_associative_store_capacity_decouple_from_retrieval_2026-07-08. STUBBED: ANN/LSH sub-linear fuzzy retrieval; literal-V=1M real-store ingest (memory-bound, 1M = analytical probe).

## ARC TASK-SHAPE DIAGNOSTIC (WorldTree V2.1 gold, 2026-07-24) -- the aggregation question ANSWERED
WorldTree V2.1 sourced LOCAL (gitignored, 83MB, 4365 questions w/ gold explanation graphs; same AI2 research-license class as ARC). Lightweight inline parse of the gold support-sets (scratchpad/worldtree_taskshape_diag.py):
- **AGGREGATE SUPPORT-SET SIZE: mean 5.69 facts/question, median 5, p90=10, max 26. Only 6.4% single-fact; 93.6% need >=2; 69.7% need >=4.** => ARC is DECISIVELY a MULTI-FACT AGGREGATION task, NOT single-fact lookup. This CONFIRMS (gold data) why single-ITEM retrieval (char-trigram/semantic-IR/fact-triples) all cap ~0.35: they fetch ONE thing when the task needs ~6.
- **CORE reasoning load is SHORT: CENTRAL facts mean 2.47, median 2** (68.6% >=2 central, 29.5% exactly 1). So it's SHORT aggregation (2-3 central facts), NOT long chains.
- **ROLE: CENTRAL 43.5% / GROUNDING 28.0% / LEXGLUE 23.5%** -- ~half the facts are lexical/grounding GLUE bridging question-words to fact-words.
- **TYPES: KINDOF (taxonomic is-a) 31.7% + SYNONYMY 19.2% = >50%** are taxonomic/lexical linking; then IFTHEN/ACTION/CAUSE/MADEOF/USEDFOR.
- **EASY vs CHALLENGE split:** Challenge mean 6.28 facts / 2.61 central / single 4.4%; Easy 5.43 / 2.42 / 7.3%. => Challenge needs MORE aggregation but MODESTLY (still short-aggregate ~2.6 central, NOT long-chain) -> the SAME aggregation mechanism lifts BOTH; Challenge's extra difficulty likely lives in DISTRACTOR design (matches the research eval-format-artifact flag), not reasoning depth. Grade split: support-set size does NOT track grade (elementary gr3-5 LARGER ~6.3 than middle gr6-8 ~5.1) -> explanation-size is NOT a clean grade-difficulty proxy (honest caveat for the human-scale mapping).
- **BRAIN-FAITHFUL ALIGNMENT (key):** core reasoning load ~2.5 CENTRAL facts (p90=5) sits INSIDE COWAN-4 working memory -> the substrate's EXISTING 4-slot role-bundle working memory (role_slot_summarizer M1.7 / working_memory_hrr_slots) is the right-sized vessel to BIND+SETTLE the aggregation. The task's shape FITS the brain mechanism already built. (Full aggregate ~6 incl. glue exceeds 4 -> the ~2 non-central grounding/lexglue facts are the SPREADING-ACTIVATION bridge, not held in focus -> consistent with focus-4 + activated-periphery.)
- **STRATEGIC READ (Director interpretation -- HYPOTHESIS, VET/test before asserting):** (1) the lever is an EVIDENCE-AGGREGATION mechanism over ~2-3 central facts + set-retrieval, NOT the khop SEQUENTIAL chainer (built for long relational FB15K paths -- likely the WRONG tool here; "aggregate not chain" CONFIRMED + refined to SHORT-aggregate). (2) The heavy LEXGLUE/SYNONYMY/KINDOF share (~40% of support) = LEXICAL/TAXONOMIC BRIDGING is a CORE component -> this reframes the SEMANTIC encoder's value UP (it's not a modest +0.042 ARC nicety; meaning-matching addresses ~40% of the actual support structure). (3) Next experimental step = an aggregation retriever vs the single-item floor -- sharpen after CLIMB FULL confirms the single-item floor. **BRAIN-FAITHFUL DESIGN CONSTRAINT (USER re-emphasized 2026-07-24, LOCKED):** the aggregator MUST be CONSTRUCTION-INTEGRATION (Kintsch/van-Dijk parallel constraint-satisfaction = the substrate's BIND+SETTLE per the architecture audit), where retrieved facts MUTUALLY CONSTRAIN and SETTLE -- NOT an engineering "retrieve top-k, sum scores, argmax" (that would beat the floor yet be brain-UNfaithful = rejected). Lexical/taxonomic bridging (SYNONYMY/KINDOF ~40%) = SPREADING ACTIVATION over semantic memory (Collins & Loftus); our GloVe+WordNet encoder is the faithful analog. Aggregate-vs-chain: brain does BOTH (hippocampal-entorhinal chains + cortical convergence); ARC's short-aggregate shape means MATCH mechanism to task (aggregation-dominant here) = the brain-faithful move; khop is the wrong tool for THIS task, not wrong in general. GUARDRAILS: nail the brain baseline first; fair discriminator (flat single-item baseline must PROVABLY fail); if brain-faithful bind+settle LOSES, presume impl-bug until proven structural.

## HONEST GRADE-SCALE CALIBRATION (Director, 2026-07-24) -- serves USER north star "describe knowledge on a human scale"
No crisp published human baseline exists for the AI2 SCIENCE ARC (web-checked; the 62-75% figures are ARC-AGI abstraction puzzles = a DIFFERENT benchmark, do NOT conflate). So the honest scale anchors on what IS solid:
1. **25% (4-choice) = chance = knows nothing** (bottom of scale).
2. **Report accuracy BY the question's real grade label (grades 3-9; ARC/WorldTree schoolGrade)** = the honest human-scale axis (grade-3 vs grade-8 accuracy), NOT one blurry number.
3. **~65% ~ "passing" that grade** (APPROXIMATE -- typical US state-exam proficiency cutoffs ~two-thirds; mark it approximate, not a hard anchor).
4. Current substrate ~0.30 = just above chance = BELOW grade level at every grade (honest). CLIMB target = grade-level ACROSS the range INCLUDING the hard (Challenge, higher-grade) items -- the brain does it, so we drive there; the 55-65%-Easy structured tier is a checkpoint, NOT the destination (do not mistake the prior-art wall for our ceiling).
- USE: when the aggregator lands, report Easy/Challenge AND per-grade accuracy vs (chance 25% / rough-passing ~65%); deflate; never claim a grade the per-grade numbers don't earn.

## PRIOR-ART LANDSCAPE (scan a6fa747d + Director web-verify, 2026-07-24) -- notes/research_arc_fact_combination_prior_art_competitive_landscape_2026-07-24.md
VERIFIED (web-confirmed vs ARC paper 1803.05457 + TupleInf 1704.05572): prior STRUCTURED multi-fact solvers (TableILP, TupleInf) reach **55-65% on ARC-EASY but ~CHANCE on ARC-CHALLENGE.** AI2's diagnosis: the Challenge bottleneck is RETRIEVAL bias, NOT the combination mechanism. Scan-reported (agent synthesis, not separately verified): TableILP ablation = multi-fact combination beats single-fact +10.5pts on curated tables; VSA-substrate + ITERATIVE-SETTLING QA is a confirmed GAP (QAVSA/ARC-VSA-2025 are single-pass and/or LLM-dependent); Thagard ECHO (1998, explanatory coherence) = a settle-network precedent (cousin of Kintsch CI) to build-on/credit.
- **TARGET = CRACK ARC-CHALLENGE (USER-LOCKED 2026-07-24: "the brain can do it, so we can too; we do not accept walls others have hit").** The prior-art ~chance-on-Challenge is a wall of THEIR METHODS (ILP / single-pass tuple-matching -- NON-brain), NOT a wall of the problem (a grade-9 student cracks Challenge => provably solvable). So the prior numbers are a FLOOR to break, never our ceiling. The surface-trap + retrieval-bottleneck findings are the DIAGNOSIS of what the brain's way must do: (1) retrieve by MEANING not surface (Challenge distractors are surface-lures), (2) SETTLE by support/contradiction that resists the lure. FRAMING for the aggregator: MINIMUM bar = mechanism works (settle beats single-fact floor + beats score-sum); TARGET = move Challenge above chance; if flat, DIAGNOSE per-item why (retrieval got wrong facts vs settle picked the lure -- gold ORACLE splits them) and PUSH again -- never accept flat as done. Easy 55-65% (prior structured tier) = a checkpoint on the way, NOT the goal. Corrected+relayed to the builder (I earlier deflated this to "aim for Easy" -- wrong, per [[feedback_brain_is_existence_proof_100pct_achievable_every_shortfall_gets_fair_test_and_brain_fidelity_gates_USER_2026-07-23]]).

## AGGREGATION RETRIEVER VET'd+BANKED 29537 (MM, commit 7c551e59f) -- HONEST NEGATIVE, combination does NOT crack ARC
**VET DOWN-CORRECTED the agent headline (construction-favorable crux CONFIRMED = mostly leakage):** a DUMB pure gold-fact-to-choice word-overlap baseline (no meaning/HD/bundle) scores Easy 0.786 / Challenge 0.713 -- BEATING the brain-faithful bundle (0.766/0.696) on BOTH splits. The gold facts were authored to explain the answer so they share surface words with it (correct-choice overlap 1.47 vs distractor 0.60; correct>distractor 67% of Q). => "combining cracks Challenge given gold" = MAJORITY LEXICAL LEAKAGE, not a meaning-combination capability. **On REAL retrieved facts, combining HURTS vs single (retr_single 0.342 > retr_bundle 0.298); end-to-end held-out Challenge ~chance (+0.046). ARC NOT cracked end-to-end.** bundle>single robust (p<1e-6) but bundle>score-sum only on Easy (p=0.0005), NOT Challenge (p=0.063). Small GENUINE residual: on lexical-HARD items bundle beats chance+single (Easy 0.512/Chal 0.436) via distributional-semantic paraphrase, NOT reasoning. Controls fire clean.
- **KEY DIAGNOSIS (the WHY, brain-faithful lever):** the Kintsch CI contradiction step NEVER FIRED -- negative fact-fact edges = 0.04% (all-positive cosine geometry; mean min-cos +0.572). Our meaning-encoder can only represent "how SIMILAR", NEVER "this CONTRADICTS this". Without contradiction, combination collapses to POSITIVE-SPREADING = glorified word-overlap (which is exactly why a dumb word-matcher ties it + why it can't resist the surface-lure). The BRAIN resists lures via contradiction/competition (ACC conflict + CI negative edges). => THE MISSING BRAIN-MECHANISM = a representation that encodes SUPPORTS-vs-CONTRADICTS, not just similarity. CI is NOT refuted (unfair test -- its key ingredient was absent), it's the missing piece.
- **NEXT (Director recommendation, brain's way -- NOT a wall, a signal we're doing it wrong):** build the contradiction/competition representation so combination does something word-overlap can't (resist lures, real support-vs-oppose). Retrieval is ALSO a real wall, but even perfect retrieval won't crack it while combination = word-overlap. Both diagnosed; the representation gap is the deeper brain-faithful lever.
- **CONTRADICTION CELL VET'd+BANKED 29538 (MM, commit 5d81c627e) -- HONEST NEGATIVE, redirect to RETRIEVAL CONFIRMED:** VET recompute confirmed + SHARPENED. (1) Coverage fired (0.308) = fair shot; settle_pol WORST arm (Chal 0.504 vs bundle 0.703 vs word-overlap 0.741); subset lift negative both seeds. (2) **CRUX = UNTESTED not refuted: the crude lexical POL detector is ANTI-PRECISE -- it fires on the CORRECT choice MORE than wrong (0.30 vs 0.18; 22% of correct contradicted vs 12% wrong) => it SUPPRESSES the right answer = why it hurts.** Root cause: the gold-authored lexical overlap that makes word-overlap win ALSO makes crude antonym/negation cues (high/low, more/less) fire on legitimately-RELEVANT correct-choice pairs. The cell's PLANTED-precise-edge self-test PASSES (CI helps GIVEN a precise signal) => refuted = "crude anti-precise contradiction hurts", NOT "contradiction useless"; precise detector UNTESTED (low-priority, headroom small). (3) NO-HEADROOM SOUND but sharpened: word-overlap-wins-given-gold is partly because the ORACLE GOLD POOL is LEXICALLY LEAKY (gold facts authored to share words w/ answer), NOT that real ARC is lexically trivial. (4) **RETRIEVAL-IS-THE-WALL = SOUND redirect (VET-confirmed): end-to-end ~chance regardless of POL; fixing aggregation does NOT move the needle. Actionable frontier = RETRIEVAL.** [prior VET-pending entry superseded]
- (superseded) CONTRADICTION CELL LANDED HARD_FAIL_2 (af29a98ef), VET a31ccc80: the polarity fix got a FAIR shot this time -- contradiction edges FIRED on 30.8% of questions (vs 29537's 0.4%, coverage HARD-FAIL-1 REFUTED) -- and STILL did not help: polarity-aware CI settle_pol - pos_only (Challenge) = -0.12; on the target Challenge lexical-HARD subset (n=126) subset-lift = -0.032/-0.056 NEGATIVE both seeds (smoke's +0.074 was noise). Leak-clean (must-fail collapse WITH POL). WHY (agent structural): (a) NO HEADROOM -- the CI settle is dominated by SPA-bundle + word-overlap; the dumb word-overlap baseline (Chal 0.741) beats EVERY aggregator given gold incl bundle 0.711 => given the right facts, ARC is largely LEXICALLY recoverable, little room for "smart combining"; (b) crude WordNet-antonym/negation POL is LOW-PRECISION (high/low, more/less fire on RELEVANT non-contradictory pairs) so at 30% firing it net-corrupts. End-to-end retr Challenge ~chance regardless => RETRIEVAL dominates (HARD-FAIL-3 corroborated). **VET-crux: contradiction REFUTED, or only CRUDE-lexical-contradiction refuted (precise signal untested)? Either way the no-headroom + retrieval-dominates findings redirect off combination.** REDIRECT (pending VET): stop tuning the combiner -> go after RETRIEVAL (get the RIGHT facts in) = the un-drilled bottleneck, the brain's way (find relevant knowledge before combining). Director over-read again (confidently recommended the contradiction fix) -- built it as a can-fail test, VET caught it; [[feedback_strategic_reads_run_ahead_of_evidence_caveat_interpretation_not_just_verdicts_2026-07-17]].
- (superseded design pointer) CONTRADICTION-REP DRILL DELIVERED (notes/research_contradiction_representation_and_settle_dynamics_2026-07-24.md) + CELL DISPATCHED (exp_dev af029d82): DIAGNOSIS confirmed = the textbook "antonym problem" (Yih/Zweig/Platt 2012 -- distributional similarity CANNOT separate supports-vs-contradicts; antonyms are paradigmatically CLOSE). Brain-faithful fix for ABSTRACT opposition = an explicit LABELED RELATION-POLARITY (attached, not derived), NOT a geometric axis, NOT a vector-negation operator (both contraindicated by biology: opponency is sensory-only; abstract antonyms = a labeled relational edge). DESIGN = POL override matrix {-1,0,+1} from (i) WorldTree IFTHEN/COUPLEDRELATIONSHIP directional rules (ii) WordNet antonyms (iii) negation cues -> feeds the already-correct Kintsch CI settle (which 29537 never fired -- no neg edges). Two independent routes (biology + our WorldTree fact-type analysis) CONVERGE on directional-rule-aware edges. DISCRIMINATOR = beat the dumb word-overlap baseline on Challenge lexical-HARD/surface-lure items; FIRST GATE = does frac_neg_edges rise (coverage, HARD-FAIL 1); leakage-audit the POL lookup. Pre-registered bands in the note. Building -> VET on landing.

## METHOD-SELECTION = the control structure for cracking Challenge (brain-faithful, USER Q 2026-07-24)
Agent verdict AGG_BEATS_FLOOR (FULL 1664Q, inline-local). Controls collapse (shuffled 0.236~chance, inverted 0.095<chance). ALL VET-PENDING (construction-favorable crux under adversarial check):
- **GIVEN GOLD FACTS (oracle = upper-bound-if-retrieval-were-perfect): combining cracks it** -- SPA HD-bundle superposition Easy 0.766 (> single 0.706 > score-sum 0.749), Challenge **0.696** (+0.446 over chance), glass-box, no LLM. => the COMBINATION step is NOT the blocker (VET-crux: is this genuine meaning-combination or gold-fact lexical leakage to the correct choice?).
- **END-TO-END (real retrieval): Challenge still ~chance (+0.046)** -- we have NOT cracked Challenge end-to-end. **RETRIEVAL is the wall:** ~50% Easy / 47% Challenge misses = oracle-right-but-retrieval-wrong; true aggregation failures only 20-25% (~1/3 surface-lure).
- **HONEST MECHANISM NEGATIVE:** the Kintsch CI signed-settle I pushed UNDERPERFORMED single (0.660 vs 0.706); contradiction edges were NEAR-ABSENT at this encoder's cosine scale (pos_only 0.658 ~= settle 0.660) => CI's key ingredient never fired = NOT a fair CI test (per discipline: brain-faithful-losing = unfair-test/impl until proven structural -> CI needs a real contradiction signal, not refuted). The winner (SPA accumulation) is STILL brain-faithful (Eliasmith), just a different mechanism (accumulate evidence vs select).
- **REFRAME (pending VET): the wall moved from COMBINATION to RETRIEVAL.** Next target = retrieve the RIGHT central facts (meaning-retrieval, query expansion, multi-hop bridging, resist surface-lure), the brain's way -- NOT more aggregator tuning. Method-selection escalation (below) + CI-with-real-contradiction remain informed side-levers.

## METHOD-SELECTION = the control structure for cracking Challenge (brain-faithful, USER Q 2026-07-24)
How the brain decides WHICH method to use (web-verified: basal-ganglia Go/NoGo gating, dopamine-trained; ACC conflict/error monitoring; dual-process escalation; Siegler learned strategy-choice): NOT a central chooser -- methods run in PARALLEL + COMPETE; the basal ganglia GATE lets the winner through (learned by reward = which method pays off for which cue); the ACC MONITOR detects CONFLICT/low-confidence and ESCALATES from fast/cheap (retrieve/pattern-match, System 1) to slow/deliberate (rule-application/multi-step, System 2). Credit: O'Reilly-Frank PBWM Go/NoGo, Botvinick-Cohen conflict monitoring, Shenhav-Botvinick-Cohen expected-value-of-control, Kahneman dual-process, Siegler strategy-choice.
- **DESIGN IMPLICATION (informed next-step, aimed at Challenge -- HYPOTHESIS, test don't assume):** do NOT hard-code "rules for Challenge / lookup for Easy." Brain-faithful method-selection = try the cheap combine-facts settle; MONITOR conflict; when conflict fires (exactly the surface-lure case: word-match answer COMPETES with fact-supported answer), ESCALATE to deliberate rule-application (apply IFTHEN/COUPLED directional rules). **WE ALREADY HAVE THE MONITOR: the trustworthy-reader gate (29465 -- coref feeling-of-knowing AUC 0.885 + coherence/CONFLICT + abstain gate) = the ACC-analog.** So method-selection is NOT from-scratch: we have the monitor; add the escalation + learn-which-method (dopamine/reward analog). This is the likely control structure once the aggregator lands + we see which Challenge misses are conflict-driven. Do NOT build ahead of the aggregator result; record as the informed direction.

## WHAT MAKES CHALLENGE HARD -- reasoning-type analysis (Director, disk-verified 2026-07-24)
MEASURED (WorldTree gold explanations joined to tablestore types, split by arcset): Easy vs Challenge fact-type distributions are LARGELY THE SAME (both ~28% KINDOF taxonomy + ~17% SYNONYMY glue). CENTRAL-fact shift Challenge-vs-Easy: MORE conditional/relational/causal -- IFTHEN 8.2 vs 6.6, COUPLEDRELATIONSHIP 5.5 vs ~0 (top12), REQUIRES 5.0 vs 3.8, AFFECT 4.4 vs 2.9; LESS pure taxonomy -- KINDOF 9.2 vs 11.4, USEDFOR 4.3 vs 6.0. => Challenge is NOT a different mechanism -- SAME combine-facts + settle, but leans more on APPLYING DIRECTIONAL RULES (IFTHEN "if X then Y", COUPLEDRELATIONSHIP "as X up Y down", CAUSE, AFFECT) vs static category lookup. READ (interpretation): cracking Challenge the brain's way = (1) retrieve the RULE (not surface-match), (2) APPLY it directionally (rule X->Y + question states X => boost the Y-answer), (3) resist the surface-lure. THE BRAIN-STEP to verify/build for Challenge = directional rule-application in the settle (a rule fact should create a SUPPORT edge to the answer it implies, not just static word-support). VET/DIAGNOSIS CRITERION when the aggregator lands: on Challenge misses, is it retrieving + applying the conditional/coupled rules, or falling for the lure? If rule-application is missing -> that's the next brain-step to build. (rotation example = a COUPLEDRELATIONSHIP: faster spin -> shorter day; lure = "years longer".)

## ARC-CHALLENGE = SURFACE-TRAP (Director inline measurement, disk-verified 2026-07-24)
MEASURED (scratchpad, ARC Test sets, naive "max stem-word-overlap choice" accuracy): EASY 0.266 (~chance 0.25), CHALLENGE **0.197 (BELOW chance)**. Challenge lure_rate 0.23 (a DISTRACTOR out-overlaps the correct answer in ~1/4 of Qs); mean max-distractor-overlap 0.74 vs correct 0.46. => surface-word matching is ACTIVELY MISLED on Challenge (worse than guessing), consistent with ARC-Challenge's definition (Qs that defeated retrieval + co-occurrence). READ (interpretation, not proven): Challenge's extra difficulty is substantially DISTRACTOR DESIGN (surface trap), not only reasoning depth. CONSEQUENCES: (1) our single-item/surface methods failing on Challenge is BY-DESIGN, not a mechanism break; (2) VET CRITERION for the aggregator = does meaning-based fact-support RESIST the surface lure and move Challenge above ~chance? (caveat: a few 3/5-choice Qs, so chance!=0.25 exactly, but 0.197<0.25<0.266 is clear).

## THE CLIMB (ARC fact-retrieval over the semantic vetted KB) -- SMOKE landed, FULL running (2026-07-24)
- **exp_arc_fact_retrieval_semantic_kb_climb_v1 SMOKE PASS (committed 927d75035, FULL inline-local running b5ivtud7g):** SemanticHDFactStore (semantic bipolar codes for subj/obj fillers) ingests ConceptNet through the trust-gate, answers ARC by max-cosine over the vetted fact set vs a same-encoder semantic-IR floor. All 4 honesty controls wired (empty 0.250, scramble 0.267 no-leak, broad answer-agnostic [ConceptNet = fixed external KB, structurally can't be test-targeted -> RESOLVES the 29530 test-targeting caveat], no answer-key leak). Monitors non-vacuous: ingest coverage 0.906, trust histogram fires, round-trip 0.958-1.0, fuzzy conflict fires. Positive control fires (floor arm climbs +0.05 in-regime = machinery detects real climb).
- **EARLY SIGNAL (SMOKE n=400):** fact arm = **KB_BELOW_FLOOR, delta_vs_floor -0.043** = fact-retrieval from ConceptNet triples LOSES to raw-sentence-IR. ARC-vocab coverage decent (0.678) => limit = **MECHANISM (short terse triples lose context), NOT coverage.**
- **FULL RUN KILLED (2026-07-24, Director decision):** the FULL (60k ConceptNet ingest) ran 68 min stuck at i=20000/60000, CPU-pegged, intervals ballooning = **O(n^2) INGEST bug** (fuzzy-conflict check scans the whole growing fact set per insert). KILLED (my task b5ivtud7g + PID 32416 confirmed dead) because (a) pathological perf AND (b) the WorldTree diagnostic SUPERSEDED the single-item paradigm it tests (single-fact insufficient; 6.4% single-fact). The single-item floor at scale will be measured cleanly as the AGGREGATION cell's BASELINE arm -> no separate FULL re-run. Smoke signal (fact-retrieval below floor, mechanism-limited) stands as DIRECTIONAL (n=400); definitive number comes from the aggregation baseline. O(n^2) fix documented for the aggregation cell (notes/aggregation_retriever_bindsettle_construction_integration_2026-07-24.md). NOT separately banked (subsumed into the aggregation cell).
- **EMERGING CONVERGENCE (hypothesis, cross-thread -- VET before asserting):** single-ITEM retrieval (char-trigram RACE/ARC ~0.35 ceiling [29535], semantic-IR, AND now fact-store triples) all cap similarly because ARC needs AGGREGATE multi-fact context, not single-item lookup -- MATCHES the reasoning research's WorldTree ~6-fact aggregate-support-set finding. If FULL confirms, the lever is NOT "more facts" but the AGGREGATION/reasoning MECHANISM -> sharpens the ARC task-shape diagnostic (aggregate-support vs path-chain). HOLD until FULL + skunkworks VET.

## READING RUNG (RACE) -- LEXICAL FLOOR VET'd+BANKED 29535 (2026-07-24)
- **RACE cell VET'd+BANKED 29535 (MM, commit 7dfc67a6c) -- honest framing = LEXICAL FLOOR, not a reading rung:** RACE cached local. Numbers reproduced bit-exact: middle 0.395, high 0.317, agg 0.356, lex_mid 0.386, mismatched 0.244, scramble 0.306, random 0.242. **CONFIRMED CRACK: the cell used CharTrigramEncoder (the ARC harness's LEXICAL bag-of-trigrams), the situation-model READER was NEVER invoked** -- HD-vs-lexical = +0.009 (adds NOTHING over token overlap), scramble only partially collapses (largely lexical, not order-sensitive comprehension). **DE-CONFOUND CLAIM RETRACTED (Director over-read, VET-corrected): "ARC floor = knowledge gap not reading gap" is NOT supported.** RACE-agg 0.356 ~= ARC-Easy char floor 0.354-0.373; RACE (answer IN-passage) does NOT exceed ARC (RACE-high is BELOW it) -> BOTH plateau at the SAME ~0.35 LEXICAL CEILING => the bottleneck is the shared char-trigram MECHANISM, NOT a knowledge-vs-reading split. Cannot separate knowledge from reading (reader untested + RACE !> ARC). **What it DOES license (genuine):** present relevant text lifts the LEXICAL mechanism +0.12-0.15 vs absent controls (mismatched->chance = real genuineness discriminator). RACE-high glass-box: picks surface-matching distractors over inferential answers. **The READER was never invoked -> reader-stress-test UNANSWERED.**
- **CORRECTED TAKEAWAY:** the char-trigram LEXICAL mechanism CAPS ~0.35 on BOTH reading (RACE) and knowledge (ARC). Breaking that ceiling REQUIRES meaning -> this REINFORCES the two active levers: (1) THE CLIMB (semantic fact-retrieval, in flight) and (2) the READER ARM (semantic encoder + situation-model reader on RACE). Both attack the same ~0.35 lexical ceiling from different sides.
- **NEXT (after VET): THE READER ARM** = re-run RACE with (a) SemanticHDEncoder (29533, meaning not lexical -- should resist the surface-distractor trap that sank RACE-high) and (b) the situation-model reader, vs the 0.395 LEXICAL floor. Does meaning/comprehension BEAT bag-of-trigrams? This answers both USER purposes properly (real reader-stress-test + does meaning lift reading-comp). HOLD until VET confirms the baseline + framing.

## READER ARM VET'd+BANKED 29536 (MM, commit 087f0d283)
MEASURED (verified): meaning NUDGES the ~0.35 lexical reading ceiling on RACE -- small, positive every split, genuine passage-reading (mismatched collapses -0.069) -- BUT single-seed NOISE-LEVEL (ARM A high +0.024 z=0.81 p~0.42; McNemar bound implausible), BELOW HARD-PASS. The lean coref-free READER rep does NOT help on RACE MC-QA (event_semantic 0.291 < char 0.358) = a PROVEN BOUND, not a reading rung (SimHash-of-meaning confirmed real: synonym 0.536 vs false-friend 0.110, so the underperformance is structural not a broken encoder). => across 29535+29536, MEASURED pattern: NO single-item method (lexical/meaning/event) breaks ~0.35 on reading.
DIRECTOR NOTE: I over-reported ARM A's +0.024 as "genuine" before this VET; VET corrected to noise-level. Logged as a live instance of [[feedback_strategic_reads_run_ahead_of_evidence_caveat_interpretation_not_just_verdicts_2026-07-17]].

## RETRIEVAL CELL VET'd+BANKED 29539 (MM, commit 64a0b9033) -- spreading VALIDATED, but end-to-end nudge was a REGIME ARTIFACT (honest correction)
**VET verdict:** the recall lift is REAL, the end-to-end "above chance" is NOT mechanism progress. (1) RECALL integrity PASSES decisively: PPR-spreading B 0.380 vs cosine A 0.280 (+0.10) is real relational structure -- shuffled-graph control D collapses to 0.0007 (rules out "just more facts"); the brain mechanism is VALIDATED on the real graph (prior synthetic-graph HARD_FAIL was confounded, confirmed). (2) **END-TO-END CORRECTED (the Director over-read, VET-caught): Challenge C 0.320 IS above chance -- BUT baseline cosine A 0.310 is ALSO above chance (regime property = gold-in-store + bundle combiner), and the mechanism lift B-vs-A +0.033 is NOT significant (z=1.09, p=0.27). So spreading did NOT move the hard set beyond where cosine already sat. The pre-reg end-to-end bar was MET but MIS-SPECIFIED (baseline clears it too).** (3) SEED redirect SOUND: seed_recall 0.365 < 0.50 make-or-break floor = the measured next wall; spreading delivers +0.10 DESPITE bad seeds. (4) discriminative re-rank HURTS (C 0.195 << B 0.380) -- DROP step 4. (5) hub-idf down-weighting NEAR-INERT (C-E +0.0046, within noise) -- the real lever is spreading (B-vs-D), not idf.
- **HONEST STATE:** we find MORE right facts (+0.10 recall, structure-proven) but that has NOT translated to better answers yet (end-to-end flat/noise). = retrieval-recall NECESSARY but so-far NOT SUFFICIENT. NEXT = SEED-QUALITY upgrade (multi-token phrases, lower SEED_COS, WordNet-expand seed terms -> raise recall past +0.15) + WATCH whether higher recall NOW translates to end-to-end (if recall gets high and score STILL flat -> the combiner/K-dilution is the next wall, per the MIDDLE-BAND redirect). Drop discriminative re-rank + hub-idf (near-inert).

## SEED-UPGRADE v2 LANDED MIDDLE_BAND (committed 67fbda82b), VET a825e693 IN FLIGHT -- coverage was the WRONG lever (VET-PENDING)
MEASURED (agent, VET recomputing): seed_recall DOUBLED 0.365->0.545 (cleared floor) BUT recall@10 FLAT (B 0.383 vs v1-seeds B0 0.380, +0.003) -- extra correct seeds CANCELED by an 8x PRECISION crater (0.342->0.068; mean_seeds 13.6->112); noise-seeds dilute PPR. Recall@10 capped by 2-hop reach + K=10 -> **PRECISION not COVERAGE is the lever** (refines 29539). End-to-end Challenge B 0.355 (+0.045) McNemar p=0.055 NOT significant (smoke p=0.0115 inflated). necessary-but-not-sufficient CONFIRMED.
- **EMERGING STRATEGIC REFRAME (Director HYPOTHESIS, pending VET -- do NOT act until confirmed):** the retrieval arc optimized RECALL, but the real wall is PRECISION + COMBINER-NOISE-ROBUSTNESS -- bind+settle works on CLEAN gold facts (Challenge 0.696) but DROWNS on the NOISY real pool (~chance). The brain's fix for BOTH = relevance-gated DISCRIMINATIVE PRECISE retrieval + competitor SUPPRESSION (Badre-Wagner, retrieval-induced-forgetting) = EXACTLY the discriminative re-rank we DROPPED for lowering RECALL -- likely the WRONG METRIC (if the wall is precision/noise, fewer-more-relevant facts is the brain's answer even at lower recall). => NEXT (pending VET): PRECISE/relevance-gated retrieval judged on END-TO-END not recall; and/or a NOISE-ROBUST combiner (find signal in a noisy pool / lower K). Test don't assume -- over-read positives repeatedly.

## ARC ARC WRAP + RETRIEVAL WALL (2026-07-25, tail 29542; full authoritative summary now in backup-doc CURRENT block)
- **BRAIN-ARCHITECTURE ANALYSIS (USER-requested):** 3 scans + our experiments CONVERGE -- the ONE missing mechanism = retrieval CONTROL/SELECTION + SUPPRESSION (VLPFC). We were building the right function in the WRONG shape/place/metric. Method formalized: deep-brain-analysis -> comparison -> accurate-duplication. Artifact: claude.ai/code/artifact/8bf13969-03ee-4d67-a70c-e98f7851852e.
- **29541 SELECTION GATE = METHOD VALIDATED (MM):** goal-bias SELECT + SUPPRESS before the unchanged combiner, judged on the ANSWER. Both halves fire+dissociate (selection decisive Easy +0.13 p<1e-6; suppression real-underpowered p=0.087). CG withheld. The method DELIVERS.
- **29542 COMBINER ESCALATION MIDDLE_BAND (MM):** confidence-gated escalation (bridge/discounted-gap-fill/abstain). Accuracy WITHIN SEED NOISE (combiner-robustness NOT the lever). Real: discount->CALIBRATION-only + ABSTAIN GATE trustworthiness (0.42@61% vs 0.37). khop-bridge = clean negative.
- **🔑 CONVERGED: RETRIEVAL RECALL (~0.38) is the recurring dominant wall.** Combining works given gold (0.69); retrieval can't reach gold. Facts ARE in the WorldTree graph -> likely a MEANING/reachability gap (thin GloVe/WordNet), NOT content-quantity. USER instinct (deepen the FOUNDATION's MEANING/grounding) likely right.
- **IN FLIGHT: MAX-RETRIEVAL diagnostic (aadf2027)** -- K-sweep + controlled re-retrieval + selection-gate-cleans-wide-pool -> attributes the wall K-bound / reachability-bound / GRAPH-bound (facts unreachable by thin meaning => the fix is grounded/richer MEANING = the brain-like deep lever). **USER 07-25 FULL-AUTO: make EVERYTHING around retrieval BRAIN-LIKE; keep docs current.** Curriculum BREADTH (all textbooks) gated behind fixing reachability.

## NEXT (sequenced)
1. Capacity-re-check lands -> the HONEST foundation-scale verdict on the REAL semantic rep (+ whitening / hybrid if needed).
2. WIRE ingestion INTO the optimized+vetted+semantic store (curated triples through the trust-gate) + fuzzy conflict via similarity (USA==United States) + re-run ARC broad -> climb above the chance floor?
3. REASONING layer -- RESHAPED by research (notes/research_multihop_reasoning_layer_biology_and_design_sketch_2026-07-24.md, DISK-VERIFIED): **the K-hop CHAINER ALREADY EXISTS + is validated** (substrate/khop.py, Merkle-audited traverse(): FB15K-237 2-hop r@5=0.705, WebQSP 97.6%, CWQ 92.6%, cycles 187/188 -- confirmed on disk). So NO chainer build needed. HYPOTHESIS (pending diagnostic, NOT yet fact): ARC-Challenge flatness may NOT be a chaining problem -- WorldTree shows ~6-fact AGGREGATE support-sets not path-chains + eval-format artifacts implicated. NEXT reasoning action = a CHEAP ARC-Challenge TASK-SHAPE DIAGNOSTIC (path-chain vs aggregate-support vs format-artifact) BEFORE any new cell. HOLD until the CLIMB lands (it shows the concrete Challenge-flat behavior on our store, sharpening the diagnostic). If aggregate-support: the mechanism is multi-fact EVIDENCE AGGREGATION, not sequential unbind. Deflated P 0.35-0.45. Candidate follow-drill: network-science (degree-heterogeneity/high-contention-node risk in real KG chains).
4. Bit-packed/on-disk storage for true millions-scale.
5. Scale curriculum (CK-12/OpenStax K-12) toward the full 1-12 human scale.
- v2 lands -> VET the curve (the real "does it learn" answer). If rising: scale Ele->Int->Adv autonomous, Director verifies each batch. If flat: diagnose.
- Also fix (v2 or v3): grade-unit alignment (condense what's READ), flag->condense causal wiring, ClarifyGate real-confidence calibration.
- IMAGE-GROUNDING = ready parallel track on CLEAN concrete-item images (shape-recog "strong", word<->picture grounding 0.99-1.0 on clean McGuffey-First; FAILS on cluttered/abstract; grounding-lift-to-reading UNPROVEN). USER: not urgent, text foundational -> deferred, switch on when wanted.
