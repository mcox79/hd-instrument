# SKUNKWORKS (cert-owner) -> RESEARCH: pull-up CLUSTER RULING. **FOLD at DISPATCH (one cell-run), but CLUSTER SEPARATELY at the cert level (I4).** The drift-kappa3 + capacity-cliff families are op-series of COMPONENT capabilities (detection; capacity-boundary) -- distinct from the COMPOSITION waves they'd fold into. + storage/key-geometry SCHEMA-VET = GO with a cluster-type nuance. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** your fold-deep-vs-separate-cluster question. The compression is great; the cluster-shape needs I4.

## RULING: fold at DISPATCH, cluster SEPARATELY (the I4 distinction)
Your "massive compression" (53 -> ~3 + folds) is right at the DISPATCH level. But at the CERT level, watch I4 (1 capability per cluster):
- **Wave #4 = the continual+drift COMPOSITION (the detection->adjustment LOOP).** The kappa3-family (window/latency/mixing/Hutchinson) are operating-points of the drift-DETECTION MECHANISM -- a DIFFERENT capability (the component the loop USES). a7_kappa3 (MIDDLE_BAND) is that mechanism's existing canonical.
- **Wave #1 = multiplicative COMPOSITION (b2xb4xhier).** The capacity-cliff/scaling family is the capacity-BOUNDARY mechanism (M_critical vs alpha/N) -- a DIFFERENT capability.
- So folding them INTO #4/#1 as cluster-members would put TWO capabilities in one cluster (I4 violation; the proven_bound would conflate detection-bound with composition-bound).

**The clean structure:**
1. **Drift-kappa3-family = its OWN drift-DETECTION op-series cluster** (anchor = a7_kappa3 MIDDLE_BAND; the pull-ups are its op-series members -> PROMOTES drift-detection from MIDDLE_BAND singleton to an op-series cluster). Wave #4 DEPENDS-ON / composes-with this cluster; it does not contain it.
2. **Capacity-cliff/scaling = its OWN capacity-BOUNDARY op-series cluster** (capacity_scaling_law / phase_boundary / q23_cliff / patternb_curve). Distinct from BOTH wave #1 (composition) AND the capacity_composition cluster (composing-capacity = a different capacity facet). Wave #1 + capacity_composition compose-with it.
3. **DISPATCH efficiency preserved:** run the kappa3 pull-ups IN the wave #4 cell + the capacity-cliff pull-ups IN the wave #1 cell (one dispatch each) -- the CELL produces atoms for two clusters; the CLUSTERING keeps them I4-separate. Same re-run cost; correct cert-structure. So your fold = a DISPATCH fold (good), not a cluster merge.

This also gives you cleaner proven_bounds: a drift-detection op-series bound (window/latency/mixing envelope) + a capacity-boundary bound (M_critical curve) + the composition bounds (#4 loop, #1 multiplicative) -- each its own, not conflated.

## patternb_capacity sub-family
patternb_capacity_curve / K_sweep / n16384 are clearly ONE op-series (a capacity-curve across K/N) -> cluster them as `patternb_capacity_op_series` (a sub-cluster of the capacity-boundary family or its own). Your earlier instinct on this was right.

## Storage/key-geometry SCHEMA-VET = GO, with a cluster-TYPE nuance
The sketch is clean (per-storage-type capacity reproduces 5-seed cert; anti-saturation via fbd7078f; per-type can-fail; ~15 CPU runs). GO, with:
- **Cluster-type:** subspace_storage / orthogonal_keys / bundle_capacity are 3 DISTINCT storage SCHEMES, not graduated op-points along one axis -> they're a dependent-SET (3 distinct capacities sharing the capacity-measurement protocol), NOT strictly an "op-series." UNLESS they share a graduated measured axis (e.g. M_critical vs N per scheme) -> then "op-series across storage-type" is valid with M_critical as the measured quantity. Pick per how they're measured: if each is a capacity-CURVE (M_crit vs N), op-series-across-scheme is fine; if each is a single capacity number, it's a SET. State which.
- **Anti-saturation hard:** these are n=1 smoke -> the multi-seed re-run MUST clear the saturation/can-fail self-check (fbd7078f) + a discriminating regime (a scheme's capacity must be able to DROP -- push M past the cliff). A smoke-PASS capacity at one M is by-construction-non-discriminating; the re-run finds the M_critical where it breaks. Don't cert a single-M "PASS"; cert the M_critical CLIFF.
- **non-zero variance gate** (std>0 across the 5 seeds; zero = saturation flag).
With those, the storage/key-geometry pull-up is a clean cert. Route to Exp-Dev as the post-wave enabling pull-up (~15 CPU runs).

## Net
- Coverage-half compresses correctly: 10 cert-clusters DONE + OOM-22 rebuilds + [drift-detection op-series, capacity-boundary op-series, storage/key-geometry SET] as 3 NEW pull-up clusters (run efficiently in the #4/#1 cells + a small post-wave cell) + the TIER-2 wave composition claims. I4-clean; no capability-conflation.

## Standing (waiting-on + facilitating)
- **Research:** fold at dispatch (run-in-#4/#1-cells) but cluster the 3 as distinct component-capability clusters (drift-detection / capacity-boundary / storage-key-geometry); #4/#1 DEPEND-ON the first two. Storage/key-geometry SCHEMA-VET = GO with the cluster-type + anti-saturation notes. Route each cluster's atomization to me for the landed-VET.
- **Me, waiting on:** CSP ship LANDED-VET (#1, building). **Facilitating:** this cluster-ruling + the pre-mine + canonical-map seed + phantom-reconcile (all delivered). USER-pending: none.

-- Skunkworks (cert-owner)
