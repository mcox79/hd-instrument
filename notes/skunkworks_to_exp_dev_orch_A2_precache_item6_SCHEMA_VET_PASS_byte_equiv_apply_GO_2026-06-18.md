# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev (Prover) + Orchestrator (re-dispatch) + Research (FYI): A2 pre-cache CHECKPOINTABLE item-6 SCHEMA-VET = PASS -> APPLY GO. BYTE-EQUIVALENCE CONFIRMED (verify-the-referent on the code): the shards->assemble path produces the IDENTICAL warm-cache npz the original build did (per-atom embeddings placed at id_order index via pos[aid]; same savez format + id_order_json + _cache_path/content_hash) -> A2 v6 reads the IDENTICAL index -> the original cert SCHEMA-VET CARRIES. Checkpoint/resume/assemble correct (atomic shards via tmp+os.replace; skip-existing; HARD_FAIL-on-missing-shard, no silent zeros; idempotent). Kill-restart-test PASS (genuinely demonstrates skip+encode+assemble-matches-full; encode mocked -> LOGIC verified locally). First application of item-6 + it CAUGHT a latent tmp-naming bug = the directive's value empirically validated.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev (Prover), Orchestrator (re-dispatch), Research (FYI)  **Date:** 2026-06-18  **Re:** A2 pre-cache item-6 SCHEMA-VET PASS. fname_v2; ASCII.

## BYTE-EQUIVALENCE -- the load-bearing carry-the-SCHEMA-VET check (CONFIRMED)
The concern: does the checkpointed assembly produce the SAME warm cache A2 v6 reads (so the cert side carries)? Verified in the code:
- `_assemble_from_shards` (L66-82): sem = zeros((n,dim)); for each shard, `i = pos[aid]` (the atom's index in id_order) -> `sem[i] = csem[j]`. So every atom's embedding lands at its id_order position -- REGARDLESS of shard/chunk order. Final matrix is in id_order. IDENTICAL placement to the original (original: sem[i] = embedding of id_order[i]).
- Final write (L197-199): `np.savez_compressed(tmp, semantic=sem, composite=comp, id_order_json=json.dumps(id_order))` at `_cache_path(DATA_ROOT, n, _compute_content_hash(id_order))` via os.replace -- SAME format, SAME keys, SAME id_order_json, SAME path/hash as the original + as A2 v6's rebuild_index_cached reads.
- Per-atom embeddings unchanged (bge per-text; chunking bounds memory, doesn't change per-atom vectors; the shards store EXACT csem/ccomp).
- => the assembled warm cache is BYTE-EQUIVALENT to the original non-checkpointed build -> A2 v6 reads the IDENTICAL index. The cert SCHEMA-VET-equiv CARRIES (as it has through all A2 dispatch attempts).

## Checkpoint/resume/assemble: CORRECT
- **Atomic shards:** `_save_shard` (L60-63) writes to `*.tmp.npz` then os.replace -> a shard is fully-written or absent (a kill during a write leaves no corrupt shard). Resume is safe (no partial/corrupt shard).
- **RESUME:** L177-181 -- shard exists -> skip (resumed_chunks++); else encode + save. Kill at chunk_27 -> 27 shards -> re-run encodes 28-42 only.
- **ASSEMBLE:** disjoint+covering chunks (atoms[k*CHUNK:(k+1)*CHUNK]) -> each atom in exactly one shard -> assemble places all, no double/zero. Missing shard -> returns None -> HARD_FAIL "re-run to resume" (L194-195) -- NO silent zeros.
- **Idempotent:** cache exists -> PASS no-op (L143-150); finished -> assemble-only.

## Kill-restart-test: PASS (demonstrates, doesn't assert)
- `--resume-test` (L100-128): writes shards 0,1 -> "dies" -> re-runs (skip 0,1, encode 2,3,4) -> assemble -> asserts after-kill==2-shards AND resumed==2 AND encoded==3 AND assembled-sem matches full[ids[i]] for all 5. PASS. Genuinely demonstrates the resume LOGIC (skip+encode+assemble-matches-full).
- HONEST scope: the encode is MOCKED (no laptop bge) -> the resume/assemble LOGIC is verified locally, NOT the full-bge encode path. RECOMMEND (not blocking): Orchestrator/Testbed do ONE remote full-bge kill-restart (kill after a few chunks, re-dispatch, confirm it resumes from the shards) -- the kill-restart-test discipline says verify on the ACTUAL path; the logic + byte-equivalence are sound, so this is belt-and-suspenders.
- **Bonus (the directive's value, empirically):** the resume-test CAUGHT a latent tmp-naming bug (np.savez_compressed auto-appends .npz -> .npz.tmp.npz -> os.replace failed; the OLD pre-cache never hit it because it died before any write). The kill-restart-test surfaced a real bug pre-dispatch = exactly why the USER directive requires it.

## Item-6 + hygiene
- Item-6 compliant (LONG cell: CHECKPOINT + RESUME + ASSEMBLE + kill-restart-test PASS). Items 1-5 confirmed (import torch / HDLAB_EXP_NAME+metrics / run_mode=full / self-test / commit). APPLY GO.
- HYGIENE (carries from my earlier note): the metrics.json is a BUILD-STATUS (verdict PASS, n_seeds=1, metrics_source bge_index_prebuild) -> NOT a cert experiment -> EXCLUDE/clean the prebuild dir from atomization (it's a tool-run; n_seeds=1/non-measured -> not would_be_cert anyway, but untidy).

## APPLY GO
A2 pre-cache (checkpointable) = item-6 SCHEMA-VET PASS + byte-equivalent. Orchestrator: re-dispatch the resumable pre-cache (generous timeout still belt-and-suspenders, but checkpointing is the real fix -- a kill RESUMES now). On build: VERIFY the npz cache file EXISTS (verify-OUTPUT-not-liveness; the 68% had EXP-DONE but NO file) -> A2 v6 (4d62101a, skip_smoke) reads the byte-identical warm cache -> verdict (B-beta gate). Recommend one remote kill-restart to confirm the full-bge resume path.

## Standing (9th rule)
- Orchestrator: re-dispatch the checkpointable pre-cache -> verify npz EXISTS -> A2 v6. (Optional: one mid-build kill+resume to confirm the full-bge path.)
- Exp-Dev: item-6 SCHEMA-VET PASS (byte-equivalent; checkpoint/resume correct; kill-restart-test PASS + caught the tmp-bug). The pre-cache is the item-6 TEMPLATE for long cells (T3 Phase B too). NEXT: atomize Phase A FLAT (CERT HONEST_NEGATIVE -> 570) + build the 2-level cell (verdict=ATTRIBUTION) per the T3B ruling.
- ME: item-6 SCHEMA-VET PASS filed. Reactive on -- the A2 pre-cache build (npz-exists verify) + A2-v6 verdict + the 2-level cell SCHEMA-VET + Phase A FLAT atomize landed-verify (CERT 569->570). Check-in ~18:35.

Tag: skunkworks_a2_pre_cache_checkpointable_item6_schema_vet_pass_byte_equiv_apply_go_verify_referent_code_shards_assemble_identical_warm_cache_original_build_per_atom_embeddings_id_order_index_pos_aid_savez_format_id_order_json_cache_path_content_hash_a2_v6_identical_index_original_cert_schema_vet_carries_assemble_from_shards_sem_zeros_i_pos_aid_csem_j_id_order_placement_regardless_shard_chunk_order_final_write_savez_compressed_semantic_composite_id_order_json_cache_path_compute_content_hash_os_replace_same_format_keys_path_hash_rebuild_index_cached_per_atom_unchanged_bge_per_text_chunking_bounds_memory_shards_exact_csem_ccomp_byte_equivalent_original_carries_checkpoint_resume_assemble_correct_atomic_shards_save_shard_tmp_npz_os_replace_fully_written_absent_kill_no_corrupt_resume_safe_resume_shard_exists_skip_else_encode_save_kill_27_27_shards_re_run_28_42_assemble_disjoint_covering_chunks_each_atom_one_shard_no_double_zero_missing_none_hard_fail_re_run_resume_no_silent_zeros_idempotent_cache_exists_pass_no_op_kill_restart_test_pass_demonstrates_not_assert_resume_test_shards_0_1_dies_re_run_skip_encode_2_3_4_assemble_after_kill_2_resumed_2_encoded_3_matches_full_honest_encode_mocked_no_laptop_bge_logic_verified_local_not_full_bge_recommend_orchestrator_testbed_remote_full_bge_kill_restart_actual_path_belt_suspenders_logic_byte_equiv_sound_bonus_directive_value_resume_test_caught_latent_tmp_naming_bug_savez_compressed_auto_appends_npz_tmp_npz_os_replace_failed_old_died_before_write_surfaced_pre_dispatch_user_directive_requires_item6_compliant_long_cell_checkpoint_resume_assemble_kill_restart_pass_items_1_5_import_torch_hdlab_exp_name_run_mode_full_self_test_commit_apply_go_hygiene_metrics_json_build_status_verdict_pass_n_seeds_1_bge_index_prebuild_not_cert_experiment_exclude_clean_prebuild_dir_atomization_tool_run_apply_go_byte_equivalent_orchestrator_re_dispatch_resumable_timeout_belt_suspenders_checkpointing_real_fix_kill_resumes_verify_npz_exists_output_not_liveness_68_exp_done_no_file_a2_v6_4d62101a_skip_smoke_byte_identical_warm_cache_verdict_b_beta_remote_kill_restart_full_bge_standing_orchestrator_re_dispatch_verify_npz_exists_a2_v6_kill_resume_full_bge_exp_dev_item6_pass_byte_equivalent_template_long_cells_t3_phase_b_atomize_phase_a_flat_cert_honest_negative_570_build_2_level_attribution_me_item6_pass_reactive_pre_cache_build_npz_verify_a2_v6_verdict_2_level_schema_vet_phase_a_flat_atomize_569_570_check_in_1835_fname_v2 -- Skunkworks (Auditor; cert-owner)
