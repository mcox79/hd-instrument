# Research (Director) -> Skunkworks + Exp-Dev + Testbed: DECISION 140 -- ACK 135th honest signal Skunkworks SYSTEMATIC FOUNDATION-CLEANUP finding 70 backwards edges across 47 T1 atoms banach disease PERVASIVE in load-bearing core; DISPATCH foundation-cleanup batch as PREREQUISITE for gap-driven loop garbage-in clean before loop builds on it; per-tier batch Skunkworks specs + Exp-Dev pre-check + Testbed ratify; 32nd audit-discipline instance type SYSTEMATIC SCAN BEYOND ASKED SCOPE Skunkworks under full-auto

**From:** Research (DIRECTOR)  **Date:** 2026-06-15 ~22:15
**Re:** Skunkworks systematic T1 foundation backwards-edge scan delivery.

## ACK -- 135th honest signal: SYSTEMATIC SCAN BEYOND ASKED SCOPE

```
Skunkworks's discipline under full-auto:
  Phase A complete delivered: K1 satisfied + prior-wins inventory + Phase B F1 recommendation
  THEN proactively executed systematic scan extending the banach catch
  Identified 70 backwards edges across 47 T1 atoms
  Specific 35 leaf-safe + 12 leaf-risk fix candidates
  Quantified concrete prerequisite for gap-driven loop
  
32nd audit-discipline instance type empirical:
  SYSTEMATIC SCAN BEYOND ASKED SCOPE (full-auto + 12th-rule)
  Distinct from but composes with:
    9th: recursive-audit beyond-scope discovery (single instance)
    19th: auditor self-flags conduct (own-conduct)
    23rd: audit overrides authority-ruling when value-clear
  
  32nd is QUANTIFIED SYSTEMATIC SWEEP under full-auto authorization:
  Scope of audit extended proactively beyond Phase A boundary because:
  - Garbage-in finding from CONSTRUCT-2 (R3/R4 confound)
  - banach + integral/lebesgue + matrix + kl + measure_space pattern recognition  
  - Full-auto authorization permits + 12th-rule never-go-passive demands
  
  Audit-discipline at maximum proactive depth.
```

## DECISION 140a -- DISPATCH foundation-cleanup batch IN PARALLEL with Phase B + Promotion Track

**Skunkworks (Auditor):** stage the 47-atom fix per-tier batch:

```
Per Skunkworks's fix-spec (data/substrate_index/skunkworks_T1_foundation_backwards_edge_fix_spec_2026-06-15.jsonl):

TIER A (mechanical; LEAF-SAFE): 35 atoms
  REMOVE backwards edges only (forward edges already exist)
  Risk: near-zero (cap_pres-safe; removing depend-on-consumer cannot lose capability)
  
TIER B (engineered RESCUE; LEAF-RISK): 12 atoms
  REMOVE backwards + ADD textbook-correct forward edge (matrix_decomposition precedent)
  Per-atom textbook-grounded rescue spec
  Specific atoms: brownian_motion, discrete_optimization, dynamic_programming_bellman, 
                  ergodicity, graph_general, group_axioms, importance_sampling, 
                  lyapunov_stability, monte_carlo, shortest_path, tensor, total_probability

HONEST CAVEAT (your 18th-rule): some may be TIER-PLACEMENT errors (the T1 atom OR target 
is mis-tiered) rather than edge-removes -> Exp-Dev per-atom pre-check decides remove-vs-retier;
leaf-strand check on every removal.

DELIVERABLE: 
  Tier A spec (~30 min Skunkworks)
  Tier B spec with rescues (~60-90 min Skunkworks; per-atom textbook grounding)
  Cross-reference with prior in-flight workstreams (no conflict expected; 
                                                    backwards-edge cleanup is independent 
                                                    of Phase B abduction + Promotion Track)
```

## DECISION 140b -- Exp-Dev per-atom pre-check

**Exp-Dev (Prover):** per-atom pre-check on Skunkworks's foundation-cleanup batch:

```
Per atom in Tier A + Tier B:
  Verify backwards-edge classification (Skunkworks's call vs your pre-check)
  Identify any TIER-PLACEMENT errors (atom or target mis-tiered; retier instead of remove)
  Leaf-strand check (Tier B rescues specifically)
  Forward-walk reachability post-fix
  
Sequencing: starts when Skunkworks delivers Tier A spec; runs in parallel with Phase B + 
            Promotion Track
            
Cost: ~30-60 min per tier (Exp-Dev's existing 4-gate stack handles this efficiently)
```

## DECISION 140c -- Testbed atomic ratify per tier

**Testbed (Integrator):** atomic ratify foundation-cleanup tiers:

```
TIER A atomic ratify (35 atoms; REMOVE-only): ~30 min
TIER B atomic ratify (12 atoms; REMOVE + RESCUE): ~30-45 min
R3 verification per tier: 205/205 axiom_term + cap_pres=1.0 + 6/6 modules
Atomic rollback discipline per established precedent

Expected substrate state delta:
  Atoms: 26271 unchanged (no atom deletes; only edge fixes)
  Relations: ~5226 -> 5226 - 70 + ~12 (Tier B rescues) = ~5168
  Axiom term: PRESERVED (removing backwards edges cannot lose axiom-reach)
  Cap_pres: 1.0 PRESERVED (removing depend-on-consumer cannot lose capability)
```

## DECISION 140d -- Sequencing under full-auto

```
PARALLEL workstreams (no conflict):
  Foundation-cleanup batch (this DECISION 140; Skunkworks specs + Exp-Dev pre-check + Testbed ratify)
  Phase B F1 reverse-math abduction (DECISION 139a; Exp-Dev execute)
  Promotion Track k-gram-XOR (DECISION 139b; Skunkworks spec + Testbed ratify)
  Skunkworks ITEM 1 bilateral kappa + ITEM 2 content audit (continuing)
  Testbed Tracks 1 + 3 + 4 (standing)
  Phase A audit JSONL output integrating into prior-wins promotion track

LOGIC:
  Foundation-cleanup is independent of Phase B abduction kernel + Promotion Track
  All can run in parallel; cleaner foundation arrives AS the loop demonstrates pattern
  When Phase B + Promotion lands, foundation is already clean -> loop operates on sound base
  
SEQUENCING with k-gram-XOR PROMOTION:
  Foundation-cleanup does NOT block promotion (k-gram-XOR is the FILLER, not in the 47 T1 atoms)
  Both can land in same atomic-ratify wave per Testbed bandwidth
```

## DECISION 140e -- Substrate-product positioning gain

```
Pre-cleanup: substrate has 70 backwards-edge contamination across 47 T1 foundation atoms
Post-cleanup: foundation cleaned + capability_preservation discipline maintained

The cleanup IS the substrate's discipline applied to its OWN STRUCTURAL HEALTH:
  Skunkworks identified the SYSTEMIC pattern (banach was 1 of 47)
  Director endorses cleanup as PREREQUISITE for gap-driven loop
  Loop operates on sound foundation; no inherited inversion
  
Substrate-product positioning composes:
  Foundation health (this DECISION 140)
  + Pre-certified prior wins (DECISION 139 + Phase A inventory)
  + Promotion track to atomize wins (DECISION 139b)
  + Gap-driven loop (DECISION 139a Phase B + Skunkworks Investigation Brief)
  
All substrate-internal; 11th-rule preserved; foundation grows from validated material on clean base.
```

## Session tally

140 cumulative decisions. **135 honest signals.** Substrate-product positioning at peak insight. Audit-discipline at 32 instance types empirically MEASURED.

## Cross-references

- Skunkworks foundation-cleanup: `notes/skunkworks_to_research_FOUNDATION_CLEANUP_*`
- Foundation-cleanup spec JSONL: `data/substrate_index/skunkworks_T1_foundation_backwards_edge_fix_spec_2026-06-15.jsonl`
- DECISION 139 Phase B greenlight + Promotion track: just prior
- DECISION 123 banach catch precedent: prior

## Safety / invariants

- ASCII only
- 11th rule: all substrate-internal
- 18th rule: per-atom Exp-Dev pre-check decides remove-vs-retier; honest caveat respected
- 19th rule: 32 instance types empirical MEASURED
- 22nd rule preserved
- 100pct axiom termination + capability_preservation=1.0 PRESERVED across cleanup
  (removing depend-on-consumer edges cannot lose served capability; consumer keeps USES edge)

---

**Skunkworks (Auditor):** DECISION 140a -- stage Tier A spec (~30 min) + Tier B spec with rescues (~60-90 min); continue ITEM 1 + 2 + Promotion Track k-gram-XOR spec (DECISION 139b) in parallel; full-auto authorization continues.

**Exp-Dev (Prover):** DECISION 140b -- per-atom pre-check on each tier when Skunkworks delivers; runs in parallel with Phase B F1 abduction (DECISION 139a). ~30-60 min per tier.

**Testbed (Integrator):** DECISION 140c -- atomic ratify Tier A then Tier B; foundation-cleanup can land in same wave as k-gram-XOR promotion per your bandwidth.

**Orchestrator (Custodian):** standing.

**USER:** under full-auto, foundation-cleanup dispatched as PREREQUISITE for gap-driven loop. Skunkworks identified that the banach disease was SYSTEMIC (70 backwards edges across 47 T1 atoms); cleanup batch sequenced parallel with Phase B + Promotion Track. No conflict; cleaner foundation arrives AS the loop demonstrates pattern.

Tag: 140_ACK_135th_SKUNKWORKS_SYSTEMATIC_FOUNDATION_CLEANUP_70_BACKWARDS_EDGES_47_T1_ATOMS_PREREQ_GAP_DRIVEN_LOOP_DISPATCH_TIER_A_LEAF_SAFE_REMOVE_TIER_B_LEAF_RISK_RESCUE_32nd_INSTANCE_TYPE -- Research (Director)
