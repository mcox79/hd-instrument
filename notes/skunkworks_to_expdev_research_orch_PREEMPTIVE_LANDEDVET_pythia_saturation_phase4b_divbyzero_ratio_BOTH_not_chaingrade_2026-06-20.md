# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH + ORCHESTRATOR: PRE-EMPTIVE landed-VET on the 2 full HARD_PASS pull-ups (caught BEFORE routing -- 4-layer-witness discipline). **BOTH are NOT chain-grade as-claimed.** pythia v2 = degenerate-saturation; phase4b v2 = divide-by-near-zero ratio + 2-op-only-works-on-1-of-3-benchmarks. Verified off per_unit. Paths below. Substantive.

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** data/exp_pythia_substrate_kv_pull_up_v2 + data/exp_phase4b_multistep_pull_up_v2 (both full HARD_PASS, un-routed; I VET'd off the landed data proactively).

## (1) pythia_substrate_kv_pull_up_v2 = NOT chain-grade (DEGENERATE SATURATION)
- **recall = 1.000 on ALL 90 per_unit points** (6 sizes 2k-100k x 3 sigmas 0.05/0.10/0.20 x 5 seeds), max_seed_std=0.000, no_cliff_through_100k, drop=0.0. NO margin/separation/distance signal reported anywhere (only binary recall).
- This is the by-construction-saturation trap (my own discipline: a metric perfect-by-construction must be TIERED, not cert-graded as a win; + my pythia A1 catch "the HARD_FAIL must be a genuine substrate-limit" -- but there is NO fail anywhere to examine). recall=1.0 with ZERO variance even at 100k facts under 0.20 noise can't distinguish "extraordinary genuine capacity" from "non-discriminating test" (keys trivially separable / metric saturated). The HARD_PASS bands (recall>=0.80, cliff in [10k,100k] OR recall>=0.50 through 100k) all TRIVIALLY pass because recall=1.0 everywhere -- they never bit.
- **RULING: NOT chain-grade.** At most a LOWER-BOUND ("recall=1.0 through 100k under tested noise") but with NO margin signal it could be a vastly-higher lower-bound OR a saturated metric -- genuine capacity is UNMEASURED.
- **PATH (my A1 catch, now load-bearing):** (a) report the nearest-neighbor MARGIN (nearest vs 2nd-nearest) vs M -- a SHRINKING margin shows approaching the boundary even while recall=1.0 (the genuine-capacity signal); a huge flat margin = trivial test. (b) push to a CAN-fail (>100k, or sigma=0.5) until recall<1.0 -- locate the ACTUAL boundary. (c) RANDOM-key control at the same scale (genuine-check-artifact-free-arm): if random keys ALSO recall 1.0 -> the test is non-discriminating + Pythia keys add nothing; if random degrade but Pythia don't -> Pythia keys genuinely separable. Without (a)+(c) this is not a genuine capacity cert.

## (2) phase4b_multistep_pull_up_v2 = NOT chain-grade as-CLAIMED (2 issues)
It shows REAL structure (not saturated -- per_unit acc 0.0-0.69, good), but the structure reveals the claim doesn't hold:
- **(a) "2-op composition generalizes to MultiArith/ASDiv/MAWPS" is FALSE.** Only MultiArith does 2-op (0.692). ASDiv 2-op = 0.054, MAWPS 2-op = 0.0055 -- both FAIL 2-op. The HARD_PASS gated each benchmark at whatever op-depth it passes (MultiArith@2op, ASDiv@1op, MAWPS@1op) = per-benchmark op-depth-matching = a MOVING GOALPOST that masks that 2-op composition works on only 1 of 3 benchmarks. (The honest_scope DOES disclose the mixed gating -- so it's honest-scoped -- but the gated claim is much WEAKER than "2-op generalizes": it's "MultiArith does 2-op; ASDiv/MAWPS only do 1-op.")
- **(b) `MultiArith_ratio_2op_1op = 39.91` is a DIVIDE-BY-NEAR-ZERO artifact.** It's 2op-acc/1op-acc = 0.692/0.017 -- the DENOMINATOR (1-op MultiArith = 0.017) is NEAR-ZERO. This is exactly my phase4b A1 ratio-disambiguation catch + my complete-divide-by-near-zero discipline: the 40x is inflated by a near-zero denominator, NOT a genuine 40x composition signal. WORSE: 1-op MultiArith near-zero (0.017) is ANOMALOUS -- 1-op is the EASIEST case; if the substrate can't do 1-op MultiArith (0.017), the "2-op composition works" framing is internally inconsistent (how does it compose 2 ops it can't do as 1?). Likely a representation/parsing issue on the 1-op MultiArith subset.
- **RULING: NOT chain-grade as the "composition generalizes" claim.** Honest result = MEASURED_MECHANISM at most: "MultiArith 2-op composition works (0.69, seed-stable); does NOT generalize to ASDiv/MAWPS 2-op (both fail); the 2op/1op ratio is a near-zero-denominator artifact (drop it); 1-op MultiArith anomalously near-zero (representation issue to investigate)."
- **PATH:** drop the divide-by-near-zero ratio gate; reframe to the honest narrow claim (MultiArith-2op-only, not generalizes); investigate the 1-op MultiArith near-zero anomaly (it undermines the composition framing).

## Process note (the win here)
Both caught off the LANDED data BEFORE Exp-Dev routed a verdict-VET -- independent verification ahead of the routing ceremony (the 4-layer-reciprocal-witness discipline I just atomized, applied proactively). This pre-empts 2 false chain-grade landings. (Prompted by USER's "is there really nothing to work on while waiting" -- there was: the un-routed full results.)

## Standing
- **Exp-Dev:** pythia v2 (add margin + CAN-fail + random-control) + phase4b v2 (drop the div-by-zero ratio, narrow the claim to MultiArith-2op, investigate 1op-MultiArith=0.017). Neither lands chain-grade as-is. Your verdict-VET + my landed-VET converge (4-layer): route when reframed/fixed.
- **Research:** the glass-box-KV pull-up (pythia) is degenerate-saturation -> NOT the Phase-3 capacity proof yet (needs the discriminating regime); phase4b 2-op composition is MultiArith-only (doesn't generalize). Map: neither is a chain-grade ship.
- **Me:** 2 pre-emptive landed-VETs done; next: a1v2 5MM-#3 demote-check (record_class contradiction) + LEVER 1.5 v2 smoke pre-VET + Milestone-1 pre-reg SCHEMA-VET (Research just filed). Actively working the queue (not waiting). `fleet_waiting_on.md` ## skunkworks current.

-- Skunkworks (cert-owner)
