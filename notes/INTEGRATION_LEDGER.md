# INTEGRATION LEDGER — every recent solution's claimed gain, live-wiring status, and the up/downstream work still needed to REALIZE the full gain

**Why this exists (owner, 2026-09-05):** recent submissions carry sweeping up/ and down/stream improvements that are needed to realize their full gains. Those improvements used to live only as prose inside individual `SOLVED.md` files + scattered follow-on problems, so the *headline wire* got integrated but the *full-gain follow-through* slipped. This ledger is the single authoritative tracker: for **every** recent solution it records (a) the **END-SCORE / GAIN the solution CLAIMED** + the instrument it was measured on, (b) what is **wired LIVE** now, (c) which **board dim actually moved** (or none-yet), and (d) every **up/downstream improvement** still needed to hit the claimed number. It is simultaneously the **ordered work-list for the top-down integration pass**.

**Accountability model:** a solution's claimed number is a *target*. We only "realized" it when a **live board dimension** moves by that much (or a named instrument gap is closed). `full_gain_realized: YES/PARTIAL/NO` is the running scoreboard of that.

**Key recurring pattern:** "landed ≠ live." A component can be promoted to `hdlab/` and even default-ON, yet its claimed gain sits on *its own instrument* while the board doesn't probe it (instrument gap), or it's capped by an unbuilt upstream (coref/parser/meaning-hub), or a downstream consumer isn't updated to *receive* its new signal. Those are the rows that matter for the pass.

**🔗 THE REVERSE-INDEX (owner 2026-09-09): `notes/CROSS_SOLUTION_IMPROVEMENT_MAP.md`.** Improvements are NOT siloed — a handful of shared components (the parser ~16, the meaning representation ~15, coref/entity-binding ~14, the generative world-model ~9) are flagged as the wall again and again. That map is the BY-TARGET reverse-index: **when you improve component X, grep it there to find every already-"done" solution that CONSUMES or FLAGGED X, and reopen/re-measure them.** MAINTENANCE: when you integrate a new SOLVED here, also append its (consumes / flags) to the map's PART 1 + PART 2. Cross-ref `notes/bf_status_registry.jsonl` (79 organs certified; live set 100%) for each component's BF status.

**Companion:** `notes/PROVISIONAL_WIRINGS.md` — everything wired in a TEMPORARY/non-final state (scoped workarounds, default-off holds, latent landings, provisional golds, lossy couplings) + what the pass resolves.

**Status vocabulary:** `LIVE` (wired + default-on, reaching a consumer) · `LATENT` (in hdlab, no live read()-time consumer) · `DEFAULT-OFF` (wired, flag off with a reason) · `NO-WIRE` (located negative — a valid pass, nothing to land) · `INSTRUMENT-GAP` (live but no board arm probes it).

> Build status: harvesting the recent SOLVED corpus via 4 read-only agents (2026-09-05). Chunk C (dimensions/state/events) landed first and is below; who-did-what/coref, knowledge/meaning, and perf chunks appending. The at-a-glance SUMMARY TABLE + the ordered TOP-DOWN WORK-LIST are built at the bottom once all chunks are in.

---

## 2026-09-09 (CONT-29 CONTINUATION) — integrated the audit-CATALOG (owner-DONE) + POSTED 2 idle-solver problems from its recommendations

- **`audit_the_live_substrate_for_non_brain_faithful_stand_ins_and_name_replacements` (pri2) INTEGRATED (`<this commit>`):** owner-DONE, REVERIFIED FIRST-HAND 30/30 (`verification/test_audit_live_standins.py`, pure-disk). A MAP (NO hdlab writes, Q111) — "integration" = accept the ranked 11-entry catalog + act on it. Its #1 blast (C1 gold-coref leak) = the in-progress de-leak (BATCH-2 below). Its 5 disk-corrections ADOPTED (spaCy purged, force-verb list retired, state/location WordNet fallbacks landed, cleanup_family/hd_fact_store DORMANT-wrt-reader, coref name-Jaccard misattributed). status→INTEGRATED, priority 2 dropped. Independent confirmation of the de-leak as the top blast-radius fix.
- **POSTED 2 idle-solver problems (`de47d4fb3`, cert 8/8) from the audit's NEW BREADTH FOLLOW-ON (the second live entry point it excluded — the FOUNDATION/grounding-acquisition subsystem):**
  - **pri4 `audit_the_grounding_acquisition_subsystem_for_non_brain_faithful_stand_ins`** — the breadth catalog for `reading_grounding_loop.py`/`substrate.py`/`gap_driven_reader.py`/`three_tier_loop.py`/`grounding_acquisition_loop.py`, same 5-field discipline + pure-disk witness (prerequisite hygiene before the learner turns on at scale).
  - **pri5 `replace_the_attractor_as_ranker_with_a_graded_population_read_in_the_grounding_loop`** — C7: `cleanup_family` sign()+attractor (`classical_hopfield`/`modern_hopfield_continuous`/`iterative_attractor`) used as a graded RANKER, live via `gap_detector` in the grounding loop; replace with a graded population read, keep the attractor for recall. Parallelizable (already located).
  - **DEDUP (Explore-agent map, first-hand):** every "obvious" follow-on is already filed/solved — generative world-model/Wall-2 salience→N400 = FILED-SOLVED across 5 owner-DONE (+ the completing lever is the open pri-1 `grow_the_causal_mechanism…`); bridging = solved (`bridging_inference`) + only ~0.1% of GUM residual; C3 parser local moves = mostly INTEGRATED; `world_knowledge_81_percent_residual` = PARTIAL located-negative, static-KB lever coverage-exhausted → folds into pri-1. So the 2 grounding-subsystem problems are the genuine unfiled frontier.
- **✅ DE-LEAK PART 2 DONE (CONT-29 continuation, "keep moving") — THE CROWN JEWEL LANDED + FLIPPED ON.** A fresh impl agent built (then I verified first-hand + finished the witness the agent hadn't written) `hdlab/crosstype_live_adapter.py` (NEW, self-contained, ZERO experiments/): the GOLD-FREE crosstype definite→name bridge adapter — builds a crosstype Doc from the reader's OWN live parse (`_cached_tag`/`_cached_parse_heads`/`_frontend_labeler`) with each mention's `.eid` = its ONLINE cluster label (NEVER `m["cluster"]`/`_gold_eid`), runs `crosstype_bridge_links(conf_thr=-3.0)`, merges each bound definite under its NAME's online label (int-safe). WIRED into `situation_reader.read()`'s `online_entity_cluster` block (sub-flag `online_entity_cluster_bridge`, default True) + **FLIPPED `online_entity_cluster` DEFAULT False→True** per no-more-default-off. VERIFIED `verification/test_deleak_crosstype_live_adapter.py` PASS (self-contained; gold-free Doc; non-mutating int merge; LIVE `read()` full-de-leak-ON no crash + `sm.entities` built; PRONOUN/coref BYTE-IDENTICAL floor/part1/full; landed +0.0838 CI-sep n=549 reproduced from disk). BOARD NO-REGRESS (smoke, de-leak ON): core aggregate 0.6294 vs floor 0.5882, NO arm crash, **coref FLAT 0.504 vs 0.506** (leak was in the entity/experiencer path, not the pronoun coref stream), who-did-what/state/wic healthy. **BOARD-VISIBLE CONFIRMATION:** the `board_crosstype_experiencer_dimension` arm RUNS live (34s, smoke n=130): bridge 0.1846 vs situation_predict floor 0.0923 = **+0.0923 CI[0.0507,0.1402] CI-sep**, twin 0.1077 loses (+0.0769 CI-sep), C1 no-regress (0.7073→0.7079) — same direction/CI-sep as the SOLVED's full-population +0.0838. **BOARD-HEALTH (owner-flagged):** the board is NOT broken — individual arms run fast; the full uncapped `--run` chains ~20 heavy arms (several `live_reparse` the whole corpus + big golds e.g. TELLMEWHY n=1500) → 30-60 min cumulative, which EXCEEDS the Bash tool ceiling (10-min timeout; `run_in_background` spuriously terminated it ~3 min). A TOOL/RUNTIME limit, not a de-leak regression or a broken instrument: run the full board DETACHED (orchestrator/queue) for the authoritative aggregate, or `--self-test` (capped) for a fast pass (the core-7-dim aggregate 0.6294 no-regress is from that).
  - **🐛✅ INSTRUMENT BUG (owner-surfaced) — FOUND + FIXED + REVERIFIED (`146e42688`, NOT the de-leak).** The `run_new_arms=True` path (full `--run`) EXITED MID-RUN with code 0 (never returned, no `metrics.json` write). **REAL ROOT CAUSE (my first-pass "argv pollution / sys.exit" guess was WRONG — disproven; a fix-agent found it):** `experiments/_hashseed_guard.py:13` does `os.execv(sys.executable, [sys.executable]+sys.argv)` on import when `PYTHONHASHSEED != "0"` — **RE-EXECS the whole process**, bypassing returns/sys.exit/atexit (why it "exits 0" mid-run). Two board guard-imports (`exp_situation_model_qa_modern_v1.py:645,1211`) were added **2026-09-07** (commits `102641f3bb` p4-event-goal + `21515fce77` predictive-causal board arms) → REGRESSION (matches the stale 22:48-prior-day `metrics.json`); the runner doesn't set `PYTHONHASHSEED` so the re-exec fired once those arms landed. `--self-test` (`run_new_arms=False`) never reached the guard import → looked fine. **FIX:** pin `PYTHONHASHSEED="0"` at the board module top before any arm import → every `_hashseed_guard` import is a no-op. **REVERIFIED FIRST-HAND:** the capped full board now RUNS TO COMPLETION (183s, EXIT=0, DONE_OK), **all 21 new arms + 7 core dims**, AGGREGATE 0.6545, coref FLAT 0.549 vs 0.5425 (de-leak no-regress), **crosstype_experiencer in-board model 0.1846 vs floor 0.0923 CI-sep** (de-leak board-visible confirmed in the full run). `--self-test` still PASS. Caveat (agent): the pin suppresses a true hashseed-0 start-process, so temporal/survival arms are best-effort deterministic (±5-chain drift, = pre-2026-09-07 state); for byte-repro the RUNNER should export `PYTHONHASHSEED=0`. Committed 1 file path-limited, NOTHING pushed. Follow-on: regenerate the canonical uncapped `metrics.json` via the orchestrator/queue (30-60min); the ~15 other `_hashseed_guard`-importing cells are safe only as standalone entry points (never import-as-library into a long process). **REALIZES the +0.0838 CI-sep experiencer gain the gold peek was faking** — the completion of the whole blocked coref/name-bridge/experiencer line. `INTEGRATED_BY_STRATEGY` marker + priority 6 dropped. ⚠️ **CORRECTION (measured on the real LitBank corpus, tick-31): the keystone `test_coref_which_entity` all-zero is NOT pre-existing — the FLIP causes it.** flip-OFF→`build_coref_questions`=4, flip-ON→0. `sm.coref_resolutions[].gold_cluster` stays gold (byte-identical, W4) but `sm.entities` now uses online negative-int ids, so the LEGACY LitBank eval's join `_named_clusters(sm)[entity_id].get(r.gold_cluster)` desyncs → 0 questions. I mis-called it "pre-existing" from a SYNTHETIC-doc byte-identity test without measuring on the actual corpus (reconciliation-discipline failure). **BLAST RADIUS (verified):** UNAFFECTED = the modern authoritative board (ran clean, 0.6545, no arm zeroed, coref 0.549 non-zero) + live reader inference (crosstype +0.0838, no crash, pronoun byte-identical) — the de-leak stands. BROKEN = only the LEGACY `exp_situation_model_qa_v1` LitBank QA eval (`build_coref_questions`/`_named_clusters`/`build_salience_question` join entity-ids↔gold_cluster) — a 19c-BANNED, modern-board-superseded layer, NOT a commit gate. **ENUMERATION COMPLETE (tick-32, owner directive discharged):** there are TWO `_named_clusters`. (1) `exp_situation_model_qa_v1._named_clusters(SM)` reads `sm.entities` ids → BREAKS under the flip; ALL its consumers are LEGACY LitBank-era eval cells (`exp_situation_model_qa_v1` QA builders, `exp_affect_chain_signal_loss_v1`, `exp_affect_register_qa_v1`, `exp_goal_register_qa_v1`, `exp_assembled_reader_all_flags_on_v1`/`_corrected_aggregate_v1`) — NONE live-inference, NONE on the modern board. (2) `exp_hybrid_unified_incumbent_coref_gum_v1._named_clusters(MENTIONS)` reads the mention list directly (fresh gold ids, NOT `sm.entities`) → UNAFFECTED; the MODERN board (`exp_situation_model_qa_modern_v1:1052/1076`) + the GUM coref-graded path use THIS one → explains the clean modern-board run. The live reader (`hdlab/situation_reader`) uses NEITHER at inference. **CONCLUSION: the flip's eval blast radius is BOUNDED to the superseded, banned-19c LitBank eval layer — NOT the authoritative modern board, NOT live inference.** My "no-regress" claim is accurate WITH that precise caveat. OPTIONAL fresh-pass (NOT blocking, low-value since 19c is banned + superseded): make the sm-based `_named_clusters` id-robust (key by name-string) OR formally retire the legacy LitBank QA cells + their `test_situation_model_qa.py` witness. Committed path-limited (strict — NOT `git add -A`, concurrent-session changes in the tree), NOTHING pushed. FOLLOW-ON (not front-run): the C4 self-containment residual (`context_grounded_valence` + `temporal_reasoner` off `experiments/`); reconcile the `board_crosstype_experiencer_dimension` arm's operating point (0.0/cue_competed → -3.0/cue_conf) or point it at the live flipped path for true "live==scored".

---

## 2026-09-09 (CONT-29 BATCH 2) — 3 MORE owner-DONE (chain_multi_step, world-model, de-leak) + the successor pri-1 posted

- **chain_multi_step (pri8) INTEGRATED (`eeaee8316`):** located-negative + a banked goal-slice organ (MeansEnd ATL-hub, +0.239 CI-sep, coverage 8.7→88%) SHELVED (no live home — needs the multi-engine competition + resolved coref; do-not-wire full-pop, all tie). Recorded the per-component upstream brain-foundational scour (3/9 BF).
- **build_the_generative_result_state_world_model (pri1) INTEGRATED (`31463fb11`):** owner-DONE TRIANGULATED located-negative. Its mechanisms are built + brain-foundational + BANKED and OVERLAP the already-integrated causal world-model (`hdlab.predictive_world_model` + `sm.causal_antecedent`, 1d3d3bbf2/71f6e3c29) — its "secondary" future work (wire the world-model+necessity live over EventRecord) is DONE, so NOTHING NEW to wire. Wall = story-specific causal-mechanism-knowledge GRANULARITY gap. priority 1 dropped.
- **POSTED the successor pri-1 (`31463fb11`, cert-passed):** `grow_the_causal_mechanism_knowledge_foundation_by_mining_directed_causal_linguistic_testimony` — the CONVERGENT frontier all 3 located-negatives name (mine directed causal-linguistic testimony → consolidate → apply per-story online, feeding the intervenable machinery; grade on the intrinsic frame). De-duped: bridging → `bridging_inference` (exists); general learner/foundation → existing problems; this causal-mechanism-testimony learner is genuinely new.
- **THE DE-LEAK `replace_the_entity_gate_gold_coref_inheritance...` (pri6) — owner-DONE, REVERIFIED 23/23, WIRE PRECISELY SCOPED, PENDING careful execution (NOT yet integrated).** THE #1 blast-radius unblocker: the live entity layer (`_build_entities`/`_apply_commonnoun_gate`) is GOLD-derived (reads `m["cluster"]`) — it FAKES the crosstype experiencer bind at C3=0.807 without reading (honest floor 0.155). The fix (SOLVED, +0.0838 CI-sep experiencer live, twin loses; C1 up; pronoun byte-identical; C2 small owner-accepted trade): replace the gold clustering with the brain-faithful ONLINE ACT-R cue-clustering (`online_cluster`, Heim file-change + Lewis-Vasishth content-addressable retrieval), then the landed-latent `crosstype_bridge` becomes load-bearing. **THE PRECISE WIRE (for the focused next pass):** (1) PROMOTE `experiments/exp_online_cue_cluster_gum_v1.online_cluster` (+ `_File`/`_role_from_rank`/`_type_match`) → a new `hdlab/online_entity_cluster.py` (byte-faithful; reuses landed `coref`/`state_of_mind`/`salience_binder`/`commonnoun_binder`/`typed_spokes` → self-contained). (2) REWIRE `situation_reader._build_entities` (L649 `by_cluster.setdefault(m["cluster"],...)`) + `_apply_commonnoun_gate` (L3982-3999) to cluster NON-pronoun mentions via `online_cluster` with FRESH **NEGATIVE-INTEGER** ids (NOT `CN:` strings — `_read_world_state`/`_resolve_commonnouns` do `rc>=0` and CRASH on a str; the crosstype SOLVED proved this); keep NAME via aliaser + the pronoun coref column untouched. (3) WIRE the crosstype bridge into `make_canonicalizer`. (4) NO-REGRESS gate: pronoun byte-identical, C1 up, C3 experiencer +0.0838, NO downstream int-cluster crash, C2 the accepted −0.0078 trade; then board re-measure + flip per no-more-default-off. Do NOT wire the part-whole route (over-merges, meronymy≠identity). CROWN-JEWEL live-entity-layer surgery — reserved for a fresh focused pass (do-it-right-not-fatigued, board Q122), scoped here so it executes cleanly.
  - **PROGRESS (CONT-29):** ✅ STEP 1 DONE — `online_cluster` promoted to `hdlab/online_entity_cluster.py` byte-faithful + self-contained (self-test 4/4, verified byte-identical to the solver + pulls no experiments modules). ✅ STEP 2a DONE (LANDED DEFAULT-OFF, `6d1330da3`) — the read() de-leak wire (reassign non-pronoun `m["cluster"]` to `online_cluster`'s negative-int ids before `_build_entities`; pronoun stream untouched); VERIFIED no-crash + pronoun byte-identical + entities built. Held default-off (`online_entity_cluster`) because it is net-positive ONLY with the bridge (alone C3 drops fake-0.807→honest-0.156). ⏭️ REMAINING (the fresh focused pass): STEP 2b the crosstype-bridge adapter (`hdlab.crosstype_bridge.crosstype_bridge_links(doc, gaz, conf_thr=-3.0)` → merge binds into the online labels → `make_canonicalizer`; the crosstype SOLVED's deferred adapter-risky follow-on — needs a live-doc adapter) + flip both default-ON + C1/C3 no-regress + board re-measure + marker/priority.
  - **STEP 2b EXACT ADAPTER RECIPE (scoped CONT-29 continuation by reading `hdlab/crosstype_bridge.py` + the reverify harness `verification/test_online_cue_cluster.py` + the cells `exp_crosstype_live_wire_gum_v1.py`/`exp_crosstype_landable_validation_gum_v1.py`):** the bridge consumes a Doc = `.toks` (`.gidx/.sent/.idx/.head/.deprel/.upos/.xpos/.form/.lemma`) + `.mentions` (`.eid/.mtype/.start_g/.end_g/.head_g/.gender/.text/.lemma_head`), enumerated in the SAME order as the reader's `role_mentions` (so `crosstype_bridge_links` returns `{midx: name_eid}` keyed by reader midx). The harness routes binds→labels via `_merge(ms, floor, binds)` which relabels each bound role midx to its NAMED entity's floor/online label; it keys the name by `_gold_eid` FOR SCORING ONLY — the bridge DECISION is gold-free (0.86 live-parse precision, twin loses). **LIVE gold-free construction:** set each Doc mention `.eid = online[midx]` (the online cluster label — names grouped by the aliaser, gold-free), so `crosstype_bridge_links` returns `{midx: name_online_label}` and the merge is trivially `online[midx] = name_online_label` before the existing negative-int `m["cluster"]` assignment. Build `.toks` from the reader's OWN live parse (`live_reparse` shape: hdlab.pos_tagger UPOS + hdlab.arceager_parser `parse_with_conf` heads + hdlab.arc_labeler deprels — the reader already loads these; reuse its parse rather than a second one where exposed, else reparse per-sentence). **PROMOTE the Doc-builder + `_merge` byte-faithfully into hdlab (e.g. extend `hdlab/online_entity_cluster.py` or a new `hdlab/crosstype_live_adapter.py`) — NO experiments/ import (self-containment invariant).** Then in `situation_reader.read()` online_entity_cluster block (L4227): after `online = online_cluster(...)`, build doc → `binds = crosstype_bridge_links(doc, self.gaz, conf_thr=-3.0)` → merge into `online` → assign `m["cluster"]`. NO-REGRESS gate: pronoun byte-identical (bridge/online touch only non-pronoun midx), C1 up, C3 experiencer +0.0838 vs honest floor 0.1548, no downstream `rc>=0` crash. Do NOT wire part-whole (over-merges). Then flip `online_entity_cluster` default-ON per no-more-default-off + board re-measure + de-leak marker + priority-drop + audit §2b.
  - **BOARD INSTRUMENT ALREADY EXISTS (confirmed CONT-29 continuation — no new arm needed):** `board_crosstype_experiencer_dimension` (`experiments/exp_situation_model_qa_modern_v1.py:1348`, registered in `run()` L2256) already scores the C3 experiencer bind floor-vs-floor+bridge-vs-twin on the HONEST situation_predict floor via `RU.score_c3_experiencer` (currently a STANDALONE simulated arm using `crosstype_bridge_links(conf_thr=0.0, mode="cue_competed")` + a `_merge`-style `merged_lab`, because nothing in the live path called the organ). POST-FLIP: (i) confirm the live gain reproduces (flip `online_entity_cluster` ON → the reader's OWN entity layer now IS online-clustering+bridge); (ii) reconcile the arm's operating point to the de-leak headline (`conf_thr=-3.0, mode="cue_conf"` gave +0.0838 CI-sep vs the arm's 0.0/cue_competed) OR point the arm at the live flipped reader path so "live == scored"; (iii) `board_namebridge_dimension` (L1461) is the sibling C2 name-bridge arm to watch for the accepted small C2 trade.

---

## 2026-09-09 (CONT-29) — INTEGRATING two owner-DONE (obl-spatial pri5 + causal pri4), FULL-CHAIN under the owner's "make the upstream changes, handle the blast radius" directive + the "upchain may not be the latest; consider OTHER downchains" caution

**OWNER DIRECTIVE (2026-09-09):** both incoming solutions had the ENTIRE upstream chain vetted 100%-brain-foundational → implement the upstream changes on ALL components, accept the blast radius, then UPGRADE the downstream regressions (very beneficial once handled). CAUTION: the solvers built in experiments/ against upstream snapshots that may NOT be the current live hdlab, and each optimized only its OWN downchain — so for every upstream change, (a) reconcile current-live-vs-solver-assumed, (b) enumerate ALL live consumers, not just the solver's, and upgrade each.

### build_the_obl_spatial_defer_consumer_to_cash_the_graded_parser_marginal  [pri5; PARTIAL/located-negative; owner-DONE; reverified 12/12; OPT-A+OPT-C LANDED, verified no-regress]
- **RECONCILIATION (the owner's caution, confirmed):** the solver's headline "landable wins" were measured against a STALE upstream. (1) **OPT-A** ("commit to the exact posterior") was measured vs the GREEDY decode (+0.0144), but the live spatial front-end already uses `decode="exact"` (P4, CONT-25 `ff8008131`). MEASURED the true live headroom (strategy `scratchpad/measure_obl_exactmap_vs_optA.py`): exact-MAP vs greedy = +0.0020 (NOT CI-sep — P4 already delivered ~nothing of OPT-A), but **the marginal-ARGMAX commit vs the live exact-MAP = +0.0133 CI[0.0061,0.0204] CI-sep** (they disagree on 1.9% of obl edges — the MAP maximizes the joint tree, the per-edge marginal integrates over ALL trees). So OPT-A is a GENUINE live win, NOT subsumed by P4 — my first-pass "already-live" assumption was WRONG, corrected by measuring. (2) **OPT-B** ("drop the obl logistic") targets `parse_confidence.obl_confidence`, which is DORMANT (zero live consumers) → moot on the live path; left as dead code (at-land-prune candidate). (3) A sibling chain (CONT-26 P2) already tested-and-REFUTED related marginal machinery (`marg_gate` precision-gate + `use_marg_alt` 2nd-best) in the SAME live extractor `joint_spatial_frames_ext` — but OPT-A's variant (commit ground to the 1-best marginal) is DISTINCT from both and untested there.
- **LANDED (`hdlab/joint_relation_frontend.py` + `hdlab/situation_reader.py`):** OPT-C — `parse_sentence` now caches the exact single-root marginals as a side-effect of the same exact decode (ONE inverse/read, reused; heads byte-identical, proven 0/400 → temporal unaffected). OPT-A — `commit_spatial_ground_heads` re-attaches each spatial-preposition GROUND nominal to the marginal-argmax over the brain-faithful candidate set (McRae/Spivey normalized-recurrence COMMIT to the posterior), wired default-on (`spatial_obl_commit`) in `_read_spatial_reasoning` BEFORE `joint_spatial_frames_ext`, so ONLY the SpatialModel figure-ground binding sees the corrected ground head. Witness `verification/test_obl_spatial_commit_landed.py` PASS 10/10 (W1 head-invariance 0/400; W2 commit purity/no-op; W3 events+timeline+causal BYTE-IDENTICAL on vs off; W4 committed ground attachment beats live exact-MAP +0.0102 CI[0.0041,0.0174] CI-sep on UD-EWT spatial obl, using the reader's OWN predicted POS — realistic; the +0.0133 used gold POS).
- **full_gain_realized: PARTIAL.** A real, brain-foundational upstream improvement to the live spatial ground attachment (+0.0102 CI-sep, no-regress), but board-INVISIBLE by the solver's own finding (the spatial dimension is construction-coverage-dominated 35% + place-typing 14% >> attachment 10%; SpaceEval saturated; where_is underpowered n=47). It IMPROVES the spatial FRAMES' precision the downstream Talmy-coverage organ will need. NOT a headline mover.
- **DOWNCHAIN ENUMERATION (owner "consider OTHER downchains"):** the obl head feeds ONLY the spatial figure-ground builder (`joint_spatial_frames_ext` via `_read_spatial_reasoning`); temporal (`joint_event_ranks`) and who-did-what (role assigner, a SEPARATE parse) are byte-identical (verified). No other live consumer of the committed ground head. The `AtLocationClassFit` THEM organ (for who-did-what patient, where location is NOT symmetric) is a SEPARATE reuse the solver flagged — NOT yet measured-positive → filed as a measured follow-on, not landed.
- **FOLLOW-ON (owner judgment, NOT landed):** the marginal precision-GATE + Talmy Source-Path-Goal construction broadening (§6 items 1-2) — land TOGETHER default-off behind the precision-sensitive where_is register once a powered modern narrative gold exists; no net win on current gold. **AUDIT FINDINGS folded (§2b):** arc scorer = surface-feature (18% confident-wrong on spatial, not sentence-internal-cue-fixable → needs the discourse loop); ground binder construction-coverage 35% is the bigger organ; front-end "SEPARATE + FROZEN" is the architectural north-star (the unfrozen linked interactive-activation cell). status:INTEGRATED, priority 5 dropped, review EXCELLENT (rigorous located-negative + one clean BF upstream win).

### generate_dont_retrieve_causal_edges_for_unmarked_narrative_causation  [pri4; PARTIAL/north-star; owner-DONE; reverified 67/67; ORGAN + WIRE LANDED, verified]
- Reverified first-hand 67/67. The prize = an ONLINE PREDICTIVE-CODING world-model (Rao-Ballard + Rescorla-Wagner, glass-box tagger, NO LLM/gold/training, simplewiki) that beats static counting +0.247 bits CI-sep on held-out surprisal, + a counterfactual-NECESSITY causal reader over it (finds real antecedents that ESCAPE the position confound, +0.308 bits CI-sep, nearest only 32%) + Stage-1 situation-model bound events (agent+patient+affect+goal, capacity-matched CI-sep) + reconciliation with the landed KGStore n-hop. Also a REFUTATION: external causal golds (MAVEN/TMW/GLUCOSE) are position-artifact traps; intrinsic surprisal-reduction is the trap-proof frame.
- **RECONCILIATION (owner caution, resolved):** the solver's online delta-rule concept→concept surprisal model is genuinely NEW — NOT a dup of `hdlab/predictive_coding.py` (a VSA write-GATE `sign(W@key)`); it is the FORWARD EVENT-SEQUENCE level the substrate lacked. `hdlab/predictive_reader.py` is the WORD/FEATURE level (static fitted centroids, within-clause anticipation); `n400_coherence_monitor` is the BACKWARD event-coherence half; and the existing `sm.causal_reasoner` does GRAPH-reachability necessity over EXTRACTED `sm.causal_links`. This is the INTRINSIC SURPRISAL necessity none of them express. So it's a complementary new organ, wired ADDITIVE (no upgrade-in-place of predictive_reader; no downchain regression). The DEEP loop-closure (N400 error against this forward model) = the pri-1 north-star (solved-but-not-owner-DONE) → NOT front-run.
- **LANDED (`hdlab/predictive_world_model.py` NEW + `hdlab/situation_reader.py` + `verification/`):** the self-contained organ (event-concept extractor = hdlab.pos_tagger UPOS-verb + WN morphy lemma, dropping the experiments `_causal_network` dep; the online delta-rule world-model; `necessity_ids`/`causal_antecedent` counterfactual-necessity read; persisted foundation asset = the predictive_reader precedent). Organ self-test 3/3. VALIDATED the promotion reproduces the solver EXACTLY on simplewiki held-out: PC beats bigram **+0.2543 CI[0.2282,0.2812]** (solver +0.247) and necessity reader-vs-random **+0.3084 CI[0.2987,0.3184]** (solver +0.308), both CI-sep. WIRED an additive read-only pass `_read_predictive_causal` → `sm.causal_antecedent(effect_event_index)` / `sm.predictive_necessity(effect,cause)` / `sm.predictive_world_model()`, default-on (`track_predictive_causal`), lazy (loads the asset at most once, abstains if absent), byte-identical off vs on. Witness `verification/test_predictive_causal_wire.py` 7/7 (byte-identical + brain-sensible live read: `causal_antecedent(died)` = the 'shot' event, +0.4554 bits, ≫ 'walked' +0.0141). Foundation asset (692KB) gitignored/rebuildable (`python -m hdlab.predictive_world_model --build`) per the arc_parser/typed_spokes precedent.
- **full_gain_realized: PARTIAL.** The forward predictive-coding world-model + intrinsic counterfactual-necessity causal reader are LIVE + brain-foundational (the north-star FORWARD level). Board-invisible by design (external causal golds are position-artifact traps → the value is INTRINSIC surprisal necessity, not a QA dim). FOLLOW-ONS: (a) **DONE (CONT-29, `board_predictive_causal_necessity_dimension` in the board cell):** the intrinsic-necessity board arm makes the win board-visible — on a HELD-OUT simplewiki slice (lines 40000-46000, NOT in the asset's training window, n=6729) the reader's argmax-necessity antecedent scores 0.3602 bits vs random-context 0.038 (**+0.3223 CI[0.3113,0.3336] CI-sep**) AND vs the NEAREST-event position control 0.1124 (**+0.2478 CI[0.2397,0.2564] CI-sep** — beats position, escapes the confound); an INTRINSIC row out of the headline aggregate, degrades gracefully if the asset is absent. Remaining follow-ons (not front-run): (b) the RICH situation-event key (finding 23: verb+agent+patient+dense-valence+goal compound events, capacity-matched CI-sep) — enrich the world-model's event basis; (c) event_type routing MFS→contextual WSD (the one flagged non-BF component); (d) THE loop-closure — N400 prediction-error against this forward model = the pri-1 generative world-model (owner-review-pending; the recurrent predictive-coding loop is the substrate's #1 north-star).

### SELF-CONTAINMENT (CONT-29 go-forward, partial) — the REAL reproducibility defect FIXED; the rest is hygiene
- **FIXED (`c6ed33a86`):** the live reader imported the UNTRACKED `experiments.exp_unified_referent_gum_v1` just to call the tiny pure `_pron_gn` — and importing it pulled a WEB of coref-scratch cells (`gum_coref` / `exp_name_entity_clustering` / 3× `exp_commonnoun_*`) at module load. INLINED `_pron_gn` byte-faithful (GENDERED_PRON verified identical to the source 8/8; 16/16 pronoun forms match) using `hdlab.typed_coref.GENDERED_PRON` → the reader closure no longer pulls that web. This was the ONLY untracked reader dep → **the clean-checkout reproducibility BREAK is now closed** (a fresh clone's reader imports + reads without the scratch web).
- **RECONCILED (correcting my own enumeration):** the reader's `_es_mod` is `hdlab.copular_binding` (the PROMOTED organ), NOT `experiments._copular_nominal_events` — the `_copular_nominal_events` references were STALE COMMENTS (fixed). So `_copular_nominal_events` was a FALSE untracked dep.
- **ALSO SEVERED (`1018d3a2c`): `_forward_prediction_live`** — the predict_revise path imported it for 2 universal constant sets (NOMINAL/PRON_LOW) + a get_tagger() duplicating the reader's OWN `_frontend_tagger()` (same `_FRONTEND_POS_ASSET`), and it CIRCULARLY imports SituationReader. Inlined the constants byte-faithful + repointed to `_frontend_tagger()` (tags verified byte-identical) → the import + circular dependency are gone.
- **DONE (`45fbe404f`): `_temporal_order_register` → `hdlab/temporal_order_register.py`** byte-faithful (only the 2 internal imports repointed to the already-landed `hdlab.temporal_ordering`/`_multiframe`). NOTE it was actually a **LIVE default-on path** (`timeline_register=True` — the "default-off" docstring was STALE, same as `track_space`), feeding `sm.timeline_order` — so this was a real live-path promotion; verified byte-identical (`extract_passage(clause_pluperfect=True)`+`DiscreteOrderRegister` order/text_rank identical on a real passage; full read → identical `sm.timeline_order`).
- **DONE (`3ed0c7f4a`): `_space_reader` → `hdlab/space_reader.py`** byte-faithful (verbatim cp; perceptual deixis repointed to `hdlab.perceptual_access_ledger` [DEIXIS_AWAY/TOWARD verified EQUAL]; the 9 motion/place helpers NOT in `hdlab.location_register` INLINED VERBATIM as private helpers — canon_node 10/10, is_motion_verb 14/14, 6 constant sets all equal). Reader's default-on `_read_space` repointed; reads clean. **Reader DIRECT experiments imports now 1 (`_belief_reader`).**
- **🔬 LIVE-spaCy CHECK (belief path) — CONFIRMED NO VIOLATION (important, owner cares).** `experiments.state_register.extract_state_events` DOES `spacy.load("en_core_web_sm")`, `track_belief` is DEFAULT-ON (its "default-off" docstring is STALE), and `_belief_reader.drive` passes `nlp=getattr(led,"_nlp",None)` to it — BUT the live PAL (`hdlab.perceptual_access_ledger`, CONT-28 de-spaCy'd) hardcodes `self._nlp = None` (explicit comment: "this ledger never touches spaCy; _nlp stays None so any getattr(led,'_nlp',None) consumer" gets None). So `nlp is None` → the `extract_state_events` spaCy branch is DEAD in the live reader (never called); the belief path uses the in-substrate fallback. **The live reader is confirmed spaCy-free, belief path INCLUDED.** This scopes the final `_belief_reader` promotion: repoint `_space_reader`→`hdlab.space_reader` + DROP/guard the dead spaCy `extract_state_events` branch (behavior-preserving live).
- **DONE (CONT-29 CONTINUATION, 2026-09-09 session cont.): `hdlab/temporal_reasoner.py` (`cbf58ace7`) + `hdlab/temporal_script_schema.py` (`956a18042`) repointed off `experiments/` to the promoted hdlab twins (`temporal_ordering_multiframe`/`temporal_order_register`).** temporal_reasoner used M only for `build_constraint_edges` (takes `tagged` as an arg → M's tagger source irrelevant) + R for the BEFORE/AFTER/ABSTAIN constants; proven byte-identical (organ 18/18 + landing all-pass identical to baseline; R constants identical; `build_constraint_edges` output identical). temporal_script_schema used M only for `extract_events_punct` (build-only ROCStories asset dep); proven byte-identical on 15 varied sentences (asset already built → future-rebuild only). **Both now ZERO experiments imports.**
- **INTEGRATED (CONT-29 CONTINUATION, 2026-09-09 session cont.): the grounding-acquisition subsystem audit `audit_the_grounding_acquisition_subsystem_for_non_brain_faithful_stand_ins` (owner-DONE, STRONG).** Reverified 15/15 first-hand. A MAP (no hdlab writes): a runtime import+call trace of `substrate.read()` → CATALOG.md is the authoritative grounding-subsystem reference. #1 (G1, CRITICAL/NOT_BF): the meaning read-out is a bag-of-words co-occurrence cosine that LOSES to word-counting on the loop's own metric — INDEPENDENTLY confirms the C7 + pri-2 + pri-5 wall (representation, not computation). Prototyped fix (multimodal ATL hub + coverage-aware fusion + MEANING objective, 6%→86% of human, SimLex proxy not live loop) CONVERGES with pri-2/pri-5 → **no new solver problem (would dup); the hdlab wire is a careful measured landing gated on pri-5.** G2: `grounded_similarity` inert (wire it, part of the measured landing). §2b folded (E-grounding). Priority 4→99, status:INTEGRATED. **Owner-DONE gate check after this: only the name-bridge solver remains INTEGRATING (its careful FIXES A–E landing).**
- **INTEGRATING (CONT-29 CONTINUATION, 2026-09-09 session cont.): the name-bridge world-knowledge solver `world_knowledge_common_noun_to_name_bridge_the_81_percent_residual` (owner-DONE, STRONG, LARGE PARTIAL — reverify DONE, landing carefully).** Reverified 11/11 + 9/9 first-hand. A whole-slice LOCATED NEGATIVE (kinship axis refuted; residual = document-local COVERAGE, 30% of gold in no static KB; selection math-audited brain-faithful + brain-optimal, 66% of the brain) + a CI-sep doc-local win. **NOT yet landed** — a fidelity-sensitive chain, NOT rushed at loop-cap. **LAND-NOW (mine, next unit): safe_kb_types gate (new hdlab, additive/latent — the safety substrate the world-model's knowledge needs) + abstention + FIXES A–E into the C8/crosstype_bridge selection math (recency-not-ACT-R / graded-not-flat / capped-sum / classifying-noun is-a / familiarity-gate) with a REQUIRED board no-regress on crosstype_experiencer+namebridge + the full_resolve capstone.** **WORLD-MODEL-GATED: the generative who-is-who route (the accuracy lever, +0.064–0.080 CI-sep) coordinated with pri-1 `grow_the_causal_mechanism` (still in a solver).** §2b folded (E3). Stays on the owner-DONE gate as INTEGRATING until the LAND-NOW pieces verify no-regress. POSTED the idle-solver problem **pri-5 `measure_end_to_end_whether_the_meaning_fusion_lifts_live_grounding_coverage`** (cert 8/8) — the one genuine unfiled gap (dedup-scout confirmed; the C7 fusion-wire's blocking measurement).
- **INTEGRATED (CONT-29 CONTINUATION, 2026-09-09 session cont.): the C7 attractor-as-ranker solver `replace_the_attractor_as_ranker_with_a_graded_population_read_in_the_grounding_loop` (owner-DONE, STRONG, `1c008f25b`).** Reverified 23/23. A LOCATED NEGATIVE (the ranking is already a graded population read; attractor confined to the recognition gate; readout swap −1.4e-5 CI incl 0) + a real finding (all-BF equal-weight composition beats the mixed chain +0.028 CI-sep; relational is-a identity = the ~214× knowledge lever). C7 RE-SCORED in §2b (attractor-as-ranker NOT active harm; iterative_attractor/cleanup_family = BF for CA3 recall, canonicalize_fast = BF). 6 organs tagged (registry now 24: BF=4 BF_SPIRIT=14 NOT_BF=4 BF_UNVERIFIED=2). **FOLLOW-ON (NOT landed — honest bounds): the fusion-wire into `canonicalize_fast` is gated on the end-to-end grounding-COVERAGE measurement the solver flagged as not-yet-done (win proven on the sense-assignment RANKING proxy, not the live loop outcome) → measure-impact-first.** POSTED the deep successor for the idle fleet: **pri-2 `the_meaning_representation_is_a_point_vector_not_a_probabilistic_population_code`** (the C7 solver's named "largest architectural lever": build a probabilistic population-code meaning rep whose intrinsic precision tracks correctness; cert 8/8).
- **REMAINING = the FINAL cell `_belief_reader` (500L; 1 direct reader import) — a careful fresh-context unit (drop the dead spaCy branch, repoint to hdlab.space_reader). Plus the MODULE-LOAD `exp_bridge1_*` web via `context_grounded_valence`/`force_dynamics_valence` (the C4 remaining port — fidelity-risky, one cell trains a perceptron at inference). All TRACKED — pure HYGIENE, NO clean-checkout break. The prior "ENTANGLED cluster" note below is superseded — `_space_reader` is DONE:** `_belief_reader` → `experiments._space_reader` (build_backbone/_frontend/_cluster_covering/_node_from_token) + `experiments.state_register` (extract_state_events); `_space_reader` → `experiments.location_register` (canon_node/_PLACE_PARTICLE/DEICTIC_RETURN_GROUND/_NON_LOCATIVE_NOUN/_WITHIN_SCENE_GROUND/is_motion_verb — NONE in `hdlab.location_register`, a DIFFERENT module) + `experiments.perceptual_access_ledger` (DEIXIS_AWAY/TOWARD — hdlab HAS these). Plus the MODULE-LOAD `exp_bridge1_*` web via `context_grounded_valence`/`force_dynamics_valence`. FIDELITY-SENSITIVE: `location_register`/`state_register`/`perceptual_access_ledger` all have CONT-28-fixed hdlab twins whose APIs DIFFER from the scratch cells → a repoint would CHANGE behavior (not byte-identical) → needs a careful multi-cell reconciliation + net-positive/no-regress measurement, NOT a rushed inline. Do as a dedicated fresh-context PROJECT (promote `_space_reader`+`_belief_reader`+reconcile `location_register`/`state_register`). Lower urgency (no clean-checkout break). **Reader DIRECT experiments imports: 6 → 2 this session (`exp_unified_referent_gum_v1` severed, `_forward_prediction_live` severed, `_temporal_order_register` promoted; `_copular_nominal_events` was a false dep).**

---

## 2026-09-08 (CONT-28) — the 100%-BRAIN-FOUNDATIONAL REFRAME + dormant-organ research + a clean located negative

**OWNER DIRECTIVE (load-bearing, 09-08, emphatic twice):** "every component in substrate has to be 100% brain-foundational — a requirement for this to work"; "we cannot supplement cheap things because it's easy." → brain-foundationality is now a HARD PASS/FAIL GATE per component (not a rating): a non-brain-foundational component is a DEFECT that BLOCKS → fix-to-brain-faithful or REMOVE, never keep for a metric, never supplement a gap with a cheap/easy stand-in. **The brain-foundational audit-and-fix is now the SPINE; board-movement is downstream of the gate.** [[every-component-must-be-100-percent-brain-foundational]]

**This session:**
- **Plan B DONE (`b619bf1cf`):** 28 `INTEGRATED_BY_STRATEGY` markers backfilled → `problem_ledger` accurate (awaiting 31→3; the 3 are not-owner-DONE). 0 genuine integration misses.
- **Docstring fix (`af1f825a2`):** `bind_event_tokens` is DEFAULT-ON (stale "default-off" doc corrected; zero behavior change).
- **Dormant-organ brain-foundationality research (5 candidates) — the "flip a dormant organ" premise mostly DISSOLVED:** typed_coref resolution-consumer + graded_coref_pick pick are ALREADY LIVE; register readouts (decode_serial/gated/divnorm) are ORPHANED by design (live path uses a different codec `bound_event_backbone`; a book-scale capacity lever only → leave dormant). **crf_tagger REVERSED under the gate:** an offline CRF/forward-backward POS decoder is a CHEAP ML SUPPLEMENT for a pinned property (calibrated graded lexical-category belief) → do NOT wire; the property must come from the brain-faithful mechanism (parallel constraint-satisfaction / the incremental parser). structural_do: the brain-faithful move is ABSTAIN (Ferreira good-enough), keep default-off.
- **The two biggest latent wins are BLOCKED on the de-leak (pri 6):** typed_coref's `sm.commonnoun_resolution.resolved_ref` is written+read-by-nothing (rightful consumer = affect/OCC + crosstype experiencer binding); the crosstype live-wire is deferred pending the same de-leak. The de-leak has ONLY a PROBLEM.md — UNPICKED by the fleet (owner fleet-routing flag). C8 encyclopedic type-comparator is half-wired (asset absent) = typed_coref's dominant fidelity gap (~88% of headroom = missing world knowledge).
- **COREF-DEFER = rigorous LOCATED NEGATIVE (keep OFF; LANDED NOTHING).** `exp_coref_defer_consumer_gum_v1.py`: selective-reliability defer on the graded pick's entropy, modern GUM he/she n=1240 → answered-acc 0.6014 @cov 0.68 vs blanket 0.5855 = +0.0159 NOT CI-sep [−0.029,+0.058], loses the twin null p95 (+0.0163); entropy right-vs-wrong AUC 0.527 (raw recency 0.5546 BEATS it). Deferring drops MORE correct than wrong → net-corrupts who-has-what. The 0.806 "entropy predicts its own errors" was 19c; DOESN'T transfer. Real finding: the modern he/she residual is a CANDIDATE-SET / gender-agreement (φ) QUALITY problem, not a confidence-gating one; upstream fidelity note — `TUNED_WEIGHTS` recency=0 yet raw recency out-predicts entropy (difficulty currency diluted by candidate count on modern's larger pools). Landed nothing (no default-off flag, no uncalibrated field — per no-more-default-off + the gate). Witness + metrics on disk. NOTHING pushed.

---

> **⚠️ REFRAMED (CONT-28, owner 09-08): the brain-foundational audit-and-fix is now the SPINE. The GO-FORWARD PLAN below (dormant-flip/realization ordered by board-value) is SUBORDINATE + gated on the 100%-brain-foundational check per component.**

## 🔭 GO-FORWARD PLAN (recorded 2026-09-08 for the next session — the substantive backlog; owner agreed it's large + sequenced fresh)

The owner-DONE integration gate is CLEAR of recent solutions (all folded in this session). The remaining work is REALIZATION + OPTIMIZATION (mine, independent of solvers), measurement-gated and board-touching — do it as FOCUSED FRESH passes (one unit at a time; a fresh agent per unit is fine; gate every flip on net-positive + no-regress; NEVER botch at deep context). Ordered:

- **(A) DORMANT-ORGAN FLIP-TREATMENT BURN-DOWN [highest-value "is it optimal" work].** The live reader imports **~52 of 182** hdlab modules (up from 19 at the 08-30 WIRING_MAP — the reasoning-phase wave wired a lot live). Still **~137 island-only capabilities + 24 ISLAND** (`tools/wiring_debt.py`, `data/capability_registry.jsonl`, `notes/WIRING_MAP.md`). ⚠️ **MANY are LATENT BY DESIGN — flip-tested net-neutral/negative and correctly kept off; do NOT re-flip those.** Per organ: measure its LIVE board impact → turn on IFF net-positive + no-regress (the flip-treatment). Drive one organ at a time.
- **(B) LEDGER/MARKER + CASUALTY AUDIT.** `problem_ledger.py --check` shows **31 "awaiting integration"** = historical integrations MISSING the `INTEGRATED_BY_STRATEGY` SOLVED.md append (recorded instead via PROBLEM.md reviews + this ledger). Verify each has a SOLVER REVIEW / ledger entry (→ append the marker) vs a GENUINE miss (→ integrate). Restore `theory_of_mind_is_proven_only_in_a_synthetic_microworld/PROBLEM.md` (a git-incident casualty, flagged MALFORMED; SOLVED intact) OR confirm-leave (old + solved). Bookkeeping, low-risk.
- **(C) P1 ONE-PARSE read() RESTRUCTURE [deferred optimization].** 4× fewer parses, byte-identical target; P4 exact-MAP already landed (`ff8008131`). A read()-architecture change, not a capability gap.
- **SOLVER-SIDE (integrate PROMPTLY on owner-DONE, full-inventory per [[implement-all-upstream-chain-fixes-in-incoming-solutions]]):** the **de-leak** `replace_the_entity_gate_gold_coref_inheritance_with_online_cue_based_clustering` (pri 6) is the SHARED unblocker that turns crosstype (+0.0528) AND typed_coref's honest-floor wins into LIVE board gains — HIGHEST-leverage. Awaiting owner review (SOLVED, NOT owner-DONE → not mine yet): `build_the_generative_result_state_world_model` (pri 1), `chain_multi_step_plans_and_scripts...` (pri 8), `generate_dont_retrieve_causal_edges...` (pri 4).
- **⚠️ PROCESS (USER 09-08, [[be-careful-with-projects-in-process]]):** never delete a `notes/problems` folder with a SOLVED.md/analysis (it's ACTIVE, not a stray — a solver may have adopted it); commit strategy PROBLEM.md's to git IMMEDIATELY (a concurrent git op silently deleted 2-3 untracked briefs this session — restored + now tracked).

---

## 2026-09-08 (CONT-26) — INTEGRATED owner-DONE `report_the_typed_coref...` (EXCELLENT, reverified 19/19) + LANDED P2 spatial semantic-typing (of the CONT-25 chain). FULL fix/prototype inventory tracked per the implement-all-upstream directive (owner re-emphasized: "keep track of ALL fixes + prototypes; I had them rigorously evaluate the brain-foundationality of ALL upstream components").

### report_the_typed_coref_organ_onto_the_live_reader_common_noun_path_and_measure  [INTEGRATED; EXCELLENT; reverified 19/19; PARTIAL/located-negative; realization wire tracked]
- **CLAIMED / instrument:** per-mention common-noun RESOLUTION on GUM modern TEST (n=2855, resolved-referent nominal-dominant gold-eid == mention-eid; same population+scorer as the URG board floor). **Reverified first-hand 19/19** (`verification/test_commonnoun_binder_live_report.py`), every headline recomputed from source.
- **HEADLINE (a located negative that redirects the wire):** the brief's mechanism (re-port the two levers onto `commonnoun_binder`) REFUTED — reaches only PARITY (0.5201 vs string-identity 0.5412, CI incl 0). The DEFICIT is the deployed binder's BINDING: the `typed_coref` binding on the identical live schema BEATS the binder +0.0767 CI-sep (0.5671 vs 0.4904) and beats string-identity +0.0259 CI-sep. Live-schema drop-in `typed_coref_liveschema_resolve` = 0.5664 (recovers the organ), twin loses CI-sep, NAME no-regress +0.0024.
- **FULL FIX / PROTOTYPE / UPSTREAM-EVALUATION INVENTORY (track ALL — owner directive):**
  1. **FIX (headline wire, tracked realization):** serve the reader's common-noun RESOLUTION consumer from `hdlab.typed_coref.TypedCorefResolver(bridge=True,bridge_write=False,type_comparator="typed_spokes")`, NOT `commonnoun_binder`; ADD a `board_commonnoun_resolution_dimension` arm; additive/default-on-safe (clustering `sm.entities` byte-identical). Keep `commonnoun_binder` for the CLUSTERING consumer.
  2. **UPSTREAM FIDELITY FINDING:** the deployed binder's person-gate + modifier-split + event-centrality tie-break are LitBank cluster-F1 OUR-INVENTIONs that HURT modern per-mention resolution. The **person-gate specifically is non-brain-foundational for coref** (the brain type-bridges OBJECTS, not only persons); removing it = +0.0235 CI-sep even inside the binder.
  3. **UPSTREAM HALF-WIRED COMPONENT:** the type comparator uses only `coref_type_license` (C5/WordNet) — it does NOT use the landed **C8 DBpedia encyclopedic route** (`entity_type_lemmas`/`type_licenses`/`available_entity_type`); the 548MB C8 asset is gitignored/not rebuilt so `available_entity_type()` abstains. FIX: wire the C8 encyclopedic route into the type comparator (upstream fix to implement).
  4. **PROTOTYPE (win):** encyclopedic route (WordNet instance-of + person-typing) +0.0102 CI-sep over the taxonomic-only fix; twin loses. Strong version = the C8 DBpedia route (sibling P31 measured +0.0955 on the name-slice).
  5. **PROTOTYPE (LOCATED NEGATIVE):** reasoning route (situation-model role-fit / agentive nominalization) = ~wash (+0.0014, CI incl 0); re-recognition is knowledge-bound, not local-role-bound (~6/2855 role-resolvable).
  6. **PROTOTYPE (for the sibling problem):** COARSE person-typing (person-name → person-CATEGORY anaphor) +0.0102 CI-sep, anaphor-type match beats a person-REACH control +0.0053 — a REAL type constraint DISTINCT from the occupation-KB located negative; handed to `world_knowledge_common_noun_to_name_bridge_the_81_percent_residual`.
  7. **DIAGNOSTIC CEILING:** ORACLE type comparator = 0.7492 (+0.2081 over floor; +0.1828 over the WordNet fix) → ~88% of achievable headroom is WORLD KNOWLEDGE the WordNet(C5) comparator misses (encyclopedic instance-of + schema/scenario associative anaphora, Sanford-Garrod).
  8. **DE-LEAK CONFIRMATION:** the pipeline never uses the gold-coref peek in a resolution DECISION (only scoring) → all numbers are the honest de-leaked floor. Confirms the `replace_the_entity_gate_gold_coref_inheritance...` posted problem.
  9. **DOWNSTREAM REGRESS-CHECK:** no downstream consumer regresses (binder unchanged for clustering; resolution stream is additive; non-writing bridge byte-identical in clustering). p11 WRITING type-license = wash-to-negative on resolution (pronoun drag) — do NOT flip.
  10. **OOD:** GENTLE (n=275) — the defect+fix replicate in direction+magnitude (+0.0255 vs GUM +0.0252), NOT CI-separated (underpowered), no TEST-tuned params.
- **AUDIT UPDATE folded (§2b):** `commonnoun_binder` = a cluster-F1 organ NOT a resolution organ, person-gated (non-brain-foundational for coref); `typed_coref` = the brain-foundational RESOLUTION path. **status:INTEGRATED, priority 4 dropped, review EXCELLENT.**
- **REALIZATION fix 1 (typed_coref resolution consumer + board arm) — PROTOTYPED + MEASURED (impl agent 2026-09-08; proposed `situation_reader` diff at scratch `typed_coref_resolution_wire_PROPOSED.py` [5 anchored edits, validated end-to-end against a real SituationReader], board arm `board_commonnoun_resolution_dimension` + witness `test_commonnoun_resolution_wire.py` 5/5 ON DISK, uncommitted; strategy to APPLY the hdlab diff + commit).** Measured GUM TEST n=2855: live `sm.commonnoun_resolution` = **0.5394**, **+0.0238 CI[+0.0115,+0.0360] over the FAIR same-lemmatizer floor (CI-sep)**, info-free twin loses (+0.0228 CI-sep), **PARITY with the gold-lemma board floor 0.5412** (−0.0018, CI incl 0). Additive/default-on-safe (`sm.entities`/`coref_acc` byte-identical; gold-free — records byte-identical with gold stripped). Appos/copula is-a seed from the reader's OWN arc-labeler parse = +0.0070 CI-sep (no second parse).
  - **🆕 NEW TRACKED UPSTREAM ISSUE (critical to perf; 2×2 decomposition): the shortfall vs the proven 0.5664 is NOT the binding — it is the reader's head-lemma KEY.** Head KEY (reader `head_lemma` vs GUM gold lemma) costs −0.0214; routing (`is_name` vs gold mtype) only −0.0032; WordNet morphy +0.0004. The gold-routed reference recovers 0.5664 CI-sep (positive control) → the binding is faithful; the gap is the **GUM gold-lemma annotation regime (sees through redacted `____` tokens + copula-head normalization — a corpus artifact NOT reproducible from raw text; standard lemmatizers do NOT help).** So the live upstream lever = a stronger reader head-lemma/normalization (a separate small upstream fix, surfaced per full-stack-upstream). full_gain_realized: PARTIAL (beats the fair floor CI-sep live; ties the gold-lemma floor; board-visible once the arm+diff land).
- **REALIZATION fix 3 (C8 encyclopedic route into the type comparator):** BLOCKED on the 548MB C8 DBpedia asset (gitignored/unbuilt → `available_entity_type()` abstains); rebuild + wire = tracked. **Judgment call for strategy (agent flagged, nothing hidden):** the board arm's strongest floor is set to the fair same-lemmatizer floor (0.5156, beaten CI-sep) with the gold-lemma floor (0.5412, parity) reported alongside.

### P2 spatial semantic-typing (of the CONT-25 `extract_spatial_and_causal` chain)  [LANDED + wired; verified; committed `891d13861`]
- **LANDED** `hdlab/joint_relation_frontend.joint_spatial_frames_ext` (byte-parity 0/12 vs the source minus the REFUTED marginal-attachment) + **WIRED** into `situation_reader._read_spatial_reasoning._model()` (the reader's spatial situation model — the consumer explicitly built to wait for this extractor; projective graph was empty). Verified active: real Bleak House read = 109 containment edges from text (vs 1 register-only); every `sm` field byte-identical off vs on (lazy, no-regress); W5d refreshed (its "extractor NOT landed" rationale was stale).
- **KEY MEASURED FINDING (instrument mismatch):** re-pointing the board's `spatial_relational` dim (SpartQA-HUMAN) to the ext CRATERS it (coverage 2.8% vs incumbent 37%; acc 0.03 vs 0.31) — SpartQA is synthetic LEFT/RIGHT/front shape-grid QA, the ext targets NARRATIVE topological/projective prose (in/on/above/below/behind + containment/thematic). So: **do NOT re-point SpartQA** (keep the incumbent for that gold); the reader keeps `spatial_text_edges` ON (right narrative extractor, cannot regress the standalone SpartQA dim); the reader's win is narrative extraction PRECISION (SpaceEval 0.5712), needing its OWN arm.
- **ADDITIONAL CONSUMERS mapped (owner "what else should consume this"):** (a) `location_register` — **LOCATED NEGATIVE (measured 2026-09-08, prototype `experiments/_p2_register_enrichment_probe_v1.py`, n=606):** feeding the ext's edges into the per-entity location-STATE register does NOT help + regresses `where_is` — figure-ground/thematic edges are the WRONG TYPE (goal/source collapse to ~4 redundant cases already caught by the register's gated motion reader; the `in`/object-object edges pollute where_is, broke 8/fixed 0; a shuffled-ground twin did BETTER = anti-informative). Do NOT feed the ext to the register; the where_is lever = named-Ground SELECTION (the `space_where_is` SOLVED). Reader follow-on comment closed. `spatial_contains`/`spatial_relative` unaffected (seeded from ext TEXT, the live P2 wire); (b) `force_dynamics_typer` — ZERO spatial consumption, yet Talmy force dynamics ARE spatial (containment/support/contact enable/prevent) — physical-causation grounding follow-on; (c) where-QA + a SpaceEval-precision board arm (live≠scored). full_gain_realized: PARTIAL (live-on-invocation; board arm + where-QA pending).



### grow_a_broad_causal_event_order_knowledge_store_for_the_implicit_event_path  [INTEGRATED; EXCELLENT; reverified 8/8; store-swap wire tracked]
- **CLAIMED / instrument:** TRACIE iid TEST (implicit-event before/after, n=1924, MODERN ROCStories-derived gold; paired clustered bootstrap). Broader store (tense-agnostic re-mine of 98,161 ROCStories → 454,129 ordered verb-pairs + consolidation gate + causal-cue blend), ungated **0.5655 vs SEED floor 0.5296 (+0.0359 CI[+0.0117,+0.0616] CI-sep)**; vs abstain +0.0655; twin loses +0.0759 CI-sep; coverage 0.29→0.607 (2.1x). Reverified 8/8 first-hand.
- **FULL FIX/PROTOTYPE INVENTORY (track ALL — owner directive):** (1) **FIX** broader 454k store; **WIRE (tracked realization):** rebuild the store asset offline + point `hdlab/temporal_reasoner` implicit branch + `hdlab/temporal_script_schema` at it (strength-gated override on the iconicity default; narrated path byte-identical, blast radius = implicit branch only; `test_temporal_reasoner_organ` 18/18 unchanged). (2) **UPSTREAM (already LIVE):** the tense-agnostic UPOS event detector = the real coverage lever (2.06x); this is an OFFLINE-mine fidelity fix (the seed used the old tense-GATED extractor), NOT a live-extractor change. (3) **LOCATED NEGATIVE:** transitive closure (coverage up, tail-accuracy ~chance). (4) **LOCATED NEGATIVE:** causal-cue directional blend (+0.001; ROCStories connective-sparse). (5) the reasoner's `SCRIPT_M_MIN` = the speed-accuracy SAT dial. (6) **DEEP CEILING FINDING:** ~0.61-0.68 per-pair is STORY-CONDITIONING — the aggregate store IS the context-free SCRIPT PRIOR (right ~60% when the story follows its script; the ~40% wrong ARE deviations); closing it needs READING the implicit event's context (categorically deeper, NOT a fidelity bug). (7) brain-fidelity: tense-agnostic = Zwaan-Langston-Graesser event-indexing (time = CUE not GATE); aspect = graded confidence feature; causal typing partially grounded (Trabasso/McKoon-Ratcliff).
- **AUDIT UPDATE folded (§2b):** the script/event-order store's ceiling is context-free script-prior; residual = story-grounded instance-order. **status:INTEGRATED, priority 12 dropped.** full_gain_realized: PARTIAL (store proven + reverified; the offline-asset rebuild + reasoner wire is the tracked realization).

---

## 2026-09-07 (CONT-25) — INTEGRATING owner-DONE `extract_spatial_and_causal_relations...` (reverified 11/11; SPATIAL win + CAUSAL located-negative; P4 + P2 landed, P3/P5 pending)

Owner-DONE (marked 21:38), reverified first-hand 11/11. A LARGE 5-landing chain (P1-P5) + 3 BUILD follow-ons (P6-P8) under the implement-all-upstream directive. Given the session depth, landing P1-P5 CAREFULLY across continuations (verify each), not in one exhausted burst. **Mechanism + the unifying law = audit §2b (newest).**

### extract_spatial_and_causal_relations_from_prose_whole_subgraph_survival  [INTEGRATING; reverified 11/11; landings pending]
- **SPATIAL = WIN (brain-foundational):** the semantic Figure-Ground typer (event-figure binding + deixis + coordination + partitive + Herskovits preposition-semantics + thematic nested binding over exact-MAP) beats the incumbent CI-sep on TYPE-PRECISION (0.5712 vs 0.5179, +0.0533) and a density/proximity floor +0.3864 CI-sep on hard negatives (floor collapses to 0.185). Recall-survival is DENSITY-CONFOUNDED → score marked-relation extraction on type-precision.
- **CAUSAL = the blessed LOCATED NEGATIVE (resolved):** causal type is UNMARKED (65.6% MAVEN) → no parse-bound typer; the generative causal-antecedent search (recency prior + world-model typing) wins on binding precision (0.472 vs 0.140, +0.339 CI-sep) but coverage-capped ~5% (CSKG refuted at 2%); coref lifts it 3.29×. THE KEYSTONE: the meaning/entity channel is built but never CONSUMED in read().
- **LANDING PLAN (P1-P5 LAND-NOW, additive/no-regress; P6-P8 BUILD):** P1 unified one-parse front-end / P2 spatial thematic binding + learned construction inventory / P3 nominal-event flip-on (0.61→0.85) / P4 exact-MAP decode default (after 8-consumer no-regress) / P5 coref→causal simulator (3.29×). P6 read()-time meaning-consumption keystone / P7 spatial parser residual / P8 full causal reader. DO-NOT: CSKG (2%), parse-marginal, object-affordance, connective causal.
- **STATUS: P4 LANDED + VERIFIED (`ff8008131`).** Routed `hdlab.joint_relation_frontend.parse_sentence` through the exact-MAP graded decode; verified byte-identical FIRST-HAND (1200 GUM sents: 230 get the improved valid-tree parse, temporal events byte-identical on all 230, 0 diffs; the reader's ONLY parse_sentence consumer is joint_event_ranks). Also FIXED the crosstype/namebridge board arms (`64daac408`) — they re-parsed all 275 GUM docs, making the full board intractable (capped to 120/150).
  - **✅ FRESH BOARD BASELINE PERSISTED (2026-09-08 02:32, `bqcck6rea`; the prior non-persist was concurrent-run interference, resolved by an explicit canonical output name + unbuffered).** aggregate_19c_free 0.6253; per-dim reference for the P-landings: **spatial_relational 0.2835** (vs floor 0.2205, CI-sep) = the dim P2 must not regress + should lift; coref 0.4681 / common_noun_coref 0.5671 / who_did_what_patient 0.8311 / state 0.8333 / wic 0.6639. **BOTH this-session board arms VALIDATE FULL-BOARD (not just smoke): crosstype_experiencer 0.2168 vs 0.1311 CI-sep over floor+twin (n=549); namebridge 0.5815 vs 0.4719 CI-sep over floor+twin (n=356).** The board is healthy; the 15 new-arm rows all present.
  - **(historical) BLOCKER note (now RESOLVED): the earlier board `--run` did NOT persist fresh metrics** (metrics.json ts stayed 02:10 despite "exit 0"; possibly the SH-hook output-dir redirect or a concurrent write). A fresh board baseline (spatial_relational + causal dims) is the no-regress reference P2 (spatial semantic-typing channels → hdlab.joint_relation_frontend) + P3 (nominal-event flip) + P5 (coref→causal) need. **NEXT: diagnose the board-run persistence, get a clean baseline (arms now capped), then land P2/P3/P5 verified vs it.** P1 (one-parse) is a read() restructure (deeper); the metric-correction (score spatial on precision) is methodological.
  - Remaining LAND: P2 (spatial channels, additive), P3 (nominal-event flip), P5 (coref→causal), P1 (one-parse restructure). BUILD follow-ons P6-P8 (meaning-consumption keystone / spatial parser residual / full causal reader) largely map to filed problems.

---

## 2026-09-07 (CONT-24) — INTEGRATING owner-DONE `acquire_wikidata_p31` (FULL-CHAIN, per the new implement-all-upstream directive) — C8 spoke + board arm landed+verified; chain continuing

Owner-DONE (marked 21:11), reverified first-hand 14/14, landing the FULL chain per the new owner directive ([[implement-all-upstream-chain-fixes-in-incoming-solutions]]: implement ALL upstream fixes, accept breakage). Committed path-limited (NOTHING pushed). **Mechanism = audit §2b (newest).**

### acquire_wikidata_p31_entity_type_kb_for_name_bridge_coref  [INTEGRATED; reverified 14/14; core + 4 upstream fixes landed, live-wire = tracked follow-on]
- **THE WIN:** name-bridge coref (anaphoric common noun → PROPER-NAME antecedent, "the artist"←Zurbaran; ~6-10% of anaphoric common nouns, unreachable by WordNet) via the brain-foundational TWO-ROUTE CLS mechanism — consolidated entity-type KB (DBpedia InstanceOf 2022.12.01) UNION episodic in-text is-a — GRADED constraint-integration select + thematic deverbal-agent route: **0.5843 vs recency floor 0.4719 = +0.1124 CI[+0.0702,+0.1573] CI-sep**; licensed subpop 0.579→0.863 (+0.284 CI-sep); shuffled-KB twin loses; 28 KB-unique wins near-disjoint from 27 in-text-unique (both routes load-bearing). A KB alone is coverage-bounded (+0.034 not-sep) — the two-route completion is what crosses the wall.
- **LANDED + VERIFIED (4 of the chain):** (-1) **C6 part-whole→bridging_inference HYBRID, FLIPPED LIVE** (`a057a63bc`) — new opt-in `c6pw` source on BridgeInference (select by the landed C6 directed typed spoke where it COVERS the decision, symmetric fallback else; byte-faithful to the reference cell's HYBRID arm) + `situation_reader.bridge_source` "hub"→**"c6pw"** (SAFE: byte-identical to hub except on covered part-whole decisions, where it uses the better directed signal — fixes "bridging picks the distractor", keyboard/computer vs desk). Organ witness 33/33, reader landing 17/17 (8 core dims byte-identical). (0) **compact-store swap** (`47ab3aec4`) — C8 reads the 81.9MB compact npz (sorted blake2b-64 hashes + uint16 class-ids, O(log n)) preferred, sqlite fallback; byte-equivalent (1444 GUM surfaces 0 mismatches; parity 0/8), 7x smaller. (1) **C8 directed entity-type spoke** in `hdlab/typed_spokes.py` (`664139141`) — proper-name entities onto the C5 taxonomy (read through the existing C5 is-a closure), island-safe; ported byte-faithfully from `experiments/_entity_type_spoke.py` + `build_entity_type_spoke_v1.norm_surface`; PARITY-EXACT vs the reference (0/8 surfaces: type_licenses + entity_type_lemmas identical) + C5/C6/C7 no-regress (test_world_knowledge_typed_spokes.py 22/22). API: `entity_classes/entity_type_lemmas/entity_type_synsets/entity_is_a/type_licenses/available_entity_type`. (2) **`board_namebridge_dimension`** arm (`0e1fa55ba`) — board-visible via the landed C8: kb_graded 0.573 vs recency 0.472 (+0.1011 CI-sep), peak kb_thematic 0.5843, shuffled-KB twin loses (+0.2135 CI-sep). Both behind run_new_arms, OUT of the headline aggregate.
- **REMAINING (1 careful follow-on + bookkeeping):** (a) the two-route LIVE-reader wire into `_apply_commonnoun_gate` (gate→graded-select name-bridge path; SAME reader-dict↔GUM-Doc adapter risk as the crosstype wire → careful follow-on, mirrors the filed `wire_the_crosstype...` brief; the entropy→coref confidence rides on it — name-bridge coref must be live first); (b) `knowledge_foundation_manifest.json` C8 registration (gitignored bookkeeping — the organ loads its assets directly, functions without it, like C5/C6/C7). **The CORE + 4 upstream fixes are LANDED + VERIFIED** (C8 knowledge in hdlab reading the compact store; name-bridge win board-visible; C6→bridging distractor-fix flipped live). Marking the problem INTEGRATED; the live-wire is the tracked realization follow-on.
- **LOCATED NEGATIVE (in-solution, do NOT re-pursue):** coercion-tolerant/graded type-license HURTS (-0.037 CI-sep) — the binary type-license is at its knee (reproduces the landed C5 result); the residual is encyclopedic COVERAGE (0.160, dominant) + granularity, not license tolerance. Dense C1 rep FAILS<floor (superposition) → the TYPED spoke is the BF type-rep.

---

## 2026-09-07 (CONT-23) — INTEGRATED owner-DONE `route_the_unified_referent`: REFUTED (unified_referent stays default-off) + WIN LANDED (cross-type definite→name bridge) + filed the next brief wave

Owner-DONE (marked 19:36 — the gate was NOT empty at recovery), reverified first-hand 29/29, landed additive, committed path-limited (NOTHING pushed). **Mechanism = audit §2b (newest, E3).**

### route_the_unified_referent_to_its_non_coref_consumers_where_it_helps  [INTEGRATED; two-part; reverified 29/29]
- **PART 1 — REFUTED (a rigorous located negative):** routing the landed unified referent to the 3 live non-coref consumers is flat-to-negative on modern GUM (137-doc TEST): C1 entity-layer +0.0036 (marginal, SUBSUMED by the dormant entity-KB resolver 0.7033); C2 hard-link −0.0089; C3 experiencer −0.078. The `reader_coref` lever is INERT on GUM regardless of source (a 19c-only effect); he/she byte-identical (16/16); twin loses on CoNLL. The reference-harness "non-coref lifts" (+0.072/+0.106) were the PRONOUN pick under a different scorer (Ariel cue-specificity). → **Keep `hdlab/unified_referent.py` DEFAULT-OFF everywhere** (already default at `situation_reader.py:939` — no code change). **CORRECTS the p12 note** that the unified referent's home is the non-coref consumers.
- **PART 2 — WIN LANDED: `hdlab/crosstype_bridge.py` (`7545f2a34`).** The goal (a per-consumer CI-sep gain) achieved a DIFFERENT brain-foundational way: a glass-box cross-type definite→name bridge. Deployable config `gated_binds(bind_mode="cue_conf")`: (1) precise-constructs predication detector (appos/copula/cop-verb/conj-shared-subject/detached/relcl/title/FrameNet-verbal + unique-gender age/gender; 0.96 precision) → (2) anaphoricity/familiarity gate (Heim; ~80% of definites non-anaphoric) → (3) cue-based ACT-R retrieval (Lewis-Vasishth; Almor desc-boost + Centering ROLE_PROMINENCE + gender + recency) → (4) full-referent competition (Heim/DRT: a NAME bind fires only when it out-competes the same-head COMMON referent) → (5) retrieval-confidence gate (McElree SAT; swept `conf_thr` = the tunable operating point). NO trained classifier, NO LLM.
- **VERIFIED FIRST-HAND:** full witness suite **29/29** (landable 4/4, upgrades 3/3, chain 2/2, precise-constructs 4/4, refutation 16/16) + **EXACT organ-vs-reference parity** (precise_constructs 0/120 docs, gated_binds 0/600 calls across 5 conf thresholds, cue_competed 0/120) + corpus-free self-test. **Measured: affect/goal EXPERIENCER lift +0.0528 (conservative) → +0.086 (liberal, full-referent competition) CI-sep** on modern GUM (275 docs), info-free twin loses, live-parse survives at 0.86 precision.
- **LOCATED NEGATIVE (in the same solution — do NOT re-pursue):** a Wikidata occupation KB (P106/P39) adds +0.000 — people are re-mentioned by AGE/GENDER/RELATION/context, not catalogued occupation (1/16 descriptor-matches-occupation). The right levers are all glass-box or discourse; there is no static-KB lever left.
- **STATUS: committable but LATENT.** The organ is `gum_coref.Doc`-schema typed; no live consumer reads it yet. **FOLLOW-ON (filed): the LIVE WIRING** — into the reader's DEFAULT `_apply_commonnoun_gate` path (NOT the dormant `entity_kb_resolver` branch), behind a new `crosstype_bridge` opt-in flag, byte-identical off, via a Doc adapter over the reader's parsed tokens → measure the live C3 experiencer lift + no-regress on C1/C2 → flip per no-more-default-off. The live consumer chain is `goal_register.make_canonicalizer` → `affect_register.bind_experiencers`.

### 📌 OWNER (flagged 2×): NO UNASSIGNED PROBLEMS on the dashboard → next brief wave FILED (idle fleet)
- The fleet's assignable queue is empty (3 SOLVEDs await owner review incl the pri-1 generative WM). Filed the next wave from the named follow-ons across recent SOLVEDs (crosstype-bridge live-wire + others) so the fleet has work; see the problems dir.

---

## 2026-09-07 (CONT-22) — TYPED COMMON-NOUN COREF promoted to a committable hdlab organ (realization; resolves Q115 for this win)

Realization work (fleet-independent, from the CONT-21 follow-on), verified first-hand, committed path-limited (NOTHING pushed). **Mechanism = audit §2b (newest).**

### hdlab/typed_coref.py — promote the typed common-noun coref win to a committable organ  [REALIZED; verified EXACT parity + witness 9/9 through the organ]
- **WHY:** the CONT-21 win (board `common_noun_coref` 0.5671 vs string-identity 0.5412) lived ONLY in the UNTRACKED, Q115-blocked board/reference cells → a fresh checkout could not reproduce it. Promoting the load-bearing algorithm to `hdlab/` makes it version-controlled + reproducible + wireable.
- **WHAT LANDED:** `hdlab/typed_coref.py` — `TypedRef` (3-view card nominal/all/kb), `TypedCorefResolver` (=`TypedResolver` alias), `appos_copula_isa`. Self-contained: ZERO experiments/ imports, ZERO frozen-asset dep (typed-spokes lever reads only nltk-WordNet). Reuses `hdlab.salience_binder` / `hdlab.coref` / `hdlab.typed_spokes`. Corpus-free self-test (`python -m hdlab.typed_coref`): three-view de-pollution + non-writing typed bridge (car↔vehicle licensed).
- **VERIFIED FIRST-HAND:** EXACT parity vs the reference `TypedResolver` (**0/137 docs mismatch, 9782 mentions** compared, identical per-mention tuples) + the headline witness `test_commonnoun_typed_identity.py` reproduces **9/9** (typed 0.5671 / string_id 0.5412 CI-sep, W2 twin loses, W5 pronoun/kb byte-identical, name +0.024, W6 typed_spokes>WordNet, W7 always-hold optimal). Committed `dd399f65b`.
- **Q115:** RESOLVED for this win (option c) — hdlab is exempt from the experiment-cell hook. The untracked board/reference/witness cells still carry byte-identical copies (on-disk scratch; point them at the organ when next touched — NOT deleted: risky 220-line edit on uncommitted scratch, no committed benefit). The broader Q115 policy fork stays OPEN for the parser reverify cells + future board arms.
- **STATUS: LATENT (committable).** The organ is committed but has NO live read()-time consumer yet — it is `gum_coref.Doc`-schema typed; the live reader's common-noun path (`hdlab/commonnoun_binder.py` / coref dict stream) is a different schema. **FOLLOW-ON (named, fidelity-risky): re-port `resolve_doc` onto the live stream → measure impact → flip if net-positive (no-more-default-off).** This converts the board-instrument proxy win into a LIVE-reader gain.

### 📌 OWNER NOTE (2026-09-07): NO MORE PROBLEMS TO ASSIGN
- The fleet problem queue is DRAINING (3 SOLVEDs in flight await owner review; nothing left to hand out). Center of gravity → (a) main-thread REALIZATION of latent wins (this organ done; queue: parser decode=exact flip + obl/spatial defer consumer 0.758→0.902; SEM segmenter live consumer) + (b) the pri-1 GENERATIVE WORLD-MODEL. Plan gate: once the 3 in-flight extraction SOLVEDs land → RE-RUN the end-to-end reasoning board.

---

## 2026-09-07 (CONT-21) — COMMON-NOUN COREF now BEATS string-identity (a strategy-filed brief, solved) + 3 wins made BOARD-VISIBLE + the GENERATIVE WORLD-MODEL filed as the main event

Owner-DONE + realization work, reverified first-hand, landed additive/no-regress, committed path-limited (NOTHING pushed). **Mechanism detail = audit §2b (newest).**

### improve_the_common_noun_coref_candidate_quality_the_resolver_trails_string_identity  [INTEGRATED; EXCELLENT; reverified 6 suites / 28 checks]  *(a brief STRATEGY filed from the p11 coref flip-gate finding — the fleet solved it; the located leverage is now CLOSED)*
- **THE WIN:** the board's `common_noun_coref` dim FLIPS from a located-negative to a WIN — **0.5671 vs same-head string-identity 0.5412 (+0.0259 CI-sep)**, +0.0792 over the incumbent 0.4879, twin loses, pronoun/kb byte-identical, name +0.024. 3 brain-faithful levers each unearthed by diagnosing WHY the naive fix failed: (1) TYPED CARD IDENTITY (nominal vs full card — ~47%-accurate pronoun binding was polluting the shared DRT card; +0.0439, pronoun byte-identical); (2) NON-WRITING (Nref) different-head bridge (resolve for THIS reference, don't commit the merge — the WRITE crashes the same-head chain 0.769→0.694, always-hold REQUIRED); (3) seed from the landed `hdlab.typed_spokes.coref_type_license`.
- **VINDICATES the p11 coref flip-gate:** the SAME typed_spokes organ that was net-NEGATIVE as a WRITING FILTER on the live pick is net-POSITIVE here as a NON-WRITING candidate SEED — the write was the cost, confirmed both directions.
- **LANDED (board-visible, not latent):** the 4 levers wired into the URG `Resolver` (`exp_unified_referent_gum_v1.py`, the board instrument) as opt-in flags (default OFF byte-identical) + the board common-row (`exp_board_coref_gum_v1.py`) calls them ON; memoized comparator ~7x. A BOARD-INSTRUMENT (URG proxy) landing, NOT the live `commonnoun_binder` path. Both files untracked-but-not-ignored → plain git add.
- **HONEST SCOPE:** a per-mention RESOLUTION-accuracy win (board's metric + downstream binding), NOT cluster-F1; the different-head residual is 84% world-knowledge → the sibling P31 entity-type-KB (filed pri 8) is the ONLY remaining accuracy lever (ranking is headroom-free). **FOLLOW-ONS: promote the typed-coref path to a hdlab organ (dedup + live-reader propagation); eval other card consumers on the nominal view; P31 KB.**

### 3 PROVEN WINS made BOARD-VISIBLE (realization — the plan's "show the wins" prep)
- **natural-logic monotonicity** arm 0.767 vs 0.535/0.503 CI-sep (+ live sm.natural_logic/entails query) — `870cf57ed`.
- **event↔goal congruence** arm 0.9750 vs 0.4917 (+0.4833 CI-sep, twin loses) — `21515fce7`.
- **SEM segmentation** arm — the loop-closure north-star: SEM rho 0.1235 vs incumbent 0.0668 vs ACTUAL humans, 55.5% of the noise ceiling, ≥ GPT-2 (honest point estimate, no CI) — `56f55e084`.

### FILED — the MAIN EVENT (owner-endorsed 2026-09-07)
- `build_the_generative_result_state_world_model` (PRIORITY 1, cert 8/8) — the generative result-state world-model / recurrent top-down loop that 5 SOLVEDs named as the shared completing lever. First slice = SDRT full non-adjacent TellMeWhy; bar = subset-win → full-population CI-sep win. **STOP filing it as a follow-on; it is now the #1 problem.** [[reasoning-machinery-shown-not-end-to-end-build-the-generative-world-model]]

---

## 2026-09-07 (CONT-20) — p7 (SDRT coherence reader) + p6 (eventive-NOMINAL channel → default-ON + script wire) INTEGRATED + p11 natural-logic REALIZED + 1 follow-on FILED

All owner-DONE / realization work, reverified first-hand, landed ADDITIVE/byte-identical, committed path-limited (NOTHING pushed). **Mechanism detail = audit §2b (newest entries).**

### p7 — sdrt_discourse_coherence_reader_for_temporal_order_and_unmarked_causal_inference_on_real_prose  [INTEGRATED; EXCELLENT (honestly-bounded); reverified 14/14]  *(one of the shared-extraction-wall problems I filed — infer unmarked discourse relations to feed the temporal + causal reasoners)*
- **THE DRILL (the owner push turned a located negative into a win):** the SDRT-lite reader infers Narration/Result/Explanation from directed causal-plausibility (Hobbs abductive coherence), NOT connectives. The first pass only TIED the real-prose floors — because the physics+psych simulator was BLIND to GOAL/INTENTIONAL causation, the DOMINANT narrative causal category (35.9% of TellMeWhy non-adjacent), even though the substrate already had the landed goal_register desire/intention lexicon it never composed. Adding a GOAL-causation engine (Malle reason-vs-cause; Trabasso goal chains) is the fidelity gap crossed.
- **MEASURED (first-hand): MECHANISM (constructed minimal pairs n=48): relation-4way 0.8125 vs connective-only 0.25 (+0.5625) + twin 0.29 (LOSES); order-edit 0.875 vs iconicity 0.5. THE REAL-PROSE POSITIVE (GOAL-typed subset n=92): goal-engine 0.5326 CI-sep over base 0.2935 (+0.239), topical 0.239, twin 0.272 (twin LOSES).** Coupling: causal graded_necessity over the inferred edges reproduces the cause-ID.
- **LANDED DEFAULT-OFF (additive, byte-identical off):** `hdlab/coherence_reader.py` (new organ, physics+psych+GOAL engines, no experiments dep) + `temporal_reasoner.before(use_coherence=False)` (Explanation reverses iconicity on the fallback branch only; narrated 10/10 byte-identical; p6's script branch untouched) + `situation_reader.sm.inferred_coherence_links` + `track_coherence` flag DEFAULT-OFF (sm.causal_links BYTE-IDENTICAL).
- **THE DEFAULT-OFF MEASURED REASON:** the goal engine wins its 36% category but the FULL non-adjacent population is +0.016 (NOT CI-sep — over-fires on the 40% non-goal subset); the completing lever is unbuilt → off until it lands, no full-pop lift claimed.
- **HONEST LOCATED NEGATIVES:** TB-Dense temporal-override (newswire reverse-order is genre, not flashback); Kintsch settling under uniform inhibition = a proven no-op; scalar means-end = wrong axis; typed-edge means-end coverage-bound (34%). THE WALL, from ~6-8 angles: every retrieval/structural lever is coverage-bound; the ONE full-population lever is a **GENERATIVE world-model** (simulate action→result-state, check the goal-state).
- **PEAK:** goal-causation subset 0.53 (real-prose, twin loses); the mechanism is proven, coupled to both reasoners no-regress. **FOLLOW-ON (ELEVATED per owner 2026-09-07): the generative result-state world-model — the recurring P1 five solvers have named; make it the MAIN EVENT, not a follow-on. Plus a narrative flashback temporal gold + GUM-eRST relation validation.**

### p6 — gate_the_eventive_nominal_event_channel_by_context_wsd_event_vs_result  [INTEGRATED; EXCELLENT (REFUTED-and-resolved); reverified W1-W4 + landing witnesses]  *(the follow-on I filed for p2's eventive-nominal precision hold-off — the answer is: don't gate, flip it on)*
- **THE REFUTATION (headline):** the WSD gate the brief asked for is NET-NEGATIVE downstream. The gate restores EXTRACTION precision CI-sep (TB-Dense 0.6517→0.7142, twins losing) but through the ACTUAL temporal reasoner it LOSES — answered-correct ungated **0.4937 (87% of the gold ceiling)** vs WSD-gated 0.4590 CI-sep WORSE; conditional accuracy FLAT → gating only drops coverage (the reasoner is robust to over-extraction, confirming p2). The precision "cost" is an EXTRACTION-INSTRUMENT ARTIFACT (gold doesn't annotate every eventive nominal).
- **WHY (drilled): event-vs-object sigs are cos 0.93 COLLINEAR in w2v (grounded 0.51) — but the grounded swap is REFUTED at power; event-vs-result is an ARGUMENT-STRUCTURE distinction, not lexical in ANY representation; all 4 brain cues (biased-competition / grounded / derived selectional-pref / frequency-dominance) land on ONE frontier → the residual is structural (~9-19% trigger-vs-named-reference) + the instrument, not a missing cue.**
- **§4i SELF-CORRECTION (owned):** overstated a "35-point reasoner gap" (invented a 90% human number + a weakened reasoner); the landed temporal_reasoner is at 0.6225 = the top of the 59-64% human agreement noise ceiling → essentially AT the achievable ceiling on narrated ordering (do NOT reopen).
- **LANDED (verified first-hand): (1) `joint_nominal_events` DEFAULT-ON UNGATED** — the load-bearing win, realizing p2's biggest held-off lever (nominal recall 0.105→0.703, survival to 0.73). Flip-on MEASURED byte-safe: sm.events + every board field BYTE-IDENTICAL ON-vs-OFF (the +48/+54 nominal events only enrich the latent `sm.temporal_reasoner()`, the sole consumer — no board dim reads it today); `all_capabilities_off` forces False. **(2) `temporal_reasoner.before()` wires the latent `temporal_script_schema`** (177,800 verb-pair orders) on the IMPLICIT-EVENT/UNKNOWN branch only (evidence≥10, |p−0.5|≥0.1) — narrated path BYTE-IDENTICAL, asset-absent abstains; converts UNKNOWNs into 0.60-on-TRACIE answers.
- **DEFERRED correctly:** the gate organ NOT promoted (a located negative with no consumer; promoting default-off = the latent-island anti-pattern — kept in experiments/ for a future precision-over-coverage consumer).
- **PRE-EXISTING FAILURES (proven p6-independent → maintenance follow-on):** `test_situation_model_qa.py::test_temporal_survives_the_keystone` (stale vs `preserve_tense` default-on since 2026-09-03) + `::test_causal_..._loses_to_adjacency` (causal 0.76>0.51 now) — re-baseline vs the current capable defaults.
- **PEAK:** the eventive-nominal channel is a safe default-ON gain as-is (the biggest temporal-survival lever, now live); narrated temporal ordering is at the human noise ceiling; the implicit-event path is newly answerable via the script organ. **FOLLOW-ONS: the broad causal/event-order store (FILED, pri 12); a well-powered selectional-preference organ; the trigger-vs-named-reference discourse organ; the 2 stale-witness re-baseline.**

### p11 NATURAL-LOGIC monotonicity reasoner — REALIZED (the CONT-18 follow-on (ii), now live+scored)
- Made the already-integrated typed-spokes natural-logic result board-visible + queryable: `board_natural_logic_monotonicity` arm (behind run_new_arms, OFF in self-test) — model **0.767** vs symmetric-cosine oracle 0.535 (+0.232 CI-sep) + majority 0.503 (+0.264 CI-sep), shuffled-monotonicity twin 0.540 loses, coverage 0.855; + live `sm.natural_logic(s1,s2)`/`sm.entails(s1,s2)` (track_natural_logic default-on lazy, byte-identical off). Reverify 22/22, byte-identical. Committed `870cf57ed`.

### FOLLOW-ON FILED (owner-requested, overlap-confirmed DISTINCT)
- `grow_a_broad_causal_event_order_knowledge_store_for_the_implicit_event_path` (pri 12, cert 8/8) — grow the temporal_script_schema verb-pair-ORDER store's coverage (29%→broader, irreversibility/entropy-typed) + keep it wired to the implicit-event path; scoped INSIDE the knowledge-foundation north star. An 8-candidate overlap sweep confirmed it is not subsumed (umbrella = the consolidation-gate foundation; parent = the temporal reasoner that built the seed; fenced vs the predictive forward-simulator + the SDRT reader).

---

## 2026-09-07 (CONT-19) — THE GLOBALLY-NORMALIZED GRADED PARSER (the Matrix-Tree north-star) INTEGRATED + 3 high-priority briefs FILED

Owner-DONE, reverified first-hand (5 core witnesses + full 14-chain green), landed ADDITIVE/opt-in (default byte-identical, heads unchanged 200/200), committed path-limited (NOTHING pushed). The measured wins are wired-AVAILABLE; the live flips are deferred to strategy with measured reasons (see below). **Full mechanism = audit §2b (newest entry).**

### parser — replace_the_greedy_arc_eager_with_a_globally_normalized_graded_parser_with_marginals  [INTEGRATED; EXCELLENT; reverified 5 core + 14-chain]  *(the deepest EXTRACTION upstream — caps patient, who-did-what, and every reasoner's edge extraction)*
- **THE REFRAME (the enabling insight):** the landed arc-factored scorer was ALREADY globally-normalized (tree score = SUM of arc potentials, no label bias) — the deviation was the greedy DECODE (invalid non-tree parse on 7.2% of sents) + no distribution/2nd-best. Built the exact posterior over it: **Chu-Liu/Edmonds MAP + single-root Matrix-Tree edge MARGINALS + exact 2nd-best, all BIT-EXACT vs brute-force (n≤4).** The marginal sums over ALL trees → gold in-support 1.000 (vs the arc-eager beam 0.399-0.492) → the search failure is STRUCTURALLY eliminated.
- **MEASURED (modern UD-EWT, first-hand): (2a)** exact MST > greedy UAS 0.7888→0.7908 (+0.0020 CI-sep); **(2b)** RAW marginal AUC 0.855 all-arc / 0.765 patient vs greedy raw conf 0.613 (a single glass-box signal ≈ the landed calibrated logistic 0.860); **(3)** deferring the live patient pick lifts selective 0.8785→0.9465 CI-sep; **(4)** shuffled-marginal twin FLAT; **(5)** additive marginal changes NO head (byte-identical), positive control = 149 invalid-tree sents where MST recovers gold +0.0184 CI-sep.
- **THE BREAKTHROUGH — an ABSOLUTE who-did-what gain (the owner's push converted a located-negative into a win):** the marginal wired into ROLE SELECTION via a Competition-Model ensemble (labeled-reader ANCHOR + marginal + arg-vs-adjunct typing) BEATS the strong labeled reader CI-sep **0.8785→0.8850 (+0.0065 CI[+0.0008,+0.0121])** — the FIRST absolute lift in this arc, ~7% of the gap to the 0.913 ceiling. The incremental lever is the MARGINAL (validity 1.20); arg/adjunct typing is load-bearing STANDALONE (shuffle-twin 0.85→0.55) but incrementally redundant with the labeled reader.
- **UPGRADES (all off the ONE marginal):** Hale parse ENTROPY (Spearman 0.70, defer +0.058 — the greedy 1-best gives 0); UNIVERSAL per-label reliability (median AUC 0.825/29 labels); single-root grammar-faithful; OOD-robust (QA-SRL AUC 0.55, twin at chance); **the OBL/spatial reader is where it EXCELS — RAW marginal AUC 0.782 BEATS the whole landed obl calibrator 0.736, obl selective 0.758→0.902, +0.048 AUC (6× the patient's).**
- **LANDED (additive/opt-in, DEFAULT byte-identical, verified first-hand):** `hdlab/graded_parser.py` (self-test brute-force EXACT) + `ParseResult.marginals` on arc_parser (opt-in `want_marginals`/`decode="exact"`) + `parse_confidence.obl_reliability_marginal` (raw marginal, logistic dropped) + patient `a2_marg` + `predicate_argument_frontend.structural_patient_pick(marginals=)` via a frozen Competition-Model ranker asset (gitignored, `git add -f`), anchored on the labeled pick. EFFICIENCY: one marginal reused; 2nd-best TREE dropped from the read path; single-root shipped (free).
- **LOCATED NEGATIVE (a full pass, PROVEN):** the 2nd-best does NOT raise ABSOLUTE patient toward 0.913 — the arc-factored SCORER under-separates the object site (gold exposed on 17/150 wrong picks; oracle headroom +0.012). Decomposed: 67% two-valid (→ grounded event-knowledge, wall-map Wall-2), 22% rankable-ungatable-without-meaning, 11% scorer-buried. The wall is the SCORER's FEATURES (surface-only), NOT the normalization/decode (both solved). Both residual organs prototyped (valency + document-Centering) recover ~0 on naturalistic gold → the who-did-what residual is substantially the irreducible world-knowledge/full-coref slice (a correction to the wall-map's "close the loop" optimism).
- **FLIP DECISIONS (deferred to strategy, measured, no-more-default-off): (i)** obl marginal net-positive but **NO live obl consumer** → dormant-available (realize by building an obl/spatial defer consumer → cashes 0.758→0.902, the biggest win); **(ii)** patient role-selection +0.0065 CI-sep but costs a **SECOND (arc-factored) parse per read sentence** (reader parses once, arc-eager) + the board under-measures who_did_what_patient → default-off pending a cost/board call; **(iii)** `decode="exact"` strict-improvement (eliminates 7% invalid parses, +0.003 UAS, no label regress) default greedy pending the live-board 8-consumer no-regress check. **PEAK:** marginal reliability AUC 0.765-0.855; obl selective 0.902; absolute who-did-what 0.8850; the parser is now brain-foundational in STRUCTURE + DECODE + posterior — the residual is the SCORER's features + the absent top-down loop, both distinct organs.
- **FOLLOW-ON PROBLEMS (file, don't re-scope):** parse-miss SELECTION (k-best forest + joint frame unification; the marginal EXPOSES 77.5%); the reversible/Winograd minority (grounded event-knowledge + full-coref — the wall-map's recurrent-loop residual).

### 3 HIGH-PRIORITY BRIEFS FILED (cert 8/8, ready to assign when a solver frees)
- **p2** `extract_spatial_and_causal_relations_from_prose_whole_subgraph_survival` — extend the p2 joint front-end to spatial (RCC8/Figure-Ground) + causal edges (the shared extraction wall's remaining reasoners; spatial edge recall 0.22/0.06/0.02, causal 3.2% ≥2-hop).
- **p4** `improve_the_common_noun_coref_candidate_quality_the_resolver_trails_string_identity` — the located coref leverage (live URG 0.4879 < string_identity 0.5412); candidate-quality not world-knowledge (the type-license was measured net-negative — barred from re-file).
- **p8** `acquire_wikidata_p31_entity_type_kb_for_name_bridge_coref` — the TYPE-2 entity-type spoke for name-bridge coref (~10%, WordNet can't reach); reuse `hdlab/typed_spokes.py`.

---

## 2026-09-07 (CONT-18) — p11 (the WORLD-KNOWLEDGE TYPED-SPOKE foundation) INTEGRATED

Owner-DONE, reverified first-hand (22/22 + 8/8 parity), landed ADDITIVE/byte-identical (C1 untouched), committed path-limited (NOTHING pushed). LATENT by design (no consumer wired this pass → the measured flip-gates are the realization). Read from the SOLVED IN FULL. **Full mechanism detail = audit §2b (the newest entry).**

### p11 — expand_the_clean_semantic_memory_foundation_with_world_knowledge_via_the_consolidation_gate  [INTEGRATED; EXCELLENT; reverified 22/22 + parity 8/8]  *(the north-star KNOWLEDGE-FOUNDATION program — realizes the "clean/typed/correctly-resolved, not more volume" thesis)*
- **THE THESIS (brain-foundational):** the frozen meaning store SUPERPOSES all of a synset's knowledge into ONE dense 200-d signature (superposition ceiling: sibling cos 0.932, effective rank 17.4/200); a SYMMETRIC cosine over it CANNOT represent a DIRECTED or TYPED relation. So the knowledge that IS already there (WordNet is-a, ConceptNet relations in the C1 bag) is UNREADABLE in typed/directed form. The brain's fix is the ATL hub-and-TYPED-SPOKE organization (Lambon-Ralph, PINNED) — admit world-knowledge relations OFFLINE through the gate as TYPED, DIRECTED, sense-resolved SPOKES (the phase-diagram move superposed-dense → indexed-typed; the manifest already DECLARES hub-and-spoke but only C1's superposition was read).
- **5 RELATION FAMILIES, each on ITS OWN consumer's MODERN instrument, every info-free twin LOSING CI-sep:**
  1. **is-a (TYPE 3, directed):** MoNLI 0.817 vs symmetric floor 0.500 (+0.317 CI-sep; symmetric ANALYTICALLY capped at chance — each pair appears both directions, opposite labels); MED 2nd gold 0.760 vs 0.563. **NON-CIRCULAR downstream win: GUM common-noun COREFERENCE +0.0116 CI-sep over recency/Centering** (the spoke as a type-licensing FILTER, not selector; shuffled-filter twin loses; INDEPENDENT of WordNet — answers the circularity worry). Freq-generality heuristic 0.865 competitive OVERALL but WRONG on the 226 freq-wrong pairs (typed 0.766/0.982 vs freq 0.000 → the graph is the mechanism, non-circular).
  2. **part-whole + instrument (TYPES 4+6, directed) on the LIVE bridging instrument:** covered 0.926/0.828 vs the symmetric read 0.095/0.027 (FOOLED below chance — the confusable distractors ARE the target's generic neighbours). **GENERALIZATION LOCATED NEGATIVE (a full pass): held-out 0.272/0.203** — meronymy has NO transitive closure → stored-typed authoritative-for-covered + distributional-for-novel HYBRID is the brain-faithful design (nearest-exemplar transfer BUILT + TESTED, 0.216 < 0.272, DILUTES; literature-confirmed partially-pinned limit — Winston-Chaffin-Herrmann/Tversky-Hemenway).
  3. **antonymy (TYPE 5, the 5th spoke):** the symmetric signature is ANTI-PREDICTIVE on opposition (AUC 0.391 — antonyms MORE similar than synonyms, co-occur in same contexts); a typed antonym spoke is REQUIRED for opposition-aware valence (symmetric-cosine oracle 0.547 ≈ chance; typed 1.000 by construction → the load-bearing result is the SYMMETRIC FAILURE + twin at chance).
  4. **event-order (TYPE 9):** ALREADY a landed organ (`hdlab.generalized_event_knowledge`, Story Cloze 0.582 CI-sep) — a 4th directed-spoke that independently CORROBORATES the architecture. DO NOT rebuild.
  5. **NATURAL LOGIC over the is-a spoke:** MED 0.767 vs majority 0.503 + symmetric-cosine oracle 0.535 (both CI-sep), **85% coverage** (up from the 15% single-substitution slice), shuffled-monotonicity twin 0.540 loses (polarity load-bearing). The parse-free sentence-level closed-class monotonicity marker IS the brain's FAST operator-recognition register.
- **STORE-ORGANIZATION PRINCIPLE (a design upgrade earned by drilling the gate wall):** is-a (C5) = a TRANSITIVE-CLOSURE store — admit by provenance+resolution, reject-then-close (schema-margin AUC 0.942 REJECTS wrong edges BEFORE closure; NEVER edge-filter a closure — pruning a clean edge disconnects ancestors, measured 0.52); part-whole (C6) = NON-closure lookup — edge-filter freely. The gate's ROLE is store-type-dependent; calibrate to the consumer (specific-pair consumers ROBUST to scattered noise, aggregating consumers not).
- **100% BRAIN-FOUNDATIONAL (this + upstream):** the solver REMOVED the one violation — spaCy demoted to a NON-ADMISSIBLE mechanism-ceiling reference (0.810); six positional/parse scope methods drilled (linear 0.514 / shallow 0.669 / NP-restrictor 0.672 / reader's-own-parse 0.505 / parse-repair 0.651 / spaCy 0.810-barred) ALL lose to the parse-free marker → research VERDICT C: the brain uses a DUAL REGISTER (fast operator-recognition ≈ our 0.767 near-human + a bounded slow structure-building register), so 0.767 is brain-faithful NOT a defect — neither a general-parser-upgrade problem (A) nor a hard ceiling (B).
- **DISK-OUTRANKS-BRIEF finding (kept honest):** the gate's "raw admission regresses at the consumer" guard does NOT hold for is-a→MoNLI (the specific-pair consumer is ROBUST: clean 0.8174 ≈ raw-noisy 0.8180); the gate's value is EDGE-LEVEL (AUC 0.94), not consumer-level → the load-bearing is-a control is the shuffled-graph twin, not a raw-noisy regression. The resolution guard (raw lemma-string over-generates cross-sense is-a on 100% of polysemous nouns) is real but costs ~0 on MoNLI's low-polysemy pairs (union 0.996 ≥ MFS 0.817).
- **LANDED (additive/byte-identical, reverified first-hand):** `hdlab/typed_spokes.py` (synset-keyed, C5/C6/C7, parity 0-disagreement vs the cells) + `knowledge_foundation_manifest.json` C5/C6/C7 (gitignored on-disk) + 2 .npz assets (gitignored, `--build` in 6.7s). C1 byte-UNCHANGED (SHA identical), `test_knowledge_factory_meaning_store` 6/6, diagnostic_context_wsd untouched.
- **RELATIONSHIP TO p4 `structured_matcher` (just landed):** overlapping edge families (is-a/part-of/antonym) but GENUINELY DISTINCT — typed_spokes = SYNSET-keyed directed-CLOSURE + natural-logic + graded holonym-prototype for the meaning FOUNDATION; structured_matcher = a LEXICAL-HEAD sign-matcher that abstains-to-hub for goal/type consumers. Keep the APIs distinct; the only worthwhile future consolidation = a shared raw-KB-edge accessor if a 3rd consumer appears (deferred, NOT forced).
- **DURABILITY CAVEAT (pre-existing infra, flagged to owner):** the manifest + assets live in GITIGNORED data/frontend_assets/ (as ALL spokes already do) → a fresh checkout rebuilds assets via `--build` but the C5/C6/C7 manifest registrations are on-disk-only (not version-controlled); the ORGAN loads its assets directly so it functions without the manifest.
- **FLIP-GATE OUTCOME (2026-09-07) — the COREF type-licensing filter is a LOCATED NEGATIVE on the LIVE instrument (measured against the live baseline, NOT the SOLVED's recency floor — the p12 lesson held):** wired C5+C6 as a binary type-license into the live common-noun coref behind default-off `commonnoun_type_license`/`isa_type_license` (byte-identical when off) + a `board_commonnoun_typelicense_dimension` arm. On the board's LIVE common_noun_coref (URG.Resolver, baseline **0.4879** — not the SOLVED's 0.6883 recency floor): filter ON **0.4683 (−0.0196 CI-sep REGRESSES)**; shuffled-graph twin 0.4644 **INDISTINGUISHABLE** (model−twin +0.0039 CI incl 0 → NOISE on this instrument, not knowledge); also regresses the shared-pool pronoun dim −0.0172 CI-sep (different-head merges pollute the DRT referent cards). MECHANISM: the different-head links are only 37% correct on this population + the URG resolver MERGES them → pollution; the SOLVED's +0.0116 was instrument-specific (a NON-merging recency tracker whose baseline already reached different-head). → **KEEP DEFAULT-OFF (a full-pass located negative); committed default-off + the board arm as reproducible evidence; do NOT flip.** (Caveat: measured on the URG board proxy; the actual live `commonnoun_binder` path has no on-disk GUM arm — the mechanism is instrument-general, same negative predicted.)
- **STANDING FINDING (valuable, from the flip-gate) → candidate follow-on problems:** the board's `common_noun_coref` dim is ITSELF a LOCATED NEGATIVE — the live URG pick 0.4879 trails its own simplest floor (string_identity 0.5412) by −0.079 → the real leverage is **URG CANDIDATE-QUALITY (a resolver problem), NOT world-knowledge**; + the name-bridge slice (common→PROPER-NAME, ~10%) needs a Wikidata P31 entity-type KB (SOLVED P2). The coref type-license becomes worth revisiting only AFTER the URG candidate-quality is fixed (it's blocked on that upstream, not a dead end).
- **FOLLOW-ONS (remaining flip-gates + new types):** (i) ~~coref filter~~ → located-negative above (blocked on URG candidate-quality); (ii) **NATURAL-LOGIC monotonicity reasoner** (a NEW consumer — C5 + the parse-free marker, 0.767/85%; needs a reader method + a board arm); (iii) **BRIDGING C6 hybrid** (typed-where-covered, distributional fallback); (iv) **AFFECT C7 antonym** for opposition-aware valence. NEW TYPES needing a KB acquisition: TYPE 2 entity instance-of (Wikidata P31 → name-bridge coref, ~10% unreachable by WordNet); TYPE 10 thematic-fit (UD-EWT/QA-SRL); the bounded restrictor-attachment slow register (VerbNet PP-subcat) for natural-logic 0.767→~0.84. **PEAK:** is-a 0.817 / natural-logic 0.767 near-human / part-whole 0.926 covered; the architecture (typed directed spokes) is the proven lever, corroborated 4× (is-a, part-whole, antonym, the already-landed event-order spoke).

---

## 2026-09-07 (CONT-17) — p1 (PREDICTIVE-CODING LOOP CLOSURE, the north-star) + p4 (structured semantic-matching) INTEGRATED

Both owner-DONE (`owner_verdict: DONE`), reverified first-hand, landed BYTE-IDENTICAL/no-regress, committed path-limited (NOTHING pushed). Both new capabilities are **LATENT by design** (byte-identical live reader) → the realization (flip-on gate / consumer wiring) is the immediate follow-through. Read from each SOLVED IN FULL (owner "miss nothing").

### p1 — close_the_recurrent_predictive_coding_loop_n400_error_against_the_forward_prediction  [INTEGRATED; EXCELLENT; reverified 9/9 core + W10 vs ACTUAL humans]  *(the wall-map's #1 north-star — the recurrent top-down loop the whole substrate was missing)*
- **COHERENCE loop-closure PASSES (the literal brief deliverable):** forward prediction-error beats the backward gist on Story Cloze val+test (n=3742) 0.5874 vs 0.52, **+0.0673 [+0.047,+0.088] CI-sep**; cross-context info-free twin collapses to 0.497 (clause 3 met). First-hand W1: fwd 0.5875 > bwd 0.5350 CI-sep.
- **SEGMENTATION — a deep corrected result (located negative → past it to a human-validated organ):** content-only forward error TIES the backward gist on modern prose (GUM paragraphs AUC ~0.55 each — the brief's pre-registered located negative); the win needs the MULTI-DIMENSIONAL (Zwaan) forward — the PROTAGONIST/entity dimension is the lever (gold coref AUC 0.551; the reader's own PARSE-LAYER PROPN+NOUN novelty 0.567 EXCEEDS gold coref, live-realizable, beats the content-backward incumbent CI-sep on all-genre +0.031). Construction control (concat ROC): fwd F1 0.80 vs bwd 0.27 (3× — the mechanism HAS the signal on genuine situation-changes; the GUM tie is the weakness of paragraph typography as a boundary, not the monitor).
- **THE HEADLINE FINDING (validated vs ACTUAL HUMAN perceived boundaries — Kumar 2023 Zenodo button-press gold, acquired+aligned):** the brain segments by SCHEMA-SWITCH / belief-update (Franklin-Gershman SEM; Kumar 2023 Bayesian surprise = KL of the forward DISTRIBUTION), **NOT prediction error** — which is exactly what the incumbent `n400_coherence_monitor` computes (3 exact gaps: prediction-error not distribution-shift; a POINT not a distribution; a FLAT gist not a schema library). Every flat content monitor is NEAR CHANCE vs humans; the SEM schema-switch organ WINS: AUC Tunnel 0.6486 / Pieman 0.599 vs point-error incumbent 0.531; rho 0.12-0.15 ABOVE incumbent 0.07 AND GPT-2 0.10-0.12; cross-story + default-param robust; WORSE on the GUM proxy (0.48) → genuinely human-aligned, not proxy-aligned.
- **THE PROXY MISLED US (the deepest lesson):** a precision-weighted Bayesian-surprise refinement that BEAT the incumbent on GUM paragraphs (+0.036 CI-sep) LOST to it vs actual humans (−0.035). A "brain-foundational" win on a convenient modern gold can be a proxy artifact; only the brain's own behaviour adjudicates.
- **NOISE-CEILING RETRACTION (owner caught the error):** "needs a neural model" is RETRACTED — LOO human ceiling rho 0.22 (humans barely agree); glass-box SEM rho 0.15 = 68% of it, ABOVE GPT-2's 0.10-0.12; content-shift/SEM/GPT-2 all plateau at ~0.12-0.15 = the text-predictable ceiling; the residual is irreducible-from-text individual variation, NOT a model-capacity gap. GENERALIZES: compute the noise ceiling before calling a result a failure — the number to beat is the ceiling, not 1.0.
- **RIGOROUS NEGATIVES (SHELVED, cause named):** online-ensemble of the two forward predictors (oracle-weight = best-single → NOT complementary, the ensemble is the wrong abstraction); online Rao-Ballard forward model (marginal — one story is too little to learn a 200×200 map); diverse offline KL distribution (below chance despite 0.91-0.98 coverage — a glass-box co-occurrence distribution can't do KL, needs a neural forward model); broadening the GEK forward store (domain-specialization TRADE — in-domain prose lifts segmentation +0.018 but regresses coherence −0.033). 5 glass-box levers (linear/episodic/structured-scene/diverse-KL/online-PC) ALL cap ~0.62 → the SCHEMA-SWITCH ARCHITECTURE carries the win, not the forward-model details.
- **§4c POLICY ISOLATION (a partial correction to the brief):** "forward beats backward" is REGIME-SPECIFIC (in-domain store + short segments); the backward gist is itself a zeroth-order (persistence) forward predictor — so the real question is WHICH forward predictor, and on OOD real prose the persistence one wins; reinstatement is a direction-INDEPENDENT lever (sweep, don't adopt 0.3).
- **DISK CORRECTION (folded to §2b): `n400_coherence_monitor` is NOT an unwired island** (the brief's premise) — it is DEFAULT-ON via `bound_event_backbone` (`bind_event_tokens=True`, situation_reader:811,3173) + `hippocampal_encoder`, so redirecting it in place is NOT free → the safe landing is opt-in modes (default byte-identical).
- **LANDED (byte-identical/no-regress, reverified first-hand): (1) NEW ISLAND `hdlab/sem_event_segmenter.py`** (SEMEventSegmenter observe()/segment(); schema library + sticky-CRP/Gaussian-MAP + boundary = MAP schema-switch; self-test PASS + embedded human-validation reproduces; sigma2 = documented phase-diagram knob, default 1.0 hub-scale, a mismatch SILENTLY stops switching). NO consumer → no-regress by construction. **(2) opt-in `forward_expect_fn` + swept `reinstate:float=0.0` on `hdlab/n400_coherence_monitor.py`** — DEFAULT byte-identical (SHA-256 of `N400CoherenceMonitor()` output IDENTICAL before/after → the default-on `bound_event_backbone` chunking is unchanged).
- **LATENT (by design) → FOLLOW-ONS:** the SEM organ has NO live consumer (a new island — moves NO board dim today, a landed-not-live island); realize by (a) a real reader consumer feeding per-proposition scene vectors (the substrate's FHRR-bound {AGENT,PRED,PATIENT} scene = SEM's HRR) + a board segmentation arm; (b) revisit whether `bound_event_backbone` should chunk with the SEM organ (measured on its episodic-store metric); (c) a consumer that calls `forward_expect_fn` to cash the +0.067 coherence win; (d) the scale-free online inverse-chi² sigma2 variant (§4o, rho 0.14-0.17, needs robustness); (e) the multi-dimensional forward projector over all 5 Zwaan indices (= the shared extraction wall the reasoners converged on). **PEAK:** coherence +0.067 CI-sep (realizable); segmentation SEM at the human-agreement ceiling (0.62 vs 0.53, above GPT-2).

### p4 — structured_semantic_matching_for_event_goal_and_type_converse_role_filler_knowledge_not_the_hub  [INTEGRATED; EXCELLENT; reverified suite 3/3+3/3+4/4+3/3, self-test PASS, parity 0-mismatch]  *(the brain's two-store split made a scored organ — the polarity-blindness of the ATL hub named in the p3 OCC SOLVED, now COMPLEMENTED)*
- **HEADLINE (LANDED, additive):** ONE glass-box structured relation-SIGN organ (`hdlab/structured_matcher.py`) complements the POLARITY-BLIND ATL distributional hub (bridging_inference): the hub gives GRADED relatedness (rel(win,lose)=0.259 ≈ rel(sell,buy)=0.285) but is BLIND to SIGN/TYPE. The organ reads the sign off a STRUCTURED store (WordNet antonymy/hypernymy/meronymy + FrameNet Perspective_on converse [1091 pairs, resource-extracted] + ConceptNet IsA/AtLocation/PartOf + the taxonomic-sibling NOT-a-kind-of edge) and ABSTAINS to the hub below margin. The brain's two-store split (ATL hub "how related" vs angular-gyrus/TPJ combinatorial "which relation, which direction"; Binder & Desai).
- **MEASURED first-hand (relatedness-MATCHED modern-vocab golds, held out from the seed): EVENT↔GOAL** (n=120): STRUCTURED **0.9750** vs HUB (swept) 0.4917 (+0.4833 CI[0.400,0.575]) vs SHUFFLED-KB TWIN 0.4917 (+0.4833) — CI-sep over BOTH; antonym isolation hub **0.0000** vs struct 0.9500. **TYPE-MEMBERSHIP transfer (spatial)** (n=120): STRUCTURED **0.9167** vs HUB 0.5000 (+0.4167) vs TWIN (+0.4250) — CI-sep; cohyponym-not-isa hub **0.0000** vs struct 0.8333. Hub relatedness MATCHED across slices → the sign is the EDGE not the magnitude; positive control the hub cannot get: win/lose ≈ sell/buy related, opposite structured sign.
- **KEY CORRECTNESS MOVE:** CONVERSE (role-swap sell/buy) SATISFIES a goal; ANTONYM (polarity-flip win/lose) THWARTS it — checked converse-BEFORE-antonym (WordNet lists buy as an antonym of sell). And the type NEGATIVE is taxonomic SIBLINGHOOD (co-hyponym signed −1), not absence-of-is-a (WordNet incompleteness) — lifted the negative slice 0.28→0.83.
- **FIDELITY DEEPENING KEPT (`use_valence=True`, unioned into antonym()):** the antonym sign is a signed DIMENSION not a symbolic edge (Osgood/vmPFC evaluative axis) — high relatedness + OPPOSITE Warriner valence sign (via hdlab/affect_lexicon). GENERALIZES: recovers the thwart sign on 100% of high-related opposite-valence pairs WordNet's list MISSES (symbolic 0.000 there); agrees with only ~half of WordNet antonyms (antonymy is multi-dimensional; valence is the goal-relevant evaluative one); the UNION covers both sets 1.000 — fidelity + coverage moved together (the project thesis).
- **SCENE-TAIL RESIDUAL — a rigorous LOCATED NEGATIVE + a proven direction:** the open-ended tail ("stood on the podium"=won). (a) ConceptNet script-bridge + hub NON-discriminative under a negative control (matched 32/50 ≈ shuffled-goal 29/50; hub 2/50) → a static causal KB and cosine BOTH fail. (b) The FAITHFUL mechanism — EPISODIC experiential SIMULATION (Barsalou/Minerva-2; learn P(scene-word|outcome-episode) offline from ROCStories) — DISCRIMINATES: pairwise 0.682 vs shuffled-episode 0.409 (+0.27), top-1 2.6× chance; the FIRST mechanism with signal there. Weak/underpowered (n=22 hand gold; ROCStories coverage) → a WORKING PROTOTYPE + PROVEN DIRECTION, a distinct organ, not a coverage patch.
- **DISK CORRECTION (memory premise was WRONG):** the `converse=True` hook was NOT landed in hdlab (it lived only in `experiments/_occ_upstream_goal_status`, off the live path). The agent BUILT the branch in `hdlab.goal_register.track_status_thwart` (new BRANCH 4, matcher-injected; converse-before-antonym; consults ONLY structured edges, never the hub fuzzy fallback) — byte-identical when matcher is None/abstains.
- **LANDED (byte-identical/no-regress, reverified first-hand):** `hdlab/structured_matcher.py` (new organ, parity 0-mismatch vs reference, incl use_valence) + `hdlab/goal_register.py` (branch 4, optional matcher=None). Live-reader BYTE-IDENTICAL: situation_reader:2660/2840 pass NO matcher → matcher=None → branch skipped; `test_goal_register_landing_organ` 6/6 (ADDITIVE), `test_occ_appraisal_landing` ALL PASS (existing dims byte-identical, L5a STRICT-SUPERSET 0 flips, L5b 0 wants regression). Positive cases proven (sell→bought→satisfied; remember→forgot→failed).
- **WIRED 1 / DEFERRED 3+1 (prefer no-regress):** WIRED = affect (`goal_register.track_status_thwart`, function level). DEFERRED = (i) the affect LIVE FLIP (situation_reader:2660 still passes matcher=None — the natural-prose end-to-end lift is the solver's own WITHDRAW-FIRST, unvalidated → run a board net-positive gate then flip); (ii) SPATIAL (needs a NEW `type_match` call site — `spatial_relational_model` reasons over ISO-Space RCC8, not lexical is-a of head nouns); (iii) TEMPORAL (`temporal_script_schema` answers verb-pair ORDER, a different relation); (iv) BRIDGING part-of (IS the hub — circular). **PEAK:** event↔goal 0.975 / type 0.917 (both +0.42-0.48 over the polarity-blind hub); scene tail = a distinct episodic-simulation organ (direction proven).
- **FLIP-GATE DONE (2026-09-07) — a MEASURED-OFF (wired-not-islanded):** wired a default-off `affect_structured_matcher` flag on `SituationReader` (in CAPABILITY_FLAGS → `all_capabilities_off` forces it off; a lazy `_structured_matcher()` builds one `StructuredMatcher(use_valence=True)` per reader, passed at situation_reader:2660 + :2840 via `functools.partial`). MEASURED: on the OCC arm the matcher fires **0/24 goals** (all pre-resolved by branches 1-3, none left `active` for the converse/antonym branch) → OCC arm UNCHANGED **0.9400→0.9400** (0/50 items change); core dims byte-identical 3/3; the only diff is a single board-invisible goal-`.status` flip. Per no-more-default-off ("no-regress ≠ net-positive") → **KEEP DEFAULT-OFF with the measured reason**; the capability is now WIRED (one flag from live), not islanded. The matcher's proven **+0.483 lives on the event↔goal-congruence gold, NOT the OCC arm** (which has no headroom) → realizing it needs a board arm that scores event↔goal congruence directly.
- **FOLLOW-ONS:** add an event↔goal-congruence board arm (reuse the p4 SOLVED's n=120 gold) that exercises the matcher → then re-run the flip-gate on THAT arm + flip on if net-positive; a NEW problem to SCALE the episodic-simulation scene-tail organ (Barsalou/Minerva-2, larger corpus + powered natural gold, glass-box, BELOW the matcher); build the spatial `type_match` call site + fold FrameNet beneficiary/role-filler + temporal script-step-type + bridging part-of.

---

## 2026-09-06 (CONT-13) — 🎯 CAPABILITY PEAK-vs-REALIZED SCOREBOARD ("are we at peak on all capabilities?")

**Owner question (2026-09-06):** are all the improvements every solution handed us RECORDED, with which we KEPT vs SHELVED, the PEAK each identified — and are we AT that peak on all capabilities? **Answer: NO — and that is the point of this ledger.** We are fully at peak (live + SCORED) on ~5 capabilities, PARTIAL (mechanism live but instrument-gap / default-off / upstream-capped) on ~9, and NOT-YET-delivering on ~4 — plus 3 owner-DONE mid-integration this session. `peak` below = the best number a solution ACHIEVED + the CEILING it named (oracle / gold-parse / no-LLM). `at peak?` = is the reader DELIVERING it live-and-scored today.

**THE ONE DOMINANT "BELOW PEAK": the MEANING channel is entirely LATENT.** Five proven gains — curated foundation +0.0755, rare-sense readout +0.065, ATL precision +0.023, clean-foundation +0.067, and word-sense shared-core granularity +0.1377 — ALL wait on ONE missing read()-time stage (`reader_meaning_channel`). Building it (the word-sense p7 integration, in flight this session) cashes the whole cluster. Second: the RELIABILITY substrate (precision_weight) is landed default-OFF pending its defer-consumer (p4). Everything else is either realized, upstream-capped, or a correctly-shelved located negative.

| Capability | Peak identified (achieved + ceiling · instrument) | Kept / Shelved | At peak? | Gap to peak |
|---|---|---|---|---|
| **Coref — pronoun** | live 0.469→0.602 (19c, graded ACT-R); GUM 0.362→0.468 (unified-referent, ISOLATED lever); ceiling oracle-unified **0.584** | graded_pick default-ON (kept); unified_referent LANDED **default-OFF** (measured: live incumbent 0.503 SUBSUMES the unified port 0.433 — +0.106 was vs a weakened baseline); HYBRID follow-on filed | **PARTIAL** | HYBRID (unify store ON TOP of the incumbent pool) + clustering→0.584 + a modern pronoun corpus (board coref is 19c) |
| **Coref — common-noun / entity** | +0.0809 CoNLL / hard-link 0.434→ceiling 0.540; board arm +0.0924; modern blind-head **0.541 = no-LLM ceiling** (81% needs world-knowledge) | entity_kb_resolver default-OFF (measured); realized via common-noun board arm | **PARTIAL / at-no-LLM-ceiling** | two-pass reader_coref live wire; the 81% world-knowledge bridge is BARRED |
| **Who-did-what AGENT** | 19c 0.041→0.61-0.69 (cm_agent); byhead QA-SRL +0.433; modern non-canonical +0.113; construction cues +0.018 | cm_agent/cm_agent_struct/byhead default-ON (kept); construction+byagent-coverage **IN-FLIGHT** (modern-board) | **PARTIAL** | remaining headroom is UPSTREAM extraction (POS+candidate), not the assigner; register-general parser (filed) |
| **Who-did-what PATIENT** | +0.086 clean UD (0.745→0.831); ceiling gold-parse **0.913** | structural_patient default-ON (kept); patient board arm scores it | **PARTIAL** | +0.15 to ceiling = the parser verb→arg attachment |
| **State / entity register** | qa_state 0/378→**0.826** (modern UD-EWT); state board arm 0.833 | bind_entity_states + robust_cop default-ON (kept) | **PARTIAL** | cross-sentence canonical-entity binding (unbuilt; +0.43 of predications) |
| **Events / predicate** | predicate_recall board +0.0667; CRF posterior 19c recall +0.041 / reach +0.31 | predicate_recall default-ON (kept); CRF posterior **LATENT** | **PARTIAL** | wire CRF→predicate_detector with a high-precision gate |
| **Temporal** | board 0.813 (19c extraction); **REASONING (p5) INTEGRATED `119f8f0ad`** on MODERN gold — before/after TB-Dense 0.5933 vs 0.5236 (integrated +TIMEX 0.6225, +0.099 CI-sep); NEW Allen OVERLAP capability (constructed 0.994 vs 0.5; real-prose subset 0.397 vs 0.0); relative-duration 1.000; UDS-Time + script knowledge organs landed (latent) | timeline extraction kept; **`sm.temporal_before`/`overlaps`/`longer` reasoner default-ON** over `sm.events` + 2 board arms; aspect-interval upstream recovers the dropped progressive (byte-identical order set) | **PARTIAL (reasoning live + arms; knowledge organs latent; extraction-capped)** | run full board to score the 2 temporal arms; wire the UDS-Time/script organs to a live scored consumer; live TIMEX/DCT + text→duration extractors are the highest-value shared upstream; copular/stative channel (P2) is the biggest real-prose overlap lever |
| **Causal** | board 0.643; multi-hop reasoner 1.000 vs adjacency 0.000 (constructed) / WIQA 0.585 / TellMeWhy non-adjacent 0.238 (all CI-sep) | **LIVE default-ON** — `hdlab/causal_reasoner.py` (multi-hop traversal + counterfactual necessity + Halpern-Pearl) as a query layer over `sm.causal_links`; causal_mental_bridge also live | **PARTIAL (live, not yet scored)** | a `board_causal_multihop_dimension` arm (WIQA/TellMeWhy); the DENSIFICATION (sparse network, default-off until edge-correctness validated) = the upstream extraction gap |
| **Spatial / location** | where_is 0.319→0.468 modern; LIVE read() +0.170; **relational reasoning (p6) INTEGRATED `fcf7dc93e`** — containment/position/path all 1.000 vs floors CI-sep on gold; position end-to-end 0.276 vs 0.213 (+0.063 CI-sep) | named-ground binding default-ON; **spatial reasoner `sm.spatial_*` default-ON over `sm.locations`** + board arm; commonsense gap-filler + extractor = follow-ons | **PARTIAL (reasoning live+arm; end-to-end extraction-capped)** | the SPACE cap is DOUBLY located = named-ground BINDING (extraction) + relation EXTRACTION RECALL (0.22/0.06/0.02 edges → 6/90 chain survival); P2 joint text→relation extractor is the sole end-to-end lever; run full board to score the spatial arm |
| **Goal / intention** | WANT 0.61 / WHY 0.98; hierarchy plot 0.68→1.00 (+0.318); purpose-filter board +0.135 | register+hierarchy+purpose-filter default-ON; goal-hier board arm built | **YES (realized+scored)** | residual: bare-purpose parser + Tier-2 abductive (meaning-gated) |
| **Affect / emotion** | feel 0.788 / valence 0.838 (stated, on affect dim); UNSTATED-emotion OCC appraisal TYPE 0.940 vs 0.440 floor (+0.500 CI-sep), prospect subset 1.000 vs 0.000, modern gold | affect register default-ON; **OCC inferred-emotion INTEGRATED (p3, `2a927deee`)** — `sm.infer_emotion` live default-ON + thwart-generalized `goal_register.track_status` (strict superset, 0 regress) + board OCC arm | **PARTIAL (live, arm folded not yet run)** | run full board to score the OCC arm; residual = event↔goal EXTRACTION density (the density-cut ceiling); the converse/role-filler layer (P2), goal-script KB (P3), intensity organ, and the unified-resolver engine-swap (§6b) are the follow-ons; stated-affect still capped by common-noun coref |
| **Belief / Theory of Mind** | BigToM CHAIN 0.849 / FALSE-belief +0.871 over 0% floor / oracle 1.000 | theory_of_mind default-ON lazy (kept); board_tom arm built = SCORED | **YES (landed chain realized+scored)** | action ceiling = goal→fact desired-value (meaning-gated); live belief-extraction |
| **Bridging inference** | WordNet meronymy 0.472 / ConceptNet 0.654 over ATL hub (meaning channel's 1st live consumer) | bridging_inference default-ON lazy (kept); board_bridging arm built = SCORED | **PARTIAL** | heavier PPR-FUSE/entropy-gate estimator (+0.036/+0.090) NOT landed; extraction-bound |
| **Meaning / WSD channel** | curated +0.0755 · rare-sense +0.065 · ATL-precision +0.023 · clean-foundation +0.067 · word-sense shared-core COARSE **+0.169** (a_s 0.5236 vs 0.3544, CI-sep, TRANSFERS live); wic arm +0.0633 (scored) | **LIVE read()-time SENSE-SELECTION consumer landed** `sm.select_sense` (hdlab/underspecified_sense_reader.py, default-on lazy additive, emit COARSE); hub-decorrelation = located negative (land nothing) | **PARTIAL (live, not yet scored)** | a `board_coarse_sense_dimension` on MODERN gold (Raganato/SemEval-2015 = acquisition gap); the fine-grain residual is a genuine within-invariant limit (§2 HOLD confirmed, brain doesn't excel there either) — the forward lever is the recurrent predictive loop (wall-map) |
| **Precision / reliability** | patient answered 0.879→**0.968** (+0.089), obl 0.758→**0.839** (+0.081), event_conf AUC 0.771 — all CI-sep, twins flat | **LIVE + flipped DEFAULT-ON** — defer-consumer (abstain-below-tau) + agent defer + `EventRecord.event_conf` (reasoning bridge); cost-fold → ZERO extra parses; emission byte-identical at tau=None | **PARTIAL (live, not yet scored)** | a MODERN selective-reliability (risk-coverage) board arm (`board_selective_reliability_dimension`, recipe recorded); obl consumer→SPACE register; NORTH-STAR = a globally-normalized parser (= the wall-map's greedy-parser fix) |
| **Parser (arc-eager/labeler)** | consolidated single-parse (board zero-regress, ~5% faster); labeler fast path ~54% read cut; 2-3.5x byte-identical | all speedups default-ON (kept, byte-identical) | **YES for speed** · accuracy = located-negative reframe | accuracy lever = precision-weighting (p4) + a register-general incremental parser (north-star, filed) |
| **Memory / retrieval** | the cue-based content-addressable RETRIEVAL convergence (validated on clean inputs); partial-cue read STRUCTURALLY CAPPED | graded retrieval landed in coref; partial-cue cap = a durable finding (cleared by understanding) | **PARTIAL** | live payoff is FRONT-END-gated (verb-argument extraction) |
| **Perf / speed** | general 2x read; ~9x DG cache; parser/tagger/labeler/affect byte-identical | all default-ON (kept, byte-identical) | **YES (fully realized)** | none owed (optional pos_tagger.emission refactor) |
| **The BOARD / instrument** | 19c-FREE MODERN aggregate **0.6135** over 7 modern dims, 4/7 CI-sep, NO LitBank; + 3 new arms (coarse-sense/selective-reliability/causal-multihop) | **LANDED — the reported comprehension board is now 19c-FREE** (LitBank demoted to informational_19c_crossref); agent_hybrid + construction guardrail flag-gated default-off (register reason); 1b SKIPPED (byhead-subsumed) | **YES (mid-upgrade complete)** | run full board `--run` to PERSIST + realized aggregate; modern coarse-WSD/temporal/goal/affect golds (named gaps) |

**SHELVED as located negatives (peak = the incumbent; correctly NOTHING to realize):** hub-decorrelation/whitening (word-sense — argmax-neutral + brain-unfaithful; +0.0176 figure RETRACTED), construction-selector for multi-DO (+0.000 over hybrid), context-free means-end atomic bridge (twin-band), distributed-selpref into the parser (below arc-eager; brain-corroborated), self-supervised contextual encoder (below the wired diagnostic). Each is a full-pass located negative with the cause named — no wire owed.

**AT PEAK today (live + scored):** goal (+hierarchy), ToM, bridging (arm), perf/speed, and — on their own modern instruments — patient/state/who-did-what-agent. **BELOW peak, biggest levers first:** (1) BUILD the read()-time meaning stage (cashes 5 latent gains); (2) turn on the reliability defer-consumer (p4); (3) land the 3 in-flight (entity-unification → word-sense → modern-board); (4) the upstream extraction front-end (POS+candidate) that caps who-did-what; (5) the register-general parser + Phase-1 predictive comprehension (the north-star that lifts meaning, temporal, causal at once). Per-solution detail = the rows below + the 2026-09-05 scoreboard (now superseded by this capability view for the "at peak?" question).

---

## 2026-09-06 (CONT-15) — BATCH 3: FULL SOLVER-WORK INVENTORY (continuing the "miss nothing" practice)

### p2 — extract_relations_from_prose_whole_subgraph_survival_the_shared_reasoner_bottleneck  [INTEGRATED `102641f3b` + FLIP-ON `04513bd08`; EXCELLENT; reverified 22 checks/7 witnesses + landing 6/6]  *(the #1 shared-bottleneck problem I filed — a solver delivered it; joint front-end promoted; the UNION-event-set enabler is DONE → `joint_temporal_events` now DEFAULT-ON, strict-superset-safe, recall 0.76; board survival arm live; `joint_nominal_events` stays OFF pending the p6 WSD gate)*
- **THE WIN (TEMPORAL): ONE glass-box joint parse-based front-end (`experiments/_joint_relation_frontend.py`) parses each sentence ONCE and reads events/states/spatial-edges/roles off the SINGLE dependency structure — the brain-foundational anti-fragmentation property (Friederici IFG structure-building → pMTG/ATL role binding; Bach neo-Davidsonian event variable). The incumbent runs THREE separate front-ends, two of which THROW THE PARSE AWAY (spatial does a linear nearest-noun scan; temporal uses a separate tagger + TENSE-GATES events) — which is why within-clause edges fail independently + multi-hop chains die at the exponent rate.** THE KEY INSIGHT: **a STATE is an event; the tense-gate (VBD/had+VBN/be+VBN only) was the wall.** Metric = WHOLE-SUBGRAPH SURVIVAL (every edge of a ≥2-hop chain recovered), extraction ISOLATED (reasoner at gold-perfect).
- **MEASURED (modern gold): TB-Dense (22 docs, 333 multi-hop chains) survival incumbent 0.1111 → joint (tense-agnostic+copular) 0.4054 (+0.2943 CI-sep, null_p95 0.0601) → +WordNet eventive-NOMINAL channel 0.7327 (+0.6216 CI-sep); event recall 0.320→0.891. END-TO-END through the ACTUAL solved connective+tense reasoner (not a proxy): 0.0971 → joint_cop 0.3480 → joint_nom 0.4937 (+0.3966 CI-sep) = 87% of the gold-event CEILING 0.5681 (conditional accuracy NOT degraded; coverage 0.58→0.85 free). Copular/stative channel: +0.0413 CI-sep on stative-touching OVERLAP edges (the temporal SOLVED's named P2 lever, recovered). GENERALIZED 100× on MAVEN-ERE (710 modern Wikipedia docs, 43,599 chains via transitive reduction, NO LDC caveat): survival 0.086 → 0.413 (cop) / 0.777 (nom), +0.3275 CI-sep, twin (denser pool 0.3209) still loses, decay holds to 24 hops. Same-tagger control (tense-gate removed, same NLTK tagger) 0.4174 → the win is the GATE not a better tagger.**
- **SPATIAL — LOCATED NEGATIVE (correctly attributed after the solver DRILLED + CORRECTED its own first claim): the joint parse-based Figure-Ground binder MATCHES the incumbent (6/90) with fewer constructions; hybrid 9/90 (not CI-sep at n=90). The wall is CONSTRUCTION COVERAGE + entity resolution (74% of the deficit: grid-frame/possession/existential/locative-verb), NOT parse UAS (only 10%) — reproduces the spatial SOLVED's own 74%/10% split. The first-pass "parse UAS is the residual" was WRONG (drilled + overturned). → do NOT land the spatial extractor.**
- **ROLE — served LIVE off the same one parse** (route_predicate_arguments; DISK correction: `role_route='wired'` is DEFAULT-ON at situation_reader.py:838, not default-off as the brief said). **APPRAISAL (3rd reasoner) — extraction lever measured: the joint pass recovers the OUTCOME event on EVERY OCC gold item (dense 39→44/44, sparse 37→44/44) the incumbent's tense-gate dropped — unstarves the EXTRACTION half; the residual is appraisal's SEMANTIC goal↔outcome matching (the reasoner's wall).** So all THREE reasoners' extraction-gated portions are located/lifted by ONE front-end.
- **KEY DEEPENING: eventive-NOMINAL via WordNet (eventive sense + deverbal link) is the single biggest lever (NOUN-event recall 0.105→0.703) but precision drops 0.79→0.65 from POLYSEMY ("building"=act sense) = a TOKEN-level WSD residual, NOT type-error, and it does NOT hurt the reasoner; register-dependent (net-positive nominal-heavy, precision-costly on newswire). Grimshaw argument-structure diagnostic as a precision gate DRILLED + FAILED (crushed recall 0.70→0.20 on bare event-nominals) → the residual is genuinely the WSD/context channel's, evidence-backed.** **PEAK:** temporal survival 0.73, end-to-end 87% of ceiling; the temporal front-end wall is now largely CLOSED on two modern golds.
- **p2 LANDING PLAN (Sec 7): (1) promote `_joint_relation_frontend.py` → `hdlab/joint_relation_frontend.py` (parse-once, tense-agnostic+copular events + spatial + roles from one ParseResult). (2) route the temporal event detector through it behind a flag, feed temporal_reasoner the enriched events — additive/byte-identical when off (confirmed live reader reads 35 events intact); MEASURE the flip-on board impact across ALL dims (adding states/present-tense events touches many consumers) → flip-on-find-the-break; keep the eventive-NOMINAL sub-channel default-OFF (precision-gated pending the WSD gate). (3) do NOT land the spatial extractor. (4) a board arm scoring the joint survival + end-to-end (board-visible independent of the flag). FOLLOW-ONS (file): P2 spatial construction+entity extractor; P3 nominal event-vs-result WSD gate (meaning channel — makes the nominal channel a safe default); P4 appraisal semantic goal↔outcome matching (already the appraisal SOLVED's next-problem); P5 the labeled/incremental parser (already filed, do not duplicate).**

### p9 — represent_negation_and_quantifier_scope_for_truth_conditional_reading_modern_gold  [INTEGRATED `200a2ed70`; EXCELLENT; reverified 10/10 + landing 4/4]
- **HEADLINE (to land, additive):** a glass-box, NO-LLM truth-conditional POLARITY + QUANTITY operator over the reader's `sm.events` (`hdlab/polarity_operator.py`, promoted). Negation reader-native (UD-EWT `sm.events`, n=131): net factuality 0.9313 vs polarity-blind 0.5038 (+0.4275 CI-sep), negated-recall 0.8769 vs 0.0000, over-negation **0.0000 clean** (BEATS the prior `negation_factuality_gate` MIDDLE_BAND 0.0303). Negation well-powered (MoNLI NMoNLI n=1202): 0.9965 vs blind **0.0035 (INVERTS)**. Quantifier reader-native (n=585): 1.0000 vs 0.5385. Quantifier well-powered (MED downward n=563): 0.8259 vs **0.1741 (INVERTS)**.
- **REUSE (not rebuild):** extends `hdlab/state_register.py` VERBATIM — the copular-state polarity primitive (`StateSpan.polarity`, `incompatible`/`_contradictory_pair`, the ATL-hub WordNet entailment `state_match`) EXTENDED onto EVENT propositions + a quantifier layer. Does not re-extract/re-type/re-parse.
- **PINNED:** negation = an OPERATOR that TOGGLES a proposition's truth (Kaup & Zwaan two-step; Tettamanti/Tomasino suppression); quantification = CARDINALITY over the token set (Johnson-Laird — none/no = ¬∃, unifying negation and "none"); scope = clause-local c-command, complement factuality from the matrix verb's IMPLICATIVE/FACTIVE signature (Karttunen; Kiparsky), NOT propagation.
- **UPGRADES KEPT (each measured): (B) the biggest — NPI/negation-licensing UNIFICATION:** sentential negation IS a downward-monotone context (Ladusaw) → NPI subset 0.2656→0.7409, full quantifier operator coverage 0.490→0.704 / acc 0.5112→0.8225; unified aggregate **n=3791 0.8225 vs blind 0.2849 (+0.5376 CI-sep) + twin +0.2464** — negation and quantification are ONE downward-monotonicity operator. (A) implicative/factive lexicon → 141 verbs (EWT no-regress). (C) restrictor-vs-scope monotonicity for universals (Barwise-Cooper) → universal subset 0.3753→0.4282. (P1) `WiredPolarityReader` landing prototype (additive default-off; downstream QA 1.00 vs polarity-blind 0.40). (P2) unified `proposition_answer` via `state_match` (copular + event share ONE representation). (P4) scanner consolidation — the operator SUBSUMES ≥5 scattered clause-local negation scanners (3-way agreement 1.0; extended 1.0 vs 0.20/0.60).
- **LOCATED NEGATIVES / withdraw-first (SHELVED, reasons named):** the parser-repair UPSTREAM (WordNet-category + Frazier parallelism + subcategorization + shared-subject) matches the surface operator on the negation gold (0.8864→0.9318) NO training — BUT the **HONEST GENERALITY CHECK proved it NET-NEGATIVE on general UD-EWT POS (0.9443→0.9375, precision 0.04, breaks 172 tags to fix 7)** → CONSTRUCTION-SPECIFIC overfit → **do NOT deploy the repair; the parse-free SURFACE operator is the general primary.** Upgrade (D) cut the over-firing 12× (precision 0.037→0.208, POS damage −165→−14) but still marginally net-negative → **the general parser fix needs (a) a PRECISE lexical resource (VerbNet subcat frames) + (b) TAGGER-CONFIDENCE gating (override only an unconfident tag), NOT a WordNet heuristic and NOT training** — 🔗 this is DIRECT EVIDENCE for the just-filed parser problem (p3). Also: the quantifier 1.0000 headline is a CONSTRUCTED template set (withdraw-first; stand on MED + the EWT negation); double-negation/negative-concord + quantifier-quantifier ∀∃ inverse-scope NOT modelled (distinct deeper phenomena, not in the bar).
- **WALL RESEARCHED:** the prior gate's MIDDLE_BAND was over-propagation through learned `conj` edges → built ACROSS with a complement GATE (never propagate into a complement) → over-neg 0.0000. ~5-6 of the 8 EWT misses are **GOLD NOISE** (the gold auto-over-propagates negation across contrastive "but"); the brain-faithful operator is arguably MORE correct than its own gold (noise-cleaned negated-recall ≈0.95).
- **AUDIT UPDATE (to fold): `EventRecord` carries NO truth-POLARITY or QUANTITY field** (all events stored POSITIVE+SINGULAR — enumerated) → reading polarity-blind doesn't just lose, it INVERTS under negation/downward-quantifiers. FRAGMENTATION: truth-conditional negation is scattered across ≥5 scanners with no unified operator — this operator is the consolidation point. **PEAK:** negation 0.9965 (MoNLI) / EWT 0.9313; quantifier MED 0.8225 unified.
- **p9 LANDING PLAN (§Proposed hdlab, additive default-off byte-identical — the affect/patient_conf pattern):** promote `_polarity_operator.py` → `hdlab/polarity_operator.py`; add `EventRecord.polarity/quantity/quantity_exception/polarity_provenance` (default None); a default-off `read_polarity` flag → a post-read pass setting ONLY the new fields (mirror `_read_state`/`predict_surprisal`); reuse `state_register.state_match`; wire the QA capstone consumer. FOLLOW-ONS: unify state_register copular + event polarity (P2); the precise-lexical-resource parser fix (P3, feeds the p3 parser problem); conditional/phrase-level monotonicity coverage (P4).

### p12 — compose_the_unified_referent_with_the_incumbent_graded_pick_pool_for_a_live_coref_gain  [INTEGRATED `674fc61f2`; EXCELLENT; reverified 39/39 + landing 4/4]  *(DUAL result: a located negative on the brief's mechanism + a BIGGER win via a different lever the disk revealed)*
- **(1) LOCATED NEGATIVE — the brief's COMPOSE mechanism (a full pass per the bar):** feeding the unified referent to the incumbent graded pool does NOT beat the live incumbent — HYBRID 0.4750 vs incumbent 0.5032 (delta −0.028, CI incl 0) on modern GUM he/she (n=1240). RIGOROUS: shuffled-grouping twin LOSES (0.3556, +0.119 CI-sep → the merges ARE real signal); **ORACLE (perfect gold-cluster unification) ALSO fails (0.4661 < 0.5032) → the ceiling is SUBSUMPTION, not clustering quality**; ACT-R decay d-sweep {2..6} doesn't recover; positive control on the fragmented-protagonist subset still loses. MECHANISM: unification MONOTONICALLY helps the WEAK isolation scorer (0.341→0.394→0.407) but HURTS the STRONG incumbent scorer (0.503→0.475→0.466) — ANTAGONISTIC on this population (merging inflates the protagonist's ACT-R activation + blurs the recency the tuned pick needs; generic-suppress already prunes what unification would consolidate). → **KEEP `hdlab/unified_referent.py` DEFAULT-OFF; do NOT add a hybrid path** (the 2026-09-06 default-off audit entry now has a MECHANISTIC reason).
- **(2) THE REAL WIN (a different lever the brief MIS-ATTRIBUTED — the disk outranked the brief): a CUMULATIVE +0.0823 CI-sep stack of THREE brain-foundational fixes to EXISTING machinery (no new organ, no gold, no LLM), live on the ACTUAL EventCentralityReader: 0.5032 → 0.5855, a 16% relative lift.** **Wire 1** — `graded_coref_pick.phi_agreement_keep` (person-feature exclusion, Benveniste — a 1st/2nd-person SPEAKER is never a 3rd-person referent) is LANDED-but-DORMANT (never called by `_graded_pool_pick`; the brief WRONGLY assumed it was in the pool) → wire as a pool pre-filter (TIER1, animacy=None, recall-safe): **+0.0387 CI[+0.012,+0.075] CI-sep**; random-drop twin LOSES (removes person-feature POLLUTION not pool size); named no-regress (+0.036); concentrates in FIRST-PERSON genres (+0.091 vs +0.019 third-person, ~5×) + GROWS with antecedent distance. **Wire 1b** — apply `_agreement_narrow` to `him` too (agreement is a GENERAL constraint, Carminati, not slot-specific; the live narrow fires only on he/she/his/her): +0.0129 CI-sep (him subset 0.412→0.588). **Wire 1c** — soften generic-suppress (`GenericDistractorFilter(use_struct=False)`, drop the never-subject STRUCT proxy, keep the quantifier NONREF sub-test): +0.0274 CI-sep (97% of its over-removals are REAL common nouns like "the woman" — the 19c-calibrated structural proxy over-fires on modern multi-genre text).
- **(3) A HIDDEN GOLD LEAK FOUND + FIXED (invariant win): the coref harness fed entity gender from GUM's GOLD Gender feat (gold at inference!).** Built a glass-box GENDER ORGAN (name gazetteer 7210 + role/occupation lexicon + gendered morphology; Quirk/Corbett) that MATCHES the gold (+0.003, 100% agreement where both fire, coverage 5.8% vs gold 5.2%) → the whole +0.082 stack runs NO-gold-at-inference. CAP: gender is ~6%-SPARSE on modern nominals (the other 94% genuinely genderless). Do NOT wire coreferent gender propagation (net-negative −0.010 even gated).
- **CEILING DRILLED (levers exhausted):** the dominant wall is HARD-FILTER OVER-REMOVAL of the gold antecedent (miss-mass: unreachable 0.032 / filter-over-removal 0.284 / mispicked 0.142) — NOT world-knowledge (only 3.2% truly unreachable, CONTRADICTING the reference's "residual=individuation+world-knowledge" for the LIVE path); 100% of agreement-narrow over-removals are GENDER-UNKNOWN gold. Residual floor = 67% same-gender ambiguity (needs world-knowledge/coherence-prior, both barred/owner-DONE-dead). LOCATED NEGATIVE: dropping the hard narrow HURTS (−0.036, floods the pool) → the faithful fix is better GENDER INFERENCE, not soft agreement. **PEAK:** live he/she 0.5855 (+0.082); residual = the genuine glass-box floor.
- **BRAIN-FIDELITY (honest itemized):** every CUE is a genuine brain cue but the DETECTORS are proxies (participant-detection = head-count proxy, not Kaplan deixis — the reader has unused `deixis_person`/`note_turn`; referentiality = never-subject STRUCT proxy that over-fires; gender-inference was a gold leak); agreement is HARD where the brain is GRADED (Carminati) = the dominant quantified deviation. Fidelity moved ~80%→low-90s.
- **p12 LANDING PLAN (Q111, all in `hdlab/event_centrality_coref`):** wire 1 (phi pre-filter in `_graded_pool_pick`, thread a midx→head map through `resolve_stream`, flag default-ON — CI-sep + recall-safe + no-regress justifies it) + 1b (`_agreement_narrow` on `him`) + 1c (`use_struct=False`) = the +0.082 stack; wire 1d (promote the GenderOrganizer, replace the gold-gender leak); KEEP `unified_referent` DEFAULT-OFF. **FOLLOW-ONS: route the unified referent to its REAL home (the WEAK-scorer / NON-coref consumers — entity-KB hard-link, affect-experiencer, situation-model entity layer — unification helps the isolation scorer +0.052 + the intrinsic pick +0.106 there, NOT the tuned he/she pick) = a NEW problem to file; a richer gender organ (occupational/NER-animacy); brain-faithful deixis for participant detection.**

## 2026-09-06 (CONT-14) — REASONING-PHASE BATCH 2: FULL SOLVER-WORK INVENTORY (owner directive "keep track of ALL improvements; read submissions in their entirety; miss nothing")

**Why this block is exhaustive:** the owner flagged that recording only the headline landing drops the bulk of each solver's work (sub-wins, located negatives, adjacent evaluations, follow-on levers, the peak identified). Below is the COMPLETE inventory of every improvement in this batch — KEPT vs SHELVED, with the peak and the named follow-ons — read from each SOLVED.md in full. **Landing status: p3/p6/p5 ALL INTEGRATED + committed.**

**🎯 BOARD REALIZED (full `--run` 2026-09-06, `data/exp_situation_model_qa_modern_v1/metrics.json`): the 3 reasoners moved from LIVE→SCORED — all CI-separated on their own modern arms.** Headline 7-dim aggregate **0.6052** UNCHANGED (by design — the reader's core dims; the reasoners score as separate arms OUT of the aggregate). New-arm scores — **ALL 9 arm-rows SCORED + CI-SEPARATED** (corrected 2026-09-07: the earlier "2 arms degraded to null" was a strategy-side EXTRACTION-SCRIPT bug — those two arms are dict-of-rows, so `.get('model_acc')` on the outer dict read None; NO board bug, nothing to fix): **occ_appraisal (affect) 0.94 vs 0.44**; **temporal_overlap 0.9938 vs 0.50**; **temporal_before_after 0.5933 vs 0.5236**; **spatial_relational 0.2835 vs 0.2205 end-to-end**; coarse_sense 0.5236 vs 0.3544; **selective_reliability/patient_defer 0.9676 vs 0.8789** + **/obl_defer 0.8388 vs 0.7581**; **causal_multihop/wiqa 0.256 vs 0.000** + **/tellmewhy_nonadjacent 0.2344 vs 0.000** — every one CI-sep. So the reasoning + reliability + causal capabilities are all now SCORED (not just live); the board is fully realized.

### p3 — infer_unstated_emotion_via_occ_appraisal_over_event_goal_congruence  [INTEGRATED `2a927deee`/`7839569cf`; EXCELLENT; reverified 9/9 + landing 12/12]
- **HEADLINE (KEPT, LIVE):** glass-box OCC appraisal (desirability×prospect→OCC type+valence) over the live affect+goal+event registers, on a modern constructed OCC gold (n=50). TYPE 0.940 vs strongest floor 0.440 (+0.500 CI-sep), VALENCE 0.940 vs 0.500 (+0.440), prospect subset {relief,fears_confirmed} 1.000 vs provably-0.000, ORACLE 1.000, goal↔event twin 0.220 loses. Landed `hdlab/occ_appraisal.py` + `sm.infer_emotion`.
- **UPSTREAM GENERALIZATIONS (KEPT, LIVE):** (1) goal-FAILURE-by-thwart — strict-superset `track_status_thwart` into `hdlab/goal_register.track_status` (0 flips / 12 active→failed additions / 0 wants() regress; the load-bearing lever — NOFIX 0.060); (2) PROSPECT confirm/disconfirm branch (OCC prospect / Lazarus relief).
- **SUB-WINS BUILT + MEASURED (default-off / follow-on, each a can-fail floor + twin):** converse/role-filler polarity layer (event↔goal matching baseline 0.417→0.833 +0.417, twin loses); goal-script terminal-state KB (sparse recovery 0.136→0.773, HARD-PASS); intensity organ (minimal-pair ranking 1.000, importance-primary fidelity fix); social/attribution (beneficiary-satisfaction, social subset 0/12→3/12 clean cases; pride/shame correctly DEFERRED — needs a norm channel); the UNIFIED abductive resolver (`_occ_unified_resolver.py` — behavior-preserving 0.940 + MIXED emotions free, 3/3 two-goal items; the recommended engine-swap).
- **LOCATED NEGATIVES (SHELVED, reason named):** the distributional hub is POLARITY-BLIND (rel(sell,buy)≈rel(win,lose)) → event↔goal matching is a role-filler/knowledge-asset job, NOT the meaning hub; argument-level N400 intensity measured INERT (importance is primary); real-prose corroboration a located negative (agreement 0.576 beats char-shuffle twin +0.087 but not majority-valence 0.630 — real prose doesn't isolate goal→emotion, the same event↔goal-attribution confound).
- **CEILING (MEASURED):** event↔goal SEMANTIC matching = the appraisal's bound (density phase cut: rule oracle-invariant 1.000, end-to-end 0.94→0.06 sparse). **PEAK:** TYPE 0.940 (oracle 1.000).
- **FOLLOW-ONS (filed in SOLVED §NEXT):** P2 role-filler polarity layer; P3 goal-script KB (corpus-mined, held-out); P4 Talmy/script scene-inference + norm channel + theme-coref; P5 intensity organ; P6 mixed-emotions/dynamics/arousal/emotion→action; + the unified-resolver engine-swap.

### p6 — reason_over_the_spatial_relational_model_containment_position_path_modern_gold  [INTEGRATED `fcf7dc93e`; EXCELLENT; reverified 11/11 + landing 17/17]
- **HEADLINE (to land, additive):** glass-box RELATIONAL SPATIAL REASONER (`hdlab/spatial_relational_model.py` over `sm.locations`). All 3 types CI-sep on MODERN gold, reasoning ISOLATED (gold relations): CONTAINMENT (transitive) 1.000 vs last-mention 0.940 (+0.060 CI-sep; multi-fact 1.000 vs 0.497 chance; two-level ablation 0.896); RELATIVE POSITION (framework converse + nested-frame inheritance) 1.000 vs 0.734 (+0.266; multi-fact 1.000 vs 0.582); PATH/TRANSFER (Goal-over-Source + vacate) 1.000 vs 0.500 (+0.500). Every primitive PINNED (Johnson-Laird/Byrne; Franklin-Tversky; Dusek-Eichenbaum hippocampal transitive inference; Peer-Epstein nested frames; Lakusta-Landau Goal-over-Source).
- **END-TO-END (KEPT):** RELATIVE POSITION also clears the bar over the reader's OWN extraction (SpartQA, cov 0.339): reasoner 0.276 vs last-mention 0.213 (+0.063 CI-sep) vs twin 0.095 — a capability claim within that scope.
- **UPGRADES BUILT + MEASURED (KEPT):** extraction constructions (part-whole "of" + locative predication: recall 0.13→0.22, chain survival 1/90→6/90); a `canon_entity` block-label bug fix (standalone block "A" article-stripped to empty node — the fix FLIPPED the end-to-end position margin from un-separated to CI-separated); nested-frame position inheritance (cross-block transitivity — the "go deeper" fix); reasoner completeness (principled indeterminate-vs-unknown abstention, consistency/contradiction detection, quantifier all/exists); efficiency (memoized reachability + set-order determinism fix so the reverify reproduces across processes); a glass-box ConceptNet commonsense gap-filler (NO LLM) ReSQ commonsense 0.019→0.126 CI-sep over baseline AND shuffled-KB.
- **LOCATED NEGATIVE (KEPT, named with counts):** SpaceEval containment/path END-TO-END is extraction-gated — the reader's parse recovers 0.22 containment / 0.06 position / 0.02 move edges, multi-hop chain survival 6/90; ReSQ implicit real-world captions stay cov 0.036. Reasoning is NOT the bottleneck, EXTRACTION is (corroborated by SpaceEval-2015 lit: best F1 ~0.845 from GOLD vs ~0.573 from raw text).
- **RESEARCHED + REVERTED (SHELVED, reason):** event-participant grounding (unmeasurable); quantifier-in-scorer (corrupts the composition margin + weakens the twin); orientation commonsense VERIFIED KB-unreachable (ConceptNet has no vertical signal — needs a perceptual-simulation faculty, not a bigger KB).
- **AUDIT CORRECTIONS (I fold at integrate):** two SPACE phrasings OVERSTATED — "metric coords ruled out" → "categorical by default, metric on demand" (Rinck Exp 3); vacate-Source AUTOMATICITY is contested (salience asymmetry PINNED, automaticity not). **PEAK:** 1.000 isolated on all 3 types; end-to-end capped by extraction recall.
- **FOLLOW-ONS:** P2 joint text→spatial-relation EXTRACTION quality (the sole end-to-end lever; whole-sub-graph survival target); P3 perceptual-simulation/visual-spatial-statistics organ for orientation commonsense; P4 land the commonsense gap-filler default-off behind the reasoner.

### p5 — reason_over_event_time_order_and_duration_on_a_modern_gold  [INTEGRATED `119f8f0ad`; EXCELLENT; reverified 18/18 + landing witness]  *(I pre-wired the tense-granularity finding + MC-TACO into this brief; the solver used MC-TACO and DRILLED it — it mismeasures duration.)*
- **CORE (KEPT):** before/after QA over the reordered timeline on TB-Dense (1990s newswire, modern): register 0.5933 vs iconicity 0.5236 (+0.0697 CI-sep); reverse-order positive control 0.1557 vs 0.000; cue-bearing subset 0.9091; twin collapses to 0.521.
- **OVERLAP — a NEW capability (KEPT):** Allen (1983) interval-intersection over aspect-derived START/END intervals. Constructed 0.9938 vs point-order 0.5000 (+0.4938); real-prose TB-Dense overlap subset recovers 0.3967 of INCLUDES/IS_INCLUDED/SIMULTANEOUS the point-order control gets 0.000 of.
- **UPSTREAM FIX (KEPT):** `_aspect_interval.py` — the extractor DROPPED finite PROGRESSIVE ("was cooking") entirely (overlap was structurally impossible); recovered as imperfective open-interval, recall 1.000 vs gold TimeML / precision 0.857, + a Vendler lexical situation-type layer (overlap recall 0.281→0.397). NO before/after regression (−0.005, +48 pairs).
- **DURATION relative (KEPT):** magnitude line 1.0000 on 2520 unstated transitive "which lasted longer" pairs, twin 0.5063 — reuses the landed `transitive_ordering` primitive.
- **UPGRADES (owner "implement ALL, brain-foundational"):** (1) INTEGRATED before/after = cue + TIMEX event-local anchoring + transitive closure + signal-class provenance → 0.6225 vs 0.524 (+0.099 CI-sep); cue channel 0.909, date channel 0.830 (de-risk found the trap: naive carry-forward anchoring HURTS 0.493<iconicity; event-LOCAL is 0.958). (2) UDS-Time event-type DURATION organ (human-annotated, no-LLM) — DISSOLVES the text-mined negative: coverage 0.385→0.698, CI-below-floor (−0.090)→tied (−0.019). (3) progressive-precision guard. (5) SCRIPT/SCHEMA organ for TRACIE implicit-event — a WIN: mined 98,161 ROCStories→177,800 verb-pairs, 0.6022 covered vs 0.478 story-internal (SymTime needs 3.5M distant-supervised for 0.80; a glass-box chain mine gets 0.60). (6) TORQUE in-context OVERLAP QA F1 0.1184 vs point-order 0.000. (7) calibrated UDS-Time + the MC-TACO FAIRNESS drill: MC-TACO's per-candidate F1/EM MISMEASURES a knowledge organ — native threshold-free relative-duration ranking concordance 0.6624 CI[0.6303,0.6955] CI-sep over chance (the organ HAS real duration knowledge the QA metric hides). (9) token/mention-indexed register — WIN for OVERLAP: same-verb simultaneity 0.8448 vs type-keyed 0.0 (Zwaan event-token indexing).
- **LOCATED NEGATIVES (SHELVED, reason named + field-corroborated):** TYPICAL duration text-mined prior CI-below floor (Vempala 2018's exact negative + TacoLM's own design decision — Gricean reporting bias, durations are structurally unstated; fix = a different SOURCE, UDS-Time, not more mining); SDRT-lite causal-MARKER channel 0.50<iconicity (markers sparse+ambiguous; the real lever is causal-world-knowledge inference, couples with the causation organ); full-population real-prose overlap does NOT beat the "never-overlap" majority (0.776 vs 0.786) — but the ~60% INCLUDES/SIMULTANEOUS "ceiling" is DOMINANTLY a MEASUREMENT ARTIFACT (holistic-TLINK IAA 59-64% but in-context QA IAA 85-98%; MATRES DROPPED these relations), validating the endpoint-decomposition Allen design.
- **3 WALLS FULLY RESEARCHED (~130 sources, majority full-text):** overlap (TIMEX/DCT anchoring + SDRT discourse, Lascarides-Asher minimal-pair proof that aspect alone can't resolve overlap); before/after cue-sparsity (field's 11.2% explicit signal; iconicity is a genuinely weak baseline the field doesn't use); typical-duration reporting bias.
- **ADJACENT COMPONENTS EVALUATED:** TIMEX/DCT reference-time anchoring (MISSING, highest-value shared upstream, DE-RISKED with a built probe: +0.090); SDRT-lite discourse reader; UDS-Time lexicon; script/schema organ; the point-order extractor (OUR-INVENTION placeholder, now progressive-upgraded). **PEAK:** before/after 0.6225 (integrated); overlap constructed 0.994; relative-duration 1.000; the real-prose ceilings are extraction + annotation-protocol, both located.
- **FOLLOW-ONS (next-PROBLEMS):** P2 copular/stative-state channel (the biggest real-prose overlap lever — the dropped copular channel, Ferretti/Kutas/McRae); P3 UDS-Time native rank-correlation eval + a duration board arm; P4 SDRT world-knowledge discourse reader coupled to the causation organ; P5 richer script induction (SymTime-class start/end+duration decomposition) + anchor-aware TORQUE scorer.
- **p5 LANDING PLAN (§Proposed hdlab landing):** promote `_aspect_interval.py` (progressive+Vendler, additive — point-order tense set byte-identical); add an OVERLAP reasoner (`overlaps(x,y)`/Allen) over `sm.events`; add relative-duration (`longer(x,y)`); do NOT land the text-mined typical-duration prior (located negative) — file UDS-Time instead; emit per-judgment SIGNAL-CLASS provenance (cue/TIMEX/closure/VAGUE) + "unknown—needs world knowledge" on implicit-event queries.

---

## 2026-09-06 (CONT-11) — REASONING-PHASE WIRES INTEGRATED (7 this session, newest-first)

The reasoning phase went live. Each below was reverified first-hand + landing-witnessed + board-self-test-checked, then committed (NOTHING pushed). **Dominant theme = INSTRUMENT-GAP (live ≠ scored): the board still scores 19c LitBank, so most of these modern-gold wins are board-INVISIBLE — the `board_*_dimension()` arms + the modern-comprehension-board (p1) close it.**

- **precision_weight_the_head_driven_readers `110280d52`** — the reasoning phase's RELIABILITY SUBSTRATE. Claimed: patient selective@50 0.8789→0.9745 (+0.0956 CI-sep), QA-SRL 0.2982→0.3414, obl 0.7581→0.8919, random-conf twins flat (UD-EWT/QA-SRL, MODERN). Landed: `hdlab/parse_confidence.py` (frozen glass-box calibrator, raw AUC 0.615→calibrated 0.858, reuses graded_competition) + `EventRecord.patient_conf/patient_defer` behind `precision_weight_roles` **DEFAULT-OFF** (measured reason: no consumer defers yet + ~2 parses/read → landed-but-latent; flip-on = a reasoning consumer defers + board read-cost check). realized_on_board: **NONE** (INSTRUMENT-GAP + DEFAULT-OFF). full_gain: PARTIAL.
- **bridging_inference `4ce5075b1`** — the MEANING CHANNEL'S FIRST LIVE read()-time CONSUMER (it was entirely LATENT). Claimed: referential-part WordNet meronymy 0.4720 / ConceptNet PartOf 0.6087 (curated mfnd 0.6541) / instrument 0.4522, CI-scale over no-inference (0.20) + salience floors, shuffled-meaning twin collapses. Landed: `hdlab/bridging_inference.py` DEFAULT-ON/lazy as `sm.bridge`/`sm.infer_bridges`, reusing the ATL PPMI+SVD hub + meaning_foundation; all 8 dims byte-identical off-vs-on. realized_on_board: **NONE** (INSTRUMENT-GAP — no meaning/bridging board dim). full_gain: PARTIAL. followon: `board_bridging_dimension()` arm; PPR-FUSE/entropy-gate estimator.
- **theory_of_mind `4ce5075b1`** — the reasoning phase's first MENTALIZING system. Claimed: BigToM belief-pred CHAIN 0.849, FALSE-belief +0.871 over a 0% floor, twins lose, oracle 1.000 (BigToM, MODERN). Landed: `hdlab/theory_of_mind.py` (believes×wants→action off the BELIEVED state + inverse) DEFAULT-ON/lazy as `sm.predict_action`/`will_act_on`/`attribute_belief`, reusing belief_timeline + goal_register; byte-identical off-vs-on. realized_on_board: **NONE** (INSTRUMENT-GAP). full_gain: PARTIAL. followon: `board_tom_action_dimension()` arm; goal→fact desired-VALUE binding via the meaning channel (the action ceiling).
- **byhead agent cue (grounded_meaning_role_cue) `4ce5075b1`** — Claimed: non-canonical agent clean-slice 0.2556→0.6889 (+0.433 CI-sep), full 0.5224→0.6866, canonical no-regress, twin loses (QA-SRL, MODERN). Landed: `graded_role_assigner.agent_supports` byhead cue (gated participle+by-PP, weight 10 outvotable) + `situation_reader.cm_agent_byhead` DEFAULT-ON; grounded selfit cue = LOCATED NEGATIVE (not landed); byte-faithful, agent-only additive-safe. realized_on_board: **NONE** (INSTRUMENT-GAP — the board's only agent gold is 19c LitBank syntactic subjects, ~no by-agent Qs; board −0.0016 = gate false-fires on archaic 'by oneself'). full_gain: PARTIAL. followon: a MODERN by-agent board arm.
- **coref pick (strengthen_the_cue_based_pronoun_coref) `14f7f5e83`** — a LIVE BOARD-MOVER. Claimed: live pooled he/she coref_acc 0.4693→0.6019 (+0.1327 CI-sep); named no-regress 0.4883→0.6165; who-has-what 0.4035→0.4735 (+0.070). Landed: `EventCentralityReader.graded_pick=True` → PINNED graded ACT-R retrieval (recency load-bearing), event-centrality forced off; retires the anti-brain-foundational rolemass pick; coref-independent dims byte-identical 12/12. realized_on_board: **YES — the ONE board-mover this batch** (coref dim RISES on its live pooled instrument; who-has-what +0.070). full_gain: YES (register-general recency mechanism; a modern pronoun corpus is the proper home). 19c-measured but a fidelity correction justified independently.
- **space where_is (…lazy_locative_pp_bridging) `1d48cffd8`** — Claimed: MODERN where_is 0.319→0.468 (+0.149); LIVE read() 0.277→0.447 (+0.170); floor + shuffled-ground twin CI-sep. Landed: named-ground binding (Talmy Figure/Ground + VerbNet Goal gate + graded ConceptNet AtLocation) DEFAULT-ON in `_space_reader` prior_ext; additive (who-did-what byte-identical). realized_on_board: **NONE** (INSTRUMENT-GAP — board `location` dim is coarse/at-ceiling 1.0, doesn't score exact-node where_is). full_gain: PARTIAL. followon: a MODERN where_is board arm; Ground-aware goal-PP attachment.
- **parser distributed-selpref (distributed_…_parser) `b07572d2c`** — EXCELLENT located negative. Claimed: REFUTED (object-class selpref for UAS, brain-corroborated); the reframe = precision-weight a good-enough parse (Friston), selective who-did-what 0.871 vs 0.780 demonstrated. Landed: **NO-WIRE** (located negative); the reframe became the precision_weight follow-on (integrated above). realized_on_board: — (nothing to realize; the value is the reframe + substrate). full_gain: N/A (correct located negative).

**BATCH SCOREBOARD: 1 board-mover realized (coref); 5 live-but-INSTRUMENT-GAP (ToM / bridging / byhead / space + precision_weight-also-latent); 1 located-negative-no-wire (parser). The dominant unfinished work = MODERN-gold board arms (p1 + the per-arm follow-ons) to score the 5 board-invisible wins — the recurring "live ≠ scored".**

---

## Already integrated this session (strategy, mid-flight)

### a_force_dynamic_meaning_hub_causal_scorer_retire_the_connective_scoping_workaround
- one_line: Mental-causation bridge — an event-TYPE representation + a folk-psych bridging selector that crosses the mental-causation wall force dynamics can't represent; the brief's literal route (retire scoping) is a located negative (kept scoping).
- claimed_gain: constructed non-adjacent mixed bridges UNIFIED 1.000 vs force-only 0.500 (+0.500 CI[+0.250,+0.750]); real LitBank coverage 16/16 event-typed (mental 11/16) vs force 3/16; faithful selector +0.875 CI-sep over recency-only.
- instrument: exp_causal_unified_bridge_event_type_v1 (constructed dissociation, n=16) + RC.GOLD real edges; NOT the live board (causal QA gold is connective-only + circular for plausibility).
- verdict: EXCELLENT PARTIAL — located negative on the literal route (full pass) + constructive mental-causation cross.
- landed_live: LIVE default-ON pure-add. hdlab/event_type.py (event-TYPE organ) + `_read_causation` PASS-2 mental-bridge behind `causal_mental_bridge` (connective links first → goal-graph superset).
- realized_on_board: NONE YET (INSTRUMENT-GAP) — mental links are non-connective; the causal board dim (connective-only gold) is byte-identical off-vs-on. +214 mental_bridge links added to the situation model for downstream consumers.
- upstream_needed: contextual WSD via GroundedSemanticGraph (type_ok 0.688→0.750, chain 0.875→0.938) — LATENT organ exists; coref-experiencer (preserved only 0.500 on real prose) — the reader's live coref is the lever.
- downstream_needed: 3 consumers to UPDATE to receive the event-type signal — causation_typing → mental typing (3/16→16/16); affect_register → OCC-appraisal inferred-emotion channel; goal-graph → motivational spine (+47 enablement edges, already additive).
- adjacent_or_optim: mine a real-corpus non-adjacent MENTAL-bridge gold (the missing scored instrument — no live mental-causal metric exists).
- followon_filed: recorded here (this ledger) rather than scattered problem files.
- full_gain_realized: PARTIAL — mechanism + event-type organ live; field accuracy + the 3 downstream signal-updates + a scored mental-causal instrument are the pass work.

### add_the_arc_labeler_fast_scoring_path_the_dominant_remaining_read_cost
- one_line: A byte-identical fast scoring path for the arc labeler's 36-label per-arc perceptron (~54% of a long-doc read) + the graded Competition-Model readout the same refactor materializes for free.
- claimed_gain: labeler scoring 8.7–9.8x (~4.2s/read), whole-read ~54% cut on 3 full docs; byte-identical (0/22,921 held-out arcs); graded readout entropy→error AUC 0.930 (twin 0.481).
- instrument: labeler-in-read timing (3 full docs) + byte-identity witness (22,921 held-out arcs, 2 populations); NOT a board dim (byte-identical → no score change).
- verdict: EXCELLENT — byte-identical speedup + a brain-faithful graded readout (positive; the parser exploration D-O = located negatives/research).
- landed_live: LIVE default-ON. `_FastLabelPlan` + `_ensure_fast` in `hdlab/arc_labeler.py`; `label()` routes through it; `_predict_label`/`_score` kept as reference. Opt-in byte-safe `label_graded` publisher (default-off). Witnesses: solver 5/5 + hdlab-landing 4/4.
- realized_on_board: NONE (byte-identical — pure speedup, the single biggest read-cost). The graded readout is default-off (no consumer wired).
- upstream_needed: the PARSER is the dominant leak (obl:agent LAS 0.0588; −21% heads even w/ gold tags) — the parser tier.
- downstream_needed: the entity_states/copular binder + who-did-what readout should consume the graded label posterior + entropy (not hard 1-best) — belongs with `consume_the_graded_pos_posterior` (row 7).
- adjacent_or_optim: per-arc INDEPENDENCE (no joint one-role-per-clause decode) = the deeper fidelity gap (Q113). Parser exploration (D-O) → the rubric.
- followon_filed: recorded here (below) — whiten-the-meaning-channel + self-sup+grounded parser.
- full_gain_realized: YES for the speed (fully live, byte-identical). The graded-readout ACCURACY payoff needs a consumer (row 7 step); the parser fidelity is the parser tier.

**Two tracked follow-ons from this submission (owner may dispatch as solver problems; kept here per "track it all in one place"):**
1. **whiten the meaning-channel embedding** (MEANING TIER / step 4) — root cause of the grounding negatives = near-collinear meaning vectors (cosine 0.92); whiten (subtract global mean + project out top-D PCs) lifts the LIVE WSD channel a_s +0.0176–0.058 (n=2676) AND flips the parser-grounding control +0.020. UNVERIFIED — needs the shuffled-diagnosticity twin + significance + vs-curated-baseline before landing. A concrete near-term testable win; fold into the meaning-stage build.
2. **self-supervised + grounded front-end parser** (PARSER TIER north-star) — the DMV+universal-prior+EM + online never-frozen predictive-coding learner induce real structure with no gold trees (0.38–0.45), grounded meaning deciding ROLE on non-canonical clauses; the gap to supervised 0.84 is grounded-experience SCALE + the missing answer key, not the mechanism. Recorded in `PARSER_JOINT_INTEGRATION_RUBRIC.md` as the true-ideal direction; a multi-week program (owner decision to dispatch).

---

## ✅ TOP-DOWN PASS EXECUTED (2026-09-05) — all 6 owner-DONE integrated; gate EMPTY

Baseline agg **0.6592** → tiers landed in order, each measured + committed:
- **Step 0 (ruler):** 3 arms folded into the board (`patient`/`goal_hierarchy`/`wic`) — realized the previously-INVISIBLE gains: **patient 0.8311 (+0.086 CI-sep)**, **goal_hierarchy 1.000 (+0.318)**, and put the **meaning channel on the board** (`wic` 0.6006→0.6639 = +0.0633). `9da95a0ea`.
- **Parser tier:** double-parse CONSOLIDATION (single arc-eager parse) — board **0.6592 zero-regression**, ~5% faster. `612b0b579`.
- **Goal tier:** advcl PURPOSE FILTER (goal-why lever) + CONTEXTUAL `_link_open_stack` edge (situation-relatedness). `5462341ec`.
- **Coref tier (entity-KB):** landed default-OFF `6b57a04e9`; then **REALIZED via a NEW common-noun coref board arm** (`exp_board_common_noun_coref_v1`, `9b97be24c`): model 0.6889 vs surface-head 0.5965 = **+0.0924 CI-sep**, shuffled-KB twin loses — the +0.0882 was never a located negative, it needed its own arm (the board coref dim scores PRONOUN; this scores COMMON-NOUN; separate brain systems, separate arms). The LIVE reader flag stays default-off pending the two-pass wired into read() (the arm uses the cached two-pass).
- **Meaning tier:** board-visible via the wic arm; the reader-side `select_sense` channel is §2-deferred (islanded). `4cf4ac041`.
- Earlier this session: causal mental-bridge `8cbb1b0ed`, arc-labeler fast path `dad8b7b11`.

**REALIZED board wins:** patient +0.086, goal-hierarchy +0.318, **common-noun coref +0.0924** (new arm), goal-why (purpose-filter), a faster board-neutral parser, and the meaning channel now SCORED (wic +0.0633). **Board-neutral/additive (honestly):** parser (neutral), causal mental-bridge + goal contextual edge (additive, not yet board-scored — need a mental-causal / marker-less gold), meaning-wire reader-side (islanded select_sense, §2-deferred). The pass added FOUR board arms (patient/goal-hierarchy/wic/common-noun) that made previously-invisible proven wins visible.

## PASS FOLLOW-ONS (the deeper realization work surfaced by the pass — owner may dispatch)
1. **The entity-KB TWO-PASS reader_coref + a COMMON-NOUN coref board arm** — the board `coref` dim scores only PRONOUN coref; add a common-noun-clustering arm (like the patient/wic arms) so the entity-KB's +0.0882 becomes visible, and supply the Step-3 reader-coref lever via a two-pass. Then flip `entity_kb_resolver` on if net-positive.
2. **The read()-time MEANING STAGE (§2-gated)** — the reader has no read()-time meaning consumer; `grounded_semantic_graph.select_sense` is islanded. Build the stage that consumes the curated foundation (the wic arm proves the value). Gated by the §2 no-transformer owner decision for the fine half.
3. **The event-TYPE downstream signals** — feed `hdlab/event_type` to the 3 named consumers: causation_typing (mental typing 3/16→16/16), affect_register (OCC inferred-emotion), goal-graph (motivational spine); + a mined MENTAL-causal gold + a marker-less goal gold (the mechanisms landed this session are additive, awaiting these instruments).

## STAGED QUEUE — (all integrated 2026-09-05; retained for the record)

Per the batch discipline: reverify + grade promptly (done), wire in the pass so board-movers are measured in the integrated reader + in tier order. None wired live yet (no `INTEGRATED_BY_STRATEGY`).

| Submission | Reverify (first-hand) | Tier / pass step | Claimed gain (instrument) | Disposition |
|---|---|---|---|---|
| `seed_the_entity_world_model_resolver...` | `test_entitykb_resolver_v2.py` **6/6** | COREF (step 2) | common-noun coref +0.0809 / relational +0.1440 CI-sep (held-out LitBank) | Land the full-chain resolver + KB asset + live `sm.entities` coref coupling into `commonnoun_binder`; NOT the agent head-match. Board-mover (coref dim). |
| `wire_the_curated_meaning_foundation...` | `test_curated_foundation_wic.py` **6/6** | MEANING (step 4) | curated+coarsening beats the LIVE PPR `select_sense` reader **+0.0633 CI-sep** on WiC (n=2038); twins lose | **REROUTED win** — the brief's who-did-what/hub + meaning-readout proposals are located negatives (parse-bound; no live meaning stage). Wire the curated taxonomic signatures + shared-core COARSENING into the live `grounded_semantic_graph.select_sense` (the board's one live meaning metric). Coarsening SCOPED to the same/diff-sense judgment (NOT fine-sense a_s). Residual to human 0.80 = deep contextualization = §2 owner decision. |
| `validate_the_ppmi_svd_means_end_bridge...` | `test_contextual_goal_attachment_modern.py` **4/4** (+5/5,+9/9) | GOAL (post-meaning) | contextual situation-relatedness beats the info-free twin **K1 0.700 vs 0.483 CI-sep** (n=797 modern gold; 19c 0.634 vs 0.537) | **REROUTED** — the brief's context-free ATOMIC bridge is REFUTED (sits in the twin band); the brain's CONTEXTUAL inverse planning (situation→goal relatedness in the live associative store) wins. **NO PARSER WORK** (parser reused unchanged — the parser-touch flag was a false positive). Wire a reliability-gated contextual edge in `build_goal_graph`; uses the meaning store (compose after the meaning tier). |

**Parser-tier note:** so far NO submission carries a parser REPLACEMENT to wire. The arc-labeler's parser sections (D-O) were DIAGNOSIS/exploration (recorded in the rubric, filed as follow-ons); the means-end did NO parser work. If a genuine parser replacement is coming, it is in a not-yet-done submission.

---

## Chunk C — dimensions / state / events (harvested 2026-09-05)

### the_situation_model_has_no_affect_emotion_dimension
- one_line: A glass-box per-character AFFECT/EMOTION register (valence primary + category) bound to the coref-resolved experiencer via a psych-verb linking frame.
- claimed_gain: "how does X feel" category (n=673) 0.788 vs floor 0.312 vs twin 0.394 (CI-sep); valence-sign (n=743) 0.838 vs floor 0.490; multi-character 391 right vs 37 reverse (10.6x).
- instrument: exp_affect_register_qa_v1 on 100 LitBank docs — this arm IS the board `affect` dim.
- verdict: EXCELLENT — positive win (+ sanctioned located negative for inferred/unstated emotion).
- landed_live: LIVE default-ON. hdlab/affect_register.py + psych_verb_frames.py + affect_lexicon.py + assets; `track_affect` default-TRUE; `_read_affect` sets sm.affect_register + feels/valence_of/feels_about.
- realized_on_board: YES — `affect` dim live ~0.79 (category) / ~0.84 (valence) on its own population. BUT capped: coref is 87% of the end-to-end affect loss (83.5% of experiencers are common-noun entities the reader never tracks); extraction+binding near-ceiling (F1 0.945) given gold coref.
- upstream_needed: COMMON-NOUN coref (the dominant loss, +0.43 F1 headroom) — NOT built (6 prototypes each ≤+0.02; needs a faithful coref organ). Filed pri-2.
- downstream_needed: none (additive; other dims byte-identical).
- adjacent_or_optim: OCC-appraisal channel for inferred affect (gated on the meaning channel); goal×affect composition (frustration/satisfaction); arousal dynamics (stored, unused); revisit context_grounded_valence.py to adopt the psych-verb frame.
- followon_filed: form_a_discourse_referent_for_every_entity... (pri 2).
- full_gain_realized: PARTIAL — live + scoring; ceiling capped by the unbuilt common-noun coref organ.

### the_situation_model_has_no_goal_intention_dimension
- one_line: A glass-box per-agent GOAL/INTENTION register (5th Zwaan dim) — desire/intend/try + purpose constructions, coref-bound, status + reinstatement.
- claimed_gain: WANT-explicit (n=234) 0.6068 vs floor 0.137 & twin 0.0171 (CI-sep); goal-why (n=1372) 0.9796 vs physical-cause 0.0408; status 1.000 vs 0.333; reinstatement 1.000 vs recency 0.000.
- instrument: exp_goal_register_qa_v1 on 100 LitBank docs + authored status/reinstatement gold; registered as the board `goal` dim.
- verdict: EXCELLENT (full-chain upgrade) — positive win + sanctioned located negative (bare-purpose parse-gated; Tier-2 abductive).
- landed_live: LIVE default-ON. hdlab/goal_register.py + `track_goals` default-TRUE + `_read_goals`. ALSO landed upstream general primitive hdlab/verb_subcat_frames.py + asset + passive-agent guard.
- realized_on_board: YES — `goal` dim live & CI-sep (WANT-explicit 0.58 over floor 0.28, twin 0.0; WHY 0.97 vs 0.03). Moved from non-existent to scoring.
- upstream_needed: register-native dependency parse for bare-purpose attachment (parse-gated 0.33 vs oracle) — NOT built (parser_arceager wall).
- downstream_needed: none (additive; witnessed 6/6).
- adjacent_or_optim: goal HIERARCHY graph (built, next row); meaning channel for Tier-2 abductive goals (unifies w/ belief inverse-planning); thwart-by-outcome status; goal×belief composition.
- followon_filed: build_the_goal_subgoal_hierarchy_graph...; register-native parser (parser_arceager).
- full_gain_realized: PARTIAL — explicit slice fully live & CI-sep; bare-purpose tail parse-gated; Tier-2 needs the meaning channel (both the sanctioned located negative).

### build_the_goal_subgoal_hierarchy_graph_for_plot_structure_comprehension
- one_line: A goal→subgoal HIERARCHY GRAPH composing the flat goal register + causal network (motivation edges, connectivity salience, open-superordinate reinstatement) for multi-hop plot-structure questions.
- claimed_gain: authored plot battery: goal-why chain (n=88) 1.000 vs flat 0.6818 (+0.3182); superordinate reinstatement (n=15) 1.000 vs recency 0.0667 (+0.9333); connectivity salience 1.000 vs 0.000; distance-invariant K=0..5; means-end bridge PoC 0.9375 on covered verbs.
- instrument: exp_goal_hierarchy_qa_v1 — 30-item AUTHORED plot battery + real-narrative incidence on 25 docs; NOT the live board.
- verdict: EXCELLENT — positive win (located negative cracked via means-end bridge PoC).
- landed_live: LIVE default-ON. hdlab/goal_hierarchy_graph.py verbatim; `_read_goals` sets sm.goal_graph + 4 callables (pure ADD).
- realized_on_board: **NONE YET — INSTRUMENT-GAP.** Only 6/149 (4%) of live board goal-why questions are multi-hop, and the board gold IS the immediate purpose → the board never probes the graph; 169/169 board answers byte-identical. The 0.68→1.00 gains are on the authored battery only. Needs a `goal_hierarchy` board arm (NOT added).
- upstream_needed: register-native dependency parse (past the 0.33 bare-purpose wall) — not built; edge COVERAGE (more edge types) is the real-narrative lever.
- downstream_needed: **a `goal_hierarchy` board arm must be ADDED** for the benefit to be visible (NOT added at integration).
- adjacent_or_optim: PPMI+SVD means-end bridge real-narrative validation (n=16 PoC, filed); inverse-planning organ (shared w/ belief); faithful two-hop enablement; corpus-validated salience arm; goal×belief composition.
- followon_filed: the means-end bridge validation + the inverse-planning organ.
- full_gain_realized: **NO** — mechanism live but LATENT-for-scoring (no board arm probes it); means-end crack is an n=16 authored PoC.

### register_robust_event_detection_turn_on_and_expand_lifts_every_who_did_what_arm
- one_line: Turn ON scoped register-robust predicate recovery + a sort-aware copula readout so who-did-what reaches the copular-state silo.
- claimed_gain: AGENT arm 0.7099→0.8044 (+0.0945 CI[+0.0829,+0.1049]) on 40 held-out; board 0.7251→0.7918 (+0.0667); copula holder 0.09→0.63, property 0.00→0.275; caught+fixed a hidden causal regression (−0.0594) via scoping.
- instrument: who-did-what agent/patient arms on 16 board docs + 40 disjoint held-out — IS the live board `events` who-did-what population.
- verdict: EXCELLENT — positive win on both arms (+ a located negative: density-robust structural causal fix fails → meaning hub).
- landed_live: LIVE. `predicate_recall` default-ON + `_read_causation` SCOPED over base events (causal byte-identical); copula silo-unification via entity_states + copula-aware readout.
- realized_on_board: **YES** — `events` who-did-what board dim moved: agent 0.7251→0.7918 (+0.0667 CI-sep); copula patient/be 0.00→0.259 on the board slice. Coref/temporal/causal byte-identical.
- upstream_needed: joint graded/calibrated-posterior joint-decoded POS+parse (open-class 19c 0.56→~1.0, copula holder 0.63→0.77, attachment 0.83→0.955) — NOT built (filed).
- downstream_needed: `_read_causation` over base events (coupled) — DONE at landing.
- adjacent_or_optim: force-dynamic causal scorer (= the causal problem I just integrated); unified sort-typed eventuality inventory (merge events+states) — filed; per-consumer FP-threshold dial.
- followon_filed: the force-dynamic meaning-hub causal scorer (now integrated); joint-decoder + unified-inventory.
- full_gain_realized: PARTIAL — copula-readout + scoped predicate_recall live on the board events dim; deeper residual (open-class 19c, copula holder attachment, force-dynamic causal) gated on the joint decoder + meaning hub.

### register_robust_event_detection_the_reader_drops_events_when_the_tagger_misses_the_verb
- one_line: A learned noisy-channel predicate detector (logistic over register-invariant cues, static asset, no LLM) recovering tagger-dropped verbs, modern→19c zero-label transfer.
- claimed_gain: recovery @ FP≤0.5: MODERN (n=89) 0.8989 (+0.3329 vs twin); 19c-TRANSFER (n=144, 0 labels) 0.5625 (+0.5386); QA-SRL 0.803; crosses the parent's 0.16 modern wall; CRF axis-1: 19c 0.582→0.806.
- instrument: recovery-of-dropped-verbs on UD-EWT / LitBank / QA-SRL vs a random-verbhood twin — NOT an end-to-end board delta (explicit).
- verdict: EXCELLENT — positive on the recovery instrument, but a LATENT-for-board landing (turn-on measured flat at this problem).
- landed_live: LANDED. hdlab/predicate_detector.py + asset. Was DEFAULT-OFF at this problem; a LATER sibling (turn_on_and_expand) flipped `predicate_recall` DEFAULT-ON (scoped) → default-on NOW.
- realized_on_board: at this problem NONE (turn-on flat 0.2519→0.2530, not CI-sep — the who-did-what eval supplies the clean main-verb slice, hiding recovery). Later realized via the sibling copula routing + scoping.
- upstream_needed: none upstream of the detector.
- downstream_needed: a free-text event-recall consumer that measures dropped-event recovery — none exists live.
- adjacent_or_optim: the CRF calibrated-posterior tagger (+0.224 on 19c — NOT landed inline, needs crfsuite runtime / shared-component swap); joint-decoded POS+parse; glass-box morphological analyzer; meaning hub for the ~33% semantic ceiling.
- followon_filed: upgrade_the_pos_tagger_to_a_calibrated_joint_decoded_posterior.
- full_gain_realized: PARTIAL — detector live/default-on, but no live board instrument measures its recovery gain (clean who-did-what slice hides it); CRF upgrade (+0.224 19c) un-landed.

### wire_the_copular_state_qa_consumer_and_turn_on_bind_entity_states
- one_line: Wire a live STATE-QA consumer ("what/who is X" → entity-state store) + turn ON bind_entity_states + the label-robust robust_cop upstream detection fix.
- claimed_gain: qa_state 0.7116 (378 predicational) vs floor 0.5714 (+0.1402 CI[+0.087,+0.196]) twin losing; base OFF 0/378; ladder →robust_cop 0.8333 →arc-eager 0.8651; qa_aggregate 0.315→0.404.
- instrument: exp_situation_model_state_qa_v1 on UD-EWT copular GOLD (predicational, non-circular) — NOT the 19c board (would be circular; 19c coverage-only 527/530).
- verdict: EXCELLENT — positive win; registered as the board `state` dim.
- landed_live: LIVE default-ON. `bind_entity_states` default-ON; robust_cop → hdlab/copular_binding.py unioned into `_read_entity_states`; state-QA consumer = board `state` arm.
- realized_on_board: `state` dim now live (was off). ~0.71 (→0.826 w/ robust_cop) but on UD-EWT (modern), NOT the 19c board (coverage-only 527/530). qa_aggregate 0.315→0.404; 4 other LitBank dims byte-identical.
- upstream_needed: copular DETECTION recall (residual is upstream: read-back|binding 0.996, routing 1.000); robust_cop (built+live) closes most; arc-eager +0.032 but 19c-NEGATIVE (needs per-register routing).
- downstream_needed: cross-sentence canonical-entity binding — key state_register on the coref entity not the surface token — NOT built (0.43 of predications become answerable once bound).
- adjacent_or_optim: cross-sentence canonical-entity binding (biggest lever, filed); identity→coref-merge for equatives (~16%, filed); a ~200-clause hand-annotated 19c copular gold; arc-eager per-region routing (modern-only).
- followon_filed: cross-sentence canonical-entity binding; identity→coref-merge.
- full_gain_realized: PARTIAL — consumer + turn-on + robust_cop fully live; headline number modern-only (no 19c board floor), arc-eager un-landed (19c-neg), cross-sentence binding unbuilt.

### consume_the_graded_pos_posterior_uncertainty_aware_starting_with_referent_np_detection
- one_line: Tests consuming the graded CRF POS posterior for name/common-noun detection — a multi-level LOCATED NEGATIVE — but a structure-first PATIENT reader was found + landed instead.
- claimed_gain: located negative (referent_per_np soft-recovery +0.0000 to who-did-what; graded animacy validity 0.511 but +0.0000 on who-did-what); SEPARATE WIN structure-first patient +0.088 on clean UD-EWT (0.673→0.760), zero params, no-regress.
- instrument: who-did-what through the live reader on 25 docs + animacy gold + CLEAN UD-EWT structural gold; the LitBank role-balanced/object gold flagged INVALID.
- verdict: EXCELLENT — rigorous located negative (no wire for the posterior) + a landed structure-first win.
- landed_live: (1) graded posterior → NO-WIRE (located negative). (2) structure-first patient LANDED default-ON: structural_patient_pick, `structural_patient` default-ON, AGENT untouched (0/2850 board agent answers changed).
- realized_on_board: structure-first patient +0.088 on CLEAN UD gold, but LitBank board OBJECT gold reads −0.006 (confounded ruler — mislabels obliques as objects; INVALID). So ~flat/slightly-neg on the current board instrument; real gain shows on clean UD only. Posterior contribution: NONE (located negative).
- upstream_needed: parser verb→argument attachment (perfect-parse ceiling 0.912; residual +0.15) — NOT built.
- downstream_needed: none regressing.
- adjacent_or_optim: name→animate animacy_lexicon fidelity fix (byte-safe, role-metric-neutral — land as correctness); **re-base ALL who-did-what eval on clean UD-EWT structural gold** (not the confounded role-balanced gold); the posterior's real axis is VERB→predicate_detector (already landed).
- followon_filed: improve_the_parser_verb_argument_attachment_for_who_did_what (pri 3).
- full_gain_realized: PARTIAL — posterior brief = full-pass located negative (no wire); structure-first patient live +0.088 on clean instrument but board-masked by a confounded ruler; +0.15 to ceiling needs the parser.

---

## Chunk A — knowledge / meaning chain (harvested 2026-09-05)

> **⚑ THE DOMINANT FINDING of this chunk: the entire meaning/knowledge channel is LATENT.** The reader has NO read()-time meaning consumer (no `reader_meaning_channel` stage). So FOUR proven gains all wait on ONE missing wire: curated foundation +0.0755, rare-sense Bayesian readout +0.065, precision-weighting +0.023, clean-foundation lift +0.067 — every one is latent on read(). Building the single meaning stage cashes in the whole cluster. This is the highest-leverage row in the ledger.

### seed_the_entity_world_model_resolver_with_a_world_knowledge_role_kinship_prior_phase1
- one_line: A role/kinship/scenario KB seed for the entity-world-model resolver — the win came from the whole upstream chain (situation-model instance binding + pronoun-into-entity + consuming the reader's REAL per-text coref), not the KB.
- claimed_gain: aggregate common-noun coref (char-cluster CoNLL) +0.0809 CI[+0.0696,+0.0909]; hard-link +0.1793 (0.2537→0.4342, 63% of the gap to 0.540 gold ceiling); downstream relational reference +0.1440 (0.3570→0.5010); named no-regress −0.0034. KB seed alone = located negative (~2%).
- instrument: 100 held-out LitBank docs, deployment self-built records, via exp_entitykb_resolver_v2 (test 6/6). NOT the live board.
- verdict: EXCELLENT — positive win (chain) wrapping a located negative (KB seed). Owner DONE.
- landed_live: NO hdlab wire yet. Proposed: hdlab/entity_world_model_resolver.py (full-chain resolve()), ship curated KB → data/frontend_assets/role_kinship_scenario_kb.json, wire reader sm.entities head-coref into commonnoun_binder.py. NONE on disk. **STAGED by strategy this session (reverified 6/6); HELD for the top-down pass (board-mover, measure in the integrated reader).**
- realized_on_board: NONE YET (own held-out LitBank/CoNLL instrument; not wired to the live coref board dim).
- upstream_needed: a better substrate pronoun/coref resolver (reader's coref only 0.58-accurate, caps hard-link 0.434 vs 0.540) — bidirectional next lever, not built.
- downstream_needed: wire reader sm.entities head-coref into the resolver/commonnoun_binder; DO NOT wire the reader AGENT head-match (regresses named −0.0064). Default-ON justified.
- adjacent_or_optim: push residual rank_miss/abstain (smaller scoring refinement).
- followon_filed: none (this IS the Phase-1 follow-on of form_a_discourse_referent_for_every_entity...).
- full_gain_realized: NO — owner-DONE, no wire on disk yet; +0.0809 coref gain not on the live board until landed.

### build_and_freeze_the_clean_curated_knowledge_foundation_the_proven_meaning_lift
- one_line: Built + froze the clean curated meaning foundation (117,614 WordNet synset sense-signatures, 44MB float16) as a static offline asset + a reading-grown associative store (C1b, 80k) + grow/prune/consult machinery.
- claimed_gain: C1 rare-sense WSD a_s 0.2512→0.3267 (+0.0755 CI-sep); C1b SimLex 0.166→0.264 (+0.098)/WordSim 0.604; composed_hub_predictor AvgSim→MaxSim which-argument +0.065 CI-sep.
- instrument: C1 via LIVE hdlab.diagnostic_context_wsd on doc-disjoint SemCor subordinate (n=2675); C1b on SimLex/WordSim; usage tweak on QA-SRL ambiguous (n=1318). NOT the live board.
- verdict: EXCELLENT — positive win (the +0.067 reproduces + ships frozen). Owner DONE.
- landed_live: hdlab/meaning_foundation.py LANDED (loader) but **LATENT — no live read()-time consumer**. Frozen assets on disk (gitignored). Usage tweak DEFERRED.
- realized_on_board: NONE YET (meaning instrument only; C1's one consumer diagnostic_context_wsd is not called live).
- upstream_needed: meaning-KB resolution residual — additive prior-override closes HALF glass-box (0.33→0.60); 0.60→1.0 needs a contextual reader = §2 owner decision (rec HOLD).
- downstream_needed: **(1) a read()-time WSD stage consuming C1 (`reader_meaning_channel` — does not exist); (2) ship C1b as the reader's default distributional store (15k→80k) replacing hub_ppmi_svd_200d; (3) rebuild composed_hub_predictor on 80k C1b + adopt MaxSim, measure on live who-did-what (the readiest gain, unmeasured combined).**
- adjacent_or_optim: per-consumer prune projections; materials-science coverage gap; step-2 online targeted-acquisition learner (6,753 collapsed sense-pairs).
- followon_filed: step-2 learner = grow_broad_coverage... (slug below); pri-4 wire_the_curated_meaning_foundation_into_a_live_consumer_and_adopt_the_maxsim_usage.
- full_gain_realized: NO — loader latent; +0.0755/+0.098/+0.065 all latent until the reader gains a meaning stage / the store is swapped / the hub is rebuilt-and-wired.

### grow_broad_coverage_correctly_resolved_rare_sense_experience_the_meaning_channel_learner_on
- one_line: Rebuilt the rare-sense meaning channel as the hippocampal-EPISODIC regime; the landable win is a Bayesian log-prior + precision-weighted READOUT, the online coverage-growth half is a located negative.
- claimed_gain: readout rare-sense a_s 0.316→0.387 (+0.065–0.072 CI-sep); coarse 0.49→0.57; frozen-weight generalizes 6/6; all-pop 0.456→0.629. Located negative: online growth deployed +0.011 (CI-sep only at frac 0.40–0.55); coverage 0.14→0.47.
- instrument: subject-weighted a_s on doc-disjoint SemCor subordinate (n=2676) via hdlab/diagnostic_context_wsd (test 26/26). NOT the live board.
- verdict: STRONG — located negative (full pass) containing a real landable readout positive. PARTIAL; owner DONE.
- landed_live: sense_prior/prior_weight Bayesian readout landed into hdlab/diagnostic_context_wsd, DEFAULT byte-identical, **KEPT default-off (only consumer consolidation_gate is off)**. Episodic/coverage store NOT landed (§2-gated).
- realized_on_board: NONE YET (readout live but default-off; no live board dim consumes it).
- upstream_needed: a richer per-occurrence contextual encoder = §2 invariant boundary (rec HOLD). If relaxed, the shelf-ready CLS episodic + coverage machinery activates.
- downstream_needed: a read()-time meaning consumer (the same missing reader_meaning_channel); only current consumer = default-off consolidation_gate.
- adjacent_or_optim: cross-corpus WiC/SemEval generalization (untested axis); coarse/polysemy-merged grain; concrete rare-sense stratum (+0.035) may be independently landable.
- followon_filed: cross-corpus WiC/SemEval generalization.
- full_gain_realized: NO — readout landed but default-off/latent; coverage half is an unwired located negative gated on §2.

### build_the_atl_hub_and_spoke_meaning_channel_online_predictive_reader
- one_line: Tested whether a richer grounded ATL hub-and-spoke sense representation crosses the ~0.35 ceiling — it does not (located negative); query-side precision-weighting gives a real CI-sep gain.
- claimed_gain: precision-weighting (gamma>1/top-k) a_s 0.313→0.336 (+0.023 CI-sep), twin loses (does NOT reach 0.35). Located negative: grounded hub 0.283 < 0.313; full chain stalls ~0.34 even with ideal gold-W.
- instrument: subject-weighted a_s on doc-disjoint SemCor subordinate (n=2676) via hdlab/diagnostic_context_wsd (W1–W8). NOT the live board.
- verdict: EXCELLENT — located negative (full pass) + one landable positive. PARTIAL; owner DONE.
- landed_live: gamma/topk precision-weighting params landed on diagnostic_context_wsd, DEFAULT byte-identical, opt-in (witness 4/4). DID NOT land grounded-hub keys / reading-W / trained encoder.
- realized_on_board: NONE YET (param live but default byte-identical; no read()-time meaning consumer).
- upstream_needed: contextual per-occurrence re-representation = §2 boundary (rec HOLD).
- downstream_needed: a read()-time WSD/meaning consumer to feel the +0.023 (missing reader_meaning_channel); consumers opt into gamma/topk.
- adjacent_or_optim: syntactic-argument-restricted precision query (last within-invariant lever, ~+0.01). DO NOT reinvest in grounding/W-coverage/exemplars/discourse (6 converging located negatives).
- followon_filed: grow_broad_coverage... (pri 5).
- full_gain_realized: PARTIAL — precision-weighting landed but default-off/latent; +0.023 realized only when a meaning stage opts in; ceiling-crossing gated on §2.

### break_the_contextual_input_encoding_ceiling_for_specific_sense_selection
- one_line: Tested a self-supervised glass-box contextual encoder — it does not cross the ~0.33 ceiling; the decisive finding: the lever is a broad-coverage, clean, SENSE-DISCRIMINATIVE W (oracle W → a_s 0.995), coverage-limited today.
- claimed_gain: pure located negative + numbered lever. Encoder best 0.293 < wired diagnostic 0.307–0.317; readout saturated (settling 0.312 = one-shot). Oracle sense-discriminative W → a_s 0.995; learned W beats topic on covered senses 0.367 vs 0.308 (+0.059); bottleneck = COVERAGE (52%).
- instrument: subject-weighted a_s on doc-disjoint SemCor subordinate (n~2676) via hdlab/diagnostic_context_wsd (test 10/10, CPU). NOT the live board.
- verdict: EXCELLENT — located negative (valid pass, no wire) + a numbered redirect. PARTIAL; owner DONE.
- landed_live: NO hdlab wire (located negative — nothing beat the wired diagnostic_context_wsd). Transformer fork refuted.
- realized_on_board: NONE (no wire).
- upstream_needed: none within the invariant (bigger encoder rejected).
- downstream_needed: none directly (a REDIRECT).
- adjacent_or_optim: the discourse/situation-model prior (Stage 4, unbuilt) is the one new brain-faithful build that could source a better W.
- followon_filed: REDIRECT into build_the_controlled_knowledge_growth_consolidation_gate (pri 1) + acceptance test exp_sg_lite_sense_discriminative_W_headroom_v1.
- full_gain_realized: NO — pure located negative; value is the numbered redirect into the knowledge-growth program.

### build_the_controlled_knowledge_growth_consolidation_gate_for_the_learner
- one_line: Built the glass-box consolidation gate (extract→consolidate→admit); PASSES when knowledge is clean (curated foundation +0.067) but the reader cannot grow clean knowledge from its own reading (narrow located negative), localizing the ceiling to the frozen sense-superposed w2v.
- claimed_gain: admitting a CONSOLIDATED clean foundation → a_s 0.2512→0.3178 (+0.067 CI-sep), raw-reading twin LOSES (−0.033). Located negative: reading-derived consolidation caps at gloss ~0.251. cls_growth safe (+0.110/6 rounds).
- instrument: subject-weighted a_s on doc-disjoint SemCor subordinate (n=2676) via hdlab/diagnostic_context_wsd (test 14/14). NOT the live board.
- verdict: EXCELLENT — gate positive wrapping a mechanism-complete narrow located negative. PARTIAL; owner DONE.
- landed_live: consolidate + raw_assocs + regression_guard promoted VERBATIM → hdlab/consolidation_gate.py (offline admission GUARD, composes with hdlab/cls_growth). Witness 7/7. NOT wired default-on; diagnostic_context_wsd unchanged.
- realized_on_board: NONE YET (gate is a live offline admission guard; the +0.067 clean-foundation LIFT is latent — needs the read()-time meaning consumer).
- upstream_needed: the clean-foundation wire (relations+SyntagNet+ConceptNet as sense atoms) = the +0.067 PASS — recommended LAND; overlaps LATENT meaning_foundation.py.
- downstream_needed: a read()-time WSD/meaning stage to feel the +0.067; cls_growth as the learner-on safety wrapper (present). Reading-derived growth must NOT be default-on.
- adjacent_or_optim: the ATL hub-and-spoke + online predictive reader named as the ONLY brain-foundational route through the ~0.35 ceiling.
- followon_filed: build_the_atl_hub_and_spoke_meaning_channel (P9).
- full_gain_realized: PARTIAL — gate + guard landed live as an offline wrapper; the +0.067 clean-foundation lift latent (no read()-time meaning consumer).

### validate_the_ppmi_svd_means_end_bridge_on_real_narrative_marker_less_goal_attachment
- one_line: Tested whether the PPMI+SVD means-end bridge validates on real narrative — it does NOT (info-free twin matches it → a goal-frequency artifact); two real walls located (purpose-extraction precision 0.27; context-free means-end knowledge).
- claimed_gain: REFUTED (located negative). Real LitBank purpose_bare: bridge K1 0.559 vs twin p95 0.533 (not sep); clean advcl-only 0.586 vs 0.574 (not sep). n=16 authored PoC 0.9375 was overfit.
- instrument: matched-population means-end discrimination + shuffled-map twin floor, via exp_meansend_realtext_validate_v1 (test 9/9). NOT the live board.
- verdict: STRONG — rigorous located negative. Status REFUTED. **⚠ No OWNER_NOTES.md / no owner_verdict yet — OPEN, do NOT front-run.**
- landed_live: NO hdlab wire. Proposed (verdict-independent) upstream purpose-precision fix in hdlab/goal_register.py; (HOLD) do NOT wire the bridge in build_goal_graph.
- realized_on_board: NONE (bridge refuted; upstream fix only proposed).
- upstream_needed: register-native dependency parser (advcl vs xcomp) for the 0.27-precision wall — filed parser_arceager.
- downstream_needed: goal-register consumers (_read_goals goal arm, goal_hierarchy_graph, affect_register, commonnoun_binder); upstream purpose fix removes 131 wrong vs 24 genuine (5.5:1) on why() — net-positive, NOT landed.
- adjacent_or_optim: CONTEXTUAL inverse planning (goal posterior conditioned on the situation model) + goal/belief unification (highest value); activation-weighted reinstatement prior for wants() (small verdict-independent fidelity upgrade).
- followon_filed: none new; points to parser_arceager + recommends a CONTEXTUAL inverse-planning brief.
- full_gain_realized: NO — bridge refuted + correctly unwired; the one net-positive piece (upstream purpose-precision fix) proposed not landed; owner verdict not yet recorded.

---

## Chunk D — perf / tagger / parser (harvested 2026-09-05)

> Most of these are byte-identical speedups = **fully realized, live**. TWO exceptions carry unrealized value: (1) the **CRF calibrated posterior organ is LATENT** (consumer predicate_detector default-off → +0.041 event recall / +0.31 reachability unrealized); (2) the **arc-labeler fast path is NOT landed yet** (land-ready, ~54% whole-read cut).

### numpy_vectorize_the_arc_parser_pos_only_joint_features_p8_named_lever
- claimed_gain: parser 1.61–2.16x (2.6x longest); parse-in-read 1.87–2.13x; byte-identical (434,330 arcs, 0 mismatch); whole-read within noise (parser ~7%).
- landed_live: LANDED VERBATIM into hdlab/arc_parser.py (length-gated n≥16), default-ON. Witness 393,225 arcs bit-identical.
- realized_on_board: NONE (byte-identical). upstream/downstream: none. adjacent: POS-tagger Viterbi (same technique, filed); typed selectional-preference features (closed inventory → same vectorization).
- full_gain_realized: YES — fully live; no accuracy follow-on owed.

### optimize_the_arc_parser_inner_loop_the_dominant_read_cost
- claimed_gain: arc parser 3.53x (parse cost −72%); byte-identical (393,225 arcs, 0/376); warm read 1.13–1.45x. (Corrected brief's "73%"→~26%.)
- landed_live: FeatCache/precompute_token/sentence_flat/sentence_scores/decode_from_scores VERBATIM → hdlab/arc_parser.py, default-ON; stock kept as _parse_reference. Witness 4/4.
- realized_on_board: NONE (byte-identical). adjacent: POS-tagger inner-loop; the numpy-vectorized gather (became the row above); mechanism-label correction (arc-FACTORED graph parser, not shift-reduce); the real accuracy wall = typed lexical-semantic grounding/PP-attachment (0.587→0.639), routed to the meaning channel.
- full_gain_realized: YES — fully live.

### optimize_the_pos_tagger_viterbi_inner_loop_the_co_dominant_read_cost
- claimed_gain: tagger 4.4–5.25x per-call (cost −77–81%); byte-identical emission matrix AND tags (0/694); read-level 1.19x.
- landed_live: _FastEmissionPlan → hdlab/pos_tagger.py + perceptron._viterbi routing (training path untouched), default-ON. Witness 3/3; full-read fingerprint unchanged.
- realized_on_board: NONE (byte-identical). downstream OPPORTUNITY the speed unlocks: hard-1-best consumers (referent_per_np, consequence_learning VERB-gate) could consume the now-affordable GRADED FB/CRF posterior. adjacent: the graded ranked-parallel decode is byte-identical on 1-best AND cheaper — cashes to accuracy only once top-down meaning re-ranks; PROPN↔NOUN = 28% of tag errors (meaning wall).
- followon_filed: graded-posterior consumption family (became consume_the_graded_pos_posterior...).
- full_gain_realized: YES for speed; the graded-decode ACCURACY payoff is a named unrealized follow-on (gated on meaning/grounding).

### upgrade_the_pos_tagger_to_a_calibrated_joint_decoded_posterior
- one_line: A likelihood-trained glass-box dependency-free (pure-numpy) calibrated CRF POS posterior P(VERB) recovering old-prose dropped verbs; the brief's JOINT tagger-parser decode is a located negative.
- claimed_gain: CRF AUROC 0.9409 / recovery 0.8727 @FP≤0.5 (vs perceptron 0.582); DEPLOYED free-text 19c event recall 0.9382→0.9792 (+0.041 CI-sep); downstream who-did-what reachability +0.3091 on genuine-drop subpop; joint decode +0.0017 (located negative); reproduces crfsuite to 7.3e-7.
- instrument: 19c LitBank transfer, spaCy-oracle event gold (n_dropped=538); witnesses 5/5, 3/3, 3/3. NOT a board dim.
- verdict: EXCELLENT — deployable dependency-free win + a located negative on the joint decode.
- landed_live: hdlab/crf_tagger.py (GlassBoxCRF, pure-numpy, no crfsuite/LLM) + asset LANDED; witness 5/5. **BUT LATENT — its consumer predicate_detector is DEFAULT-OFF → no live board consumer.**
- realized_on_board: NONE — organ latent; +0.041 event recall / +0.31 reachability are on deployment/diagnostic instruments.
- upstream_needed: base-model capacity / target-register training data (spaCy oracle recovers 0.82/0.88); the parser's OOD word-order fidelity is the dominant full-pop leak.
- downstream_needed: **predicate_detector must be turned on with a HIGH-PRECISION verb-recovery gate** (the recall-tuned threshold floods the parse: 6576 forced VERBs collapse reachability 0.698→0.497). The category-cue swap verb_margin→logit(CRF vpost) lifts predicate_detector 19c recall 0.582→0.806 — READY when the consumer wires.
- adjacent_or_optim: joint parse-decode (+0.0017 located negative) + delexicalized parser (−8 UAS) retired.
- followon_filed: successor (base-model capacity / register data); selection routed to meaning-hub/NP-head (SOLVED)/copular is-a (filed).
- full_gain_realized: PARTIAL — CRF organ landed + byte-faithful but LATENT; consumer default-off, so +0.041/+0.31 unrealized until wired with precision-guarded gating.

### route_the_redundant_nltk_perceptron_tagger_through_the_fast_hdlab_tagger
- claimed_gain: ~0.37s/read off the affect path (NLTK 195→0 calls); read cut ~11.5%; VALENCED affect byte-identical (0 flips/8947); inert NA↔None ~9% (located-negative sub-finding, 0 consumers branch on it).
- landed_live: _assign_affect tags via the shared UD-EWT tagger (no NLTK); context_grounded_valence.score_item gains need_valence. Live; witness 6/6, landing 0 flips/1124.
- realized_on_board: NONE (byte-identical valenced/affect). adjacent: **the arc-labeler naive per-label loop (~10x the affect tagger, ~0.87s/read, _FastLabelPlan built+witnessed) → the next row**; tag_punct NLTK PENN tagger in temporal = a located negative for a UPOS reroute (needs XPOS or route via EventRecord.tense) — NOT resolved; grounded-valence wire near-dormant (1 valenced firing/8947).
- followon_filed: the arc-labeler fast-path (next row); tag_punct/temporal-tense XPOS (named, filing recommended).
- full_gain_realized: YES — speedup fully live; no accuracy follow-on owed.

### add_the_arc_labeler_fast_scoring_path_the_dominant_remaining_read_cost
- one_line: A byte-identical fast scoring path for the arc labeler's 36-label per-arc perceptron + the graded brain-faithful readout it materializes for free.
- claimed_gain: labeler scoring 8.73x in-read (−4.2s/read); **whole-read ~54% cut on 3 full docs (26.56s→13.28s, CI-sep)**; byte-identical (0/22,921 arcs; full SituationModel byte-identical); graded readout argmax byte-identical (0/18,346) + entropy flags labeling errors gold-free AUC 0.930.
- instrument: labeler-in-read timing (3 full docs) + byte-identity witness (22,921 held-out arcs) + graded readout (UD-EWT test n=18,346).
- verdict: EXCELLENT-caliber — byte-identical speedup + a brain-foundational graded readout. **NOT yet integrated (PROBLEM.md still OPEN, no owner verdict).**
- landed_live: **NOT LANDED YET** — proposed Q111 diff only (add _FastLabelPlan + ArcLabeler._ensure_fast to hdlab/arc_labeler.py). Mechanism + witnesses ready in experiments/. Once landed it runs default-on (labeler is live via bind_entity_states).
- realized_on_board: NONE yet (not landed); when landed byte-identical → no board move (pure speedup, but the biggest single read-cost).
- upstream_needed: the PARSER is the dominant leak (~21% heads wrong even w/ gold POS; obl:agent LAS 0.0588); feed the parser DISTRIBUTED contextual reps; flip the dormant arc-eager parser (+0.05 free).
- downstream_needed: entity_states/copular binder + who-did-what readout should consume the graded label posterior + entropy (not hard 1-best) — belongs with consume_the_graded_pos_posterior.
- adjacent_or_optim: per-arc INDEPENDENCE (no joint one-role-per-clause decode — CRF/bipartite, Theta-Criterion), Q113; confidence-gated parser hybrid = located negative.
- followon_filed: none new; maps onto existing Q113 problems.
- full_gain_realized: NO — land-ready but NOT wired (OWNER verdict pending); a ~54% whole-read speedup sitting on the shelf. **⇒ high-value near-term land once owner-DONE.**

---

## Chunk B — who-did-what / coref (harvested 2026-09-05)

> **⚑ FINDING: the who-did-what STACK is largely live (cm_agent, cm_agent_struct, referent_per_np, structural_patient all default-on), but the board UNDER-MEASURES it.** The events QA is AGENT-only, so the +0.086 patient gain is invisible; the LitBank object/patient gold is ~76% oblique-CONFOUNDED (flagged INVALID). Realizing these gains needs a BOARD-INSTRUMENT upgrade (patient-slot QA on clean UD gold), not more mechanism.

### improve_the_parser_verb_argument_attachment_for_who_did_what
- claimed_gain: LIVE arc_parser patient on clean UD-EWT (n=1255) 0.7450→0.8311 (+0.0861 CI-sep, ~52% of the gap to 0.913 gold-parse ceiling); 19c clean-DO (n=669) +0.0972.
- instrument: CLEAN UD-EWT structural gold (LIVE arc_parser heads) — NOT the live board; the LitBank OBJECT gold is barred as confounded.
- verdict: EXCELLENT — positive win that DID land live but is board-invisible + fair located negatives (head lever; precision-weighted re-attach).
- landed_live: WIRED. 3 Q111 diffs in hdlab/predicate_argument_frontend.py (precise_passive + labeled/valency structural_patient_pick, live in route_predicate_arguments) + parser_arceager DEFAULT-ON. Real read()-time consumer.
- realized_on_board: **NONE YET** — +0.0861 on clean UD only. No-regress run: 2718/8049 patient picks changed yet all 6 board dims 0.0-delta (events QA agent-only; LitBank patient gold confounded).
- upstream_needed: a more precise register-invariant CLAUSE SEGMENTER (owned by the agent-tie problem; not a patient lever). For the patient itself: none.
- downstream_needed: **a PATIENT-slot who-did-what QA on clean gold — the live events QA is agent-only, so +0.086 is invisible end-to-end. NOT built (filed).**
- adjacent_or_optim: per-arc parse CONFIDENCE (AUC 0.81, ZERO live consumers, unwired); obl/PP head attachment (0.69→1.0 gold) for SPACE consumers; graded verb_subcat presence gate (built-unwired, WIRING DEBT 2); categorical-backbone register-general parser (built + Jabberwocky-validated, handed to the agent problem).
- followon_filed: add_a_patient_slot_who_did_what_qa_on_clean_gold; clause-segmenter → the_agent_tie_wall...
- full_gain_realized: PARTIAL — readout + arceager live in hdlab; +0.086 invisible on the board (events QA agent-only, LitBank patient gold confounded); needs a patient-slot QA on clean gold before any board move.

### swap_the_positional_role_assigner_for_the_brain_foundational_competition_model
- claimed_gain: LIVE board who-did-what AGENT cm_ON 0.2519 vs pre-referent 0.2257 (+0.0262 CI-sep); recovery over the referent regression 0.0410→ +0.2109; full stack 0.041→0.690 tuned/0.682 held; landing 0.041/0.075→0.6073 (+0.4689 CI-sep).
- instrument: **the LIVE board itself** (build_events_questions → _answer_events(agent), n=1830 agent Qs).
- verdict: EXCELLENT — a POSITIVE live-board WIN (recovers a real board regression, banks the stranded +0.336 patient).
- landed_live: WIRED default-on. hdlab/graded_role_assigner.agent_competition_pick (reusing graded_competition.net_activation) + situation_reader routes the tracked/coref set as AGENT source; cm_agent/include_pron_agents/case_filter/clause_local/cm_agent_struct all default-True.
- realized_on_board: **YES** — who-did-what AGENT (events dim) recovers 0.041/0.075→0.6073. Direct move on the live events dimension.
- upstream_needed: register-general incremental parse cue for the 75%-embedded-clause tie wall — NOW BUILT (cm_agent_struct default-on); a fuller register-general parser is the open frontier.
- downstream_needed: the board's context-cued answer_instanced readout — BUILT/landed with the wire.
- adjacent_or_optim: event/predicate-detection recall (20% of residual); coref coverage (5%); thetic/presentational/unaccusative construction detector (~20% new-agent ceiling, not built); animacy_lexicon coverage; register weight re-sweep for modern; passive PATIENT via precise_voice (still default-off).
- followon_filed: the_agent_tie_wall... (now owner-DONE, structure cue landed).
- full_gain_realized: **YES** — full stack wired default-on, board agent/events arm realizes 0.041/0.075→~0.61–0.69; named further optimizations remain but the assigned gain is realized.

### the_agent_tie_wall_is_embedded_clauses_needs_a_register_general_incremental_parse_cue
- claimed_gain: tie slice tuned 0.6372→0.7098 (+0.0726 CI-sep); held-out 0.6178→0.6739 (+0.0562); canonical no-regress; shuffled-structure twin loses CI-sep.
- instrument: LitBank 19c board who-did-what AGENT, TIE slice only (context-cued readout). NOT a full 9-dim board re-run.
- verdict: EXCELLENT — positive win landing a live wire.
- landed_live: WIRED default-on. cm_agent_struct=True; _cm_agent_for imports incremental_subject_before from hdlab.incremental_parser, feeds subj_before into agent_competition_pick; structure cue in agent_supports (AGENT_VALIDITIES["structure"]=2.5).
- realized_on_board: events (AGENT). Mechanism live + default-on, BUT headline is tie-slice instrument only — **no live board re-run**, so NONE YET as a board-dim delta.
- upstream_needed: register-robust event detection (58.6% of tie residual = predicate not detected) — predicate_recall (BUILT, was default-off, **flipped ON 2026-09-05**); coref recall; collective-human animacy patch (prototyped).
- downstream_needed: none (feeds the already-live graded_competition).
- adjacent_or_optim: gated relcl filler-gap organ (built, not wired for matrix-after-RC); collective-human animacy fold-in; gerund-possessive exception. Closed negatives: recency/prominence-Centering, eADM precision, thematic-fit.
- followon_filed: register_robust_event_detection_turn_on_and_expand (now owner-DONE).
- full_gain_realized: PARTIAL — structure cue fully live/default-on (mechanism realized), but headline is instrument-only (no board re-run); whole-arm levers (predicate_recall now on; coref recall) partly unrealized.

### construction_aware_selector_for_multi_do_who_did_what_over_the_referent_set
- claimed_gain: LOCATED NEGATIVE — construction adds +0.0000 over live hybrid_role_patient (selector n=149 +0.0000; full n=669 −0.0030; multi-DO −0.0123; end-to-end +0.0000; QA-SRL −0.0008). Side win: indef-pronoun source coverage +0.0105 CI-sep.
- instrument: cleaned-DO 19c LitBank selector + end-to-end read() (n=1354) + QA-SRL. NOT the live board.
- verdict: EXCELLENT — rigorous REFUTED located negative (full pass, selector at ceiling).
- landed_live: NO hdlab wire (located negative). The indef-pronoun win DEFERRED to wire_the_referent_to_coref_linking_pass (latent/gated).
- realized_on_board: NONE (located negative, 0.000).
- upstream_needed: turn ON referent-per-NP SOURCE (deployed 0.47/0.21→selector 0.93; +0.336) — gated on the coref linker (now built); register-native POS tagger.
- downstream_needed: none (selector already the live consumer; null = no new signal).
- adjacent_or_optim: filler-gap parser for pseudo-clefts; discourse old/new for locative inversion; small-clause extractor; meaning-fit selector (gated on the meaning channel); indef-pronoun coverage (+0.0105 prototyped). Folded 2 parent corrections into §2b (do NOT adopt ideal_pick).
- followon_filed: none as its own slug (indef-pronoun folded into the linking-pass wire).
- full_gain_realized: NO — located negative, no wire; indef-pronoun win (+0.0105, 19c) latent, deferred to the referent-per-NP source wire.

### form_a_discourse_referent_for_every_entity_not_just_named_ones_common_noun_coref
- claimed_gain: deployable former CoNLL 0.6174 vs surface_head 0.6046 (+0.0128 CI-sep); naive former = located negative (+0.0008 n.s.); entity-world-model crosses 0.255→0.540 GIVEN gold records but self-built +0.006.
- instrument: LitBank gold coref, 100 docs, CoNLL char-cluster population (NOT the board) + affect-experiencer subpop.
- verdict: EXCELLENT — located negative (world-knowledge-bound) + reframe + a small landable +0.0128.
- landed_live: WIRED default-on. hdlab/commonnoun_binder.py (situation_predict) via _apply_commonnoun_gate (commonnoun_situation_gate=True) + commonnoun_canonical=True into GR.make_canonicalizer.
- realized_on_board: **NONE YET** — goal/affect QA IDENTICAL on-vs-off (0.4615==0.4615, 0.7288==0.7288); +0.0128 on the CoNLL instrument only; experiencer subpop near-ceiling.
- upstream_needed: **an ENTITY WORLD-MODEL resolver seeded by a world-knowledge PRIOR (curated scenario/role + kinship KB) to break the identifiability wall → realizes the 0.54 ceiling. Resolver BUILT but capped; the KB prior = the STAGED entity-KB resolver (pri 2).**
- downstream_needed: character-bound canonicalizers (affect/goal/world-state) consume common-noun clusters — BUILT (commonnoun_canonical default-on); pair with the pronoun graded resolver — NOT done.
- adjacent_or_optim: reuses event_centrality_coref + graded_coref_pick. Measured-capped: WordNet bridging (7.8%), 2-pass (+0.0045), confidence-gating (+0.0056).
- followon_filed: the Phase-1 world-knowledge-prior entity-world-model resolver (= the STAGED pri-2 slug).
- full_gain_realized: PARTIAL — +0.0128 live/default-on, no board move; the big win (+0.43 headroom / 0.54 resolver ceiling) gated on the Phase-1 KB (staged this session).

### open_a_discourse_referent_for_every_np_not_just_coref_mentions
- claimed_gain: effective who-did-what PATIENT 0.4698→0.8054 (+0.3356 CI-sep) on cleaned-DO; full noisy pop (n=1354) +0.0473; candidate coverage 0.8183→0.9705 (+0.1521); twin LOSES and HURTS.
- instrument: LIVE read() with only the mention source swapped, cleaned 19c DO gold (n=149) — deployment instrument, NOT the board.
- verdict: EXCELLENT — deployment-ceiling recovery through the live reader (twin loses+hurts, REPLACE>ADD).
- landed_live: WIRED, now DEFAULT-ON. hdlab/referent_per_np.py consumed at read() under the referent_per_np flag. Originally default-off (coref collapsed 0.48→0.02); flipped ON after the linking-pass decouple.
- realized_on_board: NONE cleanly attributable — +0.336 on its own cleaned-DO PATIENT instrument; the dense set transiently REGRESSED the board AGENT (0.252→0.075) until cm_agent co-landed; whole-stack aggregate 0.2903→0.3598.
- upstream_needed: none (this IS the mention source; frame detector built-in).
- downstream_needed: (1) referent→coref LINKING pass — BUILT/owner-DONE; (2) Competition-Model AGENT assigner — BUILT/owner-DONE (cm_agent); (3) construction-aware selector — refuted.
- adjacent_or_optim: wire thematic_role_labeler as the SELECTOR (organ exists, +0.264 non-canonical, filed); meaning-fit selector (gated on meaning channel); register-native POS/NER (0.914→~1.0 cap); NP-type tag pass.
- followon_filed: wire_the_referent_to_coref_linking_pass + construction_aware_selector (P4); register-robust detection (P6).
- full_gain_realized: PARTIAL — source live/default-on + both turn-on blockers built; +0.336 patient wired but measured on its own instrument; board agent needed co-landed cm_agent to avoid net regression; the biggest remaining loss (selection→0.913) prototyped/refuted-not-landed.

### wire_the_referent_to_coref_linking_pass_so_referent_per_np_can_turn_on
- claimed_gain: coref recovers to NO regression / byte-identical (decouple−OFF −0.069 not sep; recovery over the regression +0.298); who-did-what +0.336 inherited; brief's expand-pool REFUTED −0.106 CI-sep; BONUS entity-key overlay +0.0429 CI-sep ABOVE baseline (NOT required for turn-on).
- instrument: 100 docs 19c LitBank coref, pooled pronoun coref_acc — NOT the board. (Post-integration board: coref_acc 0.5255==0.5255 on/off.)
- verdict: EXCELLENT — turn-on win (decouple unblocks default-on) wrapping a located negative (expand-pool refuted).
- landed_live: LIVE-WIRED. read() builds TWO mention views (role_mentions ← referent_per_np; coref_mentions ← coref for pronoun anaphora); referent_per_np DEFAULT-ON.
- realized_on_board: coref = **NO MOVE** (held byte-identical 0.5255; collapse prevented but no gain; the +0.043 above-baseline bonus NOT landed). events = PATIENT +0.336 live; AGENT transiently regressed until cm_agent landed.
- upstream_needed: the complete referent set IS the upstream (built/live); referent feature typing (_mk_referent opens blank cards) — NOT built; indef-pronoun source coverage (+0.0105) — not built here.
- downstream_needed: positional AGENT assigner → Competition-Model (cm_agent) — BUILT/LIVE.
- adjacent_or_optim: **overlay-by-discourse-entity coref bonus (+0.043–0.054 CI-sep ABOVE baseline) — NOT landed (needs an owner decision: it uses the coref column's provided clustering);** step-5 per-character individuation organ — MEASURED NEGATIVE (converges ~0.55).
- followon_filed: swap_the_positional_role_assigner (owner-DONE + live). Overlay-by-entity bonus flagged as candidate standalone pending owner decision.
- full_gain_realized: PARTIAL — decouple live, referent_per_np default-on, cm_agent fix live; the turn-on claim (coref no-regress + patient +0.336) realized; **the +0.043 above-baseline coref bonus NOT landed (board coref stays at baseline) — a candidate near-term win pending owner decision.**

### the_who_did_what_front_end_abstains_on_a_fifth_of_answerable_clauses
- claimed_gain: effective end-to-end (abstain=wrong) 0.9851; recomputed vs current reader 0.7877→0.9851 (+0.1973 CI-sep); QA-SRL 0.5678→0.9025 (+0.3347); twin loses; no precision regression.
- instrument: 669 clean-19c DO clauses + QA-SRL — NOT the board.
- verdict: EXCELLENT — positive recovery diagnosis, but the one clean wire landed (structural-DO filter) is KEPT DEFAULT-OFF; the verb-ID portion is a located negative (needs a trained joint POS+parse).
- landed_live: hdlab/structural_do.py + structural_do_recover flag **DEFAULT-OFF** (recovery precision 0.385 on ~76%-oblique-confounded 19c gold; unmeasured downstream harm).
- realized_on_board: NONE YET — the only wire (structural_do_recover) is default-off; +0.1973/0.985 on its own 669-clause harness. (The live reader's 0.629→0.7877 came from np_head_reduce + predict_revise, not this wire.)
- upstream_needed: register-robust predicate identification (20 no-event) — predicate_recall BUILT + LIVE (flipped on 2026-09-05); the deeper joint-POS build (beam_decoded_joint_pos_dependency_parser...) — filed, NOT built.
- downstream_needed: **the ~20 role-output organs (world_state/causation/hd_fact_store) must be re-validated to inherit the fix — PENDING; kept default-off precisely because feeding low-confidence recovered patients would assert FALSE facts.** Gate before flip: (a) a CLEAN 19c gold + info-free random-recovery twin LOSING, (b) a downstream-consumer regression check — both NOT built.
- adjacent_or_optim: structural-DO subsumes the verb_subcat_gate hard veto (landed default-off); object-gap → landed relcl_resolver (re-tread); ditransitive recipient-vs-theme (cheapest win, belongs to the non-canonical problem); non-canonical gold must be REBUILT (~96% broken).
- followon_filed: open_a_discourse_referent_for_every_np (P5); register_robust_event_detection (P6, owner-DONE + live); beam_decoded_joint_pos_dependency_parser (scaffold handed, not built).
- full_gain_realized: NO — the one wire (structural_do_recover) DEFAULT-OFF (low/confounded recovery precision + unmeasured downstream harm); +0.1973 on its own harness. Needs a clean 19c gold + random-recovery twin + a downstream regression check before realization.

---

## SUMMARY — the realization scoreboard (claimed gain → is it live on the board?)

Legend: **YES** fully realized · **PARTIAL** mechanism live, full gain pending · **NO** latent/default-off/unwired/located-negative-no-wire.

| Component | Claimed gain (instrument) | Live status | Realized on board? |
|---|---|---|---|
| **causal mental-bridge** (this session) | UNIFIED 1.000 vs force 0.500; +214 mental links | LIVE default-on pure-add | **NO** — instrument gap (no mental-causal board arm); + 3 downstream signal-updates pending |
| **entity-KB resolver** (pri 2, staged) | common-noun coref +0.0809; relational ref +0.1440 | STAGED (reverified 6/6), not wired | **NO** — held for the pass; unlocks the +0.43 coref ceiling |
| swap_positional_role_assigner (cm_agent) | agent 0.041/0.075→0.61–0.69 | LIVE default-on | **YES** — events/agent arm moved |
| improve_parser_verb_arg (patient) | patient +0.0861 (clean UD) | LIVE default-on | **NO** — events QA agent-only + LitBank patient gold confounded (needs patient-slot QA) |
| agent_tie_wall (cm_agent_struct) | tie +0.0726/+0.0562 | LIVE default-on | PARTIAL — tie-slice only, no board re-run |
| register_robust_event (turn_on) | agent +0.0945; board +0.0667 | LIVE (predicate_recall on, scoped) | **YES** — events who-did-what moved |
| copular state-QA (bind_entity_states) | qa_state 0/378→0.826 | LIVE default-on | **YES** (modern UD-EWT) — state dim; 19c coverage-only |
| affect dimension | feel 0.788 / valence 0.838 | LIVE default-on | **YES** — affect dim; capped by unbuilt common-noun coref |
| goal dimension | WANT 0.61 / WHY 0.98 | LIVE default-on | **YES** — goal dim |
| goal-hierarchy graph | plot 0.68→1.00 | LIVE default-on pure-add | **NO** — instrument gap (no goal_hierarchy board arm) |
| referent_per_np + linking-pass | patient +0.336; coref no-regress | LIVE default-on | PARTIAL — patient on own instrument; **+0.043 coref bonus NOT landed** |
| curated meaning foundation | WSD +0.0755; hub MaxSim +0.065 | LATENT (loader only) | **NO** — no read()-time meaning consumer (pri 4) |
| rare-sense Bayesian readout | a_s +0.065 | LANDED default-off | **NO** — no live meaning consumer |
| ATL precision-weighting (gamma/topk) | a_s +0.023 | LANDED opt-in byte-identical | **NO** — no live meaning consumer |
| consolidation gate | clean-foundation +0.067 | LIVE as offline guard | **NO** — the +0.067 lift latent (no read()-time consumer) |
| CRF calibrated posterior | 19c event recall +0.041; reach +0.31 | LATENT organ | **NO** — consumer predicate_detector default-off |
| arc-labeler fast path | ~54% whole-read cut, byte-identical | **NOT LANDED** (owner-DONE pending) | NO — land-ready, high-value |
| structural_do_recover | who-did-what +0.1973 (own harness) | LANDED default-off | **NO** — needs clean gold + downstream regression check |
| arc-parser vectorize / inner-loop / tagger Viterbi / affect reroute / lean profiles | byte-identical speedups | LIVE default-on | YES (perf — no score change; fully realized) |
| break_contextual_ceiling / construction_selector / means-end-bridge | located negatives | NO wire (valid pass) | — (correctly nothing to realize) |

## TOP-DOWN INTEGRATION PASS — the ordered work-list (start upstream, iterate down; measure each step)

**The five cross-cutting levers, most-upstream first. Each: turn on / wire → measure the board off-vs-on → update the downstream consumer to RECEIVE the signal → measure again → lock.**

0. **BOARD INSTRUMENT UPGRADE (do FIRST — several live gains are invisible).** The board under-measures the reader: (a) events QA is AGENT-only → add a **PATIENT-slot who-did-what QA on clean UD gold** (the LitBank patient gold is ~76%-oblique CONFOUNDED = INVALID; re-base who-did-what on clean UD); (b) add a **goal_hierarchy multi-hop board arm** (only 4% of current goal-why is multi-hop). Without this, the patient +0.086 and the plot 0.68→1.00 stay invisible and the pass can't attribute gains.
1. **Event/predicate tier — wire the CRF posterior → predicate_detector** with a HIGH-PRECISION verb-recovery gate (the recall-tuned threshold floods the parse: 6576 forced VERBs collapse reachability 0.698→0.497). Cashes +0.041 19c event recall / +0.31 reachability. Then re-validate the ~20 role-output organs (structural_do_recover's blocker).
2. **Coref/entity tier — land the STAGED entity-KB resolver (pri 2)** into the live reader (sm.entities head-coref coupling; NOT the agent head-match). Unlocks the common-noun coref ceiling (+0.43 headroom / 0.54) that caps affect experiencers (87% of affect loss) and goal/relational binding. Also decide the **+0.043 above-baseline coref bonus** (overlay-by-discourse-entity) — an owner call, near-term.
3. **Who-did-what tier — with the patient-slot QA live (step 0), realize the +0.086 patient**; sweep the remaining named optimizations (thetic/unaccusative construction detector ~20% new-agent ceiling; animacy_lexicon coverage; register weight re-sweep).
4. **Meaning tier (THE big latent cluster) — build the ONE read()-time meaning stage** (`reader_meaning_channel`) that consumes the curated foundation. Within-invariant now: the curated-foundation KEYS + rebuild composed_hub_predictor on the 80k store + MaxSim (pri 4), stacking the landed gamma/topk + sense_prior. Cashes +0.0755 / +0.065 / +0.023 / +0.067 at once. **The FINE rare-sense half is §2-GATED (the contextual-encoder owner decision — rec HOLD).**
5. **Downstream signal-updates — feed the new event-TYPE signal (from the causal landing) to its 3 consumers:** causation_typing → mental typing (3/16→16/16); affect_register → OCC-appraisal inferred-emotion channel; goal-graph → motivational spine. And mine the mental-bridge gold (the missing scored instrument).

**Land-ready now, verdict-permitting:** the arc-labeler fast path (~54% whole-read cut — land the moment it's owner-DONE).

**Then:** a fresh FULL board with everything integrated = the realized aggregate (the number we've never measured end-to-end). Every claimed gain in the scoreboard above should have a board dim that moved by then, or a named reason it can't (invariant / instrument gap / located negative).

**Gating owner decision:** §2/P9 — HOLD the no-transformer invariant (strategy rec) vs relax for one offline contextual encoder. This gates the entire FINE rare-sense / contextual-meaning cluster (steps 4's fine half). The within-invariant work (steps 0–5) proceeds regardless.
