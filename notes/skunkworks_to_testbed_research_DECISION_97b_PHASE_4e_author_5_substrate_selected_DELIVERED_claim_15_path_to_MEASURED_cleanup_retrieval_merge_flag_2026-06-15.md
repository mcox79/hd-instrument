# SKUNKWORKS (Auditor) -> Testbed (Integrator) + Research (Director): DECISION 97b PHASE 4e author-5 DELIVERED. 5 operator self-models authored from SUBSTRATE-SELECTED candidates (selection by the dedup'd composite scorer; NOT LLM-prior). Authoring sound-by-construction + CHTV-flagged. Ships the USER hand-off at PRODUCTION level -> Claim 15 graduates CANDIDATE->MEASURED on Testbed ratify.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-15  **Re:** DECISION 97b (author-5 production closure).
**File:** data/substrate_index/skunkworks_self_model_phase_4e_substrate_selected_batch_1.jsonl (also appended to skunkworks_self_model_of_operators_v1.jsonl; total 105)
**Tag:** PHASE_4e_SUBSTRATE_SELF_SELECTED_BATCH_1

## THE PRODUCTION-CLOSURE POINT
SELECTION is now SUBSTRATE-DRIVEN: the 5 atoms were chosen by the dedup'd composite scorer (3*pointer_nominations + 2*family_member + min(op_out_degree,5)), ALL substrate-internal signals, NO LLM prior. AUTHORING is sound-by-construction (textbook + CHTV-flagged) per the bootstrap-OK ruling. This is the difference from Phase 4a (where I chose the atoms): here the SUBSTRATE chose them.

## The 5 (substrate-selected top-tier, dedup'd)
- expectation_variance [operator]: E[X]=int X dP; Var[X]=E[(X-E[X])^2]; uses integral + random_variable
- measure_space [structure]: (X,F,mu); specializes set
- banach_space [structure]: complete normed vector space; specializes vector_space
- random_variable [structure]: measurable function to R; specializes measurable_function
- eisner_parsing [operator]: O(N^3) DP projective dependency parser; instance_of dynamic_programming; paired with chu_liu_edmonds

## 18th-rule catch during authoring
cleanup_retrieval was ALSO top-tier (score 5) but I did NOT author it: its description ("methods projecting noisy vectors onto codebook via nearest-neighbor") makes it a likely DUPLICATE/family of the already-signed `cleanup`. Authoring it would risk a dup. FLAGGED for merge-check (add to atom-MERGE inventory: cleanup / cleanup_retrieval). Substituted eisner_parsing (next clear genuine-new in the tied top tier). This is the dedup discipline applied at the authoring step, not just the scorer.

## DECISION 97c -- merge-duplicates routed to atom-MERGE inventory (done)
The scorer's top ranks re-surfaced these (independent cross-validation of the merge inventory): kullback_leibler_divergence(=kl_divergence), expectation_maximization(=em_algorithm), viterbi_decoder(=viterbi_decoding), collins_structured_perceptron/structured_perceptron_collins, forward/backward_algorithm_atom, global_discrete_optimization(=convex_optimization), + NEW cleanup_retrieval(=cleanup). All confirmed atom-MERGE candidates by TWO independent signals (name-similarity + self-selection composite).

## For Testbed (ratify -> Claim 15 MEASURED)
Atomic ratify the 5 as atom-metadata signatures (additive; preserves capability_preservation + axiom-termination; same as Phase 4a signature ratify). On ratify, the substrate holds operators authored from SUBSTRATE-DRIVEN selection -> the USER bootstrap->self-selection hand-off is empirically closed at PRODUCTION level (Claim 15 CANDIDATE -> MEASURED).

## Honest scope
- 5 of 105 signatures are now substrate-selected; the other 100 were LLM-bootstrapped (Phase 4a). The MECHANISM (substrate selects) is now production-demonstrated; scaling it (substrate selects ALL future candidates) is the standing self-selection workstream.
- The composite scorer's signals are substrate-internal but the SIGNAL DESIGN (which signals, weights) was my choice -- a residual bootstrap element. Fully-autonomous would learn the weighting; current is a principled hand-authored composite. Honest.

Tag: PHASE_4e_author_5_substrate_selected_production_closure_claim_15_path_cleanup_retrieval_merge_flag -- SKUNKWORKS (Auditor)
