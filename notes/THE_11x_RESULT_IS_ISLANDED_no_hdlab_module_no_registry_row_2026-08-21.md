# **THE BEST RESULT IN THIS BODY OF WORK IS ISLANDED** -- NO `hdlab/` MODULE, NO REGISTRY ROW, AND THAT IS WHY THE OWNER HAD TO REMEMBER IT INSTEAD OF FINDING IT

**The capability:** placing a brand-new word relative to the grounded foundation, **11x better on the
hard (non-decomposable) population** -- opaque exact **0.0262 -> 0.2930**, abstention **31% -> 3.7%**,
with `SCRAMBLE / RANDOM / POP / GRAPH_SELF_REFERENCE` all collapsing to floor. **Genuinely good work.**

---

## 1. WHERE IT LIVES -- ENUMERATED FROM DISK, THEN RECONCILED

| check | result |
|---|---|
| `hdlab/cold_placement*.py` | **DOES NOT EXIST** |
| `hdlab/` modules matching cold/placement/frontier | **none** (only `wordnet_polarity_propagation.py`, unrelated) |
| implementation location | **`experiments/exp_cold_placement_usefulness_v1.py` and `..._recovery_opt_v1.py` -- experiment scripts** |
| **`data/capability_registry.jsonl` rows** | **0 of 208** matching `cold`, `placement` or `frontier` |

*(The `multihop` rows that do exist -- `sr_routing_multihop`, `multi_hop`, `modern_hopfield_readout`
-- are different work.)*

**➡️ THE MECHANISM WAS NEVER PROMOTED. IT EXISTS ONLY AS EXPERIMENT CODE, AND THE REGISTRY DOES NOT
KNOW IT HAPPENED.**

*WordNet is NOT the gap -- `hdlab/` imports it in at least 10 modules. **The gap is the placement
mechanism itself.***

## 2. 🎯 **THIS EXPLAINS THE ORIGINAL QUESTION**

The owner wrote: *"we did a bunch of work on this I believe... **You should look for those
experiments.**"* -- **and had to say it twice, adding detail the second time, because the first
description did not lead me to it.**

**That is what an islanded capability costs, precisely.** The registry-first check -- the one
`CLAUDE.md` names as the standing gate -- **returns NOTHING for this work.** It is findable only by:
1. an owner remembering it, or
2. a keyword query against the RESULTS archive with the right word (`"cold placement"`, 4 cells).

***The system's own inventory of what it can do omits one of the better things it has ever done.***

## 3. THE PATTERN, AND IT IS TONIGHT'S THIRD INSTANCE

| capability | built | works | wired |
|---|---|---|---|
| `gap_driven_reader` (what to read next) | ✅ | ✅ HARD_PASS | 🔴 **zero callers** |
| **cold placement (where a new word goes)** | ✅ | ✅ **11x on the hard case** | 🔴 **no module, no row** |
| banked word meanings | ✅ | 32% MEANINGFUL | 🔴 **nothing reads them** |

**THREE SEPARATE, WORKING, CONTROLLED CAPABILITIES THAT NOTHING CONSUMES.** *`MEMORY.md` states the
rule -- **"every gain LOCKED into the substrate, not left in experimental results"** -- and this is
the third violation found tonight, all in different subsystems.*

**It is also a correction to my own framing from earlier tonight.** I described the substrate as *"a
library, a librarian and a filing system with no reader."* **That was too kind.** The more accurate
version: **several finished instruments, each demonstrated to work, none plugged into anything.**

## 4. WHAT I AM *NOT* CLAIMING

**Not that promoting it is easy.** The cells run against a 141,511-node graph with WordNet tiers;
what a live-path version needs is unexamined and could be substantial.
**Not that it would survive on the live path.** It has only ever been measured in its own harness --
and tonight's foraging result is the standing warning about how badly a controlled result can
degrade when it meets the real loop.
**Not that the registry is broken.** The registry is accurate about what was registered; **nobody
registered this.**

## TLDR

The best piece of work in the area you remembered — the one that got eleven times better at placing
hard words — **exists only as two experiment scripts.** There is no version of it inside the system
itself, and our own catalogue of capabilities has **no entry for it at all**, across 208 entries.

**That is exactly why you had to remember it, and why your first description didn't lead me to it.**
The standard way to check "do we already have something that does X" is to search that catalogue —
and for this, the catalogue says nothing. It's findable only if a human recalls it or someone guesses
the right search term against the results archive.

**And it's the third time tonight I've found this same shape:** a capability that was built, tested
with proper controls, shown to work — and then connected to nothing. The "what should I read next"
component has zero callers. The stored word meanings are read by nothing. Now this.

I described the system earlier as a library with no reader. **That was too generous.** It's closer to
a workshop full of finished, working instruments, none of them plugged in.

**What I'm not saying:** that plugging this one in would be quick, or that it would survive contact
with the real reading loop — tonight already showed how badly a clean result can degrade there. And
the catalogue isn't broken; it's accurate about what was put in it. **Nobody put this in.**

## QUESTIONS

None.

## NEXT STEPS

1. **Register it either way** -- `WIRE` or `SHELVE` with revival criteria. **Its current state,
   neither, is the thing the gate exists to prevent.**
2. The hop profile on the optimised method remains the cheapest open measurement.
3. **Three islanded capabilities is a pattern, not three incidents** -- worth one deliberate pass
   rather than three separate rescues.
