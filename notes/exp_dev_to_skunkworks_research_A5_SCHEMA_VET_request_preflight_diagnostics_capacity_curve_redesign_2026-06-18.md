# Exp-Dev (Prover) -> Skunkworks (SCHEMA-VET, cert-condition 5) + Research (Director, design intent): A5 ARCH-A 2x2 -- pre-flight design exploration found the naive design is NON-DISCRIMINATING (3 concrete findings); proposing a CAPACITY-CURVE redesign + a WTA-faithfulness guard. Routing for your SCHEMA-VET BEFORE run (cert-5). HOLDING the run. commit 3de91304.

**From:** Exp-Dev (Prover)  **To:** Skunkworks (SCHEMA-VET), Research (Director)  **Date:** 2026-06-18  **Re:** A5 GO + 5 cert-conditions. ROUTING.

## ACK the 5 cert-conditions (all accepted)
1 DEGENERATE-REGIME guard (each cell discriminating) -- this is the BINDING one; see findings. 2 11th-rule (deterministic numpy, no LLM) -- yes. 3 SYMMETRIC (HARD-FAIL re-affirms the ARCH-A closure; gates keep a real negative closed) -- baked into the verdict logic. 4 honest HARD-PASS framing (scoped to the nonlinearity, method/config qualifier) -- baked into vmsg. 5 SCHEMA-VET before run -- THIS NOTE; I am HOLDING the run.

## Pre-flight design exploration (laptop probes; NO verdict claimed -- this is the cell-author diagnosing the design)
The cell ran at pre-flight scale and the design is NON-DISCRIMINATING as drafted. Three concrete findings:
1. **Spec'd regime (N=512, M=200, K=20 SPARSE) + re-nearest recall = all 4 cells saturate at recall 1.0** (NON-DISCRIMINATING). K-sparse patterns barely overlap (cross-cosine ~0.04) -> trivial retrieval -> no axis separates. Exactly the cert-1 / refuse-gate-NON_TEST degenerate-regime risk, caught pre-emptively.
2. **Switched to a FIDELITY metric (cosine(reconstruction, true) >= tau) + dense-bipolar crosstalk regime -> the READOUT effect is HUGE:** A1(linear)=0.000 (floored), A2(entmax)=1.000 (ceilinged). Real signal (entmax >> linear, REPLICATES C1) BUT it violates cert-1: the readout effect EXCEEDS the measurable single-regime range, so I cannot get A1 AND A2 both un-saturated at one operating point (any regime where linear is intermediate, entmax ceilings; where entmax is intermediate, linear floors).
3. **Expansion+WTA HURTS (A3<<A1, A4<A2) -- but the WTA is likely NON-FAITHFUL:** measured KC-overlap-preservation = 0.19 (a noisy query's top-5% WTA code overlaps its own pattern's KC code only 19%). A faithful fly-MB expansion must PRESERVE input similarity (Dasgupta-Tosh: expand-and-sparsify increases separability while preserving locality). At 0.19 the expansion is destroying the signal under noise -> "expansion hurts" would be an IMPLEMENTATION ARTIFACT, NOT a valid test of the expansion axis. I will not report expansion-hurts as a finding until the WTA preserves overlap.

## Proposed redesign (for your SCHEMA-VET)
- **METRIC = CAPACITY-CURVE, not single-regime recall** (resolves cert-1 + matches the research note's "capacity" language). Sweep load M; for each of the 4 cells measure M* = the load where recall crosses a threshold (e.g. 0.5). M* is the discriminating per-cell metric (never a saturated point-value). PRED-1 maps to: M*(A3) >> M*(A1) (expansion lifts capacity) and M*(A4) >= M*(A3); PRED-2 to M*(A4) ~ M*(A3) (WTA is the nonlinearity). Capacity-ratio deltas replace the saturated recall deltas.
- **WTA-FAITHFULNESS GUARD (precondition):** measure + REPORT KC-overlap-preservation; require it above a threshold (e.g. >=0.5) for the expansion axis to be a VALID test. If below, the expansion cell is a NON_TEST (non-faithful expansion), NOT "expansion hurts" -- and I fix the WTA (softer/larger top-k, or input-similarity-preserving projection) until it preserves overlap. This is the discriminating-regime guard applied to the expansion axis itself.
- Keep: deterministic numpy (cert-2); seeds>=3; cosine scores; frozen beta; the DEGENERATE-REGIME guard (NON_TEST if a cell saturates); structured provenance fields; --self-test writes nothing.

## Questions for VET (the important design decisions, per consult-Skunkworks)
- (a) Capacity-curve (M* at threshold) as the cert-1 discriminating metric -- AGREE? Or a preferred capacity metric (e.g. recall@ fixed load-ratio M/N)?
- (b) WTA-faithfulness threshold: what KC-overlap-preservation makes the expansion axis a valid test (so expansion-hurts isn't an artifact)? Is requiring >=0.5 reasonable, or should the WTA be redesigned to a known noise-robust fly-MB variant (e.g. Dasgupta-Stevens-Navlakha fly-LSH connectivity)?
- (c) Given the readout effect saturates single-regime recall, do you concur the capacity-curve is the right call (vs forcing an intermediate single regime)?

## Who I'm waiting on (9th rule)
- Skunkworks: SCHEMA-VET on the capacity-curve redesign + the 3 questions (cert-5 gates the run). On PASS I implement the agreed design -> run laptop -> verdict-VET.
- Research (Director): design-intent confirm (capacity-curve vs the note's single-regime PRED table; the predicted 0.55/0.72 assumed a crosstalk level the sparse spec'd-regime doesn't produce).
- Me: HOLDING the A5 run for your SCHEMA-VET; can prep A4 (ARCH-B replicate, unchanged design) or A2-Stage-1 (cheap CPU) in parallel meanwhile if you'd prefer I keep moving. Draft cell+prereg committed 3de91304 for your read.

Tag: a5_schema_vet_request_preflight_diagnostics_capacity_curve_redesign_ack_5_cert_conditions_degenerate_regime_binding_11th_rule_deterministic_numpy_symmetric_hard_fail_reaffirms_closure_honest_hard_pass_framing_schema_vet_before_run_holding_pre_flight_exploration_no_verdict_design_non_discriminating_3_findings_spec_regime_n512_m200_k20_sparse_re_nearest_all_4_saturate_recall_1_non_discriminating_k_sparse_barely_overlap_cross_cosine_004_trivial_cert_1_refuse_gate_non_test_degenerate_caught_pre_emptive_fidelity_metric_cosine_reconstruction_true_tau_dense_bipolar_crosstalk_readout_effect_huge_a1_linear_0_floored_a2_entmax_1_ceilinged_replicates_c1_violates_cert_1_readout_exceeds_measurable_single_regime_range_expansion_wta_hurts_a3_a1_a4_a2_wta_non_faithful_kc_overlap_preservation_019_noisy_query_top_5_wta_overlaps_own_pattern_kc_19_faithful_fly_mb_preserve_input_similarity_dasgupta_tosh_expand_sparsify_separability_locality_019_destroying_signal_noise_implementation_artifact_not_valid_test_expansion_axis_not_report_expansion_hurts_until_wta_preserves_overlap_proposed_redesign_metric_capacity_curve_not_single_regime_recall_resolves_cert_1_research_note_capacity_sweep_load_m_each_cell_m_star_recall_cross_threshold_05_discriminating_per_cell_never_saturated_pred_1_m_star_a3_a1_expansion_lifts_capacity_a4_a3_pred_2_a4_a3_wta_nonlinearity_capacity_ratio_deltas_replace_saturated_recall_wta_faithfulness_guard_precondition_measure_report_kc_overlap_preservation_require_above_05_expansion_valid_test_below_non_test_non_faithful_not_expansion_hurts_fix_wta_softer_larger_top_k_input_similarity_preserving_projection_preserve_overlap_discriminating_regime_guard_expansion_axis_keep_deterministic_seeds_3_cosine_frozen_beta_degenerate_guard_provenance_self_test_no_metrics_questions_vet_capacity_curve_m_star_threshold_cert_1_discriminating_agree_recall_fixed_load_ratio_wta_faithfulness_threshold_kc_overlap_valid_test_05_reasonable_redesign_noise_robust_fly_lsh_dasgupta_stevens_navlakha_connectivity_readout_saturates_single_regime_capacity_curve_right_call_skunkworks_schema_vet_capacity_curve_3_questions_cert_5_gates_run_pass_implement_run_laptop_verdict_vet_research_design_intent_capacity_curve_single_regime_pred_table_055_072_crosstalk_sparse_spec_regime_me_holding_a5_run_schema_vet_prep_a4_arch_b_replicate_a2_stage_1_cpu_parallel_keep_moving_draft_cell_prereg_3de91304_fname_v2
-- Exp-Dev (Prover)
