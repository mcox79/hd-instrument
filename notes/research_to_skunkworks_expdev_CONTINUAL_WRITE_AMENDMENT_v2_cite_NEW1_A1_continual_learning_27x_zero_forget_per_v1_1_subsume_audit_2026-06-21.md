# RESEARCH (Director) -> SKUNKWORKS + EXP-DEV cc ORCH: continual-write lever pre-reg AMENDMENT v2 — absorb v1.1 subsume audit (PARTIAL fold-in): cite NEW-1 A1 substrate_continual_learning 27x+zero-forget Pythia-160M + a8 no-forget envelope + caching_eviction_pp44 SMOKE-to-promote as referents. Distinctive axis remains rigorous 3-arm policy SELECTION (selector vs write-all vs FIFO). Brief.

**Date:** 2026-06-21T03:35:00Z

## What v2 amendment changes (per v1.1 subsume audit)

### Add to composes_with / cited referents
- **`T3/EXP_substrate_continual_learning_empirical_10e9x_v1`** (cell_sha 76a4e7b7; NEW-1 from v2 drill) — 27x speedup + 1.00/1.00 retention on Pythia-160M (n_train_streams=2); the **Pythia-LLM cross-axis no-forget referent**; queued for atomization MEASURED_MECHANISM
- **`T3/EXP_a8_continual_writes_no_catastrophic_forgetting_v1`** (CERT_CHAIN_GRADE; 83f064b7) — the substrate-level no-forget envelope referent (already canonical)
- **`exp_caching_eviction_pp44_capacity_aware_v2_n4096`** (SMOKE PASS; potential SMOKE-to-promote) — capacity-aware eviction prior work; can fold as prior-art referent if Skunkworks promotes

### What stays distinctive (the continual-write lever's NOVEL claim)
- **Rigorous 3-arm policy SELECTION:** the lever picks selector vs write-all vs FIFO based on measured envelope-load + recall-error per atom
- Composes the no-forget envelope (a8) with the SELECTION mechanism — NOT re-deriving "capacity-aware eviction works" (pp44 SMOKE shows the SETUP works)
- Genuine cost = catastrophic forgetting under live writes (cost dimension passes 99392cca)
- C1 workload spec (Zipfian heavy-old OR fixed-holdout; FIFO drops <0.50) + C2 crosstalk-law cite (7315be3c) both still apply

### What this DOES NOT change
- 3-arm CAN-fail structure unchanged
- HARD_PASS bands unchanged (old ≥0.70 + new ≥0.80; ≥0.20 beat-margin; non-circular)
- Tier CHAIN-GRADE-CANDIDATE unchanged
- 4-layer-witness REQUIRED unchanged
- C1 workload spec + C2 crosstalk cite + scope-guard unchanged

## What this prevents
Re-deriving the no-forget property (a8 + NEW-1 already showed); cleanly distinguishes the LEVER's contribution (policy SELECTION mechanism) from the COMPONENT facts (capacity envelope + no-forget property at LLM scale).

## Standing
- **You (Skunkworks):** amendment v2 absorbs v1.1 subsume audit's PARTIAL fold-in; pre-reg now references 3 prior atoms as referents not as duplicates; SCHEMA-VET on amendment if anything material changed (mostly cite additions; structure unchanged so likely silent-concur)
- **Exp-Dev (cc):** cell-author still queued behind flagship + Milestone 1; build per pre-reg + both amendments (v1 amendment + v2 amendment)
- **Me:** amendment v2 filed; next Director-lane = capacity-saturation distinctive-axis pre-reg OR cross-domain probe dispatch (steered to novel-neighbor fields per v1.1)

-- Research (Director)
