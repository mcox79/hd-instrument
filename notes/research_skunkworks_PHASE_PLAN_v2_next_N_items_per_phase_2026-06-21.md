# PHASE PLAN v2 -- canonical forward-looking work-queue (ranked by ENABLING impact). JOINT: Skunkworks ranks; Research synthesizes.

**Program-priority constraint (USER-LOCKED):** capability DEVELOPMENT is the goal, cert-grade is the INSTRUMENT. ENABLING = highest downstream-unlock ("what builds on this?"). Axes: composition / storage / KG / continual. Refuse-gate + positioning are NOT enabling. Glass-box-LLM continues.

---

## SKUNKWORKS RANKING (input -- grounded in backlog-cert-landscape + composition graph; Director synthesizes/structures)

### Phase 0 -- capacity-dimension map
1. **sparse-onset higher-LOADS** (queued, my BUILD_GO 295882fa) -- locates crosstalk-onset boundary alpha_c(f); unlocks honest capacity-budgeting for storage sizing. **S | MM-extension** (refines a3f473dd).
2. **denser/2-level-ingest composed-reasoning LIFT** (NEW -- surfaced in my sub-audit batch 1): b_alpha_broad_v3_2level shows 2-level ingest lifts multi-hop reasoning 0->3 HARD_PASS vs the canonical envelope (eliminates the depth-cliff). Unlocks the KG/composition axis (ingest-density as a reasoning lever). **M | chain-grade-eligible IF held-out + can-fail** (does the lift generalize, not memorize?). Routed to you for a revival/scale-up drill.
3. **capacity-saturation ceiling at scale** -- crosstalk-law c is currently UNBOUNDED (7315be3c); map where capacity actually saturates N->large. Unlocks honest storage-sizing for the glass-box. **M | MM**.
- *147-to-classify honest read:* mostly GENUINE non-PASS (MIDDLE/FAIL = honest bounds/negatives), NOT hidden PASS-family -- the sub-audit's value is PRECISION + buried-positive discovery (e.g. #2 above), NOT recovering cert count toward 440. Don't bank on a big upward recovery.

### Phase 1 -- runtime levers (3 chain-grade shipped; production substantively done)
1. **Continual-write lever** (the continual axis -- only 5 chain-grade atoms here, the THINNEST enabling theme) -- when to consolidate/evict to preserve old facts under new writes. Unlocks the substrate as a LIVE store -- EVERYTHING downstream (KG growth, glass-box training) needs non-destructive continual write. **M | chain-grade-eligible** (genuine cost = catastrophic forgetting -> real selection problem, passes the lever-design discipline). **The most-enabling 6th lever.**
2. **Storage-eviction lever** (composes with #1; capacity-aware eviction under the a3f473dd envelope). **M | chain-grade-eligible**.
3. **NOT a KG-completion lever** -- flagged: per the inference-transfer design (exact-closure is perfect-by-construction; KG value is the refuse-gate not completion), a KG-completion lever is a WEAK chain-grade candidate. Don't queue it as enabling.
- *Recommendation:* ship continual-write (#1) as the 6th lever, then shift bandwidth to Phase 3. Phase 1 is otherwise done.

### Phase 2 -- 104-value-trove onboarding
- *backlog-cert-landscape RAN (for you):* enabling-theme sub-cert pull-up populations = composition 346 / sparse 318 / capacity 309 / KG 112 candidates (mostly LEGACY_EXCERPT/SMOKE_ONLY). **Caveat:** I cannot surface a "104-value cluster" tag directly -- the tool surfaces THEME clusters (composition/capacity/sparse/KG/continual/drift), which is the useful proxy. Enumerate the 104 against these themes.
1. **composition + sparse pull-ups WITH an existing can-fail design** -- the highest-leverage Phase-2 move: re-verify the sub-cert composition/sparse records that ALREADY have a discriminating regime (recovers GENUINE enabling chain-grades, not by-construction ones). **per-item S | chain-grade-eligible** (gated on DISCRIMINATING-REGIME each).
2. **continual + capacity pull-ups** -- thinner but directly feed Phase-1 continual-write + Phase-0 ceiling. **M**.
- *Discipline:* every pull-up needs a CAN-fail regime (my standing rule) -- most LEGACY/SMOKE candidates will NOT qualify without a re-run; don't mass-promote.

### Phase 3 -- glass-box-LLM (Milestone 1 pending pythia desat)
1. **Sparse-projected-KV (NEW milestone)** -- compose CERT 591 (learned key-projection, decrowds keys) + a3f473dd (sparse super-capacity, 8-20x Willshaw): project keys to decrowd, THEN sparse-encode for capacity. Unlocks the STORAGE FOUNDATION for the full glass-box at scale (many more facts/unit). **L | chain-grade-eligible.** TOP new enabling item -- composes 2 landed certs; CERT 591's own note flags "build Hebbian-superposition capacity on PROJECTED keys."
2. **M3 sparse-encoded substrate-KV** (of named M2-5, the most-enabling) -- capacity foundation everything else builds on; subsumes/overlaps #1. **L**.
3. **M2 multi-hop chain queries** -- builds on LEVER #4 depth-refuse + composition. **M**.
- *Ranking M2-5 by enabling:* M3 (sparse-KV capacity) > M2 (multi-hop) > M5 (full glass-box, depends on all) > **M4 refuse-gate integration = NOT-enabling (operational), deprioritize** per the USER rule.
- *New milestone given the toolkit:* #1 sparse-projected-KV (CERT 591 + a3f473dd). The 2-axis OOE refusal (#5b load + #4 depth) is SAFETY infra, not an enabling milestone -- keep it as a gate ON the glass-box, not a milestone.

### Cross-phase enabling thread (my one-line synthesis)
The critical enabling chain is **STORAGE**: a3f473dd sparse super-capacity + CERT 591 projected keys -> sparse-projected-KV (P3 #1) -> continual-write lever (P1 #1) -> a live, capacity-budgeted, non-forgetting substrate store = the glass-box foundation. Composition/KG ride on top of that store. Prioritize the storage chain over breadth.

-- Skunkworks (cert-owner / substrate-state)
