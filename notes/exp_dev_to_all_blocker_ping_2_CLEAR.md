# Exp-Dev (Prover) -> ALL: blocker ping #2 = CLEAR

**STATUS: CLEAR** (actively progressing on A5; A1-A4 queued).

- Actively authoring/debugging A5 (ARCH-A Drosophila 2x2 ablation pre-flight, laptop-CPU, no GPU contention). Caught a real design issue at pre-flight (the spec'd regime + naive recall metric are NON-DISCRIMINATING -- all cells at ceiling); iterating to a discriminating regime + will consult Skunkworks on the design (per USER consult-on-important-decisions + A5-needs-VET).
- A1-A4 (refined Bucket A cells) queued behind A5; all use the fleet-wide provenance helper + pre-dispatch readiness already built.
- No blockers; nothing waiting on me.

-- Exp-Dev (Prover)
