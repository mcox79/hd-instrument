# RESEARCH (Director) -> USER: 40h TACK-ON joint PROPOSAL (Director + Skunkworks; per prior 3-sprint workflow). Skunkworks-SHARPENED character: EXTEND + INTEGRATE-WITHIN-THE-ITEM-1-BOUND + DURABILITY (detect-yes, auto-fill-DEFER). 4 STRUCTURAL CALLS (capability-cluster METADATA-FIRST not AtomKind; HYP-5 REDESIGN as DEPTH-CEILING discriminating; self-heal manifest DETECTION-yes/auto-re-derive-DEFER per A5 lesson; A/B-iterate needs optimal-per-evidence cert-VET BAKED IN) + 4 MISSING-ITEMS (M1 HYPERNYM held-out replication; M2 A/B-iterate cert-VET guard; M3 DURABILITY CRON integrated; M4 WRITEUP is first narrative-data-consistency proving-ground) + cert-VALUE-FIRST REORDER (top: C-deferred A2 v6 + HYPERNYM-replication + WRITEUP + durability-cron). Bundled GO ask at the end.

**From:** Research (Director)  **To:** USER  **Date:** 2026-06-19  **Re:** 40h tack-on joint PROPOSAL. ASCII; fname_v2.

## Bottom line (one sentence)

Skunkworks's sharpen upgraded my draft from "complete + extend + USER framing" to **EXTEND + INTEGRATE-WITHIN-THE-ITEM-1-BOUND + DURABILITY (detect-yes, auto-fill-DEFER)** -- the substrate's Item-1 HONEST_NEGATIVE empirical bound (lever = coverage-completion, NOT reasoning) becomes the load-bearing anchor every integration item must carry; the WRITEUP, capability-clusters, durability cron, and the A/B-iterate framing all bind to it explicitly; the 40h's biggest cert-risk (PROLIFERATION + over-claim drift) is guarded by 4 structural calls + 4 missing-items + a cert-VALUE-FIRST reorder.

## Character (Skunkworks-sharpened)

**EXTEND + INTEGRATE-WITHIN-THE-ITEM-1-BOUND + DURABILITY (detect-yes, auto-fill-DEFER).**

Two cert-guards the 40h MUST carry:
1. **The Item-1 bound is now LOAD-BEARING:** every integration item must carry "coverage-completion, NOT reasoning." The 40h must NOT drift back to over-claiming the substrate as a reasoner. Item-1's HONEST_NEGATIVE is the anchor.
2. **Anti-proliferation:** 40h tempts new AtomKinds + many atoms. INTEGRITY favors DEPTH (rigorous cert on load-bearing items) over BREADTH. Guard new-structure additions.

## 4 structural calls (Skunkworks; cert-architecture-load-bearing)

**(C1) Capability-cluster NEW AtomKind = DEFER; METADATA-FIRST.** Phase_portrait precedent was a structurally-DISTINCT inventory object. A capability-CLUSTER is a GROUPING-relation over existing capabilities (by shared benchmark + interface). Add as METADATA first (cluster_id + shared_benchmark + interface_contract on existing capability/capability_map atoms); promote to AtomKind ONLY when the grouping is demonstrated load-bearing (needs its own cert-status / algebra / relations that metadata cannot carry). Mirrors the engine/checklist mechanizability logic: don't add structure until the need is demonstrated. (3 NEW AtomKinds added recently -- proliferation guard.)

**(C2) HYP-5 = REDESIGN as DEPTH-CEILING DISCRIMINATING test, NOT a 4th coextensive recovery.** A coextensive depth-5 completion-recovery is MEASURED_MECHANISM + LOW marginal value. The HIGH-value version, now that Item-1 established the coverage-bound: does the coverage-completion lever EXTEND to depth-5 (recover depth-5 QA) OR hit an inherent DEPTH-CEILING? Design as non-coextensive / ceiling-probing -> cert-grade-DISCRIMINATING (depth-ceiling finding bounds the lever's reach further). Coextensive HYP-5 stays MEASURED_MECHANISM + low-value.

**(C3) Self-heal manifest: gap-DETECTION = ADOPT (read-only); auto-RE-DERIVE = DEFER (cert-unsound now).** Manifest as read-only gap-DETECTOR composes with cert-FLOOR (FLOOR detects drift; manifest detects MISSING atoms). Auto-re-derive is premature + cert-risky: re-atomize recomputes provenance_quality (A5 lesson: re-tiers stale records; nearly re-certified a REFUTED record once) AND reproducibility chain is not guaranteed (cell_sha staled by rewrite; inputs may have changed). So: detection yes; auto-fill NO -- re-derive stays DELIBERATE cert-owner action with re-tier-guards (flag-the-gap, don't auto-fill). The A5-no-silent-recompute discipline.

**(C4) A/B-iterate triangle holds cert-architecturally IF it carries optimal-per-evidence cert-VET (M2 below) + the Item-1 bound.** Clusters define interfaces (metadata-first); portrait inventories regimes per cluster (INVENTORY_NON_CERT); WRITEUP integrates BOUNDED by Item-1. Sound, with the cert-VET guard.

## 4 MISSING-ITEMS (Skunkworks; what I wasn't seeing)

**(M1) HYPERNYM held-out replication (Design-B on HYPERNYM) -- HIGHEST VALUE addition for the WRITEUP's central claim.** Item-1 showed PART_OF coverage does not transfer (held-out null). Is the bound PART_OF-specific or GENERAL? Replicate Design-B on HYPERNYM (the denser relation). If HYPERNYM ALSO yields a held-out null, the bound is ROBUST across relations (coverage-not-reasoning GENERALIZES) -- a strictly stronger cert-grade input for the WRITEUP than a single-relation bound. Cheap (Design-B cell generalizes; swap relation). Cert tier: CERT_CHAIN_GRADE HONEST_NEGATIVE if null again, OR a SURPRISE if HYPERNYM transfers -- both load-bearing.

**(M2) The A/B-iterate framework needs the optimal-per-evidence cert-VET BAKED IN from day 1.** If we "A/B-iterate capabilities" (swap implementations, measure vs benchmark), the cert-risk is a new implementation silently labeled "better" without cert-grade comparison -- exactly the RULE_optimal_per_evidence_cert_vet_discipline. So capability-cluster framework MUST embed: any "B better than A" within a cluster gets cert-grade-comparison VET (cert-grade-better / honest-scope / not-coextensive / not-over-strong-label / discriminating). Without this guard, A/B-iterate vision becomes an over-claim engine. **Cert-integrity precondition for the whole direction.**

**(M3) DURABILITY CRON (integrated), not 3 separate tools.** Daily-snapshot + invariant-check + manifest-gap-detection should run TOGETHER as ONE daily durability cron: snapshot Store + run invariant-check (TRUE-HARD + hygiene) + run manifest-gap-detection + flag drift. The integrated durability layer (cert-FLOOR + backup + gap-detection automated). One cron, not three tools.

**(M4) The WRITEUP is the FIRST real application of the Item-11 narrative-data-consistency SCHEMA-VET.** Skunkworks's framing-VET will check (beyond the 4 binding conditions) that the WRITEUP's OWN claims match its cited evidence (every "X" claim resolves to a cert-grade source atom with matching data -- the narrative-data-consistency gate applied to the integration artifact itself). The WRITEUP is the proving-ground for that gate.

## Cert-VALUE-FIRST REORDER (Skunkworks)

**TOP (cert-load-bearing):**
1. **C-deferred A2 v6 on 43892** -- closes A-now/C-deferred cert-chain at scientifically-complete CERT_CHAIN_GRADE (cell exists; just remote-sync + verdict-VET). Cleanest cert-grade closure.
2. **HYPERNYM held-out replication (M1)** -- hardens Item-1 bound to multi-relation-robust; highest-value addition for WRITEUP central claim.
3. **WRITEUP** -- integration anchor; bound to Item-1 + 4 binding conditions + narrative-data-consistency gate (M4). HIGH-value, HIGH-risk -> strongest framing-VET.
4. **DURABILITY CRON (M3)** -- cert-FLOOR + backup + gap-detection integrated automated.

**NEXT:**
5. HYP-5-as-depth-ceiling (C2 redesigned)
6. Phase-portrait v2
7. Capability-cluster METADATA + M2 cert-VET guard
8. ConceptNet apply

**OPPORTUNISTIC / AT-BANDWIDTH:**
9. Item-4 reconcile (Skunkworks's dup-instances + my dispositions)
10. AUDIT_LESSON v2 taxonomy
11. Cross-domain analogy prep (may dead-end)

## Honest scope (Skunkworks-confirmed)

Bandwidth: 5-6 pre-emptive VETs (HYPERNYM-replication cell + HYP-5 cell + Phase-portrait v2 + ConceptNet apply + durability-cron) + 2 framing-VETs (WRITEUP + capability-cluster-metadata) + reactive. Manageable per prior pattern.

Cert-growth potential (per Skunkworks's framing):
- **C-deferred A2 v6 grown-corpus:** CERT_CHAIN_GRADE if bands met (1)
- **HYPERNYM held-out replication:** CERT_CHAIN_GRADE HONEST_NEGATIVE if null (likely) OR HARD_PASS-like SURPRISE if transfers
- **HYP-5 depth-ceiling:** CERT_CHAIN_GRADE DISCRIMINATING if non-coextensive design
- 3 cert-grade candidates is HEALTHY scope for 40h

## Bundled GO ask (composing prior 3-sprint workflow)

**Director ask 1:** GO on Top-4 (C-deferred A2 v6 + HYPERNYM-replication + WRITEUP + durability cron) -- cert-load-bearing + integrity-first.

**Director ask 2:** GO on Next 4 (HYP-5-as-depth-ceiling + Phase-portrait v2 + capability-cluster-METADATA-first-with-M2-cert-VET + ConceptNet apply) -- extend-within-bound + the A/B-iterate triangle materialized.

**Director ask 3 (opportunistic):** lull-fill 9/10/11 as natural openings.

**USER acknowledgements (no decisions needed; just visibility):**
- **C1 (capability-cluster METADATA-first):** Skunkworks's anti-proliferation guard accepted (3 NEW AtomKinds added recently); promote to AtomKind ONLY when need demonstrated.
- **C2 (HYP-5 depth-ceiling):** original HYP-5 plan was marginal coextensive; redesign as discriminating ceiling-probe materializes cert-grade value.
- **C3 (manifest detection-yes/auto-fill-defer):** auto-re-derive composes with your A5 lesson on no-silent-recompute; correct guard.
- **C4 (A/B-iterate + cert-VET):** your prior conversational A/B-iterate framing requires the cert-integrity precondition; M2 makes it explicit.

## What launches NOW under FULL AUTO if you GO

- **Director:** WRITEUP v1 DRAFT already routed for Skunkworks framing-VET (data/writeup_v1_substrate_as_reasoning_engine_DRAFT_pre_skunkworks_framing_VET.json; gitignored locally); reactive on framing-VET; Item 4 catalog dispositions reactive on Skunkworks; capability-cluster METADATA design after C-deferred + HYPERNYM-replication land.
- **Exp-Dev:** HYPERNYM held-out replication cell (mirrors Item 1 Design B; swap relation); HYP-5 depth-ceiling cell (redesign); ConceptNet apply prep; durability-cron sketch; phantom-edge removal (Skunkworks confirmed; gated).
- **Skunkworks:** WRITEUP framing-VET (when v1 DRAFT lands); HYPERNYM-replication SCHEMA-VET + verdict-VET; durability-cron SCHEMA-VET; reactive on rest. At-bandwidth: dup-instance reconciliation + stale-canonical-doc AUDIT_LESSON inst 96 atomize + catalog dispositions.
- **Orchestrator REPLACEMENT:** C-deferred A2 v6 dispatch when remote consumer self-syncs to 43892; durability-cron runner setup.

## Composes with standing discipline

- Item-1 HONEST_NEGATIVE bound = load-bearing anchor for every integration
- USER scour-FULL-substrate-breadth (binding condition 1 of WRITEUP framing-VET)
- USER NEGATIVITY-BIAS-symmetric (binding condition 3; cuts both directions)
- USER A5-no-silent-recompute lesson (guards self-heal manifest auto-fill)
- Skunkworks cert-architecture engine/checklist/SCHEMA-VET separation + mechanizability dimension (guards capability-cluster AtomKind decision)
- Item-11 narrative-data-consistency SCHEMA-VET (the WRITEUP is the first proving-ground; structured-claim convention for future 8th-gate)

## Honest expectations (cert-output)

- 1-3 cert-grade outcomes (C-deferred A2 v6 + HYPERNYM-replication + HYP-5 depth-ceiling)
- 1 integrated WRITEUP atom (RESEARCH_FINDING; load-bearing for next-cycle agenda; the FIRST narrative-data-consistency proving-ground)
- 1 durability cron LANDED (cert-FLOOR + backup + gap-detection automated)
- Phase-portrait v2 + capability-cluster METADATA + ConceptNet apply (substantive non-CERT extensions)
- Multiple cert-hygiene cleanups (catalog dispositions + dups + stale-canonical-doc)

This sprint DEEPENS + EXTENDS + HARDENS with the Item-1 bound as ANCHOR + cert-integrity guards everywhere. Different rhythm from prior 3 sprints; integrity-density + cert-grade-density per cap-h is high.

## Standing (9th rule)

- **USER (you):** ratify ask filed (Top-4 + Next-4 + opportunistic). All standing-discipline rules carry forward; push-fix CLOSED + pipeline restored; remote consumer self-syncs to 43892 -> gated chain unblocks.
- **Skunkworks:** pre-emptive SCHEMA-VET queue (5-6 cells) + 2 framing-VETs (WRITEUP + capability-cluster-metadata) + reactive on rest + at-bandwidth (dups + stale-doc + dispositions).
- **Exp-Dev:** HYPERNYM-replication + HYP-5-depth-ceiling + ConceptNet apply + durability-cron sketch + phantom-edge removal + capability-cluster METADATA when Skunkworks ratifies.
- **Orchestrator REPLACEMENT:** C-deferred A2 v6 dispatch + durability-cron runner setup.
- **Me (Director):** WRITEUP framing-VET cycle + catalog dispositions reactive + capability-cluster METADATA design + reactive on cascade.

Awaiting your GO on Top-4 + Next-4 (or sub-set + USER asks on the 4 structural calls).

-- Research (Director)
