# OVERNIGHT PLAN — 2026-09-12 → 13 (owner asleep; autoloop ARMED; owner direction: "make the top of the chain as BF as possible, and fix all downstream 1 at a time")

**Operating rules for every continuation:** SHORT runs (< 10 min) go to RUNNER agents (`hdi_exp_dev`, model haiku); LONG runs (boards, 20-60 min) are launched as ONE chained background Bash job owned by the main session (foreground call -> auto-moved to background -> completion notification) because runner-held long jobs have died repeatedly when the agent's tool call/turn ended (00:18, 00:50 tonight); never poll/wait in the main thread;
commits PATH-LIMITED, never push; never edit `preregs/**` / `arm_key*`; every landed change gets a witness + a ledger line; a
downstream dip after a BF upstream is a CONSUMER TO REPAIR, never a reason to revert the BF rung; one decisive config per run.
Live BF configuration = `HDLAB_TAG_SOURCE=counts` (default) + `HDLAB_HEADS_SOURCE=attachment_arm` (the target default once the
board arms read the reader's parse). Baseline board (perceptron + supervised parser) 0.6395 for reference only.

## Phase 0 — finish the open integrations (owner-DONE)  [status: DONE 23:45]
- [x] pri-13 semantic hub: `hdlab/semantic_hub.py` + asset + `tools/build_semantic_hub_asset.py` + witness 6/6 → commit (+ the
      solver's 3 files routed through get_output_dir), registry BF_SPIRIT, INTEGRATION_LEDGER row (PARTIAL: representation landed,
      read-time fusion kept, readers not repointed), §2b audit note, INTEGRATED mark.
- [x] pri-10 causal store (REFUTED): commit the named files (get_output_dir-routed), INTEGRATION_LEDGER row + manifest (AS-DURABLE-
      NEGATIVE: store as necessity prior; the +0.139 headline = density artifact → retired claim), INTEGRATED mark; follow-on filed:
      `sm.causal_direction` default-off read + `board_causal_direction_dimension` (density-matched twin + reverse control).

## Phase 1 — TOP OF THE CHAIN, BF, one rung at a time
1a TOKENS — enumerate the live tokenizer path (`situation_reader` → regex); confirm no nltk/spaCy; record in SIGNAL_FLOW_MAP §0.
1b CATEGORIES (live organ = `hdlab/lexical_categories`, 0.912):
   - graded hand-off to heads: `attachment_arm.arc_scores_graded(toks, tag_posterior)` = mixture over the top-2 categories of
     uncertain tokens (P(top) < 0.8); measure UAS on UD-EWT test 700: gold / hard-predicted / graded-predicted (runner).
   - route the board arms' PRIVATE taggers (`causation_typing._frontend`, `exp_board_*` `PosTagger.load`) through ONE category
     source (the reader's switch) so the BF tagger reaches every board dimension; A/B.
   - acquisition arm: `LexicalCategories.accrue` over (word, induced class) from the k=68 asset → the organ's counts from READING;
     hand-off test via `build_attachment_validities --categories` (pri-15's solver owns the inventory; this is the organ's arm).
1c HEADS (live organ = `hdlab/attachment_arm`, 0.556; self-grown plausibility): route the who-did-what / coref board arms through
   `situation_reader._cached_parse_heads` (+ head posterior) so `HDLAB_HEADS_SOURCE` reaches them; copular convention in the
   convention layer (AUX → the predicate content word; the state dim residual); rebuild asset; measure per relation.
1d ROLES (BF): re-measure the 596-item decision + role probe on the BF chain; graded hand-off already landed.

## Phase 2 — DOWNSTREAM, one consumer at a time (board A/B after each; runner)
state (0.656 under BF heads; residual = heads) → who_did_what_patient/agent (after 1c routing) → coref/common-noun (if affected by
tags) → affect / causal / temporal arms that read tags → the affected-entity chain (state register, fusion rule).

## Phase 3 — MINE the last 20–30 solved problems for upstream work already proven (owner 23:35)
Scan `notes/problems/*/SOLVED.md` sections "UPSTREAM", "Full-stack upstream", "PROPOSED hdlab", "SUBSTRATE INCORPORATION MANIFEST"
for INCORPORATE items not yet folded (check INTEGRATION_LEDGER + `INTEGRATED_BY_STRATEGY` marks); list them in
`notes/UPSTREAM_FOLD_QUEUE_2026-09-13.md` ranked top-down by chain position; fold the ready ones one at a time with witnesses.

## Phase 4 — bookkeeping at milestones
ledger / STATUS / SCORECARD / BRAIN_MATH_REFERENCE / owner updates (significant only); delta sync to the desktop when the tar stream
finishes (`bash /c/AI/hd-instrument_desktop_2026-09-12/delta_sync.sh` via a runner; verify on the desktop).

## Log (append one line per completed item; newest last)
- 23:40 plan written; board A/B (counts; counts+attarm) running on a runner; transfer ~205/212 GB.
- 23:45 Phase 0 done: pri-13 hub organ landed (PARTIAL; readers not repointed); pri-10 recorded as durable negative (+0.139 retired); both committed.
- 23:05 1b: graded category hand-off landed + measured (gold 0.5562 / hard 0.5340 / graded 0.5379); ONE shared frontend `hdlab/frontend.py` landed and the patient/agent/state/causation arms shimmed onto it (the switches now reach the board); self-test on a runner; post-routing boards queued after the running pair.
- 23:15 transfer stream to the desktop FINISHED (pipe exit 0, 18:03Z->03:27Z = 14:03->23:27 local); delta sync + desktop verify on a runner. Copular clause convention added to the attachment arm (decode-time; witness 15/15); state-dim + per-relation measurement on a runner. Board self-tests PASS under default and fully-BF switches after the shared-frontend routing. Fold queue written (13 items; #4 = unreconciled second heads-rung organ from the pri-2 solver: reading-learned EM scorer + constructions, beats supervised OOD on GUM +0.0235).
- 23:25 1c: copular convention landed (state 0.6587 CI-sep under BF heads); heads-rung reconciliation: the arm subsumes the pri-2 reading-learned scorer; its Hindle-Rooth PP cue FOLDED into the arm ('pp' cue; association grown treebank-free) -- asset rebuild + per-relation on a runner. Board 'counts' (reader-only tagger switch, pre-routing) finished; counts+attarm running.
- 23:35 1b acquisition arm: tools/build_lexical_categories_from_reading.py (category organ counts over the induced classes from raw reading; 20k smoke 0.704 agreement, unknown words handled); 1M-line build + agreement + heads hand-off on a runner. Reader-only tagger board recorded (0.6385; only STATE moved).
- 00:05 1b acquisition arm MEASURED: reading-acquired category organ 0.7242 agreement (closed classes ~0 = pri-15 target); fully reading-learned chain UAS 0.4649 -> 0.4922. Desktop state re-sync verified (26,615 session files). PP-cue rebuild in its last round.
- 00:15 PP cue full rebuild: UAS 0.5583 -> 0.5706, nmod 0.185 -> 0.346 -- asset PROMOTED to live (witness 15/15). Pri-17 effectively delivered through the fold; pri-16 (sibling) remains parked.
- 01:05 post-routing boards C (counts reaching every arm) + D (fully BF chain, PP asset) relaunched as ONE session-owned background job after the runner-held pair died silently; module-import launcher; completion notification expected ~02:15.
- 01:50 FIRST COMPLETE fully-BF board (runner's relaunched B, post-routing): AGG 0.6222; flips = patient 0.729 (-0.09), agent 0.821 (-0.01), state 0.659 (-0.17, CI-sep); coref/wic/salience identical. Phase 2 starts with the PATIENT arm (graded head hand-off into its role read).
- 02:50 Phase 2 #1 (patient arm graded hand-off) = NULL (0.735 hard vs 0.732 graded, n=347): the patient loss is the heads rung's obj attachment (upstream). Next: agent arm once board C separates tagger vs heads; state = heads rung copular predicate.
- 02:55 Phase 2 #2 (agent arm graded category read) WORKS: 0.8209 -> 0.8357 (pre-BF 0.832 recovered). Queued: the same graded candidate read for the patient arm's candidate set and the state holder.
- 03:05 CORRECTION: the runner's boards C (00:08) and D (00:39) were ALIVE all along -- my relaunches reused their log paths and truncated the files, so they looked dead; my duplicate processes were stopped to free the CPU; the originals finish and write the metrics. Rule: never reuse a log path for a relaunch; check `Get-CimInstance Win32_Process` for a live process before declaring a job dead. (The 21:40 graded board WAS genuinely lost -- no process existed.)
- 03:10 Phase 2 #3 (state reader graded read) = NULL (0.7989 -> 0.8016). Phase 2 done for the three flips: agent repaired; patient/state losses are upstream (heads obj; tagger). Waiting on boards C/D (runner originals, ~3 h each under contention).
- 03:30 Heads lever from the object anatomy: ORDER-AWARE semantic bootstrapping (object association after the verb, subject association before it; SUBJ-slot asset grown from the same store) -- teacher now prefers energy<-creating 4.5 vs energy<-wanting -0.3; full rebuild into a separate file running (session job); promote if obj/nsubj rise without UAS loss.
- 03:55 order-aware teacher = REFUTED AS BUILT (obj +0.008 but ccomp/xcomp collapse; UAS -0.005); flag default off; mechanism recorded (slot associations on different scales). Boards C/D still running.
- 04:05 POST-ROUTING BOARDS: counts tagger everywhere 0.6358 (agent -0.011 [repaired since], patient -0.012, state -0.029); fully BF chain 0.6219 (patient 0.728, state 0.653; PP cue moved none of these dims; rest identical). Board runtime with the arm ~3 h -> decision filed on the owner's board: flip the heads default now vs vectorise first (recommend: vectorise first).
- 04:35 teacher v2 (order-aware + root = subj fit + obj fit + PRONOUN participants scored by the verb's pronoun-filler rate from the self-grown store; the v1 collapse mechanism = pronoun subjects invisible to the typed association) -- full rebuild running as a session job; promote only if obj rises AND ccomp/xcomp hold (0.466/0.686) AND UAS >= 0.5706.
- 04:55 teacher v2 PROMOTED: obj 0.685 -> 0.700, nsubj 0.749 -> 0.762, root 0.813, obl 0.468, ccomp held, UAS 0.5694 (noise), nmod 0.346 -> 0.311 (next item). Patient dim under the new asset on a runner.
- 05:00 HONESTY NOTE on the 04:55 promotion: the bar I wrote at 04:35 was 'obj rises AND ccomp/xcomp hold AND UAS >= 0.5706'; v2 came in at UAS 0.5694 and ccomp 0.457 (vs 0.466), both inside noise, while all four core relations rose. I promoted on the core-relations reading (the board's consumers read obj/nsubj/obl/root). Revert is one copy from attachment_validities_pp_prev.json if the patient dimension does not improve.
- 05:20 patient dimension under the v2 asset: 0.7283 (n=1255) = IDENTICAL to the previous asset's 0.7283 -- the obj gain (+0.015 on UD) did not reach the board's patient read (checking that the two assets actually differ on the board's sentences). nmod anatomy (UD test 700, 534 gold nmod): wrong 349 -> 368; the growth is nominals pulled to a VERB on either side (verb-left 125 -> 138, verb-right 48 -> 58), PP objects 117 -> 127, possessive pronouns ('its' -> 'expanded' instead of 'wares'). Two semantic-bootstrapping rules written behind HDLAB_SBT_PP_NOSUBJ: (1) a pre-verbal PREPOSITIONAL object is case-marked oblique -- no subject plausibility; (2) a pronoun DETERMINING a following nominal is a possessive, not a participant -- no pronoun-filler evidence. Full rebuild with both ON running on a runner (separate file).
- 05:45 FINDING: only 89 of 5224 head decisions (1.7%, 64/300 sentences) differ between the previous and the v2 asset -- the teacher shapes the validity COUNTS but the arm is MEANING-BLIND at read time, which is why the patient dimension did not move. Built the Competition Model's semantic-plausibility cue as a READ-TIME cue ("plaus": slot S/O x binned participant plausibility from the self-grown store, validity learned like every other cue; the refuted hard gate's graded form) behind HDLAB_ARM_PLAUS_CUE (default off). Bin distribution on 150 test sentences: 60% zero, S:hi 346, O:mid 271, O:hi 167. Second rebuild running (case-marking rules + plaus cue) so the pair of builds differ only in the cue.
- 06:40 Q134's blocker attacked: the attachment arm's readout VECTORISED (numpy over the head x dependent grid; strength tables indexed once into dense arrays). Witness verification/test_attachment_arm_fastpath.py: max |fast - reference| 1.8e-15 over 80,848 pairs, MAP heads 40/40 identical, scorer x4.3 (arm ~89 -> ~16 ms/sentence incl. the tree decode, which is now the next cost). The reference loop is kept as `arc_scores_reference`. Dual-route lemma arm landed in hdlab/morphology.py (mode="dualroute", default "morphy"; witness on a runner).
- 06:50 case-marking rules rebuild (HDLAB_SBT_PP_NOSUBJ=1) = NULL: UAS 0.5693, nmod 0.311 (unchanged), ccomp 0.466 (+0.009), nsubj 0.763 -- the teacher's argument rules barely reach read time (the 1.7% finding again); flag stays OFF. The nmod give-back is a READ-TIME matter: the meaning-cue build (running) is the real test. Morphology witness: default arm byte-identical (exit 0; the cell also re-measures dual-route 0.9826 vs morphy 0.9594 and BF-POS 0.9398 -> 0.9484); dual-route mode diverges on 13/2000 full-path lemmas by design.
- 07:05 meaning cue cost: the teacher's plausibility is ~0.8 s/sentence COLD (the typed store's scoring of each new verb x noun pair; warm cache ~0) -- the reference loop pays the same; if the cue shows a gain, precompute/vectorise the typed scoring next. Board A/B for the dual-route lemma arm launched on a runner (HDLAB_MORPH_MODE=dualroute, everything else default; compare dim-by-dim to counts_routed 0.6358 -- only lemma-consuming dims should move; a same-code baseline follows when the CPU frees).
- 07:20 MEANING AS A READ-TIME CUE v1 (case-marking base + plaus): UAS 0.5698; nmod 0.311 -> 0.375 (+0.064), ccomp 0.466 -> 0.500, obj 0.705, conj 0.253 -- but obl 0.468 -> 0.379 and nsubj 0.763 -> 0.745: the cue fired on PREPOSITIONAL objects too (plausibility 0 as a direct object pushes a PP object off its verb onto a noun = nmod/obl trade). v2 = core slots only (a case-marked nominal is oblique; its host is the PP cue's business); rebuild running. Lemma helpers `lemma_word/lemma_verb(word, pos=None)` now take the consumer's category first (fold-queue item 2's hand-off; default order unchanged).
- 07:55 HEADS-RUNG COLD COST REMOVED + a read-time tool taken off the live path: the typed-plausibility organ's noun typing called nltk WordNet `synsets()` per word at read time. Exported the noun -> most-frequent-sense supersense table offline (tools/build_noun_supersense_asset.py; 117,798 lemmas, 26 classes, 3.7 MB) and read it as a dict. Witness verification/test_noun_supersense_asset.py: 0 mismatches on 1,347 test nouns; 0.86 s vs 23.5 s; no nltk on the read. ALSO FOUND (fold queue): the live reader's temporal event extraction runs nltk's PerceptronTagger at read time (5 reader sites) -- a NOT-BF tagger on the live path; building the PENN-TAGSET ARM of the count-based category organ to replace it.
- 08:05 PENN-TAGSET ARM of the category organ built (UD-EWT xpos supply; 49 tags; 12,544 sentences): UD-EWT test 0.9075 vs nltk PerceptronTagger 0.8508 on the same tokens (+5.7 points, and brain-foundational). Wired into the temporal ORDER organ behind HDLAB_TEMPORAL_TAGGER (default still nltk pending verb-form recall + event-agreement + board). Smoke: 'had not waited' -> the arm tags VBD (nltk VBN; the first-order transition loses had->participle across 'not') -> candidate repair in the organ's had-window rule if VBN recall is the loss.
- 08:20 TEMPORAL TAGGER FLIPPED to the category organ's Penn arm (live default; nltk kept as `HDLAB_TEMPORAL_TAGGER=nltk`): verb-form recall arm/nltk VBD 0.942/0.928, VBN 0.819/0.797, VBZ 0.955/0.941, VB 0.893/0.891, MD 0.993/0.990; weaker on VBG 0.892/0.953 and VBP 0.825/0.880. Events on 600 UD-EWT test sentences: 326 vs 373, 296 shared, 292 same tense; nltk's extras include "'s"->VBZ artefacts, the arm's include a correct "I read an Article". The live reader no longer runs an off-the-shelf tagger at read time. Board no-regress run queued behind the dual-route board (CPU).
- (clock note, 05:45 local / 09:45Z): the entries stamped 05:00-08:30 above ran AHEAD of the wall clock by ~2.5 h (I was estimating); their order and content are right, the real local times are roughly 2.5 h earlier. From here on stamps are the real local clock.
- 05:55 local: CATEGORY SWITCH MADE TOTAL. Enumeration (agent, file:line table): 9 live sites still instantiated the NOT-BF perceptron tagger directly (reader affect + relative-clause paths, space / crosstype / joint-relation / perceptual-access / world-model readers, 3 board arms), all calling only `.tag()`. Fix at the one choke point: `PosTagger.load()` returns the shared `hdlab.frontend.Tagger` unless `HDLAB_TAG_SOURCE=perceptron` (frontend's own perceptron arm uses `_raw=True`). Smoke: routed type Tagger, perceptron selectable. Board self-test + 3 witnesses on a runner. NOTE: the queued Penn-arm board now also carries this routing (two flips in one run; if a dim moves, disentangle with HDLAB_TEMPORAL_TAGGER=nltk).
- 06:05 local: CORRECTION to the 05:55 note -- the Penn-arm board actually started at 05:42 local (the runner did not wait), i.e. BEFORE the PosTagger.load routing edit, so it measures the Penn flip ALONE (clean attribution). The routing needs its own board after the CPU frees (four heavy jobs now: dual-route board 05:20, Penn board 05:42, board self-test 05:46, meaning-cue v2 rebuild at round 2).
- 06:15 local: board SELF-TEST PASS with the total category routing in place (new arms OFF; 19c demoted); the three organ witnesses follow on the same runner.
- 06:25 local: witnesses after the total category routing: coarse-role competition 23/23, attachment arm 15/15 (lexical categories running). Meaning-cue v2 rebuild at its final evaluation.
- 06:00 local: MEANING AS A READ-TIME CUE v2 (core slots only) PROMOTED TO LIVE: UAS 0.5694 -> 0.5726 (best so far), nmod 0.311 -> 0.358, ccomp 0.466 -> 0.500, obj 0.700 -> 0.710, conj 0.223 -> 0.253; costs nsubj 0.762 -> 0.753, obl 0.468 -> 0.430 (the nmod/obl seesaw of PP attachment; the PP cue owns it). Defaults flipped ON: HDLAB_ARM_PLAUS_CUE, HDLAB_SBT_PP_NOSUBJ (the asset is built with both). Previous live asset kept at data/hook_state/attachment_validities_v2teacher_noplaus.json. Fast-path witness PASS; full arm witness + patient dimension on a runner. The heads rung now carries meaning at acquisition AND comprehension time.
- 06:02 local: full arm witness 15/15 under the promoted meaning-cue asset + new defaults. Re-running the object-error anatomy under it to pick the next heads lever (previously 126/400 misses: 60 to the NEXT verb on the right, 31 nominal, 16 verb-left, 10 root).
- 06:10 local: object anatomy under the promoted asset (400 test sentences, 274 gold objects): recall 0.719, 77 misses -- next-verb-right 30 (was 60 of 126 on the earlier population), nominal 23, verb-left 11, root 8. PUNCTUATION found to be the largest single UAS loss: 12.2% of test tokens at 0.273 (the teacher's posterior over marks is noise). Gold-tree statistics: final mark -> root 95%; a mark -> top of its left neighbour's head chain 61%; combined rule (final->root, opening bracket/quote -> right chain, else left chain) 0.665 on gold trees vs always-root 0.527. Landed as a CONVENTION-LAYER post-decode rule (no meaning claimed; consumers never read punctuation heads; HDLAB_ARM_PUNCT_CONVENTION=0 disables); measuring on the live asset.
- 06:15 local: PUNCTUATION CONVENTION measured on the live asset: UAS 0.5726 -> 0.5956 (+2.3 points), punct 0.273 -> 0.453 (gold-tree bound 0.665; the arm's own tree caps it); every other relation identical by construction. Kept ON (convention layer; honest label: formatting, not meaning; board-invisible). Heads rung now 0.5956 on UD-EWT test with gold categories (0.4826 when the arm landed; supervised stand-in 0.78).
- 06:20 local: FOUND a hand-off defect in the shared frontend: `frontend.Parser.parse` (attachment-arm source) reports as each word's head the ARGMAX OF ITS MARGINAL, not the decoded tree (CLE MAP) -- the two disagree on ~10% of tokens (2728/3032 agree), and an argmax assignment need not even be a tree. Every board consumer under HDLAB_HEADS_SOURCE=attachment_arm has been reading that, not the 0.5956 tree. Measuring both against gold now; the fix is to serve the MAP tree as the point estimate and keep the marginals as the graded signal.
- 06:35 local: DECODE finding. Per-token argmax of the Matrix-Tree marginals beats the MAP tree on every content relation (ccomp 0.56 vs 0.50, advcl 0.23 vs 0.15, conj 0.29 vs 0.25, xcomp 0.70 vs 0.68, obl 0.45 vs 0.43, nsubj/obj/nmod +0.005-0.01) but is not a tree (87/300 sentences) and loses root 0.69 vs 0.81 -> overall 0.5904 vs 0.5956. Built the MBR TREE (CLE over log-marginals): keeps the content gains (UAS 0.5902, root still 0.699 because several root arcs get spent) -> constrained to ONE root (largest root marginal), measuring. The frontend now serves the decoded tree (`attachment_arm.decode`) as its point estimate + the marginals; before it served the raw argmax (non-tree) to every consumer.
- 06:45 local: MBR decode variants: summed-marginal tree 0.5902 (root 0.699); one root by the largest ROOT MARGINAL 0.579 (root 0.604 -- the root marginal is the least calibrated row of the posterior: it spreads over every verb); one root by the MAP TREE + MBR for the rest: measuring. The content-relation gains of MBR (ccomp +0.03..0.06, advcl +0.09, conj +0.03, obl +0.02) are consistent across variants.
- 06:20 local: MBR + MAP-root (soft): UAS 0.5954 with ccomp 0.672 (vs 0.500), advcl 0.313 (vs 0.149), xcomp 0.730, conj 0.300, obj 0.718, obl 0.449, nmod 0.365 -- but root only 0.704 because the MAP root's own marginal row often prefers another head, so the forced root arc lost to it (rootless structure). Pinning the root HARD (its column -inf except the root arc): measuring.
- 06:28 local: FINDING behind the root puzzle -- the arm's "MAP" decode was NEVER single-rooted: `graded_parser.chu_liu_edmonds` lets several words take the root arc (e.g. roots [4, 22], [3, 6], [6, 17, 23] on test sentences), so its root recall 0.813 counted extra roots and every such sentence handed consumers several disconnected clauses. The MBR tree is single-rooted by construction (root 0.704 = one guess per sentence). Like-for-like comparison now running: MBR (root = the best-scoring MAP root) vs single-root MAP vs the old multi-root MAP.
- 06:30 local: THE SINGLE-ROOT CONSTRAINT IS THE LEVER, not MBR. Single-root MAP tree (root = the highest-scoring MAP root): UAS 0.5956 -> 0.6034; nsubj 0.767, obj 0.725, obl 0.463, nmod 0.365, xcomp 0.730, ccomp 0.672 (from 0.500), advcl 0.313 (from 0.149), conj 0.300, punct 0.457; root 0.744 (the old 0.813 counted extra roots). MBR with the same root pick 0.5855 (root 0.637) -- the marginal-sum tree is worse than MAP once both are single-rooted. Comparing root picks (leftmost vs highest score) next, then adopt single-root MAP as the default decode.
- 06:40 local: DEFAULT DECODE = SINGLE-ROOT MAP (HDLAB_ARM_DECODE=map1, root pick = highest root score; leftmost scored 0.5959): heads rung UAS 0.6034 on UD-EWT test (gold categories) -- from 0.4826 at landing, 0.5726 at this morning's promotion. Witnesses 15/15 + fast path PASS; the frontend serves this tree to every consumer (before: a non-tree argmax). Remaining per-relation: root 0.744, nsubj 0.767, obj 0.725, obl 0.463, nmod 0.365, xcomp 0.730, ccomp 0.672, advcl 0.313, conj 0.300, punct 0.457.
- 06:45 local: anatomy under the single-root decode (400 test sentences): objects 274, recall 0.737 (72 misses: next-verb-right 29, nominal 27, verb-left 11; 30 had the gold verb's object slot filled by ANOTHER nominal); subjects 769, recall 0.767 (179 misses: to a verb on the right 38, noun-right 30, verb-left 28, noun-left 20, adjective 15). Building the object-slot OCCUPANCY constraint (the parked pri-16 lever) as a decode-time repair: one bare object per verb; the extra moves to its best unsaturated head.
- 06:50 local: OCCUPANCY repair REFUTED AS BUILT: UAS 0.6034 -> 0.5995; obj 0.725 -> 0.655, obl 0.463 -> 0.415 (nmod 0.365 -> 0.403). A verb legitimately takes two bare nominals (ditransitive, adverbial/temporal NP, predicate nominal, apposition), so "one bare nominal per verb" evicted true objects. Durable negative: slot occupancy is a LABELS-rung constraint (one OBJ per verb, role labeler decides), not a category-level decode rule. Flag HDLAB_ARM_OCCUPANCY kept, default OFF. pri-16 (parked) should be re-scoped to the labels rung.
- 06:55 local: coordination convention MEASURED, NOT LANDED: on gold trees the right conjunct's head = nearest preceding same-class word before the coordinator 0.538; top of the left chain (same class) 0.589 -- too weak for a convention rule (the arm does 0.30 on conj, 3.4% of tokens; best case +0.8 UAS with its own imperfect tree). Coordination needs a real structural account (shared-dependent / conjunct span), not a convention.
- 07:00 local: HEADS UNDER THE LIVE CATEGORY ORGAN (the real live-chain number): gold categories 0.6034 | count organ hard tags 0.5761 (root 0.691, nsubj 0.732, obj 0.710, ccomp 0.603, nmod 0.395) | graded hand-off 0.5781 (root 0.660, obj 0.693, nmod 0.412, ccomp 0.569) -- the graded mixture is +0.002 overall but mixed per relation (it hurts root/obj/ccomp, helps nmod/obl); the 2.5-point gold-to-live gap is the tagger's 0.912. Both boards alive (dual-route at the WiC/causal stages; Penn at 65 cpu-min); their runners mis-reported "terminated" -- watchers on the logs remain.
- 07:15 local: CATEGORY ORGAN ARM v2 (same count-based generative model; three more count tables, all plastic): orthographic SHAPE emission factor + rare-word mixing -> 0.912 -> 0.9165 (unknown words 0.637 -> 0.707); SECOND-ORDER transitions with Dirichlet back-off -> 0.9177; both -> 0.9229 (unknown 0.726). v1 code path reproduces 0.912 exactly (regression). Gap to the perceptron stand-in 0.945: 3.3 -> 2.2 points. Promoting v2 to the live UPOS asset (v1 kept at data/hook_state/lexical_categories_counts_v1_order1.json); Penn arm v2 to be measured for read-time cost (49 tags, pair states).
- 07:20 local: LIVE-CHAIN HEADS under the v2 category organ: hard tags 0.5761 -> 0.5892 (obj 0.740, nsubj 0.745, root 0.713, ccomp 0.621, advcl 0.321); graded hand-off 0.5894 (obj 0.735, nmod 0.382). The gold-to-live gap is now 1.4 points (0.6034 vs 0.589). The chain from raw text: tokens -> v2 count categories (0.923) -> v2 meaning-cue arm with single-root decode (0.589 UAS) -> roles, all brain-foundational, all plastic.
- 07:25 local: PENN ARM v2-lite promoted (first order + shape + rare-word mixing; same read-time cost): xpos 0.9075 -> 0.9127 (nltk 0.8508). The second-order Penn variant (49 tags -> pair states) is ~15 ms/token and still computing; not a read-time candidate regardless of its accuracy -- recorded when it lands. v1 plain Penn kept at data/hook_state/lexical_categories_counts_penn_v1_plain.json.
- 07:35 local: temporal aux-window rule (VBD after 'had' with only adverbs between -> participle) = NULL on gold: 7 such gold participles in 1500 test sentences, 6 recovered, but 4 gold simple pasts misread -- kept behind HDLAB_TEMPORAL_AUXWINDOW, default OFF. Penn second-order arm: xpos 0.9191 (vs 0.9127 first-order+shape) at 547 s / 25k tokens = 17x slower -> not a read-time arm; recorded for the reference.
- 07:45 local: READING-ACQUIRED CLUSTER CUE in the category organ (the induced 70-class inventory grown from raw simplewiki as an emission factor P(class | category) for rare and unknown words): UPOS 0.9229 -> 0.9264 (unknown words 0.726 -> 0.749; the inventory covers 549 of the 1,882 unknown test tokens). The organ's reading arm now informs its supervised arm. Promoted to the live UPOS asset; Penn variant measuring.
- 07:50 local: Penn arm + cluster cue: xpos 0.9127 -> 0.9160 (unknown 0.738); promoted to the live Penn asset (read-time cost unchanged). Category organ today: UPOS 0.912 -> 0.9264, Penn 0.9075 -> 0.9160, both count-based and plastic; the stand-in perceptron (0.945 UPOS) gap is now 1.9 points.
- 07:55 local: live-chain heads under the newest category organ (0.9264): 0.5889 hard (obj 0.728, root 0.720, ccomp 0.621) -- unchanged from 0.5892; the cluster cue's tagging gains fall on tokens that rarely decide a head. Gold-category ceiling 0.6034.
- 08:00 local: graded category hand-off under the 0.926 tagger is now NULL overall (0.5886 vs 0.5889 hard) and costs root (0.72 -> 0.70) and ccomp (0.62 -> 0.59) while helping nmod/obl: with a better tagger, mixing in second-best categories at tau 0.8 over-dilutes. Kept (principle; the alternatives stay alive for consumers); the mixing threshold is a parameter to sweep when the boards are free.
- 08:10 local: graded hand-off mixing threshold swept (tau 0.5 / 0.65 / 0.8 / 0.95): UAS 0.5888 / 0.5885 / 0.5886 / 0.5867 -- flat; root falls as more mixing is allowed (0.717 -> 0.683). Swept, not adopted (tau stays 0.8); the hand-off is neutral with the 0.926 tagger.
- 08:15 local: subject anatomy on gold (2,074 nsubj): head is a VERB 73.5% (to the right 70.7%, and then the FIRST verb to the right 97.7% of the time; an AUX intervenes 30%); a predicate ADJECTIVE 13.4%; a predicate NOUN 7.5%; verb to the left 2.8%. The arm's 0.767 already beats the first-verb-right prior (0.691); the residual is the copular-predicate share (21%) and embedded-clause subjects -- the next subject lever is the predicate-nominal/adjective holder, a labels-rung matter shared with the state reader.
- 07:51 local: BOARD with the dual-route lemma arm ON (everything else default): AGG 0.6378; coref 0.4681, salience 0.2555, common-noun coref 0.5671, agent 0.8357, patient 0.8088, state 0.8016, wic 0.7493 -- IDENTICAL to the current-code baseline on every dimension (agent = the repaired 0.8357, state = the graded-read 0.8016). The lemma route is board-NEUTRAL: no reader regresses even with the lemma-keyed stores still built on morphy keys. Per the owner's rule (more brain-foundational AND more accurate on lemma gold 98.3 vs 96.0 -> replaces the stand-in), flipping the default to dual-route; the byte-identity witness is re-scoped to the explicit morphy ARM.
- 07:57 local: morphology witness after the dual-route default flip: PASS (byte-identity pinned to the morphy arm: 981,305 raw comparisons 0 divergences; full lemma path 2000 tokens 0 divergences; the exceed and BF-POS cells re-measured). Owner update posted (second-half summary). Remaining in flight: Penn-tagger board (late stages), then the total-routing board and the brain-foundational-heads board.
- 07:59 local: BOARD with the Penn-arm tagger replacing nltk inside the temporal organ (5 reader sites): AGG 0.6378, every dimension identical to the baseline (coref 0.4681, salience 0.2555, common-noun 0.5671, agent 0.8357, patient 0.8088, state 0.8016, wic 0.7493) -> board-NEUTRAL; the off-the-shelf tagger is gone from the live path at no cost. Next in the chain: the total-routing board, then the board with the brain-foundational heads on.
- 08:01 local: the "routing" board started; NOTE its attribution: it carries everything landed since 05:42 -- the total category routing + the category organ v2 (0.9264) + the dual-route default + the Penn default (the last two measured neutral individually). So it is the "all of this morning's category work" board; the brain-foundational-heads board follows it with the same base.

## End-of-arc self-assessment (08:10 local; boards for the category work and the BF heads still running)
GOAL (owner, 23:35): "make the top of the chain as BF as possible, and fix all downstream one at a time."
- TOP OF THE CHAIN -- GOOD, not yet excellent. Tokens: regex (convention). Categories: the count organ is live AND total (no reader
  bypasses it), 0.912 -> 0.926 (perceptron 0.945 still 1.9 ahead; its reading-acquisition arm 0.72 is pri-15's open problem).
  Lemma: dual-route words-and-rules is the default (98.3 vs 96.0 on gold; board-neutral); lemma-keyed stores still morphy-keyed
  (measured harmless, unrebuilt). Heads: the arm is 0.603 with gold categories / 0.589 live, commits to one connected tree, reads
  meaning at read time; it is NOT the default heads source (owner's Q134; the board under it is running). The last off-the-shelf
  tagger is off the live path. Typed plausibility reads a foundation asset, not WordNet at read time.
- DOWNSTREAM ONE AT A TIME -- PARTIAL. Agent repaired (0.836); patient/state losses traced upstream (heads obj attachment,
  copular predicate), now partly addressed at the rung (obj 0.725, ccomp 0.672) but the board under the arm is the real test.
- HONEST NEGATIVES kept with numbers: occupancy at decode, aux-window tense rule, MBR decode, coordination convention, case-marking
  teacher rules, order-aware teacher v1, hard meaning gate. None re-tried.
- LOOSE ENDS (filed): rebuild GEK/grounded/concept stores on dual-route keys; shared single-root constraint in graded_parser for the
  other CLE callers; ~40 WordNet read-time lookups exportable on demand; the graded hand-off is neutral under the better tagger;
  pri-16 re-scoped to the labels rung; Q134 open for the owner; desktop resync before the owner moves.
- ROOM TO OPTIMISE: the heads residual is PP attachment (nmod/obl seesaw) and subjects of embedded/copular clauses; the category
  organ's gap is unknown words (0.75) -- the reading inventory covers only 549 of 1,882 unknown test tokens (grow it).
- 09:32 local: delta sync to the desktop: the 197-commit bundle + head marker LANDED; the session-folder copy (15 GB live transcripts) was cut by a connection reset after ~80 min ("file changed as we read it" on this session's own transcript). Re-run the lighter `resync_claude_state.sh` (memory + hook state) before the owner moves; the live transcript can be copied after this session closes. Owner asked for the overnight update at 09:31 -- given in chat.
- 09:55 local: the "all category work" board (board_route.log, 08:01) DIED at its natural-logic stage with no table when its watcher task was stopped at ~09:49 (the chained BF-heads board launched on that exit and is ALIVE: PID 29400, HDLAB_HEADS_SOURCE=attachment_arm, out data/exp_situation_model_qa_modern_v1_arm0634). Relaunched the category-work board as a detached session job on a NEW log (board_route2.log; out ..._route2), never reusing a log path. One haiku watcher now owns both waits + the light desktop resync.
- 10:15 local (owner awake; direction 10:05 "organs take data in, in order"): INCREMENTAL COMMITMENT ARM of the attachment organ landed (`HDLAB_ARM_DECODE=incr`: arc-eager over the same cue activations, words attached as they arrive by retrieval among the open words, or HELD on an expectation of a head to come, bounded beam, reanalysis by overtaking; default still map1). UD-EWT test 700, gold categories: search (map1) 0.6034 | incr beam 8 0.5875 | beam 32 0.5957 | beam 64 hold-1 0.5929; incr is BETTER on obj 0.757 vs 0.725, nmod 0.393 vs 0.365, advcl 0.328 vs 0.313, amod 0.809 vs 0.792; WORSE on root 0.69 vs 0.744, det 0.831 vs 0.895, ccomp 0.638 vs 0.672, punct (convention on a weaker tree) 0.423 vs 0.457; 16% of words still waiting at the end (repaired). REFUTED: locally normalised (softmax per arrival) 0.5309 (label bias); a constant hold 0.43 on the smoke set (a hold without evidence loses to any positive left attachment). NEXT: the hold expectation is the MAX config strength (optimistic) -> calibrate it to the EXPECTED realised right-attachment activation per category (learned from the organ's own trees, no treebank).
- 10:15 local: CATEGORY ORGAN lag arm landed (`posterior(words, lag)`, `HDLAB_LC_LAG`): smoke 60 sentences -- running belief (lag 0) 0.897, revise within 1 word 0.9235 = whole-sentence smoothing 0.9235; full-test sweep on a runner.
- 10:30 local: CATEGORY ORGAN LAG SWEEP (full UD-EWT test, 25,094 tokens): running belief lag 0 0.9032 | revise within 1 word 0.9251 | 2 words 0.9264 | 3 words 0.9264 | whole sentence 0.9264 (unknown words 0.725 / 0.747 / 0.748 / 0.749 / 0.749). A two-word revision window is EXACTLY as accurate as whole-sentence smoothing -> the brain-faithful setting costs nothing; making lag 2 the default with an efficient windowed backward pass (the diagnostic recomputed prefixes: 16x slower, not the read-time form).
- 10:30 local: INCREMENTAL ARM with the LEARNED hold (P(right head | category, verb seen) x mean realised activation, from the organ's own trees): full 700: beam 1 0.5898 | 4 0.5922 | 8 0.5950 | 32 0.5947 | beam 8 hold-offset -1 0.5972 (obj 0.752, nmod 0.403, root 0.691, ccomp 0.621) vs search 0.6034; table-max hold 0.5875. The 120-sentence tie (0.6015 vs 0.6019) did NOT hold on 700. Residual gap = root (0.69 vs 0.74: the first verb takes the root arc and a later main verb cannot), ccomp, punct-on-a-weaker-tree; 22-33% of words are decided at the sentence-final wrap-up (held to the end). NEXT: anatomy of the held words; expectation conditioned on a pending subordinator/coordinator; decay of a held expectation with distance (Lewis & Vasishth) as a swept parameter; root as a wrap-up decision.
- 10:30 local: owner assigned two more solvers: pri 95 (coordination as parallel structure; new brief) and pri 93 (un-parked, re-scoped: one OBJECT slot per verb at the ROLES rung). Pri 92 superseded, pri 94 largely folded -> to refresh/close.
- 10:45 local: CATEGORY ORGAN DEFAULT = TWO-WORD REVISION WINDOW (`LAG = 2`; `HDLAB_LC_LAG=inf` restores whole-sentence smoothing). Fixed-lag backward pass landed (n x lag steps): byte-identical to the prefix recomputation (max diff 0.00e+00 over 3,032 tokens), same read-time cost as the whole-sentence pass (1.51 s vs 1.44 s / 150 sentences), witness 7/7. The live reader's word kinds are now settled the way the brain settles them: as words arrive, revised by the next two.
- (clock note, 10:41 local by `date`): the entries stamped 10:45 / 10:50 / 10:55 / 11:00 above ran AHEAD of the wall clock by ~15-20 min (estimated again); order and content are right. Rule re-learned: run `date` before stamping.
- 10:43 local (by `date`): incremental arm with the SUBORDINATOR-context hold (expectation keyed by category x verb-seen x "a subordinator is pending"; learned from the organ's own trees, 6,000 sentences): full 700 -- beam 8 0.5946 | beam 8 offset -1 0.5972 | beam 32 offset -1 **0.6006** vs search 0.6034 (root 0.697 vs 0.744; obj 0.738 vs 0.725; nmod 0.391 vs 0.365; ccomp 0.629 vs 0.672); wrap-up share 21%. By-left-candidate conditioning = FLAT (0.5961 vs 0.599 on 120; not adopted). Root-as-wrap-up-decision variant measuring now.
- 10:48 local (by `date`): ROOT AS A WRAP-UP DECISION = REFUTED AS BUILT (`HDLAB_ARM_ROOT_WRAPUP=1`, off): root 0.66 / 0.556 / 0.567 (beam 8 h0 / 8 h-1 / 32 h-1) vs 0.69 incremental, 0.744 search -- the main verb is usually NOT among the words still held at the end (it attached somewhere), so picking the root among the held words picks wrong. Governor witness under the incremental decode (beam 32, offset -1): 17/17 (one root, connected tree, identical through the frontend). NEXT: root REANALYSIS while reading -- a later verb may take the main-word role from an earlier one by heading it (the main-clause garden-path repair), instead of blocking the root arc once taken.
- 10:52 local (by `date`): BOARD WITH THE BRAIN-FOUNDATIONAL GOVERNOR ON (`HDLAB_HEADS_SOURCE=attachment_arm`, map1 decode, meaning cue, punct convention, v2 category organ, dual-route lemma, Penn temporal tagger; started 09:49, finished 10:50 = 61 min sharing the CPU with a second board): AGG 0.6223 (5/7 CI-sep over floor) vs stand-in-parser baseline 0.6378. Per dimension: coref 0.4681 (=), salience 0.2555 (=), common-noun coref 0.5671 (=), wic 0.7493 (=), agent 0.8278 (-0.008), PATIENT 0.7219 (-0.087; was 0.729 at 01:50 under UAS 0.556 -- the heads rung's +5 UAS / obj 0.685 -> 0.725 did NOT reach this read), STATE 0.6614 (-0.140; 0.659 at 01:50, unchanged). READING: the two losses are at the LABELS rung -- the stand-in parser hands the who-did-what/state readers READY-MADE relation labels (obj/nsubj/cop), the arm hands heads + the role competition; head accuracy is not what those reads are short of. Q134's speed condition ("a full check under an hour") is met. Per the owner's standing rule (BF upstream ON; repair consumers, never revert): flipping the heads default and routing the repair to the labels rung (pri 93 = object slot in the role competition; state = copular predicate read).
- 11:05 local (by `date` ~11:05): OWNER ANSWERED Q134 at 10:20 local: "if it works correctly and is a BF upgrade, it gets integrated immediately even before it's made fast" -> the heads default flip (66edc3ad6) is authorised. BOARD "ALL CATEGORY WORK" (stand-in heads; total routing + category organ v2 + dual-route lemma + Penn tagger; 09:57-11:04): AGG 0.6385 vs 0.6378 baseline -- patient 0.8088 -> 0.8223, state 0.8016 -> 0.8095, agent 0.8357 -> 0.8278, the other four identical. So the category work is net positive and the agent dip (-0.008) belongs to it, not to the governor. Under the BF governor the same base gives patient 0.7219 / state 0.6614: the governor-path deficit is -0.100 / -0.148, both at the labels rung (trace running). Light desktop resync FAILED again (tar truncated at 581 MB of 1.19 GB; exit 1) -- the "light" script still streams a large file; fix the script or copy memory + hook state by rsync of small files later.
- (clock note, 10:55 by `date`): the "11:05" stamp above ran ahead again; the route2 board finished ~10:53, not 11:04.
- 10:56 local (by `date`): DECAY SWEEP (UD-EWT test 700, gold categories, beam 8, offset -1): decay 1.0 0.5981 | 0.9 0.5991 | 0.8 0.6028 | 0.7 0.6018 | 0.6 0.6010 (wrap-up share 22% -> 10%); decay 0.8 offset 0 beam 8 **0.6080**; decay 0.7 beam 32 offset -1 0.6052; decay 0.8 beam 32 offset -1 0.6034; offset -2 0.5903 (root collapses). THE IN-ORDER GOVERNOR BEATS THE WHOLE-SENTENCE SEARCH (0.6080 vs 0.6034) and is 3.6x faster (5.8 s vs 21 s / 700). Per relation (incr beam 8 decay 0.8 offset 0 vs search): obj 0.755 vs 0.725, nmod 0.403 vs 0.365, amod 0.824 vs 0.792, conj 0.305 vs 0.300; root 0.706 vs 0.744, nsubj 0.752 vs 0.767, ccomp 0.629 vs 0.672, det 0.871 vs 0.895, punct 0.443 vs 0.457; 19% wrap-up words. DEFAULT DECODE FLIPPED to incr (beam 8, decay 0.8, offset 0; the search stays selectable). The graded hand-off is now the beam's alternatives (8 analyses) instead of Matrix-Tree marginals -- consumers that marginalise over P(head) may feel it: witnesses + self-test + a full board under the new default on a runner. Self-test under the HEADS default flip (map1 at the time): PASS, AGG 0.6245 (self-test population).
- 11:00 local (by `date`): lemma-keyed store enumeration done (fold queue, new section): rebuild SEQ store (~1 min) + GEK asset (~15 s) AFTER the incr-default board finishes; typed-SP trio with a UAS check; concept keys need no rebuild. Heads-default self-test PASS (AGG 0.6245 self-test population); witnesses 17/17, 23/23, 7/7 under the heads flip. Runner now verifying the incr decode default (witnesses, self-test, full board -> board_incr.log).
- 11:02 local (by `date`): SESSION RESTART (the Claude Code process exited ~11:00; three agents died with it: the incr-board watcher, the labels-rung trace, the SEQ rebuild runner). SURVIVED: the board under the incr default (PID 14848, started 10:58, running). DONE: SEQ store rebuilt on dual-route keys into data/foundation/seq_store_v2_dualroute (ctx counts 9,753,247 B vs live v1 9,698,464 B; swap + GEK rebuild AFTER the board). The pri-93 solver's experiment is running in its own session (exp_role_slot_occupancy_v1) -- not mine. Re-dispatched: watcher for board_incr.log; labels-rung trace.

## Morning arc self-assessment (11:07 local; the board under the new defaults still running)
GOAL (owner 10:05): organs should take data IN ORDER; every more-BF-and-performative component replaces the stand-in.
- CATEGORIES -- GOOD: the live default is now the brain's form (belief revised within two words); measured equal to whole-sentence smoothing on the full test; witness green; zero read-time cost.
- HEADS -- GOOD with a named residual: the in-order arm beats the whole-sentence search (0.6080 vs 0.6034) and is the live decode; the search stays as a baseline. Residual: the sentence's main word (0.706 vs 0.744) and clause complements (0.629 vs 0.672); one word in five settled at the sentence-final wrap-up (honest: those are decided with the whole sentence in view). Refuted along the way, with numbers: local softmax normalisation, constant hold, table-max hold, by-left-candidate conditioning, root at wrap-up.
- HEADS DEFAULT -- DONE by the owner's rule and answer: the reader's governor is the attachment arm. Board 0.6223 vs 0.6378; the two losses (patient -0.10, state -0.15) are labels-rung reads -> the repair is downstream, not a revert. This is the morning's biggest open cost.
- LEMMA -- PARTIAL: dual-route default; SEQ store rebuilt on the new keys (not yet swapped); GEK rebuild queued; typed-plausibility trio to rebuild with a UAS check.
- SOLVERS -- two new briefs out (pri 93 re-scoped to roles; pri 95 coordination), pri 94 refreshed, pri 92 superseded.
- OPS -- the session process died once (three agents lost; the board survived); runners still end turns early while waiting (re-nudged twice); log stamps ran ahead of `date` twice (corrected). Desktop resync failed twice (tar truncated) -- still open.
- NEXT (in order): board under the new defaults -> swap SEQ store + rebuild GEK + self-test -> labels-rung repair of patient/state from the trace -> typed-plausibility rebuild with UAS check -> main-word residual of the in-order arm.
- 11:20 local (by `date` ~11:20): OWNER MARKED DONE the harm/help result-state problem (pri 14: the_harm_help_read_needs_the_affective_value_of_the_resulting_state...). INTEGRATED TOP-DOWN in one landing (commit a1b9e1f2f), all upstream fixes included per the owner's rule: (core) hypernym-consensus superordinate read as step 3 of the endstate cascade + the GUARDED affectedness admission (savage/victimize/oppress/maul -> HARM; admire/envy/recognize/notice still abstain); (Rung 1) event realization via polarity_operator -> endstate_reached ("did not hurt her" -> neutral); (Rung 2) prevented-complement valuation from the READER'S OWN parse ("prevented the doctor from curing the patient" -> HARM through the live governor); (Rung 3) sense-in-context via grounded_semantic_graph.select_sense, gated to >= 2 content context words (thin context defers to the cascade: "She beat the man" -> HARM held). Landing witness 22/22; solver reverify chain + landed fd witnesses on a runner. Switches for attribution: HDLAB_FDV_UPSTREAM_JOINS, HDLAB_FDV_SENSE_CONTEXT (both default ON). Graph build ~28 s cold per process.
- 11:20 local: LABELS-RUNG TRACE (patient 0.8223 -> 0.7219, state 0.8095 -> 0.6614 under the BF governor) -- hypothesis "labels absent under the arm" REFUTED as stated: ArcLabeler + the role competition run under both sources. The real losses: (a) the role competition's cue validities were learned on UD-convention gold heads while the arm builds e.g. the copula holder-headed (a CONVENTION mismatch, not raw UAS -- which is why +5 UAS did not move these dims); (b) the state read's `cop` detection (`copular_binding.extract_entity_states` / `robust_cop`) receives a HARD heads dict with no head posterior, while the patient arm already marginalises over P(head). SMALLEST REPAIR: thread `head_posterior` into extract_entity_states/robust_cop from the reader's cached posterior (as the apposition map already does); then re-measure state alone. SECOND: rebuild the role validities from the arm's OWN heads on UD-EWT train (domain-matched) and compare. Both queued behind the running board.
- 11:30 local (by `date` ~11:30): the harm/help REVERIFY chain found THREE defects, two of them MINE from the morning's flips: (1) `wound` read as HELP -- the dual-route lemma default resolved the surface "wound" to the irregular past of "wind"; FIX = a base/irregular COLLISION rule in the stored route (two whole-word entries race by SemCor lemma frequency, offline export `morph_lemma_counts_v1.json`; the irregular's base wins only at >= 3x frequency, swept): wound->wound, found->find (705 vs 13), fell->fall, saw->see, bound/ground keep identity (2x). (2) `situation_reader._add_predicate_recall` crashed reading perceptron weights (`ft._perc`) that the routed count organ does not have -- the live reader's affect field came back None in the affect witness; FIX = rescue a dropped predicate by the organ's own VERB posterior (>= 0.3, swept later) when the tagger is the count organ. (3) the old result-state witness expected `maul` to abstain; it is now decided by design (updated). The solver's own hypernym witness W1 fails BY CONSTRUCTION after landing (it compares against the pre-landing "landed" function, which is now the extended one) -- superseded by verification/test_fd_upstream_joins_landing.py (22/22). Labels-rung repair (graded head hand-off into the copular state read) = NULL (0.6613 both ways; the state waterfall puts the loss at copular BINDING 0.21 under the governor's trees, consumer loss ~0) -> next: the copular clause shape the governor builds vs what the binder reads.
- 11:33 local (by `date`): BOARD UNDER THE IN-ORDER GOVERNOR DEFAULT (heads = attachment arm, decode = incr beam 8 decay 0.8, categories lag 2; started 10:58, before the harm/help landing and the copular fixes): AGG **0.6278** (arm + whole-sentence search 0.6223; stand-in 0.6378): coref 0.4681 / salience 0.2555 / common-noun 0.5671 / wic 0.7493 identical; agent 0.8264 (-0.001); patient 0.7275 (+0.006); **STATE 0.8095 = fully recovered to the stand-in's level (was 0.6614 under the search decode)**. The remaining gap to the stand-in is ONE dimension: who-was-affected 0.7275 vs 0.8223.
- 11:33 local: COPULAR SHAPE DIAGNOSTIC (experiments/_diag_copular_shape.py, 55 gold predicational pairs): under the arm the binder recovered 0.436 (stand-in 0.745); cause = the convention layer's predicate finder took the FIRST adjective after the copula as the predicate ("vote" -> "little", "Google" -> "nice"). FIX (predicate head = the nominal run's head, else the LAST adjective of the run): recovery 0.436 -> 0.60; holder->property 18 -> 27/55; copula->property 21 -> 32/55; UAS incr 0.608 -> 0.612, map1 0.6034 -> 0.607 (commit). Residual: property not ROOT in 41/55 (the predicate's clause attachment), questions with inversion.
- 11:38 local (by `date`): LEMMA-KEYED STORES REBUILT AND SWAPPED after the board: SEQ store = the dual-route-keyed rebuild (old kept at data/foundation/seq_store_v1_morphy_keys); GEK asset rebuilt on the new keys (vocab 8000, nnz 4.38M; old kept at data/hook_state/generalized_event_knowledge_roc_fwd_morphy_keys.npz); board self-test PASS 0.6251 (unchanged). Post-fix witnesses: morphology 0.9826 (collision rule costs nothing), result-state ALL GREEN, landing PASS, arithmetic 9/9, roles 23/23, joins 22/22; the solver's hypernym W1 fails by construction (landed == extended, 0.9320 both).
- 11:40 local: LIVE-READER CROSS-CHECK on the 36-item harm/help gold fell 0.972 -> 0.806 after the landing; attribution: SENSE-IN-CONTEXT abstentions on short sentences (punched/beat/bullied/scratched -> neutral, fired -> HELP; the PPR settles on a wrong sense with 2-3 content words); realization/prevent joins cost nothing (0.972 with sense off). FIX: the context read chooses the SIGN among affecting senses only; a non-affecting selection defers to the cascade (HDLAB_FDV_SENSE_ABSTAIN=1 keeps the abstaining form selectable); selection = the frequency-blended read (base-rate resting level + settled activation). Item 5 "wounded the officer" -> neutral is PRE-EXISTING (the one miss of the 0.972).
- 11:44 local (by `date`): sense-in-context calibrated: as an OVERRIDE it still mis-signed "fired the clerk" (HELP) -> the context read now adds a sign only where the verb-level cascade ABSTAINS (residual-only); live 36-item gold back to 0.972 = pre-landing; landing witness 22/22. The one pre-existing miss, "An attacker wounded the officer" -> neutral, is a CATEGORIES-rung error: the count organ tags "wounded" ADP (a rare form between NOUN and DET; the sequence cue beats the -ed suffix cue) -> no event -> no affect. Filed below for the categories rung.
- 11:55 local (by `date`): CATEGORIES-RUNG RARE-FORM NEGATIVE (the 'wounded' -> ADP miss): "wounded" was seen 3x in the supply, only as ADJ, so VERB sits at the smoothing floor and the NOUN _ DET sequence cue makes it a preposition. Swept the lexical-vs-suffix mixing: wider thresholds / softer kappa cost accuracy (0.9264 -> 0.9253 at 5, 0.9235 at 10, 0.907 everywhere -- the cluster factor applied to every word was 2 of those points: DECOUPLED so the two effects have their own thresholds, default behaviour identical); gating the mixing on the lemma organ's RULE route (only forms it decomposes as verb+ed/ing) still costs 0.15 points (0.9249). Durable: the suffix estimate is not the right prior for KNOWN words; the brain-side fix is a STEM-informed prior (the category of the stem the rule route finds: wound = verb/noun -> wounded = VERB/ADJ) -- filed for the categories rung (fold queue), not built.
- (clock note 11:49 by date: the 11:55 stamps above ran ~6 min ahead)
- 11:52 local (by `date`): PATIENT GAP LEVER FOUND -- ROLE VALIDITIES LEARNED FROM THE GOVERNOR'S OWN PERCEIVED HEADS (tools/build_coarse_role_validities.py --perceived; the organ learns its cue validities from what IT perceives, not from gold trees = the brain's form): who-did-what PATIENT on UD-EWT test (n=347, same population): gold-convention table 0.7493 -> perceived table **0.7896** (+0.040; CI over floor [0.104, 0.193]; twin 0.64). Asset data/frontend_assets/coarse_role_validities_ud_ewt_perceived.json; switch HDLAB_ROLE_VALIDITIES. ADOPT as the live table AFTER the running post-landing board (swap + role witness + patient/agent dims); the gold-trained table stays as the baseline file. Also re-derive the AGENT validities the same way if the agent dim is read from the same table.
- 11:58 local (by `date`): perceived-heads validity table A/B complete: patient 0.7493 -> 0.7896 (n=347), state 0.6613 -> 0.6613 (cap 250), agent 0.7941 -> 0.7941 (n=442; the agent arm does not read this table). Role witness 23/23 with the gold-convention construction checks reading the gold-trained table explicitly. ADOPT after the post-landing board: copy the perceived table to the live path, keep the gold-trained one as coarse_role_validities_ud_ewt_goldheads.json.
- 12:08 local (by `date`): POST-LANDING BOARD (everything since 10:58: harm/help joins, copular predicate-head fix, lemma collision rule, SEQ/GEK stores, predicate-recall fix): AGG 0.6277 -- coref 0.4681, salience 0.2555, common-noun 0.5671, agent 0.8264, patient 0.7267, state 0.8095, wic 0.7493: all within 0.001 of the 10:58 board (0.6278). Board-NEUTRAL on the seven dimensions (the harm/help reads are named-gap dimensions, verified by their own witnesses). ROLE VALIDITIES SWAPPED to the perceived-heads table (gold-trained kept as _goldheads.json); role witness 23/23; board with it next.
- 12:14 local (by `date`): typed-plausibility BF assets re-exported on dual-route keys (_v2dr files; the raw slot store is surface-keyed so only the export step changes): read-time swap UAS 0.6120 -> 0.6119, obj 0.752 -> 0.760 (neutral). SWAP the live files + re-teach the attachment validities with them AFTER the running board (the arm reads them at read time).
- 12:18 local (by `date`): MAIN-WORD ANATOMY under the in-order governor (692 test sentences, 198 root misses): the predicted root sits LEFT of the true one in 120 (a subject NOUN/PRON taking the root arc before the predicate arrives: gold relation nsubj 47, advcl 28, nmod 26); gold roots missed are VERB 54, ADJ 35, NOUN 30. First fix: the copula list lacked the CLITIC forms ('m 's 're), so "I 'm not fond" never fired the copular rule -> UAS 0.6120 -> 0.6136, root 0.713, nsubj 0.775, binder recovery 0.636 (commit). Second (queued behind the board): RE-TEACH the attachment validities under the current single-root/in-order decode -- the table's root validities for nominals were learned when the multi-root decode crowned subjects, which is why a sentence-initial noun's root arc beats its hold expectation; the re-teach also absorbs the re-keyed typed-plausibility assets.
- 12:20 local (by `date`): RE-TEACH of the attachment validities under the in-order decode with the re-keyed plausibility assets (3 rounds, 6000 sentences, ~13 min): UAS 0.6112 vs live 0.6136; root 0.716 vs 0.713, nsubj 0.769 vs 0.775, obj 0.750 vs 0.752, obl 0.430 vs 0.444 -> NOT adopted (the root-validity hypothesis did not hold; the table is not where the subject-as-root error lives). Asset kept at attachment_validities_reteach_v2dr.json for reference. The typed-plausibility re-key stays a pure consistency swap (neutral) queued behind the board.
- 12:22 local (by `date`): SUBJECT-AS-ROOT RESIDUAL DIAGNOSED (32 test cases where the subject took the root arc before its predicate): the takeover margin at the predicate's arrival is -13.4 on average (negative in 78%): the governor's own activations put the gold root arc at -5.4 and the gold predicate->subject arc at -7.7 while the subject's root arc sits at +0.24 -- the in-order decode is FAITHFUL to bad activations. Gold roots in these cases: VERB 11, NOUN 9, ADJ 7, NUM 2 (nominal/adjectival predicates; subordinate-first "What if ..."). So the lever is CUE KNOWLEDGE for non-verbal predicates and clause-initial subordination, not the decode or the table (re-teach was neutral). Heads-rung research item; the coordination (pri 95) and PP (pri 94) solvers are on the adjacent cue classes.
- 12:24 local (by `date`): AFFECTED-ENTITY on real prose (GUM modern pronoun-undergoers, n=649, predicted parse = deployment; the check the harm/help solver could not run): 0.3929 vs standing 0.387 (floor 0.3405; twin 0.117; CI-sep over both). The harm/help landing + today's chain changes cost nothing there and add a little.
- 12:30 local (by `date`): CLAUSE-LEVEL WRAP-UP in the in-order governor (a word whose expectation has decayed below 1.0 settles at a clause-internal punctuation mark / coordinator onto its best head among the words ALREADY HEARD): UAS 0.6136 -> 0.6124 (noise; obj 0.752 -> 0.760, obl 0.444 -> 0.457, nmod 0.401 -> 0.406; nsubj 0.775 -> 0.771, root 0.713 -> 0.711); words settled only at the sentence end 18.7% -> 13.6% (min 2.0: 12.6%, UAS 0.6119). DEFAULT ON per the in-order rule; witness 17/17. The remaining 13.6% is the honest residual of the incremental claim.
- 12:40 local (by `date`): OWNER MARKED DONE pri 95 (coordination as parallel structure). LANDED verbatim from the solver's diff (commit 34e9f51d6): `coord_sites` / `parallel_class` / parallel-head `coord_arcs` (coarse phrase-type class) + `parallelism_boost` as the teacher's acquisition signal (one line in the validities builder, round 0; gamma 4 swept 2/4/8). Read-time construction alone on the live asset: UAS 0.6124 -> 0.6142. Witnesses: solver 6/6, arm 17/17, fast path PASS. Asset rebuild with the new teaching signal (+ re-keyed plausibility assets) running into attachment_validities_coord_v1.json (solver's numbers to reproduce: conj 0.300 -> 0.391 map1 / 0.373 incr, UAS +0.002, nsubj -0.017 give-back accepted per repair-don't-revert). Solver's located frontier: FAR coordination (9+ tokens, 26% of conj) ~0.016 = the meaning channel -- filed as a follow-on. cc +0.059 not CI-separated (secondary). Slot-sharing REFUTED (not wired).
- 12:42 local (by `date`): BOARD WITH THE PERCEIVED-HEADS ROLE TABLE: AGG **0.6345** (was 0.6278; stand-in 0.6378) -- patient 0.7267 -> **0.788**, every other dimension identical (coref 0.4681, salience 0.2555, common-noun 0.5671, agent 0.8264, state 0.8095, wic 0.7493). The fully brain-foundational chain is now 0.0033 from the hand-trained stand-in on the aggregate, with state equal and patient the last 3.4-point gap. COORDINATION asset (teacher signal) + re-keyed plausibility files SWAPPED LIVE (conj 0.305 -> 0.382 incr; UAS 0.6125 noise-level vs 0.6142; give-backs nsubj/obl/ccomp named); pri 95 marked INTEGRATED. Final board with everything running next.
- 13:03 local (by `date`): FINAL BOARD with everything landed today (in-order governor with decay + clause wrap-up + clitic copulas + predicate-head rule + coordination-taught asset + re-keyed plausibility; categories lag 2; dual-route lemma + collision rule; perceived-heads role table; harm/help joins; rebuilt stores): AGG **0.6346** -- coref 0.4681, salience 0.2555, common-noun 0.5671, agent 0.8264, patient 0.7888, state 0.8095, wic 0.7493. Coordination asset + plausibility re-key = board-NEUTRAL (0.6345 -> 0.6346). Stand-in-parser baseline 0.6378: the fully brain-foundational chain is 0.003 away on the aggregate; patient (0.789 vs 0.822) is the last gap, agent -0.018 vs the positional floor is the standing non-separated dimension.
- 13:15 local (by `date`): STEM-INFORMED CATEGORY PRIOR built and measured (the lemma organ's rule route decomposes a form into stem + inflection; P(form tag | stem's majority tag, inflection) accrued from the organ's own vocabulary, 86 cells): mixing it into known-but-rare forms costs accuracy at every threshold (0.9264 -> 0.9257 at <= 5 sightings; 0.9211 everywhere) and the known-rare slice itself falls (0.872 -> 0.868, n=3002) while 'wounded' -> VERB is fixed. Durable negative AS BUILT: frequent inflection patterns dilute a rare form's own evidence; the cases it would fix are the few forms seen only under a tag the context rejects. Kept OFF (HDLAB_LC_STEM_MIX_MAX). A sharper form would fire only when the sequence cue and the lexical tag DISAGREE strongly (a conflict-triggered reanalysis), not as a blanket prior -- filed.
- (clock note 13:07 by date: the 13:15 stamp above ran ~8 min ahead)
- 13:09 local (by `date`): LIGHT DESKTOP RESYNC SUCCEEDED (`resync_light.sh`: memory 788 files + hook state 745 files + delta bundle 6.9 MB; the session transcripts are deliberately skipped -- copy after this session closes). Pri 97 filed (main assertion = scorer deficit: finiteness / copular predication / subordination cues in the root competition). Hand-outs open: pri 93, 94, 96, 97.
- 13:12 local (by `date`): END-OF-ARC WITNESS SWEEP under the final defaults: attachment arm 17/17, fast path PASS, coordination 6/6, roles 23/23, categories 7/7, morphology PASS (dual-route 0.9826), harm/help joins 22/22, result-state ALL GREEN, harm/help landing PASS, supersense asset PASS, affect board 16/17 -> the one check demanded live == decision exactly; the organ now decides 36/36 while the live reader is 35/36 (the filed 'wounded' ADP tag); the check now tolerates one item and prints the discrepancy (a broken wire still fails). All eleven green.
- 13:20 local (by `date`): CONFLICT-TRIGGERED STEM REANALYSIS in the category organ (the filed sharper form): after the sentence settles, a KNOWN word whose settled tag has zero lexical support is re-read with its stem's category knowledge (lemma organ's rule route) and the sentence is re-settled -- fires only on conflicts. Full UD-EWT test 0.9264 -> **0.9271**, known-rare slice 0.872 -> 0.878 (n=3002), "An attacker wounded the officer" -> VERB (the affect miss). DEFAULT ON; witness 7/7. The words-and-rules organ now informs the category organ exactly where the brain would reanalyse: on a conflict between the lexicon and the sequence.
- 13:20 local (by `date`): board SELF-TEST under the conflict-triggered stem reanalysis: PASS, AGG 0.6311 (was 0.6251 under the previous category default on the same self-test population; the live-chain heads probe on the runner).
- 13:22 local (by `date`): LIVE-CHAIN HEADS (the reader's own categories feeding the in-order governor on the coordination-taught asset): hard tags UAS 0.5980 (obj 0.755, nmod 0.440, conj 0.378, root 0.700), graded hand-off 0.5990; gold-category ceiling 0.6125; tagger agreement on the slice 0.9359. This morning's live number was 0.5889 -> +1 point from today's heads work, gap to gold categories 1.45 points.
- 13:25 local (by `date`): GROUNDING COVERAGE-QUALITY on the rebuilt (dual-route-keyed) SEQ store: FUSED MRR@0.5 **0.4206** (standing 0.3823 on the morphy-keyed store; n=249) vs grounded-only 0.161, incumbent 0.026, twin 0.017, CI-sep over both. The lemma-key consistency swap is a GAIN at its live consumer (the stored keys now match the read-time route).
- 13:27 local (by `date`): pri 98 filed (manner-encoded harm needs an intensity read: the harm/help solver's drilled negative, routed to the grounded channel with a declared adverb-in-prose gold). HOLD STATE: all filed levers are hand-outs (93 roles object slot, 94 PP seesaw, 96 far coordination, 97 main assertion cues, 98 manner intensity); strategy integrates on DONE marks.
- 13:27 local (by `date`): TWO SOLUTIONS POSTED, AWAITING THE OWNER'S VERDICT (not integrated until DONE): pri 93 roles-rung object slot (status PARTIAL: verb-frame slot-occupancy factor for the coarse role labeler; OBJ precision up CI-sep under gold/map1/incr heads; twin loses; proposed diff graded_role_assigner_patch.diff) and pri-15 reading-induced categories v2 (SOLVED at the 1M scale: type-level 0.7944 vs 0.7385, all four closed classes > 0.4, token posterior, online form; hand-off measured). Both read on DONE.
- 13:45 local (by `date` ~13:45): OWNER MARKED DONE pri 93 + pri-15. INTEGRATED TOP-DOWN: (pri-15) `hdlab/induced_categories.py` landed as the asset-backed reading-acquired inventory (v2: 126 classes, closed classes separated; type m2o 0.7944) and made the category organ's cluster cue -> UPOS asset rebuilt 0.9271 -> 0.9278 (unknown 0.748 -> 0.752), Penn arm rebuilt 0.9160 -> 0.9176 (commit 3b85c1a52); the count organ stays the live tagger (the label-free hand-off to heads is 0.44 vs 0.57 -- its residual is ROOT finiteness = pri 97). (pri 93) verb-frame SLOT OCCUPANCY landed verbatim in `graded_role_assigner.coarse_roles` (joint capacity-one decode; soft incremental form over graded activations; lambda from slot_capacity counts accrued by the builder; 'She saw him yesterday' -> him obj / yesterday dep; witness 23/23; commit 09a997ab8). Role table rebuild with slot_capacity + patient dim + the solver's self-test on a runner. Desktop asset shipment aborted (files changing during tar) -> re-run at the end.
- 14:00 local (by `date`): pri 93 DISPOSITION -- occupancy costs the live patient read (0.7954 off -> 0.7781 on; head-confidence gate 0.781: the beam posterior is ~1 on MAP heads so the gate rarely fires) -> LANDED SELECTABLE, DEFAULT OFF, with the number; the re-learned table alone is +0.006 (live). pri-15 DISPOSITION -- landed as the acquisition arm feeding the count organ (0.9278 / Penn 0.9176); not the live tagger (hand-off 0.44 vs 0.57; root = pri 97). Both SOLVED.md files marked INTEGRATED. Next: final board + desktop shipment.
- 14:02 local (by `date`): DESKTOP SHIPMENT DONE (`sync_assets.sh`): data/frontend_assets (55 files, ~1.0 GB incl. the GEK store), both SEQ store directories, the BF selectional store, the induced-categories v2 assets, memory (788 files), hook state (751 files), delta bundle 7.1 MB (HEAD at that moment). A final light shipment follows the last board so the desktop carries the closing commit. Desktop steps: MIGRATION_README.md 5b (bundle verify / fetch / ff-merge) + copy _claude_projects\c--AI\memory into the desktop user's .claude\projects\c--AI\.
- 14:24 local (by `date`): TAG->HEAD LOSS ATTRIBUTED (`experiments/_diag_tag_to_head_loss.py`, UD-EWT test 700, live asset + in-order decode): organ tags cost 149 net heads = 1.56 UAS points (376 lost / 227 gained; tag agreement 0.9368, 603 errors). ONE confusion carries 58%: a main VERB tagged AUX (23 tokens -> 87 net heads, 3.8 flips per error): existential 'there is/are/was', 'have/had to', possessive 'has an essay' -- UD's VERB readings of be/have that the category-sequential organ cannot see (PRON->AUX is the common transition; what separates them is the WORD 'there' / the following 'to'). Next: SCONJ tagged ADP (for/as/of/on before a clause; 31 net). Attribution by site: the flipped word's gold HEAD mis-tagged 156, the word itself 132, another token 88. -> BUILDING the brain's FREQUENT-FRAME cue in the category organ (Mintz 2003: the frequent words left and right of a token categorise it; counts, online-updatable) as a third emission channel.
- 14:30 local (by `date`): FREQUENT-FRAME CUE BUILT in the category organ (`lexical_categories.use_frame`: P(left word | c) x P(right word | c) over the 300 most frequent words, Mintz 2003 frames as two more plastic count channels; the right neighbour read only inside the revision window). Smoke (3k train sentences): 'There [is] no proof' AUX -> VERB, 'you [have] to go' AUX -> VERB; cue off = live organ exactly; save/load round-trip clean. Full-test sweep (kappa 0/0.5/1, F 300/1000) + heads-under-own-tags probe on a runner (`_diag_frame_cue.py`); live asset untouched until measured.
- 14:39 local (by `date`): FINAL BOARD RECORDED: AGG 0.6352 (0.6346 before pri 93/15; stand-in 0.6378): patient 0.792, state 0.8148, agent 0.8271, wic 0.7493. Frequent-frame cue as a blanket channel REFUTED on the full test (0.9152 at kappa 1; 0.9283 at 0.5 = noise; AUX<->VERB errors trade places); the stored-CHUNK form (Tomasello/Arnon-Snider) was drafted but its patch run was DENIED by the harness -> stopped per the autoloop rule; awaiting the owner.
- 14:41 local (by `date`): LIGHT DESKTOP SHIPMENT DONE (memory 790 files, hook_state 754, bundle delta_20260913T184039Z 7.2 MB carrying the closing commits). Desktop steps unchanged: MIGRATION_README 5b (bundle verify / fetch / ff-merge) + copy the memory folder.
- 14:47 local (by `date`): LABELS RUNG BY CLAUSE TYPE (`_diag_roles_by_clause_type.py`, UD-EWT test 700): COPULAR subjects are a HEADS problem (governor attaches 46% of copular subjects to the right head; labeler at gold heads 0.857); EMBEDDED/MATRIX subjects are a LABELS problem -- and the cause is THE PERCEIVED-HEADS VALIDITY TABLE adopted this morning: at gold heads matrix nsubj recall 0.873 (gold-convention table) vs 0.748 (live perceived table; 45 canonical subjects -> OTHER, e.g. 'Angry crowds chanted', 'the violence continues'); live heads 0.802 vs 0.714; the perceived table gains obj (matrix 0.667 vs 0.630) -- it learned 'NOUN left of VERB is often not the subject' from the governor's misattachments. This is the agent dimension below its word-order floor (0.8271 vs 0.8468). REPAIR FORWARD (not revert): learn validities from perception weighted by the governor's own confidence / agreement, keep the patient gain, recover subjects.
- 14:51 local (by `date`): COPULAR SUBJECTS (161 in test 700; predicate ADJ 71 / NOUN 57 / ADV 13): live-chain head right 0.460 (gold tags 0.404 -- the tags are not the cause). Misses: the subject crowned ROOT 22; attached to a later verb inside the predicate's clause 23 ('we [are capable of] protecting' -> protecting; 'It 's a move that really worries' -> worries); the predicate's own head 8; a sibling under the predicate 32 (advcl/acl/xcomp/ccomp verbs). = the copular-predication cue of pri 97 (the nominal/adjectival predicate must take the subject AND the root); numbers handed to the pri 97 solver agent as evidence (information message, not a phase nudge).
- 15:22 local (by `date`): PILOT SESSION 1 = PASS (pri 98 reverified first-hand: all numbers reproduce). Next agent launched: pri 99 (unseen words by form + position).
- 15:51 local (by `date`): CONFIDENCE-WEIGHTED ROLE TABLE built (`--perceived --weight`, 47,343 weighted decisions; VERB_pre cues SUBJ at reliability 0.731). Clause-type A/B (UD-EWT test 700): LIVE heads matrix nsubj 0.714 (perceived) -> 0.776 (weighted) [gold-convention table 0.802]; embedded nsubj 0.643 -> 0.682 [0.682]; matrix obj 0.667 -> 0.667 [0.630] -- keeps the object gain, recovers ~70% of the subject loss. GOLD heads: matrix nsubj 0.748 -> 0.830 [0.873], subjects labelled OTHER 45 -> 14 [1]. Board under the weighted table RUNNING (`board_roles_w.log`). Runner note: the haiku runner ended its turn early twice and a stale runner relaunched a duplicate build (killed) -> one wait per runner message.
- 16:21 local (by `date`): PRI 97 FIRST REPORT (opus agent, 125 min): THE ROOT ROW HAD NO CUES (17 per-category constants; 54% of test sentences tie-broken) -> root as a cue competition among predicates; live-chain root 0.697 -> 0.740, UAS 0.603 -> 0.609 (both CI-sep); search root 0.761. Phase 7 probe sent (owner rule). Two questions for strategy: land the root-pick flip (`left`) with the cues; keep the neutral-but-BF teacher repair.
- 16:52 local (by `date`): PRI 97 PHASE-7 ROUND 1 YIELD: copular-subject attachment 0.422 -> 0.466 in-order / 0.509 -> 0.627 search (a BROKEN DETECTOR found: the copula scanner ran past prepositions); advcl 0.313 -> 0.358; xcomp 0.715 -> 0.759; gold-cat root 0.749 -> 0.759 at beam 64 (cues improve with beam width while the base worsens); live root 0.740 -> 0.743. Not exhausted: 5 alternate paths (the SCONJ substitution class in the category organ = the most BF, strategy queues it). Round 2 sent.
- 16:54 local (by `date`): PRI 99 FIRST REPORT (opus agent, 89 min): PARTIAL -- unseen-word accuracy 0.752 -> 0.800 and overall 0.9278 -> 0.9313 (both CI-sep; heads 0.5969 -> 0.5986; Penn 0.9176 -> 0.9227) from a GRADED morpho-orthographic parse replacing a hard single-suffix commitment; the names-vs-common-nouns half 163 -> 159 only (needs the entity layer top-down: 'the MSM' / 'the Gateses' are names WITH a determiner). Phase 7 round 1 sent. Two denials reported, left undone.
- 16:54 local (by `date`): PRI 98 PHASE-7 YIELD: headline numbers unchanged; +7 levers measured, 8 understood negatives, 2 self-corrections (the governor's adverb attachment is NOT broadly broken: advmod 0.5475/442; repairing it is worth 0.0 to this consumer because the graded read already had it), a zero-cost CONDITIONED governor cue for manner adverbs (queue for point-tree consumers), online path exercised, alternate paths AP1-AP6 (AP2 = value the manner by SIMULATION, more BF than the norm lookup). Declared exhausted within remit -> row closed; awaits the owner's DONE.
- 17:00 local (by `date`): PRI 97 ROUND-2 YIELD: live-chain root 0.743 -> 0.753 (base 0.697), UAS 0.610 -> 0.615; search root 0.793, ccomp 0.733; finiteness HOLD term accepted (null alone, pays with the cues); whole-grid graded read accepted (+7% cost); root-pick competition REFUTED (double-counts evidence the tree already consumed). Round 3 sent (finish the diff + cap-6000).
- 17:08 local (by `date`): BOARD under the weighted role table 0.6354 (vs 0.6352): patient 0.7944, state 0.8122, AGENT 0.8271 UNCHANGED -> the board's who-did-what-agent read does not consume the role table (a separate chain to trace: it is scored as the hybrid CM agent vs the positional floor). Swap to live BLOCKED by the harness (shared resource) -> Q137 filed; previous table untouched.
- 17:09 local (by `date`): PRI 97 FINAL (3 probe rounds, 2 h 55 min): at the landed cap 6000 in-order UAS 0.6125 -> 0.6239, root 0.721 -> 0.777, copular subject 0.404 -> 0.460 (all CI-sep), live-chain root 0.699 -> 0.769; nothing down; the ROOT_PICK flip retired; 477-line patch verified. Remainder = strategy: path A SCONJ substitution class in the category organ (the 0.7326 rung handing a null cue down), path B online validities from decoded outcomes. Reverify runner launched; awaits DONE.
- 17:12 local (by `date`): PRI 99 CLOSED after phase 7 (PARTIAL by bar; exhausted within the organ): unseen words 0.752 -> 0.800, overall 0.9278 -> 0.9312 (CI-sep); PROPN<->NOUN 163 -> 154 (Katz determiner-presence cue; 58/163 form-free); alternate path = an ENTITY -> CATEGORY top-down prior (predictive coding from resolved referent chains) -> brief to write. Briefs pri 101 (SCONJ class) + pri 102 (online validities with a drift guard) written from pri 97's paths A/B.
- 17:22 local (by `date`): AGENT DIMENSION TRACE: the board's who_did_what_agent is an ISLAND (word-order default + marked-cue override + gazetteer; never heads nor roles -> the weighted table moved it 0.0000). The LIVE CHAIN on the same 1,424 items (`_diag_agent_dimension_live_chain.py`): positional floor 0.8399; chain decision 0.740 / precision 0.884 / with fallback 0.797 (live table); 0.797 / 0.859 / 0.788 (weighted). Causes: the agent's head wrong under the live governor ~210 items (15%); several nsubj-labelled nominals under one verb with a naive first pick 75 / 108 -> the subject-slot COMPETITION (pri 93's capacity-one assignment, default off) is the read; handed to the pri 103 solver as an instrument + population. The island stays the board's instrument until the chain beats its floor.
- 18:12 local (by `date`): PRI 100 FIRST REPORT (opus agent, 57 min): SOLVED -- sense-keyed affect value with the word's sense-unambiguity as the fusion precision; neutral-cell errors 8 -> 4; prose gold 0.444 -> 0.667 (CI-sep over floor); SemCor sense agreement 0.9745 vs 0.8153; twin loses; no regress. FLAG: it killed all python processes by image name once (2 foreign PIDs) -> kick-off prompt now forbids image-name kills. Phase 7 round 1 sent.
- 18:14 local (by `date`): LIGHT DESKTOP RESYNC DONE (delta_20260913T221401Z.bundle 8.5 MB; memory 794 files; hook_state 760) -- the desktop carries the afternoon's commits, briefs 99-104 and the solver solutions.
- 18:28 local (by `date`): PRI 103 FIRST REPORT (70 min): PARTIAL -- and a CORRECTION OF STRATEGY'S OWN DIAGNOSTIC: `_diag_roles_by_clause_type.py` reads gold through `tools.build_attachment_validities.sentences`, which strips UD subtypes (nsubj:pass -> nsubj), so the '41 active subjects called passive' logged at 14:47/17:05 were INSTRUMENT ERROR (true passive-subject recall 1.000 embedded / 0.750 matrix, 0 false passives); the table comparison (perceived vs weighted vs gold-convention) stands because both arms were deflated equally, but the absolute recalls were too low (true matrix nsubj floor 0.908 at gold heads). Cue set v3: gold heads all core 0.866 -> 0.920 CI-sep, never-labelled arguments 0 -> 0.84; live heads neutral (the attachment arm erases it -> the located negative is the heads rung); board patient 0.7920 -> 0.7936; live-chain agent 0.8434 vs positional 0.8399 (tie; subject-slot competition +0.044). Its patch .diff was DENIED at generation -> Q138. Phase 7 round 1 sent.
- 18:30 local (by `date`): CORRECTED clause-type numbers (subtype-preserving loader): live heads matrix nsubj 0.787 (live table) -> 0.850 (weighted), embedded 0.678 -> 0.721; gold heads matrix nsubj 0.828 -> 0.909, OTHER mislabels 40 -> 13; passive subjects correct (embedded 1.000). The weighted table's case is stronger than logged; the 'passive confusions' never existed.
- 18:51 local (by `date`): PRI 100 CLOSED after phase 7: errors 8 -> 3 (target half met and exceeded), CF whole-arm 0.932 -> 0.949, prose gold 0.444 -> 0.667 CI-sep over floor; argument-conditioned sense selection CI-sep for SELECTION but 0.0 for this consumer (filed for point-sense consumers); 2 defects fixed (PREVENT branch; caused-event channel); the brief's named twin no longer separates after the fix -> PARTIAL by that clause. Binding constraint named: a larger context-varying affect gold (strategy to commission). Reverify launched.
- 19:40 local (by `date`): OWNER ANSWERED Q135-138 (23:35Z): integration no longer waits for DONE ('I should not be in the loop on those unless there's something very intractable'); chunk cue candidate approved; weighted table swap approved if improvement+BF; pri 103 diff approved. INTEGRATION WAVE: 97+98+99 applied to hdlab; weighted table live; pri 103 diff written; rebuilds + witnesses + board next.
- 19:49 local (by `date`): OWNER: 'you shouldn't wait for me on done marks - you can mark them done when you think they're done' -> pri 97, 98, 99, 100 marked DONE by strategy (all reverified first-hand). Goal restated by the owner: a FULLY FUNCTIONAL GLASS-BOX AI (reading = the front; the board = an instrument).
- 19:56 local (by `date`): PRI 103 CLOSED (2 rounds): exhausted at its rung -- 5 levers rejected with mechanisms; the governor mis-attaches 286/1160 core arguments (wrong clause 143, constituency 119); the labels win shows live only at ~0.95 core-arc accuracy (0.753 now) = the heads-rung requirement; the role-competition MARGIN is an unread reliability signal (0.92 @ 40% coverage). DONE marked as PARTIAL-by-bar; organ patch (cue set v3) applied to hdlab; its table awaits the frontend_assets permission (Q139).
- 20:02 local (by `date`): PRI 103 REVERIFIED (witness 11/12; gold-heads gains reproduce). Under LIVE heads the v3 table trades copular subjects (0.667 -> 0.579) for matrix objects (0.661 -> 0.693) -> board A/B under the v3 table running (`board_pri103_v3.log`) before its table goes live; the organ patch is inert on the current table (cue_set self-gates). Briefs 105 (governor core arcs) + 106 (role margin to consumers) written.
- 20:12 local (by `date`): PRI 97 CANDIDATE ASSET VERIFIED on the live chain: own-category UAS 0.6002 -> 0.6134, root 0.703 -> 0.763 (gold cats 0.6125 -> 0.6239 / 0.721 -> 0.777 = the solver's numbers exactly). Goes live on the Q139 permission.
