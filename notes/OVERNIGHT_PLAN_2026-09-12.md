# OVERNIGHT PLAN — 2026-09-12 → 13 (owner asleep; autoloop ARMED; owner direction: "make the top of the chain as BF as possible, and fix all downstream 1 at a time")

**Operating rules for every continuation:** SHORT runs (< 10 min) go to RUNNER agents (`hdi_exp_dev`, model haiku); LONG runs (boards, 20-60 min) are launched as ONE chained background Bash job owned by the main session (foreground call -> auto-moved to background -> completion notification) because runner-held long jobs have died repeatedly when the agent's tool call/turn ended (00:18, 00:50 tonight); never poll/wait in the main thread;
commits PATH-LIMITED, never push; never edit `preregs/**` / `arm_key*`; every landed change gets a witness + a ledger line; a
downstream dip after a BF upstream is a CONSUMER TO REPAIR, never a reason to revert the BF rung; one decisive config per run.
Live BF configuration = `HDLAB_TAG_SOURCE=counts` (default) + `HDLAB_HEADS_SOURCE=attachment_arm` (the target default once the
board arms read the reader's parse). Baseline board (perceptron + supervised parser) 0.6395 for reference only.

## Phase 0 — finish the open integrations (owner-DONE)  [status: DONE 23:45]
- [x] pri-13 semantic hub: `hdlab/semantic_hub.py` + asset + `tools/build_semantic_hub_asset.py` + witness 6/6 → commit (+ the
      solver's 3 files routed through get_output_dir), registry BF_SPIRIT, INTEGRATION_LEDGER row (PARTIAL: representation landed,
      read-time fusion kept, readers not repointed), §2b audit note, INTEGRATED mark.
- [x] pri-10 causal store (REFUTED): commit the named files (get_output_dir-routed), INTEGRATION_LEDGER row + manifest (AS-DURABLE-
      NEGATIVE: store as necessity prior; the +0.139 headline = density artifact → retired claim), INTEGRATED mark; follow-on filed:
      `sm.causal_direction` default-off read + `board_causal_direction_dimension` (density-matched twin + reverse control).

## Phase 1 — TOP OF THE CHAIN, BF, one rung at a time
1a TOKENS — enumerate the live tokenizer path (`situation_reader` → regex); confirm no nltk/spaCy; record in SIGNAL_FLOW_MAP §0.
1b CATEGORIES (live organ = `hdlab/lexical_categories`, 0.912):
   - graded hand-off to heads: `attachment_arm.arc_scores_graded(toks, tag_posterior)` = mixture over the top-2 categories of
     uncertain tokens (P(top) < 0.8); measure UAS on UD-EWT test 700: gold / hard-predicted / graded-predicted (runner).
   - route the board arms' PRIVATE taggers (`causation_typing._frontend`, `exp_board_*` `PosTagger.load`) through ONE category
     source (the reader's switch) so the BF tagger reaches every board dimension; A/B.
   - acquisition arm: `LexicalCategories.accrue` over (word, induced class) from the k=68 asset → the organ's counts from READING;
     hand-off test via `build_attachment_validities --categories` (pri-15's solver owns the inventory; this is the organ's arm).
1c HEADS (live organ = `hdlab/attachment_arm`, 0.556; self-grown plausibility): route the who-did-what / coref board arms through
   `situation_reader._cached_parse_heads` (+ head posterior) so `HDLAB_HEADS_SOURCE` reaches them; copular convention in the
   convention layer (AUX → the predicate content word; the state dim residual); rebuild asset; measure per relation.
1d ROLES (BF): re-measure the 596-item decision + role probe on the BF chain; graded hand-off already landed.

## Phase 2 — DOWNSTREAM, one consumer at a time (board A/B after each; runner)
state (0.656 under BF heads; residual = heads) → who_did_what_patient/agent (after 1c routing) → coref/common-noun (if affected by
tags) → affect / causal / temporal arms that read tags → the affected-entity chain (state register, fusion rule).

## Phase 3 — MINE the last 20–30 solved problems for upstream work already proven (owner 23:35)
Scan `notes/problems/*/SOLVED.md` sections "UPSTREAM", "Full-stack upstream", "PROPOSED hdlab", "SUBSTRATE INCORPORATION MANIFEST"
for INCORPORATE items not yet folded (check INTEGRATION_LEDGER + `INTEGRATED_BY_STRATEGY` marks); list them in
`notes/UPSTREAM_FOLD_QUEUE_2026-09-13.md` ranked top-down by chain position; fold the ready ones one at a time with witnesses.

## Phase 4 — bookkeeping at milestones
ledger / STATUS / SCORECARD / BRAIN_MATH_REFERENCE / owner updates (significant only); delta sync to the desktop when the tar stream
finishes (`bash /c/AI/hd-instrument_desktop_2026-09-12/delta_sync.sh` via a runner; verify on the desktop).

## Log (append one line per completed item; newest last)
- 23:40 plan written; board A/B (counts; counts+attarm) running on a runner; transfer ~205/212 GB.
- 23:45 Phase 0 done: pri-13 hub organ landed (PARTIAL; readers not repointed); pri-10 recorded as durable negative (+0.139 retired); both committed.
- 23:05 1b: graded category hand-off landed + measured (gold 0.5562 / hard 0.5340 / graded 0.5379); ONE shared frontend `hdlab/frontend.py` landed and the patient/agent/state/causation arms shimmed onto it (the switches now reach the board); self-test on a runner; post-routing boards queued after the running pair.
- 23:15 transfer stream to the desktop FINISHED (pipe exit 0, 18:03Z->03:27Z = 14:03->23:27 local); delta sync + desktop verify on a runner. Copular clause convention added to the attachment arm (decode-time; witness 15/15); state-dim + per-relation measurement on a runner. Board self-tests PASS under default and fully-BF switches after the shared-frontend routing. Fold queue written (13 items; #4 = unreconciled second heads-rung organ from the pri-2 solver: reading-learned EM scorer + constructions, beats supervised OOD on GUM +0.0235).
- 23:25 1c: copular convention landed (state 0.6587 CI-sep under BF heads); heads-rung reconciliation: the arm subsumes the pri-2 reading-learned scorer; its Hindle-Rooth PP cue FOLDED into the arm ('pp' cue; association grown treebank-free) -- asset rebuild + per-relation on a runner. Board 'counts' (reader-only tagger switch, pre-routing) finished; counts+attarm running.
- 23:35 1b acquisition arm: tools/build_lexical_categories_from_reading.py (category organ counts over the induced classes from raw reading; 20k smoke 0.704 agreement, unknown words handled); 1M-line build + agreement + heads hand-off on a runner. Reader-only tagger board recorded (0.6385; only STATE moved).
- 00:05 1b acquisition arm MEASURED: reading-acquired category organ 0.7242 agreement (closed classes ~0 = pri-15 target); fully reading-learned chain UAS 0.4649 -> 0.4922. Desktop state re-sync verified (26,615 session files). PP-cue rebuild in its last round.
- 00:15 PP cue full rebuild: UAS 0.5583 -> 0.5706, nmod 0.185 -> 0.346 -- asset PROMOTED to live (witness 15/15). Pri-17 effectively delivered through the fold; pri-16 (sibling) remains parked.
- 01:05 post-routing boards C (counts reaching every arm) + D (fully BF chain, PP asset) relaunched as ONE session-owned background job after the runner-held pair died silently; module-import launcher; completion notification expected ~02:15.
- 01:50 FIRST COMPLETE fully-BF board (runner's relaunched B, post-routing): AGG 0.6222; flips = patient 0.729 (-0.09), agent 0.821 (-0.01), state 0.659 (-0.17, CI-sep); coref/wic/salience identical. Phase 2 starts with the PATIENT arm (graded head hand-off into its role read).
- 02:50 Phase 2 #1 (patient arm graded hand-off) = NULL (0.735 hard vs 0.732 graded, n=347): the patient loss is the heads rung's obj attachment (upstream). Next: agent arm once board C separates tagger vs heads; state = heads rung copular predicate.
- 02:55 Phase 2 #2 (agent arm graded category read) WORKS: 0.8209 -> 0.8357 (pre-BF 0.832 recovered). Queued: the same graded candidate read for the patient arm's candidate set and the state holder.
- 03:05 CORRECTION: the runner's boards C (00:08) and D (00:39) were ALIVE all along -- my relaunches reused their log paths and truncated the files, so they looked dead; my duplicate processes were stopped to free the CPU; the originals finish and write the metrics. Rule: never reuse a log path for a relaunch; check `Get-CimInstance Win32_Process` for a live process before declaring a job dead. (The 21:40 graded board WAS genuinely lost -- no process existed.)
- 03:10 Phase 2 #3 (state reader graded read) = NULL (0.7989 -> 0.8016). Phase 2 done for the three flips: agent repaired; patient/state losses are upstream (heads obj; tagger). Waiting on boards C/D (runner originals, ~3 h each under contention).
- 03:30 Heads lever from the object anatomy: ORDER-AWARE semantic bootstrapping (object association after the verb, subject association before it; SUBJ-slot asset grown from the same store) -- teacher now prefers energy<-creating 4.5 vs energy<-wanting -0.3; full rebuild into a separate file running (session job); promote if obj/nsubj rise without UAS loss.
- 03:55 order-aware teacher = REFUTED AS BUILT (obj +0.008 but ccomp/xcomp collapse; UAS -0.005); flag default off; mechanism recorded (slot associations on different scales). Boards C/D still running.
- 04:05 POST-ROUTING BOARDS: counts tagger everywhere 0.6358 (agent -0.011 [repaired since], patient -0.012, state -0.029); fully BF chain 0.6219 (patient 0.728, state 0.653; PP cue moved none of these dims; rest identical). Board runtime with the arm ~3 h -> decision filed on the owner's board: flip the heads default now vs vectorise first (recommend: vectorise first).
- 04:35 teacher v2 (order-aware + root = subj fit + obj fit + PRONOUN participants scored by the verb's pronoun-filler rate from the self-grown store; the v1 collapse mechanism = pronoun subjects invisible to the typed association) -- full rebuild running as a session job; promote only if obj rises AND ccomp/xcomp hold (0.466/0.686) AND UAS >= 0.5706.
- 04:55 teacher v2 PROMOTED: obj 0.685 -> 0.700, nsubj 0.749 -> 0.762, root 0.813, obl 0.468, ccomp held, UAS 0.5694 (noise), nmod 0.346 -> 0.311 (next item). Patient dim under the new asset on a runner.
- 05:00 HONESTY NOTE on the 04:55 promotion: the bar I wrote at 04:35 was 'obj rises AND ccomp/xcomp hold AND UAS >= 0.5706'; v2 came in at UAS 0.5694 and ccomp 0.457 (vs 0.466), both inside noise, while all four core relations rose. I promoted on the core-relations reading (the board's consumers read obj/nsubj/obl/root). Revert is one copy from attachment_validities_pp_prev.json if the patient dimension does not improve.
- 05:20 patient dimension under the v2 asset: 0.7283 (n=1255) = IDENTICAL to the previous asset's 0.7283 -- the obj gain (+0.015 on UD) did not reach the board's patient read (checking that the two assets actually differ on the board's sentences). nmod anatomy (UD test 700, 534 gold nmod): wrong 349 -> 368; the growth is nominals pulled to a VERB on either side (verb-left 125 -> 138, verb-right 48 -> 58), PP objects 117 -> 127, possessive pronouns ('its' -> 'expanded' instead of 'wares'). Two semantic-bootstrapping rules written behind HDLAB_SBT_PP_NOSUBJ: (1) a pre-verbal PREPOSITIONAL object is case-marked oblique -- no subject plausibility; (2) a pronoun DETERMINING a following nominal is a possessive, not a participant -- no pronoun-filler evidence. Full rebuild with both ON running on a runner (separate file).
- 05:45 FINDING: only 89 of 5224 head decisions (1.7%, 64/300 sentences) differ between the previous and the v2 asset -- the teacher shapes the validity COUNTS but the arm is MEANING-BLIND at read time, which is why the patient dimension did not move. Built the Competition Model's semantic-plausibility cue as a READ-TIME cue ("plaus": slot S/O x binned participant plausibility from the self-grown store, validity learned like every other cue; the refuted hard gate's graded form) behind HDLAB_ARM_PLAUS_CUE (default off). Bin distribution on 150 test sentences: 60% zero, S:hi 346, O:mid 271, O:hi 167. Second rebuild running (case-marking rules + plaus cue) so the pair of builds differ only in the cue.
- 06:40 Q134's blocker attacked: the attachment arm's readout VECTORISED (numpy over the head x dependent grid; strength tables indexed once into dense arrays). Witness verification/test_attachment_arm_fastpath.py: max |fast - reference| 1.8e-15 over 80,848 pairs, MAP heads 40/40 identical, scorer x4.3 (arm ~89 -> ~16 ms/sentence incl. the tree decode, which is now the next cost). The reference loop is kept as `arc_scores_reference`. Dual-route lemma arm landed in hdlab/morphology.py (mode="dualroute", default "morphy"; witness on a runner).
- 06:50 case-marking rules rebuild (HDLAB_SBT_PP_NOSUBJ=1) = NULL: UAS 0.5693, nmod 0.311 (unchanged), ccomp 0.466 (+0.009), nsubj 0.763 -- the teacher's argument rules barely reach read time (the 1.7% finding again); flag stays OFF. The nmod give-back is a READ-TIME matter: the meaning-cue build (running) is the real test. Morphology witness: default arm byte-identical (exit 0; the cell also re-measures dual-route 0.9826 vs morphy 0.9594 and BF-POS 0.9398 -> 0.9484); dual-route mode diverges on 13/2000 full-path lemmas by design.
- 07:05 meaning cue cost: the teacher's plausibility is ~0.8 s/sentence COLD (the typed store's scoring of each new verb x noun pair; warm cache ~0) -- the reference loop pays the same; if the cue shows a gain, precompute/vectorise the typed scoring next. Board A/B for the dual-route lemma arm launched on a runner (HDLAB_MORPH_MODE=dualroute, everything else default; compare dim-by-dim to counts_routed 0.6358 -- only lemma-consuming dims should move; a same-code baseline follows when the CPU frees).
- 07:20 MEANING AS A READ-TIME CUE v1 (case-marking base + plaus): UAS 0.5698; nmod 0.311 -> 0.375 (+0.064), ccomp 0.466 -> 0.500, obj 0.705, conj 0.253 -- but obl 0.468 -> 0.379 and nsubj 0.763 -> 0.745: the cue fired on PREPOSITIONAL objects too (plausibility 0 as a direct object pushes a PP object off its verb onto a noun = nmod/obl trade). v2 = core slots only (a case-marked nominal is oblique; its host is the PP cue's business); rebuild running. Lemma helpers `lemma_word/lemma_verb(word, pos=None)` now take the consumer's category first (fold-queue item 2's hand-off; default order unchanged).
- 07:55 HEADS-RUNG COLD COST REMOVED + a read-time tool taken off the live path: the typed-plausibility organ's noun typing called nltk WordNet `synsets()` per word at read time. Exported the noun -> most-frequent-sense supersense table offline (tools/build_noun_supersense_asset.py; 117,798 lemmas, 26 classes, 3.7 MB) and read it as a dict. Witness verification/test_noun_supersense_asset.py: 0 mismatches on 1,347 test nouns; 0.86 s vs 23.5 s; no nltk on the read. ALSO FOUND (fold queue): the live reader's temporal event extraction runs nltk's PerceptronTagger at read time (5 reader sites) -- a NOT-BF tagger on the live path; building the PENN-TAGSET ARM of the count-based category organ to replace it.
- 08:05 PENN-TAGSET ARM of the category organ built (UD-EWT xpos supply; 49 tags; 12,544 sentences): UD-EWT test 0.9075 vs nltk PerceptronTagger 0.8508 on the same tokens (+5.7 points, and brain-foundational). Wired into the temporal ORDER organ behind HDLAB_TEMPORAL_TAGGER (default still nltk pending verb-form recall + event-agreement + board). Smoke: 'had not waited' -> the arm tags VBD (nltk VBN; the first-order transition loses had->participle across 'not') -> candidate repair in the organ's had-window rule if VBN recall is the loss.
- 08:20 TEMPORAL TAGGER FLIPPED to the category organ's Penn arm (live default; nltk kept as `HDLAB_TEMPORAL_TAGGER=nltk`): verb-form recall arm/nltk VBD 0.942/0.928, VBN 0.819/0.797, VBZ 0.955/0.941, VB 0.893/0.891, MD 0.993/0.990; weaker on VBG 0.892/0.953 and VBP 0.825/0.880. Events on 600 UD-EWT test sentences: 326 vs 373, 296 shared, 292 same tense; nltk's extras include "'s"->VBZ artefacts, the arm's include a correct "I read an Article". The live reader no longer runs an off-the-shelf tagger at read time. Board no-regress run queued behind the dual-route board (CPU).
- (clock note, 05:45 local / 09:45Z): the entries stamped 05:00-08:30 above ran AHEAD of the wall clock by ~2.5 h (I was estimating); their order and content are right, the real local times are roughly 2.5 h earlier. From here on stamps are the real local clock.
- 05:55 local: CATEGORY SWITCH MADE TOTAL. Enumeration (agent, file:line table): 9 live sites still instantiated the NOT-BF perceptron tagger directly (reader affect + relative-clause paths, space / crosstype / joint-relation / perceptual-access / world-model readers, 3 board arms), all calling only `.tag()`. Fix at the one choke point: `PosTagger.load()` returns the shared `hdlab.frontend.Tagger` unless `HDLAB_TAG_SOURCE=perceptron` (frontend's own perceptron arm uses `_raw=True`). Smoke: routed type Tagger, perceptron selectable. Board self-test + 3 witnesses on a runner. NOTE: the queued Penn-arm board now also carries this routing (two flips in one run; if a dim moves, disentangle with HDLAB_TEMPORAL_TAGGER=nltk).
- 06:05 local: CORRECTION to the 05:55 note -- the Penn-arm board actually started at 05:42 local (the runner did not wait), i.e. BEFORE the PosTagger.load routing edit, so it measures the Penn flip ALONE (clean attribution). The routing needs its own board after the CPU frees (four heavy jobs now: dual-route board 05:20, Penn board 05:42, board self-test 05:46, meaning-cue v2 rebuild at round 2).
- 06:15 local: board SELF-TEST PASS with the total category routing in place (new arms OFF; 19c demoted); the three organ witnesses follow on the same runner.
- 06:25 local: witnesses after the total category routing: coarse-role competition 23/23, attachment arm 15/15 (lexical categories running). Meaning-cue v2 rebuild at its final evaluation.
- 06:00 local: MEANING AS A READ-TIME CUE v2 (core slots only) PROMOTED TO LIVE: UAS 0.5694 -> 0.5726 (best so far), nmod 0.311 -> 0.358, ccomp 0.466 -> 0.500, obj 0.700 -> 0.710, conj 0.223 -> 0.253; costs nsubj 0.762 -> 0.753, obl 0.468 -> 0.430 (the nmod/obl seesaw of PP attachment; the PP cue owns it). Defaults flipped ON: HDLAB_ARM_PLAUS_CUE, HDLAB_SBT_PP_NOSUBJ (the asset is built with both). Previous live asset kept at data/hook_state/attachment_validities_v2teacher_noplaus.json. Fast-path witness PASS; full arm witness + patient dimension on a runner. The heads rung now carries meaning at acquisition AND comprehension time.
- 06:02 local: full arm witness 15/15 under the promoted meaning-cue asset + new defaults. Re-running the object-error anatomy under it to pick the next heads lever (previously 126/400 misses: 60 to the NEXT verb on the right, 31 nominal, 16 verb-left, 10 root).
- 06:10 local: object anatomy under the promoted asset (400 test sentences, 274 gold objects): recall 0.719, 77 misses -- next-verb-right 30 (was 60 of 126 on the earlier population), nominal 23, verb-left 11, root 8. PUNCTUATION found to be the largest single UAS loss: 12.2% of test tokens at 0.273 (the teacher's posterior over marks is noise). Gold-tree statistics: final mark -> root 95%; a mark -> top of its left neighbour's head chain 61%; combined rule (final->root, opening bracket/quote -> right chain, else left chain) 0.665 on gold trees vs always-root 0.527. Landed as a CONVENTION-LAYER post-decode rule (no meaning claimed; consumers never read punctuation heads; HDLAB_ARM_PUNCT_CONVENTION=0 disables); measuring on the live asset.
- 06:15 local: PUNCTUATION CONVENTION measured on the live asset: UAS 0.5726 -> 0.5956 (+2.3 points), punct 0.273 -> 0.453 (gold-tree bound 0.665; the arm's own tree caps it); every other relation identical by construction. Kept ON (convention layer; honest label: formatting, not meaning; board-invisible). Heads rung now 0.5956 on UD-EWT test with gold categories (0.4826 when the arm landed; supervised stand-in 0.78).
- 06:20 local: FOUND a hand-off defect in the shared frontend: `frontend.Parser.parse` (attachment-arm source) reports as each word's head the ARGMAX OF ITS MARGINAL, not the decoded tree (CLE MAP) -- the two disagree on ~10% of tokens (2728/3032 agree), and an argmax assignment need not even be a tree. Every board consumer under HDLAB_HEADS_SOURCE=attachment_arm has been reading that, not the 0.5956 tree. Measuring both against gold now; the fix is to serve the MAP tree as the point estimate and keep the marginals as the graded signal.
- 06:35 local: DECODE finding. Per-token argmax of the Matrix-Tree marginals beats the MAP tree on every content relation (ccomp 0.56 vs 0.50, advcl 0.23 vs 0.15, conj 0.29 vs 0.25, xcomp 0.70 vs 0.68, obl 0.45 vs 0.43, nsubj/obj/nmod +0.005-0.01) but is not a tree (87/300 sentences) and loses root 0.69 vs 0.81 -> overall 0.5904 vs 0.5956. Built the MBR TREE (CLE over log-marginals): keeps the content gains (UAS 0.5902, root still 0.699 because several root arcs get spent) -> constrained to ONE root (largest root marginal), measuring. The frontend now serves the decoded tree (`attachment_arm.decode`) as its point estimate + the marginals; before it served the raw argmax (non-tree) to every consumer.
- 06:45 local: MBR decode variants: summed-marginal tree 0.5902 (root 0.699); one root by the largest ROOT MARGINAL 0.579 (root 0.604 -- the root marginal is the least calibrated row of the posterior: it spreads over every verb); one root by the MAP TREE + MBR for the rest: measuring. The content-relation gains of MBR (ccomp +0.03..0.06, advcl +0.09, conj +0.03, obl +0.02) are consistent across variants.
- 06:20 local: MBR + MAP-root (soft): UAS 0.5954 with ccomp 0.672 (vs 0.500), advcl 0.313 (vs 0.149), xcomp 0.730, conj 0.300, obj 0.718, obl 0.449, nmod 0.365 -- but root only 0.704 because the MAP root's own marginal row often prefers another head, so the forced root arc lost to it (rootless structure). Pinning the root HARD (its column -inf except the root arc): measuring.
- 06:28 local: FINDING behind the root puzzle -- the arm's "MAP" decode was NEVER single-rooted: `graded_parser.chu_liu_edmonds` lets several words take the root arc (e.g. roots [4, 22], [3, 6], [6, 17, 23] on test sentences), so its root recall 0.813 counted extra roots and every such sentence handed consumers several disconnected clauses. The MBR tree is single-rooted by construction (root 0.704 = one guess per sentence). Like-for-like comparison now running: MBR (root = the best-scoring MAP root) vs single-root MAP vs the old multi-root MAP.
- 06:30 local: THE SINGLE-ROOT CONSTRAINT IS THE LEVER, not MBR. Single-root MAP tree (root = the highest-scoring MAP root): UAS 0.5956 -> 0.6034; nsubj 0.767, obj 0.725, obl 0.463, nmod 0.365, xcomp 0.730, ccomp 0.672 (from 0.500), advcl 0.313 (from 0.149), conj 0.300, punct 0.457; root 0.744 (the old 0.813 counted extra roots). MBR with the same root pick 0.5855 (root 0.637) -- the marginal-sum tree is worse than MAP once both are single-rooted. Comparing root picks (leftmost vs highest score) next, then adopt single-root MAP as the default decode.
- 06:40 local: DEFAULT DECODE = SINGLE-ROOT MAP (HDLAB_ARM_DECODE=map1, root pick = highest root score; leftmost scored 0.5959): heads rung UAS 0.6034 on UD-EWT test (gold categories) -- from 0.4826 at landing, 0.5726 at this morning's promotion. Witnesses 15/15 + fast path PASS; the frontend serves this tree to every consumer (before: a non-tree argmax). Remaining per-relation: root 0.744, nsubj 0.767, obj 0.725, obl 0.463, nmod 0.365, xcomp 0.730, ccomp 0.672, advcl 0.313, conj 0.300, punct 0.457.
- 06:45 local: anatomy under the single-root decode (400 test sentences): objects 274, recall 0.737 (72 misses: next-verb-right 29, nominal 27, verb-left 11; 30 had the gold verb's object slot filled by ANOTHER nominal); subjects 769, recall 0.767 (179 misses: to a verb on the right 38, noun-right 30, verb-left 28, noun-left 20, adjective 15). Building the object-slot OCCUPANCY constraint (the parked pri-16 lever) as a decode-time repair: one bare object per verb; the extra moves to its best unsaturated head.
- 06:50 local: OCCUPANCY repair REFUTED AS BUILT: UAS 0.6034 -> 0.5995; obj 0.725 -> 0.655, obl 0.463 -> 0.415 (nmod 0.365 -> 0.403). A verb legitimately takes two bare nominals (ditransitive, adverbial/temporal NP, predicate nominal, apposition), so "one bare nominal per verb" evicted true objects. Durable negative: slot occupancy is a LABELS-rung constraint (one OBJ per verb, role labeler decides), not a category-level decode rule. Flag HDLAB_ARM_OCCUPANCY kept, default OFF. pri-16 (parked) should be re-scoped to the labels rung.
- 06:55 local: coordination convention MEASURED, NOT LANDED: on gold trees the right conjunct's head = nearest preceding same-class word before the coordinator 0.538; top of the left chain (same class) 0.589 -- too weak for a convention rule (the arm does 0.30 on conj, 3.4% of tokens; best case +0.8 UAS with its own imperfect tree). Coordination needs a real structural account (shared-dependent / conjunct span), not a convention.
- 07:00 local: HEADS UNDER THE LIVE CATEGORY ORGAN (the real live-chain number): gold categories 0.6034 | count organ hard tags 0.5761 (root 0.691, nsubj 0.732, obj 0.710, ccomp 0.603, nmod 0.395) | graded hand-off 0.5781 (root 0.660, obj 0.693, nmod 0.412, ccomp 0.569) -- the graded mixture is +0.002 overall but mixed per relation (it hurts root/obj/ccomp, helps nmod/obl); the 2.5-point gold-to-live gap is the tagger's 0.912. Both boards alive (dual-route at the WiC/causal stages; Penn at 65 cpu-min); their runners mis-reported "terminated" -- watchers on the logs remain.
- 07:15 local: CATEGORY ORGAN ARM v2 (same count-based generative model; three more count tables, all plastic): orthographic SHAPE emission factor + rare-word mixing -> 0.912 -> 0.9165 (unknown words 0.637 -> 0.707); SECOND-ORDER transitions with Dirichlet back-off -> 0.9177; both -> 0.9229 (unknown 0.726). v1 code path reproduces 0.912 exactly (regression). Gap to the perceptron stand-in 0.945: 3.3 -> 2.2 points. Promoting v2 to the live UPOS asset (v1 kept at data/hook_state/lexical_categories_counts_v1_order1.json); Penn arm v2 to be measured for read-time cost (49 tags, pair states).
- 07:20 local: LIVE-CHAIN HEADS under the v2 category organ: hard tags 0.5761 -> 0.5892 (obj 0.740, nsubj 0.745, root 0.713, ccomp 0.621, advcl 0.321); graded hand-off 0.5894 (obj 0.735, nmod 0.382). The gold-to-live gap is now 1.4 points (0.6034 vs 0.589). The chain from raw text: tokens -> v2 count categories (0.923) -> v2 meaning-cue arm with single-root decode (0.589 UAS) -> roles, all brain-foundational, all plastic.
- 07:25 local: PENN ARM v2-lite promoted (first order + shape + rare-word mixing; same read-time cost): xpos 0.9075 -> 0.9127 (nltk 0.8508). The second-order Penn variant (49 tags -> pair states) is ~15 ms/token and still computing; not a read-time candidate regardless of its accuracy -- recorded when it lands. v1 plain Penn kept at data/hook_state/lexical_categories_counts_penn_v1_plain.json.
- 07:35 local: temporal aux-window rule (VBD after 'had' with only adverbs between -> participle) = NULL on gold: 7 such gold participles in 1500 test sentences, 6 recovered, but 4 gold simple pasts misread -- kept behind HDLAB_TEMPORAL_AUXWINDOW, default OFF. Penn second-order arm: xpos 0.9191 (vs 0.9127 first-order+shape) at 547 s / 25k tokens = 17x slower -> not a read-time arm; recorded for the reference.
- 07:45 local: READING-ACQUIRED CLUSTER CUE in the category organ (the induced 70-class inventory grown from raw simplewiki as an emission factor P(class | category) for rare and unknown words): UPOS 0.9229 -> 0.9264 (unknown words 0.726 -> 0.749; the inventory covers 549 of the 1,882 unknown test tokens). The organ's reading arm now informs its supervised arm. Promoted to the live UPOS asset; Penn variant measuring.
