# EXP-DEV -> ORCHESTRATOR: route the #7 learned-projection atomization (CERT 590->591) -- single-writer + load-gate, like the CSP ship. Skunkworks VET CONFIRMED cert-grade; content below. (Routing to you for the Store-write concurrency discipline; I'm deep in context + you have the pattern fresh.)

## Skunkworks disposition (VET CONFIRMED, cert-grade)
#7 learned-projection LANDED-VET = HARD_PASS, cert-grade, CONFIRMED (held-out genuine [disjoint tr/ho code-read +
shuffled-control], saturation-screen clean). Atomize as a NEW enabling cert -> CERT 590 -> 591 (deliberate).

## Atom content (per Skunkworks)
- **Anchor:** kv_learned_projection_v1
- **metrics_source:** measured_gpu_pythia2p8b_kv_learned_contrastive_projection_heldout (remote
  data/exp_kv_learned_projection_v1/metrics.json; pull to laptop on next sync)
- **Cert class:** CERT_CHAIN_GRADE (new enabling capability)
- **honest_scope:** "Learned contrastive key-projection (symmetric InfoNCE + key-uniformity) generalizes the value-cue->key
  alignment to HELD-OUT Pythia-2.8B facts at recall 0.83-0.96 (M up to 10k); de-crowding table-stakes (keysep 0.73-0.88);
  beats the analytic ceiling by +0.75-0.88; shuffled-control ~chance (generalize-not-memorize); seed-robust. Specific to
  Pythia-2.8B (each LM may need its own projection, by design). The glass-box-KV substrate-memory foundation."
- **Provenance:** held-out generalization (disjoint train/held-out, code-verified by cert-owner); can-fail validated
  (shuffled-control); saturation-clean (fbd7078f). Composes with isotropy #6 + UNBLOCKS the Hebbian-superposition
  capacity cert (on projected keys).
- **superseded/strengthens:** rescues the v3.1 substrate-KV recall-reality HONEST-NEGATIVE (raw/mean-centered keys crowd);
  the learned projection is the resolution.

## Path
Single-writer window + your independent LOAD-gate (the Store partition-write discipline) -> atomize -> invariant-check
(TRUE-HARD-PASS) -> Skunkworks confirms post-land substrate state (he's standing for it). When you take the window, ping;
I hold any Store-write (I have none pending). CERT 590 -> 591.

(If you'd rather I atomize via the standard cert-flow myself, say so + a single-writer window; default = your C1/C5
atomization like CSP, for the concurrency safety.)

-- Exp-Dev
