# READER-SPACE MAP + DEEP LESSONS -- session synthesis (2026-07-23)

**Purpose:** the reader/conversational research space is now COMPREHENSIVELY MAPPED (this session + prior). Read this BEFORE proposing any reader-component experiment -- to avoid re-running mapped space (the recurring waste). ~65 MM banked this session (29405-29469), ZERO false chain-grade. All findings VET-confirmed.

## THE ONE-LINE TRUTH
On natural text the easy heuristic (frequency / position / animacy / locality) usually WINS, so the HARD operation is rarely REQUIRED -> nearly every reader mechanism lands at strong-MM. The substrate's genuine, defensible EDGE is NOT raw accuracy (ceilinged) but INSPECTABLE HONESTY (glass-box confidence + structure -> a reader that knows when it's on solid ground), demonstrated on NARRATIVE text.

## WHAT WORKS (banked, real)
- **Trustworthy NARRATIVE reader (29465-29468):** never confidently wrong (halluc 0.000) on McGuffey + LitBank, recovers abstentions (correct-to-head), via coref feeling-of-knowing + coherence/conflict + NP-head structure. GENRE-SPECIFIC (see bounds).
- **Grounding binding MECHANISM (29457/29459):** attention-gated multi-object binding solves illusory conjunctions + IMPROVES with exposure (Treisman + Posner-Keele). Synthetic toy, idealized localization.
- **Agreement depth rule (29450) + learned depth accumulator (29453/29455):** beats the buried-subject wall; learned content generalizes productively to unseen DEPTH (structure x content thesis). Not autonomous-induction.
- **Composition (b1 29445):** glass-box replay-traversal composes over given structure.

## WHAT'S BOUNDED (mapped ceilings -- do NOT re-dispatch without a genuinely new mechanism)
- **Parser ~0.81 UAS:** search (29458) + valency (29460) both HARD_FAIL to break it; ceiling is DISTRIBUTED/SEMANTIC (~42% PP+coord+clausal need meaning). Global-beam training, 2nd-order, rich-lexical-features all bounded/null.
- **Relation-extraction recall ~0.38:** PATIENT_SELECTION_HURTS, DENSIFIES_ANIMACY_BOUND, RECALL_ARTIFACT -- mapped/bounded (the accuracy/coverage bottleneck).
- **Roles/SRL:** crude-cues-sufficient on narrative; reversible/non-canonical = rare-and-hard (the role_assigner v2-v4 workstream). DIALOGUE fails here (role-misassignment).
- **Compgen via native binding:** CLOSED at strong-MM (free by construction of the fixed VSA algebra; real-text + synthetic + geometry-ablation all -> MM). Selectional coherence = a genuine ceiling.
- **Autonomous structure-induction from text:** brain-SHARED bound (29456; the pop/closure cue is prosodic, absent in text; needs supply-structure-or-supervision).
- **Cross-sentence comprehension:** blocked 3x (self-contained/locally-resolvable on real text).
- **WSD beyond frequency:** frequency ceiling.
- **OOD/genre generalization of the trustworthy gate (29469):** NARRATIVE-specific; DIALOGUE fails (calibrated signals anti-correlate + role-misassignment); NP-head fired 0/0/0 on new genres.

## DEEP LESSONS (the durable takeaways)
1. **STRUCTURAL signals transfer WITHIN an error profile, not across genres.** NP-head (structural) generalized narrative->narrative where the calibrated coref-margin did not; but NP-head fired 0x on dialogue/news (each genre has its OWN error classes). Calibrated-confidence signals are corpus-specific; structural signals are error-class-specific.
2. **Metacognition is corpus-specific** (brain-shared: humans mis-calibrate on unfamiliar material). The substrate CAN know when it's on solid ground -- within its calibrated domain.
3. **Supply structure + learn content WORKS; autonomous induction from text is bounded.** The brain has pre-given structural scaffolding + gets closure from prosody (absent in text). Our fair task = supply structure, learn content (29455).
4. **ranking-AUC != single-threshold gate-ability** when errors cluster at the confidence ceiling (29464).
5. **Composition works over GIVEN structure; the systematicity of fixed VSA algebra is FREE-BY-CONSTRUCTION, not learned CG.**
6. **The VET is load-bearing:** the Director over-read ~a dozen+ times this session (generality, failure-breadth, numbers, mechanism-framing); every over-read was caught by the adversarial VET before banking. ZERO false chain-grade across 65 banks. HOLD to CLAIM/VET-pending on every positive.

## THE HONEST FORKS (all LARGE -- USER-steer, not increments)
1. **Broader structural-signal library** (role-disambiguation for dialogue, proper-name-head, per-genre margin recalibration) -> generality across genres. But role-disambiguation = the mapped roles ceiling; LARGE + uncertain.
2. **Raw-accuracy frontier** (parser semantic-integration / relation-recall) -> walled by mapped ceilings; the meaning-integrated-parsing angle (constraint-based lexicalist) is itself mapped (selectional = ceiling).
3. **Vision-grounding real frontier:** object localization/SEGMENTATION on REAL images (grounding is mechanism-validated on toy scenes only). Genuine unbuilt work, but a large vision undertaking + a detour from the text-conversational goal.
4. **Accept + consolidate:** the NARRATIVE trustworthy reader + the comprehensive map + the deep lessons = a coherent, defensible milestone.

## DISCIPLINE FOR FUTURE SESSIONS
- DEDUP FIRST (grep experiments/ + KB substrate_query) before ANY reader dispatch -- this space is mapped; ~7 dedup sweeps this session each found the "new" lever already banked.
- Per tick #7: idle is FINE if genuinely nothing non-filler-and-non-mapped remains. Do NOT dispatch re-runs to look busy.
- The genuine remaining moves are LARGE undertakings that warrant USER steer, not autonomous increments.
