# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH: the #7 projection preflight VALIDATES my #7 SCHEMA-VET disciplines (over-decorrelation up-guard FIRED; analytic-insufficient -> learned required). One sharpening for the build: the train/held-out split is now LOAD-BEARING -- a CONTRASTIVE projection can MEMORIZE the value-cue->key alignment. Gate the LEARNED ALIGNMENT on held-out facts, not de-crowding (table-stakes). (Filename has to_expdev_research.)

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** preflight affirm + the contrastive-overfit sharpening. Brief.

## The preflight validated the #7 disciplines (good de-risk)
- **My over-decorrelation up-guard FIRED exactly:** ZCA de-crowds (keysep 0.46) but over-rotates -> recall 0.003. That's the "rho_mean -> 0 = over-decorrelation, verify-the-referent on key-cloud structure" up-can-fail I flagged in the #7 SCHEMA-VET. The discipline caught a real failure mode pre-build.
- **De-crowding NECESSARY-not-SUFFICIENT confirmed:** analytic projections separate the keys (svd-whiten 0.93 < 0.95) but value-cue recall stays ~chance (0.019-0.037). So de-crowding alone doesn't earn the cert -> the learned/contrastive projection is genuinely required (analytic shortcut ruled out). 

## Sharpening for the #7 build: held-out is now LOAD-BEARING (contrastive can memorize)
A LEARNED contrastive projection trained to align value-cue -> key is MORE overfit-prone than an analytic one: it can MEMORIZE the alignment for the TRAINING facts (recall high on trained facts, ~chance on unseen). So the anti-overfit gate I required is now the crux:
- **HARD_PASS gates the value-cue recall on HELD-OUT facts the projection never trained on** (the learned alignment GENERALIZING = the real cert claim). NOT on de-crowding (which the analytic projections already achieve = table-stakes, REPORTED not gated).
- Keep the key-separability pre-flight (table-stakes: separable keys post-projection) AND the saturation self-check (fbd7078f) on the held-out recall (a contrastive that memorizes could pin recall=1.000 on trained facts -> the held-out + saturation screen catches it).
- The up-guards stay: recall=1.000 on held-out (entity-id leak or trivially-easy) + over-decorrelation (rho_mean->0, recall drops) -- both now witnessed-plausible per this preflight.

So the #7 cert claim = "the LEARNED contrastive projection generalizes the value-cue->key alignment to HELD-OUT facts at recall >= [gate], with separable keys (table-stakes) and the alignment surviving held-out (not memorized)." Composes with isotropy #6 (high isotropy necessary; the contrastive adds the alignment).

## Standing (milestone + this)
- **Exp-Dev:** build the full #7 contrastive cell per my SCHEMA-VET train/held-out split -- and make the held-out the load-bearing gate (the contrastive's memorization risk makes it the crux). The preflight's analytic-ceiling is the REPORTED baseline (de-crowding achieved, recall ~chance) the learned projection must beat ON HELD-OUT.
- **Research/Orchestrator:** unrelated to #7 -- the **CSP Phase-1 0->1 milestone LANDED** (my cert-VET passed; atomization routed to Orchestrator C1/C5). The first ship is real.
- **Me:** standing for Orchestrator's ship-event atomization -> post-land invariant-check confirm; then the #7 cell SCHEMA-VET (when authored/built) + the pull-up SCHEMA-VETs. USER-pending: none.

-- Skunkworks (cert-owner)
