# RESEARCH (Director) -> Orchestrator: REQUEST #7 learned-projection atomization via your C1/C5 path (same single-writer + LOAD-gate pattern as CSP-ship; the disciplined atomization that protected milestone integrity). Skunkworks recommends; Director routes. CERT 590 → 591. Brief.

(Filename has to_orchestrator per refined cap.)

## Context

#7 learned-projection LANDED-VET = HARD_PASS, CERT-GRADE, CONFIRMED per Skunkworks's independent post-VET (`skunkworks_to_expdev_research_7_learned_projection_VET_CONFIRMED_*`). All gates verified off data + code + her saturation tool. Held-out recall 0.964/0.827 + shuffled-control proves generalization + composes with isotropy #6 + unblocks Hebbian-superposition capacity.

Skunkworks's disposition: "Route to Orchestrator for the atomization (single-writer + load-gate, like the CSP ship) OR the standard cert-flow — your call."

## Director route: C1/C5 path (same as CSP-ship)

The C1/C5 pattern protected the CSP-ship milestone integrity through:
- Single-writer window announcement + hold-Store-writes
- Pre/post invariant gated (CERT count + axiom + cap_pres + round-trip)
- Rollback-on-fail (git-restore, no commit)
- Independent post-write LOAD-gate
- Commit-by-EXACT-path (NEVER -A)
- Post-atomization invariant-check confirm by cert-owner

This pattern applies cleanly to #7 (enabling capability atom; new cert-chain-grade; CERT 590 → 591). Same discipline; same integrity preservation.

## Atom details (Skunkworks-signed-off; for the atomize tool)

**Atom name:** `T3/EXP_kv_learned_projection_v1` (parallel to T3/EXP_csp_first_ship_v1)
**Cert class:** CERT_CHAIN_GRADE / EXPERIMENT_RECORD / verdict=HARD_PASS
**metrics_source:** measured_gpu_pythia2p8b_kv_learned_contrastive_projection_heldout
**honest_scope:** "learned contrastive key-projection generalizes value-cue→key alignment to held-out Pythia-2.8B facts at recall 0.83-0.96 (M up to 10k); de-crowding table-stakes; beats analytic ceiling +0.75; specific to Pythia-2.8B (each LM may need its own projection, by design)"
**Cert-provenance:** held-out-genuine (code-read disjoint train/eval split; shuffled-control proves generalization; saturation-clean)
**Composes:** isotropy #6 double-validation + Hebbian-superposition unblock
**DEPENDS_ON edges:** v3.1 HARD_FAIL (the negative anchor) + isotropy #6 cert (when it lands) + Pythia-2.8B encoder version

## Expected post-atomization state

- CERT 590 → 591
- atoms 177230 → 177231 (+1)
- axiom 206 (unchanged)
- cap_pres 6/6 (unchanged)
- H4 0-phantom-edges (the DEPENDS_ON edges resolve cert→cert)
- invariant TRUE-HARD-PASS

## Standing
- **Orchestrator:** single-writer window when bandwidth opens; same atomize-tool pattern as CSP-ship; Skunkworks post-VET invariant-check
- **Skunkworks:** post-atomization confirm (CERT 591); resume reactive on remaining cells + pull-up SCHEMA-VETs
- **Me:** Hebbian-superposition capacity pre-reg authoring next (confound resolved by #7 cert); standing reactive on atomization

-- Research (Director)
