# RESEARCH (Director) -> USER: substantive update -- depth-cliff VERDICT COMPLETE with both atoms LANDED + HYP-4 0.200->0.853 NEW data point (3 HARD_PASS on the recovery axis; scaling rule "n-hop needs n-level" now has 3 data points; recovery 0.993/0.931/0.853 measures coverage-scales-with-depth empirically). + 4-layer cert-discipline catch this hour (Exp-Dev verify-the-referent CAUGHT MY mining-script claim on PP-371; I counter-verified the Store directly; my claim was actually correct -- but the multi-layer verification IS the discipline working).

**From:** Research (Director)  **To:** USER  **Date:** 2026-06-18  **Re:** depth-cliff completion + HYP-4 NEW + 4-layer cert-discipline. ASCII; fname_v2.

## Bottom line (one sentence)

The 20h-plan centerpiece is now FULLY ATOM-GROUNDED: Phase A FLAT (CERT_CHAIN_GRADE HONEST_NEGATIVE; CERT 569->570 additive) + Phase A2 2-level recovery (MEASURED_MECHANISM ATTRIBUTION; +1,110 second-hop edges; 0 new atoms) -- with **HYP-4 0.200->0.853 NEW data point** confirming the "n-hop needs n-level" scaling rule has 3 progressive recovery data points (0.993 at 2-hop, 0.931 at 3-hop, 0.853 at 4-hop -- the decay measuring residual N-level-out-of-corpus miss) + cert-discipline working at a 4th layer this hour (Exp-Dev verify-the-referent CAUGHT my mining-script claim on PP-371 housekeeping; I counter-verified the Store directly and re-confirmed my claim; the multi-layer verification IS the discipline working in BOTH directions, even when the eventual answer is unchanged).

## HYP-4 = NEW substantive data point worth surfacing

We didn't pre-discuss HYP-4; the focus was 2/3-hop bands. The recovery 0.200 -> 0.853 (+0.653) is actually the sharpest single delta and load-bearing for two reasons:

1. **3 progressive data points on the scaling rule.** "Complete 2 levels of canonical parent-links: 2-hop -> 0.993, 3-hop -> 0.931, 4-hop -> 0.853." The progressive decay measures residual N-level-out-of-corpus miss exactly, consistent with "coverage requirement scales with depth."
2. **Confirms diagnosis isn't artifact of a specific depth.** Same 2-level intervention recovers 2/3/4-hop bands proportionally. If HYP-4 had stayed flat, we'd have to consider an algorithmic ceiling at 4-hop; recovery removes that hypothesis. **Strengthens the coverage-limited diagnosis substantially.**

The 333 "among-new" edges (out of +1,110 total; new intermediates' OWN parents within in5k corpus) account for the extra reach to 4-hop -- the gold-blind rule extends naturally to 2-level + partial-3-level completion. All caveats remain: HYPERNYM/taxonomic/WordNet/in5k scope; coverage-scales-with-depth not one-shot; coextensiveness on magnitudes; diagnosis-plus-lever not blind-capability-magnitude.

## The 4-layer cert-discipline catch this hour

This is the cert-discipline working at a layer I didn't have visibility into before:

**Layer 1:** Director substrate-mining script reports `RETRIEVAL_reasoning_routing_pp371 current_best = "T2/prototype_bundle_cleanup"` + recommends back-fill to `PP-371_reasoning_routing` (housekeeping side-item from the main RETRIEVAL_multi_hop update).

**Layer 2:** Exp-Dev (Prover) does cert-honest sequencing on the capability-update batch AND ALSO does verify-the-referent on Director's claim: re-reads the Store, returns None, FLAGS the discrepancy ("Director's premise's referent isn't in the Store; SKIPPING").

**Layer 3:** Director (me) counter-verifies via direct grep of the jsonl file. Result: the Store DOES have `current_best_solution: "T2/prototype_bundle_cleanup"` for `RETRIEVAL_reasoning_routing_pp371`. My original claim was correct.

**Layer 4:** Both files filed (Director's counter-verify + Exp-Dev's catch); Exp-Dev re-verifies; the AUDIT_LESSON is the disagreement-as-information (likely cause: similar atom names `PP-371_reasoning_routing` vs `RETRIEVAL_reasoning_routing_pp371` are confusable + the gap atom IS the one that returns None; OR Python Store API regression -- we saw it return 0 atoms earlier in this session for `all_atoms()`).

**What's load-bearing:** the answer (back-fill IS valid; my original claim correct) DOESN'T matter much for the discipline. What matters is the multi-layer verification CAUGHT and SURFACED the disagreement before either side acted on it. **This is verify-the-referent working in both directions.** Composes with the NEGATIVITY-BIAS-symmetric rule (don't accept either "yes there's a source" OR "no there's no source" without verification; the discipline cuts both ways).

This is the same compounding pattern as earlier today (Exp-Dev catches FrameNet partial-ingest -> proactive fix -> Skunkworks self-catches SCHEMA-VET gap -> 8th gate candidate emerges then properly declined). Cert-discipline catches its own custodians at every layer; today the discipline caught DIRECTOR's mining-script claim too. Substrate-autonomy at the meta-meta-layer.

## Substrate state right now

- atoms 43,890 + 2 EXPERIMENT_RECORD additions (the depth-cliff verdict atoms)
- **CERT 570** (Phase A FLAT additive); Phase A2 doesn't add CERT (MEASURED_MECHANISM tier; coextensive)
- HYPERNYM edges 5,103 + 1,110 second-hop = 6,213 total (depth-cliff backbone now 2-level dense)
- FRAME_* edges 2,070 unchanged (FrameNet LANDED earlier)
- axiom_term 206/206 + cap_pres 6/6 preserved through all 3 mutations today (FrameNet + T3 Phase A + T3 Phase A2)
- self-cert engine 7 gates LIVE (8th gate candidate atom-add-mechanism declined-as-engine kept-as-checklist; engine stays clean)
- 4-session architecture: Orchestrator REPLACEMENT SESSION UP + cleared blocker_ping_38 (your bootstrap prompt worked)

## What's still in-flight (cascade)

- **Skunkworks recovery tier-verify** on Phase A2 2-level atom (Exp-Dev routed: verdict=ATTRIBUTION + MEASURED_MECHANISM + CERT 570 unchanged + coextensive caveat). Expected fast.
- **Skunkworks capability-update VET-on-landing** on the RETRIEVAL_multi_hop + PP-multihop_revival current_best change (cert-evidence chain complete now that both atoms landed). Light-touch verify.
- **Skunkworks PP-371 co-verify** (your update on the disagreement; my counter-verify says back-fill IS valid).
- **Testbed Phase A2 2nd-witness** (standard 8-pattern + STRENGTHENS edge composite). Phase A FLAT already HARD_PASS 8/8.
- **Exp-Dev re-verify on PP-371** + (on confirm) execute the back-fill.
- **Orchestrator** A2 v6 dispatch on the checkpointable rebuild path.
- **Skunkworks** 2 small methodology atoms at bandwidth (cert-architecture engine/checklist separation + optimal-per-evidence cert-VET unification).

## What I'm waiting on / who's blocking

- **Skunkworks (highest):** recovery tier-verify + capability-update VET-on-landing + PP-371 co-verify + bandwidth atoms
- **Exp-Dev:** PP-371 re-verify + (post-VET) apply capability-update batch + A2 pre-cache rebuild + kill-restart-test + A2 v2 dispatch outcome
- **Testbed:** Phase A2 2nd-witness; any new HARD_PASS verifies
- **Orchestrator (REPLACEMENT):** A2 v6 dispatch on checkpointable rebuild path
- **USER (you):** nothing currently gated on your sign-off. Heads-up moments next: ARC-3 second-direction menu (ConceptNet-led) is just-in-time stagger; phase-portrait v1 has 2 fresh data-points (backbone-density-axis + HYP-4 progressive scaling) to ground it.

You have the strongest cert-run of the program complete with the deepest evidence-chain we've yet assembled: depth-cliff diagnosed + scaling-rule quantified + cert-discipline catches running at 4 layers including catching the Director. The pattern continues.

-- Research (Director)
