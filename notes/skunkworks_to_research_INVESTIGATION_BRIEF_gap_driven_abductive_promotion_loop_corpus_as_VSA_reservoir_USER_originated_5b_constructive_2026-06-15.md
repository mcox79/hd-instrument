# SKUNKWORKS (Auditor) -> Research (Director): INVESTIGATION BRIEF -- USER-originated PATH for Claim 5b-constructive. The GAP-DRIVEN ABDUCTIVE PROMOTION LOOP with the corpus as a VSA reservoir (single-match / combine / interpolate). NEED-PULL not generate-push. Requested by USER: "share with director for a full investigation."

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-15  **Re:** USER design dialogue (this session) culminating in a path that routes around every wall the CONSTRUCT line hit. USER directive: full investigation.

## PROVENANCE (honest)
The core ideas here are USER-ORIGINATED in live design dialogue: (1) invention should be NEED-PULL (do tasks, see failure, get the piece that fixes it) not generate-push; (2) abduce the SHAPE of the missing piece and PATTERN-MATCH the corpus; (3) the match can COMBINE multiple corpus atoms and/or INTERPOLATE between them. Skunkworks refined: reverse-math grounding for the shape, OEIS-as-reservoir, the gap-closure-sidesteps-R2 observation, and the native-VSA framing. This composes with the team's empirical findings (5x drill retraction of grounding-bound; CONSTRUCT-1 hollow; CONSTRUCT-2 R1-works/R2-representational/R3-scale-confound). Per the bootstrap-OK ruling, USER design direction is legitimate; this is substrate-on-its-own (no LLM in the loop).

## THESIS
The "substrate cannot invent new math" conclusion was triply over-narrowed: (a) "needs external truth" was RETRACTED (5x drill); (b) CONSTRUCT-1/2 showed construction-in-a-vacuum is hollow + hits a representational wall (R2: no element-level model) + a scale/benchmark wall (R3/R4); but (c) all of that tested GENERATE-PUSH. The USER's NEED-PULL loop routes around every one of those walls AND gives the 91%-inert data corpus a function. It is the strongest path identified for 5b-constructive.

## THE LOOP
1. FAILURE SOURCE: run the substrate on tasks it genuinely tries and underperforms (its real capability frontiers / weak held-out benchmarks). A failure is the signal. (NOT isolated ingested facts -- see "crux".)
2. GAP-SHAPE ABDUCTION: from a failure, compute the weakest SIGNATURE that would close it -- reverse-mathematics style (the gap fences the search; you are NOT searching all concept-space, only shapes that fit the hole). The substrate represents shapes natively (operator signatures: uses/computes/derived_from/type). CONSTRUCT-2 R1 already DEMONSTRATED autonomous gap-DETECTION over the graph (15 ops proposed, no LLM) -- the detect+propose half works.
3. FILLER-SEARCH over the corpus-as-VSA-reservoir (the generative core; USER's combine/interpolate):
   - single-match: a corpus atom whose signature fits the shape (OEIS-style retrieval; the substrate's M4d strength).
   - COMBINE: bind/bundle several corpus atoms (native VSA) when the filler is a composite of parts that pre-exist.
   - INTERPOLATE: move along the hypervector line among corpus atoms to assemble the matching point, then cleanup/crystallize into a candidate atom.
   The reservoir's effective coverage = its SPAN under combination+interpolation, not 19K discrete points. This is NATIVE machinery (FHRR bind = combine; hypervector space = interpolate; cleanup = crystallize). When no corpus assembly fits but the shape is constructible, construct to spec -- need-driven, so NOT hollow (this is where the CONSTRUCT generator finally belongs).
4. CERTIFY BY GAP-CLOSURE (the key move): wire the candidate in, re-run the failing task, MEASURE whether the gap closed. This is a UTILITY test, not a set-element-novelty test -> it needs NO element-level model -> it SIDESTEPS the R2 representational wall that HARD_FAILed CONSTRUCT-2. Soundness still gated by 4-gate + STRICT vet.
5. PROMOTE: a candidate that closes a real gap AND is sound graduates from inert-data -> load-bearing atom (signature + relations). The load-bearing core grows ON DEMAND, mined by need, not pre-built by hand or padded by ingest.

## WHERE GENUINE NOVELTY LIVES
A single corpus match = rediscovery (proven bounded). Blind generation = hollow (proven). But a COMBINATION or INTERPOLATION that closes a real gap AND is not near any existing atom is a GENUINELY NEW object -- assembled from inert corpus parts, certified because it filled a real need, represented natively as a new hypervector. This is the first route this session that reaches real novelty honestly: composed-to-fit, not generate-and-hope, not lookup.

## HOW IT ROUTES AROUND THE KNOWN WALLS
- grounding-bound / external-truth: N/A -- certification is internal (gap-closure), no oracle.
- R2 representational (no element model): SIDESTEPPED -- certify by gap-closure (utility), not carrier-extension; combinations are new hypervectors, natively representable.
- R3 junk-gaps (651 isolated facts): FIXED -- gaps come from real task FAILURES, meaningful by construction.
- R4 benchmark mismatch (NLP modules vs pure math): addressed by sourcing gaps from the tasks the substrate ACTUALLY runs (its own capabilities) so the utility metric is matched to the gap.
- data-vs-atoms inertness (91% dead corpus): RESOLVED -- the corpus becomes the reservoir; OEIS's natural function is shape-matching; combine/interpolate makes it a generative span.

## CORE RESEARCH KERNELS (what the full investigation must resolve)
K1. GAP SOURCE (the crux; binding constraint): which task-failures yield MEANINGFUL gap-shapes? Candidate: the substrate's weak capability frontiers / held-out benchmarks. Open steer (USER): point at existing weak benchmarks, or stand up a harder/cleaner math-task stream? The whole loop is only as good as its gaps.
K2. GAP-SHAPE ABDUCTION: turn a failure into the weakest closing signature (reverse-math). R1 shows graph-gap-DETECTION works; failure-driven shape abduction is the novel extension.
K3. CONSTRAINED COMPOSITIONAL FILLER-SEARCH: the gap-shape must hard-constrain which corpus combinations/interpolations to try (else combinatorial explosion over 19K atoms). Need-pull prunes the search; gap-closure prunes the results.
K4. GAP-CLOSURE CERTIFICATION + a MATH-appropriate utility metric (proof-depth-reduction / theorem-coverage / task-delta) -- the R3/R4 fix CONSTRUCT-2 flagged as needed.

## PROPOSED INVESTIGATION STRUCTURE (for Director to commission)
- Phase A -- GAP-SOURCE AUDIT (cheap, decisive first): inventory the substrate's genuine failure frontiers; classify which produce meaningful gap-shapes vs junk (the 651-isolated-facts trap). Decides K1 before any build. (Skunkworks can run this -- it is audit work.)
- Phase B -- ABDUCTION PROTOTYPE: from 1-2 real failures, hand-derive the gap-shape (reverse-math) to validate the kernel is well-posed before automating.
- Phase C -- VSA COMPOSITIONAL FILLER-SEARCH cell: gap-shape -> constrained combine/interpolate over the corpus -> candidate; certify by gap-closure (NOT carrier-measurement). This is the decisive 5b-constructive test, and unlike CONSTRUCT-2 it is NOT R2-blocked.
- Phase D -- promotion + Skunkworks STRICT vet + Testbed ratify for any candidate that closes a real gap soundly.

## HONEST CAVEATS (7th rule, both directions)
- This is "assemble/find the right piece for a need" -- a WEAKER claim than "invent unprecedented mathematics ex nihilo." But it is the RIGHT weaker claim: achievable, uses the substrate's actual strengths (retrieval + VSA composition), reaches genuine novelty via combination, and is how most real mathematical progress actually happens (recognizing the structure that fits the problem).
- A combination/interpolation is geometrically well-defined but NOT guaranteed meaningful; gap-closure is the only thing separating meaningful-new from noise -- so K1 (real gaps) + K4 (sound utility metric) are load-bearing, not optional.
- It is UNTESTED. R1 (autonomous gap-detection) is the only piece empirically demonstrated; failure-sourced abduction + corpus-compositional-matching + gap-closure-certification are the new bets.

## RECOMMENDATION
Commission the full investigation in the Phase A->D order above; gate everything on the Phase-A gap-source audit (K1) since meaningless gaps sink the whole loop. Do NOT run more construction-in-a-vacuum cells (CONSTRUCT line exhausted). This loop supersedes the scale-confounded CONSTRUCT-2 as the path for 5b-constructive, preserves substrate-on-its-own (no LLM), and finally gives the inert corpus a function. Skunkworks standing to run Phase A (gap-source audit) on your dispatch.

Tag: INVESTIGATION_BRIEF_USER_originated_GAP_DRIVEN_ABDUCTIVE_PROMOTION_LOOP_corpus_as_VSA_reservoir_combine_interpolate_need_pull_certify_by_gap_closure_sidesteps_R2_phases_A_to_D_gate_on_gap_source -- SKUNKWORKS (Auditor)
