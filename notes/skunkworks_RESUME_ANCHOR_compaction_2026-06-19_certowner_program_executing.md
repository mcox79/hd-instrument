# SKUNKWORKS (cert-owner) RESUME ANCHOR -- compaction 2026-06-19. Program EXECUTING full-auto; cert-architecture-layer role; reactive verdict-VETs + I-checks marker-gated. Current state + pending queue + cert-architecture state + standing disciplines below.

## SUBSTRATE STATE (verify on resume via invariant-check)
177221 atoms / CERT 587 / axiom 206 / cap_pres 6/6 / TRUE-HARD-PASS / 0 graph-hygiene flags / capint_integrated 433. Run `tools/skunkworks_substrate_invariant_check_v1.py` to confirm TRUE-HARD-PASS first thing.

## THE PROGRAM (USER-RATIFIED, executing full-auto)
4-phase + my C0-C6 cert-architecture layer (merged doc: research_skunkworks_to_USER_PHASE_PROGRAM_MERGED_v1_RATIFICATION). Phase 0 MAP operating-condition phase diagram (sub: 0a scope/0b mine[Drill#1]/0c gap-probe/0d synthesize) -> Phase 1 SHIP levers (CSP-first partial-parallel; rest gate on 0d) -> Phase 2 ONBOARD the 104-trove -> Phase 3 GLASS-BOX LLM. Embark-trigger: current cert-stream completes. USER quote: "let's go - full auto for the plan."

## PENDING REACTIVE QUEUE (resume here)
1. **q_b1 A/B verdict-VET** (GPU ~1.7h): 2-arm (control + candidate-2 cleanup-between-hops; candidate-C tropical DEFERRED -- composition-vs-recall mismatch). VET: verify cell_commit matches dispatched cell + iso-protocol + LOCKED bands (HARD_PASS=PASS d>=287 AND no-regression d100+d276; HARD_FAIL=no-extension OR worse-than-control OR regresses) + candidate-2 honest-scope + v1.2 I7/I8/I9 swap-gating. = the Barrier-1 cliff-extension cert-test + the resonator smoke->cert promote. pre-reg v4 FINAL.
2. **NER v3 verdict-VET** (GPU, MARKER-GATED): the local metrics.json is STALE v1 -- do NOT verdict-VET until the v3-marker present: detail.substrate_4type!=None AND bench_4type.llm[].variants AND metrics_source=="measured_gpu_substrate_vs_qwen_ladder_promptfair_4type_18type" AND n_seeds==5. VET: the PRECISE prompt-fairness (substrate must beat BEST-prompted-1.5B, not the crippled one) + 18-type + honest-scope-to-tested-ladder. pre-reg v3.
3. **3-small + NLP Track-A I-checks** (Research applies; math I-check PASSED, unblocked): 3-small = pp49_hrc depth-WINDOW cross-domain cluster (IF benchmark-shared, canonical=depth_8 PASS + 2 scale_points; else 3 singletons) + ALREADY_SEPARATES is_bound=False + singletons. NLP = ~19 singletons + optional ner_gazetteer pair. -> integration-check.
4. **Drill #1 coverage-matrix VET** (cert-VET the cell-classifications -- it's a cert artifact).
5. **Next value-coverage pull-ups** (the 104 queue; top-10: Pythia cognitive-core / phase4b_multistep / effective-rank-SVD / neurogenesis) -> SCHEMA-VET each w/ the DISCRIMINATING-REGIME requirement.
6. At-bandwidth: integration-check v1.3 (Phase-3 LM_PREDICTED tier + no-masquerade) when Phase-3 approaches; C1 dependent-set precision (eval-axis-dependency metadata) when Phase-1 ships approach.

## CERT-ARCHITECTURE LAYER (C0-C6; my standing discipline)
- C0 phase-diagram cert-grade-by-construction (Phase 0). C1 state-change cert-protocol (`tools/skunkworks_state_change_cert_protocol_v1.py`; Phase-1 ship gate; COMMITTED). C2 discriminating-regime requirement (every value-coverage pull-up). C3 integration-check v1.2 [I7/I8/I9 swap-gating LIVE] -> v1.3 [LM_PREDICTED + no-masquerade, pending]. C4 value-coverage check (`tools/skunkworks_value_coverage_check_v1.py`; 5th cert-lens; flags 104; run on cadence). C5 reconciliation protocol (git-window-archaeology + dependent-cert-atom reproduce-check + don't-dismiss-hygiene-flags; inst-243). C6 cert-tier discipline (KNOWN/COMPOSED/PREDICTED; Phase-3).

## STANDING DISCIPLINES INSTITUTIONALIZED THIS SESSION
- **Discriminating-regime requirement:** every value-coverage pull-up needs a regime where it CAN fail (caught the continual-writes degenerate-trap + the conformal tautology-trap). The TEMPLATE for all 104.
- **Version-marker verdict-VET discipline:** verify the metrics match the EXPECTED run (pre-reg version + cell_commit + expected-fields), NOT file-existence (the NER stale-v1 catch; composes parent-80).
- **Substrate-state-change cert-protocol (C1):** regression-check dependent cert atoms before any lever-ship (the PART_OF-revert lesson).
- **Metrics-pull cert-dependency:** GPU verdict-VETs need remote metrics synced (RESTORED via size-cap; keeps all metrics.json). push-before-merge hardening = tracked TODO.
- **Cert-curation:** don't over-atomize successes (inst-244 DECLINED -- the disciplines working isn't a new lesson).

## KEY OUTCOMES THIS SESSION
- Value-mining rectification COMPLETE: scour (447 MEDIUM+ wins) + inst-242/243 + value-coverage check LIVE (the durable never-overlook guarantee) + curated-not-exhaustive policy.
- Cert-integrity reconciliation CLOSED: PART_OF-revert (restore reverted +125 edges + a phantom) bounded to 2 + re-applied + verified; CERT 585; H4=0.
- 3-barrier architecture exchange: B1 resonator cleanup-between-hops (smoke 6x -> q_b1 candidate-2); B2 glass-box-LLM design + LLM-ingest foundation (cert-grade); B3 nonlinear/entmax readout (cert-grade). All converge on better-decoding.
- Value-coverage pull-ups: continual-writes HARD_PASS CERT 586 (bounded WIN, no-catastrophic-forgetting up to alpha=0.30) + conformal MIDDLE_BAND CERT 587 (honest BOUND); both discriminating-regime-validated.

## COORDINATION + MONITOR
- GPU runs autonomous (~1.7h); parallel lanes (Drill#1 + Track-A applies + next pull-ups) should advance independently (flagged to Research). Metrics-pull RESTORED.
- Monitor bo6ulm7dc (notes-for-skunkworks). VERIFY it's firing post-compaction; re-establish if not.

-- Skunkworks (cert-owner)
