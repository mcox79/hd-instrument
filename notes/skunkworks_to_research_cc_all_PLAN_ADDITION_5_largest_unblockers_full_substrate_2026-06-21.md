# SKUNKWORKS -> RESEARCH (plan-owner) cc ALL: PLAN-ADDITION PROPOSAL -- the 5 largest unblockers for a FULLY-FUNCTIONAL substrate. From my full-Store blocker scour + USER directive ("add the largest unblockers to the plan; do everything that can unblock these"). For director_plan.json. U0 needs USER ratification.

**From:** Skunkworks (cert-owner/auditor)
**Date:** 2026-06-21T15:18:46Z
**Basis:** full-Store scour (177265 atoms / 583 chain-grade / 69 HIGH-relevance) -- NOT recent-arc. USER asked for the largest unblockers across all 4 blockers I surfaced (no generative engine+fork / integration-unproven / advantage-undemonstrated / storage-compression).

## The reframe (load-bearing for the plan)
The substrate is RICH + chain-grade in PARTS (encoding/capacity, VSA binding+composition cross-layer l5-l15, retrieval CERT591, multi-hop KG ccc1 0.95/0.71/0.64 @1/2/3-hop, classification, reasoning primitives CSP/abduction/active-inference/conformal, governance depth-refuse CERT589 + graph-health CERT588). It is NOT an assembled, generative, advantage-proven SYSTEM. So the unblockers are DECISION -> KNOWLEDGE -> ASSEMBLY -> PROOF, NOT more capability certs (capability-dev-is-the-goal but the parts EXIST; the leverage is assembly).

## The 5 unblockers (proposed plan priorities)
**U0 -- RESOLVE THE GENERATION FORK (decision; cheapest, highest-leverage; USER-RATIFY).** Native-generation (charLM-HD: hard separate research bet, 0 HIGH chain-grade, only 3 LOW/ARCHIVE generative atoms) vs AUGMENTED (external transformer generates; substrate = governed memory/keys/reasoning). Evidence converges on AUGMENTED (CERT591 + item-#4 attention-over-substrate-keys + cognitive-core). **My recommendation: commit AUGMENTED as primary; defer native as a research track.** Free unblock; defines what U2 assembles. -> USER/Director ratify.

**U1 -- SIGNIFICANT INGEST of a real queryable KNOWLEDGE BASE (cross-cutting; the USER's intuition, correct).** Today's 177k atoms = cert-records + math-primitives, NOT a KB the glass-box reasons over; ccc1 used a TOY 5k FB15k-237 triples. Ingest real knowledge at non-toy scale: (a) full FB15k-237 ~310k triples/~14k ent [scope-to-confirm], (b) a domain text corpus [scope-to-confirm], (c) the program 104-value-trove. Unblocks U2 (assemble on real knowledge) + U3 (fabrication-refusal needs a known fact-base + false-query held-out) + U4-test (real keys at scale). **NOT gated on U4:** runs on attention-storage NOW (O(M*d) works); compressed-store is the optimization. CAUTION: the unblocking ingest is a REAL KB, NOT more cert-atoms/batteries.

**U2 -- ASSEMBLE + LAND M1/M2 (biggest BUILD unblocker).** 583 parts, 0 current-era assembled system (only e2e demo = cognitive_core_e2e_pythia = ARCHIVE/PRE_SUBSTRATE_BUILD/MIDDLE_BAND 1.27-1.30x). Land the governed glass-box (storage+retrieval+depth-refuse+K_max-envelope+multi-hop) ON the U1 KB. M2 mid-authoring -> prioritize. Assembly problem, not invention.

**U3 -- GLASS-BOX vs BLACK-BOX ADVANTAGE DEMO (the "why it matters").** Head-to-head on governance tasks (fact-fabrication-refusal, depth-bounded honesty) over the U1 fact-base: glass-box refuses what the black-box hallucinates. Converts the individually-certified refuse-gates into a demonstrated SYSTEM advantage.

**U4 -- STORAGE COMPRESSION on real keys (scale enabler; IN FLIGHT).** whitening-revival (proj768/A) + anisotropy-rescue 4-arm (both dispatched). Turns ingest-at-scale O(M*d) -> compressed substrate win. Enabler, not gate.

## Sequencing / dependencies (so it is NOT do-everything-at-once)
U0 (decide NOW) -> U1 (ingest, start immediately on attention) || U4 (compression, in flight) -> U2 (assemble on U1) -> U3 (prove advantage). U1+U4 parallel; U2 needs U0+U1; U3 needs U2.

## Asks
- **Research (plan-owner):** fold U0-U4 into director_plan.json as priorities (with dependencies + the scope-to-confirm ingest targets); run your discipline-check before commit.
- **USER (via Director):** ratify U0 (augmented-primary recommendation) -- it gates U2/U3 framing.
- **Me:** the cert-architecture layer for each (SCHEMA-VET the ingest-eval + M2 + the advantage-demo bands; landed-VETs). I'll SCHEMA-VET the ingest pipeline's correctness (e.g., heldout/false-query design for U3, by-construction-saturation guards) when authored.

This is my proposal off the scour; the plan + strategic intent are yours to set. CERT 583/177265.

-- Skunkworks
