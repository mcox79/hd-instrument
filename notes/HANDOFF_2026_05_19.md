# Handoff 2026-05-19 — next session start here

## Read first
1. This file
2. `notes/STATE_2026_05_19.md` (current state of program)
3. `data/session_events.jsonl` tail (recent events from dashboard helper)

Older notes (`master_plan_2026_05_18.md`, `overnight_log.md` early entries)
have stale-headers pointing here; don't trust their closures.

## Day's results — confirmed headlines

- **R10 K-sweep** (publication-grade): post-shift gap monotone in K, 5 seeds:
  K=16 +0.008 / K=32 +0.048 / K=64 +0.106 / K=128 +0.139 / K=256 +0.193.
  Lippl-Stachenfeld redundancy theorem provably FAILS at K>=8.
- **R10 N=8192**: gap shrinks ~12-50% per K (M1 mechanism confirmed).
- **R10 hyperparam sweep**: best config (nc=50, lam=0.3, beta=16) gives
  +0.318 at K=64 single-seed -- **3x default**. Needs multi-seed verify;
  `r10_best_config_multiseed` is in GPU queue, running.
- **Random replay** during Phase B: +0.66 at K=4, +0.73 at K=32. Robust 3-seed.
- **R3 (revised)**: +0.032 R3-Laplace-alone at K=4 (~80% of original +0.154
  was broken-normalizer artifact). Does NOT compound with replay+R10.
- **ACF (asymmetric)**: K=2048 hurts regardless of r, K=3072 gives 85% with r>=0.01.
  Cliff rescues confirmed; need K-dependent r.
- **K-cliff cross-validated**: 2 independent 30-trial sweeps identical.
  Cliff at K/N≈0.56, sharp 10%→0% in one step.

## Day's results — closed negatives with mechanism

- **Triple compound FALSIFIED**: random replay × R10 × R3 substitute, not
  orthogonal. All three correct same W-drift problem.
- **MIR-style priority replay**: closed by rank-equivalence math
  (canonical Aljundi 2019 + 3 rescues all lose to random).
- **Iterative Hopfield as label readout**: protocol mismatch
  (Ramsauer iterates to nearest pattern, not label).
- **R3+R10 compound**: over-determined by 4 arguments (shared evidence
  base, two-stage variance ceiling, mode-connectivity, empirical null).

## Reopened by research (don't trust earlier "closed" framings)

- Wave 9 MPS — never tested properly; u-MPS+DMRG would be the test
- Wave 8 Clifford — R3 grade-aware readout untested
- Wave 10A RG-flow — Krotov-WTA + linear Layer 1 is the literature-rec fix
- Wave 4.5 gradient W — preconditioned delta rule is the lit-rec move

## Currently running (started today)

- **GPU**: `replay_preshift_K4` (Stein prediction #1 — does replay during
  Phase A hurt pre-shift bpc at K=4?). REPLAY_FRACTION ∈ {0, 0.25, 0.5, 0.9},
  3 seeds. **Pull result first** when you start. Predict: pre-shift bpc
  grows with replay (regularizer = pure bias in low-noise regime).
- **CPU**: `acf_K_dependent_retry` (paper's per-K r schedule). Predict:
  matches baseline at K=2048, rescues K>=2560. ~1-2h.

## Queue state at handoff

- **GPU pending**: `r10_best_config_multiseed` (probably already running
  or done). Check queue.log first.
- **CPU pending**: nothing after ACF K-dep finishes.

## Pending unbuilt experiments (in priority order)

1. **r10_best_config follow-up at K=64 single-seed verify** (the +0.318 sleeper).
   Multi-seed at K=64 only (K=128/256 already in r10_best_config_multiseed).
   If verified, headline R10 K=256 could push toward +0.5 bpc.
2. **R3 with corpus-B-disjoint concepts** (R3-Laplace research suggestion #2).
   Tests "shared evidence base" hypothesis directly.
3. **Stein prediction #3**: adaptive β(P) tuned by online entropy of retrieval
   scores. Single regime-tracking knob could subsume ACF r-table + R10 α + β(P).
4. **Schlag-Irie slow projection** (~150 LOC). HIGHEST EV pre-shift mover per
   pre-shift research. Respects "no backprop on W" constraint. Predicted
   +0.10-0.25 bpc. Big build.
5. **Multimodal demo** (~3 days minimum): CLIP+random-replay BWT.
   Smallest credible "substrate transfers" demo per multimodal research.

## Pending unbuilt analyses

- R10 hyperparam sweep verification at multiple K (only K=64 single-seed currently)
- ACF K-dependent verification with more K levels
- Pre-shift bpc Bayes-floor empirical estimate (corpus entropy + Good-Turing)

## Open methodological question

R3 +0.154 was retracted to +0.032. This shrinks the post-shift R3 win
to ~1/5 the original. **R3 is still a real but small effect; not in the
publication-grade tier with R10/replay.** STATE_2026_05_19.md has the
revised number.

## Honest framing

**The substrate's three publication-grade stories are:**
1. Decompose/edit/recompose/interpret (substrate uniqueness)
2. Random replay during Phase B: +0.66-0.73 BWT recovery, robust
3. R10 high-K concept-fusion: +0.193 at K=256 monotone in K (theorem-falsifying)

**The methodological contribution worth writing up:**
> "Compound interventions do not stack when they consume the same evidence
> base, regardless of normalization." (4 independent arguments converging.)

**Pre-shift bpc is the WRONG goal** per pre-shift research. Substrate is
near Bayes floor at K=4; unique-enabling stories live in regimes
transformers can't occupy. Don't try to move pre-shift bpc.

## Infrastructure notes

- Dashboard at http://127.0.0.1:8765 (read-only, 4 panes + events strip).
  Survives session restart via `tools/dashboard/start_dashboard.ps1`.
- `hdlab.session_log` writes events to `data/session_events.jsonl` -- use
  for cross-session context recovery. Already integrated into dashboard.
- Multi-runner heartbeat race: there are zombie python.exe procs from past
  ssh -f launches. They write "idle" to heartbeat which confuses display.
  Trust `queue.json` status + experiment log freshness, not heartbeat alone.
- Queue rename `overnight_queue → gpu_queue` proposed but not done (would
  touch runner + dashboard + ~40 seed files). Cosmetic; postpone.

## Research syntheses produced today (notes/wave14b_*.md)

15+ deep-research syntheses. Most important:
- `wave14b_r3_laplace_synthesis_research.md` — substitution-not-orthogonality theorem
- `wave14b_preshift_bpc_research.md` — pre-shift is wrong goal; Schlag-Irie is highest EV
- `wave14b_stein_shrinkage_research.md` — bias-variance with regime-dependent optimum
- `wave14b_multimodal_research.md` — 3-day smallest credible demo
- `wave14b_compound_falsification_research.md` — triple compound falsified
- `wave14b_mir_failure_diagnosis.md` — MIR closed by rank-equivalence math
- `wave14b_r10_deep_dive.md` — R10 single-seed wasn't underpowered (now over-determined)

## What to do first next session

1. Pull `replay_preshift_K4` result (Stein pred #1 verdict)
2. Pull `r10_best_config_multiseed` result (potential headline 3x amplifier)
3. Pull `acf_K_dependent_retry` result on CPU
4. Update STATE with all 3
5. Then decide: build #1 (R10 best-config at K=64) or move to Schlag-Irie
   pre-shift experiment

Have fun.
