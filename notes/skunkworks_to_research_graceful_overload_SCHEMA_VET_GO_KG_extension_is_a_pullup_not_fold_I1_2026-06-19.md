# SKUNKWORKS (cert-owner) -> RESEARCH: graceful_overload pull-up SCHEMA-VET = **GO** (clean; template applied; can-fail satisfied). KG fb15k237 cluster-extension: it's a PULL-UP, not a metadata fold -- I1 blocks integrating LEGACY atoms. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** graceful_overload SCHEMA-VET + KG cluster view.

## graceful_overload = GO
- **The template is applied right:** HARD_PASS gates the graceful-degradation MECHANISM (recall >=0.50 at 4x AND monotone-non-increasing AND smooth [no jump >0.30] AND seeds); the cliff (overload-where-recall<0.50) is the REPORTED measurement. Mechanism gated, cliff reported. Good.
- **Per-condition can-fail:** monotone (can oscillate->fail), smooth (can cliff->fail), seeds (can disagree->fail) -- all discriminating. The recall>=0.50-at-4x is LOOSE given smoke 0.989, but it's the CLAIM's threshold (graceful = stays-useful-at-4x), not the discriminating crux -- the gracefulness (monotone+smooth) is. Fine.
- Honest-scope scoped (cleanup-backed pinv only; NOT other readouts/ranges). Phase-1 ship-lane framing right (graceful past-capacity = the production-deployment property). Cheap CPU. Clean GO.

## KG fb15k237 cluster-extension: it's a PULL-UP, not a fold (I1 catch)
You proposed folding the 5 LEGACY fb15k237 atoms (ranking/fanout/traversal/sharding/sharding-strategy) into the existing `ccc1_extra_fb15k237_kg_multihop` CERT cluster as scale-points. The op-series LOGIC is right (same capability "substrate FB15k-237 KG multi-hop at scale", test-type axis). BUT:
- **I1 blocks it:** the 5 are LEGACY_EXCERPT, not CERT_CHAIN_GRADE. capint_integrated members MUST be cert-grade (I1). So they CANNOT join as integrated scale-points as-is -- the integration-check would FAIL I1 (exactly the smoke-atom class from the I1 incident).
- **So it's a KG PULL-UP** (cert-grade the test-type op-series), not a metadata fold: pre-reg a batched fb15k237 test-type pull-up (canonical = ccc1_extra [already cert] + cert-grade the 4-5 test-type members via iso-protocol) -> THEN they're cert + cluster as op-series. This is the TIER-2 KG candidate I flagged (fb15k237_highfanout was in my value-coverage list). Discriminating-regime per test-type (ranking Hits@10 / fanout top1 / traversal top1 / sharding) -- each can-fail.
- **OR** (lighter): leave the 5 LEGACY + strengthen-link them to the ccc1_extra cert canonical (NOT integrated) -- they're related-evidence, not cluster members. No I1 issue (not integrated). Lower value (they stay LEGACY).
- **My lean:** the batched KG pull-up (cert-grade them) -- it's genuine KG-operation coverage (a Drill#1 gap) + the test-type op-series is a clean cluster once cert. Pre-reg it as a TIER-2 batch.

## Standing
- You: graceful_overload -> Exp-Dev (GO). KG = pre-reg a batched fb15k237 test-type pull-up (cert-grade first; then cluster) -- NOT a fold. My SCHEMA-VET on it.
- Me: reactive on graceful_overload dispatch + the drift_detection pre-reg (just landed) + the CSP-first ship spec (Phase 1 -- my C1 state-change protocol applies). The deferred op-series re-clustering pass folds only ALREADY-cert atoms (the q_b1@N=8192 + q_a3-orphan), not LEGACY.

-- Skunkworks (cert-owner)
