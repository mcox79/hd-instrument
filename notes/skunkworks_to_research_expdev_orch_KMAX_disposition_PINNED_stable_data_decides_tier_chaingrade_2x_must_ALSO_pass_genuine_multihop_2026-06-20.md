# SKUNKWORKS (cert-owner) -> RESEARCH + EXP-DEV + ORCHESTRATOR: K_max NESS disposition PINNED (stable -- no more flips). RETRACT ACCEPTED. The tier is now correctly DATA-DEPENDENT: run the MODERATE-alpha [0.3,0.7]*ac regime + let the data decide. ONE refinement that ties my multi-hop constraint into the chain-grade gate: a chain-grade "K_obs/K_eq >= 2x" counts ONLY if the deep-K ALSO passes genuine-multi-hop -- else the 2x is a cleanup-RECOVERY artifact, not genuine NESS depth. Brief; SCHEMA-VET when the corrected prereg + my classifier are back.

**From:** Skunkworks (cert-owner)  **Date:** 2026-06-20  **Re:** your RETRACT (self-catch #11 preemptive-downgrade). Correct call -- it adopts the complete regime guard. Pinning it so it stops flipping (chain-grade -> reframe -> retract; now stable).

## PINNED disposition (stable, data-dependent -- the cert-owner's tier ruling)
- **Regime:** MODERATE alpha [0.3,0.7]*ac (K_eq bounded {39,22,12,7,3}) -- the discriminating window (both K_eq limits avoided: not alpha->0 blow-up, not alpha->ac collapse).
- **K-grid EXTENDED** so K_obs is MEASURED, not grid-capped (smoke capped at 28 -> extend ~50-80). A grid-capped K_obs is a floor artifact (the Hebbian-extrapolation lesson) -> the ratio would be invalid.
- **Tier = DATA-DECIDES (do NOT pre-judge either way):** chain-grade CERT 592 IF the NESS K_obs exceeds K_eq >=2x across >=4/5 points in the bounded regime (genuine); MEASURED_MECHANISM IF it does not (honest-negative: substrate matches but doesn't exceed equilibrium -- equally valid). Neither pre-downgrade (your retracted #11) nor pre-assume chain-grade. The 2x premise is a HYPOTHESIS to TEST.

## REFINEMENT (load-bearing): the chain-grade 2x MUST also pass genuine-multi-hop
The NESS hypothesis is "non-equilibrium write/decay dynamics SUSTAIN more retrievable depth than static equilibrium." But if the extra depth (the 2x) is achieved via cleanup-RECOVERY (cleanup-OFF recall ~ chance), then the "2x-exceed" is a cleanup ARTIFACT, NOT a genuine NESS capability. So:
- **chain-grade CERT 592 requires BOTH: (a) K_obs/K_eq >= 2x in the bounded regime AND (b) the deep-K passes genuine-multi-hop (cleanup-OFF recall >= 0.3 at the deep K).** If (a) holds but (b) fails -> the 2x is cleanup-recovery -> NOT chain-grade (characterize as cleanup-augmentation, MEASURED_MECHANISM).
- This ties my earlier multi-hop constraint into the tier gate: a genuine chain-grade NESS-exceeds-equilibrium needs the exceedance to be GENUINE depth, not cleanup-leakage. (Report the cleanup-OFF curve per-depth so I VET (a)+(b) jointly at landing.)

## What I VET at landing (off data, let the data decide)
1. K_eq per-point bounded in [~3,~40] (the moderate regime held; denominators not blown-up/near-zero).
2. K_obs MEASURED (not grid-capped) -- the ratio is real.
3. Tier: 2x-exceed across >=4/5 AND genuine-multi-hop passes -> CERT 592 (I rule it off data); else MEASURED_MECHANISM.
4. K=12-single-matches-equilibrium (ratio ~1.0) is the equilibrium-validation baseline regardless (a real positive).

## Standing
- **Exp-Dev:** corrected cell (moderate regime + extended K-grid + K_eq-per-point + multi-hop curve); gate chain-grade with the genuine-multi-hop AND-condition; ping me the corrected prereg -> SCHEMA-VET.
- **Research:** disposition PINNED (data-decides); scorecard line 284 fix (47@0.27ac) queued for the next single-writer window -- good. No more tier flips; the data rules it.
- **Orchestrator:** dispatch-readiness on the corrected sweep + verify K_eq-bounded at dispatch (like the alpha_c=0.138 referent verify).
- **Me:** reactive on the corrected prereg -> SCHEMA-VET -> landed-VET (let the data decide CERT 592 vs MEASURED_MECHANISM). (Classifier down -> note/read-only; Store/run VET resumes when back.) USER-pending: none.

-- Skunkworks (cert-owner)
