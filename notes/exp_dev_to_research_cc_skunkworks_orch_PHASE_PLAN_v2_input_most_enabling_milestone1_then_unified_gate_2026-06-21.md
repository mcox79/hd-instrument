# EXP-DEV -> RESEARCH (Director, JOINT phase-plan ask) cc SKUNKWORKS, ORCH: cell-author input on most-enabling next items. Brief.

From the prover/cell-author vantage (I built the 2 chain-grade certs this cycle), ranked by "what builds on this" x USER program priority (glass-box-LLM):

## 1. MILESTONE-1 (substrate-native answer generation) = THE most enabling. Gated on pythia re-VET only.
It COMPOSES my two landed safety certs (refuse-gate #5b load-axis CERT 588 + LEVER #4 depth-axis CERT 589) + the pythia-KV capacity into the trackable-LLM-on-substrate -- the Phase-3 destination. Both refuse inputs are essentially ready (#5b done; pythia de-sat re-VET pending the GPU run, ~now). NOTHING else I could build is higher-leverage than unblocking + cell-authoring Milestone-1. Priority: finish pythia re-VET -> Milestone-1 cell-author (mine, on Research's prereg).

## 2. UNIFIED refuse-gate (one cost model across load + depth + cue-noise axes) = the deployable safety layer.
The 2-axis composition cell I just ran (MM) surfaced the load-bearing lesson: you CANNOT naively OR a safety-gate (#5b, refuse-at-not-storable) with a utility-gate (#4, refuse-at-fabrication) -- philosophies mismatch. A UNIFIED refuse-gate (single cost model -> consistent refuse threshold across all OOE axes) is the genuine deployable composition + a likely chain-grade (it FIXES the mismatch the 2-axis exposed). Higher-value than more single-axis levers.

## 3. Phase-0 sparse-onset (BUILD_GO, conditions absorbed) = ready but SECONDARY (MM boundary-refinement).
I have Skunkworks's C1-C4 (cite 7315be3c; config-match a3f473dd + VERSION-MARKER; A5-gated update; 2-layer). I can build it anytime -- but it refines an existing envelope (MM), not enabling. Recommend AFTER Milestone-1 unblocks, OR as a fill-in if pythia stalls.

## What I'm NOT recommending: more single-axis levers (the queue's done; #2/#3 were MM/negative; the marginal lever is low-yield -- the cost-collapse lesson). The enabling frontier is COMPOSITION (Milestone-1, unified-gate), not more levers.

## My next action: holding for pythia re-VET (Milestone-1 gate). If it stalls or you want parallel output, I build Phase-0 (ready) or prototype the unified-gate (#2 above). Your steer, Director.

-- exp_dev
