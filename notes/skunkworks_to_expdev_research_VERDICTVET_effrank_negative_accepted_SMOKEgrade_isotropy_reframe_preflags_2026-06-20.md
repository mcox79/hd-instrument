# SKUNKWORKS (cert-owner) -> EXP-DEV + RESEARCH: VERDICT-VET of effrank-SVD HONEST-NEGATIVE. **Negative ACCEPTED, filed at SMOKE-grade (n=3) -> does NOT increment CERT.** De-risk methodology = real deliverable, recorded. Isotropy REFRAME (#6) GO-in-principle + 2 binding pre-flags for the authoring. (Filename has to_expdev_research.)

**From:** Skunkworks (cert-owner)  **To:** Exp-Dev + Research  **Date:** 2026-06-20  **Re:** effrank verdict-VET.

This is a model honest-negative -- the "don't force a PASS" instinct + the 3-flaw de-risk are exactly right. My cert dispositions:

## 1. d_eff hypothesis: REFUTED, accepted as negative knowledge -- but SMOKE-grade, not cert-grade
- **The negative is REAL** (capacity ANTI-correlates with SVD-d_eff: pythia d_eff=351/cap=2.6 vs MiniLM d_eff=238/cap=170). File it: "substrate associative capacity is NOT predicted by SVD effective-rank/participation-ratio for real encoders." Keep as negative knowledge per USER-LOCKED "keep REFUTED as negative knowledge."
- **BUT it is SMOKE-grade, not a cert-grade refutation.** Evidence = 3 cached encoders, one of them (MiniLM) seed-unstable at CV=0.44. n=3 with an unstable point is strong DIRECTIONAL evidence, not a cert-grade negative. So provenance = **RESEARCH_FINDING / accepted-negative (TIER-3)**, NOT CERT_CHAIN_GRADE. **It does NOT increment the CERT count** (a proven NON-relationship at smoke-grade is negative knowledge, not a certified capability). Record the anti-correlation table as the supporting evidence at its true grade.
- This is the correct outcome: we neither force a PASS nor over-credit the negative.

## 2. The de-risk methodology IS a cert-worthy deliverable (record separately)
The 3 fixed flaws are textbook by-construction-saturation traps from the discipline catalog, caught correctly:
- whitening erased the d_eff bottleneck (1/sqrt(w) inflates small-variance dirs) -> measurement-artifact;
- NN-lookup has no capacity bottleneck (trivially separable) -> by-construction-trivial;
- recall@fixed-M saturated -> by-construction-ceiling -> swept-continuous fix.
Record the **Hebbian-auto-associative capacity measure** (whitening-OFF + superposition-crosstalk + threshold-crossing + deduped diverse corpus) as a METHODOLOGY atom = "the correct way to measure substrate associative capacity." That's the durable, reusable deliverable here, independent of which hypothesis wins.

## 3. Isotropy REFRAME (TIER-2 #6): GO in principle, with 2 BINDING pre-flags for the authoring
The REFRAME is well-motivated (encoder-selection is enabling for Phase 3). But the outlined pre-reg has 2 flaws I need fixed BEFORE I'll SCHEMA-VET-GO the authored version -- pre-flagging now so they're baked in:

**PRE-FLAG A -- the n=5 cross-encoder correlation is underpowered AND confounded (the load-bearing gate must move).** Pearson r>0.80 across 5 encoders is not cert-grade: at n=5, r=0.80 is not significant (p~0.10) and a single encoder swings it. Worse, it is CONFOUNDED -- encoders differ in training objective, dimension, and corpus, not just isotropy. **Fix:** make the load-bearing HARD_PASS gate a WITHIN-encoder CONTROLLED-ISOTROPY INTERVENTION -- take ONE encoder, vary its isotropy directly (anisotropy injection/removal, or whitening-strength sweep), and show MEASURED capacity tracks isotropy MONOTONICALLY with the encoder held fixed. That isolates isotropy as causal. The 5-point cross-encoder correlation then becomes corroborating REPORTED evidence (cliff-style), not the gate. (Same correlation-vs-controlled-mechanism discipline as the earlier effective-rank catch.)

**PRE-FLAG B -- analytic-vs-empirical (tautology / verify-the-referent guard).** Hebbian crosstalk IS literally the sum of pairwise dot products. Mean-pairwise-cosine isotropy is therefore ANALYTICALLY linked to capacity -- "isotropy predicts capacity" is partly true BY THE CROSSTALK FORMULA, not by empirical discovery. If the gate's isotropy metric reduces to the same pairwise-cosine that defines crosstalk, ">0.80 correlation" is a near-tautology (always-true direction -> fails can-fail). **Fix:** the cert claim must isolate the EMPIRICAL, non-analytic part -- does an INDEPENDENT, standard isotropy metric (IsoScore) predict the MEASURED capacity on HELD-OUT encoders BETTER than d_eff does? I.e., is it a PREDICTIVE discovery (generalizes to unseen encoders, supports encoder-selection) rather than a restatement of crosstalk = Sigma pairwise-dots? State the held-out protocol; that's what makes it a real cert vs a formula-restatement. Research's existing >0.99 up-guard partly senses this -- make it explicit as the analytic-overlap guard.

With A + B baked in, the isotropy claim becomes genuinely cert-able and enabling (it tells Phase 3 which encoders to pair with substrate KV). Without them it's an underpowered confounded correlation that's partly analytic.

## 4. Backlog cert-rule from the systemic-OOM note (Orchestrator, 74 logs): OOM/no-log = INCOMPLETE, NOT a NEGATIVE
Critical for the "certify the backlog" sweep + the USER negatives-catalog: an OOM'd / no-log GPU run produced NO science result. It must be classified **INCOMPLETE (re-dispatch with the chunked/serialized pattern)**, NEVER filed as a capability-failure or a science-negative. This is the verify-the-referent guard applied to negatives: a missing result is not a refutation. (Distinct from the effrank case, which IS a real science negative.) I'll carry this into the negatives-catalog cert-tiering. Large-N enabling certs (composition N>2048, capacity>N2048, sparse-large-N, KG-sharded) need chunk-rebuild before re-dispatch, per Orchestrator -- budget cert-effort as "chunk + dispatch," and do NOT let their prior OOMs count as evidence either way.

## Standing
- **Exp-Dev:** d_eff negative filed SMOKE-grade (no CERT increment); record the Hebbian capacity measure as a methodology atom. Isotropy-vs-capacity cell: build to the A+B-corrected design (within-encoder controlled-isotropy intervention as the gate; independent-IsoScore-on-held-out-encoders as the predictive claim) -- I'll SCHEMA-VET the authored #6 pre-reg against A+B.
- **Research:** #6 authoring must bake in pre-flags A+B before SCHEMA-VET. The OOM=INCOMPLETE-not-NEGATIVE rule feeds your negatives-2x catalog (just saw your plan note -- cert-tiering input coming separately).
- **Me:** next = negatives-catalog cert-tiering input + continual+drift #4 SCHEMA-VET; reactive on CSP-first ship LANDED-VET (Phase-1 milestone gate).

-- Skunkworks (cert-owner)
