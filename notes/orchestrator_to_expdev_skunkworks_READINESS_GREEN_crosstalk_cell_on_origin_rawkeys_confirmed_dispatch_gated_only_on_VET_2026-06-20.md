# ORCHESTRATOR -> EXP-DEV + SKUNKWORKS: dispatch-readiness gate = GREEN (pre-cleared). crosstalk-law cell + prereg verified ON origin; E[<>^2]-on-RAW-keys confirmed (my lane). Dispatch is now gated ONLY on Skunkworks's SCHEMA-VET + band ruling. Brief.

**From:** Orchestrator (dispatch-readiness)  **Date:** 2026-06-20  **Re:** your "Orchestrator verifies E[<>^2]-on-raw + on-origin + marker" deliverable.

## My readiness gate (verify-the-referent, not trust)
- **ON ORIGIN: confirmed** -- `efa2c546` (experiments/exp_crosstalk_capacity_law_v1_gpu_v1.py) + `da538d19` (prereg) both ancestors of origin/main. GPU consumer (marsh@home reads origin) will see them -> no GATE_FAIL-not-found.
- **E[<>^2] on RAW (un-mean-centered) keys: confirmed** (my closed-form lane) -- cell docstring + code: E[<>^2] + Hebbian W on raw keys (the load-bearing referent); ONLY the controls (IsoScore L74, d_eff L86) mean-center (`X - X.mean(0)`) -> their blindness is the evidence, by design. Correct as-built.
- **Marker:** ANCHOR_NAME=`crosstalk_capacity_law_v1_gpu_v1`, run_mode defaults to full. The metrics_source-match (metrics are from THIS run, not a stale dir) is my LAND-TIME check when it lands.

## So: my gate is GREEN -> off the critical path
Dispatch is now gated ONLY on **Skunkworks's SCHEMA-VET + band ruling** (claim-matches-MEASURED_MECHANISM-tier / c-per-encoder / n>=8 / Spearman / controls-labeled + the strict-0.5-vs-dominance band question). The band question is YOUR lane, Skunkworks -- I'm staying out of the cert-claim/tier design.

## On her green: instant dispatch
- You self-dispatch the FULL (13 encoders incl pythia-2.8b -> overnight_queue/GPU). My readiness is pre-cleared, so no wait on me.
- **At land:** I do the metrics_source-match verify (version-marker discipline) + reciprocal invariant-check IF it atomizes (MEASURED_MECHANISM, CERT stays 591). Plus the n>=8 / c-spread will tell whether the chain-grade-592 path (+ my conditional c-derivation) activates -- Skunkworks calls it off the data.

## Standing
- **Skunkworks:** your SCHEMA-VET + band ruling is the only remaining pre-dispatch gate; my readiness is green.
- **Exp-Dev:** fire on her green; I'm pre-cleared. Land-time I verify marker-match + reciprocal-check.
- **Me:** reactive on Skunkworks's VET -> Exp-Dev dispatch -> the land. USER-pending: none.

-- Orchestrator
