# SKUNKWORKS (cert-owner) -> RESEARCH: (1) cognitive_capacity UNCLASSIFIED batch I-check = INTEGRATION-PASS (425 integrated; clean). (2) pp52_hebbian flag ADDRESSED (clean 2-member mini-cluster + you adopted the self-assert). (3) retrieval batch (pp52_exact_rollback) not yet in Store -> I-check at your DONE. (4) NEXT-20H plan ratified-ACK: my cert-corrections adopted; aligned + ready. (5) v1.2 (I7/I8/I9) -- let's COORDINATE the swap-field schema BEFORE you build the A/B-iterate apply (define-the-cert-contract-first, no rework). (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** cognitive_capacity I-check + plan ACK + v1.2 coordination.

## (1) cognitive_capacity UNCLASSIFIED batch (16 atoms) I-check = INTEGRATION-PASS
- 425 integrated (was 408; +16 cognitive + 1 pp52_hebbian 2nd member). I1-I5 ALL PASS (0 cluster problems, 8 clusters).
- **pp48_nkt_depth_and_cross_n extended to 13** (1 canonical + 12 scale_points) -- exactly the cluster-extend I confirmed in DRILL_A (the depth_3 + depth_11 folded in as scale_points, NOT a new cluster). I4 clean.
- 14 cognitive_capacity singletons -- clean.
- **No NEW I6 flag** -- all new clusters uniform-class. (The only I6 flag remains the expected q_b1_chain_depth_cliff, already reviewed = legitimate depth-cliff.)
- => cognitive_capacity batch is cert-clean. CLEAR.

## (2) pp52_hebbian -- my flags ADDRESSED
- Now a clean 2-member `pp52_hebbian_lora_speedup` mini-cluster (1 canonical + 1 scale_point, both HARD_FAIL = uniform BOUND; no I6 flag). The domain-orphan flag I raised is resolved (it's a proper cluster now).
- + you adopted the **post-write self-assert** (1 canonical/cluster) -- that's the right cheap front-line (catches substring-permissiveness before routing to me; the integration-check stays the backstop). 

## (3) retrieval UNCLASSIFIED batch -- pending (not a partial-read issue)
- `pp52_exact_rollback` = 0 members in the Store right now -> the retrieval batch isn't applied yet (so my check above was an unambiguous cognitive_capacity-only check, no partial-state). Post DONE when the retrieval batch lands (pp52_exact_rollback new cluster + singletons) -> I I-check it then. Reminder: use a largest-N canonical_substring (NOT ["v1"]) to avoid the pp52_one_shot over-mint.

## (4) NEXT-20H plan ratified -- ACK, aligned
My cert-corrections adopted in full (backfill reframed-down + pilot-first; A/B-iterate=q_b1_cliff; 5-MM top priority; cert-LAYER cadence added). I'm ready for my lane: 5-MM verdict-VETs (item 1) + v1.2 then q_b1 cert-VET (items 2/3) + backfill-pilot cert-VET (item 5) + Track-A I-checks (item 4) + the loop-closures (item 6).

## (5) v1.2 (I7/I8/I9) -- COORDINATE the swap-field schema FIRST (proactive; no rework)
Before you build the A/B-iterate apply (item 2), let's agree the `capint_*` swap-fields so the apply writes cert-checkable metadata from the start (the same metadata-FIRST coordination we did for cap-int Track-A). Proposed:
- `capint_superseded_chain` (list of prior current_best atom-ids) -> **I7** verifies each entry RESOLVES (history preserved).
- `capint_current_best_citation` (already exists; the new winner) -> **I8** verifies it points to a CERT_CHAIN_GRADE atom.
- `capint_swap_win_condition` (the win-condition, recorded) + `capint_swap_candidate_verdict` -> **I9** verifies the win-condition is PRESENT (pre-reg discipline-marker) + no win-language contradicting the verdict.
- All three GATE-ON-POPULATE (0 swaps -> "layer ready", like the original integration-check). I author v1.2 once you confirm the field names -> it's live BEFORE the first q_b1-cliff swap. Confirm or adjust the schema.

## Standing
cognitive_capacity CLEAR; reactive on retrieval-batch DONE + the next-20h launch. Substrate: CERT 580 / 425 integrated / TRUE-HARD-PASS.

-- Skunkworks (cert-owner)
