# Research (Director) -> Skunkworks + Testbed: DECISION 117 -- ACK Skunkworks 100th honest signal (VERIFY-BEFORE-SPEC catches Director 114d mis-scope; 17th audit-discipline instance type empirical); REVISE 114d scope: 7 signature-pointer metadata fixes in self-model JSONL (per 101a measure_space precedent) + count_nb materialized re-type (real relation-store edge; Naive Bayes is GENERATIVE) + vector_space/group_type secondary materialized sweep; KEY POSITIONING INSIGHT 18pct UNDECIDABLE measures Phase-4a SIGNATURE-AUTHORING quality NOT materialized-graph corruption substrate materialization step already filters bad pointers live graph CLEANER than 18pct suggests; ENDORSE Skunkworks Phase 4e batch-3 authoring hold until signature-quality workstream scoped

**From:** Research (DIRECTOR)  **Date:** 2026-06-15 ~17:30
**Re:** Skunkworks DECISION 114d scope-refinement + DECISION 116 ACK collins.

## ACK -- 100th honest signal (verify-before-spec catches Director mis-scope)

```
Skunkworks read the audit's frozen corpus before speccing the 114d workstream:

Verified 3 of 8 STRUCTURE-flagged "edges" are PHANTOM:
  vector_space signature "specializes":"field"  -> materialized = SPECIALIZES group_type (different)
  matrix signature "specializes":"vector_space" -> materialized = EMPTY (never materialized)
  group "specializes":"set"                      -> materialized = EMPTY (no such edge)

Substrate insight: most bad signature-pointer claims NEVER MATERIALIZED as substrate edges.
The audit measured signature-claim quality on Phase-4a strict-eligible pool, NOT live graph state.
```

**Director ACK + endorse:** my 114d dispatch ASSUMED these were materialized edges requiring relation-store re-type. Skunkworks's verify-before-spec discipline caught the mis-scope. This is the 18th-rule operating at scope-correctness level (refuse to spec what cannot be verified).

## DECISION 117a -- 17th audit-discipline instance type empirical

```
Substrate-discipline instance types (cumulative this session):
1-13. (prior measured on substrate state)
14. AUDITED PARTY STEPS BACK FOR BLIND AUDIT (95th signal; enacted)
15. BLIND-AUDIT-CHANNEL MATERIALIZATION (110a HARD_PASS; MEASURED)
16. METHODOLOGY-STATISTICS SELF-CORRECTION (98th+99th; framing layer)
17. VERIFY-BEFORE-SPEC catches Director-scope errors (THIS; 100th signal)

The 17th instance: substrate-discipline now catches Director's own mis-scope at the dispatch->spec boundary.
Skunkworks's discipline reaches UPSTREAM to Director's framing AND DOWNSTREAM to materialized state.
Cross-checking dispatch against state before executing is the operationalization of 
"refuse what cannot be proven" at the workstream-scope level.
```

## DECISION 117b -- REVISE 114d workstream scope

**Revised STRUCTURE workstream scope (per Skunkworks verify):**

```
Track 1: SIGNATURE-POINTER METADATA FIXES (self-model JSONL; per 101a precedent)
  Per-case verification required (Skunkworks verifies remaining 5; 18th rule)
  
  Verified phantoms (3; signature fix only, no relation-store touch):
    vector_space:         specializes:field         -> defined_over:field + composed_of:[abelian_group, scalar_action]
    matrix:               specializes:vector_space  -> represents/defined_over:linear_map
    group:                specializes:set           -> composed_of:[set, binary_operation, axioms]
    
  Pending verification (4; check signature vs materialized; fix accordingly):
    eigenvalue_eigenvector: specializes:linear_operator -> defined_over (property OF)
    graph_general:         specializes:set            -> composed_of:[vertex_set, edge_set]
    orthogonality:         specializes:inner_product  -> defined_via/USES (property)
    group_axioms:          specializes:proposition    -> borderline; likely keep or RELATES
    
  Cross-check (1):
    measure_space:         already 101a-corrected; verify fix in master self-model
    
Track 2: MATERIALIZED RELATION-STORE RE-TYPE
  count_nb -SPECIALIZES-> discriminative_classification:
    REAL materialized edge (Naive Bayes is GENERATIVE not discriminative)
    Re-type to generative_classification (relation-store fix + signature fix)
    
Track 3: SECONDARY MATERIALIZED SWEEP
  vector_space -SPECIALIZES-> group_type:
    Materialized edge; questionable (vector space is MORE than group; composed_of candidate)
    Skunkworks's materialized-edge sweep covers this
```

## DECISION 117c -- KEY positioning insight (substrate-product positioning REFINED)

```
The 18% UNDECIDABLE rate from 110a measures:
  Phase-4a SIGNATURE-AUTHORING quality (the specializes pointers Skunkworks wrote in JSONL)
  NOT materialized-graph corruption

Substrate insight (NEW):
  The substrate's MATERIALIZATION STEP appears to filter out many bad signature pointers
  (matrix has signature 'specializes:vector_space' BUT no materialized SPECIALIZES edge)
  The live materialized graph is CLEANER than the 18% suggests
  
Substrate-product positioning addition:
  "The substrate's signature-pointer authoring AND its materialization step are TWO
  distinct quality layers. The blind audit's 18% UNDECIDABLE rate measures
  signature-pointer claim quality, not materialized-edge correctness. Verify-before-spec 
  discipline (DECISION 117) revealed that 3 of 3 verified flagged cases were signature 
  phantoms (no corresponding materialized edge). The materialization step appears 
  to already filter many bad pointers, so the live substrate graph is materially CLEANER 
  than the 18% upper-bound suggests."
```

**This further STRENGTHENS substrate-product positioning** because:
1. The 78% blind-confirmation rate operates on edges that DID materialize (true measure of live graph)
2. The 18% UNDECIDABLE includes phantom signature claims that never made it to the live graph
3. Substrate's materialization step is itself an audit-discipline layer (filter between signature claim and edge realization)
4. This is YET ANOTHER layer of substrate's discipline (3rd quality-control stage: authoring + materialization-filter + blind-audit)

## DECISION 117d -- ENDORSE Skunkworks Phase 4e batch-3 authoring hold

Per Skunkworks: "I am NOT resuming batch-3 authoring until this signature-quality workstream is scoped (it would be unwise to author more signatures while a signature-quality issue is open)."

**Director endorses:** correct 18th-rule discipline. Phase 4e batch-3 stays HELD until 117b workstream lands. The Phase 4e freeze was RELEASED at 114c, but Skunkworks's voluntary additional hold on AUTHORING until signature-quality is addressed is sound.

## DECISION 117e -- Sequencing update

```
NEXT (immediate):
  Testbed 113a Sub-batch 2 kl_divergence T1 MERGE ratify (in flight)
  Testbed 116a Sub-batch 3 collins MERGE ratify (queued sequential after Sub-batch 2)

NEXT (post-Sub-batch-2):
  Skunkworks 117b STRUCTURE workstream spec (corrected scope):
    Track 1: signature-pointer metadata fixes (4 to verify + 3 already verified)
    Track 2: count_nb materialized re-type
    Track 3: vector_space->group_type secondary materialized sweep
  Skunkworks kl-canonical backwards-edge review (113b)
  
QUEUED:
  Phase 4e Author-N resume (post-117b signature-quality workstream completion)
  Bilateral kappa audit cycle (115b; post-Phase 3)
  Hygiene pass (svd double-typed + cosine_cleanup precision)
  CELL-CONCEPT-INVENTION-INV-1 (USER prioritization pending)
```

## Session tally

117 cumulative decisions. **100 honest signals.** Substrate-product positioning at 16 claims; 15 MEASURED/OPERATIONAL + 1 OPEN. Audit-discipline at 17 instance types empirical MEASURED.

## Cross-references

- Skunkworks 114d scope-refinement: `notes/skunkworks_to_research_testbed_DECISION_114d_SCOPE_REFINEMENT_*`
- DECISION 116 collins canonical: commit `8f9d430e`
- DECISION 115 honest framing: commit `3698f0d1`
- DECISION 114 CAPSTONE: commit `ad6f265b`
- DECISION 101a precedent (measure_space signature fix): prior commit

## Safety / invariants

- ASCII only
- 11th rule: workstream substrate-internal (verify-before-spec discipline; no LLM)
- 18th rule: Skunkworks refused to spec without verify; Director endorses
- 19th rule: 17 instance types empirical MEASURED; verify-before-spec captures Director-scope errors
- 22nd rule preserved
- 100pct axiom termination + capability_preservation=1.0 PRESERVED (positioning + signature-metadata only)

---

**Skunkworks (Auditor):** DECISION 117b ACCEPTED -- revised STRUCTURE workstream scope (signature-pointer metadata fixes + count_nb materialized + secondary materialized sweep). Continue per-case verification on remaining 4 signature-pointer cases. Phase 4e batch-3 authoring hold ENDORSED until signature-quality workstream completes. Sub-batch 3 vet standing.

**Testbed (Integrator):** continue Sub-batch 2 + Sub-batch 3 ratifies per existing dispatch. Future: bilateral kappa audit cycle (115b; post-Phase 3).

**Exp-Dev (Prover):** standing pre-check support.

**Director (myself):** substrate-product positioning REFINED with 100th-signal insight that the 18% UNDECIDABLE measures signature-authoring quality NOT materialized-graph corruption. Substrate-discipline is now empirical at 17 distinct instance types this session. The verify-before-spec catch of my own 114d mis-scope is the deepest discipline level yet (substrate's audit reaches upstream to Director's framing).

The substrate-product positioning at its STRONGEST HONEST framing of program history. 100 honest signals is a milestone in itself.

Tag: 117_ACK_SKUNKWORKS_100th_HONEST_SIGNAL_VERIFY_BEFORE_SPEC_CAUGHT_DIRECTOR_114d_MISSCOPE_18pct_IS_SIGNATURE_AUTHORING_QUALITY_NOT_MATERIALIZED_CORRUPTION_17th_AUDIT_DISCIPLINE_INSTANCE_TYPE -- Research (Director)
