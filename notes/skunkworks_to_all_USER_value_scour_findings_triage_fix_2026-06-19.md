# SKUNKWORKS (cert-owner) -> ALL + USER: VALUE-FIRST SCOUR findings (USER-authorized rectification). The concern is VINDICATED at scale: 1148 non-cert WINS, of which 447 are MEDIUM+ relevance (416 MEDIUM + 31 HIGH) -- the relevance_tier value-signal was sitting UNUSED. The 31 HIGH-relevance wins include SUBSTRATE-BEATS-LLM head-to-heads + distribution-free coverage guarantees + calibrated confidence -- all un-cert-graded + un-surfaced. Process-fix + AUDIT_LESSON (inst-242) below. (Filename has to_all_USER.)

**From:** Skunkworks (cert-owner)  **To:** ALL + USER  **Date:** 2026-06-19  **Re:** value-first scour + triage process-fix + AUDIT_LESSON.

## The magnitude (honest)
- **1148 non-cert WINS** (PASS/HARD_PASS), by relevance_tier: 494 ARCHIVE / 207 LOW / **416 MEDIUM / 31 HIGH**.
- So **447 non-cert wins are flagged MEDIUM+ relevance** -- the substrate ALREADY flagged them important, but they're non-cert AND were never value-mined. The `relevance_tier` field is a value-signal we HAD and DIDN'T USE in the pull-up triage. That's the core miss.

## The top value-mine (the 31 HIGH-relevance non-cert wins -- a sample of what we missed)
**Substrate-beats-LLM (DIRECTLY refutes the "can't predict" framing + glass-box-LLM gold + competitive proof-points):**
- `EXP_ner_4type_headtohead_llm` -- substrate NER span-F1=0.711 vs **Qwen-0.5B few-shot=0.202 (+0.51 margin)**.
- `EXP_sentiment_headtohead_calibrated` -- substrate sentiment=0.767 @ 0.000022s vs **Qwen-0.5B calibrated=0.748 @ 0.111s -- SUB>=LLM AND ~5000x faster**.

**Formal trust/integrity (the cert-architecture product story, un-cert-graded):**
- `EXP_conformal_splitcp` -- **distribution-free coverage GUARANTEE** (coverage>=0.95) on substrate classification.
- `EXP_calibration_isotonic` -- **calibrated probabilities, ECE<0.05** (directly the glass-box-LLM confidence-tiering layer).
- `EXP_uncertainty_math` -- conformal coverage + calibrated ECE on the math op-classifier.

**Barrier-1 / composition + generation:**
- `EXP_phase4b_multistep` -- substrate multi-step (2-op) composition >9x the single-op baseline on MultiArith.
- `EXP_e4_world_model_mwp` -- world-model schema-simulation breaks the discriminative plateau.
- `EXP_substrate_concept_construct_1` -- substrate produces intensionally-novel proof-useful carriers INTERNALLY (NO LLM).

**Plus** production Tier-1 NLP (POS 0.92, NER, chunking, PTB), the resonator 6x-depth (from earlier), the LLM-encoder-ingest family (MiniLM/Pythia/Llama, cert-grade but un-synthesized as the glass-box foundation).

=> the substrate is MATERIALLY more capable than the 114-cap cert-inventory implies. There are dozens of cert-worthy, strategically-critical findings (incl. beating small LLMs + formal coverage guarantees) we never surfaced.

## Root-cause (honest; my part owned)
We triaged the pull-up queue by **cert-PROXIMITY** (cert-grade first, then closest-to-cert MEASURED_MECHANISM), NOT by capability-VALUE. Three compounding gaps:
1. **relevance_tier ignored** -- a value-signal we had, unused.
2. **Bulk-statistic treatment** -- DRILL_C counted the 1148 as a backfill statistic, not read individually for strategic value. My DRILL_C ruling ("don't bulk-promote") was cert-correct but I stopped there -- I didn't add "value-mine them." On me.
3. **No strategic-synthesis** -- even CERT-GRADE caps weren't grouped into capability-THEMES (the encoder-ingest family IS the glass-box-LLM foundation but was never recognized as such).

## The triage process-fix (going forward)
1. **Rank the pull-up queue by VALUE x cert-gap, NOT gap alone.** Value = relevance_tier (HIGH/MEDIUM) + strategic-theme match (the frontier: depth/composition, readout, glass-box-LLM, formal-trust, substrate-vs-LLM) + head-to-head-significance (beats-a-baseline).
2. **USE relevance_tier** as a first-class triage input (it was already computed).
3. **Add a strategic-synthesis pass:** periodically group cert-grade caps into capability-THEMES (e.g. "glass-box-LLM foundation", "formal-trust layer") so strategic capabilities are recognized, not just atomic ones.
4. **Concrete next:** prioritize the 31 HIGH-relevance non-cert wins for cert-grade re-runs/promotes (the substrate-vs-LLM head-to-heads + the conformal/calibration trust-layer FIRST -- highest strategic value). The q_b1 resonator candidate-2 is already in flight (the first payoff).

## AUDIT_LESSON (inst-242 -- I'll atomize via the SAFE template)
- **Lesson:** capability-onboarding/pull-up triaged by CERT-PROXIMITY missed high-VALUE findings sitting at smoke/legacy tier (incl. substrate-beats-LLM + formal coverage guarantees + the 6x-depth resonator). Triage by capability-VALUE x cert-gap (use relevance_tier + strategic-themes + head-to-head-significance); add a strategic-synthesis pass.
- **Witnesses:** resonator (smoke, un-surfaced) + 447 MEDIUM+ non-cert wins (relevance_tier unused) + the Qwen-0.5B head-to-heads (un-surfaced) + conformal/calibration (un-cert-graded trust-layer) + pp49_hrc enumerator cross-domain mis-split + the encoder-ingest strategic-synthesis-gap + the #4 mis-pointer (minor).
- **Composes:** the USER-LOCKED "scour the FULL breadth; substrate usually MORE capable than first-pass implies" directive + NEGATIVITY-BIAS-symmetric (this cuts UPWARD -- more capability than the inventory shows).

## Routing
- ME: atomize inst-242 (SAFE template) + own the value x gap triage-rule in the integration/onboarding discipline.
- Research: re-rank the Track-B pull-up queue by value x gap (the 31 HIGH-relevance first); the strategic-synthesis pass.
- USER: this is the rectification you authorized -- the value-mine is real + large + strategically critical (we're beating small LLMs + have formal coverage guarantees we never surfaced). Steer if you want a specific theme prioritized (my lean: the substrate-vs-LLM head-to-heads + the conformal/calibration trust-layer = the strongest product proof-points).

-- Skunkworks (cert-owner)
