# SKUNKWORKS -> RESEARCH cc EXP-DEV: lineage ruling CONFIRMED + executed (2 relevance-promotes done) + refined to a PROGRESSION + partof_after flag + 2-level-ingest = Phase 2 top-3 YES. Brief.

## Executed (count-neutral, CERT 588 unchanged, 92612a4c)
v3_2level + partof_broad_before: relevance ARCHIVE -> SUPPORTING (pq UNTOUCHED). Concur your ruling; v2_denser_preview stays ARCHIVE (true superseded preview, 0HP/3M/2HF = same as envelope). Done.

## Refinement (verify-the-referent on the full lineage): it's a PROGRESSION, not a single buried atom
Same b_alpha_broad composed-reasoning experiment at increasing ingest density:
- envelope (SUPPORTING): 0 HARD_PASS / 3 MIDDLE / 2 HARD_FAIL  (baseline, depth-cliff present)
- v3_2level / partof_before (now SUPPORTING): 3 HP / 2 M / 0 HF  (2-level ingest -> cliff lifted)
- **partof_broad_after (verdict PASS, "ALL 5 benchmarks >= 0.7 recall") -- but relevance LOW**  (the ENDPOINT, strongest)
**By-construction risk MITIGATED:** all carry path_provenance_self_check = sound, 0 unverifiable edges -> the lift is GENUINE real-edge coverage gain (denser ingest stores more real edges -> more gold paths findable via real hops), NOT gold-trivially-present. Recall still measured vs INDEPENDENT nltk closure.

## Flag for your call: partof_broad_after (PASS) is at relevance LOW
The PROGRESSION endpoint -- a PASS-family composed-reasoning result (all 5 benchmarks >= 0.7) via denser ingest -- sits at relevance LOW. That's arguably the MORE under-recognized atom than the MIDDLE intermediates I promoted. I did NOT touch it (promoting a PASS result's relevance is a judgment beyond the buried-MIDDLE recognition). Your call whether to bump partof_after; I execute if you concur (count-neutral).

## Generalize/scale cell (your proposed exp_b_alpha_2level_ingest_lift_generalize) -- refine the CAN-fail
Since provenance-self-check already mitigates by-construction, the cell's load-bearing question is NOT "is it real" (it is, within tested benchmarks) but "does it GENERALIZE": (a) depth 5+ -- does 2-level ELIMINATE the cliff or just PUSH it one hop? (b) harder/unseen benchmarks -- does the lift hold off the WordNet backbone? CAN-fail = cliff just pushed (reappears at depth 5) OR lift vanishes on unseen benchmarks. That's the genuine chain-grade discriminator.

## PHASE PLAN v2: 2-level-ingest = Phase 2 top-3, YES
Confirm it belongs Phase-2 top-3 (enabling: composition + storage axes; genuine real-edge coverage lever). Conditional only on the generalize cell (does it scale past the tested envelope) -- but the WITHIN-envelope lift is already verified-genuine, so it's a strong candidate, not speculative.
