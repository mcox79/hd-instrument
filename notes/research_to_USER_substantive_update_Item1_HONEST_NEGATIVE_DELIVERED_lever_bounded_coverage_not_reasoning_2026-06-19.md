# RESEARCH (Director) -> USER: substantive update -- Item 1 DELIVERED. Cert-grade HONEST_NEGATIVE LANDED via genuine non-coextensive held-out PART_OF test; lever EMPIRICALLY BOUNDED as coverage-completion-NOT-reasoning. CERT 571->572 incoming. This is the exact anti-over-claim evidence the WRITEUP (Item 3) needs + the highest-integrity outcome the discipline could deliver: a proven LIMIT, not a victory-lap.

**From:** Research (Director)  **To:** USER  **Date:** 2026-06-19  **Re:** Item 1 HONEST_NEGATIVE delivered. ASCII; fname_v2.

## Bottom line

The 3rd sprint's discriminating cert-experiment DELIVERED a cert-grade HONEST_NEGATIVE: the universal-lever finding from sprints 1+2 (HYPERNYM + PART_OF both coverage-limited; 2-level completion = universal lever) is now EMPIRICALLY BOUNDED. The lever is PER-SYNSET-COVERAGE-COMPLETION, NOT transferable-via-reasoning. Completing TRAIN-subset synsets does NOT help answer HELD-OUT synsets' own-edge-dependent 2-hop QA. The substrate's n-hop WordNet QA is COVERAGE, not REASONING/inference-transfer.

CERT 571 -> 572 incoming (Exp-Dev atomize imminent; Skunkworks reactive landed-verify after).

## The numbers (Skunkworks independently reproduced; exact match)

```
held-out before -> after:  0.576 -> 0.598   (delta +0.022)   <- the BOUND
train control:             0.603 -> 0.724   (delta +0.121)   <- completion WORKS on completed synsets
non-coextensiveness:       VERIFIED (heldout_edges_in_train_completion=0)
n_heldout_positives:       92  (>=30 minimum)
false-positives:           0   (verified-unreachable held-out negatives)
test-validity:             ESTABLISHED (control moves; non-degenerate; fp=0; the null is REAL refutation, not non-test)
```

Skunkworks re-ran the cell themselves and got EXACT-MATCH numbers (their verify-the-referent + reproducibility discipline; the test is deterministic + cert-grade-solid).

## Why this is the highest-integrity outcome

1. **GENUINE non-coextensive test** (unlike sprint 1+2's HYPERNYM/PART_OF coextensive MEASURED_MECHANISM recoveries). The train-completion does NOT add held-out synsets' own edges by construction; a positive would have been cert-grade-discriminating; the NEGATIVE is equally cert-grade as a PROVEN BOUND.

2. **The test-validity gate PASSES** (control moves +0.121 = completion works on completed synsets; baseline non-degenerate; fp=0). So the held-out null is a REAL transfer-failure, NOT a broken/degenerate test. (The DEGENERATE-REGIME-NOT-REFUTATION audit-lesson is the FLIP side; this test passes it.)

3. **The discipline cut toward HONEST LIMITS, not wins.** The cert-grade outcome was always going to be either jump (= surprising; mandatory leakage-audit before granting) OR null (= a proven coverage-bound). Either way cert-grade + load-bearing. The actual outcome (null) is more informative — it tells us EXACTLY what the universal-lever is and isn't.

4. **It is the precise empirical anti-over-claim evidence the WRITEUP needs.** Per Skunkworks's binding framing-VET condition #2 (honest-scope: universal lever is WordNet-RELATION-COVERAGE-specific; reasoning-engine framing bounded to MEASURED), the WRITEUP MUST carry this bound: "n-hop WordNet QA = coverage-completion, NOT inference-transfer." Now the bound is empirically grounded in a cert-grade atom.

## Honest-scope (carries verbatim into the atom)

- **The bound:** completing TRAIN synsets' 2-level edges did NOT transfer to answer HELD-OUT synsets' own-edge-dependent 2-hop QA. The deterministic BFS does NOT INFER a held-out synset's absent edges from OTHER synsets' completions.
- **Scope:** PART_OF / meronymic / WordNet / deterministic-BFS / in5k closure. NOT a general-reasoning claim.
- **Marginal-transfer note (NEGATIVITY-BIAS-symmetric):** the +0.022 held-out lift is NOT zero (~18% of control's +0.121) -- a few held-out chains route hop-2 through a SHARED TRAIN intermediate whose edge got completed. Report the small transfer; conclusion holds (far below the control).
- **Test-validity note:** baseline built from STORED meronyms (asymmetric pre-Item-1 state), NOT nltk's symmetric closure (which would no-op the completion); fp=0.

## What this means for the substrate-as-reasoning-engine framing (Item 3 WRITEUP)

The framing CAN now say: "the substrate has cert-grade evidence of CAPABILITY-COMPLETION via canonical-direct-link 2-level ingest (HYPERNYM + PART_OF universal-lever), bounded to PER-SYNSET-COVERAGE-COMPLETION (Item 1 HONEST_NEGATIVE; deterministic BFS does not infer-transfer)."

The framing CANNOT say: "the substrate reasons across the WordNet ontology via inference-transfer" — that's exactly the over-claim Skunkworks's verify-discipline + this experiment now empirically RULE OUT.

This is your prior catch (recapture-program-specific findings extrapolated to substrate-wide capability claim) now backed by an empirical limit, not just a methodological caveat. Stronger evidence for honest-scope.

## What I'm doing next

- Item 4 catalog audit (un-gated; Skunkworks's worklist gives me the starting cleanup queue + I scour all 52 AUDIT_LESSONs for composes_with resolves)
- Item 3 WRITEUP scour-FULL-substrate-breadth precursor (per Skunkworks's binding condition 1: 571 CERT + 432+ domain-positives across NLP/cognitive/audit/KG/retrieval BEFORE drafting; not depth-cliff arc only)
- Reactive on Exp-Dev's Item 1 atomize (CERT 571->572) + Skunkworks's landed-verify + Skunkworks's algebra-violator + dup-instances fixes + Skunkworks's framing-VET on the WRITEUP

## Substrate state

- atoms 43,900 (43,899 + cell_sha-marker inst 95)
- CERT 571 → 572 incoming (Item 1 HONEST_NEGATIVE atomize)
- AUDIT_LESSON 52
- engine 7 LIVE (8th gate not yet; narrative-data-consistency stays SCHEMA-VET per Item 11 verdict; mechanizability dimension added to METHODOLOGY_RULE)
- Pipeline restored (origin/main c4451230); 67-commit cert-arc durable on GitHub

Sprint character delivered as scoped: PROVE-THE-LEVER-GENERALIZES (falsifiably) — answered honestly (it doesn't; bounded to coverage-completion). INTEGRATE-HONESTLY (WRITEUP) — now with the empirical bound to carry. HARDEN-THE-FLOOR (invariant-check v1.1) — LIVE.

-- Research (Director)
