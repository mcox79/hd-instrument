# Director BACKUP -- CURRENT STATE 2026-07-05 (clean; supersedes 2026-07-04)

**Read end-to-end first. Self-contained. Supersedes director_POST_COMPACTION_BACKUP_FULL_STATE_2026-07-04.md.**

## STEP 0 on pickup
`date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp` (every turn-end).
Then `python tools/inflight_monitor.py`. Companion doc = `notes/integrated_short_term_spec_sheet_5x_drills_what_we_want_how_brain_does_it_2026-07-05.md` (per-goal specs + VET-results + LATEST-LANDINGS).

## 6-HOUR UNATTENDED FULL-AUTO WINDOW (opened 2026-07-05 ~17:45Z; USER away until ~23:45Z)
USER: "full auto authorized no survey questions implement your recommendations. I will be away for 6 hours." + "keep moving - no holding or standing" + "there is a ton of work to do." NO AskUserQuestion EVER (blocks all progress -- [[feedback-no-askuserquestion-tool]]). Make every call myself; state + continue; never wait for ack. 20-min cron 3f99d7f4 (:07/:27/:47) drives cadence -- on each fire: inflight_monitor, process landings (VET via hdi_skunkworks -> atomize), fire next wave, keep THIS doc current. Run at full budget (5 concurrent), cycle waves as agents land. Only hard blocks = USER-locked safety gates (re-encode HELD; harness-gated origin push handled by orchestrator). AUTONOMOUS CADENCE per cron fire: (1) any FULL landed? -> VET + atomize. (2) any smoke passed? -> route to orchestrator for canonical FULL. (3) any drill spec'd? -> ship the cell. (4) budget free? -> fire next envelope/brain-component build. (5) keep recovery doc + todo current. WAVE-1 (fired ~17:45Z): orchestrator 3-anchor dispatch (cfrpe/reframe/hard-comp FULLs), hubness-debias rescore (prize rank-1), reasoning-deeper envelope, generation-higher-vocab envelope, brain-component-multiples drill. WAVE-2 backlog: perception/encoder (weakest capability, USER primary focus, OOM-fixed cell exists), integration-hard FULL dispatch, the drilled specs -> cells, comprehension superposition/scale envelope, memory deg8+ canonical FULL.

## WHAT THIS SESSION DID (2026-07-05)
USER goal (locked): build a **fully-functional glass-box-LLM-capable substrate** (perception, memory, reasoning, generation, generalization, comprehension, control -- all inspectable, brain-grounded). NOT vs-LLM.
Arc: 5x-drilled every short-term goal -> built + independently VET'd a first cell of each capability -> iterated on the hard ones -> tested end-to-end composition -> opened comprehension + control -> USER added the BRAIN-COMPONENT thrust.

## HONEST AUDIT-SCOPED SCOREBOARD (no smoke -- the audits trimmed several of my over-claims; lead with these)
- **PERCEPTION**: BETTER than "mediocre" -- **BURIED WIN found (encoder scoping drill a767d9c4, 2026-07-05):** an in-batch-RKD-only arm (step1b_v3c) already cleared 0.85 with algebra INTACT (spearman 0.886 5-seed, keyed@J5=1.000), but was buried UNPROMOTED inside a HARD_FAIL cell whose verdict came from a DIFFERENT co-arm's algebra failure (InfoNCE tail-corruption + GLOBAL-landmark breaking bind/unbind). Proven blocker = TRAINING-OBJECTIVE MISMATCH, NOT teacher-dependence/capacity/sparsity. 0.85-NATIVE unrealistic at 970K scale -> distill-from-BGE pragmatic (maybe closer to shipped than tracked). **BURIED WIN VERIFIED off-disk (5 seeds, a5e3ceb): spearman 0.886 [0.852,0.897], keyed@J5=1.000, GLOBAL co-arm 0.033-0.317 (drove the false aggregate HARD_FAIL).** Ship-metric carrythrough cell (exp_encoder_step2step3_inbatch_rkd_shipmetric_carrythrough_v1, commit b9be16fd9): smoke HARD_PASS but HONEST FULL-FORECAST = MIDDLE_BAND (measured, not smoke-inflated): cosine-to-gold ~0.827 (straddles 0.80, algebra roundtrip 1.000 INTACT) BUT **ret-agree@10 ~0.221 MISSES 0.30 -- the proxy-to-real gap: spearman 0.886 does NOT imply top-10 retrieval agreement (the ship-relevant metric).** **FULL LANDED seed_7 MIDDLE_BAND (verified off-disk): cosine_to_gold=0.861 CLEARS 0.80, algebra iso@J5=1.0/composed@J10=0.983 INTACT, but ret_agree10=0.184 MISSES 0.30 -- retrieval-agreement gap CONFIRMED (matches forecast).** Perception's FIRST ship-metric verdict = MIDDLE (real signal, gap = top-10 retrieval). Seeds 13/23/29/31 CRASHED on a checkpoint-resume bug (intermediate dir not seed-namespaced -> cross-seed contamination); FIX DONE (a52bc85, commit 230dabdde, seed-namespace verified 2-seed no-contamination) -> RE-DISPATCHING 4 seeds (ad963be) for the 5-seed baseline (seed_7 already valid). LATENT v3c same-seed-resume gen.set_state bug DEFERRED (rare, edits shared v3c, regression risk). ARCHITECTURAL NOTE: the distill chain (BGE->MLP->block-STE) has a DIFFERENT input space than the char-positional Step2/3 chain -- "wire the checkpoint in" was over-simplified; a self-contained teacher-anchored ship metric was built instead. **RETRIEVAL GAP HAS A PROVEN FIX (graded-code cell aabfe84 disk-check, Fix#28): the GSBC graded-code / STE-anneal lever ALREADY LANDED HARD_PASS on the same 177899-concept cache (v11/v12), invisible to the scoping note (which only inventoried step1b).** v11 (exp_encoder_v11_gsbc_graded_sparse_v1): graded GSBC ret_agree10=0.3986 vs hard 0.2117 = +0.187, CLEARS 0.30 gate, cosine 0.834 + keyed@J5=1.000 preserved. v12 (gwta EXPAND2X): ret 0.6027, J8=J16=1.000. => **perception's retrieval gap (seed_7's only miss) is SOLVED by graded codes.** Only fresh gap = composed@J10 for GSBC_FULL (v11 gated J5; v12 J16=1.000 likely covers it). **RE-VERDICT DONE (a12d295, atom math#25 MM): PERCEPTION PASSES the ship gate with graded codes -- retrieval gap SOLVED.** v11 GSBC_FULL ret_agree10=0.3986/0.3968 (clears 0.30), lift +0.187 (cv 0.002), cosine 0.834 (>=0.80), algebra keyed@J5=1.000 + composed@J10>=0.983 (sound monotone bracket on same-geometry block arm; v12 EXPAND2X is a DIFFERENT code so doesn't directly cover J10 but the block-family arm does). CAUSE CONFIRMED: hard-STE SIGN quantization was the SOLE bottleneck (seed_7 failed ONLY the ret axis); graded codes fix exactly it. Tiered MEASURED_MECHANISM = ONE co-measurement reservation (no single artifact co-measures ret+cosine+J10 on one deployed code w/ exact J=10) -- a CERT nicety NOT a capability gap; confirming FULL is OPTIONAL/high-prior-PASS. **PERCEPTION = PASS (retrieval SOLVED via graded GSBC codes).** 0.85-native unrealistic at 970K -> distill-from-BGE pragmatic.
- **MEMORY**: SOLID. dense-Hopfield recall->1M (CHAIN_GRADE); bundle ~200 items/N8192 (MM, linear-memory law ~40 floats/item); hubs deg5-7 via protected/permutation-index binding (MM); deg8+ via redundancy banks 0.40->0.99 (HARD_PASS smoke, FULL deferred -- 1.3GB BGE cache local-only).
- **REASONING**: **CHAIN_GRADE** (VET-confirmed). Regenerative digital-repeater cleanup ~0.70 depth-5, ~9-10 usable hops, faith 1.0. NOTE: the earlier "modest 0.26" was a chain-key-COLLISION artifact (collision-corrected v2 = strong). **ITERATIVE-CLEANUP LEVER CLOSED (exp_cortex_iterative_attractor_cleanup_depth_ceiling_v1, commit 103aab7fc, smoke HARD_FAIL, decisive):** iterative/resonator modern-Hopfield attractor cleanup is BIT-IDENTICAL to single-shot argmax (3/3 seeds, delta_usable=0) -- one-shot snap is ALREADY the MAP decoder for a near-orthogonal codebook (self-test: attractor@beta=60==argmax). FULL skipped (confirmatory negative). **NEW MECHANISM (corrects the crosstalk model): the depth ceiling is COLLISION-BOUND (N_TEST/key-capacity), N-INDEPENDENT -- NOT crosstalk (argmax stays ~52sigma; breaking needs M~N^2). REAL lever for deeper chains = MORE KEY SLOTS (richer relation/node vocab V_CHAIN*P) + SHARDED storage, not iteration/bigger-N.** **LEVER CONFIRMED + DEPTH EXTENDED (exp_reasoning_depth_keyslots_sharding_v1, commit 31f53bff6, smoke HARD_PASS @ full-N): usable depth 5.3(baseline K=2048/coll0.249) -> 8-9(2x/coll0.14) -> 18+(4x/coll0.066, CENSORED at D_MAX); keyslots == sharding (OVERLAY, identical collision physics); shuffled-structure control = 0 (extension from STRUCTURE not store size); cv 0.03.** So REASONING IS NOT just CG @ ~10 hops -- it's EXTENDABLE to 18+ via richer-vocab/sharding (aligns w/ prior 24-hop sharded-reasoning atom). FULL dispatching (un-censors 4x depth, N-independence, NT=40). VET on landing.
- **GENERATION**: **CHAIN_GRADE** (VET-confirmed) + **ENVELOPE PUSHED to V=65536 (RNS/CRT, HARD_PASS smoke @ FULL regime 2026-07-05, commit 4c7088694, FULL dispatching a7acfa4).** RNS/CRT sub-blocks (moduli 40.41.43, V_eff=70520 via 124 residue codes = 528x codebook compression) hold exact-ordered 1.000 to V65536/D26 where the CORRELATED (realistic native-GSBC) single-block cliffs to 0.111; scram-residue control collapses 0.000 (CRT load-bearing). HONEST SCOPE (agent-flagged, don't over-claim): RNS MATCHES the iid single-block ceiling, does NOT beat it in general -- its defensible wins are (1) 528x capacity-efficiency, (2) correlation-immunity BY CONSTRUCTION (residue labels non-semantic -> decorrelated codebooks; the grid-cell property Kymn/Fiete). Smoke ran AT full regime. **FULL LANDED HARD_PASS (2026-07-05, verified off-disk): rns_crt=1.000 @ V65536/D26, correlated single_corr cliffs 0.16, gap 0.840, wall 48s.** VET+atomize in flight (ab4d993).
- **INTEGRATION (goal-level: does it COMPOSE)**: **HARD_PASS at FULL, VET-CONFIRMED chain-grade (LANDED + VET'd 2026-07-05, af274c4).** Honest scope (VET trimmed 3 over-claims): it's end-to-end **OBJECT-SLOT recovery** = 1.000 (subj/rel handed in CLEAN -- NOT a 3-slot compositional/exact-ordered-triple test); reasoning hop is **load-bearing** (sever it -> broken-reasoning collapses to chance 0.000); a bridge is needed vs naive-randproj (=0.000) BUT a symbolic cleanup->lookup TIES the co-trained bridge (co-trained NOT shown uniquely necessary at this scope). discriminators fire for the right reason (not saturation-vacuous). The correlation_cone 0.51 RED FLAG resolved = BGE anisotropy (fillers share a cone -> task HARDER, not near-chance). Load-bearing survivor: discrim_gap=1.000. **HARD-REGIME PROBE DONE (exp_integration_...HARD_v2, commit 24e84083, smoke verified off-disk) -- decisive NEGATIVE that is GOOD for the glass-box story:** at hard regime (V=4096 hub-crowded, D=10, hops=3, subj+obj both recovered-noisy-and-bridged) the co-trained LINEAR bridge does NOT beat symbolic cleanup -- the OPPOSITE: cot_minus_sym = -0.236 (easy) / **-0.805 (hard)**, learned_separates=False, symbolic dominates every regime (hard: sym obj_acc=0.903 vs cot 0.125; sym end2end=0.861 vs cot 0.056). MECHANISM: symbolic re-emits a CLEAN codeword via NN-argmax (bit_agree=0.977) where the learned linear bridge stays noisy (0.764) and loses the hub-crowded generation argmax. => **the substrate's reason->generate composition is EFFECTIVELY SYMBOLIC / attractor-cleanup, NOT a learned bridge -- which is BETTER for glass-box (inspectable NN-argmax over a known codebook, no opaque learned component needed).** The learned bridge is not load-bearing and is not needed. SCOPE: bounded to LINEAR bridges (MLP/nonlinear denoiser is an OPTIONAL follow-up -- strategically unnecessary since symbolic already composes transparently). **FULL LANDED HARD_FAIL (2026-07-05, verified off-disk): co-trained linear bridge margin=-0.706 vs parameter-free symbolic cleanup -- composition EFFECTIVELY SYMBOLIC confirmed at scale (glass-box positive).** VET+atomize in flight (ab4d993).
- **COMPREHENSION**: STRONGER than "holds hard" -- **ENVELOPE CHARACTERIZED (exp_comprehension_envelope_superposition_vocab_v1, commit 704ff5539, smoke HARD_PASS @ full-N/full-grid, verified off-disk).** frame-classify + content-conditioned role-typing (selectional-restriction). ORDER recovery holds ACROSS THE WHOLE GRID: to 8 constituents @ V=1000, NO in-grid cliff (min 0.975). FULL-PARSE (order + all superposed fillers) holds to 6 constituents/V500, graceful cliff only at extreme corner (D8/L4 x V1000 = 0.520). Occupancy-blind at CHANCE (0.503); role-blind decode COLLAPSES to 0.0 at load>=2 -> selectional restriction PROVEN NECESSARY not just helpful. FULL dispatching (ad963be, remote_cpu 3-seed). (The older hard_comprehension_v1 FULL is still PROT-022-blocked but this envelope cell supersedes/sidesteps it.)
- **FRONTIER / GENERALIZATION (the prize)**: **DIAGNOSIS REVISED 2026-07-05 -- more hopeful + more accurate.** Content-conditioned bilinear moved transfer off shuffle-invariant-0, but exact-single-answer plateaus ~0.11 / collapses V>=300, and richer-content=HARD_FAIL. **BUT the reframe drill (a79824) found the wall is NOT a raw fan-out/entropy ceiling** -- only 10-30% of subjects are multi-valid; subject-identity oracle ceiling 0.81-0.94 >> observed 0.06-0.09; a kNN on the SAME features lands in the same band as the trained scorers (rules OUT under-param, 3 model families converge). The real mechanism = **near-miss content-neighbor competition**: the true object lands in the top-k but not rank-1 (crowded by semantic neighbors). Score by RANK not argmax and **Hits@10 real-minus-shuf recovers to +0.26 (AtLocation) / +0.18 (CausesDesire), MRR +0.11** -- and the morphological control (DerivedFrom) shows the OPPOSITE (rank-1 concentrated) = content-specific, not noise. So single-answer was the ILL-POSED metric for a one-to-many task. REFRAME `exp_schema_relation_hitsatk_mrr_reframe_v1` (filtered Hits@k/MRR, Bordes 2013; commit 9cf4cc8c9) landed **MIDDLE_BAND at smoke (verified off-disk) -- HONEST RECALIBRATION (was over-hopeful):** it VALIDATED the diagnosis (Hits@10 rms clears +0.20: AtLocation +0.213, CausesDesire +0.449 -> the object IS in the top-10 more than shuffled, exact-match WAS too harsh) but does NOT fully rescue -- MRR misses (AtLocation +0.108 < 0.15), i.e. object in the NEIGHBORHOOD but not reliably RANK-1; near-neighbor competition limits precise resolution. So the prize goes HARD_FAIL(exact) -> MIDDLE(rank): REAL partial recovery, NOT a broad win. FULL LANDED MIDDLE_BAND, VET-CONFIRMED + HEADLINE TRIMMED (ab4d993): the "Hits@10 rms=0.653" was a BEST-OF-SLOT PHANTOM (REAL Hits@10 ~0.75 across ALL slots; rms only looked big because JOINT-SHUFFLED collapses to 0.10 -- co-trained overfits random labels -- and best-of-slot picks that collapsed-baseline slot). **FAIR popularity-robust lift = Hits@10 rms 0.17-0.20 (BELOW the 0.20 gate); rank-1 fair lift 0.087 = EXACTLY the prior entropy-ceiling, UNCHANGED.** So the reframe recovers REAL but SUB-GATE signal; rank-1 NOT meaningfully improved when read fairly. MORE middle than the headline -- Director's phantom-caution validated. (META atom: best-of-slot over paired-rms inflates when one slot's control collapses -> anchor on the popularity-robust slot's ABSOLUTE lift.) ENVELOPE-PUSH DRILL DONE (a3d3ef9 -> notes/research_hubness_popularity_debias_rank1_sharpening_2026-07-05.md): the rank-1 crowding is diagnosed = **HUBNESS (Nk Gini 0.87-0.95, textbook Radovanovic signature) + LABEL-PRIOR bias (train-freq corr 0.42-0.80)**, split by relation (CausesDesire ~all label-prior; AtLocation genuine geometric hubness) -- NOT diffuse noise, NOT attractor territory. POST-HOC FIX TESTED (ae0d9a3, exp_schema_relation_hubness_debias_rescore_v1, commit 4de621d1a) -> **MIDDLE_BAND (smoke): the cheap route does NOT rescue the prize.** CSLS+logit-adjust post-hoc rescore only helps HUB-COLLAPSED WEAK scorers (undertrained 80-step JOINT: REAL Hits@1 0.067->0.533) NOT full-strength ones (FROZEN = scale-representative shows NO genuine sharpening); 1/4 cells a genuine win, needs >=2. **KEY METHODOLOGY CATCH (banked): the paired rms-MRR lift is a PHANTOM -- gameable by crushing the SHUFFLED control (Hits@1 0.567->0.0) while REAL rank-1 DEGRADES yet rms "lifts" +0.49; the agent added an anti-phantom gate (REAL-abs Hits@1 non-degradation) -- ALWAYS gate on real-absolute not paired-rms.** **SCORER-SIDE LEVER CLOSED (both post-hoc CSLS AND trained-margin, 2026-07-05):** hard-neg margin cell (a426516, exp_schema_relation_hardneg_mined_scorer_v1, commit 47dde849e) smoked MIDDLE (only CausesDesire clears, +0.122 JOINT MARGIN) BUT the agent's own JOINT-at-500-steps (full-convergence) preview shows the win EVAPORATES: +0.122 -> aggregate -0.10 across seeds (actively HURTS on seed19 where CE-baseline converges to 0.467). **The smoke win was a JOINT-80-step UNDERTRAINING artifact -- SAME pattern as the post-hoc CSLS (both only help the UNDERTRAINED scorer).** FROZEN inert to both margin+logit (fixed content-geometry hubness, Feldbauer-Flexer). Hard-neg FULL SKIPPED (JOINT-500 preview decisive, predicted HARD_FAIL). => the prize rank-1 residual pointed at content-decorrelation; de-hub (a42a8b3) tested it. **DE-HUB = HARD_FAIL (commit 1aeb78494, PIVOTAL): Local Scaling de-hub WORKED GEOMETRICALLY (Nk-Gini halved both spaces 0.789->0.442 gen, 0.455->0.229 enc) but ~ZERO downstream lift on BOTH (gen Hits@1 +0.019 seed-noise, enc ret_agree10 +0.002 net-zero, anti-phantom clean, synth positive-control fires). => HUBNESS IS A CORRELATE (rho 0.545), NOT THE CAUSE -- removing it moves neither capability.** BOTH LEVERS NOW CLOSED (scorer-side undertraining-artifact + content-side causally-inert). => the prize rank-1 residual is the ONE-TO-MANY ENTROPY CEILING itself -- single-answer rank-1 for multi-valued relations is likely GENUINELY ILL-POSED; neighborhood/set recovery (Hits@k, ~0.75 absolute / 0.17-0.20 real-minus-shuf) is the ACHIEVABLE form. LAST RESCUE: gen-side V-scan de-hub FULL. **GEN-ONLY MODE ADDED (aafe921d, commit c7a9dad6f) + smoke shows a RESCUE SIGNAL: de-hub lift GROWS with V (V100=-0.011 -> V300=+0.019, trend hi-lo=+0.031) -- if it continues to V1000, de-hub DOES help at realistic scale. RESCUE BACK IN PLAY -> V-scan FULL (V300 vs V1000, 3 seeds) DISPATCHING.** If lift keeps growing to V1000 and clears -> de-hub rescued (prize not fully ceiling-bound); if it plateaus/flat at V1000 -> prize CLOSED as one-to-many entropy ceiling. (PERCEPTION retrieval was SOLVED by graded-code, not de-hub -- so de-hub's only remaining relevance is the generalization V-scan.) **CROSS-THREAD VALIDATED (probe aedb433): hubness is MECHANISM-SHARED across the 2 content spaces (BGE-small-384d gen + BGE-large-1024d encoder; NOT one index but cross-model hub-corr rho=0.545, same top hub words food/animal/clothing). DE-HUB CELL SHIPPING (a42a8b3, exp_content_dehub_joint_lever_v1): Local Scaling at-INPUT + retrain (NOT post-hoc which went phantom) -> lifts BOTH generalization Hits@1 AND encoder ret_agree10? HARD-PASS both>=+0.05; P 0.20 joint / 0.48 one-side. This is the ONLY attack on the FROZEN content-geometry hubness (scorer-immune) + the same lever for perception retrieval.** Hubness FULL = LOW-PRI (predicted MIDDLE). (logQ INAPPLICABLE=full-softmax small-vocab. CLS de-prioritized.)
- **CONTROL**: **HARD_FAIL at FULL, VET-CONFIRMED structural (HF_STRUCTURAL_BOUND; LANDED + VET'd 2026-07-05).** Honest framing (VET): goal-cosine gating plateaus ~0.42 << ORACLE 0.994; the best heuristic captures only **11% of the 0.642 headroom**. The "no additive value" is PARTLY TAUTOLOGICAL -- WM and additive were the SAME target-cosine signal (goal_wm==goal==E[target]; combined per-seed byte-identical to WM), so lead with the headroom/11% framing NOT the additivity string. (depth-3 rows vacuous = endpoint-only-target artifact, not a finding.) ORACLE 0.994 proves the task solvable -> genuine structural gap. **FIX WORKS AT SMOKE (2026-07-05, verified off-disk): exp_pfc_gate_cfrpe_trained_v1** = Go/NoGo gate TRAINED by cfrpe RPE on transition DYNAMICS (PBWM/Frank; commit 7380279fc). GONOGO=0.479 vs ADDITIVE=0.115 vs ORACLE=0.969: **gonogo_lift=+0.365 (>> +0.15 bar), captures ~47% of headroom vs old gate's 11%.** ANTI-TAUTOLOGY GUARD PASSED (the steer worked, load-bearing not asserted): reach_tcos_corr=-0.086/corr_ok, dynamics_lift=+0.438 (beats identity-reach control) -> RPE signal genuinely INDEPENDENT of target-cosine. MIDDLE_BAND smoke; FULL first read HARD_FAIL_ADDITIVE_RAIL -- **but VET (ae4c230b, atom f144563ee) OVERTURNED it: DOWNGRADE HARD_FAIL -> MEASURED_MECHANISM (BROKEN RAIL, not effect-collapse).** The additive BASELINE fell to 0.015 < its 0.05 measurability floor at depth-6/V-2400, tripping the rail-priority branch ABOVE the gonogo branch -> a TEST-DESIGN failure, not structural. Read fairly, the RPE mechanism is ALIVE + SCALE-ROBUST: go/additive ratio GREW 2.85x@d3->7.0x@d8, paired p=1.2e-14, per-hop reach_rank=0.495 vs 0.25, reach_tcos_corr=-0.045 (target-cosine-independent = v3's revival criterion MET). The "collapse" was a depth confound (dd=6 not in smoke depths) + per-hop factor ~0.59 flooring all arms. **CONTROL MECHANISM WORKS (MEASURED_MECHANISM); the FULL regime was unfair (baseline floored).** FIX v2 SMOKE-PASSED (ac801f5e, exp_pfc_gate_cfrpe_trained_v2, commit 1d606f4ec, verified off-disk): **HARD_PASS at FAIR regimes {V200_d4,V600_d4} -- gonogo_lift +0.287, closure 0.360 (>0.25 = 25% headroom), reach_rank 0.509 (>0.50 target), baseline-in-band=true (fixes v1's floored 0.015), anti-tautology reach_tcos_corr=-0.079.** The META_RULE is BAKED IN (returns INCONCLUSIVE_NO_FAIR_REGIME not HARD_FAIL if baseline floors; V2400_d6 auto-excluded as unfair control). FULL DISPATCHING (a4b317, overnight GPU). HONEST CAVEAT (meta-lesson): smoke only tested LOW-V fair regimes; FULL tests V800-2400 -- real question is whether a fair regime SURVIVES at higher V, or the baseline floors everywhere (-> INCONCLUSIVE, an honest not-failure). ENSEMBLE-cfrpe stays DESTAGED. SR-TD floor 0.0176 = irreducible successor stochasticity NOT under-training (reach_rank is the real signal, above target). >>> META-LESSON (banked, META_RULE atom): do NOT swing off a VERDICT STRING in EITHER direction -- smoke over-states AND a rail-triggered HARD_FAIL can under-state; re-read the effect off the paired/multiplicative/per-hop statistic + check the baseline is IN BAND before trusting an absolute-lift discriminator.

**Key meta-finding:** >=4 brain components are EMPIRICALLY REQUIRED for higher function (ablation-style): hippo-index for hubs, regen-cleanup for reasoning-depth, grid-modular for generation, TEM content-conditioning for generalization. Not decoration -- measured requirements.
**BRAIN-COMPONENT INVENTORY UPDATE 2026-07-05:** the trained BASAL-GANGLIA Go/NoGo gate (RPE-trained, PBWM/Frank) moved from MISSING -> HAVE (smoke, strong) via exp_pfc_gate_cfrpe_trained_v1. This was the drill's #1+#2 ranked build ("the missing TRAINING half of a gate we already had"). Confirms the thrust's thesis: the weakness was a missing training SIGNAL (dopamine-RPE), not a missing part. Neuromodulation-as-training-signal (cfrpe used to TRAIN control, not just as an arch ingredient) is now demonstrated. Next brain-component builds (staged): population-coded BG signal (vs scalar), ensemble RPE, 3-tier CLS; cerebellum/thalamus still DEFERRED (unloaded -- but integration's end-to-end loop is starting to provide the multi-subsystem traffic that would load a thalamic router).

## IN FLIGHT (as of ~16:30Z 2026-07-05; check inflight_monitor + task notifications)
- **a16c02f (hdi_exp_dev)**: authoring + SMOKE `exp_pfc_gate_cfrpe_trained_v1` -- the TOP brain-component build (Go/NoGo gate trained by cfrpe RPE; targets CONTROL HARD_FAIL, 0.642 headroom). Spec = brain-component drill Section 4. SMOKE-local then stage FULL for orchestrator.
- **a79824 (research)**: generalization reframe drill -- mechanism of the one-to-many ceiling (fan-out vs conditional-entropy) + design the RANK/SET (Hits@k/MRR) reframe cell spec. Genuinely-new leverage (NOT re-litigating CLS-dual-store, flagged weak/mixed).
- **af274c4 (hdi_skunkworks)**: landed-VET of integration FULL (HARD_PASS, saturation scrutiny) + control FULL (HARD_FAIL, confirm headroom).
- PRIOR in-flight all LANDED (prize=HARD_FAIL confirmed; integration=HARD_PASS; control=HARD_FAIL; hard-comprehension FULL still blocked on PROT-022).

## NEXT-SESSION FIRST ACTIONS
1. **PRIZE RESULT IN (2026-07-05 ~15:40Z): richer-content = HARD_FAIL_ONE_TO_MANY_CEILING_GENUINE** (data/exp_schema_relation_richer_content_vscan_v1/metrics.json). Jointly-trained content does NOT beat frozen at V>=300 (joint-minus-frozen=0.002<0.02); discriminators fired -> the one-to-many entropy ceiling is GENUINE for single-answer relational transfer on thin generic-sentence content. So broad generalization is NOT won via richer content. => NEXT ROUTES (this is THE open problem): (a) CLS slow-consolidation (brain-component route -- DIFFERENT mechanism: offline replay abstracts schemas, not content-richness); (b) REFRAME the task -- one-to-many means single-answer transfer is ill-posed; try RANK/SET prediction (Hits@k over the plausible-object set) instead of exact-single-object, which may be the honest correct metric. Route this decisive negative to a 5x-drill (important-negative). Needs Skunkworks landed-VET of the HARD_FAIL + the ceiling claim.
   **STATUS 2026-07-05 ~16:55Z: REFRAME DRILL LANDED + CELL SHIPPING (a2e71e5).** Drill's decisive finding: the wall is near-miss content-neighbor competition, NOT fan-out/entropy (kNN on same features = same band, rules out under-param); under RANK metrics Hits@10 recovers to +0.26/+0.18. Cell `exp_schema_relation_hitsatk_mrr_reframe_v1` authoring+smoke now (bands: Hits@10>=0.20 & MRR>=0.15, >=2rel x >=2enc, V>=300). ON SMOKE-CLEAR: orchestrator fires canonical FULL -- THIS is the prize's live shot at a broad win. If MIDDLE: sparse-coding set-recovery next. CLS NOT the route (drill flagged weak/mixed).
2. **TOP brain-component build FIRED (a16c02f, hdi_exp_dev): `exp_pfc_gate_cfrpe_trained_v1`** -- Go/NoGo gate trained by cfrpe RPE (drill ranked #1+#2; NOT CLS). Targets CONTROL HARD_FAIL (0.642 headroom). On SMOKE-clear: orchestrator fires the canonical FULL. This is the "missing training half of the BG gate" thesis (PBWM/Frank): static additive bias was near ceiling; RPE-trained Go/NoGo should close >=25% of headroom (HARD-PASS) or prove the weakness is architectural (HARD-FAIL <=0.05 lift).
3. VET IN FLIGHT (hdi_skunkworks af274c4): integration HARD_PASS (saturation scrutiny -- the 1.0 + near-chance correlation_cone 0.51) + control HARD_FAIL confirm. Hard-comprehension FULL still BLOCKED on PROT-022 (one-line testbed fix, see HAZARDS).
4. Continue: fire staged brain-component builds; iterate the open capabilities (generalization broad, control strong, integration hard-regime).

## PRINCIPLES BANKED THIS SESSION (memory files + MEMORY.md)
- **no-smoke** (rate good/mediocre/bad, deflate OUR claims) -- caught ~4 over-claims via VETs this session (comprehension-trivial, prize-narrow, reasoning-mis-diagnosed, generation-rounded).
- **prior-work-informs-NOT-constrains** (scour to not rediscover, but test brain-aligned OR high-prob-superior new mechanisms).
- **research-every-finding (middle/negative) for MECHANISM + ENVELOPE-PUSH** (a tier is not the finding; a negative is a mechanism-clue + a direction).
- **BRAIN-COMPONENT-DRIVEN development** (USER standing thrust: inventory->required->multiples->build-missing). [[project_brain_component_driven_development_thrust...]].
- honest capability framings [[reference_honest_capability_framings_generation_frame_known_memory_linear_memory_law_2026-07-05]].

## DISCIPLINE / HAZARDS (obey)
- Verify off-disk before claiming (Fix#28); the audits scoped down several headlines -- LEAD with VET-surviving scoped numbers.
- Canonical run = remote queue; SMOKE-only-local; GPU FULL via orchestrator; SCP untracked caches explicitly.
- **GIT-INDEX RACE** flagged: running many concurrent agents caused a pathspec-less commit to sweep a concurrent agent's staged Store atoms (no data loss). Mitigation: `git commit <explicit-pathspec>`, avoid `git add -A`, tighten concurrency. I over-extended (5-6 concurrent) -- be more measured.
- Re-encode of substrate HELD (not needed; not fired).
- **INFRA BUG PROT-022 -- FIXED 2026-07-05 (commit 8c70202df, hdi_testbed, 7/7 both-branch verify).** Was: `check_declared_referents` in `tools/queue_add.py` (line 425/429) keyed the ssh-referent-check only on queue_name, so on-remote (`HDLAB_QUEUE_ADD_ON_REMOTE=1`) it ssh-to-self loopbacked -> false "referent MISSING" -> exit 10, blocking ALL referent-declaring remote cells. Fix: `check_over_ssh = is_remote and not on_remote`; on-remote checks local FS `(REPO/ref).exists()` directly; on-local ssh path unchanged. **BUT NOT YET DEPLOYED TO THE REMOTE (2026-07-05): the remote `C:/dev/hd-instrument/tools/queue_add.py` is SCP-only and does NOT pull origin -- it still runs the STALE buggy gate.** So hard-comprehension FULL (the ONLY of the 3 cells declaring a KB_REFERENT) is STILL BLOCKED (exit 10). cfrpe + reframe FULLs dispatched FINE (zero referents -> gate no-op). **REMEDY NEEDS USER AUTH: deploy fixed queue_add.py to marsh@home:C:/dev/hd-instrument/tools/ -- orchestrator's SCP + git-push were harness-AUTO-DENIED (consequential remote-persistence gate). Surface on USER return; non-critical (hard-comprehension is a confirmation, smoke already holds). Do NOT bypass the gate / no --allow-missing-referent.**
- Deferred tidy: director_plan.json refresh (stale pre-reframe C0-C6); capability_scorecard revision; a97751df light-VET; auto-refill daemon needs USER elevated cmd; n11 teacher-free revival; memory deg8+ canonical FULL (data-locality); push accumulated commits (durability sweep ad38e6 ran).
