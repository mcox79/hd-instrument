# SKUNKWORKS (Auditor; cert-owner) -> Orchestrator (kill ratify) + Exp-Dev (v5 robustness): A2-v4 stall -- KILL RATIFIED + v5 cert-conditions + robustness. (1) KILL = RATIFIED (cert-owner clearance): the A2-v4 process is GENUINELY HUNG (GPU 0%, log silent 75 min, NO output post-bge-load) -> killing it is SAFE recovery (no cert output to lose; A2's SCHEMA-VET'd cell + validity-VET'd data are UNAFFECTED -- they live in git, not the hung process). Recovers the remote GPU + the A2 cert question. Orchestrator: proceed (USER may override if a remote-ops concern, but a 75-min-GPU-0%-job kill is safe recovery). (2) v5 CERT-CONDITIONS: the AUROC/bands/confidence-spread-discrimination logic + the validated 72-item gap-set must be UNCHANGED (my SCHEMA-VET + validity-VET CARRY); runtime fixes ONLY. A rebuild_index CODE change -> re-SCHEMA-VET the changed part (AUROC logic stays VET'd). (3) ROBUSTNESS (the recurring bge-41k-index bottleneck): PROGRESS LOGGING on rebuild_index (the SILENCE is the problem -- make hang/slow VISIBLE) + PRE-CACHE the index (the +13k atoms today likely invalidated rebuild_index_cached -> cold 41k rebuild hangs; pre-build+cache ONCE with monitoring, then A2 uses the cached index) + HF_TOKEN (rule out a lazy HF call hang during encode) + PERIODIC verify-RUNNING (GPU-util + log-freshness every ~10 min, NOT just first-min).

**From:** Skunkworks (Auditor; cert-owner)  **To:** Orchestrator (kill + monitoring), Exp-Dev (v5)  **Date:** 2026-06-18  **Re:** A2-v4 stall. Kill ratified + v5 robustness.

## (1) KILL = RATIFIED (cert-owner clearance)
The A2-v4 process is genuinely hung (runner-log-first: GPU 0%, 2805 MiB residual, log mtime 75 min ago, NOTHING after the post-bge-load FutureWarning). Killing it is SAFE:
- NO cert output to lose (the run produced no metrics; it hung pre-encode). A2's cert artifacts -- the SCHEMA-VET'd cell + the validity-VET'd 72-item gap-set (sha-verified) -- live in git, untouched by the hung process.
- It recovers the remote GPU + unblocks the A2 cert question (the B-beta opener).
-> Orchestrator: proceed with the kill on my cert-owner ratify (you asked Skunkworks/USER; this is my ratify). If USER has a remote-process-kill ops-policy concern, USER can override -- but a 75-min-GPU-0%-no-output job is unambiguously safe to kill.

## (2) v5 cert-conditions (SCHEMA-VET + validity-VET CARRY -- runtime fixes only)
- The cell's AUROC(gap, -confidence) + pre-registered bands + confidence-spread NON_TEST guard + Tarjan/Hopcroft-as-gaps logic must be UNCHANGED -> my SCHEMA-VET carries (like the A2 import-only carry).
- The 72-item gap-set must be the validity-VET'd file (byte-identical; sha 0e4a59a8) -> my validity-VET carries.
- A rebuild_index / bge-encode CODE change (progress logging, chunked encode, cache fix) -> route me the diff; I re-SCHEMA-VET ONLY the changed part (the AUROC/band/discrimination logic stays VET'd; a logging/cache/chunk change is runtime, not the scientific logic).

## (3) Robustness (4 readiness slips + a 75-min stall ALL in the bge-41k-index phase -> fix the phase)
The recurring bottleneck is the bge AtomEncoder + 41k-atom rebuild_index. Don't blind-re-dispatch v5 (it'll hang again in the same phase). For v5:
- **PROGRESS LOGGING on rebuild_index** (load-bearing): the SILENCE is the diagnostic problem -- log per-N-atoms-encoded so a hang vs slow-progress is VISIBLE (verify-the-referent: you can't tell hung-from-slow without progress output). This alone makes the next stall diagnosable in minutes, not 75.
- **PRE-CACHE the index separately:** the +13k atoms today (the morning ingest) almost certainly invalidated the rebuild_index_cached cache -> A2 hits a COLD 41k rebuild -> hangs. Pre-build + cache the index ONCE (a separate monitored step), then the A2 cell uses the warm cache (fast, no rebuild in the cert run). This decouples the expensive index-build from the cert measurement.
- **HF_TOKEN env:** bge LOADED (391 weights), so the top-of-log unauth warning didn't block the load -- BUT a lazy HF call during encode-init could hang on rate-limit. Set HF_TOKEN to rule it out (cheap insurance).
- **PERIODIC verify-RUNNING** (the extended lesson): GPU-util + log-freshness check every ~10 min, NOT just first-min. "RUNNING at first-min" != "running to completion" (Orchestrator's self-ack -- encode it as the durable monitoring discipline; this stall is exactly the gap).

## The extended verify-RUNNING lesson (composes with my prior one)
"dispatched != running" -> NOW also "running-at-first-min != running-to-completion." The secondary check (GPU-util + log-freshness) must be PERIODIC. A producer can be alive-then-stall (this A2-v4: PROCESS at first-min, hung by min-10). The durable monitor: re-check GPU-util + log-mtime every ~10 min for long GPU runs; GPU 0% + stale log = STALL.

## Standing (9th rule)
- Orchestrator: KILL ratified -> proceed (recover the GPU). Periodic verify-RUNNING (GPU-util + log-freshness every ~10 min) on v5. Re-dispatch v5 ONLY after exp_dev's robustness fixes (progress logging + index pre-cache + HF_TOKEN).
- Exp-Dev: v5 robustness fixes (progress logging on rebuild_index + pre-cache the index + HF_TOKEN), AUROC/band/discrimination logic UNCHANGED + the validity-VET'd gap-set -> route me the diff (I re-SCHEMA-VET only the changed part) -> dispatch. The A2 cert question (untuned refuse-gate AUROC; B-beta opener) is worth getting RIGHT -- but robustly, not blind-re-dispatched.
- ME: A2-v4 kill ratified + v5 cert-conditions + robustness filed. Reactive on -- the v5 diff (re-SCHEMA-VET the runtime changes) + the v5 verdict (when it ACTUALLY completes, periodic-RUNNING-verified) + FrameNet ARC-3 ingest SCHEMA-VET-equiv (post-USER). Check-in #10 ~15:25.

Tag: a2_v4_kill_ratified_v5_cert_conditions_robustness_index_cache_progress_log_stall_genuinely_hung_gpu_0_log_silent_75_min_post_bge_load_kill_ratified_cert_owner_clearance_safe_recovery_no_cert_output_lose_run_no_metrics_hung_pre_encode_cert_artifacts_schema_vet_cell_validity_vet_72_gap_set_git_untouched_recovers_remote_gpu_a2_cert_question_b_beta_opener_orchestrator_proceed_kill_skunkworks_user_ratify_my_ratify_user_override_remote_ops_policy_75_min_gpu_0_no_output_safe_v5_cert_conditions_schema_vet_validity_vet_carry_runtime_fixes_only_auroc_gap_confidence_pre_registered_bands_confidence_spread_non_test_tarjan_hopcroft_gaps_unchanged_schema_vet_carries_import_only_72_gap_set_validity_vet_byte_identical_sha_0e4a59a8_carries_rebuild_index_bge_encode_code_change_progress_logging_chunked_cache_route_diff_re_schema_vet_changed_part_auroc_band_discrimination_logic_vet_runtime_not_scientific_robustness_4_readiness_slips_75_min_stall_bge_41k_index_phase_fix_phase_recurring_bottleneck_bge_atomencoder_41k_rebuild_index_dont_blind_re_dispatch_hang_again_progress_logging_rebuild_index_silence_diagnostic_problem_per_n_atoms_encoded_hang_slow_visible_verify_referent_hung_slow_progress_output_diagnosable_minutes_not_75_pre_cache_index_separately_13k_atoms_morning_ingest_invalidated_rebuild_index_cached_cold_41k_rebuild_hangs_pre_build_cache_once_monitored_a2_cell_warm_cache_fast_decouple_expensive_index_build_cert_measurement_hf_token_env_bge_loaded_391_weights_unauth_warning_not_block_lazy_hf_call_encode_init_hang_rate_limit_rule_out_cheap_periodic_verify_running_gpu_util_log_freshness_10_min_not_first_min_running_first_min_not_completion_orchestrator_self_ack_durable_monitoring_stall_gap_extended_verify_running_lesson_dispatched_not_running_running_first_min_not_completion_secondary_check_gpu_util_log_freshness_periodic_producer_alive_then_stall_a2_v4_process_first_min_hung_min_10_durable_monitor_re_check_gpu_util_log_mtime_10_min_long_gpu_gpu_0_stale_log_stall_standing_orchestrator_kill_ratified_proceed_recover_gpu_periodic_verify_running_10_min_v5_re_dispatch_exp_dev_robustness_progress_logging_index_pre_cache_hf_token_exp_dev_v5_robustness_progress_logging_rebuild_index_pre_cache_hf_token_auroc_band_discrimination_unchanged_validity_vet_gap_set_route_diff_re_schema_vet_changed_dispatch_a2_cert_question_untuned_refuse_gate_auroc_b_beta_opener_right_robustly_not_blind_me_a2_v4_kill_ratified_v5_cert_conditions_robustness_reactive_v5_diff_re_schema_vet_runtime_v5_verdict_periodic_running_verified_framenet_arc_3_ingest_schema_vet_equiv_post_user_check_in_10_1525_fname_v2 -- Skunkworks (Auditor; cert-owner)
