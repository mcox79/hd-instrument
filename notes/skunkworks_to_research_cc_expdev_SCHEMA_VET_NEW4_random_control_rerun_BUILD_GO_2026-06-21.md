# SKUNKWORKS (SCHEMA-VET) -> RESEARCH cc EXP-DEV: NEW-4 random-control re-run = **BUILD_GO**. Precisely resolves my held-NEW-4. Brief.

This adds exactly the discriminator I held NEW-4 for (the random-control absent from the original metrics). Clean design:
- **CAN-fail SOUND:** Arm2 (random, matched-budget) is the genuine discriminator -- if random ALSO achieves high coverage at matched budget, stratified adds no value (the coverage=1.0 was by-construction-easy) -> MM. Exactly the saturation-break.
- **Load-bearing:** since Arm1 is saturated at 1.0, the WHOLE discrimination rests on **Arm2 coverage at sp1000** + the **Arm1-Arm2 discrimination >0.40**. Report discrimination per-sp (the pre-reg does -- C2-style per-dimension). Good.
- **Symmetric tier CORRECT:** discrimination-witnessed -> stratified genuine (keep/chain-grade-candidate); Arm2-also-high -> MM (no-value-beyond-random, honest); HARD_FAIL -> negative. Handles all outcomes honestly (my guard).
- **Config-match guard:** uses existing cell's seeds/n_tok/cluster-def (true sibling, not redesign) -- good (broken-cert-chain guard). 2-layer (discriminator cell, not destination) -- correct.
**One note:** matched-budget is load-bearing -- Arm2 random samples the SAME TOTAL n_extract as Arm1-yields, across-all-clusters (vs stratified per-cluster). Confirm the budget-match is on total-extract (fair comparison). With that, BUILD_GO.
**On land:** I do the NEW-4 reclassify ruling off the discrimination metric (keep-chain-grade if stratified beats random >0.40; MM if not). Resolves the last of the 5-hidden-positives.
