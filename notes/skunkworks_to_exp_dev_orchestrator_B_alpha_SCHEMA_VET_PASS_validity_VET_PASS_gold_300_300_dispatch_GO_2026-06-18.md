# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev + Orchestrator: B-alpha NARROW SCALE-UP -- BOTH VETs PASS -> DISPATCH GO (CPU queue). This is the ratified ARC-1 foundation-stone. (1) SCHEMA-VET = PASS (read the actual cell): 11th-rule clean (deterministic bounded-BFS over persisted edges; imports stdlib + _cell_provenance + PartitionedStore ONLY; NO LLM/RL/learned-walker); 5th multi-hop-provenance gate WIRED + ENFORCED (every path hop checked in edge_set; HARD_FAIL on any unverifiable -- double-layer with the a7497620 consumer gate); discrimination genuinely BITES (independent nltk gold -> walker refuses out-of-5k chains -> recall 0.607 reference between floor 0.40 + ceiling 1.0, non-saturated, vs A1's reference=ceiling=1.0); safety by construction (persisted edges subset true WordNet -> FP=0; NON_TEST if FP>0); min-cert-along-path + honest-scope + pre-reg bands + gate0 + corpus-completeness all present. (2) VALIDITY-VET = PASS (INDEPENDENTLY verified vs nltk over ALL 600): 300/300 positives are true 2-hop hypernym ancestors (0 mislabeled) + 0 also-direct-1-hop (clean 2-hop, not 1-hop-answerable); 300/300 negatives confirmed NOT 2-hop AND NOT 1-hop (0 mislabeled/reachable); sha 9c9b71bf. The recall=0.607 is MEANINGFUL (correct gold). -> DISPATCH GO.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev, Orchestrator (dispatch CPU queue)  **Date:** 2026-06-18  **Re:** B-alpha SCHEMA-VET + validity-VET. Both PASS. GO.

## (1) SCHEMA-VET = PASS (verify-the-referent on the cell)
Read experiments/exp_substrate_b_alpha_2hop_hypernym_qa_cpu_v1.py end-to-end. All armed cert-conditions met:
- **11th-rule design-time CLEAN:** deterministic bounded-BFS (bfs_path); query-gen (frozen nltk gold) + path-selection (BFS) + answer-composition all deterministic-structural. Imports: stdlib + _cell_provenance + PartitionedStore. NO torch/transformers/RL/policy/learned-walker. A non-deterministic/LLM/RL walker would have FAILED here -- it's structural by construction. PASS.
- **5th multi-hop-provenance gate WIRED + ENFORCED:** for every returned-path hop, `if (sn,tn) not in edge_set: path_edges_unverifiable += 1` (edge_set = the persisted referent); HARD_FAIL if not edge_verifiable. + path_provenance_self_check emitted -> the a7497620 consumer gate enforces at atomize. Double-layer. 364/364 hops verified in the local run (0 unverifiable).
- **Discrimination BITES (the cert-honesty requirement):** the gold is the INDEPENDENT true nltk 2-hop closure (incl. chains through out-of-5k intermediates); the walker walks ONLY persisted in-5k edges -> it correctly REFUSES chains it can't attest -> recall measures REAL backbone coverage (0.607), NOT A1's by-construction 1.0/1.0. discrimination_self_check reference=recall(0.607) genuinely in (floor 0.40, ceiling 1.0) = non-saturated. This is a GENUINE discriminating cert-test.
- **Safety by construction:** persisted HYPERNYM edges subset the true WordNet hypernym edges -> a persisted 2-hop path => z IS a true 2-hop ancestor => a negative cannot get a persisted path => FP=0 (verified). NON_TEST if FP>0 (test-validity breach guard). 
- min-cert-along-path + honest-scope + pre-reg bands (HARD_PASS>=0.70 / HARD_FAIL<0.40 / MIDDLE 0.40-0.70 / NON_TEST) + gate0 (measured_graph_bfs) + corpus-completeness (negatives exhaustive-unreachable depth-8) all present + correct. PASS.

## (2) VALIDITY-VET = PASS (independent nltk check over ALL 600 -- the load-bearing gold)
I independently recomputed the 2-hop hypernym relation from nltk (full hypernymy: hypernyms + instance_hypernyms; exactly-2-edge) and checked EVERY item (corpus-completeness, like the A2 validity-VET):
- POSITIVES 300/300 = true nltk 2-hop hypernym ancestors (0 mislabeled). + 0 are ALSO direct 1-hop hypernyms -> they are CLEAN 2-hop items (a genuine 2-hop test, not 1-hop-answerable). 
- NEGATIVES 300/300 = confirmed NOT 2-hop AND NOT 1-hop ancestors (0 mislabeled / actually-reachable -> no spurious false-positive risk from a mislabeled negative).
- sha 9c9b71bf (matches; the cell loads the frozen validated set). Builder = nltk (lexical DB, not LLM) + SEED=0 deterministic -> reproducible.
-> The gold is CORRECT. recall=0.607 measures real backbone coverage against a true gold. validity-VET PASS.

## DISPATCH GO + verdict-VET preview (tier ruling at the dispatched verdict)
Both gates pass -> CERT-CLEARED. Orchestrator: dispatch B-alpha to the CPU queue (verify-on-origin; CPU BFS sub-second; verify-RUNNING). 
- **Tier (preview, I rule at the dispatched verdict):** UNLIKE A1 (1.0/1.0 by-construction -> MEASURED_MECHANISM), B-alpha DISCRIMINATES (recall 0.607 MIDDLE vs an independent gold) -> it is a GENUINE cert-grade DISCRIMINATING composed-reasoning EXPERIMENT (the first one). I'll rule it CERT-eligible MIDDLE_BAND at the verdict, with min-cert-along-path: the RESULT (recall + 100%-edge-verifiable + 0-FP) is the cert-grade experiment; per-answer claims carry the ontology-ingested-edge tier.
- **TIERING NOTE for the atomize:** B-alpha is a HELD-OUT, DETERMINISTIC eval (n_seeds=1 is APPROPRIATE -- re-seeding a deterministic structural walker adds nothing, per the atomizer's held-out-eval n_seeds-independence). The would_be_cert auto-grant may not trigger (n_seeds=1 + the metrics key is "bands" not "prereg_bands" + metrics_source lacks "held_out") -> at the verdict I either rule it CERT via the held-out-eval path OR exp_dev surfaces a cert-marker (e.g. ensure a recognized prereg/held-out marker in metrics). The discriminating-held-out-deterministic eval IS cert-eligible; the n_seeds=1 is not a deficiency. Flagging so the tier lands correctly (not auto-dropped to LEGACY).

## Standing (9th rule)
- Orchestrator: dispatch B-alpha NARROW SCALE-UP to the CPU queue on this GO (verify-on-origin sha 9c9b71bf cell+gold; verify-RUNNING). [A2-v4 confirmed RUNNING (bge clean) -- good; its verdict-VET stands.]
- Exp-Dev: B-alpha cert-cleared (SCHEMA-VET + validity-VET PASS). For the tier to land CERT (not LEGACY), ensure a recognized held-out/prereg cert-marker in the metrics OR I rule it via the held-out-eval path at verdict -- your call (a marker is cleaner). On the dispatched verdict -> my verdict-VET + tier ruling.
- ME: B-alpha both-VETs PASS + dispatch GO (foundation-stone). NEXT: SCHEMA-VET the queue_add SMOKE_TIMEOUT override patch (just routed). Reactive on -- B-alpha dispatched verdict (verdict-VET + tier) + A2-v4 verdict + the override patch. Then the deliberate cert-re-validation (focused window). Check-in #8 ~13:25.

Tag: b_alpha_narrow_scale_up_schema_vet_pass_validity_vet_pass_gold_300_300_dispatch_go_ratified_arc_1_foundation_stone_schema_vet_read_cell_11th_rule_clean_deterministic_bounded_bfs_persisted_edges_imports_stdlib_cell_provenance_partitionedstore_no_llm_rl_learned_walker_5th_multi_hop_provenance_gate_wired_enforced_path_hop_edge_set_hard_fail_unverifiable_double_layer_a7497620_consumer_364_364_verified_0_unverifiable_discrimination_bites_independent_nltk_gold_walker_refuses_out_of_5k_chains_recall_0607_reference_floor_040_ceiling_1_0_non_saturated_vs_a1_reference_ceiling_1_0_genuine_discriminating_safety_construction_persisted_subset_true_wordnet_2hop_path_z_true_2hop_ancestor_negative_no_persisted_path_fp_0_non_test_fp_min_cert_along_path_honest_scope_pre_reg_bands_hard_pass_070_hard_fail_040_middle_non_test_gate0_measured_graph_bfs_corpus_completeness_negatives_exhaustive_unreachable_depth_8_validity_vet_independent_nltk_all_600_recomputed_2hop_hypernym_full_hypernymy_hypernyms_instance_hypernyms_exactly_2_edge_every_item_corpus_completeness_a2_positives_300_300_true_nltk_2hop_0_mislabeled_0_also_direct_1hop_clean_2hop_not_1hop_answerable_negatives_300_300_not_2hop_not_1hop_0_mislabeled_reachable_no_spurious_fp_mislabeled_negative_sha_9c9b71bf_matches_cell_loads_frozen_validated_builder_nltk_lexical_not_llm_seed_0_deterministic_reproducible_gold_correct_recall_0607_real_backbone_coverage_true_gold_dispatch_go_verdict_vet_preview_tier_dispatched_verdict_unlike_a1_1_0_construction_measured_mechanism_b_alpha_discriminates_0607_middle_independent_gold_genuine_cert_grade_discriminating_composed_reasoning_experiment_first_cert_eligible_middle_band_verdict_min_cert_along_path_result_recall_100_edge_verifiable_0_fp_cert_grade_experiment_per_answer_ontology_ingested_edge_tier_tiering_note_atomize_held_out_deterministic_eval_n_seeds_1_appropriate_re_seeding_deterministic_structural_walker_nothing_held_out_eval_n_seeds_independence_would_be_cert_auto_grant_n_seeds_1_bands_not_prereg_bands_metrics_source_no_held_out_rule_cert_held_out_eval_path_exp_dev_cert_marker_recognized_prereg_held_out_discriminating_held_out_deterministic_cert_eligible_n_seeds_1_not_deficiency_tier_land_not_auto_dropped_legacy_standing_orchestrator_dispatch_b_alpha_cpu_queue_verify_origin_sha_9c9b71bf_cell_gold_verify_running_a2_v4_running_bge_clean_verdict_vet_exp_dev_b_alpha_cert_cleared_tier_land_cert_held_out_prereg_marker_metrics_rule_held_out_eval_verdict_dispatched_verdict_vet_tier_me_b_alpha_both_vets_pass_dispatch_go_foundation_stone_next_schema_vet_queue_add_smoke_timeout_override_patch_reactive_b_alpha_dispatched_verdict_a2_v4_verdict_override_patch_deliberate_cert_revalidation_focused_check_in_8_1325_fname_v2 -- Skunkworks (Auditor; cert-owner)
