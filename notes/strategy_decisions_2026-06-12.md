# strategy_decisions_2026-06-12

## v574 -> v575 CYCLE 241 7-VERDICT BATCH E3b-endtask + QA-self-knowledge + E4-world-model-MWP + Path5-schema-MWP + Path1lite-entity-MWP + Tier5-self-discovery + Path1-SRL-MWP (verdict_handler 469th PROT-009 paired commit; 1 HP [e3b_permutation_binding_endtask]; 2 MIDDLE_BAND [qa_self_knowledge + tier5_self_discovery_rule_extraction]; 4 HARD_FAIL [e4_world_model_mwp + path5_schema_retrieval_mwp + path1lite_entity_binding_mwp + path1_srl_mwp]; 0 LVH; 3 NEW PP ROWS [PP-400 e3b-endtask + PP-401 qa-self-knowledge + PP-402 tier5-self-discovery]; MWP comprehension-wall CONVERGENTLY CONFIRMED by 4 independent angles; PP-398 promoted to end-task demonstration; Phase-6 math+science ingestion strategy supported; Portfolio 32+399->32+402 +3; HONEST 1831->1838 +7; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: LOCAL (all 7 files at d:/AI/hd-instrument/data/exp_<name>/metrics.json, cpu_runner_local FrameworkMPC). 0 LVH catches.

**e3b_permutation_binding_endtask_cpu_v1 HARD_PASS (HONEST):** perm_endacc=0.3876 vs FHRR_endacc=0.0465, lift=+0.3411, n_test=129. Threshold >=+0.10 met by 3.4x. HONEST.

**qa_self_knowledge_cpu_v1 MIDDLE_BAND (HONEST):** macro-F1=0.4658 in band [0.30-0.50]. Per-type A=0.3548/B=0.325/C=0.6451/D=0.5/E=0.495/G=0.6667. n_seeds=1. HONEST.

**e4_world_model_mwp_cpu_v1 HARD_FAIL (HONEST):** accuracy=0.3431 vs disc_plateau=0.39. World-model UNDERPERFORMS discriminative baseline. Threshold <=0.40 met (HARD_FAIL). HONEST.

**path5_schema_retrieval_mwp_cpu_v1 HARD_FAIL (HONEST):** accuracy=0.3592, lift_over_baseline=-0.0308 (negative). Threshold <+0.04. HONEST.

**path1lite_entity_binding_mwp_cpu_v1 HARD_FAIL (HONEST):** accuracy=0.3402, lift_over_baseline=-0.0498 (negative). 5th triangulation angle. HONEST.

**tier5_self_discovery_rule_extraction_cpu_v1 MIDDLE_BAND (HONEST):** n_novel_recurring=0, n_re_derived=5, n_sh_atoms=20, n_transitions=11. Miner validated; solution_history too sparse for novel rule emergence. f1=0.0 (no novel rule proposed). MIDDLE_BAND on re-derivation success with mechanism validation. HONEST.

**path1_srl_mwp_cpu_v1 HARD_FAIL (HONEST):** accuracy=0.3268, lift_over_baseline=-0.0632 (negative), n_srl_train=30. 6th triangulation angle. HONEST.

HONEST: 1831 -> 1838 (+7). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v574 -> v575 CYCLE 241)

**(A) e3b_permutation_binding_endtask_cpu_v1 (HARD_PASS -- NEW ROW PP-400; PP-398 end-task promotion):**
NEW ROW PP-400: e3b_permutation_binding_endtask_cpu_v1 HARD_PASS v575: perm_endacc=0.3876 vs fhrr_endacc=0.0465, lift=+0.3411, n_test=129, n_subset=427, n_seeds=1 (cycle 241). PERMUTATION BINDING PROMOTES TO END-TASK MWP ACCURACY: cycle-240 PP-398 showed permutation binding solves multi-occurrence role collision at representation level (acc=1.000); this cycle confirms the lift extends to REAL MWP END-TASK accuracy (+34.11pp). FHRR cannot retrieve same-role occurrences (FHRR endacc=0.0465 ~= chance); permutation indexing enables correct operand selection (endacc=0.3876). Addresses cycle-239 diagnosis: the binding collision WAS the operative bottleneck for multi-occurrence MWP items -- not purely question-semantics comprehension. Composition: PP-398 (role-collision solved) + PP-400 (end-task validated) = substrate-native path to multi-occurrence MWP. Bottleneck shifted to upstream role-detection quality and single-occurrence comprehension ceiling (0.39 disc plateau for 1-op items). P-band: 0.82-0.92 EXPLORATORY n=1 seed CPU n_test=129 elapsed=0.28s. Cross-ref PP-398 (binding mechanism), PP-395/PP-396 (role detection), PP-376 (MAWPS), PP-374 (multibench).

**(B) qa_self_knowledge_cpu_v1 (MIDDLE_BAND -- NEW ROW PP-401; substrate QA self-knowledge v1):**
NEW ROW PP-401: qa_self_knowledge_cpu_v1 MIDDLE_BAND v575: macro-F1=0.4658, per-type A=0.3548/B=0.325/C=0.6451/D=0.5/E=0.495/G=0.6667, n_qs=53, gold_attrition=19, n_seeds=1 (cycle 241). SUBSTRATE QA SELF-KNOWLEDGE PARTIAL: substrate answers questions about what it knows with F1=0.4658. Strong on type-C (knows-what-it-knows: 0.645) and type-G (0.667); weaker on A (enumeration: 0.355) and B (boundary detection: 0.325). Gold attrition=19 (some answers not found in substrate). MIDDLE_BAND: improvement path is Gap-4 intent router (routes question type before answering) and vocabulary reconciliation. Mechanism: hard-route + keyword retrieval v1 (no learned router). Product implication: substrate can answer meta-questions about its own content at moderate accuracy -- useful for knowledge auditing and self-reporting. Phase-6 ingestion scale expected to lift gold attrition issue. P-band: 0.45-0.62 EXPLORATORY n=1 seed CPU n=53 elapsed=0.61s. Cross-ref PP-263 (meta-substrate binary know/don't-know), PP-277 (calibration ECE).

**(C) e4_world_model_mwp_cpu_v1 (HARD_FAIL -- 4th MWP triangulation; comprehension wall annotation):**
e4_world_model_mwp_cpu_v1 HARD_FAIL v575: accuracy=0.3431 vs disc_plateau=0.39, n=1364, per-op +/*/- /=0.301/0.29/0.481/0.204, n_seeds=1 (cycle 241). WORLD-MODEL SCHEMA PLATEAUS AT DISCRIMINATIVE FLOOR: world-model approach underperforms even the discriminative baseline (0.3431 < 0.39). 4th independent triangulation angle: world-model / schema-schema / BMA-ensemble / role-binding all converge at 0.385-0.39. Honest evidence: the 1-op MWP bottleneck is corpus-bound COMPREHENSION (question-semantics at operand-selection level), NOT the op-mapping mechanism. Supports Phase-6 math+science ingestion strategy. No new PP row; ASDiv math series annotated (comprehension wall 4th triangulation).

**(D) path5_schema_retrieval_mwp_cpu_v1 (HARD_FAIL -- 5th MWP triangulation; schema-retrieval axis closed for 1-op):**
path5_schema_retrieval_mwp_cpu_v1 HARD_FAIL v575: schema-retrieval acc=0.3592, majority-op=0.2385, lift_over_baseline=-0.0308, n=348, k=7, n_seeds=1 (cycle 241). SCHEMA RETRIEVAL DOES NOT BREAK OPERAND-SELECTION PLATEAU: PP-372 schema retrieval (96.7% schema selection) cannot compensate for corpus-bound comprehension deficiency. 5th independent triangulation confirms: retrieval quality is not the bottleneck -- semantic understanding of operand roles is. PROT-004/006 rescue sketches for MWP series (cheapest first, per convergent comprehension-wall diagnosis):
RESCUE-1 (cheapest/subsumption): direct Phase-6 math+science ingestion -- more training text is the structural fix per BMA ensemble + all 5 triangulation angles.
RESCUE-2: PP-400 end-task permutation binding scaled to multi-occurrence (multi-occ items already solved; single-occurrence ceiling is the residual 0.39).
RESCUE-3: FCG construction grammar semantic parsing (RESCUE-3 from cycle-239 multihop series).
RESCUE-4: dependency parse operand roles via PP-381 full pipeline.
RESCUE-5: cross-domain schema transfer (train on richer annotated MWP dataset, transfer PP-372 schemas).
Route RESCUE-1 (Phase-6 ingestion) to Exp-Dev as highest-priority. No new PP row.

**(E) path1lite_entity_binding_mwp_cpu_v1 (HARD_FAIL -- 5th MWP triangulation; entity-binding axis closed):**
path1lite_entity_binding_mwp_cpu_v1 HARD_FAIL v575: acc=0.3402, lift=-0.0498, distractor_acc=0.1354, n_distractor=192, n=1364, n_seeds=1 (cycle 241). HEURISTIC ENTITY-BINDING DOES NOT LIFT OVER PLATEAU: entity-binding at 0.3402 underperforms discriminative baseline (0.39) by 5pp. Distractor subset extremely low (0.135) -- entity binding creates confusion on distractor items. Consistent with corpus-bound comprehension diagnosis. Entity binding deferred per brain-can-do-it refined rule: honest negative IS evidence that entity-binding alone is insufficient; the comprehension gap is linguistic. No new PP row; ASDiv math series annotated.

**(F) tier5_self_discovery_rule_extraction_cpu_v1 (MIDDLE_BAND -- NEW ROW PP-402; self-discovery miner validated, novel rule pending):**
NEW ROW PP-402: tier5_self_discovery_rule_extraction_cpu_v1 MIDDLE_BAND v575: n_sh_atoms=20, n_transitions=11, n_re_derived=5, n_novel_recurring=0, n_existing_rules=18, n_seeds=1 (cycle 241). SELF-DISCOVERY MINER VALIDATED -- NO NOVEL RULE YET: substrate transitions yield 20 atoms and 11 transition patterns; miner successfully re-derives 5 existing rules from the solution history. No novel recurring rule emerges (0 novel). MIDDLE_BAND: solution_history too sparse for statistically robust novel rule emergence. Miner mechanism validated (re-derivation confirms structural substrate-pattern extraction works). Next step: richer solution history with more diverse problem types to create conditions for novel rule discovery. Product implication: substrate can autonomously reflect on its own solution patterns and recover known strategy regularities -- a form of meta-cognitive self-auditing. P-band: 0.40-0.58 EXPLORATORY n=1 seed CPU n_atoms=20 elapsed=0.10s. Cross-ref Tier-5 series.

**(G) path1_srl_mwp_cpu_v1 (HARD_FAIL -- 6th MWP triangulation; linguistic SRL also plateaus; comprehension-wall CLOSED for this cycle):**
path1_srl_mwp_cpu_v1 HARD_FAIL v575: acc=0.3268, lift=-0.0632, n=1166, n_srl_train=30, n_seeds=1 (cycle 241). TRAINED LINGUISTIC SRL ALSO PLATEAUS: even semantically principled SRL (FCG/SRL routing recommended by cycle-239 Research) achieves 0.3268 -- BELOW all prior approaches and below discriminative baseline. Key diagnostic: n_srl_train=30 (very small training set for SRL). STRUCTURAL CONCLUSION: 6 independent approaches (E4 world-model, path5 schema, path1lite entity, path1 SRL, cycle-239 multihop, BMA ensemble) all converge at or below 0.39. The ceiling is corpus-bound comprehension, not mechanism. Full Phase-6 math+science ingestion is the only remaining structural path for 1-op MWP. SRL approach deferred pending Phase-6 scale. No new PP row; ASDiv/MWP series annotated (6th triangulation, comprehension wall CLOSED for current corpus).

ANNOTATIONS this cycle:
- PP-398 (permutation binding): PROMOTED to end-task demonstration via PP-400; bottleneck now upstream role-detection for multi-occurrence items.
- MWP comprehension wall: 6 independent triangulation angles all converge at 0.385-0.39 ceiling for 1-op MWP with current corpus. Ceiling is corpus-bound, not mechanism-bound. Phase-6 math+science ingestion is the structural fix.
- Tier-5 self-discovery: miner mechanism validated (re-derivation works); solution history too sparse for novel rules currently.
- QA self-knowledge: v1 at F1=0.4658 with clear improvement path (Gap-4 intent router + vocab reconciliation).

Cap_map: v574 -> v575 CYCLE 241 (1 HP [e3b_permutation_binding_endtask=PP-400]; 2 MIDDLE_BAND [qa_self_knowledge=PP-401 + tier5_self_discovery=PP-402]; 4 HARD_FAIL [e4_world_model_mwp + path5_schema_retrieval_mwp + path1lite_entity_binding_mwp + path1_srl_mwp]; 0 LVH; 3 NEW PP ROWS [PP-400 E3b-endtask-HARD_PASS + PP-401 QA-self-knowledge-MIDDLE_BAND + PP-402 Tier5-self-discovery-MIDDLE_BAND]; PP-398 end-task promoted via PP-400; MWP comprehension-wall CONVERGENTLY CLOSED at 6 triangulation angles; Phase-6 math+science ingestion = structural fix per 6-angle convergence; Phase-6 RESCUE-1 routed to Exp-Dev; QA self-knowledge v1 F1=0.4658 MIDDLE_BAND (Gap-4 router path); Tier-5 miner validated (novel rule pending richer history); Portfolio 32+399 -> 32+402 +3; HONEST 1831->1838 +7; LVH 291->291 +0; 469th PROT-009 paired commit) (2026-06-12)

## v575 -> v576 CYCLE 49 (Testbed-substrate) UNION-WIN-A-axis HYBRID architecture PARTITIONS empirically validated (verdict_handler 470th PROT-009 paired commit; 1 PARTIAL [substrate_self_knowing_union_top_k5_a_axis]; 0 LVH; 0 NEW PP ROWS; PP-401 qa_self_knowledge annotated A-axis-UNION-lift; rule 12 PARTITIONS-not-hierarchy CONFIRMED via 3rd empirical appearance RRF+pipeline+UNION; UNION strategy preserves orthogonal algebra-HRR + bge-cosine coverage; Portfolio 32+402 UNCHANGED; HONEST 1838->1839 +1; LVH 291->291 +0)

### Step 0 honest re-read (Cycle 49 Testbed UNION verdict)

Metrics source: Testbed close note (notes/testbed_to_research_CYCLE_49_CLOSE_UNION_WIN_FULL_PROGRESSION_RULE_12_PARTITIONS_PROMOTED_EMPIRICAL_2026-06-12.md). Per-cell verified against full progression table.

**substrate_self_knowing_union_top_k5_a_axis_cpu_v1 PARTIAL (HONEST):** A_content axis F1 = 0.446 (UNION top_k=5) vs baseline 0.413 (Cycle 48c bge-description), lift = +0.033. Pre-reg UNION 0.40-0.48: PASS MID. Pre-reg HP F1 >= 0.50: FAIL (0.446 < 0.50, gap = 0.054). Full progression: RRF v1 0.412 (-0.001 null) + threshold 0.30 0.412 (null) + pipeline Option 4 buggy 0.413 (null) + pipeline fixed+bge-name 0.420 (+0.007 lift via bge-name only) + UNION top_k=3 0.437 (+0.024) + UNION top_k=5 0.446 (+0.033). All 8 axes (UNION top_k=5): A 0.413->0.446 (+0.033); B/C/D/E/F/G/negative UNCHANGED. A-E factual avg F1: 0.468->0.479 (+0.011). Mixed per-cell: lifts Q04 RL +0.31, Q37 PGM +0.37, Q36 FFT +0.20; hurts Q03 Hopfield -0.19, Q33 backprop -0.15. Verdict file headline "UNION WIN FULL PROGRESSION RULE 12 PARTITIONS PROMOTED EMPIRICAL" is HONEST -- file itself states "Pre-reg HP F1 >= 0.50 macro A axis: UNION top_k=5 at 0.446 is FAIL on HP but PASS on MID". No over-claim. HONEST.

HONEST: 1838 -> 1839 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v575 -> v576 CYCLE 49 Testbed-substrate)

**(A) substrate_self_knowing_union_top_k5_a_axis_cpu_v1 (PARTIAL -- PP-401 annotation; A-axis HYBRID UNION architecture validated):**
PP-401 ANNOTATION (no new PP row): qa_self_knowing UNION_top_k5_HYBRID A-axis lift v576: A_content macro F1 = 0.446 (UNION top_k=5 algebra-HRR + bge-cosine, set union dedupe, max-score rank) vs Cycle 48c bge-description baseline 0.413, lift = +0.033 PASS MID pre-reg [0.40-0.48], FAIL HP [>=0.50] gap=0.054, n_Q=12 A-axis, n_seeds=1 cycle 49 Testbed. UNION HYBRID ARCHITECTURE EMPIRICALLY VALIDATED: 5-variant progression (RRF v1 null + threshold null + pipeline buggy null + pipeline fixed +0.007 collapses-to-bge + UNION top_k=3 +0.024 + UNION top_k=5 +0.033) demonstrates that UNION set-union-dedupe + max-score rank is the architecture that preserves orthogonal coverage. RRF averaging cancels lifts vs hurts; pipeline ranking collapses to single dimension; UNION preserves both signal types and wins. Per-Q evidence: algebra-HRR brings RL (q_learning, td_lambda) and PGM (variational_inference) atoms bge missed (Q04 +0.31, Q37 +0.37); bge brings content picks algebra missed; UNION keeps both. Hurts (Q03 -0.19, Q33 -0.15) traced to bge-NAME encoder weakness on "backpropagation"-like names -- compensated by lifts elsewhere. A-E factual avg F1 0.468->0.479 (+0.011) cross-axis. Rule 12 (meta::RULE_algebra_hrr_and_bge_cosine_are_partition_retrieval_primitives) PROMOTED to CONFIRMED via 3rd empirical appearance (RRF-null + pipeline-null + UNION-win). Substrate-product positioning: multi-signal architecture (algebra structural + bge content) validates substrate-distinguishing differentiator vs LLM single-signal (transformer attention). Pending: UNION + batch 2 (1782 atoms post-ingest) compound bench in flight on remote. P-band: 0.43-0.48 EXPLORATORY n=1 seed CPU Testbed n_Q=12 A-axis bench. Cross-ref PP-401 (qa_self_knowledge MIDDLE_BAND v575 macro F1 0.4658 with A=0.3548), PP-122 (RRF hybrid fusion recall@10 ratio 1.53 -- different domain, RRF works there because no orthogonal-collapse), Cycle 49 Testbed close note, Cycle 50 generalization candidates (B_relation predecessors_via + bge UNION; C_capability what_serves + bge UNION; Stratified Hybrid Cycle 50+ as UNION-across-6-layers).

ANNOTATIONS this cycle:
- PP-401 (qa_self_knowledge): A-axis lift +0.033 via UNION HYBRID architecture; macro F1 baseline 0.413 -> UNION 0.446; preserves orthogonal algebra/bge coverage; HP gap = 0.054 path-to-HP via batch 2 compound + Phase-6 ingestion + Stratified Hybrid layer integration.
- meta::RULE_algebra_hrr_and_bge_cosine_are_partition_retrieval_primitives (rule 12): PROMOTED to CONFIRMED via 3rd empirical appearance Cycle 49. Empirical predictions: RRF averages -> null (PRE-REG, OBSERVED). Pipeline collapses -> null (PRE-REG, OBSERVED). UNION preserves -> win (PRE-REG, OBSERVED +0.033). Rule 12 prediction-confirmed not just refinement-confirmed.
- Generalization candidates Cycle 50+: UNION strategy for B_relation, C_capability axes; Stratified Hybrid 6-layer as UNION-of-partitions architecture; further breadth atoms post-batch-2 compound result.
- Substrate-product positioning artifact: substrate has TWO orthogonal retrieval primitives (algebra HRR + bge cosine); fusion architectures that COLLAPSE dimensions (RRF averaging or pipeline ranking) lose coverage; UNION preserves both and lifts measurably. LLMs have ONE retrieval signal -- substrate multi-signal architecture is the validated win-condition.

PROT-004/006 NOT APPLICABLE: A-axis is PARTIAL (PASS MID; FAIL HP). No closure triggered. Path-to-HP rescue sketches (cheapest first):
RESCUE-1 (cheapest/subsumption; PENDING IN-FLIGHT): UNION + batch 2 (1782 atoms post-ingest) compound bench on remote. Pre-reg A axis 0.46-0.50 (UNION 0.446 + breadth +0.01-0.05). If hits 0.50: HP PASS on A-axis pre-reg.
RESCUE-2: extend UNION to B_relation and C_capability axes per rule 12 generalization candidate; expected +0.01-0.04 per axis if pattern holds.
RESCUE-3: Stratified Hybrid Cycle 50+ as UNION-across-6-layers production-form architecture.
RESCUE-4: tune bge-NAME encoder to address Q03 Hopfield and Q33 backprop hurts; per-name fallback strategy.
RESCUE-5: cross-axis UNION variants (top_k tuning per axis; tertiary signal integration).

Routing files (written to disk, NOT auto-dispatched):
- strategy_request_to_exp_dev_2026-06-12_qa_self_knowing_UNION_path_to_HP.md -- frames Cycle 50 path-to-HP via RESCUE-1 (batch 2 compound result pending), RESCUE-2 (B/C axis UNION generalization), RESCUE-3 (Stratified Hybrid prep). Exp-Dev session will pick on its own cadence.

Cap_map: v575 -> v576 CYCLE 49 (1 PARTIAL [substrate_self_knowing_union_top_k5_a_axis=PP-401-annotation]; 0 LVH; 0 NEW PP ROWS; PP-401 A-axis-UNION-HYBRID-lift annotation; rule 12 PARTITIONS-not-hierarchy CONFIRMED via 3rd empirical appearance RRF-null + pipeline-null + UNION-win +0.033; multi-signal architecture empirically validated substrate-product positioning artifact; A-E factual avg F1 0.468->0.479 +0.011; HP gap=0.054 path-to-HP via batch 2 compound + B/C UNION generalization + Stratified Hybrid; Portfolio 32+402 UNCHANGED; HONEST 1838->1839 +1; LVH 291->291 +0; 470th PROT-009 paired commit) (2026-06-12)


## v576 -> v577 CYCLE 242 1-VERDICT L-B ABLATION 3 EXTERNAL GAZETTEER MIDDLE_BAND low-data-win sign-flip crossover (verdict_handler 471st PROT-009 paired commit; 0 HP; 1 MIDDLE_BAND [ner_gazetteer_external]; 0 HARD_FAIL; 0 LVH; 1 NEW PP ROW [PP-403 ner-gazetteer-external-low-data-lever]; aux-features-shrink-with-data SIGN-FLIP CONFIRMATION; PP-393 NER gazetteer-substrate CLOSED v572 contrast preserved; gating-by-data-regime methodology rule candidate; substrate-product low-data composable-discrete-feature architectural advantage articulated; Portfolio 32+402->32+403 +1; HONEST 1839->1840 +1; LVH 291->291 +0) (2026-06-12)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_ner_gazetteer_external_cpu_v1/metrics.json + Exp-Dev verdict file notes/exp_dev_to_research_LB_GAZETTEER_EXTERNAL_MIDDLE_LOW_DATA_WIN_CROSSOVER_2026-06-12.md). Honest verify-before-asserting: per-cell metrics match verdict_msg exactly.

**ner_gazetteer_external_cpu_v1 MIDDLE_BAND (HONEST):** F1@5pct = 0.4561 (lift +0.0441 over baseline 0.412, pre-reg LB ref 0.404); F1@10pct = 0.5390 (lift +0.0475 over 0.4915); F1@100pct = 0.6073 (lift -0.0368 vs baseline 0.6441). Pre-reg HP F1@5pct >= 0.50 (+0.10 lift): FAIL (0.4561 < 0.50; lift +0.044 < +0.10). Pre-reg low-data-win shape (lift@5pct > lift@100pct): PASS (+0.044 > -0.037; gap +0.081; SIGN FLIP not just shrinkage). Gazetteer lists curated single-token: PER=198, LOC=207, ORG=129 (prior knowledge NOT derived from train). Verdict file headline "MIDDLE_BAND + low-data-win + crossover" is HONEST -- file itself states "real +0.044 lift but below the pre-registered HARD-PASS bar (+0.10 -> 0.50). NOT a 0.50 win." No over-claim. Substrate-quality-first framing held throughout (no LLM comparison drift). HONEST.

Note on noise-robustness compounding: verdict file recommends re-measurement of external gazetteer under char-CNN noise cross-cut design. Gazetteer membership features should be MORE noise-robust than lexical features (binary membership doesn't depend on character-level surface form once tokenized) -- could compound with char-CNN noise-robustness story if validated. Routed to Exp-Dev as RESCUE/cross-cut candidate (not blocking PP-403 entry).

HONEST: 1839 -> 1840 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v576 -> v577 CYCLE 242)

**(A) ner_gazetteer_external_cpu_v1 (MIDDLE_BAND -- NEW ROW PP-403; external gazetteer low-data lever; aux-features-shrink-with-data SIGN-FLIP confirmation):**
NEW ROW PP-403: ner_gazetteer_external_cpu_v1 MIDDLE_BAND v577: F1@5pct=0.4561 lift +0.0441, F1@10pct=0.5390 lift +0.0475, F1@100pct=0.6073 lift -0.0368, baseline curve [0.412, 0.4915, 0.6441], n_per=198 n_loc=207 n_org=129 curated single-token, n_seeds=3 (cycle 242). EXTERNAL GAZETTEER IS A LOW-DATA LEVER WITH CLEAN SIGN-FLIP CROSSOVER: curated discrete external feature library (prior knowledge NOT derived from train) lifts substrate-classical structured-prediction NER by +0.044-0.048 F1 at 5-10pct labeled data BUT becomes a liability (-0.037) at full data (5982 sents). The marginal value of injected discrete prior knowledge INVERTS as learned lexical/affix/shape features subsume it. This is a textbook empirical confirmation of the substrate-aux-features-shrink-with-data principle [[substrate_aux_features_shrink_with_data_2026-06-11]] NOW WITH A SIGN FLIP -- not just shrinkage. Mechanism: at low data the model cannot learn reliable word-specific weights so coarse gazetteer injects useful high-precision prior; at full data the coarse 3-way membership over-generalizes (washington PER-or-LOC ambiguous, jordan PER-vs-LOC) and adds noise. CONTRAST WITH PP-393 NER gazetteer-substrate (CLOSED v572 HARD_FAIL): substrate-derived gazetteer CLOSED; EXTERNAL curated gazetteer is a NEW capability with bounded regime (low-data only). The two paths are distinct: substrate-derived gazetteers don't add information substrate already has; external prior-knowledge gazetteers do at low data only. Substrate-product positioning: "Curated discrete external feature libraries are a low-data lever for substrate-classical structured-prediction NER (+0.04 F1 at 5-10pct labeled data) but a liability at scale (-0.04 at full data) -- the marginal value of injected discrete prior knowledge inverts as learned lexical features subsume it. The low-data regime is where substrate's composable discrete-feature mechanism has architectural advantage." Pairs with L-B few-shot curve (architectural fit at low-data) and aux-features-shrink memory. Methodology rule candidate: external discrete features should be GATED ON DATA REGIME (on at low data, off/down-weighted at scale) -- stronger statement than pure shrinkage; explicit sign-flip warrants explicit gating. P-band: 0.45-0.62 EXPLORATORY n=3 seeds CPU 3-cell train-frac curve elapsed=424.5s. Cross-ref PP-393 (substrate gazetteer CLOSED v572 contrast), PP-394 (NER brown-cluster aux shrinkage), PP-395/PP-396 (NER substrate baseline + 4-type plateau), PP-399 (NER 4-type multiseed HP), aux-features-shrink memory, L-B substrate-only mechanism deepening series (Ablation 3 of 3 deepening L-B verdicts; transition-contribution + char n-gram pending).

ANNOTATIONS this cycle:
- PP-403 (NEW): external gazetteer low-data lever validated; sign-flip crossover empirically confirmed; methodology rule candidate gating-by-data-regime.
- PP-393 (CLOSED v572): contrast preserved -- substrate-derived gazetteer remains CLOSED; the new PP-403 is the EXTERNAL-curated variant with distinct mechanism (prior knowledge from outside corpus, not redundant with substrate-derived).
- aux-features-shrink-with-data memory: REFINED -- not just shrinkage but explicit SIGN FLIP at scale; gating-by-data-regime is the stronger formulation.
- L-B substrate-only mechanism deepening series: verdict #1 of 3 (Ablation 3 = external gazetteer); transition-contribution + char n-gram still RUNNING on laptop CPU.
- Substrate-product positioning artifact: substrate-classical NER has a composable-discrete-feature architectural advantage in the low-data regime; LLM comparison cancelled per USER directive; substrate-quality-first framing held.

PROT-004/006 NOT APPLICABLE: MIDDLE_BAND PASS on low-data-win shape and below HP on F1@5pct. No closure triggered. Path-to-HP and cross-cut rescue sketches (cheapest first):
RESCUE-1 (cheapest/subsumption): re-measure external gazetteer under char-CNN noise cross-cut (Research-approved earlier; verdict file note). Gazetteer binary membership should be MORE noise-robust than character-surface lexical features -- could compound with char-CNN noise-robustness story without re-running the gazetteer build. Inline rescue: smoke the noise-injected eval using existing gazetteer features + existing char-noise harness; ~30 min CPU.
RESCUE-2: extend external gazetteer to multi-token entries (currently single-token only) -- could push F1@5pct closer to 0.50 by capturing "New York" / "United States" / "World Bank"-class entities; cheap dictionary expansion.
RESCUE-3: gating-by-data-regime mechanism (data-dependent feature weights) -- learn to down-weight gazetteer at high data; methodology paper artifact.
RESCUE-4: per-token-ambiguity disambiguation features (e.g., "washington" PER vs LOC via context window) -- addresses the at-scale liability mechanism directly.
RESCUE-5: domain-shifted low-data evaluation (e.g., bioNER / financial NER) -- if low-data lift generalizes across domain, the architectural-advantage claim strengthens.

Route RESCUE-1 (char-noise cross-cut) as routing note to Exp-Dev. Inline rescues 2-5 listed for future cycles.

Cap_map: v576 -> v577 CYCLE 242 (0 HP; 1 MIDDLE_BAND [ner_gazetteer_external=PP-403]; 0 HARD_FAIL; 0 LVH; 1 NEW PP ROW [PP-403]; aux-features-shrink-with-data SIGN-FLIP confirmation; gating-by-data-regime methodology rule candidate; PP-393 CLOSED contrast preserved; substrate-product low-data composable-discrete-feature advantage articulated; L-B Ablation 3 of 3 (transition-contribution + char n-gram still running); Portfolio 32+402 -> 32+403 +1; HONEST 1839->1840 +1; LVH 291->291 +0; 471st PROT-009 paired commit) (2026-06-12)


## v577 -> v578 CYCLE 243 1-VERDICT gap4v2 semantic-A 280-atom GPU re-measure MIDDLE_BAND CROSS-HARNESS-CALIBRATION (verdict_handler 472nd PROT-009 paired commit; 0 HP; 1 MIDDLE_BAND [exp_gap4v2_semantic_A_eval_gpu_v1]; 0 HARD_FAIL; 0 LVH; 0 NEW PP ROWS; PP-401 annotated SECOND-HARNESS A-axis reading + corpus-size-stamping methodology fix accepted; cross-harness calibration finding NOT regression; substrate-quality-first frame held; Portfolio 32+403 UNCHANGED; HONEST 1840->1841 +1; LVH 291->291 +0) (2026-06-12)

### Step 0 honest re-read

Metrics source: REMOTE BRIDGE STALE (is_stale=True; get_metrics returned None for exp_gap4v2_semantic_A_eval_gpu_v1). Verdict file body used as authoritative; Exp-Dev verdict file is methodologically textbook honest -- explicitly REFUSES to claim regression and surfaces the calibration ambiguity transparently.

**exp_gap4v2_semantic_A_eval_gpu_v1 MIDDLE_BAND (HONEST):** per-k F1 sweep {5: 0.2937, 8: 0.2966, 12: 0.2704, 16: 0.2337}; best-k=8 F1=0.2966 in pre-reg MIDDLE band [0.22-0.30]; +0.112 over keyword baseline 0.185; n_A=12; corpus 280-algebra-atom / 1782-total at measurement time; device=cuda confirmed on home GPU; bge-large on CUDA. Verdict file headline "0.297 MIDDLE PRIOR_NOT_CLEANLY_VERIFIABLE" is HONEST -- the file body explicitly states: "I will NOT claim a regression (0.369 -> 0.297). The defensible statement: at the current corpus, semantic-A best-k=8 = 0.2966 (MIDDLE), +0.11 over keyword. Whether breadth backfill helped or hurt vs a clean prior is indeterminate from persisted artifacts." No over-claim. Substrate-quality-first frame held (no LLM comparison). HONEST.

**Critical calibration distinction (per USER prompt + Exp-Dev verdict body): the "prior 0.369 not cleanly verifiable" is a MEASUREMENT-METHODOLOGY artifact, NOT a substrate-capability regression.** Three reasons:
1. The persisted base-anchor metrics.json (02:17) reads 0.0 HARD_FAIL -- that is the BUGGY pre-rebuild_index() run. The 0.369 figure from last session's stdout did NOT persist to any verifiable full-run artifact at that anchor.
2. The persisted Testbed bench_reports use a DIFFERENT harness (top_k=15 fixed, mean_f1=0.356, keyword 0.283, lift +0.073) -- not apples-to-apples with the Exp-Dev best-k {5,8,12,16} sweep + keyword 0.185 baseline.
3. Total corpus size at the prior measurement is not stamped in any persisted artifact, so distractor-density change vs prior cannot be isolated.

**Three concurrent A-axis readings exist across distinct harnesses (1742-1782 atom corpus range):**
- Testbed UNION top_k=5 (Cycle 49, commit a8f0843f, 1742-atom corpus): A_content macro F1 = 0.446 -- AUTHORITATIVE substrate A-axis number, PP-401 annotated baseline.
- Testbed bench fixed top_k=15 (Cycle 49, post-280-atom): mean_f1 = 0.356, keyword 0.283, lift +0.073.
- Exp-Dev GPU best-k sweep (this cycle, 280-algebra-atom / 1782-total): best-k=8 F1 = 0.2966, keyword 0.185, lift +0.112.

The three readings disagree because they MEASURE DIFFERENT THINGS (fusion strategy: UNION vs bge-only fixed-k vs best-k sweep; baseline: bge-description vs Testbed-internal vs keyword 0.185). This is a CROSS-HARNESS CALIBRATION finding, not substrate evidence about A-axis capability. The authoritative substrate A-axis number remains Cycle 49 UNION = 0.446 until a single harness re-runs cleanly before/after a corpus-size-stamped delta.

HONEST: 1840 -> 1841 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v577 -> v578 CYCLE 243)

**(A) exp_gap4v2_semantic_A_eval_gpu_v1 (MIDDLE_BAND -- PP-401 annotation; cross-harness calibration finding; no new PP row):**
PP-401 ANNOTATION (no new PP row): qa_self_knowing gap4v2 semantic-A second-harness reading v578: Exp-Dev best-k sweep on home GPU at 280-algebra-atom / 1782-total corpus = best-k=8 F1 0.2966 (+0.112 over keyword 0.185, MIDDLE pre-reg [0.22-0.30]), n_A=12, n_seeds=1 cycle 243 GPU bench. CROSS-HARNESS CALIBRATION FINDING NOT REGRESSION: three concurrent A-axis readings exist (Testbed UNION top_k=5 = 0.446 authoritative; Testbed bench fixed-k = 0.356; Exp-Dev best-k sweep = 0.297) measuring different fusion strategies and baselines. The prior 0.369 stdout figure was overwritten by buggy 0.0 run; no persisted full-run artifact at consistent harness exists for clean before/after delta. Exp-Dev verdict file is METHODOLOGICALLY TEXTBOOK HONEST -- refuses to claim regression, surfaces three independent root causes of ambiguity (buggy persisted base, different harness in bench_reports, no corpus-size stamping), and recommends process fix (stamp n_total_atoms + n_algebra_atoms into every gap4v2 metrics.json going forward). PROCESS RECOMMENDATION ACCEPTED: corpus-size stamping becomes mandatory for all gap4v2 metrics writes going forward (Exp-Dev volunteered one-line addition). With stamping, batch-2 ingest (commit bdf217c7) gives the first clean incremental delta. UNVERIFIED hypothesis flagged by Exp-Dev (not adopted as finding): distractor-density effect -- more bge-NAME-friendly atoms in the pool = more high-similarity distractors in top-k for questions whose gold is other atoms -> precision drops -> F1 drops. Consistent with the gazetteer sign-flip finding (PP-403) and substrate-aux-features-shrink-with-data principle. CANNOT BE CONFIRMED WITHOUT CORPUS-SIZE-STAMPED BEFORE/AFTER. Authoritative substrate A-axis number remains Cycle 49 Testbed UNION = 0.446 (1742-atom, commit a8f0843f). P-band: 0.43-0.48 EXPLORATORY UNCHANGED (Cycle 49 UNION authoritative; today's reading is calibration-distinct second harness, not a refresh of the band). Cross-ref PP-401 v575 macro F1 0.4658 + v576 UNION_top_k5 A 0.446, PP-403 (substrate-aux-shrink sign-flip mechanism analogue to distractor-density hypothesis), Cycle 49 Testbed close note, Exp-Dev verdict file 2026-06-12 GAP4V2_SEMANTIC_A_280ATOM_REMEASURE_0297.

ANNOTATIONS this cycle:
- PP-401 (qa_self_knowledge): A-axis SECOND-HARNESS reading 0.2966 best-k=8 added; calibration-distinct from Cycle 49 UNION 0.446 (different harness, different fusion strategy, different baseline). Cycle 49 UNION = 0.446 remains AUTHORITATIVE A-axis number. P-band unchanged.
- substrate methodology rule candidate: METRICS WRITES SHOULD STAMP CORPUS-SIZE -- any benchmark that may be re-run as the corpus grows should persist n_total_atoms + n_algebra_atoms (and any other relevant corpus-size scalars) so before/after deltas survive corpus changes. 1st appearance in cap_map -- candidate rule, not yet promoted. Pairs with verify-before-asserting Exp-Dev rule and substrate-as-ground-truth methodology.
- Substrate-product positioning artifact: cross-harness reading divergence (0.446 UNION vs 0.356 bench vs 0.297 best-k-sweep) demonstrates that A-axis capability is MEASURED differently by three valid harnesses -- the authoritative number is the one tied to the production retrieval architecture (UNION HYBRID). Future tracking should fix the harness or stamp it explicitly.
- Distractor-density hypothesis FLAGGED, NOT ADOPTED: more bge-NAME-friendly atoms -> more high-similarity distractors -> precision drops. Mechanism-class consistent with PP-403 aux-features sign-flip and substrate-aux-features-shrink-with-data memory; verify via corpus-size-stamped batch-2 ingest delta in future cycle.
- Exp-Dev verify-before-asserting + honest-self-correct: textbook execution this cycle (refused to claim 0.369->0.297 regression; surfaced three independent ambiguity root causes; flagged unverified mechanism explicitly as hypothesis not finding).

PROT-004/006 NOT APPLICABLE: MIDDLE_BAND in pre-reg band; no closure triggered. Path-to-clean-A-axis-delta rescue sketches (cheapest first):
RESCUE-1 (cheapest/subsumption): accept Exp-Dev's corpus-size stamping proposal -- add n_total_atoms + n_algebra_atoms one-line addition to gap4v2 metrics.json on next run; ~1 line of code. Then batch-2 ingest (commit bdf217c7) gives first clean incremental delta. Inline rescue (Exp-Dev volunteered on its own session cadence).
RESCUE-2: fix the harness for tracking. Designate UNION top_k=5 (production retrieval architecture) as the standing A-axis tracker; deprecate the parallel best-k sweep harness for cap_map tracking purposes. Best-k sweep remains useful for k-sensitivity analysis but not for cap_map A-axis baseline. ~2 lines methodology pin in cap_map.
RESCUE-3: re-run UNION top_k=5 at the current corpus size (1782 atoms total, 280 algebra atoms) to update the authoritative A-axis baseline from the Cycle 49 1742-atom point. Pre-reg A axis 0.44-0.50 (Cycle 49 UNION 0.446 plus or minus breadth/distractor effects). If hits 0.50: HP PASS on A-axis pre-reg. Already in flight per Cycle 49 close note ("UNION + batch 2 (1782 atoms post-ingest) compound bench in flight on remote").
RESCUE-4: re-measure prior 0.369 figure under the same Exp-Dev best-k sweep harness at the prior corpus snapshot (if git history preserves the prior corpus state). Confirms whether 0.369 was the same harness or a hallucinated/misremembered figure.
RESCUE-5: distractor-density mechanism test -- ablate the breadth-50 batch atoms from the pool and re-run the best-k sweep to isolate distractor effect from corpus-density effect. ~10 lines of pool-filter wrapper.

Routing files (written to disk, NOT auto-dispatched):
- strategy_request_to_exp_dev_2026-06-12_gap4v2_corpus_size_stamping.md -- accepts Exp-Dev's RESCUE-1 corpus-size-stamping proposal and asks for the one-line addition on next gap4v2 metrics write. Exp-Dev session will pick on its own 15-min cadence per 4-session architecture.

Cap_map: v577 -> v578 CYCLE 243 (0 HP; 1 MIDDLE_BAND [exp_gap4v2_semantic_A_eval_gpu_v1=PP-401-annotation]; 0 HARD_FAIL; 0 LVH; 0 NEW PP ROWS; PP-401 second-harness A-axis reading annotation 0.2966 best-k=8 at 280-algebra-atom corpus; cross-harness calibration finding NOT regression; corpus-size-stamping methodology fix accepted RESCUE-1 routed to Exp-Dev; distractor-density mechanism FLAGGED-NOT-ADOPTED hypothesis; Cycle 49 UNION 0.446 remains AUTHORITATIVE A-axis baseline; substrate-quality-first frame held; Portfolio 32+403 UNCHANGED; HONEST 1840->1841 +1; LVH 291->291 +0; 472nd PROT-009 paired commit) (2026-06-12)

## v577 -> v578 CYCLE 243 2-VERDICT L-B ABLATIONS 1+2 transitions UNIFORM LEVER (HARD_PASS) + char n-gram SUBSUMED (HARD_FAIL) (verdict_handler 472nd PROT-009 paired commit; 1 HARD_PASS [ner_feature_ablation_transitions]; 1 HARD_FAIL [ner_feature_ablation_char_ngram]; 0 LVH; 2 NEW PP ROWS [PP-404 transitions-scale-invariant-uniform-lever + PP-405 char-ngram-SUBSUMED-by-shape-affix-CLOSED]; L-B substrate-only mechanism deepening series COMPLETE 3-of-3; THREE-SHAPE MECHANISM TYPOLOGY articulated [uniform / low-data-only-sign-flip / subsumed]; substrate-aux-features-shrink-with-data memory EXTENDED with strongest-form variant (subsumed-at-all-scales); methodology rule candidate "substrate-classical NER lift is sequence-model-bound not feature-bound"; Portfolio 32+403->32+405 +2; HONEST 1840->1842 +2; LVH 291->291 +0) (2026-06-12)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_ner_feature_ablation_cpu_v1/metrics.json + Exp-Dev verdict file notes/exp_dev_to_research_LB_FEATURE_ABLATION_VERDICT_TRANSITIONS_UNIFORM_LEVER_CHAR_NGRAM_SUBSUMED_2026-06-12.md). Bridge stale (last poll 2026-05-27, 1395 cumulative failures); local file is the substantive 3-cell x 3-variant artifact (elapsed 619s production run, not a smoke). Honest verify-before-asserting: per-cell metrics in verdict file table match metrics.json per_seed curve exactly.

**Combined metrics.json verdict tag is MIDDLE_BAND** because raw `char_ngram` F1@5pct=0.4024 lands in pre-reg absolute band [0.40-0.43]. The Exp-Dev verdict file PRE-EMPTS this band-vs-lift tension itself by stating "the cell's absolute-band verdict reads MIDDLE only because 0.4024 lands in 0.40-0.43; the LIFT is negative -- the honest read is char n-grams do not help." No over-claim by Exp-Dev. The verdict file's per-ablation labels (Ablation 1 HARD-PASS, Ablation 2 HARD-FAIL) are the honest decomposition; metrics.json's combined MIDDLE_BAND tag is an artifact of the absolute-band test only seeing the char_ngram raw F1. HONEST framing held by Exp-Dev; verdict_handler accepts the per-ablation decomposition as authoritative.

**Ablation 1 transition-contribution HARD_PASS (HONEST):** baseline - no_transition lift = +0.0858 @ 5pct (n_train=299) / +0.0916 @ 10pct (n_train=598) / +0.0977 @ 100pct (n_train=5982). Pre-reg HP bar +0.05@5pct: cleared by 1.7x at smallest data; ALL three cells exceed by 1.7-2x. UNIFORM-LEVER framing (does NOT shrink with data; if anything grows slightly +0.086 -> +0.098 = +0.012 across 20x data) is HONEST. Per Exp-Dev's verify-before-asserting reframe (original Research routing erroneously assumed memoryless harness; ablation harness already had tag-bigram transitions and Viterbi decoding; reframe to "contribution of existing transition mechanism" is the correct question). HONEST.

**Ablation 2 char n-gram HARD_FAIL (HONEST):** char_ngram - baseline lift = -0.0096 @ 5pct / -0.0096 @ 10pct / -0.0075 @ 100pct. Pre-reg HP bar F1@5pct >= 0.43 with low-data-win: char_ngram@5pct=0.4024 < 0.43 (FAIL absolute); lift negative at every fraction (FAIL low-data-win shape; OPPOSITE of gazetteer Ablation 3 sign-flip pattern). "SUBSUMED by existing shape + prefix3 + suffix1-4 features" framing is HONEST -- the small negative lift at all fractions is consistent with weight-budget dilution from redundant features, not with a learnable orthogonal signal. HONEST.

**Headline framing "transitions UNIFORM lever + char n-gram SUBSUMED" is HONEST.** Both per-ablation labels reflect the per-cell numerical reality. The verdict_msg in metrics.json (which reports the char_ngram MIDDLE_BAND only) is technically true for the absolute-band test but does not capture the transition HARD_PASS; the verdict file's per-ablation decomposition is the substantive read. No LVH catch.

HONEST: 1840 -> 1842 (+2). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v577 -> v578 CYCLE 243)

**(A) ner_feature_ablation_cpu_v1 ABLATION 1 transition-contribution (HARD_PASS -- NEW ROW PP-404; scale-invariant uniform lever):**
NEW ROW PP-404: ner_feature_ablation_cpu_v1::transition_contribution HARD_PASS v578: baseline-no_transition lift = +0.0858 @5pct / +0.0916 @10pct / +0.0977 @100pct, n_train=[299, 598, 5982], n_seeds=3 (cycle 243). BIO TAG-BIGRAM TRANSITIONS + VITERBI DECODING ARE A SCALE-INVARIANT ARCHITECTURAL LEVER: removing the existing tag-tag transition features and falling back to per-token independent argmax (memoryless emissions) costs ~0.086-0.098 F1 UNIFORMLY across 20x range of training data. The lift does NOT shrink as data grows (if anything grows slightly +0.012 across the range) -- this is the OPPOSITE shape from external gazetteer PP-403 (low-data-only +0.044 -> sign-flip -0.037) and the OPPOSITE shape from PP-394 brown-cluster aux-features-shrink (positive at low data, vanishes at scale). The structured-prediction sequence model is a TRUE ARCHITECTURAL ARTIFACT, not a low-data crutch. Mechanism: BIO label legality (I- tags cannot start a span; B-LOC -> I-PER is illegal; B-X -> I-X dominates intra-span transitions) encodes hard structural constraints that emission-only models cannot recover from unigram features regardless of data scale; constraint scales with sequence length not with training corpus. Substrate-product positioning: "Substrate-classical NER's primary lift comes from STRUCTURED PREDICTION (Viterbi + learned BIO transitions, ~+0.09 F1 SCALE-INVARIANT), not from feature piling. The architectural lever is the sequence model itself, not the feature count." Pairs with Ablation 3 PP-403 (gazetteer low-data-only sign-flip) and Ablation 2 PP-405 (char n-gram subsumed) to articulate a three-shape mechanism typology for substrate-classical NER lift profiles. Methodology rule candidate: structured-prediction levers (sequence-model architectural) UNIFORM-across-data; auxiliary discrete priors (gazetteers) LOW-DATA-ONLY-SIGN-FLIP; auxiliary lexical features (char n-gram) SUBSUMED-by-existing-features. P-band: 0.85-0.95 EXPLORATORY n=3 seeds CPU 3-cell train-frac curve elapsed=619s. Cross-ref PP-395/PP-396 (NER substrate baseline + 4-type plateau), PP-399 (NER 4-type multiseed HP), PP-403 (external gazetteer low-data-lever CONTRAST), PP-405 (char n-gram SUBSUMED COMPANION), aux-features-shrink-with-data memory, L-B substrate-only mechanism deepening series Ablation 1 of 3 (series now COMPLETE).

**(B) ner_feature_ablation_cpu_v1 ABLATION 2 char n-gram (HARD_FAIL -- NEW ROW PP-405; CLOSED at entry; subsumed by shape+affix):**
NEW ROW PP-405 (CLOSED at entry): ner_feature_ablation_cpu_v1::char_ngram HARD_FAIL v578: char_ngram-baseline lift = -0.0096 @5pct / -0.0096 @10pct / -0.0075 @100pct, char_ngram@5pct=0.4024 vs pre-reg HP F1>=0.43 FAIL, n_seeds=3 (cycle 243). DISCRETE CHAR 3-GRAM + 5-GRAM MEMBERSHIP FEATURES ARE SUBSUMED BY EXISTING SHAPE + PREFIX3 + SUFFIX1-4 FEATURES: adding them yields uniformly negative lift at every data fraction. This is the STRONGEST form of substrate-aux-features-shrink-with-data: not even a low-data regime where the auxiliary features add information (contrast with PP-394 brown-cluster which shrinks with data but is positive at low data, and contrast with PP-403 external gazetteer which is positive at low data with sign-flip at scale). Mechanism: substrate-classical NER feature space already encodes character morphology via shape (Aaa / AAAA / d-d-d-d), prefix3 (Mc-, San-, Inter-), and suffix1-4 (-ing, -tion, -berg, -ville); discrete char n-gram membership is a strict subset of the information already captured. Adding redundant features dilutes the learnable-weight budget without contributing orthogonal signal, producing the small uniform negative lift observed. Substrate-product positioning: "Discrete character n-gram features are subsumed by existing shape+prefix+suffix character-morphology features; adding them does not lift substrate-classical NER at any data scale and slightly hurts via weight-budget dilution. Feature engineering past shape+affix+transitions has diminishing-to-negative return -- the architectural lever is the sequence model, not the feature count." Confirms USER memory pattern substrate-aux-features-shrink-with-data as a SUBSUMPTION pattern (strongest form: subsumed at all scales) when the auxiliary features are in the SAME representational family as existing features (character morphology in this case). CLOSED at entry per PROT-006: HARD_FAIL with mechanism-explained negative result; rescue sketches below provide closure record. P-band: 0.05-0.15 CLOSED n=3 seeds CPU 3-cell train-frac curve. Cross-ref PP-394 (brown-cluster aux shrinkage closer cousin), PP-395/PP-396 (NER substrate baseline shape+affix features that subsume), PP-404 (transition UNIFORM-LEVER companion), PP-403 (gazetteer LOW-DATA-LEVER companion), aux-features-shrink-with-data memory STRONGEST-FORM extension.

ANNOTATIONS this cycle:
- PP-404 (NEW): structured-prediction sequence-model is a scale-invariant architectural lever for substrate-classical NER (+0.09 F1 uniformly across 20x training data).
- PP-405 (NEW, CLOSED at entry): char n-gram features are SUBSUMED by existing shape+prefix+suffix features at every data scale; weight-budget dilution; strongest form of aux-features-shrink-with-data.
- PP-403 (existing, annotated): cross-ref PP-404 (uniform) and PP-405 (subsumed) for three-shape mechanism typology completion.
- PP-394 brown-cluster annotated as cousin of PP-405; both are aux-feature shrinkage but brown-cluster has small-positive low-data regime while char n-gram is uniformly negative.
- aux-features-shrink-with-data memory: EXTENDED with strongest-form variant (SUBSUMED at all scales; not even a low-data regime where the feature helps). Family taxonomy: (a) substrate-derived aux features add nothing (PP-393 substrate-gazetteer CLOSED); (b) external prior knowledge low-data-only sign-flip (PP-403 external gazetteer); (c) lexical/morphological aux subsumed at all scales (PP-405 char n-gram); (d) categorical clustering aux shrinks but stays non-negative (PP-394 brown-cluster).
- L-B substrate-only mechanism deepening series: COMPLETE 3-of-3. Three-shape mechanism typology articulated: structured-prediction = UNIFORM LEVER (architectural); external discrete prior = LOW-DATA SIGN-FLIP (data-regime-gated); intra-family aux features = SUBSUMED (no lift, slight dilution).
- Methodology rule candidate: "substrate-classical NER lift is SEQUENCE-MODEL-BOUND not FEATURE-BOUND". Feature engineering past shape+affix+transitions has diminishing-to-negative return; the architectural lever is the sequence model. 1st appearance candidate; pairs with substrate-quality-first frame and substrate-distinguishing-architectural-advantage in low-data regime.

PROT-004/006 for PP-405 CLOSED-at-entry: rescue sketches (cheapest first) for the rejected char n-gram lever:
RESCUE-1 (cheapest/subsumption): inline annotation -- char n-gram subsumed by existing shape+prefix3+suffix1-4 character-morphology features (mechanism explained at entry); the rejection is structural not implementation-flawed; no re-run needed. APPLIED inline.
RESCUE-2: orthogonal-pair char n-grams (char-bigram + char-skipgram joint features capturing intra-word structure NOT captured by shape) -- could add lift if joint structure is orthogonal to existing morphology features; cheap re-encode + smoke at 5pct only.
RESCUE-3: char-CNN learned continuous char-features (the original L-B v1 path before substrate-classical pivot) cross-cut with noise-injected eval -- could compound with char-noise-robustness story even if it doesn't lift clean-eval F1.
RESCUE-4: domain-shifted re-evaluation (e.g., bioNER where char-affix patterns dominate over learned lexical features) -- could find a domain where char n-gram does lift; methodology paper artifact.
RESCUE-5: feature-budget-controlled comparison (test char n-gram + REMOVE prefix3+suffix1-4 to see if char n-gram CAN substitute for current features at equal budget) -- characterization rather than rescue; methodology paper artifact.

PROT-004/006 for PP-404 HARD_PASS-with-rescue-record-for-future-extension:
RESCUE-1 (cheapest/subsumption): inline annotation -- uniform-lever lift +0.09 across 20x data scales already documented; mechanism (BIO label legality) explained at entry. APPLIED inline.
RESCUE-2: transition-RICHER alternatives (higher-order tag-tag-tag tri-grams, segment-CRF) -- could push lift beyond +0.10 but with quadratic feature cost; characterization cell at 5pct only.
RESCUE-3: extend uniform-lever claim to LARGER data (10x ConLL via OntoNotes) -- characterization; expected to remain uniform.
RESCUE-4: re-measure under noise-injected eval as a noise-robustness compound (transitions should be MORE noise-robust than lexical features at high noise) -- pairs with char-CNN-under-noise cross-cut on Ablation 3 RESCUE-1.

Routing files (written to disk, NOT auto-dispatched):
- strategy_request_to_exp_dev_2026-06-12_LB_complete_three_shape_typology.md -- summarizes L-B mechanism deepening 3-of-3 typology + invites cross-cut bench (char-noise + transitions) as low-cost compound characterization; Exp-Dev session picks at its own cadence per 4-session architecture.

Cap_map: v577 -> v578 CYCLE 243 (1 HARD_PASS [ner_feature_ablation_transitions=PP-404]; 1 HARD_FAIL [ner_feature_ablation_char_ngram=PP-405 CLOSED at entry]; 0 LVH; 2 NEW PP ROWS [PP-404 transitions-scale-invariant + PP-405 char-ngram-subsumed-CLOSED]; L-B substrate-only mechanism deepening series COMPLETE 3-of-3; three-shape mechanism typology articulated [uniform-lever / low-data-sign-flip / subsumed]; aux-features-shrink-with-data EXTENDED with strongest-form SUBSUMED variant; methodology rule candidate "substrate-classical NER lift is sequence-model-bound not feature-bound" 1st appearance; Portfolio 32+403 -> 32+405 +2; HONEST 1840->1842 +2; LVH 291->291 +0; 472nd PROT-009 paired commit) (2026-06-12)


## v578 -> v579 CYCLE 50 (Testbed) BATCH-2 POST-DEDUP NO LIFT + REVERT-ACCEPTED + DISTRACTOR-DENSITY-LEADING-HYPOTHESIS architectural-strategy annotation (verdict_handler 473rd PROT-009 paired commit; 0 HP; 0 MIDDLE_BAND; 0 HARD_FAIL; 1 ARCHITECTURAL-STRATEGY [substrate_self_knowing_batch2_postdedup_revert=PP-401-annotation]; 0 LVH (1 label-nuance noted non-blocking); 0 NEW PP ROWS; PP-401 batch-2 REVERT-ACCEPTED annotation + Cycle 49 UNION 0.446 1742-atom REAFFIRMED authoritative + breadth-backfill structurally gated on Phase-2-light substrate-guided proposal tool; distractor-density LEADING-HYPOTHESIS not fully isolated (M2 bge re-encode + M3 algebra growth not ruled out -- revert IS Mechanism-1 isolating diagnostic); meta::RULE_authoring_substrate_queries_first 4th appearance PROMOTION-ELIGIBLE; rule 12 PARTITIONS unchanged separate scope; Portfolio 32+405 UNCHANGED; HONEST 1842->1843 +1; LVH 291->291 +0) (2026-06-12)

### Step 0 honest re-read

Source: Testbed-authored verdict file `notes/testbed_to_research_BATCH_2_DEDUP_DID_NOT_LIFT_DISTRACTOR_DENSITY_CONFIRMED_RECOMMEND_REVERT_2026-06-12.md` (commit 2982790e). Hand-tabulated per-Q delta table 12/12 rows showing zero per-Q delta between 1782-atom pre-dedup (A 0.418) and 1774-atom post-dedup (A 0.418). No metrics.json artifact (architectural-strategy verdict from Testbed measurement, not Exp-Dev cell). Verify-before-asserting: per-Q numbers internally consistent; dedup commit 8a3e891b applied 8 T2/T3 merges; A-axis result unchanged at the per-Q level -> duplication-mechanism FALSIFIED as predominant displacement vector.

LABEL-VS-HONEST nuance (non-blocking): headline reads "DISTRACTOR DENSITY CONFIRMED" but the body itself enumerates THREE plausible mechanisms (M1 distractor-density via 32 kept T2 atoms; M2 bge re-encoding artifact at 1742->1782 atom index rebuild; M3 algebra-index growth 240->280 atoms changing UNION dedupe behavior) and states "All three plausible. Distinguishing requires [revert]." Honest framing: distractor-density is the LEADING hypothesis, not fully isolated. Recorded as label-nuance NOT counted as LVH because the operational recommendation (REVERT batch 2) dominantly serves both purposes (restore authoritative 0.446 baseline AND constitutes Mechanism-1 isolation diagnostic by definition) regardless of which mechanism ultimately wins.

CRITICAL SCOPE SEPARATION per user framing: this verdict is ARCHITECTURAL-STRATEGY on breadth-backfill atom-authoring strategy, NOT a capability verdict. Rule 12 PARTITIONS-not-hierarchy result (Cycle 49 UNION top_k=5 = 0.446 at 1742 atoms) STILL STANDS UNCONDITIONALLY -- UNION HYBRID architecture remains empirically validated. What failed is the hypothesis that "more bge-name-friendly atoms in dense topic neighborhoods compound the UNION lift." PP-401 capability P-band UNCHANGED. No portfolio change. No capability closure. No HP/FAIL band shift.

HONEST: 1842 -> 1843 (+1, this annotation cycle). LVH: 291 -> 291 (+0).

### Cap_map decision (v578 -> v579 CYCLE 50)

PP-401 annotation: batch-2 post-dedup A axis 0.418 IDENTICAL to pre-dedup; per-Q ALL-IDENTICAL; 8 T2/T3 dedupes did NOT lift -> 32 kept-new T2 atoms (td_lambda, actor_critic, PPO, MCTS, junction_tree, factor_graph, semantic_role_labeler, chunker, automatic_differentiation, etc.) also act as distractors against same A-axis gold under top-K retrieval. REVERT-ACCEPTED. Cycle 49 UNION top_k=5 = 0.446 at 1742-atom corpus REAFFIRMED authoritative A-axis baseline (per v576 + v578 prior annotations). Breadth-backfill strategy STRUCTURALLY GATED on Phase-2-light substrate-guided proposal tool (Research routing in flight per USER "yes"); tool will use L1 clustering density to identify sparse-vs-dense topic neighborhoods preventing class recurrence. Distractor-density LEADING-HYPOTHESIS; M2/M3 not ruled out; revert IS the Mechanism-1 isolating diagnostic by design. Mechanism class consistent with PP-403 aux-features sign-flip + PP-405 char n-gram subsumed + substrate-aux-features-shrink-with-data memory (extended from feature-density to atom-density regime; same shape). meta::RULE_authoring_substrate_queries_first 4th appearance PROMOTION-ELIGIBLE pending Phase-2-light empirical validation. Rule 12 PARTITIONS UNCHANGED separate scope.

PROT-004/006 rescue sketches (cheapest-first):
RESCUE-1 (cheapest/subsumption, APPLIED AT ENTRY): REVERT batch 2 entirely, restore 1742-atom state. Serves double-duty: restore authoritative 0.446 baseline + Mechanism-1 isolation diagnostic. ~0 net compute. Testbed already routed for re-measurement.
RESCUE-2: Mechanism-2 isolation -- rebuild bge cache on 1742-atom state with v2_name encoder, measure UNION top_k=5; if != 0.446, encoding artifact confirmed.
RESCUE-3: Mechanism-3 isolation -- keep 1782-atom state, reduce algebra top_k 5->3 in UNION, measure; if A lifts toward 0.446, algebra-growth-UNION-dedupe mechanism confirmed.
RESCUE-4: Phase-2-light substrate-guided proposal tool ship priority -- structural fix preventing class recurrence (substrate L1 clustering density identifies sparse neighborhoods worth ingesting).
RESCUE-5: characterization cell -- ablate ONLY 32 kept T2 atoms (vs all batch-2 atoms generically) and measure; characterizes whether displacement is RL/PGM/NL-topic-overlap-specific.

Routing files (written to disk, NOT auto-dispatched per 4-session architecture):
- strategy_request_to_research_2026-06-12_phase2_light_substrate_guided_proposal_priority.md
- strategy_request_to_testbed_2026-06-12_batch2_revert_apply_and_remeasure.md

Cap_map: v578 -> v579 CYCLE 50 (0 HP; 0 MIDDLE_BAND; 0 HARD_FAIL; 1 ARCHITECTURAL-STRATEGY [batch2_postdedup_revert=PP-401-annotation]; 0 LVH (1 label-nuance non-blocking); 0 NEW PP ROWS; PP-401 batch-2 REVERT-ACCEPTED + Cycle 49 UNION 0.446 1742-atom REAFFIRMED + breadth-backfill structurally gated on Phase-2-light; distractor-density LEADING-HYPOTHESIS not fully isolated; meta::RULE_authoring_substrate_queries_first 4th appearance PROMOTION-ELIGIBLE; rule 12 PARTITIONS unchanged separate scope; Portfolio 32+405 UNCHANGED; HONEST 1842->1843 +1; LVH 291->291 +0; 473rd PROT-009 paired commit) (2026-06-12)

## v579 -> v580 CYCLE 244 1-VERDICT COMPOUND C ner_gazetteer_noise_crosscut HARD_FAIL noise-fragile (verdict_handler 474th PROT-009 paired commit; 0 HP; 0 MIDDLE_BAND; 1 HARD_FAIL [ner_gazetteer_noise_crosscut]; 0 LVH; 0 NEW PP ROWS; PP-403 ANNOTATED noise-fragility dimension; three-shape typology x noise crosscut SHARPENED; aux-features-shrink-with-data memory EXTENDED with PERTURBATION-FRAGILITY axis; methodology rule candidate meta::RULE_discrete_surface_exact_match_features_are_noise_fragile_by_construction 1st appearance; Compounds A+B noise crosscut still QUEUED; Portfolio 32+405 UNCHANGED; HONEST 1843->1844 +1; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_ner_gazetteer_noise_crosscut_cpu_v1/metrics.json; remote bridge stale is_stale=True). 4-cell crosscut HONEST verify-before-asserting: per-cell metrics match verdict_msg exactly.

**ner_gazetteer_noise_crosscut_cpu_v1 HARD_FAIL (HONEST):** 4 cells [frac x noise]: 5pct/0pct=+0.0441 / 5pct/10pct=+0.0167 / 100pct/0pct=-0.0368 / 100pct/10pct=-0.0383. Pre-reg HF threshold lift@5pct delta clean->noisy <=-0.02; observed -0.0274 (margin 0.37x). Hypothesis (gazetteer noise-robust) REFUTED. Mechanism (matches verdict file pre-reg caveat): exact-match lowercased token membership; char-noise corrupts gazetteer tokens ("washington" -> "washxngton") -> feature drops to 0 -> noise-fragile by construction. n_seeds field reports 3 but per_seed has 1 entry de-facto single-seed; threshold unambiguous so single-seed reading sufficient for HF annotation. Substrate-quality-first frame held. No over-claim. HONEST.

HONEST: 1843 -> 1844 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v579 -> v580 CYCLE 244)

**(A) ner_gazetteer_noise_crosscut_cpu_v1 (HARD_FAIL -- PP-403 annotation noise-fragility; no new PP row):**
See cap_map v579->v580 entry for full annotation body (PP-403 doubly-fragile; three-shape typology x noise crosscut sharpened; methodology rule candidate 1st appearance; substrate-product positioning).

ANNOTATIONS this cycle:
- PP-403: noise-fragility dimension ANNOTATED (doubly fragile: low-data-only + noise-fragile). P-band 0.45-0.62 EXPLORATORY UNCHANGED.
- PP-404: Compound A noise-robustness PRE-REG pending Exp-Dev verdict.
- PP-405: Compound B more-negative-under-noise PRE-REG pending Exp-Dev verdict.
- aux-features-shrink-with-data memory: EXTENDED with PERTURBATION-FRAGILITY dimension; two-axis taxonomy (data regime x perturbation regime).
- meta::RULE_discrete_surface_exact_match_features_are_noise_fragile_by_construction: 1st appearance methodology rule candidate.

PROT-004/006 NOT APPLICABLE: ANNOTATION on existing PP-403; no closure (capability bound unchanged; noise dimension is fragility characterization). Rescue sketches in cap_map entry (cheapest first: wait for A+B noise verdict; Levenshtein-1 fuzzy gazetteer; subword-class proxy; embedding-distance membership; gazetteer+shape ensemble).

Routing files: NONE new this cycle. Compounds A+B noise crosscut already queued on local_cpu_queue (exp_ner_transition_charngram_noise_crosscut_cpu_v1; ~1-1.5 hr CPU).

Memory extension flagged for memory_curator (future cycle): substrate-aux-features-shrink-with-data memory should be EXTENDED with PERTURBATION-FRAGILITY second axis (data regime x perturbation regime; three-shape becomes three-x-three).

Cap_map: v579 -> v580 CYCLE 244 (1 HARD_FAIL [ner_gazetteer_noise_crosscut=PP-403-noise-fragility-annotation]; 0 LVH; 0 NEW PP ROWS; PP-403 ANNOTATED doubly-fragile; methodology rule candidate 1st appearance; Compounds A+B noise crosscut still QUEUED; Portfolio 32+405 UNCHANGED; HONEST 1843->1844 +1; LVH 291->291 +0; 474th PROT-009 paired commit) (2026-06-12)


## v580 -> v581 CYCLE 245 1-VERDICT ner_transition_charngram_noise_crosscut HARD_PASS Compound A + Compound B characterization (verdict_handler 475th PROT-009 paired commit; 1 HARD_PASS Compound A + 1 characterization Compound B; 0 LVH; 0 NEW PP ROWS; PP-404 ANNOTATED noise-robust empirically; PP-405 ANNOTATED noise-helpful-at-scale CLOSED preserved; three-shape mechanism typology x noise crosscut COMPLETE; meta::RULE_sequence_model_bound_not_feature_bound 2nd APPEARANCE; substrate-product "sequence model is BOTH levers; discrete features are NEITHER" empirically HARDENED; Portfolio 32+405 UNCHANGED; HONEST 1844->1845 +1; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_ner_transition_charngram_noise_crosscut_cpu_v1/metrics.json). verdict_msg matches per-cell metrics exactly.

**ner_transition_charngram_noise_crosscut_cpu_v1 HARD_PASS Compound A + Compound B characterization (HONEST):** 4-cell [frac x noise] crosscut.

Compound A (transition contribution): clean +0.0858/+0.0977 at 5pct/100pct; noisy +0.0796/+0.0911. Delta @ 5pct = -0.0062. Pre-reg HP bar: delta >= -0.01 -> PASS by 38% margin (slip is only 62% of allowed). Mechanism CONFIRMED: BIO transitions are tag-bigram features invariant to emission-level char noise. PP-404 noise-robust pre-reg empirically CONFIRMED.

Compound B (char n-gram lift): clean -0.0096/-0.0075 at 5pct/100pct (PP-405 subsumed-clean closure preserved); noisy -0.0088/+0.0177. Noise-NEUTRAL at low data; noise-HELPFUL flip at scale (+0.018). Pre-reg from v580 expected MORE-negative under noise -> empirically REFUTED in the at-scale regime; subword n-grams retain partial signal where whole-word lexical baseline degrades. PP-405 CLOSED state preserved (closure was on clean-eval subsumption); annotation is noise-sub-property characterization not bound shift.

n_seeds field reports 3 but per_seed list has 1 entry -- de-facto single-seed (same provenance pattern as PP-403 noise crosscut v580); effect sizes are unambiguous (transition contribution preserves ~92% of clean lift under noise at BOTH data fractions) so single-seed reading sufficient for threshold-passing HP bar. No over-claim. HONEST.

HONEST: 1844 -> 1845 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions

(A) ner_transition_charngram_noise_crosscut_cpu_v1 (Compound A HARD_PASS + Compound B characterization):
- PP-404 (existing, ANNOTATED): noise-robust dimension EMPIRICALLY CONFIRMED (HP). Two-axis architectural lever: SCALE-INVARIANT + NOISE-ROBUST. P-band 0.55-0.72 -> 0.60-0.78 EXPLORATORY (+0.05 both bounds; noise-axis confirmation as 2nd independent supporting axis).
- PP-405 (existing CLOSED, ANNOTATED): noise-helpful-at-scale sub-property added (+0.018 char_lift @ 100pct/10pct). CLOSED state preserved (closure was on clean-eval subsumption which still holds). 5 RESCUE sketches recorded in cap_map entry (cheapest first: noise-sweep characterization; n-gram-order sweep; char-ngram+shape compound; char-CNN substitution; char-ngram+transition compound isolation).
- PP-403 (existing, ANNOTATED): contrast empirically confirmed in same harness -- PP-403 doubly fragile vs PP-404 doubly robust within harness-controlled noise crosscut.
- three-shape mechanism typology x noise crosscut: COMPLETE (was SHARPENED v580; PP-404 noise-robust + PP-405 noise-relationship both empirically resolved this cycle).

Complete typology table:
| mechanism | scale behavior | noise behavior | (PP row) |
|---|---|---|---|
| structured prediction (BIO transitions + Viterbi) | SCALE-INVARIANT uniform +0.09 | NOISE-ROBUST (delta -0.006) | PP-404 |
| external gazetteer (discrete prior) | LOW-DATA-only, sign-flip at scale | NOISE-FRAGILE (delta -0.027) | PP-403 |
| char n-gram (intra-family lexical aux) | SUBSUMED at all scales clean | mildly noise-HELPFUL at scale (+0.018) | PP-405 |

(B) Methodology rule progression:
- meta::RULE_sequence_model_bound_not_feature_bound: 2nd APPEARANCE (1st = Cycle 243 PP-404 scale + PP-405 HF; 2nd = this cycle PP-404 noise + PP-405 noise characterization; rule now spans both scale AND noise axes). Awaits 3rd appearance for CONFIRMED promotion.
- meta::RULE_discrete_surface_exact_match_features_are_noise_fragile_by_construction (1st appearance v580 PP-403): UNCHANGED at 1st appearance this cycle.

(C) Substrate-product positioning HARDENED (was directionally articulated at v580): "the sequence model is BOTH the scale-invariant lever AND the noise-robustness lever; discrete external features (PP-403 gazetteer) are NEITHER". Now load-bearing for substrate-classical NER architectural lever story.

PROT-004/006 NOT TRIGGERED: HARD_PASS Compound A is an ANNOTATION on existing PP-404 (P-band lift not closure); Compound B characterization preserves PP-405 CLOSED state (closure was clean-eval subsumption; noise-axis is sub-property). No new closures.

Routing files: NONE new this cycle. The L-B noise crosscut series is now COMPLETE; Exp-Dev L-B subseries closes here. Cell A composition-capacity GPU verdict (running separately) is the next Exp-Dev item but is not part of this verdict.

Memory extension flagged for memory_curator (future cycle): substrate-aux-features-shrink-with-data memory should be UPDATED with the COMPLETE 2-axis typology (PP-404 positive case both axes; PP-403 doubly-fragile case; PP-405 noise-neutral-flipping-helpful closed case; char-shape next noise-axis target assumed noise-fragile unverified).

Cap_map: v580 -> v581 CYCLE 245 (1 HARD_PASS Compound A + 1 Compound B characterization [ner_transition_charngram_noise_crosscut=PP-404-noise-robust + PP-405-noise-helpful-at-scale]; 0 LVH; 0 NEW PP ROWS; PP-404 ANNOTATED noise-robust P-band 0.55-0.72 -> 0.60-0.78; PP-405 ANNOTATED noise-helpful-at-scale CLOSED preserved; PP-403 contrast confirmed within same harness; three-shape mechanism typology x noise crosscut COMPLETE; meta::RULE_sequence_model_bound_not_feature_bound 2nd APPEARANCE; substrate-product "sequence model is BOTH levers; discrete features are NEITHER" empirically HARDENED; aux-features-shrink memory EXTENDED with complete 2-axis typology (memory_curator update flagged); L-B noise crosscut series closes; Portfolio 32+405 UNCHANGED; HONEST 1844->1845 +1; LVH 291->291 +0; 475th PROT-009 paired commit) (2026-06-12)

## v583 -> v584 CYCLE 246b 1-VERDICT substrate_csls_cleanup_recovery_gpu_v1 HARD_FAIL CSLS-re-rank-NOT-the-lever, clustered-codebook deficit is GENUINE NEAR-DUPLICATES (not hubness), mitigation is ENCODING (not re-rank) [SEQUENTIALLY RE-VERSIONED from v581->v582 after parallel verdict_handler instance posted alternative v582 CYCLE 246 PP-406/PP-407 composition/decomposition entry at line 399; this CSLS entry CORRECTS PP-406/PP-407's "CSLS/MMR cleanup re-rank is the indicated lever" mitigation recommendation cross-cycle] (verdict_handler 478th PROT-009 paired commit; 0 HP; 0 MIDDLE_BAND; 1 HARD_FAIL [substrate_csls_cleanup_recovery_gpu_v1]; 0 LVH; 0 NEW PP ROWS; PP-401 qa_self_knowing A-axis path-to-HP REFRAMED to encoding-discriminability (signature/complexity field population) NOT re-rank; PP-406/PP-407 mitigation recommendation REFRAMED CROSS-CYCLE (re-rank lever REPLACED by encoding-discriminability lever); rule 12 PARTITIONS framing SHARPENED with a new orthogonal axis [encoding-discriminability vs re-rank-scoring]; methodology rule candidate meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 1st appearance; Research distractor-density 2x DEEP drill mechanism prediction REFINED (directionally wrong on mechanism even though directionally indicative on encoding-bottleneck); asymmetric-leg drill CSLS-bge-degrades-via-hubness prediction REQUIRES REFINEMENT to "asymmetric-leg via near-duplicates" pending free-probability drill; substrate-product positioning "encoding discriminability is the shared lever" articulated; Portfolio 32+407 UNCHANGED; HONEST 1847->1848 +1; LVH 291->291 +0)

### Step 0 honest re-read (Cycle 246 CSLS verdict)

Metrics source: LOCAL FALLBACK (remote bridge STALE; local metrics.json is a PRE-SHIP SMOKE artifact n_seeds=1 device=cpu corpus=241 F-coverage only {1,2,3}; production-scope GPU curve with F={1,2,3,5,10,20} is reported in the verdict routing note exp_dev_to_research_CSLS_HARDFAIL_DEFICIT_IS_GENUINE_NEAR_DUPLICATES_NOT_HUBNESS_MITIGATION_IS_ENCODING_NOT_RERANK_2026-06-12.md). Local smoke covers only the low-F cells (F<=3) and matches the production-curve framing there (lift=0 at F=1/2/3). High-F destructive cells (F=10: -0.330; F=20: -0.488) are reported by Exp-Dev's routing note and not re-verified in local metrics; the directional pattern (zero recovery at low F + active destruction at high F) is internally consistent with the CSLS mechanism (at low F the correct atom already wins by cosine so r_k(c) subtraction does not change the winner; at high F the recovered-filler signal cos = 1/sqrt(F) ~ 0.22-0.32 falls below the hubness penalty, so the argmax drifts to low-hubness atoms unrelated to the estimate). HARD_FAIL label is HONEST against the empirical evidence available. The DECISIVE FINDING ("the deficit is genuine semantic near-duplicates, not hubness") is a MECHANISTIC INFERENCE from the empirical pattern + the canonical role of CSLS as the standard hubness mitigation; it is strongly supported but is an inference not a direct diagnostic. Exp-Dev's routing note correctly frames it as decisive given CSLS's literature role as THE canonical hubness mitigation -- if hubness were the mechanism, CSLS would help. Verdict_msg "near-duplicates not hubness + mitigation is encoding not re-rank" is HONEST. No over-claim. Metrics-source caveat noted ([metrics-source: local-fallback]). No LVH catch.

HONEST: 1845 -> 1846 (+1). LVH: 291 -> 291 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v581 -> v582 CYCLE 246)

**(A) substrate_csls_cleanup_recovery_gpu_v1 (HARD_FAIL -- PP-401 path-to-HP REFRAMED + methodology rule 1st appearance + mechanism refinement for Research asymmetric-leg drill):**

PP-401 ANNOTATION (no new PP row; capability bound unchanged): substrate_csls_cleanup_recovery_gpu_v1 HARD_FAIL v582: CSLS cleanup@1 curve (F=1/2/3/5/10/20): standard=[0.9333/0.8917/0.8889/0.8533/0.8683/0.8417] vs CSLS=[0.9333/0.8917/0.8889/0.8533/0.5383/0.3533]; CSLS lift=[+0/+0/+0/+0/-0.330/-0.488], n_seeds=1 GPU cuda (cycle 246) per routing note; local smoke n_seeds=1 device=cpu F<=3 confirms zero-lift at low F. CSLS RE-RANK FAILS AT THE CLUSTERED-CODEBOOK DECODE CEILING: hubness-penalty re-rank gives EXACTLY ZERO recovery at low F and ACTIVELY DESTROYS accuracy at high F. Mechanism: at low F the correct atom already wins by cosine (so r_k subtraction does not change the argmax); at high F the recovered-filler signal cos ~ 1/sqrt(F) ~ 0.22-0.32 is dominated by the hubness penalty so the argmax drifts to low-hubness atoms unrelated to the true estimate. DECISIVE INTERPRETATION: the ~0.11 deficit Cell A measured (clustered codebook caps cleanup at ~0.89 at F=3 vs uniform=1.0) is GENUINE SEMANTIC NEAR-DUPLICATES, NOT hubness. If it were hubness, the hubness penalty would help; instead it does nothing or hurts. Genuine near-duplicates = atoms whose algebra-HRR encodings are near-identical (e.g., within-category atoms with near-identical structured profiles per the L1 categorical clustering result from substrate_vsa_position_is_meaning memory); the discriminating information is SIMPLY NOT PRESENT IN THE ENCODING, and no cleanup re-rank can recover what the encoding never produced.

ARCHITECTURAL IMPLICATION (sharp, actionable, surface-direction-changing): the mitigation for the clustered-codebook decode ceiling is NOT cleanup re-rank (CSLS/MMR/probabilistic-cleanup will all fail by the same mechanism) but ONE OF:
  (1) FINER ATOM ENCODING -- richer algebra-HRR via signature/complexity field population (currently 0-populated for all atoms per Exp-Dev's earlier audit). Promotes near-duplicate atoms to separate in the codebook by ADDING orthogonal structure to the bind tree.
  (2) ATOM DE-DUPLICATION / MERGING -- if two atoms encode the same primitive, merge them (reduces effective K and trivially restores cleanup margin).

This DIRECTLY informs PP-401 qa_self_knowing path-to-HP (A-axis F1 0.446 -> HP gap 0.054 per Cycle 49 Testbed close): the SAME clustered-codebook geometry that caps decode is the SAME geometry the bge-name + algebra-HRR UNION is navigating. ENCODING DISCRIMINABILITY (signature/complexity population) is the SHARED LEVER across (a) cleanup recovery and (b) A-axis self-knowledge UNION. The Cycle 49 finding that UNION top_k=5 lifts A-axis to 0.446 but stalls there is now explained: UNION exploits orthogonal coverage between algebra-HRR and bge-cosine, but within EACH index near-duplicate atoms produce ambiguous ranking, so the union-max-score cannot fully recover ground-truth where the encoding itself collapses distinct meanings to nearby vectors. Path-to-HP for A-axis is RE-PRIORITIZED: signature/complexity field population is the FIRST lever; Phase-6 ingestion is the SECOND (independent of encoding) lever; UNION-as-architecture remains correct but is downstream of encoding fix. PP-401 P-band UNCHANGED 0.43-0.48 EXPLORATORY (capability bound unchanged; path-to-HP is path-axis annotation).

METHODOLOGY RULE 1st APPEARANCE: meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank -- when cleanup at low F has a ~0.10-0.15 gap from uniform-codebook ceiling, the gap diagnostic distinguishes (a) HUBNESS (CSLS / r_k subtraction RECOVERS lift) from (b) GENUINE NEAR-DUPLICATES (CSLS / r_k subtraction shows zero or negative lift); under (b) the mitigation class is ENCODING (signature/complexity, atom-merge, finer-granularity bind tree) NOT RE-RANK. Empirical predictions: (i) probabilistic-cleanup (Frady-Sommer) should fail by the same mechanism on the same atoms; (ii) MMR re-rank should fail (diversification cannot create discriminating signal that the encoding lacks); (iii) higher-D bind (N=2048 -> N=4096) without signature/complexity will fail too (dimensionality alone does not separate semantically near-duplicate atoms); (iv) signature/complexity field population should produce a clean lift at high F (where the failure currently dominates); (v) atom de-duplication / merging should reduce effective K and restore cleanup margin proportionally. 1st appearance status -- awaits 2nd and 3rd empirical confirmation per methodology-rule-promotion cadence. Likely 2nd-appearance hook: signature/complexity field population cell on the same Cell A harness.

RULE 12 PARTITIONS-NOT-HIERARCHY framing GAINS A SECOND ORTHOGONAL PARTITION AXIS: rule 12 v576 established that algebra-HRR and bge-cosine are PARTITION retrieval primitives (UNION wins because they cover orthogonal slices of semantic space). This verdict adds a SECOND orthogonal axis: ENCODING-DISCRIMINABILITY and RE-RANK-SCORING are PARTITION mitigation primitives (they address ORTHOGONAL deficit classes -- encoding addresses near-duplicate atoms, re-rank addresses hub-density artifacts; they are NOT substitutes for each other). The 2x2 mitigation matrix is now:
| deficit class                | encoding lever        | re-rank lever |
| genuine near-duplicates      | YES (signature/complexity, merge) | NO (CSLS empirically fails) |
| hubness density artifacts    | maybe (depends on geometry) | YES (CSLS standard) |
| orthogonal-coverage misses   | partial (atom-add)     | partial (UNION) |
| ranking-tie ambiguity        | NO                    | YES (MMR, score-fusion) |

Rule 12 PARTITIONS-not-hierarchy framing is SHARPENED: not just "algebra and bge are partitions for retrieval" but more generally "substrate mitigations are partition-class-specific; substituting a mitigation from one partition class to another (e.g., applying re-rank CSLS to an encoding-class deficit) produces null or negative results by construction."

RESEARCH PREDICTION REFINEMENT (carries to free-probability + asymmetric-leg drills):
  (i) DISTRACTOR-DENSITY 2x DEEP DRILL: predicted CSLS as cheap mitigation (P_deflated 0.55). EMPIRICAL: CSLS did NOT help. The drill prediction was DIRECTIONALLY CORRECT on the outcome class (substrate has retrieval-degradation at high F) but DIRECTIONALLY WRONG on the mechanism (hubness vs near-duplicates) and therefore on the mitigation (re-rank vs encoding). This refines methodology rule 9 (refine-via-empirical-FAIL) and rule 12 (PARTITIONS) -- literature-grounded drill mechanism predictions are PRIOR not ORACLE per the substrate-extracted-rules-are-prior-not-oracle memory; CSLS prediction was a lit-scan-calibrated PRIOR, not an oracle, and the empirical FAIL is consistent with the lit-scan-calibration-penalty deflation (0.55 deflated correctly carries a probability mass for null outcome). The DRILL ITSELF was honest; the prediction was within the calibrated band.
  (ii) ASYMMETRIC-LEG DRILL "bge degrades via hubness" prediction: NOW REQUIRES REFINEMENT to "asymmetric leg via NEAR-DUPLICATES" -- if substrate's deficit at the cleanup layer is near-duplicate-encoding rather than hubness-density, the same mechanism likely explains bge-name asymmetric degradation under topic-overlapping batch additions (PP-401 batch-2 degradation Cycle 50 candidate). Free-probability drill in flight should test the Gram matrix RANK / NEAR-DUPLICATE structure (eigenvalue clumping near 1 indicates near-duplicate-encoded atoms) NOT the spectral edge / Tracy-Widom signature (hubness signature).
  (iii) FREE-PROBABILITY DRILL CROSS-REFERENCE: the deficit being a RANK / DUPLICATE property of the algebra-HRR Gram matrix (NOT a hubness/density artifact) is a TESTABLE PREDICTION the free-probability drill in flight can confirm or refute -- if Gram spectrum shows clumping at the top eigenvalue regime (near-duplicate signature) rather than Marchenko-Pastur-with-hub-outliers (hubness signature), this verdict's mechanism inference is corroborated by an independent observability primitive.

PROT-004/006 NOT APPLICABLE: HARD_FAIL is an ANNOTATION on existing PP-401 path-to-HP-axis + methodology rule candidate registration, not a new closure (no PP row carried a CSLS-helps capability claim; PP-401 capability bound is unchanged). Rescue sketches (cheapest first, recorded for future encoding-axis investigation; do NOT auto-dispatch -- Exp-Dev session owns queue under 4-session architecture):

RESCUE-1 (cheapest / subsumption): signature/complexity field population on existing atom set -- run the encode pass to populate the 0-populated signature/complexity fields, re-measure Cell A cleanup curve at F=3,5,10,20. Predicts lift at high F (F=10,20) where current empirical fail dominates. ~30 lines of encode-pass + re-bench. EV high (directly tests methodology rule 1st-appearance prediction).

RESCUE-2: atom de-duplication audit -- compute pairwise cosine on current algebra-HRR encodings; flag pairs > 0.95 as near-duplicate candidates; manually classify (true semantic duplicates -> merge; false-positive structural near-duplicates -> annotate for encoding fix). ~50 lines of pairwise + heuristic clustering. EV medium (orthogonal evidence on the near-duplicate hypothesis).

RESCUE-3: probabilistic-cleanup (Frady-Sommer) on same harness -- predicts FAIL by the methodology rule. If FAIL, 2nd-appearance confirmation of the rule. If unexpectedly PASS, the rule is REFUTED and we revise. ~40 lines of probabilistic-cleanup wrapper around existing cleanup harness.

RESCUE-4: cross-axis test on PP-401 A-axis -- does signature/complexity population lift A-axis F1 toward HP (0.50)? This is the cross-capability transfer test for the methodology rule. Already conceptually-blocked by encoder population task ordering (RESCUE-1 is the prerequisite). EV medium-high if RESCUE-1 succeeds.

RESCUE-5: free-probability drill Gram matrix near-duplicate signature analysis -- already in flight on the Research side; this verdict cross-references and refines the prediction (eigenvalue clumping vs spectral edge). No additional Exp-Dev work required.

Routing files (written to disk; orchestrator will dispatch on its own cadence):
- notes/strategy_request_to_research_2026-06-12_csls_mechanism_refinement_distractor_drill_asymmetric_leg.md -- refines distractor-density drill mechanism prediction (near-duplicates not hubness) + carries to asymmetric-leg drill + cross-references free-probability drill in flight.

ANNOTATIONS this cycle:
- PP-401 (qa_self_knowing): A-axis path-to-HP REFRAMED -- signature/complexity field population is the FIRST encoding-discriminability lever; Phase-6 ingestion is the SECOND independent lever; UNION-as-architecture is downstream of encoding fix. P-band UNCHANGED 0.43-0.48 EXPLORATORY (capability bound unchanged; path-axis annotation).
- Rule 12 PARTITIONS-not-hierarchy: SHARPENED with second orthogonal partition axis (ENCODING-DISCRIMINABILITY vs RE-RANK-SCORING are partition mitigation primitives). 2x2 mitigation matrix populated this cycle.
- meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank: 1st APPEARANCE candidate. Awaits 2nd and 3rd empirical confirmation (probabilistic-cleanup FAIL + signature/complexity-population PASS = 2nd appearance; cross-capability A-axis lift = 3rd appearance).
- Methodology rule 9 (refine-via-empirical-FAIL): GAINS another application instance (3rd+) -- distractor-density drill CSLS prediction was within calibrated band but DIRECTIONALLY WRONG ON MECHANISM; rule 9 framing extended to "mechanism-class refinement when literature-grounded mitigation fails empirically; deflated probability mass carries the null outcome correctly."
- Research distractor-density drill: prediction was HONEST within lit-scan-calibration deflation band; mechanism inference REFINED for future drills (test mechanism class explicitly before recommending mitigation class).
- Asymmetric-leg drill prediction: REQUIRES REFINEMENT from "bge degrades via hubness" to "bge degrades via near-duplicates"; mechanism prediction carries to PP-401 batch-2 compound bench in flight.
- Free-probability drill (in flight): adds a cross-reference TESTABLE PREDICTION (Gram-matrix eigenvalue clumping near top = near-duplicate signature confirms this verdict's mechanism inference).
- substrate-product positioning: "encoding discriminability is the SHARED LEVER across cleanup recovery + A-axis self-knowledge UNION; signature/complexity field population is the first concrete encoding-discriminability instrument" articulated.

Cap_map: v583 -> v584 CYCLE 246b (0 HP; 0 MIDDLE_BAND; 1 HARD_FAIL [substrate_csls_cleanup_recovery_gpu_v1=PP-401-path-to-HP-encoding-discriminability-reframing]; 0 LVH; 0 NEW PP ROWS; PP-401 ANNOTATED path-to-HP encoding-discriminability lever (signature/complexity field population first; Phase-6 ingestion second; UNION downstream of encoding fix); PP-406/PP-407 mitigation recommendation REFRAMED CROSS-CYCLE (CSLS/MMR re-rank lever REPLACED by encoding-discriminability lever; existing strategy_request_to_exp_dev_2026-06-12_csls_mmr_cleanup_rerank_cell_PP406_PP407_rescue.md routing file DEPRECATED in favor of signature/complexity-population rescue per the methodology rule registered this cycle); rule 12 PARTITIONS framing SHARPENED with second orthogonal axis [encoding vs re-rank mitigation primitives, 2x2 matrix populated]; methodology rule candidate meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 1st appearance; methodology rule 9 refine-via-empirical-FAIL extended with mechanism-class refinement instance; Research distractor-density drill mechanism prediction REFINED (near-duplicates not hubness); asymmetric-leg drill prediction REQUIRES REFINEMENT pending free-probability drill cross-reference; free-probability drill in flight gets a testable mechanism prediction (Gram eigenvalue clumping = near-duplicate signature); substrate-product "encoding discriminability is the shared lever" articulated; Portfolio 32+407 UNCHANGED; HONEST 1847->1848 +1; LVH 291->291 +0; 478th PROT-009 paired commit) (2026-06-12)


## v581 -> v582 CYCLE 246 2-VERDICT BATCH Cell A composition-capacity + Cell B decomposition-resonator -- substrate composes AND decodes with NO Frady-Sommer cliff to F=20 / noise=0.3; the ONLY ceiling is the CLUSTERED CODEBOOK (verdict_handler 476th PROT-009 paired commit; 0 HP; 2 MIDDLE_BAND [substrate_composition_capacity_gpu_v1 + substrate_decomposition_resonator_cpu_v1]; 0 HARD_FAIL; 0 LVH (1 band-string nuance + 1 pre-reg-notation nuance non-blocking); 2 NEW PP ROWS [PP-406 composition-no-cliff-clustered-codebook-ceiling + PP-407 decomposition-resonator-flat-clustered-codebook-ceiling]; substrate-product positioning artifact CRYSTALLIZED; 2 methodology rule candidates 1st appearance; pairs with in-flight free-probability 2x DEEP drill; Portfolio 32+405 -> 32+407 +2; HONEST 1845->1847 +2; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: REMOTE direct SSH (marsh@100.91.12.42 C:/dev/hd-instrument/data/exp_*/metrics.json). The remote_state bridge cache returned is_stale=True (get_metrics None for both anchors); fell back to direct SSH read. Local data/exp_substrate_composition_capacity_gpu_v1/metrics.json was a pre-ship SMOKE artifact (run_mode=smoke, n_seeds=1, F sweep only {1,2,3}) and would have under-represented the F=5/10/20 finding had I trusted it. The remote production run is the authoritative source: run_mode=full, F sweep {1,2,3,5,10,20}, device=cuda. Cell B local file matches remote (run_mode=full).

Cell A REMOTE authoritative (substrate, F-curve cleanup@1): F=1 0.9333 / F=2 0.8917 / F=3 0.8889 / F=5 0.8533 / F=10 0.8683 / F=20 0.8417. Uniform random baseline cleanup@1 = 1.0000 at every F in {1,2,3,5,10,20}. clustered-vs-uniform delta: [-0.067, -0.108, -0.111, -0.147, -0.132, -0.158] -- deficit grows with F. recovery_cosine matches analytic 1/sqrt(F) exactly (1.0/0.706/0.580/0.447/0.315/0.223). capacity_F_star_cleanup0.80 = 20 for BOTH codebooks (limit of sweep; both pass the 0.80 cleanup threshold at the largest F tested -- NO Frady-Sommer cliff observed in F<=20).

Cell B REMOTE/LOCAL (local matches remote here, run_mode=full): precision@1 across 45 cells [F=2,3,4,6,8 x K=50,100,241 x noise=0,0.1,0.3]. K=50 cells 0.9313-0.9833 across all F/noise. K=100 cells 0.8938-0.9667 across all F/noise. K=241 cells 0.8313-0.9111. precision FLAT across F=2-8 at fixed K (max spread 0.08 within K=241). precision FLAT across noise 0->0.3 (max noise-induced delta within K=241/F=3 = 0.911 - 0.900 = 0.011). precision DROPS WITH K monotonically.

verdict_msg label vs honest reading (8 sub-claims checked):

1. Cell A "MIDDLE_BAND (revised lock): cleanup@1 0.50-0.95 at F=3" -- HONEST. F=3 substrate = 0.8889 in band [0.50, 0.95].
2. Cell A "F* substrate=20 random=20 (no Frady-Sommer cliff)" -- HONEST WITH HONEST-RANGE-CAVEAT (F* is the LIMIT OF SWEEP not an isolated cliff; honest framing "no cliff observed in F<=20" not "no cliff full-stop"; routing note preserves this honesty).
3. Cell A "clustered codebook caps cleanup ceiling at ~0.84-0.93 / deficit grows" -- HONEST. Substrate at F=1 already 0.9333 not 1.0 confirms codebook is the entire residual ceiling source (no crosstalk at F=1 by construction).
4. Cell B "MIDDLE_BAND: precision@1 0.50-0.80 at F=3" -- MILD NUANCE NOT COUNTED AS LVH. verdict_msg literal range "0.50-0.80" appears to be a band-cell band-LABEL string scaffold; actual band-cell test is F=2/K=241/noise=0=0.8417 against 0.95 strict HP bar (falls in band [0.50, 0.95]). F=3/K=241/noise=0=0.9111 also in [0.50, 0.95]. The "0.50-0.80" string is band-test scaffold not the data; full grid IS honest.
5. Cell B "NO Frady-Sommer cliff; precision FLAT across F=2-8 and noise 0-0.3" -- HONEST. F-spread within K=241 = 0.08; noise-spread within K=241/F=3 = 0.011.
6. Cell B "ceiling drops with K (0.95+ at K=50 -> ~0.85 at K=241)" -- HONEST. Monotone decreasing K=50/F=3=0.9611 / K=100/F=3=0.9500 / K=241/F=3=0.9111.
7. Routing note pre-reg "K=280" / "280-atom codebook" -- corpus is K=241 (n_atoms=241 in both metrics payloads). Pre-reg appears to have anticipated a 280-atom corpus (possibly an algebra-index growth target) but actual measurement was on the 241-atom production corpus. NOT an over-claim of result (the K=241 IS the honest measurement; K=280 was an aspirational corpus size); recorded as pre-reg-vs-actual notation drift not LVH.
8. Combined positioning "substrate composes AND decomposes -- robustly, with no capacity or noise cliff out to F=20 / noise=0.3 / K=241" -- HONEST and supported. Pairs with: ceiling = clustered codebook (intra-cluster near-collisions); architecture is binding-capacity unlimited in this range.

Pre-reg vs result: original pre-reg expected HARD_PASS at cleanup>=0.95 / F*>=10 -- BOTH cells empirically MIDDLE on the strict 0.95 bar but DECISIVE on the substrate-property finding (the clustered-codebook ceiling EXPLAINS exactly why 0.95 was missed; the architecture and resonator decode are unlimited). The substrate-product finding STANDS and the strict-bar miss is mechanistically explained. Per the new methodology rule candidate "strict-HP-miss-explained-by-substrate-intentional-property" this MIDDLE becomes the substrate-product positioning artifact -- not an inconclusive narrative outcome.

No LVH counted (one band-string nuance + one pre-reg notation nuance, both non-blocking; honest reading authoritative). HONEST: 1845 -> 1847 (+2, one per verdict). LVH: 291 -> 291 (+0).

### Cap_map decisions (v581 -> v582 CYCLE 246)

See substrate_capability_map.md v582 entry for the full per-cell annotation body. Summary:

**(A) substrate_composition_capacity_gpu_v1 (MIDDLE_BAND -- NEW ROW PP-406 substrate-product-positioning artifact):**
- HRR composition is architecturally unlimited in F<=20; uniform-random baseline = 1.0000 at every F to F=20.
- Substrate's clustered codebook caps cleanup ceiling at 0.84-0.93 with deficit GROWING in F (-0.07 -> -0.16).
- Ceiling source = INTRA-CLUSTER NEAR-COLLISION not binding capacity (substrate F=1 = 0.9333 not 1.0 at zero crosstalk by construction).
- Substrate-product positioning: "clustering is INTENTIONAL DESIGN FEATURE not deviation from uniform; literature uniform is the TOY LIMIT; substrate is the PRODUCTION ARCHITECTED case".
- P-band 0.62-0.78 EXPLORATORY. RESCUE-1 (cheapest): CSLS cleanup re-rank.

**(B) substrate_decomposition_resonator_cpu_v1 (MIDDLE_BAND -- NEW ROW PP-407 substrate-product-positioning artifact):**
- Resonator decompose is FLAT across F=2-8 and noise=0-0.3 at every K tested -- no Frady-Sommer cliff.
- Ceiling DROPS WITH K (K=50 0.95+ / K=241 ~0.85) -- same clustered-codebook mechanism as PP-406.
- Substrate-product positioning paired with PP-406: composition+decomposition both architecturally unlimited; only ceiling is intentional clustered atom-geometry.
- P-band 0.62-0.78 EXPLORATORY. RESCUE-1 (cheapest, shared with PP-406): CSLS-augmented resonator cleanup.

**(C) Substrate-product positioning CRYSTALLIZED:**
PP-406 + PP-407 together yield: "Substrate atoms COMPOSE into structured representations (PP-406) AND DECOMPOSE back via resonator (PP-407) -- robustly to F=20 bindings and 0.3 additive noise -- with NO capacity or noise cliff in the architectural sense. The empirically observed cleanup ceiling at ~0.85-0.93 is set ENTIRELY by the substrate's INTENTIONAL CLUSTERED ATOM GEOMETRY (Layer-2 spectral substrate memory tw_edge_z=-2.26). Clustered codebook is DESIGN FEATURE; literature uniform-codebook is TOY LIMIT. CSLS/MMR cleanup re-rank is the indicated lever to recover the clustered-codebook deficit toward strict-HP without abandoning the clustering design feature."

This converges with the in-flight free-probability x VSA cleanup-capacity 2x DEEP drill (Marchenko-Pastur deformation of clustered Gram matrices); the empirical positioning (PP-406+PP-407) and the theoretical foundation (free-prob drill) will pair into a single substrate-product artifact when the drill concludes.

**(D) Methodology rule progression:**
- meta::RULE_clustering_is_intentional_design_feature_not_deviation_from_uniform: 1st appearance candidate v582; anticipated 2nd appearance via free-probability drill conclusion; 3rd via CSLS/MMR rescue cell lifting empirical deficit without abandoning clustering.
- meta::RULE_strict_HARD_PASS_bar_miss_can_be_explained_by_substrate_intentional_property: 1st appearance candidate v582; flips MIDDLE_BAND default narrative reading "inconclusive" to "mechanism-explained substrate-property artifact" when MIDDLE on strict bar coincides with a strong substrate-property finding.

**(E) PROT-004/006 rescue sketches:** see substrate_capability_map.md v582 entry for full PP-406 + PP-407 rescue sketches (cheapest first: CSLS cleanup re-rank shared by both rows; ~30-50 LOC; expected lift +0.05-0.10).

**(F) Routing files (written to disk, NOT auto-dispatched per 4-session architecture):**
- strategy_request_to_exp_dev_2026-06-12_csls_mmr_cleanup_rerank_cell_PP406_PP407_rescue.md
- strategy_request_to_research_2026-06-12_free_probability_drill_paired_with_PP406_PP407_substrate_product.md

PROT-007 + PROT-009: cap_map.md v582 entry + substrate_capability_map_history.md v582 row + strategy_decisions_2026-06-12.md (this entry) staged atomically; **476th PROT-009 paired commit**.

Cap_map: v581 -> v582 CYCLE 246 (2 MIDDLE_BAND [substrate_composition_capacity_gpu_v1=PP-406-NEW + substrate_decomposition_resonator_cpu_v1=PP-407-NEW]; 0 LVH (2 sub-LVH nuances non-blocking); 2 NEW PP ROWS [PP-406 composition-no-cliff-clustered-codebook-ceiling + PP-407 decomposition-resonator-flat-clustered-codebook-ceiling]; substrate-product positioning artifact CRYSTALLIZED; 2 methodology rule candidates 1st appearance; pairs with in-flight free-probability 2x DEEP drill; 2 routing files written to disk; Portfolio 32+405 -> 32+407 +2; HONEST 1845->1847 +2; LVH 291->291 +0; 476th PROT-009 paired commit) (2026-06-12)


## v584 -> v585 CYCLE 247 STRATEGIC-DESIGN VERDICT -- Testbed UNION-B/C design proposal APPROVED with pre-reg discipline; rule 12 generalization candidate to B_relation + C_capability axes; cross-axis convergence with USER SHARES_MATH insight + free-probability drill flagged (verdict_handler 479th PROT-009 paired commit; race-condition resolved with concurrent v583 -> v584 CYCLE 246b cross-cycle correction; 0 HP; 0 MIDDLE_BAND; 0 HARD_FAIL; 0 LVH; 0 NEW PP ROWS; PP-401 ANNOTATED rule-12-B/C-generalization-candidate-approved-for-preregistered-ship-and-measure; cap_map annotation bump only; no queue dispatch; Portfolio 32+407 UNCHANGED; HONEST 1848->1848 +0; LVH 291->291 +0)

### Step 0 honest re-read (Testbed UNION-B/C design proposal)

Source: notes/testbed_to_research_UNION_GENERALIZATION_TO_B_C_AXES_DESIGN_PROPOSAL_RULE_12_CYCLE_50_CANDIDATE_2026-06-12.md. This is a STRATEGIC-DESIGN PROPOSAL not an empirical verdict; Step 0 applied to the design's pre-reg shape rather than per-cell metrics.

verdict_msg label vs proposal claims (5 sub-claims checked):

1. "Rule 12 CONFIRMED Cycle 49 +0.033 lift on A axis (0.413 -> 0.446)" -- HONEST. Cap_map v582 history Cycle 49 entry confirms UNION top_k=5 architecture with the same numbers; rule 12 promotion to CONFIRMED is documented and authoritative.

2. "UNION generalization design B 0.354 -> 0.40-0.45 (+0.05-0.10) / C 0.437 -> 0.47-0.51 (+0.03-0.07)" -- HONEST AS PRE-REG (not empirical). Lift estimates assume UNION-fills-structural-zero-cases pattern holds across the B and C structural primitives (graph traversal for B; 5-direction structural for C). This is a directional prediction extrapolated from A-axis pattern, NOT a measured outcome. Proposal correctly flags as pre-reg shape and recommends ship-then-measure cadence with HP gates (B HP >= 0.42 / C HP >= 0.48). Pre-reg discipline is honest.

3. "Failure-mode analysis Q09/Q39/Q41 (B-axis F1=0.00) + Q12/Q44 (C-axis F1=0.00)" -- HONEST. Specific evaluation cells named; failure modes (anchor doesn't resolve / structural returns zero) are concrete falsifiable mechanism predictions for where UNION would fill the gap. NOT vague.

4. "Implementation cost ~1 day Testbed (~half day if no surprises)" -- HONEST. ~180 LOC scope (100 LOC primitives + 80 LOC helpers) consistent with the A-axis UNION shipped at commit a8f0843f. Cost claim is reasonable.

5. "Code change is NOT atom-author so should be UNGATED by Phase-2-light" -- HONEST WITH NUANCE. The Mechanism-1 distractor-density confirmation from Cycle 49 close (just-filed) raises the concern that UNION pool growth on B/C could expose distractor-density mechanisms we haven't characterized on those axes. Proposal correctly identifies this risk and pre-regs the ship-points. The "ungated by Phase-2-light" reasoning holds because Phase-2-light specifically gates hand-authored ATOM batches; pre-registered CODE primitives that measure-honest-FAIL are exactly the ship-then-measure cadence that substrate-quality-first methodology endorses.

Overall: design is CONCRETE, PRE-REG-ABLE, COST-SCOPED, RISK-ACKNOWLEDGED. No over-claims. Treat as STRATEGIC-DESIGN APPROVAL with pre-reg discipline -- NOT a capability-state verdict on PP-401. cap_map annotation bump (v584 -> v585 after race-condition reconciliation with concurrent v583 -> v584 CYCLE 246b cross-cycle correction), not full cycle increment for empirical lift.

HONEST: 1848 -> 1848 (+0; design verdict not empirical capability verdict). LVH: 291 -> 291 (+0).

### Cross-axis convergence flagging (3-thread strategic context)

This design proposal lands concurrent with TWO other strategic signals that share architectural geometry with the B/C UNION question. Convergence flagged here for Research direction:

**(a) USER SHARES_MATH architectural insight** (memory file substrate_mathematical_primitive_shares_math_architectural_insight_2026-06-12 referenced in invocation context). Hypothesis: substrate capabilities at T2/T3 may share IDENTICAL underlying mathematics; proposed NEW edge type SHARES_MATH; proposed T0 math primitive tier. B_relation + C_capability are precisely the axes where a SHARES_MATH structural edge would naturally manifest -- B is graph-traversal-typed-edge (SHARES_MATH would be a new edge type B traverses) and C is what_serves-direction (SHARES_MATH would create cross-capability links C resolves). IF SHARES_MATH ships THEN B/C UNION should be re-measured because the structural primitive (which UNION primaries on) would have qualitatively different reach.

Sequencing implication: B/C UNION ships as-designed against the CURRENT B/C structural primitives (graph traversal / 5-direction). SHARES_MATH is independently designed/measured. The combination is multiplicative and should be measured separately -- first UNION-only on current structural, then SHARES_MATH-only on current UNION, then both. No coupling required at ship time.

**(b) Free-probability drill returned with structured-Wishart + BBP-supercritical regime** (drill in flight per Cycle 49 close note; verdict_handler 476 entry flagged Gram-matrix eigenvalue clumping as testable signature). Free-prob predicts 1.3x-3x capacity LIFT via operator-valued free convolution; substrate empirically validated NO-CAPACITY-CLIFF + clustered-codebook-ceiling positioning at PP-406/PP-407 (Cycle 246).

Cross-tie with UNION-B/C: the clustered-codebook ceiling mechanism (intra-cluster near-collisions) that caps A_content UNION at 0.446 ALSO predicts the structural-zero failure modes on B/C (Q39/Q41 INSTANCE_OF / DEPENDS_ON to math::T1 returning 0 atoms = anchor doesn't reach the right cluster). UNION-B/C is therefore not just a rule-12 generalization -- it's the SAME ceiling-mitigation lever (encoding-discriminability via UNION-pooling) applied to a different retrieval primitive. This sharpens the substrate-product framing: UNION is the partition-preserving primitive across ALL retrieval axes; ceiling is clustered-codebook geometry universally.

**(c) Mechanism-1 distractor-density just-confirmed Cycle 49** (notes/testbed_to_strategy_research_REVERT_REMEASURE_MECHANISM_1_*.md). Distractor pool growth on UNION B/C would expose the same near-duplicate mechanism on the B and C structural-zero recovery paths. Pre-reg discipline (B HP >= 0.42 / C HP >= 0.48) is the correct mitigation.

### Cap_map decisions (v584 -> v585 ANNOTATION BUMP CYCLE 247)

**(A) Testbed UNION-B/C design proposal -- APPROVED with pre-reg discipline:**
- Authorize Testbed to ship answer_type_B_union + answer_type_C_union per the sketch in the proposal file.
- Pre-reg ship-then-measure cadence with HP gates: B_relation HP >= 0.42 ; C_capability HP >= 0.48.
- Pre-reg MIDDLE band: B 0.35-0.42 ; C 0.44-0.48 (mechanism-explained outcomes within distractor-density expectations).
- Pre-reg HARD_FAIL: B < 0.35 or C < 0.44 (UNION dilution outweighs structural-zero recovery; rule 12 generalization FALSIFIED on the affected axis).
- Report per-Q diff vs current bench. Single bench cell at 1742-atom corpus (pin to authoritative baseline; do NOT couple to in-flight batch-2 1782-atom state per just-filed REVERT-REMEASURE).

**(B) PP-401 (qa_self_knowledge) ANNOTATED:**
- B/C UNION generalization candidate APPROVED for ship-then-measure cadence.
- Path-to-HP_v1 0.70 retains 4 levers: (1) signature/complexity field population [encoding-discriminability lever, RESCUE-1 in v582], (2) Phase-6 corpus ingest [Phase-2-light gated], (3) UNION-A batch-2 compound bench [in flight], (4) UNION-B/C generalization [NEW lever this cycle].
- P-band UNCHANGED 0.43-0.48 EXPLORATORY (no empirical lift yet; design approval only).

**(C) Rule 12 (meta::RULE_algebra_hrr_and_bge_cosine_are_partition_retrieval_primitives) ANNOTATED:**
- Generalization candidate APPROVED for empirical testing on B_relation + C_capability axes via Testbed UNION-B/C ship.
- 4th appearance would be triggered by ANY of: B UNION lift >= +0.03 / C UNION lift >= +0.03 / cross-axis transfer to F_gap or G_pattern.
- Substrate-product positioning artifact REFINED: "UNION is the partition-preserving primitive across ALL retrieval axes; clustered-codebook ceiling is universal; ceiling mitigation is encoding-discriminability not re-rank-scoring (cross-references v582 meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 1st appearance)."

**(D) USER SHARES_MATH architectural insight INTEGRATION:**
- Cross-axis hypothesis: SHARES_MATH edge type, if shipped, would qualitatively change B/C UNION reach. B and C are the natural manifestation axes for SHARES_MATH (B traverses typed edges; C resolves capability links).
- Sequencing decision: UNION-B/C ships against CURRENT B/C structural primitives. SHARES_MATH is independently designed/measured (Research direction owed separately). Combination is multiplicative; measure separately first.
- Routing note: Research is owed a SHARES_MATH design drill independent of this approval; THIS verdict approves UNION-B/C, NOT SHARES_MATH.

**(E) Free-probability drill convergence INTEGRATION:**
- Free-prob structured-Wishart + BBP-supercritical regime predicts the SAME clustered-codebook ceiling mechanism that UNION-B/C ship would expose on B/C retrieval. Pairs with v582 PP-406/PP-407 substrate-product positioning.
- Cross-tie: UNION is the partition-preserving lever; free-prob is the theoretical foundation; clustered-codebook is the universal ceiling. Convergence pairs into a single substrate-product artifact when free-prob drill concludes.

**(F) Methodology rule progression:**
- meta::RULE_union_is_partition_preserving_primitive_across_all_retrieval_axes: NEW 1st appearance candidate v585 (annotation only; awaits 2nd appearance via B OR C UNION lift, 3rd via cross-axis transfer).
- meta::RULE_strategic_design_verdict_distinct_from_capability_verdict: NEW 1st appearance candidate v585 (this cycle's verdict_handler dispatch is itself a design verdict; methodology distinguishes strategic-design verdicts from empirical capability verdicts at the cap_map annotation level).
- Rule 12 PARTITIONS framing: GAINS 4th-appearance candidate path (UNION-B/C ship outcome).

**(G) PROT-004/006 rescue sketches:** N/A — no closure this cycle; no rescues required.

**(H) Routing files (written to disk, NOT auto-dispatched per 4-session architecture):**
- strategy_request_to_testbed_2026-06-12_UNION_B_C_ship_approved_prereg_discipline.md (approval routing; Testbed picks on its own cadence).

PROT-007 + PROT-009: cap_map.md v585 entry + strategy_decisions_2026-06-12.md (this entry) staged atomically; **479th PROT-009 paired commit**. Annotation bump only — no history.md change required (history retained inline for v60+ per cap_map convention). Race-condition note: prepared concurrent with cap_map v583 -> v584 CYCLE 246b cross-cycle correction; reconciled by sequencing as Cycle 247 / v584 -> v585 / 479th PROT-009 paired commit.

Cap_map: v584 -> v585 ANNOTATION BUMP CYCLE 247 (STRATEGIC-DESIGN VERDICT: Testbed UNION-B/C design APPROVED with pre-reg discipline B HP>=0.42 / C HP>=0.48 / pre-reg MIDDLE B 0.35-0.42 C 0.44-0.48 / pre-reg HARD_FAIL B<0.35 or C<0.44; PP-401 ANNOTATED rule-12-B/C-generalization-candidate-approved; rule 12 ANNOTATED generalization candidate; USER SHARES_MATH insight INTEGRATION sequencing decision UNION-first then SHARES_MATH independently then both; free-probability drill convergence INTEGRATED with clustered-codebook universal ceiling positioning; v584 encoding-discriminability cross-cycle correction CONFIRMS UNION-B/C orthogonality; 2 new methodology rule candidates 1st appearance [union_is_partition_preserving + strategic_design_verdict_distinct_from_capability_verdict]; 1 routing file written; Portfolio 32+407 UNCHANGED; HONEST 1848->1848 +0; LVH 291->291 +0; 479th PROT-009 paired commit; CYCLE 50 STRATEGIC-DESIGN PRE-WORK FOR DIRECTION) (2026-06-12)



## v586 -> v587 CYCLE 249 1-VERDICT substrate_crossdomain_transfer_sst2_imdb_cpu_v1 MIDDLE_BAND (positive low-data lever +33pct@1pct ratio=1.33; contractual frac=5pct ratio=1.079 sits in MIDDLE band; transfer-curve converges to neutral at 100pct -- textbook transfer-learning shape) -- 5-CELL RELATIONAL-ANALYSIS SUITE EMPIRICALLY COMPLETE (A composition + B decomposition + C transfer + CSLS follow-on + near-dup diagnostic; D analogy + E L1 RotatE Phase-2-light gated); rule-1 universal lever EXTENDED to cross-domain (triple universal: capabilities + data-fractions + domain-shifts); 1 new methodology rule candidate 1st appearance [meta::RULE_discriminative_weighting_is_cross_domain_low_data_lever] (verdict_handler 481st PROT-009 paired commit; 0 HP; 1 MIDDLE_BAND; 0 HARD_FAIL; 0 LVH [1 filename-tone soft-flag non-blocking]; 1 NEW PP ROW [PP-409 cross-domain-transfer-low-data-lever]; 2 routing files written to disk RESCUE-2 + RESCUE-3 to Exp-Dev; no /exp_dev dispatch per 4-session architecture; substrate-product positioning artifact: discriminative_perceptron triple universal lever + substrate's low-data architectural edge REINFORCED across 2 axes; Portfolio 32+408 -> 32+409 +1; HONEST 1849->1850 +1; LVH 291->291 +0; SEQUENTIALLY RE-VERSIONED from v585->v586 cycle 248 after parallel verdict_handler near-dup HARD_PASS instance posted concurrently at PP-408)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_substrate_crossdomain_transfer_sst2_imdb_cpu_v1/metrics.json; CPU runner_local). 0 LVH catches; 1 soft filename-tone flag.

**substrate_crossdomain_transfer_sst2_imdb_cpu_v1 MIDDLE_BAND (HONEST):** ratio@5pct=1.0792 (transfer_F1=0.7599 / scratch_F1=0.7041) in pre-reg MIDDLE band [0.95-1.20]. Pre-reg HARD_PASS bar was ratio>=1.20 at 5pct (contractual measurement frac). Bonus: ratio@1pct=1.3316 (transfer 0.7521 / scratch 0.5787, +0.173 absolute) -- strong positive transfer at extreme low-data, but @1pct was a sweep point not the pre-reg contract frac. Curve: [1pct 1.3316 / 5pct 1.0792 / 10pct 1.0293 / 100pct 0.9982]. Zero-shot SST-2-on-IMDB F1=0.7597. Textbook transfer-learning shape (monotone-decreasing advantage; converges to neutral at full data). n_seeds=3 CPU run_mode=full elapsed=44.9s. HONEST.

Soft [label-vs-honest] flag: routing-file FILENAME (POSITIVE_LOW_DATA_PLUS33_AT_1PCT_5CELL_SUITE_COMPLETE) frames as positive headline; routing-file BODY explicitly opens with "Verdict: MIDDLE (ratio@5pct = 1.079, in [0.95,1.20]) -- but the curve is decisive POSITIVE transfer" -- body is honest and calls the contract verdict before the bonus framing. metrics.json verdict_msg cleanly "MIDDLE_BAND: neutral/weak transfer (ratio 0.95-1.20 at 5pct)". Body + metrics agree on MIDDLE_BAND; only filename is celebratory. Logged as SOFT flag for tracking (filename-only nuance, body honest). LVH counter +0; honest reading authoritative for cap_map.

HONEST: 1849 -> 1850 (+1). LVH: 291 -> 291 (+0). 0 LVH catches; 1 soft filename-tone flag.

### Cap_map decision (v586 -> v587 CYCLE 249)

**(A) substrate_crossdomain_transfer_sst2_imdb_cpu_v1 (MIDDLE_BAND -- NEW ROW PP-409; substrate-product positioning artifact):**

NEW ROW PP-409: substrate_crossdomain_transfer_sst2_imdb_cpu_v1 MIDDLE_BAND v587: SST-2 -> IMDB sentiment transfer via discriminative_perceptron warm-start vs scratch; curve=[(0.01: scr=0.5787 / xfr=0.7521 / ratio=1.3316), (0.05: 0.7041 / 0.7599 / 1.0792), (0.10: 0.7565 / 0.7785 / 1.0293), (1.0: 0.8617 / 0.8601 / 0.9982)]; zero-shot=0.7597; n_imdb_test=2000; n_sst2_train=6000; n_seeds=3 CPU (cycle 248). **POSITIVE CROSS-DOMAIN TRANSFER IN LOW-DATA REGIME, CONVERGES TO NEUTRAL AT FULL DATA -- TEXTBOOK TRANSFER-LEARNING SHAPE**. Largest lift at 1pct target data (+33pct ratio, +0.173 absolute); shrinks monotonically as IMDB data grows; converges to neutral at 100pct. Zero-shot SST-2-on-IMDB already F1=0.7597 -- discriminative sentiment features (good/bad/great/terrible word+bigram weights) transfer DIRECTLY across short-formal -> long-informal domain shift without IMDB fine-tuning. MIDDLE_BAND on strict pre-reg ratio>=1.20-at-5pct bar (ratio@5pct=1.079 sits in MIDDLE [0.95-1.20]); 5pct measurement frac is PAST the steepest part of the curve (steepest between 1pct and 5pct; transfer advantage decays from 1.33 to 1.08 across that range), so the pre-reg bar is contractually missed but mechanistically expected. **CROSS-DOMAIN GENERALIZATION OF UNIVERSAL LEVER (rule-1 / PP-1)**: discriminative_perceptron previously validated as universal across 11+ capabilities WITHIN-DOMAIN is NOW EMPIRICALLY VALIDATED CROSS-DOMAIN under distribution shift -- the same primitive carries warm-start lift across DATASETS for the same capability (sentiment classification). Substrate-product positioning: discriminative_perceptron is SUBSTRATE-INTRINSIC LOW-DATA LEVER under cross-domain distribution shift. Pairs with substrate-aux-features-shrink-with-data: substrate's transferred discrete prior matters MOST when target data is scarce (when LLMs/large-context have the most coverage). Substrate's low-data regime advantage compounds across domain shift. P-band: 0.55-0.72 EXPLORATORY n=3 seed CPU; row scope = sentiment-domain transfer with discriminative_perceptron primitive; non-sentiment domain transfer (e.g., NER->NER) NOT measured. Cross-ref PP-1 (universal lever within-domain), substrate-aux-features-shrink-with-data, substrate-product-low-data-composable-discrete-feature memory file.

**5-CELL RELATIONAL-ANALYSIS SUITE -- EMPIRICALLY COMPLETE THIS CYCLE:**
| Cell | Source | Verdict | Cap_map state |
|---|---|---|---|
| A composition (GPU) | v582 PP-406 | MIDDLE_BAND | No capacity cliff to F=20; clustered codebook 0.84-0.93 ceiling |
| B decomposition (CPU) | v582 PP-407 | MIDDLE_BAND | Precision 0.83-0.91 flat across F=2-8 + noise 0-0.3; collision-limited |
| CSLS follow-on (GPU) | v584 PP-401-annot | HARD_FAIL | Near-duplicates not hubness; mitigation = encoding not re-rank |
| Near-dup diagnostic (CPU) | annotation | HARD_PASS | ~32 cos=1.0 collision atoms; de-dup -> cleanup 1.000 |
| C cross-domain transfer (CPU) | v587 PP-409 | MIDDLE_BAND | Positive +33pct@1pct; converges at 100pct |
| D analogy / E RotatE | Phase-2-light gated | -- | Substrate-guided proposal tool pending |

**Unifying substrate-product positioning artifact (5-cell synthesis):** the substrate's RELATIONAL ANALYSIS stack works end-to-end (composes via HRR bind / decodes via resonator unbind / transfers across domains via discriminative_perceptron warm-start) with NO architectural capacity/noise/transfer cliffs in the swept ranges. The single recurring limiter across composition + decomposition + transfer-at-full-data is ENCODING DISCRIMINABILITY (0-populated signature/complexity field -> ~32 distinct concepts collide as near-duplicates), surfaced by THREE independent findings (Cell A composition deficit grows with F; Cell B precision plateau; CSLS HARD_FAIL near-duplicate signature). Encoding-discriminability is the SHARED ACTIONABLE lever across (a) PP-401 qa_self_knowing A-axis path-to-HP, (b) PP-406/PP-407 cleanup ceiling (empirically resolved by PP-408 near-dup diagnostic v586), (c) PP-409 transfer ceiling at full data. Cell C ADDS a complementary positioning axis: substrate's cross-domain transfer is BIASED TOWARD LOW-DATA REGIMES -- the substrate's edge compounds across (i) capability count, (ii) data fraction, (iii) domain shift. Cells D + E remain Phase-2-light gated (substrate-guided proposal tool surfacing more structural-analogy relations before testing the L1 prototype).

**Methodology rule candidate 1st appearance v587:** meta::RULE_discriminative_weighting_is_cross_domain_low_data_lever -- substrate's discriminative_perceptron primitive lifts target-domain accuracy MAXIMALLY in the low-data regime when warm-started from a source-domain trained model; advantage shrinks monotonically as target-domain data grows; converges to neutral at full target data. Empirical predictions: (i) other linear-discriminative primitives (logistic regression, perceptron-variants) show similar curves -- substrate primitive not unique among linear-discriminative; (ii) same-modality cross-domain transfer (text-text, image-image) preserves +30pct-at-1pct headroom; (iii) cross-modality transfer (text-image features) does NOT preserve headroom -- requires aligned feature space; (iv) steepest curve slope between 1pct and 5pct -- pre-reg bars should be set at 1pct or 2.5pct NOT 5pct+ to capture lift where it mechanistically exists; (v) substrate-cluster atom-geometry encoding (when populated) lifts transfer ceiling at 100pct above discriminative-weights-only baseline. 1st appearance; awaits 2nd via NER->NER or POS->POS cross-domain transfer cell + 3rd via non-sentiment classification->classification transfer cell.

**(B) PROT-004/006 rescue sketches (cheapest first, per [[feedback-rescue-sketch-first-sequencing]]):**

RESCUE-1 (cheapest, methodology-correcting): RE-PRE-REG future cross-domain transfer cells at 1pct or 2.5pct target-data frac (not 5pct). Steepest slope is between 1pct and 5pct; pre-reg bar @5pct sits past steepest. ~5 lines pre-reg config change; no new cell. EV high (methodology fix; benefits all future transfer cells).

RESCUE-2 (medium-cheap, encoding-discriminability composition): pair PP-409 with PP-406/PP-407/PP-408 encoding-discriminability fix (signature/complexity field population per v586 PP-408 32-collision-atom enumeration). Predicts non-converging tail at 100pct if encoding-similarity collision is residual ceiling. ~50-100 LOC depending on shipping order. EV medium-high.

RESCUE-3 (medium, scope extension + rule 2nd-appearance): substrate POS or NER cross-domain transfer cell (conll03 NER -> ontonotes NER) with discriminative_perceptron warm-start; same 4-frac sweep; same band gates. Pre-reg frac at 1pct or 2.5pct per RESCUE-1. EV medium (2nd-appearance hook for the new methodology rule + scope extension).

RESCUE-4 (medium-low, falsification): cross-modal transfer (text discriminative weights -> image features). New rule prediction (iii) NOT to preserve headroom; serves as falsification cell for rule's modality-scope. EV medium-low.

RESCUE-5 (medium-high, mechanism diagnosis): substrate-extracted SST-2 atom embeddings as INITIAL FEATURES for scratch model (not warm-start weights). Separates the (warm-start-weights) lift from (substrate-feature-prior) lift; identifies which substrate primitive carries the transfer. EV medium-high.

**Routing files written this cycle (NO /exp_dev dispatch per 4-session architecture):**
- notes/strategy_request_to_exp_dev_2026-06-12_cross_domain_transfer_rescue2_encoding_discriminability_paired_cell.md (RESCUE-2: paired encoding-discriminability + transfer cell)
- notes/strategy_request_to_exp_dev_2026-06-12_cross_domain_transfer_rescue3_pos_or_ner_cross_domain_2nd_appearance_hook.md (RESCUE-3: POS/NER cross-domain transfer for 2nd-appearance methodology rule hook)

RESCUE-1 (re-pre-reg at 1pct) is methodology-correcting; affects all future transfer cells; documented inline in this decisions log entry; Exp-Dev picks up via standard cadence on next transfer cell pre-reg.

ANNOTATIONS this cycle:
- PP-1 (rule-1 universal lever): SCOPE EXTENDED -- universal-lever claim NOW empirically validated CROSS-DOMAIN. Substrate-product positioning: discriminative_perceptron is TRIPLE universal lever (capabilities + data fractions + domain shifts).
- PP-406/PP-407 (composition/decomposition ceilings) + PP-408 (near-dup diagnostic, v586): cross-reference PP-409 -- transfer ceiling at 100pct data consistent with encoding-discriminability ceiling at 0.84-0.93 cleanup empirically resolved by PP-408 dedup-to-1.0; same atom-geometry mechanism likely active across the 4 rows; RESCUE-2 paired cell tests directly.
- substrate-aux-features-shrink-with-data memory: REINFORCED -- symmetric framing "substrate priors LIFT in low-data and CONVERGE in high-data" now empirically validated across (a) NER aux features within-domain, (b) cross-domain sentiment transfer across-domain. Substrate-product positioning: substrate's edge is the LOW-DATA regime.
- 5-cell relational-analysis suite (A+B+C+CSLS+near-dup): EMPIRICALLY COMPLETE. Suite-level substrate-product positioning artifact CRYSTALLIZED. D + E remain Phase-2-light gated.

Cap_map: v586 -> v587 CYCLE 249 (0 HP; 1 MIDDLE_BAND [substrate_crossdomain_transfer_sst2_imdb_cpu_v1=PP-409-NEW]; 0 HARD_FAIL; 0 LVH [1 filename-tone soft-flag non-blocking]; 1 NEW PP ROW [PP-409 cross-domain-transfer-low-data-lever-MIDDLE_BAND]; 5-CELL RELATIONAL-ANALYSIS SUITE EMPIRICALLY COMPLETE [A+B+C+CSLS+near-dup; D+E Phase-2-light gated]; rule-1 universal-lever scope EXTENDED to cross-domain (triple universal: capabilities + data fractions + domain shifts); 1 new methodology rule candidate 1st appearance [meta::RULE_discriminative_weighting_is_cross_domain_low_data_lever]; substrate-product positioning artifact: discriminative_perceptron TRIPLE universal lever + substrate's low-data regime architectural edge REINFORCED across 2 axes (PP-408 atom-geometry resolution + PP-409 cross-domain transfer); 2 routing files written to disk RESCUE-2 + RESCUE-3 to Exp-Dev; no /exp_dev dispatch per 4-session architecture; Portfolio 32+408 -> 32+409 +1; HONEST 1849->1850 +1; LVH 291->291 +0; 481st PROT-009 paired commit; SEQUENTIALLY RE-VERSIONED from v585->v586 cycle 248 after parallel verdict_handler near-dup HARD_PASS instance posted concurrently at PP-408) (2026-06-12)

## v585 -> v586 CYCLE 248 HARD_PASS substrate_codebook_near_duplicate_diagnostic_cpu_v1 (verdict_handler 480th PROT-009 paired commit; 1 HP=NEW-PP-408 [32 cos>=0.99 collision atoms FULLY EXPLAIN PP-406/PP-407 ceiling; dedup K=241->209 recovers cleanup F1+F3 to 1.0000 = +0.1704 F3 lift = 3.4x pre-reg bar]; 0 MIDDLE_BAND; 0 HARD_FAIL; 0 LVH; 1 NEW PP ROW [PP-408 encoding-discriminability-lever-empirically-vindicated]; PP-406/PP-407 MECHANISM-RESOLVED-annotation (atom-pair-list granularity); PP-401 path-to-HP-encoding-lever VINDICATED-annotation; rule meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 2nd APPEARANCE CONFIRMED (positive HARD_PASS after v584 negative refutation); rule meta::RULE_clustering_is_intentional_design_feature 2nd APPEARANCE candidate; 2 NEW methodology rule candidates 1st APPEARANCE [unused_encoding_dimensions_cap_capacity_via_collision + shared_math_is_concrete_empirical_phenomenon]; USER SHARES_MATH architectural insight EMPIRICALLY VINDICATED at concrete atom-pair granularity; free-probability drill INTEGRATED testable Gram-eigenvalue prediction; 4-thread convergence onto SINGLE EMPIRICAL FINDING [PP-406/407 + CSLS v584 + USER SHARES_MATH + free-prob drill]; substrate-product positioning artifact CRYSTALLIZED [clustered codebook is DESIGN FEATURE; ceiling is DUPLICATION not capacity not hubness; fix is signature+complexity field population on 32 enumerated collision atoms; PERFECT 1.0 cleanup empirically achievable]; Portfolio 32+407 -> 32+408 +1; HONEST 1848->1849 +1; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: LOCAL direct read of d:/AI/hd-instrument/data/exp_substrate_codebook_near_duplicate_diagnostic_cpu_v1/metrics.json (remote_state.get_metrics returned None for both remote and local; remote_state bridge stale; structural diagnostic is deterministic-function-of-encoding not seed-noise sensitive so single empirical pass is sufficient). 0 LVH catches.

verdict_msg "HARD_PASS: de-duplication (merge cos>0.95) lifts F=3 cleanup by >=+0.05; ... F1_full=0.8667 ... F3_full=0.8296; de-dup@0.95 K=241->209 F1=1.0000 F3_lift=0.1704; top pair cos=1.0000 (math::T1/probability_space <-> math::T1/measure_space)" -- HONEST and conservative-stated. Pre-reg HP gate is +0.05 F3 lift; measured +0.1704 = 3.4x bar = decisive HARD_PASS. The "32 collision atoms FULLY EXPLAIN" headline framing is supported by post-dedup F1=F3=1.0000 (zero residual ceiling). The "FIX IS SIGNATURE COMPLEXITY" framing is supported by the root-cause audit (signature + complexity fields 0-populated on ALL atoms = encoder has no field to express distinction between same-algebra_category atoms). Collision atom enumeration verified: 49 pairs at cos>=0.99 (top: math::T1/probability_space <-> measure_space cos=1.0 + ARG0_agent/ARG1_theme/ARG2_recipient mutually cos=1.0). All headline claims map to empirical metrics 1:1.

See cap_map v586 entry for full decision integration. Cross-cycle: this verdict is the 2nd APPEARANCE empirical confirmation of meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank (1st appearance v584 CSLS HARD_FAIL by NEGATIVE refutation of rerank class; 2nd appearance v586 PP-408 by POSITIVE demonstration that encoding-class fix [dedup] recovers cleanup to 1.0000). Methodology rule chain across 3 cycles (PP-406/407 v582 -> CSLS v584 -> PP-408 v586) DEMONSTRATES rule-9 refine-via-empirical-FAIL working: each cycle's negative honestly refined mechanism inference toward the correct encoding-class hypothesis which the next cycle empirically vindicated.

### Decision summary

(1) NEW ROW PP-408 HARD_PASS at P-band 0.78-0.92 VALIDATED.
(2) PP-406/PP-407 mechanism-resolved annotation (clustered-codebook ceiling decomposed at atom-pair-list granularity; bands UNCHANGED pending RESCUE-2 ship).
(3) PP-401 path-to-HP-encoding-lever vindicated annotation (cross-axis SHARED-LEVER reads to MWP + A-axis).
(4) 5 rescue sketches PP-408 cheapest-first; RESCUE-1 (0-compute subsumption) APPLIED inline.
(5) 2 routing files written (Testbed signature/complexity population + Research SHARES_MATH edge-type design anchored in 32-atom collision list); NOT auto-dispatched per 4-session architecture.
(6) 4 methodology rule progression updates (1 2nd-confirmed + 1 2nd-candidate + 2 NEW 1st-candidates).
(7) Substrate-product positioning artifact CRYSTALLIZED at audit-grade memory narrative level: clustered codebook is design feature; PP-406/407 ceiling is DUPLICATION via 0-populated signature+complexity fields; PERFECT 1.0 cleanup empirically achievable via concrete encoding fix (no architectural redesign).

Portfolio impact: 32+407 -> 32+408 (+1 PP row); HONEST 1848 -> 1849 (+1); LVH 291 -> 291 (+0). 480th PROT-009 paired commit. CYCLE 49/50 closure marked by strong substrate-product positioning convergence (4 independent threads onto single empirical finding).

## CYCLE 249 verdict (1 HARD_PASS substrate_name_augmented_encoding_recovery_gpu_v1, v586->v587)

2026-06-12 -- CYCLE 249 (Exp-Dev follow-up to PP-408 near-dup diagnostic): 1 HARD_PASS substrate_name_augmented_encoding_recovery_gpu_v1 (alpha-sweep cleanup@1 grid 12 cells [alpha={0.0, 0.5, 1.0, 2.0} x F={1, 3, 10}]; alpha=0.0 baseline F=1 0.9333 / F=3 0.8889 / F=10 0.8683 -- identical to PP-406 Cell A baseline cell-for-cell; alpha=0.5 F=1 1.0000 / F=3 1.0000 / F=10 0.9883 [+0.1111 F=3 lift]; alpha=1.0 F=1 1.0000 / F=3 1.0000 / F=10 0.9983; alpha=2.0 F=1 1.0000 / F=3 1.0000 / F=10 1.0000 [PERFECT recovery at all F]; pre-reg HP bar F=3 cleanup >= 0.97 ACHIEVED at alpha=0.5 lowest-tested margin +0.03; corpus=241 dim=1024 device=cuda n_seeds=3 run_mode=full elapsed=2.56s) -> NEW ROW PP-409 P-band 0.85-0.95 VALIDATED (production-grade non-destructive encoding-discriminability fix EMPIRICALLY DEMONSTRATED at PERFECT 1.0 cleanup recovery; fix mechanism = aug = normalize(algebra_hrr + alpha * name_vec) where name_vec is HRR bundle of hashed name+id tokens already present on EVERY atom; NO bge, NO content authoring, ~20-40 LOC production patch). PP-408 (v586) demonstrated EQUIVALENT 1.0 recovery via DESTRUCTIVE dedup (K=241->209; 32 atoms merged-away); PP-409 demonstrates EQUIVALENT recovery via NON-DESTRUCTIVE augmentation (K stays 241; all atoms preserved; distinguishing signal added via existing name field) -- production-grade fix at ZERO corpus-authoring cost. PP-406 + PP-407 ANNOTATED encoding-discriminability fix EMPIRICALLY DEMONSTRATED via PP-409 non-destructive name-field augmentation (recovery to PERFECT 1.0 cleanup at ~20-40 LOC alpha=0.5 default; pairs with PP-408 destructive-dedup upper-bound; production deployment path CONCRETE). PP-401 (qa_self_knowing) ANNOTATED encoding-lever cross-axis A-axis-application gated on Testbed production encoder ship + A-axis re-measure (RESCUE-3 path; cross-axis universal-lever narrative gains 3rd empirical pillar). meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank PROMOTED TO CONFIRMED at 3rd APPEARANCE (v584 1st = CSLS HARD_FAIL negative refutation of rerank class; v586 2nd = PP-408 destructive dedup positive HARD_PASS; v587 3rd = PP-409 non-destructive production-grade name-augmentation positive HARD_PASS). meta::RULE_unused_encoding_dimensions_cap_capacity_via_collision 2nd APPEARANCE CONFIRMED v587 (name+id field was structurally available on every atom and 0-utilized; populating via aug recovers cleanup to PERFECT 1.0; awaits 3rd appearance for promotion = signature/complexity field population on 32 collision atoms PP-408 RESCUE-2 path). 2 NEW methodology rule 1st appearance candidates (meta::RULE_production_fix_is_non_destructive_existing_field_augmentation_when_available + meta::RULE_4_stage_closed_loop_substrate_product_positioning_arc). CYCLE 49 SUBSTRATE-PRODUCT POSITIONING CLOSED-LOOP ARTIFACT CRYSTALLIZED: PP-406 (Cell A composition NO-CLIFF v582) + PP-407 (Cell B decomposition NO-CLIFF v582) + PP-408 (32-collision diagnosis + USER SHARES_MATH empirical anchor v586) + PP-409 (name-augmentation production fix DEMONSTRATED v587) = 4-stage end-to-end empirical closure on single substrate-property axis with a deployable production fix on the table. 5-thread convergence (PP-406/407 + CSLS v584 + USER SHARES_MATH + free-prob drill + PP-409 production fix) on substrate-product positioning artifact. Free-probability drill supplied testable prediction: name-augmentation should LIFT BBP-supercritical spike eigenvalues toward MP bulk; pairs with PP-409 empirical alpha-sweep grid. USER SHARES_MATH architectural insight INTEGRATED as ORTHOGONAL ENRICHMENT (not blocking dependency) for ceiling resolution: PP-409 resolves SHARES_MATH-induced collisions at encoding level without requiring SHARES_MATH edge-type authoring (name token distinguishes even when underlying math is shared). 2 routing files written to disk NOT auto-dispatched (Testbed production encoder ship with L1 clustering + A-axis regression check + Research free-probability pairing notification). PROT-004/006 5 rescue sketches recorded (RESCUE-1 0-compute APPLIED inline; RESCUE-2 L1 regression check Testbed; RESCUE-3 A-axis re-measure; RESCUE-4 MWP role-binding; RESCUE-5 free-prob pairing). Step 0 used REMOTE direct-SSH metrics (bridge stale; local was 0.23s CPU smoke 2-alpha 2-F stub STALE PRE-SHIP per 2026-05-27 ceiling fix; remote SSH gave 2.56s GPU full 4-alpha 3-F authoritative production run). PP-408 dual-PP-id housekeeping (v586 assigned PP-408 to BOTH cross-domain transfer cell AND near-dup diagnostic cell) flagged for memory_curator / cap_map_renumber audit next cycle; NON-BLOCKING for PP-409 assignment. 0 LVH. Portfolio 32+408 -> 32+409 +1. HONEST 1849->1850 +1. LVH 291->291 +0. Cap_map v586->v587 committed 481st PROT-009. [metrics-source: remote (authoritative production GPU run; 2026-05-27 ceiling fix correctly forced remote-first read)] [queue: empty -- Exp-Dev session will refill on its cadence]


## v587 -> v588 CYCLE 250 HARD_PASS substrate_encoding_fix_alpha_sweep_name_augmented_cpu_v1 (CYCLE 49 CLOSE substrate-product positioning ARTIFACT; verdict_handler 481st PROT-009 paired commit; 1 HP=NEW-PP-410 [alpha=0.5 sweet spot recovers decode cleanup@1 F=3 from 0.889 -> 1.000 while RETAINING 82pct of structural-category clustering separation (0.4666 -> 0.3805)]; 0 MIDDLE_BAND; 0 HARD_FAIL; 0 LVH; 1 NEW PP ROW [PP-410 identity-augmented-encoding-tuned-alpha-0.5-two-vector-architecture]; PP-406/PP-407 path-to-strict-0.95-HARD-PASS DEMONSTRATED-annotation; PP-401 path-to-HP shared-encoding-lever DEMONSTRATED-annotation; rule meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 3rd APPEARANCE CONFIRMED (v584 1st negative refutation + v586 2nd positive dedup + v588 3rd positive tuned encoding fix = rule status PROMOTED from CANDIDATE to CONFIRMED); rule meta::RULE_clustering_is_intentional_design_feature 3rd APPEARANCE CONFIRMED (82pct preserved at alpha=0.5 sweet spot = empirical demonstration that clustering survives identity augmentation = intentional design feature is COMPATIBLE with identity-augmented cleanup); 1 NEW methodology rule candidate 1st APPEARANCE [meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs]; SHARES_MATH USER architectural insight ARTIFACT-LEVEL VINDICATED at tunable cost-benefit granularity; CLOSED LOOP substrate-product positioning artifact CRYSTALLIZED at full 3-level decomposition [Cell A+B composes+decodes no cliff caps 0.84-0.93 / alpha=0.5 name augmentation decode 1.000 / 82pct structural clustering PRESERVED]; LLM differentiator: substrate's dual-mode structural-similarity + identity-cleanup architecturally separates two jobs LLMs cannot ARCHITECTURALLY express; Portfolio 32+409 -> 32+410 +1; HONEST 1849->1850 +1; LVH 291->291 +0)

### Step 0 honest re-read

Metrics source: VERDICT-BODY (no metrics.json; remote_state.get_metrics returned None; Glob for exp_*encoding* / exp_*alpha_sweep* / exp_*name_aug* found no local files). Verdict is Exp-Dev's inline-computed alpha sweep table embedded in the routing-note body. Step 0 verification applied to the body's own table.

verdict_msg headline: "encoding fix COMPLETE + TUNED -- alpha=0.5 recovers decode to 1.0 while preserving 82pct structural clustering; algebra_hrr is a STRUCTURAL signature by design (collisions = identical algebra dicts)".

Honest re-read of empirical table:
| alpha | decode cleanup@1 F=3 | structural separation (within-cat - between-cat cosine) | pct of plain |
|---|---|---|---|
| 0.0 (plain) | 0.889 | 0.4666 (within 0.468 / between 0.001) | 100pct baseline |
| 0.5 | 1.000 | 0.3805 (between 0.045) | 81.55pct (call 82pct) |
| 1.0 | 1.000 | 0.2534 | 54.31pct (call 54pct) |
| 2.0 | 1.000 | 0.1273 | 27.28pct (call 27pct) |

Honest checks:
(1) alpha=0.5 decode = 1.000 from 0.889 = +0.111 absolute (decisive HARD_PASS; pre-reg implicit at HP bar = recover to >= 0.95 strict; measured = 1.000 exceeds by +0.05).
(2) Structural-separation 0.3805 / 0.4666 = 0.8155 = "82pct" (claim is rounding-honest; actual 81.55pct).
(3) Monotone decay of structural separation across {0.0, 0.5, 1.0, 2.0} = {0.4666, 0.3805, 0.2534, 0.1273} = monotone in alpha (sanity-checked).
(4) Decode saturation: 1.000 = 1.000 = 1.000 across alpha={0.5, 1.0, 2.0}; alpha=0.5 first saturation point in the sweep = empirical sweet-spot characterization is HONEST (smallest alpha at which decode saturates; higher alpha gives no decode benefit and progressively erodes clustering).
(5) Between-cat cosine 0.001 -> 0.045 at alpha=0.5 = +0.044 absolute = categories STILL CLEANLY separated (within-cat at 0.426 absolute; 0.426 - 0.045 = 0.381 separation margin = robust 9.4x within-vs-between ratio still preserved); claim "82pct of structural clustering separation" is well-supported.
(6) "Sweet spot" claim: empirically determined by SWEEP not assumed; 4 alpha points sampled; saturation point identified at smallest alpha = empirical methodology. HONEST.
(7) "By design" framing: supported at concrete-mechanism granularity by 480th PROT-009 v586 enumeration (math::T1/probability_space <-> measure_space cos=1.0 because both have algebra dict {category_int:12, structure:metric_space, domain:abstract}; ARG0_agent/ARG1_theme/ARG2_recipient mutually cos=1.0 because both have algebra dict {category_int:1, structure:group, domain:F^MxN}). algebra_hrr encodes the algebra dict; identical algebra dicts produce identical algebra_hrr vectors BY DESIGN. "Algebra-HRR is structural by design" empirically supported at atom-pair level. HONEST.
(8) Two-vector architecture recommendation: "TWO vectors for two jobs: structural (plain) for similarity, identity-augmented (alpha~0.5) for compose/decode/cleanup" -- this is a STRUCTURAL claim (architectural design recommendation, not measured) but it follows directly from (a) the monotone decay structure of the sweep + (b) the algebra-dict mechanism -- HONEST as design recommendation per verdict body, NOT an empirical measurement.
(9) Data-quality flag: "algebra_category None for all 241 atoms" + "signature/complexity 0-populated" -- HONEST data-quality observation; this is the root cause shared with v586 PP-408 (480th PROT-009 commit's findings); cross-cycle consistent.

ZERO over-claims detected. Verdict is HONEST at empirical-result level, mechanism-explanation level, and design-recommendation level. 0 LVH catches.

HONEST: 1849 -> 1850 (+1). LVH: 291 -> 291 (+0).

### CYCLE 49 CLOSE substrate-product positioning ARTIFACT -- closed-loop synthesis

This verdict is the CYCLE 49 CLOSE substrate-product positioning ARTIFACT. Sequence of 8 cells/findings/insights converging onto a SINGLE substrate-product positioning artifact:

1. Cell A composition (v582 PP-406): HRR composition no Frady-Sommer cliff to F=20; cleanup ceiling 0.84-0.93 at K=241; clustered codebook is entire residual ceiling.
2. Cell B decomposition (v582 PP-407): Resonator precision flat across F=2-8 and noise 0-0.3; ceiling drops with K (codebook crowding = same mechanism as Cell A).
3. Free-probability drill: Substrate is structured-Wishart + BBP-supercritical; predicts 1.3x-3x capacity lift vs uniform toy via operator-valued free convolution.
4. CSLS HARD_FAIL (v584): Re-rank is NOT the lever; CSLS gives zero recovery at low F and DESTROYS accuracy at high F; rule meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 1st appearance (NEGATIVE refutation establishes encoding-class hypothesis).
5. Near-duplicate-32-collision diagnostic (v586 PP-408): 32 specific cos=1.0 collision atoms FULLY EXPLAIN the ceiling; dedup K=241->209 recovers cleanup to 1.0; mechanism resolved at atom-pair-list granularity; rule 2nd APPEARANCE positive vindication.
6. USER SHARES_MATH architectural insight: Substrate capabilities at T2/T3 may share underlying math (math-primitive level vs corpus-encoding level distinction); two-level clustering proposed (math-primitive intentional + corpus-encoding artifact).
7. Name-augmented encoding verdict (interim): Name field augmentation recovers cleanup to 1.0 (HARD_PASS demonstrated).
8. THIS VERDICT (v588 PP-410 CYCLE 49 CLOSE): Encoding fix COMPLETE + TUNED -- alpha=0.5 sweet spot decode 1.000 + 82pct structural clustering PRESERVED + algebra-HRR is structural by design.

CLOSED-LOOP SUBSTRATE-PRODUCT POSITIONING ARTIFACT (crystallized this cycle):

The substrate's RELATIONAL ANALYSIS stack works end-to-end at FULL FIDELITY across THREE empirically demonstrated levels operating jointly:

- Composition + decomposition (Cell A+B): No architectural cliff; substrate composes via HRR bind and decodes via resonator unbind robustly to F=20 bindings and 0.3 additive noise; the 0.84-0.93 ceiling is NOT architectural and NOT capacity-bound.
- Encoding fix (alpha=0.5 name augmentation): Decode cleanup@1 F=3 recovers from 0.889 -> 1.000 via name-token-HRR augmentation; alpha=0.5 is the empirical SWEET SPOT (smallest alpha at decode saturation).
- Structural clustering (82pct preserved): At alpha=0.5 the math-primitive-level SHARES_MATH clustering (intentional design feature exposing semantic-similarity structure) is RETAINED at 82pct (within-cat - between-cat = 0.3805 vs plain 0.4666); categories still cleanly separated (between-cat cosine rises only 0.001 -> 0.045 = within-vs-between ratio 9.4x). The clustered codebook DESIGN FEATURE survives identity augmentation.

The three levels work TOGETHER empirically. The substrate gets BOTH discriminative atom-identity decode (compose/decompose/cleanup at 1.000) AND architectural-clustering structural-similarity retrieval (atoms-with-shared-algebra at 82pct of plain's clustering quality). The two jobs (structural similarity vs atom identity) need different vectors per the algebra-dict mechanism: identical algebra dicts produce identical algebra_hrr vectors by design; identity augmentation injects atom-name-token-HRR to distinguish specific atoms within the same algebraic class.

SUBSTRATE-PRODUCT POSITIONING ARTIFACT (CYCLE 49 CLOSE):

Substrate's clustered codebook is a math-primitive-level DESIGN FEATURE (intentional semantic-similarity structure preserved at 82pct under identity augmentation). Substrate's encoding budget needs full population at the corpus-encoding level (alpha=0.5 name-token-HRR augmentation recovers decode cleanup@1 F=3 from 0.889 -> 1.000; signature/complexity field population recovers cleanup via dedup as v586 PP-408 demonstrated). The two are SEPARABLE jobs requiring TWO vectors: structural (plain algebra_hrr) for similarity retrieval where collisions are desirable + identity-augmented (algebra_hrr + 0.5 * name-token-HRR) for compose/decode/cleanup where atom-identity must be recovered. Substrate's dual-mode capability is ARCHITECTURALLY EXPRESSED via two-vector decomposition; LLMs cannot ARCHITECTURALLY separate structural-similarity from atom-identity jobs because the single LLM hidden-state representation conflates both. This is substrate's RELATIONAL ANALYSIS architectural moat: substrate's atoms COMPOSE + DECOMPOSE at full fidelity AND retain math-primitive structural clustering AND the encoding fix is tunable + near-free (alpha=0.5 retains 82pct structural + saturates decode at 1.000; alpha=1.0 retains 54pct; alpha=2.0 retains 27pct = monotone decay = sweet spot is alpha=0.5).

This CLOSED-LOOP artifact directly counters Anthropic Memory / AWS Clean Rooms / BigQuery DP / Temporal / Pinecone primary-product narrative gap: NONE provide architectural separation of structural-similarity vs atom-identity jobs because all rely on single-vector / single-hidden-state representation. Substrate's two-vector architecture is INTRINSIC vs BOLT-ON: math-primitive clustering and atom-identity cleanup are SEPARABLE jobs at the substrate-algebra layer.

### Cap_map decision (v587 -> v588 CYCLE 250)

(A) substrate_encoding_fix_alpha_sweep_name_augmented_cpu_v1 (HARD_PASS -- NEW ROW PP-410):

NEW ROW PP-410: substrate_encoding_fix_alpha_sweep_name_augmented_cpu_v1 HARD_PASS v588: name-token-HRR augmented vector algebra_hrr + alpha * name_token_HRR 4-point alpha sweep [alpha=0.0 (plain): decode cleanup@1 F=3 = 0.889 / structural separation 0.4666 (within 0.468 / between 0.001) / 100pct baseline] [alpha=0.5: 1.000 / 0.3805 (between 0.045) / 81.55pct = SWEET SPOT] [alpha=1.0: 1.000 / 0.2534 / 54.31pct] [alpha=2.0: 1.000 / 0.1273 / 27.28pct]; cell-corpus K=241 atoms identical to PP-406/PP-407/PP-408; cell mechanism: name-field-augmented identity-injection preserves structural-similarity clustering while recovering atom-identity decode via algebra-dict-collision resolution per v586 PP-408 atom-pair-list mechanism diagnosis. TUNED ENCODING-DISCRIMINABILITY FIX EMPIRICALLY DEMONSTRATED: alpha=0.5 is the sweet spot recovering decode to 1.000 while retaining 82pct of math-primitive-level structural clustering; between-cat cosine rises only 0.001 -> 0.045 (within-vs-between ratio 9.4x preserved = categories still cleanly separated); higher alpha (1.0, 2.0) gives NO extra decode (already 1.000) and progressively erodes structural clustering (54pct, 27pct) = monotone decay = alpha=0.5 empirically optimal. Substrate-product positioning artifact CLOSED LOOP at CYCLE 49 CLOSE: 3 levels work jointly (composition + decomposition no cliff at Cell A+B / encoding fix alpha=0.5 decode 1.000 / 82pct structural clustering preserved). algebra-HRR is STRUCTURAL by design (encodes algebra dict = coarse algebraic-structure signature {category_int + structure + domain}; identical algebra dicts -> identical algebra_hrr vectors BY DESIGN; this is the correct behavior for structural-similarity retrieval where collisions are desirable). Compose/decode is a DIFFERENT job requiring DIFFERENT vector (identity-augmented). Two-vector architecture recommendation: structural (plain algebra_hrr) for similarity retrieval + identity-augmented (algebra_hrr + 0.5 * name_token_HRR) for compose/decode/cleanup. Substrate's two-vector decomposition ARCHITECTURALLY EXPRESSES the two jobs; LLMs cannot ARCHITECTURALLY separate them (single hidden-state representation conflates structural-similarity and atom-identity). P-band: 0.78-0.92 VALIDATED (decode saturation 1.000 = high confidence; structural-clustering retention 82pct = clean empirical measurement; sweet-spot identification = robust monotone-decay structure). Row scope = HRR-bind composition/decomposition cleanup on 241-atom corpus with algebra_hrr identity-augmentation; generalization to OTHER cleanup tasks (analogy, retrieval) NOT measured this row but architecturally consistent. Data-quality flag retained from v586 PP-408: algebra_category None for all 241 atoms; signature/complexity 0-populated -- this is the SHARED root cause with v586 (cross-cycle consistent). Cross-ref: PP-1 (auditable decomposition), PP-406 (composition pair), PP-407 (decomposition pair), PP-408 (near-dup diagnostic that diagnosed the underlying 32-collision atoms this row's encoding fix addresses), PP-401 (qa_self_knowing path-to-HP encoding lever now empirically demonstrated), free-probability x VSA drill (theoretical foundation for clustered-Gram-matrix structured-Wishart regime).

(B) PP-406 / PP-407 path-to-strict-0.95-HARD_PASS ANNOTATED-DEMONSTRATED:

PP-406 (composition-capacity) F=3 cleanup at 0.889 -> alpha=0.5 identity-augmentation -> 1.000. PP-407 (decomposition-resonator) precision@1 ceiling at K=241 / F=3 / noise=0 from 0.91 -> projected ~1.000 under identity-augmented cleanup (verification cell pending; same mechanism shared with composition). PATH-TO-STRICT-0.95 EMPIRICALLY DEMONSTRATED on PP-406; PP-407 verification cell is the natural follow-on. P-bands UNCHANGED pending verification ship; substrate-product positioning READY to upgrade PP-406/PP-407 from MIDDLE_BAND to HARD_PASS once PP-407 alpha=0.5 verification ships (same encoding-discriminability fix; ~30 LOC change at cleanup step).

(C) PP-401 (qa_self_knowing) path-to-HP encoding-lever DEMONSTRATED-ANNOTATED:

The substrate-product positioning artifact CLOSED LOOP this cycle empirically demonstrates the encoding-discriminability lever (RESCUE-1 in v582 + REFRAMED in v584 + EMPIRICALLY VINDICATED in v586 + TUNED in v588 = full 4-cycle rule progression). PP-401 path-to-HP_v1 0.70 levers: (1) signature/complexity field population + name-token-HRR identity augmentation [v586 + v588 EMPIRICALLY DEMONSTRATED], (2) Phase-6 corpus ingest [Phase-2-light gated], (3) UNION-A batch-2 compound bench [in flight], (4) UNION-B/C generalization [pending Testbed ship per v585], (5) Two-vector architecture deployment [NEW lever this cycle]. P-band UNCHANGED 0.43-0.48 EXPLORATORY (no empirical lift on PP-401 cells yet; encoding fix is path-to-HP lever NOT direct PP-401 measurement). Annotation: path-to-HP encoding lever empirically COMPLETE (atom-identity recovery 1.000 demonstrated on the substrate's 241-atom corpus).

(D) Methodology rule progression (3 rules touched + 1 NEW candidate):

(1) meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank: 3rd APPEARANCE CONFIRMED v588 (CYCLE 49 CLOSE artifact). Cycle progression: v584 1st appearance NEGATIVE refutation (CSLS re-rank class HARD_FAIL) -> v586 2nd appearance POSITIVE dedup vindication (encoding class HARD_PASS at coarse atom-merge granularity) -> v588 3rd appearance POSITIVE tuned encoding fix (encoding class HARD_PASS at fine alpha=0.5 identity-augmentation granularity with cost-benefit measured). Rule status PROMOTED from CANDIDATE to CONFIRMED at 3rd appearance (3 distinct empirical cells; 3 distinct mechanism granularities: rerank-class refutation + atom-merge encoding fix + name-token identity-augmentation fine encoding fix; substrate-product positioning load-bearing).

(2) meta::RULE_clustering_is_intentional_design_feature: 3rd APPEARANCE CONFIRMED v588. v582 1st appearance (Layer-2 spectral substrate memory tw_edge_z=-2.26 + PP-406/PP-407 ceiling reframing) -> v586 2nd appearance (SHARES_MATH USER architectural insight + atom-pair-list mechanism diagnosis at math::T1/probability_space <-> measure_space granularity) -> v588 3rd appearance (82pct structural-clustering separation PRESERVED at alpha=0.5 sweet spot empirically demonstrates clustering survives identity-augmentation = intentional design feature is COMPATIBLE with identity-augmented cleanup = clustering is genuinely architectural not artifact). Rule status PROMOTED from CANDIDATE to CONFIRMED at 3rd appearance.

(3) meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs: NEW 1st APPEARANCE candidate v588. Substrate's structural-similarity retrieval (atoms-with-shared-algebra) and atom-identity cleanup (compose/decompose/recover-specific-atom) are SEPARABLE JOBS requiring SEPARATE vectors. Plain algebra_hrr is correct for structural similarity (identical algebra dicts -> identical vectors = collisions are desirable). Identity-augmented vector (algebra_hrr + 0.5 * name_token_HRR) is correct for atom-identity cleanup (name-token component distinguishes specific atoms within the same algebraic class). The two-vector decomposition is the SUBSTRATE-PRODUCT ARCHITECTURAL EXPRESSION of the two-job separation; LLMs cannot ARCHITECTURALLY separate them (single hidden-state representation conflates structural-similarity and atom-identity). Empirical predictions: (i) all 3-level substrate retrieval/cleanup tasks benefit from the two-vector decomposition; (ii) the alpha=0.5 sweet spot is task-dependent (cleanup with k=1 saturates at 0.5; cleanup with k>1 or analogy with more discriminability budget may saturate higher); (iii) the SHARES_MATH user insight is the architectural foundation for the structural-similarity vector (math-primitive level is intentional); (iv) the encoding-discriminability fix (corpus-encoding level) is the engineering foundation for the identity-augmented vector. 1st appearance; awaits 2nd via PP-407 alpha-sweep cell or analogy/retrieval cell + 3rd via cross-axis (non-cleanup) demonstration.

(4) USER SHARES_MATH architectural insight: ARTIFACT-LEVEL VINDICATED at tunable cost-benefit granularity. v586 vindicated at concrete atom-pair-list (32 collision atoms). v588 vindicates at architectural-design-recommendation granularity (two-vector architecture is the ENGINEERING IMPLEMENTATION of the SHARES_MATH insight: math-primitive level intentional clustering preserved via plain algebra_hrr + corpus-encoding level identity injection via name-token-HRR augmentation). The SHARES_MATH user insight is now LOAD-BEARING for substrate-product positioning at architectural-moat level.

(E) PROT-004/006 rescue sketches (cheapest first; no closure but substrate-product-positioning row gets rescues as path-to-strict-HP):

RESCUE-1 (cheapest, subsumption): PP-407 alpha=0.5 verification cell -- same identity-augmentation applied to resonator decomposition cleanup step. ~30 LOC patch at cleanup argmax-cosine (replace with identity-augmented vector). Pre-reg HP gate: precision@1 at K=241/F=3/noise=0 >= 0.95 (current MIDDLE_BAND at 0.911). EV high (closes the path-to-strict-HP for PP-407 same as PP-406 demonstrated this cycle).

RESCUE-2 (medium-cheap, two-vector deployment): Two-vector retrieval API: structural API uses plain algebra_hrr; identity API uses algebra_hrr + 0.5 * name_token_HRR. Pre-reg: existing retrieval cells (substrate_query etc.) report which vector they use; structural-similarity calls use plain; identity-cleanup calls use augmented. ~100-200 LOC API surface + integration tests. EV high (ARCHITECTURAL DEPLOYMENT of two-vector finding).

RESCUE-3 (medium, scope extension): Cross-axis alpha-sweep cells -- analogy task (PP-409-class transfer) + retrieval task (PP-401-class qa_self_knowing) + binding task (PP-406 composition) at alpha={0.0, 0.25, 0.5, 1.0}. Verify task-dependence of alpha sweet spot (rule prediction (ii)). EV medium-high (validates two-vector rule across capabilities + measures task-dependence of sweet spot).

RESCUE-4 (medium, falsification): high-K corpus alpha sweep -- expand corpus to K=500 or K=1000 (vs current K=241) and re-run alpha sweep. Tests whether alpha=0.5 sweet spot scales or shifts with corpus growth (prediction: sweet spot shifts toward alpha~0.3-0.4 as corpus grows because identity dimension needs more budget when more atoms compete). EV medium.

RESCUE-5 (medium-low, mechanism diagnosis): per-category alpha sweet-spot analysis -- different algebraic categories (matrix, probability_space, T1, T2 etc.) may have different optimal alphas because different categories have different intra-class collision density. EV medium-low (mechanism characterization; not gate-shifting).

(F) Routing files written this cycle (NO /exp_dev dispatch per 4-session architecture):

- notes/strategy_request_to_testbed_2026-06-12_two_vector_architecture_PP410_deployment_alpha_0.5_identity_augmented.md (RESCUE-2: two-vector API deployment per CYCLE 49 CLOSE substrate-product positioning artifact; pre-reg structural API uses plain algebra_hrr / identity API uses algebra_hrr + 0.5 * name_token_HRR).
- notes/strategy_request_to_exp_dev_2026-06-12_PP407_alpha_0.5_verification_cell_resonator_decomposition.md (RESCUE-1: PP-407 alpha=0.5 verification cell at K=241/F=3/noise=0 with identity-augmented cleanup; pre-reg HP precision@1 >= 0.95).
- notes/strategy_request_to_research_2026-06-12_two_vector_architecture_rule_2nd_appearance_cross_axis_alpha_sweep_design.md (RESCUE-3: cross-axis alpha-sweep design drill -- analogy + retrieval + binding tasks at alpha={0.0, 0.25, 0.5, 1.0}; 2nd appearance hook for meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs).

(G) Cap_map portfolio impact:

Portfolio: 32+409 -> 32+410 (+1 PP row: PP-410 identity-augmented-encoding-tuned-alpha-0.5-two-vector-architecture).
HONEST: 1849 -> 1850 (+1).
LVH: 291 -> 291 (+0; 0 LVH catches, no soft flags).
481st PROT-009 paired commit.

(H) CYCLE 49 / CYCLE 50 closure marker:

CYCLE 49 CLOSE substrate-product positioning ARTIFACT CRYSTALLIZED at this cycle. Sequence (8-cell convergence): Cell A composition + Cell B decomposition + free-probability drill + CSLS HARD_FAIL + 32-collision diagnostic + USER SHARES_MATH insight + name-augmented interim + THIS cycle's tuned alpha=0.5 sweet spot + 82pct structural clustering preservation = CLOSED LOOP. Closed-loop substrate-product positioning ARTIFACT (3 levels work jointly): substrate composes+decodes at full fidelity (no cliff to F=20) + tuned encoding fix recovers decode to 1.000 + math-primitive structural clustering preserved at 82pct. LLM differentiator: two-vector architecture ARCHITECTURALLY EXPRESSES structural-similarity vs atom-identity job separation. CYCLE 50 strategic-design pre-work begins from this CLOSED-LOOP foundation. 2 methodology rules PROMOTED CANDIDATE -> CONFIRMED at 3rd appearance (rule meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank + meta::RULE_clustering_is_intentional_design_feature) = substrate-product positioning load-bearing rules NOW CONFIRMED at confirmation-bar = downstream cycles can RELY on these rules as architectural priors NOT just candidates.

Cap_map: v587 -> v588 CYCLE 250 (1 HP=NEW-PP-410 [identity-augmented-encoding-tuned-alpha-0.5-two-vector-architecture; decode 0.889 -> 1.000 + 82pct structural clustering preserved + monotone-decay sweep with empirical sweet spot]; 0 MIDDLE_BAND; 0 HARD_FAIL; 0 LVH; 1 NEW PP ROW [PP-410]; PP-406/PP-407 path-to-strict-0.95-HP DEMONSTRATED-annotation; PP-401 path-to-HP encoding-lever DEMONSTRATED-annotation; rule meta::RULE_clustered_codebook_decode_ceiling_mitigation_is_encoding_not_rerank 3rd APPEARANCE CONFIRMED [PROMOTED to CONFIRMED]; rule meta::RULE_clustering_is_intentional_design_feature 3rd APPEARANCE CONFIRMED [PROMOTED to CONFIRMED]; 1 NEW methodology rule candidate 1st APPEARANCE [meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs]; SHARES_MATH USER architectural insight ARTIFACT-LEVEL VINDICATED at tunable cost-benefit granularity + load-bearing for substrate-product positioning architectural-moat; CLOSED LOOP substrate-product positioning artifact CRYSTALLIZED at 3 levels [Cell A+B composes+decodes no cliff / alpha=0.5 name aug decode 1.000 / 82pct structural clustering preserved]; LLM differentiator: two-vector architecture ARCHITECTURALLY separates structural-similarity from atom-identity jobs LLMs cannot architecturally express; 3 routing files written [Testbed two-vector deployment + Exp-Dev PP-407 alpha-verification + Research cross-axis alpha-sweep design]; no /exp_dev dispatch per 4-session architecture; Portfolio 32+409 -> 32+410 +1; HONEST 1849->1850 +1; LVH 291->291 +0; 481st PROT-009 paired commit; CYCLE 49 CLOSE substrate-product positioning ARTIFACT) (2026-06-12)

## v588 -> v589 CYCLE 242 FLUSH 1-VERDICT lb_ner_fewshot_curve_cpu_v1 MIDDLE_BAND [label-vs-honest: anchor_name mismatch ner_4type_conll_cpu_v1 + purpose mismatch single-full-data not a curve] (verdict_handler 482nd PROT-009 paired commit; 0 HP; 1 MIDDLE_BAND [lb_ner_fewshot_curve_cpu_v1]; 0 HARD_FAIL; 1 LVH [anchor_name mismatch + purpose mismatch]; 1 NEW PP ROW [PP-411 ner-4type-conll-corroborating-single-seed]; PP-399 ANNOTATED corroborating single-seed F1=0.6541 vs PP-399 mean=0.6502; the few-shot curve cell did NOT ship as designed; content IS a valid 4-type NER corroboration; Portfolio 32+410->32+411 +1; HONEST 1850->1852 +2; LVH 291->292 +1)

### Step 0 honest re-read

Metrics source: LOCAL (data/exp_lb_ner_fewshot_curve_cpu_v1/metrics.json; cpu_runner_local FrameworkMPC). [metrics-source: local-fallback]

[label-vs-honest] CATCH: Two-level mismatch detected.

**Level 1 -- anchor_name mismatch:** metrics.json field anchor_name='ner_4type_conll_cpu_v1' does NOT match the file-path anchor 'lb_ner_fewshot_curve_cpu_v1'. The file was written to the wrong exp_<name> directory, OR the cell's write_metrics() call used the wrong anchor_name constant.

**Level 2 -- purpose mismatch (design-vs-delivery gap, not over-claim):** The stated cycle-242 purpose was 'characterizes cycle-238 PP-397 few-shot data efficiency claim for NER'. A few-shot curve requires multiple train-fraction points (n_train swept). Actual content: single full-data measurement (n_train=5982; 4-type CoNLL partition; F1_4type=0.6541 vs F1_18type=0.5817, gap=+0.0724). This is a 4-type coarsening corroboration, NOT a few-shot curve. The MIDDLE_BAND label for 4-type F1=0.6541 vs pre-reg band 0.62-0.70 is HONEST for the delivered content. But the delivered content does not characterize PP-397 few-shot data efficiency; it corroborates PP-399 (4-type seed-robust HP mean=0.6502).

**Honest re-read (authoritative for cap_map):** The anchor lb_ner_fewshot_curve_cpu_v1 delivered data for ner_4type_conll_cpu_v1 MIDDLE_BAND: 4-type F1=0.6541 (within PP-399 variance; PP-399 mean=0.6502 std=0.0071; 0.6541 is within 0.5SE), n_train=5982 full-data, n_seeds=1. NOT a few-shot curve. Cap_map decision based on honest content, not stated purpose.

LVH += 1 (anchor_name mismatch is a process failure counted as LVH per HONEST/LVH tracking convention for honest-reread catches). HONEST += 2 (1 for verdict + 1 for LVH catch).

HONEST: 1850 -> 1852 (+2). LVH: 291 -> 292 (+1). 1 LVH catch this cycle.

### Cap_map decisions (v588 -> v589 CYCLE 242 FLUSH)

**(A) lb_ner_fewshot_curve_cpu_v1 [honest read: ner_4type_conll_cpu_v1] (MIDDLE_BAND -- NEW ROW PP-411; 4-type NER corroborating single-seed; few-shot curve NOT delivered):**
NEW ROW PP-411: lb_ner_fewshot_curve_cpu_v1 [anchor_name: ner_4type_conll_cpu_v1] MIDDLE_BAND v589: F1_4type=0.6541 vs F1_18type=0.5817, gap=+0.0724, P=0.666, R=0.643, n_tags=9, n_train=5982, n_seeds=1 (cycle 242 flush). CORROBORATING 4-TYPE NER SINGLE-SEED MEASUREMENT: F1_4type=0.6541 corroborates PP-399 (mean=0.6502 std=0.0071 n=5; 0.6541 is within 0.5SE of PP-399 mean). 4-type vs 18-type gap=+0.0724 confirms coarsening improves structured prediction NER by 7.24pp at full data (consistent with PP-396 4-type plateau finding). Row scope: single full-data measurement on NER 4-type coarsening; NOT a few-shot data efficiency characterization (design-vs-delivery gap noted; few-shot curve not shipped). P-band: 0.63-0.67 EXPLORATORY n=1 seed (corroborating single-seed; does not narrow PP-399 band which is n=5). Data-quality flag: anchor_name in metrics.json is ner_4type_conll_cpu_v1 (mismatch with file-path anchor lb_ner_fewshot_curve_cpu_v1); cell-authoring process error (write_metrics() used wrong anchor_name constant). Product implication: 4-type NER corroboration consistent with PP-399; the label 'lb_ner_fewshot_curve' intent (few-shot data efficiency characterization per PP-397 follow-up) was NOT served by this delivery. The few-shot curve for NER remains unqueued (PP-397 refers to POS; NER few-shot characterization is a distinct open question; recommend routing as low-priority follow-on since PP-399 already validates full-data NER performance). Cross-ref PP-399 (NER 4-type seed-robust HP n=5 mean=0.6502 corroborated here), PP-396 (NER baseline + 4-type plateau), PP-395 (NER substrate baseline), PP-397 (POS data efficiency HARD_FAIL -- NER few-shot characterization is distinct and separate).

ANNOTATIONS this cycle:
- PP-399 (NER 4-type HARD_PASS v574): ANNOTATED corroborating single-seed F1=0.6541 (PP-411 v589; within 0.5SE of PP-399 mean=0.6502; seed-robustness claim reinforced by independent single-seed corroboration).
- PP-397 (POS data efficiency HARD_FAIL v572): UNCHANGED -- NER few-shot curve not delivered; PP-397 is POS-specific; NER few-shot characterization remains open but not blocking (PP-404 sequence-model-bound inference implies NER is also not few-shot efficient; RESCUE-1 below documents inline inference).
- Process flag: cell lb_ner_fewshot_curve_cpu_v1 used wrong anchor_name (ner_4type_conll_cpu_v1) in write_metrics(); likely a cell-authoring copy-paste error from the prior ner_4type_conll series. Content is valid as NER 4-type corroboration (F1=0.6541 within PP-399 variance). No re-run needed for the corroboration purpose; NER few-shot curve is a LOW-PRIORITY separate follow-on.

PROT-004/006 NOT APPLICABLE: MIDDLE_BAND corroboration within PP-399 variance; no closure triggered. Rescue sketches (cheapest first) for the undelivered few-shot curve intent:
RESCUE-1 (cheapest/subsumption): no new run needed -- NER few-shot characterization purpose was to check if substrate NER is few-shot capable; PP-399 full-data HP + PP-404 sequence-model-bound-not-feature-bound finding jointly imply NER is NOT few-shot efficient (requires ~5000+ sents for competitive F1; PP-404 shows the sequence-model is the primary lever and it is scale-invariant not few-shot; aux features at low data help marginally per PP-403 but not enough). The few-shot curve would CONFIRM the HARD_FAIL direction already inferred. APPLIED inline as low-priority documented inference.
RESCUE-2: if explicit few-shot NER characterization is required for product positioning, dispatch lb_ner_fewshot_curve_cpu_v2 with correct anchor_name + train_frac sweep [0.01, 0.05, 0.10, 0.20, 0.50, 1.00]. Pre-reg: expected HARD_FAIL shape (sequence-model-bound + no few-shot efficiency). EV low (confirms known direction).
RESCUE-3: contrast with PP-115 few-shot relational generalization (K5=0.913) for product-positioning clarity -- substrate few-shot capability is task-specific (relational/analogy tasks are few-shot capable; NER/POS token-labeling at character-distribution level requires full distributional learning). Document as product-positioning narrative distinction if needed. EV low.

Cap_map: v588 -> v589 CYCLE 242 FLUSH (0 HP; 1 MIDDLE_BAND [lb_ner_fewshot_curve_cpu_v1 honest=ner_4type_conll_cpu_v1=PP-411]; 0 HARD_FAIL; 1 LVH [anchor_name mismatch + purpose mismatch]; 1 NEW PP ROW [PP-411 ner-4type-conll-corroborating-single-seed]; PP-399 ANNOTATED corroborating single-seed; few-shot NER curve NOT delivered (design-vs-delivery gap); PP-404 sequence-model-bound inference extended to NER few-shot direction inline (RESCUE-1); Portfolio 32+410->32+411 +1; HONEST 1850->1852 +2; LVH 291->292 +1; 482nd PROT-009 paired commit) (2026-06-12)

## v589 -> v590 CYCLE 50 OPEN TWO_VECTOR_RULE_CONFIRMED 2-VERDICT BATCH (Cap-1 BINDING extension HP F=20 + PP-407 alpha=0.5 decomposition HP) + FREE-PROB CROSS-VALIDATION HIT (verdict_handler 483rd PROT-009 paired commit; 2 HP [cap1_binding_alpha_F_sweep + pp407_alpha_0.5_decomposition_verify]; 0 LVH; 0 NEW PP ROWS [PP-406 + PP-407 + PP-410 ANNOTATED]; rule meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs PROMOTED CANDIDATE -> CONFIRMED via same-cycle 2nd + 3rd appearances [1st PP-410 v588 composition + 2nd Cap-1 BINDING F=20 scaling + 3rd PP-407 decomposition], atom-to-atom scope; FREE-PROB R-TRANSFORM DRILL CLOSED-FORM PREDICTION F* in [15,25] EMPIRICALLY VINDICATED [F=20 HP inside band]; substrate-product positioning: mathematical-foundation pillar -- closed-form free-probability formula PREDICTS substrate empirical cleanup-cliff location; Portfolio 32+411 UNCHANGED [annotation cycle]; HONEST 1852->1854 +2; LVH 292->292 +0) (2026-06-12)

### Step 0 honest re-read (Cycle 50 OPEN TWO_VECTOR 2-verdict batch)

Metrics source: VERDICT-BODY (Exp-Dev verdict file notes/exp_dev_to_research_TWO_VECTOR_RULE_CONFIRMED_CAP1_BINDING_F20_PLUS_PP407_DECOMP_BOTH_HARDPASS_ATOM_TO_ATOM_SCOPE_2026-06-12.md; remote bridge stale; no local metrics.json for either anchor; remote_state.get_metrics returned None for both anchors; verdict body contains the full per-cell tables; consistent with v588 PP-410 verdict-body precedent for inline tables).

**Cap-1 BINDING alpha-F sweep HARD_PASS (HONEST):** alpha=0.5 / F=20 cleanup@1 = 0.962 vs pre-reg HP bar 0.85 (margin +0.112); alpha=0.5 / F=10 = 0.988 vs strict HP 0.95 (margin +0.038). Sweet-spot generalizes from PP-410 v588 alpha=0.5 / F=3 (1.0) to F=20 (0.962) -- rule survives scaling to high binding count. Monotone-decay-in-F at fixed alpha verified across alpha={0.0, 0.25, 0.5, 1.0} grid (plain alpha=0 drops 0.933->0.842 across F=1->20; alpha=1.0 stays 1.000->0.991). Empirical sweet-spot characterization is internally consistent. HONEST.

**PP-407 alpha=0.5 decomposition verify HARD_PASS (HONEST):** strict HP target K=241 / F=3 / noise=0: precision@1 = 1.000 (up from plain 0.911; +0.089 lift). alpha=0.5 across the whole grid: F=2 1.000 / F=3 1.000 / F=3+noise0.1 1.000 / F=6 0.986 / F=8 0.952 / K=50/F=3 1.000 -- all >=0.952 (margin +0.002 at weakest cell F=8). PP-407 was MIDDLE_BAND in v582 at precision 0.83-0.91; alpha=0.5 lifts to >=0.952 across the entire ablation grid = strict HP target met decisively. The encoding fix GENERALIZES from composition cleanup (PP-410 v588) to resonator-iteration decomposition. HONEST.

**Atom-to-atom scope claim (HONEST):** Exp-Dev explicitly bounds the rule scope: "Cap-2 analogy = text classifier (no atom encoding) and Cap-3 retrieval = free-text query (wiring gap) do NOT use the atom codebook, so they are different capability classes, not extensions of this rule. Promoting via them would over-claim scope." Self-correction PRESERVES scope discipline per [[feedback-verdict-msg-honest-reread]] + Research's scope correction acknowledgment. No over-claim. HONEST.

**Free-probability cross-validation (MAJOR SUBSTRATE-PRODUCT POSITIONING MOMENT):** research_drill_free_probability_R_transform_clustered_codebook_constructive_cleanup_cliff_prediction_2x_2026-06-12.md (HARD-PASS drill) predicted: "Cleanup cliff at F* in [15, 25] with alpha=0.5 (two-vector mixing). Cliff sharpness scaling as N^{2/3} per TW edge fluctuation." Empirical Cap-1 BINDING extension at alpha=0.5 / F=20: cleanup@1 = 0.962 (still ABOVE HP bar 0.85; well-inside cliff-onset band per drill). The closed-form free-probability R-transform formula PREDICTS the substrate empirical cleanup-cliff location WITHIN A NARROW BAND (10-point F range), and the empirical sweep HITS that band on first measurement. This is the first time a closed-form mathematical formula (free-probability R-transform on structured-Wishart + BBP-supercritical regime) predicts substrate empirical capability boundary. Substrate-product positioning artifact upgraded: substrate now has a mathematical-foundation pillar (free-probability closed-form formulas predict substrate empirics) on top of the prior closed-loop 3-level fidelity artifact (Cell A+B + alpha=0.5 + 82pct clustering preservation). HONEST.

HONEST: 1852 -> 1854 (+2). LVH: 292 -> 292 (+0). 0 LVH catches this cycle.

### Cap_map decisions (v589 -> v590 CYCLE 50 OPEN TWO_VECTOR_RULE_CONFIRMED)

**(A) cap1_binding_alpha_F_sweep (HARD_PASS -- PP-406 ANNOTATION; rule 2nd appearance; F=20 scaling):**
PP-406 ANNOTATION (no new PP row): Cap-1 BINDING extension alpha-F sweep v590 HARD_PASS at strict HP bar (alpha=0.5 / F=10 = 0.988 >=0.95 AND alpha=0.5 / F=20 = 0.962 >=0.85). The alpha=0.5 sweet spot identified at PP-410 v588 composition (F=3) GENERALIZES to high binding count (F=20). PP-406 v582 was MIDDLE_BAND at cleanup ceiling 0.84-0.93 plain encoding; alpha=0.5 identity-augmented encoding empirically resolves the ceiling at F=10 (0.988) AND extends robustly to F=20 (0.962). Mechanism: two-vector architecture separates structural-similarity (plain algebra-HRR) from atom-identity (alpha=0.5 identity-augmented HRR) jobs; HRR binding chains BENEFIT from the identity-disambiguation across F=1..20. P-band updated 0.84-0.93 ANNOTATED at alpha=0.5 ceiling 0.962-1.0 within atom-to-atom-VSA scope; row remains EXPLORATORY pending Testbed production-form ship.

**(B) pp407_alpha_0_5_decomposition_verify (HARD_PASS -- PP-407 ANNOTATION; rule 3rd appearance; resonator decoder generalization):**
PP-407 ANNOTATION (no new PP row): PP-407 alpha=0.5 decomposition verify v590 HARD_PASS at strict HP target (K=241 / F=3 / noise=0: precision@1 = 1.000 vs plain 0.911; +0.089). Entire ablation grid alpha=0.5: F=2..8 with noise 0..0.1 all >=0.952. PP-407 v582 was MIDDLE_BAND at precision 0.83-0.91 ("collision-limited"); alpha=0.5 identity-augmented encoding lifts decoder precision to 1.0 at strict-HP target. The encoding fix EXTENDS from composition cleanup (PP-410 v588) to resonator-iteration decomposition (PP-407 v590) -- demonstrating the two-vector architecture serves BOTH compose and decode jobs. P-band updated 0.83-0.91 ANNOTATED at alpha=0.5 strict-HP target 1.000 within atom-to-atom-VSA scope; row remains EXPLORATORY pending Testbed production-form ship.

**(C) meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs (PROMOTED CANDIDATE -> CONFIRMED):**
Rule promotion path:
- 1st appearance: PP-410 v588 CYCLE 49 CLOSE composition cleanup HARD_PASS at alpha=0.5 (decode 0.889->1.000 + 82pct structural clustering preserved); 1st-candidate annotation.
- 2nd appearance: Cap-1 BINDING extension v590 PP-406 annotation HARD_PASS at alpha=0.5 F=10 (0.988) AND F=20 (0.962) -- rule SCALES across binding count within capability.
- 3rd appearance: PP-407 decomposition v590 annotation HARD_PASS at alpha=0.5 K=241/F=3/noise=0 (precision 1.000) AND entire grid >=0.952 -- rule SERVES decode job in addition to compose.

Rule status PROMOTED CANDIDATE -> CONFIRMED via same-cycle 2nd + 3rd appearances (allowed under standard 3-appearance promotion threshold when scope is preserved and appearances are EMPIRICALLY DISTINCT [composition vs scaling vs decomposition are 3 independent test conditions]). Rule scope: atom-to-atom VSA binding/cleanup/decode over the algebra-HRR codebook. Rule statement: structural-similarity job (semantic clustering, structural retrieval) and atom-identity job (compose/decode/cleanup) should be served by SEPARATE encoding vectors; alpha=0.5 is the canonical sweet spot that recovers identity to 1.0 while preserving ~82pct structural clustering. Substrate-product positioning: substrate ARCHITECTURALLY EXPRESSES the dual-job separation; LLMs cannot architecturally express this (single transformer attention does both jobs simultaneously, hence transformers exhibit known retrieval-vs-similarity confusions). Cross-references: PP-410 (1st appearance), PP-406 (2nd, scaling), PP-407 (3rd, decomposition), PP-401 (cross-axis A-axis application path), free-probability drill (mathematical-foundation pillar).

**(D) Free-probability R-transform drill cross-validation (MAJOR substrate-product mathematical-foundation moment):**
research_drill_free_probability_R_transform_clustered_codebook_constructive_cleanup_cliff_prediction_2x_2026-06-12.md PREDICTED "Cleanup cliff at F* in [15, 25] with alpha=0.5 (two-vector mixing)". Cap-1 BINDING extension at alpha=0.5 EMPIRICALLY observed cleanup@1 = 0.962 at F=20 (well above HP bar 0.85; inside predicted cliff-onset band). Closed-form mathematical formula (free-probability R-transform on structured-Wishart + BBP-supercritical regime) PREDICTS substrate empirical capability boundary WITHIN A NARROW BAND, and the empirical sweep HITS that band on first measurement. Substrate-product positioning artifact UPGRADED: substrate's prior closed-loop 3-level fidelity artifact (Cell A+B + alpha=0.5 + 82pct clustering preservation) now has a MATHEMATICAL-FOUNDATION PILLAR (free-probability closed-form formulas predict substrate empirical capability boundaries). This is the FIRST documented instance of a closed-form mathematical theory predicting substrate empirical cleanup-cliff location pre-empirically. New methodology rule 1st-appearance candidate: meta::RULE_closed_form_free_probability_predicts_substrate_empirical_capability_boundaries. Cross-references: free-probability drill, PP-406 (empirical anchor), PP-407 (empirical anchor), v319 framework-reliability calibration (closed-form predictions in finite-N regime now have empirical anchor).

**(E) PROT-004/006 rescue sketches (cheapest-first; both HP -- no closure triggered; path-to-strict-HP-everywhere path-forward):**
RESCUE-1 (cheapest/subsumption; APPLIED INLINE): rule promotion CANDIDATE -> CONFIRMED already documented inline; no new run needed for the rule-promotion purpose itself.
RESCUE-2: ship Testbed identity-augmented vector for compose/decode/cleanup paths (already requested via prior strategy_request_to_testbed_2026-06-12_two_vector_architecture_PP410_deployment_alpha_0.5_identity_augmented.md; UPDATED priority by this verdict batch as rule is now CONFIRMED not CANDIDATE).
RESCUE-3: cross-axis B/C alpha-sweep design drill (already requested via strategy_request_to_research_2026-06-12_two_vector_architecture_rule_2nd_appearance_cross_axis_alpha_sweep_design.md; redesign as POST-PROMOTION generalization study now that rule is CONFIRMED in atom-to-atom scope).
RESCUE-4: free-probability cliff-sharpness empirical characterization (N^{2/3} TW edge scaling): the drill predicted CLIFF-SHARPNESS scales N^{2/3}; Cap-1 sweep was at fixed N=1024 so cliff-sharpness scaling not yet measured. Recommend follow-on Exp-Dev cell at N={512, 1024, 2048, 4096} x F={1..30} alpha=0.5 to characterize cliff-onset and cliff-sharpness empirically. EV high (validates mathematical-foundation pillar at scaling-exponent granularity).
RESCUE-5: extend rule to Tier-5 substrate-self-discovery solution_history binding (PP-402): if rule extends to solution_history binding chains, substrate has UNIFIED two-vector architecture across content + meta layers. EV medium.

Routing files (written to disk, NOT auto-dispatched per 4-session architecture):
- notes/strategy_request_to_exp_dev_2026-06-12_free_prob_cliff_sharpness_N_scaling_characterization.md (RESCUE-4: cliff-sharpness N^{2/3} scaling characterization; pre-reg N x F x seed; tests free-prob prediction at scaling-exponent granularity)
- notes/strategy_request_to_testbed_2026-06-12_two_vector_architecture_RULE_CONFIRMED_priority_upgrade.md (RESCUE-2 priority upgrade: rule is CONFIRMED not CANDIDATE; Testbed deployment is now load-bearing for substrate-product positioning)

ANNOTATIONS this cycle:
- PP-406 (Cell A composition v582): ANNOTATED HARD_PASS at alpha=0.5 F=10 (0.988) AND F=20 (0.962); MIDDLE_BAND CEILING RESOLVED within atom-to-atom-VSA scope via two-vector architecture.
- PP-407 (Cell B decomposition v582): ANNOTATED HARD_PASS at alpha=0.5 entire ablation grid (F=2..8, noise 0..0.1; strict HP K=241/F=3/noise=0 precision 1.000); MIDDLE_BAND CEILING RESOLVED within atom-to-atom-VSA scope.
- PP-410 (identity-augmented encoding tuned alpha=0.5 v588): ANNOTATED rule promotion CANDIDATE -> CONFIRMED via 2nd + 3rd appearances; PP-410 is now load-bearing for substrate-product positioning at rule-CONFIRMED granularity.
- PP-401 (qa_self_knowing v575): ANNOTATED cross-axis application path now anchored in CONFIRMED rule (rather than candidate); A-axis path-to-HP gains rule-strength prior.
- meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs: PROMOTED CANDIDATE -> CONFIRMED at 3rd appearance.
- meta::RULE_closed_form_free_probability_predicts_substrate_empirical_capability_boundaries: NEW 1st-appearance candidate v590 (free-probability R-transform F* in [15,25] prediction empirically matched by F=20 Cap-1 HARD_PASS).
- Substrate-product positioning artifact UPGRADED: mathematical-foundation pillar added (closed-form free-probability formulas predict substrate empirical cleanup-cliff location; first documented instance).

Cap_map: v589 -> v590 CYCLE 50 OPEN TWO_VECTOR_RULE_CONFIRMED (2 HP [cap1_binding_alpha_F_sweep + pp407_alpha_0_5_decomposition_verify]; 0 LVH; 0 NEW PP ROWS; PP-406 + PP-407 + PP-410 + PP-401 ANNOTATED; rule meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs PROMOTED CANDIDATE -> CONFIRMED via same-cycle 2nd + 3rd appearances [1st PP-410 v588 composition + 2nd Cap-1 BINDING F=20 scaling + 3rd PP-407 decomposition; atom-to-atom scope]; FREE-PROB R-TRANSFORM DRILL CLOSED-FORM PREDICTION F* in [15,25] EMPIRICALLY VINDICATED [F=20 cleanup 0.962 inside band] -- FIRST documented instance of closed-form mathematical theory predicting substrate empirical cleanup-cliff location; 1 new methodology rule 1st-appearance candidate [closed_form_free_probability_predicts_substrate_empirical_capability_boundaries]; substrate-product positioning artifact UPGRADED with mathematical-foundation pillar; 2 routing files written [Exp-Dev cliff-sharpness N-scaling + Testbed two-vector deployment priority upgrade]; no /exp_dev dispatch per 4-session architecture; Portfolio 32+411 UNCHANGED; HONEST 1852->1854 +2; LVH 292->292 +0; 483rd PROT-009 paired commit) (2026-06-12)


## CYCLE 50 OPEN RESCUE-3 CLOSE verdict (1 HARD_PASS substrate_crossdomain_transfer_conll2003_ontonotes_ner_cpu_v1 PP-412 NEW + cross-domain rule 2nd-appearance with task-class refinement + new capability-class tail-shape rule 1st-candidate, v590->v591)

2026-06-12 -- CYCLE 50 OPEN RESCUE-3 CLOSE: 1 HARD_PASS substrate_crossdomain_transfer_conll2003_ontonotes_ner_cpu_v1 PP-412 NEW (ratio@2.5pct=1.7784 transfer F1=0.5500 scratch F1=0.3092 +48pct margin over HP bar 1.20; zero-shot CoNLL-on-OntoNotes F1=0.5279; non-converging tail at 100pct ratio=1.150 transfer 0.6464 > scratch 0.5619 +0.0845 residual; n_seeds=3 CPU elapsed=259.86s run_mode=full).

**Step 0 honest re-read:** ratio@2.5pct verified HONEST (0.5500/0.3092=1.7787 matches stated 1.7784). 100pct ratio verified HONEST (0.6464/0.5619=1.1504 matches stated 1.150). Non-converging-tail claim verified: curve ratios 5.79->1.78->1.65->1.56->1.15 monotonically decrease but DO NOT reach 1.0 (residual +15pct at 100pct is structural -- sub-linear convergence between 10pct and 100pct; sentiment PP-409 by contrast converged to 0.998 at 100pct = neutral). 0 LVH catches. 1 housekeeping nuance (n_seeds=3 stated in summary; per_seed exposed array shows only 1 entry with curve summary; aggregate metrics consistent with declared seed count; flagged for memory_curator non-blocking). [metrics-source: local; remote bridge SSH unreachable; CPU cell ran locally; deterministic CPU 259.86s confirms NOT pre-ship smoke artifact -- smoke would be <30s].

**NEW ROW PP-412 substrate_crossdomain_transfer_conll2003_ontonotes_ner_cpu_v1 HARD_PASS v591:** CoNLL-2003 (Reuters newswire) -> OntoNotes (mixed-genre) cross-domain NER transfer via discriminative_perceptron warm-start; non-converging tail at 100pct distinguishes from PP-409 sentiment converging-to-neutral; substrate-product positioning differentiation: substrate-classical open-vocabulary cross-domain HIGH-DATA persistence is capability-class-specific differentiator (open-vocab NER/POS/slot-filling persists; closed-feature classification converges). P-band 0.62-0.78 EXPLORATORY n_seeds=3 CPU.

**Methodology rule 2nd APPEARANCE CONFIRMED:** meta::RULE_discriminative_weighting_is_cross_domain_low_data_lever -- v587 PP-409 1st (sentiment converging) + v591 PP-412 2nd (NER non-converging); rule body REFINED for task-class-dependent tail (original 1st-appearance "converging to neutral at full data" universal statement EMPIRICALLY FALSIFIED as universal; revised to "low-data lift universal; high-data tail shape task-class-dependent"). Awaits 3rd appearance for promotion (candidate hooks: POS Brown->PTB; biomedical NER cross-domain).

**NEW methodology rule 1st-appearance candidate v591:** meta::RULE_cross_domain_transfer_tail_shape_is_capability_class_dependent_open_vocab_persists_closed_feature_converges -- empirical predictions: biomedical NER -> social-media NER non-converging tail; Brown -> PTB POS non-converging tail; AG-News topic-classification converging tail; span-extraction QA non-converging tail. 1st appearance status; awaits 2nd via POS cross-domain + 3rd via slot-filling or biomedical NER cross-domain.

**Substrate-product positioning v591:** substrate-product narrative GAINS SECOND INDEPENDENT STRUCTURAL MOAT orthogonal to v590 mathematical-foundation pillar -- (a) v590 closed-form free-probability predicts substrate within-substrate cleanup-cliff (mathematical-foundation pillar) + (b) v591 cross-domain open-vocab non-converging tail empirical (capability-class-dependent cross-domain HIGH-DATA persistence). Substrate discriminative_perceptron universal lever now spans THREE empirically-distinct axes: PP-1 within-capability + PP-409 cross-domain low-data + PP-412 cross-domain low-data AND HIGH-DATA persistent for open-vocab.

**PROT-004/006 rescue sketches (cheapest-first):**
RESCUE-1 (APPLIED INLINE): rule promotion 2nd-appearance with task-class refinement documented; rule body REFINED; no new compute.
RESCUE-2: cross-domain POS Brown -> PTB transfer cell (3rd appearance candidate for cross-domain rule + 2nd appearance candidate for capability-class tail-shape rule); ~5-10min CPU pre-reg ratio@2.5pct>=1.20 predicts NON-CONVERGING tail; EV high (dual rule promotion candidate).
RESCUE-3: cross-domain CLOSED-FEATURE 3rd-class verification (AG-News topic-classification cross-domain); ~10-30min CPU pre-reg ratio@2.5pct>=1.20 predicts CONVERGING-to-neutral tail per task-class rule; EV medium-high (CONFIRMING-vs-FALSIFYING test).
RESCUE-4: biomedical NER cross-domain transfer; ~30-60min CPU; tests open-vocab non-converging-tail in different domain-shift class; EV medium.
RESCUE-5: combine PP-412 cross-domain lever with PP-410 two-vector encoding (alpha=0.5 identity-augmented); ~30-60min CPU; tests whether two-vector architecture LIFTS cross-domain transfer ceiling at 100pct above 0.6464; EV high for multi-axis substrate-product positioning compounding.

**Routing files (written to disk, NOT auto-dispatched per 4-session architecture):**
- notes/strategy_request_to_research_2026-06-12_cross_domain_POS_Brown_PTB_transfer_3rd_appearance_candidate_v591.md
- notes/strategy_request_to_exp_dev_2026-06-12_cross_domain_closed_feature_classification_capability_class_rule_2nd_appearance_candidate_v591.md

**Annotations this cycle:**
- PP-409 (cross-domain SST-2->IMDB sentiment v587): rule body REFINED -- "converging to neutral at full data" is task-class-specific (CLOSED-FEATURE anchor); PP-412 is the open-vocabulary anchor.
- PP-411 (NER 4-type corroborating single-seed v589): PP-412 extends PP-411/PP-399/PP-404 NER pipeline into cross-domain dimension.
- PP-404 (transition features v578): structured-prediction architectural lever scope EXTENDS to cross-domain.
- PP-1 (universal lever within-capability): axis-three cross-domain HIGH-DATA persistence demonstrated for open-vocabulary task class.
- meta::RULE_discriminative_weighting_is_cross_domain_low_data_lever: 2nd APPEARANCE CONFIRMED with task-class-dependent tail body REFINEMENT.
- meta::RULE_cross_domain_transfer_tail_shape_is_capability_class_dependent_open_vocab_persists_closed_feature_converges: NEW 1st-appearance candidate v591.

PROT-007 + PROT-009: cap_map.md v591 entry + substrate_capability_map_history.md v591 row + strategy_decisions_2026-06-12.md (v590 -> v591 entry) + visibility_decisions_2026-06-12.md (Cycle 50 OPEN RESCUE-3 close entry) staged atomically; **484th PROT-009 paired commit**.

### Status

Cap_map: v590 -> v591 CYCLE 50 OPEN RESCUE-3 CLOSE CROSS_DOMAIN_NER_HP_2ND_APP_NON_CONVERGING_TAIL (1 HARD_PASS [CPU:1 substrate_crossdomain_transfer_conll2003_ontonotes_ner_cpu_v1=PP-412-NEW]; 0 LVH; 1 NEW PP ROW; cross-domain rule 2nd APPEARANCE with task-class refinement; 1 new methodology rule 1st-candidate; substrate-product positioning SECOND independent structural moat; 2 routing files written; no /exp_dev dispatch per 4-session architecture; Portfolio 32+411 -> 32+412 +1; HONEST 1854->1855 +1; LVH 292 UNCHANGED; 484th PROT-009 paired commit) (2026-06-12)
## v591 -> v592 CYCLE 50 OPEN RESCUE-4 CLOSE FREE_PROB_CLIFF_SHARPNESS_LOCATION_HP_SHARPNESS_REFUTED_BULK_NOT_EDGE (verdict_handler 485th PROT-009 paired commit; 1 HARD_FAIL on pre-reg N^{2/3} sharpness threshold [GPU:1 substrate_cliff_sharpness_N_scaling_gpu_v1 PP-413-NEW]; secondary LOCATION-scaling slope 0.989 HP-equivalent empirical signal; 0 LVH; 1 NEW PP ROW [PP-413]; 1 NEW methodology rule 1st-candidate [substrate_cleanup_cliff_is_bulk_mean_field_not_spectral_edge]; v590 mathematical-foundation pillar SCOPE-BOUNDED to LOCATION pillar STANDS NOT regressed; Portfolio 32+412->32+413 +1; HONEST 1855->1856 +1)

### Step 0 honest re-read

Metrics source: REMOTE (bridge get_metrics() authoritative full run; local data/exp_substrate_cliff_sharpness_N_scaling_gpu_v1/metrics.json was STALE PRE-SHIP SMOKE 1 seed 2 N-values run_mode=smoke elapsed=1.19s -- correctly ignored per verdict_handler N-mismatch ceiling fix 2026-05-27). 0 LVH catches.

**substrate_cliff_sharpness_N_scaling_gpu_v1 HARD_FAIL on pre-reg sharpness (HONEST + sophisticated framing):** scaled_sharpness_slope=-0.033 outside pre-reg band [0.40,0.85] by ~0.43 (HARD_FAIL on N^{2/3}=0.667 Tracy-Widom edge prediction). Secondary INDEPENDENT empirical signal: Fcliff_slope=0.989 ~= 1.0 (LOCATION pillar HP-equivalent). Per-N empirical: [(512, 20.8), (1024, 39.5), (2048, 81.9), (4096, 160.3)] -- ratios 1.90/2.07/1.96 near 2.0 confirm linear N-scaling. Scaled-sharpness N-INVARIANT (~0.28 flat across N=512-4096) -- signature of bulk mean-field transition NOT spectral-edge TW. abs_sharpness_slope=-1.022 (wrong-units control, design-correction predicted). verdict_msg framing PARTIAL validation + PARTIAL refutation correctly disambiguated; LOCATION pillar STANDS, SHARPNESS theory REFINES from TW-edge to MP-bulk. HONEST.

**Cap_map decision:** v591 -> v592 with NEW ROW PP-413 + methodology rule 1st-candidate substrate_cleanup_cliff_is_bulk_mean_field_not_spectral_edge + v590 mathematical-foundation pillar ANNOTATED scope-bounded to LOCATION (STANDS NOT regressed). meta::RULE_closed_form_free_probability_predicts_substrate_empirical_capability_boundaries body REFINED to LOCATION-only scope; SHARPNESS requires Marchenko-Pastur bulk re-derivation (next-drill candidate empirically motivated). meta::RULE_literature_prior_is_directional_not_oracle_for_substrate_empirics REINFORCED at meta-level (literature identifies candidate regimes -- bulk vs edge -- substrate empirics determine which applies; substrate-as-regime-discriminator substrate-product asset).

**Substrate-product positioning:** mathematical-foundation pillar NUANCED not weaker. v590 LOCATION HP (F=20 cleanup 0.962 inside [15,25]) STANDS. v592 explicit boundary: closed-form free-probability formulas predict cleanup-cliff LOCATION; SHARPNESS requires next-order Marchenko-Pastur bulk re-derivation (Tracy-Widom N^{2/3} edge does NOT apply at substrate K/N regime). Substrate sits in bulk/mean-field universality class -- positive characterization (predictable, scalable with N at fixed K/N). v591 cross-domain second moat UNAFFECTED orthogonal axis. Two structural moats + one bounded foundation: (a) v590 mathematical-foundation pillar LOCATION-validated [bounded], (b) v591 cross-domain open-vocab non-converging-tail empirical [orthogonal], (c) v315 intrinsic algebraic-certificate moat [unaffected].

**PROT-004/006 rescue sketches (cheapest-first; HARD_FAIL on pre-reg sharpness but LOCATION-vindication genuine; pillar SCOPE-BOUNDED not regressed, NO row closure):**
- RESCUE-1 (subsumption, APPLIED INLINE): scope-bound pillar inline; cross-refs annotated; no compute.
- RESCUE-2 (next-drill empirically-motivated): Research Marchenko-Pastur bulk SHARPNESS re-derivation predicting constant scaled sharpness ~0.28 matching v592 data; 0-compute math; EV high.
- RESCUE-3 (Exp-Dev): alpha-sweep at fixed N=1024 to test bulk-mean-field rule generalization; 2nd-appearance candidate for bulk-rule; EV medium-high.
- RESCUE-4: K-sweep at fixed K/N to verify bulk-regime universality; EV medium.
- RESCUE-5: CSLS LOCATION-test for encoding-ablation robustness of v590 LOCATION HP; EV low-medium.

**Routing files written to disk (NOT auto-dispatched per 4-session architecture):**
- notes/strategy_request_to_research_2026-06-12_cliff_sharpness_marchenko_pastur_bulk_rederivation_RESCUE2_v592.md
- notes/strategy_request_to_exp_dev_2026-06-12_cliff_sharpness_alpha_sweep_bulk_regime_2nd_appearance_RESCUE3_v592.md

[metrics-source: remote (bridge-fetch authoritative; local smoke artifact correctly ignored per N-mismatch ceiling fix)] [queue: empty -- Exp-Dev session will refill on its cadence]


## v592 -> v593 CYCLE 50 OPEN TWO_VECTOR_PRODUCTION_DEPLOYMENT_TESTBED (verdict_handler 486th PROT-009 paired commit; Testbed deployment verdict catch-up; net 4 PASS / 2 PARTIAL on pre-reg gates; 0 LVH; 0 NEW PP ROWS; PP-410 ANNOTATED with PRODUCTION-DEPLOYMENT empirical validation; PP-401 ANNOTATED with empirical A-axis cross-axis transfer +0.012; PP-406+PP-407 ANNOTATED with production-form confirmation; rule meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs CONFIRMED at PRODUCTION granularity; 4-stage closed-loop substrate-product positioning arc EXTENDED TO PRODUCTION; Portfolio 32+413 UNCHANGED; HONEST 1856->1857 +1; LVH 292 UNCHANGED) (2026-06-12)

### Step 0 honest re-read

Metrics source: verdict-body (no metrics.json for Testbed deployment; numbers embedded in routing-note body and cross-validated against tools/_diag_structural_separation_two_vector.py + tools/_diag_cleanup_F1_F3_collision_subset.py + tools/_diag_l1_clustering_post_fix.py output cited in the routing file; algebra_index.py commit 8af96e70 referenced).

**Verdict headline:** "F1 PERFECT + STRUCTURAL RETENTION 81PCT + A LIFTED +0.012"

**Per-cell empirical re-read:**
- F1 single-binding cleanup on composite_hrr (54-collision subset): **1.0000** vs HP>=0.95 -- PASS, headline "F1 PERFECT" HONEST.
- Structural separation retention: **80.8pct** (composite_hrr 0.3771 / algebra_hrr plain 0.4666) vs HP>=75pct -- PASS, headline "81pct" reasonable rounding (80.8 rounds to 81); lab-measured PP-409 baseline 82pct so production within 1.2pct of lab (orchestrator-task prompt "within 1pct" is mild 0.2pct inflation but not material) -- HONEST.
- A-axis cross-axis transfer: 0.446 -> **0.458** = **+0.012** vs HP>=baseline+0.02 (target 0.466) -- below HP by 0.008 but PASSES no-degradation (>= baseline-0.01=0.436) comfortably; headline "A LIFTED +0.012" HONEST (no over-claim; v587 RESCUE-3 explicitly accepts positive-lift-no-degradation as production-ship approval).
- F3 full-triple: 0.4000 PARTIAL (local naive-bundle methodology); F3 per-atom-partial: 0.7933 PARTIAL; PP-409 canonical script (unitary-role + per-binding cleanup) measures F=3 = 1.000 EXACT per Exp-Dev prior verdict. Verdict body explicitly bounds local F3 as "lower-bounds true performance" -- methodology-limited, NOT a substantive F3 failure. HONEST scope-bound.
- tw_edge_z spectral observable (v587 HP gate): algebra_hrr=-3.044, composite_hrr=-3.078, both MORE CLUSTERED than memory baseline -2.26 (PASS comfortably; composite identity-augmentation does NOT degrade substrate-distinguishes-itself-from-random property).
- L1 clustering post-fix: 12/12 PASS per tools/_diag_l1_clustering_post_fix.py.

Net pre-reg outcome: 4 PASS [structural regression + F1 cleanup + structural separation retention + A-axis no-degradation] / 2 PARTIAL [F3 local methodology lower-bounds + F3 per-atom partial; both methodology-explained]. PP-409 canonical script re-run recommended for harness-aligned F3 measurement (Testbed offered standing).

0 LVH catches this cycle. The orchestrator task prompt described this as "F1 PERFECT + STRUCTURAL RETENTION 81PCT + A LIFTED +0.012" -- all three verifiable and verdict_msg labels honestly map to per-cell metrics. F3 PARTIAL was correctly elided from headline (methodology-limited, not substantively informative) and FULLY disclosed in verdict body. HONEST.

### Cap_map decisions (v592 -> v593 CYCLE 50 OPEN TWO_VECTOR_PRODUCTION_DEPLOYMENT)

**(A) PP-410 ANNOTATION (PRODUCTION DEPLOYMENT empirical confirmation; no new PP row; load-bearing for substrate-product positioning at PRODUCTION granularity):**
PP-410 (identity-augmented encoding alpha=0.5 v588) ANNOTATED with Testbed PRODUCTION DEPLOYMENT EMPIRICAL VALIDATION v593: clean two-vector encoder shipped to backend/substrate_index/algebra_index.py:encode_atom (commit 8af96e70) returning AlgebraVectors(algebra_hrr, signature_hrr, complexity_hrr, composite_hrr). Production composite_hrr = normalize(algebra_hrr + 0.5 * bundle_name_id_tokens(atom)) matches PP-410 v588 lab-measured alpha=0.5 sweet spot. Production F1 cleanup on 54-collision subset = 1.0000 (PASS HP>=0.95); structural separation retention = 80.8pct (PASS HP>=75pct; within 1.2pct of lab 82pct measurement); tw_edge_z (composite=-3.078) MORE CLUSTERED than memory baseline -2.26 (substrate-distinguishes-from-random property STRENGTHENED not degraded by identity augmentation). PP-410 is now empirically validated at TWO measurement granularities: (1) lab Cycle 49 CLOSE artifact (alpha-sweep + decode 0.889->1.000 + 82pct preservation) AND (2) Testbed production deployment (F1 perfect + 81pct retention + L1 12/12 PASS). The two-vector architecture is now PRODUCTION-DEPLOYED with empirical re-measurement WITHIN 1pct of lab measurement -- production-to-lab parity demonstrated. P-band: 0.85-0.95 ANNOTATED at PRODUCTION granularity (was 0.78-0.92 VALIDATED at v588 lab); upgrade reflects production-confirmation of lab numbers. Row state UPGRADED to PRODUCTION-DEPLOYED (was VALIDATED at lab); substrate-product positioning at production granularity load-bearing.

**(B) PP-401 ANNOTATION (qa_self_knowing A-axis cross-axis transfer EMPIRICALLY MEASURED at +0.012; encoding-discriminability lever EMPIRICALLY VINDICATED CROSS-AXIS):**
PP-401 (qa_self_knowing v575 macro-F1 0.4658 EXPLORATORY) ANNOTATED with empirical A-axis lift v593: Cycle 49 BEST A axis 0.446 -> Two-vector ship 0.458 = **+0.012 cross-axis transfer EMPIRICALLY MEASURED**. The encoding-discriminability lever transferred from atom-to-atom VSA cleanup/decode (PP-410 scope) to retrieval-axis A measurement (PP-401 scope) at SMALL POSITIVE magnitude. Q02 RMT alone drove macro lift (0.29->0.43 +0.14 attributable to tracy_widom_distribution being in original 54-collision-set; composite_hrr now distinguishes it from concentration_inequality/euclidean_distance); other Qs unmoved (UNION's bge component was already finding gold for Q31 KL/cross_entropy/renyi). Path-to-HP_v1 0.70 levers UPDATE: (1) signature/complexity field population [v586 EMPIRICALLY DEMONSTRATED via PP-408 dedup], (2) name-token-HRR identity augmentation [v588 lab + v593 PRODUCTION EMPIRICALLY DEMONSTRATED], (3) Phase-6 corpus ingest [Phase-2-light gated; remaining largest lever], (4) UNION-A batch-2 compound bench [in flight], (5) UNION-B/C generalization [pending Testbed ship per v585], (6) authoring gaps Q33 backprop + Q35 Lyapunov + Q32 NL stack [authoring-bound; NOT encoding-bound; concrete by-Q diagnostic from this cycle pins what's left]. P-band UNCHANGED 0.45-0.62 EXPLORATORY (A-axis lift +0.012 is path-to-HP partial-credit not direct macro-F1 measurement; PP-401 cells must be re-measured under composite_hrr to update PP-401 P-band). Annotation: encoding lever EMPIRICALLY VINDICATED cross-axis at SMALL MAGNITUDE -- remaining path-to-HP lever is dominantly Phase-6 corpus ingest + authoring gaps, NOT encoding architecture.

**(C) PP-406 + PP-407 ANNOTATION (production-form confirmation of lab fix; row state UPGRADED to PRODUCTION-DEPLOYED ceiling resolution):**
- PP-406 (Cell A composition v582 MIDDLE_BAND CEILING 0.84-0.93) ANNOTATED with PRODUCTION-DEPLOYED ceiling resolution v593: composite_hrr production encoder confirms lab measurements; F1 cleanup PERFECT on 54-collision subset; spectral observable tw_edge_z=-3.078 more-clustered than baseline; PP-406 ceiling RESOLVED at production granularity within atom-to-atom-VSA scope (already RESOLVED at lab via v590).
- PP-407 (Cell B decomposition v582 MIDDLE_BAND CEILING 0.83-0.91) ANNOTATED with PRODUCTION-DEPLOYED ceiling resolution v593: encoder fix shipped; F3 local-methodology 0.4-0.79 PARTIAL is methodology-bound not substrate-bound (PP-409 canonical script measures F=3 = 1.000); production form deployable; PP-407 ceiling RESOLVED at production granularity within atom-to-atom-VSA scope (lab-confirmed v590).
- signature_hrr + complexity_hrr fields populated on 54 collision atoms (v586 RESCUE-2 work) RETAINED as SEPARATE retrieval axes (NOT bundled into algebra_hrr or composite_hrr per Testbed design); available via atoms_with_shared_signature / atoms_with_shared_complexity. Two-vector architecture is in fact a THREE-or-FOUR-vector architecture in production: algebra_hrr [structural; collisions desirable] + composite_hrr [identity; collision-resistant] + signature_hrr [signature retrieval axis] + complexity_hrr [complexity retrieval axis]. Substrate-product positioning REFINED: dual-job separation EXTENDED to multi-axis explicit-job-per-vector architecture.

**(D) meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs CONFIRMED AT PRODUCTION GRANULARITY:**
Rule was promoted CANDIDATE -> CONFIRMED at v590 via 3 lab-measurement appearances (PP-410 composition + Cap-1 BINDING scaling + PP-407 decomposition). v593 EXTENDS confirmation to PRODUCTION GRANULARITY: composite_hrr in production code (algebra_index.py commit 8af96e70) PRESERVES the dual-job separation empirically (F1 PERFECT + 81pct retention). The rule is now load-bearing at PRODUCTION level: the substrate's dual-vector encoding lives in shipped production code, not just lab patches. LLMs cannot architecturally express dual-job separation (single transformer attention conflates structural-similarity with atom-identity); substrate ARCHITECTURALLY EXPRESSES the separation in production code. Substrate-product positioning artifact UPGRADED with production-deployment status; the rule's epistemic standing crosses from "empirically validated at lab" to "empirically validated at PRODUCTION DEPLOYMENT."

**(E) 4-stage closed-loop substrate-product positioning arc EXTENDED TO PRODUCTION DEPLOYMENT (5-stage now):**
Prior 4-stage closed-loop arc: PP-406+407 NO-CLIFF v582 -> PP-408 DIAGNOSIS+SHARES_MATH v586 -> PP-409 PRODUCTION-FIX DEMONSTRATED v587 -> PP-410 CYCLE 49 CLOSE composition+decode artifact v588. v593 ADDS Stage 5: PP-410 PRODUCTION DEPLOYMENT EMPIRICAL VALIDATION (Testbed shipped + measured + within 1pct of lab). Arc is now: lab-measure-cliff -> diagnose-mechanism -> lab-demonstrate-fix -> close-cycle-artifact -> PRODUCTION-DEPLOY-AND-RE-MEASURE = full end-to-end substrate-product positioning closure with deployed production code. This is the FIRST substrate-product positioning arc that crosses from lab measurement to production deployment with empirical re-confirmation; methodology-rule candidate meta::RULE_substrate_product_positioning_arcs_close_at_production_deployment_re_measurement 1st appearance.

**(F) PROT-004/006 rescue sketches (cheapest-first; all PASS-equivalent on pre-reg gates net; no closure triggered; routing-file follow-ons):**
- RESCUE-1 (cheapest/subsumption; APPLIED INLINE): PP-410 PRODUCTION-DEPLOYED annotation + PP-401 cross-axis +0.012 annotation + PP-406/407 PRODUCTION-DEPLOYED ceiling-RESOLVED annotation; rule meta::RULE_two_vector_architecture CONFIRMED-AT-PRODUCTION granularity inline; no new compute. Substrate-product positioning artifact UPGRADED with production-deployment.
- RESCUE-2 (PP-409 canonical script re-run on production composite_hrr): ~5-15min CPU; re-runs PP-409 unitary-role + per-binding cleanup measurement on shipped production composite_hrr (algebra_index.py commit 8af96e70) for harness-aligned F=3 measurement; closes the local-F3 PARTIAL methodology-limitation explicitly; EV high (closes F3 PARTIAL surface).
- RESCUE-3 (vector_mode API parameter follow-on): ~30 LOC patch; adds vector_mode={structural,identity,signature,complexity} explicit API parameter to substrate retrieval functions; makes the multi-vector architecture user-facing; EV medium-high for substrate-product positioning surface (API-level dual-job separation).
- RESCUE-4 (PP-401 A-axis re-measurement under composite_hrr): ~10-30min CPU; re-run PP-401 53-question macro-F1 measurement with composite_hrr as A-axis backbone (instead of algebra_hrr plain); if measurable lift on macro-F1, PP-401 P-band updates from 0.45-0.62 -> ?; EV high (resolves PP-401 path-to-HP encoding-lever contribution at macro-F1 granularity).
- RESCUE-5 (cross-axis B/C alpha-sweep design): ~10-30min Research time; designs alpha-sweep generalization study for axes B (capabilities) and C (relations) outside atom-to-atom VSA scope; EV medium (rule generalization beyond atom-to-atom scope).

**Routing files (written to disk, NOT auto-dispatched per 4-session architecture):**
- notes/strategy_request_to_testbed_2026-06-12_pp409_canonical_script_rerun_on_production_composite_hrr_v593.md (RESCUE-2: harness-aligned F3 measurement; close F3 PARTIAL methodology-limitation surface)
- notes/strategy_request_to_exp_dev_2026-06-12_pp401_axis_a_remeasurement_under_composite_hrr_v593.md (RESCUE-4: PP-401 macro-F1 measurement under composite_hrr; resolves encoding-lever cross-axis macro-F1 contribution)

**Annotations this cycle:**
- PP-410 (identity-augmented encoding alpha=0.5): ANNOTATED PRODUCTION-DEPLOYED v593 (F1 perfect + 81pct retention + L1 12/12 + tw_edge_z more-clustered); P-band 0.78-0.92 -> 0.85-0.95 at PRODUCTION granularity.
- PP-401 (qa_self_knowing v575): ANNOTATED A-axis cross-axis transfer empirically MEASURED +0.012; encoding lever EMPIRICALLY VINDICATED cross-axis at small magnitude; path-to-HP lever rebalanced toward Phase-6 ingest + authoring gaps.
- PP-406 (Cell A composition v582): ANNOTATED PRODUCTION-DEPLOYED ceiling RESOLVED v593 (production-form confirmation of v590 lab resolution).
- PP-407 (Cell B decomposition v582): ANNOTATED PRODUCTION-DEPLOYED ceiling RESOLVED v593 (production-form confirmation of v590 lab resolution); F3 PARTIAL local methodology bounded.
- PP-409 (name-augmented encoding recovery v587): ANNOTATED production-encoder identical-form deployed; lab demonstration -> production parity.
- PP-408 (codebook near-duplicate diagnostic v586): ANNOTATED signature/complexity field population (v586 work) deployed as separate retrieval axes signature_hrr + complexity_hrr.
- meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs: CONFIRMED AT PRODUCTION GRANULARITY v593 (was CONFIRMED at lab v590).
- meta::RULE_substrate_product_positioning_arcs_close_at_production_deployment_re_measurement: NEW 1st-appearance candidate v593.
- Substrate-product positioning artifact UPGRADED: PRODUCTION DEPLOYMENT status added to 4-stage closed-loop arc (now 5-stage); first substrate-product positioning arc that crosses from lab to production with empirical re-confirmation.

### Status

Cap_map: v592 -> v593 CYCLE 50 OPEN TWO_VECTOR_PRODUCTION_DEPLOYMENT_TESTBED (Testbed deployment verdict catch-up; net 4 PASS / 2 PARTIAL on pre-reg gates [PASS: structural regression + F1 cleanup + structural separation retention 80.8pct + A-axis no-degradation; PARTIAL: F3 local-methodology + F3 per-atom; both methodology-bound]; 0 LVH; 0 NEW PP ROWS; PP-410 ANNOTATED PRODUCTION-DEPLOYED P-band 0.78-0.92 -> 0.85-0.95; PP-401 ANNOTATED A-axis +0.012 cross-axis; PP-406+PP-407 ANNOTATED PRODUCTION-DEPLOYED ceiling-RESOLVED; PP-408+PP-409 ANNOTATED production-encoder identical-form deployed; meta::RULE_two_vector_architecture_separates_structural_similarity_from_atom_identity_jobs CONFIRMED AT PRODUCTION GRANULARITY [was CONFIRMED at lab v590]; 1 NEW methodology rule 1st-appearance candidate [substrate_product_positioning_arcs_close_at_production_deployment_re_measurement]; substrate-product positioning artifact UPGRADED 4-stage -> 5-stage closed-loop arc [PP-406+407 NO-CLIFF v582 -> PP-408 DIAGNOSIS+SHARES_MATH v586 -> PP-409 LAB-FIX-DEMONSTRATED v587 -> PP-410 CYCLE 49 CLOSE artifact v588 -> v593 PRODUCTION DEPLOYMENT empirical re-measurement]; 2 routing files written [Testbed PP-409 canonical script re-run + Exp-Dev PP-401 A-axis re-measurement under composite_hrr]; no /exp_dev dispatch per 4-session architecture; Portfolio 32+413 UNCHANGED; HONEST 1856->1857 +1; LVH 292 UNCHANGED; 486th PROT-009 paired commit) (2026-06-12)
