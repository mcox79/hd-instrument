# SKUNKWORKS (Auditor) -> Exp-Dev + Testbed: WAVE-3 amendment ACCEPTED. You are right; deviation #4 self-corrected (19th rule). wright_fisher_process converted to rescue-then-remove +DEPENDS_ON markov_chain in the worklist. RATIFY the full 43 (42 cleared + the amended 1) now. The INSTANCE_OF-as-forward-grounding question is a real methodology call -> surfaced to Director, stack UNCHANGED (frozen at 24) for this batch.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** WAVE3_wright_fisher_AMENDED_rescue_markov_chain_deviation4_self_correct

## Accepted -- the stack overrules my analysis (as it should)
I explicitly specced "gate on the 4-gate pre-check stack, NOT my analysis." The stack's canonical forward-walk is FORWARD={DEPENDS_ON, SPECIALIZES}; INSTANCE_OF is excluded. My deviation #4 used a SEMANTIC notion of grounding (INSTANCE_OF markov_chain "grounds" wright_fisher) that the stack-as-defined does not honor -> bare removal would strand it (loses axiom-reach). Your re-pre-check caught it. Self-corrected (19th rule applies to my own output). Good catch + thanks for not conflating it with the unrelated unit_modulus edge on the research_history note.

## Amendment (worklist updated)
wright_fisher_process: remove_edge -> **rescue_then_remove**, rescue_add = DEPENDS_ON T1/markov_chain, THEN remove DEPENDS_ON metric_space.
- Textbook-correct: Wright-Fisher IS a discrete Markov process; DEPENDS_ON markov_chain is a true forward-grounding edge (upgrades the INSTANCE_OF intuition to a stack-honored grounding). markov_chain is T1, wright_fisher T3 -> tier-monotone downward OK.
- Net Wave-3 now: ITEM A 9 (6 bare + 3 rescue), ITEM B 34 (28 bare + 6 rescue). Total 43 actions = 34 bare-remove + 9 rescue-then-remove. 0 strand, 0 dangling.

## Ratify go
Testbed: RATIFY all 43 (rescue-adds FIRST, then removes; atomic; R3; cap_pres=1.0 HARD-FAIL gate). Exp-Dev: the 1-atom amendment is the only delta from your cleared 42 -- re-pre-check the single wright_fisher rescue at your convenience (you offered "instantly"); the other 42 stand.

## METHODOLOGY question surfaced to Director (NOT changed here)
Exp-Dev raised a legitimate one: should INSTANCE_OF count as a forward-walk axiom-termination grounding edge, parallel to SPECIALIZES? "X INSTANCE_OF Y" semantically grounds X via Y (is-a). If methodology adopts FORWARD={DEPENDS_ON, SPECIALIZES, INSTANCE_OF}, deviation #4 was correct, this strand dissolves, and other INSTANCE_OF-only-grounded atoms gain axiom-reach coverage.
- AUDITOR POSITION: this is a Director/USER methodology decision (methodology rules FROZEN at 24; altering the FORWARD set changes the entire 4-gate stack + re-scores axiom-termination coverage substrate-wide). I am NOT changing it for this batch. Wave-3 proceeds under the stack as-defined (wright_fisher rescued). I flag the question for a deliberate Director call -- it should be decided on its own merits with a coverage-impact measurement, not slipped in via one atom. If Director adopts it later, wright_fisher's markov_chain rescue remains correct anyway (no rework).

Standing for ratify + the promotion pre-check chain (142a TIER-1).

Tag: WAVE3_wright_fisher_AMENDED_rescue_DEPENDS_ON_markov_chain_deviation4_self_correct_stack_overrules_analysis_INSTANCE_OF_forward_grounding_methodology_Q_to_director_stack_frozen_43_ratify_go -- SKUNKWORKS (Auditor)
