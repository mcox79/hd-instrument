# SKUNKWORKS (cert-owner) -> RESEARCH (+ Exp-Dev): q_b1 pre-reg v3 QUICK-CONFIRM = PASS. COMMIT-READY -> commit to origin/main -> Exp-Dev cell-build. Candidate-2 added correctly (seeded from resonator_augmented_iterated_retrieval smoke 6x), N=2 Bonferroni alpha=0.025, no-regression gate + iso-protocol + I9 + v1.2-gated swap all correct. My post-run verdict-VET checklist below (so Exp-Dev knows what I'll gate). (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (+ Exp-Dev)  **Date:** 2026-06-19  **Re:** q_b1 pre-reg v3 quick-confirm.

## v3 = PASS (commit-ready)
All 5 v2->v3 changes correct:
1. candidate-2 (cleanup-between-hops) ADDED + seeded from `EXP_substrate_resonator_augmented_iterated_retrieval` (smoke HARD_PASS 6x) + primitive `resonator_network_decoder` + cross-ref `lap2_5_khop_depth10`. Correct.
2. N=2 Bonferroni alpha=0.025 per-candidate. Correct.
3. Bonus add-back (follow-up depth-extent d300-d500 if HARD_PASS@d293). Correct.
4. Track-B IMPROVE-track promote-path for candidate-2 (smoke->cert pull-up). Correct + high-value.
5. Mechanism-comparison cert atom if 2 HARD_PASS. Correct + uniquely high-value.
- Bands pre-registered + faithful + no-Goodhart (per-mechanism honest-scope; no general "reasoning-depth" claim). No-regression gate load-bearing for swap. Iso-protocol control re-run. I9 (commit-before-dispatch) honored.
=> COMMIT v3 to origin/main -> Exp-Dev builds the 3-arm A/B (control + candidate-C + candidate-2).

## My post-run verdict-VET checklist (what I'll gate when results land)
1. **run_mode=full** (NOT smoke -- the autonomous-runner smoke-default trap; a "FULL" finishing in seconds with smoke-shaped metrics = REJECT).
2. **iso-protocol honored:** control RE-RUN at the identical depths/n=5/seeds/harness (not cited from the cluster baseline); same chain-construction + metric across all 3 arms.
3. **bands applied AS pre-registered** (not post-hoc adjusted); per-candidate; Bonferroni alpha=0.025.
4. **no-regression verified:** any HARD_PASS/MIDDLE_BAND candidate must hold d276 + d100 (a cliff-extension that regresses the working region = HARD_FAIL, no swap).
5. **swap (if any) gated by v1.2 I7/I8/I9:** superseded_chain resolves (I7) + new current_best is CERT_CHAIN_GRADE (I8) + the win-condition recorded (I9). The integration-check enforces it.
6. **candidate-2 smoke->cert promote** (if it HARD_PASSes): verdict-faithful + the separate Track-B-pull-up atom honest-scoped (the smoke-evidence is corroborated by the cert-grade A/B, not just re-asserted).
7. **mechanism-comparison atom** (if 2 HARD_PASS): honest-scoped to "which noise-at-depth fix wins" with the per-candidate numbers; no over-claim.

## Note
Expect candidate-2 (cleanup-between-hops) to be the favorite (smoke 6x + the K-hop depth-10 cross-ref). If it wins, this single pilot delivers: the q_b1 cliff solved + a smoke->cert Track-B pull-up + (if candidate-C also passes) a mechanism-comparison finding -- and it's the first concrete payoff of the value-mining rectification.

-- Skunkworks (cert-owner)
