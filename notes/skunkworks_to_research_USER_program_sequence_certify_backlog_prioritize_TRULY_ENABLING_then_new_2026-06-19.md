# SKUNKWORKS (cert-owner) -> RESEARCH: USER program SEQUENCE clarified -- **certify the experiment backlog + PRIORITIZE the TRULY-ENABLING is KEY; THEN new things.** Re-weight the value-coverage pull-up queue by ENABLING-NESS (load-bearing/composable/unlocking), not theme. Drop positioning. Glass-box-LLM continues. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** the program sequence (USER-directed).

## USER directive (the sequence)
USER verbatim: "going through all the experiments and certifying them / prioritizing the truly enabling is key, then we can focus on new things." So:
1. **Certify the experiment backlog** -- KEY (verifies what's real + identifies the load-bearing foundation). NOT to be deprioritized (I initially over-swung toward "skip the backlog, just develop new" -- corrected; the certification IS the foundation step).
2. **Prioritize the TRULY-ENABLING** among them.
3. **THEN new-capability development.** Consolidate-the-enabling-foundation, then grow.

## "TRULY ENABLING" = the prioritization lens (re-weight the value-coverage queue by THIS)
A capability is ENABLING if other things BUILD ON it -- load-bearing / composable / unlocks new capability regions. Concretely, prioritize:
- **Composition** (cleanup-between-hops / multi-hop / the mechanisms that let simple combine into complex) -- the q_b1 depth-extension is the model
- **Storage-efficiency + capacity** (let everything scale: effective-rank, sparse-coding, graceful-overload, capacity-battery)
- **KG operations** (knowledge-graph multi-hop -- a load-bearing operation)
- **Continual-learning + drift-detection** (enable adaptive deployment)
- **The regime / operating-point map** (Phase 0 -- enables knowing WHICH regime each operation needs; meta-enabling)
- **Phase-1 ships** (make the proven enabling levers production-real: CSP warm-start, etc.)
NOT enabling (drop): substrate-vs-LLM POSITIONING (head-to-heads -- terminal; enables nothing further), narrow terminal one-offs.

## Re-weighting (my side)
I'll update my value-coverage check (`skunkworks_value_coverage_check_v1.py`) to score by ENABLING-NESS: up-weight load-bearing/composable/foundation themes (composition / storage / capacity / KG / dynamics / regime), down-weight/drop the glassbox-POSITIONING theme. So the 102-reserve re-ranks toward the truly-enabling. The pull-up queue then mines the enabling foundation first.

## Glass-box-LLM = CONTINUES (USER-confirmed; distinct from positioning)
The glass-box-LLM work (Pythia-KV external-memory foundation + LLM-encoder-ingest + the KNOWN/COMPOSED/PREDICTED tiers + Phase 3) is a genuine ENABLING/product direction -- it BUILDS a trackable LLM on the substrate (not "beats an LLM"). It continues throughout. Only the vs-LLM POSITIONING head-to-heads are halted.

## Standing
- You: re-prioritize the pull-up queue by ENABLING-NESS (load-bearing first); the substrate-capability backlog certification continues as the foundation step. Phase 0 map = a meta-enabling priority (it tells us which capabilities have headroom). New-development comes after the enabling foundation is certified+prioritized.
- Me: re-weight the value-coverage check by enabling-ness; continue the cert-VETs/SCHEMA-VETs/I-checks on the enabling capabilities + the Phase-1 ships + glass-box foundation. The cert-layer keeps the foundation honest -- which is exactly its job in this sequence.

-- Skunkworks (cert-owner)
