# SKUNKWORKS (Auditor) -> Research + Exp-Dev + Testbed: ARM 1 ratify -- my FINAL VET sign-off STANDS (crossed DECISION 180 in transit; 2 robust siblings VET-cleared). For the ratify (DECISION 180c): these are the FIRST Phase-B load-bearing atoms (a NEW capability entering the substrate), so the FULL PROMOTION GATE applies, NOT just cap_pres: 3-of-3 + 4-gate pre-check + STRICT vet + GROUNDING-DEP VERIFICATION (the FORM-A cleanup-distinct-count atom must ground in EXISTING atoms -- verify, don't fabricate phantom deps per the 53rd instance). ACK 65th instance type (compute-estimate-must-be-measured-verified). This is the highest-stakes integrity step of the BUILD -- maximal discipline.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** ARM1_ratify_FULL_promotion_gate_not_just_cap_pres_3of3_4gate_STRICT_grounding_dep_verify_first_phase_B_load_bearing_atoms

## Sign-off stands; the ratify is a PROMOTION, so the full gate applies
DECISION 180c frames the ratify around cap_pres=1.0 + compute_backend stamp. cap_pres is necessary but NOT
sufficient: ratifying CAP_cardinality_recall_exact_count_single_role + CAP_cardinality_quantifier_most makes a
NEW capability load-bearing in the substrate (the first Phase-B grow-the-basis addition). The FULL promotion
discipline must run, per my checklist sec G:
```
  [ ] 3-of-3 gate:
      (1) cap_pres preserved (Testbed HARD-FAIL gate) -- the one 180c names.
      (2) RE-EXPRESSIBILITY: the capability reconstructs FROM the atoms (the cleanup-distinct-count mechanism +
          the readout are expressible in substrate terms, not an opaque measurement).
      (3) CLOSES-A-GAP / serves-with-MEASURED-utility: cardinality was a genuine basis-gap (binding-orthogonal;
          C1 fails at every N). Now closed with MEASURED utility (exact-count RMSE 0.209; most acc 0.839). Met.
  [ ] 4-gate pre-check: forward-walk + corpus-monotone + axiom-term + dangling (run before the write).
  [ ] STRICT vet on the atom prose: the atom text must state what was MEASURED (exact-count single-role RMSE
      0.209 mean / 5 seeds / N=4096; most acc 0.839) -- NOT "cardinality solved", NOT the downgraded at-least-k,
      NOT the excluded compound. Type-correct: exact-count=AGGREGATE/RMSE; most=RATIO/capability-recall.
  [ ] GROUNDING-DEP VERIFICATION (53rd-instance discipline -- do NOT fabricate phantom deps): the FORM-A new
      atom for the cleanup-distinct-count mechanism MUST ground in EXISTING substrate atoms (cleanup_retrieval /
      codebook-correlation / role-filler-unbind). VERIFY each grounding atom EXISTS before wiring DEPENDS_ON;
      if a prerequisite is missing, author it first (as I did for hopfield_pattern_deletion), do NOT thin-ground.
```

## Atom-specific notes for the 2 ratifies
- CAP_cardinality_recall_exact_count_single_role: type AGGREGATE (RMSE). Prose must scope to SINGLE-ROLE within
  capacity-envelope (the COMPOUND was excluded as capacity-artifact -- must NOT appear in the atom's claim).
- CAP_cardinality_quantifier_most: type RATIO/capability-recall (acc 0.839, worst-seed margin +0.247 over fair
  non-evadable C1 0.570). Prose: "most/majority quantifier", not "all quantifiers" (at-least-k is MIDDLE, excluded).
- at-least-k: NOT ratified (MIDDLE). If filed at all, file as a MIDDLE-band finding atom, never as HARD-PASS.

## ACK 65th instance type (COMPUTE-ALLOCATION-UNDERESTIMATE-CAUGHT-BY-USER-THERMAL-OBSERVATION)
Sound. The general lesson: compute-cost estimates must be MEASURED-VERIFIED before scheduling (theoretical
"thermal-safe" != measured 261s/cell). Composes with 62nd (empirical-witness > prior). I'll honor the standing
USER policy: route any heavy independent verification I run (e.g. an independent 38-op spot-check) to the REMOTE
DESKTOP; keep only sub-minute checks on the laptop.

## Net
Sign-off stands. Ratify the 2 robust atoms ONLY under the FULL promotion gate (3-of-3 + 4-gate + STRICT prose +
grounding-dep verification), not cap_pres alone -- this is the first Phase-B capability entering the load-bearing
core, the highest-stakes integrity step. at-least-k stays MIDDLE. I VET the resulting atom prose + grounding
chain post-ratify (read-only; classifier-independent) before it's final. ARM 2 38-op (remote) + ARM 3 scoping next.

Tag: ARM1_ratify_FULL_promotion_gate_3of3_reexpressibility_closes_gap_measured_utility_4gate_forward_walk_corpus_monotone_axiom_term_dangling_STRICT_prose_scope_single_role_within_envelope_compound_excluded_most_not_all_quantifiers_at_least_k_MIDDLE_excluded_GROUNDING_DEP_VERIFY_FORM_A_cleanup_distinct_count_grounds_in_existing_cleanup_retrieval_no_phantom_53rd_instance_not_just_cap_pres_first_phase_B_load_bearing_atoms_ACK_65th -- SKUNKWORKS (Auditor)
