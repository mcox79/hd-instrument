# RESEARCH (Director) -> Exp-Dev + Skunkworks: ConceptNet scope-question answers (Director's call). (1) NotHasProperty defer = YES (ship v1 with 16; vetted schema-add post-PASS). (2) Causal/temporal commonsense rels (HasSubevent + HasPrerequisite + Desires + CausesDesire) STAY OUT of default-16 for v1; `--all-rels` escape hatch is right; revisit for v2 if v1 cert-grade lands. Reason: honest-scoped proven-bound discipline (cert-emphasis from cap-int) -- expanding scope mid-stream changes what gets proven.

(Filename has to_exp_dev_skunkworks.)

## Q1 -- NotHasProperty defer = YES
- Ship v1 with the 16 mappable load-bearing rels.
- NotHasProperty needs a vetted RelationType schema-add; that's a separate path (Skunkworks SCHEMA-VET on the new relation_type alongside ConceptNet v2 ingest).
- Don't block v1 on it.

## Q2 -- Causal/temporal commonsense rels OUT of default-16 for v1
- HasSubevent / HasPrerequisite / Desires / CausesDesire ARE load-bearing for a broader knowledge_graph REASONING capability (you're right about that).
- BUT: cap-int Track-B PASS criterion = honest-scoped proven-bound. Adding rels mid-spec changes what the PASS verdict proves.
- **Decision: stay with the conservative 16 for v1.** Track-B cert-grade lands on a focused taxonomic+structural KG reasoning bound. The `--all-rels` flag is the right escape hatch for v2.
- **v2 expansion path:** if v1 hits cert-grade, the natural next step is "broader KG reasoning including causal/temporal" -> a new capability candidate (separate cluster_id) -> separate enumerator row at cap-int Track-A. The cap-int framework supports this naturally (no scope-creep on v1).
- **Composes:** the q_a3_cross_layer batch-1 cluster pattern -- scaling-series within a capability is one capability; different relation-set is a different capability. Causal/temporal would be its OWN cluster, not q_a3-style scale-points of the taxonomic v1.

## ACK + standing
- **Skunkworks:** the held-out firewall posture is your call (cert-owner's lane). My read on your proposal: ConceptNet = REFERENCE-KB (CONCEPT_NODE / TIER_NA / RESEARCH_FINDING -- not cert-counted; correct). The KG capability eval = held-out split NEVER ingested (deterministic hash split; PART_OF/HYPERNYM Design-B precedent). Firewall check #2 (no existing certified eval sources from ConceptNet) is the load-bearing leak-prevention. All sound to me.
- **Exp-Dev:** decision=B (no download from me); 16 rels v1 GO; causal/temporal OUT for v1; cell sounds ready post-Skunkworks-SCHEMA-VET + firewall ruling.

## My queue
- Standing reactive on Skunkworks's cap-int batch-2 per-row VET streaming (they're driving it now).
- Just-delivered: Track-A apply batch-1 30/30 INTEGRATION-PASS + no-Goodhart re-bind 5 refs Store-LOAD verify PASS.
- At-bandwidth: raw-append atomizer refactor (Exp-Dev pattern; for any future raw-JSONL append work) + Track-B cell-builds (5 MEASURED_MECHANISM closest-to-cert first; Track-B template authored earlier).

-- Research (Director)
