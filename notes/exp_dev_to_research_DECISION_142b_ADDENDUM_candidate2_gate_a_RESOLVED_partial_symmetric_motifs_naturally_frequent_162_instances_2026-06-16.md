# Exp-Dev (Prover) -> Research (Director): DECISION 142b scoping ADDENDUM. Candidate #2 (ternary-motif partial-symmetry) gate-a RESOLVED with data: partial-symmetric motifs are NATURALLY FREQUENT (162 instances) + semantically MEANINGFUL in the real graph -> NOT gerrymandered. Upgrades #2 viability. Did the motif-frequency mining my memo flagged as essential pre-work (analysis-only, in-lane). 146th honest signal.

**From:** EXP-DEV (Prover)  **Date:** 2026-06-16  **Tag:** DECISION_142b_ADDENDUM_candidate2_gate_a_RESOLVED

## Mined the real graph for natural partial-symmetric ternary-motif frequency (gate-a guard)
```
symmetric pairs (X~Y via SHARES_MATH/RELATES/DUAL): 258
MOTIF-A convergent  {X,Y}~ + both DEPEND_ON Z : 88 instances, 39 distinct shared-targets Z
MOTIF-B divergent   X DEPEND_ON {Y,Z}, Y~Z    : 74 instances, 58 distinct anchors X
```
REAL + MEANINGFUL examples (NOT hand-picked):
- em_algorithm -> {forward_algorithm, backward_algorithm}  (the forward-backward symmetric pair)
- sequence_decoding -> {forward_algorithm, backward_algorithm}
- cauchy_schwarz_inequality -> {hilbert_space, inner_product}
- module_ring -> {group, vector_space}
- gibbs_inequality -> {cross_entropy, kullback_leibler_divergence}
These are genuine "X depends on a SYMMETRIC PAIR of foundations" motifs -> natural partial symmetry (sym in {Y,Z}, X distinguished).

## Impact on the scoping ranking
- Candidate #2 gate-a (NATURAL, non-gerrymandered): RESOLVED -- 162 frequent, diverse, meaningful motifs. #2 upgrades from "HIGH if frequent" to "HIGH, frequency-confirmed."
- REMAINING caveat for #2 (test on Phase-B build, NOT now): gate-b/c -- does hyperedge-completion over these motifs REQUIRE partial symmetry, or is it role_filler-closable (e.g. role_filler with a SHARED role for the pair = symmetric handling)? The motif must be encoded as ONE composite where the symmetric-pair + distinguished-arg can't be trivially separated by roles. This is the make-or-break test on USER GO -- same discipline as the link-prediction negative (don't claim a gap a single op closes).

## Net
Candidate #2 is now a DATA-BACKED principled Phase-B option (real frequent motifs), ranked alongside #1 (cardinality, binding-orthogonal). Both ready to inform USER's Phase-B GO when consolidation lands. I did NOT build the task (gated on GO); this is the gate-a feasibility pre-work my scoping memo promised. Standing for promotion pre-check support (142a) + USER Phase-B GO.
-- EXP-DEV (Prover)
