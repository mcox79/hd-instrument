# SYNTHESIS: is the platform mature enough for chain-grade? (2026-07-20)

Integrates the USER-directed drill fleet (5x negatives + 5x brain-given-failures + platform-maturity audit) + the incomplete-KG edge-check, answering: are the base substrate elements mature enough, brain-wise, for chain-grade comprehension+reasoning to function -- or is a base element too immature/missing? Drills: A (construction-determined reasoning), B (extraction/grounding), C (brain how-it-does-it, pending), D (platform maturity), edge-check (incomplete-KG). Written by Director (parents stall on write); sub-scan content captured faithfully.

## THE ANSWER (blunt, high-confidence -- 4 independent drills converge): NO, but NARROWLY. The platform is missing ONE load-bearing base element: an ERROR-DRIVEN / PREDICTIVE-CODING CORRECTION LOOP that lets the substrate LEARN and REPAIR its own compositional model from experience.

This single gap is a SUFFICIENT explanation for why BOTH chain-grade attempts came back construction-determined: with no learning mechanism, every model is either HANDED (construction-determined, oracle=1.000) or HAND-RULED (bounded, 0.557) -- there is no third path (genuine learning) because the loop that would provide it does not exist. We have been trying to RUN (chain-grade) before the substrate can WALK (learn from its own errors).

## PLATFORM MATURITY, element-by-element (Drill D, measured):
- **Capacity: MATURE.** Measured m<=24 robust binding = brain-matched (Cowan/Halford ~4-24 relational ceiling), wide headroom. NOT the wall.
- **Discourse / working-memory buffer: SETTLED.** The two-layer symbolic/HD architecture built + VET'd this session independently converges with situation-model literature (Kintsch CI, Gernsbacher, hippocampal reinstatement). NOT the wall.
- **Cleanup / attractor memory: IMMATURE (cheap fix).** Measured as a hard STEP-FUNCTION (sigma 2.0->3.0 cliff) where the brain has GRADED, experience-shaped CA3 pattern-completion. Fix = modern-Hopfield cleanup (validated on disk, just not wired in). = build-order item 1, FIRED (a894372d).
- **Encoding: PARTIALLY mature.** Structure/content factorization split correct + validated, BUT filler content is NOT similarity-structured in production (random vectors) -> generalization is algebraic not meaningful. = build-order item 3.
- **THE MISSING crux: error-driven/predictive-coding correction loop. MISSING entirely.** = build-order item 2, THE CRUX.

## WHY THIS IS THE ANSWER (cross-drill convergence):
- **Drill A (construction-determined reasoning):** genuine learning in neuro-symbolic systems lives in the LEARNED FRONT-END (NVSA/NS-CL), NOT the fixed reasoning algebra; "free composition" is the DESIRED Fodor-Pylyshyn architectural property but is NOT a learned achievement (not a chain-grade); the ranked genuine-learning VSA targets (schema-induction #1, structure-discovery #2) are all LEARNING problems the substrate can't currently do.
- **Drill B (extraction/grounding):** extraction/parsing is the brain's OWN late-maturing (age 9+ into adolescence), experience-hungry, LEARNED bottleneck, dissociable from (early, cheap) reasoning; plausibility needs GRADED/grounded (distributional) codes not categorical (design-class limit, = why the SCV/thematic-fit failed); predictive-coding = the brain's error-driven learning engine; grounding provides the error signal + constraints.
- **Edge-check (incomplete-KG):** the substrate has NO glass-box EDGE on reasoning-over-KG (single-hop=parity with RotatE, multi-hop 5.5x WORSE than SGD per our OWN prior history, conjunctive=generic math). Gap-filling IS a learned brain capability but solved by SLOW STATISTICAL EXTRACTION (a training/learning-analog), NOT glass-box algebra. => reasoning is not the prize; LEARNING-TO-EXTRACT is.
- **Drill D (maturity):** integrates the above -> the missing base element is the error-driven correction loop; the static hand-built extractor (0.41-0.46 honest, below 0.70 trustworthy) has NO mechanism to notice or fix its own errors.

ALL FOUR converge on the SAME thing: the substrate lacks a genuine LEARNING mechanism (error-driven, self-correcting, predictive). That absence -- not capacity, not the reasoning algebra, not the task -- is why chain-grade doesn't work yet.

## PRE-REGISTERED BUILD ORDER (Drill D; brain-faithful; component-by-component):
1. **Attractor-style cleanup swap** [cheap, narrow, decisive] -- modern-Hopfield, on disk. FIRED (a894372d). Converts the step-function cliff to graded CA3-style completion + fixes partial/missing-cue completion.
2. **The error-driven / predictive-coding CORRECTION LOOP** [THE CRUX] -- the substrate predicts, measures its own error (surprise / prediction-error / coherence, NO external LLM), and repairs its compositional model. This is the missing base element; it is what turns hand-rules/handed-vectors into genuine learning. A SUBSTANTIAL architectural addition -- design carefully; USER-steer welcome.
3. **Similarity-structured content vectors for fillers** -- so generalization is meaningful (grounded-adjacent), not just algebraic.
(Neuromodulation / surprise-gating + full offline replay = refinements on top of (2), NOT prerequisites.)

## DRILL C (brain how-it-does-it given failures) -- SHARPENS ITEM 2 into a buildable mechanism (P=0.42)
notes/drill_brain_how_it_does_it_given_failures_5x_2026-07-20.md. Findings: (1) the brain's structural code = a HYBRID (scaffolded attractor manifold + LEARNED predictive binding), NOT purely "learned from grounded experience." (2) GROUNDING DEMOTED to sufficient-not-necessary -- the strongest N400 error-signal models are AMODAL -> we do NOT need sensorimotor grounding to build the learning loop; an amodal predictive error signal suffices (de-risks item 2). (3) **THE SINGLE MECHANISM whose absence explains ALL 3 failures = CONTRASTIVE PREDICTIVE CODING over RIVAL HYPOTHESES scored against REAL EXOGENOUS DATA** -- buildable NOW by re-wiring the existing predictive_coding.py + the SCV rival-candidate machinery. So item 2 (the crux) is now SPECIFIED: generate RIVAL hypotheses (SCV rival parses) -> PREDICT from each -> score against REAL exogenous data (prediction-error/surprise vs real text, NOT a curated oracle) -> the CONTRAST (which rival predicts real data better) = the learning signal -> repair the model. The SCV FAILED as a STATIC contrastive selector (VET 29360, 0 even gold-perfect) BUT its rival-candidates + contrast-structure are exactly the substrate for a contrastive-predictive-coding LOOP scored against REAL data (the distinction: static-selector vs predictive-LEARNING-loop-vs-real-continuation). MUST-FAIL DESIGN (Drill C): 3-arm = CONTRAST vs ABSOLUTE (per-candidate, known to fail) vs SHUFFLED-control (scramble -> learning must collapse) on the existing Spoke1/SCV components. Non-construction-determined guard = the error signal is REAL EXOGENOUS DATA (not self-generated/oracle).

## HONEST FRAME
The platform is NOT blanket-immature -- capacity + WM-buffer are mature, cleanup has a cheap fix, encoding is half-done. The ONE genuinely-missing, load-bearing element is the error-driven learning loop. The USER's instinct ("are the base elements mature enough for this to function?") was correct: no, and the specific missing element is now identified. This CONVERGES with the entire session (reader bounded, reasoning free, every chain-grade construction-determined) into one root cause: NO SELF-CORRECTING LEARNING MECHANISM. The next real work is building it (item 2), with item 1 (cleanup) as the cheap prerequisite-adjacent first step. Deflated: item-2 design is the crux and non-trivial; C (brain how-it-does-it) pending will sharpen the loop's mechanism.
