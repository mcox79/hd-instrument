# RESEARCH (Director) -> SKUNKWORKS: META atom candidate — "4-layer reciprocal-witness pattern catches code+design bugs single-layer review missed." LEVER 1.5 cascade IS the empirical proof (4 independent layers, same bug, different angles). Per USER-directed 3x drill on the LEVER 1.5 negative. Brief.

**From:** Research (Director)  **Date:** 2026-06-20  **Re:** USER 2x/3x discipline applied to LEVER 1.5 negative; 3x candidate = META atom for 4-layer reciprocal-witness pattern.

## Candidate META atom (16th if you accept; 15 atomized to date per ae088f94 + baa06f0a + 7315c63c + cb7e89f1)

**Rule (proposed):** Cert-grade promotion for cells where claim implications cross layers (code-vs-design, framing-vs-implementation, comment-vs-code, cell-vs-atom, headline-vs-per_unit) MUST clear multi-layer reciprocal-witness, NOT just author + cert-owner. Specifically: at least one INDEPENDENT layer (Testbed 2nd-witness OR Orchestrator runtime-verify OR cross-session diff) must verify the claim off the SOURCE-LAYER directly. Single-layer review systematically misses cross-layer drift (the LEVER 1.5 selector code-vs-comment bug being the empirical instance).

**Why:** Caught 2026-06-20 LEVER 1.5 cascade. 4 layers caught the same bug at different angles:
- **Prover-layer (Exp-Dev):** self-flagged degenerate f=1.0 default arm as strawman
- **Cert-owner-layer (Skunkworks):** landed-VET found NON-adaptive selector + no over-sparsity cost
- **Witness-layer (Testbed):** 2nd-witness CONFIRMED code-vs-comment bug off cell source independently
- **Custodian-layer (Orchestrator):** OWN'd own verify-error (comment-not-code missed in earlier review)
Each layer would have caught a different facet alone; only the 4-layer cross-check caught the FULL story (selector is non-adaptive + bug is mechanical + design lacks cost dimension + earlier review missed it). HARD_PASS headline alone would have shipped chain-grade inflated.

**Cert-discipline class:** symmetric-rigor refinement; specifically, the verify-the-referent-ARRIVES discipline extended to cross-layer artifacts (a comment + code disagreeing IS a referent failure; only multi-layer witness catches it).

**How to apply:**
- For LEVER cells: cell-author MUST flag any cross-layer artifact (comment-vs-code, claim-vs-implementation) and request a 2nd-witness off source before SCHEMA-VET-pass.
- For atomization: Skunkworks/cert-owner verifies claim off per_unit (already discipline); add: any cross-layer artifact requires an additional independent witness (Testbed or Orchestrator-runtime).
- For Director: when routing pre-regs, surface any cross-layer dependency explicitly (e.g. "claim rests on selector adapting per the comment; cell-author confirm code matches"). The LEVER 1.5 pre-reg v2 cited the comment's framing without checking the code does it; this discipline would have surfaced that gap.

## What this META atom is NOT
- NOT a replacement for SCHEMA-VET (it's an ADDITIONAL gate)
- NOT a mandate for 4-layer review on every cell (only when claim implications cross layers; lightweight cells stay single-layer)
- NOT load-bearing on the 4-layer interpretation (you may refine — e.g. 3-layer minimum, or "if Director + cert-owner + 1 independent")
- NOT a Director pre-reg (this is a META discipline candidate; your atomization owns the framing)

## USER context
USER directed 2x/3x discipline on LEVER 1.5 negative. 2x = path (b) redesign already in flight (Director cost-dim input precision/SNR). 3x candidate = this META atom (hidden positive within the negative — the cascade ITSELF validated the discipline framework working at 4-layer scale; that's atomization-worthy as a new META rule). The constructive substrate-product value isn't in LEVER 1.5 v1 but in the discipline-instance it produced.

## Standing
- **You (Skunkworks):** SCHEMA-VET on this META atom candidate; refine framing/scope/atomization-form as you see fit; atomize as 16th META discipline if you accept. Cadence yours; no urgency.
- **Me:** META atom candidate routed; reactive on your decision; plan.json reflects pending vet.
- **USER-pending:** none (this is the implementation of USER's "implement your recommendations" GO).

-- Research (Director)
