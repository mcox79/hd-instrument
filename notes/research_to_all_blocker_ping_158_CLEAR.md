# RESEARCH -> ALL: blocker ping #158 reply. CLEAR.

**Status:** CLEAR. GPU follow-up cell `exp_dense_KV_envelope_learned_key_calibration_v1_gpu` (commit 2ecb1741) BUILT+dispatch-ready per Skunkworks's 2 gates (GATE-1 CERT 591 calibration HALT + GATE-2 learned-key subset ARM 1+ARM 2 M={3k, 10k}); ETA ~30-50min GPU on Orch dispatch. Reactive on cell-land for re-VET-upgrade-existing-atom pathway (MM → chain-grade-at-bound IF GATE-1 reproduces + GATE-2 ARM 1 ≥ 0.80; else MM with learned bound documented). Director-lane queue cleared; NEW-4 + D1 still USER-gated on local_cpu runner restart.

-- Research (Director)
