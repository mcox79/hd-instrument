# RESEARCH (Director) — SESSION RESUME ANCHOR (compaction 2026-06-20). Private; rehydrate via this note + capability_scorecard.md + recent notes/. The fleet activation is filed separately (`research_to_all_FLEET_ACTIVATE_*`).

## Session role + standing protocols

- I am Director / Research (Skunkworks role per memory MEMORY_archive). NOT Skunkworks (cert-owner).
- USER STANDING directive LIVE: "drive all night + when idle/waiting → ask others what's holding them up + facilitate; do this every time"
- USER-LOCKED rules (memory): substrate-capability-first + no-LLM-positioning + no papers/no product-positioning + verify-the-referent + research-can-be-wrong-only-proven-load-bearing + always-include-intuitive-explanation + always-reconsider-frameworks + obey-USER-pause + state-waiting-on every response + ACTIVE-state-check every 10-15 min between monitor events + no-stand-default-at-phase-boundary + lean-discipline (ACK cuts ~50%) + progress-notes >15 min + research-never-goes-passive + symmetric-verify-both-directions + verify-the-referent-arrives + capability-optimal-substrate-mining + route-research-needs-to-research + intuitive-summary-must-scour-full-substrate + long-cells-must-checkpoint + monitor-must-be-armed-post-compaction (re-arm monitor)
- Standing hourly Skunkworks check-in (per project memory)

## Substrate state at compaction

- **CERT 591** (atoms 177235; TRUE-HARD-PASS; 0 hygiene flags)
- **CERT 591 → 592 IMMINENT:** K_max NESS chain-grade EARNED per Skunkworks FINAL TIER RULING; atomization pending Exp-Dev doc-fix on prereg 4992d3a6 (corrected genuine-check + ext_hopfrac extension-check; the OLD mis-spec'd check description in prereg needs update; cert claim is the corrected one regardless)
- 3 chain-grade ships when 592 atomizes (CSP 590 + #7 591 + K_max NESS 592)
- 3 MEASURED_MECHANISM characterizations (Hebbian + crosstalk-law + sparse-boundary)
- Substrate-KV mechanism SETTLED = NN-retrieval with #7 learned-projection (Phase 3 foundation)
- 9 META atomized disciplines + several pending atomize (alpha-semantic disambiguation + divide-by-near-zero both-limits + data-decides-tier-no-preempt + repeat-conflation-warning)

## 12 Director self-catches (all CITE-WITHOUT-VERIFY family at 11 layers)

#1 vs-LLM tier prioritization (USER HALT)
#2 substrate-distinctive lens (USER program-sequence)
#3 cite-HARDPASS-without-referent-check (N6/C/D)
#4 standing-reactive-without-cell-existence-verify (CSP not-built; enabled milestone)
#5 cite-formula-without-regime-verify (Hebbian 1/rho_mean²)
#6 cite-without-verify-projection-application (Hebbian v1 not de-crowded)
#7 cite-pq-class-without-verify (Hebbian CERT 592 over-label; Skunkworks corrected)
#8 cite-without-verify-predictor-non-circularity (isotropy law assumed IsoScore non-circular)
#9 cite-without-disambiguate-alpha-semantics (sparse 6×/25× alpha-LOAD vs sparse-fraction)
#10 cite-without-verify-specific-cited-number (sparse 6× phantom sweep-endpoint)
#11 preemptive-tier-downgrade-without-data (K_max premature MEASURED_MECHANISM reframe + retract chaos)
#12 repeat-alpha-semantic-conflation (sparse-#2 third instance; load-vs-f conflation persistent pattern)

## Verify-the-referent at 11 layers fleet-wide bidirectional

atom-grade + cell-existence + saturation/can-fail + metrics-path (smoke vs full) + cell-on-disk provenance/mapping + formula-specification (regime) + projection-application + measurement-vs-extrapolation + K_max baseline divide-by-near-zero (BOTH limits) + sparse-boundary semantic-definition (alpha-LOAD vs sparse-fraction) + formula-output-vs-cited-prediction (scorecard mislabel catch)

## Active in-flight (where to pick up post-resume)

**Immediate (Director-side reactive):**
- CERT 591 → 592 atomization landing imminent (Skunkworks's window when Exp-Dev doc-fix confirms)
- Canonical-evidence map v4 refresh post-CERT-592 (12 DONE → 15 rows: 10 SEED + CSP 590 + #7 591 + Hebbian + crosstalk-law + K_max NESS)
- Substrate-product synthesis filed (commit a7e1ef26; needs minor update to reflect 1-earned-chain-grade-increment per Skunkworks's correction)
- Hardening proposal routed to Testbed (commit d67f17ba); Orchestrator runtime input received; USER-pending power-settings nod + register-auth post-dry-run

**TIER-2 wave status (Director artifacts):**
- composition #1 (commit 9bbb6954; chunked-W; SCHEMA-VET'd GO)
- sparse-boundary #2 (REFRAMED to f-axis per Exp-Dev cell-read; pre-reg authoring TBD)
- KG fb15k237 #3 (commit 2c6eca96; sharding split or defer per Skunkworks)
- continual+drift #4 (commit ac4a0e59; SCHEMA-VET clean)
- isotropy #6 (SUPERSEDED by crosstalk-law MEASURED_MECHANISM)
- K_max C2 envelope (commit 0f5d6ba5; now → K_max NESS Anchor-1 corrected = CHAIN-GRADE earned)
- learned-projection #7 (atomized CERT 591)
- Hebbian-superposition (atomized MEASURED_MECHANISM)
- refuse-gate #5 DEFERRED pending SQ6 SMOKE

**Phase-1 lever queue:**
- LEVER #1 CSP LIVE
- LEVER #1.5 capacity sweet-spot (Skunkworks SCHEMA-VET refined ship order)
- PCA + sparse_coding + multiplicative_composition queued

## USER-pending list (concrete decisions)

1. Power-settings nod (Phase 1 hardening; immediate; reversible)
2. Register-auth (Phase 1 hooks; AFTER Testbed dry-run + Orchestrator approval)
3. Phase 3 hardening cost/policy decisions (concurrency reduction / separate workspaces / higher tier) when Phase 1+2 prove insufficient

## Resume protocol (post-compaction)

1. **Re-arm monitor IMMEDIATELY post-compaction** (per memory: cron/durables don't survive compaction):
   ```
   Monitor tool with persistent:true, command: `bash tools/notes_monitor.sh research`
   ```
2. Verify monitor firing via `ls notes/` (never trust CronList)
3. Read recent ~10 notes/ since this resume-anchor's commit
4. State-check: any new CERT atomizations? (`git log --oneline -20` + check substrate state per Skunkworks invariant-check tool)
5. End EVERY response with "who I'm waiting on" per 9th rule
6. Standing reactive on cascade

## Top forward Director work next-cycle

1. Canonical-evidence map v4 refresh post-CERT-592 atomization (15 DONE rows)
2. Standing reactive on Exp-Dev cells landing + Skunkworks SCHEMA-VETs + atomizations
3. Hardening proposal USER-pending items routing as decisions come
4. Substrate-product synthesis update with 1-earned-chain-grade + 4-dissolved framing (Skunkworks's headline correction)

## Holds (for memory)

- vs-LLM tier indefinite (USER-LOCKED)
- Refuse-gate #5 pending SQ6 SMOKE outcome

-- Research (Director)
