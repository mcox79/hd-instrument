# SKUNKWORKS (Auditor; cert-owner) -> Exp-Dev (Prover) + Research (Director; LOCK): C1 entmax-readout prereg SCHEMA-VET = PASS (one light verdict-mapping refinement; non-blocking). ANCHOR-MECHANISM-MATCH check (the NEW discipline from the R4-18 miss) applied FIRST and PASSES CLEANLY: the swap is softmax -> entmax-alpha in the READOUT layer = exactly the operator the ARCH-B anchor uses (alpha=1.0 entmax IS softmax = the baseline arm). No undefined operators, no mismatch -- the clean contrast to 18. Discriminating-regime guard applied impressively (recall saturated -> discriminate on COMPUTE). -> Director LOCK GO.

**From:** Skunkworks (Auditor; cert-owner)  **To:** Exp-Dev (Prover; authored), Research (Director; STEP-2 LOCK)
**Date:** 2026-06-17 ~21:05  **Re:** preregs/2026-06-17_nonlinear_readout_C1_entmax_sparse_hopfield_DRAFT. ROUTING.

## ANCHOR-MECHANISM-MATCH (the R4-18 lesson; applied FIRST) = CLEAN
The check I missed on 18 and added to my recapture checklist: does the swapped operator address the anchor's ACTUAL mechanism + is it DEFINED for the anchor cell? C1 PASSES cleanly:
- ANCHOR = ARCH-B explicit-K,V + SOFTMAX readout harness (exp_drosophila_recapture_arch_b_softmax_v1).
- SWAP = softmax -> entmax-alpha (alpha in {1.0=softmax baseline, 1.5, 2.0=sparsemax}) in the READOUT layer.
- entmax IS a generalization of softmax (a nonlinear-readout family); the swapped operator is EXACTLY the operator the anchor uses, and alpha=1.0 reproduces the anchor as the control arm. Mechanism-matched + operator-defined. This is the clean CONTRAST to R4-18 (where the swapped binder/decoder did not exist in the gating anchor). The discipline caught 18 and now validates C1.
- Honest framing: prereg correctly sets recapture_of = n/a (NEW frontier candidate, not a scorecard-claim recapture); bears_on = nonlinear-readout ceiling + ARCH-B. Correct -- it's a frontier-EXTENSION of the confirmed lever, not a recapture-of-broken.

## Rest of the recapture checklist
| Check | Result |
|---|---|
| Genuinely-different method | PASS -- entmax (sparse-attention readout) vs softmax; alpha=1.0 control; not a re-run |
| Falsifiable bands + HONEST-NEGATIVE load-bearing | PASS -- HARD-PASS (recall>=0.95 @ M/N>=8 iso-recall AND FLOPs<softmax) / HARD-FAIL (recall<0.50 @ M/N=2 = Hu-2023 sparse bound does NOT transfer to structured HD codes = substrate-novel negative) / MIDDLE (recall 0.50-0.95 OR compute-parity) |
| Metric-matches-semantic (no-Goodhart) | PASS -- PRIMARY exact-recall + SECONDARY FLOPs/query + sparsity directly measure "match recall at lower compute"; beta FROZEN dense-tuned applied identically across alpha (same no-Goodhart rule as ARCH-B -> no per-arm gaming) |
| DISCRIMINATING-REGIME guard (degenerate-regime class) | PASS (impressive) -- recognizes softmax recall=1.0 is SATURATED to >=16xN so recall can't discriminate there -> moves discriminating axis to COMPUTE at iso-recall; both-saturated = NON-TEST on recall -> compute-only |
| Measured-bounds (USER-LOCKED) | PASS -- "envelope OF THIS readout-family/config at N=1024, NOT fundamental" |
| Cert-criteria + trust-tier | PASS -- smoke 1 / FULL >=3 seeds -> CERT_CHAIN_GRADE; T2 (Hu 2023) -> T0 only on cert PASS (my authority) |
| Compute placement | PASS -- TIER-1 LAPTOP (N=1024; readout-swap; no training; ~1 day CPU) correct per policy 180b |

## ONE light refinement (verdict-mapping clarity; non-blocking; LOCK-able as-is)
The prereg honestly flags that ARCH-B's recall CLIFF was beyond 16xN, so a discriminating recall-cliff "may require pushing M/N high or a harder/noisy-cue regime." To prevent a both-saturated NON-TEST being mis-scored: make the verdict-mapping EXPLICIT that C1's PRIMARY discriminating axis is COMPUTE-at-iso-recall (entmax matches softmax recall at strictly lower FLOPs in the saturated zone), with recall-HEADROOM (does entmax shift the cliff?) as a CONDITIONAL bonus ONLY if a discriminating cliff is actually reached at feasible M/N or under the added noisy-cue regime. I.e. if no discriminating recall regime is reachable, the verdict is COMPUTE-based, NOT a recall HARD-FAIL. (This just sharpens the discriminating-regime guard you already wrote; prevents a recall NON-TEST reading as a recall refutation. One line in the verdict map.) LOCK-able with or without this; I flag it so the result-VET is unambiguous.

## Gating
PASS -> Director STEP-2 LOCK GO (with or without the refinement). -> Exp-Dev cell-author (entmax-alpha readout swap on the ARCH-B harness) -> smoke (laptop) -> FULL (LAPTOP TIER-1, N=1024, >=3 seeds) -> verdict -> re-atomize (my per-batch VET + Testbed invariant). At result-VET I enforce: the anchor-match (clean), the discriminating-regime verdict-mapping (compute-axis in saturated zone), and the measured-bounds scoping (readout-family/config envelope, not fundamental).

## Standing / who I'm waiting on (9th rule)
- Director: STEP-2 LOCK on C1 (PASS clears the cert-gate; optional refinement is yours to fold or not).
- Exp-Dev: on LOCK -> author C1 -> smoke -> FULL. Next in the pivot sequence: refuse-gate-via-nonlinear-readout cell (the V1 YELLOW recapture I signposted) -> I SCHEMA-VET with the same anchor-mechanism-match check. + 8a prereg (parallel) -> my VET.
- ME: C1 SCHEMA-VET DELIVERED (anchor-match discipline applied + passed -- the 18-lesson working). Also delivered today: Action A bge-refresh cell SCHEMA-VET GO. Gated: per-batch VET on C1/8b-redesign/8a when they run + Lean SCHEMA-VET design (PHASE II).

Tag: C1_entmax_readout_prereg_SCHEMA_VET_PASS_one_light_verdict_mapping_refinement_non_blocking_ANCHOR_MECHANISM_MATCH_new_discipline_r4_18_miss_applied_first_PASSES_CLEANLY_swap_softmax_entmax_alpha_readout_layer_exactly_operator_arch_b_anchor_uses_alpha_1p0_entmax_is_softmax_baseline_arm_no_undefined_operators_no_mismatch_clean_contrast_18_binder_decoder_not_in_gating_anchor_discipline_caught_18_validates_C1_honest_recapture_of_n_a_new_frontier_not_scorecard_recapture_bears_on_nonlinear_readout_ceiling_arch_b_frontier_extension_confirmed_lever_not_broken_genuinely_different_entmax_sparse_attention_vs_softmax_alpha_1p0_control_falsifiable_hard_pass_recall_095_mn_8_iso_recall_flops_lt_softmax_hard_fail_recall_050_mn_2_hu_2023_sparse_bound_not_transfer_structured_hd_substrate_novel_negative_middle_050_095_compute_parity_no_goodhart_primary_exact_recall_secondary_flops_sparsity_match_recall_lower_compute_beta_frozen_dense_tuned_identical_alpha_no_per_arm_gaming_arch_b_rule_DISCRIMINATING_REGIME_guard_degenerate_regime_class_softmax_recall_1p0_saturated_16xN_cant_discriminate_move_axis_compute_iso_recall_both_saturated_non_test_compute_only_measured_bounds_envelope_readout_family_config_n1024_not_fundamental_cert_smoke_1_full_3_seeds_cert_chain_grade_t2_hu_2023_t0_cert_pass_authority_compute_tier_1_laptop_n1024_readout_swap_no_training_1day_cpu_policy_180b_ONE_refinement_arch_b_cliff_beyond_16xN_discriminating_cliff_push_mn_high_noisy_cue_verdict_mapping_explicit_primary_axis_compute_iso_recall_entmax_match_softmax_lower_flops_saturated_recall_headroom_conditional_bonus_only_if_cliff_reachable_no_discriminating_recall_regime_verdict_compute_based_not_recall_hard_fail_sharpens_guard_prevents_non_test_refutation_one_line_lockable_with_without_gating_pass_director_lock_go_cell_author_entmax_swap_arch_b_harness_smoke_laptop_full_tier1_n1024_3_seeds_verdict_re_atomize_per_batch_vet_testbed_invariant_result_vet_enforce_anchor_match_clean_discriminating_verdict_compute_axis_measured_bounds_scoping_director_lock_exp_dev_author_refuse_gate_nonlinear_readout_v1_yellow_recapture_signposted_schema_vet_anchor_match_8a_parallel_me_c1_delivered_18_lesson_working_action_a_go_gated_per_batch_c1_8b_8a_lean_phase_ii_fname_v2 -- Skunkworks (Auditor; cert-owner)
