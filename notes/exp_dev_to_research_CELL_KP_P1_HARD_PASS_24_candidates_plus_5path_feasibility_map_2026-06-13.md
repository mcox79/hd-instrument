# exp_dev -> research: CELL KP path P1 (frequency-promotion) HARD_PASS -- 24 T3->T2 candidates. + full 5-path feasibility map (P1 ready; P3/P5 GATED at current corpus; P2 big-build; P4 approximable).

**From:** exp_dev  **Date:** 2026-06-13. Knowledge-promotion operator, per your priority steer. NO LLM. read-only, no heat.

## P1 frequency-promotion: HARD_PASS
24 of 110 T3 atoms qualify (in-deg >= 3 AND referenced by >= 3 distinct corpus partitions) -- cross-domain foundational algos:
discriminative_perceptron (in-deg 52, 7 corpora), pca_whitening, bayesian_inference, dijkstra, hmm_transition, beam_search,
zca_whitening, forward_algorithm, collins_structured_perceptron, dynamic_programming, hungarian_assignment, ...
Candidate list saved: data/substrate_index/bench_reports/kp_p1_frequency_promotion_candidates.json. READ-ONLY -- did NOT write
the canonical substrate (promotion + the +0.01-macro benchmark-improvement check are Testbed-integration steps).

## 5-path feasibility map (verify-before-build on the full operator at CURRENT corpus)
- **P1 frequency-promotion**: READY -- 24 candidates (above). Implemented + passing.
- **P2 DRUM/NeuralLP differentiable rule mining** (T1-axiom-candidates): substantial ML build (~2 days, differentiable rule
  learner); not a quick cell -- recommend a dedicated build cycle. Deferred.
- **P3 SHARES_MATH bisimulation** (T2 archetypes): GATED -- SHARES_MATH edges = 0 in the corpus. Nothing to quotient until
  SHARES_MATH edges are authored (Strategy/Research commit). Blocked.
- **P4 sleep-replay consolidation** (T2 cortical archetypes): APPROXIMABLE now via clustering of T3 algebra/composite vectors
  (numpy, no heat); moderate build. Can ship next if you want the 2nd feasible path.
- **P5 Curry-Howard type promotion** (T0 candidates, depth>=10): GATED -- the proof/DEPENDS_ON graph is depth ~1.3 (L6-PROOF
  finding); depth>=10 derivation chains do not exist until much deeper authoring. Blocked.

## Implication for the "3-of-5 HARD-PASS" operator criterion
At the current corpus only P1 (+ P4 if built) is feasible; P3/P5 are precondition-gated and P2 is a multi-day build. So the
full operator's 3-of-5 PASS needs: (a) SHARES_MATH edges authored (unblocks P3), (b) deeper DEPENDS_ON authoring (unblocks P5),
or (c) the P2 DRUM build. P1 is a concrete down-payment + the candidate list is ready for Testbed to promote + benchmark-validate.

## Routing
- **Research/Testbed:** P1 candidates ready to promote (Testbed) + benchmark-validate (+0.01 macro check). For the full operator,
  prioritize SHARES_MATH-edge authoring (unblocks P3) +/- the P2 DRUM build cycle. I can ship P4 (clustering replay) next as the
  2nd feasible path on request.
- **exp_dev:** continuing per USER "keep going."
