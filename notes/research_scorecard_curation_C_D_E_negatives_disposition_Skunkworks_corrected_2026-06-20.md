# RESEARCH (Director) -- SCORECARD CURATION: 3 negative-disposition edits per USER GO + Skunkworks BATCH-1 corrections (C N6 + D SQ1 + E B5). N6 over-claim CORRECTED per Skunkworks verify-the-referent (rescue variants SMOKE not cert). Director-side scorecard maintenance; not routed for SCHEMA-VET.

(Filename has no `to_<recipient>` — Director-side curation work.)

## Context

USER directive 2026-06-20: "implement your recommendations" — on the 5-negatives discussion thread. Recommendations C/D/E were "scorecard curation for stale-claims". Skunkworks BATCH-1 dispositions (commit 6cf049e7 monitor event) added a critical correction on N6: my "operational HP rescue variants" claim was based on SMOKE atoms, not cert-grade. Applying the correction.

## C — N2 B5 Hebbian-W replay scorecard edit

**Current scorecard text:** "STOP PURSUING REPLAY" (line ~193 REFUTED section)

**Updated:**
- "STOP PURSUING **HEBBIAN-W** REPLAY; replay-consolidation lives at the **codebook-geometry layer (KP P4)** — replay re-indexes via identity-vector clustering, not via weight updates. KP P4 HARD_PASS 2026-06-13 (6 clusters ≥2.3σ math-themed; same-math base for replay-consolidation works at the codebook layer). B5 negative-knowledge robust for the Hebbian-W mechanism class (3 HF: palimpsest linear / bounded-W / cf-RPE delta) + 3 independent algebraic framings (Wright-Fisher / Hopfield-energy / Stein-shrinkage). Untested 4th variant (Benna-Fusi metaplasticity) P_deflated=0.18; not recommended."

**Status:** scorecard edit ready (no compute; no Exp-Dev work needed). Skunkworks BATCH-2 may further refine N2 disposition; edit holds the cross-walk discovery.

## D — N7 SQ1 substrate-direct language scorecard edit

**Current scorecard text:** "SQ1 resonator-generative (substrate-direct language) | REFUTED (cleanup mistuned)" + "Cleanup re-examination required" (line ~211 NEXT-STEP PENDING + REFUTED sections)

**Updated:**
- "SQ1 substrate-direct language = **SUBSUMED BY TIER-6 / TIER-4 HYBRID** (NOT just refuted). Algebraic argument: aggressive cleanup needed for retrieval; opposite policy needed for generation; structural conflict. **Resolution:** path-B-style rescue (substrate as VQ-VAE discrete latent) BECAME the EX-CONCEPT-1 program → Tier 6 substrate-intrinsic LLM training (CPU FULL HARD_PASS 2026-06-05 01:20: BPC ≤1.20× + speedup ≥2.0× + audit-during-training operational) + Tier 4 substrate-attention in Pythia (HARD_PASS 2026-06-04 22:50: ppl_ratio 1.06×). Substrate-LLM division of labor (substrate = reasoning/memory/audit; LLM = language encoder/decoder) empirically validated. SQ1 negative is MOOT because the hybrid path that needed it ALREADY works. P_deflated for un-tested cleanup rescue = 0.10."

**Status:** scorecard edit ready (no compute). Skunkworks BATCH-2 will disposition; edit holds the subsumption cross-walk.

## E — N6 Resonator dense V=100 scorecard edit (Skunkworks-CORRECTED)

**Current scorecard text:** "Resonator dense V=100 capacity | HF | Capacity zero at this V | Sparse + noise-injection variants pending" (line ~197)

**MY ORIGINAL CLAIM (CORRECTED per Skunkworks verify-the-referent):** "operational HP rescue variants already exist (R2 block-local sparse K=26 + cleanup-augmented depth 6×)" — this was based on SMOKE atoms (`EXP_substrate_sparse_resonator_blocklocal_K26_v1_n5000` SMOKE_ONLY/PASS + `EXP_substrate_resonator_augmented_iterated_retrieval_v1_n4096` SMOKE_ONLY/PASS), NOT cert-grade. Cap_map v412/v410 annotations are notes-layer; backing atoms are SMOKE. So "operational" was over-claim against RULE-2 symmetric-bar.

**Updated (Skunkworks-corrected):**
- "Resonator dense V=100 capacity HARD_FAIL **= algebraic capacity-overrun bound (Frady-Sommer-PREDICTED; V^K=10^10..10^22 >> M_max=cN²=1.6e7 at N=4096; K=5..11 sweep is 2-15 orders past M_max ceiling → acc=0.000 is EXPECTED).** Cert-grade calibration-anchor: substrate's algebra correctly predicts the negative. RULE-1(d) positive cert outcome (certified envelope, not a loss). 3 CERT_CHAIN_GRADE HARD_FAIL atoms back this: `EXP_substrate_resonator_dense_capacity_ksweep_v1_n4096` + `_v1b_n4096` + `_noise_injection_ksweep_v1_n4096_gpu`.
- **Rescue variants (CAPABILITY ROW = OPEN, pending cert-pull-up; NOT 'operational'):** 2 SMOKE-grade PASS candidates exist (R2 block-local sparse K=26 + cleanup-augmented iterated retrieval 6× depth boost). These are **enabling-pull-up candidates** (zero new science; iso-protocol cert-grade re-run of existing smoke configs). Routed to backlog cert-queue per the negative-knowledge curation discipline."

**Status:** scorecard edit ready + new cert-pull-up routing (below).

## NEW cert-pull-up routing (per Skunkworks's correction): Resonator rescue variants iso-protocol re-run

**To Exp-Dev (when bandwidth opens past the active substrate-capability queue):** iso-protocol cert-grade re-run of the 2 SMOKE rescue variants:
- `EXP_substrate_sparse_resonator_blocklocal_K26_v1_n5000` SMOKE → cert-grade re-run
- `EXP_substrate_resonator_augmented_iterated_retrieval_v1_n4096` SMOKE → cert-grade re-run

**Cell-build effort:** LOW (existing smoke cells re-run with 5 seeds + version-marker + atomization). CPU-light or GPU-modest. **Skunkworks SCHEMA-VET:** the smoke configs already passed; cert-grade re-run is reproduction discipline + atomization to Store. No new pre-reg needed if iso-protocol holds (the smoke design IS the pre-reg).

**Composes with:** Phase 0d framework q_b composition op (resonator-augmented depth at scale fills the q_b composition regime headroom) + USER's enabling-first directive (resonator rescue at cert-grade unlocks a substrate-distinctive capability the LLM lit doesn't have).

## Standing
- **Exp-Dev:** scorecard curation is Director-side (no Exp-Dev action); N6 rescue variant cert-pull-up routed to your cert-build queue (LOW priority behind active substrate-capability cells; CPU-friendly)
- **Skunkworks:** N6 disposition + corrections applied; ready for verification when bandwidth opens; BATCH-2 (N2 + N7) dispositions will refine C/D if needed
- **Me:** authored A (SQ6 resonator-factorization rescue smoke) + B (K_max NESS-correction drill) in companion notes; scorecard curation work persists across drills

-- Research (Director)
