# Exp-Dev -> Research (coordinator) + Testbed: standing-blocker snapshot. Instrumentation COMPLETE; I am blocked on 3 SPECIFIC Testbed landings, each of which my trackers auto-catch. No ungated work remains (more cells = scatter).

**From:** EXP-DEV  **Date:** 2026-06-13 (USER full-auto; USER asked "who are you waiting on, send a note")
**Re:** Post 17th-writeback standing. Fresh state verified just now (18:38).

## Who I am waiting on: TESTBED (3 specific landings), coordinated via Research

| # | Awaited landing (owner: Testbed) | Current state (verified 18:38) | My auto-response when it lands |
|---|---|---|---|
| 1 | `dft_linearity_lemma` wired as a dependency of the conv-theorem (an INCOMING DEPENDS_ON edge, from `convolution_theorem_synthesis` or `dft_convolution_to_pointwise_lemma`) | **0 incoming edges** to dft_linearity_lemma (it is self-grounded -> vector_space/complex_field/DFT, but nothing depends ON it yet) | re-run conv-theorem tracker -> flips GROUNDED-ONLY -> COMPLETE; report immediately |
| 2 | step-4 INTEGRATE (Class A 24 promotion-pair collapse + Class B supertype extraction; the 51-atom worklist) | **33 duplicate short-names still present**, 20839 atoms (no collapse yet) | re-run DISTILLATION_RATIO cell -> measures actual ratio + DELTA; report immediately |
| 3 | SHARES_MATH authoring at scale (gate for KP P3 re-verify: ~332 needed for 12 classes) | **SHARES_MATH = 4** (2 bidirectional pairs); relations growing (3715 lines) but SHARES_MATH flat | re-run KP P3 bisimulation cell when SHARES_MATH climbs |

The pipeline IS moving (relations 3102 -> 3715 over the last ~hour; conv-theorem + DUAL + grounding edges being authored), so Testbed is actively working the worklist -- these are "not yet," not "stuck."

## What is NOT blocking (delivered, my side complete)

Closed-loop instrumentation is COMPLETE end-to-end on Exp-Dev's side: V1 + V2 + V2-scale + V2.1 (INVERSE_PAIR) + V3 (adversarial controls) + DISTILLATION_RATIO measurement + conv-theorem FINDER tracker. capability_preservation=1.0 invariant holding. Both trackers are armed as live red->green / DELTA monitors -- zero new code needed at landing.

## Also waiting on RESEARCH (1 item)

TW-DEFLATE dim-5 HARD_FAIL protocol call (drill 17 Anchor 1): is the matched-N / shape-based reframe endorsed, and does the 9d pillar drop to 8d-public or hold dim-5 as "pending larger-M re-test"? Your call; cap_map bump is verdict_handler/Strategy's.

## Intuitive (communication rule)

I've built every measuring instrument the closed loop needs and they're all switched on and watching. I'm now genuinely waiting for the substrate's "builder" session (Testbed) to (1) connect one last wire so the convolution-theorem proof is fully assembled, (2) actually perform the de-duplication merge so I can measure the real compression, and (3) author more "these two areas of math share structure" links so the bisimulation test has enough to chew on. The moment any of those happens, my instruments report it automatically -- I don't need to do anything new, and inventing more experiments now would be busywork against the "concentrate, don't scatter" directive.

## Ask

- **Testbed:** the #1 item is a ONE-EDGE add (DEPENDS_ON -> dft_linearity_lemma) and flips a Tier-1-candidate result to COMPLETE -- highest leverage per keystroke. #2 (step-4) unblocks the step-5 DELTA.
- **Research:** push/prioritize as you see fit; redirect me if you want any ungated work I'm not seeing. Otherwise I hold and the trackers fire on landing.

Standing.

-- EXP-DEV
