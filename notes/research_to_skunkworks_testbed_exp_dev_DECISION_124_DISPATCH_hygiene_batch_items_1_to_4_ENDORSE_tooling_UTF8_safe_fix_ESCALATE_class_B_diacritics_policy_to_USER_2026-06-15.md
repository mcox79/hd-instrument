# Research (Director) -> Skunkworks + Testbed + Exp-Dev: DECISION 124 -- ACK Skunkworks 110th honest signal (delivered-batch-not-ETA + comprehensive corpus-wide non-ASCII scan 245 atoms 3 classes); DISPATCH hygiene batch items 1-4 (banach_space + svd + cosine_cleanup + 4 math atoms ASCII transliteration; within Director scope); ENDORSE tooling UTF-8-safe fix recommendation (one-line encoding fix preserves imported names + stops Windows cp1252 crash); ESCALATE Class B diacritics policy (64 OEIS mathematician name atoms; LOSSY-data-strip vs tooling-fix) to USER ruling; DEFER Class C em-dashes (133 provenance atoms; low priority); 26th audit-discipline instance type empirical CORPUS-WIDE INVARIANT SCAN REVEALS POLICY-CLASS STRUCTURE substrate's discipline surfaces policy-class distinctions requiring authority-level ruling not just individual instances

**From:** Research (DIRECTOR)  **Date:** 2026-06-15 ~18:05
**Re:** Skunkworks STATUS reply -- hygiene batch DELIVERED + 245-atom non-ASCII scan.

## ACK -- 110th honest signal: delivered-batch-not-ETA + comprehensive scan

```
Skunkworks's conduct:
  Status request asked "ETA on hygiene batch?"
  Skunkworks answered by SHIPPING the batch + the scan, not by replying with ETA.
  Per their 12th-rule lesson: "delivered the batch + scan result rather than reply ~30-60 min"
  
Skunkworks correctly recognized:
  ETA-reply is operational waste when delivery is possible
  Scan opportunity is load-bearing when invariant-class characterization is unknown
  Shipping the scope-honest answer beats shipping the convenient answer
```

**Director ACK + endorse.** Skunkworks's discipline at conduct-and-deliverable-fusion level: status requests CAN be answered by shipping the requested work, not just predicting its arrival.

## 26th audit-discipline instance type empirical

```
26. CORPUS-WIDE INVARIANT SCAN REVEALS POLICY-CLASS STRUCTURE (110th)
    Substrate's discipline surfaces policy-class distinctions (Class A/B/C) requiring 
    authority-level ruling, not just individual instances. The scan produces a 
    landscape of ~245 atoms across 3 distinct policy-classes:
      A. authored math (clear violation; fix in batch)
      B. imported proper-name diacritics (genuine judgment call; LOSSY to strip)
      C. provenance em-dashes (low-priority cosmetic)
    
    Substrate's audit operates not just at instance-detection level but at 
    POLICY-CLASS-STRUCTURE level: characterizing the violation space + escalating 
    the judgment-call class to appropriate authority.
```

## DECISION 124a -- DISPATCH hygiene batch items 1-4 (within Director scope)

**Exp-Dev (Prover):** pre-check items 1-3 (4-gate stack):

```
1. banach_space backwards-edge fix:
   REMOVE banach_space -DEPENDS_ON-> inner_product (self-contradicting per definition)
   REMOVE banach_space -DEPENDS_ON-> hilbert_space (backwards; general-depends-on-special)
   Leaf-strand SAFE (retains normed_vector_space + metric_space + vector_space)

2. svd double-typed cleanup:
   REMOVE svd -DEPENDS_ON-> matrix_decomposition (redundant; SPECIALIZES subsumes)

3. cosine_cleanup precision:
   ADD cosine_cleanup -SPECIALIZES-> cleanup (precise is-a; was DEPENDS_ON + family RELATES)

Pre-check stack (per atom touched): forward-walk + corpus-scoped tier-monotone + 
                                     axiom-term + dangling
```

**Testbed (Integrator):** atomic ratify items 1-3 (post-pre-check) + item 4:

```
4. ASCII-transliterate 4 authored math atoms (text-only; no 4-gate):
   spectral_theorem_synthesis: lambda/perp/!=/in/-> -> ASCII
   self_adjoint_real_eigenvalues_lemma: same char-map
   bayes_rule_synthesis: same
   product_rule_probability_lemma: same
   
   Math-fidelity vet by Skunkworks pre-ratify
   Text-only metadata fix (no relation-store mutation)
```

**Cost:** ~20-30 min Testbed (4 items; mostly mechanical).

## DECISION 124b -- ENDORSE tooling UTF-8-safe fix

```
Skunkworks's recommendation:
  Fix substrate_atom_lookup_v1.py to be UTF-8-safe (one-line encoding fix)
  Preserves imported names + stops Windows cp1252 crash
  Independent of Class B policy ruling (the crash is a real bug regardless)

Director endorses: tooling-correctness is independent of content-policy ruling.
The Windows cp1252 default codec choice is the actual bug; switching to UTF-8 
is the engineering fix.

Exp-Dev or Skunkworks: ship the one-line encoding fix as separate engineering item 
(no atomic ratify needed; tooling-only).
```

## DECISION 124c -- ESCALATE Class B diacritics policy to USER

**USER policy question:**

```
Class B context (Skunkworks scan):
  64 OEIS mathematician-name atoms with legitimate diacritics
  Examples: Erdos (U+0151 double-acute), Mobius (U+00F6 umlaut), 
            Andre (U+00E9 acute), Jonsson (Icelandic eth), etc.
  
  These are imported proper-name spellings (mathematician names per OEIS conventions).
  
The two policy paths:
  
  PATH A: LOSSY data-transliteration
    Erdos-double-acute -> Erdos (strip diacritic)
    Information loss: name corruption (no longer matches OEIS source)
    But: substrate-content uniformly ASCII; tooling crash resolved at data layer
    
  PATH B: tooling-fix preserves imported names
    Keep Erdos-double-acute in atom name as-imported
    Fix tooling to be UTF-8-safe (one-line encoding fix; already endorsed 124b)
    Information preserved + tooling crash resolved at correct architectural layer
    ASCII-only rule interpreted as: about AUTHORED content (no emoji/em-dash/smart-quotes 
                                     /math-symbols in our writing), 
                                     NOT about IMPORTED proper-name stripping

Director recommendation: PATH B (tooling fix preserves imported information; 
                                  ASCII rule is authored-content-intended).
                                  This is also Skunkworks's recommendation per the
                                  18th rule (refuse lossy change without proof of intent).

USER ruling welcome.
```

## DECISION 124d -- DEFER Class C em-dashes (low priority)

```
Class C: 133 provenance em-dashes (U+2014) in history/decision atoms 
         auto-created from note titles

These are PROVENANCE METADATA not substrate-math content. Bulk em-dash -> "--" is 
safe and trivial when convenient. No urgency.

Skunkworks: bulk-em-dash batch at your convenience (could fold into a future 
            hygiene wave; no Director action required).
```

## Updated waiting-on state (per USER-LOCKED rule)

```
Waiting on (immediate):
- Exp-Dev: pre-check items 1-3 (~10-15 min; 4-gate stack)
- Testbed: ratify items 1-4 post-pre-check + Skunkworks vet (~20-30 min total)
- Skunkworks: vet ratify items 1-4 + math-fidelity vet item 4
- USER: policy ruling on Class B diacritics (PATH A LOSSY-strip vs PATH B tooling-fix)
- USER (optional; no urgency): Phase-5-v2 / 15th rule promotion / positioning consolidation

Standing:
- Orchestrator: producer health + infrastructure (status reply still pending)
- Class C em-dash cleanup: future hygiene wave at Skunkworks's convenience
```

## Session tally

124 cumulative decisions. **110 honest signals.** Substrate-product positioning at 16 claims; 15 MEASURED/OPERATIONAL + 1 OPEN. Audit-discipline at 26 instance types empirically MEASURED.

## Cross-references

- Skunkworks STATUS reply: `notes/skunkworks_to_research_STATUS_hygiene_batch_DELIVERED_*`
- Spec JSONL: `data/substrate_index/skunkworks_phase3_hygiene_batch_consolidated_spec_2026-06-15.jsonl`
- DECISION 123 banach_space + 22nd-24th instance types: commit `da9abcd4`
- DECISION 122 Phase 3 complete: commit pending Skunkworks's vet ACK
- DECISION 109b svd + cosine_cleanup cosmetic deferred: prior commit
- DECISION 123c spectral_theorem_synthesis non-ASCII: prior

## Safety / invariants

- ASCII only
- 11th rule: hygiene + scan substrate-internal (no LLM)
- 18th rule: Skunkworks refused mass-mutation without ruling; Director ESCALATES not RULES on Class B
- 19th rule: 26 instance types empirical MEASURED
- 22nd rule preserved
- 100pct axiom termination + capability_preservation=1.0 expected to PRESERVE across items 1-4

---

**Skunkworks (Auditor):** DECISION 124a SHIPPED -- continue standing vet on items 1-4 ratify; vet math-fidelity on item 4 transliterations (ASCII char-map fidelity vs Greek/math originals); Class C bulk-em-dash cleanup at your convenience; Phase 4e Author-N batch 3 hold continues until items 1-4 land (signature-quality discipline).

**Exp-Dev (Prover):** DECISION 124a DISPATCH -- pre-check items 1-3 via 4-gate stack; ~10-15 min; report PASS/FAIL per atom. Plus DECISION 124b ENDORSE -- engineer the one-line UTF-8-safe tooling fix for substrate_atom_lookup_v1.py (independent of policy ruling).

**Testbed (Integrator):** DECISION 124a DISPATCH -- atomic ratify items 1-4 when Exp-Dev pre-check passes + Skunkworks vet passes; ~20-30 min; standard R3 verify.

**USER:** DECISION 124c ESCALATION -- policy ruling needed on Class B diacritics (PATH A LOSSY-strip vs PATH B tooling-fix). Director recommends PATH B (tooling fix preserves imported information). Skunkworks recommends same. No urgency; convenient time for ruling.

Tag: 124_DISPATCH_HYGIENE_BATCH_ITEMS_1_TO_4_ENDORSE_TOOLING_UTF8_FIX_ESCALATE_CLASS_B_DIACRITICS_POLICY_TO_USER_DEFER_CLASS_C_EM_DASHES_26th_AUDIT_DISCIPLINE_TYPE -- Research (Director)
