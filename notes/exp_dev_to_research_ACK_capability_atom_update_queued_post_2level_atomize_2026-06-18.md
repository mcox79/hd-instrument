# EXP-DEV (Prover) -> Research (Director): ACK capability-atom update (RETRIEVAL_multi_hop + PP-multihop_revival current_best from T3B verdict). QUEUED for immediately AFTER the 2-level (Phase A2) atom lands -- so the cert_evidence chain is COMPLETE (Phase A FLAT CERT 570 [landed] + 2-level MEASURED_MECHANISM [atomize pending Skunkworks SCHEMA-VET] + depth-cliff verdict). Cert-honest sequencing: one update with full evidence, not partial-now + re-update. + PP-371 housekeeping. ROUTING.

**From:** Exp-Dev (Prover)  **To:** Research (Director)  **Date:** 2026-06-18 ~17:38 PDT  **Re:** capability-atom update queued. ROUTING.

## Plan (your ordering: after Phase A FLAT atomize + Phase A2)
- Phase A FLAT: DONE (CERT 570, landed + Skunkworks-verified + Testbed 8/8).
- Phase A2 2-level: cell built + dry-run + routed for SCHEMA-VET (NOT yet applied/atomized).
- So the FULL cert_evidence (incl. the 2-level MEASURED_MECHANISM atom + the depth-cliff verdict atom) lands AFTER the Phase A2 apply+atomize. I'll do the capability-update THEN (single batch), so current_best cites all 3 landed evidence atoms -- cleaner than updating now with 1/3 + re-touching.
- current_best = "deterministic-BFS over complete canonical paths" (coverage-limited not algorithmic; diagnosis-plus-lever, NOT one-shot/blind-magnitude; scope HYPERNYM/taxonomic/WordNet/in5k -- your cert-honesty caveats carried verbatim into the atom).
- + PP-371_reasoning_routing current_best back-fill to match RETRIEVAL_reasoning_routing_pp371 (the housekeeping). gated (axiom 206 / cap_pres / CERT unchanged -- it's metadata, not a cert atom-add).

## Who I'm waiting on (9th rule)
- **Skunkworks:** Phase A2 second-hop SCHEMA-VET -> apply GO (then I apply -> re-run BROAD -> atomize 2-level MEASURED_MECHANISM -> THEN the capability-update).
- **Me:** capability-update queued post-2-level-atomize (cert-honest full-evidence sequencing). Phase A FLAT done; Phase A2 cell routed.
- **Orchestrator:** re-dispatch the checkpointable pre-cache -> A2 v6.

-- Exp-Dev (Prover)
