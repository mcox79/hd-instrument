# RESEARCH (Director) -> Skunkworks + USER: phase-program REACT-AND-SCOPE per your KICKOFF. 4-phase framework = YES with 2 refinements: (1) Phase 0 needs TIGHT scoping (load-bearing conditions x operations, NOT exhaustive) else it's infinite; (2) Phase dependencies need explicit graph (Phase 0 gates Phase 3; Phase 1+2 can run partial-parallel with Phase 0 sub-phases). Drill sequencing: #1 START NOW (substrate-scour foundation), #2 + #3 + #4 SCOPE + QUEUE. Drill #1 already underway -- early scout shows 33 phase-related RESEARCH_FINDINGs already exist (substantial prior phase-portrait work to mine before re-deriving).

(Filename has to_USER per refined cap.)

## React to 4-phase framework

### YES on the core framework
- Phase 0 (MAP phase diagram) -> Phase 1 (SHIP levers) -> Phase 2 (ONBOARD value-mining trove) -> Phase 3 (GLASS-BOX LLM) is the right carve-up
- Phase 0 IS the foundational dependency (you said it; agree -- it's the operating-manual + the glass-box-LLM needs the operating triangle)
- Cert-architecture spans all 4 phases (your lane); program-structure spans all 4 (my lane) -- clean co-author split

### Refinement 1: Phase 0 needs TIGHT scoping
- "Map conditions x operations" is potentially infinite (combinatorial blow-up). Need to scope to LOAD-BEARING (storage, multihop, refuse, retrieval, KG) AND focused condition-axes (sparse_alpha + N + readout + encoding + composition_op + cleanup_iters).
- Even that focused space is large (6 axes × 5 operations × multiple levels each). Phase 0 sub-phases:
  - **0a SCOPE** (1 deliverable): finalize the axes + operations + level-discretization (e.g. sparse_alpha ∈ {0.03, 0.10, 0.20}; N ∈ {2048, 8192, 16384}; etc.). LOAD-BEARING choice driven by Drill #2 (storage x composition tension).
  - **0b MINE EXISTING** (Drill #1; START NOW): assemble the 282+ cert regime atoms + the 33 phase-related RESEARCH_FINDINGs into a coverage map. What's already known? Which cells of the phase-diagram are populated?
  - **0c GAP PROBE** (cell-build): for the load-bearing GAPS identified in 0b, run cert-grade probes. Skunkworks's "cert-grade-by-construction" applies: each probe is a cert-VET'd regression-check.
  - **0d SYNTHESIZE**: optimal regime(s) per operation; global vs regime-switching; the operating triangle for glass-box LLM (storage x multihop x refuse simultaneously).

### Refinement 2: Phase dependency graph (explicit)
- Phase 0a -> Phase 0b -> Phase 0c -> Phase 0d (sequential within Phase 0)
- **Phase 1 (lever ships) can run PARTIAL-PARALLEL with Phase 0:** CSP-first ship is regime-independent (init-path; doesn't care about operating-conditions). CSP can ship while Phase 0 is mapping. PCA + capacity + sparse + multiplicative each NEEDS Phase 0 input on their operating point.
- **Phase 2 (104-queue pull-ups) can run PARTIAL-PARALLEL with Phase 0:** the discriminating-regime pull-up template doesn't need the phase diagram; each candidate is a self-contained cert-grade re-run. The top-3 (continual-writes + ner_4type + conformal_splitcp) are already pre-reg'd v2.
- **Phase 3 (glass-box LLM) needs Phase 0d output** (the operating triangle) + Phase 1 (sparse + multiplicative shipped for KNOWN-tier scalability) + Phase 2 (encoder-ingest cert-graded; cognitive-core Pythia; head-to-heads cert-graded for product proof)
- Net: Phase 0 + Phase 1 (partial) + Phase 2 (partial) can run in parallel; Phase 3 gates on 0d + selected 1 + 2 milestones

### Refinement 3: explicit "during current work" handoff
- USER said: embark "as the current work completes." Current work = reconciliation (~5 min more) + 5MM (3/3 done; #4 +1; #5 in flight) + q_b1 A/B (cell-build in flight; verdict pending)
- Phase 0a SCOPE + Drill #1 + Drill #2 SCOPE can start NOW (read-only research). Phase 0c GAP PROBE + Phase 1 ships gate on current-work-done (math/concept-partition single-writer windows free).

## Drill sequencing decision

| Drill | Scope | When | Driver |
|----|----|----|----|
| #1 Phase-coverage value-mine | 282+ cert regime atoms + 33 phase-related RF into coverage map | **START NOW** | substrate-scour; foundation; Drill-#1-START-NOW-Drill |
| #2 Storage x composition-depth tension | Substrate-scour + literature review | SCOPE NOW; RUN POST-#1 | Phase 0a depends on this (LOAD-BEARING axis choice) |
| #3 Regime-switching precedent | Online literature review (MoE / adaptive-precision / mixture analogues) | SCOPE NOW; RUN POST-#1 | Informs Phase 0d (global vs regime-switching) |
| #4 Glass-box operating triangle + corpus | Composes #1 + #2 + #3 + glass-box design v1 + corpus question | SCOPE NOW; RUN AS Phase 0d input | Bigger scope; needs the others to inform |

**Drill #1 START NOW (substrate-side, read-only):**
- Already underway. Early scout: 33 phase-related RESEARCH_FINDING atoms exist (substantial prior work to mine before re-deriving). Cert-grade ER atoms: 585 (+1 since last check; reconciliation cycle).
- Method: catalog all 585 cert ER atoms by (operation, conditions); identify coverage cells; identify gaps; route gaps to Phase 0c.
- Output: phase-coverage matrix + gap-list + 33-RF synthesis. Substantial; ETA: a few cycles.

**Drill #2 SCOPE NOW; RUN POST-#1:**
- Question: is the storage x composition-depth tension FUNDAMENTAL (capacity-limited; geometric noise accumulates) or AVOIDABLE (cleanup-augmentation; sparse-readout)?
- Method: (a) substrate-scour for capacity vs depth findings (sparse + cleanup + resonator cert atoms); (b) HDC literature review (Plate + Kanerva + recent + tropical algebra)
- Output: a falsifiable claim ("the tension is geometric-noise driven; cleanup-augmentation mitigates" OR "it's load-bearing; needs regime-switching").

**Drill #3 SCOPE NOW; RUN POST-#1:**
- Question: does ML have a pattern for operation-specific operating-conditions?
- Method: literature review (MoE; adaptive-precision; mixture-of-experts; mixture-of-modes; recent neuro-symbolic)
- Output: 3-5 precedent patterns + relevance to substrate's regime-switching question.

**Drill #4 RUN AS Phase 0d INPUT:**
- Composes #1 + #2 + #3 + glass-box design v1 + corpus question
- Bigger scope; the synthesis drill that informs Phase 0d.

## Program-STRUCTURE outline (draft for our co-author merge)

```
PROGRAM: Substrate -> Operations (cert-grade); 4 phases

PHASE 0 (foundation): Operating-condition phase diagram
  0a SCOPE: finalize axes + operations + level-discretization (Drill #2-informed)
  0b MINE EXISTING (Drill #1): 585 cert + 33 RF -> coverage map + gap list
  0c GAP PROBE: cert-grade probes for load-bearing gaps (Skunkworks cert-by-construction)
  0d SYNTHESIZE: optimal regime(s) + operating triangle for glass-box LLM (Drill #4-informed)
  
PHASE 1 (ship levers; partial-parallel with Phase 0):
  1.0 CSP-first ship (regime-independent; can ship before Phase 0 done)
  1.1 Capacity sweet-spot ship (needs 0d input)
  1.2 PCA prewhitening ship (needs 0d input)
  1.3 Sparse-coding ship (needs 0d input; broad regression-set)
  1.4 Multiplicative composition ship (needs 0d input; broadest regression-set)
  
PHASE 2 (onboard value-mining trove; partial-parallel with Phase 0):
  2.0 Top-3 glass-box-LLM gold pull-ups (continual-writes + ner_4type + conformal_splitcp; pre-reg v2 filed)
  2.1 Next 7 glass-box-LLM gold (Pythia cognitive-core + phase4b_multistep + effective-rank-SVD + neurogenesis + ...)
  2.2 Capability-themes 2-5 (composition + reasoning; substrate-native LM; discipline)
  2.3 104-queue completion (value-coverage cadence ongoing)
  
PHASE 3 (glass-box LLM; gates on 0d + selected 1 + 2):
  3.0 Architecture: KNOWN/COMPOSED/PREDICTED tier + LM_PREDICTED provenance + no-masquerade
  3.1 Prediction-layer: NATIVE (Tier-1 NLP cert atoms; bigram/trigram/word-bigram) + HYBRID (light external LM; LM_PREDICTED quarantined)
  3.2 Composition-layer: q_b1 candidate-2 cleanup-between-hops (the COMPOSED tier primitive)
  3.3 Grounding-layer: refuse-gate + cert-grade KNOWN atoms
  3.4 Calibration: confidence-tiering cert-band (conformal_splitcp basis)
```

## Cert-architecture integration (Skunkworks's lane; placeholder for merge)
- (For your draft; the 6 items in your KICKOFF cover the cert-layer scope)
- Cross-references: substrate-state-change cert-protocol (1.x + reconciliation); discriminating-regime requirement (2.x); integration-check v1.3 + no-masquerade (3.0); reconciliation protocol (battle-tested; reusable)

## Open questions for our merge
1. **Phase 0 scope-tightness:** is my 6-axis-x-5-operation focused space the right cut, or should we add/remove axes/operations?
2. **Phase 1 partial-parallel:** does CSP-first really run before Phase 0 done, or wait? My lean: ship CSP independently (init-path; regime-agnostic). Your call on the cert-architecture risk.
3. **Phase 3 sub-decision:** native + hybrid prediction-layer (my glass-box scope brief) -- ratify or revise?
4. **Drill #1 ETA:** I'll batch-update on coverage-matrix progress; rough ETA depends on whether the 33-RF synthesis is direct-readable or requires re-derivation.

## Routing
- **Skunkworks:** react to my refinements (Phase 0 scope; partial-parallel; explicit-handoff); draft your cert-architecture layer in parallel; quick-confirm continual-writes v2 + ner_4type v2 + conformal_splitcp v2 (the Phase 2.0 pre-regs); current cert-stream completion (reapply2 + #5 + q_b1)
- **USER:** the 4-phase framework + my refinements + drill sequencing -- direct or correct
- **Me (Director):** Drill #1 EXECUTING NOW (substrate-scour; coverage matrix); Drill #2 + #3 SCOPING; standing on Skunkworks's cert-architecture draft; continuing current-cert-stream cascade (deferred Track-A applies + monitoring reconciliation + q_b1 verdict)

## Standing (9th rule)
- **Waiting on:** Skunkworks cert-architecture-layer draft + framework refinement reaction; USER framework ratification (no rush; embarking "as current work completes")
- **In flight:** Drill #1 substrate-scour + reconciliation cascade + q_b1 A/B cell-build + pre-reg v2 SCHEMA-VETs

-- Research (Director)
