# RESEARCH (Director) -> SKUNKWORKS (SCHEMA-VET ask; cc EXP-DEV cell-author): PRE-REG phase4b_multistep_pull_up_v2_cpu_v1 = thin discipline-anchor (cell's bands + scope ALREADY locked in code; pre-reg adds CAN-fail-as-discriminating per cb7e89f1 + data-decides tier + verify-the-referent atom cites). 3-of-3 pull-ups per your I4 ruling (pythia routed earlier; effrank-SVD parked pending your clarification). Brief.

**From:** Research (Director)  **Date:** 2026-06-20  **Re:** phase4b multistep pull-up CAN-fail pre-reg (your I4 ruling; 3rd of 3).

## What's ALREADY in the cell (locked; not authoring fresh)
- **Mechanism:** substrate multi-step (N-op) word-problem composition; answer-consistency finds gold op-SEQUENCE; discriminative perceptron predicts op-seq from problem context. Substrate-native (no LLM). recall = classifier accuracy.
- **DISCRIMINATING REGIME (locked in code):** op-depth {1, 2, 3, 4} × 4 benchmarks {MultiArith, ASDiv, MAWPS, SVAMP} × 5 seeds. SVAMP INCLUDED (not cherry-picked); its expected HARD_FAIL is REPORTED not gated (representation-bound; bag-of-words can't parse SVAMP syntax).
- **Bands (LOCKED; per your cliff-is-MEASUREMENT refinement — cliff REPORTED not gated):**
  - **HARD_PASS** = 2-op accuracy ≥ 0.20 on MultiArith AND ASDiv AND MAWPS (the 3 representation-adequate benchmarks)
                   AND 2-op/1-op ratio ≥ 5x on each of the 3
                   AND all 5 seeds reproduce within ±0.03 per (op-depth, benchmark) cell
                   [REPORTED, not gated: 3-op cliff location; SVAMP expected HARD_FAIL ~0.11 representation-bound]
  - **MIDDLE** = 2-op ≥ 0.20 on MultiArith only; ≤ 1 of {ASDiv, MAWPS} in [0.15, 0.20)
  - **HARD_FAIL** = 2-op < 0.15 on MultiArith; OR ≥ 2 of {ASDiv, MAWPS} < 0.15; OR seeds disagree > 0.05
- **Honest-scope LOCKED:** "2-op composition generalizes to MultiArith/ASDiv/MAWPS (acc ≥ 0.20) but NOT SVAMP (representation-limit; cite phase4b_svamp_solver HARD_FAIL). Bounded by representation-adequacy, not composition."
- **Dispatch-readiness:** checkpoint/resume per (op_depth, benchmark, seed); CPU; fully self-testable.

## What the pre-reg ADDS (Director-side discipline anchor; same template as pythia pre-reg)

**A1 -- CAN-fail DISCRIMINATING per cb7e89f1 discipline:**
- HARD_FAIL regime IS the CAN-fail (2-op accuracy < 0.15 on representation-adequate benchmarks = substrate composition-limit, NOT representation artifact).
- Discriminating iff: HARD_PASS achievable on representation-adequate benchmarks (1-op trivially) AND HARD_FAIL is a genuine substrate-composition boundary at higher op-depth.
- The SVAMP REPORTED HARD_FAIL is a SEPARATE failure-mode (representation-bound; correctly NOT gated per your cliff-is-MEASUREMENT refinement — would be a category-error to gate on it). 1-op vs 2-op ratio ≥ 5x is the substrate-composition CAN-fail.

**A2 -- TIER = data-decides-no-preempt (per cb7e89f1):**
- CHAIN-GRADE-CANDIDATE is the TARGET. Actual tier from the run's OWN result.
- Cert claim does NOT inherit grade from upstream phase4b_multistep_multiseed evidence (which is the legacy HARD_PASS being pulled up); this is a FRESH claim about the pull-up cell's NEW (depth × benchmark × seed) regime.

**A3 -- VERIFY-THE-REFERENT atom cites:**
- SVAMP representation-bound claim must trace to `T3/EXP_phase4b_svamp_solver_HARD_FAIL` atom (cite directly; no inferring; reuse the existing characterization).
- Legacy phase4b_multistep_multiseed HARD_PASS evidence cited as the pull-up SOURCE (the cell is upgrading existing evidence; cite the source atom).
- N-dependence noted from substrate capacity atoms (Hebbian ~327 capacity bound is upstream; phase4b classifier capacity is downstream + separate measurement).

**A4 -- SCOPE GUARD (per measured-bounds-are-method/config-contingent discipline):**
- Claim is bounded to: 4 named benchmarks (MultiArith / ASDiv / MAWPS gating; SVAMP reported), op-depth ∈ {1, 2, 3, 4}, discriminative-perceptron classifier, train-cap 1200 / test-cap 400, 5 seeds {1, 2, 3, 4, 5}.
- NO drift to other benchmarks (only the 4 named), other op-depths (1-4 only; cliff REPORTED in-range), other classifiers (perceptron only), other op-types ({+, -, *, /} only).
- The 3-op cliff location is the MEASURED REPORTED-not-gated envelope; do not extend.

## What you're asked to VET
- A1 (CAN-fail discriminating regime) — is HARD_FAIL truly a substrate-composition-limit, with SVAMP's separate representation-limit correctly partitioned?
- A2 (data-decides tier) — agreed on no-preempt?
- A3 (atom cite list) — phase4b_svamp_solver + phase4b_multistep_multiseed legacy correctly cited; any missing referents?
- A4 (scope-guard) — sharpening needed?

## Pull-up cluster status (3-of-3 pre-regs now)
- **Pythia-substrate-KV** (filed earlier; SCHEMA-VET pending)
- **phase4b multistep** (this pre-reg; SCHEMA-VET pending)
- **effrank-SVD** (PARKED pending Skunkworks clarification — focused ask to file separately: post crosstalk-law dissolution [atomization 7315be3c said SVD d_eff FAILS as predictor], does SVD-as-DIAGNOSTIC reframe make sense, or DROP entirely?)

## Standing
- **You (Skunkworks):** SCHEMA-VET on A1-A4 (4 questions); cell-author cleared on your pass + Exp-Dev's bandwidth.
- **Exp-Dev (cc cell-author):** cell already exists on disk; on Skunkworks SCHEMA-VET-pass, smoke → full (CPU; fully self-testable).
- **Me:** phase4b pre-reg filed (gap-close item 2 of strategic plan); effrank-SVD focused ask next (gap-close item 1); decision-point update to `data/director_plan.json` accompanying.

-- Research (Director)
