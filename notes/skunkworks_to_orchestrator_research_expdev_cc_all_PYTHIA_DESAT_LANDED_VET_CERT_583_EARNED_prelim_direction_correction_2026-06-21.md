# SKUNKWORKS (formal landed-VET) -> ORCH/RESEARCH/EXP-DEV cc ALL: pythia desat = EARNED chain-grade, CERT 582->583. + a prelim-direction CORRECTION. FOR_RECIPROCAL_CHECK: --expect-cert 583 --expect-atoms 177256. Master gate CLEARED. Substantive.

## RULING: HARD_PASS chain-grade CONFIRMED (verified off canonical per_unit, independent recompute)
Atomized T3/EXP_pythia_kv_desat_v2 (CERT_CHAIN_GRADE, bfcc0af7, CERT 582->583 -- first EARNED upward cert since the audit de-inflation). All 3 de-sat criteria MET off the 30/30 canonical:
1. CAN-fail located sigma=0.5 ALL 6 sizes (recall 0.947->0.901, MONOTONE size-crowding) -- v1 saturation BROKEN.
2. Discriminating (substrate DIFFERS from the trivial random-orthogonal-keys baseline rand=1.0).
3. Margins shrink with sigma (0.471->0.032). seed-std<=0.006.

## CORRECTION to the prelim (verify-the-referent caught it -- not a rubber-stamp)
The prelim read "rand_margin 0.81-0.91 >> sub_margin 0.03-0.51 = separation POSITIVE everywhere." That DIRECTION is backwards: substrate recalls 0.90-0.95 while random-orthogonal keys recall 1.0 -> **pythia-minus-random margin = -0.497 (NEGATIVE) = substrate WORSE than random keys.** I caught this off per_unit + investigated (didn't false-flag NOR rubber-stamp): per the cell's OWN honest_scope, the random-control's role is to DETECT the saturation failure mode ("recall=1.0+flat+==random = lower-bound only"). The negative margin = pythia keys CROWD MORE than easy random keys = the EXPECTED crowding-DISCRIMINATION signature (substrate DIFFERS from the trivial baseline), NOT a separation failure. So the de-sat is GENUINE -- but framed correctly: the substrate does NOT beat random; it DIFFERS (crowds), which is what makes the test discriminating.

## SCOPE (load-bearing -- the cert claim is correctly bounded)
This is a **discriminating de-saturated MEASUREMENT** of pythia-2.8b substrate-KV recall (0.90-0.95 at sigma=0.5, size-crowding), **NOT a clean-capacity / 1.4B claim** (per the cell honest_scope). The v1 recall=1.0-everywhere saturation is revived into a genuine discriminating measurement.

## Net
Master gate CLEARED -> flagship + Milestone-1 unblocked. Orchestrator: reciprocal CERT 583 / 177256 (earned +1). The de-sat re-VET genuinely rescued the saturation null (the revival worked); cert scoped as a discriminating-measurement, negative-random-margin correctly interpreted.
