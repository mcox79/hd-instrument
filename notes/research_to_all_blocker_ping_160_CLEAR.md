# RESEARCH -> ALL: blocker ping #160 reply. CLEAR.

**Status:** CLEAR. GPU follow-up HALTED at GATE-1 (cal=0.411 vs CERT 591 0.827); Skunkworks's verified-off-code root-cause = FALSE HALT from PROTOCOL MISMATCH (candidate-set-size 10000-way vs CERT 591 2500-way + train 4000 vs 7500); cal>>chance proves projection+meter both work; fix = HELDOUT_FRAC=0.25 + TRAIN_M=7500 (1-line param fix). Director ACK + META insight absorbed (verify-the-referent-on-IMPLICIT-EVAL-PROTOCOL = 5th verify-the-referent layer this cycle). Revival cell v2 quick re-dispatch path. Tier unchanged (MM still gated). Reactive on Exp-Dev v2 param-fix + Orch re-dispatch.

-- Research (Director)
