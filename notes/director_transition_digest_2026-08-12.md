# Director Transition Digest — 2026-08-12

Purpose: digest of RATIONALE (not just current state) from prior session docs, for a director session that started fresh (VS Code -> new client, fresh context). Read-only synthesis. Written incrementally; if truncated, see COVERAGE line at bottom for exactly what was read.

Sources (priority order):
1. notes/director_POST_COMPACTION_BACKUP_2026-08-04.md (~616KB, 794 lines — paged)
2. notes/THE_PLAN.md
3. notes/WHERE_WE_ARE_NOW.md
4. notes/SUBSTRATE_CHARTER_read_first.md (cross-check only)

---

## A. USER DIRECTIVES

[backup L3, 2026-08-12] USER (frustrated at over-analysis), quoted: "the LEARNER should build grounded experience -- move beyond this, read textbooks + curriculum, full auto all night, grow the foundation, get beyond this base." STILL ACTIVE — this is the current ACTIVE MISSION banner (topmost, dated 2026-08-12).

[backup L3] USER directive: "STOP drilling the grounding-SIMILARITY wall (CLOSED: same-idea is TAXONOMIC not similarity ... do NOT re-drill norm/encoder/WordNet probes -- the WordNet probe was KILLED per USER)." ACTIVE STANDING BAN.

[backup L3] USER: "focus on 2 and 3" (of a 3-item list) -- (2) validate the foundation is truly grounded + properly organized (correctness, not just growth-cleanliness); (3) scale+speed (CA3/codebook indexing). Explicit sequencing: "Only AFTER 2+3 validate solid+scalable, keep growing big." ACTIVE — current directed focus as of 2026-08-12, supersedes any impulse to keep growing the foundation size before these two gates.

[backup L3] USER full-auth 2026-08-12: authorized push of branch `dataprox/mcguffey-graded-corpus`[sic, actually `dataprep/mcguffey-graded-corpus`] to origin/GitHub (code+history only; `data/foundation` NOT git-tracked, deliberately not pushed — reproducible from code+corpora instead). ACTIVE, one-time authorization already exercised.

[backup L3, same paragraph] "CONCURRENT SESSION editing hdlab/lexical_similarity.py + capability_registry.jsonl + untracked orphan hdlab files -- DON'T touch (tracking cleanup blocked, awaiting USER)." ACTIVE STANDING CONSTRAINT — matches this task's own instruction to leave those files alone.

[backup L5, 2026-08-11] Roadmap set by USER (paraphrase inside director's own resume note, not a direct quote): land curriculum HARD_PASS -> architecture audit (per-component brain-fidelity) -> shore up meh components -> first CONVERSATION milestone (receptive side built; build expressive/production side). Framed as "USER-set, full auto, brain-foundational, right-path." ACTIVE (high-level roadmap), though superseded in sequencing specifics by the 08-12 "focus on 2 and 3" directive above.

[backup L21] USER, quoted: "full auto, brain faithful, right thing not the easy thing" — used by director to justify confronting the fill-in-the-unsaid wall directly rather than taking the MAVEN-ERE (A) benchmark-pivot shortcut. ACTIVE standing principle (recurs throughout as "do the hard blocking thing").

[backup L34] USER CORRECTION, load-bearing (2026-08-11 ~14Z): "store context-dependent facts in SUPERPOSITION, NOT an average." Diagnosed the v1 fade bug (entity-keyed symbolic resolution collapsed to one fate). ACTIVE, confirmed correct and adopted (v3 FHRR superposition store, L37 "STORAGE SOLVED").

[backup L39] USER: "make sure this test is fair" — led to the v4 fair-test lockdown that falsified the passage-binding hypothesis. Standing discipline, not a one-off.

[backup L42-43] USER caught director overstating a negative: quoted "if you read a book about physics you can learn physics from it" — corrected the director's premature "ALL no-LLM routes exhausted" conclusion (see Dead Ends / lesson below). This is the origin instance of the standing rule "don't generalize a narrow implementation failure to impossible" (already in MEMORY.md).

[backup L61, L92-94, L106] Recurring USER-authorized "07-14 pivot": FOUNDATION = any external tool, FULL+VETTED (may use LLM OFFLINE); RUNTIME REASONING = glass-box, NO external LLM at inference (stated as "THE invariant"); grounding optional. ACTIVE, load-bearing architecture principle referenced repeatedly through the whole log as the license for using an LLM to build (not run) the knowledge foundation.

[backup L47] "RUBICON" concept: crossing into first actual LLM use (offline, foundation-building only) requires explicit USER go — director treats this as USER's call each time it comes up, never self-authorizes it. ACTIVE standing constraint (role-separation on LLM use).

[backup ~L61-77, "USER cracked the resolution"] USER-authored the crutch-that-fades design: external CRUTCH fills world-knowledge gaps only when substrate flags one; substrate consolidates/generalizes the fill into schemas; crutch fires less over time. "USER framing correction (locked): knowledge lives in the SUBSTRATE; the LEARNER's job is to HELP the substrate accumulate (generalize), not be the warehouse." SETTLED DECISION, see Section B.

(more USER directives continue further into the file — additional entries appended below as later sections are read.)

---

## B. SETTLED DECISIONS

[backup L67-77] "Crutch-that-fades" architecture is the settled design for how the substrate acquires world knowledge during a learning phase: an external CRUTCH (CSKG, ATOMIC-derived) fills gaps ONLY when the substrate's own predictive-coding gate flags one; fills get consolidated/generalized into native schemas (hd_fact_store); crutch-firing rate drops over exposure (power-law, "never flat-to-zero"). Reason: brain-faithful (scaffolding/ZPD/CLS/Fitts-Posner automaticity) AND charter-clean (resolves 07-14 foundation-vs-runtime tension: crutch is a *learning-phase* scaffold, not a permanent inference tool). Do not re-litigate as "just bolt on a permanent KB."

[backup L34-37] Context-dependent facts MUST be stored in FHRR/VSA superposition of bound (entity,fate) pairs per process-register, retrieved by unbind+cleanup — NOT symbolic entity-keyed averaging/dict-resolution. Settled after USER correction; v3 FHRR store proved retrieval_self_consistency 0.9556 with per-process disambiguation (water@water-cycle vs water@respiration coexist). Do not revert to entity-keyed symbolic storage for context-dependent facts.

[backup L47, L106, L94] The "07-14 pivot" is settled program architecture, not merely a hypothesis: FOUNDATION built by external vetted tools (LLM allowed OFFLINE), RUNTIME reasoning strictly glass-box/no-LLM-at-inference. Reason: after exhaustive glass-box-only attempts on entity-level world knowledge failed (see Dead Ends C), this was judged the honest, already-authorized resolution rather than a new relaxation.

[backup L59] Capability-registry audit (2026-08-10, 3 parallel read-agents + director spot-VET) concluded the substrate has ~130 hdlab modules / 5,680 experiment cells / 82 registry rows, "0 FABRICATED / truly-phantom." Both apparent "phantom capability" scares that session were FALSE ALARMS (mislabeled search scope, not lost work) — settled, do not re-open a "we lost work" panic without new on-disk evidence. Standing rule adopted from this: every capability writeup states WHERE (hdlab vs experiment cell) + DATA SCOPE (synthetic/toy vs real) [[feedback_capability_audit_writeups_must_state_where_and_scope]].

[backup L92, "STORE ARC 2A-2G COMPLETE"] Candidate-retrieval store problem is SOLVED and CLOSED at full 1.2M scale via sparse+sharded+context-gated+CA3-attractor composition (shortlist-hit ~0.85, brain-faithful "hippocampal recall" metric). Single-item argmax final-selection is explicitly NOT the store's job — that belongs to the downstream context-validating loop (Stage-3). Settled after Stage-2G HARD_FAIL falsified the "deeper leaf-splitting will fix single-argmax" hypothesis. Do not re-open store-capacity tuning; if single-answer selection is weak, the fix belongs in the retrieve-validate LOOP, not the store.

[backup L106, L110-113] The substrate's UNIVERSAL binding constraint, independently re-derived 9+ times across unrelated cells/arcs (DesireDB, MCScript2.0, WIQA, ProPara, islanded-organ audit) is EXTRACTION: turning real prose into structured meaning. Every mechanism (bind/bundle, coref, situation-accumulate, schema, retrieve-validate loop) works on oracle/clean structured input and collapses when fed self-extracted real text. This is treated as settled fact about where the program's difficulty lives, not a one-off negative result.

[backup L92-93] Static (non-inferential) structured matching on real comprehension benchmarks LOSES to bag-of-words — demonstrated twice independently (MCScript2.0 scenario-discrimination E3/E3b, and the real MCQA task C6a9e0083). Settled: do not attempt "replace BoW with structured matching" again; value must come from INFERENCE on top of a content base ("augment-not-replace + abstain-gate" architecture, validated no-regression).

---

## C. DEAD ENDS

[backup L41, retracted L43] "Purist glass-box (no-LLM) acquisition of entity-level world knowledge is impossible" — 4 independent routes tried and initially declared exhausted: (1) small learned binder from ProPara-scale data (memorized, 0.0 unseen generalization); (2) structured KBs WordNet/ConceptNet/GloVe (98-99% coverage but don't encode process-specific fates, e.g. "wood relatedto fire" not "wood consumed-by combustion"); (3) shallow (thing,verb) extractor over scattered Simple Wikipedia (thin ~18% slice); (4) pure selectional-preference distributional learning (rigorously re-tested at full power, zero in-distribution signal). IMPORTANT CORRECTION (L43): the sweeping "impossible" conclusion drawn from (1)-(3) was RETRACTED by the director after USER pushback — each was a fair test of a NARROW/shallow implementation, not of the capability itself. The genuinely untested case is a DENSE, EXPLICIT, textbook-style corpus (not scattered encyclopedia) fed to the working 0.90-precision extractor. Retry condition: only reopen "no-LLM reading can't supply world knowledge" if the textbook-density version has also been fairly tried (this became the origin case for the standing rule in MEMORY.md about not generalizing narrow failures).

[backup L41 continued, later re-confirmed L45-47] After the textbook-density re-test angle was also exhausted through further probes (v3/v4/v5 fade tests + a final properly-powered selectional-preference check), the conclusion WAS reaffirmed as genuinely closed at accessible scale — "the wall's knowledge is not in structured KBs, not in word-similarity, not distributionally learnable... it is genuinely LLM-scale / massive-exposure knowledge." This time treated as robust because 3 independently-designed fair tests converged, not a single narrow test. Retry condition: would need either (a) a genuinely dense textbook corpus at much larger scale than what was tried, or (b) a materially different learning architecture — currently judged not worth re-attempting; the settled path forward is the 07-14-pivot LLM-offline-seed instead.

[backup L39] Passage-context binding hypothesis (that binding facts to a persisting discourse/situation-model context across a whole passage, rather than per-sentence, would let no-LLM reading re-derive seed-level process-conditioned knowledge) — FALSIFIED under full fairness lockdown (v4): passage-scope arm == sentence-scope arm to 4 decimal places; the real disambiguation blocker is PROCESS-VOCABULARY OVERLAP (e.g. respiration/photosynthesis/carbon_cycle share carbon/oxygen/glucose), which longer context cannot resolve because the disambiguating signal is the relational/directional structure itself (near-circular for a no-LLM reader). Retry condition: would need a genuinely process-dense/blocked corpus (textbook-like) plus relational/directional disambiguation structure — judged diminishing-returns, not pursued further; superseded by accepting "seed + reading-extends" as the honest achievable bootstrap.

[backup L33-38] Storage-side hypotheses for why the "crutch fade" didn't fully work were tried and ruled out one at a time: symbolic entity-keyed averaging (v1, root-caused as an artifact, fixed by superposition v3) and process-conditioned SENTENCE-level tagging (v2, capped by 85.5% sentence-level yield loss). Both superseded; current understanding is storage is solved (superposition), the wall moved upstream to ACQUISITION/disambiguation (see passage-binding dead end above).

[backup L44 "PURIST B-PROBE"] First selectional-preference probe (thing,verb argument-fate features, 6-tag presence-only scheme) returned HARD_FAIL_NO_GENERALIZATION but was flagged as UNDER-POWERED (zero signal even in-distribution) rather than a clean negative, per the standing "flat/degenerate learning result = broken experiment, not a ceiling" rule — NOT accepted as a dead end until re-run properly-powered (see L47, which then closed it for real).

[backup L54-57] Discrimination-feature iteration on the SIQa crutch-retrieval "which candidate does the graph evidence support" problem — 2 iterations (79c354a6d, e7cee79ae) both landed real-but-≤0 vs BoW, scramble-clean (mechanism real, not artifact) but not useful. EXIT reached per pre-committed exit criterion: "STOP grinding discrimination-feature iteration" after 2 clean negatives. Retry condition: only via the deeper situation-model/extraction fix, not more feature iteration on this specific loop.

[backup L56] A believed-real "Stage-2A retrieve-validate loop" organ was flagged mid-session as possibly a "phantom" (never actually built) — later found in the L59 capability audit to be a FALSE ALARM; it's real, just lives in experiments/ not hdlab/, so a naive hdlab-scoped search missed it. Lesson banked: search scope errors can look like lost work; verify both experiments/ and hdlab/ before declaring something phantom.

[backup L61-71] Multiple ProPara "world-knowledge bridging" sub-approaches were tried in sequence and each superseded the last: generic verb-lexicon+SRL (survival 0.22, SRL structurally can't source co-participation for unmentioned entities) → +ConceptNet co-participation (survival 0.223, promiscuous/non-causal, 52% domain-coverage gap on scientific processes) → offline-LLM-distilled process-physics KB (18 hand-vetted general rules, survival only 0.18 — SAME as the reused-KB attempts, meaning "build your own KB" did NOT beat "reuse an existing one"). The distilled-KB result RETRO-CORRECTED the earlier "oracle bridging knowledge is load-bearing" finding: the oracle's real value was actually INSTANCE-SPECIFIC trigger-verb localization, not general world-knowledge — so the true residual is a READING/EXTRACTION problem (which unmentioned step a known effect lands on), not a knowledge-supply problem. This directly motivated the pivot to (B) frame-activation reading.

[backup L73] ProPara frame-activation matcher v1 (literal set-intersection matching of role-words to text tokens) — root-caused as broken (shares zero tokens between "combustion" and "fire consumes wood") and swapped for graded `concept_similarity`; superseded, do not reintroduce literal token-intersection matching for frame/role identification.

[backup L96-100] WIQA causal-chain-loop hypothesis (multi-hop causal edge-sign propagation adds real causal reasoning value) — FALSIFIED decisively (be812e883): on every subset where the loop fired, a scrambled-edges control scored equal or higher; the apparent lift was topology/coverage-structural, not causal-edge reasoning. Further, WIQA's causal SIGN was found to leak the answer label at 1.0 (oracle diagnostic), meaning WIQA's actual difficulty is EXTRACTING signed structure, not reasoning over it once extracted — so WIQA mis-fits the loop's intended reasoning contribution. Learned-extraction of edge-polarity also HARD_FAILED (severe overfit, train 0.982/test 0.412) with the diagnosis that edge-polarity is world-knowledge, not a learnable text pattern. Settled: STOP iterating WIQA as the flagship; do not re-dispatch WIQA causal-chain cells.

[backup L84-93] MCScript2.0 as flagship benchmark — abandoned twice over independent findings: (i) content/BoW alone scores ~0.63 (near SOTA 0.72), so it's largely content-saturable and doesn't isolate reasoning value; (ii) its causal-"why" fraction is only 0.7%, mistargeting the program's strongest organ (causal-validate loop); its temporal subset (32.7%) is real but thin and content-swamped. Superseded by pivot to WIQA (which then also dead-ended, see above) and later to MAVEN-ERE.

[backup L91-92] Two "scale wall" storage rescues attempted and abandoned in sequence: RESONATOR FACTORIZATION for KGStore capacity — HARD_FAIL (decode/basin-proliferation problem, never beat baseline even at 1K); sharding-by-source-provenance (Stage-2D) — real partial win but capped by 3 oversized shards (skew wall) and a weak (0.20 margin) shard key. Both superseded by hierarchical subject-tier sub-sharding (Stage-2E), which then itself hit a fine-decode wall closed by declaring single-argmax "stricter than the brain" (see Settled Decisions).

[backup L106] Glass-box-from-scratch EXTRACTION (building comprehension without any external tool) — treated by the director as "empirically falsified across 9+ independent cells" as of 2026-08-10, motivating the 07-14-pivot commitment. Retry condition: none named; this is the reasoning that licensed moving to externally-vetted extraction/foundation-building instead.

(continuing — more dead ends appear further into the file, especially around the 08-11 ProPara/MAVEN-ERE arcs already partially covered in section A/B; appended as encountered.)

[backup L92-93 / WHERE_WE_ARE_NOW.md L157] RECENCY-SHAPED relational selection — a recurring, independently-rediscovered dead end across THREE separate subsystems: (1) coreference antecedent selection (`_pick_strict_cb`, "recency-falsified" — grabs the nearest antecedent instead of the coherent one, 0/4 on pronoun traps); (2) causal-coherence selection (the in-sim reverse-replay selector v1-v4 arc, memorizes/degrades rather than tracking true cause); (3) goal-outcome discourse binding (recency_binding_accuracy only 0.333). Lesson banked as a STANDING PATTERN, not three separate bugs: "the substrate keeps approximating with RECENCY-shaped selectors that fail the recency-trap" — the fix class is a general COHERENCE-BASED relational binding/selection organ (select by explanatory/goal coherence, not proximity), not a per-subsystem patch. Relevant if any future component does antecedent/candidate selection — check it isn't quietly recency-shaped.

[WHERE_WE_ARE_NOW.md L151] Text-based lexicon induction (recovering held-out affect/withhold-verb lemmas from raw corpora via glass-box selectional-association) — HARD_FAIL, caught as a false pass by the agent itself (pre-registered bands nominally passed but VET revealed the affect gate was inert ~1.2% fire-rate on any sentence, the scorer was degenerate from a windowing artifact, and the induction never actually fed the loop — recovered_target_lemmas=[]). Deeper finding standing behind this negative: affective/experiential meaning (valence, harm-feel) is NOT inducible from text distribution at all (Andrews/Vigliocco/Vinson 2009, field-wide unresolved cap) — only STRUCTURAL/lexical patterns are text-inducible via selectional association. This is the literature-level origin of the "you can't learn revenge from a book" framing that recurs throughout the whole log. Retry condition: none for affect-from-text; structural-only induction is retestable with a fixed (non-degenerate) scorer if ever needed again.

[WHERE_WE_ARE_NOW.md L21, L28-30] Coherence-selector arc (predecessor-SR / reverse-replay antecedent selection) — v1 HARD_FAIL (pure memorization, chance on held-out); v2 HARD_PASS but only in a favorable regime (known types, novel instances only); v3 MIDDLE_BAND (genuinely abstract rule confirmed on novel types, but degrades on 2-hop to 0.557); v4 bidirectional/meet-in-middle — 2-hop improves modestly (+0.07) but 3-hop collapses below chance (~0.35 vs random 0.50), and the "meeting premium" test was confounded (forward/backward maps were over the same permutation, largely redundant) so that specific claim was never cleanly established. Director explicitly called STOP on further in-sim multi-hop grinding at this point ("approaching INSTRUMENT-PERFECTIONISM on a synthetic toy") and pivoted to real-text transfer instead. Retry condition for 3-hop: would need either complementary (non-redundant) forward/backward maps or iterative multi-step composition — not attempted, judged low-priority since narrative causation is mostly 2-hop.

---

## D. STANDING CONSTRAINTS

[backup L356, "THE GOAL (unchanged)"] Core invariants, stated as unchanging across the whole document: "glass-box always; NO borrowed embedding / NO external LLM at inference / NO bolt-on reader-parser; supplying DATA/knowledge/a lexicon is fine; store LOCAL-ONLY, git-commit after every bank, NO origin push without in-session auth. Brain = existence-proof + reference standard." ACTIVE — this is the top-level charter, restated verbatim at L373 as well ("supplying DATA/lexicon/a-representation-SCHEMA is fine, supplying a MECHANISM that works un-brain-like is the anti-pattern").

[backup L358-363, "USER BARS (locked, govern everything)"] Verbatim locked list:
  - "EARNED understanding not supplied-assignment; brain-foundational in EVERY aspect; supply only genuine innate seed, EARN the rest."
  - "FORMALIZE before building: map the brain (SHAPE+POSITION+METRIC) -> per-component compare vs our organs -> name the gap -> brain-accurate build. For EVERY mechanism: which BRAIN STRUCTURE + does it SHARE a built process -> REUSE the organ."
  - "VET every negative HARD (incl. whether each component is brain-foundational); VET positives AS HARD AS negatives ... Report MEASURED not READ; disk-verify agent claims."
  - "Full-auto: drive, do what's RIGHT not easy; select by brain-foundational-right not cheap. Keep docs current. WIRE gains, don't island."
  - USER 2026-08-04 standing rules (active via the 30-min self-drive cron): (1) every negative -> drill hard + per-element brain-fidelity audit; (2) every negative -> also ask "is a needed COMPONENT missing — especially a LEARNING capability?" with explicit routing (used-ability-wrong->loop / missing-primitive->BUILD / missing-fact->SUPPLY / missing-LEARNING->REUSE/EXPAND hdlab/learner); (3) VET positives as hard as negatives, PER-AXIS never aggregate, triple-check load-bearing data; (4) READ THE CODE not the label; (5) keep all docs current.
  All ACTIVE, none marked superseded anywhere in the file — this is the constitution the rest of the log operates under.

[backup, recurring throughout, e.g. L92, L114, L138] "NEVER git add -A" on the store/working tree; commits must be TARGETED. A "concurrent-git churner" (~10k dirty files from a concurrent session / remote-state-cache sync) has repeatedly WIPED uncommitted working-tree changes — the documented mitigation is atomic write+add+commit in one process (scratchpad/bank_docs_atomic.py pattern), never leaving uncommitted docs sitting. ACTIVE, recurs as a live hazard multiple times across the log, not a one-off.

[backup L114, L124] "git-commit after every bank" — sonnet model = research/exp_dev; opus = director/skunkworks/audit (model assignment discipline, matches CLAUDE.md's "superpowers" section "name the model on every dispatch").

[backup L356, L373] Store is LOCAL-ONLY; NO origin push without in-session auth — this is the general rule; the L3 top-banner item about the 08-12 branch push is an explicit, logged, one-time exception granted by USER, not a relaxation of the standing rule.

[backup L3] Concurrent session is editing hdlab/lexical_similarity.py, data/capability_registry.jsonl, and untracked orphan hdlab files — DO NOT TOUCH, tracking cleanup is blocked pending USER. Directly matches this task's own do-not-touch instruction, confirming it's still live as of the newest entry in the file.

[backup L61, L94, L106] Charter distinction repeatedly re-applied as a constraint on what counts as an allowed "external tool" use: FOUNDATION-building may use an external/offline LLM if vetted; RUNTIME inference must remain glass-box with zero LLM involvement. Every arc that reaches for external help re-derives and re-confirms this boundary rather than treating it as settled once — treat it as an active constraint to re-check on every new external-tool proposal, not just cite from memory.

---

## E. OPEN THREADS

[backup L3, current] IN-FLIGHT at the moment of the last compaction (2026-08-12): "cycle-2 bio finishing (aabae9339) + self-directed-gap-loop 'know what to read next' building (a7ace81e) -- VET both on disk on resume." First action on resume should be checking these two.

[backup L3] The USER's "focus on 2 and 3" directive (validate foundation correctness/organization; scale+speed) is explicitly NOT YET STARTED as of the last banner — a harness build was noted "in flight (hdi_exp_dev a1286a7f)" for item (2) validation. Check its state first.

[THE_PLAN.md L67-68] "IMMEDIATE NEXT (when building resumes — NOT started per USER)": R1 global/landmark RKD objective fix on the concept encoder. STATUS: almost certainly abandoned/superseded per the contradiction noted in Section F — but technically never explicitly closed out in the docs read, so it is formally still "open" in THE_PLAN.md's own terms. Treat as dead unless USER explicitly revives it.

[backup L106] "The FLAGSHIP-benchmark pivot ... is the USER's call; this gate-test INFORMS it" — several USER-facing forks were surfaced as prose recommendations through the 08-09/08-10/08-11 arcs (e.g., MAVEN-ERE (A) vs learned-grounded-binder (B) vs reconsider-thesis (C)); per the log, USER responded with self-drive "make the call, drive" more than once rather than picking explicitly — the director exercised default judgment each time. No item here appears to be currently BLOCKED awaiting a USER answer as of the L3 top banner (the mission is actively running full-auto), but a fresh session should not assume every historical "HELD for USER" note is still open — most were resolved by a later self-drive call. Cross-check the specific fork against the top-of-file (L1-40) timeline before treating any as still pending.

(WHERE_WE_ARE_NOW.md not yet read at time of this entry — additional open threads may be added below.)

--- CHARTER cross-check notes (SUBSTRATE_CHARTER_read_first.md) ---

[charter §0] The charter formally defines a "3-doc system": THE_PLAN.md (WHAT+WHY, changes on real pivots only), SUBSTRATE_CHARTER_read_first.md itself (timeless rules + terse frontier pointer, read FIRST), and WHERE_WE_ARE_NOW.md (fuller live state/history, updated every session). This 3-doc system is itself a standing convention — a fresh session should read all three in that order, not just the backup doc. NOTE: this task's source list treats director_POST_COMPACTION_BACKUP as source #1/highest priority instead of following this exact 3-doc order — that's a deliberate override for this transition-digest task per its own instructions, not evidence the 3-doc convention has changed.

[charter §2a INVARIANTS, D] Restated crisply: (1) glass-box, reasoning inspectable, no external LLM at inference; (2) no borrowed embedding as the meaning organ AND no bolt-on existing reader/parser as the comprehension organ — "supplying KNOWLEDGE/DATA/STRUCTURE is fine; supplying the meaning/comprehension MECHANISM is the forbidden shortcut. (Borrowed models = DIAGNOSTIC-only, then discarded.)"; (3) brain = existence proof + reference standard, a shortfall is never a ceiling. ACTIVE, matches backup's "THE GOAL"/"USER BARS" verbatim in spirit.

[charter §2b, D] Additional standing disciplines not as explicitly stated in the backup: "VET every load-bearing verdict; REPLICATE positives across seeds/runs BEFORE believing. A single-seed/single-run 'win' is a HYPOTHESIS, not a result." Also "MEASUREMENT-FIRST: before trusting a metric, prove it detects the effect in a POSITIVE CONTROL." Also "COMPONENT-FIDELITY-FIRST (USER 2026-07-28): nail each component brain-faithful ONE-BY-ONE, then assemble. Judge each component on its OWN BRAIN METRIC ... NOT on a downstream task-win." Also "Check prior work FIRST — on the filesystem AND the substrate (`tools/director_kb_query.py`, and the capability registry)."

[charter §2c, D] "Store writes LOCAL-ONLY, binary/newline='' ... git-commit atoms+ledger after every bank. NO origin push / remote-persist without in-session USER auth." Also: docs must be timeless where they claim to be static; time-bound specifics live only in BACKUP + registry; living docs get stable dateless filenames updated in place (a dated filename is itself flagged as a "staleness trap").

[charter §2d, D] Formal CAPABILITY-INTEGRATION GATE: query `tools/capability_registry_query.py --serves "<need>"` before designing a cell; at land-time anything cert/HARD_PASS/chain-grade MUST get a `data/capability_registry.jsonl` row with gate_decision WIRE(+target) or SHELVE(+revival_criteria) — "No limbo."; integration status is auto-computed from the import graph, never hand-typed; SESSION START runs the audit as the durability anchor (not a cron).

[charter §2e, D] "Only STOP/KILL what THIS session spawned (a concurrent session may be live). Full-auto = make the call, don't stack questions; at a strategic fork give a PROSE recommendation." (Confirms the AskUserQuestion ban implicitly via "prose recommendation," made explicit and emphatic elsewhere in MEMORY.md.) "Heartbeat every turn-end."

[charter §4, D] The literal ANTI-DRIFT self-check phrase to run before every dispatch: "Does this serve the current focus (per the BACKUP doc), the brain's way, and did I check the registry/prior-work first? If you're testing tricks over supplied symbolic KBs, reaching for a borrowed vector/reader, re-deciding a settled direction, building before checking the registry/prior-work, or believing a single-seed positive — YOU HAVE STRAYED. Re-anchor here."

[charter §3, history stack cross-check, B/C] The charter's own dated "CURRENT FRONTIER" stack (most-recent-first) independently confirms the backup's chronology: 2026-08-11 LATEST-3 (three-tier architecture proven, pivot to reading) is the charter's topmost/current entry — i.e., **the charter itself is STALE relative to the true newest state**: it has not been updated past 2026-08-11 LATE, while the backup doc's top banner is dated 2026-08-12 and describes substantial further progress (foundation growth cycles 1-2, persistence proof, self-directed gap loop). See Contradictions section. Everything charter §3 lists below the 08-11 LATEST-3 entry is explicitly self-labeled HISTORY by the charter and adds no new active items beyond what's already captured from the backup.

---

## F. CONTRADICTIONS

[THE_PLAN.md L1-3 vs backup L1-3] THE_PLAN.md's top "STRATEGIC STATUS" banner is dated 2026-08-08 LATE and describes the DesireDB goal-achievement arc's honest reckoning as the current frontier, ending with "STRATEGIC FORK PENDING USER" between (i) consolidate-and-stop, (ii) attack deep grounding, (iii) pivot to auditability, (iv) step back. This is STALE relative to the backup doc, which shows the program moved dramatically past this point over 2026-08-09 through 2026-08-12: DesireDB was in fact banked+wired (goal_achievement.py, macro-F1 0.686→ cross-corpus parity), then the whole program pivoted through simulation-engine, crutch-that-fades, MAVEN-ERE, ProPara frame-activation, and now (as of 2026-08-12) the active mission is reading-based foundation growth. THE_PLAN.md does NOT reflect any of this. A reader following THE_PLAN.md alone would think the DesireDB fork is still open and unresolved — it is not; do not re-surface that fork. THE_PLAN.md is the OLDER/staler document; the backup (2026-08-12) is authoritative.

[THE_PLAN.md L27 "THE CURRENT FOCUS / the blocker" = REPRESENTATION layer / concept-encoder scaling] vs backup's current mission (read textbooks, grow the foundation via reading, validate correctness, scale/speed). THE_PLAN.md still frames "IMMEDIATE NEXT" (L67-68) as "R1 global/landmark RKD objective fix on the concept encoder" — this build target appears to have been completely superseded/abandoned in the backup's narrative (which never mentions it after 2026-08-08) in favor of the comprehension/extraction/foundation-reading arcs. Likely simply stale (THE_PLAN.md not updated since 08-08), not an active alternate plan — flag so it isn't mistaken for a live parallel directive.

[backup L3 vs backup L3, same paragraph, internal contradiction] The 2026-08-12 top banner states BOTH "branch dataprep/mcguffey-graded-corpus PUSHED to origin/GitHub ... all committed code+history+cells+mechanism backed up OFF-laptop" AND, in the same top-of-file banner block, "Banked HEAD 362614675+; branch dataprep/mcguffey-graded-corpus; LOCAL-only, NO origin push." These two clauses read as contradictory (pushed vs local-only-no-push) within the SAME entry. Likely the "LOCAL-only, NO origin push" phrase is inherited boilerplate from the standing-constraint reminder (see Section D) and was not updated to reflect the one-time authorized push — but a fresh-context session should verify current git remote state directly (`git log origin/dataprep/mcguffey-graded-corpus`) rather than trust either clause blindly.

[charter §3 vs backup L1-3] SUBSTRATE_CHARTER_read_first.md's §3 "CURRENT FRONTIER" (the section explicitly documented as rewritten every session) is dated 2026-08-11 LATEST-3 and has not been updated to reflect the backup's 2026-08-12 ACTIVE MISSION banner (foundation-growth-by-reading, cycles 1-2, persistence proof, self-directed gap loop, "focus on 2 and 3"). Per the charter's own §0 definition, this section is supposed to be updated every session — it is at least one session behind the backup. A fresh session reading the charter alone (as instructed by CLAUDE.md's STEP 0) would land one full arc behind; the backup doc must be treated as authoritative over the charter's §3 when they disagree, despite CLAUDE.md nominally saying charter comes first.

[WHERE_WE_ARE_NOW.md L3 vs charter §3 vs backup L1-3] WHERE_WE_ARE_NOW.md's topmost banner is dated 2026-08-09 LATEST — this is the STALEST of the three living docs (backup=08-12, charter=08-11, WHERE_WE_ARE_NOW=08-09), despite the charter's own §0 explicitly designating WHERE_WE_ARE_NOW.md as "updated EVERY session" and the "fuller live state." It is roughly 3 days / multiple major arcs (simulation-engine, crutch-that-fades, MAVEN-ERE, ProPara frame-activation, reading-foundation-growth) behind. A fresh session must NOT treat this file as reflecting anything past the DesireDB goal-achievement arc's field-parity conclusion — everything since (the entire "commit to Direction B" program and its many sub-arcs) is absent from it. Rank order of currency for resolving any disagreement: backup (2026-08-12) > charter (2026-08-11) > WHERE_WE_ARE_NOW.md (2026-08-09) > THE_PLAN.md (2026-08-08).

---

## COVERAGE

1. **notes/director_POST_COMPACTION_BACKUP_2026-08-04.md** (794 lines, ~616KB) — FULLY READ in detail: lines 1-215 (2026-08-10 through 08-12, the entire crutch-that-fades / simulation-engine / MAVEN-ERE / ProPara frame-activation / foundation-growth-by-reading arcs — the current-most material), lines 355-390 (THE GOAL, USER BARS locked, the 2026-08-07 authoritative current-state block), lines 748-794 (KEY VERIFIED RESULTS, KEY ORGANS reuse map, RECOVERY PROCEDURE, and the 2026-08-09 CONSOLIDATED FRONTIER block). NOT read in comparable depth: lines 216-354 (2026-08-08/08-09 DesireDB goal-achievement arc detail — sampled via WHERE_WE_ARE_NOW.md instead, which covers much of the same ground in different wording) and lines 391-747 (2026-08-04 through 08-07 situation-model / thematic-role-labeler / role-labeler build arcs, and the 2026-08-04 capability-reconciliation audit). A full section-header map of the whole file was extracted via grep (all `##`/`###` headers), confirming no additional "RESUME HERE" or "CURRENT STATE" banner exists in the unread middle section beyond what the header list shows — the unread region is earlier-arc detail explicitly self-labeled HISTORY/superseded by material I did read, but I did not verify its dead-ends/settled-decisions line by line, so some finer-grained items from that middle section (2026-08-04 through 08-08, i.e., the goal-owner attribution pipeline build, the coherence-selector v1-v4 arc detail beyond what WHERE_WE_ARE_NOW.md gave, and the capability-reconciliation audit specifics) are NOT captured here beyond what surfaced via WHERE_WE_ARE_NOW.md and the header scan.

2. **notes/THE_PLAN.md** (69 lines) — FULLY READ.

3. **notes/WHERE_WE_ARE_NOW.md** (157 lines, ~102KB) — READ lines 1-34 and 145-157 in full (the newest banner block covering 2026-08-09 down through 2026-08-03/04, and the tail of the 2026-08-04 arc: self-extension/induction-frontier/discourse-scale-binding findings). NOT read in detail: lines 35-144 (middle of the 2026-08-04 arc — real-text transfer probes, grounded-valence-read detail, and intermediate coherence-selector steps); a header/keyword grep across the full file found no additional major section boundaries or LOCKED/BANNED constraints in that unread middle beyond what was already captured elsewhere, but individual dated sub-findings in that range were not individually verified.

4. **notes/SUBSTRATE_CHARTER_read_first.md** (61 lines) — FULLY READ, used for cross-check as instructed.

**Currency ranking used to resolve disagreements (newest first):** director_POST_COMPACTION_BACKUP (2026-08-12) > SUBSTRATE_CHARTER §3 (2026-08-11) > WHERE_WE_ARE_NOW.md (2026-08-09) > THE_PLAN.md (2026-08-08). All three "living" docs other than the backup are stale relative to it by varying amounts — see Section F.
