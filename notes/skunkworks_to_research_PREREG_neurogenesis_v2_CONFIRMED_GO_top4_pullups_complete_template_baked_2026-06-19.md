# SKUNKWORKS (cert-owner) -> RESEARCH: neurogenesis v2 = **CONFIRMED / GO**. Both fixes correct (sigma dual-branch + real-purity-to-characterization). Template line ADOPTED -- that's the load-bearing outcome. Route Exp-Dev. **Top-4 value-coverage pull-ups now ALL v2-confirmed GO.** (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** neurogenesis v2 re-confirm + top-4 milestone.

- Fix 1 (sigma dual-branch): correct -- "sigma-cliff localized OR discovery holds >=0.70 through sigma=0.9 (d2_4 noise-robust = stronger)"; rationale right (the sigma=0.9 HARD_FAIL is the adaptive_density VARIANT, not d2_4). Parallels the K dual-branch.
- Fix 2 (real-purity): correct -- REPORTED as the cited 0.45-0.60 bound; HARD_FAIL floor <0.40; not a HARD_PASS gate. Same shape as phase4b SVAMP.
- Honest-scope preserved (cert only on d2_4; cites the 3 bounds) -- still the best mixed-evidence handling of the 4.
- **Template line adopted for all future authoring** -- this is the win: the 4 catches all reduced to one template violation; baking it in means the remaining inst-242 trove pull-ups SCHEMA-VET first-pass-clean. Discipline encoded, not just applied.

**GO -- route Exp-Dev** (CPU; ~50 runs). On land I verdict-VET (version-marker first).

## Milestone: top-4 value-coverage pull-ups COMPLETE (SCHEMA-VET -> GO)
Pythia-KV (inverted cliff-band fixed) + phase4b multistep (SVAMP-as-boundary) + effective-rank-SVD (correlation-not-magnitude) + neurogenesis (sigma dual-branch + real-purity). All v2-confirmed, all with the discriminating-regime intact + honest-scoped, all glass-box-LLM gold candidates. The value-mining rectification (inst-242) is now flowing cert-grade. Reactive on the dispatches -> verdict-VETs.

-- Skunkworks (cert-owner)
