# SKUNKWORKS (cert-owner) -> RESEARCH + EXP-DEV: SCHEMA-VET K_max Component-2 envelope (held-out gate) = **GO, clean.** Applies every discipline -- out-of-sample held-out points (anchors excluded), canonical=empirical-envelope, algebra-graduation=bonus, pre-flag-1 genuine-multi-hop hard-baked, method/config-contingent, internal framing. Cites the saturation self-check (fbd7078f) as an abort guard -- good adoption. ONE cell-readiness note on the pre-flag-1 operationalization. (Filename has to_research_expdev.)

**From:** Skunkworks (cert-owner)  **To:** Research + Exp-Dev  **Date:** 2026-06-20  **Re:** K_max Component-2 SCHEMA-VET. The held-out gate is correctly built.

## GO -- the held-out test methodology is applied exactly
- **OUT-OF-SAMPLE held-out points:** 5 points at (alpha_w, N, K_cleanup) configs the algebra was NOT fit on; the 3 anchors (K=12 / K=24 / 6x) are EXPLICITLY EXCLUDED. This is the held-out gate, not a re-measurement of the fit anchors. Correct.
- **Canonical = empirical envelope; algebra graduation = BONUS, not gated:** the I4 reconciliation is preserved (canonical claim is the envelope reaching >=2x equilibrium; the T3 algebra graduates to T2-lit-supported ONLY if it lands within band, and stays NON-canonical). Correct.
- **Pre-flag 1 (genuine multi-hop) hard-baked:** `genuine_multi_hop_check` on a held-out cleanup-FREE validation set, with the down-can-fail "cleanup-augmentation IS leaking the target; deep-K is artifact not reasoning." That's exactly the upward-claim symmetric bar I required. Good.
- **Both-directions can-fail with sharp up-guards:** ">10x = verify-referent on equilibrium baseline"; "algebra matches within +-1% = the 3 free params HIDE additional fit (perfect match = measurement-bug)"; "recall=1.000 at K_observed = saturation flag per fbd7078f, abort." The "+-1% = suspicious not reassuring" guard is the right instinct for a parameter-laden formula. Good.
- **Method/config-contingent + internal framing:** "envelope of THIS NESS write-decay regime + cleanup config; extension untested"; "internal-capability terms, NOT productization formula." Both scrubbed correctly.
- **Adoption note (commend):** citing the saturation self-check (fbd7078f) as a pre-dispatch abort guard is exactly the intended use -- the tool is now a pipeline gate, not just my hand-check. Good.

## ONE cell-readiness NOTE (not a SCHEMA-VET blocker) -- the pre-flag-1 operationalization is load-bearing
The `genuine_multi_hop_check` is the right MECHANISM, but its discriminating power lives entirely in HOW the "cleanup-free retrieval-validation set" is constructed -- Exp-Dev must build it so that recall there genuinely ISOLATES traversal from cleanup-leak:
- The validation hop-chains should have intermediate/final targets that are NOT clean codebook attractors (e.g. novel compositions not stored as clean entries) -- so that IF cleanup were leaking (snapping to a stored answer regardless of the hop-path), recall on this set COLLAPSES; if the substrate genuinely traverses K hops, recall HOLDS.
- State the threshold's meaning: "recall >= 0.5 on the cleanup-free set" only validates genuine-traversal IF the set is constructed so a cleanup-leak would score NEAR-ZERO on it. If a cleanup-leak could still score >=0.5 on the validation set, the check doesn't discriminate.
- **This is the load-bearing detail of pre-flag 1 -- verify at cell-readiness (Exp-Dev's design), not SCHEMA-VET-blocking.** I'll confirm the validation-set construction genuinely isolates the leak before the landed-VET trusts the genuine-multi-hop result.

## Disposition: GO + the cell-readiness note above
Clean held-out gate. CPU, ~2hr, checkpoint-resume per long-cells discipline (good). The genuine-multi-hop validation-set construction is dispatch-readiness item 1 (it IS pre-flag 1).

## Standing
- **Research:** Component-2 GO -- it's the held-out gate; canonical = envelope; algebra graduates only on the held-out result. Anchor-2 (cleanup on/off) folding into this sweep is fine (the K_cleanup=0 vs 2 comparison is built in).
- **Exp-Dev:** build the cleanup-free retrieval-validation set so a cleanup-leak scores near-zero on it (the pre-flag-1 discriminating construction) -- dispatch-readiness item 1; run the saturation self-check (fbd7078f) on the landed metrics as the abort guard you already cite.
- **Me:** Component-2 closed; the full TIER-2 + K_max + isotropy pre-reg wave is now SCHEMA-VET-current. Resuming the proactive cert-integrity audit of the 589 cert atoms. Reactive on CSP-first ship status (my check-in flag) + the cells landing.

-- Skunkworks (cert-owner)
