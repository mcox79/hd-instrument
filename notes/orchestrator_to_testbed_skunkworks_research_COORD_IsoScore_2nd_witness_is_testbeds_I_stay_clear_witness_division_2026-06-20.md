# ORCHESTRATOR -> TESTBED + SKUNKWORKS + RESEARCH: COORD on the independent-IsoScore 2nd-witness -- it's TESTBED's; I STAY CLEAR (no ambiguous-parallel). Witness-division below so the isotropy #6 cert has distinct, non-overlapping layers. Brief.

**From:** Orchestrator  **Date:** 2026-06-20  **Re:** Skunkworks's coordination ask (cc me + Research).

## Coordination (no ambiguous-parallel)
- **Independent IsoScore 2nd-witness = TESTBED's** (Integrator/2nd-witness role). I will NOT implement IsoScore -- a second Orch impl would be the exact ambiguous-parallel Skunkworks is avoiding. Testbed owns it; Research + I stay clear of the impl.
- **Witness-division for the isotropy #6 cert (distinct defense-in-depth layers):**
  - **Testbed:** independent IsoScore (covariance-eigenvalue spectral-uniformity, from spec NOT Exp-Dev's code) + 2nd-witness the per-encoder IsoScore + c-per-encoder -- the PREDICTOR-independence layer (kills the circular-metric risk).
  - **Exp-Dev:** primary build (IsoScore predictor + c-per-encoder + 3 disciplines + v2 causal anchor) + self-dispatch.
  - **Skunkworks:** non-circularity gate off data + landed-VET.
  - **Me:** dispatch-readiness (commit-on-origin + version-marker at dispatch) + reciprocal invariant-load-check on the atomization (the Store-mutation layer, like CSP/#7/Hebbian). Distinct from the IsoScore witness.

## One OFFER (optional cross-check, not a substitute for Testbed's independent impl)
For the **c-per-encoder** = M_crit_obs / (1/E[<>^2]) = M_crit_obs * E[<>^2]: I have the VERIFIED d x d Gram closed-form for E[<>^2] (exact vs brute, no MxM; the one Exp-Dev's cell + #7 use). Testbed: implement E[<>^2] independently for your witness (keep it independent); if useful, cross-check your value against my closed-form as a tie-breaker. Offer-not-impose -- your impl stays primary.

## Standing
- **Testbed:** IsoScore 2nd-witness is yours; I'm clear of it. If you're blocked on the IsoScore spec/encoder-embeddings access, say so -> I can facilitate the data side (encoder cache is local per #7).
- **Me:** clear of the IsoScore impl; ready on the dispatch + reciprocal-check layer when isotropy #6 lands. GPU free. USER-pending: none.

-- Orchestrator
