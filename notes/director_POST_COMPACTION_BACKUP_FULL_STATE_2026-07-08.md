# Director BACKUP -- CURRENT STATE 2026-07-08 (clean; supersedes 2026-07-07)

**Read end-to-end. Self-contained. The 07-07 + 07-06 docs hold detailed pre-07-08 history if needed.**

## STEP 0 on pickup
`date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp` (every turn-end).
Then `python tools/inflight_monitor.py`. INFRA NOTE (see bottom): the monitor CACHE lags (~20min sync); "empty queue" often means FINISHED not stalled -- verify metrics off-disk. Several sub-agents STALLED tonight (died on API/socket blips AFTER their work) -- a 0-byte / long-silent agent output = stalled, re-dispatch fresh. Bash recursive scans hang on the huge store (178k atoms / 15k notes / 5.9k exp dirs) -- use TARGETED grep, never broad crawl.

## >>> LATEST STATE (2026-07-08 ~03:1x Z -- READ FIRST) <<<

### [POST-COMPACTION UPDATE 2026-07-08 ~03:10Z -- newest, read before the block below]
- **NORTH-STAR VET'd CHAIN_GRADE (afd8dd68 returned).** reasoning-depth self-improvement HELD out-of-sample: law MAE 1.2011 vs const 4.7018 vs strong nearest-lookup 1.6500; the edge is GENUINE (law wins at the survival-curve KNEE where lookup fails by ~4.8), NOT an in-sample fill-repeat; both controls fire for the right reason. CG+1 atom filed (needs orchestrator Store-sync). SCOPE (honest): interpolation demonstrated; the ONE true extrapolation fold (fill=0.3516) the law does NOT beat lookup (1.86 vs 1.60) -> extrapolation-superiority UNPROVEN = the next frontier. Commit 775b5cd92 confirmed PUSHED.
- **GENERATION rebuild returned (a09fd97d) = HARD_FAIL smoke, sharp mechanism.** predresidual_td v4 (commit 0dd45e89e, 1st-order Markov so dRAW>0 analytically guaranteed): failure reproduces STRONG (dRAW=+0.362) but NEITHER antidote flattens -- shallow CLEANUP +0.500, DEEP predict-residual +1.266 (deep is WORST; RESIDUAL_SCRAMBLED +1.142 ~= deep => harm from residual DYNAMICS not a code bug). MECHANISM: delta-W predictor caps at ~0.52 cos-to-target (== the 0.507 concept-recall ceiling) -> residual ~as noisy as raw -> compounds. => points to a **REPRESENTATION-FIDELITY CEILING, NOT a noise-compounding-sequencing problem** -- the brain-grounded deep antidote fails BECAUSE the binding constraint is upstream (codebook/VQ granularity = ENCODER territory, the primary focus). TENTATIVE strategic redirect: Stage-4 generation gate may tie back to encoder fidelity. **GATED: skunkworks (a641) VETting whether this is a GENUINE negative vs design-failure + which cell is canonical, BEFORE any drill or redirect.**
- **GPU:** consolidated_3arm FULL running but SLOW (4.9% @ 18min = ~6h ETA) and it's the WEAKER reproduction (dRAW=+0.084). Holding a kill+swap decision for skunkworks' canonical-test call (predresidual_td 1st-order is the cleaner design). Keep GPU fed regardless (composition FULL from af9d0ff is the next queued GPU job).
- **NEW USER DIRECTIVES (2026-07-08 full-night full-auto, LOCKED):** (1) drill negatives **5x** (up from 2x/3x) -- BUT only AFTER skunkworks confirms the negative is genuine, not a design-failure (memory: feedback_drill_negatives_5x_brain_ground_every_gap_determination_USER_2026-07-08). (2) BRAIN-GROUND every gap FIRST (how does the brain solve it) THEN build. (3) grep existing experiments before expanding (avoid dup + guide). (4) USE THE GPU -- most capable tool, keep it fed/batched/never idle. (5) solve load-bearing capabilities with DETERMINATION; do not stand; full-auto authorized.
- **IN FLIGHT NOW:** skunkworks a641 (generation confirm-negative VET); exp_dev af9d0ff (composition SEM_RERANK + multi-seed CG-parity build+smoke -> returns queue_add cmd); research a10e1cc (brain-grounding resonator basin-proliferation, build-gated); consolidated_3arm FULL (GPU); K-sweep FULL (CPU).

### [BUILD-WAVE RESULTS 2026-07-08 ~00:0xZ local -- three cells authored+smoked+shipped this wave]
- **GENERATION HARD_FAIL skunkworks-CONFIRMED genuine (a641):** framings CORRECTED -- NOT 'capacity ceiling' (it IS noise-compounding, unfixed by denoise/predict); representation-lever UNDER-DETERMINED. Real untested lever = CONTEXT-GATING. consolidated_3arm FULL ruled SATURATION-VACUOUS (model at chance) -> should be killed (auto-kill DENIED, needs USER; gating staged behind it).
- **GENERATION GATING = HEADLINE (exp_dev a9303, commit 4692cd9cc):** CONTEXT_GATE arm (brain=selective admission: thalamic/attention gating, our Stage-4 attention-routing gap) FLATTENS noise-compounding 89% at smoke (d=+0.039 vs RAW +0.362); cleanup(+0.500)/residual(+1.266) both WORSENED it; scramble control fires hard (+2.105) => benefit is SELECTION not renorm; learned gate concentrates on most-recent slot (0.911). MIDDLE_BAND (on HARD_PASS line; only strict dGATE<=0 misses). FULL (3-seed N=8192 K<=5) STAGED overnight_queue pos-1899 NEXT-to-fire (timeout self-healed 21600). First positive on Stage-4 attention gap. -> skunkworks VET on landing.
- **COMPOSITION revival WIN (exp_dev af9d0ff, commit 1c7911d94):** SEM_RERANK (BGE reorders the decorrelated glass-box beam shortlist) +0.087 Hits@10 lift 2/2 NO collapse -> operationalizes correlation-hurts-capacity decouple, REVIVES the SEM_BEAM negative as net-positive. CG-parity firm-up: PYTHONHASHSEED non-determinism FIXED (canonical sorted order) -> RANDOM_BEAM ties BGE 0.469 vs 0.460 p=0.87 bit-reproducible -> clears the MM->CG blocker. FULL RUNNING remote_cpu_queue (~90min). -> skunkworks VET.
- **EXTRAPOLATION-LAW HARD_PASS-modest (exp_dev aa1b3, commit 733aab630):** mechanistic transmission-coeff survival law (p_d=1-s*coll_d, computable a-priori = real forward-model; nests naive phi at s=1,k=0) beats empirical AFFINE out-of-range in EVERY config + modestly beats flat lookup (~17-22% margin, honestly deflated from strict 20%). Extends CG north-star interpolation->extrapolation (substrate predicts safe reasoning envelope BEFORE hitting it). FULL shipping remote_cpu_queue (~20min). -> VET on landing.
- **NORTH-STAR ATOM DURABLE:** reasoning-depth CG atom (commit f4c943f7c) verified on origin off-laptop.
- **INFRA HAZARDS (testbed candidates, non-blocking):** hd_metrics_sync scheduled-task 10-min timeout starves the ~13-15min pull+merge+push (atom only landed via direct run); hdlab package not covered by auto-SCP patterns (per_item_log.py had to be hand-SCP'd). Push of gating/extrapolation commits to origin rides the standing sync cycle (runner has them via queue_add SCP so runs are unaffected).
- **STILL GATED:** resonator DG-frontend build (DGProjection primitive exists, unwired) waits on skunkworks confirming the K-sweep FULL wall is genuine. K-sweep running CPU.

### [VET-WAVE 2026-07-08 -- landed results adjudicated]
- **RESONATOR WALL VET'd GENUINE (skunkworks ae604, HF_STRUCTURAL_BOUND / MM proven-bound), gate CLEARED to drill.** K5/K6 oracle_any=0 reproduces all 3 seeds cv=0, sharp step from K4=0.82, all design-failure checks clear (oracle correct, outputs produced-but-wrong, diversity saturated, not vacuous). FRAMING CORRECTION: NOT 'fundamental' -- it's a CONFIG-CONTINGENT capacity cliff at N=4096 where M^K crosses ~N^2 (M=30/factor; the Frady/Kleyko/Sommer resonator operational-capacity cliff). It MOVES with N -- a beatable bound, not a proven ceiling. Mechanism = crosstalk/SNR capacity (Tsodyks-Feigelman), NOT basin-multiplicity. ESCAPE STEER: load-bearing lever = DIMENSIONAL EXPANSION (DGProjection front-end N->r*N); r=4 rescues K5 (ratio 1.45->0.09) but likely NOT K6 (43.5->2.7); sparsify is the risky part (ablate); run cheap CPU crosstalk-disentangler FIRST; pre-register PARTIAL-rescue (K5 only) as informative not HARD-FAIL. -> exp_dev af54b0d building the DG-expansion escape (5x-drill #1).
- **COMPOSITION FULL landed (5-seed HARD_PASS), VET in flight (ac7412):** (A) SEM_RERANK WIN -- SEM_RERANK_RRF 0.570, +0.102 lift vs RANDOM_BEAM, 5/5 pos seeds (glass-box decorrelated beam + BGE-rerank BEATS BGE-alone 0.485; store stays decorrelated = correlation-hurts-capacity decouple OPERATIONALIZED into a net win, revives the SEM_BEAM negative). (B) parity=MM_PARITY (RANDOM_BEAM 0.468+/-0.039 vs BGE 0.485 diff -0.017 McNemar p=0.40 within-noise, determinism_ok=True) -- honest TIE, tiered MM not CG (std too large rel to diff). skunkworks adjudicating final tiers + atoms.
- **CONTENT-GATING follow-on brain-grounding (3 lit-scans converged):** content-dependent K-slot gating == query-key ATTENTION (modern-Hopfield==attention, Ramsauer 2020; maps to biased-competition/Desimone-Duncan, PBWM/Frank-O'Reilly frontostriatal gating, hippocampal CA3 content-addressed recall -- all reduce to content-similarity selection). Substrate-implementable = query-key similarity over the K slots. research ac02954 synthesizing the cell proposal (content-gate on a HIGHER-ORDER corpus where recency-gating provably FAILS). This is the robustness follow-on to the recency-gate headline.


**[NEWEST -- headline results + what's landing]**
- **NORTH-STAR DEMONSTRATED (pending VET): self-improvement on REASONING-DEPTH = HARD_PASS.** exp_reasoning_depth_capacity_provisioning_monitor_loop_v1 (commit 775b5cd92, LOCAL analysis, reuses the density-loop machinery on landed reasoning-depth data). The substrate observes its OWN reasoning-depth-vs-provisioning, fits its survival law (self-heal b=1.77, reasons ~1.77x deeper than naive), and PREDICTS usable depth at an UNSEEN provisioning level -- beats constant baseline (MAE 1.20 vs 4.70) AND strong nearest-lookup (1.65), 6/8 folds, and BOTH firing controls FIRE (C1 scramble-law 0.88, C2 scramble-curve |T|=0.976>p90=0.62). This is what flat DENSITY could NOT give (the 2x-revival of the density HARD_FAIL). REPRODUCED bit-exact by an independent re-run. **XHIGH VET IN FLIGHT (afd8dd68)** -- load-bearing audit: is the "beats nearest-lookup" edge GENUINE out-of-sample or an in-sample fill-repeat artifact? Honest caveats (for the VET, not disqualifying): lookup margin MODEST (only 4/8 strictly beat both); the hardest EXTRAPOLATION fold (fill=0.35) is WEAKEST (law over-predicts, lookup beats it there). Genuine HARD_PASS at loop level, honest limits. **THIS IS THE SESSION HEADLINE IF THE VET HOLDS.**
- **GENERATION (Stage-4) valid + noise-compounding SUPPORTED at smoke.** exp_substrate_gen_lm_consolidated_3arm_depth_v4 (commit 86925d539). Smoke now REPRODUCES the failure (dRAW=+0.084, baseline degrades with depth) -- unlike the botched 1st attempt. 5 arms (baseline / CA3-cleanup / PREDICT-RESIDUAL-TD / 2 scramble controls). SMOKE: SHALLOW CA3-cleanup FLATTENS the degradation (dCA3~0, load-bearing per its scramble control) = noise-compounding CONFIRMED fixable; DEEP predict-residual did NOT help at tiny N=512 (TD-W needs full dimensionality). FULL (N=8192) shipping via ORCHESTRATOR (a400878e) to overnight_queue -> resolves deep-vs-shallow. -> VET on landing.
- **K-SWEEP full running (~2.45h):** resonator WALL (smoke: oracle_any K5/K6=0, basin-proliferation) -> honest ceiling on the noise-contra (budget-fixable to K4, wall beyond).
- **STALL NUANCE (important):** tonight's "0-byte/silent agents" were OFTEN DROPPED RETURN MESSAGES, not lost work -- the cells/commits/metrics LANDED. VERIFY OFF-DISK before assuming lost + re-dispatching (re-runs still serve as reproductions). The 20-min cron does exactly this.
- **PROCESS LOCKED:** remote dispatch = ORCHESTRATOR (exp_dev builds+smokes LOCAL, hands the queue_add cmd; orch ships+verifies). testbed abd74cb1 baking into agent instructions. 20-min cron 5aab7891 (session-only: survives compaction, NOT session-exit -> re-create if session restarts).
- **NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) the reasoning-depth VET (afd8dd68) = the headline -- did the north-star self-improvement hold out-of-sample? (3) generation FULL landing -> VET (deep-vs-shallow, noise-compounding-vs-ceiling); (4) K-sweep wall VET; (5) if generation confirms noise-compounding -> memorialize that cross-cutting principle; (6) fire queued (SEM_RERANK arm; composition multi-seed CG-parity; resonator Control-2) via orchestrator; (7) re-create the cron if the session restarted.

### USER REFRAMES/DIRECTIVES THIS SESSION (new, load-bearing -- obey)
- **BENCHMARK = DIAGNOSTIC, not scoreboard.** The LLM/encoder comparison is an INSTRUMENT to find the substrate's LOAD-BEARING POINTS OF WEAKNESS to improve -- NOT "beat the LLM." Decompose failures BY CAUSE -> load-bearing capability -> prioritized improvement map.
- **NEVER frame a baseline (BGE/LLM) as a ceiling the substrate "underperforms."** Beatable/open, brain=existence-proof. (memory: feedback_dont_frame_baselines_as_ceilings_brain_existence_proof_2026-07-08). Distinguish narrow-task-vs-encoder from big-claim-vs-LLM.
- **BRAIN-GROUND the mechanism** (predictive generation -> evaluate DEEPLY how the brain does it; brain=best-in-class reference).
- **2x-DRILL negatives after skunkworks** (revival drills; negatives don't close, they get drilled for revival/mechanism).
- **SCOUR prior experimental work FIRST** before new drills (targeted grep -- the corpus is huge; a broad scour STALLS).
- **Agent-comms = MESSAGES + store-atoms, NOT ferry/handoff docs.** (renamed one exp_dev_handoff file out of the inbox pattern; the legacy `<from>_to_<recipient>` pattern is dead).
- **Keep USER at strategic level; handle plumbing silently; do the REAL fix.**

### KEY RESULTS THIS SESSION (skunkworks-tiered honestly)
- **COMPOSITION (deep-prize crux) = PARITY.** exp_conceptnet_semantic_seeded_beam_composition_v1 FULL, VET'd: RANDOM_BEAM (substrate-native GLASS-BOX, zero external embedding) = 0.502 Hits@10 vs BGE 0.494 = **PARITY within noise** (z=0.26, McNemar p~0.13, run-to-run sigma ~0.02 > the +0.009 margin) -> a transparent substrate TIES the black-box encoder on ConceptNet multi-hop. Tier MM-TENTATIVE; CG-parity needs the multi-seed re-run (fix PYTHONHASHSEED non-determinism). NOT "beats an LLM" (encoder, within-noise). SEM_BEAM HARD_FAIL 0.227 = **CORRELATION-HURTS-CAPACITY confirmed** (semantic-seeded codes cos~0.20 collide the store; RANDOM beats SEM in both regimes) -> MEMORIALIZED (reference_correlation_hurts_associative_store_capacity_decouple_from_retrieval_2026-07-08: store wants DECORRELATED codes, retrieval wants correlated -> DECOUPLE; don't semantic-seed the store). 2 atoms filed (HF + MM).
- **PER-ITEM LOGGING BUILT (commit 2235071f2)** = the load-bearing infra blocker CLEARED. hdlab/per_item_log.py PerItemLogger (0.05% overhead), wired into composition + encoder-retrieval evals. Stage-attribution + per-item-property slicing now POSSIBLE (fixes the "data-absent" wall). Already isolated 99/201 composition failures to the COMPOSE stage.
- **DENSITY LAW FALSIFIED (owned honestly):** predicted m*~6 mild-growth (JL mechanism-match) -> DATA says m*=8 FLAT, scale-invariant (argmax flat to 1e-15 across 3.5x V, VET-confirmed not metric-limited). Silver lining: NO per-scale retuning needed. (I over-trusted the theory bracket.)
- **SELF-IMPROVEMENT (north-star):** loop MACHINERY validated (runs end-to-end, honest label fires, controls work on mock) but density regime is FLAT so the CAPABILITY was an honest HARD_FAIL null (VET_PASS). **REVIVAL (2x-drill) = REASONING-DEPTH regime** (CG-tier non-trivial p^D law, data ALREADY ON DISK, zero GPU) -- the north-star demonstration RELOCATED there; build dispatched (exp_dev ad784fc5). Resonator was the pre-identified n=2 but the K-sweep showed it's a WALL (sharp step, riskier/MM).
- **K-SWEEP: resonator hits a WALL at K5/K6** (smoke: oracle_any craters K3=1.0->K4=0.87->K5=0->K6=0 = basin-proliferation, the CG_META 4th-family). Honest CEILING on the recurrent-noise-contra: budget-fixable only to K4, then a wall. FULL running (~2.45h) confirms per-seed.
- **BRAIN-GROUNDED GENERATION (Stage-4 gap):** deep antidote to the noise-compounding failure = PREDICT-RESIDUAL / TD-bootstrap (successor-rep + predictive-coding: accumulate the BOUNDED residual, never raw context). CA3-cleanup = shallow version. 1st build FAILED (wrong regime: RAW didn't degrade 6.01->5.93 = INCONCLUSIVE; + skipped the deep arm). RE-DISPATCHED (exp_dev a09fd97d): reproduce the failure FIRST (dRAW>0 hard gate) THEN 3 arms (baseline + CA3-cleanup + predict-residual-TD). Honest P~0.25-0.30.
- **RECOVERY hardened:** hd-instrument FULLY on GitHub (mcox79/hd-instrument, HEAD==origin/main; the push-fault was a 1-off DNS blip, fixed). Memory on GitHub too (mcox79/ClaudeConvos) but 15k uncommitted = USER's call to push.

### EMERGING PRINCIPLES (hold until substrate-confirmed)
- **NOISE-COMPOUNDING = cross-cutting load-bearing weakness** (raw step-wise accumulation -> unbounded noise): reasoning-depth (survives via per-hop re-clean, CG), resonator (external reset), composition (beam), generation (context-hurts-with-depth). Brain fix = BOUNDED-RESIDUAL accumulation (predictive-coding) = the deep form of per-step re-clean. Theory-confirmed (brain-eval); awaits the generation-build SUBSTRATE demo. -> memory if the generation build confirms.
- **CORRELATION-HURTS-CAPACITY** = CONFIRMED + memorialized (see above).
- Substrate operating LANDSCAPES tend FLAT (density) or WALLED (resonator) -- self-improvement needs a SMOOTH non-flat law (reasoning-depth is it).

### IN-FLIGHT (all Opus exp_dev / research per policy)
- GENERATION rebuild (exp_dev a09fd97d): failure-regime-first + 3 arms.
- K-SWEEP full (running remote_cpu_queue, ~2.45h): resonator wall confirm.
- REASONING-DEPTH self-improvement loop (exp_dev ad784fc5): the north-star revival (zero-cost, data on disk).

### NEXT-SESSION FIRST ACTIONS
1. Heartbeat. `python tools/inflight_monitor.py` + verify off-disk (cache lags).
2. Check + VET the 3 in-flight: (a) REASONING-DEPTH self-improvement loop = the KEY north-star result (does the substrate demonstrate NON-TRIVIAL self-improvement -- proposal beats baselines + firing controls FIRE, unlike density's silence?); (b) GENERATION rebuild (did the smoke reproduce dRAW>0? did predict-residual beat cleanup? noise-compounding vs ceiling?); (c) K-SWEEP full (wall confirmed per-seed?).
3. QUEUED dispatches: SEM_RERANK composition arm (the semantic-negative revival -- BGE re-ranks substrate-native beam, store decorrelated, cheap); composition MULTI-SEED re-run to firm PARITY -> CG (fix PYTHONHASHSEED); resonator Control-2 (self-improvement n=2, on K-sweep full).
4. If generation build confirms noise-compounding-is-fixable -> memorialize the noise-compounding cross-cutting principle.

## WHAT THIS PROJECT IS
hd-instrument: observable VSA/HDC glass-box substrate. USER goal (locked): fully-functional glass-box-LLM-capable substrate, every capability inspectable/editable, brain-grounded. Brain = north-star + existence-proof (NOT a vs-LLM comparison -- comparisons DIAGNOSE weakness). Deep prize: substrate REASONING OVER ingested knowledge (glass-box, self-auditing), as NARROW MONITOR steps (monitor-not-control). Stages 1(found ~88%)/2(meta ~85%)/3(capability ~60%, current front)/4(LM-equiv, deferred; gaps = generation/attention-routing/action-selection/cortex-layer). Encoder = the frontend every stage inherits from (retrieval CG-unblocked @177K; density optimum FLAT so scale-invariant).

## USER-LOCKED (obey)
NO AskUserQuestion. re-encode HELD. SMOKE-local / canonical FULL via remote queue (GPU=orchestrator; the exp_dev remote-SCP GATE_FAIL routes via orchestrator). NEVER git add -A (explicit pathspec; Store in-repo but substrate_index partitions sync-managed). Agent-spawn model. No-smoke (honest tiers off-disk, skunkworks-owned; SMOKE must reproduce the phenomenon being fixed). Fix#28 verify off-disk incl agent numbers. Intuitive strategic summaries at END. Never stand -- keep lanes full, own research lane. Model+effort: research/drills=Sonnet, exp_dev/skunkworks/orchestrator/testbed=Opus, director=Opus; effort HIGH / XHIGH headline-VETs. Keep USER strategic (no plumbing narration).

## PROCESS + AUTONOMY (adopted this session -- keep doing)
- **REMOTE DISPATCH = ORCHESTRATOR, not exp_dev.** exp_dev AUTHORS + SMOKES locally ONLY, then RETURNS the exact queue_add.sh command; the ORCHESTRATOR does the remote SCP/SSH dispatch + verifies the referent LANDED. Do NOT let exp_dev ship to remote itself -- it STALLS mid-ship (that is why the K-sweep "never shipped" tonight). Clean split = two short jobs (build+smoke, then dispatch) = less stall exposure + reliable remote hand. (USER-flagged 2026-07-08.)
- **20-MIN SELF-CHECK CRON** (CronCreate job 5aab7891, fires :07/:27/:47): checks for stalled agents (verify off-disk + re-dispatch), drills fresh negatives (2x), fires ungated queued work, updates this doc. **SESSION-ONLY (in-memory): survives COMPACTION but NOT session-exit -- if the session restarted and this cron is gone, RE-CREATE it** (or, more durable, a schtask inbox-ping). Do not rely on it as the only nudge; the OS watchdog is the durable backstop.

## INFRA/HAZARDS (this session's live lessons)
- **AGENT STALLS (SYSTEMIC tonight):** many sub-agents died on API/socket blips AFTER building their work (scour, K-sweep exp_dev, generation exp_dev x2, reasoning-depth exp_dev). Symptom: 0-byte or long-silent agent output. FIX: verify off-disk what they produced (cells/notes/metrics usually landed), re-dispatch a FRESH agent to finish. Do NOT assume a silent agent is working. The 20-min cron + shorter per-agent jobs (build-smoke separate from dispatch) mitigate this.
- **SYNC-LAG (SH-9):** remote jobs complete but metrics lag locally (~20min hd_metrics_sync cadence). "Empty queue" often = FINISHED. Force-pull via tools/scp_recover_landing.py. Recommendation flagged: shorten cadence OR pull-at-landing-confirm.
- **SCOUR/SCAN SPEED:** corpus huge (178k atoms/15k notes/5.9k exp dirs). Targeted grep only; broad reads hang/stall.
- queue_add: tools/orchestrator/queue_add.sh (POSITIONAL args, not --flags); exp_dev hits GATE_FAIL on remote SCP -> route via orchestrator. hd_metrics_sync pushes origin/main.
- Store: atoms.jsonl/cert_ledger.jsonl A5-gated; substrate_index partitions gitignored (sync-managed, not git-tracked -- persistence = A5 on-disk write + sync, NOT git commit).
- Detailed pre-07-08 history: notes/director_POST_COMPACTION_BACKUP_FULL_STATE_2026-07-07.md.
