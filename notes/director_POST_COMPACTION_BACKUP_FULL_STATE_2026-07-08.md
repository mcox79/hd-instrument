# Director BACKUP -- CURRENT STATE 2026-07-08 (clean; supersedes 2026-07-07)

**Read end-to-end. Self-contained. The 07-07 + 07-06 docs hold detailed pre-07-08 history if needed.**

### ================= SESSION-2 CONSOLIDATED STATE (2026-07-08 ~15:3xZ -- SUPERSEDES the incremental blocks below; READ THIS FIRST) =================

**WHERE WE ARE:** deep in the ENCODER arc, but it broadened into a full multi-front program. The encoder's semantic barrier is CG-certified; the "just decouple / two-head" fix HARD_FAILED at production scale (a real capacity wall); the deep-prize reasoning loop got its first CG cert; teacher-free encoding started working; the KB doubled in size; and the experiment-error classes got hardened at the source.

**CERTIFIED / PROVEN THIS SESSION (all VET-earned):**
- **BARRIER (CG):** concept-recall ceiling is SEMANTIC-fidelity-bound + N-invariant; capacity saturated. Encoder OBJECTIVE is the lever, not dimensionality. (atoms 2162f4b1e)
- **REDIRECT (CG):** matching BGE geometry self-limits via TEACHER-CROWDING (median_NN 0.921); native decorrelated code wins superposition but loses pointwise -> the honest fix is DECOUPLE store-codes from retrieval-discrimination (the certified correlation-hurts-store law, again). (atom cdfe7b465)
- **REASONING-LOOP (CG, deep-prize milestone, atom on origin db3e72699):** glass-box self-auditing retrieve->gate->audit->requery loop composing certified attention-gate + value-gate + Merkle-audit; loop beats single-shot 495:0 all 5 seeds; causal hand-edit flips recompute + fires tamper 100/100. SCOPE CAVEAT: clean engineered ceiling-saturated regime = certifies MECHANISM + glass-box editability, NOT reasoning-in-the-wild. Extension (real KB, non-ceiling) building = a7673b.
- **Composed attention x value gate FULL = HARD_PASS** (brain-component integration at scale). **Resonator deflation drill FULL = HARD_PASS.** **Self-manager dial#1 (adaptive halting) FULL done** (smoke: accuracy 0.73 vs 0.13 at equal compute).

**THE CAPACITY WALL (the big honest finding):** two-head FULL @V40000 = HARD_FAIL_ONE_HEAD_FAR_MISS. Retrieval/pointwise head HOLDS at scale (SC 0.98); STORE/superposition head degrades with bundle size (SP 0.99@J3 -> 0.75@J5 -> 0.20@J8) = a FIXED-N additive-superposition capacity wall, NOT decorrelation. This is the brain's working-memory limit (Miller/Cowan ~4). I OVER-CLAIMED two-head as "the winner" off a small-V smoke -- it was the saturation-vacuous trap (see process below). BRAIN FIX = time-multiplex (theta-gamma slots) + sequential cancellation, NOT bigger bundles. LEVER (building, high-P 0.48, a1b057c2): SLOT-PEEL BUNDLING -- retarget the CHAIN-GRADE theta-gamma nested-slot ENCODE + the MM resonator slot-peel DECODE from multiplicative K-binding to additive J-bundling; decisive 3-arm test FLAT vs SLOT_NODEFLATE vs SLOT_PEEL, J={3,5,8,12}. Encoder-workaround negatives: phase-traversal condenser = GENUINE NEGATIVE (info wall, hard sign+top-k); graded-sparse = borderline@V8000, FULL DEFERRED (skeptical, PROT-021 blocked); two-band = saturation-vacuous smoke (redesign folded into capacity pivot).

**TEACHER-FREE (unlocked + working at smoke):** the repulsion term is the UNIVERSAL load-bearing ingredient (confirmed confound-free: no-repulsion ablation collapses off-target cosine 0.004->0.957; source of positives -- teacher vs graph -- is secondary). Teacher-free graph-relational codes are LESS crowded than BGE (0.004 vs 0.558). SCOPE: works on the DENSE CN 2-core (median deg 3); raw relations.jsonl is actually median-deg-1 (the "median 6" was the KB-INGESTED concept_relations graph, a different denser construction) -> sparse tail needs the degree-agnostic SimGRACE fallback (also passes). FULL running remote_cpu (06e5a493d) + VET. abffe592.

**INGEST FIX (major, DONE):** KB densified 970k->1.28M entities, median degree 1.0->2.0, 27% deg>=5; ConceptNet subgraph 2->133,306 nodes. Root cause = wasted ~14GB random-codebook alloc + a lock-steal bug (NOT the stale WinError in the state file). Durable, atomic-swapped, backed up (data/_testbed_kb_backup_2026-07-08/). Cleared the teacher-free KB-sparsity gate.

**SELF-MANAGER CHAPTER:** = 6 content-free scalar DIALS layered OVER the certified value-gate + attention-gate (make existing frozen knobs adaptive; NOT a new module). Dial#1 done. Consolidation (a77857, integrate-new-without-forgetting) + reasoning-loop are the forward chapters. Notes: research_neuromodulatory_self_manager_controller + research_energy_scaled_selective_depth_retrieval + research_hippocampal_biology_consolidation_loop_brain_first (all biology-first).

**PROCESS UPDATES THIS SESSION (LOCK IN):**
- **NO PAUSE. STOP writing `pause_state: ACTIVE` in dispatch prompts** -- it is FALSE (no data/orchestrator_paused.flag on disk), a vestigial template line, and it made agents report a phantom pause. The real rule is REMOTE-DISPATCH-VIA-ORCHESTRATOR (exp_dev builds+smokes, hands the queue_add; orchestrator ships) -- NOT a pause. (USER caught this.)
- **SATURATION-VACUOUS SMOKE = the #1 error class:** a smoke that passes at small V/N where the frontier/negative CONTROL also passes tests NOTHING -> the FULL then HARD_FAILs (bit us on two-head AND two-band). FIX BAKED IN: `from _seed_checkpoint import assert_discriminator_fires` -- the control-that-must-fail MUST fail at smoke V else the smoke hard-fails loudly. Every new cell inherits it via the exp_dev.md checklist.
- **ERROR-HARDENING (testbed aed18897, all landed + tested):** (A) discriminator-fires gate [above]; (B) tools/exp_guard.py `timeout` -- code-computed floor over ALL work axes (V/iters/batch/seeds/arms), trained_encoder floor = 3h, blocks >4h; (C) scp_recover tri-state probe (transient ssh error no longer = "remote missing"); (D) atomic write_metrics (tmp+os.replace, all callers inherit); (E) exp_guard.py `smoke` -- heavy smokes route REMOTE (never shrink below discriminating scale). Residual (harness-level, NOT in-repo-fixable): agent API-stalls, fan-out re-send loops, ~20min sync lag.
- **TIMEOUT FLOOR:** encoder V40000 5-seed multi-arm FULL needs timeout >= 10800s (3h). Cells resume-from-partials (re-dispatch only reruns missing seeds). PROT-021 blocks >=4h no-checkpoint jobs (good gate).
- **SMOKES:** must be FAST + DISCRIMINATING; if impossible locally -> route remote (USER: heavy local smokes waste the machine + session).
- **RECOVERY-RITUAL FIX:** director_kb_query.py default --schema-version v1->v2 (KB is v2 since 06-27; the bare STEP-0 query was silently refusing). Verified working (returns backup doc cosine=1.0).

**IN FLIGHT NOW (~15:3xZ):** slot-peel bundling build (a1b057c2); KB-reasoning-loop build (a7673b); consolidation/CLS build (a77857); teacher-free FULL + VET (remote_cpu, monitor b6efp0p0p); anchor-sweep FULL on GPU (resume, ~3h) + skunkworks VET pending; two-head skunkworks VET pending (monitor bm7bxbchv on anchor-sweep terminal).

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) inflight_monitor + verify off-disk; (3) read the in-flight returns -- the SLOT-PEEL result is the KEY one (does time-slotting+cancellation beat the fixed-N capacity wall?); (4) teacher-free FULL VET (does R1 repulsion hold 5-seed); (5) KB-reasoning-loop (does the CG loop generalize to real non-ceiling knowledge?); (6) consolidation smoke; (7) two-head + anchor-sweep VETs; (8) do NOT write pause_state in dispatches; (9) if slot-peel resolves the capacity wall, that's the encoder path forward; if not, the two-head/two-separate-codes is confirmed necessary and the store needs more dimensions or chunking.

### ================= END SESSION-2 CONSOLIDATED (older incremental blocks below are HISTORY) =================

### [SELF-MANAGER + COARSE-TO-FINE BRAIN-GROUNDING DELIVERED 2026-07-08 ~13:00Z -- newest]
Both drills from the "3 thoughts = next architectural chapter" steer (line ~11 below) landed: notes/research_neuromodulatory_self_manager_controller_2026-07-08.md (Half A) + notes/research_energy_scaled_selective_depth_retrieval_coarse_to_fine_2026-07-08.md (Half B). Fusion + ranked build order filed as director_plan.json priority `self_manager_and_energy_scaled_retrieval_brain_grounding_2026-07-08` (read that entry's `notes` field for full detail). One-line takeaway: the self-manager is 6 separable content-free SCALAR feedback loops sitting ABOVE the certified value-gate + attention-gate as a modulating layer (not bespoke); coarse-to-fine drill-down should stay LOCAL/reflexive per-item (parameter-free, matches existing discipline) with the self-manager only retuning the AGGREGATE threshold/budget the local reflex uses -- both drills independently converged on this exact division of labor. Build order: (1) zero-new-code re-analysis of phase_traversal artifacts (shortlist-gate condenser to Tier1 top-k) once its smoke lands; (2) substrate_acc_evc_adaptive_halting_v1 cheap test on exp_pfc_bg_composed_attention_value_gate_v1's DD param; (3) IF both pass, wire the actual fusion cell (combinedgate arbitration margin gates phase-traversal condense, threshold ACC/EVC-tuned); (4)+ homeostasis/sleep, other neuromodulator channels, RHT cascade, ACh-gate (deferred last, weakest evidence). NEXT ACTION: dispatch (1)+(2) via exp_dev once bandwidth allows (currently GPU busy on two-head encoder FULL, local phase_traversal smoke still running).

## STEP 0 on pickup
`date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp` (every turn-end).
Then `python tools/inflight_monitor.py`. INFRA NOTE (see bottom): the monitor CACHE lags (~20min sync); "empty queue" often means FINISHED not stalled -- verify metrics off-disk. Several sub-agents STALLED tonight (died on API/socket blips AFTER their work) -- a 0-byte / long-silent agent output = stalled, re-dispatch fresh. Bash recursive scans hang on the huge store (178k atoms / 15k notes / 5.9k exp dirs) -- use TARGETED grep, never broad crawl.

## >>> LATEST STATE (2026-07-08 ~09:3x Z -- READ FIRST) <<<

### [USER STRATEGIC DIRECTION 2026-07-08 ~12:xxZ -- 3 thoughts = next architectural chapter]
- **(1) EVOLUTIONARY BUILD-ORDER as a design prior:** survival/action core (basal-ganglia action-selection = ANCIENT) -> value+memory (dopamine reward, hippocampus) -> general cortex LAYERED ON TOP modulating downward, built from ONE canonical microcircuit tiled+repurposed. Lessons: build value/action/memory core solid FIRST then layer a modulating general layer on top (we're doing this: composed attention x value gate); and PERFECT ONE MOTIF then tile it (we found it: content-addressed similarity-selection == attention == Hopfield == value-gating). Caveat: evolution forced by a body/survival pressure we lack -> strong prior not a script.
- **(2) SELF-MANAGER GAP (real, USER-flagged, on the build-missing list):** the brain's neuromodulatory systems (DA/NE/ACh/5HT) = content-free GLOBAL-SETTINGS dials (gain, learning-rate, precision, explore/exploit, when-to-consolidate) + ACC conflict/error monitor + sleep. We have the "predict my safe envelope" half of self-management (self-improvement CG) but NOT the real-time "tune my own settings" half. Genuine missing organ.
- **(3) EFFORT/ENERGY-SCALED SELECTIVE-DEPTH RETRIEVAL (USER's own introspection = the target):** cheap coarse gist read by default, drill down at higher energy ONLY where needed (energy = resolution). This is what the PHASE-TRAVERSAL encoder reaches toward; substrate edge = VOLUNTARY inspectable selective-depth (brain does it automatically, no control knob).
- **FUSION (the chapter):** #2 is the CONTROLLER (hand on the dial), #3 is what it CONTROLS (energy/resolution of memory access); #1 says build the controller as a MODULATING LAYER over the certified core, not a bespoke module. Brain-grounding drill dispatched (ab5a532, Sonnet) -> ranked buildable list + build order. This is the candidate next chapter after the encoder-crowding workarounds resolve.

### [MOBILE-WINDOW OP CONSTRAINT 2026-07-08 ~11:5xZ -- ACTIVE until operator back above ground]
- **Operator is going MOBILE (subway).** They will signal "heading underground." Constraint: local smokes OK BUT must be PAUSABLE = short + checkpoint/restartable + atomic metric writes (a suspend/kill must resume-from-checkpoint, not restart-from-zero). Bake "pausable/restartable smoke" into every exp_dev dispatch this window.
- **Substantial work goes REMOTE** (only already-dispatched remote-queue jobs progress while the laptop is offline; agents + main session pause on disconnect). Two-head encoder FULL is ON REMOTE GPU (overnight_queue, a6bbe44b) = the autonomous-progress job for the window.
- **ON "heading underground" SIGNAL:** (1) set data/orchestrator_paused.flag (block new dispatch mid-descent); (2) ensure any in-flight local smoke is at a safe checkpoint or let it finish; (3) note resume state here; (4) clear the flag when they're back.


### [ENCODER-FIDELITY PIVOT 2026-07-08 ~09:3xZ -- THE NEW SPINE, USER-ENDORSED]
- **USER STEERED THE PROGRAM TO THE ENCODER.** After the attention-routing arc completed at CG, USER asked what the single load-bearing gap is + are we positioned to fix it. Answer (disk-grounded): every downstream mechanism now works GIVEN good representations; the bottleneck moved UP to the encoder's ~0.507 per-concept recall ceiling that EVERY stage inherits (backup line ~98). USER endorsed the pivot + authorized full-auto: "once you understand the barriers, drill deeper on brain encoding to improve this."
- **BARRIER VERDICT = CHAIN-GRADE-CERTIFIED (FULL landed-VET afebac, independent recompute off 10 per-seed partials reconciled exact; commit 2162f4b1e):** the 0.507 ceiling is **SEMANTIC-FIDELITY-bound, NOT capacity-bound.** UNANIMOUS across all 5 seeds: **semantic_fidelity(+0.489) > correlation(+0.255) > capacity(+0.088 SATURATED), D_cv=0.017 (inside CG bar), N-invariant cos (span 0.003 vs teacher 0.463).** Saturation control telemetry-sensitive (cap lever fires +0.441 starved vs +0.088 provisioned = genuine saturation not blindness). Atom A5-write authorized (math corpus, verified_off_data=True). Smoke ranking held at FULL. Doc-only flag: cell docstring band stale (says D_control<=-0.15) but code correctly gates on the saturation lever -- science sound. cos-to-target moves +0.462 with teacher quality, 0.018 with dimensionality. => MORE DIMENSIONS ~INERT; the ENCODER OBJECTIVE is the whole lever. (My prior crosstalk-dominant hypothesis was PARTIALLY wrong: decorrelation is real but #2, not the driver.) Physics caveat from the cell: capacity+semantic are ENTANGLED through the argmax margin -- capacity only binds when the encoder is imperfect, so a clean capacity-only regime is physically impossible; only cos-to-target isolates, and it is purely semantic.
- **KEY CONSTRAINT:** current encoder approach = DISTILLATION FROM BGE, which caps at the teacher (parity, not past it -- we already showed BGE-parity on composition). To EXCEED requires a brain-grounded objective beyond distillation.
- **IN FLIGHT (the deep-drill + build-gating work):** (1) af7165 orchestrator -- decomposition FULL + skunkworks landed-VET (certify the semantic verdict at scale). (2) a0226b research (Sonnet) -- DEEP drill: ranked encoder OBJECTIVES that beat distillation-parity (Olshausen-Field sparse coding, predictive-coding-as-LEARNING not cleanup, decorrelation/ICA pressure, contrastive/self-sup, teacher-free discriminative coding). (3) af798c exp_dev -- disambiguate STUDENT-UNDERFIT (encoder < its BGE teacher -> fix training, v2 MLP already aims here) vs TEACHER-CAP (BGE itself ~0.5 -> must replace the objective); OPPOSITE fixes, measure it.
- **BRAIN-GROUND PASS-1 COMPLETE (4 lit-scans, feed the deep-drill):** sparse-coding/DG pattern-separation (Tsodyks-Feigelman capacity ~1/(a|ln a|); optimal ~10% active; DG expand-then-sparsify orthogonalizes; Myers-Scharfman recency-inhibition), population-fidelity/decorrelation (Moreno-Bote information-limiting-correlations = hard ceiling N can't break -> whitening is the lever there; Atick-Redlich full-whiten only low-noise), attractor-cleanup (Ramsauer modern-Hopfield cap 2^(d/2), 1-step converge, beta = basin-sharpness knob), predictive-coding/laminar (Rao-Ballard, Bastos microcircuit, Salvatori PC-assoc-mem -- but predict-residual CLEANUP already failed circularly in our generation cell; PC only as a LEARNING objective is the open question).
- **DEEP-DRILL DELIVERED (notes/research_encoder_objective_beyond_bge_distillation_2026-07-08.md):** pure distillation is a CEILING not a floor (only pulls toward teacher geometry, never REPELS off-target apart). Exceed-parity = ADD a repulsion/decorrelation term ON TOP of the distill anchor. Rank1 distill+VICReg-covariance (P0.27, top bet, 1 loss term), Rank2 sparse-recon-on-input (P0.30, 'sees what teacher structurally can't' via superposition), Rank3 self-sup InfoNCE relational (P0.30, dodges the 122-pair scarcity that HF'd supervised contrastive), Rank4 JEPA (P0.16, deprioritized). Bio synthesis: sparsity+slowness+decorrelation acting together.
- **>>> REDIRECT = CHAIN-GRADE CERTIFIED (adversarial VET a748b9, CG+1, atom commit cdfe7b465):** the ceiling is NEITHER student-underfit NOR teacher-cap. On the LOAD-BEARING superposition recall@J task the substrate NATIVE decorrelating code beats matching-BGE-geometry by SP_gap -0.693 at FULL (teacher SP@J5 0.171 vs decorrelated 0.865; trained-distill-v2 sits at 0.420 = pulled toward the cap), all 5 seeds cv0.024. **MECHANISM = BGE TEACHER-CROWDING** (median_NN cos 0.921, 62% of concepts >0.90 similar -> BGE bundle-sums collapse onto a shared direction; native code near-orthogonal 0.009). Telemetry-sensitive (perturb swings teacher 0.171->1.000). => **DISTILL-HARDER IS THE WRONG FIX.** VET GUARDRAIL (do NOT over-claim): NOT "decorrelation is simply better" -- the SAME cell measures its COST (native LOSES single-concept pointwise fidelity 0.556 vs teacher 0.997). **HONEST RECONCILED CONCLUSION: replace pure-BGE distillation with a substrate-native objective that DECOUPLES decorrelated store/superposition codes from pointwise retrieval discrimination -- get BOTH, don't trade.** This IS the certified law reference_correlation_hurts_associative_store_capacity_decouple_from_retrieval, now surfacing at the encoder. Composes the CG decomposition (semantic-fidelity is the lever; BGE-geometry just isn't the way to get it).
- **PAYOFF BUILD COURSE-CORRECTED (exp_dev a2fbeddeaa):** distill+VICReg-decorrelation cell, now with BGE-anchor-WEIGHT as a SWEPT lever (full->native-only) + eval on superposition-recall (PRIMARY) + single-concept fidelity + off-target geometry + a substrate-native arm. SMOKE-local; GPU FULL GATED on my go after the disambiguation VET + this smoke both land = the real decision point (native-only vs hybrid-anchor-weight).
- **NEXT ON RETURNS:** reconcile disambiguation-VET + course-corrected-build-smoke => pick the winning objective (likely decorrelation-primary substrate-native, BGE anchor weak/zero) => ship the GPU FULL. Do NOT over-commit GPU before the reconciliation.

### [ARC COMPLETE 2026-07-08 ~07Z -- ATTENTION-ROUTING CAPSTONE = CHAIN-GRADE]
- **🏆🏆 FULL ATTENTION-ROUTING = CHAIN-GRADE (CG+1). The Stage-4 attention-routing gap is substantially addressed at chain-grade.** exp_substrate_gen_lm_combinedgate_recency_content_v8 (commit 4227e7e97) VET'd CHAIN_GRADE: a PARAMETER-FREE biased-competition gate genuinely ARBITRATES a recency prior vs a content-cue bias -- COMBINED=1.000 all 3 seeds (cv~0), beats BOTH single gates (COMB-REC +0.327, COMB-CON +0.274) AND rescues EACH single's failure sub-regime (conflict-win +1.000 where recency fails; cue_absent-win +0.801 where content fails). NO residual positional-prior leak (the smoke-caught learned-W confound stayed FIXED at FULL -- CONTENT_ONLY on cue_absent = 0.20 exactly = cap, not the buggy 0.70); readout verified parameter-free; telemetry-sensitive (scramble 1.0->0.34; T1/T2 guards pass); honest operating range (falls back to recency below q*=0.15, never catastrophic). Composes v5(recency MM)+v6(content-clean MM)+v7(content-noisy CG). Scope excludes multi-cue(>2) + value-based/PBWM gating (= the NEXT gap, action-selection).
- **ATTENTION-ROUTING ARC (COMPLETE, the night's spine):** generation HARD_FAIL (noise-compounding) -> fix is SELECTION not denoising -> recency-gate v5 (MM) -> clean-content v6 (MM) -> noisy-content v7 (CG, infers relevance from noise-floor cue) -> combined-arbitration capstone v8 (CG). Brain-grounded throughout (biased-competition == query-key attention == Hopfield == PBWM). Each step honestly tiered under adversarial VET, each strictly harder.
- **EXTRAPOLATION honestly RESOLVED (MM, well-evidenced):** genuine test (abserr vs re-fit-quadratic, non-tautological) -- mech law beats a FAIR STRONGER competitor (3-param quad) far-out-of-range 4/5 seeds; quad predicts impossible NEGATIVE depth at the percolation-critical fill while mech stays bounded -- but cv=0.265>0.15 (structural, integer-quantization) caps it at MM. North-star = CG interpolation + MM extrapolation (genuine forward-model, cross-seed-capped). Replaces the earlier TAUTOLOGICAL far-fold metric (my over-claim, corrected).
- **HONEST SESSION TALLY (all VET-earned under adversarial audit): CG+4** (reasoning-depth interpolation north-star; SEM_RERANK composition revival; content-noisy attention-routing; combined-arbitration capstone), **MM+5** (parity; extrapolation-base; gating-recency; resonator slot-peel escape; content-clean), **HF+1** (resonator DG-expansion). 7 CG math atoms + 14 ledger ops today. All durable on origin.
- **NEXT GAP (in flight): value-based ACTION-SELECTION** (basal-ganglia Go/NoGo, dopamine RPE) -- brain-grounding dispatched (research af5cf3); the attention gate selects INPUT-context by competition, action-selection selects OUTPUT by learned VALUE. Also queued: resonator drill#3 (deflation isolation low-SNR).
- **METHODOLOGY LESSON (lock in): a discriminator that ANALYTICALLY cancels the telemetry is tautological -- ALWAYS verify the metric is telemetry-SENSITIVE (perturb-a-seed-moves-it + not-bit-identical-across-seeds guards) before trusting cross-seed robustness. Bit us twice tonight (far-fold margin; v8 smoke positional-leak); both caught.**

### [NIGHT CULMINATION 2026-07-08 ~06Z -- attention-routing arc + honest tally]
- **🏆 CONTENT ATTENTION-ROUTING = CHAIN-GRADE (CG+1): the first CG on the Stage-4 attention-routing gap.** exp_substrate_gen_lm_contentgate_noisycue_v7 (commit aa0ebb9bb) VET'd CHAIN_GRADE: the substrate INFERS relevance from a genuinely noise-corrupted cue (cue degraded to cosine~q, verified per-seed; cue_snr~1.8 = +0.65sigma over distractor-max, cos_true~0.02 at the detectability edge) and routes to the correct context slot -- CON=0.729 3/3 seeds (cv0.064), lift_vs_recency +0.454, crushing the 1/(K-1)=0.20 cap; breaks only at the physics detectability floor (cue_snr~0.9). Discriminator is telemetry-SENSITIVE (NOT the far-fold tautology). VET corrected the inflated pre-reg 'realistic q=0.08' framing (6-sigma=easy) but the CG HOLDS anchored at the honestly-hard q=0.02. Composes v5 recency-gate MM + v6 clean-content MM (answers v6's own revival criterion).
- **ATTENTION-ROUTING ARC (the night's spine):** generation HARD_FAIL (noise-compounding, skunkworks-confirmed) -> the fix isn't denoising, it's SELECTION -> recency-gate v5 (MM, flattens 99.6% in simple regime) -> clean-cue content-gate v6 (MM, executes content-selection with explicit cue) -> noisy-cue content-gate v7 (**CG**, INFERS relevance from a noise-floor cue). Brain-grounded throughout (biased-competition == query-key attention == PBWM gating == Hopfield). Each step honestly tiered, each strictly harder.
- **HONEST SESSION TALLY (all VET-earned under adversarial audit): CG+3** (reasoning-depth interpolation north-star; SEM_RERANK composition revival; content-attention-routing-noisy), **MM+5** (RANDOM_BEAM-BGE parity; mechanistic extrapolation base; gating-recency; resonator slot-peel escape; content-gate-clean), **HF+1** (resonator DG-expansion, mechanism-clarifying).
- **HONESTY EVENTS (both directions, self-corrected):** (1) I OVER-CLAIMED the far-fold extrapolation as 'strong 5-seed cross-seed CG-bound' -- skunkworks proved the margin metric was ANALYTICALLY TAUTOLOGICAL (actual telemetry cancels in the overshoot regime -> bit-identical across seeds by construction); corrected to MM-FIRM, and dispatched the GENUINE test (per-seed abs-error vs re-fit quadratic at the percolation-critical crossing). (2) the v7 CG was itself anchored DOWN from the inflated q=0.08 headline to the honestly-hard q=0.02. LESSON (add to methodology): a discriminator that analytically cancels the telemetry is tautological -- always verify the metric is telemetry-SENSITIVE before trusting cross-seed robustness.
- **NORTH-STAR stands HONESTLY: CG interpolation + MM extrapolation** (extrapolation NOT promoted; genuine test re-running). Resonator wall = proven-HF negative, K5/K6 conjunctive-binding capability delivered via slot-escape (MM). All atoms durable on origin.


### [POST-COMPACTION UPDATE 2026-07-08 ~03:10Z -- newest, read before the block below]
- **NORTH-STAR VET'd CHAIN_GRADE (afd8dd68 returned).** reasoning-depth self-improvement HELD out-of-sample: law MAE 1.2011 vs const 4.7018 vs strong nearest-lookup 1.6500; the edge is GENUINE (law wins at the survival-curve KNEE where lookup fails by ~4.8), NOT an in-sample fill-repeat; both controls fire for the right reason. CG+1 atom filed (needs orchestrator Store-sync). SCOPE (honest): interpolation demonstrated; the ONE true extrapolation fold (fill=0.3516) the law does NOT beat lookup (1.86 vs 1.60) -> extrapolation-superiority UNPROVEN = the next frontier. Commit 775b5cd92 confirmed PUSHED.
- **GENERATION rebuild returned (a09fd97d) = HARD_FAIL smoke, sharp mechanism.** predresidual_td v4 (commit 0dd45e89e, 1st-order Markov so dRAW>0 analytically guaranteed): failure reproduces STRONG (dRAW=+0.362) but NEITHER antidote flattens -- shallow CLEANUP +0.500, DEEP predict-residual +1.266 (deep is WORST; RESIDUAL_SCRAMBLED +1.142 ~= deep => harm from residual DYNAMICS not a code bug). MECHANISM: delta-W predictor caps at ~0.52 cos-to-target (== the 0.507 concept-recall ceiling) -> residual ~as noisy as raw -> compounds. => points to a **REPRESENTATION-FIDELITY CEILING, NOT a noise-compounding-sequencing problem** -- the brain-grounded deep antidote fails BECAUSE the binding constraint is upstream (codebook/VQ granularity = ENCODER territory, the primary focus). TENTATIVE strategic redirect: Stage-4 generation gate may tie back to encoder fidelity. **GATED: skunkworks (a641) VETting whether this is a GENUINE negative vs design-failure + which cell is canonical, BEFORE any drill or redirect.**
- **GPU:** consolidated_3arm FULL running but SLOW (4.9% @ 18min = ~6h ETA) and it's the WEAKER reproduction (dRAW=+0.084). Holding a kill+swap decision for skunkworks' canonical-test call (predresidual_td 1st-order is the cleaner design). Keep GPU fed regardless (composition FULL from af9d0ff is the next queued GPU job).
- **NEW USER DIRECTIVES (2026-07-08 full-night full-auto, LOCKED):** (1) drill negatives **5x** (up from 2x/3x) -- BUT only AFTER skunkworks confirms the negative is genuine, not a design-failure (memory: feedback_drill_negatives_5x_brain_ground_every_gap_determination_USER_2026-07-08). (2) BRAIN-GROUND every gap FIRST (how does the brain solve it) THEN build. (3) grep existing experiments before expanding (avoid dup + guide). (4) USE THE GPU -- most capable tool, keep it fed/batched/never idle. (5) solve load-bearing capabilities with DETERMINATION; do not stand; full-auto authorized.
- **IN FLIGHT NOW:** skunkworks a641 (generation confirm-negative VET); exp_dev af9d0ff (composition SEM_RERANK + multi-seed CG-parity build+smoke -> returns queue_add cmd); research a10e1cc (brain-grounding resonator basin-proliferation, build-gated); consolidated_3arm FULL (GPU); K-sweep FULL (CPU).

### [BUILD-WAVE RESULTS 2026-07-08 ~00:0xZ local -- three cells authored+smoked+shipped this wave]
- **GENERATION HARD_FAIL skunkworks-CONFIRMED genuine (a641):** framings CORRECTED -- NOT 'capacity ceiling' (it IS noise-compounding, unfixed by denoise/predict); representation-lever UNDER-DETERMINED. Real untested lever = CONTEXT-GATING. consolidated_3arm FULL ruled SATURATION-VACUOUS (model at chance) -> should be killed (auto-kill DENIED, needs USER; gating staged behind it).
- **GENERATION GATING = HEADLINE (exp_dev a9303, commit 4692cd9cc):** CONTEXT_GATE arm (brain=selective admission: thalamic/attention gating, our Stage-4 attention-routing gap) FLATTENS noise-compounding 89% at smoke (d=+0.039 vs RAW +0.362); cleanup(+0.500)/residual(+1.266) both WORSENED it; scramble control fires hard (+2.105) => benefit is SELECTION not renorm; learned gate concentrates on most-recent slot (0.911). MIDDLE_BAND (on HARD_PASS line; only strict dGATE<=0 misses). FULL (3-seed N=8192 K<=5) STAGED overnight_queue pos-1899 NEXT-to-fire (timeout self-healed 21600). First positive on Stage-4 attention gap. -> skunkworks VET on landing.
- **COMPOSITION revival WIN (exp_dev af9d0ff, commit 1c7911d94):** SEM_RERANK (BGE reorders the decorrelated glass-box beam shortlist) +0.087 Hits@10 lift 2/2 NO collapse -> operationalizes correlation-hurts-capacity decouple, REVIVES the SEM_BEAM negative as net-positive. CG-parity firm-up: PYTHONHASHSEED non-determinism FIXED (canonical sorted order) -> RANDOM_BEAM ties BGE 0.469 vs 0.460 p=0.87 bit-reproducible -> clears the MM->CG blocker. FULL RUNNING remote_cpu_queue (~90min). -> skunkworks VET.
- **EXTRAPOLATION-LAW HARD_PASS-modest (exp_dev aa1b3, commit 733aab630):** mechanistic transmission-coeff survival law (p_d=1-s*coll_d, computable a-priori = real forward-model; nests naive phi at s=1,k=0) beats empirical AFFINE out-of-range in EVERY config + modestly beats flat lookup (~17-22% margin, honestly deflated from strict 20%). Extends CG north-star interpolation->extrapolation (substrate predicts safe reasoning envelope BEFORE hitting it). FULL shipping remote_cpu_queue (~20min). -> VET on landing.
- **NORTH-STAR ATOM DURABLE:** reasoning-depth CG atom (commit f4c943f7c) verified on origin off-laptop.
- **INFRA HAZARDS (testbed candidates, non-blocking):** hd_metrics_sync scheduled-task 10-min timeout starves the ~13-15min pull+merge+push (atom only landed via direct run); hdlab package not covered by auto-SCP patterns (per_item_log.py had to be hand-SCP'd). Push of gating/extrapolation commits to origin rides the standing sync cycle (runner has them via queue_add SCP so runs are unaffected).
- **STILL GATED:** resonator DG-frontend build (DGProjection primitive exists, unwired) waits on skunkworks confirming the K-sweep FULL wall is genuine. K-sweep running CPU.

### [GATING FULL LANDED 2026-07-08 ~05:45Z -- THE HEADLINE, VET in flight abb31]
- **CONTEXT_GATE FULL (N=8192, 3-seed, commit 4692cd9cc) = near-COMPLETE flatten of noise-compounding at scale.** RAW_BIND degrades d(K5-K1)=+0.649; CONTEXT_GATE = {K1:2.707,K2:2.709,K3:2.705,K5:2.710} d=+0.003 (~99.5% reduction, IMPROVED from smoke +0.039 -- gate advantage widens at higher K). CONTEXT_GATE_SCRAMBLED blows up {K2:4.02,K3:4.16,K5:4.44} = firing control fires HARD (benefit is SELECTION not renorm). Cleanup/residual arms only partially flatten -> whole-cell verdict MIDDLE_BAND (CONTEXT_GATE misses strict dGATE<=0 by +0.003). skunkworks abb31 XHIGH VET adjudicating per-arm tier + honest scope. HONEST SCOPE: this is a RECENCY gate on a 1st-order corpus (optimal gate = attend-most-recent = simple); CONTENT-dependent gating on higher-order corpus (relevant != recent) is the untested harder follow-on (research ac02954 scoping). Frame = "selection flattens noise-compounding where optimal selection is simple", NOT "attention-routing solved". First positive on the Stage-4 attention-routing gap.
- **RESONATOR WALL ESCAPED (5x-drill #2, theta-gamma slot-peel):** smoke HARD-PASS, slot_peel K5/K6 vanilla 0.000 -> 1.000, expansion-independent, wins despite vanilla R10+oracle handicap. Brain phase-slot trick (resolve factors one-at-a-time via peel+deflate). FULL running remote_cpu_queue. HONEST = re-encoding SIDESTEP (delivers K5/K6 conjunctive-binding capability), not vanilla-convergence-fix. Drill#3 (isolate deflation higher-K/lower-N) + ACF decode-side queued. -> VET on FULL landing.
- **RESONATOR drill#1 (DG-expansion) CLOSED HF+1:** confirmed genuine, mechanism = REACHABILITY/convergence wall NOT crosstalk (expansion rescued K4->1.0 but K5=0 hard-discontinuity; crosstalk framing was a miscite). Expansion/DG/bigger-R levers PROVEN dead.
- **SESSION SCORE (2026-07-08 night): CG+2** (reasoning-depth interpolation north-star; SEM_RERANK semantic revival), **MM+2** (RANDOM_BEAM-vs-BGE parity firm; mechanistic extrapolation extension), **HF+1** (DG-escape, mechanism-clarifying). Resonator wall ESCAPED (drill#2, pending FULL+VET). Generation redirected HARD_FAIL->gating headline. All atoms durable on origin. Every negative revived or drilled WITH mechanism gain.

### [VET-WAVE 2026-07-08 -- landed results adjudicated]
- **RESONATOR WALL VET'd GENUINE (skunkworks ae604, HF_STRUCTURAL_BOUND / MM proven-bound), gate CLEARED to drill.** K5/K6 oracle_any=0 reproduces all 3 seeds cv=0, sharp step from K4=0.82, all design-failure checks clear (oracle correct, outputs produced-but-wrong, diversity saturated, not vacuous). FRAMING CORRECTION: NOT 'fundamental' -- it's a CONFIG-CONTINGENT capacity cliff at N=4096 where M^K crosses ~N^2 (M=30/factor; the Frady/Kleyko/Sommer resonator operational-capacity cliff). It MOVES with N -- a beatable bound, not a proven ceiling. Mechanism = crosstalk/SNR capacity (Tsodyks-Feigelman), NOT basin-multiplicity. ESCAPE STEER: load-bearing lever = DIMENSIONAL EXPANSION (DGProjection front-end N->r*N); r=4 rescues K5 (ratio 1.45->0.09) but likely NOT K6 (43.5->2.7); sparsify is the risky part (ablate); run cheap CPU crosstalk-disentangler FIRST; pre-register PARTIAL-rescue (K5 only) as informative not HARD-FAIL. -> exp_dev af54b0d building the DG-expansion escape (5x-drill #1).
- **COMPOSITION FULL landed (5-seed HARD_PASS), VET in flight (ac7412):** (A) SEM_RERANK WIN -- SEM_RERANK_RRF 0.570, +0.102 lift vs RANDOM_BEAM, 5/5 pos seeds (glass-box decorrelated beam + BGE-rerank BEATS BGE-alone 0.485; store stays decorrelated = correlation-hurts-capacity decouple OPERATIONALIZED into a net win, revives the SEM_BEAM negative). (B) parity=MM_PARITY (RANDOM_BEAM 0.468+/-0.039 vs BGE 0.485 diff -0.017 McNemar p=0.40 within-noise, determinism_ok=True) -- honest TIE, tiered MM not CG (std too large rel to diff). skunkworks adjudicating final tiers + atoms.
- **CONTENT-GATING follow-on brain-grounding (3 lit-scans converged):** content-dependent K-slot gating == query-key ATTENTION (modern-Hopfield==attention, Ramsauer 2020; maps to biased-competition/Desimone-Duncan, PBWM/Frank-O'Reilly frontostriatal gating, hippocampal CA3 content-addressed recall -- all reduce to content-similarity selection). Substrate-implementable = query-key similarity over the K slots. research ac02954 synthesizing the cell proposal (content-gate on a HIGHER-ORDER corpus where recency-gating provably FAILS). This is the robustness follow-on to the recency-gate headline.


**[NEWEST -- headline results + what's landing]**
- **NORTH-STAR DEMONSTRATED (pending VET): self-improvement on REASONING-DEPTH = HARD_PASS.** exp_reasoning_depth_capacity_provisioning_monitor_loop_v1 (commit 775b5cd92, LOCAL analysis, reuses the density-loop machinery on landed reasoning-depth data). The substrate observes its OWN reasoning-depth-vs-provisioning, fits its survival law (self-heal b=1.77, reasons ~1.77x deeper than naive), and PREDICTS usable depth at an UNSEEN provisioning level -- beats constant baseline (MAE 1.20 vs 4.70) AND strong nearest-lookup (1.65), 6/8 folds, and BOTH firing controls FIRE (C1 scramble-law 0.88, C2 scramble-curve |T|=0.976>p90=0.62). This is what flat DENSITY could NOT give (the 2x-revival of the density HARD_FAIL). REPRODUCED bit-exact by an independent re-run. **XHIGH VET IN FLIGHT (afd8dd68)** -- load-bearing audit: is the "beats nearest-lookup" edge GENUINE out-of-sample or an in-sample fill-repeat artifact? Honest caveats (for the VET, not disqualifying): lookup margin MODEST (only 4/8 strictly beat both); the hardest EXTRAPOLATION fold (fill=0.35) is WEAKEST (law over-predicts, lookup beats it there). Genuine HARD_PASS at loop level, honest limits. **THIS IS THE SESSION HEADLINE IF THE VET HOLDS.**
- **GENERATION (Stage-4) valid + noise-compounding SUPPORTED at smoke.** exp_substrate_gen_lm_consolidated_3arm_depth_v4 (commit 86925d539). Smoke now REPRODUCES the failure (dRAW=+0.084, baseline degrades with depth) -- unlike the botched 1st attempt. 5 arms (baseline / CA3-cleanup / PREDICT-RESIDUAL-TD / 2 scramble controls). SMOKE: SHALLOW CA3-cleanup FLATTENS the degradation (dCA3~0, load-bearing per its scramble control) = noise-compounding CONFIRMED fixable; DEEP predict-residual did NOT help at tiny N=512 (TD-W needs full dimensionality). FULL (N=8192) shipping via ORCHESTRATOR (a400878e) to overnight_queue -> resolves deep-vs-shallow. -> VET on landing.
- **K-SWEEP full running (~2.45h):** resonator WALL (smoke: oracle_any K5/K6=0, basin-proliferation) -> honest ceiling on the noise-contra (budget-fixable to K4, wall beyond).
- **STALL NUANCE (important):** tonight's "0-byte/silent agents" were OFTEN DROPPED RETURN MESSAGES, not lost work -- the cells/commits/metrics LANDED. VERIFY OFF-DISK before assuming lost + re-dispatching (re-runs still serve as reproductions). The 20-min cron does exactly this.
- **PROCESS LOCKED:** remote dispatch = ORCHESTRATOR (exp_dev builds+smokes LOCAL, hands the queue_add cmd; orch ships+verifies). testbed abd74cb1 baking into agent instructions. 20-min cron 5aab7891 (session-only: survives compaction, NOT session-exit -> re-create if session restarts).
- **NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) the reasoning-depth VET (afd8dd68) = the headline -- did the north-star self-improvement hold out-of-sample? (3) generation FULL landing -> VET (deep-vs-shallow, noise-compounding-vs-ceiling); (4) K-sweep wall VET; (5) if generation confirms noise-compounding -> memorialize that cross-cutting principle; (6) fire queued (SEM_RERANK arm; composition multi-seed CG-parity; resonator Control-2) via orchestrator; (7) re-create the cron if the session restarted.

### USER REFRAMES/DIRECTIVES THIS SESSION (new, load-bearing -- obey)
- **BENCHMARK = DIAGNOSTIC, not scoreboard.** The LLM/encoder comparison is an INSTRUMENT to find the substrate's LOAD-BEARING POINTS OF WEAKNESS to improve -- NOT "beat the LLM." Decompose failures BY CAUSE -> load-bearing capability -> prioritized improvement map.
- **NEVER frame a baseline (BGE/LLM) as a ceiling the substrate "underperforms."** Beatable/open, brain=existence-proof. (memory: feedback_dont_frame_baselines_as_ceilings_brain_existence_proof_2026-07-08). Distinguish narrow-task-vs-encoder from big-claim-vs-LLM.
- **BRAIN-GROUND the mechanism** (predictive generation -> evaluate DEEPLY how the brain does it; brain=best-in-class reference).
- **2x-DRILL negatives after skunkworks** (revival drills; negatives don't close, they get drilled for revival/mechanism).
- **SCOUR prior experimental work FIRST** before new drills (targeted grep -- the corpus is huge; a broad scour STALLS).
- **Agent-comms = MESSAGES + store-atoms, NOT ferry/handoff docs.** (renamed one exp_dev_handoff file out of the inbox pattern; the legacy `<from>_to_<recipient>` pattern is dead).
- **Keep USER at strategic level; handle plumbing silently; do the REAL fix.**

### KEY RESULTS THIS SESSION (skunkworks-tiered honestly)
- **COMPOSITION (deep-prize crux) = PARITY.** exp_conceptnet_semantic_seeded_beam_composition_v1 FULL, VET'd: RANDOM_BEAM (substrate-native GLASS-BOX, zero external embedding) = 0.502 Hits@10 vs BGE 0.494 = **PARITY within noise** (z=0.26, McNemar p~0.13, run-to-run sigma ~0.02 > the +0.009 margin) -> a transparent substrate TIES the black-box encoder on ConceptNet multi-hop. Tier MM-TENTATIVE; CG-parity needs the multi-seed re-run (fix PYTHONHASHSEED non-determinism). NOT "beats an LLM" (encoder, within-noise). SEM_BEAM HARD_FAIL 0.227 = **CORRELATION-HURTS-CAPACITY confirmed** (semantic-seeded codes cos~0.20 collide the store; RANDOM beats SEM in both regimes) -> MEMORIALIZED (reference_correlation_hurts_associative_store_capacity_decouple_from_retrieval_2026-07-08: store wants DECORRELATED codes, retrieval wants correlated -> DECOUPLE; don't semantic-seed the store). 2 atoms filed (HF + MM).
- **PER-ITEM LOGGING BUILT (commit 2235071f2)** = the load-bearing infra blocker CLEARED. hdlab/per_item_log.py PerItemLogger (0.05% overhead), wired into composition + encoder-retrieval evals. Stage-attribution + per-item-property slicing now POSSIBLE (fixes the "data-absent" wall). Already isolated 99/201 composition failures to the COMPOSE stage.
- **DENSITY LAW FALSIFIED (owned honestly):** predicted m*~6 mild-growth (JL mechanism-match) -> DATA says m*=8 FLAT, scale-invariant (argmax flat to 1e-15 across 3.5x V, VET-confirmed not metric-limited). Silver lining: NO per-scale retuning needed. (I over-trusted the theory bracket.)
- **SELF-IMPROVEMENT (north-star):** loop MACHINERY validated (runs end-to-end, honest label fires, controls work on mock) but density regime is FLAT so the CAPABILITY was an honest HARD_FAIL null (VET_PASS). **REVIVAL (2x-drill) = REASONING-DEPTH regime** (CG-tier non-trivial p^D law, data ALREADY ON DISK, zero GPU) -- the north-star demonstration RELOCATED there; build dispatched (exp_dev ad784fc5). Resonator was the pre-identified n=2 but the K-sweep showed it's a WALL (sharp step, riskier/MM).
- **K-SWEEP: resonator hits a WALL at K5/K6** (smoke: oracle_any craters K3=1.0->K4=0.87->K5=0->K6=0 = basin-proliferation, the CG_META 4th-family). Honest CEILING on the recurrent-noise-contra: budget-fixable only to K4, then a wall. FULL running (~2.45h) confirms per-seed.
- **BRAIN-GROUNDED GENERATION (Stage-4 gap):** deep antidote to the noise-compounding failure = PREDICT-RESIDUAL / TD-bootstrap (successor-rep + predictive-coding: accumulate the BOUNDED residual, never raw context). CA3-cleanup = shallow version. 1st build FAILED (wrong regime: RAW didn't degrade 6.01->5.93 = INCONCLUSIVE; + skipped the deep arm). RE-DISPATCHED (exp_dev a09fd97d): reproduce the failure FIRST (dRAW>0 hard gate) THEN 3 arms (baseline + CA3-cleanup + predict-residual-TD). Honest P~0.25-0.30.
- **RECOVERY hardened:** hd-instrument FULLY on GitHub (mcox79/hd-instrument, HEAD==origin/main; the push-fault was a 1-off DNS blip, fixed). Memory on GitHub too (mcox79/ClaudeConvos) but 15k uncommitted = USER's call to push.

### EMERGING PRINCIPLES (hold until substrate-confirmed)
- **NOISE-COMPOUNDING = cross-cutting load-bearing weakness** (raw step-wise accumulation -> unbounded noise): reasoning-depth (survives via per-hop re-clean, CG), resonator (external reset), composition (beam), generation (context-hurts-with-depth). Brain fix = BOUNDED-RESIDUAL accumulation (predictive-coding) = the deep form of per-step re-clean. Theory-confirmed (brain-eval); awaits the generation-build SUBSTRATE demo. -> memory if the generation build confirms.
- **CORRELATION-HURTS-CAPACITY** = CONFIRMED + memorialized (see above).
- Substrate operating LANDSCAPES tend FLAT (density) or WALLED (resonator) -- self-improvement needs a SMOOTH non-flat law (reasoning-depth is it).

### IN-FLIGHT (all Opus exp_dev / research per policy)
- GENERATION rebuild (exp_dev a09fd97d): failure-regime-first + 3 arms.
- K-SWEEP full (running remote_cpu_queue, ~2.45h): resonator wall confirm.
- REASONING-DEPTH self-improvement loop (exp_dev ad784fc5): the north-star revival (zero-cost, data on disk).

### NEXT-SESSION FIRST ACTIONS
1. Heartbeat. `python tools/inflight_monitor.py` + verify off-disk (cache lags).
2. Check + VET the 3 in-flight: (a) REASONING-DEPTH self-improvement loop = the KEY north-star result (does the substrate demonstrate NON-TRIVIAL self-improvement -- proposal beats baselines + firing controls FIRE, unlike density's silence?); (b) GENERATION rebuild (did the smoke reproduce dRAW>0? did predict-residual beat cleanup? noise-compounding vs ceiling?); (c) K-SWEEP full (wall confirmed per-seed?).
3. QUEUED dispatches: SEM_RERANK composition arm (the semantic-negative revival -- BGE re-ranks substrate-native beam, store decorrelated, cheap); composition MULTI-SEED re-run to firm PARITY -> CG (fix PYTHONHASHSEED); resonator Control-2 (self-improvement n=2, on K-sweep full).
4. If generation build confirms noise-compounding-is-fixable -> memorialize the noise-compounding cross-cutting principle.

## WHAT THIS PROJECT IS
hd-instrument: observable VSA/HDC glass-box substrate. USER goal (locked): fully-functional glass-box-LLM-capable substrate, every capability inspectable/editable, brain-grounded. Brain = north-star + existence-proof (NOT a vs-LLM comparison -- comparisons DIAGNOSE weakness). Deep prize: substrate REASONING OVER ingested knowledge (glass-box, self-auditing), as NARROW MONITOR steps (monitor-not-control). Stages 1(found ~88%)/2(meta ~85%)/3(capability ~60%, current front)/4(LM-equiv, deferred; gaps = generation/attention-routing/action-selection/cortex-layer). Encoder = the frontend every stage inherits from (retrieval CG-unblocked @177K; density optimum FLAT so scale-invariant).

## USER-LOCKED (obey)
NO AskUserQuestion. re-encode HELD. SMOKE-local / canonical FULL via remote queue (GPU=orchestrator; the exp_dev remote-SCP GATE_FAIL routes via orchestrator). NEVER git add -A (explicit pathspec; Store in-repo but substrate_index partitions sync-managed). Agent-spawn model. No-smoke (honest tiers off-disk, skunkworks-owned; SMOKE must reproduce the phenomenon being fixed). Fix#28 verify off-disk incl agent numbers. Intuitive strategic summaries at END. Never stand -- keep lanes full, own research lane. Model+effort: research/drills=Sonnet, exp_dev/skunkworks/orchestrator/testbed=Opus, director=Opus; effort HIGH / XHIGH headline-VETs. Keep USER strategic (no plumbing narration).

## PROCESS + AUTONOMY (adopted this session -- keep doing)
- **REMOTE DISPATCH = ORCHESTRATOR, not exp_dev.** exp_dev AUTHORS + SMOKES locally ONLY, then RETURNS the exact queue_add.sh command; the ORCHESTRATOR does the remote SCP/SSH dispatch + verifies the referent LANDED. Do NOT let exp_dev ship to remote itself -- it STALLS mid-ship (that is why the K-sweep "never shipped" tonight). Clean split = two short jobs (build+smoke, then dispatch) = less stall exposure + reliable remote hand. (USER-flagged 2026-07-08.)
- **20-MIN SELF-CHECK CRON** (CronCreate job 5aab7891, fires :07/:27/:47): checks for stalled agents (verify off-disk + re-dispatch), drills fresh negatives (2x), fires ungated queued work, updates this doc. **SESSION-ONLY (in-memory): survives COMPACTION but NOT session-exit -- if the session restarted and this cron is gone, RE-CREATE it** (or, more durable, a schtask inbox-ping). Do not rely on it as the only nudge; the OS watchdog is the durable backstop.

## INFRA/HAZARDS (this session's live lessons)
- **AGENT STALLS (SYSTEMIC tonight):** many sub-agents died on API/socket blips AFTER building their work (scour, K-sweep exp_dev, generation exp_dev x2, reasoning-depth exp_dev). Symptom: 0-byte or long-silent agent output. FIX: verify off-disk what they produced (cells/notes/metrics usually landed), re-dispatch a FRESH agent to finish. Do NOT assume a silent agent is working. The 20-min cron + shorter per-agent jobs (build-smoke separate from dispatch) mitigate this.
- **SYNC-LAG (SH-9):** remote jobs complete but metrics lag locally (~20min hd_metrics_sync cadence). "Empty queue" often = FINISHED. Force-pull via tools/scp_recover_landing.py. Recommendation flagged: shorten cadence OR pull-at-landing-confirm.
- **SCOUR/SCAN SPEED:** corpus huge (178k atoms/15k notes/5.9k exp dirs). Targeted grep only; broad reads hang/stall.
- queue_add: tools/orchestrator/queue_add.sh (POSITIONAL args, not --flags); exp_dev hits GATE_FAIL on remote SCP -> route via orchestrator. hd_metrics_sync pushes origin/main.
- Store: atoms.jsonl/cert_ledger.jsonl A5-gated; substrate_index partitions gitignored (sync-managed, not git-tracked -- persistence = A5 on-disk write + sync, NOT git commit).
- Detailed pre-07-08 history: notes/director_POST_COMPACTION_BACKUP_FULL_STATE_2026-07-07.md.
