# Director BACKUP -- CURRENT STATE 2026-07-08 (clean consolidation; supersedes all earlier dated blocks)

### ========== 🧭 2026-07-15b POST-COMPACT RESUME (READ FIRST; newest of all, supersedes COMPACTION-FINAL below) ==========

**RESUMED post-compaction. USER re-authorized full-auto: "get this capability fully upgraded and brain-equivalent if not exceeding; foundation must be ideal to work seamlessly with reasoning + substrate."**

**🎯 THE REFRAME THAT CHANGES THE REAL-DATA PICTURE (additive-capturable negative-drill LANDED, P_deflated 0.45, notes/drill_negative_why_real_interactions_additive_capturable_where_genuine_2026-07-15.md):** the 3 prior real-data negatives (chem / LLM-epistasis / Costanzo fair-test REFUTE) all measured at BULK/genome-wide scale, where genuine molecular non-additivity is MEASUREMENT-HIDDEN by two compatible mechanisms: (1) Hill-Goddard-Visscher (2008) allele-frequency projection — population regression structurally absorbs epistasis into the additive axis; (2) Simpson's-paradox aggregation dilution — pooling a minority of interacting pairs with a majority of non-interacting ones washes out the signal. **=> the REFUTEs are EXPECTED at bulk scale, NOT evidence conjunctions are absent. FIX = SUBSET-CURATION, not domain abandonment: restrict to structurally-defined interaction-rich pockets** — top pick = NEAR-ZERO-SINGLES synthetic-lethal / paralog-buffered AND-gate pairs (both single-KO fitness ~WT by DIRECT measurement, double-KO severe) where the additive escape-hatch PROVABLY cannot apply (full knockouts, not segregating alleles). Brain-consistent: only ~5-10% of PFC/PPC neurons carry genuine nonlinear selectivity (Parthasarathy 2022) = same fraction as genuine interaction-variance in nature; the brain RESERVES conjunctive machinery for the identifiable minority (dendritic nonlinearity spatially localized, Ujfalussy 2016). **=> a brain-equivalent FOUNDATION sources its conjunction modules from exactly these pockets, not bulk averages.**

**THE DISCRIMINATING EXPERIMENT DISPATCHED (exp_dev, remote): subset-curated Costanzo cell — restrict to near-zero-singles synthetic-lethal pocket + re-run SYM-vs-strong-additive + readability gate + interaction-variance-fraction (curated vs matched-random) as PRIMARY discriminating stat.** Resolves the two live diagnoses in ONE cell: (a) if readable non-additivity present + SYM beats strong-additive on the curated pocket => prior REFUTEs were BULK-MEASUREMENT ARTIFACTS, thesis proves end-to-end, foundation module #1 REAL; (b) if signal present but SYM ties additive => OPERATOR is the bottleneck (structural reframe) => dispatch learned low-rank bilinear bind z=(Pa)⊙(Qb) (brain-recommended #1, minimal upgrade of elementwise=P=Q=identity); (c) if no signal even in best pocket => genuine rarity, real-data conjunctions may need purpose-built data. HARD-PASS bands (from drill): interaction-variance-fraction >=3x matched-random AND >=30% rel held-out error reduction.

**✅ INTEGRITY FLAG CLEARED (orchestrator certified): the fair-test REFUTE is now CERTIFIED OFF-DISK.** run-3 (ts_iso 2026-07-15T18:58:59Z, verdict REFUTE_NO_TRANSFER, run_mode=full) — all 7 fields match the report: readable_rel 0.15693 / add-vs-mean 0.15693 / sym-vs-mean 0.12821 / novel_hi rel-MAE -0.03407 / pos_ctrl 0.90781 / frac_hi 0.39964 / novel_hi_n 1140. Gates: refute=true, readable_ok=true, rel_hi_pass=false, hard_pass=false. Local now authoritative (byte-identical to remote; routine 20-min hd_metrics_sync had already closed the SH-9 gap). => the additive-capturable VET (a89fbd90) stands on certified numbers. The bulk-scale REFUTE is REAL; the subset-curation cell tests whether it flips in the near-zero-singles pocket.

**🚨 DISCRIMINATING VERDICT LANDED (costanzo_nearzero_singles_curated_ivf_v1, ts 19:49:55Z, FULL, commit 613fa311a) = UNREADABLE_ESCALATE (a 4TH outcome, not the 3 pre-scoped) = DATASET-SNR NULL, NOT a thesis verdict.** The near-zero-singles pocket (n=211, both SMF in [0.90,1.10]) is MEASUREMENT-NOISE-DOMINATED: readable_rel=0.0029 (<<0.15 gate; add-vs-mean=0.0029 ~= 0 = no learnable target at all). IVF curated=0.778 < matched-random=0.912 (ratio 0.85, not >=3) — high IVF here is a NOISE artifact (additive can't fit noise either), NOT interaction. Machinery VALIDATED (pos_ctrl 0.908, neg_ctrl -0.018, must-fails fire -0.41/-0.47, oracle 0, power=True). **IRONY + real finding: the pocket cleanest IN THEORY (near-zero-singles => additive escape-hatch provably can't apply) is the NOISIEST IN MEASUREMENT in Costanzo (small differences of low-signal few-replicate quantities). Costanzo per-pair epsilon EXHAUSTED as a source (3rd Costanzo null of the noise class: bulk-linchpin 185d8bd57, fair-test bcd66a6d8, curated 613fa311a).** BOTTLENECK NOW SHARPLY IDENTIFIED = MEASUREMENT SNR (not operator — reads planted at 0.908; not genuine rarity — couldn't test). Cell's own ESCALATE branch => higher-SNR non-additive dataset (enCas12a paralog-buffering PMC7558751 / DrugComb synergy). SCOUT DISPATCHED (verify access/machine-readability/pairwise-structure/SNR BEFORE ingest — 4 data-eng walls so far: Kramer/ALMANAC format, Costanzo bulk+curated noise; INGEST-CRISPNESS discipline).

**QUEUED (confirm-bottleneck-first): operator upgrade (bilinear) OR module #2 (BioGRID) — DE-PRIORITIZED (bottleneck is data-SNR, not operator); gated on a READABLE real non-additive target existing at all. PARALLEL confirmed-component build: reachability-audit tool + scramble-control fix (grounding deliverable + relational-failure predictor). IN-FLIGHT research (don't duplicate): consolidation-narrow drill (aacc1f46), recombinant-replay + prioritized-replay lit-scans (adfb38da, a69d0442).** OPS: NO LOCAL COMPUTE (all remote); full-auto; commit-early.

**✅✅ MODULE #2 UPGRADED to HELD-OUT PREDICTIVE + ATOMIZED CHAIN_GRADE (module-2b heldout v2 landed + skunkworks-verified off-disk a2314d82): on 400 conjunctions NEVER STORED (no direct edge in either module), HUB 0.831 vs single-ceiling 0.250 -> the cross-module hub PREDICTS unseen interactions by composing 2 independently-built real datasets through shared identity, NOT just recovers stored ones. => foundation architecture is GENUINELY PREDICTIVE on real data (construction -> capability). ATOMIZED (canonical Store, local, remote-persist pending user auth). Original construction-proof detail below:**
**✅ MODULE #2 (CROSS-MODULE INTERFACE) = HARD_PASS, VET'd CHAIN_GRADE (construction-proof, scoped) — the FIRST clean real-data positive + the foundation-architecture proof (commit 224666483, skunkworks a7e1d82e).** Identity-anchored bind/unbind over a canonical-ID hub COMPOSES a cross-module conjunction on REAL 2-module data (BioGRID physical 186MB x Costanzo genetic 521MB yeast, exact-ORF join, 5356 shared ORFs, 598 queries, 5 seeds cv 0.0185). HUB 0.830 vs single-constraint ceiling 0.254 = margin 0.576 (>=0.15 bar, 3.8x; beats MERGED 0.437 by 1.9x). **IDENTITY ANCHOR IS LOAD-BEARING (core finding): remove hub -> 0.83 collapses to 0.088 (NO_HUB/SCRAMBLE).** join_precision 0.989, fuzzy_gain 0 (exact, not lossy), not saturation-vacuous. 🚨 SCOPE (VET, do NOT over-read): CONSTRUCTION PROOF (store+compose+recover), NOT prediction — metrics don't expose held-out-vs-in-sample query semantics; MAP 0.83<1.0 consistent w/ construction-under-load. UPGRADE criterion = HELD-OUT query split -> construction-proof -> predictive-capability (QUEUED as module-2b). Also: easy identity case (exact-ORF, no crosswalk stress = the reachability-audit lever); single-organism. => the foundation's cross-module PLUMBING works on real data (brain-aligned ATL hub-and-spoke); contrasts the content-reasoning negatives (the walls are in READING sparse real interaction, not in stitching modules). Atomization-ready CHAIN_GRADE; A5 write HELD for USER auth (added to batch).

**✅ JOINT-CODE OPERATOR CAPSTONE = HARD_PASS, VET'd CHAIN_GRADE GENUINE (skunkworks a23cfd71, commit e5ff86ca4, FULL 5-seed) — the operator half's both-solver, COMPLETE (synthetic construction-grade).** ONE shared real content emb, read by TWO native VET'd operators as SELECTIVE READOUTS (commutative product-fold + learned rank-R for PARITY 0.998; non-commutative transition-chain for DOMINANCE 1.0) solves BOTH at specialist level with ZERO measured interference (parity rel_drop -0.006 cv 0.004; dominance 1.0=1.0), decisively beating the prior joint-dual MIDDLE (0.816/0.164). M=I identity EXACT (0.0 -> genuinely ONE shared code, not two glued); non-commutativity real at M!=I (delta 1.107). Head-disc: config->dom STRUCTURALLY fails (swap-invariance exact); order->par MEASURED-fails (0.471, sub-chance 4/5). Order-attributed (gap 0.578); must-fails fire. = the brain's one-code-two-readouts (Bernardi). 🚨 VET CORRECTIONS (do NOT claim): NOT "beats specialist" (MATCHES within noise, the win is ZERO-INTERFERENCE; the -0.006 was specialist seed-variance); NOT "order provably sub-chance" (seed29 0.596); NOT rank-R-validated (SATURATED here, needs tunable-rank plant = separate cell); NOT real-data (synthetic K=4); CONSTRUCTION-proof (learned); native-vs-lens = cross-cell inference. => OPERATOR MACHINERY COMPLETE on synthetic: SYM(symmetric) + TRANSITION(asymmetric) + JOINT(both, zero-interference), all VET'd CHAIN_GRADE. FRONTIER = real-data transfer (TRANSITION-real cell) + content (dense).

**✅ ASYMMETRY 3-WAY HEAD-TO-HEAD = HARD_PASS (TRANSITION_OP), VET'd CHAIN_GRADE GENUINE (skunkworks a61d3a0e, every number reproduced off-disk) — a WORKING brain-grounded ASYMMETRIC operator (commit 290400320, FULL 5 seeds local). PRECISE BOUND (VET-scoped): non-commutative matrix-chain (TEM-successor) op reads antisymmetric DOMINANCE at 1.000 (5/5, sd=0) = role-keyed ceiling, clearing FREQ 0.778 by +0.22 (FREQ is the honest bar, NOT chance 0.62), where SYM 0.477 + BILINEAR 0.485 PROVABLY cannot; order-attributed (test-order-permute collapses to 0.422 sub-chance, Delta 0.578); must-fails fire; at M=I reduces EXACTLY to SYM so the win is LEARNED not smuggled + capacity-matched BILINEAR still fails => it's the NON-COMMUTATIVE STRUCTURE that pays. 🚨 DON'T CLAIM (VET): NOT one-op-does-both (FAILS parity 0.685 bimodal << SYM 0.99 = dominance SPECIALIST); NOT wall-broken-general (antisymmetric-ordinal dominance on SYNTHETIC only; real-data + other asymmetric relations untested); NOT emergent (CONSTRUCTION-proof, learned M_i); only TRANSITION passed (hetero/phase negative-inconclusive). Of the 3 brain-theorized directed-relation mechanisms, TRANSITION_OP (successor/TEM non-commutative matrix chaining -- the substrate's GHRR matrix-vector bind) reads role-asymmetric DOMINANCE at 1.000 = role-keyed specialist, >> SYM 0.477 / BILINEAR 0.485 / FREQ 0.778. The commutativity wall the bilinear could NOT break is BROKEN. Cleanly attributed: TRANSITION - SHUFFLED_ORDER = 0.578 (permuting slot order collapses it -> genuine sequence-order non-commutativity, not a leak); must-fails fire. HETEROASSOC failed (~FREQ, lookup-gap inconclusive), PHASE_ORDER failed (offset absorbed under commutative bind) -- as the design drill predicted. 🚨 SCOPE (hold til VET): DOMINANCE SPECIALIST -- does NOT clear parity (0.685 < SYM 0.99); NOT a both-in-one unifier (both = joint code SYM+TRANSITION w/ selective readouts = brain's one-code-two-readouts). Synthetic arena. Faithful-build correction: grouping (M_i@s)(x)e[x_i] [asymmetric, init=SYM] not the symmetric-in-slots literal; asserted in self-test 18/18. => the reasoning MECHANISM now has WORKING brain-grounded SYMMETRIC (SYM) + ASYMMETRIC (TRANSITION_OP) operators. Atom held for user + VET.

**🚨🧠 VERDICTS LANDED + A BRAIN-ALIGNMENT REFRAME (the key strategic turn of the session):**

- **PARALOG THESIS (651030f7e, FULL) = UNREADABLE_ESCALATE AGAIN (4th real-data UNREADABLE null; N FINE this time = 360/403 curated, power_ok=True).** readable_rel=0.0042 (<<0.15); a strong additive barely beats a MEAN-predictor (add-vs-mean 0.0042) -> the target is MOSTLY FLAT with SPARSE spikes; IVF ratio 1.001; SYM worse than additive (novel rel-MAE -0.39). Machinery CLEAN (pos_ctrl 0.908, must-fails fire). Even HIGH-SNR Dede (r=0.87-0.94) is UNREADABLE in the near-zero-singles pocket.
- **🧠 THE REFRAME (USER-prompted "is this test aligned with how the brain does it?" -> brain-grounded YES-and-NO): the TEST DESIGN is MISALIGNED with the brain, and that is the likely ROOT CAUSE of ALL 4 real-data UNREADABLE nulls.** ALIGNED: the bind (=conjunctive coding vs additive default, the ~5-10% nonlinear minority) + novel-pair generalization + real measured grounding. MISALIGNED: we framed it as CONTINUOUS-MAGNITUDE REGRESSION over a DENSE field + a VARIANCE-based readability gate — but the brain does SPARSE DETECTION of specific salient conjunctions (hippocampal pattern-separation, sparse expansion coding, the ~5-10% nonlinear minority). Real interaction IS sparse (minority of pairs lethal; most neutral) -> a mostly-flat target reads as "no signal" under a variance lens even though the interaction is real+strong in the sparse minority. ALSO: near-zero-singles curated the PRECONDITION (both singles ~0) not the OUTCOME (double lethal) -> the pocket dilutes the sparse lethal minority. => synthetic DENSE plants read fine (match the regression framing); real SPARSE interaction reads as noise. FIX (under VET+drill): DETECTION/RANKING readout (does the bind rank true-lethal pairs above neutral, OUTCOME-curated + held-out, no leakage) = the brain-aligned framing where the mechanism may finally show real-data value. Discipline lesson: brain-reference the READOUT design, not just the bind. VET (skunkworks ac879338) + sparse-detection drill (research a0a6de5a) IN FLIGHT.
- **BILINEAR WALL-BREAK (29b53e63b, FULL) = ANOTHER_SPECIALIST (parity-only) = the commutativity wall HOLDS, cleanly.** Learned rank-1 low-rank bilinear (zero-init = elementwise baseline) TIES the symmetric specialist on parity (0.978 vs 0.992) but FAILS dominance (0.485 vs role-specialist 1.0, < even FREQ 0.778); rank4 negligible -> NOT capacity, ARCHITECTURAL. => a single learned product op CANNOT do both symmetric+asymmetric; native/product bind bounded to COMMUTATIVE (confirms the memory anchor); asymmetric REQUIRES role-keying / the joint-dual two-channel code (MIDDLE design remains necessary). Clean atomization-ready bound. VET in flight (skunkworks ac879338).
- **MODULE #2 (BioGRID cross-module interface, 5abe17085) = brain-aligned (ATL hub-and-spoke) — SHIPPED to remote (real-data ingest, fills the idle CPU).** Tests identity-anchored bind/unbind over the shared-ORF hub (HUB vs MERGED-smearing vs NO_HUB vs SCRAMBLE).
- **IN FLIGHT (research, idle-pool): reachability negative-drill (a5785885), factual-core foundation-tier drill (a48143be), sparse-detection reframe drill (a0a6de5a). VET (ac879338) on both negatives.**

### ========== END 2026-07-15b POST-COMPACT RESUME ==========

### ========== 🧭 2026-07-15 COMPACTION-FINAL (superseded by RESUME above; still valid end-state) ==========

**SESSION END-STATE (reasoning half validated+scoped; foundation = binding constraint; grounding subsumed):**
1. **FAIR real-data linchpin REFUTED, VET'd = ADDITIVE-CAPTURABLE (conditional -- run-3 metrics NOT synced to local; NEXT SESSION SSH-pull + certify).** On real readable Costanzo yeast epistasis, symmetric bind gains NO advantage over a strong per-gene additive; the readable signal is main-effects-dominated; pos_ctrl 0.908 proves the bind CAN read planted interaction -> "the interaction isn't in THIS data," NOT a mechanism failure. 3rd real pocket main-effects-dominated (chem/epistasis-gen/Costanzo) -> NARROWS the beats-simpler-methods thesis but does NOT close it (chemistry-QSAR + drug-synergy pockets UNTESTED). commit bcd66a6d8.
2. **🧭 SCALE REFRAME ANSWERED BY DRILLS = LIKELY A MISDIAGNOSIS; THE GAP IS STRUCTURAL, NOT SCALE.** Brain drills: reasoning is NOT continuously scale-gated -- brain runs a TWO-STAGE BOOTSTRAP (build a relational SCAFFOLD from just 10-100 STRUCTURED instances, then reuse on sparse data); our 7k-point module is PLENTY. **THE REAL GAP: the compositional operator (symmetric bind) isn't EXPLOITING the REPEATED RELATIONAL-TEMPLATE structure already in the data.** Per-concept density floor ~13 features (per-concept richness, not total scale). => **NEXT-SESSION REFRAME: FIX/CHANGE THE OPERATOR to exploit repeated-relational-template structure -- NOT scale-up grounding.** Build a combined relation-multiplicity + reachability audit. Notes: drill_brain_extrapolation_...2026-07-15.md, drill_brain_grounded_data_...2026-07-15.md.
3. **NEGATIVE-DRILL (mechanism-limits, partial): the two-channel JOINT-CODING INTERFERENCE COST is BRAIN-CONSISTENT (accept it, not a gap)** -- unavoidable biological fact; brain manages via sparsity/expansion/phase-gating, never eliminates; zero-interference tensor-products need impossible resources.
4. **CONSOLIDATION fully scoped = NARROW** (replay = contested-code forgetting-fix; REFUTED on conjunctions + inductive-real). 3-mechanism picture: bind=conjunction-reasoning, replay=interference-fix, pattern-separation-hedge=storage-crosstalk-fix (hedge CHAIN_GRADE).
5. **GROUNDING (Thrust B) SUBSUMED** by the measured-attribute foundation (instrument-measurement IS grounding, Hacking/Kripke) + 1 reachability-audit tool. Foundation ARCHITECTURE COMPLETE: two-tier (LLM-gen factual core + INGESTED-real conjunction modules) + registry conventions + cross-module interface SOLVED (canonical-ID hub; module #2 = BioGRID exact-ORF-join teed up).

**IN FLIGHT (deliver post-compaction, land as notes/): 3 NEGATIVE-DRILLS (additive-capturable-where-genuine aabb3757 / consolidation-narrow aacc1f46 / mechanism-limits a046af7f -- 1 of 3 sub-results in [joint-coding brain-consistent]).**

**NEXT-SESSION FIRST ACTIONS: (1) read the drill notes (scale=structural-not-scale; the 3 negative-drills; the additive-capturable-where-genuine + consolidation-missing-mechanism); (2) SSH-pull run-3 Costanzo metrics + certify the fair-test REFUTE off-disk; (3) ACT ON THE STRUCTURAL REFRAME -- change/enrich the operator to exploit repeated-relational-template structure (NOT scale-up); (4) test chemistry-QSAR/drug-synergy pockets (untested). HELD FOR USER: atomization batch (5 VET'd). QUEUED: combined relation-multiplicity+reachability audit; module #2 interface test; reachability-scramble-control fix; ordinal_encoding fix. OPS: NO LOCAL COMPUTE (all remote); full-auto; commit-early.**

### ========== END COMPACTION-FINAL ==========


### ========== 🧭 2026-07-15 LATEST — CURRENT STATE NOW (READ FIRST; supersedes all blocks below) ==========

**REASONING MECHANISM = VALIDATED + FULLY SCOPED (the scaffold half is essentially done + honestly bounded):**
- bind = conjunction-reasoning: CHAIN_GRADE on synthetic (learned SYMMETRIC bind discovers non-additive parity 0.99 vs additive 0.40). Two-channel joint-dual code = MIDDLE (both channels discovered, brain-aligned nonlinear-mixed-selectivity, interference mostly-inherent joint-coding cost).
- replay/consolidation = NARROW: catastrophic-forgetting fix for CONTESTED codes (transductive/interference); REFUTED on inductive-real-CSKG + on conjunctions. Not general/inductive/conjunction.
- pattern-separation HEDGE = CHAIN_GRADE (structured storage beats freq via interference-avoidance).
- 3 SEPARATE mechanisms for 3 separate problems. My over-reads (tension, discovery, REFUTE-pessimism) all VET-corrected -- report narrow, let VET widen.

**FOUNDATION = the binding constraint now (mechanism proven, substrate still knows ~nothing). SOURCING PIVOT (USER-confirmed):**
- Conjunction MODULES = INGEST real experimental data w/ MEASURED 2-way non-additivity; factual CORE = LLM-gen. (LLM-gen produces ADDITIVE multi-feature data, 2-for-2: chem + epistasis both additive-capturable vs a STRONG baseline; mechanism reads genuine interactions [ARBITRARY SYM=1.0] but LLMs don't generate them.)
- ARCHITECTURE COMPLETE + de-risked: two-tier + registry conventions (provenance/versioning/held-out-first/FIXED-gate-w-pos+neg-controls) + cross-module INTERFACE SOLVED (hub-identity + spoke-relation; canonical-ID crosswalks [HGNC/PubChem/DrugBank via UniChem/MyGene/Bioregistry] dissolve the ~51% link problem for scientific modules; brain-aligned hub-and-spoke). Note: notes/foundation_module_registry_architecture_and_module1_spec_2026-07-15.md.
- INGEST-CRISPNESS lesson: prefer MACHINE-READABLE research data, VERIFY download+format up front (ALMANAC CellMiner xlsx = 3 parse strikes; Kramer per-compound lacked structure; Costanzo URL 404'd on a filename typo).

**🚨 MODULE #1 LINCHPIN LANDED = REFUTE (held pending VET a57067090 -- the DEEPEST adjudication; do NOT over-correct, unwanted result):** costanzo_epistasis_nativepair_bind_readout_v1 (commit 185d8bd57, PATH_A real data parsed: 7802 gene-pairs, frac_hi 0.164 clears noise floor, machinery VALIDATED pos_ctrl 0.908, power=True). ON REAL MEASURED yeast epistasis, high-|epsilon| NOVEL subset: SYM does NOT beat capacity-matched strong additive -- rel_MAE = -0.032 (needed >=0.30; SYM marginally WORSE); SYM/SADD novel_hi MAE 0.266/0.257. **UNLIKE the 2 prior refutes (chem/epistasis = LLM-gen, weak-baseline/additive-capturable), THIS is REAL genuinely-measured non-additive data w/ a STRONG additive + validated controls + power = the cleanest real-data test, and it REFUTED.** **✅ VET ADJUDICATED (a57067090, 8 seeds not 2) -- NOT the deep reckoning; my/prereg deep-reckoning framing was TOO HARSH (4th over-read the VET corrected).** The refute is a NARROW, genuine ENCODING-DATA-SCALE NULL, failure mode = HARD-FOR-BOTH (NOT additive-capturable): the strong additive does NOT capture the hi target either (beats a MEAN-predictor by only ~2.9%, WORSE than mean on lo; both MAE ~0.26 vs oracle 0) -> NEITHER arm reads it; SYM was NEVER presented a READABLE non-additive target to fail on. Root cause = SIGNAL NOISE: cells_per_pair ~1.8 (per-pair epsilon noise-dominated) + sparse 500-token encoding over 7802 pairs. SYM NOT crippled (pos_ctrl 0.908). **=> does NOT refute the THESIS ("symmetric-bind reads real non-additive structure") + does NOT establish "real interactions are additive-capturable" -- scoped to "yeast-SGA epsilon at THIS encoding/sample-size is unlearnable for ANY arm."** CROSS-ARC: all 3 real-data linchpins INCONCLUSIVE-ON-SIGNAL, NOT thesis refutes -- Kramer(fragmentation-blocked), ALMANAC(format-blocked), Costanzo(signal-noise-blocked) = 3 data-engineering walls, 0 clean mechanism refutes. **NEW SOURCING CRITERION (4th): HIGH-SNR / READABLE-SIGNAL (the additive must clear MEAN materially = a readable target EXISTS) -- gate this BEFORE the SYM-vs-additive comparison.** REVIVAL DISPATCHED: re-run w/ a readable-signal slice + signal-readability GATE first.
- **🚨 REVIVAL LANDED = FAIR-TEST REFUTE (commit bcd66a6d8, 8 seeds; VET pending additive-capturable-vs-mechanism-gap): readability gate PASSED (readable_rel 0.157 >=0.15 -> a genuine readable signal EXISTS this time, NOT an SNR escape).** On that readable stringent Costanzo slice (TOP_ORF=400, |eps|>0.12, >=2 replicates, frac_hi 0.400, novel_hi_n 1140), SYM does NOT beat strong-additive: novel_hi rel-MAE -0.034 (needed >=0.30). Well-controlled (pos_ctrl 0.908 = SYM reads planted interactions, not crippled; must-fails fire; power=True). **KEY NUANCE: readable_rel = add-vs-mean 0.157 vs sym-vs-mean 0.128 -> the readable signal is LARGELY ADDITIVE (main effects); SYM reads LESS than additive.** So this fair test = REFUTE on real readable epistasis. VET distinguishes: (a) ADDITIVE-CAPTURABLE vs (b) mechanism/encoding gap.
  - **✅ VET (a89fbd90) CONDITIONAL READ = ADDITIVE-CAPTURABLE (NOT mechanism-gap), + a DATA-SYNC FLAG:** (1) 🚨 CHAIN-OF-CUSTODY: the run-3 (18:58Z readable-gated) metrics NEVER SYNCED to local -- on-disk is the STALE 17:38Z run; the readable_rel 0.157 numbers came from the ORCHESTRATOR REPORT, NOT verified off-disk (VET reproduces -0.0341 from the reported MAEs = internally consistent, but UNCERTIFIED). NEXT SESSION: SSH-pull the run-3 metrics to certify. (2) CONDITIONAL VERDICT (arithmetic decisive): ADDITIVE-CAPTURABLE -- the readable signal reduces MAE 15.7% via a PER-GENE additive (which provably can't represent pairwise terms -> that variance IS main effects); SYM (strictly more expressive) reads LESS + loses to additive; pos_ctrl 0.908 proves SYM CAN read planted interaction at this scale -> the interaction ISN'T IN THE DATA, not a mechanism failure. Limit: no direct interaction-residual arm on novel (indirect signature); "residual too weak to matter" IS the thesis-narrowing claim. (3) SCOPE: NARROWS the beats-simpler-methods thesis (3rd real pocket = main-effects-dominated, consistent w/ conjunctions-minority) but does NOT close the program -- single-slice (top-400 ORF/|eps|>0.12/mean-agg/32-dim); chemistry-QSAR + drug-synergy pockets UNTESTED. Disposition MEASURED_MECHANISM/proven-bound (conditional on sync). **CONNECTS TO THE SCALE REFRAME: "interaction isn't in THIS data" could be (i) real interactions genuinely rare, OR (ii) we're testing at a scale far below where the brain's interactions become learnable -> the 3 scale/grounding drills adjudicate.**
- **🧭 USER STRATEGIC REFRAME (2026-07-15, KEY -- may reframe the whole real-data struggle): are we REASONING at a SCALE way too far ahead of our GROUNDED-DATA base?** The brain does interaction-reasoning from a VAST, slowly-accumulated, densely-grounded base; ours is a thin sliver (7k gene-pairs, one module). Maybe the mechanism can't show value until the grounded foundation is MUCH larger/richer -- i.e. the REFUTE may be a SCALE artifact (too little grounded context), not mechanism OR readability. Brain = blueprint (this CAN be done). DRILLS LAUNCHED to answer: composition/type/scope/GROWTH of the brain's grounded-data basis + how the brain EXTRAPOLATES from it + the grounded-scale-vs-reasoning-demand RATIO (are we too far ahead). This is the leading strategic hypothesis to test next session.

**🧠 GROUNDING (Thrust B) = LARGELY SUBSUMED -- the deepest unbuilt half DISSOLVES (drill a730b15f, P_deflated 0.45):** abstract concepts are grounded by RELAY/COMPOSITION from grounded anchors, NOT perception (Harnad zebra=horse+stripes; fMRI-physicists-on-fermions); INSTRUMENT-MEASUREMENT is a legit grounding channel ON PAR with sensorimotor (Kripke/Putnam causal reference + Hacking entity-realism "if you can spray them they're real") -> the MEASURED-interaction data we ingest IS grounding, not a proxy. VERDICT = CONDITIONALLY SUBSUMED: canonical-ID hub solves IDENTITY, measured-attribute modules solve GROUNDING (correctly); NO sensorimotor layer needed (3 literatures converge). THE ONE GAP: identity != grounding (a bare ID w/ no reachable measured content = ungrounded name-tag = closed-relational-island/octopus failure) -> build a cheap REACHABILITY-AUDIT tool (graph traversal certifying measured-reachability) that DOUBLES as a PREDICTIVE DIAGNOSTIC for where relational reasoning underperforms (ties to the 6-instance relational-failure track record). => "substrate knows nothing" is answered by the FOUNDATION ITSELF + 1 cheap tool, NOT a separate grounding thrust. Note: notes/drill_grounding_scoping_is_it_subsumed_by_foundation_hub_or_separate_2026-07-15.md. [[project_grounding_subsumed_by_measured_attribute_foundation_instrument_grounding_reachability_audit_2026-07-15]].

**IN FLIGHT:** Costanzo linchpin (orchestrator a1041f9a, running -- THE verdict). **DONE this cycle:** foundation-pipeline scout (module-#2 = BioGRID yeast, exact-ORF-ID join w/ Costanzo, zero crosswalk); grounding scoping-drill (subsumed + reachability-audit). **HELD FOR USER:** atomization batch (4 VET'd; needs direct auth). **QUEUED (concrete, mostly gated on linchpin):** reachability-audit tool (grounding deliverable + relational-failure predictor); module #2 (BioGRID interface stress-test); ordinal_encoding cell-bug fix (low-pri). **OPS:** NO LOCAL COMPUTE (all remote, USER re-locked); full-auto; commit-early (survived 2 API-death agents).

### ========== END 2026-07-15 LATEST ==========

### ========== 🧭 2026-07-15 — CANONICAL FULLS RECOVERED (post-restart; superseded by LATEST above) ==========

**Laptop force-restarted (unauthorized) mid-session; REMOTE box survived + kept running. Orchestrator recovered ALL canonical 5-seed FULLs (SH-9 sync-gap SCP-recovered; local cache was 13h stale). Store integrity OK (no unauthorized atom write -- 2 dirty atoms are old grounding-thread). AUTHORITATIVE 5-SEED VERDICTS:**

| Anchor | 5-seed verdict | status |
|---|---|---|
| **interaction_nonadditive_discovery_v1** (reasoning CORE, 5-seed) | **VET'd CHAIN_GRADE (scoped).** PROVEN: (A) construction (matched op represents non-additive); (B1) learned SYMMETRIC bind DISCOVERS parity (0.992 vs add 0.404, CV 0.056, memorize@chance) = real learned non-additive discovery. **DOWN-SCOPE (my over-read): the "role-keying<->symmetry TENSION" is UNSUPPORTED -- cell reports symmetry_tension=False (BILINEAR unstably does parity, CV 0.36); B2 honest content = role-keying NECESSARY for asymmetric dominance VS A SYMMETRIC CODE (dominance is role-ADDITIVE, not product-interaction). "no single op does both" holds only for Hadamard not bilinear.** | VET DONE (afffb2a3) |
| **interference_avoidance** (de-risk HEDGE, **3-seed** not 5) | **VET'd CHAIN_GRADE construction-proof** (reproduced bit-exact): orth=1.0 vs add-collapse 0.273 (gap 0.727) vs freq_oracle 0.654 = genuine theoretical ceiling (not strawman); disjoint control 0.000 exact; additive collapse = analytic Hebbian crosstalk. Bound: HAND-CONSTRUCTED code (construction-proof of storage value-prop, NOT learned discovery); gap-vs-add is the durable claim (orth>freq N-contingent). | VET DONE (afffb2a3) |
| consol_interleaved_replay_v1 | HARD_PASS (0.909) | VET'd BELIEVED (construction-proof/transductive/planted) |
| joint_dual_channel_readout_v1 (two-channel unification) | **MIDDLE** (both channels discovered + head-disc clean; interference over-tol = the known 0.083 crosstalk + 0.10 inherent joint-coding cost) | validated-partial; brain-aligned (nonlinear mixed selectivity, lambda=0) |
| random_expansion_parity_dominance | PARTIAL_DOMINANCE_ONLY | 5-seed confirms (doesn't unify) |
| ordinal_magnitude_encoding | **FAILED (cell bug: metrics_invalid missing)** | finding (additive freq-capped) STANDS from preview; canonical lost to bug; low-pri cell-fix |
| consol_inductive_entity_replay_cskg_v1 | RUNNING (re-shipped post-restart) | will land |

**NET STATE (VET-corrected): the reasoning-mechanism half LANDED = CHAIN_GRADE for (A) construction + (B1) learned SYMMETRIC-bind discovers non-additive PARITY (the solid capability result) + role-keying-necessity-vs-symmetric-code; the "hard role-keying<->symmetry TENSION" headline was my OVER-READ (down-scoped -- bilinear unstably does both). Two-channel joint-dual (MIDDLE) STANDS on its own evidence (both channels in one code, brain-aligned nonlinear mixed selectivity) but its MOTIVATION softens from "resolving a hard tension" to "a reliable way to carry both where a single bilinear is unstable." De-risk hedge = CHAIN_GRADE construction-proof (3-seed). Consolidation BELIEVED. Foundation de-risked (chem non-additive cluster). Lost cells (chem-integration real-data proof + consol-on-conjunctions) RE-DISPATCHED. PROCESS WINS: commit-early+ship-remote made restart nearly lossless; raw-then-VET caught the tension over-read (again -- 3rd over-read this session the VET corrected).**

**🚨 REAL-DATA PROOF = REFUTE (the linchpin negative; VET in flight a2f9a9e8; commit 7cc2c1663, 10 seeds, discriminator-valid).** The frontier's learned SYMMETRIC product bind does NOT read the real chem-hazard conjunction better than a CAPACITY-MATCHED additive: SEEN SYM 0.721 vs LEARN_ADD 0.743 (gap -0.022), NOVEL -0.090, SYM-FREQ only +0.069. Must-fails fire, oracle 1.0, fairness checked (SYM->1.0 on synthetic plant = not under-trained; SYM vs ADD differ ONLY in compose op). **LOCALIZATION (drilled): the cluster's headline "non-additivity 0.263" was measured vs a WEAK ordinal-lstsq additive (0.435); vs a capacity-matched CATEGORICAL additive (0.743 = generator exact-conditional) the non-additive advantage VANISHES -> the real chem cluster is mostly MAIN-EFFECTS, NOT genuinely non-additive against a strong baseline. Synthetic parity had ZERO additive info (product was the only option); real chem has HIGH additive info, so the product bind's niche does not apply.**
  - **SCOPE (deflated, don't over-correct): this is primarily a FOUNDATION-DATA finding (the non-additivity GATE was too weak -- gated vs weak-additive not strong-capacity-matched), NOT a proven mechanism-death.** Re-confirms real conjunctions are minority + main-effects-dominate EVEN in the targeted chemistry pocket. OPEN: does a GENUINELY-strong-non-additive real target exist (epistasis AND-gate, true QSAR nonadditivity, additive-info genuinely LOW) where the mechanism wins? chem-hazard wasn't it.
  - **✅ VET ADJUDICATED (skunkworks a2f9a9e8) -- REFUTE is GENUINE (10 seeds, 0/10 pass) but a DATA/FOUNDATION-GATE finding, NOT a mechanism failure. MY "synth->real transfer fails" FRAMING IS WRONG (VET load-bearing correction; I over-corrected pessimistically = 4th framing correction this session, this one in the pessimistic direction).** DECISIVE: on ARBITRARY-seen (real chemicals, pure non-additive labels) SYM=1.000 vs ADD=0.830 -- the mechanism READS GENUINE INTERACTIONS on real data; transfer machinery INTACT. The chem cluster is ~98% MAIN-EFFECTS vs a STRONG categorical additive (non-additivity +0.42 vs weak ordinal-lstsq but +0.022 vs strong categorical; agrees w/ exact conditional on 90.4% rows). The "0.263" was a WEAK-baseline artifact (monotone-scale encoding mismatch = the match-code-to-data lever). SYM 0.721 ~= MEMORIZE 0.720 = extracts full available signal, nothing non-additive to exceed. Fairness clean both ways.
  - **REVIVAL AUTHORED + shipping remote (a64cddf8 -> orchestrator a65e0bad; NO local compute per re-lock): (1) STRONG categorical-multinomial additive gate (weak-vs-strong reported side by side); (2) genuine EPISTASIS/synthetic-lethality pocket (gene pair x pathway-class, DNA-repair split so not additive-count); (3) sonnet+opus generators. cells generated_epistasis_nonadditive_v1 + epistasis_bind_readout_integration_v1, commits aa0018231/679948f87, artifact SCP'd to remote (no push).**
    - **🔬 FOUNDATION FINDING (truth, not mechanism): epistasis-severity facts are INTRINSICALLY DEBATABLE -- adversarial truth 0.758 sonnet / 0.746 opus, BOTH below 0.85 bar, STRONGER GENERATOR DOESN'T FIX IT (opus marginally worse). => POCKET SELECTION must weigh FACT-CRISPNESS, not just interaction-genuineness (chemistry-QSAR potency may be crisper than epistasis-severity). A named REFUTE branch (generator can't vet epistasis at bar) -- but the MECHANISM question is separable: runs on the 132 vetted-TRUE rows.**
    - **🚨 LINCHPIN LANDED REMOTE = MIDDLE_BAND (VET in flight a42c6b18; hold): SYM 0.5931 vs capacity-matched STRONG categorical-additive 0.5530 = margin +0.040 (< 0.10 bar); ROLE 0.589 ~= SYM; FREQ 0.505; chance 0.470. SYM does NOT decisively beat strong additive. CAVEAT: the GATE cell verdict = INCONCLUSIVE_GATE_INVALID (strong additive CANNOT cleanly separate additive from non-additive) -- multi-feature dependence present (joint MI 1.30 vs single 0.67, margin 0.631) but a flexible categorical additive captures most of it => the epistasis cluster may NOT be genuinely non-additive at the STRONG bar either.**
    - **🔻 EMERGING PATTERN (2nd cluster, VET-gated -- do NOT over-correct yet): BOTH LLM-generated clusters (chem ~98% main-effects; epistasis inconclusive/MIDDLE) FAILED the strong non-additivity bar, EVEN in pockets picked for interaction. The MECHANISM works on genuinely-non-additive data (ARBITRARY-seen SYM=1.0) -- but we cannot GENERATE genuinely-non-additive real data via LLM (it produces plausible MULTI-FEATURE-but-ADDITIVE data a flexible additive captures). STRATEGIC FORK: genuine non-additive structure may need REAL EXPERIMENTAL DATASETS -- NOT LLM-generation.**
    - **✅ VET ADJUDICATED (a42c6b18, recompute-off-disk): (1) linchpin MIDDLE is a SEEN-ONLY MEMORIZATION edge NOT generalizable interaction -- SYM +0.040 on seen REVERSES to -0.117 on NOVEL, SYM~=MEMORIZE; framing SYM as any "positive interaction edge" = OVER-READ (Fix#28). (2) epistasis cluster is ADDITIVE-CAPTURABLE robustly (interactive-minus-strong-additive <=0 in ALL 10 seeds; the joint-MI margin is multi-feature main-effects not interaction -- proven by an additive-synth yielding the same margin). (3) gate INCONCLUSIVE = clean-negative data-fact + a routing artifact + a real gate weakness (its addsynth positive-control is SATURATION-VACUOUS -- no proof the gate WOULD catch real non-additivity). (4) mechanism FINE: ARBITRARY-control SYM=1.0 = working sensitivity control. (5) PATTERN = SUPPORTED-DIRECTIONAL 2-for-2 (both LLM-gen clusters = additive multi-feature, same signature), NOT a proven wall (n=2; no genuine non-additive positive control passed a gate yet).**
    - **🎯 DECISION (VET-justified, surfaced to USER): PIVOT the conjunction MODULES from GENERATE -> INGEST real experimental datasets where 2-way non-additivity is EMPIRICALLY MEASURED (QSAR matched-molecular-pair nonadditivity, DepMap/Costanzo quantitative epistasis, NCI-ALMANAC/DrugComb drug-synergy excess). LLM-gen stays for the factual CORE; conjunction modules = ingested-real. => the two foundation tiers differ by SOURCING METHOD, not just density. Research scout dispatched (a708fb86) for the #1 real dataset. ONE-MORE-DRILL (makes it chain-grade): ingest 1 real measured-non-additive dataset -> FIXED gate (genuine non-additive positive control, not saturation-vacuous) -> re-run transfer proof. If SYM beats strong-additive on REAL MEASURED non-additive data = thesis proven end-to-end + module #1 real; if not = the deep reckoning.** => corrected picture is ENCOURAGING: mechanism transfers to real genuine interactions; foundation just needs strong-gate + right-pocket + stronger-generator. Atomization: interaction-core atom bound = "reads genuine real interactions (ARBITRARY-seen SYM=1.0); natural chem-hazard cluster was main-effects (gate-too-weak)" -- NOT "fails to transfer." Q1 foundation-spec ADD: strong categorical gate is mandatory (the weak gate was the bug). Q2 mechanism: transfer-capable CONFIRMED (not just synthetic).

**🔬 CONSOLIDATION-ON-CONJUNCTIONS = REFUTE (commit ace442e2e, 5 fresh seeds, clean; local FULL decisive, remote optional) -- SHARPENS not kills.** Interleaved-replay does NOT beat compute-matched continual on a conjunction: INTER 1.0 = CONT 1.0, schedule-gap 0.000 (holds OFF-ceiling P=3: 1.0 vs 0.998, not saturation); freq-at-chance CONFIRMED (parity single-driver MI=0), readout beats freq +0.523 (genuine conjunction). **LOCALIZATION: a symmetric conjunction is globally recoverable from ANY learning block -> NO contested-code interference for replay to fix. => replay's advantage is INTERFERENCE-GATED (contested codes across blocks = the CSKG forgetting case), NOT conjunction-gated. My "consolidation payoff shows on conjunctions" hypothesis = WRONG.** CLARIFIED 3-SEPARATE-MECHANISM picture: (1) conjunction-reasoning = the BIND/readout (proven; not consolidation); (2) consolidation/replay = catastrophic-forgetting fix for CONTESTED codes in LEARNING (real CSKG, absent on symmetric conj); (3) interference-avoidance HEDGE = crosstalk fix in STORAGE (chain-grade). Consolidation atom scope TIGHTENS: contested-code forgetting fix, NOT general inference-structure win. QUEUED follow-on (not critical-path): interference-BEARING conjunction arena (shared codes contested across blocks).

**🧩 REAL-DATA INGEST (module #1) -- Kramer PATH-B + native-pair is the linchpin:** Kramer 2021 chemistry MMP nonadditivity SHIPPED remote (commit 66cc9bafe) = PATH_B ESCALATE (per-compound CSVs carry abundant measured Nonadd_pC [frac>0.3 = 0.48] but NO cycle/transformation columns -> R1,R2 unrecoverable w/o RDKit+MMP fragmentation; cell refused a broken encoding). **✅ FIXED GATE VALIDATED: positive control fires (SYM beats additive on planted interaction rel=0.908) + negative control fires (SYM does NOT beat additive on additive arena rel=-0.018) -> the saturation-vacuous hole the VET flagged is CLOSED; the instrument genuinely detects non-additivity + doesn't false-positive.** => escalate to NATIVE-PAIR datasets (the two entities ARE constituents, interaction measured per pair, no fragmentation): NCI-ALMANAC drug1xdrug2+ComboScore OR Costanzo gene1xgene2+epsilon. NATIVE-PAIR cell in flight (a40596065, all-remote) = THE real-data linchpin w/ validated gate. Kramer per-compound route CLOSED (per-circle pipeline output or native-pair only).

**🧠 CONSOLIDATION FULLY SCOPED (consol-inductive FULL landed = REFUTE_REPLAY_NO_INDUCTIVE_ADVANTAGE):** 3 results settle it -- transductive-planted BELIEVED (construction-proof, replay beats continual via catastrophic-forgetting fix on CONTESTED codes); inductive-real-CSKG REFUTE (no novel-entity advantage); conjunctions REFUTE (no schedule advantage, conjunction = readout effect). **=> replay's value is NARROW + SPECIFIC: a catastrophic-forgetting fix for CONTESTED codes in transductive/interference settings -- NOT general inference-manufacturing, NOT inductive, NOT conjunction-specific.** Tightens the consolidation-MM atom bound. Clean REFUTEs (consistent), not VET'd. Three-mechanism picture confirmed: bind=conjunction-reasoning, replay=interference/forgetting-fix, hedge=storage-crosstalk-fix -- separate mechanisms, separate problems.

### ========== END 2026-07-15 ==========

### ========== 🧭 2026-07-14d — RESULTS LANDING (14c below = the frontier arc) ==========

**FULL-AUTO 3-halves push landing results. All wanted results HELD til skunkworks landed-VET (operating loop [[feedback_full_auto_operating_loop_vet_drill_negatives_2x_brain_reference_optimize_substrate_2026-07-14]]).**

- **CODE-LEARNING half: Consolidation P1 FULL = HARD_PASS, VET'd = BELIEVED (construction-proof / MEASURED_MECHANISM, NOT a chain-grade capability win -- skunkworks a976c0ead RE-SCOPED my too-strong "validated at scale" framing).** Numbers CLEAN (reproduce 4dp, tight CV, sub-relation lift real, genuinely sub-ceiling): INTERLEAVED mrr 0.909 vs CONTINUAL 0.418 / SHUFFLE 0.361 / POP 0.331; beat_gap 0.490; uses REAL AdditiveKGMap readout; determinism clean. **HONEST BOUNDS (carry them):** (1) TRANSDUCTIVE not inductive -- held-out = novel (head,rel) PAIRS, both individually seen; NOT novel-entity generalization. (2) CONSTRUCTION-PROOF -- arena PLANTED to match the TransE readout's inductive bias (mechanism-analog != task-analog); could have failed (earned controls: SHUFFLE-flat + genuine forgetting curve) but does NOT prove real KGs carry this or that inductive generalization holds. (3) CONTINUAL not compute-matched (prereg line 24 "matched total gradient steps" is FALSE, needs correction) -- BUT the beat SURVIVES structurally (each domain saturates at 0.926 then shared D[r] catastrophically overwritten; compute-matched continual would forget WORSE). (4) HEAVILY PRECEDENTED arc (98 "consolidat" atoms; replay-beats-continual known) -- NOVEL slice = live-AdditiveKGMap integration + transductive-Saxe-hierarchy readout + staging-at-scale; atomize as targeted EXTENSION not rediscovery. (5) 2 signatures soft (dose peaks P4-8 then mild overfit; staging low-res 4/5). A5 atomize as MM/construction-proof HELD pending user-auth. **=> ENVELOPE-PUSH (optimize to capability): inductive (novel-entity) + REAL reduced-CSKG (not planted arena) consolidation.**
  - **ENVELOPE-PUSH DONE (commit 1719170f8, smoke; FULL ready-to-ship HELD for machine load; both VET bounds removed = real k30 CSKG N=2335/151k edges + inductive novel-ENTITY split + COMPUTE-MATCHED continual):** MECHANISM SURVIVES the harder test -- interleaved-replay BEATS compute-matched continual +0.030 MRR on real inductive (NOT a planted-arena artifact; upgrades the mechanism claim). BUT interleaved only MATCHES frequency/POP (dose plateaus barely above POP then overfits) -> likely MIDDLE_BAND. **HONEST SYNTHESIS: real single-relation inductive inference is FREQUENCY-CAPPED (the program's core reason we pivoted to conjunctions). Consolidation improves LEARNING DYNAMICS (beats continual) but can't beat frequency on a frequency-capped task. => consolidation's CAPABILITY payoff should show on CONJUNCTION tasks (frequency-uncapped), NOT single-relation = the next drill (ties consolidation to the conjunction frontier).**
- **✅ FOUNDATION half de-risked: chemistry pocket yields the FIRST genuinely NON-ADDITIVE vettable cluster (data/foundation_clusters/chem_pair_hazard_nonadditive_v1.json, 135 vetted-true pairs).** Verdict label says REFUTED but that fires ONLY on generation-truth (0.833 < 0.85 haiku ceiling); **conjunctivity + genuine non-additivity CONFIRMED** (interactive-vs-additive 0.263 >= 0.15; additive-synth control 0.028, shuffle 0.091; dominance ratio 0.583, no dominant driver). Chem-mixing hazard is genuinely relational (ammonia+water=none,+bleach=severe). Drill-2 pocket prediction VALIDATED; pocket-targeting SEPARATES conjunctive-structure(SOLVED) from factual-truth(generator-quality gap, fix=stronger generator). Foods-style additive-capture failure mode is GONE.
- **🎯 INTEGRATION cell dispatched (ad2ba1a86, THE real-data proof):** does the frontier's LEARNED SYMMETRIC bind (proven on synthetic parity) discover the REAL chem non-additive conjunction on novel pairs, beating additive + frequency (chem hazard is symmetric non-additive)? Connects reasoning mechanism x real foundation data end-to-end. + label-noise robustness (real truth 0.833).
- **IN FLIGHT:** combined two-channel code cell (a339e90b, frontier); chem integration cell (ad2ba1a86); consol VET (a976c0ead). **4 earlier FULLs (frontier 59056b6d4 / tension a20ce349d / de-risk cd813a95e / encoding cd2ada930) -> confirm landing + route load-bearing ones (frontier HARD_PASS_B, de-risk HARD_PASS) to VET.**
- **GIT NOTE:** commit a6d93fbae holds BOTH consol + chem cells (foundation agent's `git add -A` race, CLAUDE.md-warned); files durable, worktree-clean, NOT pushed, store untouched. Cosmetic mislabel; clean later.

- **🎯🧠 FRONTIER RESULT -- COMBINED TWO-CHANNEL CODE (joint_dual_channel_readout_v1) LANDED (smoke 2-seed, FULL pending; verdict MIDDLE_BOTH_DISCOVERED_HEADDISC_CLEAN_INTERFERENCE_OVER_TOL): the brain-derived joint-code+selective-readout design WORKS -- one code carries BOTH symmetry classes, separable by readout; MIDDLE only on a ONE-SIDED parity interference = a tuning target, not a rethink.** 7/8 gates pass. DOMINANCE (asymmetric): JD_ORDER head=1.000 = MATCHES role-keyed specialist (rel_drop -0.005, ZERO interference). PARITY (symmetric): JD_CONFIG head=0.783 discovers it but BELOW symmetric-product specialist 0.950 (rel_drop 0.175; carrying the order channel costs 0.083 dualhead_drop). Head-discrimination CLEAN both ways (wrong head ~chance). Only failing gate = parity_no_interference (G5). INTERFERENCE IS ONE-SIDED (order channel contaminates parity readout, not vice versa) -- role-keyed + symmetric-product terms share dims. Core thesis (Bernardi joint-code+selective-readout on native primitives) VALIDATED. 5-seed FULL shipped (commit 947d8c913, remote self-test PASS, pending ~35-60min) -> landed-VET.
  - **DEEP FINDINGS (agent weak-point localization):** (1) the parity gap 0.176 = ~0.083 pure dual-head CROSSTALK (small -- the contract's worry is minor) + ~0.10 REPRESENTATIONAL cost (reading symmetric channel out of a role-keyed joint code: CONFIG_SOLO 0.854 vs dedicated SYM_PROD 0.949) = inherent price of joint coding, seed-variable. (2) **LAMBDA MUST = 0: a linear readout of a superposition is linearly entangled -- a bundled symmetric term leaks the parity SIGN into the role-unbind and breaks head-disc (parity JD_ORDER 0.41->1.0 as lambda 0->0.25). Clean separation = role-keyed bundle is the joint code + config read by a NONLINEAR (product) lens the linear order-head CANNOT FAKE = the brain's NONLINEAR MIXED SELECTIVITY (Rigotti/Fusi). The design converged empirically onto the brain's actual trick: one code, LINEAR readout for order + NONLINEAR readout for configural.** EMB_D=96. => the two-channel design is VALIDATED + mechanistically brain-aligned; interference-reduction has LOW headroom (crosstalk only 0.083; the 0.10 is inherent joint-coding cost) -> NOT a priority; the design is essentially DONE.
  - **LOAD MYSTERY SOLVED:** the machine saturation was ~11 STRAY TORCH PROCESSES from earlier sweeps (agent cleared them) + concurrent smokes. Machine now CLEAN. (Lesson: check for orphaned torch procs when fs slows, not just concurrent-agent count.)

### ========== END 2026-07-14d ==========

### ========== 🧭 2026-07-14c — CURRENT THRUST (the frontier arc that led here) ==========

**🎯 THE CONVERGENCE (two independent threads point at the SAME next experiment): the ADDITIVE/monotone regime is characterized (solved-but-modest); the high-value conjunctions are NON-ADDITIVE and require an INTERACTION / NON-COMMUTATIVE bind. THAT is the enrichment thrust now.**

**Three results landed this session (all verified on disk):**
1. **Generation cell -> MIDDLE_BAND (foods cluster, commit e7e5f1135, 5 seeds, truth 0.896).** A genuinely-conjunctive REAL target DOES exist and can be generated: joint_MI 1.80, best_single 0.55 (water_content), dominance ratio 0.306 = NO single driver dominates (a counterexample to strict single-driver-dominance, found OUTSIDE the drill's pockets). Monotone beats modular (MONO_WEIGHT 0.420 vs MODULAR 0.204), must-fails fire (ARB -0.094, SHUF -0.143), oracle 1.0. BUT beats FREQ_NULL by only **+0.088** (< +0.10 HARD band -> MIDDLE) -- because per-feature frequency already captures much of a no-dominant-driver ADDITIVE target. metrics: data/exp_generated_conjunction_monotone_foods_v1/metrics.json; cell experiments/exp_generated_conjunction_monotone_foods_v1.py.
2. **THE STRUCTURAL INSIGHT (exp_dev, honest -- it declined the chemistry/epistasis steer for a real reason):** a monotone-ADDITIVE code STRUCTURALLY CANNOT win the drill's high-interaction pockets. Inline no-API probe: monotone-freq = -0.018 on AND-gate (synthetic-lethality style), -0.006 on parity (QSAR-nonadditivity style), +0.423 only on balanced-additive. => the STRONG conjunctions in epistasis/QSAR are NON-ADDITIVE interaction structures; pivoting a monotone-additive cell there = a structural non-result. Recommendation: a SEPARATE interaction/multiplicative-bind cell for those pockets.
3. **DISCOVERY VET = PROVEN (scoped)** [detail in 2026-07-14b block below]: the architecture discovers COMMUTATIVE/abelian structure; asymmetric = ARCHITECTURAL WALL (readout symmetric under a<->b). Same lever as #2: break bind commutativity.
4. **Drill 1 (ordinal magnitude coding, notes/drill_ordinal_magnitude_coding_and_composition_bestinclass_2026-07-14.md + exp_dev handoff note):** our thermometer has the monotone part right but is MISSING the brain's LOG/Weber compression. Top additive-regime encoding upgrade = LOG-SPACED thermometer bins; #2 = reliability-weighted additive (Ernst-Banks). (This improves the ADDITIVE regime; orthogonal to the non-additive thrust.)
5. **Drill 2 (conjunctive-determination prevalence, notes/drill_realworld_conjunctive_determination_prevalence_and_targets_2026-07-14.md):** single-driver-dominance is the GENERAL case (main effects >> interactions ~10x); genuine no-dominant-driver conjunctions concentrate in chemistry-QSAR / genetic-epistasis / drug-synergy / threshold pockets (all NON-ADDITIVE, per #2). HEDGE (decouples the bet): conjunctive CODING independently justified by pattern-separation/interference-avoidance even for single-driver attrs. [[project_realworld_conjunctions_are_minority_regime_target_chemistry_epistasis_pockets_2026-07-14]].

**🚦 NEXT-SESSION #1 (now sharply defined + doubly-motivated): an INTERACTION / NON-COMMUTATIVE / MULTIPLICATIVE-BIND cell** that reads out NON-ADDITIVE conjunctions (AND-gate/parity/multiplicative = the epistasis/QSAR pockets) where the monotone-additive code provably loses (-0.018/-0.006). This is the discovery-VET's "break commutativity" lever AND the generation cell's "non-additive pockets need an interaction code" recommendation = the same experiment. Route to exp_dev: generate/plant a non-additive conjunction (AND-gate or multiplicative), interaction-bind readout vs FREQ_NULL + monotone-additive (expected-fail contrast) + must-fails. SECONDARY (additive regime polish): log-spaced-thermometer upgrade cell (Drill 1). A5 atomization of the discovery-bound: HOLD until the non-commutative cell lands (then atomize once).

**A5 DECISION:** HOLD -> atomize discovery-proven-bound ONCE after the non-commutative-bind cell (avoid churn; user deferred to director 2026-07-14b).

**🧪 CAMPAIGN UPDATE (Thrust-1 push, 4 threads dispatched -- all routing to remote_cpu_queue per no-local-smokes correction after USER flagged idle dash):**
- **LOG-THERMOMETER cell LANDED (PREVIEW; canonical multi-seed FULL now on remote_cpu_queue, commit cd2ada930, anchor ordinal_magnitude_encoding_vs_combination_v1) -- SETTLES the additive-regime fork:** reproduction gate exact (0.5725 vs 0.573). Anchor 1 (log/Weber ENCODING) = HARD_FAIL across 3 schemes x 2 clusters (+0.006 animals, -0.031 foods) -> uniform binning was NOT the bottleneck; the brain's log-compression doesn't help this task. Anchor 2 (Ernst-Banks reliability-weighted COMBINATION) = conditional (HARD_PASS animals closes 32.8% failures +0.071; HARD_FAIL foods). => the ADDITIVE regime is FREQUENCY/DATA-CAPPED, not encoding-limited -> do NOT invest more in additive encoding; reinforces the NON-ADDITIVE frontier as the value. (Hold strong framing until landed remote VET.)
- **BRAIN NON-ADDITIVE-CODING DRILL DELIVERED (a48d3739, notes/drill_brain_nonadditive_interaction_relational_coding_bestinclass_2026-07-14.md, P_deflated 0.42) -- relayed into the frontier cell.** Brain does non-additive via NONLINEAR EXPANSION (conjunctive/mixed-selectivity/multiplicative-gain) -> higher-dim -> simple LINEAR readout; two separable fixes (interaction-capacity vs order-asymmetry). **TOP substrate mechanism = LEARNED LOW-RANK BILINEAR / gated multiplicative bind: z=(Pa)(*)(Qb), P/Q learned (Kim 2016 low-rank bilinear pooling). Our elementwise bind is ALREADY the P=Q=identity special case, so learning P,Q is a MINIMAL upgrade -- and LEARNABLE (not construct).** Brain-aligned via gain fields (Zipser-Andersen 1988) + PFC rank x item (Dehaene 2015). Recommended head-to-head (= the frontier cell's arms): Rank-1 learned-bilinear vs Rank-2 fixed-permutation (asymmetry-only, construct) vs Rank-3 fixed-random-expansion+learned-readout. NOTE substrate already has GHRR matrix-vector bind (June-2026 pilot, non-commutative) -- bilinear is cheaper/better-precedented.
- **🛡️ DE-RISK CELL LANDED (SMOKE HARD_PASS; HOLD mechanism-story til landed-VET -- WANTED result, be extra disciplined): the interference-avoidance hedge HOLDS.** interference_avoidance_conjunctive_vs_additive_v1 (commit cd813a95e, canonical FULL 3seeds x 48units on remote_cpu_queue). For a SINGLE-DRIVER-dominated attribute, conjunctive/orthogonal storage beats BOTH additive AND frequency incl the ORACLE dominant-factor baseline: at M=256 orth=1.000 vs add=0.268 vs freq_oracle=0.672 (gap_orth_add 0.732, gap_orth_freq 0.328); additive COLLAPSES below no-storage baseline (crossover M=64 = self-cannibalizing crosstalk in shared feature pool); must-fail disjoint control gap=0.000 (genuine interference-avoidance, not confound); 2/2 smoke seeds. => structured codes have a SECOND value-prop (pattern-separation storage, the hippocampal split) INDEPENDENT of attribute interactivity -- de-risks the whole conjunction bet. ON LANDING: route FULL to skunkworks landed-VET before atomizing.
- **🚨 FRONTIER CELL -- VET'd = MIDDLE_BAND (directional, NOT certifiable); the "REFUTE_B" was a FALSE-REFUTE on a STALE+BUGGY run; my earlier over-read ALSO corrected. Real result = a ROLE-KEYING<->SYMMETRY TENSION.** (skunkworks a4465a1, exemplary VET.)
  - **BUG (load-bearing, known class):** plant_regime() seeds the ARBITRARY/SHUFFLE must-fail RNG with abs(hash((family,regime))) = Python SALTED hash (PYTHONHASHSEED per-process) = nondeterministic must-fail GATES = the list(set()) hash-seed class already in memory. CLEAN metrics deterministic; only the cleanliness gates moved (tripped on n=99 noise) -> flipped A->MIDDLE, B->REFUTE. Cell also RACED (agent re-ran it after my relay; I VET'd/read a stale run first). NO locatable prereg (bands only in docstring) -- flag.
  - **CORRECTED findings (off reproducible CLEAN numbers):** (a) GENUINE WIN I MISSED: shared-code SYMMETRIC PRODUCT bind DISCOVERS symmetric non-additive PARITY at 0.980 vs learned-additive 0.384 (chance 0.518) -- buried because that arm was only wired as a must-fail contrast. (b) The DOMINANCE win is ROLE-KEYING, NOT the product/bilinear op (role-keyed LEARN_ADD also =1.0); honest margin vs FREQ floor = +0.14, not the +0.53 (which used the crippled symmetric arm as baseline). (c) AND2/MULT are NOT interaction-discovery wins -- learned-additive matches them (MULT log-additive, AND2 threshold-additive). (d) role-keyed bilinear FAILS parity (LINT 0.318; cv~0.5). 
  - **THE REAL RESULT = ROLE-KEYING<->SYMMETRY TENSION:** the SAME lever (role-keying) that enables asymmetric (dominance) DESTROYS symmetric (parity); the symmetric product does the reverse. Neither specialized bind does BOTH. NOT "discovery bounded to additive." (My prior over-read -- AND2/MULT/dominance as bilinear discovery wins breaking the wall -- was WRONG; the hold-then-VET discipline caught it. Discipline validated.)
  - **FIXES APPLIED + CANONICAL SHIPPED (aae41ec6, commit 59056b6d4, all 6 VET fixes + remote-portability fix + MUSTFAIL_TOL 0.07->0.10):** deterministic FAM_IDX/REG_IDX seeding (verdict now STABLE, no more flip); LEARN_SYM wired as explicit parity-discovery arm; dominance re-baselined vs FREQ_NULL; AND2/MULT stated transform-additive (not wins); 5 seeds (7,13,17,23,29); real prereg written. Canonical 5-seed FULL on remote_cpu_queue (QUEUE_ADD_EXIT=0, remote self-test 11.5s, PROT-023 preflight passed clean). **2-seed deterministic PREVIEW = HARD_PASS_A | HARD_PASS_B_SYMMETRY_MATCHED_DISCOVERY (HOLD til 5-seed lands+VET):** PARITY LEARN_SYM 0.98 >> LEARN_ADD 0.384 (+0.60, genuine symmetric non-additive discovery); DOMINANCE role-keyed 1.00 vs FREQ_NULL 0.86 (+0.14 honest); symmetry_tension=True. Gaps large -> CV unlikely to move it, but 5-seed confirms. ON LANDING -> skunkworks landed-VET, then atomizable as MEASURED_MECHANISM (symmetric bind discovers symmetric non-additive; role-keying trades parity for order; the tension is the result).
  - **XOR cell (aa51a955) REFRAMED + LANDED (smoke 2-seed, PRELIMINARY, FULL 5-seed on remote_cpu_queue, commit a20ce349d, real prereg written): verdict PARTIAL_DOMINANCE_ONLY -- random expansion does NOT unify.** RE is a DOMINANCE specialist (mirror of role-keyed bilinear): DOMINANCE RE=1.0 (D_exp threshold=16, +0.535 over symmetric specialist), AND2/MULT=1.0; PARITY RE=0.313 SUB-CHANCE (threshold=None across the whole sweep, worse than bilinear -0.116), across BOTH ReLU + Fourier. Principled: high-order parity is the classic hard case for random-feature/kernel methods (dimensionality gives train-separability, NOT novel-combo generalization). Must-fails fire clean (genuinely generalizing not memorizing; cell VALID). **=> THE TENSION MAP IS NOW CLEAN: symmetric-product does parity-not-dominance; role-keyed-bilinear + random-expansion both do dominance-not-parity. NONE does both. Undifferentiated high-D mixing does NOT dissolve the tension.** UNIFICATION likely needs an EXPLICITLY symmetric-AND-asymmetric COMBINED/decomposed code (both channels), not a reservoir.
  - **🧠 UNIFICATION DRILL DELIVERED (a0f0213b, notes/drill_brain_unifies_symmetric_asymmetric_binding_factorization_2026-07-14.md, P_deflated 0.38): brain unifies via JOINT CODE + SELECTIVE LINEAR READOUT** (Bernardi/Fusi/Salzman 2020 Cell: SAME population, different linear projections extract abstract-invariant vs conjunctive), NOT one operator, NOT separate-subsystems-composed-downstream. Buildable Rank-1 design: **z = (r1(x)a + r2(x)b) + lambda*(a(x)b)** [CONSTRUCT: native bind+bundle+role vectors] + TWO LEARNABLE readout heads (H_ORDER=unbind-by-role for asymmetric/dominance; H_CONFIG=role-agnostic projection on a(x)b for symmetric/parity). = the substrate realization of structure-content factorization (the PRIMARY FOCUS). Caveats: joint-code-selective-readout is a research synthesis; WHERE-asymmetry-lives is CONTESTED (transition-dynamics vs plasticity-split vs additive-readout, 3 unreconciled).
  - **🎯 COMBINED TWO-CHANNEL CODE CELL DISPATCHED (a339e90b, THE central experiment):** does JOINT_DUAL (z + both heads) discover BOTH parity AND dominance on novel combos, with NO cross-channel interference (must match each specialist) + head-discrimination (wrong head fails each family)? Reuses the VET-clean frontier arena/controls; >=5 seeds; routes remote. DISPATCHED NOW (design grounded + tension robust across 2 cells) rather than waiting for the FULLs (which confirm tension MAGNITUDE, not existence).
  - **PROCESS GAP:** exp_dev validity-preflight did NOT catch hash-seed nondeterminism in a must-fail control -> harden preflight to flag hash()-seeded RNG in cells (recurrence of a memory'd class).
- **REMOTE:** de-risk FULL + encoding FULL canonical runs on remote_cpu_queue (dashboard-visible). Infra: local runners ZOMBIE in runner_status (stale hb; local_cpu_queue empty) -- remote runner processed landings; flag testbed if recurs.
- **QUEUED DEPTH (confirm-bottleneck-first, after frontier lands):** consolidation P1 integration; TEM structure-content factorization bottleneck (PRIMARY FOCUS core, untested).

**🚀 FULL-AUTO AUTHORIZED (2026-07-14 USER) + 3-HALVES PARALLEL PUSH (never-stand re-locked):** operating loop = keep docs current / optimize-substrate-not-just-measure / brain-reference-when-in-doubt / ALWAYS skunkworks-VET + drill-negatives-2x-for-mechanism. [[feedback_full_auto_operating_loop_vet_drill_negatives_2x_brain_reference_optimize_substrate_2026-07-14]]. NOW BUILDING all 3 coupled halves in parallel (not single-tracking the frontier):
- **REASONING MECHANISM:** combined two-channel code cell (a339e90b) -- joint-code + selective-readout frontier.
- **CODE-LEARNING/MAINTENANCE:** consolidation P1 integration cell (a4e64d0f) -- interleaved-replay manufactures inference structure + beats continual at scale (Claim B confirmed, finally integrating).
- **FOUNDATION/DATA:** chemistry-epistasis conjunction-cluster cell (a47687c4) -- can the right pockets (QSAR-nonadditivity/epistasis) yield a GENUINELY conjunctive vettable cluster where organismal domains failed.
- **+ 4 canonical FULLs on remote_cpu_queue -> skunkworks landed-VET on landing:** frontier(59056b6d4), tension(a20ce349d), de-risk(cd813a95e), encoding(cd2ada930).
- **QUEUED DEPTH:** TEM transition-dynamics (richer asymmetry, informed by combined-code result); A5 atomization once VET'd; grounding (Thrust B) once scaffold settles.

### ========== END 2026-07-14c ==========


### ========== 🧭 2026-07-14b — NEXT-SESSION #1 DONE: ENCODING QUESTION SETTLED (READ THIS FIRST, then the consolidated block below) ==========

**✅ MONOTONIC-CODE CELL RESOLVED (inline, over the on-disk 191-animal cluster; directional/toy but decisive + must-fail-clean). The encoding lever is CONFIRMED; the beats-frequency margin is DATA-limited not code-limited.** Held the mechanism (native bind) + split + FREQ_NULL + must-fails IDENTICAL to the refute cell; changed ONLY the code modular->monotone. scratchpad/monotonic_code_ordinal_conjunction.py + mono_data_vs_code_diag.py.
- **LEVER CONFIRMED (GOOD):** same task, swap modular FPE -> monotone code = **0.159 (below chance) -> 0.395 (equal-weight) / 0.573 (learned-weight)** on CLEAN novel. +0.24 to +0.41 absolute purely from matching code to data structure. Must-fails fire clean (ARBITRARY -0.04, SHUFFLE -0.06). Learned weights `[0.18, 0.97, 44.5, 10.1]` = the code DISCOVERS activity+diet drive metabolic rate (matches biology + the MI).
- **BUT beats-freq margin is MODEST (+0.062 over FREQ_NULL 0.511) and DATA-limited (GOOD diagnostic):** planting a BALANCED all-4 monotone target on the SAME entities -> MONO beats FREQ by **+0.387** (equal-weight construction alone wins 0.758 vs 0.358). So monotone code crushes freq WHEN the target is strongly conjunctive; the real metabolic cluster is WEAKLY conjunctive (activity MI 1.11 of 1.88 joint), so freq_null already owns the dominant single relation and no code has headroom. Pure equal-weight construction is NOT enough on real data (0.395 < 0.511); you need LEARNED weighting = the additive_map/learned-code direction.
- **=> ENCODING SETTLED: monotone (not modular) for ordinal magnitude, learned-weighted. The bottleneck is now the DATA (weak conjunctivity), exactly as flagged.** NEW NEXT-SESSION #1: **GENERATE strongly-conjunctive REAL structure** — a vettable cluster whose held-out target genuinely needs combining >=2 constituents with NO single dominant driver (no activity-style MI monopoly) — then show the monotone learned-weight code beats freq_null on novel combos at real scale (routed to hdi_exp_dev: generation build-time-local + monotone measurement + ARBITRARY/SHUFFLE must-fails + equal-vs-learned contrast + multi-seed). This is the honest beats-frequency test with encoding no longer the confound.
- Ratings (no-smoke): lever-confirmation GOOD; data-limited-diagnosis GOOD; certification NOT done (single 191-cluster, directional, hand-rolled learned-weight fit) — directional, not tiered.

**✅ DISCOVERY HARD-PASS VET'd = PROVEN (skunkworks a42c62e6, 2026-07-14b): TIER proven (chain-grade) for the SCOPED claim; verdict SURVIVES, no false-pass.** Independent off-disk recompute exact (F1_CYCLIC adv 0.4274, F2_XOR adv 0.4119, F4_ARB lift -0.0017, CV tight); self-test re-run reproduces bit-for-bit (30.8s REAL gradient training, not scaffold). Adversarial checks clean: (Q1) MEMORIZE baseline fair (over-parameterized 37k vs 8k params, fits seen=1.0 novel~0) -> advantage is ARCHITECTURAL; arbitrary must-fail genuine (DISC fits F4 SEEN 0.876 but novel at chance = fails to GENERALIZE not to learn). (Q2) NO leak: no homomorphism regularizer (unlike the confounded prior toy), only CE on 1229 disjoint train pairs (0 leaked); F4 control proves shared-code does NOT induce generalization independent of the rule = real discovery. (Q3) honest bounds CORRECT as pre-registered: discovers ONLY commutative/abelian structure matched to the bind's algebra; **F3_ASYM is an ARCHITECTURAL WALL -- readout Re<E[a](*)E[b],E[r]> is SYMMETRIC under a<->b so it CANNOT represent asymmetric relations (hand-designed FPE also only reaches chance on F3 = real algebraic wall, not optimization);** ~0.41 abs = ~41% of FPE-reachable 1.0 ceiling (limited coverage); cannot discover asymmetric/arbitrary. HONEST-BOUNDS SENTENCE (always carry it): "shared-code + compositional-similarity-readout, plain CE no hand-designed algebra, GENUINELY DISCOVERS 2 abelian families (cyclic Z_L, XOR) from data, generalizes to novel pairs ~0.41 top-1 (27x chance), noise-robust -- but STRICTLY BOUNDED to commutative structure matched to the bind's own algebra = proven limited-coverage abelian-discovery, NOT general compositional discovery."
- **KEY ENRICHMENT IMPLICATION (feeds the additive_map THEN step):** the commutative bind's a<->b symmetry is THE wall for asymmetric/1-to-N/inverse relations. Enriching the composition op = BREAK commutativity (ordering-sensitive / non-commutative bind, e.g. RotatE-with-order or permutation-binding) -- that's the concrete lever for the deficit, not just "richer coords."
- **A5 ATOMIZATION = DIRECTOR-DECISION HELD (user deferred to director 2026-07-14b; NOT churn now).** Skunkworks recommends atomizing to `math` corpus as MEASURED_MECHANISM/proven-bound ("shared-code + commutative-bind readout discovers abelian compositional structure from data ~0.41 novel, 2 families, noise-robust; bounded to commutative-algebra-matched; cannot do asymmetric/arbitrary"), superseding the prior conjunction construction-proof atom's "must-hand-design" limitation. **DECISION: HOLD -> atomize ONCE AFTER the non-commutative-bind enrichment step** (capture discovery + whether breaking commutativity extends it = one clean write, avoid write-then-supersede churn on canonical store; result won't change so nothing lost by waiting). TRIGGER TO REVISIT = when the non-commutative/ordering-sensitive bind cell lands (extends coverage OR re-bounds it). Still needs direct user-auth at execution time.

### ========== END 2026-07-14b ==========

### ========== 🧭 2026-07-14 END-OF-SESSION — CONSOLIDATED CURRENT STATE (READ FIRST; SUPERSEDES every block below, which is the step-by-step arc/history) ==========

**🏗️ THE PROGRAM (USER-authorized pivot, unchanged):** build the ideal KNOWLEDGE FOUNDATION (external tools OK for building) + the REASONING MECHANISM that cashes it in. Runtime stays glass-box (NO external LLM at inference). Brain = best-in-class reference. [[project_PIVOT_build_ideal_knowledge_foundation_from_existing_tools_USER_AUTHORIZED_2026-07-14]]

**🎯 THE SESSION'S RESOLVED PICTURE (correct final state; all directional/toy unless noted VET'd):**
1. **Single-relation prediction is a DEAD END** — homophily-solvable; the decisive cell on real CSKG REFUTED it cleanly (v3: structured codes 0.153 < homophily 0.174; controls clean). The prize is **CONJUNCTIONS / composition**, not single-hop. (reasoning theory: resolution scales with # constraints brought to bear.)
2. **The reasoning architecture = SHARED-code + COMPOSITIONAL-readout that DISCOVERS structure from data + native BIND that COMPOSES.** Key synthesis: **this IS our existing additive_map** (hdlab/additive_map.py: shared entity coords X + readout X_h+D_r~X_t + learned SGD) — VET-confirmed real-data MRR 0.128 (held-out-entity, inductive). => the reasoning thread is NOT a from-scratch build; it's IMPROVE THE EXISTING VALIDATED ARCHITECTURE. An inline test showed the shared-code architecture GENUINELY discovers compositional structure (additive novel 0.789 vs memorize-baseline 0.11 vs arbitrary-control 0.032=chance). = the brain's learn-don't-hand-design move (TEM/successor). **CONFIRMED at multi-seed scale on the REAL bind path (exp_dev cell structure_discovery_shared_code_readout_v1, ran REMOTE run_mode=full 3 seeds 57s, verdict ARCHITECTURE_DISCOVERS_COMPOSITIONAL_STRUCTURE_ACROSS_ABELIAN_FAMILIES): F1_CYCLIC DISC 0.428 vs MEMO 0.0001, F2_XOR DISC 0.412 vs 0.0003 (discovers a 2nd abelian family), F4_ARBITRARY must-fail fires 0.014 (no leak), F3_ASYM bounded 0.032 (bind COMMUTATIVITY limit not learning), F1_NOISY 0.344 robust; all 4 HP gates True. HONEST BOUNDS (worth skunkworks VET next session): discovers COMMUTATIVE structure matched to the bind's algebra; absolute ~0.41 = limited-coverage generalization gap not a wall (FPE ceiling 1.0 reachable); CANNOT discover asymmetric/arbitrary composition. metrics: data/exp_structure_discovery_shared_code_readout_v1/metrics.json.**
3. **Native bind is a CONSTRUCTION_PROOF, not a capability win (skunkworks VET downgrade):** it solves conjunctions that MATCH its algebra (modular/additive) — CLEAN 1.0 is tautological; the ARBITRARY control (non-additive) collapses it to chance = the real-world case. So bind is the right COMPOSE op once the algebra is known; the open problem = get the RIGHT code for real content.
4. **🔻 FOUNDATION FIRST-BATCH REFUTE (the key real-data finding, exp_dev a49a516a7, 3 seeds): the ENCODING is the weak link.** Generation WORKS (backend/llm/anthropic_client.py live, truth 0.90; NOTE: no "93.75% pilot" artifact exists — MEMORY overclaim). Real conjunction structure CAN be generated (joint MI 1.88 > single 1.11). BUT native bind does NOT read it out (NATIVE_BIND novel 0.159 < chance vs FREQ_NULL 0.511, dissociation -0.35) because real ordinal attributes (metabolic rate, size) are **MONOTONIC, not modular** — modular codes wrap around and scramble them. **THE LEVER: match the CODE to the DATA's structure — MONOTONIC/thermometer codes for magnitudes (= the brain's magnitude-coding scheme: monotonic number-line/ANS for magnitude, cyclic grid only for space), NOT modular.** Also: generated conjunctions were only WEAKLY conjunctive (partly single-relation) -> generate MORE strongly-conjunctive structure. DO NOT scale the foundation on modular bind.
5. **🧠 CONSOLIDATION (Claim B) = CONFIRMED (the maintenance half):** SGD prediction-task loop manufactures inference-capable structure — all 4 CLS/Saxe signatures (dose-response, structure-before-content staging, interleaving-beats-continual, shuffle-flat). = hippocampal replay/CLS.

**🧠 BRAIN-ALIGNMENT (reinforced by the deflations, not drifted):** learn-don't-hand-design (discovery architecture = TEM/successor); match-code-to-dimension (monotonic magnitude / cyclic space = the brain's own scheme — the foundation refute REDISCOVERED it); consolidation manufactures structure (Claim B = CLS). Every honest-negative this session pushed us ONTO a brain mechanism, not away.

**🚦 NEXT-SESSION PRIORITIES (ranked):**
1. **MONOTONIC/thermometer constructable-code cell** (the foundation refute's direct pointer): does a monotonic code read out real generated ordinal conjunctions where modular failed? + generate explicitly-additively-factorizable / more-strongly-conjunctive structure.
2. **Collect the discovery-cell verdict** (exp_dev ad844e77, may/may not have shipped at pack-up): does the shared-code architecture discover across MULTIPLE composition families (informs richer composition op).
3. Enrich the additive_map's composition op beyond additive/TransE (richer = handles symmetric/1-to-N/inverse relations) — HONEST CAVEAT: prior multiplicative KGStore couldn't match additive 0.128, so not guaranteed.
4. Then integrate consolidation (P1) + grounding (both still to build).

**📦 IN-FLIGHT AT PACK-UP (2026-07-14 end):** foundation batch DONE (refute, committed a49a516a7 local — push optional); conjunction cell DONE (construction-proof, VET'd, MEMSMOKE+FULL landed); discovery cell (ad844e77) still authoring — if shipped runs remote overnight, else a fast re-run. Nothing critical local-bound. Skunkworks offered to atomize conjunction as CONSTRUCTION_PROOF to Store (A5-gated) — HELD pending user-auth for store write.

**🔑 KEY POINTERS:** notes/foundation_build_first_batch_design_2026-07-14.md; notes/consolidation_to_structure_implementable_algorithm_2026-07-14.md; notes/research_grid_code_algebraic_binding_generalization_2026-07-14.md (code-construction = spectral/successor per relation); hdlab/additive_map.py (THE validated architecture to improve); scratchpad/{conjunction_discriminates,homomorphism_reg,structure_vs_homophily,cskg_conjunction_by_bucket}.py; data/exp_generated_conjunction_native_bind_animals_v1/metrics.json (the refute); data/foundation_clusters/animals_ordinal_conjunction_v1.json (first generated cluster).
### ========== END CONSOLIDATED CURRENT STATE ==========

### ========== 🧭 2026-07-14 LATE — DETAILED SESSION ARC (history; the CONSOLIDATED block above supersedes — read below only for step-by-step detail) ==========

**🏗️ THE PROGRAM (USER-authorized pivot 07-14): BUILD THE IDEAL KNOWLEDGE FOUNDATION (from existing external tools) + BUILD THE REASONING MECHANISM THAT CASHES IT IN.** Two coupled halves, not competitors.
- **FOUNDATION** = a dense, vetted, module-extensible knowledge base built by MERGING existing KBs + LLM-GENERATION (full+vetted). REVERSES the old "no-ingest/bootstrap-it-yourself" lock (that lock now governs only the RUNTIME core: runtime reasoning stays glass-box, NO external LLM at inference). [[project_PIVOT_build_ideal_knowledge_foundation_from_existing_tools_USER_AUTHORIZED_2026-07-14]]
- **REASONING MECHANISM** = the session's load-bearing finding (below).

**🧠 THE REASONING THEORY (this session's core result; [[project_reasoning_theory_constraints_brought_to_bear_dense_plus_constraint_satisfaction_2026-07-14]]):** reasoning resolution scales with the **NUMBER OF CONSTRAINTS BROUGHT TO BEAR**. VSA toy: acc vs #constraints-queried = 0.05/0.15/0.40/0.75/0.93/0.97/**0.99** (K=1..7). DENSITY = capacity for constraints; QUERY-WIDTH = use of them; SAME lever. SINGLE-HOP is modest because it's 1 constraint (underdetermined; ~96% one-to-many unwinnable). ESCAPE = **ADDITIVE constraint-satisfaction** (brain's attractor/CA3 pattern-completion) — works in VSA (0.99) where joint TENSOR-BINDING fails (0.018). Multi-hop CHAINS are NOT the escape (CG on synthetic only; 6 real-knowledge HARD-FAILs; chains bring few constraints; the prize is CONJUNCTIONS). Phase-diagram dial = query-width (storage/dim ruled out). **🚨 REAL-KNOWLEDGE RESULT (agent a6bbdfd0 LANDED — CORRECTS the theory, retrieval-vs-inference distinction): constraint-satisfaction REPRODUCES for PATTERN-COMPLETION/RETRIEVAL (identify a stored concept from K constraints: 0.62→0.99 real, saturates K=3, controls at chance) BUT is FLAT AT CHANCE for genuine ZERO-SHOT held-out-relation INFERENCE. CRUX: random VSA codes carry NO SHARED STRUCTURE to triangulate an UNSEEN answer; genuine INFERENCE of new facts needs STRUCTURED/CORRELATED codes (learned/grounded — e.g. additive-map SGD codes), NOT random codes. So constraint-satisfaction escapes underdetermination for RETRIEVAL; inferring NEW facts is the harder prize requiring structured codes. This reconnects to correlation-HELPS-generalization vs correlation-HURTS-capacity tension + why the additive-map used LEARNED codes. NEXT-EXPERIMENT REFRAME: test constraint-satisfaction over STRUCTURED/grounded codes (does shared code-structure enable zero-shot inference), not random codes.**

**🔧 REFRAME EXECUTED (2026-07-14 LATE, inline prototype scratchpad/structured_codes_inference.py; controls clean [shuffle-collapse + info-ceiling-confirmed + unbind-baseline]; toy/local/single-world -> DIRECTIONAL not certified): a6bbdfd0's "flat at chance for inference" was PRIMARILY THE READOUT, not a hard limit of random codes.** Zero-shot held-out-relation inference over a latent-correlated world: UNBIND-from-self (a6bbdfd0's op) = 0.23 BELOW marginal 0.37 (degenerate: cue has 0 info on held-out rel). NEIGHBOR-RETRIEVAL (CA3 pattern-completion) BEATS marginal: random codes +0.102, structured +0.134; SHUFFLE collapses both to ~0. => (1) constraint-satisfaction DOES infer new facts with the RIGHT readout; a6bbdfd0 used the wrong one. (2) even random codes infer (exact-match similarity preserved in discrete world). (3) structured codes add only +0.025 HERE -- they pay off where exact-match fails (graded/compositional/continuous/cross-domain = real grounding). CORRECTED: inference prize is reachable by constraint-satisfaction IF (a) pattern-completion readout not self-unbind, (b) structured codes to generalize beyond exact-match. Brain does exactly this (semantic-neighbor geometry + CA3 + grid/TEM factorization + replay/consolidation manufactures the structure). Full detail in the reasoning-theory memory REFINEMENT line.

**🚨 REAL-DATA VERDICT (2026-07-14 LATE, the decisive cell LANDED on real reduced-CSKG memsmoke, seed7 -- canon!=preview VINDICATED, HONEST DEFLATION):** the synthetic self-test looked great (STRUCT_NEIGHBOR 0.523 >> RANDOM_NEIGHBOR 0.241) but REAL reduced-CSKG DEFLATES it: **SN=0.1533 ~= RN=0.1518 -- structured codes barely beat RANDOM in AGGREGATE** (the strong claim does NOT hold aggregate on real data). The SHUFFLE positive-control LEAKED (SHUF-POP=+0.048 of the SN-POP=+0.062 gain = 77% retained; needed collapse<=0.010) -> most of the neighbor-retrieval signal is NON-RELATIONAL (value-set homophily / degree that survives relation-shuffling = a frequency-like confound). Verdict INCONCLUSIVE_CONTROL_OR_LEAK_FAILED -> **FULL correctly HELD.** The ONLY clean structure edge is on the NOVEL-graded stratum (SN 0.098 vs RN 0.050, rel_gain 0.97, sign_frac 1.0) -- exactly the narrow win the graded-world drill predicted -- BUT the shuffle leak means we can't yet cleanly attribute even THAT to relational vs homophily structure. **DIAGNOSIS (hypothesis, exp_dev verifying vs code): the shuffle only permutes relation labels but preserves each concept's VALUE SET, so value-homophily leaks; real CSKG has strong non-relational degree/homophily structure the current control doesn't break.** IN FLIGHT: exp_dev (aad530ff) fixing the shuffle to a proper must-fail (break concept<->value homophily) + ADDING a frequency/homophily baseline so the real question -- does STRUCTURE beat FREQUENCY not just POP -- is testable, esp on novel-graded; then re-run MEMSMOKE; ship FULL only if structure beats the homophily baseline WITH the shuffle now collapsing, else report the honest deflation (structure doesn't beat frequency on real reduced-CSKG). **This does NOT refute the brain mechanism (Claim B / consolidation manufactures structure = CONFIRMED below); it says our CURRENT additive-map structured codes don't yet beat frequency/homophily on real data -- which is precisely what the FOUNDATION build (better structured codes) + the CONSOLIDATION loop (P1) aim to fix. Motivates the build, doesn't refute the direction.** MEMSMOKE metrics: data/exp_relational_inference_neighbor_vs_unbind_structured_cskg_memsmoke_v2/metrics.json. Caveat: reduced core (2869 nodes/20 rels, single seed, n_dim512/epochs200) -- FULL (full core, n_dim1024/k24/epochs500/3seeds) may separate structure from frequency differently; but ship FULL only AFTER the control is clean.

**🧠 CLAIM B (consolidation MANUFACTURES inference-capable structure) = CONFIRMED (2026-07-14 LATE, inline SGD prototypes scratchpad/p1_sgd_manufactures_structure.py + p1b_continual_vs_interleaved.py; glass-box, directional-not-certified):** the faithful CLS/Saxe mechanism (learn value codes by SGD on a masked-attribute PREDICTION task = word2vec over each concept's attributes) manufactures inference structure with ALL FOUR predicted signatures: (1) DOSE-RESPONSE (held-out inference lift grows with SGD passes ->+0.10 exact/+2.25 graded, saturates = Saxe log); (2) STRUCTURE-BEFORE-CONTENT staging (coarse_acc 0.87 >> fine_acc 0.33 -- superordinate learned, subordinate lags); (3) INTERLEAVING-beats-CONTINUAL (true continual learning catastrophically FORGOT early kinds -0.047 below marginal while interleaved retained +0.058; overall interleaved +0.083 vs continual +0.005 = the CLS interleaving claim); (4) SHUFFLE flat (clean control). Earlier co-occurrence-DIFFUSION toy was a NULL (wrong mechanism/regime); the faithful SGD mechanism is unambiguous. => the reasoning-mechanism build target (P1 = interleaved-replay refit of the additive-map, + Oja/Sanger glass-box variant) is de-risked by the RIGHT mechanism. Full algorithm: notes/consolidation_to_structure_implementable_algorithm_2026-07-14.md.

**🎯 DEEP DIAGNOSIS of the real-data deflation (2026-07-14 LATE, inline scratchpad/structure_vs_homophily.py; DIRECTIONAL toy but ROBUSTLY reproduces the real SN~=RN pattern -> the reframe is load-bearing): the single held-out-relation task is HOMOPHILY-SOLVABLE, so NO code (structured OR random) beats a value-set-nearest-neighbor baseline.** Built a world with a relational signal (kind->value) + a tunable homophily/degree confound (pop shared across relations, mimics real CSKG). Result across ALL confound levels incl pop_strength=0 (PURE relational): STRUCT ~= RAND ~= HOMO (e.g. 0.460/0.461/0.485 at ps=0; STRUCT-HOMO ~0 to -0.026 always), ALL beating POP by ~+0.19. So: (a) reproduces real MEMSMOKE SN~=RN (structured codes = random codes for neighbor-retrieval on this task); (b) neither beats the HOMOPHILY baseline -- even a value-overlap nearest-neighbor predictor ties/beats the learned codes -- because SHARING VALUES ~= SAME KIND ~= SHARES the held-out value, so nearest-neighbor-by-value ALREADY captures the relational signal; structured codes add nothing ON TOP. **CRUX / THE REFRAME: neighbor-retrieval (the CA3 readout) is fundamentally a HOMOPHILY predictor; single-relation held-out prediction cannot separate relational structure from value-homophily/frequency. To show structure's value (and justify the build's beats-frequency thesis), the TASK must REQUIRE what homophily CANNOT do = MULTI-CONSTRAINT CONJUNCTIONS / composition (the reasoning-theory prize [[project_reasoning_theory_constraints_brought_to_bear_dense_plus_constraint_satisfaction_2026-07-14]]), NOT single held-out-relation prediction.** This UNIFIES: reasoning theory (conjunctions are the prize) + real-data deflation (single-relation is homophily-solvable, SN~=RN, shuffle leaks) + the whole beats-frequency program. => DECISIVE-EXPERIMENT REFRAME (director-owned): after exp_dev lands the clean homophily-baseline control (which will confirm STRUCT~=HOMO on the single-relation task), redesign the task toward a CONJUNCTION query where the answer needs composing multiple constraints (homophily-unsolvable) -- THAT is where structured/manufactured codes must beat frequency, or the thesis fails. Caveats: one toy; SGD codes possibly improvable; real homophily is degree-based. But STRUCT~=RAND~=HOMO robustly reproduces the real pattern with a clean mechanistic reason.

**✅ REFRAME VALIDATED (2026-07-14 LATE, inline scratchpad/conjunction_discriminates.py, 4 seeds, 40% novel (A,B) pairs): a CONJUNCTION/INTERACTION target DISCRIMINATES structure from homophily where single-relation did NOT.** Compared a kind-determined single-relation target vs an interaction target R=(A+B) mod L, arms MARGINAL/HOMOPHILY/EXACT_PAIR(memorization)/BIND(FHRR phase-binding compositional). Result: SINGLE-relation -> HOMOPHILY 0.593 >> marg 0.139 (homophily wins, reproduces deflation). INTERACTION -> HOMOPHILY 0.069 ~= marg 0.037 (COLLAPSES to chance -- additive value-overlap cannot capture the conjunction); EXACT_PAIR 0.608 = memorizes SEEN pairs but FAILS the 40% novel (memorization != generalization); BIND(FHRR) = **1.000, generalizes to ALL novel pairs** (pure algebra: code(v)=g^v, bound=g^(A+B), decode argmax_r<g^r,bound>). => (1) conjunction tasks are where structure BEATS frequency AND memorization AND generalizes; (2) the winning mechanism is BINDING/COMPOSITION = the substrate's NATIVE op (FHRR/HRR bind); (3) single-relation prediction was provably the wrong test. **HONEST CAVEAT: (A+B) mod L is a CLEAN algebraic interaction tailor-made for phase-binding (BIND=1.000 is best-case); REAL KG conjunctions are not clean modular arithmetic -- the BUILD challenge is finding/learning the right binding structure for real (non-clean) conjunction queries. But the PRINCIPLE is validated: conjunctions discriminate; composition/binding is the mechanism.** => REDESIGNED DECISIVE EXPERIMENT (director-owned, next): a real-KG CONJUNCTION query where the answer needs composing >=2 constraints, arms = homophily/memorization baselines vs the substrate's bind-based compositional readout (+ manufactured/structured codes from P1), on NOVEL constraint-combinations. This is the honest beats-frequency test + directly exercises the substrate's native binding + connects to the reasoning-theory constraint-satisfaction curve (0.05->0.99 vs #constraints).

**🎯 FOUNDATION TARGET SHARPENED (2026-07-14 LATE, inline scratchpad/cskg_conjunction_feasibility.py + cskg_conjunction_by_bucket.py, 500-600k edge slice): the current CSKG has ~ZERO conjunction structure -- even within the SEMANTIC buckets reasoning needs.** (1) Composition: CSKG is 76% LEXICAL (455k/600k edges = synonym/relatedto/definedas); semantic buckets threadbare (func 11k, caus 4.9k, part 4.7k, prop 961, soc 111). (2) Conjunction narrowing per held-out SEMANTIC relation (median candidate-set @1 constraint vs @3): prop 1.0->1.0, func 2.0->2.0, caus 2.0->2.0, part 1.0->1.0 = +0% narrowing (spat weak +14%). One constraint already resolves each semantic relation to 1-2 candidates; conjunctions add nothing. => the foundation build must GENERATE conjunction structure WHOLESALE (entities with rich INTERACTING multi-relation profiles where a held-out value depends on a COMBINATION), NOT densify existing buckets (too sparse + single-constraint-deterministic to become conjunctive). Confirms the pivot's generation-not-merge framing + adds the specific target: multi-relation INTERACTION structure. Caveat: transductive candidate-set measure, directional; but aggregate + per-bucket + 76%-lexical converge robustly.

**🧠 BRAIN-COMPONENT INVENTORY vs BEST-IN-CLASS (2026-07-14 LATE, director synthesis for USER):** recreating the brain's inference architecture; per-component status + distance from best-in-class (the brain) + weak-link localization:
- (1) content-agnostic BINDING (CDZ) -> VSA FHRR/HRR bind = AT best-in-class (native, algebraic, 1.0 on clean conjunctions). STRONG.
- (2) anti-smearing / pattern-separation (dentate) -> sharded store + correlation-hurts-capacity = AT best-in-class, brain-confirmed. STRONG.
- (3) pattern-completion (CA3) -> neighbor-retrieval readout = mechanically fine, but IS fundamentally a homophily predictor on single-relation (why v3 refuted). ADEQUATE mechanism, wrong task exposed it.
- (4) replay/consolidation (CLS) -> P1 SGD prediction-task loop = CONFIRMED (Claim B, 4 signatures) BUT not integrated into substrate + learned-from-scratch codes WEAK (0.11 vs algebraic FHRR 1.0). MEDIUM, sub-optimal.
- (5) structure-content factorization (grid/TEM) -> additive relational map (shared relation operators) = PARTIAL; the g-sees-only-structure BOTTLENECK not enforced/tested. MEDIUM, untested.
- (6) structured semantic geometry (ATL hub) -> additive-map learned codes = WEAK (tie random/homophily on real data, v3 refuted). FAR.
- (7) GROUNDING (hub-spoke, Quinian) -> ZERO (pure symbol graph). FARTHEST.
- (8) multi-constraint conjunctions -> constraint-satisfaction validated on synthetic (0.05->0.99) + conjunction reframe; DATA absent (CSKG ~no conjunctions). Mechanism OK, DATA gap binding.
**WEAK LINKS RANKED (where we're far from best-in-class):** #1 FOUNDATION/DATA (no conjunctions, no grounding, 76% lexical) = binding constraint; #2 STRUCTURED CODES that carry inference (native-algebra-applied-to-real-conjunctions -- the learned 0.11 vs algebraic 1.0 gap = THE sub-optimal mechanism: we have bind, lack the right codes to bind); #3 TEM structure-content bottleneck (untested). STRONG/at-frontier: bind op, anti-smearing. Confirmed-but-unintegrated: consolidation. DRILL FIRED: how the brain gets ALGEBRAIC/compositional codes for ARBITRARY (non-spatial/non-modular) relations -> close the 0.11->1.0 gap.

**🔬 CODE-CONSTRUCTION DRILL LANDED + DECISIVE TEST RUN (2026-07-14 LATE): weak-link #2 PRECISELY LOCALIZED.** Drill (notes/research_grid_code_algebraic_binding_generalization_2026-07-14.md): the 0.11->1.0 gap is a MISSING CODE-CONSTRUCTION STAGE, not the bind operator; brain re-derives a SPECTRAL/successor-representation code per relation-class (Stachenfeld 2017 SR = TD-learned Laplacian eigenbasis) then binds with fixed FHRR op. Candidate #1: SR/Laplacian codes -> unchanged FHRR bind, HARD-PASS>=0.70. **I RAN the decisive test (scratchpad/sr_spectral_code_bind.py + localization): candidate #1 as naively implemented HARD-FAILED (SPEC_EMP_PHASE 0.067 ~ chance << 0.20), BUT localized cleanly: the spectral->phase->bind PIPELINE WORKS (CYCLIC_PHASE, graph that ENCODES the group = 1.000); the failure is STRUCTURE/GRAPH DISCOVERY -- my empirical marginal a->r transition graph is UNINFORMATIVE (uniform for symmetric ops like +, averaging over b washes out the group). SGD_FREE=0.351, HAND_FHRR=1.000.** => REFINED weak-link #2: (a) given KNOWN/constructed group structure -> spectral/FPE codes + bind = 1.000 SOLVED (this is exactly what the shipped conjunction-mechanism cell uses: FPE = known cyclic group); (b) DISCOVERING the compositional structure (the right graph) from data for ARBITRARY relations = the OPEN sub-problem (naive transition-graph fails; needs joint-(a,b)->r structure or the drill's candidate #3 homomorphism-regularization auxiliary loss, untested). So the reasoning-mechanism is SOLVED where structure is known/constructable, OPEN where it must be discovered. This is a precise, honest localization -- not a dead end; it says the foundation should SUPPLY constructable structure (FPE/known-group codes) rather than rely on discovering it. Inline-toy churn on discovery has diminishing returns; the priority signal is the conjunction-mechanism real-CSKG verdict.

**✅✅ CONJUNCTION-MECHANISM HARD-PASS ON REAL CSKG (2026-07-14 LATE, MEMSMOKE landed; FULL dispatched; raw-then-VET, HOLD full framing until FULL+VET): the mechanism half is CONFIRMED -- native-bind composition BEATS frequency on conjunctions at real scale.** Cell exp_conjunction_native_bind_vs_homophily_cskg_v1 (real CSKG vocabulary, PLANTED compositional conjunctions, FPE/known-group codes = the CONSTRUCTABLE-structure case, real hdlab.binding.bind): verdict NATIVE_BIND_COMPOSITION_BEATS_FREQUENCY_ON_CONJUNCTIONS. CLEAN NBIND=1.000 vs FREQ_NULL=0.013 diss=0.987; NOISY NBIND=0.643 vs 0.020 diss=0.623; ARBITRARY must-fail FIRES (gap 0.007); SHUFFLE must-fail FIRES (gap -0.013); F1(tie)=False; ceiling_ok. => THE LOGICAL CHAIN IS CLOSED: (1) prize=conjunctions (single-relation refuted), (2) current data lacks conjunctions (feasibility audit), (3) MECHANISM (native-bind + constructable codes) beats frequency on conjunctions at real scale [THIS], => (4) FOUNDATION BUILD is DE-RISKED + GREENLIT: supply conjunction structure with CONSTRUCTABLE (FPE/known-group) codes -- exactly the weak-link-#2 SOLVED case, not the open discovery case. CAVEATS (raw-then-VET): MEMSMOKE not FULL (FULL pending); PLANTED (semi-synthetic) conjunctions prove the MECHANISM if structure is supplied, NOT that real-world conjunctions are present/learnable (the foundation build's job); FPE = known-group (constructable) codes. NEXT: collect+VET conjunction FULL; then LAUNCH foundation build (generate conjunction-bearing structure + constructable codes for real concept clusters, incrementally measure-as-you-go).

**🔻 CONJUNCTION VET DOWNGRADE (skunkworks acf2de82, 2026-07-14 LATE): the conjunction HARD-PASS = CONSTRUCTION_PROOF, NOT a capability win.** CLEAN NBIND=1.0 is a TAUTOLOGY (planted label = exactly what FHRR bind computes on FPE codes; algebraic extrapolation by construction, NO training). Genuinely shown (weaker): (a) bind provably solves additive-Z_L conjunctions incl novel, noise-robust; (b) real-vocab freq null stays at chance at scale (real neg-control, but target is by-construction single-constraint-unsolvable); (c) must-fails fire clean (not saturation-vacuous). THE ARBITRARY control (compositional but NON-additive) -> NBIND at CHANCE = the real-world case: bind solves conjunctions THAT MATCH ITS ALGEBRA, not conjunctions generally. FOUNDATION: LICENSES a scaffold built with a KNOWN constructable algebra (bind solves free); does NOT license assuming real content is natively modular-additive. => open problem = DISCOVER the algebra for real content OR coordinatize content into a constructable algebra.

**🚀 DISCOVERY IS TRACTABLE -- the ARCHITECTURE is the lever (2026-07-14 LATE, inline scratchpad/homomorphism_reg.py + arbitrary-control VET; DIRECTIONAL toy, confirming cell routed to exp_dev ad844e77): a SHARED-code-space + COMPOSITIONAL-similarity-readout architecture DISCOVERS compositional structure from data, no hand-designed algebra, no regularizer.** Put A,B,answer in ONE embedding; readout logits[r]=Re<bind(E[a],E[b]), E[r]>; plain CE. Result (novel-(A,B)-pairs only, L=24): ADDITIVE compositional = 0.789 (>> chance 0.042, vs SEPARATE-tables memorize baseline 0.11); ARBITRARY table (must-fail) = 0.032 = chance. => GENUINE discovery (generalizes iff real compositional structure exists; fails on arbitrary = not memorization/leak). This is the brain's own move (learn relational structure via factorized architecture -- TEM/successor -- NOT hand-design it). ADDRESSES the open problem the VET flagged. CAVEATS: one toy, additive-only, 0.789 not 1.0, my code (arbitrary-control guards the obvious leaks). exp_dev cell ad844e77 confirms/refutes across MULTIPLE structure families + scale (HARD-PASS = discovery >> memorize on >=2 non-arbitrary families + arbitrary at chance). IF it holds: the foundation can LEARN structure from generated/real content (needn't hand-design algebra) = turns construction-proof into real capability. NEW STRATEGIC PICTURE: reasoning = shared-code compositional-readout architecture that DISCOVERS + native bind that COMPOSES + foundation that SUPPLIES compositional content + consolidation (P1) that MANUFACTURES/maintains. All four now have positive (toy/directional) evidence.

**🔗 THE DISCOVERY ARCHITECTURE IS ALREADY OUR ADDITIVE_MAP -- discovery is REAL-DATA-VALIDATED, not just a toy (2026-07-14 LATE, director audit of hdlab/additive_map.py, confirming skunkworks' cross-thread hypothesis).** AdditiveKGMap = shared entity code space X[N,k] + compositional readout score(t)=-||X_h + D_r - X_t|| (compose head+relation, read out by distance to answer code) + learned-SGD coords. This IS the shared-code + compositional-similarity-readout architecture my discovery toy used -- in its ADDITIVE (TransE) form. => the discovery finding is ALREADY a VALIDATED REAL-DATA capability: additive_map's held-out-entity MRR 0.128 (inductive, chain-grade, VET-confirmed) IS this architecture DISCOVERING additive relational structure on real CSKG. De-risks the discovery finding far beyond the L=24 toy. **THE 0.128 CEILING EXPLAINED:** additive_map composes by ADDITION ONLY (TransE h+r=t) -> captures only TRANSLATION-type relations; real relations (symmetric/1-to-N/inverse/hierarchical) that addition can't represent are the deficit (known TransE limitation). => IMPROVEMENT LEVER: the discovery architecture with a RICHER composition op = the substrate's NATIVE FHRR COMPLEX bind (rotation, RotatE-style -- handles symmetry/inversion/composition addition can't; my discovery toy used exactly this complex bind). HONEST CAVEAT: the substrate's prior MULTIPLICATIVE KGStore reportedly couldn't match additive 0.128 -> "richer composition beats additive on real CSKG" is NOT guaranteed; may hinge on LEARNED-optimized complex coords (RotatE) vs the prior store's approach. OPEN -- the exp_dev discovery cell (ad844e77, sweeps composition families) informs it. REFRAME: the reasoning thread is not a from-scratch build -- it's IMPROVE THE EXISTING VALIDATED ARCHITECTURE (additive_map) by enriching its composition op + supplying compositional foundation content, not inventing a new mechanism.

**🔻🧪 FOUNDATION FIRST-BATCH = HONEST REFUTE (exp_dev a49a516a7, 2026-07-14 LATE, ran INLINE to completion -- generation is build-time-local, measurement seconds; verdict GENERATED_CONJUNCTION_PREMISE_REFUTED_OR_NOT_ADDITIVE, 3 seeds): the ENCODING is the weak link, not generation.** Generation infra EXISTS + WORKS (backend/llm/anthropic_client.py, live keys, claude-haiku health ok; NOTE corrected a MEMORY overclaim -- no "93.75% pilot" artifact on disk, that number was prose only; hdlab/generation.py is the WRONG substrate-native generator). Generated ONE real cluster (191 animals, 4 ordinal constituents + held-out metabolic_rate, NO rule revealed to generator), adversarial-vet TRUTH=0.90. RESULT: conjunction structure IS present+vetted (joint MI 1.88 > best-single 1.11, margin 0.77) BUT native FHRR bind does NOT read it out -- NATIVE_BIND novel 0.159 (BELOW chance 0.20) vs FREQ_NULL 0.511 = dissociation -0.35 (needed +0.15). WHY: real generated conjunctions are (a) partly SINGLE-RELATION-loaded (activity alone MI 1.11 of 1.88 -> freq null solves 0.51), (b) MONOTONIC not MODULAR -- metabolic rate is ordinal/monotonic but the cell's modular (mod-L) FPE codes WRAP AROUND and scramble monotonic biology -> below chance. => planted(modular)->generated(monotonic) transfer REFUTED. **THE LEVER (confirmed on real data): match the CODE to the DATA's structure -- MONOTONIC/thermometer (non-cyclic) codes for ordinal magnitudes, NOT modular.** Also: generated conjunctions were only WEAKLY conjunctive (partly single-relation) -> need to generate MORE strongly-conjunctive structure. DO NOT scale the foundation build on native additive/modular bind (it mismatches real ordinal content). NEXT-SESSION #1 PRIORITY (the direct pointer): MONOTONIC/thermometer constructable-code cell -- does a monotonic code read out real generated ordinal conjunctions where modular failed? (+ generate explicitly-additively-factorizable structure). Cell+metrics committed a49a516a7 (local; push optional). This is the CONVERGENCE of the whole thread: mechanism works for algebra-MATCHED structure (construction-proof); real content has DIFFERENT structure (monotonic, weakly-conjunctive); the fix = match encoding to real structure + strengthen generated conjunctivity. NOT a wall -- a precise, measured next step.

**FOUNDATION STATE (measured this session):**
- Current substrate = LEXICAL/TAXONOMIC SKELETON: 93.4% of 141k concepts ≤1 semantic bucket; only 0.44% ≥4. Full on-disk CSKG (6M edges) re-ingest → only 5.7% (free warm-start, INSUFFICIENT; 4M/6M is lexical). => build = a **GENERATION problem** (LLM-gen the missing property/functional/causal), not just merge. Domain gaps: science 0.6%, math 1.1%, folk-psych 0.8%.
- GENERATION PILOT PASSED (fair/representative, INDEPENDENTLY verified): densifies 1→~3-4 buckets, TRUTH 93.75% (adversarial judge; errors=schema/polysemy/nuance NOT fabrication), 94% connectivity, 100% neg-control refusal. LLM-generation VIABLE+VETTABLE. Vetting spec: schema-coherence + sense-disambiguation + cross-model at scale.
- DENSITY DE-RISK: single-hop density MEANINGFUL (+0.147, 3x, on the 83% of cases taxonomy fails) but MODEST single-hop ceiling (~0.25). The REAL payoff is MULTI-CONSTRAINT (the reasoning theory).
- IDEAL SPEC (notes/research_ideal_foundation_spec_size_density_optimum_2026-07-14.md): dense CORE (Wikidata+ConceptNet+ATOMIC+WordNet+Wiktionary+YAGO4+BabelNet) + open MODULE registry (PubChem/periodic/GeoNames/UMLS). Type-schemas differ by relation COMPOSITION (which relations per type) NOT dramatically by density-amount (fair test: only natural-kinds are notably more induction-friendly). Multilingual concept-centric. KB-merge is LOSSY (~51% link precision) = the hard vetting part.

**NEXT ACTIONS (identified path — execute, don't wait):**
1. **DECISIVE (DEPLOYED ~pre-compaction, background agent a6bbdfd0 — COLLECT its result first thing):** confirm the reasoning theory on REAL knowledge — VSA constraint-satisfaction over real concepts, retrieval/inference accuracy vs #constraints-queried (K=1..). Does the toy 0.05→0.99 constraints→resolution relationship reproduce on real (noisier) knowledge, and at what real ceiling? Writes metrics.json to scratchpad/data. If it reproduces → the reasoning-mechanism bet is confirmed; if not → rethink. (real-substrate version of the 0.99 toy).
2. BUILD stages: reserve held-out FIRST → re-ingest full CSKG (free) → generate+vet missing buckets INCREMENTALLY (measure-as-you-go safety valve) → domain modules → validate.
3. Reasoning mechanism (multi-constraint additive constraint-satisfaction) = first-class build target alongside the foundation. Full plan: notes/foundation_build_plan_2026-07-14.md.

**⚠️ SUPERSEDED / NOW SECONDARY — do NOT act on the old plan:**
- **PINPOINT** (was "THE decider" for data-vs-decoder tail) = SUPERSEDED by the pivot; off critical path (may still be running on GPU — ignore/retire).
- **GROUNDING-TAIL research** (cold-placement / spoke / consolidation / confidence) = #3 runtime-autonomous-grounding, SECONDARY (foundation is built by external tools, not autonomous bootstrap).
- **CAPACITY LEVERS** (RNS/Hopfield/graph-spectral/V(D)J) = all VET-deflated, CLOSED.
- The pre-pivot AdditiveKGMap "milestone 0.128", SR scaffold-bind, grounding-by-redundancy, etc. = valid components but the program has MOVED to foundation+reasoning-mechanism. The 11:50Z block below details these — historical only.

**PROCESS/DISCIPLINES (banked this session):** exp_dev HARDENED (.claude/agents/exp_dev.md: compute-proportionality = match method to question / no heavy KGE-fit for a gate; no-heavy-local-detached-fit; complete-or-handoff never arm-and-return) [[feedback_harden_exp_dev_selftests_must_exercise_real_api_too_many_cell_errors_USER_2026-07-13]]; **do LIGHTWEIGHT measurements INLINE, don't over-route** [[feedback_do_lightweight_measurements_inline_dont_over_route_to_heavyweight_cells_USER_2026-07-14]]; **EXECUTE identified paths WITHOUT waiting for confirmation** [[feedback_execute_identified_correct_path_without_waiting_for_confirmation_USER_2026-07-14]]; raw-then-VET; deflate CLAIMS not ambition; verify off-disk.

**KEY POINTERS:** notes/foundation_build_plan_2026-07-14.md (build plan + reasoning theory §9d/9e); notes/research_ideal_foundation_spec_size_density_optimum_2026-07-14.md; notes/research_dense_kg_prior_art_and_source_depth_2026-07-14.md; scratchpad/{attractor_constraint_sat,density_x_constraints,phase_sweep_constraint_sat}.py (the VSA constraint-satisfaction prototypes).
### ========== END CURRENT STATE ==========

### ========== 🧭 2026-07-14 ~11:50Z (PRE-PIVOT — HISTORICAL DETAIL ONLY; everything below is SUPERSEDED by the CURRENT STATE above) ==========

**THE FOCUS (USER 07-14):** recreate the BRAIN'S STRUCTURE-CONTENT FACTORIZATION, glass-box in VSA. Two thrusts = two HALVES of ONE mechanism: **A = reusable relational SCAFFOLD (reasoning)** + **B = grounded CONTENT bound onto it (grounding)**. The brain keeps a reusable map SEPARATE from content, binds new items on via a content-agnostic op (+ anti-smearing + replay). Milestone (AdditiveKGMap) = a weak SEED of this -> deepen, don't pivot. GUARDRAILS: (1) CONFIRM-BOTTLENECK-FIRST; (2) DON'T-LOCK-IN; (3) RAW-THEN-VET (NO lead/win framing pre-VET); (4) NO FILLER CELLS (run only PRIORITY experiments; idle machine is FINE). Memory: [[project_focus_recreate_brain_factorization_structure_content_two_thrusts_USER_2026-07-14]].

**THE BOTTLENECK (confirmed on-disk, bottleneck-reframe drill):** the aggregate 0.128 MRR is a MIXTURE, not a wall. Mid/high-degree entities are at 60-133% of their OWN oracle ceiling (representation is NOT the bottleneck; recipe warm-start is bounded to ~0.009 headroom -- additive is already 93.7% of its oracle). The ENTIRE deficit is the SPARSE TAIL: cold (0-support) + d1 (1-support). ALL structure mechanisms need CONNECTIVITY (edges) the tail lacks. => the tail needs edge-free CONTENT (grounding) or more edges (INGEST). Deep lever = **GROUNDED INGEST AT SCALE** (substrate is a pure symbol graph, ~zero grounded data).

**SCORECARD (VET'd unless noted):**
- ✅ **MILESTONE (chain-grade, VET-confirmed):** live+persistent `hdlab/additive_map.py::AdditiveKGMap` reproduces held-out-ENTITY MRR 0.128 via GENUINE live fit (float-drift proof, not replay), persistence round-trip real, SCALES at 19.4x N (retention 0.96). Capability SHORED UP. But 0.128 / hits@1~0.05 = MEDIOCRE absolute = the Phase-2 optimize lane.
- ❌ **CAPACITY levers ALL VET-DEFLATED** (no deployable win -- they attacked the already-at-ceiling aggregate, the reframe explains why): RNS residue=CODES-ABSENT (can't compress arbitrary labels = a LAW, 5 fields); Hopfield readout=FIDELITY-not-RETRIEVAL (task needs retrieval); graph-spectral=TRANSDUCTIVE-inflated + worse-than-additive; V(D)J=diversity-collapse (no revival).
- 🟡 **SR SCAFFOLD-BIND = the ONE genuine positive (VET-CONFIRM, MM):** successor-representation (brain scaffold-bind analog) codes compose for held-out entities LEAK-FREE at 0.073 (~7x over the failed LAP baseline) -- but SUB-ADDITIVE (1.75x below 0.128); the SR CODEBOOK is the lever (degree-weighting aggregation immaterial). Brain-factorization has legs but doesn't beat additive yet. Envelope: PUSH SR CODEBOOK.
- 🟡 **GROUNDING (Thrust B):** gated-fusion (VET-CONFIRM MM) = combiner-fix real (learned gate recovers grounding's channel that naive fusion diluted) BUT real attribute-SEMANTIC contribution only +0.094 (noisy 7/8 seeds; the +0.30 headline is 68% structural typical-entity prior). law-consistency (VET DOWNGRADE) = COUPLING ARTIFACT (used a CLEAN predictor for a 99%-correlated target = tautology; collapses when predictor also corrupted -- NOT a tail-fix). rule-induction (VET-CONFIRM negative) = weak (below POP, ~5x below additive), helps HUBS not the tail (rules need edges to fire) BUT high-precision -> rule-GATED compose envelope.
- => **NO tail-fix DEMONSTRATED yet.** Grounding carries real-but-modest signal; the deep hypothesis (grounded ingest at scale) is un-refuted + un-demonstrated.

**IN-FLIGHT (~11:50Z, verified off-disk):** GPU: [running] recipe spectral-init warm-start (bounded ~0.009) -> [pending#1 PRIORITIZED] **pinpoint v2 = THE REDIRECT** (oversampled tail cold=334/d1=183 + Test3 reachability -> classifies each bucket decoder/data/representation-limited; 9h; v1 was timeout-killed, v2 fixes tail-oversample+Test3+timeout) -> [pending#2] rule-gated compose. CPU: **grounding-by-redundancy SHIPPED+running (16d60ad06) = SMOKE HARD_PASS, the most on-target grounding signal (HOLD for FULL+VET):** the GENUINE ground-by-consistency (fixes the law-consistency artifact) -- ALL attrs jointly corrupted (NO clean sibling), cross-law CONSENSUS (median of other-3 law-predictions) is the ONLY arm that CORRECTS (+0.30 log-MAE) while no-redundancy/scramble/wrong-exp all make it WORSE + collapse; localize FULL 0.80 vs 0.43. Caveat: DETECTION barely beats marginal (+0.05) -- redundancy needed for WHICH-one+fix, not just "something off". **CORRECTION 07-14 12:05Z (verify-off-disk, prior "FULL COMPLETED 5s HARD_PASS" claim was WRONG -- Fix#28): the FULL actually CELL_CRASHED on a hard validity assert `HP_FULL_BEATS_NOREDUN_PARTNER` (FULL_auc_pc 0.760 < NOREDUN 0.913). This gate was ALREADY failing in SMOKE (partner_beats_noredun=false, all 3 seeds) -> it's a reliable by-design property (when the correlated sibling is ALSO corrupted, cross-law consensus is polluted so single-law detects better there), NOT scale-inversion. The cell's own smoke-verdict logic treats it non-fatal (HARD_PASS) but _validity_checks HARD-asserts it -> mis-specified gate aborted the FULL before it reported. SMOKE core claims LOOK STRONG + robust across 3 seeds: FULL CORRECTS (+0.298) vs all must-fails negative (NOREDUN -0.389/SCR -0.318/WRONG -0.101); FULL LOCALIZES (0.802 >> NOREDUN 0.458 >> chance 0.25); FULL DETECTS marginally (+0.043). LANDED-VET DONE (skunkworks a0da9512): TIER = **MEASURED_MECHANISM (genuine-but-modest, provisional -- NOT chain-grade off a local smoke).** Core mechanism GENUINE + leak-free (every arm sees bg-noise on 100% of cells, no zero-noise predictor, RELATIONAL is separate KGStore); localization robust (FULL 0.802 beats NOREDUN +0.344 EVERY seed, cv 0.05); all must-fails collapse 3/3. WEAK LINK = thin margins over the STRONG no-law baselines (FULL-vs-MARGINAL loc +0.078; FULL-vs-RELATIONAL corr dips below 0.15 bar on seed7, passes only on aggregate) + noisy correction gain (cv ~0.3, seed19 only 0.188) -- NOT the must-fails. HONEST BOUND (fold into write-up): when the correlated PARTNER is also corrupted, FULL localization collapses to ~chance (loc_pc 0.375, consensus polluted). GATE ALREADY FIXED ON DISK (partner-detection demoted to report-only ~lines 476-486/701-704 before the smoke; the CELL_CRASHED stub is from a STALE pre-fix file -> current file will NOT re-crash). ACTION TAKEN 12:10Z: re-shipped CURRENT file for CANONICAL FULL via orchestrator a4eb441c (remote_cpu, --allow-duplicate, canon!=preview -> do NOT certify off the smoke). VET expects FULL to tier MEASURED_MECHANISM unless extra seeds [7,13,19,23,29]@8192 tighten the thin no-law margins. NOT filler -- THE ground-by-consistency envelope (fixes the law-consistency artifact).** CPU then correctly IDLE (priority cell done, nothing else priority-ready -> no-filler rule = idle OK; ordinal cell authored but NOT shipped). GPU stays loaded through compaction (recipe->pinpoint->rule-gated). STORE: 11 atoms pushed origin/main 3532a1001 (user-auth); scaffold-bind-MM + law-consistency-MM + rule-induction-MM atoms pending NEXT sync (needs user-auth push).

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) **COLLECT + VET the PINPOINT REDIRECT** (data/exp_anchor_compose_bottleneck_pinpoint_cskg_v2/metrics.json -- per-bucket decoder/data/representation; THE decider for where ALL tail work aims); (3) collect recipe (bounded), rule-gated (modal-negative on flat vocab), grounding-by-redundancy, ordinal -- raw-then-VET each; (4) ON PINPOINT: if tail=DATA-limited -> grounded INGEST is the lever (the deep build); if decoder-limited -> decode work; (5) fire PRIORITY backlog only (push-SR-codebook GPU; grounding-by-redundancy follow-up) -- NO filler; (6) subsequent store-sync of the 3 pending VET atoms; (7) collect still-pending drill parents (A-field/B-field/B-quantum/B-wildcard).

**DISCIPLINES THIS SESSION:** raw-then-VET WORKED (held 3 wanted-positives -> 1 survived [SR scaffold-bind], 2 deflated [Hopfield, graph-spectral], 1 was an artifact [law-consistency] -- NO false claim escaped); NO FILLER CELLS (USER 07-14); verify-off-disk (mis-read pinpoint terminal-by-timeout as "landed", orchestrator caught it -- Fix#28); tighten concurrency (git-races from many concurrent committing agents; orchestrator path-scoped commits handle it); store-sync + origin push need DIRECT in-session user-auth. Crons: heartbeat a40824ce + self-drive 036e6ab5 (30min) active.

**PRE-COMPACTION DRILLS LAUNCHED (~11:55Z, will land post-compaction -> COLLECT their notes/ deliverables next session):** (1) **GROUNDED-INGEST-AT-SCALE** build plan (the deep lever if pinpoint=data-limited: no-LLM bulk measured-data sources + entity-join + VSA wiring + reach the sparse tail; a48fbb3f); (2) **STRENGTHEN SR SCAFFOLD-BIND** (why the one genuine positive is sub-additive + ranked glass-box levers to close the gap to 0.128; feeds the push-SR-codebook experiment; ac2a2d54). Both write topic-slug notes files.

**KEY POINTERS:** `hdlab/additive_map.py::AdditiveKGMap` (milestone); **notes/research_brain_grounding_spoke_building_canonical_reference_2026-07-14.md (THE grounding design reference -- hub/spoke, Quinian, text-spoke experiment + trap)**; notes/research_grounded_ingest_at_scale_2026-07-14.md (deep-lever plan); notes/research_sr_compose_close_gap_to_additive_map_2026-07-14.md (SR=Bellman-fixed-point, score-fusion lever); notes/research_llm_reasoning_interp_glass_box_lens_2026-07-14.md (IIA gate + reasoning-vs-frequency); notes/research_grounding_topicB_synthesis_and_next_levers_2026-07-14.md (grounding levers A/B/C); notes/research_drill_reframe_true_bottleneck_2026-07-13.md (the sparse-tail bottleneck).
**🔄 SESSION-RESUME 2026-07-14 ~12:15Z (post-compaction; USER: get caught up + check running/finished):** Both pre-compaction drills LANDED + 3 priority cells now in flight (all genuine priority, no filler; confirmed on-disk).
- GPU (survives compaction): recipe running -> **pinpoint v2 redirect** pending (STILL the pivotal decider) -> rule-gated pending.
- remote_cpu: **grounding-by-redundancy CANONICAL FULL = LANDED HARD_PASS (12:15Z, run_mode=full, n_seeds=5 [7,13,19,23,29], n_dim=8192, elapsed 2.2s -- tiny allometry arena so ~instant).** CONFIRMS MEASURED_MECHANISM at canonical scale, reproduces smoke: FULL corr +0.301 (smoke +0.298) vs all must-fails negative (NOREDUN -0.400/SCR -0.449/WRONG -0.055); FULL loc 0.800 vs NOREDUN 0.431 (chance 0.25); detect +0.052 marginal; honest bound persists (partner-corr detect 0.728<0.900, report-only). VET's predicted tier held (extra seeds didn't erode margins). => READY FOR ATOM-FILING off the CANONICAL (not smoke) at next store-sync. CPU then correctly IDLE (fast cell done, join in-hand, next CPU priority gated on join-VET verdict -> no-filler idle OK).
- **🚨 exact-ID join go/no-go = HARD_FAIL (DECISIVE, exp_dev a140d6, dbcc... commit 9bf7d9ddd; data/exp_grounded_ingest_tail_join_v1/):** numeric-literal grounding (Wikidata quantity-claims) reaches only **1.80% (9/500)** of the sparse tail via exact-ID join (<< 5% HARD-FAIL bar). Failure split: 75.4% no-title-match, 22.8% Wikidata-page-but-NO-numeric-claim, 1.8% hit. 9 hits genuine (cities->pop/area, compounds->mass/density); scramble control 0/45 clean. => **the substrate's sparse tail is NON-NUMERIC** (94% CN lemmas: proper nouns / rare taxonomic leaves / abstract relations with no measured-numeric analog) -> **bulk NUMERIC ingest CANNOT reach it; redirect grounding content-type to gloss/definition TEXT, not numeric literals.** This is a load-bearing REDIRECT of the grounded-ingest deep lever (was assumed numeric). CRITICAL VET IN FLIGHT (skunkworks a997d264): FIXABLE-vs-FUNDAMENTAL -- join key was enwiki-title BECAUSE ingest stored metadata={} (no crosswalk); CSKG was built MERGING ConceptNet+Wikidata so a QID crosswalk likely EXISTS upstream + our ingest DISCARDED it -> if so the 1.8% is a LOSSY-INGEST ARTIFACT not a wall (re-ingest crosswalk could revive numeric reach). NB the "crosses 1->2 support" HARD-PASS half is degenerate (tail scoped to support==1, any hit trivially crosses) -> verdict rests on hit-rate alone. Remote canonical ship SKIPPED (cell network-free + deterministic + source-sha256-pinned = local==remote). **VET DONE (skunkworks a997d264) = genuine HF_STRUCTURAL_BOUND, redirect CONFIRMED + STRENGTHENED, BANKED [[project_substrate_sparse_tail_is_nonnumeric_grounding_must_be_text_gloss_not_numeric_2026-07-14]]:** (1) my "fixable crosswalk" hypothesis DISCONFIRMED on disk -- ConceptNet 5.7 source has no QIDs, on-disk CSKG dump has ZERO lemma->Q SameAs edges -> crosswalk-revival CLOSED. (2) join KEY IS lossy (62.6% of misses casing/hyphen/redirect-suppressible) so 1.8% is a lossy-KEY floor NOT total reach -- but numeric conclusion SURVIVES: only 7.3% of matched entities carry a quantity claim -> perfect key still projects ~3-6% << 15%. (3) tail genuinely non-numeric (verb-phrase lemmas/abstract/WN-synsets/taxonomic leaves). => DON'T build numeric literal-fusion pipeline; grounding content-type -> TEXT/GLOSS (the 22.8% QID-but-no-number bucket). ATOM to file at next store-sync: HF_STRUCTURAL_BOUND, revival="acquire a lemma->external-numeric-ID crosswalk". Optional casing-key de-risk NOT fired (no-filler; VET predicts it confirms the wall). TEXT-GROUNDING BUILD HELD until pinpoint confirms tail=DATA-limited (confirm-bottleneck-first).
- **SR-additive SCORE-FUSION** (exp_dev aab523e1) authoring -> ship: fuse the two landed FULLs (SR-compose 0.073 + additive 0.128) at score-level (glass-box RRF/weighted-sum), targets the cold/sparse tail, could EXCEED 0.128 = top Thrust-A lever to close the milestone gap. Independent of pinpoint (different lane).
- DRILLS LANDED: (a) grounded-ingest -> notes/research_grounded_ingest_at_scale_2026-07-14.md (deep-lever plan; mechanism precedented LiteralE ~31%/cold-start-rescue, sufficiency-scale novel P0.35; first step = the join test now running). (b) SR-scaffold-bind -> notes/research_sr_compose_close_gap_to_additive_map_2026-07-14.md (KEY: SR-compose BEATS its own oracle b/c SR eq IS the local Bellman fixed point = composing-from-neighbors IS the object's recursion at a new point; ranked levers, top=score-fusion now running). handoff processed: notes/exp_dev_handoff_research_sr_compose_close_gap_to_additive_map_2026-07-14.md.
- ORDINAL cell (f6b5a2141) authored-committed-but-NOT-shipped (filler, correctly parked). PENDING store-sync unchanged (needs user-auth push).

**🧠 GROUNDING-THRUST DESIGN RESOLVED + STRATEGIC ADVANCES (2026-07-14 ~13:00Z; the grounding thrust now has a full BRAIN-DECIDED design -- BUILD HELD until pinpoint=data-limited, confirm-bottleneck-first):**
- **HUB-AND-SPOKE frame (canonical ref: `notes/research_brain_grounding_spoke_building_canonical_reference_2026-07-14.md`):** the brain grounds by ROUTING each concept to the modality SPOKE(s) that fit it (vision/action/magnitude/interoceptive/affect/space/social/language-situational), all feeding an amodal HUB (anterior temporal lobe = OUR relational scaffold). Our substrate = HUB + **ZERO spokes**. The NUMERIC spoke is empty for our tail (why the join HARD_FAIL) -> build the spoke that MATCHES = **TEXT/SITUATIONAL**, not numeric.
- **QUINIAN BOOTSTRAPPING = literally our architecture (Carey; domain-general per Gentner / NatHumBehav 2023):** children acquire PLACEHOLDER symbols defined ONLY by their mutual relations, then FILL IN meaning by analogically mapping that relational skeleton onto a grounded CORE (Spelke's 5: objects/agents/number/space/social). Our graph = the placeholder skeleton; grounding = fill-by-mapping. **The brain's own developmental template VALIDATES our hub-first-then-spoke plan.** Content-agnostic BIND = CDZ (Damasio: store the conjunction, reconstruct by divergent re-firing = our VSA bind); ANTI-SMEAR = dentate-gyrus pattern-separation (= our own CORRELATION-HURTS-CAPACITY finding, brain-confirmed). Banked [[project_grounding_is_build_a_spoke_on_the_hub_quinian_bootstrap_validates_architecture_2026-07-14]].
- **FIRST BUILDABLE (post-pinpoint, if DATA-limited): minimal TEXT/SITUATIONAL spoke** = bind EXOGENOUS gloss/situation-frame content onto each tail concept's graph code, matched to subtype (event->Nelson script frame; social/moral->social-relation; emotion->valence). **MUST-FAIL / THE TRAP:** a gloss that is just the concept's OWN NEIGHBORS re-encoded must NOT beat the bare graph (= symbols-about-symbols / DoQ trap; genuine grounding needs EXOGENOUS content -- Louwerse SIH: language is a shortcut INTO grounding, not grounding itself). P0.40.
- **LLM-INTERP drill (`notes/research_llm_reasoning_interp_glass_box_lens_2026-07-14.md`):** field CONFIRMS reasoning-vs-frequency is the real frontier (islands of causally-verified reasoning [induction heads / Othello-GPT / grokking] drowned in frequency-effects + non-causal chain-of-thought). TRANSFERABLE GATE = Interchange-Intervention-Accuracy + specificity (causal test of genuine-encoding-of-a-variable) -- CHEAP for us because coords are known by construction = a real glass-box DIFFERENTIATION. INHERITED open Q (NOT sidestepped) = storage-vs-computation asymmetry (superposition capacity-efficient for STORAGE, unproven for active COMPUTATION) -> maps onto our additive-vs-multiplicative-bind / SR-vs-additive-compose cells. CANDIDATE = apply IIA+specificity to additive_map/SR-compose stratified by degree bucket (a causal reasoning-vs-frequency test; HELD for pinpoint).
- **IN-FLIGHT NOW (~13:00Z):** GPU recipe running -> **pinpoint (NEXT, the decider)** -> rule-gated -> **SR-fusion (SHIPPED #3, dbcc79e70; HARD-PASS = fused beats additive +0.02, targets cold/sparse tail, could exceed 0.128).** grounding-by-redundancy CANONICAL HARD_PASS landed (atom filed). CPU correctly IDLE (no-filler). Brain-spoke drill PARENT bugged (delegated+returned-early) -> director synthesized the 3 landed sub-scans into the canonical ref himself.
- **TEXT-SPOKE GO/NO-GO LANDED (a277748f1, HARD_PASS, ~14:30Z) = FEASIBILITY GO (hold framing -- wanted result, local run):** exogenous gloss text REACHES the tail at **83.2%** (416/500; WN-native 21/21, CN-lemma->WN 392/476, FN 3/3; ConceptNet DefinedAs/HasContext DON'T EXIST in graph =0) vs numeric 1.8% -- AND is genuinely exogenous (exo-token frac 0.895, 87.5% clear bar; must-fails: synthetic-from-own-graph=0.0, scramble=0.0). CLEARS the FIRST gate (text exists + exogenous, NOT symbols-about-symbols) but AVAILABILITY != USEFULNESS. Caveats the usefulness test MUST handle: (1) CN->WN lemma-match risks WRONG-SENSE glosses (polysemy); (2) exogeneity could be function-word-padded -> need CONTENTFUL; (3) WordNet-gloss-dependent single-source. NEXT = the usefulness test (does binding gloss as intrinsic content IMPROVE cold inference, stratified name-transparent/opaque + rel-type) -- HELD for the cold-placement drill design (brain-ground-first). Cell network-free/deterministic (sha256-pinned, nltk-free at runtime) so local==remote; remote canonical ship optional (queue_add handed off, not fired).
- **ARCHITECTURE CRYSTALLIZING (USER design thread, bank as a unit once drills land):** grounding-the-tail = (1) PLACE cold entity PROVISIONALLY by intrinsic CONTENT (name/gloss re-derives held-out synonym/IS-A anchor -> inherit category relations; cold = rare-variant-of-common, 59% of graph is degree-1); (2) CONSOLIDATION/SLEEP loop EVALUATES+UPDATES those provisional relations (replay reinforces confirmed / prunes contradicted / extracts schema; sleep IMPROVES relational inference = our prize); (3) relations carry graded CONFIDENCE+PERMANENCE (new=low/labile -> reinforce-toward-stable or break/replace; confidence GATES plasticity = stability-plasticity). We HAVE: retrieval-confidence (kg_traversal tau) + VSA-magnitude native strength + cert-tiers. GAP: stored edges BINARY (metadata={} 100%), no per-relation persistent confidence+permanence lifecycle. Drills in flight: cold-placement (a3ee2482), consolidation+confidence (a4e663d8).
- **POST-PINPOINT PRIORITY CANDIDATES (fire per verdict, NO filler before):** if tail=DATA-limited -> minimal TEXT-SPOKE cell (design above); either verdict -> IIA-on-compose gate + push-SR-codebook. Collect+VET raw-then-VET: pinpoint, SR-fusion, recipe, rule-gated.
**🏗️🚨 PROGRAM PIVOT (USER-AUTHORIZED 07-14, NOW THE PRIMARY DIRECTION — supersedes the grounding-tail research as focus): BUILD THE IDEAL KNOWLEDGE FOUNDATION FROM EXISTING TOOLS.** Full memory: [[project_PIVOT_build_ideal_knowledge_foundation_from_existing_tools_USER_AUTHORIZED_2026-07-14]]. 3-way distinction: (1) FOUNDATION = use ANY external tool, build FULL+VETTED (a BUILD; reverses no-ingest lock, which now governs only the RUNTIME core); (2) RUNTIME REASONING = glass-box, no external LLM at inference (invariant); (3) autonomous grounding = #3-secondary (the cold-placement work; optimization landed METHOD_LIMITED_PARTIALLY_FIXABLE, opaque 2.6%->29% w/ fuller synonym/hypernym content = HELD pending VET, reinforces densifying). GUARDRAILS: hold out clean test slice; runtime glass-box; value = transparent reasoning that beats frequency not having-facts.
- **IDEAL-FOUNDATION SPEC (drill notes/research_ideal_foundation_spec_size_density_optimum_2026-07-14.md):** CORE (Wikidata+ConceptNet+ATOMIC+WordNet+Wiktionary+YAGO4-constraint+BabelNet-labels) + OPEN MODULE REGISTRY (PubChem/periodic/Materials-Project/GeoNames/UMLS/SNOMED) attached via SHARED-CORE-ENTITY anchoring; cross-module interconnect THROUGH shared entities (reconcile on overlap). DENSITY target = per-concept RELATION-TYPE-BUCKET coverage (>=4-5 of 7: taxonomic/property/part-whole/functional/causal/spatial/social), NOT node-count. Multilingual = concept-centric + label layer. HONEST: KB-merge is LOSSY (~51% entity-link precision, ~28% fact-loss) = "fully vetted" is the hard part.
- **🎯 FOUNDATION-GAP AUDIT (director-run 07-14, on our active partition, ZERO new data):** our current CSKG = a LEXICAL/TAXONOMIC SKELETON. **93.4% of 141k concepts are UNDER-characterized (<=1 semantic bucket); only 0.44% reach the >=4-bucket ideal.** Dominated by CN_SYNONYM(71k)+IS_A(62k); property/functional/causal/part/spatial/social buckets threadbare (HAS_PROPERTY 710, CAUSES 1.8k). => THE BUILD TARGET IS NOT "more entities" -- it's ENRICH THE ENTITIES WE HAVE across the 7 buckets by ingesting the missing relation types from the core KBs (which have them; we ingested a thin lexical/taxonomic slice). Bounded + measurable. NEXT: scope the enrichment build (map which core-KB supplies which bucket for our entities + pilot enrichment on a sample -> measure bucket-coverage lift). AWAITING USER STEER on build scope.
### ========== END 2026-07-14 ~11:50Z COMPACTION-READY ==========

### ========== 🧭 2026-07-14 ~11:00Z OVERNIGHT RESULTS (detail; superseded by 11:50Z above) ==========
**3 FULLs landed overnight (all run_mode=full, verified off-disk); VETs in flight -- HOLD framing per raw-then-VET (director over-framed 2 priors). Machines IDLED ~02:20-11:00 after backlog drained (honest gap; refilled 11:00).**
1. **scaffold-bind (graph_spectral_compose_sr_ppmi_nystrom_v1) = SCAFFOLD_BIND_TRANSFERS -- brain-factorization FIRST INDUCTIVE HIT.** SR (successor-representation, the brain scaffold-bind analog) codes composed for HELD-OUT entities: SR_FLAT 0.0731 / SR_NYS 0.0738 vs LAP(parent worst codebook) 0.0099 vs RAND 0.0040 = ~7x over the failed LAP baseline. PPMI_FLAT 0.0180 also credited. winner_axis=INTERACTION_SR_NYS (but SR_FLAT~SR_NYS so NYS aggregation likely immaterial -- the result is "SR codebook transfers inductively"). Still ~1.75x BELOW additive 0.128. => the graph-spectral inductive-compose "negative" was only tested with the WORST codebook; the SR/brain-analog codebook DOES transfer inductively (leak-free per cell). VET ad2444b4 (crux=leak-check + is it real+sub-additive). 3 seeds.
2. **law-consistency = VET DOWNGRADE -- COUPLING ARTIFACT, NOT a tail-fix (a6f5f079).** The HARD_PASS is real vs its gates but the crux failed: only the TARGET (mass) was corrupted, the PREDICTOR (length) stayed CLEAN -> when VET corrupted length too, LAW cold R2 collapsed 0.978 -> -1.55 (BELOW the relational floor it beat). mass~length pearson(log)=0.993 = near-tautology (use clean sibling to predict 99%-correlated target = attribute-attribute regression, NOT grounding). "degree-invariant" trivial (global regression ignoring graph); "one law grounds many" FALSE (2 moderate laws failed); 5-seed "robust" vacuous. => ground-by-law is NOT the convergence's tail-fix. GENUINE ENVELOPE (VET-pointed, untested) = GROUNDING-BY-REDUNDANCY: corrupt ALL attrs jointly, use cross-law redundancy to localize the inconsistent value. [ORIGINAL over-frame below, now corrected:] ~~HARD_PASS degree-invariant tail-fix~~ Ground-by-LAW (mass=length^3): [detect corrupted] AUC 0.999 vs no-law 0.717; [correct] +0.833 vs -0.584; [impute COLD tail] +0.978 vs RELATIONAL -1.513 vs MEAN -0.393 = DEGREE-INVARIANT (needs NO neighbors = the connectivity-limited-tail fix). must-fails collapse (wrong-exp auc 0.726, scramble 0.514). 5 seeds. VET a6f5f079 (CRUX = same-source COUPLING artifact? mass~length 98% corr -- does it hold when PREDICTOR also corrupted?). If VET-clean = the convergence's proposed bottleneck-fix, realized.
3. **10x-N (additive_map_fixed_support_scaling_cskg_v1) = SCALES_AT_10X_FIXED_SUPPORT_RETAINS -- MILESTONE VALIDATED AT SCALE.** N-ladder [25752->498540, 19.4x], candidate-growth-normalized R_norm=0.964 (>=0.70 SCALES), top-N fires, relation_signal True. The VET-flagged untested "N-invariance beyond 1.5x" claim now MEASURED-confirmed at 19.4x.
- PINPOINT = **FAILED, NOT LANDED (director verify-off-disk error corrected by orchestrator): TIMEOUT-KILLED (14461s > 14400s ceiling), only seed-7 Test2 ran, Test3 (reachability) NEVER ran, and only 19/3000 held-out queries were cold+d1 (n_cold15/n_d1=4) -> tail UNDER-SAMPLED -> redirect UNANSWERABLE.** Partial hint (uninterpretable, n=4): k-budget helps DENSE mass (tracks oracle) but does NOT recover the tail -> WEAKLY hints tail-not-decoder-limited, but no Test3 = can't separate data-vs-representation. RE-DESIGNED (exp_dev a6a73474: OVERSAMPLE cold/d1 to n>=150-300, RUN Test3, split/bigger-timeout to fit -- 4h was too low). STORE-SYNC = USER-AUTHORIZED 2026-07-14, pushing (orchestrator ae652afa). IN-FLIGHT (11:00): GPU=recipe warm-start (a1fd6f6e); CPU=rule-gated compose (a4928cd3); pinpoint-redesign (a6a73474); 2 VETs (scaffold-bind ad2444b4, law-consistency a6f5f079). 10x-N SCALES collected off-disk.
### ========== END 2026-07-14 ~11:00Z OVERNIGHT ==========

### ========== 🧭 2026-07-14 ~04:00Z CONVERGENCE ==========
**Tonight's VET'd results converge on ONE honest, deflated conclusion: the number moves via GROUNDED CONTENT AT SCALE, not a cleverer algorithm.**
1. BOTTLENECK = SPARSE TAIL (cold 0-support / d1 1-support); mid/high-degree already at 60-133% of oracle. (bottleneck-reframe, on-disk P0.65)
2. CONNECTIVITY is the tail's wall (VET-corrected framing, a742e635 CONFIRM negative): rule-induction FULL = MIDDLE_BAND genuine negative -- discrete AnyBURL/RuleN Horn-rule induction SPECIFICALLY is weak (RULE_INDUCT 0.0248, 5.2x below additive 0.128, own oracle 0.0265 barely > POP so better mining can't rescue it on this thin 29-rel KG). CORRECTIONS to my over-reach: (a) NOT "structure fails" -- ADDITIVE is also structure-only + 5x better; the real point = ALL structure mechanisms need CONNECTIVITY (edges), which the sparse tail lacks -> grounding helps because it's EDGE-FREE content. (b) rules TIE POP on MRR but beat POP 3x on PRECISION (hits@2), lose on coverage = HIGH-PRECISION/LOW-COVERAGE -> good as a GATE (rule-gated compose = live envelope), not standalone. (c) "can't help 0/1 tail" is INFERRED from the monotone degree trend (low0.0105/mid0.0194/high0.0429, robust 3/3 seeds), NOT directly measured (cold tail excluded/unscorable). Real signal genuine (shuffle-controlled, planted pos-control HARD_PASS). => the tail's wall is CONNECTIVITY; grounding (edge-free content) + more-edges (ingest) are the two levers.
3. GROUNDING (content) DOES help the tail -- gated-fusion FULL = HARD_PASS, VET-CONFIRMED (MM +1): the convex gate RECOVERS grounding's channel (fixes the naive-fusion dilution) -- but the honest decomposition = 68.5% structural typical-entity-prior (SCRAMBLE-REL +0.204) + only 31.5% genuine attribute-SEMANTICS (GATED-SCRAMBLE +0.094, REAL but NOISY: 7/8 seeds, cv 0.70). Modest because grounded with a TINY 5-trait dataset. Scope: low-support d1 placement via similarity, NOT multi-hop reasoning.
=> **CONVERGENCE: the sparse-tail bottleneck is a CONTENT/GROUNDING problem; grounding works but is DATA-STARVED; the real lever = GROUNDED INGEST AT SCALE (the foundational build -- substrate is a pure symbol graph w/ ~zero grounded data). Pinpoint cell (GPU-queued behind 10x-N) confirms the tail=data-limited half directly.** Store-sync pending: +grounding-MM, +gated-fusion-MM, +rule-induction-MM(pending VET) atoms (needs user-auth push).
### ========== END 2026-07-14 ~04:00Z CONVERGENCE ==========

### ========== 🧭 2026-07-14 STRATEGIC FOCUS + STATE (supersedes all below 01:10Z) ==========
**THE FOCUS (USER-aligned 2026-07-14): RECREATE HOW THE BRAIN does inductive relational reasoning = STRUCTURE-CONTENT FACTORIZATION, glass-box in the VSA substrate.** The two major thrusts are the two HALVES of ONE brain mechanism (they converge, not compete): **THRUST A (reasoning/relational)** = the reusable relational SCAFFOLD/structure kept SEPARATE from content; **THRUST B (grounding)** = grounded CONTENT bound onto the scaffold. The brain grounds AND factorizes as one system (grid/entorhinal reusable map + hippocampal content pinned on; TEM/successor-representation). Our milestone (AdditiveKGMap: compose a held-out-entity code by binding relation-operators onto its neighbors) is ALREADY A WEAK SEED of the factorization -> DEEPEN it, don't pivot. GLASS-BOX = copy the PRINCIPLE (reusable structure + content-agnostic bind + anti-smearing/pattern-separation + replay/consolidation), NOT a neuro-sim.

**🎯 BOTTLENECK CONFIRMED (guardrail-1 RESOLVED, bottleneck-reframe drill 07-14, on-disk-verified P0.65): the 0.13 aggregate MRR is a DEGREE-STRATIFIED MIXTURE, NOT a representation wall.** Mid/high-support buckets (d2_3..d8plus) are ALREADY at 60-133% of their OWN oracle ceiling (NO representation headroom there). The ENTIRE deficit is the SPARSE TAIL: COLD (0-support, scores BELOW random -> likely DATA/coverage not representation) + D1 (1-support, ~85%-of-oracle headroom -> the one bucket where decoder/representation work is justified). => this VINDICATES the 8 capacity negatives (they diagnosed the aggregate, dominated by an already-at-ceiling population -- attacked a NON-bottleneck) AND VALIDATES the pivot: grounding (gated-fusion, VET-scoped to low-support placement) + rule-induction (aimed at degree-starved d1/d2_3) BOTH already target the true sparse-tail bottleneck. NEXT (pinpoint cell a957ce1d in flight): is the cold/d1 deficit DECODER-limited (Test2 decoder-budget sweep recovers d1) or DATA-limited (Test3 cold/d1 graph-path unreachable)? -> redirects ALL further work. Note notes/research_drill_reframe_true_bottleneck_2026-07-13.md.
**GUARDRAILS (do not skip):** (1) **CONFIRM THE BOTTLENECK FIRST** -- RESOLVED above (sparse tail cold/d1, not representation); pinpoint cell decides decoder-vs-data. (2) **DON'T LOCK IN** (USER standing) -- leading hypothesis today, reconsider as evidence lands. (3) **raw-then-VET, NO lead/win framing pre-VET** (director over-framed Hopfield + graph-spectral as wins; both VET-deflated -- discipline rule banked).

**MILESTONE (VET-CONFIRMED chain-grade): live+persistent hdlab/additive_map.py::AdditiveKGMap = capability SHORED UP** (construction/integration + persistence proof; genuine live fit proven by float-drift, not replay). ABSOLUTE 0.128 MRR / hits@1~0.05 = MEDIOCRE, UNCHANGED = the Phase-2 optimize lane. 10x-N-fixed-support scale-validation RUNNING GPU (~1.5h into 8h).

**PHASE-2 CAPACITY = ALL LEVERS VET-DEFLATED (no deployable capacity win; additive 0.128 remains best):** RNS residue=CODES_ABSENT (can't compress arbitrary labels = a LAW, 5 independent fields); Hopfield readout=FIDELITY-not-RETRIEVAL (our single-hop task needs retrieval; envelope=multi-hop, conditional/free-gate-first); graph-spectral=TRANSDUCTIVE-inflated + worse-than-additive + doesn't transfer inductively (envelope=brain-like SCAFFOLD-BIND, drilling); V(D)J=genuine-negative addressable-diversity-collapse (best case=parity, NO revival). => beating 0.128 needs a DIFFERENT EXTRACTION mechanism (brain-factorization / rule-induction) OR grounding, NOT a capacity trick.

**GROUNDING (THRUST B) = session's BEST VET-CONFIRMED POSITIVE:** grounding CARRIES genuine leak-free held-out-RELATION signal (GROUNDED_ONLY 0.618 >> RELATIONAL 0.387, beats POP 8/8 seeds); "redundant" REFUTED = equal-weight fusion DILUTED it. BETTER-FUSION (gated, glass-box) = VET-confirmed win-path, AUTHORING (exp_dev ad8c588e -> CPU). Scoped: low-support entity placement via similarity, NOT multi-hop reasoning. Wiring proven (FPE + level-code). Designed next levers: consistency-against-invariants, ordinal-encoding, active-sampling -> notes/research_grounding_topicB_synthesis_and_next_levers_2026-07-14.md.

**DRILL RESULTS (2026-07-14 more-theories + envelope drills):**
- **RULE INDUCTION = the STANDOUT new lever + a concrete brain-factorization realization: mine confidence-weighted Horn rules (ZERO-training graph stats, AnyBURL-style) + apply via the substrate's ALREADY-PROVEN forward-chaining (PP-196 STRIPS / PP-252 defeasible); rules generalize to ANY entity = SIDESTEPS the capacity wall; glass-box (inspectable rules); brain-aligned (PFC rule cells / TEM amortize entity representation). CELL AUTHORING (exp_dev ab484b2 -> CPU). rule-APPLICATION already HARD_PASS; rule-INDUCTION is the untested axis. P 0.22 hard-pass / 0.45 novel-worth.**
- RECIPE (shore-up A): top cheap lever = spectral-Laplacian INIT warm-start of the entity table (+3-15% rel MRR, zero-cost single swap; more modest than earlier +32-42% headline -- deflated). rank-2 snapshot-ensembling (infra-blocked), rank-3 full hyperparam sweep.
- HOPFIELD multi-hop envelope: CONDITIONAL (fidelity CAN bind in 2-3 hop chains per DAgger + density-evolution IF single-hop error near crossover) -- FREE arithmetic gate on landed multi-hop metrics FIRST; Stage-5 cell has a DENSE_HOPFIELD_CLEANUP arm to amend (measure retrieval not just fidelity).
- NEURO-SYMBOLIC (feeds rule-induction above): pick-2-of-3 {inductive, glass-box, cheap}; AnyBURL/RuleN come closest; heavy differentiable-ILP/DeepProbLog/MLN = do-NOT-build (exponential at KG scale).
- PENDING drills: composition-operator, BOTTLENECK-REFRAME (guardrail-1), graph-structure-inductive-transfer (scaffold-bind) envelope, 4 Topic-A/B parents (A-field, B-field, B-quantum, B-wildcard).

**IN-FLIGHT (~2026-07-14 late):** GPU: 10x-N scale-validation. CPU: rule-induction authoring->ship + better-fusion authoring->ship. VETs DONE: milestone(CG), Hopfield(MM re-scope), graph-spectral(MM re-scope), grounding-reasoning(MM POSITIVE), V(D)J(MM negative-closed), reciprocal+closed-form(HF). exp_dev SCP ship path is locked-DENIED -> hand queue_add to orchestrator; queue_add is SCP-based, NO origin push (needs direct user auth).

**STORE-SYNC PENDING (needs DIRECT USER-AUTH push -- FLAG TO USER):** ~8 atoms today (milestone-CG, Hopfield-MM, graph-spectral-MM, grounding-MM, V(D)J-MM, reciprocal-HF, closed-form-HF, closed-form-budget). All A5-gated on local disk (needs_orchestrator_store_sync=True); the canonical-store push awaits in-session USER authorization.
### ========== END 2026-07-14 STRATEGIC FOCUS + STATE ==========

### ========== 🧭 2026-07-14 ~01:10Z PHASE-2 SOLUTION-TEST RESULTS ==========
**MILESTONE (VET-CONFIRMED, chain-grade): live+persistent AdditiveKGMap = capability SHORED UP (construction/integration proof; 0.128 unchanged = Phase-2 lane). Then USER: "test all potential solutions, load CPU+GPU." Ran the solution-test matrix. RESULTS so far (each FULL, each landed-VET where noted):**
- **A/dense-Hopfield readout = MEASURED_MECHANISM (VET CONFIRM +1, atom filed needs_sync), but RE-SCOPED HARD -- NOT a deployable end-task win.** Raw FULL looked like HARD_PASS (corr_lift 3.25x on real correlated codes, controls fire, reproduced bit-exact). VET's 3 downward corrections: (1) lift is in RECONSTRUCTION-FIDELITY (cosine>=0.80) NOT RETRIEVAL/argmax -- pairwise ALREADY finds the correct NN (argmax n-invariant; argmax-gain +0.01-0.07 vs gate-gain +0.63-0.95); our KG task is RETRIEVAL so this likely does NOT move 0.128 MRR. (2) lift VANISHES to 1.0x at large N (N2048 all strata, both arms saturate M_CAP) = small-to-mid-N censor-deflated effect. (3) capacity-metric on SYNTHETIC codes, not end-task, not live store. => DIRECTOR OVER-FRAMED it as "first Phase-2 win" -- CORRECTED: confirmed capacity mechanism, NOT deployable for single-hop retrieval. Do NOT run naive plug-into-AdditiveKGMap single-hop follow-up (VET predicts no lift). ONLY opening = MULTI-HOP chaining (intermediate-reconstruction fidelity compounds) -- targeted check only if prioritized. cell 3fa59f6b4.
- **A/graph-spectral codes FULL = GRAPH_STRUCTURE_LIFTS_PARTIAL_MIDDLE (3 seeds, controls fired) -- THE MOST PROMISING PHASE-2 LEAD.** ORACLE ceiling LIFTED BIG by closed-form glass-box PPMI graph codes: PPMI_ORACLE 0.1189 vs RAND 0.0231 = +0.096, = 0.866x the additive oracle 0.137 -- graph structure IS capturable in a no-SGD glass-box code basis, near the learned-additive ceiling (NB: coordinate-fit closed-form was DEAD, but graph-CODE closed-form nearly matches -- reopens the strict/glass-box story via codes not coords). BUT inductive COMPOSE lift FAILED: +0.006 < 0.010 bar -- structure is IN the codes, the compose mechanism doesn't EXTRACT it. => next lever points itself: a compose/readout that cashes out the PPMI structure. LAP weak (0.011), SR mid (0.051), PPMI best. Skunkworks VET = CONFIRM MECHANISM (+1 atom) but DEFLATES the "lead": the ORACLE arms fold held-out QUERY edges into the codebook (declared transductive ceiling, FAIR vs additive which does same, NOT a hidden leak) -> the 0.119 partly ENCODES the test adjacency, NOT pure capacity; proof = train-only inductive compose COLLAPSES (+0.006 fail). Only PPMI/NetMF lift; normalized-LAPLACIAN HURTS (0.011<random 0.023). DEPLOYMENT: spectral codebook transfers only 8.3% of its own oracle (0.0099/0.119) vs additive's 93.4% (0.128/0.137) = STRICTLY WORSE than additive. => NOT a deployable lead; compose-extraction follow-up NOT fired (ceiling is transductively inflated, VET "not a slam-dunk"). cell 89514aa01. **DIRECTOR PATTERN CORRECTION: over-framed 2 raw FULLs as promising (Hopfield "first win", graph-spectral "lead") -> both VET-deflated. NEW RULE: raw FULLs are PRELIMINARY-pending-VET, NO lead/win framing until skunkworks CONFIRM.** HONEST PHASE-2 STATUS: ALL capacity levers (Hopfield/graph-spectral/V(D)J/RNS) re-scope DOWN under VET, NO deployable capacity win; additive 0.128 remains best; beating it likely needs a fundamentally different inductive-EXTRACTION mechanism OR grounding (new exogenous info), NOT a capacity trick. Still-open (not yet deflated): grounding-improves-REASONING test (authoring a09e96ee), 10x-N scale-validation (GPU running), Topic-B levers (consistency-against-invariants, active-sampling).
- **A/V(D)J segment-library + clonal retrieval FULL = MEASURED_CAPACITY_MARGINAL (3 seeds): NO LEVER. SEG_SOFT 0.0178 vs SEG_HARD 0.0096 (soft-beats-hard 0.008 < 0.010 bar) vs same-cost MONO 0.4135 (~23x below). Measurable (better than RNS codes-absent) but monolithic dominates, no deployable clonal lever. Honest negative, banked. cell e7947f695.**
- **B/mammal-allometry grounding fair-test = MIDDLE_BAND GROUNDING_REDUNDANT (FULL, 5 seeds): A+B 0.597 vs A 0.605 (-0.008); best-lam +0.018, lifespan-only. 2nd grounding-redundant (periodic confounded; mammal independent-but-taxonomy-strong via phylogenetic conservatism). PATTERN: grounding adds little where the relational graph already predicts the attribute. STILL-UNTESTED fair case = domain where graph is independent AND UNINFORMATIVE about the attribute. cell 4efbebf3b.**
- **A/10x-N-fixed-support scale-validation of the milestone: authoring->GPU (a5744893). Closes the VET-flagged untested "N-invariance beyond 1.5x" claim.**
- HONEST META: 3 levers tested, all re-scoped DOWN on the real task (Hopfield fidelity-not-retrieval, spectral correlation-hurts, grounding-redundant). Mapping what does NOT deploy = narrowing honestly. DISCIPLINE WIN: held smokes, VET corrected the director's Hopfield over-frame (whipsaw). Store-sync pending (user-auth): milestone-CG + Hopfield-MM + negatives atoms.
### ========== END 2026-07-14 ~01:10Z ==========

### ========== 🧭 2026-07-13 ~23:35Z TRACK-A/B RESULTS + CONVERGENCE ==========
**Two-track state (USER-affirmed: Track A = relational capacity optimization; Track B = grounding foundation; run BOTH in parallel).**
1. **TRACK A leading lever DEAD -- RNS/CRT residue coding = NOT_DEPLOYABLE_CODES_ABSENT (verified off-disk data/exp_map_builder_residue_module_ceiling_v1/metrics.json, full 3 seeds, 623s).** RNS_CLEAN=0.0008 (at RANDOM floor 0.0003) vs MONO_MATCHED (same 21M cost, d4579)=0.4660 -> clean-mono=-0.465 (~600x below same-cost monolithic; not decode-limited, clean-noisy=0.0002). Oracle FIRED (mono 0.466), pos_controls/broken clean. => arbitrary entity IDs carry NO exploitable numeric/residue structure; the grid-cell/residue lever cannot grab it.
   **CONVERGENCE INSIGHT (load-bearing, strategic): the reason the cheap-capacity trick failed is that our entity codes are STRUCTURELESS arbitrary labels -- you cannot cheaply compress randomness. The capacity IS reachable (MONO_MATCHED 0.466 >> current 0.137) but only via (expensive) monolithic dimension. Cheap high-capacity codes REQUIRE structure to exploit -- and STRUCTURE is exactly what GROUNDING (Track B) provides (measured attributes ARE structure). So Track A and Track B CONVERGE: grounding is not only about meaning, it is what unlocks efficient capacity. => do NOT keep hunting exotic structured-code capacity tricks (backups Kronecker/block factorizer will fail the SAME way on structureless codes); route the capacity payoff THROUGH grounding. NB deflate: strong inference from 1 decisive cell + the per-relation reframe, not iron-clad -- but it is the right read.** (Also nuance: MONO at moderate d4579/21M gives 0.466 ORACLE -- monolithic-at-moderate-dim may be less-undeployable than the 8192 framing implied, BUT prior dim-sweep showed the compose MECHANISM cashes in a shrinking fraction, so realized-magnitude gain is unproven; low-value to chase vs grounding.)
2. **PER-RELATION REFRAME = PARTIAL, confirms at-SOTA-on-winnable (data/exp_anchor_per_relation_info_ceiling_cskg_v1/metrics.json, full, 2 seeds [7,13] -- caveat).** DETERMINED relations (tph<=2.0, only 9.2% of queries): best 0.2165 vs oracle 0.2048, frac_of_ceiling 1.057, in SOTA band [0.18-0.22] = additive IS at-SOTA on the winnable slice. UNDERDETERMINED (90.8%): 0.1299 vs oracle 0.1352, capped<=0.13=False (NOT saturated -- small residual headroom). unwinnable_gap_share=0.965 = 96.5% of the gap-to-SOTA is structurally-unwinnable one-to-many, NOT substrate weakness. Reproduces v1 (ANCHOR 0.1275 vs 0.1282). Controls fired; guard-fix pop_at_floor=True.
3. **MILESTONE PASSED (verified off-disk data/exp_additive_map_acceptance_gate_v1/metrics.json, full 3 seeds [7,13,17], 2607s): ACCEPTANCE_PASS_ADDITIVE_MAP_REPRODUCES_VET.** Live hdlab.additive_map.AdditiveKGMap reproduces VET EXACTLY: mean ANCHOR_COMPOSE MRR 0.12821 vs VET 0.12821 (in band [0.10,0.16]); fires_vs_random True (rand 0.00048); relation_signal_vs_scramble True (scr 0.00984); persist_reload_ok True; coord_source=learned_sgd_kge_anchor1. => the proven inductive-reasoning mechanism is now a LIVE, PERSISTENT, VALIDATED substrate capability = SHORED UP. Persistence works -> downstream re-scores off saved codes (CPU) not re-fit (GPU) = re-fit tax GONE. HONEST: construction-milestone (capability now live+reproducible), NOT a magnitude leap (still 0.128, at-SOTA-on-winnable). Skunkworks VET = **CONFIRM (chain-grade), +1 CG atom filed (needs_orchestrator_store_sync).** Genuine LIVE fit proven NOT a replay: mean 0.12820 = exact mean of 3 distinct live per-seed [0.11692,0.13797,0.12973]; seed-7 FLOAT-DRIFTS vs VET by 1.5e-5 (device summation order) w/ bit-identical cardinalities = 43min real SGD, a replay would be bit-identical. persist_reload_ok = REAL round-trip (save safetensors+index -> load on cpu -> allclose(score_all) + entity_to_idx eq; caveat: persisted dir on remote, not synced local). Controls fire (rand 264x, scr 13x, all 3 seeds), arena leak-free. VET framing (honest, banked): ACCEPTANCE_PASS = CONSTRUCTION/INTEGRATION + persistence proof, NOT a new capability; 0.128 (hits@1~0.05) MEDIOCRE absolute, unchanged. Open lane = Phase-2 absolute-capability optimization (= the solution-test matrix now running: Hopfield-readout, graph-spectral, V(D)J, grounding-fusion + 10x-N scale-validation).
- TRACK B: periodic-table grounding cell (Phase 1) SHIPPED to remote_cpu + PREVIEW (commit a2e37aae8, exp_grounding_periodic_xchannel_fpe_v1, canonical run on remote_cpu now, VET pending -- hold mechanism story per whipsaw). PREVIEW verdict = MIDDLE_BAND / GROUNDING_REDUNDANT_WITH_RELATIONAL. **PROVEN (the core grounding-mechanism question = YES): measured numbers ARE VSA-wireable into the glass-box bind/bundle algebra -- FPE + level-code both encode cleanly, decode-degradation did NOT bite (raw rel-err 0.005, cleanup 0.001 at bundle-5, FPE distance-decay Spearman 0.999), B grounds real properties alone (0.31 over mean), ablation machinery proven (synthetic exog positive control A+B beats A gap ~1.0).** BUT B NOT load-bearing OVER the relational channel HERE (A+B 0.567 vs A_alone 0.536, +0.031 only) -- because the periodic table's group/period/block structure was REVERSE-ENGINEERED FROM those properties (Risk#1 confound: symbols already secretly encode the measurements). => a CONFOUNDED first testbed; the WIRING is proven, the VALUE-ADD test is undercut by the domain. **FAIR test of B-adds-over-A needs an A-INDEPENDENT domain (commonsense concepts where is-a/synonym edges are NOT derived from mass/density -- Phase 1b / Phase-2 FoodData/PubChem).** Connects to convergence insight: grounding adds value precisely where symbols LACK structure (= our actual arbitrary CSKG graph), which is exactly the fair-test case. Plan: notes/research_grounding_foundation_build_plan_2026-07-13.md. Phase 0 (CSKG reachability audit) available parallel.
- 10-DRILL 5x PROGRAM (USER-requested) IN FLIGHT: Topic A (cheap capacity+structure) + Topic B (grounding w/o LLM), x5 lenses each. Parent deliverables landing. B-bio DONE = minimal grounding loop (sensor->comparator->irreversible-asymmetric-consequence); kinetic-proofreading (double-independent-check-then-discard write-rule) = most transferable lever. Grounding-wildcard + quantum sub-scans converge on GROUNDING-BY-CONSISTENCY-AGAINST-KNOWN-LAWS/INVARIANTS + ordinal-before-absolute as cheapest symbol-only path; DPI/Information-Causality PROVES closed-system internal reshuffling cannot manufacture referent-correspondence (grounding needs a real external channel). SYNTHESIZE per-topic when each 5-set lands.
- CPU idle briefly (RNS done 10min); periodic-table cell refills it on ship.
### ========== END 2026-07-13 ~23:35Z ==========

### ========== 🧭 2026-07-13 ~21:30Z VET CORRECTIONS (corrected/extended by 23:35Z block above) ==========
**Landed-VET (skunkworks, off-disk recompute, both A5-gated atoms filed needs_orchestrator_store_sync) OVERTURNED two things the 20:00Z block records. Trust these over the 20:00Z text.**
1. **RECIPROCAL cold-rescue = HARD_FAIL, NOT "PARTIAL WIN".** The cold 35x-lift is a SINGLE-SEED ARTIFACT: seed 7 (n=15 cold queries, recip 0.0379) carries the entire 0.0148 aggregate; seeds 13/17 near-floor (0.0046, 0.0018); cross-seed cv=1.11; the leak-free recip>scramble control passes only 2/3 seeds and INVERTS in seed 17 (recip 0.0018 < its own scramble 0.0025). Meanwhile inverse-bundling ROBUSTLY REGRESSES the working buckets (d4_7 -0.023/-0.009/-0.010, all 3 seeds negative). => a LOSING modification; inverse-bundling is a DEAD cold-rescue lever. The BROKEN_TEST_CONTROL_BEATS_POP mis-fire IS a confirmed false F.4 guard artifact (guard baseline was a reciprocal-only scramble retaining the working anchor base, vs a structurally-zero POP), but fixing the guard does NOT rescue the result (cold 0.0148 < 0.020 bar anyway). **=> The TAIL-DEGREE-GATED reciprocal fold-in is CANCELLED (gating a losing mechanism is pointless); exp_dev stood down off it.** Revival (if ever): ~200+ cold queries + per-seed 3/3 scramble-control + full-scramble guard baseline + likely a DIFFERENT mechanism (e.g. relation-type prior for degree-1), NOT inverse-bundling.
2. **CLOSED-FORM STRICT_DEAD = genuine BUT FAMILY+BUDGET-SCOPED, not unconditional.** Confirmed genuine at k=24 (closed-form anchor 0.0097 = 7.6% of learned 0.128; scramble at floor; 3-seed cv~0.05; planted-grid self-test STRICT_VIABLE => harness answerable, not a test-design failure). BUT the closed-form family's OWN transductive INFO-ORACLE also collapsed (0.0087 at k=24) => at k=24 the closed-form can't even REPRESENT training edges = a representation-BUDGET limit, not proven a family wall. "learned k=24 essential / strict path closed" holds FOR THIS FAMILY+BUDGET ONLY. **=> STRICT-PATH REVIVAL PROBE LANDED (full, 3 seeds, ~9.7min, verified off-disk data/exp_anchor_compose_closedform_budget_sweep_cskg_v1/metrics.json) = STRICT_DEAD_UNCONDITIONAL_ACROSS_BUDGET. CF_ORACLE(k) FLAT across the whole sweep: k24=0.0087 -> k64=0.0094 -> k128=0.0096 -> k256=0.0102 (rise 0.0015 << 0.020 needed; best 0.0102 vs viable-bar 0.0687; plateaus ~25x above random floor but ~13x BELOW the additive oracle 0.137). CF_ANCHOR best 0.0111@k64 = ~12x below additive 0.128. Monotone but ~0.0005/doubling -> never reaches viable in any realistic budget. Controls sane (reproduce_k24=True, oracle_fires_kmax=True, broken=False). => the strict/pure glass-box coord path is DEAD UNCONDITIONALLY (upgraded from family+budget-scoped); LEARNED coords are ESSENTIAL; functional AdditiveKGMap STANDS as the answer; strict-path line CLOSED FOR GOOD. Consistent with READOUT_IS_THE_LIMITER. (Optional: batch-VET-audit the unconditional call, but the base was already VET-confirmed genuine + the trajectory makes it safe.)**
- Also collected: code_vs_readout diagnostic = READOUT_IS_THE_LIMITER (full, 3 seeds), BUT honest read = code<->readout COUPLING (NA native-code/add-readout floors 0.0004; AN add-code/native-readout 0.0062 < NN 0.0231; only matched AA=0.137 works) = additive advantage is a JOINT matched-pair property, NOT a cheap "swap the native readout" fix; ATTRIBUTION CAVEAT (mechanism self-test attribution_selftest_verdict=INCONCLUSIVE) so the "readout is THE limiter" causal label is SUGGESTIVE not clean (arms trustworthy, pos_controls fire). Confirms native can't match; path-confirming, not path-changing.
- GPU order (verified off-remote 21:14Z): per-relation RUNNING (started 21:11Z), acceptance-gate PENDING next (USER declined preempt -- "just let them run"). Acceptance gate = still THE milestone validation; runs after per-relation.
### ========== END 2026-07-13 ~21:30Z VET CORRECTIONS ==========

### ========== 🧭 2026-07-13 ~20:00Z END-OF-DAY COMPACTION-READY (corrected by 21:30Z block above; supersedes 07:00Z and below) ==========
**TODAY: the inductive map-builder went from PROVEN MECHANISM -> a LIVE, backed-up, hardened SUBSTRATE CAPABILITY; the optimization map is RESOLVED; the strict-glass-box path is CLOSED (learned coords essential).**

**THE CAPABILITY (the shore-up milestone):** `hdlab/additive_map.py::AdditiveKGMap` -- the VET-confirmed additive inductive map-builder promoted to a LIVE, PERSISTENT, maintained substrate capability. COEXISTS with the multiplicative KGStore (CLS multi-memory-systems analog; ZERO existing files modified -> zero CERT-584/585 regression). API: fit / compose_entity / insert_entity(row-append) / score_all(direct-distance) / save+load(safetensors). COORD-SOURCE SEAM: `CoordinateSource` interface, `LearnedSGDCoordinateSource` default = swappable. Unit-test PASS. Committed + ON REMOTE (SCP'd to C:/dev/hd-instrument) + PUSHED TO ORIGIN (169311df8+17cfcb5d1). ACCEPTANCE GATE (exp_additive_map_acceptance_gate_v1) QUEUED on GPU -> fits live on CSKG, must reproduce VET MRR ~0.1282 (band [0.10,0.16]) + PERSISTS codes one-time (kills the re-fit waste for all downstream). Design note: notes/research_additive_map_builder_integration_endgame_2026-07-13.md. Memory: [[project_additive_map_builder_integration_endgame_functional_plus_strict_via_shared_api_2026-07-13]].

**THE MAP -- FULLY RESOLVED (all VET-confirmed):**
- ADDITIVE magnitude: composer/scorer SATURATED (peel/hard-neg = dead levers, best arm 0.138 ~= its oracle). The 0.14 aggregate is a RELATION-MIXTURE INFO-CEILING artifact NOT a capacity wall: on DETERMINED relations we ALREADY reach 0.177-0.219 = IN commonsense-SOTA band (0.18-0.22); one-to-many relations cap ~0.10 EVEN AT ORACLE (unwinnable by construction). => additive is essentially at-best-in-class where definable. (per-relation reframe cell formalizes this, GPU-pending; magnitude VET MEASURED_MECHANISM.)
- NATIVE multiplicative store CANNOT match additive: write-rule=CLOSED (VET HARD_FAIL, capacity saturation at 25-350x over cliff, not the lever); DIMENSION relieves the CAPACITY/oracle ceiling (0.023->0.78@8192, REAL not artifact) BUT the compose MECHANISM captures a shrinking fraction (0.014->0.052) + O(n_dim^2) not deployable; CODES closed (code-family sweep CODES_DONT_STRUCTURED_HURTS_LEARNED_NEEDED -- structured glass-box codes don't carry the geometry).
- STRICT glass-box path DEAD (both non-learned options): structured CODES (CODES_DONT) + CLOSED-FORM COORDINATES (STRICT_DEAD_CLOSEDFORM_NEAR_RANDOM, spectral+ALS collapses to random on real CSKG). => LEARNED coords are ESSENTIAL. FUNCTIONAL (learned-k24 AdditiveKGMap) is THE answer; strict path documented-closed. (USER glass-box ruling: FUNCTIONAL reading accepted -- k24 additive is transparent in structure/readout/compose, learned only in coords.)

**AGENT HARDENING (USER-directed: "too many cell errors"):** exp_dev cell-authoring hardened at the SOURCE. `.claude/agents/exp_dev.md` Gate F (the LOADED hdi_exp_dev system prompt) + `experiments/_validity_preflight.py`, now ENFORCE (per-check default, director sign-off): F.1 self-test must exercise the REAL substrate code path (not synthetic-only), F.2 substrate-signature bind (inspect.signature), F.3 LOCAL<->REMOTE code-drift (prefer base/portable signatures -- root cause of the init_entities GATE_FAIL), F.4 guard-baseline-vs-arena-floor (root cause of false BROKEN_TEST_CONTROL_BEATS_POP). New declared-and-failing cells BLOCKED pre-ship. Pushed origin. Memory [[feedback_harden_exp_dev_selftests_must_exercise_real_api_too_many_cell_errors_USER_2026-07-13]]. VALIDATED live: the closed-form cell passed the new gates cleanly.

**IN FLIGHT (~20:00Z):** GPU: codes/readout isolation RUNNING (diagnostic: is native gap codes-vs-readout -- now somewhat moot since native isn't the path); per-relation reframe + acceptance-gate PENDING (GPU, each re-fits ~3h). CPU: IDLE (strict-path probing closed; reciprocal + closed-form both landed). RECIPROCAL cold-rescue LANDED = PARTIAL WIN: cold bucket 0.00004->0.0148 (35x above random 0.00043, scramble-controlled, construction_fired) = inverse-table bundling GENUINELY rescues cold, BUT modest (<0.020 clean bar) + slight overall regression -0.0009 (adequate-bucket dilution). FIX = TAIL-DEGREE-GATED reciprocal (apply inverse-bundling ONLY to cold/low-support entities) -> fold into AdditiveKGMap as the cold-tail coord/compose option. NB verdict BROKEN_TEST_CONTROL_BEATS_POP = FALSE (3rd instance of the POP-guard mis-fire; cell predates F.4 hardening which now blocks this class). NOTE the recurring re-fit tax (each cell re-runs the 3 additive SGD fits ~3h) is what the AdditiveKGMap PERSISTENCE fixes going forward.

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) COLLECT reciprocal (data/exp_anchor_compose_reciprocal_cold_rescue_cskg_v1/metrics.json -- sync if not local) + the ACCEPTANCE GATE = THE capability validation (data/exp_additive_map_acceptance_gate_v1/metrics.json: reproduces 0.1282 live? persists codes? = capability SHORED UP) + codes/readout (data/exp_kg_store_code_vs_readout_isolation_v1/metrics.json, diagnostic native codes-vs-readout) + per-relation reframe (data/exp_anchor_per_relation_info_ceiling_cskg_v1/metrics.json, formalizes additive at-SOTA-on-determined + query-fraction) as they land; closed-form already landed STRICT_DEAD (data/exp_anchor_compose_closedform_coord_cskg_v1); (3) VET (skunkworks) the closed-form STRICT_DEAD (confirm genuine, not an under-powered closed-form) + reciprocal + acceptance gate; (4) ON acceptance-gate PASS -> the AdditiveKGMap capability is VALIDATED-LIVE = SHORED UP; the proven inductive reasoning is now something the substrate HAS; (5) if reciprocal cold-rescue holds -> fold the inverse-bundling into AdditiveKGMap (cold-tail fix); (6) STRICT PATH CLOSED -> no more strict probing (functional stands, documented); (7) STORE-SYNC any new atoms (needs_orchestrator_store_sync -- needs DIRECT USER AUTH for the push). CPU/GPU load-balance: persistence (acceptance gate) converts future GPU re-fits to CPU re-scores.

**DISCIPLINES BANKED TODAY (memory):** OPTIMIZE-THEN-NATIVIZE + glass-box-realizable guardrail [[feedback_optimize_then_nativize_optimized_generalization_defines_substrate_requirement_USER_2026-07-13]]; FOCUS ON SUBSTRATE+OPTIMIZATION not presentation [[feedback_focus_on_substrate_and_optimization_not_presentation_artifacts_USER_2026-07-13]]; NO ORIGIN PUSH without DIRECT USER AUTH (agent-relayed auth does NOT satisfy the harness -- it blocked a push authorized only via SendMessage; USER must authorize in-session directly); exp_dev HARDENING; 3 director-predictions corrected by adversarial VETs today (write-rule-didnt-run [Fix#28 stale-local], hard-neg-lifts-ceiling [2-seed noise], separability-artifact [was real capacity relief]) -> STATE READS AS PREDICTIONS, hold conclusions for VET; local<->remote code drift real (repo thousands of commits ahead of runner). CRONS a40824ce+194ceab3 active.
### ========== END 2026-07-13 ~20:00Z END-OF-DAY ==========

### ========== 🧭 2026-07-13 ~07:00Z MILESTONE (superseded by 20:00Z above; kept as detail) ==========
**THE ARC HIT A GENUINE INDUCTIVE-GENERALIZATION RESULT: ANCHOR_COMPOSE = VET-CONFIRMED CHAIN_GRADE (CG +1). The substrate infers held-out-ENTITY edges -- entities ENTIRELY ABSENT from train -- via a ZERO-training composed code, where per-entity fits score LITERAL ZERO. This is the mechanism the deep prize needs. Honestly bounded: MECHANISM PROOF, LOW absolute magnitude (not a capability win yet).**
**MAGNITUDE CALIBRATION (drill abd876b4, P_deflated 0.55): vs the CORRECT task class (commonsense-KG INDUCTIVE = InductivE/ConceptNet MRR 0.18-0.21 / Hits@10 0.29-0.50; NOT curated-ontology WN18RR/FB15k MRR 0.37-0.73 = apples-to-oranges), our 0.128 MRR / 0.278 Hits@10 = ~60-95% of SOTA depending on metric = WITHIN STRIKING DISTANCE (not orders off), and ZERO-TRAINING vs their trained models. GAP-CLOSE LEVERS (quantified, cheap, reuse harness, HELD behind Stage-0 native fork): (A) SIC-peel SEQUENTIAL decode of the support bundle (replaces flat mean; brain=theta-gamma slotting+predictive-coding; field=VSA resonator 8x component-recovery; P 0.40) + (B) hard-negative scorer refit (+0.02-0.04 MRR, P 0.45). note notes/research_inductive_map_builder_best_in_class_magnitude_levers_2026-07-13.md.**

**RESULT (canonical FULL, verified off-disk data/exp_anchor_compose_inductive_entity_cskg_v1/metrics.json, run_mode=full 12074s, seeds[7,13,17]):** a held-out entity's code = degree-invariant ADDITIVE bundle mean_i(X[h_i]+D[r_i]) over its SUPPORT edges (ZERO gradient training for the unseen entity). **INTEGRATION-STATUS HONESTY (verified in code 07-13, corrects an earlier overclaim): the cell uses EXPERIMENT-LOCAL fit/score modules (experiments/_kge_anchor1_fit.py fit_kge_anchor1 + experiments/_course_c_rotate_core_v1.py additive_direct_scores) -- it does NOT import hdlab/store.py or cleanup_family. this is a STAND-ALONE experiment on a CSKG fit, NOT wired into the live substrate KGStore. **DEEPER CORRECTION (integration drill 07-13, code-read): the live KG store is hdlab/kg_traversal.py::KGStore (NOT store.py=TraceStore) = FIXED-atom, HIGH-dim (~1-2k), MULTIPLICATIVE (Hadamard-bind) one-shot HEBBIAN memory w/ bilinear E@(W@key) readout + it holds the CERT-584/585 CHAIN_GRADE multi-hop capability (must not regress). ANCHOR_COMPOSE is the OPPOSITE regime on every axis: SGD-trained, low-dim k=24, ADDITIVE (TransE), direct-distance. So the native algebra is MULTIPLICATIVE not additive -- my 'native additive geometry' claim was WRONG. Integration needs a real BRIDGE, NOT a plug-in. THE DECISIVE FORK: does the NATIVE multiplicative bind + the same compose-support-bundle pattern ALSO do inductive generalization (=cheap native path, capability genuinely native) or NOT (=need additive adjunct bridge, costly/less-native)? INTEGRATE-FIRST before magnitude-optimize (tuning the additive recipe is wasted if the native path differs). STAGE-0 NATIVE-BIND TEST DISPATCHED (exp_dev a7537b09, remote CPU, reuses the confirmed held-out-entity arena + all controls, cleanup_family format-agnostic readout). This resolves 'active substrate capability vs stand-alone additive proof'. P_deflated 0.25. **STAGE-0 LANDED = HARD_PASS_NATIVE_BIND_INDUCTIVE (verified off-disk, run_mode=full, n_dim=1024, seeds[7,13,17]): the NATIVE multiplicative-Hebbian store (majority-sign bundle of its OWN Hebbian tail-recall + native bilinear readout, no additive/SGD, read-only no CERT-584/585 regression) DOES inductive generalization -- NATIVE MRR 0.0140 vs MEMORIZE 0.0003/RANDOM 0.0005/SCRAMBLE 0.0036/IDSHUF 0.0039, ORACLE 0.0231 fires 51.3x, native_margin 0.0135 vs HARD_PASS>=0.0113. => THE CAPABILITY IS ACHIEVABLE NATIVELY (in KIND), not only additive. BUT MAGNITUDE ~9x LOWER than additive (0.014 vs 0.128) AND the NATIVE ORACLE CEILING ~6x lower (0.023 vs 0.137) = the native fixed-atom random-bipolar representation carries far less recoverable signal for this task. HONEST PICTURE: substrate does the STRUCTURE natively, its representational capacity caps MAGNITUDE ~6x below the additive optimum. THE NATIVIZE CHALLENGE (quantified) = raise the native representational ceiling (n_dim / code quality), NOT just wiring. This was BONUS/non-gating per optimize-first, but directly informs nativize: after we optimize the additive mechanism, closing native-vs-additive representational gap is the real integration work.**
**🎯 UNIFIED FRONTIER SYNTHESIS (07-13, load-bearing): the whole frontier -- reach commonsense-SOTA ADDITIVELY *and* NATIVIZE the magnitude -- collapses to ONE problem: RAISE THE ACHIEVABLE CEILING (recoverable signal), NOT refine the composer. Evidence: (a) magnitude cell (aaa04dab) flagged flat-mean is ALREADY 93% of the additive oracle -> composer near-solved, +0.009 headroom; the SCORER (hard-neg) lifts the ceiling (synthetic 0.275->0.316). (b) native-ceiling drill (a70bcd83): the native 6x gap's DOMINANT LIMITER is the WRITE RULE (naive Hebbian ~0.14N), NOT dim/codes; a DECORRELATING closed-form glass-box write rule (PSEUDO-INVERSE/STORKEY) reports 3.5-14x, GO (not a wall), P0.30-0.35. TWO SYMMETRIC GLASS-BOX CEILING-LEVERS: additive=hard-neg fit (2x2 cell aaa04dab running GPU); native=decorrelated write rule (exp_dev a85559269 cheap decisive test, swap KGStore write rule + re-run native-bind, non-regressive to CERT-584/585, reuse existing hebb_vs_pseudoinverse cells). Composer solved; ceiling is the frontier; both sides GO within glass-box. This is the OPTIMIZE (additive) + de-risked-NATIVIZE (native ceiling) picture converged.** Held-out MRR ANCHOR=0.1282 (hits@10 0.278, hits@100 0.788, cv 0.068) vs ADDITIVE=0.00004/ONESHOT=0.00006 (per-entity memorize arms at 1/N FLOOR), RANDOM=0.0005, SCRAMBLE=0.0098 (relation-scramble must-fail fires 13x), ORACLE=0.1373 (ANCHOR ~= transductive oracle, even BEATS it seed13 +0.018 = closed-form additive mean rivals gradient-fit ceiling), oracle fires 284x. **VET (skunkworks a50b714e, adversarial, 3/3 off-disk) = CONFIRM CHAIN_GRADE, NO LEAK: support/query provably disjoint; NO alias/synonym/inverse shortcut (lexical relations filtered at CSKG ingest, core=29 commonsense rels, held entities tail-only + forward-only eval); win SCALES WITH SUPPORT-DEGREE (d2_3 0.06->d4_7 0.15->d8+ 0.13) = genuine-composition fingerprint; scramble kills it => the RELATION OPERATORS carry the signal not anchor identity/degree.**

**VET's HONEST CORRECTIONS (banked): (1) MECHANISM PROOF not capability win -- absolute MRR 0.128 is LOW; CG is on mechanism-existence + relational-attribution. (2) the prereg's global-degree low+mid-stratum gate is VACUOUS here (held entities have train-degree 0 -> tertiles [0,0]); LOAD-BEARING stratifier = by-SUPPORT-degree. (3) ORACLE is a SOFT (transductive) ceiling. LAST loophole still open: CROSS-ENTITY IDENTITY-SHUFFLE control (compose t from a DIFFERENT held entity's support -> predicted collapse to RANDOM if the win is t-specific) -- this is the final leak-closer.**

**IN FLIGHT (07-13 ~07:30Z):** FOLLOW-UP cells AUTHORED + self-test PASS + committed cfd0ffc94, SHIPPING via orchestrator a6b1121e to overnight_queue GPU (A first, gating): (A) **exp_anchor_compose_identity_shuffle_cskg_v2** = CROSS-ENTITY IDENTITY-SHUFFLE must-fail (8th arm: re-attributes each composed bundle to the WRONG held entity; MUST collapse <=0.20 or REFUTES the CG; self-test collapsed 0.0093), ~2.2h GATING -- **LANDED FULL = HARD_PASS_IDENTITY_CLOSED (verified off-disk, seeds[7,13], 5164s): IDSHUF mrr=0.0025 collapse_ratio=0.016 (collapses HARDER than scramble 0.0087, near random) while ANCHOR reproduces 0.1275, oracle 272x. => win is provably ENTITY-SPECIFIC; ALL refutation vectors shut (support-query + relation-scramble + identity-shuffle); CHAIN_GRADE FULLY VALIDATED, no open loophole. Needs skunkworks atomize (batch w/ scaling VET).** (B) **exp_anchor_compose_scaling_ladder_cskg_v3** = 3 rungs (k12/sf.50 base-reproduce -> sf.25 halfsupport -> k8/bigN sparsecore), bands HOLDS>=0.40/GRACEFUL>=0.15/COLLAPSE<0.15 retention, = the does-it-scale answer. **LANDED FULL = SCALING_HOLDS (verified off-disk, 17087s): base_reproduced True (0.1269 vs 0.1277 Gate D); r0_base retention 1.0; r1_halfsupport (sf0.50->0.25) retention 1.006 = HALVING support does NOT hurt; r2_sparsecore_bigN (k12->8, N 25752->38772) retention 0.939 = HOLDS at bigger+sparser. All 3 rungs HARD_PASS. **VET af61dc53 = CONFIRM (fair, off per_seed arm_hits): support-halving CONSERVATIVE (eval got harder, mass to lower bins, within-bin mrr preserved), r2 bigN fair (1.5x candidate set), memorize floor + oracle fires per rung, cv<0.09. +1 CG (scaling) + 0 CG (idshuf confidence upgrade), atoms filed. HONEST SCOPE (VET caveat): 'scales' proven for SUPPORT-DEGREE-ADEQUATE held entities across halved-support + 1.5x N; the TRUE large-N test (grow N ~10x HOLDING per-entity support-degree fixed) is NOT yet run (1.5x confounded w/ sparser core) -- N-invariance beyond 1.5x is MECHANISM-INFERRED not directly measured; COLD/degree-1 held entities at random floor = structural boundary (support-degree-1 relation-prior fallback = open lever). LOW_ABS (mechanism proof, not capability win). FOLLOW-UPS (queued behind native-capability fork): 10x-N-fixed-support rung + cold-entity fallback.** Landing monitor bv20o68y4 (both, run_mode=full). ON LAND -> skunkworks landed-VET. EVAL-BIAS debias 3/3 LANDED = aggregate MIDDLE_BAND (SETTLED, no whipsaw -- all 3 seeds consistent): beat-POP SURVIVES degree-matched candidates (dm_margin +0.038/+0.018/+0.040, ALL positive, mean +0.032 = glass-box still beats frequency under the fair pool) but NOT a clean REDEEMED_WIN -- partial_r mean 0.185 (reduced from pooled 0.29 = hubness confound MATERIAL but stays >0.15 leak-free bar), dm_margin not uniformly >=0.02. => the earlier rotation held-out-EDGE MIDDLE_BAND beat-freq is DOUBLY-CONFIRMED (VET'd + survives degree-matching, modestly); leakage partly-confound-partly-residual. SECONDARY to the ANCHOR_COMPOSE CG headline. GPU idle (ladder ships shortly). Landing monitors b4z4l7gf3 (debias) -- re-arm for the new ladder anchors on exp_dev return.

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) COLLECT the IDENTITY-SHUFFLE control (data/ exp_dev's anchor) -- if it COLLAPSES to ~RANDOM = last leak closed, CG stands FULLY; if NOT = escalate (win not entity-specific); (3) COLLECT the SCALING LADDER -- does ANCHOR-margin HOLD as support_frac/k_core/N stress it (support-degree-stratified curve)? = the does-it-scale answer; (4) COLLECT debias seed_23 -> aggregate 3-seed REDEEMED_WIN/MIDDLE verdict on whether rotation's fair beat-POP survives degree-matching; (5) STORE-SYNC the batch (orchestrator): CG-anchor-compose atom + multi-seed-MM-VET atoms + graph-P3 + eval-bias + the meta 'global-degree-stratification-vacuous-for-heldout-entity' guard (all needs_orchestrator_store_sync on-disk); (6) IF ladder holds -> best-in-class optimize toward higher absolute magnitude (the capability-win phase); the map-builder is now the substrate's inductive reasoner -- wire deeper into KGStore. DISCIPLINES held this session: WHIPSAW (held the HARD_PASS memsmoke as PREVIEW, held the FULL for VET, VET corrected my degree-stratification + magnitude overclaim); VERIFY-OFF-REMOTE (caught a false ship-stall, the memsmoke-vs-full confusion, 2 inconclusive forks); INFO-CEILING-BEFORE-FIX (the fork was an arena-metric ceiling not underfit -> filtered-MRR fixed it, oracle fires 32.8x); NO LOCAL COMPUTE. Cron a40824ce+194ceab3 active; watchdog research stale-threshold raised 20->60min (source edit, generation path is stop-hook -- testbed verify).
### ========== END 2026-07-13 ~07:00Z MILESTONE ==========

### ========== 🧭 2026-07-12 ~23:10Z COMPACTION-READY (superseded by 07-13 MILESTONE above; kept as detail) ==========
**REASONING RESULT (multi-seed PROVEN) + the DECISIVE FORK RE-OPEN (1st run inconclusive-underfit; full-fidelity re-run v2 RUNNING on GPU, oracle-gated, ~3.5h).**

**RESULT (banked, verified off-remote, 3-seed cv):** glass-box ROTATION (RotatE / FHRR phase; PP-275 primitive) BEATS FREQUENCY (POP) on the fair low+mid-degree stratum, ROBUST 3/3 seeds (rotate 0.077/0.092/0.095 vs POP 0.044/0.056/0.050 = +0.033-0.045; scramble collapses ~0.018; seed-cv passes; oracle fires) = the arc's core bar CLEARED. BUT MIDDLE_BAND, not clean: additive-TransE BEATS rotate on the fair stratum 3/3 (0.104/0.109/0.104) -- rotation NOT best-in-class (director's rotation-is-the-fix thesis disconfirmed; rotate only wins high-degree + agg-MRR). Leakage g_backdoor=False (geom-poprank r~0.28-0.31 stable) BUT (a) fair-stratum stratification IS the field's leakage remedy so the fair beat-POP is trustworthy (aggregate MRR dropped), (b) degree-community drill: standard link-pred eval is ITSELF degree-biased (ICML2025 arXiv:2405.14985) -> leakage may be partly an EVAL-PROTOCOL artifact, cheap to check (degree-corrected neg-sampling). Data: data/exp_course_c_rotate_cskg_l2_seed_{7,17,23}_gpu1024_v1/metrics.json. NOTE dim4096 tier DROPPED (primary metric is fpe_dim-INDEPENDENT so gpu1024=definitive; dim4096-on-CPU just times out).

**VET SHARPENING (skunkworks a7688ea3, 07-13, all numbers reproduced bit-for-bit, +1 MM atom filed needs_orchestrator_store_sync):** the fair beat-POP is CONFIRMED robust (not a split artifact, must-fails 7/7 fire) BUT the honest framing is STRONGER than "rotation not best-in-class": on the FAIR test **additive BEATS rotation 3/3 (rot-add -0.018/-0.027/-0.009)**; rotation's ONLY additive-beat is the HIGH-degree stratum (+0.074..+0.109) that CO-MOVES with the backdoor r~0.29 => **"RotatE>TransE" is DEGREE-CONFOUNDED and FLIPS to TransE>RotatE on the fair test.** My "additive at fair info-ceiling" read was TOO STRONG (additive/oracle 0.97 seed17 but 0.84/0.86 seeds7/23 = 11-16% residual headroom) -> additive is the genuinely BETTER FAIR OPERATOR, not saturated; rotation is a DIFFERENT operator winning only where popularity leaks. g_backdoor NOT cleanly dismissable (r~0.29 + high-degree-concentrated beat = consistent/material) -> the eval-bias cell (a21dc63f) must adjudicate (VET predicts rotation's additive-beat does NOT survive debiasing). **DIRECTIVE FOR THE PIVOT: do NOT scale rotation; the mechanistically-indicated code is DEGREE-INVARIANT / ADDITIVE-hybrid. When building ANCHOR_COMPOSE, bind with the ADDITIVE/degree-invariant operator, NOT rotation. HAKE prior LOWERED (it is rotation-family + phase; may inherit the high-degree/popularity artifact) though still worth collecting for the IS_A-hierarchy modulus question + the r-increase HARD-FAIL guardrail.**

**THE DECISIVE FORK -- STILL OPEN (1st run INCONCLUSIVE_ORACLE_UNDERFIT; full-fidelity re-run v2 RUNNING on GPU):** held-out-ENTITY inductive probe -- does the geometry GENERALIZE to entities ENTIRELY ABSENT from train (margin >=0.05 over random-code) or MEMORIZE (<0.02). 1st run (exp_course_c_heldout_entity_inductive_probe_gpu1024_v1, commit 1f9544295) = INCONCLUSIVE_ORACLE_UNDERFIT: ROTATE+ADDITIVE held-out hits@10 both 0.0000 (LOOKS like memorize) BUT the ORACLE positive control did NOT fire (0.0123, needs >=0.10) -> the full-scale fit UNDER-TRAINED (the probe moderated epochs for a CPU budget), so the zero arms are UNINTERPRETABLE. The oracle-fire GUARD correctly prevented a false "memorize" call. Mechanism self-test fired clean at tiny scale (oracle 0.307). RE-RUN v2 SHIPPED + RUNNING on GPU (anchor course_c_heldout_entity_inductive_probe_gpu1024_v2, commit 9eb299eba, base cell exp_heldout_entity_inductive_probe_cskg_v1.py un-moderated ep200->500 + n_neg64->128, monitor bnhbx4tfn, ~3.5h from ~23:50Z, resumable ckpt/20ep; ORACLE FIRED at remote self-test 12.1s = the full run should reach interpretable fidelity). ORACLE-GATED outcomes: oracle fires + arms~0 -> PROVEN MEMORIZE; arms +0.05 over random -> GENERALIZE; oracle can't fire even at max fidelity -> INCONCLUSIVE_ORACLE_CEILING (= per-entity KGE can't support held-out-entity inference at scale, itself points to the factorized map-builder; ep=750 headroom remains). Do NOT misread the 2026-07-09 HARD_FAIL_CG_MEMORIZED_SEARCH (OLD SR-code cell) as this verdict. THIS SETS THE NEXT PHASE.

**STRATEGIC CONVERGENCE (4 drills, all landed):** optimizing KGE FORM (rotation/additive/HAKE) is necessary-but-NOT-sufficient to SCALE -- per-entity fits MEMORIZE. Scalable answer = a TEM-style STRUCTURE/CONTENT-FACTORIZED MAP-BUILDER (relations = REUSABLE operators, content bound in) WIRED INTO the native store -- KGStore ALREADY has the bones (storage=map=reasoner: bind=query=ingest; factorization key=E[s]*R[p], CERT-585 chain-grade; consolidation replay_cycle exists). Form+scaling+storage = ONE question = FACTORIZATION. Drill notes: research_next_form_decision_from_first_result_hake_hybrid_2026-07-12 (build HAKE + r-increase HARD-FAIL guardrail); research_does_it_scale_..._scaling_law_2026-07-12 (the generalize test above + scaling-ladder Anchor2 + handoff exp_dev_handoff_...); research_storage_is_the_map_..._2026-07-12 (3 integration cells: promote KGStore E/R->complex unit-phase=FHRR bind, refine via consolidation); research_kg_degree_community_diagnostic_2026-07-12 (eval-bias reframe + CSKG is SCHEMA-POOR = headwind for factorization + the 8-item graph diagnostic).

**IN FLIGHT (07-13 pickup, updated):** **FORK RE-RUN v2 LANDED = INCONCLUSIVE_ORACLE_UNDERFIT (2nd time; verified off-disk data/exp_course_c_heldout_entity_inductive_probe_gpu1024_v2/metrics.json, wall 11110s).** Held-out hits@10-vs-ALL-25.7k: ONESHOT=0.0, ADDITIVE=0.0 (per-entity fits give unseen entities USELESS codes = memorize-consistent, as the inductive-KGE literature GUARANTEES by construction), RANDOM=0.0004, ORACLE_TRANSDUCTIVE=0.0143 (needs >=0.10, DID NOT FIRE). **DIAGNOSIS = ARENA INFO-CEILING, NOT epoch-underfit: v1 oracle 0.0123 (ep200) -> v2 0.0143 (ep500), 2.5x epochs barely moved it -> the rank-vs-all-25.7k hits@10 metric is too strict (even perfect codes ~0.014); the 0.10 gate was copied from the easier held-out-EDGE arena (oracle fired 0.107 there) WITHOUT computing THIS arena's ceiling.** Do NOT re-run more epochs. **METRIC-FIX DISPATCHED (exp_dev a3f39aad): compute the held-out-entity info-ceiling under FAIR KGE-standard metrics (FILTERED-MRR, hits@{1,3,10,100}), set the oracle-fire gate <= a fair fraction of the ceiling, fold into ANCHOR_COMPOSE so ONE FULL run reveals the firing metric + whether the composed bundle beats the 0.0 memorize arms; keep degree-stratified, hold at READY.** NB the fork now empirically confirms the MEMORIZE side (per-entity arms 0.0) -- the pivot is warranted; the fix is about making the arena INTERPRETABLE so ANCHOR_COMPOSE's win is measurable. **METRIC-FIX DONE (exp_dev, commit 06c50feac): recomputed off-disk = ORACLE mrr 0.01280 vs RANDOM 0.00039 = 32.8x separation -> THE ARENA IS ANSWERABLE, the hits@10-vs-all-25.7k metric was just BLIND to it (held-out entity structurally constrained by its own sparse edges). Cell upgraded to FILTERED hits@{1,3,10,100}+MRR rank-vs-ALL (no sampled-neg -> no degree-bias reintroduction), scale-free oracle-fire gate (ORACLE>=3x RANDOM mrr; we have 32.8x), ANCHOR bands = fractions of MEASURED oracle headroom. Self-test PASS (oracle fires under MRR 69.6x, anchor_margin 0.391, scramble must-fail fires). ONE FULL run now yields firing-metric + mechanism win.** **>>> ANCHOR_COMPOSE FULL RUNNING ON GPU (VERIFIED off-remote 03:36Z: overnight_queue status=running, self-test passed 18s, timeout21600, seeds[7,13,17], k24/ep500, resumable ckpt/20ep). <<<** (1st ship STALLED -- orchestrator aa6685f5 tried an origin push, harness-gated, waited; caught by off-remote verify = NOT queued. Re-shipped via orchestrator ad4958a6 = queue_add SCP only, NO origin push, memsmoke skipped [identical-compute fork ran 11110s no-OOM + resumable], exit=0 REMOTE VERIFY present. LESSON: queue_add SCPs the cell file; NEVER frame a ship as needing origin push -- it stalls on the harness gate.) THE decisive pivot test: does zero-training composed-bundle BEAT the 0.0 memorize arms under filtered-MRR with the oracle firing. **MEMSMOKE PREVIEW LANDED = HARD_PASS (run_mode=memsmoke ep25/2seeds, NOT canonical -- orchestrator ran a de-risk one-shot via HDLAB_RUN_MODE=memsmoke): held-out MRR ANCHOR=0.1269 vs ADDITIVE=0.0000/ONESHOT=0.0001 (memorize arms at ZERO), ORACLE=0.1255 (ANCHOR ~= ORACLE = near-ceiling!), RANDOM=0.0004, SCRAMBLE=0.0086 (must-fail fires); H=0.1251 oracle-fires 334.7x; anchor_margin 0.1265 vs HARD_PASS>=0.0626 (2x clear); broken=False. STRONG but PREVIEW ONLY.** **>>> CANONICAL FULL LANDED = HARD_PASS_INDUCTIVE_ANCHOR_COMPOSE (verified off-disk: run_mode=full, 12074s ~3.4h, seeds[7,13,17], cuda). Held-out MRR ANCHOR=0.1282 (hits@10 0.278, hits@100 0.788) vs ADDITIVE=0.00004 / ONESHOT=0.00006 (per-entity memorize arms at LITERAL ZERO across all hits@k); ORACLE=0.1373 (ANCHOR ~93% of ceiling), RANDOM=0.0005, SCRAMBLE=0.0098 (relation-scramble must-fail fires); oracle fires 284x; anchor_margin 0.1277 vs HARD_PASS>=0.0684 (1.9x clear); broken=False; seed7 0.117 consistent w/ agg 0.128. <<<** = IF IT SURVIVES VET, the arc's FIRST GENUINE INDUCTIVE-GENERALIZATION result (substrate infers held-out-ENTITY edges where per-entity fits score 0 = the deep prize). **WIN CLAIM HELD pending skunkworks HARD landed-VET (a50b714e): PRIMARY refutation target = SUPPORT->QUERY LEAK (bundle composes from 50% of the held-out entity's edges to predict the other 50%; must prove disjoint + no shared-tail/inverse/alias shortcut + genuine-L2 discipline applied + n_cold=154 handling); secondary = memorize-arms-fairly-0 (not a code-assignment bug), oracle legitimacy, scramble, per-seed cv<0.15.** canonical != closure; whipsaw. Debias seed_7+17 landed SEED_MIDDLE_BAND (2/3: beat-POP survives degree-matching weakly, dm_margin +0.018/+0.038; partial_r 0.17-0.20 reduced-but-not-<0.15); seed_23 pending. NOTE ANCHOR~=oracle even at ep25 = the zero-training mechanism is robust to fit fidelity (fit only shapes the anchor operator) = a GOOD property, confirm at FULL. Debias seed_7 landed SEED_MIDDLE_BAND (1/3; wait for seed_17/23 to aggregate REDEEMED_WIN). **GRAPH-P3 DIAGNOSTIC LANDED = SUPPORTS_FACTORIZED_MAP_BUILDER (verified off-disk):** P3 map-builder gate HARD_PASS (community Q=0.50, 7 communities, schema_alignment 0.243); it is a **12-CORE (every entity degree>=12 -> adequate bundle SNR even on the tail)**; BUT P1 skew HARD_FAIL (max/mean deg=164 super-hubs -> eval MUST be degree-stratified, SNR sizing per-node/adaptive) + P2 cardinality MIDDLE_BAND (isa worst-tertile fails); hardest relations (isa/synonym/similarto/usedfor...) = weak-point target. => ANCHOR_COMPOSE go/no-go = GO. **HAKE = FAILED (no metrics written; deprioritized -- VET already lowered its prior as rotation-family likely inheriting the popularity artifact; NOT re-run, parked; revisit post-fork only if the operator line resurfaces).** **ANCHOR_COMPOSE AUTHORED + SELF-TEST PASS + READY-HELD (commit 5773ab4d9, NOT pushed/queued):** cell experiments/exp_anchor_compose_inductive_entity_cskg_v1.py + prereg preregs/2026-07-12_anchor_compose_inductive_entity_cskg_v1.md. Mechanism = degree-invariant ADDITIVE bundle E_derived[t]=mean_i(X[h_i]+D[r_i]) over support edges = ZERO-training held-out-entity code, reusing fit_kge_anchor1 + additive_direct_scores (additive per VET). SUBSUMES the fork: ANCHOR_COMPOSE + ADDITIVE_TRANSE + ONESHOT_ROTATE share the SAME fit; only diff = composed-bundle (inductive) vs random-init row (memorize) -> isolates the entity-code knob. 7 arms paired, degree-stratified 3-way, oracle-gated, 4 preflight checks. SELF-TEST PASS (adversarial: first version FAILED its own scramble on a smooth low-dim grid -> redesigned to high-dim where the operator is necessary; held-out hits@10 ANCHOR=0.656 vs ADDITIVE=0.000/ONESHOT=0.033/RANDOM=0.000, scramble margin 0.393, oracle=1.000; caught+fixed additive lr bug). = CONSTRUCTION-PROOF on synthetic, CSKG FULL = the real test. FULL DESIGNED-HELD: overnight_queue GPU, anchor anchor_compose_inductive_entity_cskg_v1, seeds [7,13,17], k24/ep500/n_neg128, ckpt_every20 resumable, timeout 21600. FIRE = orchestrator makes 5773ab4d9 visible on remote (SCP via queue_add) + MEMSMOKE (--memsmoke ep25 seeds[7,13]) no-OOM check + queue_add FULL. GATE: fork lands oracle_fires=True. EVAL-BIAS debias cells running remote_cpu (smoke done, seed_7 running, seed_17/23 pending -> partial-r vs log-deg REDEEMED_WIN test). 2 PRE-COMPACTION DRILLS **LANDED (07-13 pickup)**: (a) PIVOT BUILD SPEC = notes/research_inductive_entity_generalizing_factorized_map_builder_2026-07-12.md -> designed **ANCHOR_COMPOSE** (a new entity = a BUNDLE of relation-operator-bound anchor-neighbors, reusing KGStore.key/score_all + cleanup_family.peel_sic_readout UNCHANGED, **ZERO-TRAINING for unseen entities**; NBFNet/GraIL/NodePiece/ULTRA + HDGL + TEM/grid-cell rebinding). NEW load-bearing result: a 3-field capacity convergence (VSA SNR~5log(D/d) = KGE d>=2c+1 = CA3 capacity) => **capacity tracks LOCAL DEGREE not global N** = the scaling answer (degree-bounded cost -> holds at scale). P_deflated 0.30 HARD-PASS / 0.35-0.40 beats-transductive-arms. GATED on fork verdict (Gate 0) + graph-P3 anchor-connectivity for SNR sizing. READY to dispatch to exp_dev the moment fork=memorize/ceiling. (b) DEGREE-DEBIASED eval = notes/research_degree_debiased_fair_evaluation_2026-07-12.md -> PARTIAL REDEEM: r=0.31 g_backdoor is likely a hubness (pooled gold-degree) CONFOUND not a real leak, BUT the win-gate candidate pool isn't degree-matched (Aiyappa ICML2025/Shomer WWW2023); direction under debiasing OPEN -> **eval-bias re-scoring cell DISPATCHED to exp_dev (remote CPU, refits rotate+POP only, degree-matched candidates + within-stratum partial-r; a21dc63f)** = redeems-or-confirms the MIDDLE_BAND + clears the pivot's eval gate. Multi-seed MIDDLE_BAND landed-VET also DISPATCHED (skunkworks a7688ea3). NB: monitors + crons a40824ce+194ceab3 are SESSION-ONLY but SURVIVED this compaction (both active); a full session RESTART loses them -> re-collect off metrics.json paths per NEXT-ACTIONS, re-arm cron. Landing monitor bth685s9k armed (fork/HAKE/graph).

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) COLLECT the FORK RE-RUN v2 verdict (data/exp_course_c_heldout_entity_inductive_probe_gpu1024_v2/metrics.json; CHECK oracle_fires>=0.10 FIRST -- v1 was INCONCLUSIVE_ORACLE_UNDERFIT so only an oracle-fired result is interpretable) = generalize vs memorize; (3) IF MEMORIZE (predicted) -> strategic PIVOT: BUILD the factorized map-builder wired into the native store (storage drill's 3 integration cells), GATED on graph-diagnostic P3 (does schema-poor CSKG support factorization) + the eval-bias check (degree-corrected neg-sampling -- may clean the leakage / redeem the current result); also run scaling-ladder Anchor2. IF GENERALIZE (surprise) -> push HAKE + scaling on the current line; (4) collect HAKE (phase+modulus beat rotate? esp cardinality-heavy relations; the r-increase HARD-FAIL guardrail) + graph diagnostic (P1-P4); (5) VET the multi-seed MIDDLE_BAND (skunkworks); (6) DEFERRED: comparator SPLIT_IDENTITY_BREACH cell-fix (consume the landed split, don't rebuild), persist-fit-codes durability gap. OBSERVABILITY = DASHBOARD FIXED (commit 02c86fcb4; dash_gui.py now reads its own fresh local cache not the dead web-dash 8765, responsive under slow SSH 5s-bound, single-instance guard; launch: .venv/Scripts/pythonw.exe tools/dash_gui.py or tools/dash_gui.bat). USER uses the TKINTER dash_gui.py, NOT web-dash. (Web-dash 8765 left down; GUI no longer depends on it.)

**DISCIPLINES/LESSONS (this session):** ALL COMPUTE REMOTE, NO LOCAL (USER-locked); CHECKPOINT/RESUME every long run (c140054e1; seed_7 wasted 6h with no checkpoint); VERIFY OFF REMOTE not agent claims (caught false "running", 3x OOMs, split-identity bugs, HAKE-stub-points-at-rotate, degree-biased-eval); WHIPSAW hold-for-VET (held beat-POP; didn't misread old SR memorize-result as the rotate verdict); LAPTOP SLEEP/OUTAGE = local view FREEZES but REMOTE (runners on marsh@home, SEPARATE machine C:/dev/hd-instrument) keeps running -- reconnect = pull ground truth, don't panic; re-route CPU-bound runs to idle GPU for speed (fpe_dim-independent metrics); cron 194ceab3 (:13/:43) self-drive + a40824ce heartbeat.
### ========== END 2026-07-12 ~23:10Z COMPACTION-READY ==========

### ========== 🧭 2026-07-12 ~02:10Z COMPACTION-READY (superseded by 23:10Z above; kept as detail) ==========
**THE WIN ATTEMPT: additive-TransE was the wall; glass-box ROTATION (RotatE-equiv) is the fix; decisive CSKG test is ONE MEMORY FIX from running. Diagnosis triple-confirmed + mechanism proven on synthetic; the real geom-vs-POP number is NOT yet produced (OOM'd pre-eval).**

**DIAGNOSIS (triple-confirmed): the CSKG fit was ADDITIVE TransE (`s=gamma-||X_h+D_r-X_t||`), structurally disqualified for CSKG's SYNONYM(symmetric)+IS_A(hierarchical) mix.** (a) code-level read (director); (b) scour note `notes/research_how_others_beat_frequency_dissect_training_glassbox_recreate_functional_form_gap_2026-07-11.md` (RotatE Table1 + TransH prove it; the 0.9 oracle-fire gate has ZERO precedent at scale = unwinnable, field standard is degree-stratified geom-vs-POP); (c) frontier probe PARTIAL verdict `FRONTIER_FIT_PLATEAU_REPRESENTATION_WALL` (additive plateaus direct-h@10 0.42->0.59 on the DENSE core, its own logic says 'change functional form not capacity'). Ladder `LADDER_FIT_LIMITED` (direct climbs 0.14->0.42, FPE=0.0 = bandwidth-underflow bug, now fixed to median-heuristic).

**THE FIX = rotation score (our PROVEN operator).** PP-275 FHRR phase-rotation primitive (in-domain Hits@1 0.899) reused as the score fn; MECHANISM PROVEN on synthetic self-test (bit-identical across processes): rotation beats discrete 0.131 vs 0.049, beats additive (rot-add 0.098), beats POP; oracle recovers 0.41-0.9 (no impl bug). Decisive cell has an ADDITIVE-TransE HEAD-TO-HEAD arm (claim = glass-box reproduces RotatE>TransE), measures geom-vs-POP degree-stratified on the FAIR low+mid stratum DIRECTLY (0.9 gate DROPPED), pinned sorted-dedup split, all 4 validity-preflight checks.

**BLOCKER (current): decisive memsmoke = CUDA OOM (deterministic, both seeds), NOT logic/thesis.** Mechanism self-test + 4 preflight checks PASSED, CSKG loaded (25752 nodes/511164 edges); died in `fit_kge_rotate` neg-scoring tensor (batch8192 x n_neg64 x k = +3.13GiB on 8GiB card). Memory smoke DID ITS JOB (caught OOM before 3 FULLs burned). MEMORY FIX IN FLIGHT (exp_dev a1dcd7c): batch 8192->2048 + chunk neg block + empty_cache between seeds, keep full dim/recipe/measurement, target peak <5500MB. NOTE: laptop slept ~22:00->01:55Z (froze local view + killed a watcher agent; remote stayed healthy) delaying + then the run OOM'd.

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) collect the MEMORY FIX (exp_dev a1dcd7c commit) -> orchestrator RE-MEMSMOKE (verify peak<5500MB, terminal, no OOM) -> on clean SHIP the 3 FULL seeds `course_c_rotate_cskg_l2_seed_{7,17,23}_v1` (overnight_queue GPU, timeout 14400, per-seed isolation); prereg `preregs/2026-07-11_course_c_rotate_cskg_l2.md`; cells `experiments/exp_course_c_rotate_cskg_l2_*` + core `experiments/_course_c_rotate_core_v1.py`; (3) COLLECT seed_7 = THE geom-vs-POP WIN number (does glass-box rotation beat frequency on the fair low+mid stratum + beat the additive arm?); ~2h/seed serial so seed_7 first; (4) VET HARD (skunkworks landed-VET: fair-stratum, must-fail scramble loses to POP, degree-stratified, seed-flip cv<0.15, additive-arm really loses) before calling it a WIN; (5) if WIN -> STRIVE TO PERFECTION (USER): best-in-class optimize (recipe/capacity; scour's next lever = modulus/phase-split for IS_A hierarchy if a hierarchy-stratum gap remains) + same-split comparator yardstick (deferred; run WITH the settled glass-box number on pinned split); (6) if rotation does NOT beat POP even fair -> honest negative, escalate (ComplEx form / the modulus lever / representation).

**PROCESS WINS THIS SESSION (banked, durable):** VALIDITY-PREFLIGHT GATE built (commit b87481574, WARN-first) + cell-authoring template requires it (06f819e01) -- 4 checks (positive-control-passes / metric-moves / full-gates-at-selftest / deterministic-must-fail); mechanizes the fairness disciplines so bad tests block PRE-dispatch. Poster-child: percolation v2 declared all 4 + caught 2 fairness bugs pre-dispatch. FLIP WARN->ENFORCE after bake (director call). SPLIT-DETERMINISM bug fixed (754c5620b: `list(set())` PYTHONHASHSEED nondeterminism -> different held-out edges across processes; fix=`sorted(set())`; recurred in the rotate self-test too, fixed 1da351227) [[reference_cskg_split_nondeterministic_list_set_dedupe_pythonhashseed_cross_process_2026-07-11]]. Comparator DEFERRED (run with settled glass-box on pinned split). Grounding percolation v1 HARD_FAIL was VET'd = ARTIFACT (confounded degree-scramble null; buried POSITIVE: grounded seeds beat random k<=2); v2 redesign (label-shuffle null) shipped commit d03ec6bba, pending CPU.

**DISCIPLINES (load-bearing):** NO LOCAL SMOKES (all exec remote); multi-seed MEMORY smoke before GPU FULL (caught this OOM); FAIRNESS+weak-point-localization first-class; measure the REAL prize not an unwinnable proxy (dropped the 0.9 gate); WHIPSAW hold-wanted-results-for-VET; VERIFY off remote files not agent claims (caught 2 false 'running', the confounded null, the OOM). Backups: the scour note, `notes/research_decisive_rerun_decision_tree_oracle_capacity_ladder_2026-07-11.md`.
### ========== END 2026-07-12 COMPACTION-READY ==========

### ========== 🧭 2026-07-11 COMPACTION-READY (superseded by 2026-07-12 above; kept as detail) ==========
**THE REASONING ARC (make-relational-real / beat-frequency) DECOMPOSED + RESOLVED into 3 VERIFIED pieces; the decisive full-core run = INCONCLUSIVE (capacity under-fit, NOT a reasoning verdict); capacity-fix re-run in flight.** Full intuitive detail: notes/the_last_piece_intuitive_reasoning_vs_frequency_courses_2026-07-10.md.

**VERIFIED + DURABLE (all committed, all adversarially VET'd):**
1. RANKER LINE CLOSED -- no substrate glass-box unsupervised ranker beats frequency on FB15k-237 (2 fair HARD_FAILs). Not the lever.
2. CORPUS FOUND + VET-VERIFIED GENUINE = CSKG (VET a46eadfa): genuine 2-hop-composition reasoning-headroom 0.276 (HIGH-degree 0.226 at pop 0.412 -- reasoning beats freq EVEN AT HUBS); WN18RR rejected as inverse-lookup artifact. Discriminator = L2/L1I/alias reach decomposition. Headroom = OPPORTUNITY not demonstrated reasoning.
3. MAP-BUILDER OPERATOR CONFIRMED at FULL (phase-rotation/SSP): 0.972 vs discrete 0.001, degree-invariant, transitive A->C works. stage3_hrr prereq = fixable method-limit. Construction-proof on synthetic; capability test = CSKG.
4. DESIGN VALIDATED (SCHEMA-VET ae257cab CLEAN): the decisive cell is INDUCTIVE-not-transductive (a win = genuine composition not memorization), apples-to-apples POP, held-out clean, oracle-gate sound, controls adversarial. 2 landed-VET watch-items (max-arm seed-flip cv<0.15; mine params match a46eadfa).
5. PHASE-2 STAGED (drill af344fb3 + handoff notes/exp_dev_handoff_research_reasoning_realization_gap_closure_prep_2026-07-11.md): KGE middling->strong is a proven cheap curve (recipe-tuning +32-42% > architecture jump; glass-box ~90-95% of opaque SOTA). 5 sequenced anchors, Anchor-1 (CE loss + self-adversarial neg-sampling + N3) FIRST. Our 0.42 ceiling ~ untuned-TransE on a harder 2-hop task -- NEVER market as SOTA.

**THE DECISIVE RESULT (data/exp_course_c_map_builder_cskg_l2_genuine_v1/metrics.json, full, 3 seeds, wall 6463s): INCONCLUSIVE_GEOMETRY_READOUT_UNDERFIT.** The transductive ORACLE did NOT fire (0.023 vs random 0.000) -- the coord-fit failed to fit 25.7k entities at the configured capacity, so geom_best=0.023 vs POP=0.175 is a FIT-CAPACITY artifact, NOT a reasoning verdict (pre-registered outcome-c). At the small CSKG slice the oracle fired + the embedded small-N signal held (geom 0.23 > POP 0.17 on 150 L2 edges, replay adds value) -- so the mechanism looks right WHERE THE FIT WORKS; the task is to make the fit work at full 25.7k scale (capacity). GPU OOM'd 3x (scoring -> coord-fit base -> multi-seed accumulation); the CPU full run completed (no OOM) and gave this INCONCLUSIVE.

**IN FLIGHT (running THROUGH compaction, collect on return):** (a) ORACLE CAPACITY LADDER -- **CONFIRMED RUNNING on remote_cpu** (cell exp_course_c_oracle_capacity_ladder_v1.py, commit 149ef944c FIXED; self-test-gate bug fixed = --self-test routes to a tiny-synthetic branch exit-0, verified gate passed 5.9s; anchor PRESENT in remote queue.json count=1, live child PID 30548, run_mode=full, started 13:12:50Z, ~3h). Collect metrics data/exp_course_c_oracle_capacity_ladder_v1/metrics.json on return -- verify a FRESH result (the prior INCONCLUSIVE metrics at that path is STALE until this overwrites). INCIDENTAL (NOT the verdict, L0 control only): the accidental partial-full gave oracle_direct h@10=0.142 vs oracle_fpe 0.036 at the repro-0.023 config -- direct-distance ~4x the FPE-kernel readout = an EARLY READOUT-LIMITED hint (the FPE readout may be the bottleneck, not the fit); the full ladder's dual-readout disentangles it. Root-cause found: fit_transe_coords is FULL-BATCH margin-ranking (only 600 grad steps total regardless of data size; gold need only beat 10 random negs of 25751) = under-trained + weak ranking pressure at 25.7k scale -> oracle collapsed 0.023. Ladder: full-25752-scale SINGLE-seed (memory-flat), 6 points (L0 repro-control -> L1 more-epochs -> L2 minibatch -> L3/L4 Anchor-1 recipe[CE+self-adversarial+N3+reciprocal] -> L5 +dim), reads ORACLE under BOTH FPE-kernel AND direct-distance readouts (DISENTANGLES fit-limited vs readout-limited -- the key ambiguity), previews geom-vs-POP degree-stratified at cheapest firing point. Anchor-1 recipe = experiments/_kge_anchor1_fit.py. The per-seed-isolation harness (_run_seed_isolated) EXISTS but was disconnected from main() = the 'lost fix'; rewire it for the decisive 3-seed re-run (_v2 anchor) once firing_config known. (b) GROUNDING relational-role reframe drill = **LANDED** (notes/research_abstract_grounding_relational_role_vs_attribute_reframe_2026-07-11.md). VERDICT (P0.30 refined / 0.12 strong-form): relational/map-role grounding is NEURALLY REAL (Garvert 2017) but NOT a free-standing bypass of exogenous anchoring -- it is PARASITIC via COMPOSITION-REACHABILITY (Harnad/Bender-Koller): abstract concepts get grounded ONLY by being compositionally REACHABLE from grounded CONCRETE anchors. This RECONCILES all 3 attribute-negatives (magnitude=wrong-shape, social=engineering-failure, metaphor=missing-must-fail-control) AND reframes the 6-instance on-substrate relational-code losing streak as a CLOSED-RELATIONAL-ISLAND **DATA/connectivity problem, NOT an operator problem** (abstract concepts were disconnected from grounded anchors). KEY: grounding is a REACHABILITY/PERCOLATION question -- is the abstract layer connected to the grounded floor above the percolation threshold? UNIFIES with reasoning: the SAME map grounds IF the reachability is there -> testable on CSKG (dense+compositional may supply the reachability paths our thin/islanded prior graphs lacked). NEXT-DRILL: network-science/percolation reachability audit of CSKG (abstract->grounded-anchor connectivity). (c) GROUNDING PERCOLATION drill **IN FLIGHT** (research a5bc9c3f, runs through compaction): is grounding a percolation/reachability property + does CSKG connect the abstract layer to a grounded concrete floor above threshold (where our thin/islanded prior graphs failed)? -> delivers the concrete remote acceptance-test spec for the next-session grounding experiment.

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) COLLECT the ORACLE CAPACITY LADDER (data/exp_course_c_oracle_capacity_ladder_v1/metrics.json; shipped remote_cpu). READ: does the oracle FIRE (hits@10>=0.90) at any ladder point + which (firing_config: epochs/dim/recipe)? + the FIT-vs-READOUT verdict (if direct-distance fires but FPE-kernel doesn't = READOUT-limited, fix the FPE readout not the fit; if neither fires = FIT-limited, need more capacity; if both fire = solved) + the geom-vs-POP preview at the firing point. THEN dispatch the DECISIVE 3-SEED RE-RUN (_v2 anchor): rewire _run_seed_isolated into main() + fit_kge_anchor1 at firing_config, REMOTE only (CPU-safe, or GPU only after a >=2-seed memory smoke w/ isolation verified) -> the real reasoning number -> landed-VET (2 watch-items) -> if positive, Phase-2 Anchors 2-5 to best-in-class; if oracle STILL won't fire even at L5 -> the fit/readout may need a different representation (escalate); (3) COLLECT the grounding drill (aba30612) -> possible new grounding direction (grounding-via-map); (4) FOUNDATIONAL BUILD once the mechanism realizes CSKG: ingest CSKG as the substrate's real reasoning corpus (the 'build our own corpus' USER thrust); (5) re-fire the meta-audit if wanted (held -- multi-seed-memory-smoke lesson captured).

**DISCIPLINES (this session, load-bearing):** 🚫 NO LOCAL SMOKES -- ALL execution to remote (USER 07-11) [[feedback_no_local_smokes_route_all_execution_to_remote_USER_2026-07-11]]; multi-seed MEMORY smokes (single-seed masked the 3x OOM); ⚖️ FAIRNESS + WEAK-POINT-LOCALIZATION first-class (caught rankfair-smoke-wash, WN18RR-artifact, metaphor-fairness-fake, inductive-not-transductive); 🏆 BEST-IN-CLASS (2-phase: work-fair then optimize-to-frontier); WHIPSAW hold-wanted-results-for-VET (held throughout); design-VALIDATED so trust the verdict. Backups: courses note, Phase-2 handoff, best_in_class_optimization_course_scorecard_2026-07-10.md.
### ========== END 2026-07-11 COMPACTION-READY ==========

### ========== 🌅 OVERNIGHT 07-10/11 -- THE ARC CONVERGED (superseded by COMPACTION-READY above; kept as detail) ==========
**The reasoning-beats-frequency arc RESOLVED into a clean 3-piece plan, each VET'd; decisive CSKG FULL now running.** Full detail: notes/the_last_piece_intuitive_reasoning_vs_frequency_courses_2026-07-10.md.
1. **RANKER LINE CLOSED** (2 independent FAIR HARD_FAILs, VET'd a165be25): no substrate glass-box unsupervised ranker (resonator/confidence/residualization/HD-geometry/type/freq-cap) beats frequency on FB15k-237; +0.164 headroom real but unrealizable on FB15k. Ranker was never the lever.
2. **CORPUS FOUND + VET-VERIFIED GENUINE = CSKG** (VET a46eadfa, HIGH conf): 7-corpus survey; CSKG L2-genuine 2-hop-composition reasoning-headroom 0.276 (HIGH-degree 0.226 at pop 0.412 -- reasoning beats freq EVEN AT HUBS where FB15k collapses to 0.028). WN18RR REJECTED as 91% inverse-lookup artifact. Decisive discriminator = L2(genuine-composition)/L1I(inverse)/alias reach decomposition + leak-fraction. NUANCE: headroom = OPPORTUNITY not demonstrated substrate reasoning.
3. **MAP-BUILDER OPERATOR CONFIRMED at FULL** (exp_dev, GPU, device=cuda): phase-rotation/SSP-FPE operator reach@1 0.972 vs old discrete-bind 0.001; degree-invariant; transitive A->C falls out of the geometry; all must-fails fire. stage3_hrr prereq was a fixable method-limit not a wall. CONSTRUCTION-PROOF on synthetic (clean grid) -- consolidation inconclusive there (no headroom to add); the CAPABILITY test is CSKG.
**DECISIVE EXPERIMENT RUNNING (orchestrator a7b888b3, overnight_queue GPU, cell exp_course_c_map_builder_cskg_l2_genuine_v1.py commit d28ffdedc):** map-builder ON CSKG -- does the substrate REALIZE CSKG's 0.276 L2-genuine reasoning opportunity, beating frequency (degree-stratified, HIGH-degree headline), + does consolidation add value on the underdetermined real corpus? 439 genuine held-out reasoning edges (inverse/alias stripped).
**3 OUTCOMES (read the landed metrics -- data/exp_course_c_map_builder_cskg_l2_genuine_v1/):** (a) geom beats POP on L2-genuine -> FIRST GENUINE REASONING WIN -> VET it hard, then optimize to best-in-class; (b) oracle fires but geom loses -> honest negative, substrate can't realize opportunity -> better consolidation/readout; (c) ORACLE FAILS at k=24/600ep/dim4096 on 23.6k entities -> INCONCLUSIVE_READOUT_UNDERFIT (capacity wall) -> BUMP k/dim (or smaller core) and re-run, NOT a substrate verdict (smoke under-fit warns this is live). Don't over-read any outcome; hold for VET.
**SESSION HYGIENE:** atom backlog CLEARED (pushed 9d34891e8, 0 flags remaining). Course-C operator-fix FULL is a construction-proof still un-VET'd (bundle with the CSKG-result VET). Line B (grounding: social-relational HARD_FAIL_AFFECT_RELABEL, magnitude MIDDLE_BAND -> abstract needs Anchor-3 metaphor bridge) = secondary, parked.
### ========== END OVERNIGHT 07-10/11 ==========

### ========== 🔌 OFFLINE / PAUSE-ALL 2026-07-10 (~22:xxZ -- superseded by OVERNIGHT block above) ==========
**USER lost internet, paused ALL work. data/orchestrator_paused.flag SET. All background agents DIED on API ConnectionRefused when the outage hit.** ON RETURN: (1) clear the pause flag ONLY when USER says resume; (2) VERIFY OFF-DISK what each dead agent produced before re-dispatching (CLAUDE.md STEP-0 pt3 -- artifacts usually landed; re-dispatch only the missing piece). WHAT WAS IN FLIGHT when internet dropped:
- **afd0d1cd** = LINE A P1 symbolic RANKING proof on the FAIR low+mid degree stratum (does a better ranker beat freq where reasoning is possible). 25 tool-uses -> likely has a cell/prereg on disk; check experiments/ + data/ for a symbolic-ranking cell.
- **a07f8163** = CORPUS: CSKG dense-core headroom acceptance test (is it a FAIR reasoning testbed). 20 tool-uses -> check for cell + any headroom table.
- **a15dd11f** = LONG remote_cpu multi-corpus headroom SURVEY (barely started, 5 tool-uses -- probably minimal on disk; re-dispatch).
- **a0939bcc** = LONG overnight_queue(GPU) substrate-native RANKER sweep (8 tool-uses -- check for cell draft).
- **a71310f9** = Phase-2 best-in-class RANKER frontier drill (+2 lit-scan children aac4d726/a9d22aa4) -- check notes/ for a partial ranker note; children returned rich findings inline in prior sessions, may be recoverable from their task output files.
- **Line B social FULL** = was RUNNING on remote_cpu (may or may not survive depending on whether the remote machine kept power/net); on return check data/exp_spanning_grounded_core_reach_social_v1/ + the chain-landing log.
- **PENDING STORE-SYNC (needs_orchestrator_store_sync=True):** VET atoms A1(decode-fix HF)/A2(beat-freq-bar-is-degree-structured MM)/A3(meta guard) + grounded-math MIDDLE_BAND + decode diagnostics. On-disk in data/substrate_index/{math,meta}/, NOT git-committed -- orchestrator sync on return.
- **NEXT REAL STEP (Line A):** collect afd0d1cd (did ranking beat freq on the FAIR stratum?) -> if yes, build substrate-native ranker (frontier = a71310f9 drill); if no, the fair bar isn't winnable even symbolically -> lean harder on the corpus (a07f8163). Reasoning weak-link = RANKING (compose CG, decode near-optimal, capacity PP-127 proven). Best-in-class course: notes/best_in_class_optimization_course_scorecard_2026-07-10.md.
### ========== END OFFLINE / PAUSE-ALL ==========


**Read end-to-end; self-contained + current. Deep pre-07-08 history: notes/director_POST_COMPACTION_BACKUP_FULL_STATE_2026-07-07.md. CRON = a40824ce (:13/:43).**

### ========== 2026-07-10 EVENING (~20:xxZ -- READ FIRST; BOTH FULLs landed = TWO DISTINCT negatives, VET'd; crux=COMPOSE-crosstalk(resonator fix) + grounded-core=SPAN/alignment; my 'convergence' REJECTED; supersedes LATE below) ==========
**BOTH DECISIVE FULLs LANDED (both honest negatives that CONVERGE on locating the wall -- VET in flight, skunkworks ae1cb):**
- **CRUX ENGINE FULL = HARD_FAIL** (data/exp_crux_engine_fb15k237_vsa_gt_v1/metrics.json, run_mode=full, ~225s): substrate-native generate-and-test (bind/unbind PROPOSE + verifier + 3 beat-frequency levers: head-conditional multiplicity, hop-normalized conf, negative-evidence) does NOT beat frequency. SUBSTRATE_GT h@1 0.138/mrr 0.161/h@10 0.201 vs POP_RELFREQ 0.262/0.338/0.487 -> beats_freq=FALSE. **REAL PROGRESS inside the failure: bind/unbind is now LOAD-BEARING (ablated_ratio 0.19) = a genuine SUBSTRATE-native negative, NOT a construction-proof like Step-1; and the cleaner FULL regime IMPROVED it (smoke mrr 0.092 -> full 0.161, N_DIM 2048 reduced crosstalk) -- helped, insufficient.**
- **GROUNDED-CORE FULL = SPAN_FAIL_MECHANISM** (data/exp_spanning_grounded_core_reach_v1/metrics.json, run_mode=full, 40s): reach uniformly LOW across 5/6 domains (span_per_domain PHYSICAL 0.5/ABSTRACT 0.21/EMOTIONAL 0.81/MATH 0.0/SOCIAL 0.31/TEMPORAL 0.25, agg 0.348; only EMOTIONAL reaches). Cell SELF-DIAGNOSED as a MECHANISM/DECODER bottleneck (the diffusion-with-restart reach decoder), NOT a core-span gap -- matches the loop-closer grounding-doesnt-chain. (VET adjudicating mechanism-vs-span.)
**THE CONVERGENCE = REJECTED by the VET (skunkworks ae1cb, adversarial -- I OVER-UNIFIED; my 4th wanted-narrative correction this session).** They are TWO DISTINCT negatives, NOT one wall:
- CRUX leaks at COMPOSE = bundle-CAPACITY crosstalk (N_DIM-sensitive). VET-confirmed: vsa_recall@10 0.203 vs symbolic 0.491 (my compose-crosstalk prediction RIGHT); but CONDITIONED ON RECALL, VSA verify+rank are HEALTHY (cond_mrr 0.415 >= symbolic 0.377) -- so it is NOT rank/propose-verify, it IS the bundle 'collapse-to-clean-answer' being capacity-limited. Smoke->full mrr 0.092@N1024 -> 0.161@N2048 (doubling N_DIM ~doubled mrr = crosstalk-limited-compose signature).
- GROUNDED-CORE is NOT a decoder problem (the cell's 'uniform decoder bottleneck, pause core-expansion' self-diagnosis is FALSIFIED -- a uniform decoder cannot yield EMOTIONAL 0.87; narrow-physical + scramble controls prove the decoder HEALTHY). The failure is REPRESENTATION SPAN/ALIGNMENT: reach tracks probe-to-core COSINE which is already domain-structured (emotional +0.48, physical +0.27, social +0.015, MATH -0.43 = ANTI-grounded). The core does NOT cosine-align with math/social -> a SPAN/COVERAGE gap, NOT a decode loss. 'Pause core-expansion' is UNSUPPORTED.
- Capacity-crosstalk != coverage-misalignment. Only a loose theme survives ('distributed vectors carry low SNR for structured retrieval') -- NOT a shared mechanism or single lever.
**THE FIX (crux only): RESONATOR NETWORKS (Frady, Kent, Olshausen, Sommer 2020, Neural Computation 'Resonator Networks 1'; in-memory factorization Nature Nano 2023).** They FACTORIZE a product of bound codevectors into its factors via ITERATIVE cleanup against codebooks -- the combinatorial decode our SINGLE-SHOT unbind fails at (5+ orders of magnitude larger; the 'iterative collapse'/measurement operator). CRUX v2: replace single-shot decode with a resonator-network decode -> recover VSA candidate-recall toward symbolic (0.20->~0.49) THEN beat frequency; on GPU-for-real (resonator iterative matmuls ARE GPU-heavy, unlike the 1.7%-util rule-mining). GROUNDED-CORE gets a DIFFERENT fix (NOT the resonator): the core needs better DOMAIN-ALIGNMENT/expansion for the anti-grounded domains (math -0.43, social +0.015) -- align/add core primitives that cosine-cover those axes; do NOT 'pause core-expansion'.
**ATOMS FILED (VET, A5-gated, verified off-data; needs_orchestrator_store_sync=True -> STORE-SYNC PENDING):** math HARD_FAIL (crux substrate-native negative, composes symbolic Step-1) + math MEASURED_MECHANISM (grounded-core span/alignment negative) + meta CERT-NEUTRAL (decode-convergence REJECTED guard). Today on-disk: math atoms 14, meta 4. **SYNC-HYGIENE FLAG: grounded-core FULL metrics.json is MISSING at data/exp_spanning_grounded_core_reach_v1/ (only _selftest/); the landed JSON is embedded in data/logs/chain_landing_spanning_grounded_core_reach_v1.log -- orchestrator sync bug, fix.**
**GPU MUST ACTUALLY USE GPU (USER caught, fix24 recurrence): the crux 'GPU FULL' ran at 1.7% util -- it barely touched CUDA (mostly CPU rule-mining; only theta-polar on GPU). It COMPLETED (225s) so not a true stall, but the resonator rebuild (iterative matmuls = genuinely GPU-heavy) MUST move the VSA compute to CUDA. [[feedback_fix24_gpu_dispatch_must_actually_use_gpu]].**
**PROCESS LOCKED (USER, this session): SMOKE-ONLY-LOCAL -- tiny smoke local (seconds); intensive FULL -> orchestrator -> remote_cpu/overnight(GPU), DASH-VISIBLE. Both FULLs ran correctly on remote this time. [[feedback_dont_over_scope_smokes_into_heavy_local_runs_keep_tiny_route_intensive_via_orchestrator_2026-07-10]].**
**CONVERGENCE ARCHITECTURE (still the target, notes/convergence_architecture_grounding_is_the_verifier_2026-07-10.md): grounding = the VERIFIER (a measurement operator), geometry PROPOSES, engine composes head-conditionally. The convergence-verifier experiment (does a grounded-attribute-checking verifier out-infer graph-support-only) is GATED on first getting the engine's DECODE to work (resonator fix).**
**IN FLIGHT / LANDED (2026-07-10 ~20:3xZ post-compaction pickup):**
- (a) CRUX v2 resonator-decode = **LANDED HARD_FAIL_A (encode/bundling SNR-floor), VET IN FLIGHT (skunkworks a359da34)** (cell exp_crux_engine_v2_resonator_decode_v1.py commit 4ac96030 local not-pushed; prereg preregs/crux_engine_v2_resonator_decode_v1.md). All 4 drill guardrails folded in (residual re-bind gate, batched restarts R8/16, decoupled near-orthogonal entity codes mean|cos|0.020, ACF hook). SELF-TEST PASS (resonator recovers 3 buried golds vs single-shot 1; gate rejects spurious; bind load-bearing h@1 1.00 vs add 0.10). REAL-DATA: res_gated_recall@10 0.058 at N=2048 vs single-shot 0.175, symbolic 0.317 -- BELOW 0.25 SNR-floor fail band + WORSE than single-shot. DIAGNOSIS (exp_dev, VET-pending): the leak is at ENCODE/BUNDLING not decode -- all rule-path tails crammed into ONE bundle q, K>>capacity even at N=2048; bigger N barely moved it (0.020->0.058 from 512->2048); codes decoupled (rules out correlation); resonator cond_mrr 0.464>symbolic 0.404 (ranks excellently what it recovers, recovers too few = pure recall wall). **This VINDICATES the defer-collapse drill's exact prediction: deferred-collapse/resonator fixes 'info present but hard-read', NOT 'info destroyed by compose-crosstalk' -- and the crux HARD_FAIL_A band WAS the instrument; it fired on the 'destroyed-upstream' side.** exp_dev correctly did NOT burn GPU reconfirming (discriminator fired decisively at full-N). **WALL 1 RELOCATED: reasoning-engine bottleneck is COMPOSE/ENCODE bundling-capacity, not decode.** VET QUESTIONS (skunkworks): does HARD_FAIL_A hold; is gated-vs-single-shot fair (gate may manufacture 'worse'); is v2's ENTITY-CODEBOOK-recovery a faithful continuation of v1's rule-bundle decode or a re-scope; canonical-FULL needed; is v3-shard sound. **v3 SHARDED per-rule-path recovery = candidate next lever, HELD pending disambiguation.**
- **UPDATE (~21:xxZ): DISAMBIGUATION RESOLVED = DECODE-SIDE, encode-floor FALSIFIED (exp_dev ae31e269, cell exp_crux_v2_decode_budget_sweep_v1.py, DISAMBIG_DECODE_SIDE).** The decode-budget sweep (t_iter 20->100 on the FROZEN crux-v2 bundle) shows: raw-recall@10 DEAD FLAT 0.133 (a METRIC ARTIFACT -- rank_score=t_iter-it structurally freezes top-10, can NEVER move) BUT gold-surfaced-AT-ANY-RANK RISES monotonically 0.213->0.300 (62%->87% of reachable golds) as t_iter->100. => THE GOLD SURVIVES ENCODE (info IS in the bundle); the wall is DECODE-side (rank-scoring + too-strict re-bind gate + iteration budget), NOT an encode/bundling SNR-floor. crux v2's 'encode/SNR-floor' self-diagnosis was WRONG; the VET's skepticism (under-iterated t_iter=20, gate too tight, nonconv=0.95 metric artifact) is CONFIRMED. **This is GOOD NEWS: info not lost, just read out badly = tractable mechanical fix, not a capacity wall.**
- **SHARD STOOD DOWN (a8545cee): encode-floor premise falsified -> sharding is NOT the fix for this wall (shard cell left as a not-yet-needed PP-127-family capacity primitive for future reuse IF a real encode-floor appears).**
- **DECODE-SIDE FIX BUILDING (ae31e269 resumed, cell exp_crux_v2_decode_side_fix_v1.py):** 3 changes on the frozen bundle -- (1) rank by residual-sim/CONFIDENCE not recovery-order (so late-but-high-sim golds rank into top-10; THE load-bearing change, 87% of golds surfaced but not ranked in); (2) relax/replace the re-bind gate (gated@10 flat 0.060 while at-any-rank 0.300); (3) t_iter>=80. BARS: STAGE-1 confidence-ranked recall@10 clears single-shot 0.175-0.207 toward symbolic 0.317-0.347; STAGE-2 mrr beats frequency POP_RELFREQ 0.338 (THE prize number). Degree-stratified (gap concentrated mid/high). SMOKE-ONLY-LOCAL -> FULL orchestrator.
- **PROCESS WIN (for USER's 'are we converging' concern): the chain worked -- exp_dev claimed encode-floor -> VET said not-confirmed/under-iterated -> disambiguation CONFIRMED decode-side. 3 independent checks converged on the real answer. Running both (shard + disambiguation) per USER let us redirect BEFORE wasting a FULL on the wrong fix. WALL 1 now localized to a SPECIFIC, MECHANICAL, TRACTABLE decode-side fix (rank/gate/budget) on a bundle that PROVABLY retains the info.**
- **BANKED DIAGNOSTIC (for store-sync): recall@10 with rank_score=t_iter-it is STRUCTURALLY INSENSITIVE to decode budget = a metric artifact that masked decode-side recovery (telemetry-sensitivity lesson).**
- **UPDATE 2 (~21:5xZ): DECODE-SIDE FIX FALSIFIED -> diagnosis swung BACK to compose SNR-floor (exp_dev ae31e269, cell exp_crux_v2_decode_side_fix_v1.py, HARD_FAIL_A_DECODE_FIX_INSUFFICIENT).** Confidence-rerank + relaxed gate + t_iter>=80: recall@10 0.140raw/0.040gated < single-shot 0.207; mrr 0.040 << POP 0.337. Self-test PASSED (fix works on clean planted bundle) but real frozen bundle FAILED. MECHANISM (tested, not guessed): the gold's matching-pursuit COEFFICIENT is genuinely LOW -- it surfaces late BECAUSE weak -- so no coefficient-rerank promotes it above legit-higher-coeff distractors; single-shot matched-filter already near-optimal for a weak buried component, and IT loses to frequency too. => PRESENT-but-uncompetitively-WEAK = compose-time bundling SNR floor. exp_dev CORRECTS its own 'decode-side' call: decode lever tested+exhausted, v3-SHARD RE-WARRANTED. Diagnosis path encode->decode->encode-SNR, each swing test-driven (don't-over-correct-on-raw-read loop working).
- **🚨 BIG FAIRNESS CATCH (USER's directive at arc level, GATES everything): POP_RELFREQ (frequency) sits at ~0.972 of the reach-ceiling (0.347) on this subgraph; Step-1 independently freq 0.487 vs ceiling 0.514 -- BOTH near-ceiling. So 'BEAT FREQUENCY' may be UNWINNABLE BY CONSTRUCTION (the reader-wall/4-cells trap). Also SYM (symbolic 'ceiling') mrr 0.128 < POP mrr 0.337 -> even the oracle-ish engine loses to freq on mrr -> the mrr bar may be above the achievable ceiling. If freq saturates the ceiling, every Line A 'failure' is a TEST artifact not a substrate wall.**
- **RESOLVED (~22:2xZ): FAIRNESS-CEILING VET LANDED (skunkworks aa7f151f) -- THE CLARIFYING RESULT. JOB1: decode-fix HARD_FAIL CONFIRMED (single-shot matched-filter is OPTIMAL linear detector; below-floor coefficient unrecoverable by ANY linear rank; decode lever EXHAUSTED); SHARD NOT re-warranted (even a PERFECT substrate matching the symbolic reach-oracle STILL loses aggregate freq: SYM h@10 0.281/mrr 0.206 < POP 0.495/0.346; winnable stratum is a RANKING not SNR bottleneck -> sharding is the wrong tool). JOB2 (load-bearing): the aggregate 'beat frequency' bar was UNFAIR = a DEGREE-DOMINATION ARTIFACT.** Independent full-graph recompute (reproduces Step-1) degree-tertile table (q1=47,q2=279): LOW reach-ceiling 0.345 / POP h@10 0.107 -> headroom 0.320; MID 0.528 / 0.421 -> headroom 0.299; HIGH 0.647 / POP **0.964** (freq EXCEEDS the reasoning ceiling via non-relational popular-tail signal no path-reasoner can reach) -> headroom 0.027; ALL 0.506/0.495 -> 0.011. **The aggregate 'no headroom' is HIGH-degree hubs (frequency-guessable by construction) DROWNING the real low/mid signal. LOW+MID (bottom 2/3) has 24-30pts of REAL recall headroom -- and there the bottleneck is a RANK-ceiling (symbolic reaches 0.345 low but ranks only 0.097 into top-10; already ties/beats POP on low-degree mrr 0.058 vs 0.053), NOT recall/SNR.** VET atoms: A1 decode-fix HF, A2 MM 'beat-freq bar is DEGREE-STRUCTURED' (corrects Step-1 'no headroom anywhere' overclaim UPWARD), A3 META guard 'before ruling a beat-baseline bar UNFAIR, stratify by the baseline's OWN signal' (needs_orchestrator_store_sync).
- **REFRAME DISPATCHED (exp_dev afd0d1cd): Line A = does a better symbolic RANKING of already-reached candidates beat frequency ON THE LOW+MID DEGREE STRATUM (the fair regime, ceiling >> freq)? SYMBOLIC ONLY, NO HD/GPU (VET: prove cheaply first; if symbolic can't win fair, substrate-native can't). Fairness+localization apparatus mandatory (stratum baseline, scramble must-fail, no freq-leak, degree-stratified). This is the 'does reasoning beat frequency on a FAIR test' number.** v3-SHARD DROPPED (wrong tool, VET). Durable escape from the freq wall = the DENSE GROUNDED CORPUS (relations compositionally-derivable, not frequency-guessable; FB15k-237 high-degree tails are a proven dead-end for aggregate 'reasoning beats freq').
- (superseded) HELD: v3-SHARD gated on the FAIRNESS/INFO-CEILING VET. VET jobs: (1) confirm decode-fix HF + shard re-warrant; (2) LOAD-BEARING = compute info-ceiling vs frequency across regimes (full FB15k-237, degree strata, relation types) -- is there ANY regime with material headroom (winnable target for v3-shard), or does freq saturate the ceiling everywhere -> REFRAME Line A target (need a task/graph where inference has headroom over freq = the dense grounded corpus). ON VET RETURN: headroom-exists -> build v3-shard targeting that regime; saturated -> Line A bar was unfair, pivot to building the dense-corpus testbed (the constructive move) rather than more fixes on an unwinnable bar.
- **Line B parallel: social-relational grounding channel = HARD_FAIL_AFFECT_RELABEL at smoke (affect-ablation FIRED, caught 87% valence-correlation confound). BOTH scalar channels (magnitude MATH, social) converge: abstract domains NOT scalar-groundable -> need STRUCTURAL grounding (Anchor 3 metaphor-bridge / mentalizing). Social FULL shipping to cskg regime (orchestrator a22ba48, regime differs from cn-smoke) + 2 VET atoms syncing.**
- (b) RESONATOR drill = **LANDED** (research ab43e2e; note notes/research_resonator_decode_capacity_ceiling_crux_v2_2026-07-10.md). HEADLINE: plausible recovery of the 2.4x gap (0.203->0.491), P_deflated=0.50 capped / plain-resonator-alone 0.30 -- GATED on 3 named risks (topology mismatch sequential-vs-simultaneous; correlated non-i.i.d. codebooks = open lit gap FB15k-237 lands in; silent spurious-convergence). KEY: substrate ALREADY has validated ACF rescue (cap_map row 51, 50x+ on codebook-SIZE axis) + restart-budget law -- wire from start, don't re-derive. No closed-form resonator capacity law exists (Frady "stymied"); empirical quadratic-in-N only. Brain: genuine CA3-attractor analog for decode; bigger brain-faithful lever = DG-decorrelation front-end (unwired) or theta-gamma sequential slotting (encode-side redesign, NOT this cycle).
- (c) ABSTRACT/MATH/SOCIAL grounding drill -- STILL RUNNING (research a052a49ee82ae3043): why math anti-grounded (-0.43), embodied-metaphor (Lakoff) + numerical-cognition (Dehaene), core-expansion for non-perceptual domains. Maps to the grounded-core SPAN/alignment fix.
- STORE-SYNC = **DONE** (orchestrator ac46f0bd): commit 06f0b8ee6 (3 VET atoms crux-HF/grounded-core-MM/convergence-REJECTED-meta + 4 referential siblings, A5-gated); grounded-core metrics.json FIX VERIFIED on disk (data/exp_spanning_grounded_core_reach_v1/metrics.json, run_mode=full). Origin push finishing in background (local commit durable).
- USER-REQUESTED DRILL: value of a DELIBERATELY-UNGROUNDED (defer-collapse / compute-in-superposition) component = **DONE** (research a0e22b2a; note notes/research_defer_collapse_superposition_compute_value_2026-07-10.md). HEADLINE: premature point-estimation is a PROVABLE info-loss move (sum-product vs max-product, Fisher-info-under-quantization, SMC/ESS resampling, BCJR-vs-Viterbi) -- we ALREADY rediscovered it (soft>hard decision + convergence propose/collapse); drill upgrades design empirical->principled + names missing piece = ADAPTIVE ESS-style collapse-TRIGGER (not fixed schedule). Ranked: deferred-collapse (LOAD-BEARING, P0.50) > sketch-in-superposition (real gap, ZERO Bloom/CountSketch in hdlab today, P0.45) > generative-interpolation-as-proposer (verifier-gated only, P0.35) > reservoir-computing (unclear fit, P0.20) > quantum-interference (weakest, dequantization argues against, P0.15). SKEPTIC engaged: defer-collapse fixes "info present but hard-read", NOT "info destroyed by compose-crosstalk" -- and the CRUX-V2 HARD-FAIL band IS the instrument distinguishing them. NEW BANKED CANDIDATES (both gated/sequenced, NOT dispatched): (1) ESS-gated deferred-collapse cheap test (bolts onto crux soft-chain scaffolding; scrambled-ESS must-fail + SNR-floor boundary control) -> feeds the CONVERGENCE-VERIFIER design; (2) sketch-in-superposition pre-filter (cardinality/overlap without decode, independent cheap build). NEXT-DRILL candidate (open): reservoir<->VSA hybrid connection.
NOTE agent labels invert my drill names (harness ab43e2e="resonator drill", a052a49="grounding drill") -- go by note CONTENT.
**NEXT-SESSION FIRST ACTIONS (VET DONE -- both atomized, convergence REJECTED, two separate fixes):** (1) heartbeat; (2) STORE-SYNC the 3 pending atoms (crux HF, grounded-core MM, convergence-REJECTED meta -- needs_orchestrator_store_sync=True) + fix the grounded-core metrics.json sync bug (landed JSON stuck in the chain_landing log, not written to data/exp_spanning_grounded_core_reach_v1/); (3) **CRUX v2 ALREADY DISPATCHED pre-compaction (exp_dev a4f4b4699980af3e8) -- COLLECT it first**: RESONATOR-NETWORK decode (iterative-collapse factorization, Frady 2020) replacing single-shot unbind, ON GPU FOR REAL (VSA compose+resonator iters -> CUDA; prior crux was 1.7% util CPU rule-mining). Two-stage bar: STAGE-1 resonator recovers VSA candidate-recall 0.203->toward symbolic 0.491 (closes compose-crosstalk); STAGE-2 then beats POP_RELFREQ (mrr>0.338). On its return: read the tiny-smoke (did recall recover? GPU device=cuda confirmed?), then it hands FULL to orchestrator->overnight_queue(GPU); ship + VET. The ONLY confirmed weak link is COMPOSE (verify+rank healthy); (4) GROUNDED-CORE = a SEPARATE fix (NOT resonator): the core has a SPAN/ALIGNMENT gap (math -0.43 anti-grounded, social +0.015) -> add/align core primitives to cosine-cover those domains; the cell's 'pause core-expansion' is WRONG per VET; (5) THEN the convergence-verifier experiment (grounding-as-verifier, notes/convergence_architecture_grounding_is_the_verifier) once the engine decode works; (6) disciplines LOCKED: SMOKE-ONLY-LOCAL (tiny local; intensive->orchestrator->remote/GPU dash-visible) + GPU-must-actually-use-GPU. DEEP STATE: TWO distinct walls -- crux=COMPOSE bundle-crosstalk (resonator+bigger-N_DIM fix, my prediction confirmed); grounded-core=SPAN/ALIGNMENT (core-alignment fix). bind/unbind now genuinely load-bearing (real substrate negatives, not construction-proofs). Director over-unified the convergence -> VET rejected (whipsaw discipline held, 4th correction).
### ========== END 2026-07-10 EVENING ==========

### ========== 2026-07-10 LATE (~1x:xxZ -- SUPERSEDED by EVENING above; the INGEST PIVOT + generate-and-test engine detail) ==========
**THE BIG PIVOT (USER-driven): the relational-inference wall is an INGEST problem, and the path is DENSITY + a GENERATE-AND-TEST mechanism, on a GROUNDED SPANNING CORE.** After the whole arc's negatives (code/encoder/richness/grounding all fail to move inductive inference), two USER questions cracked it: "both candidates?" and "is ingest the thing?".
**ENGINE arc (Step-1, VET'd + banked, CERT delta 0):** exp_gt_induction_fb15k237_dense_v1 (commit 7529b6c02) = generate-and-test rule-induction (symbolic AMIE) on DENSE FB15k-237. EARNED (VET-corrected DOWN from director's "first forward result" over-frame -- 3rd over-frame of the session): (a) **DENSITY IS NECESSARY = CG-solid** (dense infers h@1 0.171 / mrr 0.212; fair degree-downsample to avg-deg 3 -> 0.000). (b) VERIFIER load-bearing (ablation halves ranking; thin 3.8% margin). (c) **PROVEN NEGATIVE: simple L1+L2 rules do NOT beat the per-relation FREQUENCY prior** (lose h@1/h@10/mrr; reach-ceiling 0.514 barely > freq 0.487). (d) SCOPE = CONSTRUCTION-PROOF (zero substrate primitives), NOT a capability. Atoms: math 29206, ledger 1583.
**INGEST REFRAME (USER challenge "are we testing the corpus right? those must have vast knowledge"):** verified off-disk -- our ConceptNet is a **BOUNDED 10% SLICE** (max_edges=200k cap, min_weight>=2.0 filter; 179,781 edges; avg-deg 2.68, NO 10-core). Density is an INGEST ARTIFACT, not inherent. **DENSITY FLOOR calibrated off-disk (k-core, verify-before-build): FB15k-237 CLEARS (10-core 9549 nodes @ deg 39); CoDEx-L FAILS (core tops deg 23); our ConceptNet FAILS.** [[project_substrate_has_zero_grounded_measured_attribute_data_pure_symbol_graph_2026-07-10]].
**INGEST = FOUNDATIONAL FIRST-CLASS DESIGN (USER, load-bearing):** select for INFERENTIAL CAPACITY + dimensional SPAN, not volume/density/confidence. USER's spanning-core insight: **the first corpus must be a SPANNING BASIS -- new knowledge grounds only by correlating to the core, so a narrow-but-dense core (FB15k-237 named-entities) dooms everything that can't project onto it to ungroundedness.** Core = grounding-kernel (~1500 words) + NSM 65 primes + Lancaster sensorimotor, multi-channel grounded at ingest; ACCEPTANCE = 6-domain GROUNDING-REACH test (reuses the fusion discriminator). Spec: notes/research_deliberate_ingest_spec_spanning_grounded_core_2026-07-10.md.
**FOUNDATION FOUND + VERIFIED: CSKG.** Pre-merged commonsense KG (ConceptNet+ATOMIC+WordNet+Wikidata+FrameNet+Roget+VisualGenome; 2.2M/6M; 251k identity links -- solves cross-source resolution off-the-shelf). Core-density GATE PASSED off-disk: cross-cutting commonsense subgraph (strip 79% lexical dilution) = **12-core 23,632 nodes @ deg 38** (~2.5x FB15k-237's spine), ON MERIT. And it is COMMON-CONCEPT -> dense AND perceptually-groundable AND spanning-compatible (resolves the dense-vs-groundable tension). notes/cskg_commonsense_core_kcore_density_gate_2026-07-10.md.
**NOVELTY (due-diligence, USER "did they already do it?"):** CSKG = a RESOURCE not a reasoner (no grounding). Our exact combo (self-contained + glass-box + VSA + measured-grounding + spanning-core + generate-and-test) UNBUILT. Convergent partial: Rel-SAR (published VSA propose+verify abductive reasoner -- BORROW its design, validates the mechanism) + PRISM (hobbyist VSA cog-arch); Soar/OpenCog/NARS do NOT do this. Deflated novelty 0.35. Differentiation = self-contained/glass-box/grounded-from-primitives (vs COMET/LLMs borrowing grounding). notes/research_cskg_prior_art_novelty_due_diligence_2026-07-10.md.
**CRUX ENGINE IN FLIGHT (exp_dev aa0d66856396ae7c6, cell exp_crux_engine_fb15k237_vsa_gt_v1.py, agent-side smoke):** substrate-NATIVE generate-and-test = bind/unbind-compose PROPOSE (borrow Rel-SAR) + verifier + richer rules. BAR (unforgiving) = **BEAT THE FREQUENCY BASELINE** (h@1/mrr, not just degree) AND bind/unbind LOAD-BEARING (ablation must hurt) AND no LOW-tail collapse. INSTRUMENTED with (a) per-stage DIAGNOSTIC WATERFALL (candidate-recall VSA-vs-symbolic | verifier-lift | conditioned-rank) + (b) degree/freq-STRATIFIED scoring vs BOTH baselines (LOW-tail collapse = HARD_FAIL). = localizes the weak link pass OR fail.
**WEAK-LINK MAP (scour, evidence corrected director's compose-first guess):** #1 = RANK-vs-frequency (Step-1 lost there); recurring KILLER = degree/frequency confound = TAIL-COLLAPSE (reversed 4+ cells: geom-code HIGH +0.264 / LOW -0.040). Severity: RANK > density > propose > verify > COMPOSE (compose CG-in-isolation but UNTESTED as VSA-propose at scale = director's watch-item; waterfall measures it). notes/weak_link_map_generate_and_test_engine_five_stages_2026-07-10.md.
**CRITICAL PATH / FOCUS:** working system = substrate-native generate-and-test that BEATS FREQUENCY, on a dense GROUNDED SPANNING CORE. GATES both resolved (engine: density-necessary yes, mechanism-must-beat-frequency; foundation: CSKG core dense+groundable PASS). NEXT depends on the crux verdict.
**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) read the crux SMOKE off-disk -- the WATERFALL (which stage leaked: VSA-vs-symbolic candidate-recall) + degree/freq-STRATIFIED (does SUBSTRATE_GT beat FREQUENCY on LOW+MID, or tail-collapse) + bind/unbind-ablation (load-bearing?); (3) if PASS -> ship canonical FULL + skunkworks VET, then build the SPANNING GROUNDED CORE on CSKG (grounding-kernel+NSM+sensorimotor, grounding-reach test) + run engine on it = the convergence; (4) if FAIL -> the waterfall says WHICH stage to fix (likely rank-vs-frequency or compose-crosstalk); iterate the mechanism before the grounded-core build; (5) borrow Rel-SAR's actual method (fetch source) for the mechanism.
**PROCESS (load-bearing, banked): director OVER-FRAMED wanted results 3x this session** (grounding "degree-uniform"->degree-decreasing; fusion "robust"->knife-edge; engine "first forward result/ranking-edge"->loses-to-frequency/construction-proof) -- the VET/scour corrected each DOWN. Whipsaw discipline is the load-bearing safeguard; relay wanted smokes/previews as HELD, do NOT add unearned silver linings. [[feedback_dont_over_correct_on_raw_full_either_trust_adversarial_recompute_over_own_swings_2026-07-10]].
**~19:xxZ UPDATE (both FULLs running remote, dash-visible; PROCESS FIXED):** USER caught a discipline drift -- I had OVER-SCOPED smokes into heavy LOCAL runs (VSA compose on 14k entities, CSKG 6M-edge assembly), violating SMOKE-ONLY-LOCAL (local=laptop=tiny-smoke-only; intensive FULL -> orchestrator -> remote_cpu/overnight(GPU)). Fixed + memorialized [[feedback_dont_over_scope_smokes_into_heavy_local_runs_keep_tiny_route_intensive_via_orchestrator_2026-07-10]]. NOW BOTH decisive FULLs running correctly:
- **CRUX ENGINE FULL (overnight_queue GPU, running, SHA 8c238125d, monitor chain_crux_landing):** substrate-native generate-and-test with 3 beat-frequency levers (head-conditional multiplicity + hop-normalized conf + negative-evidence). SMOKE: REAL WIN = bind/unbind now LOAD-BEARING (ablation ratio 0.19 vs 0.99 pre-lever) = genuine SUBSTRATE capability, NOT a construction-proof (unlike Step-1). OPEN: no beat-frequency at tiny/noisy N (mrr 0.092 vs POP_RELFREQ 0.316; drop = hop-norm+neg-evidence VARIANCE in the low-SNR smoke regime, NOT multiplicity; exp_dev did NOT p-hack the smoke). FULL (N_DIM 2048, MIN_SUPPORT 10, all 237 rels) = the judge. WATERFALL confirms director's COMPOSE-crosstalk prediction: VSA recall@10 0.13 vs symbolic 0.44 @ N_DIM 1024 -> higher N_DIM should recover; if not, the fix is better cleanup (resonator/iterative-collapse).
- **GROUNDED CORE FULL (remote_cpu_queue, running, SHA e8ec245ca+79e1fa22c, monitor chain_landing_spanning_grounded_core, CSKG SCP-staged):** does the composed spanning core (NSM primes + grounding-kernel + Lancaster sensorimotor, on CSKG cross-cutting commonsense) SPAN meaning-space (6-domain grounding-reach)? Self-test PASSED (spanning reaches 6/6, narrow-core fails 5/6, scramble fails). Real-data span = the FULL verdict.
**CONVERGENCE ARCHITECTURE (director-derived, notes/convergence_architecture_grounding_is_the_verifier_2026-07-10.md): GROUNDING = THE VERIFIER, not the geometry.** Reconciles the whole arc: geometry PROPOSES (bind/unbind, compositional), engine composes head-conditionally, GROUNDED VERIFIER checks proposed relations against measured reality (= the loop-closer showed grounding-in-geometry fails; candidate-B showed the verifier is load-bearing; the deepest target = active/verifiable grounding). VALIDATED by the VSA<->quantum isomorphism (bundling=superposition, grounding=MEASUREMENT/collapse, capacity-crosstalk~uncertainty; published, verified) -- the verifier IS a measurement/projection operator. Grounded-verifier weak-links pre-mapped: coverage, attribute-relevance, separability, degree-confound, circularity. CONVERGENCE EXPERIMENT (after both FULLs land): does a grounded-attribute-checking verifier out-infer graph-support-only (ablation + scramble controls)? = the test of whether grounding is load-bearing FOR INFERENCE.
**NEXT-SESSION FIRST ACTIONS (updated):** (1) heartbeat; (2) read the CRUX FULL off-disk -- does SUBSTRATE_GT beat POP_RELFREQ (h@1/mrr)? read the WATERFALL (VSA-vs-symbolic candidate-recall = compose crosstalk; verifier-lift; conditioned-rank) + degree/freq-stratified tail; VET; (3) read the GROUNDED-CORE FULL -- per-domain reach, does it SPAN or which dimension missing; VET; (4) BRANCH: crux beats-freq -> real substrate inference engine, build the CONVERGENCE (grounded verifier); crux HARD_FAIL -> waterfall localizes (compose-crosstalk -> resonator cleanup / bump N_DIM; or rank; or fundamental); (5) EXECUTION DISCIPLINE now locked: tiny smoke local, intensive FULL -> orchestrator -> remote/GPU, dash-visible.
### ========== END 2026-07-10 LATE ==========

### ========== 2026-07-10 DRIVE (~15:xxZ -- SUPERSEDED by LATE block above; grounding MM arc detail) ==========
**GROUNDING WORKS (weakly) -- FIRST FAIR POSITIVE, banked MEASURED_MECHANISM (VET'd, cert_ledger 1578, math atom #8 today).** An exterior MEASURED attribute (Brysbaert concreteness, human ratings) diffused over the REAL ConceptNet graph via the normalized-Laplacian diffusion-with-restart CONSOLIDATION loop adds held-out prediction over relational-only, VALUE-dependent (scrambled fires -0.54), DEGREE-INVARIANT on the low/mid tail (LOW +0.087). Fairness gate CLEARED (F_triv 0.000 < F_A 0.547 < C 0.689). REPRODUCED bit-for-bit by VET (not telemetry). **BUT WEAK: grounding_gap 0.059 mean but cv=0.69, only 2/5 seeds clear 0.05, carried by 1 seed (0.135); HIGH-degree stratum WASHES OUT (0.029, 3/5 neg).** Mechanism (honest+coherent): the measured channel adds power PRECISELY WHERE relations UNDERDETERMINE the attribute (low/mid degree); at high degree F_A saturates near C so the exterior channel adds nothing. **This is PASSIVE grounding (diffuse a borrowed measured channel) -- the arc's 'grounding needs ACTIVE intervention / actively-sampled exogenous referent' wall still stands ABOVE this.** VET banked the MM->CG criterion: gap>=0.05 AND cv<0.15 AND leave-one-seed-out robust AND HIGH stratum non-negative.
**KEY ENABLER FINDING (off-disk verified + memorialized): the substrate has ZERO grounded/measured attribute data** -- concept partition 189,654 relations, metadata EMPTY 100% (pure symbol graph: SYNONYM/IS_A taxonomy); science+wikidata 0 numeric. So grounding testbeds MUST ingest fresh grounded data (curl to data/grounding_testbed/ with provenance, NOT canonical store). [[project_substrate_has_zero_grounded_measured_attribute_data_pure_symbol_graph_2026-07-10]].
**IN FLIGHT (2hr full-auto drive):** (a) exp_dev a547 building the MULTI-ATTRIBUTE FUSION strengthener = the 'get it working' build: fuse concreteness + Lynott-Connell sensory-modality + (valence/AoA if independent) via ATL-hub reliability-weighting, targeting the VET promotion criterion; HARD guardrails from the fusion drill (notes/research_multi_attribute_grounding_fusion_ATL_hub_2026-07-10.md) = PER-ATTRIBUTE INDEPENDENCE GATE (imageability/concreteness r=-0.80 trap) + REDUNDANCY-FAKING must-fail control + coverage-density diagnostic first. (b) exp_dev a297 building the PILLAR-2 native-router cell (low-rank SR/spectral router vs dense-collapse curve + oracle-leak + degree controls; notes/pillar2_native_router_build_spec_2026-07-10.md). 
**SEQUENCING:** strengthen grounding to ROBUST first (multi-attribute), THEN build rung-2b LOOP-CLOSER (does grounded geometry fix INDUCTIVE RELATIONAL inference degree-invariantly = the ORIGINAL wall; notes/grounding_rung2_loop_closer_design_2026-07-10.md). Don't build relational-transfer on a marginal base. Rung-2c = grounded-INGEST capability pipeline (the foundational build). Deepest target = ACTIVE/verifiable-oracle grounding (math/code/sim; dual-number toehold).
**DESIGN ARTIFACTS (all fair-test-gated):** scour notes/relational_capability_track_record_scour_2026-07-10.md; grounding+fairness notes/grounding_anchor_design_first_testbed_2026-07-10.md; rung-2 notes/grounding_rung2_loop_closer_design_2026-07-10.md; pillar-2 notes/pillar2_native_router_build_spec_2026-07-10.md; spectral-math notes/research_spectral_diffusion_concept_geometry_2026-07-10.md.
**ARC RESOLVED (~16:xxZ, the finish-line answer -- pending loop-closer FULL+VET confirmation):** the rung-2b LOOP-CLOSER (does grounded geometry improve held-out RELATIONAL inference) = **HARD_FAIL_GROUNDING_DOESNT_TRANSFER** (smoke, self-test VALIDATED: detects planted transfer 0.10>0.05, scramble control fires -> genuine). GROUNDED vs UNGROUNDED relational lift = +0.004 (~nil), LOW stratum -0.019. FULL shipped remote_cpu 3-seed (as-authored; prereg+cardinality gate lock 3 seeds; CPU-kept to avoid f32/f64 confound on the near-zero measurement), SHA 42620e194, ETA ~1.5-2.5hr, monitor bdn07o7yd -> skunkworks VET on landing.
**THE EARNED MAP of "make relational real":** SOLVED/banked = composition (bind/unbind CG), reason-OVER-known (traversal MM, goal-conditioning MM), and NEW this session = GROUNDING of concepts to exterior measured meaning (concreteness+fusion MM, real, degree-robust, from ZERO grounded data). REMAINING WALL precisely located = inductive relational INFERENCE (infer NEW relations beyond the ingested graph). Ruled out with evidence as levers: better codes (additive/TransE HF), better encoder (structure-aware HF), relation-richness (ladder HF), density-subsetting (k-core HF), learned-SR (HF), AND now GROUNDING/geometry (loop-closer HF) + low-rank routing (Pillar-2 HF). **UNIFYING PRINCIPLE (2 independent negatives converge): geometric/grounding SMOOTHING is the wrong tool for DISCRETE HIGH-CAPACITY operations (routing, relational chaining). Grounding anchors what a concept MEANS; it does not manufacture relational STRUCTURE not latent in a thin graph.**
**NEXT-ARC FORK (the two candidates left standing, neither tested):** (A) MORE KNOWLEDGE -- genuinely ADD dense real RELATIONAL knowledge (not attribute-grounding, not subsetting) and test if inductive inference improves = the thin-graph hypothesis, the arc's convergent lean (substrate-knows-almost-nothing); we now HAVE a grounded-ingest pipeline to build it. (B) ACTIVE/GENERATIVE inference mechanism -- something that PROPOSES + TESTS candidate relations rather than reading off a smooth surface (active grounding / abduction / intervention). DIRECTOR LEAN = (A) more real relational knowledge (best-supported by the 5-negative convergence), but HOLD the build until the loop-closer FULL+VET confirm the negative, AND surface the fork to USER (program-reshaping choice). Deepest target unchanged = ACTIVE/verifiable-oracle grounding (math/code/sim; dual-number toehold).
**DRIVE TALLY (2hr full-auto):** grounding built from zero -> MM (concreteness) -> strengthened MM (fusion) banked; Pillar-2 router HF banked (low-rank != addressing); loop-closer HF shipping (grounding != chaining). Whipsaw discipline held throughout -- every wanted result (grounding HARD_PASS, fusion "degree-uniform", router rise-with-load smoke) held until VET, and VET corrected director framing DOWN 3x (pop-shortcut->degree-dependence; degree-uniform->degree-decreasing; fusion-robust->knife-edge). Cert today: ~11 math atoms, ledger ~1580.
### ========== END 2026-07-10 DRIVE ==========

### ========== 2026-07-10 CONTINUED (~14:xxZ -- GEOMETRIC-CODE LEVER CLOSED + PIVOT TO CONSOLIDATION+GROUNDING) ==========
**USER STRATEGIC LOCK (07-10): RELATIONAL = THE CORE REQUIREMENT / program SPINE.** "make the relational aspect of substrate real - this is the way." Memorialized [[project_relational_capability_is_the_core_requirement_make_it_real_USER_2026-07-10]]. Full relational track-record scour off-disk: notes/relational_capability_track_record_scour_2026-07-10.md (seam: memorized-structure=all CG [bind/unbind, traversal, regen-cleanup] vs inductive-inference=all HF; 2nd wall surfaced: proven depth-50 traversal RIDES ON A PARTITION-ORACLE ROUTER, every native/learned/typed router failed at sqrt(N/M) load).
**GEOMETRIC-CODE DEGREE-CONTROL RETEST FULL LANDED + VET'd + ATOMIZED (cert_ledger 1576->1577, math HARD_FAIL).** The decisive retest of "additive-geometric (TransE) codes double inductive inference": VERDICT = **HARD_FAIL, additive-geometric codes are NOT a degree-INVARIANT inductive lever.** Numbers (3 seeds, all controls valid incl. the now-FIXED DistMult transductive 0.986): aggregate reach@1 TRANSE 0.187 vs DISCRETE 0.089 (d 0.098) REAL, but degree-STRATIFIED d(te-dis) LOW=-0.040[3/3 seeds neg] / MID=+0.085[3/3] / HIGH=+0.264 -> the win CONCENTRATES on high-degree and REVERSES on the rare tail (discrete WINS the low-degree tail). **VET FRAMING CORRECTION (symmetric, anti-negativity): the cell's "POPULARITY_SHORTCUT" label OVER-states -- pop_recovers=FALSE (pop_recover_frac 0.524<0.60), TransE beats popularity by 0.089 and has a genuine MID-tier advantage popularity entirely lacks. The FAIL is TAIL-COLLAPSE (degree-DEPENDENCE), NOT popularity-recovery. The geometry does something REAL, just only for well-connected concepts; it fails exactly where we need it (rare/novel).** DIRECTOR OVER-STATED to USER as "pure popularity mirage" -> corrected per VET. Converges with 4 prior negatives (learned-SR codes_necessary=False, richness slope 0.002, structure-encoder deltaM5 neg, graph-inductive-ceiling MM) -> **CODE-FORMAT IS NOT THE INDUCTIVE LEVER. Last code-geometry lever CLOSED.**
**SSP construction drill (notes/research_ssp_fractional_binding_..._2026-07-10.md): even fractional-binding SSP won't cure it -- degree bias lives in COORDINATE-FITTING FROM THIN SKEWED DATA, not the code; no binding-op swap (HolE/ComplEx confirm) fixes it (P_deflated 0.28). Triangulates: code is not the bottleneck, KNOWLEDGE is.**
**THE PIVOT (USER-driven, the new architecture bet):** stop swapping codes; build a PROCESS + a GROUND TRUTH. (1) a SLOW CONSOLIDATION LOOP ("sleep"/CLS: hippocampus-fast + neocortex-slow gist extraction) that REARRANGES concept codes by accumulated MEANING over many passes -- lets geometry SETTLE (degree-invariant because driven by AGREEMENT not frequency), vs the one-shot fit that shortcut. (2) GROUNDING = independent channels that must AGREE (the ball-looks-soft/squeezes-hard SURPRISE); consolidation is the ENGINE, exterior multi-channel correlation is the FUEL. Ground-truth menu ranked by how much it PUSHES BACK: semantic/distributional co-occ = consolidation FUEL not an anchor (reorganizes, doesn't ground); multi-channel agreement (structural+attribute) = degree-invariant, buildable; ingested empirical attributes = real referents; verifiable/executable oracles (math/code/sim, active intervention) = strongest (we have the dual-number bake-in toehold). All self-contained (data/oracles, not a borrowed model). Geometry/SSP RELOCATES to Pillar-2 (native routing), where it is genuinely the right tool.
**IN FLIGHT (07-10 ~14:xxZ):** (a) exp_dev a519 building the MINIMAL CONSOLIDATION-LOOP degree-invariance cell = direct REMATCH vs the failure (rearrange real ingested codes to honor cross-channel agreement; reuse the retest's degree-strata + pop-baseline + oracle-leak; HARD_PASS = consolidated geometry SURVIVES the low-degree tail where one-shot collapsed; load-bearing constraint = the two channels must be GENUINELY INDEPENDENT or it re-derives the shortcut). (b) research a0918 prior-art drill (consolidation/replay, multi-view agreement, degree-bias fixes, grounding sources, failure lit -- for ideas/pitfalls, NOT truth). Pillar-2 native-router design note DONE: notes/pillar2_native_router_geometric_design_2026-07-10.md (Vector-HaSH + SSP on our own FHRR; anti-collapse=low-rank SR spectral basis, UNTESTED inference must measure; native-LOCAL+external-recalibration NOT oracle-free).
**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) collect consolidation-cell smoke (a519) + prior-art drill (a0918); (3) if smoke clears -> orchestrator ships FULL, skunkworks VET (does consolidation produce degree-INVARIANT geometry = the rematch verdict); (4) GROUNDING is now CENTRAL (the exogenous referent gates inductive inference) -- pick + build the ground-truth anchor (multi-channel agreement + a verifiable oracle); (5) Pillar-2 native-router cell from the design note; (6) Pillar-3 relational primitives (same-different/object-permanence to CG, agency untested) deferred.
### ========== END 2026-07-10 CONTINUED ==========

### ========== OVERNIGHT UPDATE 2026-07-10 (~1x:xxZ -- SUPERSEDED by the block above; kept as detail) ==========
**GEOMETRIC-CODE MECHANISM FULL LANDED = MIDDLE_BAND_PARTIAL_GEOMETRIC_INFERENCE (3 seeds, LEANS POSITIVE, VET-PENDING -- HOLD per the night's whipsaw lesson).** The pivotal next-arc test: does giving the substrate ADDITIVE-GEOMETRIC (TransE h+r~=t) codes enable held-out INDUCTIVE inference where its DISCRETE binding codes fail? Result (COMPLETABLE reach@1): TRANSE=0.187 vs DISCRETE=0.089 (2.1x) vs RANDOM=0.014 vs ORACLE ceiling=0.649; MRR TRANSE=0.317 vs DISCRETE=0.165. **d(transe-discrete)=0.098 -- JUST under the +0.10 HARD_PASS bar (a hair short); codes_necessary=True (geometry does real work).** So geometric codes ROUGHLY DOUBLE inductive inference over the current discrete codes but land just shy of the ambitious "definitive win" bar -> MIDDLE. This is the FIRST positive-leaning lever for inductive inference (richness-alone was refuted). Shipped straight to FULL on the self-test gate (local KGE smoke was compute-blocked) -> NO smoke pre-read.
**VET VERDICT (landed, math MM +1 confound-OPEN + meta +1): the TransE 2x over discrete is REAL and 3-seed robust (cv 0.031, d per-seed [0.108,0.088,0.098]) -- BUT whether it is genuine GEOMETRIC inference or a DEGREE/FREQUENCY CONFOUND is UNRESOLVED.** The cell did NOT stratify held-out hits by degree, has NO degree-only baseline, and its additive-specificity control (DistMult 0.012 < random 0.014) is DEGENERATE (didn't train -- proves nothing). #4 already established KGE held-out wins on THIS ConceptNet subgraph are largely PA/DEGREE artifacts, so the risk is real. HONEST CONCLUSION: additive-geometric codes = PROMISING but UNCONFIRMED inference lever (MEASURED_MECHANISM, confound-open, ~29% of oracle) -- NOT a proven degree-controlled lever, NOT a confounded dead-end. Director LEANED-POSITIVE-but-HELD = VET-CONFIRMED correct (no over-claim). **CORRECTION: the earlier "discrete ties random on held-out" framing is WRONG in this filtered-reach metric -- discrete=0.089 is +0.074 (6x) OVER random 0.014 (the "tie" was a different far-neg-AUC metric at ceiling); so the discrete codes DO carry inductive signal, geometry ~doubles it.** NEXT CELL (mandatory before any CG promotion): degree/frequency-STRATIFIED decomposition of TransE's held-out hits + a DEGREE-ONLY popularity baseline + a properly-trained DistMult/ComplEx control -- does the geometry win SURVIVE degree control?
**COMPACTION STATE (2026-07-10): reasoning arc RESOLVED + this overnight lead. NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) collect the geometric-code VET (dispatched ~now, anchor grounding_additive_geometric_inductive_v1, metrics data/exp_grounding_additive_geometric_inductive_v1/metrics.json) -> is the 2x-geometry-over-discrete REAL (a machinery lever for inference = pursue geometric codes as barrier #1's specific objective) or confounded; (3) if real -> build up the geometric-code encoder (push past the 0.10 bar: more KGE training / RotatE / combine with the richness axis); (4) cross-domain transfer (drill's #1 axis) as the parallel/heavier lever; (5) GROUNDING (2nd real wall, exogenous-referent) still queued; (6) store-sync (~37+ atoms pending); (7) batch-VET ladder-FULL (richness-not-lever, clean) if not already. RICHNESS-ALONE REFUTED (ladder FULL MIDDLE, slope 0.002, degree-controlled clean). All in the RESOLUTION block below.
### ========== END OVERNIGHT UPDATE ==========

### ========== SESSION RESOLUTION 2026-07-09 (~00:xxZ -- CORRECTED by #4 FULL below; extends into OVERNIGHT UPDATE above) ==========
**VET-FINAL (#4 FULL VET'd, math atom #36 MEASURED_MECHANISM -- this is the EARNED conclusion; the smoke narrative below SUBSTANTIVELY HOLDS):** the #4 FULL scored MIDDLE_BAND but the VET (degree-controlled recompute, 3 seeds) found the "inversion" was ITSELF AN ARTIFACT, not a substantive reversal: (1) best_minus_code=+0.062 is a **PA DEGREE ARTIFACT** -- PA (pure node degree) is the only method beating codes in sparse, and it COLLAPSES 0.755->0.622 in the degree-homogenized k-core. (2) Excluding PA: the one genuinely-relational winner is GCN at only +0.038 sparse -> +0.008 dense (its features are 3/4 degree-derived; vanishes under degree control); classic CN/AA/RA/JC LOSE to codes (-0.054). (3) The MIDDLE_BAND-vs-HARD_FAIL label hinged ENTIRELY on PA crossing the 0.75 cap by 0.005 via an internal gate inconsistency (cap-gate included PA; density-gate excluded it) -- with the relational subset the prereg's own rationale endorses, it scores HARD_FAIL_KNOWLEDGE_IS_THE_LIMIT. **HONEST CONCLUSION: ~85% KNOWLEDGE-thin / ~15% small degree-confounded machinery. SIGNAL_EXISTS=False survives robustly; codes ARE at the degree-controlled RELATIONAL ceiling. ROADMAP: richer INGEST = PRIMARY lever; a message-passing/GNN encoder = SMALL bounded SECONDARY probe (NOT co-equal, NOT a ceiling-breaker).** Density: relational-rise +0.033 marginal (2/3 seeds below margin) BUT codes gain a ROBUST +0.063 from denser k-core -> density helps the codes.
**DIRECTOR WHIPSAW (owned, both directions): over-relayed the smoke as "resolved" to USER (premature -- needed degree-control), THEN over-corrected to "un-resolved / might be machinery" (also wrong -- the inversion was a PA/gate artifact). The VET's degree-controlled recompute landed the earned truth IN BETWEEN: the USER's original knowledge-thin intuition HOLDS (~85%), rigorously. Lesson: hold conclusion-narrative until FULL+VET, hardest when the preview is clean AND confirms a wanted story; and don't over-correct on a raw FULL either -- the degree/gate confound needed the VET.**
**DENSITY-PAYOFF RESOLVED (VET'd, HF atom + META methodology atom):** the density-payoff FULL = HARD_FAIL (density doesn't raise substrate reasoning reach) is a **BRANCHINESS/SUBSET CONFOUND, NOT a refutation of richer-knowledge.** The KNOWN_T oracle ceiling ALSO collapses on the dense k-core (0.462->0.043, 10.7x) -> the dense graph is intrinsically harder to route (higher out-degree = more same-relation siblings = lower 1-of-many reach@2 ceiling); CEILING-NORMALIZED the substrate's relational signal is STRONGER on dense (learned/oracle 0.80 vs 0.25). CRUCIAL SCOPE: the "DENSE" arm was a k-core = a 469-node SUBSET of the 4440-node graph (FEWER nodes, not ADDED knowledge). So it refutes ONLY "densify-a-subset-via-kcore = lever," NOT "richer knowledge." META atom banked: check the oracle ceiling across a density/richness ladder + normalize by dynamic-range BEFORE reading a flat gain as "doesn't help." DIRECTOR HELD interpretation = VET-CONFIRMED correct (no over-read this round -- the whipsaw lesson applied).

**==== NIGHT'S ARC RESOLVED (the honest bottom line) ====** The substrate REASONS OVER known knowledge (goal-conditioned SR = MM, reach@2 ~0.46; goal-conditioning tames branchiness). It CANNOT INFER BEYOND the ingested graph (held-out inductive reasoning barely beats random codes, margin ~0.011) -- and this held-out INFERENCE is the genuine hard frontier. WHY: (a) the graph is thin -- our codes are at its degree-controlled relational ceiling, no method captures strong inductive signal (#4 VET, ~85% knowledge-limit / ~15% tiny degree-confounded GNN edge); (b) naive "add edges via density" is a confounded non-lever (branchiness). ROADMAP (VET-scoped, NEXT ARC): (1) test the UNVARIED RICHNESS AXES -- ADD relation-TYPES / distinct ENTITIES / cleaner edges (real knowledge addition, NOT subsetting), match out-degree to avoid the branchiness confound; (2) attack INDUCTIVE-INFERENCE directly (better code-space generalization to withheld nodes -- the small GNN/message-passing edge is the cheap bounded probe); (3) reasoning-OVER-known (the MM) is the STANDING capability to build on. GROUNDING (2nd real wall, exogenous-referent) still queued as a separate front.
**PROCESS (the night's meta-win): USER rigor converted a premature "it's the encoder / resolved" into a rigorous 5-hypothesis elimination; Director whipsawed once (over-relay -> over-correct, VET landed the truth between) then HELD correctly on density-payoff. Lesson banked: hold conclusion until FULL+VET; don't over-correct on a raw FULL either (confounds need adversarial recompute); trust the degree/ceiling-controlled VET over own swings; be hardest on wanted results.**

**[SMOKE NARRATIVE -- substantively CONFIRMED by VET above, modulo the ~15% GNN secondary caveat]:**
**[SMOKE NARRATIVE, PREMATURE -- corrected above] THE REASONING WALL IS KNOWLEDGE-THINNESS, NOT THE MACHINERY. (5-hypothesis elimination, USER-driven.)** After the SR deep-prize came back MM (graph-search-on-handed-map, not autonomous reasoning), the CG path = LEARNED reachability (reason from own codes, generalize to HELD-OUT structure). It FAILED -- and the full investigation of WHY resolved cleanly:
- **#1 ENCODER (codes too blurry) = CANONICALLY REFUTED.** encoder-sharpening FULL 3 seeds: structure-aware training did NOT improve held-out M5 (deltaM5 -0.012/-0.017/-0.024, best=hybrid; node2vec transductive-worse). Sharper LOCAL structure (M3 up) does not convert to held-out generalization.
- **#2 TEST-UNSOLVABLE = REFUTED** (fairness audit): held-out task is FAIR (0.44 ceiling is intrinsic branchiness NOT severance; full-map arm un-severed, held-out subset slightly easier; wide headroom; negative genuine + telemetry-sensitive).
- **#3 READOUT-TOO-WEAK = REFUTED** (strong-readout cell): RIDGE + KERNEL readouts on the SAME codes STILL tie random on held-out (margin -0.019 at reach@1). A smarter use of the codes does not rescue it.
- **#5 COMPOSITION-OVER-HOPS = REFUTED** (readout reports reach@1 primary): fails already at SINGLE hop, so it's not a chaining/compounding problem.
- **#4 KNOWLEDGE-TOO-THIN = CONFIRMED (smoke; FULL shipping a5d7638d).** graph-inductive-ceiling: NO method beats our codes -- classic LP (CN/AA/RA/JC) 0.642, GNN 0.677, PA(degree) 0.717, our CODE_COSINE 0.721 (best_minus_code = -0.004). Our codes are AT the graph's OWN structural ceiling; SIGNAL_EXISTS(>=0.85)=False. On the sparse graph only DEGREE (PA) clears 0.65 -- relational predictors near chance = thin-knowledge signature. Self-test VALID (detects signal: SBM 0.925 vs random ER 0.614). **CEILING RISES WITH DENSITY: +0.078 in the 5-core (mean-deg 5.6->8.7) -> DENSER KNOWLEDGE ENABLES MORE REASONING.** = USER's very first intuition ("substrate too small / very little knowledge?") CONFIRMED + quantified (refined: not too few NODES -- size_flat -- but too SPARSE/thin for structural induction).

**THE REFRAME (load-bearing):** the substrate CAN reason OVER known knowledge (SR on the known map = MM, reach@2 0.434 = real multi-hop traversal of ingested graph). It CANNOT infer BEYOND the ingested graph (held-out = INDUCTIVE reasoning) -- and on a graph this sparse, inferring-beyond is NOT achievable by ANY method (our machinery is at the ceiling). So "can't reason" was a KNOWLEDGE problem, not an architecture problem, for the whole arc. Ties to the foundational anchor: SUBSTRATE KNOWS ALMOST NOTHING.

**STRATEGIC PIVOT (the roadmap changes): the reasoning enabler is INGEST (richer/denser knowledge), NOT a better encoder/reader/reasoner.** The machinery is fine (at the ceiling). Path to inductive reasoning = feed more/denser/cleaner knowledge; the density-ceiling curve quantifies the payoff.

**NEXT-SESSION FIRST ACTIONS (updated):** (1) heartbeat; (2) collect #4 FULL (a5d7638d) + VET the headline (is codes-at-ceiling / no-signal / density-rise earned at full scale -- this is the biggest claim, VET hard); (3) DENSITY-CONFIRMATION experiment: does the substrate's ACTUAL learned-SR held-out reach RISE on a DENSE subgraph (control the density-vs-difficulty / PA-degree confound the #4 agent flagged)? = the payoff proof that richer knowledge -> the substrate reasons; (4) PIVOT roadmap: richer/denser KNOWLEDGE INGEST as the reasoning enabler (was treating reasoning as a machinery gap all arc -- it's an ingest gap); (5) batch-VET encoder-FULL + readout-FULL + learned-SR-FULL + #4-FULL; (6) GROUNDING (2nd real wall) exogenous-referent build still queued. Store-sync pending.

**PROCESS (USER rigor drove this):** USER pushback -- "certain it's the encoder?" -> "other explanations?" -> "too small?" -- converted a premature "it's the encoder / locked" over-claim into a clean 5-hypothesis elimination that landed on the KNOWLEDGE (the USER's original instinct). Over-claim caught THREE times this arc; the fix = enumerate the hypothesis space, test to falsify, don't commit to the first story. Off-disk-read discipline reinforced (read canonical metrics myself, not agent reports).
### ========== END SESSION RESOLUTION ~00:xxZ ==========

### ========== SESSION DELTA 2026-07-09 (~23:2xZ -- extends into RESOLUTION above) ==========
**BOTH LOAD-BEARING WALLS NOW RESOLVED via the fairness lens (the session's core instrument):**
- **READER WALL = DISSOLVED** (fairness artifact; substrate at 81-86% of info-ceiling; goal-conditioning MM banked). [detail in ~22:0xZ block below]
- **GROUNDING WALL = REAL (canonically CONFIRMED at FULL, VET'd):** the ~0.38 self-play failmask-correlation SURVIVES difficulty control -- raw 0.381, shared-difficulty floor only 0.024, difficulty-partialled residual 0.358 (4 convergent partialling methods 0.358-0.379), determinacy 0.996 (task WELL-POSED, unlike reader's 32%), mirror control valid, self-test separates planted-difficulty(collapse) from planted-coupling(stay). = a GENUINE mechanism coupling / real substrate bound, the OPPOSITE of the reader. So the fairness lens DISSOLVED one wall + CONFIRMED the other = proven wall-vs-artifact INSTRUMENT (banked as a META atom). **-> the GROUNDING THESIS STANDS as the real path: grounding needs an EXOGENOUS, actively-sampled referent / genuinely differentiated internal channels (the self-play halves share a REAL blind spot).**
  - HONEST CAVEATS: grounding per_item_masks.npz sidecar is ABSENT on disk (result well-cross-checked via aggregate+exact-arithmetic+controls, but NOT raw-per-item-re-auditable -- fix sidecar persistence). Fairness metric fix = attenuation-free CROSS-MODEL co-failure decomposition (single-model partial was attenuation-limited).
- **AUTONOMOUS REASONING (the CG deep prize) -- goal-conditioned RETRIEVAL works, autonomous DERIVATION is hard:**
  - Goal-conditioning MM (supplied waypoint) = the STANDING capability (reach@2 0.499, 3-part-standard capability, +0.303 canonical).
  - Autonomous subgoal DERIVATION: 2 proximity heuristics FAILED -- greedy goal-cosine = MIDDLE_BAND MM (reach@2 0.181, 0.363x ceiling, dimension-scales but insufficient); landmark/hub routing = HARD_FAIL (0.111, WORSE than greedy; nearby-hub-not-on-geodesic-toward-goal; NB the mechanical precheck said STOP, exp_dev overrode on betweenness-gini-0.866 judgment, cell failed anyway).
  - DIAGNOSIS: proximity (to goal OR hub) != REACHABILITY (leads-toward-goal). The fix = SUCCESSOR REPRESENTATION (= personalized PageRank; the ONE signal both multi-step AND goal-conditioned, explaining both failures). Closed-form sparse solve (I-gamma*T)x=e_goal per goal, pick neighbor max x[v].
  - **SR-ROUTING RESULT (VET'd): HARD_PASS earned but TIER = MEASURED_MECHANISM, NOT CG. Deep-prize CG (autonomous REASONING) NOT delivered.** SR reach@2=0.434 (3 seeds 0.428/0.436/0.439, cv 0.011, grew smoke 0.366->full), @3=0.416 (1.116x supplied ceiling); beats greedy 0.181 + landmark 0.111 with identical goal access. BUT scope settled AGAINST reasoning: the routing intelligence is CLOSED-FORM GRAPH SEARCH over the KNOWN transition matrix, NOT the substrate composing its LEARNED codes. Tells: (i) self-test aliases the learned-code term to useless -> SR STILL routes 1.0 (SR alone routes); (ii) reach@4=0.635 >> supplied@4=0.149, SR hands G's graph location while learned-code lookahead decays = graph-search signature. Learned codes load-bearing only for RETRIEVAL/commit (capped at binding-fidelity 0.500, ratio 0.869), NOT for routing. Certified MM: "autonomous goal-directed traversal recovering 0.869x supplied ceiling via closed-form SR-reachability over the known graph." PROCESS: FULL metrics never synced to local d:/AI (landed marsh@home:C:/dev); skunkworks SSH-pulled authoritative + recomputed; Director read numbers off orchestrator REPORT not own off-disk (minor slip, caught, numbers held).
  - **PATH TO THE CG DEEP PRIZE (VET-mapped, THE next experiment): LEARNED / PARTIAL-GRAPH SR** -- derive the reachability signal from the substrate's OWN learned codes (successor FEATURES) or an INCOMPLETE graph it must infer, NOT the god's-eye transition matrix T. That = the substrate genuinely REASONING (routing intelligence from learned representations), not graph-searching a handed map. Plus the reciprocal-necessity discriminator (SR-only arm, base=0 on real data) to isolate the learned-code contribution. Brain-ground (successor features are LEARNED in the brain) -> build.
- **CERT (today): 34 math atoms + META. Batch VET restored 4 grounding HFs to CONFIRM_STANDING + banked fairness instrument. Store-sync PENDING (needs_orchestrator_store_sync=True -- batch a dedicated sync).**
- **NEXT-SESSION FIRST ACTIONS (updated):** (1) heartbeat; (2) collect SR-routing cell verdict (a244011a) -> if PASS ship FULL + VET = autonomous reasoning; if FAIL, autonomous-derivation frontier bounded (goal-conditioning-supplied is the capability); (3) GROUNDING is REAL -> build the exogenous-referent / differentiated-channel grounding cell (the confirmed real path); (4) build goal-conditioning as operational hdlab layer (PFC-goal component) + re-dogfood director_kb_query; (5) attack ~0.395 reader true-chaining residual (per-hop cleanup/encoder); (6) dedicated store-sync of today's atoms.
- **SESSION META-WIN:** stopped throwing mechanisms at walls; built the FAIRNESS INSTRUMENT that tells real walls from fake ones (dissolved reader, confirmed grounding). Lessons banked: 📏 compute-test-info-ceiling-before-fix-cells; 🏗️ construction-proof!=capability (3-part standard); hold-stories-pre-VET (both directions -- caught downward over-reads too). MEMORY.md tiered (secondary index).
### ========== END SESSION DELTA ~23:2xZ ==========

### ========== SESSION-CONSOLIDATED 2026-07-09 (~22:0xZ -- extends into DELTA above; supersedes the ~18:3xZ block below) ==========
**THE HEADLINE: THE READER MULTI-HOP "WALL" WAS AN UNFAIR TEST, NOT A SUBSTRATE LIMIT. Reader is AT CEILING; goal-conditioning is a real MM capability; the CG path (deep prize) is named + in flight.**
- **READER RESOLVED (VET-adjudicated):** the multi-hop test was UNDERDETERMINED-BY-CONSTRUCTION -- the per-hop query bind(role_r, Z[cur]) is a pure function of (cur, relation), so all k same-relation siblings give the IDENTICAL query -> no decoder beats 1/k. Ceiling reach@1=0.562/reach@2=0.198. The substrate MEASURES setacc@1=0.862 (routes to a TRUE sibling 86%), achieved/ceiling=0.806 (81% of info-max). **The reader chains fine; the "wall" was our unfair query.** (This dissolves a 4-cell arc of HARD_FAILs that mis-attributed a test artifact to the substrate.)
- **GOAL-CONDITIONING = REAL MM CAPABILITY (passes the 3-part standard: different-channel + live-alternative + necessity-under-ablation):** supplying a waypoint to the query lifts downstream reach@2 4.1x (0.121->0.500), hop-1 to 0.756; +0.303 cross-seed, GREW smoke->full (not washed); runs on the substrate's OWN bind op (clean codes=1.0 vs real 0.756 -> binding fidelity is the graded limiter = genuine capability, NOT construction-proof). **SCOPE: goal-conditioned RETRIEVAL with a SUPPLIED waypoint -- NOT autonomous chaining yet.**
- **THE CG PATH (deep prize, banked expansion criterion): AUTONOMOUS SUBGOAL DERIVATION** -- same lift when the substrate DERIVES the subgoal itself from (query, final-goal). Brain-ground drill IN FLIGHT (research a7d38786: hippocampal preplay/forward-sweeps + PFC subgoal-decomposition + VSA realization). On return -> build the autonomous-subgoal cell = autonomous glass-box multi-hop reasoning.
- **IDEAL IMPLEMENTATION (arc-convergent, smoke+FULL supported):** reader = bind/unbind (CG) + LOCAL-NEIGHBORHOOD scoping (MM, hippocampal-index, 6x/670x) + GOAL-CONDITIONING (MM) + KEEP correlated codes (do NOT orthogonalize -- v3 proved decoupling HURTS). = hippocampal goal-directed replay + PFC goal-maintenance + local pattern-completion. 3 of 4 pieces certified; goal layer is the build.
- **HONEST RESIDUAL:** on fully-determined (unique-successor) chains, true 2-hop fidelity = reach@2 0.395 (< independent-hop 0.478 = genuine compounding error), improvable NOT a floor. = the real reader-improvement target (per-hop cleanup / encoder fidelity).
- **LEDGER CORRECTED (net -1HF +1MM, symmetric honesty):** v2 grounding_multihop_local_chain_index_v2 HARD_FAIL_SEMANTIC_FLOOR -> HF_TEST_DESIGN_FAILURE (KEEP count-tax LOCAL-6x-GLOBAL MM leg); one-root synthesis MM -> DEMOTED (reader wall was query-underdetermination NOT encoder-collapse); decisive_win MM -> AMENDED. Banked positives: reader-at-ceiling + goal-conditioning MM.

**GROUNDING NOW STANDS ALONE (one-root demoted) -> NEXT MAJOR FRONT = GROUNDING FAIRNESS AUDIT:** is the grounding channel-invariant ~0.33 residual ALSO a query/task underdetermination artifact, or a real substrate bound? Apply the SAME info-ceiling lens. Could dissolve the grounding wall OR cleanly isolate the real problem. (Grounding prior state: composition solved, needs exogenous+active-intervention referent -- but that whole framing must now be re-checked for fairness.)

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) collect the autonomous-subgoal drill (a7d38786) -> build the CG autonomous-subgoal cell (the deep-prize experiment); (3) GROUNDING fairness audit (info-ceiling lens on the grounding cells); (4) build goal-conditioning as operational hdlab layer (PFC-goal component) + re-dogfood director_kb_query with it; (5) attack the ~0.395 true-chaining residual (per-hop cleanup/encoder).

**PROCESS LESSONS BANKED TODAY (all in memory + MEMORY.md top anchors):** (a) 📏 COMPUTE THE TEST'S INFO-CEILING BEFORE ITERATING FIX-CELLS -- a sub-ceiling score is a TEST artifact not a substrate wall (this cost 4 cells; USER caught it in 1 question); (b) 🏗️ CONSTRUCTION-PROOF != CAPABILITY WIN -- 3-part standard (different-channel + live-alternative + necessity-under-ablation); (c) hold stories pre-VET (extended to positive sub-findings). MEMORY.md TIERED (18.5->13.3KB): situational disciplines moved to MEMORY_locked_disciplines_index.md secondary; top = always-on anchors only. Scalable pattern: situational memories -> section indexes.

**INFRA:** store fully synced (81-atom backlog cleared, partitions load-clean, queryable). Session cert tally today: 31 math atoms / 32 ledger entries. 1 CPU + 1 GPU runner (concurrency capped at 2 -- USER lever to add runners if more parallelism wanted). CRON a40824ce.
### ========== END SESSION-CONSOLIDATED ~22:0xZ ==========

### ========== SESSION-CONSOLIDATED 2026-07-09 (~18:3xZ -- SUPERSEDED by ~22:0xZ block above; kept as detail) ==========
**THE DAY'S ARC (composition SOLVED; GROUNDING = the wall; grounding needs ACTION):**
- **CG:** bind/unbind operator = CHAIN_GRADE (perfect systematicity + lossless typed-binding under superposition). Composition is solved.
- **GROUNDING WALL, located precisely over 3+ confirmed negatives (settling HF, differentiation HF, DG-pattern-sep HF, B1+EXOG HF):** a purely-internal/symbolic substrate CANNOT manufacture referential grounding from itself -> needs an EXOGENOUS REFERENT (real data as 'world', NOT an external model). AND (grounding decider B1+EXOG, VET-confirmed): PASSIVE prediction of shared data GROUNDS (perturb-ratio ~3.6 causal) but does NOT decorrelate the two self-play halves (a SHARED target correlates them) -> **must be ACTIVELY SAMPLED, not passively predicted.** = how infants ground (manipulate, not watch; Held-Hein kitten-carousel). Mechanism (5x negative-drill, brain+ML+control-theory convergence): internal loops decorrelate NOISE not shared BIAS; only an exogenous ground-truth breaks a shared-bias fixed point. Brain-consistent: wake-sleep also converges on shared bias, brain grounds via the external world.
- **~0.38 STRUCTURAL CEILING (KEY OPEN CRUX):** corr(failmask) plateaued at ~0.38 across FOUR mechanisms (DG 0.377 / B1 0.393 / B1_EXOG 0.382 / B2_ACT smoke 0.329-first-below). Being triangulated by 2 live probes (below).
- **GROUNDING SYNTHESIS (USER-driven, durable):** grounding = 3 layers that COMPOSE = how humans do it: (1) BAKE-IN innate priors (=evolution's grounding; dual-number MM + object-permanence MM banked, both structural-support/mechanism-analog); (2) INGEST human-authored content (=culture; borrowed/parasitic grounding, the cheap bootstrap LLMs use -- YES use it, but partial/hollow + passive); (3) ACTIVE INTERVENTION (=personal experience; owned grounding by acting). LLMs 'get around' grounding by borrowing layer-2 + a dash of layer-3 (RLHF/tools) -> CONFIRMS the rule (they bypass, don't solve).
- **READER/ENCODER ARC (3 stages; wall NOT cracked, target named):** fidelity climbed 0.28->0.42->0.53 + sparsity/exact-fidelity solid, BUT multi-hop chaining (reach>=2 on real learned codes) STAYED WALLED. Stage-3 VET correction (my over-attribution, owned): the DG-expansion sign-flip is BIND-OPERATOR-AGNOSTIC + config-contingent (dense flipped too, more); block-binding's real win = sparsity+fidelity only. SHARPENED: reach ceiling = LEARNED-CODE CROSSTALK/cleanup at graph scale, NOT the bind operator. **STAGE-4 = attack cleanup/decorrelation at scale, NOT another bind swap.**

**IN FLIGHT (collect via poll_landing/off-disk FIRST -- monitor cache lags):**
- **B2_ACT active-intervention grounding FULL** (monitor bhz73w6va, remote_cpu, ETA ~40-50min from ~18:1xZ) = THE GROUNDING DECIDER. Smoke: corr 0.329 (FIRST below the 0.38 plateau, leaning-positive), grounding 0.452, perturb 3.70. Bands: HARD_PASS = corr<=0.20 AND grounding>=0.50 AND beats 0.382 AND perturb>=2 (ACTIVE INTERVENTION BREAKS THE WALL -> grounding path works, build it out); HARD_FAIL_STRUCTURAL_CEILING = corr in [0.35,0.42] (redirect: the shared MessageChannel/encoder is the wall).
- **CHANNEL-ABLATION cell** (exp_dev agent a58e83990, building/smoking agent-side, NOT on remote queue yet) = parallel probe: vary the shared MessageChannel (wider/separate/continuous/degraded-control) -> does corr MOVE? If yes -> channel IS the bottleneck; if pinned ~0.38 -> deeper (shared encoder/task). Together with B2_ACT = triangulation of the 0.38 floor.

**NEXT-SESSION FIRST ACTIONS:** (1) heartbeat; (2) poll_landing/off-disk collect B2_ACT verdict + channel-ablation smoke; (3) per B2_ACT: HARD_PASS -> active-intervention grounding WORKS, scale/build it (this is the deep-prize path); HARD_FAIL_STRUCTURAL_CEILING -> the channel-ablation result localizes the wall (channel vs shared-encoder); (4) READER Stage-4 = cleanup/decorrelation-at-scale cell (learned-code crosstalk); (5) BAKE-IN track continue (agency-detector / geometry / same-different next primitives; 2-for-2 MM so far); (6) route Stage-3 HF to the 5x cleanup-drill.

**PROCESS (USER-reinforced today):** REPORT VERDICTS, HOLD mechanism-stories AND positive sub-findings until landed-VET (4 over-reads caught+corrected today -- lean harder on VETs before ANY causal attribution) [[feedback_hold_mechanism_stories_until_VET_report_smoke_verdicts_only_2026-07-09]]; verify runner state OFF-DISK via poll_landing.py (never trust dash/status=running -- caught idle-runner=sync-lag ~4x today); NEVER-STAND keep lanes full; DOGFOOD director_kb_query on scours + rate GOOD/MEDIOCRE/BAD (live-dogfooded BAD conf 0.32/30s -- the encoder fix is what makes it good); combiner/drift-rescue arc CLOSED+PARKED (single-best-channel final, secondary to grounding). Framing corrections symmetric (VETs corrected me UP too: 5-seed not 3). USER independence anchor: a system being its own independent partner is PROVEN (brain) -- engineer differentiation, don't assume [[feedback_system_can_be_its_own_independent_partner_proven_by_brain_engineer_differentiation_USER_2026-07-09]].

**INFRA:** poll_landing.py ADOPTED+PROVEN (landing-detection fixed, SSH-noise root-caused; zero sync-lag false-negatives since adoption). Dash is RUNNER-centric -> agent-side builds/drills don't show on web dash (use inflight_monitor LOCAL EXPERIMENTS); dash-feed durable-fix needs a controlled restart = USER say-so. Testbed candidates (low-pri): (a) surface local subprocess builds on the dash [USER offered]; (b) harden poll_landing SH-4 double-prefix; (c) atom-commit-ownership convention (skunkworks lands staged-not-committed, orchestrator catches each). CRON a40824ce (:13/:43). Session cert tally off-disk: math ~19 atoms (CG+1, MM ~6, MIDDLE_BAND+2, HF ~10) + meta+1; ledger ~1531->1547.
### ========== END SESSION-CONSOLIDATED 2026-07-09 ==========

### ========== PRIOR-SESSION OVERNIGHT CONVERGENCE (2026-07-09 ~05:5xZ -- SUPERSEDED by SESSION-CONSOLIDATED above; kept as detail) ==========
**THE NIGHT CONVERGED ON ONE PRINCIPLE:** bound compounding reasoning-drift ONLY with an INFORMATIONALLY-INDEPENDENT correction channel -- earned 5 ways (Kalman innovation-orthogonality, DNA kinetic-proofreading, DAgger external-oracle, RG/info-bottleneck, pop-genetics) + cross-confirmed BOTH directions: the FAILURE (barrier #2 chain-drift = genuine STRUCTURAL mechanism-independent HARD_FAIL, 4 self-derived rescues dead at op4_V1200_d8) AND the ESCAPE (community-routed innovation MM: independent channel doesn't compound, fresh_slope 0.001 vs compound 0.098).
**TWO DECISIVE GPU FULLs IN FLIGHT (overnight_queue, monitored, ~1-2h, cron collects):**
- **KB-GROUNDED-CHECK (running, commit 4f566616b)** = the new-class mechanism: verify each hop vs the RAW ingested KB (exogenous, zero shared params w/ SR estimator; independence screen CLEAN |corr|=0.004). HARD_PASS = barrier #2 BROKEN -> XHIGH VET. HARD_FAIL = wall is DEPTH-FUNDAMENTAL (independence already confirmed) -> bounded-depth framing.
- **GENERATION DRIFT-FREE (queued, commit 3793f9d94)** = candidate cert: bounded-plan competitive-queuing readout is DEPTH-INVARIANT/drift-free BY CONSTRUCTION (the principle's limiting case -- no running estimate to compound). The 3x "INCONCLUSIVE" was a METRIC-WIRING BUG (intra_decline discriminator built but never gated on); corrected -> REPLAY flat 0.0 vs ACCUMULATE compounds 0.167->0.217, beats random-restart +1.0. On land -> HIGH VET (positive-cert, mechanism/structural scope, real-language reader-gated).
**DURABLE ON ORIGIN (b5462d526):** #2 HF, innovation MM, reframe MIDDLE_BAND atoms. **HONESTY:** 2 director over-claims (innovation "strong CG", reframe "BGE blind") were VET-CORRECTED DOWN -- the discipline working.
**REFRAME (barrier #1) = MIDDLE_BAND:** native encoder beats lexical char default but trails BGE overall; "BGE blind on divergence" was TAUTOLOGICAL (mask = bge_rank>50). Direction: STRENGTHEN native semantics (dense>graded; top-1 relational binding) + test a hybrid native+BGE ensemble.
**NEXT (cron/morning):** collect the 2 GPU verdicts -> VET; then per outcome -- KB-grounded HARD_PASS=barrier broken / HARD_FAIL=bounded-depth; generation HARD_PASS=drift-free cert; + native-semantics strengthening; dash durable fix (wall-clock poll cap); cross-modal binding (VET-first). Prove->VET->integrate; drill genuine negatives.
### ========== END OVERNIGHT CONVERGENCE ==========

### ========== MORNING 07-09 (~12:2xZ) -- SHARED LEVER = FED-KNOWLEDGE COVERAGE ==========
**TODAY'S THROUGH-LINE:** both live-front barriers point to ONE lever = the richness/coverage of the FED KNOWLEDGE (the substrate's "reality"). Reader: meaning can't bootstrap from symbols alone, needs a grounded spark. Drift: a check can't be independent unless it fails on DIFFERENT cases, which needs independent ground truth. Same root under both.
- **STACKED-CORRECTIONS (cell 8e934ca2e; FULL shipped overnight_queue GPU via orchestrator a809999e; on land -> skunkworks VET):** smoke = HONEST NEGATIVE. Both channels independent of the noisy estimator (indep_corr ~-0.004) BUT their FAILURE-MASKS CORRELATED ~0.49 (shared KB-coverage gaps) -> OR-gate dilutive, P(HARD_PASS)~0.10. Confirms the brain-drill amendment empirically: independence = FAILURE-MODE, not architecture. **PIVOT (the actionable win, 1st in 6 attempts): drift-wall fix = DATA-COVERAGE (grow KB edge density where kb_fresh_rate is high), NOT channel-count.** FULL = canonical memorialization of the scope-bound; then spin the data-coverage cell.
- **READER grounding-SNOWBALL (cell ad430466, smoking):** tests USER's own hypothesis = transitive grounding-inheritance (Gunther 2018). Stage 1 prove hollow-skeleton (relational near-ceiling vs grounded-attribute near-chance); Stage 2 attach REAL size/weight to ~100-atom seed -> measure meaning propagating to near-seed atoms with MONOTONIC distance-decay (the fingerprint separating real grounding from leakage). P~0.35, genuinely uncharted in HD encoders. On smoke clear -> ship FULL via orchestrator.
- **2 DRILLS BANKED (brain-first):** (a) reader-meaning: relational vs grounded meaning are CATEGORICALLY different, not a scale gap (congenitally-blind color study: color explains 0% of blind vs 23% of sighted similarity = "hollow but true"); words-to-words NEVER bootstraps; grounding needs a real non-symbolic seed, NOT an external LM (= laundered 2nd-hand grounding, looks grounded/isn't, no real efficacy loss from the self-contained ban). notes/research_native_encoder_relational_structure_vs_grounded_meaning_2026-07-09.md. (b) brain-self-check: brain does NOT have "only itself" -- graded PORTFOLIO of independent channels (embodied action-perception, cerebellar forward model, hippo/neocortex CLS, social); principle SURVIVES amended to failure-mode independence; failure literature (dream/confabulation/echo-chamber) confirms from the failure side. notes/research_brain_independent_channels_resolve_compounding_error_tension_2026-07-09.md.
### ========== END MORNING 07-09 ==========

### ========== AFTERNOON 07-09 (~15:00Z -- GROUNDING IS THE WALL) ==========
**LANGUAGE-ACQUISITION 5-DRILL PROGRAM -> MASTER MAP** (notes/research_MASTER_MAP_language_acquisition_biology_to_substrate_2026-07-09.md). Thesis: language = recombine general primitives + a THIN bind/unbind composition layer + prediction loop, fed in curriculum order, grounded by self-play. Bind/unbind operator QUADRUPLE-converged as the #1 missing piece. STRUCTURAL(processing-bias) vs CONTENT(fed-fact) grounding reframe. Neural inventory: HAVE predictive_coding/sequence_memory/action_selection/hippocampal_encoder/vwfa/kg_traversal; MISSING dorsal-repetition-shortcut, Broca-recursive-builder(=binding layer), cerebellar-forward-model-into-generation.
**USER FRAMING CORRECTION (locked, memory filed):** a system being its own independent partner is PROVEN (every brain); independence need NOT be external -- engineer DIFFERENTIATION (genuinely different subsystems: clock/wiring/objective + upstream pattern-separation), don't assume it from a split. [[feedback_system_can_be_its_own_independent_partner_proven...]]
**THE DAY'S CONVERGENCE: COMPOSITION SOLVED, GROUNDING IS THE WALL.** bind/unbind operator = CHAIN_GRADE (systematicity + lossless binding). But EVERY grounding failure points the SAME way -- a purely-internal/symbolic substrate can't manufacture grounding from itself: settling HF, snowball MM(shallow 1-hop), stacked-corr HF(admission-precision NOT correlation), differentiation HF(shared blind spot UPSTREAM of split), encoder HF(learned typed structure too NOISY to chain, recall 0.28 vs ~1.0 needed). -> all point to needing a MINIMAL EXOGENOUS ANCHOR = real non-symbolic data as a 'world' to predict/ground against (NOT an external model -- honors self-contained).
**DECISIVE RUN IN FLIGHT:** DG self-grounding FULL (commit 5babe50ac, poll_landing monitor bg1u55dl7, ETA 1.5-2.5h from ~15:00Z). Smoke foreshadowed MARGINAL (DG upstream pattern-separation on cross-fit: corr 0.291/ground 0.410, only +0.036 over B1). HARD_PASS=corr<=0.20 AND ground>=0.50 AND material-over-B1; HARD_FAIL(a)=redirect to exogenous-referent. **This verdict decides whether to COMMIT to the minimal-exogenous-world grounding arc.**
**RESEARCH SLATE (USER 'worth new research?' = YES, converged):** MAIN ARC=minimal-exogenous-world grounding (gated on DG FULL); LIVE=encoder-architecture (clean composable codes -- drill in flight); RUNNING=prediction-error native-learning-signal probe (af4417 -- the BRIDGE: predicting input-stream = grounding against data-as-world, may unify both threads).
**INFRA FIXED:** landing-monitor root-caused + fixed (poll_landing.py, commit 2d7652962, adopted+proven live; SSH banner/CRLF/codepage noise was blanking `type queue.json|grep`, blank misread as 'not landed'). Dash feed-fix still needs a controlled restart = USER say-so.
**PROCESS (USER-reinforced, caught 3 over-reads today):** report smoke VERDICTS, HOLD mechanism-stories/fix-directions until landed-VET [[feedback_hold_mechanism_stories_until_VET...]]; verify runner state via poll_landing/off-disk, never trust 'status=running' or flaky monitors; never-stand = keep every lane full, don't park on one verdict.
**CERT TODAY: 3 MM + 1 CG + 4 HF + 1 meta (11 atoms).** IN FLIGHT: DG-FULL(decider) | prediction-error probe af4417 | encoder-architecture drill | dual-number bake-in probe | combiner smoke a6470a5f(drift-front, minor). NEXT on DG verdict: HARD_FAIL(a)->commit exogenous-world arc (build prediction-error/data-anchor grounding); HARD_PASS->extend self-grounding + integrate.
**SYNTHESIS (USER-driven, 07-09 late) -- GROUNDING = BAKE-IN + EXOGENOUS, they COMPOSE:** (1) The ingest grounds ONLY when used as an EXOGENOUS REFERENT (predict/check-against real data), NOT as internal symbols to shuffle -- SAME data, opposite result; the grounded ingest IS our grounding channel (borrowed/parasitic like LLMs, partial/hollow, corpus thin -- open worry it's too thin -> richer/active data). (2) Innate INSTINCTS are grounded by EVOLUTION (species-level, against the real world over deep time), NOT the individual -- WE play evolution: baking priors that reflect real-world structure = a grounded SCAFFOLD, which STILL needs exogenous data to FILL referential content. (3) Humans do BOTH: evolution-scaffold + lifelong experience-fill. Our plan MIRRORS it -- BAKE-IN track (dual-number MM = first win) + EXOGENOUS track (B1+EXOG) COMPOSE/MULTIPLY (right priors -> less data needed, faster convergence, like a baby vs blank slate). LLMs 'get around' grounding by BORROWING human-grounded text + RLHF/tools/agents = CONFIRMS exogenous-required (they bypass, don't solve), not a counterexample; the brain's own wake-sleep loop also converges on shared bias -> brain grounds via the external world. Candidate research thread: does bake-in measurably REDUCE the exogenous-data burden (head-start hypothesis)?
**LATE-AFTERNOON 07-09 (~17:15Z) -- TWO BIG VERDICTS PENDING:** (1) GROUNDING DECIDER B1+EXOG FULL running (brh34ianp, ~40-55min): smoke = anchor IS causally grounded (perturb 3.78) BUT did NOT decorrelate the halves (EXOG 0.366 vs B1 0.327) -> LEANING pre-registered HARD_FAIL(a) -> ACTIVE-INTERVENTION. Deep mechanism: a SHARED prediction target makes both halves MORE alike (grounds but doesn't independize) -> points at ACTION (each agent needs its OWN exogenous stream via acting). If FULL confirms -> NEXT ARC = active-intervention grounding (this is the day's converging thesis: full grounding needs ACTION not just passive data). (2) READER-FIX Stage-3 sparse-block-binding FULL running (b9c847980, ~8-15min) = ENCODER-WALL CRACK CANDIDATE: brain binds sparse codes in LOCAL BLOCKS then completes downstream (DG->CA3), never dense-on-sparse. Smoke enabling signals CLEAN+STRONG: block bind/unbind fidelity 1.0 on sparse (vs 0.80 dense), sparsity exactly preserved, **DG-expansion FLIPPED -0.21->+0.28 net-positive**, recall 0.37->0.65; functional reach>=2 NOT readable at smoke (small-N reach-saturation artifact) -> FULL is the clean test. If HARD_PASS -> reader wall cracks (RANK-1 plain block-bind is load-bearing; RANK-2 resonator over-smooths). BAKE-IN track 2-for-2 (dual-number MM + object-permanence HARD_PASS, both structural-support/mechanism-analog). COMBINER (drift close-out) last GPU run. 4 FULLs in flight. NEXT on landings: grounding HARD_FAIL(a)->brain-ground+build active-intervention grounding; Stage-3 HARD_PASS->reader wall cracked, integrate + re-dogfood KB API; MIDDLE/FAIL->next encoder iteration. Held discipline all day: verdicts-not-stories-pre-VET (caught 3+ smoke over-reads), verify off-disk via poll_landing, never-stand.
### ========== END AFTERNOON 07-09 ==========

## STEP 0 on pickup (every session)
1. Heartbeat (every turn-end): `date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp`
2. `python tools/inflight_monitor.py` -- monitor CACHE lags ~20min; "empty queue" usually = FINISHED not stalled -> VERIFY OFF-DISK. Sub-agents + VETs are NOT dashboard-visible ("empty dashboard" != idle).
3. Agent stalls are systemic (die on API/socket blips AFTER their work lands): a 0-byte/long-silent agent output = verify off-disk what it produced (cell/notes/metrics/atom usually landed), re-dispatch FRESH only for the missing piece.
4. Bash recursive scans hang on the huge store (178k atoms / 15k notes / 6k exp dirs) -- TARGETED grep only, never a broad crawl.

## WHAT THIS PROJECT IS
hd-instrument: observable VSA/HDC GLASS-BOX substrate. USER goal (locked): a fully-functional glass-box-LLM-capable substrate, every capability inspectable/editable, brain-grounded. **Brain = north-star + existence-proof, NOT a vs-LLM scoreboard** (LLM comparison is a DIAGNOSTIC to find load-bearing weak points). **Deep prize: substrate REASONING OVER ingested knowledge -- glass-box, self-auditing, monitor-not-control.** Stages: 1 foundational (~88%, operational), 2 meta (~85%), 3 capability (~60%, current front), 4 LM-equiv (deferred; gap = generation).
**USER-LOCKED FOUNDATIONAL PRINCIPLE (2026-07-08): the substrate is SELF-CONTAINED and knows nothing but what it is fed; the encoder must CO-EVOLVE with the substrate (learn+improve as it grows) -> NO external model may be bolted into the core.** This is why the encoder must be NATIVE (see barrier #1).

## CURRENT FRONT: the 4 load-bearing barriers (the LLM-diagnostic's weak points), being closed
- **#1 READER (encoder) = THE #1 GAP. Path = NATIVE (USER-locked).** Every downstream stage inherits the reader's quality. The current operational KB reader is lexical `char_trigram_v1` (weak: dogfood MEDIOCRE/BAD -- misses recent+semantic). A far better reader exists (graded-code m=5 closes the retrieval-agreement metric `ret_agree10` 0.19->0.45 cross-seed, joint gate holds) BUT it is BGE-DISTILLED (needs an external BGE text->dense front-end) -> flipping to it violates the self-contained/co-evolving principle. So it is REGISTERED-BUT-OFF (hdlab/gsbc_graded_encoder.py) and we will NOT flip to it. THE NATIVE-PATH QUESTION: does the graded-code resolution trick close ret_agree10 on the NATIVE teacher-free encoder (CG, 06e5a493d), or is native MEANING the limiter (-> strengthen teacher-free semantics first)? THIS is the live headline test.
- **#2 CHAIN DRIFT (compounding error over long reasoning chains).** Fix built = replay-generate-then-select (generate whole candidate paths fwd+reverse, commit the one where directions AGREE = an INFORMATIONALLY-INDEPENDENT check; both prior fixes failed by re-checking a signal against itself). Smoke is N-CONFOUNDED (reverse signal is noise below N=8192) so only the FULL tests it. COIN-FLIP P~0.25-0.30; BOTH outcomes valuable (pass = long chains work; fail = drift bound doubly-confirmed even vs independent correction -> keep chains SHORT, lean on per-hop re-clean / the glass-box loop).
- **#3 CROWDED STORE = SOLVED (both axes).** Hippocampal-indexing + community-routing = wake only the relevant neighborhood. v1 = MEASURED_MECHANISM (decouples crosstalk from TOTAL store size; naive collapses 0.79->0.0 at 58k, routed stays flat 1.0; atom DECOUPLES_CROSSTALK on origin). v2 NESTED community-of-communities closes the per-community-load axis (single-tier collapses 0.99->0.02 under per-community overload, nested stays flat 1.0 both tiers) -- SMOKE HARD_PASS, FULL confirming.
- **GENERATION = the remaining genuinely-OPEN weakness (Stage-4).** Not built. Known: fails by NOISE-COMPOUNDING; fix = SELECTION not denoising (context-gate flattened it ~99.5% but only as RECENCY on a 1st-order corpus; CONTENT-gating on higher-order corpus = the hard open piece). Deep predict-residual FAILED (capped at the ~0.52 concept-recall ceiling) => generation is ENTANGLED with / BOTTLENECKED by the reader (#1) -> its BUILD is sequenced AFTER the native reader. Heavy brain-grounding drill in flight.

## WHAT IS OPERATIONAL NOW (integration phase, all on origin)
USER challenged "certs pushed but is anything INTEGRATED / are we using optimal settings?" -> staged audit (notes/audit_staged_integration_and_optimal_settings_2026-07-08.md): Stage-1 foundation REAL+green, but the gap was INTEGRATION DEBT (4741 exp cells vs ~73 hdlab modules; certs NOT auto-promoted). FIXED bottom-up -- proven mechanisms moved from experiment-cells into the operational hdlab/ library, each ADDITIVE + verification-witnessed + no-regression (verification suite 132 -> 204 tests):
- **Stage-1:** peel/SIC bundled-recovery readout (cleanup_family.BUNDLE_READOUTS); BSC first-class primitive (binding.py).
- **Stage-2:** lock-in amp (lock_in_amp.py); compose-freq routing (compose_freq_routing.py -- CG scope-CAVEAT'd: routing only wins in the high-in-degree regime, delta-rule carries it elsewhere).
- **Stage-3:** retained-trace energy-mode (context_retention.py); CLS discrete-budget consolidation (hippocampal_encoder.py); **the glass-box reasoning loop itself (glass_box_loop.py) + Merkle audit primitives (hdlab had NO hash-chain audit before) + arbitration-margin gate**; BG Go/NoGo action-selection (action_selection.py, MM tier).
- **Self-manager:** adaptive-halting dial (self_manager.py) -- LOCAL, and its independent VET is IN FLIGHT (was wired before VET; see rigor rule).

## DURABLE CERT TALLY (on origin)
- **DEEP PRIZE = CHAIN_GRADE at 80x:** glass-box self-auditing multi-hop reasoning over REAL ConceptNet knowledge, non-ceiling at 48,000 entities (base + SCALE atoms). Scope: real TOPOLOGY with random codes; certifies relational-retrieval + self-audit routing, not semantic-embedding reasoning, not open-domain.
- **CG:** encoder semantic-barrier (semantic-fidelity-bound, not capacity); decouple-store-from-retrieval redirect; peel/SIC readout transfers to real codes (argmax 0.20@J8 -> peel 0.94, the two-head "capacity wall" was a READOUT limit); CLS integrate-without-forgetting; retained-trace non-destructive energy-scaled selective-depth; teacher-free relational encoder.
- **MM:** CA3-completion (n=8 firmed); community-bounded retrieval (total-V scale-invariance); BG Go/NoGo action-selection.
- **Honest negatives (kept as scope-bounds):** peel/SIC does NOT help single-cue NN retrieval (bundle-recovery only); SimGRACE degree-agnostic dead; decouple/peel-SIC does NOT fix ret_agree10 (that gap is quantization RESOLUTION, fixed by graded codes).

## PROCESS RULES (locked -- obey)
- **PROVE -> INDEPENDENT VET -> INTEGRATE.** Never wire a mechanism into hdlab on its own FULL self-verdict + integration witness alone; it must pass the adversarial skunkworks landed-VET FIRST. (Slipped once on the halting dial; VET closing it now.) [[feedback_integrate_only_independently_vetted_mechanisms_vet_before_wiring_USER_2026-07-08]]
- **REASONING PROPORTIONAL to decision difficulty** -- fast/shallow on routine/hold/self-check/obvious turns, deep only on genuine forks/diagnoses/irreversible ops. The cron = spur to MOVEMENT-or-one-line-stop, never a deliberation loop. [[feedback_reasoning_proportional_no_overthink_idle_hold_turns_USER_2026-07-08]]
- **DOGFOOD the substrate API** (director_kb_query) during scours + rate GOOD/MEDIOCRE/BAD, do-both vs grep. [[feedback_dogfood_substrate_api_during_normal_queries_evaluate_performance_USER_2026-07-08]] (Tally this session: mostly MEDIOCRE/BAD -- char_trigram default -- a standing case for the native encoder.)
- **NO-SMOKE honesty:** off-disk tiers, skunkworks-owned; rate GOOD/MEDIOCRE/BAD, default deflated; brain=existence-proof, NEVER frame a baseline as a ceiling.
- **DISCRIMINATOR MUST FIRE at scale** (assert_discriminator_fires): the must-fail control must FAIL at smoke V else the smoke is SATURATION-VACUOUS and the FULL HARD_FAILs. Discriminators must be telemetry-SENSITIVE (perturb-moves-it), not analytically pinned.
- **REMOTE DISPATCH = ORCHESTRATOR** (exp_dev builds+smokes LOCAL, hands the queue_add; orchestrator ships+verifies). No pause flag exists; do NOT write `pause_state: ACTIVE`. queue_add.sh = POSITIONAL args; Pattern-5b now auto-SCPs shared hdlab/ modules to the runner.
- **PUSH PATH:** origin/main push goes through a FRESH orchestrator dispatch (the working authorized path); a SendMessage-resume push gets harness-gated. Push-policy for this session = resume origin syncing (USER full-auto). NEVER git add -A (explicit pathspec; substrate_index partitions are sync-managed, persistence = A5 on-disk write + push).
- **BOTTOM-UP / foundation-first integration** (brain-evolution order), not highest-value-first. BRAIN-GROUND every gap first. SCOUR/grep existing before building. Drill genuine negatives (after skunkworks confirms genuine).
- **Model+effort:** research/drills=Sonnet; exp_dev/skunkworks/orchestrator/testbed=Opus; director=Opus. Effort HIGH / XHIGH headline VETs. NO AskUserQuestion. Keep USER strategic (handle plumbing silently). Never stand -- keep lanes full; cron = move-or-one-line-stop.
- **CRON:** CronCreate job 5aab7891 (:07/:27/:47) -- SESSION-ONLY (survives compaction, NOT session-exit -> re-create if the session restarted). OS watchdog = durable backstop.

## INFRA / HAZARDS
- **SYNC-LAG (SH-9):** remote metrics lag ~20min; force-pull via tools/orchestrator/scp_recover_landing.py; verify off the authoritative runner disk (Fix#28), not queue.json status.
- **Store:** atoms.jsonl / cert_ledger.jsonl are A5-gated; substrate_index partitions gitignored (sync-managed).
- **ERROR-HARDENING live** (from _seed_checkpoint / tools/exp_guard.py): assert_discriminator_fires, timeout floor (encoder V40000 5-seed >=10800s), atomic write_metrics, tri-state scp_recover, heavy-smoke-routes-remote.

## IN FLIGHT + NEXT ACTIONS (~23:xxZ, FULL-AUTO ALL NIGHT)
IN FLIGHT: **#1 REFRAME** (exp_dev a971689b -- native encoder on the actual graph TASK, not BGE-agreement) | **#2 chain-drift replay FULL** (GPU, coin-flip; read jointly w/ innovation arm-C) | **GENERATION build** (re-dispatched exp_dev aa608fbe finishing the on-disk cell `exp_substrate_gen_lm_replay_propose_score_commit_v9_n8192_gpu.py` -- smoke was incomplete after the prior agent was killed) | **INNOVATION build** community-routed reasoning (exp_dev ac697b52).
DONE THIS STRETCH (synced to origin): **#3 CROWDED STORE fully certified MM both axes** (v1 total-V DECOUPLES_CROSSTALK + v2 nested per-community-load); halting-dial VET=MM, hdlab wiring STANDS (rigor gap closed); DASH FIXED (feed live; root cause = uncapped SSH poll wedged 7h; DURABLE FIX flagged NOT built = wall-clock poll cap in tools/dashboard/ssh_client.py run_parallel + watchdog self-heal on FEED_STALE).
**#1 REFRAME (important, live headline):** native+graded TRANSFERS (resolution solved -- graded 0.189 > coarse 0.134) but native ceiling 0.199 < 0.30 `ret_agree10`-vs-BGE bar. KEY INSIGHT: ret_agree10 = agreement-with-BGE (DISTRIBUTIONAL); native learned GRAPH-RELATIONAL structure (modularity_z 336) BY DESIGN; the deep-prize task IS a graph -> **BGE-agreement may be the WRONG yardstick.** Reframe exp measures native on the actual graph TASK. native competitive-on-task -> metric was wrong, native flip viable on task-merit; else -> strengthen teacher-free SEMANTICS first. (The graded-code m=5 BGE-distilled encoder is REGISTERED-BUT-OFF hdlab/gsbc_graded_encoder.py -- NOT flipped to, external-model, violates co-evolving principle.)
**INNOVATION (community-routed glass-box reasoning) = SMOKE HARD_PASS (strong; FULL shipping abe950bd, then VET; commit f5809f301).** ROUTED reasoning stays FLAT 1.0 across V=580->30000 where FLAT store COLLAPSES 0.45->0.0 IN-CHAIN; glass-box audit incl. Merkle-audited routing HOLDS (routing_causal_flip=1.0 -- routing is a first-class auditable step); PRED-C RESOLVED: routing-noise BOUNDED with depth when routing is an INDEPENDENT coarse channel (FRESH slope 0.0004) vs COMPOUNDS when derived from fine channel (0.0936) => extends deep prize to arbitrarily-large-KB reasoning AND independently CROSS-CONFIRMS barrier #2's principle (independent correction doesn't compound). Read arm-C jointly w/ #2 GPU. Candidate cert on VET. Note: notes/research_community_routed_glassbox_reasoning_scale_invariant_brain_first_2026-07-08.md.
**#1 REFRAME SMOKE HARD_PASS (FULL running remote_cpu, 3-seed, ship aaf3ca9d):** on the graph TASK's own ground truth (held-out link-pred), native WINS the divergence set (0.073 vs BGE 0.000 -- BGE BLIND to graph-relational targets; char 0.017) but trails OVERALL (0.155 vs 0.311) undertrained-at-smoke (~16x). => reframe DIRECTIONALLY CONFIRMED (native learned real graph structure BGE can't); FULL (800ep/12000n) decides HARD_PASS_REFRAME_CONFIRMED (native competitive-or-better overall -> ret_agree10-vs-BGE was wrong yardstick, native flip viable) vs MIDDLE_BAND (strengthen native semantics). Cell 1b5784a4e on origin.
QUEUED: RE-DISPATCH generation build (stalled); generation FULL post-smoke + content-gate implicit-relevance test (the generation crux); cross-modal binding integration (VET-first); dash durable fix; other self-manager dials; native-ceiling FULL (only if reframe says BGE-agreement is the right target); sync local commits.
**CRON a40824ce** (:13/:43, every 30min, session-only) = full-auto driver, 4 asks: (1) check experiments landed/in-flight -> route+re-dispatch stalls; (2) skunkworks-confirmed negatives/drills to perform; (3) forward-looking research / load-bearing items to explore; (4) keep THIS doc current for post-compaction continuity. Decide fast, do REAL substantive work (not minimal). Brain=north-star + cross-domain (AI/materials/biology/physics). prove->VET->integrate; drill genuine negatives. Re-create if session restarts.
USER STANDING (this stretch): full-auto all night; drill ALL negatives after skunkworks deems genuine; push toward success; brain = north star, cross-domain inspiration; keep docs current so compaction keeps momentum. KEEP prove->VET->integrate.
