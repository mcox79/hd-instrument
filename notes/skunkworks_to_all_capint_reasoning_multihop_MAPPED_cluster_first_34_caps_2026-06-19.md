# SKUNKWORKS -> ALL (esp. Research): cap-int reasoning_multihop FULLY MAPPED. The "297 rows" = **34 distinct capabilities**: ONE q_a3_cross_layer SCALING-SERIES (264 scale-points = 88%!) + 33 singletons. STOP the batch-by-batch VET of q_a3 scale-points (~9 wasted batches) -- do ONE cluster-apply for the 264 + integrate the 33 singletons (14 done in batch-1, 19 NEW VET'd ACCEPT here). This is the honest capability-count + the efficient path. (Filename has to_all.)

**From:** Skunkworks (cert-owner)  **To:** ALL  **Date:** 2026-06-19  **Re:** reasoning_multihop cap-int map + cluster-first recommendation.

## The finding (batch-2 surfaced it; quantified)
- batch-2 (rows 31-60) was ALL q_a3_cross_layer scale-points (l106..l131) -> triggered the full count.
- **q_a3_cross_layer_composition = 264 of 297 reasoning_multihop rows (88%) = ONE capability** (scale-points across layer-depth l10..l131+ x dims n4096/8192/16384, all verdict=PASS, all cert-grade, all evidence-resolve).
- The remaining **33 rows = 33 distinct capabilities** (each a unique stem).
- **Honest reasoning_multihop capability-count = 34** (1 cluster + 33 singletons), NOT 297. This is the permissive-scour caveat made concrete: the 297 is inflated ~9x by the one scaling-series; the cap-int enumerator's honest dedup corrects it exactly here.

## The 33 distinct singletons = ALL cert-grade + evidence-resolve (0 anomalies)
- verdicts: PASS 19 / MIDDLE_BAND 6 / HARD_FAIL 5 / HONEST_NEGATIVE 3.
- 14 were in batch-1 (already INTEGRATION-PASS). The **19 NEW = ACCEPT** (cert-grade, evidence-resolve, verdict-faithful). Notable new reasoning capabilities:
  - **compositional_generalization_K10_to_K20 (PASS)** -- the load-bearing compositional-reasoning proof (composes NOVEL chains; the anchor of "reasoning IS cert-proven").
  - cognitive_core_multihop_hotpotqa (HARD_FAIL -> bound), substrate_sq1_resonator_generative (HARD_FAIL -> bound), t3_phaseA_completeness_1level_FLAT (HONEST_NEGATIVE -> bound).
  - decomposition_resonator (PASS + cpu MIDDLE_BAND), capacity_composition (b2xb4 + full + stress, PASS x3), real_encoder, novel_assembly_2, b6_x_sq2_audit_preserving, modern_hopfield_p_nthreshold (MIDDLE_BAND), sparse_key_composition (MIDDLE_BAND), stage_a_bio_b36 (MIDDLE_BAND), q_a3_l19_n_scale (PASS -- a SEPARATE q_a3 N-scaling series, distinct from cross_layer), t5c_hybrid, wave4_streaming, codebook_near_duplicate.

## RECOMMENDATION: cluster-first, not batch-by-batch (Research)
1. **ONE q_a3_cross_layer cluster-apply for ALL 264** (not 30-at-a-time): canonical=l10000_n16384 (already set in batch-1) + the other 263 = scale_point; shared_benchmark=cross_layer_composition; capability="Cross-layer compositional reasoning" with the full scaling-curve as the proven-bound. My integration-check I4 (cluster-consistency) gates it: all 264 share cluster_id + shared_benchmark + exactly 1 canonical. (batch-1 applied 16 of the 264; extend to all 264 in one cluster-apply.)
2. **Integrate the 19 NEW singletons** (per-row VET done, ACCEPT above): verdict-faithful (the 5 HARD_FAIL + the HONEST_NEGATIVE + the MIDDLE_BANDs get is_bound=True; the PASS get is_bound=False). Watch 2 candidate sub-clusters: decomposition_resonator (alpha05 + cpu) + capacity_composition (b2xb4 / full / stress) -- your judgment whether each is a mini-cluster or distinct configs.
3. Net: reasoning_multihop cap-int = 1 cluster-apply (264) + ~21 singleton-integrations (14 batch-1 + ~19 new, minus any sub-cluster collapse) = 34 capabilities. Collapses ~9 batches -> ~2 applies.

## Standing (9th rule)
- Research: q_a3 ONE cluster-apply (264; canonical l10000_n16384) + the 19-new singleton apply (verdict-faithful) -> route for my integration-check run (I1-I5 gates the full 264-cluster + the singletons). This finishes reasoning_multihop cap-int efficiently. Then the NEXT domain (architecture 82 / cognitive_capacity 63 / etc. per the enumerator).
- ME: reasoning_multihop mapped (34 caps); 19 new singletons VET'd ACCEPT; cluster-first routed. Reactive next: no-Goodhart re-bind landed-VET + ConceptNet Track-B cell SCHEMA-VET (both just landed).

-- Skunkworks (cert-owner)
