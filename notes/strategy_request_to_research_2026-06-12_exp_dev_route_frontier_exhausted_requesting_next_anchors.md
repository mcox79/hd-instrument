# strategy_request_to_research: Exp-Dev route-mechanics + index-characterization frontier EXHAUSTED -- requesting next anchors/direction.

**From:** Exp-Dev -> Research  **Date:** 2026-06-12 Cycle 50. Per USER directive "ask research if you need more work". NO LLM frame.

## What Exp-Dev completed + verified this session (all committed, reports filed)
1. **qa_self_knowledge path-to-0.70 ROUTE mechanics -- EXHAUSTIVELY tested, every axis has a verdict:**
   - A (content): bge-top5 (drop keyword) -> +0.0096 macro. VALIDATED full-stack.
   - E (methodology): bge cosine-threshold ~0.70 (route_E was keyword-only) -> +0.272 E-F1 / ~+0.045 macro. VALIDATED full-stack.
   - Combined A+E: **macro 0.5204 -> 0.5711 (+0.0506)**, zero B/C/D/G regression. SHIP-candidate to Testbed.
   - B (relation): route_B v3 + 10 candidate edges -> B 0.325 -> 0.6985 (banked).
   - C (capability): bge route REFUTED (-0.219, functional gold) AND structural 1-hop propagation REFUTED (-0.323, precision
     crash) -> C is serves_capability-AUTHORING-bound, no runtime route shortcut.
   - Route-fixability taxonomy ESTABLISHED + tested: TOPICAL gold (A/E) route-fixable; FUNCTIONAL gold (C) authoring-bound.
   - Corpus-vs-route ceiling: 90pct of gold route-fixable (reachable), only 10pct (19 atoms) needs ingest; 0 unreachable edges.
2. **Two-vector production index -- characterized + validated end-to-end on REAL atoms:**
   - alpha-plateau [0.15,10] ~70x (0.5 central); scaling >=18x atom headroom; query-SNR the real bottleneck (cos~0.45).
   - Identity channel (composite_hrr) real-substrate 0.996; structural channel (algebra_hrr) dict-overlap Pearson 0.99.
3. **Coverage-gap diagnosis:** 13.9pct is two populations -- 1245 history (correctly bge-served) + 256 structured backfillable
   (144 T1-foundational concrete list); structured coverage actually 48.6pct.
4. **Substrate-classical capability properties:** low-data fit is capability-class (seq-labeling 63-87pct >> classification 54pct);
   noise-robustness is mechanism-class.

## What is now ROUTED to Research (authoring-bound, NOT Exp-Dev-buildable)
- C-axis serves_capability backfill (many C-gold NONE; Q44 spectral_observability isolated). Propagation could be a
  human-verified AUTHORING AID (suggest serves_capability from 1-hop DEPENDS_ON) but not a runtime route.
- 144 T1-foundational algebra-dict backfill (vector_space, shannon_entropy, kl_divergence, axioms, ...).
- ~10pct atom-absent ingest (Phase-6) + the 19 specific absent C/A gold atoms.

## Open leads Exp-Dev found but did not pursue (Research: prioritize or redirect?)
- **signature_hrr / complexity_hrr channels:** populated for only 54 of 242 algebra-covered atoms. Uncharacterized -- worth a
  channel-validation (like the algebra/composite validation) + a coverage/backfill assessment?
- **G-axis** (0.67, n=3): not route-diagnosed (small, already strong) -- low priority.
- A structural-propagation AUTHORING-AID tool to bulk-suggest serves_capability for human review.

## ASK
Exp-Dev's route-mechanics + index-characterization frontier is genuinely exhausted (every lever tested with a verdict). The
remaining qa path-to-0.70 gap is authoring/ingest-bound (your domain). **What should Exp-Dev build/measure next?** Options:
(a) new substrate-classical capability anchors; (b) signature/complexity channel validation; (c) a measurement to de-risk an
upcoming Research drill; (d) re-baseline the benchmark after you ship A/E route fixes + any backfill. Awaiting direction or a
strategy_request_to_exp_dev anchor.

**Compute note (per USER 2026-06-12):** laptop CPU UNPAUSED; primary focus = desktop GPU; desktop CPU coordinates with Testbed.
