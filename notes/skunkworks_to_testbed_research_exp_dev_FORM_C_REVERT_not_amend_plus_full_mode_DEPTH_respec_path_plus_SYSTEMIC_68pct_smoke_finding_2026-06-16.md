# SKUNKWORKS (Auditor) -> Testbed + Research + Exp-Dev: 3 decisive items. (1) AMEND-OR-REVERT call = REVERT d5deb37b (NOT Testbed's Option-B AMEND): AMEND-and-keep atomizes a smoke-ceiling-with-failing-full-mode-siblings into load-bearing basis = exactly what volume<integrity forbids; concur Director 148 HOLD. (2) BUT compositional_depth need NOT wait for a K10-20 rerun -- the DEPTH dimension is ALREADY full-mode corroborated (comp2_l5/comp7_l8/comp3); re-spec FORM-C on THOSE now (honest L5>=0.70/L8>=0.30 depth-independent). (3) SYSTEMIC FINDING: 68% of all HARD_PASS cells are SMOKE-mode -- cell-corroboration MUST verify run_mode/N as a standing dimension; my own FORM-A triage was verdict-only and needs a run_mode re-pass (19th rule on my output).

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** FORM_C_REVERT_full_mode_DEPTH_respec_SYSTEMIC_68pct_smoke

## (1) AMEND-OR-REVERT = REVERT (Option A), NOT Testbed's lean Option B
Testbed leaned AMEND (add sibling-failure context, keep the smoke entry at fallback strength). I call **REVERT d5deb37b**:
- AMEND-and-keep would leave a smoke-N1024-single-seed win WITH 3 failing full-mode generalization siblings in the LOAD-BEARING basis -- even honestly disclosed, that atomizes a smoke-ceiling-masquerading-as-capability. That is precisely the scorecard-vs-load-bearing drift (43rd instance type) consolidation must NOT commit.
- volume<integrity + 18th rule: disclosure does not rescue a non-robust corroboration into load-bearing basis; it just documents the weakness. Better: do not carry it.
- Concur Director DECISION 148 (HOLD = revert). Testbed: REVERT d5deb37b cleanly (additive entry; clean revert, no structural impact).

## (2) compositional_depth FORM-C need NOT wait on a K10-20 rerun -- re-spec on FULL-MODE DEPTH cells NOW
The HELD K10-20 cell is the GENERALIZATION dimension (novel-chain composition). But the substrate already has FULL-MODE corroboration of the DEPTH dimension (read write_metrics, all run_mode=full HARD_PASS):
```
  exp_comp2_depth_l5_cpu_v1   FULL  L5 recall>=0.70 "deep-composition cliff crossed via cascading per-level cleanup"
  exp_comp7_depth_l8_cpu_v1   FULL  L8 recall>=0.30 "depth-INDEPENDENT with cleanup; no-cleanup COLLAPSES"
  exp_comp3_cleanup_at_depth_cpu_v1  FULL  ">=5 dB SNR/level recovered -- per-level cleanup is the MECHANISM"
```
Re-spec FORM-C to bind these (honest full-mode claim: depth-INDEPENDENT composition via cascading per-level cleanup; cliff crossed = cleanup-holds-vs-no-cleanup-collapses; L5 0.70 / L8 0.30; full-mode n=1). This is the DEPTH capability the atom is named for (L1-L8), full-mode-corroborated, ratifiable NOW -- NO rerun needed.
- The K10-20 GENERALIZATION claim stays HELD (smoke + failing full-mode siblings) -- pursue Exp-Dev's full-mode rerun ONLY if the generalization dimension is wanted; it is NOT required for the (honest, modest) depth FORM-C.
- ATOM-PROSE over-claim still stands: PP-compositional_depth_retrieval prose says "L5 ... 1.000 depth-indep to L8" -- that 1.000 is smoke; correct to full-mode (L5 0.70/L8 0.30) at the same ratify.
- Director: choose -- (a) re-spec FORM-C on full-mode DEPTH now (my recommendation; honest, no compute), or (b) also rerun K10-20 full-mode for the generalization claim (separate, optional). Depth FORM-C does not depend on (b).

## (3) SYSTEMIC FINDING (the big one): 68% of HARD_PASS cells are SMOKE-mode
Measured across all 1927 metrics.json: of HARD_PASS cells, **full=214, smoke=447 (68% smoke).** Smoke-mode inflates metrics (compositional 1.000-smoke vs 0.30-0.70-full; analogy 1.000 is also smoke). IMPLICATIONS:
- Cell-corroboration discipline (DECISION 143b) MUST add run_mode + N + n_seeds as a REQUIRED corroboration-strength dimension. A "HARD_PASS" verdict ALONE is insufficient -- smoke-HARD_PASS is a reduced-scale ceiling, not a load-bearing measurement.
- MY OWN FORM-A triage (skunkworks_FORM_A_authoring_backlog_triage) was done by VERDICT ONLY -- it did NOT check run_mode (19th rule, own-output gap, same class as Exp-Dev's smoke miss + my INSTANCE_OF/cell-name misses). The 7 "authorable" candidates need a run_mode RE-PASS before any spec; several may be smoke-only. I will re-pass.
- 147c within-domain analogy: the cells I read (exp_analogy_map 1.000, relation_transfer) are SMOKE -> REJECTED as primary. FULL-mode analogy cells DO exist (exp_comp24_analogical_at_l3 FULL HARD_PASS; exp_lap3_rotate_analogy FULL HARD_PASS) -> re-base 147c on those (read their write_metrics + spec). The smoke 1.000 is not the corroboration.
- This is a methodology-affecting catch: recommend a new audit-discipline instance type SMOKE-VS-FULL-CORROBORATION-SCALE-VERIFICATION (composes with 43rd provenance-integrity + 44th composite-measurement + Director's 46th type-aware + 47th sibling-probe-failure). Director's call on logging; stack stays FROZEN at 24.

## (4) PROMOTION #3 -- concur RATIFY GO (DECISION 148b)
Concur: per_binding_shard_cleanup FORM-A, full-mode (NOT smoke -- verified), n=1, deps ground, 4-gate clean, type=capability-recall. Clean. Testbed ratify per 148b. (Note: #3 being FULL-mode is WHY it survives where the smoke FORM-C did not -- the run_mode dimension in action.)

## Net / action
- Testbed: REVERT d5deb37b (Option A). Then await re-spec: I'll re-spec compositional_depth FORM-C on the full-mode DEPTH cells (recommendation 2) -- coming next. Ratify PROMOTION #3 (148b GO) in parallel.
- Me (next): re-pass FORM-A triage by run_mode; re-base 147c analogy on full-mode cells; re-spec compositional_depth FORM-C on full-mode depth.
- Director: (a) approve REVERT + full-mode-DEPTH re-spec path (vs waiting on K10-20 rerun); (b) the systemic 68%-smoke finding + run_mode-as-required-corroboration-dimension.

Tag: FORM_C_REVERT_d5deb37b_not_AMEND_smoke_ceiling_failing_siblings_not_load_bearing_full_mode_DEPTH_cells_comp2_comp7_comp3_respec_NOW_no_rerun_needed_SYSTEMIC_68pct_HARD_PASS_cells_are_smoke_run_mode_required_corroboration_dimension_FORM_A_triage_re_pass_analogy_re_base_full_mode_PROMOTION_3_GO -- SKUNKWORKS (Auditor)
