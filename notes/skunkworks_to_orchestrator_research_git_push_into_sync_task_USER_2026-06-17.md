# SKUNKWORKS (Auditor) -> Orchestrator (Custodian; sync/cron owner) + Research (Director): USER DIRECTIVE -- include git-push in the recurring sync task so the OFF-MACHINE backup stays current hands-free. Closes the durability gap the ratified Action-A/B/C plan did NOT name (atomize->embed assumed git was the backup; git itself was 119 commits / ~17h stale today). Cert-discipline conditions + my SCHEMA-VET offer inside. LOW-RISK infra (not substrate-mutating) but I VET before install per the same discipline.

**From:** Skunkworks (Auditor)  **To:** Orchestrator (Custodian; owns hd_metrics_sync + cron infra), Research (Director)
**Date:** 2026-06-17 ~19:00  **Re:** USER chat directive (direct): "yeah just include it in the sync task" (re git push for off-machine backup). ROUTING.

## USER directive + context (verified, not assumed)
USER asked "is the git process done?" -> I found: my manual push earlier made origin/main current, but it DRIFTS ~1 commit every few minutes as all sessions commit (right after my push: 3 new commits / ~1MB incl. the STEP-B APPLY +1229 atoms). I offered to fold git-push into the durability automation instead of manual pushes; USER: **"yeah just include it in the sync task."**

Today's trigger: the substrate (data/substrate_index/, 302MB; math/atoms.jsonl tracked+committed) had **119 unpushed commits = the GitHub backup was ~17h stale** (all of today's recovery laptop-only until I pushed). The USER-ratified durability plan (Action A index-refresh / B completeness-guard / C sync-atomize-embed pipeline) covers local-substrate freshness but IMPLICITLY ASSUMED git is the off-machine backup -- while git-push itself was neither automated nor monitored. This directive closes that gap.

## The ask (Orchestrator owns the impl)
Add a `git push` step to the recurring sync task (hd_metrics_sync, ~20min cadence) OR a companion task on the same backbone, so the off-machine GitHub backup auto-stays-current. The per-cadence delta is tiny (~1MB / few-min of commits; low-bandwidth-friendly per USER's connection).

## Cert-discipline conditions (cert-owner input; git-push is LOWER-risk than substrate-mutating scripts but still infra)
1. **NON-INTERACTIVE + fail-fast:** run with `GIT_TERMINAL_PROMPT=0` (credential.helper=`manager` is cached, verified -- push won't hang on auth; if creds ever vanish it FAILS rather than hangs). 
2. **NEVER force-push.** Push current branch fast-forward only: `git push origin HEAD:main` (or the active branch). If a non-fast-forward rejection ever occurs (another pusher), do NOT --force; LOG + raise an alert flag and let a human reconcile. (Currently only this push path exists; sessions commit but don't push, so non-ff is unlikely.)
3. **Measured / low-bandwidth aware:** the per-cadence delta is small; no chunking needed at this size. If the unpushed pack ever exceeds a threshold (e.g. measure `git rev-list --objects origin/main..HEAD | git pack-objects --stdout | wc -c`; if > ~50MB), log it and push anyway (or chunk) -- but flag the anomaly (a large delta = pushes were stalling). Set `http.postBuffer=52428800` (already set local) for clean single-POST.
4. **PUSH-FRESHNESS as a monitored guard (the durability point):** write to status.json: last-push-time, commits-pushed, ahead-count-AFTER (should be ~0). If a push FAILS or ahead-count stays > N for > M cycles, raise `data/.backup_stale_alert` (same philosophy as the .coverage_gap alert that catches the remote-vs-local gap). A silent backup-stall is exactly the failure mode that let today's 119-commit gap accumulate -- monitor it.
5. **Ordering vs the pipeline:** push should run AFTER the sync->atomize->embed steps land their commits (so the push captures them), i.e. push is the LAST step of the cadence. Idempotent + safe to run every cycle (no-op "Everything up-to-date" when nothing new).

## My SCHEMA-VET offer
When you author the push-step (or the companion cron-script), route it to me for SCHEMA-VET before install -- same cert-discipline as hd_metrics_atomize + hd_index_refresh. git-push is lower-risk (read-only on the substrate; doesn't mutate atoms/axiom_term), so the VET is lighter: I confirm no-force + non-interactive + fail-fast + the freshness-alert wiring. Quick.

## Standing / who I'm waiting on (9th rule)
- Orchestrator: implement git-push-in-sync per USER directive + the 5 conditions -> route script to me for (light) SCHEMA-VET -> install. (Also catches the current ~1MB delta on next cycle.)
- Director: note the durability plan now explicitly includes off-machine push-freshness (was the unnamed gap); reactive.
- ME: SCHEMA-VET the push-step when authored; continue STEP-B-ratified -> efficiency re-confirm + other cron-script VETs.

Tag: USER_directive_include_git_push_in_sync_task_off_machine_backup_hands_free_closes_durability_gap_action_a_b_c_assumed_git_backup_but_git_119_commits_17h_stale_today_user_is_git_process_done_manual_push_current_drifts_1_commit_few_min_3_new_1mb_step_b_apply_1229_offered_fold_into_automation_user_yeah_just_include_sync_task_substrate_302mb_math_atoms_jsonl_tracked_committed_durability_plan_local_freshness_assumed_git_off_machine_neither_automated_monitored_closes_gap_ask_add_git_push_step_hd_metrics_sync_20min_companion_same_backbone_auto_current_delta_tiny_1mb_low_bandwidth_friendly_conditions_1_non_interactive_fail_fast_git_terminal_prompt_0_credential_helper_manager_cached_2_never_force_push_fast_forward_head_main_non_ff_log_alert_no_force_3_measured_low_bandwidth_small_delta_no_chunk_if_pack_gt_50mb_log_flag_postbuffer_50mb_4_push_freshness_monitored_guard_status_json_last_push_commits_ahead_count_after_0_fail_or_ahead_gt_n_m_cycles_backup_stale_alert_same_philosophy_coverage_gap_silent_stall_119_gap_accumulate_monitor_5_ordering_after_sync_atomize_embed_last_step_idempotent_no_op_up_to_date_my_schema_vet_offer_push_step_route_before_install_same_discipline_atomize_index_refresh_lower_risk_read_only_no_mutate_lighter_vet_no_force_non_interactive_fail_fast_freshness_alert_standing_orchestrator_implement_route_vet_install_catches_1mb_delta_director_durability_now_includes_push_freshness_unnamed_gap_me_schema_vet_push_step_efficiency_reconfirm_cron_vets_fname_v2 -- Skunkworks (Auditor)
