# Upstream Fold Queue — 2026-09-13

Owner directive (2026-09-12 23:35, going to sleep): "the last ~20 or 30 problems all focused on upstream
fixes and optimizing components based on that" — find the UPSTREAM fixes the solvers already PROVED that
strategy has NOT yet folded into the live substrate, ranked top-down by chain position.

## Method (read-only; no code touched, no experiments run)

1. Listed `notes/problems/*/SOLVED.md` by mtime, newest first; took the newest 30 (full list below).
2. Read each file's YAML frontmatter (`status` / `result` / `floor` / `controls` / `files_changed` /
   `reverify`) plus its closing `INTEGRATED_BY_STRATEGY` marker (all 30 of the newest files already carry
   one — this project closes every SOLVED with a strategy disposition, so "not yet folded" here means
   "the marker itself names something as a follow-on / latent / default-off / not-wired", not "the file is
   unprocessed").
3. Cross-checked each marker against `notes/INTEGRATION_LEDGER.md` (which duplicates the same
   LANDS / CLAIMED / INCORPORATION-MANIFEST / DO-NOT fields for most 2026-09-09..09-12 entries, with extra
   file:line detail) by grepping each slug.
4. Kept only items that are (a) about an upstream rung — tokenizer / lemma / categories(POS) / heads(parser)
   / roles / entities/coref / the reader's shared frontend — or an upstream-adjacent organ explicitly named
   in tonight's landings, (b) carry a measured number + a witness, and (c) are NOT already marked landed/
   flipped-on by the marker or ledger (a "follow-on", "latent", "default-off", "NOT wired", "measure-first",
   "do-not-enable-yet" item counts as unfolded; "LANDED", "FLIPPED ON", "default-on" does not).
5. Ranked the survivors top-down by chain position: tokens → lemma → categories → heads → roles →
   entities/coref → downstream readers/meaning.

**Tonight's landings (2026-09-12 late), named because several queue items collide with them:**
`hdlab/morphology.py` (glass-box morphy port, byte-identical, 13 read-path repoints — pri-12),
`hdlab/lexical_categories.py` (count-based generative POS tagger, forward-backward posterior, live default
— the owner-DONE pri-15/glassbox_pos_bayes prototype), `hdlab/attachment_arm.py` (heads rung, Competition-
Model cue-competition with semantic bootstrapping + self-grown plausibility store), `hdlab/semantic_hub.py`
(consolidated lexical-semantic representation organ). The reader switches `HDLAB_TAG_SOURCE` /
`HDLAB_HEADS_SOURCE` between the new and legacy organs.

Newest-30 files (mtime order; `==` marks a problem this queue draws on):
```
wire_the_mined_directed_causal_store_into_the_live_causal_reasoner_and_measure
consolidate_the_graded_lexical_semantic_stores_into_one_learned_convergence_hub_with_task_readouts  ==
grounding_coverage_quality_metric_a_correct_link_gold_not_a_grounded_count
the_lemmatizer_is_wordnet_morphy_at_inference_not_a_glass_box_morphological_decomposition  ==
type_generalized_selectional_preference_densifies_coverage_but_the_who_affected_wall_is_ambiguity  ==
pos_tagger_is_a_notbf_maxmargin_perceptron_not_a_calibrated_graded_category_posterior  ==
harm_help_valence_is_a_fitted_verb_list_not_the_substrates_force_dynamic_arithmetic
measure_end_to_end_whether_the_meaning_fusion_lifts_live_grounding_coverage  ==
wire_the_context_gated_sense_read_into_the_live_meaning_path_and_measure
consolidate_the_six_coreference_organs_into_one_cue_based_entity_resolver  ==
scale_the_reading_learned_arc_scorer_the_brain_foundational_parser_acquisition  ==
who_was_affected_needs_the_forward_salience_prior_join_undergoer_to_discourse_entity
the_common_noun_binder_is_string_identity_not_the_brains_content_addressable_typed_coref  ==
the_learned_is_a_taxonomic_identity_channel_is_the_dominant_unbuilt_meaning_lever
the_parser_is_a_frozen_supervised_hard_decode_not_the_brains_graded_probabilistic_parse  ==
grow_the_causal_mechanism_knowledge_foundation_by_mining_directed_causal_linguistic_testimony
audit_the_reward_action_selection_vigor_control_cluster_for_non_brain_faithful_stand_ins
world_knowledge_common_noun_to_name_bridge_the_81_percent_residual  ==
the_meaning_representation_is_a_point_vector_not_a_probabilistic_population_code  ==
audit_the_grounding_acquisition_subsystem_for_non_brain_faithful_stand_ins
replace_the_attractor_as_ranker_with_a_graded_population_read_in_the_grounding_loop
replace_the_entity_gate_gold_coref_inheritance_with_online_cue_based_clustering
audit_the_live_substrate_for_non_brain_faithful_stand_ins_and_name_replacements
build_the_generative_result_state_world_model
chain_multi_step_plans_and_scripts_for_the_generative_world_model
generate_dont_retrieve_causal_edges_for_unmarked_narrative_causation
build_the_obl_spatial_defer_consumer_to_cash_the_graded_parser_marginal
wire_a_defer_consumer_for_calibrated_confidence_and_realize_precision_weighting
structured_semantic_matching_for_event_goal_and_type_converse_role_filler_knowledge_not_the_hub
strengthen_the_cue_based_pronoun_coreference_resolver_the_shared_upstream_accuracy_cap
```

---

## THE QUEUE — ranked top-down by chain position

| rank | rung | problem slug | exact item to fold (files/functions) | measured gain + witness | solver says it needs | cost | risk/conflict with tonight's landings |
|---|---|---|---|---|---|---|---|
| 1 | **lemma** | `the_lemmatizer_is_wordnet_morphy_at_inference...` | Enable the **dual-route morphology EXCEED organ** (not just the byte-identical port): `experiments/exp_dualroute_morphology_exceed_v1.py` / `exp_dualroute_generalization_v1.py` / `exp_dualroute_optimize_and_upstream_trace_v1.py` — a brain-faithful words-and-rules dual route that beats `wn.morphy` on real ambiguous forms. Ledger (line 40): "DO-NOT enable dual-route yet." | lemma acc **0.9836 vs morphy 0.9603** (+0.0233, CI[0.0221,0.0246], n=195,045 tokens / 285 docs, UD-EWT+GUM, 23/24 genres CI-sep positive); twin 0.7255/0.7375 loses. Witness `verification/test_glassbox_morphology.py`. | downstream lemma-keyed stores (GEK, grounded vocab, coref `concept_lemma`) must be **rebuilt** before enabling, because they were built on morphy-identical output (ledger line 39). | M | **Direct.** `hdlab/morphology.py` landed tonight IS the byte-identical port this item builds on top of — this is its immediate, already-deferred next step, explicitly gated in the ledger's own DO-NOT. |
| 2 | **lemma→categories** | `the_lemmatizer_is_wordnet_morphy_at_inference...` | Feed POS to morphology in the live chain: the dual-route lemmatizer given gold POS beats morphy by **+4.84 pts** — bigger than the lemma fix itself. Needs an upstream POS source; ledger line 40: "DO-NOT wire the BF POS into the live chain (supervised acquisition; the chain's top is the induced-categories rung)." | gold-POS morphology accuracy 0.9389→0.9614 when fed a POS (twin-losing); lemma-alone exceed-optimum 0.9836 vs morphy 0.9603 (above). | an **unsupervised/induced** category inventory, not a supervised-labelled one (the induced-categories rung, not yet landed). | M | **Partial overlap** with tonight's `hdlab/lexical_categories.py` (count-based POS, still accrues counts from UD-EWT's *labelled* tag column — an offline FOUNDATION supply, not yet the induced inventory this item wants). Reconciling which POS source feeds morphology is unresolved. |
| 3 | **categories** | `pos_tagger_is_a_notbf_maxmargin_perceptron...` | Expose the dormant calibrated `hdlab.crf_tagger.GlassBoxCRF` marginals (`.marginals()`) as a POS source, **gated behind the parser joint-decode that consumes them** — per the ledger (line 48): "measured standalone −0.010 who-affected → lands OFF with that measured reason, ON only with the parser joint decode that consumes the marginals." | standalone CRF activation: global UPOS **0.94198→0.94402** (+0.002, no-regress) but who-was-affected undergoer recall **−0.010** (n=1500 UD-EWT) — a located negative for the standalone swap; gold category survives CRF top-2 ~0.79 but is discarded at argmax (the hard-commit signature). | a **joint POS-parse decode** that reads the CRF marginals through the parser's tree score (prototyped: −0.005 net-negative at current parser quality — the arc SCORER, not the tagger, is the bottleneck). | M | **Direct overlap** with tonight's `hdlab/lexical_categories.py` (a *different* BF POS organ — generative Bayes forward-backward, not CRF). Two unreconciled calibrated-POS approaches now exist on disk (`crf_tagger.GlassBoxCRF` vs `lexical_categories.py`); neither is wired to expose a joint marginal to `attachment_arm.py`. |
| 4 | **heads** | `scale_the_reading_learned_arc_scorer...` | Wire the **reading-learned arc scorer + BF item-based constructions**, read as a graded top-2 distribution, as a register-general alternative/fallback to the live head path. Ledger (line 78): "NO LIVE FLIPS... the land-ready pieces target `graded_parser`/`arc_parser`, NOT the live default ARC-EAGER head path." Files: `experiments/exp_readlearned_*_v1.py` (20 files; scorer, constructions, downstream A/B, BF-POS scaffold, MBR/adaptive readout). | UAS 0.4784 (full UD-EWT, no treebank); **+BF constructions 0.5142/content 0.5444**; downstream verb-argument recall **matches** supervised in-domain (+0.0073, CI incl. 0) and **BEATS it out-of-domain CI-sep** (GUM +0.0235 CI[0.0150,0.0329]) — the supervised scorer degrades OOD (0.9283→0.8967), the reading-learned one holds (0.8613→0.8394); info-free shuffled-top2 twin loses. Witness `verification/test_readlearned_arc_scorer_scale.py`. | a live consumer/board arm that exercises OOD register generality (the GUM win is the realizable case; in-domain the supervised scorer ties it, so this only pays off where the live path sees non-UD-EWT-like prose). | L | **Direct, largest reconciliation risk.** Tonight's `hdlab/attachment_arm.py` is a *second, independently built* heads-rung organ (Competition-Model cue-competition, not EM/DMV-class scorer+constructions) with its own numbers (prior-informed+constructions 0.5303; knowledge-free 0.4316; supervised 0.78, different population than this item's 0.4784/0.5444). **Neither has been measured against the other**; both claim to be brain-foundational alternatives to arc-eager, and only `attachment_arm.py`'s `HDLAB_HEADS_SOURCE` switch is named as wired. |
| 5 | **heads** | `the_parser_is_a_frozen_supervised_hard_decode...` | Route-through the **exact graded decode + marginal reliability + top-2 reliability-gated reach** on the arc-eager live path (NOT the arc-factored path it was measured on). Ledger (CONT-126, line 79): "recorded as a MEASURE-FIRST follow-on scoped to `arc_parser`'s remaining live consumers, with the top-2 reach to be re-measured on the live arceager path." Files: `experiments/exp_parser_graded_decode_regimes_v1.py`, `exp_parser_graded_downstream_whodidwhat_v1.py`. | arc-level UAS **+0.00199 CI-sep** (exact decode vs greedy hard-decode, n=24,120 tokens); downstream patient-arc F1 **+0.0036 CI-sep** (top-2 marginal reach, reliability-gated; twin loses) — but only fixes 95/5048 head errors (1.9%; ~99% of error is the SCORER, a located negative also folded). Witness `verification/test_parser_graded_route_through.py`. | the reliability AUC replacement for the fitted-logistic `parse_confidence` (AUC 0.855 vs 0.736) is explicitly land-ready and NOT yet re-measured on arceager. | S | **Direct overlap** with item 4 and with tonight's `attachment_arm.py` — a third heads-rung reliability signal (`marginal reliability`) that has not been reconciled with `attachment_arm.py`'s own "exact single-root Matrix-Tree marginal" posterior, which may already supersede it. |
| 6 | **heads→entities** | `the_common_noun_binder_is_string_identity...` | Land the prototyped **`boundary_nphead` raw-text span-head selector** as the coref mention-head selector on un-annotated text (not yet built into the live reader; listed as a follow-on "(MEDIUM)" in the SOLVED body). | recovers −0.049 of the parser head-selection wall down to −0.015 on the honest de-leaked common-noun-resolution population (inert on gold-head GUM, i.e. only matters off-gold). | a de-risking path for coref mention-heads that doesn't require a better `arc_parser` — i.e. it is a workaround for item 4/5's unresolved heads rung. | M | Depends on which heads organ (arc-eager / reading-learned scorer / `attachment_arm.py`) ends up live before building a selector on top of it. |
| 7 | **roles** | `type_generalized_selectional_preference...` | Wire the **gated consumer** of `hdlab/typed_selectional_preference.py` (module IS promoted/landed) into the who-affected meaning reranker with the swept margin gate (τ≈0.3), and add a board no-regress row. Ledger line 50: "**MEASURE:** `affected_entity` row + board no-regress" — not yet run as a board arm. | coverage on hard zero-record verb-patient pairs **40%→85%** (type generalization works); clean-signal-subset gain **+0.07 to +0.156** where discriminating; full hard-set recovery is a located negative (no τ beats syntax floor 0.4714) — so the gate must be scoped to the clean-signal subset only. | the ambiguous ~70% residual needs the generative situation model (pri-1), not more selectional knowledge — selectional preference is necessary-but-insufficient. | S | Low — module already exists (`hdlab/typed_selectional_preference.py`); this is a consumer-wire + board-measurement gap, not an architecture conflict. |
| 8 | **entities/coref** | `consolidate_the_six_coreference_organs...` | Upgrade `EntityResolver.retrieve()` (`hdlab/entity_resolver.py`) from its current byte-identical-equivalent core to a **graded Lewis-Vasishth cue-combination** (behavior-changing). Marker (line 271): "FOLLOW-ONS (open, filed): the BF PRIZE = upgrade `retrieve()` to graded L&V cue-combination (behavior-changing, separate problem)." | N/A yet — this is the NEXT step after the proven-equivalent consolidation (12/12 + 2/2 + 3/3 witnesses all byte-identical); no number exists for the graded upgrade itself. | — (filed as a separate problem, not started). | L | None direct; `entity_resolver.py` is stable (byte-identical consolidation), so this is additive. |
| 9 | **entities/coref** | `the_common_noun_binder_is_string_identity...` | Propagate the `concept_lemma` re-key from `_resolve_commonnouns` to the other organs sharing `commonnoun_binder.head_lemma` (`online_entity_cluster`, and ~5 more per ledger line 89: "`online_entity_cluster` keys on the same crude `head_lemma` (re-key optional, measured no-regress −0.0001)"). `commonnoun_binder` itself keeps its NOT_BF tag until this is done. | re-keying `online_entity_cluster` on `concept_lemma`: entity-layer CoNLL **0.6975→0.6974 (flat, −0.0001)** — measured, harmless, just not done everywhere. | "full retirement [of `head_lemma`] = the posted coref-consolidation flagship" (i.e. item 8). | S | Low-value numerically (flat) but is the literal unfinished half of a fix that otherwise landed; depends on `hdlab/morphology.py` (tonight) for `concept_lemma`'s underlying lemma call. |
| 10 | **entities/coref** | `world_knowledge_common_noun_to_name_bridge...` | Wire the **generative who-is-who online fine-type identity file** (document-local possessed-noun/role/apposition accrual) as a name-bridge coref consumer, behind the already-landed `hdlab/safe_kb_gate.py` familiarity gate. Marker: "generative WIN world-model-gated → handed to `grow_the_causal_mechanism` (pri-1)." `grow_the_causal_mechanism`'s own marker says it is still "INTEGRATING... staged next-steps" (full knowledge-DB ingest + grounded world-model not done) — so this is blocked, not landed. | **+0.0720 CI[0.0160,0.1360]** over the strongest floor (`kb_thematic` 0.416) on the document-local name-bridge subslice (n=125, GUM); beats its shuffle-the-file twin **+0.1520 CI-sep**; within-item asymmetry control +1.64 CI-sep (fine-grained, not coarse over-licensing). | the generative file needs "the grounding bridge" — a perturbable world-model — which is item-11/`build_the_generative_result_state_world_model`'s unfinished program. | L | Gated on an unfinished downstream program, not on tonight's landings directly; lowest near-term feasibility of the entities-rung items but has the cleanest number. |
| 11 | **downstream readers (meaning/lexical-semantics)** | `consolidate_the_graded_lexical_semantic_stores...` | **Repoint readers** (WSD / typing / is-a-bridging similarity backend) to `hdlab/semantic_hub.py` per-consumer, after an importer enumeration of `meaning_fusion.py` / `composed_hub_predictor.py` (candidates to retire/fold). Marker: "NO reader repointed (per consumer, measured — follow-ons...)." | hub ties/beats each of 5 graded spokes on its own best task (distributional 0.628≥0.595, grounded 0.628≥0.517, valence 0.291≥0.278, visual 0.600≥0.591, w2v 0.673≥0.406; n=2900 MEN); twin −0.0005 loses. On the live grounding-gate decision the hub ALONE underperforms the landed fusion (0.2304 vs 0.2945, CI-sep below) and as a 4th pool ties it (+0.0002, n.s.) — so this is a representation-consolidation win, not a read-time swap. | `FusedSenseRanker` (the live grounding gate) stays as-is per pre-registration; only the *other* consumers (WSD/typing/bridging) are candidates to repoint. | M | **Direct** — `hdlab/semantic_hub.py` IS tonight's landing; "readers not repointed" is its own named gap, not yet touched. |
| 12 | **downstream readers (meaning)** | `the_meaning_representation_is_a_point_vector...` | Wire the 2 still-latent/default-off pieces into a live consumer: the **valence_polarity_channel** (antonymy un-blinding, no WordNet) and **`convergent_cue_reader.intrinsic_gain_w`** (population-consensus reliability → deferral). Marker: "3 BF pieces landed default-off/latent... LIVE fusion-flip gated on pri-5." pri-5 (`measure_end_to_end...`) has since landed `FusedSenseRanker` but its own marker explicitly lists "NOT wired (per W21–W23): valence axis / confidence reweighting" — i.e. these 2 pieces are still not in the live fusion. | antonymy AUC 0.535→0.75 (valence channel, no WordNet); reliability-gated deferral **+0.049 CI-sep** (population-consensus reliability vs the fitted `convergent_cue_reader.w`). | — (both are additive, parameter-free per their own claim). | S each | Low conflict; additive channels into the already-landed `FusedSenseRanker` fusion. |
| 13 | **downstream readers (instrument, not an organ)** | `grounding_coverage_quality_metric...` | Add the proposed **board GUARD ARM** (FA-sweep invariance auto-asserted on every board run) so the count-vs-quality distinction is checked continuously, not only by the standing witness. Ledger line 32: "a FOLLOW-ON (board runtime; the witness guards it today)." | the flat-on-count / moves-on-quality 2×2 itself (not a capability number — an instrument change). | — | S | None; pure measurement infrastructure. |

---

## Already folded (landed/flipped-on; listed so nothing above double-counts them)

- `harm_help_valence_is_a_fitted_verb_list...` — **fully landed top-down**: force-dynamic arithmetic in `force_dynamics_valence.harm_help` (NOT_BF→BF_SPIRIT), upstream `nominal_head_correction` in `pos_tagger.py`, voice post-correction in `arc_labeler.label`, affect decision fed the bound predicate; board affect arm re-pointed (0.944 vs 0.778 organ-alone; live 0.972 vs 0.333 pre-fix). Commit `bbe6d9dd2`.
- `measure_end_to_end_whether_the_meaning_fusion_lifts_live_grounding_coverage` — **LANDED LIVE**: `FusedSenseRanker` in `hdlab/reading_grounding_loop.py`, grown-SEQ store, referent (DINOv2) arm, new board arm `grounding_coverage_quality`. (Its 2 unwired sub-pieces are item 12 above.)
- `wire_the_context_gated_sense_read_into_the_live_meaning_path_and_measure` — board WiC dimension re-pointed at the live wire; new `board_scws_graded_dimension`.
- `who_was_affected_needs_the_forward_salience_prior...` — `hdlab/affected_entity_resolver.py` landed, read()-time `sm.affected_entity` wire landed (the earlier "reader doesn't surface object pronouns" note was a test artifact, since corrected), board arm landed.
- `replace_the_entity_gate_gold_coref_inheritance_with_online_cue_based_clustering` — full Q111 landing done + flipped on.
- `build_the_obl_spatial_defer_consumer_to_cash_the_graded_parser_marginal` — OPT-A + OPT-C landed, no-regress verified.
- `audit_the_grounding_acquisition_subsystem...` / `replace_the_attractor_as_ranker...` / `audit_the_live_substrate...` — all 3 are MAPs/audits (no hdlab writes intended); their concrete action items (wire `grounded_similarity.grounded_reliability`, the fusion-wire into the grounding gate) were satisfied by `measure_end_to_end...`'s landing (consumes `grounded_similarity.grounded_reliability` per its own marker); the 5 disk-corrections (spaCy purge etc.) were adopted directly.
- `wire_a_defer_consumer_for_calibrated_confidence...`, `structured_semantic_matching_for_event_goal...`, `strengthen_the_cue_based_pronoun_coreference_resolver...` — pre-existing integrations from a prior session; `INTEGRATED_BY_STRATEGY` markers were backfilled 2026-09-08 for bookkeeping only, no new code.

## NOT upstream / skip (in the newest 30, excluded from the queue above)

- `wire_the_mined_directed_causal_store_into_the_live_causal_reasoner_and_measure` — causal-reasoning organ (downstream reasoner), REFUTED; nothing wired, durable negative.
- `grow_the_causal_mechanism_knowledge_foundation...` — causal knowledge-foundation (downstream reasoner), status PARTIAL/INTEGRATING with its own staged plan; not a tokenizer/lemma/POS/heads/roles/entities fix.
- `audit_the_reward_action_selection_vigor_control_cluster...` — reward/action-selection/vigor control, unrelated to the linguistic extraction chain.
- `build_the_generative_result_state_world_model` / `chain_multi_step_plans_and_scripts_for_the_generative_world_model` / `generate_dont_retrieve_causal_edges_for_unmarked_narrative_causation` — the downstream generative-world-model program itself (the "main event" after the chain, not a rung in it).
- `the_learned_is_a_taxonomic_identity_channel_is_the_dominant_unbuilt_meaning_lever` — REFUTED: the premise (taxonomic identity is the dominant unbuilt lever) was false once measured against the current strongest floor; nothing to fold, no live wire proposed.

## Full list of files read (newest 30 `SOLVED.md`, by mtime)

See the fenced block in the Method section above for the complete ordered list (`==` marks files drawn on in the table).
