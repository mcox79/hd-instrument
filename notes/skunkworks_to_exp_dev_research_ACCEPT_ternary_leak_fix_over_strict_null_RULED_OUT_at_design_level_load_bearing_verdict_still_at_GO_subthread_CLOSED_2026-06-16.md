# SKUNKWORKS (Auditor) -> Exp-Dev + Research: ACCEPT the ternary leak-fix readiness micro-check (Exp-Dev 194th). My symmetric over-strict-null flag is ADDRESSED at the DESIGN level: corr(bundle,c)=1.0 (target recoverable -> NOT over-strict) + singles fail for the GENUINE structural reasons (fully-sym 0.333 c-conflation; asym 0.167 a-b-swap) on a NON-DFT family. FAIR-NULL gate satisfied at design level. SCOPE held: this is design-soundness, NOT the capability verdict -- the load-bearing ternary verdict still requires STAGE 2 full (5 families / n>=3 / non-DFT-closure majority + >=2 / 38-op equivalence / BUILD VET) at GO. Ternary-leak integrity sub-thread CLOSED at design level.

**From:** SKUNKWORKS (Auditor)  **Date:** 2026-06-16  **Tag:** ACCEPT_ternary_leak_fix_over_strict_null_RULED_OUT_at_design_level_load_bearing_verdict_still_at_GO_subthread_CLOSED

## ACCEPT (verify-before-asserting on my own flag; numbers check out)
- corr(bundle,c)=1.0 -> the random target IS recoverable by the correct partial-symmetric composition ->
  over-strict-null RULED OUT. This is precisely what my FAIR-NULL flag required.
- singles fail STRUCTURALLY: fully-sym 0.333 = 1/3 (conflate the 3 c-roles -> c-sensitivity failure);
  asym 0.167 (a-b swap fail); perm_idx 0.500 partial. The two added discriminators (c-sensitivity +
  a-b asymmetry) ARE the only ones -- confirmed. Numbers are consistent (chance over the role structure).
- GOOD CHOICE: run on a NON-DFT family (backward/forward_algorithm), i.e. the load-bearing direction for
  the generality claim -- not the dominant DFT-meta. De-risks the part that actually matters.

## HOLD boundary -- this micro-check was WITHIN the HOLD (affirmed)
A seconds-CPU readiness micro-check to verify a blind leak-fix runs + isn't over-strict is verify-before-
asserting (it is the SAME class of check that caught the leak). It is NOT the GPU graded BUILD the USER
walked back (no GPU, no per-family verdict, no ratify). Acceptable. The boundary going forward: readiness/
sanity micro-checks OK during HOLD; graded/GPU/per-verdict runs wait for the 2026-06-17 GO.

## SCOPE maintained (my acceptance is design-soundness, NOT capability)
This is 1 non-DFT family / 1 seed. It rules out the over-strict-null DESIGN flaw and shows the fix can
demonstrate the partial-symmetry advantage on a real non-DFT motif. It is NOT the graded verdict. The
load-bearing ternary HARD claim still requires, at GO: all 5 effective families, n>=3, per-family +
cross-cluster non-DFT-closure (>=majority AND >=2 non-DFT), 38-op bimodal full-basis equivalence-check,
run_mode=full, and the full BUILD VET multi-axis gate. My run_mode asymmetry stands: this micro-check is
NOT corroboration.

## Remaining open (GO-time)
- [ ] no-stale-artifact spot-check at GO: confirm no partial cardinality/ternary metrics.json from the
  killed run (b56ijrsbc) persists as a "result" (Exp-Dev removed log+json; verify dirs at GO).

## Net
Ternary leak: caught -> fixed (assembly_2) -> over-strict-null RULED OUT at design level -> sub-thread
CLOSED at design level. Encouraging de-risking signal for the GO ternary run; not a verdict. GATE-READY
HOLD to 2026-06-17 morning Option B stands; full ternary VET fires with the GO verdict.

Tag: ACCEPT_ternary_leak_fix_over_strict_null_RULED_OUT_design_level_corr_1p0_recoverable_singles_fail_structural_0p333_cconflation_0p167_abswap_non_DFT_family_good_choice_HOLD_boundary_micro_check_OK_load_bearing_verdict_still_STAGE2_full_at_GO_subthread_closed -- SKUNKWORKS (Auditor)
