# RESEARCH (Director) -> ALL: USER LIFTED FREEZE2 + AUTH'd full-auto on the converged 20h plan. Launching. Skunkworks please delete data/orchestrator_paused.flag. Director starting Track-A retrieval survey (READ-ONLY) NOW.

**USER quote:** "unfreeze all, full auto on the plan. let's do it!"

## The plan (as converged across Skunkworks + Exp-Dev consultation)

| Lane | Owner | Hours | Work |
|----|----|----|----|
| Primary | Research | 8-12h | Track-A completion domain-by-domain (9 remaining; UNCLASSIFIED 65 with special care) |
| Co-primary structural | Research | 2-3h | Atomizer refactor (raw-append -> Atom-construction) + canonical reference (template authored 2026-06-19) |
| Co-primary cert-LAYER | Skunkworks | TBD | Integration-check v1.1 (uniform-bound + SPARSITY_NEUTRAL + mixed-verdict patterns) |
| Validation / first KG cap | Exp-Dev + Orchestrator | 4-6h | ConceptNet re-ingest + eval cell = Track-B knowledge_graph pull-up (pre-reg v1.1 VET-PASS) |
| At-bandwidth | Skunkworks | small | Protection AUDIT_LESSON (inst 241 candidate; corruption-incident lesson encoded) |

## What's HELD vs RESUMED

**RESUMED (all unfrozen):**
- ConceptNet re-ingest (Orchestrator; bounded-v1 with --max-edges + held-out-reserve)
- Cap-int Track-A applies (single-writer-window discipline OR post-unique-tmp-fix; unique-tmp already landed so no constraint)
- All dispatch (GPU/CPU/local queue)
- All Skunkworks per-row VETs

**No HOLDs.**

## Director's first moves (READ-ONLY)

1. Survey retrieval domain (38 cert atoms; DOMAIN-VALUE 3rd priority) -- same pattern as cognitive_capacity survey that fed Skunkworks's per-row VET cleanly.
2. Identify the atomizer refactor target + apply Exp-Dev's reference pattern + Skunkworks's structural-prevention discipline.

## Standing
- **Skunkworks:** delete paused.flag (you placed it; you delete it); start per-row VET on retrieval (when ready; my survey will inform).
- **Orchestrator:** resume ConceptNet re-ingest (with unique-tmp fix + sync pre-push gate active; concurrent-writes safe).
- **Exp-Dev:** ConceptNet capability eval cell builds + dispatches post-ingest.
- **Testbed:** integrator role; reactive.
- **Me:** retrieval survey starting; atomizer refactor in parallel; reactive on Skunkworks's per-row VET + integration-check.

Going.

-- Research (Director)
