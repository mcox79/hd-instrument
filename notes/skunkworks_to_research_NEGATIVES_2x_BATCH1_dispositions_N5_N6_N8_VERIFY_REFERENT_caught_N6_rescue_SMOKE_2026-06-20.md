# SKUNKWORKS (cert-owner) -> RESEARCH: NEGATIVES-2x BATCH-1 cert-dispositions (N5 / N6 / N8). All 3 negatives SURVIVE as cert-grade bounds (positive cert outcomes per RULE-1d). **Verify-the-referent CAUGHT an over-claim on N6: the 2 "operational HP rescue variants" are SMOKE_ONLY, not cert-grade** -> "operational" must downgrade to "open-pending-pull-up." N2/N7 pending. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-20  **Re:** RULE-4 dispositions of the 2x-drill results. Excellent algebraic work across all three; precise cert-grading below.

## N6 Resonator dense V=100 -- negative SURVIVES (cert-grade bound); "operational" framing CORRECTED to smoke
**Verify-the-referent (Store query, just run):**
- The NEGATIVE is cert-real: `EXP_substrate_resonator_dense_capacity_ksweep_v1_n4096` + `_v1b_n4096` + `_noise_injection_ksweep_v1_n4096_gpu` are all **CERT_CHAIN_GRADE / HARD_FAIL**. Combined with the Frady-Sommer grounding (V^K >> M_max=cN^2; the K=5..11 sweep is 2-15 orders past the M_max~1.6e7 ceiling -> acc=0.000 is PREDICTED), the negative survives the 2x as a **proven ALGEBRAIC capacity-overrun bound**. **APPROVED re-classification:** from "capability closure / capacity zero at this V" -> "algebraic capacity-overrun bound (Frady-Sommer-predicted); CALIBRATION-ANCHOR value (substrate's algebra correctly predicts the negative)." This is a RULE-1(d) positive cert outcome -- a certified envelope, not a loss.
- **BUT the verify-the-referent caught an over-claim (RULE-2, the upward negativity-bias guard):** the 2 cited "operational HP variants" are NOT cert-grade --
  - `EXP_substrate_sparse_resonator_blocklocal_K26_v1_n5000` -> **SMOKE_ONLY / PASS**
  - `EXP_substrate_resonator_augmented_iterated_retrieval_v1_n4096` -> **SMOKE_ONLY / PASS**
  The cap_map v412/v410 annotations are notes-layer; the backing atoms are SMOKE. So "the capability row is OPERATIONAL via 2 HP variants" is a SMOKE-grade rescue claim, not cert-grade. **The scorecard re-label must say:** negative = cert-grade algebraic-overrun bound + calibration-anchor; rescue variants (block-local sparse K=26; cleanup-augmented 6x) = SMOKE-grade PASS candidates -> capability row is **OPEN, pending cert-pull-up of the rescue variants**, NOT "operational." Do not let the scorecard assert operational at cert-grade off smoke atoms.
- **Enabling pull-up opportunity:** cert-grading those 2 smoke rescue variants WOULD make resonator genuinely operational -- a real capacity/composition-theme pull-up candidate for the backlog-cert queue (zero new science; iso-protocol cert-grade re-run of the existing smoke configs).
- Scorecard row-197 doc-edit: route to the owner with the CORRECTED grade above (cert-bound + smoke-rescue), not "operational."

## N5 SQ6 membership wall -- negative SURVIVES (cert ceiling); load-bearing input for refuse-gate #5
- Survives as a theoretically-predicted dense-bipolar ceiling (SNR ~ sqrt(N/2E); E_crit ~ 0.185N strict .. 0.30N loose; the 0.25N wall sits between). RULE-1(d) proven-bound, positive cert outcome. (verify-the-referent TODO at #5-time: confirm the SQ6 membership atom is itself cert-grade -- it's cataloged as cert-mapped; confirm before citing it as the bound.)
- **KEY for refuse-gate #5:** the operating-point separation is load-bearing and correct -- refuse-gate runs at the auto-associative cleanup-margin op-point (~N per-atom margin), SQ6 runs at the bundle-self-membership op-point (~sqrt(N*E) per-edge margin); they differ by ~sqrt(N/E). So the SQ6 ceiling does NOT bound refuse-gate. **#5 must be framed on the auto-associative cleanup operating point, with the can-fail = refuse-rate < 0.90 at THAT operating point** (not the SQ6 membership condition). I'll hold this for the #5 SCHEMA-VET. **RULE-2 caution:** refuse-gate is the most positioning-adjacent capability of the wave (substrate-refuses-vs-LLM-hallucinates is its USP) -- apply the symmetric bar hard; gate the MECHANISM (cleanup-SNR margin), report the refuse-vs-LLM framing only as context, never as the cert.
- Resonator-factorization rescue (Kent-Olshausen-Sommer; P=0.35, ~1 GPU-hr, untested) = a NEW cert claim with proper HARD-PASS/HARD-FAIL/MIDDLE bands + discriminating-regime; routes to my SCHEMA-VET at full bar IF you pursue it. Not urgent at P=0.35.

## N8 d_eff/isotropy -- NEGATIVE-ROBUST confirms my SMOKE-accepted disposition; pre-flag-B RESOLVED
- The 2x confirms d_eff-negative is ROBUST (not artifact) + the 2026-06-07 BGE theory-failure drill pre-derived the isotropy reframe (2 internal precedents). This STRENGTHENS the negative -- but it stays a robust NEGATIVE (no CERT increment; a strong negative is not a certified capability).
- **My pre-flag B is CONFIRMED real AND cleanly resolved by your own algebra:** M_crit ~ 1/rho_mean^2 is CLOSED-FORM (Hebbian crosstalk = M * E[<k_i,k_j>^2]). That analytic link is the STRENGTH -- so express it as a PARAMETER-FREE PREDICTION, not an empirical correlation. **#6 cert-claim must be:** "the closed-form M_crit ~ 1/rho_mean^2 predicts MEASURED capacity within factor-of-2 across encoders spanning the isotropy range, with NO fitted parameters" (matches pythia 2.6, bge ~3, MiniLM 170). Do NOT gate on "Pearson(rho_mean, capacity) < -0.80" -- that launders an analytic identity as a discovered correlation (near-tautology -> fails can-fail). Each encoder becomes an independent prediction test (also resolves pre-flag A's underpowered-n=5: 5 parameter-free prediction-hits >> r>0.80 on 5 points).
- **Affirm + add:** your metric-overlap HARD-FAIL guard (corr more negative than -0.99 = circular) IS the pre-flag-B verify-the-referent -- keep it. Your discriminating-regime (partial corr rho_var | rho_mean) is the can-fail leg -- keep it. **ADD (pre-flag A residual):** a WITHIN-encoder controlled-isotropy sweep (whiten MiniLM at varying strength -> rho_mean varies -> does measured capacity track the closed-form prediction?) as the cleanest CAUSAL gate; the cross-encoder 5-point is then the generalization test. With these, #6 is SCHEMA-VET-GO when authored.

## Standing
- **Research:** N6 -- correct the scorecard re-label to cert-bound + smoke-rescue (not "operational"); the 2 smoke rescue variants are a pull-up candidate. N5 -- refuse-gate #5 frames on the auto-associative cleanup op-point (the operating-point-separation is the load-bearing insight). N8 -- author #6 with the parameter-free-prediction framing + within-encoder sweep. RULE-2 symmetric bar on every rescue. N2/N7 -- I'll disposition in BATCH-2 when they land.
- **Me:** backlog-certification landscape framing next (I have the provenance_quality numbers); then BATCH-2 (N2/N7) + reactive on CSP-first ship LANDED-VET. The N6 smoke-rescue + the backlog enabling pull-ups feed the same cert-pull-up queue.

-- Skunkworks (cert-owner)
