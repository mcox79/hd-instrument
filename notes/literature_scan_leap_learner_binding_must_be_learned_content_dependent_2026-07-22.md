# Literature scan (Director live-web, 2026-07-22): the leap-learner must ACQUIRE binding geometry from data (content-dependent), not use fixed algebra

Closes the three WEB-FETCH REQUESTS left for the Director in `research_drill_is_learned_not_algebra_free_CG_bar_coherent_2026-07-22.md` (fork-B). Director ran these via live WebSearch/WebFetch (headless subagents cannot web-auth). SOURCE-CONFIDENCE FLAG: findings are from WebSearch result-text + WebFetch small-model summaries of the PDFs, NOT a full close read of each paper -- treat as HYPOTHESIS-strength literature signal, disk/PDF closer-read pending before any load-bearing propagation (extends the disk-verify-agent-claims discipline to web summaries).

## The convergent finding (independent literature <-> our own atoms)
Our session established, from the inside: fixed-FHRR binding is FREE-ALGEBRA-bound (atom 29437: random geometry beats real -> the fixed operator ignores content structure); linear learners over that fixed algebra learn recency/similarity heuristics, not structure (29440 synthetic, 29443 real-text agreement). Ceiling = the LEARNER, not the representation (29443 Stage-1 representability=1.0).

The literature says the SAME thing from the outside:
- **"Compositional Generalization Requires Linear, Orthogonal Representations in Vision Embedding Models" (arXiv:2602.24264, 2026):** (a) structured embeddings SIGNIFICANTLY OUTPERFORM scrambled variants on OOD compositional generalization -> generalization is CONTENT-DEPENDENT; (b) CRITICAL -- the orthogonal structure must be **LEARNED from data, NOT imposed by architecture**: random orthogonal matrices without training FAIL CG, and "fixed binding algebras without semantic grounding fail to generalize compositionally"; (c) "neither fixed orthogonal systems nor random structures suffice -- the orthogonal structure must be acquired through training on appropriately structured data." This is the fork-B discriminator, answered in the vision domain, firing the LEARNED way: structured-beats-scrambled (OPPOSITE of our fixed-algebra 29437 signature where random-beats-real).
- **"A Theoretical Analysis of Compositional Generalization in Neural Networks: A Necessary and Sufficient Condition" (arXiv:2505.02627):** the N&S condition = (1) STRUCTURAL ALIGNMENT (the computational graph matches the true compositional structure of the task) AND (2) INFORMATION ENCODING (components encode "just enough" -- not over/under). A pre-training diagnostic over architecture + regularization + data. (Does not itself address fixed-vs-learned binding -- abstract-level only.)
- Broader search corroboration: "after models generalize, embeddings display structured low-dimensional geometry"; "structured embeddings substantially outperform random ones for systematic generalization"; iterated-learning + simplicial-embedding methods (openreview irRHgjePdR) as mechanisms for structure to EMERGE.

## Fork-B web-fetch requests -- status
1. Does an MLC content-structure ablation already exist? NOT found as an exact MLC scrambled-vs-structured meaning-space ablation. So the exact MLC test remains un-run in the lit reviewed.
2. Named test distinguishing "isomorphic-to-fixed-algebra" from "content-dependent generalization"? Not a single named test, BUT 2602.24264's structured-vs-scrambled OOD protocol IS that test in operational form.
3. Closest existing empirical result to the cheap decisive test? **YES -- 2602.24264** essentially ran structured-vs-scrambled on a LEARNED (contrastive) operator and it fires the learned/content-dependent way. So our in-substrate geometry-ablation on a learned operator would be a CONFIRMATION, not a from-scratch discovery -- de-risks the leap-test's expected direction.

## Actionable for THE LEAP-TEST (nonlinear structure-induction learner on the validated 29443 agreement testbed)
- The leap-learner must **ACQUIRE the binding/representation geometry from the agreement data** (content-dependent, learned), replacing/augmenting fixed-FHRR -- this is the fork-B "meta-learned binding operator," now corroborated by an independent 2026 result.
- SUCCESS SIGNATURE = the **content-dependence FLIP**: scrambling meaning-structure should HURT the learned operator (it has nothing else to exploit), OPPOSITE of 29437 where scrambling HELPED the fixed algebra. This is the same geometry-ablation, pointed at the learned learner.
- Design guardrail from 2505.02627: the learner's computational graph must ALIGN with the task's compositional structure (subject-head dependency) and encode "just enough" (the held-out-lexeme split already blocks lexical memorization).

## THE GLASS-BOX CRUX (the real decision for USER)
The literature's instances of "learned, content-dependent binding geometry" are GRADIENT-TRAINED neural nets (MLC seq2seq transformer; contrastive vision encoder). That is closer to the neural black-box the USER previously distinguished FROM the glass-box atomize+sleep loop. THE open question: can a GLASS-BOX consolidation mechanism (atomize specific cases -> SLEEP reshapes the representation geometry INSPECTABLY) achieve the same "acquired content-dependent geometry" WITHOUT an opaque gradient-trained operator? 
- If YES: the leap is glass-box-feasible, no invariant tradeoff -- build the sleep-reshapes-geometry consolidation learner and run the content-dependence flip on 29443.
- If it REQUIRES gradient-trained opacity: the leap trades the glass-box invariant -> a program-level redirect decision that is the USER's, not self-authorizable.
This is the fork to bring to USER alongside the sharding-store-architecture compile (they connect: the acquired geometry needs a store to consolidate into; sparse-code / SR-routing findings from the sharding compile may shape the consolidation mechanism).

Links: [[feedback_chase_the_leap_learned_glue_is_glassbox_atomize_sleep_puns_testbed_USER_2026-07-22]], [[feedback_glassbox_is_not_handrules_substrate_can_learn_llm_language_efficiently_brain_proof_USER_2026-07-18]]. Governing negatives: atoms 29437 (fixed-algebra free), 29440 (linear->similarity), 29443 (real-text agreement, ceiling=learner).
