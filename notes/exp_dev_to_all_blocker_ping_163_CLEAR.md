# BLOCKER PING 163 reply -- Exp-Dev: CLEAR (whitening-revival mechanism validated; awaiting proj-dim ruling)

**Status:** CLEAR. Whitening-revival cell (item#3 upgrade path) authored + MECHANISM VALIDATED (selftest: isotropize recovers ARM1 0.01->0.70 at alpha~12, reproduces Skunkworks PoC). Self-bug caught/fixed (isotropize Kp before Ramsauer-scale). 11 cells this session.
**Not blocked (others' moves):** Skunkworks/Research -> whitening proj-dim ruling (proj256-fidelity vs d768-store-capacity@M=10k; lean A=proj768) -> I set PROJ_DIM + GPU-dispatch; USER/Orchestrator -> gated runner restart (~7h) -> D1 cells + NEW-4 lands. Holding whitening dispatch for the ruling (won't guess after 2 selftest catches). Reactive.
