# SKUNKWORKS (cert-owner) -> RESEARCH (+ Exp-Dev): q_b1 pre-reg v2 re-VET = PASS (all 3 refinements applied correctly). + I now RECOMMEND ADDING candidate-2 (cleanup-between-hops) NOW, not just reserving it: the substrate-scour found SMOKE-grade evidence it works (resonator_augmented_iterated_retrieval: plain_depth 4 -> cleanup_depth 24 = 6x, HARD_PASS smoke). So it's the EVIDENCED candidate (vs candidate-C tropical-algebra = literature-untested). Make it N=2 Bonferroni (alpha=0.025), seed candidate-2 from the resonator config. Pre-reg is commit-ready. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (+ Exp-Dev)  **Date:** 2026-06-19  **Re:** q_b1 pre-reg v2 re-VET + candidate-2 SCHEMA-VET.

## Re-VET v2 = PASS
All 3 of my SCHEMA-VET refinements applied correctly:
1. NO-REGRESSION gate (d276 + d100 in the test set; bands require no-regression for HARD_PASS/MIDDLE_BAND; HARD_FAIL on regression). Correct.
2. ISO-PROTOCOL control (standard HDC composition RE-RUN at identical depths/n=5/harness; cluster baseline informational-only). Correct.
3. BONFERRONI-ready (per-candidate bands; alpha/N). Correct.
- Bonus (prune d300/d400 to focus 5-seed budget on the cliff region d100/d276/d280/d287/d293): ACCEPT for the pilot. ONE add-back: if a candidate HARD_PASSes at d293, a FOLLOW-UP depth-extent run (d300-d400+) characterizes the NEW cliff (don't leave the new ceiling unmeasured). Pilot stays 5-depth; follow-up only if a candidate wins.
- I9 discipline (commit pre-reg to git origin/main BEFORE dispatch; honest-scope; cert-VET against THESE bands) -- correct, matches v1.2 (LIVE 9ee18e06).

## Candidate-2 SCHEMA-VET = APPROVE + ADD NOW (it's evidence-backed)
The substrate-scour (USER-directed) found the cleanup-between-hops mechanism ALREADY DEMONSTRATED at smoke-grade:
- **`EXP_substrate_resonator_augmented_iterated_retrieval`** (HARD_PASS, SMOKE_ONLY): "cleanup/resonator augmentation extends reasoning depth >=1.5x. plain_depth=4.0 -> cleanup_depth=24.0 ratio=6.0x (lower bound)." 
- + `EXP_lap2_5_khop_depth10` (full): deterministic traversal EXACT to depth-10 "no empirical depth ceiling" via per-binding sharding (cleanup exact each hop).
- Mechanism primitive exists: `resonator_network_decoder` (iterative multi-factor cleanup).
=> candidate-2 (cleanup-between-hops: re-resonate each intermediate onto a clean stored atom between hops, reset the noise floor) is the HOME-GROWN, EVIDENCED candidate. Candidate-C (tropical-algebra) is literature-only-untested. So the A/B becomes a MECHANISM-COMPARISON, and candidate-2 is the favorite.
- **SCHEMA-VET candidate-2:** clean A/B addition (op-substitution: cleanup-between-hops vs standard composition, everything else equal). Honest-scope = "resonator/cleanup-between-hops extension of HDC composition" specifically. Seed the candidate-2 cell from the `resonator_augmented_iterated_retrieval` config (the demonstrated mechanism) -> the cert-grade A/B PROMOTES the smoke-evidence (Track-B pull-up + IMPROVE-track win in one).
- **N=2 -> Bonferroni alpha=0.025** (the pre-reg already specifies this). Both candidates tested against the same iso-protocol control.

## Why this matters (the value-mining connection)
The resonator finding was SMOKE_ONLY + un-surfaced until the USER pointed me at it -- the exact blind spot from the value-mining concern. Cert-grading it via this A/B both solves the q_b1 cliff AND demonstrates the rectification value (a high-value smoke finding pulled to cert).

## Routing
- Research: ADD candidate-2 (cleanup-between-hops, seeded from the resonator config) -> N=2 Bonferroni -> commit the pre-reg (v3) to git origin/main -> Exp-Dev builds the A/B cell (control + candidate-C + candidate-2). I'll quick-confirm the v3 (candidate-2 added) before dispatch.
- Me: cert-VET the A/B verdict when it runs (the swap, if any, gated by v1.2 I7/I8/I9). Expect candidate-2 to be the favorite.

-- Skunkworks (cert-owner)
