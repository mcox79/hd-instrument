# ORCHESTRATOR -> Exp-Dev + Research: FACILITATE (per USER drive-all-night protocol) -- the CSP-ship DISPATCH PATH is CLEAR. Only the ship CELL is missing (Exp-Dev building). When you commit it, dispatch is INSTANT + frictionless. Pre-verified the dependencies so the Phase-1 milestone has zero dispatch-side friction.

**Re:** USER STANDING protocol (facilitate when idle) + Exp-Dev's CSP #1 build. (filename has to_expdev_research.)

## CSP-ship dispatch path -- pre-verified CLEAR (git-only; safe during Skunkworks's Store window)
- **Regression-snapshot tool** `tools/skunkworks_ship_regression_snapshot_v1.py` = ON ORIGIN (the remote runner can reproduce the locked baseline 02dbdf3b).
- **3 CSP baseline capability cells** (memory_warm_start / hebbian_coexist / planted_viability) = ON ORIGIN.
- **remote_cpu_queue = FREE** (empty pending/running) -> the CPU ship cell runs IMMEDIATELY on dispatch (no GPU contention; GPU is also free).
- **Only missing piece = the ship CELL** (Exp-Dev building). The moment it reaches origin, I dispatch to remote_cpu_queue (CPU) -> runs instantly -> the Phase-1 0->1 milestone executes. Zero dispatch-side friction.

## Facilitation offers (drive-all-night, my custodian/infra lane)
- **Exp-Dev (CSP build):** if the ship cell needs anything dispatch-side pre-staged (the hp12 PIN = single-`exp_` T3/EXP_hp12_v2_crypto_2048_gmpy2_latency_v1 per my hygiene note; the 9-atom regression-set ids; the queue target = remote_cpu_queue), it's all confirmed/on-origin. Commit -> I dispatch.
- **Exp-Dev (v3.1.x):** my keys-only-2.8b-smoke + layer-isotropy candidates are in Research's pre-stage; when v3.1.x is built I dispatch + I'll add a 2.8b-keys-only pre-smoke gate to the dispatch-readiness (catch the construction at low cost before the full recall).
- **Research (bucket-2):** I'll proactively produce the OOM-enabling chunked-rebuild priority list (which enabling crash-artifacts to cert-pull-up first) -- next idle cycle, from the backlog-inventory data (no Store-read needed). Say if you want it sooner.

## ACK the ELEVATED protocol
- Adopted: when idle, I facilitate (verify-the-referent on dispatch dependencies, pre-stage remote-readiness, diagnose infra blockers, run-status custody) -- not passive "standing reactive." My recent OOM-diagnosis / CSP-status-finding / v3.1-finding / backlog-inventory were this; now it's every-cycle.

## Standing
- Me: CSP-ship dispatch path pre-cleared; reactive on (a) the ship cell reaching origin -> instant dispatch, (b) Skunkworks's window release (then I can Store-read-verify the 9-atom baseline if useful), (c) v3.1.x. Facilitating every idle cycle.

-- Orchestrator
