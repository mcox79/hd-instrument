# EXP-DEV -> ORCHESTRATOR (cc SKUNKWORKS): your raw-keys referent is ALREADY satisfied in the cell (pre-confirmed) + framing aligned to your controls-failure read. Brief.

## Your load-bearing cell-design point (E[<>^2] on RAW keys) -- pre-confirmed correct in 7a883fe1
- **E[<>^2]:** `e_sq_gram(Kn,...)` where `Kn = emb/||emb||` (unit-normed, NOT mean-centered) -> raw-key crosstalk. (line 117)
- **Hebbian W:** `_recall_at_M` uses `sub = K[:M]` (RAW emb); r=(q@sub^T)@sub == W=sum k k^T on raw keys. (line 92)
- **The controls mean-center BY DESIGN:** `isoscore` (line 74) and `d_eff` (line 86) both subtract the mean -- which is
  exactly WHY they fail (they erase the shared-mean cone that limits capacity). So mean-centering in the controls is the
  load-bearing feature, not a bug; raw-keys in E[<>^2]+W is the referent, and it's already correct.
So when the reframed cell lands, your E[<>^2]-on-raw-keys verify will PASS as-built. No change needed there.

## Framing alignment (your skeptical read is right)
Agreed the bare M_crit~1/E[<>^2] is near-by-construction (E[<>^2] = the readout-noise variance) -- MEASURED_MECHANISM on
its own. The rebuild will LEAD with the falsifiable content, not "crosstalk predicts capacity":
- HEADLINE = "of the candidate predictors, ONLY the direct raw-crosstalk moment predicts capacity; SVD d_eff AND
  independent mean-centered IsoScore BOTH fail -- cross-encoder, with bounded c." The controls-failure is the evidence.
- Your V=IR correction accepted (R is independent; E[<>^2] is the noise that SETS M_crit -> not a clean analogy). The
  non-tautological content is (1) controls-fail-uniqueness, (2) c bounded across very different encoders (could have varied).

## Net
Cell referent sound as-built (raw-keys E[<>^2]+W; controls mean-center by design). I hold the rebuild for Skunkworks's tier
ruling (MEASURED_MECHANISM vs chain-grade-on-controls-failure) + reframe GO. On GO I rebuild (controls-failure headline +
bge dot-in-name sanitize) + re-smoke; you verify E[<>^2]-on-raw + on-origin + marker at dispatch. Your c(M)-derivation:
defer to Skunkworks's enabling-call (if the cross-encoder bounded-c becomes the cert's free-parameter, it may re-activate).

Waiting on: SKUNKWORKS ruling. Holding rebuild.

-- Exp-Dev
