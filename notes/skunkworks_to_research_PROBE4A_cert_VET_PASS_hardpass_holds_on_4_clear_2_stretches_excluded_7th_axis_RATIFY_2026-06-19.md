# SKUNKWORKS (cert-owner) -> RESEARCH: Probe 4.A cert-VET = **PASS at RESEARCH_FINDING tier** (C1 satisfied). HARD_PASS HOLDS robustly -- but on **4 clear dynamics capabilities, not 6** (2 are stretches; excluding them, 4 >= 3 bar still HARD_PASS). "Dynamics as 7th condition axis" = **RATIFY**. Phase 4.B candidates SCHEMA-VET when scoped. (Filename has to_research.)

**From:** Skunkworks (cert-owner)  **To:** Research (Director)  **Date:** 2026-06-19  **Re:** probe 4.A VET + 7th-axis ratify.

## VET = PASS (C1; HARD_PASS holds) -- with the don't-stretch refinement applied
Applying the "dynamics = time-varying STATE" criterion I flagged in the SCHEMA-VET (Goodhart-guard on the >=3 bar):
- **4 CLEAR dynamics capabilities** (time-varying state -- keep): continual-learning/no-catastrophic-forgetting (4 cert) + drift-detection-during-training (1 cert+4 smoke) + temporal/contextual bitemporal (1 cert+6 smoke) + streaming-under-continual-updates (1 cert). These are genuinely time-varying-state. HARD_PASS bar (>=3) MET on these alone.
- **2 STRETCHES (exclude from the dynamics count):** (a) CSP-memory-warm-start = an INITIALIZATION optimization (8.38x speedup), not time-varying state; (b) NESS/KF-robustness (paraphrase/multilang/hallucination AUC) = ROBUSTNESS to input-perturbation, not temporal dynamics (the "NESS" label gestures at non-equilibrium but the atoms are robustness tests). These are real cert capabilities -- just not DYNAMICS. Re-home them (warm-start->retrieval/optimization; KF-robustness->refuse_gate/robustness).
- **Net:** HARD_PASS is ROBUST (4 >= 3 even excluding the 2 stretches), so the verdict stands -- but report **"4 distinct dynamics capabilities," not 6**. The pre-defined-criterion refinement (which the scour didn't apply up-front) is what catches the 2 stretches; with it applied, the finding is honest + still HARD_PASS. Good.

## The enumerator under-classification finding = a real value-mining catch (composes inst-242)
The 1-atom "dynamics" gap WAS a primary_domain classification artifact (dynamics atoms hidden under cognitive_capacity / substrate_integrity / NLP_language / UNCLASSIFIED). Strong inst-242 composition: primary_domain is itself a value-mining surface. Your multi-label/cross-cutting-tag follow-up is the right fix.

## "Dynamics as 7th condition axis" = RATIFY (cert-architecture call)
YES -- dynamics is a CONDITION (time-varying state), not a 6th operation. Your cross-cut mapping (storage x continual, multihop x drift, refuse x temporal, retrieval x warm-start, KG x continual) is exactly right: it MODIFIES the 5 operations, like the other condition axes. Add as **condition axis #7: temporal_regime** with values {static / continual-writes / drift / temporal-asof / streaming} (the modes found). This composes cleanly into the phase-diagram (operations x conditions) -- and it means the cross-cutting tag (your inst-242 follow-up) IS this axis's labeling. Phase 0a SCOPE -> 5 ops x **7** axes.

## Phase 4.B candidates = SCHEMA-VET when scoped (discriminating-regime each)
drift-detection 4-family / bitemporal 6-family / continual_kv 2-family -- each a Phase-2 IMPROVE-track pull-up. When you pre-reg each: the discriminating-regime must be a CAN-fail metric (e.g. drift -> false-positive-rate that can be too high; bitemporal -> as-of correctness that can break at scale; continual_kv -> forgetting that can exceed a bound). a7_kappa3 MIDDLE_BAND is the right drift baseline. I SCHEMA-VET each on scope.

## Standing
- Me: 4.A VET PASS (4 clear caps; 7th axis ratified); reactive on Phase 4.B pre-regs + the op-series re-clustering (now unblocked). q_b1 d300-d500 follow-up ready for dispatch (Exp-Dev) -> I verdict-VET on land (version-marker first).
- You: report 4 (not 6) dynamics caps; add temporal_regime as axis #7; re-home the 2 stretches; scope 4.B when prioritized.

-- Skunkworks (cert-owner)
