# DIRECTOR POST-COMPACTION BACKUP — 2026-08-04 (self-contained; READ FIRST)

## 🚀🚀 POST-COMPACTION LAUNCH POINT (2026-08-07 PM — READ THIS FIRST, then the CURRENT STATE block below; full B detail = notes/PLAN_B_grounding_word_context_affect_superposition_map_2026-08-07.md)
**WHERE WE ARE:** Direction = **B (GROUNDING), USER-confirmed + full-auto**. A (competency library) should come FREE via exposure+learning, NOT hand-built [[feedback_B_grounding_is_right_A_competencies_should_be_learned_free_via_exposure_USER_2026-08-07]]. Plan of record (USER co-designed) = the **WORD->CONTEXT->AFFECT SUPERPOSITION MAP**: word = superposition of (context (x) sense+affect) collapsing via a context KEY; social valence = MENTAL HARM (reuses owned physical-harm valuation). 3 layers: grounded affect primitives / learned word->context->affect map (dictionary MENU supplied, selection+affect EARNED) / context-collapse read. Learning signal = stories-with-consequences (self-improving reader) = unifies B + A-steer.
**✅ PROVEN (all mechanism-level, CLEAN signal, Director-VET'd isolated experiments):** S1 social valence grounds like physical harm (ca1d70d1a, 0.833 held-out); S2 superposition map + taught collapse (04af969c4); S4 map LEARNED FROM EXPOSURE (d423cce63, consequence-as-teacher + anti-drift, scramble collapses); RICHER selectional context KEY = the linchpin (0527afeab, finer-WordNet-class bundle > animacy 1.0 vs 0.5). COVERAGE lever = Levin last-resort tier WIRED to production (50c0e5ab4, prod polarity 17->18, cert 220/3).
**🧱 THE WALL (measured, honest):** applying the earn-from-exposure loop to REAL PROSE FAILS — the teaching signal doesn't carry (Snorkel fc21752f3 + sharpened-credit-assignment b5fdd956c both HARD_FAIL: scramble does NOT collapse, primary < majority floor). NOT coverage (self-resolved to 439/1655 via this session's goal_typing hardening), NOT the combination method (Dawid-Skene EM validated), NOT credit-assignment sharpening alone. **THE BINDING WALL = CONTEXT EXTRACTION, and its CONCRETE first culprit (Director read-the-code of the actually-credited 'verbs') = VERB IDENTIFICATION / LEMMATIZATION IS BROKEN on real prose — the pipeline credits NOUNS (boy/sister/cheek/egg/gown), NAMES (phillip/walter), and STEMMER GARBAGE (com/fr/kne/capaciou/rebelliou) as 'outcome verbs' -> the loop is fed NOISE.** B architecture is SOUND on clean signal + WAITING; real-prose application is context-extraction-limited (B + context-extraction = ONE frontier, now MEASURED).
**✅ VERB-ID FIX TESTED = HARD_FAIL, VET'd on disk (12aa5eab3, exp_wordnet_verbid_earn_from_exposure_v1):** the WordNet is_verb+morphy swap WORKS at its job -- verb cleanliness 0.758->1.000 (734/734 genuine verbs, all 11/11 garbage canaries boy/sister/phillip/com/fr/kne cleared) -- BUT scramble-lift only 0.0167->0.0388 (still <=0.05 NO_SIGNAL, far below 0.10), primary 0.4444 < floor 0.6389, learnable_subset collapsed to n=1. **VERB-ID = NECESSARY, NOT SUFFICIENT.** Residual culprits (read-the-code of the still-"clean" credited set): (1) WordNet is_verb is DICTIONARY-MEMBERSHIP, not CONTEXTUAL POS -- still admits denominal noun-uses (paper/table/head/father/house/witch pass as 'verbs'); (2) predicate-selection (which of 2 co-clause verbs is the loaded outcome verb); (3) coverage/overlap (cleaned OOV barely intersects the 36-item gold eval).
**🎯🎯 CONVERGENCE FINDING (2026-08-08, Director-VET'd, load-bearing -- REFRAMES THE PROGRAM):** the deep grounding is ALREADY BUILT + PROVEN. `exp_grounded_appraisal_sim_earned_v1` = **MECHANISM_EARNS** (heldout 1.000; floors beaten RANDOM 0.168/MEMORIZED 0.206/NO_APPRAISAL 0.239; construction-determined guard R>=0.25|NA>=0.90 did NOT fire; revenge 1.000 bystander 0.000 Hamlin-clean; earned_restore 1.000 vs recency 0.303; 5 seeds). `exp_grounded_appraisal_transfer_to_text_v1` = **EXTRACTION_BOTTLENECK** (theta REUSED no-retrain, theta_reuse_behavioral_match=True; CAUSAL n=7: oracle-extract arm_a **1.000** -> real-extract arm_b 0.629 -> learned-valence arm_c 0.514, recency floor 0.143; IRONY n=10: oracle 0.600/arm_c 0.700; BENEFICIARY: capability GAP no-bridge). **=> GROUNDING IS EARNED IN SIM (that cell is rigorous). The SOLE remaining wall for the WHOLE B program = CONTEXT EXTRACTION FROM REAL PROSE.**
**⚠️ DIAGNOSTIC CORRECTION (2026-08-08, Director read-the-code-VET'd, agent a68ad03b -- corrects my OWN over-read above):** the transfer-to-text "arm_a=1.0" is NEAR-TAUTOLOGICAL, NOT proof that grounding transfers to real prose. score_causal_item (transfer cell :395-449) HARDCODES coh_oracle=[1,0] (hands the theta the answer slot), feeds SYNTHETIC fixed-position events (100/200), patients hardcoded "VICTIM" (real coreference NEVER exercised), and the only real "extraction" (arm_b) is resolve_valence_blind = a 23-word UNSTEMMED HARM/HELP bag-of-words. arm_b=0.629 is largely SEED-ARBITRARY OOD extrapolation (coh=[0,0] never occurs in the theta's training -> arbitrary lean; 4/5 land correct only because their true blocker is narratively more recent). Dominant failure = MISSING-ANTECEDENT 5/6 divergent (lexicon doesn't fire HARM on "drove the knife"/"punished"/negligence); +1 role-confusion (item 005: agent-of-harm vs patient-of-harm, no role-awareness); RECENCY-CREEP structurally cannot fire (never 2 HARM candidates). **HONEST STATE: grounding earned IN SIM = solid; sim->REAL-PROSE transfer = UNPROVEN (harness is semi-synthetic). Context-extraction wall CONFIRMED + at a BASIC level (23-word lexicon, no stemming, no role-awareness, coref unexercised).** The diagnostic's #1 rec (expand+lemmatize the hand-lexicon to move THIS number) = REJECTED (eval-gaming a construct-invalid instrument + the supply-a-bigger-hand-list anti-pattern). RIGHT move = build a FAITHFUL transfer instrument (real coref, real event positions, ROLE-AWARE valence extraction, fix ill-posed item 007) so it can actually answer "does sim grounding transfer to real prose"; only then is "close arm_b->arm_a" meaningful. This is the field's 45-yr wall; brain = existence-proof it's solvable.
**🎯🎯 CONSOLIDATED FINDING (2026-08-08, TWO HARD_FAILs + 1 diagnostic, all Director-VET'd on disk -- TRIANGULATED, sharpens the wall):** BOTH candidate-DETECTION levers HARD_FAIL, ruling detection OUT as the binding wall:
  - **CONTEXTUAL-POS lever (545c34cf8, exp_contextual_pos_verbid_earn_from_exposure_v1) = HARD_FAIL.** Owned trained pos_tagger (StructuredPerceptron, load-validated 22/22: the/his father->NOUN, they paper->VERB) REJECTED 674/689 (97.82%) denominal noun-uses WordNet admitted (registered denominal nouns ['paper']->[] zero). It cleanly SOLVED candidate-noise -- yet scramble-lift 0.0388->**-0.0278** (WORSE, still NO_SIGNAL), primary 0.4444->0.3611, learnable_subset n 1->**0**. Gate-D positive control reproduces the WordNet arm bit-identically (0.4444/0.0388) = apples-to-apples. => cleaner verb-ID does NOT carry the signal; **candidate-gate cleanliness (verb-ID/POS) is NOT the wall.**
  - **CAUSAL-ATTRIBUTION diagnostic (a68ad03b) = the transfer harness is semi-synthetic (see correction above); its real failures = MISSING-ANTECEDENT 5/6 (lexicon) + agent-vs-patient ROLE-CONFUSION (item 005), NOT recency-creep, NOT candidate selection.**
  **=> COMMON DEEP CULPRIT (both frontiers converge): REFERENT / ROLE / GOAL **LINKAGE** -- attaching the extracted event/consequence/valence to the CORRECT agent + the CORRECT goal. Earn-from-exposure: 10 clean verbs registered, ZERO overlap the eval bank (learnable n=0 = teacher/eval coverage-disjoint) + scramble~0 (referent-linkage can't attach consequence->right verb/goal). Transfer: role-confusion (no agent/patient role-awareness) + can't bind harm-event->right agent. This is the RELATIONAL-BINDING / SITUATION-MODEL (#5 DMN) layer, NOT candidate detection.**
**🔎 VET REFINEMENT (2026-08-08 self-drive, disk-VET'd exp_component5_wired_endtoend_v1 + _gold_isolated, wire-don't-island reuse check):** the BINDING half of linkage is ALREADY SOLVED with REAL roles -- goal->outcome->owner binding (GoalOutcomeRegister bind/unbind) fed REAL Component-3 frame_primary_role labels = outcome_binding_accuracy **1.0**, gold_vs_real_delta **0.0** (real roles == gold), N=23 recency-trap bank, role-scramble collapses, beats recency 0.0435 (MIDDLE_BAND only for small-N; mechanically HARD_PASS). So the ATTACH step works WHEN the outcome event is well-formed. Reconciles with naturalistic owner-acc 13/44 (~13-18 OUTCOME_NEVER_TYPED after this session's 2a unification). **=> SHARPENED WALL: not the attach, but PRODUCING the typed + role-aware + valenced OUTCOME EVENT from NATURALISTIC prose (OUTCOME_NEVER_TYPED on goal_bearing_modern_eval_v1 + transfer missing-antecedent/role-confusion + earn-from-exposure learnable n=0 all = this same upstream extraction). Binding / valuation / owner-selection DOWNSTREAM are proven; the frontier is OUTCOME-EVENT EXTRACTION/TYPING (role+valence) from real prose. Owned + PROVEN, don't rebuild: goal_owner_select (Component-5, real-role 1.0), coreference_resolver, thematic_role_labeler/frame_primary_role (Component-3, wired), GoalOutcomeRegister, context_grounded_valence (text-facing, Bopen=1.0). ISLANDED (wire target): grounded-appraisal sim organ (validated_mechanism_earns_islanded). NEXT (self-drive, IN FLIGHT): per-item diagnostic of the never-typed items -> categorize tractable-extraction-fix vs deep-pragmatic vs context_grounded_valence-typeable.**
**🎯🎯 NEVER-TYPED DIAGNOSTIC RESULT (2026-08-08 self-drive, a32e4191 + Director-VET'd; DECISIVE for approach):** OUTCOME_NEVER_TYPED = **13/44** (VET'd: verify_path_unification_2a_part1.py before 18 -> after 13). Per-item buckets: **(A) TRACTABLE EXTRACTION FIX = 4** (agg_anne_picnic + agg_anne_liniment = contraction-tokenization bug "i'd"/"i'm" never split -> breaks _speaker_attributed_goal_holder + detect_affect_state Pattern B; agg_matthew = that-clause goal-complement uncovered; onestop_carle = sentence-splitter nested-quote regex consumes only 1 trailing quote). **(B) DEEP-PRAGMATIC / world-knowledge = 9** (concession speech-act, benefactive-acceptance bridging, dialogue-entailment rejection, result-adjective valence, ToM/reverse-psychology, implicit-avoidance+third-party-substitution, numeric-threshold, domain-world-knowledge) -- ~7 DISTINCT construction types, each ~N=1. **(C) GROUNDED-VALENCE-TYPEABLE = 0** (Director-VET'd on the CERTIFIED score_item path: constant RECIPROCITY/-0.9951 for struck/helped/wounded/comforted/stabbed/rescued/scolded/praised alike -- context_grounded_valence does NOT generalize past its 12-pair closed hand-list; the wire shortcut is CLOSED). **=> DECISIVE: the 3 general infra bug-fixes (contraction-tokenization [general, touches multiple organs], nested-quote splitter, that-clause complement) recover only 2-3 items; the tail is ~69% B-DOMINATED = an UNBOUNDED library of one-off pragmatic constructions. Hand-building it is a LOSING GAME -- STRONGLY validates the USER B-steer (competencies must be LEARNED from exposure, NOT hand-built [[feedback_B_grounding_is_right...]]). The core question is now crisp: HOW TO BOOTSTRAP the construction-LEARNER past the extraction/credit circularity. Brain-foundational answer = DEVELOPMENTAL CURRICULUM (learn simple constructions where the signal is clean FIRST -> those competencies scaffold extraction of the next tier). STRATEGIC FORK surfaced to USER; 3 infra fixes = cheap parallel hygiene, safe to land.**
**🎯 EXACT NEXT ACTION (launch here):** the wall = **REFERENT/ROLE/GOAL LINKAGE**, not detection or lexicon size. Brain-foundational = relational role-filler binding (fMRI-vindicated, OWNED) + situation model (#5 DMN) + thematic-role assignment (agent!=patient). Two concrete sub-targets, both LINKAGE: (a) ROLE-AWARE valence attribution (agent-of-harm vs patient-of-harm -- item-005 class); (b) CREDIT-ASSIGNMENT LINKAGE (which consequence bears on which goal/agent -- 08-03 drill Part B: reverse-replay + SR-transport predecessor + coherence-selector, explicitly NOT coref recency-selector). STEP 0 = a CONSTRUCT-VALID LINKAGE INSTRUMENT: the current transfer harness HARDCODES coref (patients='VICTIM') + oracle coh -- it does NOT exercise linkage at all, so it can't measure a linkage fix. Build a faithful eval that exercises real coref + real role-assignment FIRST, then the linkage mechanism (reuse owned coreference_resolver / thematic_role_labeler / situation_model / GoalOutcomeRegister -- wire-don't-island). REJECTED: expand-the-hand-lexicon (eval-gaming a construct-invalid instrument). Committed through 545c34cf8 (+ this doc). STANDING: don't run 2 committing agents in parallel (git-race); test-first-then-wire; VET+/-per-axis run cert/witness MYSELF in .venv; read-the-code; keep docs current; on wall->discuss+overcome-with-diagnosis; detach-forbidden dispatches; NEVER touch concurrent-session dirty files / NO git add -A.


Branch `dataprep/mcguffey-graded-corpus`, all LOCAL, NO push. Supersedes the 2026-08-03_NIGHT backup. Live blow-by-blow = `notes/WHERE_WE_ARE_NOW.md` TOP BANNER (authoritative; read after this). Permissions were fixed this session (project + global `.claude/settings.json` broad allow + deny backstop) — compound commands run prompt-free; if a settings prompt recurs, reload.

## THE GOAL (unchanged)
A glass-box HDC/VSA (FHRR) substrate that genuinely COMPREHENDS narrative + you can CONVERSE with — earning meaning the brain's way. Invariants: glass-box always; NO borrowed embedding / NO external LLM at inference / NO bolt-on reader-parser; supplying DATA/knowledge/a lexicon is fine; store LOCAL-ONLY, git-commit after every bank, NO origin push without in-session auth. Brain = existence-proof + reference standard.

## USER BARS (locked, govern everything)
- EARNED understanding not supplied-assignment; brain-foundational in EVERY aspect; supply only genuine innate seed, EARN the rest.
- FORMALIZE before building: map the brain (SHAPE+POSITION+METRIC) -> per-component compare vs our organs -> name the gap -> brain-accurate build. For EVERY mechanism: which BRAIN STRUCTURE + does it SHARE a built process -> REUSE the organ.
- VET every negative HARD (incl. whether each component is brain-foundational); VET positives AS HARD AS negatives (this session an agent caught its OWN false-pass — keep that bar). Report MEASURED not READ; disk-verify agent claims.
- Full-auto: drive, do what's RIGHT not easy; select by brain-foundational-right not cheap. Keep docs current. WIRE gains, don't island.
- **🆕 STANDING RULES (USER 2026-08-04, active in the 30-min self-drive cron d4fd7d9a):** (1) EVERY negative -> DRILL HARD + evaluate MINUTELY for brain-foundational (each element SHAPE+POSITION+METRIC vs the exact brain mechanism). (2) EVERY negative -> ALSO ask "is a needed COMPONENT MISSING — especially a LEARNING capability?" (route: used-ability-wrong->loop / missing-primitive->BUILD / missing-fact->SUPPLY / missing-LEARNING->REUSE/EXPAND hdlab/learner, don't build parallel; the shelved labeler was missing OOV-frame-learning). (3) VET positives AS HARD AS negatives, PER-AXIS never aggregate, TRIPLE-CHECK load-bearing data/results. (4) READ THE CODE not the label (9+ mislabels caught this session). (5) keep ALL docs current (backup+MEMORY+charter). See [[feedback_every_negative_check_missing_component_especially_learning_USER_2026-08-04]].

## THE BIG PICTURE — WHAT THIS SESSION ESTABLISHED (the arc)
1. THESIS VALIDATED: the recurring wall was never the MECHANISMS (brain-faithful, work given structure) — it is the missing grounded ~6yo FOUNDATION. Measured: grounded-harm read 4/7 vs frozen-lexicon 1/7, NO_KNOWLEDGE=0.0 (supply grounded knowledge -> the brain-faithful mechanism reads text). "You can't learn revenge from a book."
2. SELF-EXTENSION MACHINERY BUILT + VALIDATED (the mechanism that GROWS the foundation, so we don't hand-build every competency): read -> TYPE causal structure -> predictive_coding residual flags "my schema library can't explain this" (NOVELTY organ, HARD_PASS, WE ALREADY OWNED IT) -> MINT a new causal-role type -> gate through an INDEPENDENT 2nd view (NELL anti-drift) -> consolidate -> re-read. Validated on controlled data (exp_self_extension_loop_v1: anti-drift ABLATION proves the 2nd view load-bearing) AND real prose with an INDEPENDENT GROUNDED 2nd view (exp_self_extension_grounded_realprose_v1: co-fire-on-noise=0.0 disk-verified, mcca_004 mints).
3. INDUCTION FRONTIER SPLIT (prior-art, vindicates the pivot at literature level): AFFECT = earned/grounded SEED (NOT text-inducible, distributional-affect cap, field-wide unresolved); STRUCTURE/lexicon = text-inducible via glass-box selectional-association; MINT = validated grounding-transfer (Harnad "symbolic theft"). Lexicon-induction from text HARD_FAILED this attempt (exp_lexicon_induction_selectional_association_v1: false-pass CAUGHT+rejected; the deeper finding = grounded reads INERT at DISCOURSE scale).
4. DISCOURSE-SCALE grounded reading: deep brain drill adjudicated the wall = REPRESENTATION GAP (disk-verified: live situation-model role_vocab=["agent","mentioned"], no goal/outcome dims). FIX validated: extended the register with GOAL/OUTCOME dims mirroring CausalLinkRegister (exp_situation_model_goal_outcome_dimension_v1: TREATMENT fires 0.833 on naturalistic dispersed goal-block vs BASELINE 0.0 local-window, controls clean).

## 🎯🎯 CURRENT STATE (2026-08-07 — AUTHORITATIVE, FULLY-UPDATED; supersedes every dated block below including the 2026-08-06 COMPACTION-PREP block that follows)

**GOAL (unchanged):** glass-box HDC/VSA (FHRR) substrate that genuinely COMPREHENDS narrative + you can CONVERSE with, earning meaning the brain's way. Invariants: glass-box always; NO borrowed embedding / NO external LLM at inference / NO bolt-on reader-parser (supplying DATA/lexicon/a-representation-SCHEMA is fine, supplying a MECHANISM that works un-brain-like is the anti-pattern); store LOCAL-ONLY, git-commit after every bank, NO origin push w/o in-session auth. Brain = existence-proof + reference standard.

**THE ARC (2026-08-06 -> 08-07): verb-valence grounding CLOSED -> DID-IT-HAPPEN pivot -> FIRST EARNED NUMBER-MOVE.**
- **VERB-VALENCE GROUNDING SHELVED (evidence-closed).** 5 decisive HARD_FAILs trying to type OOV outcome MET/UNMET by grounding the VERB'S valence (structural-read / reward-constant / consequence-learning / dictionary-lookup / combined dictionary+consequence). The combined-tool HARD_FAIL (329f01733) exposed the DEEP finding: **lexical-valence != goal-congruence** -- even a PERFECT verb-valence dictionary can't solve MET/UNMET, because MET/UNMET is a goal-RELATIONAL COMPOSITE: (a) verb result-pole + (b) DID the wanted thing HAPPEN (negation/failure/achievement) + (c) goal-relation. The whole arc attacked only (a).
- **EVAL DRIVER-DECOMPOSITION (evidence, 74e93064a) + GENERALIZATION PROBE (dfc345b1d) = pivot confirmed.** On 36 OOV items: DID-IT-HAPPEN drives 42% (15/36), goal-relation 36%, VERB-POLE only 6% (2/36). Fresh zero-overlap 21-item set: did-it-happen 52%, verb-pole 14% (under the 15% fail-line). => pivot AWAY from verb-grounding (6% ceiling, evidence-closed) TOWARD **did-it-happen wired as a signal INTO the congruence organ** (brain-foundational = ACC expectancy-violation). Keep the cheap in-lexicon verb fast-path (do NOT zero it).
- **✅ DID-IT-HAPPEN BUILT + FIRST NUMBER MOVE (842e5840c, Director ran the witness).** occurrence-gate (reuse _verb_negated_before on the OUTCOME side, flip same<->opposed on negation) + recurrence channel (goal-verb recurs in outcome) + window-widening (fix the sents[-1] hardcode) wired into congruence_decision, strict-ADD. The build HARD-FAILed at net=0 first (7058d026b) because STARVED by upstream referent-extraction bugs (Director reproduced GAP-C: "boat did not sink" -> referent poisoned to "not" -> UNMET, should be MET). REFERENT FIX (GAP-B desired-None + GAP-C negator-skip): **full-36 6/36 -> 8/36 (net +2), 15-subset 6/15 -> 7/15 (net +1), EARNED (congruence-native, not lexicon-luck), GAP-C flips to MET, ZERO regressions (cert 220/3, fair 48/48, real 6/10, NOISE 0 leaks, numeric-threshold not false-MET), NO corners.** PARTIAL (below the +2/15 HARD-PASS bar) -- the FIRST earned movement of the new direction since verb-valence. DEEPER CEILING: all 15 desired goal verbs are OOV of CLASS_REGISTRY so only the RECURRENCE channel works on the real eval; the 4 remaining items each need a fix beyond referent extraction.
- **SUPPORTING WINS banked (all HARD-PASS, cert 220/3, Director-VET'd):** goal-recognition coverage 19/44->33/44 (conative try + intention decide/determine were MISCLASSIFIED as aspectual; 051f6d0ef); negation-scope guard for goals (4/4->0/4 false-fire, complement-negation "tried NOT to cry" still fires; c2f88ea91); precision-quirk fix (goal-clause "wanted to FIX" no longer mis-typed as an outcome -> bystanders abstain; d355b0f36).
- **OWNER-ATTRIBUTION lever SIZED (3b675d281, de-risk): build it THIN + REUSE-HEAVY AFTER did-it-happen.** 25% deciding on natural prose (between trap-curated 69% + coarse 19%); dominant pattern = CROSS-ENTITY outcome-binding (right owner name, wrong entity gets the fact), not name-misattribution; we already own the pieces (goal-coherence candidate-gen + GoalOutcomeRegister).
- **⚠️ OWNER-ATTRIBUTION VET CORRECTION (2026-08-06, Director ran the VET myself, load-bearing honesty): real principled owner-acc = 13/44, NOT the 22/44 headline.** On goal_bearing_modern_eval_v1 the Plot-Units agent's 22/44 reproduces, but 9 of the 22 are ALPHABETICAL tie-break artifacts (outcome never typed -> all candidates tie at 1.0 -> select_outcome_owner falls to winners[0]=alphabetically-first, which coincidentally = the protagonist). RELABELING PROBE confirms decisively: rename amy->zztarget on lw_ice_rescue_amy and the pick flips to jo (wrong). SYSTEMATIC HARDENING (Director, all 9 luck-correct items relabeled to alphabetically-last): **9/9 flip to wrong -> the 13/44 principled number is ROBUST, all 9 are confirmed alphabetical artifacts** (this probe IS 2d's HARD-PASS gate, pre-validated). => owner-attribution is not a broken organ, it is a STARVED one (18/44 OUTCOME_NEVER_TYPED is the same root). Single-winner-argmax cannot represent 2-character rescue structure (= Plot-Units cross-character-links gap = the ToM build = increment 2d).

**🔭 PRIOR-ART LANDSCAPE (USER-requested deep recon, 5 scans; notes/prior_art_{classical_symbolic,modern_neurosymbolic,vsa_hdc_for_language}_2026-08-06.md).** ONE convergent finding: **the goal/outcome REPRESENTATION was SOLVED decades ago** (Lehnert Plot Units 1981: success=M->actualize->+ / failure=M->-, ownership=character-track, cross-character links; Trabasso Goal->Attempt->Outcome episode; TALE-SPIN per-character goal-list-with-failure-reasons; BORIS affect-as-goal-status-signal + TAUs for zero-lexical bridging) -- all role-filler-shaped, adoptable as a target SCHEMA. **But the WALL -- automatic RAW-PROSE -> that representation + GROUNDING -- is the field's 45-YEAR, CROSS-PARADIGM, STILL-UNSOLVED core** (AESOP 2010 automated Plot Units -> 45% F1 on toy fables, named "goal detection + successful-plan-completion" [= our did-it-happen] as THE unsolved piece; VSA never resolves where atomic-vector MEANING comes from = our grounding wall, field-wide). **Our VSA-narrative-comprehender combination is CONFIRMED NOVEL.** => our walls are NOT design errors; they are the field's central hard problem, which the brain proves is solvable. Baker-Tenenbaum inverse-planning = interpretable goal-INFERENCE but needs a pre-given world + no achievement-verification. **USER greenlit adopting recommendations WITH a discipline: be clear what we pull in, TEST vs what we have, verify it's done OUR (brain-foundational/glass-box) way. Plot Units comparison/adoption-plan drill IN FLIGHT (a58ba5f4, recon-only, adopt-schema-not-mechanism, our-way-gated).** Stanford sieve coref = scrutinize hardest (bolt-on-mechanism risk; likely LEARN-FROM not adopt-wholesale). TALE-SPIN cautions noted (goal-tracking alone != comprehension; watch implicit world-model preconditions).

**🧠 BRAIN-COMPONENT MAP (roadmap, notes/brain_component_map_narrative_comprehension_ROADMAP_2026-08-06.md).** 11 systems, each tagged: OWNED->leverage = role-filler binding (#3, fMRI-vindicated) + bridging-inference (#10, praise/affect bridges HARD_PASS) + predictive-coding (#11 thin). PARTIAL->strengthen (current convergence) = referent-tracking (#4, just fixed GAP-B/C), mentalizing/goal-recognition (#6), goal-monitoring/DID-IT-HAPPEN (#7 = ACC expectancy-violation, moving now), valuation (#8 structural). MISSING->frontier = the integrated SITUATION MODEL (#5, DMN) + GROUNDED affective spokes / felt valence (#2/#8/#9 = the grounding wall). did-it-happen is a first component of #5/#7.

**NEXT (evidence-ordered):** (1) the 4 remaining did-it-happen items each need a fix BEYOND referent extraction -- goal-governing-verb recognition (no_desiderative_goal_found: lw_laurie/woz_lion/agg_gilbert); OOV-verb-no-object-referent (intercede: agg_anne_mrs_barry); ECM copula-referent ("wish IS to get": woz_dorothy); distractor-clause + GAP-2 first-match (lw_meg) + GAP-4 lemma given->give (woz_scarecrow). (2) owner-attribution companion (thin/reuse-heavy, cross-entity binding). (3) Plot-Units adoption (whatever passes the our-way gate). (4) DEEP frontier: the grounding wall (#2/#8) + the integrated situation model (#5). (5) Tier-2 over-fire on incidental verbs (gather); registry rows.

**KEY DOCS:** this block (authoritative). Then: notes/prior_art_*_2026-08-06.md (the landscape); notes/brain_component_map...ROADMAP (the roadmap); notes/research_goal_bearing_eval_driver_decomposition + ..._generalization_probe (the pivot evidence); notes/research_owner_attribution_natural_prose_derisk; the did-it-happen prereg 2026-08-06_did_it_happen_occurrence_gate_v1.md. Standing rules unchanged (VET positives per-axis + run witnesses myself; read-the-code; wire-don't-island; brain-foundational-right; keep docs current; NO bolt-on mechanism; recon changes nothing w/o USER confirm; 30-min self-drive cron). Committed through 842e5840c (+ the Plot-Units comparison's pending notes).

## 🧭 SITUATION-MODEL (#5 / DMN) = CONFIRMED NEAR-TERM KEYSTONE + BUILD SPEC FILED (2026-08-06 PM, full-auto push, USER-authorized "do it, brain-foundational in every way")
USER strategic sequence this push: DMN deep-dive -> "is that the thing that unlocks the rest? holistic plan" -> "incorporate all the things we're considering" -> "do it, full auto, brain-foundational" -> "when hitting a wall, do a deep brain-fidelity audit." DELIVERED:
- **PLOT-UNITS VET (done, our-way-gated):** NOT a wholesale target we're missing — 4 core elements KEEP-OURS (equal-or-better than Lehnert 1981), 2 REJECT. Adds exactly ONE real thing: CROSS-CHARACTER causal links (= USER's own planned ToM build; external validation, not redirection) + 2 cheap wire-don't-island adaptations. Director disk-VET'd the 2 load-bearing claims: appraise() IS a symmetric n_unmet>n_met tally (order-blind, TERMINATION gap real); lw_ice_rescue_amy exists as a genuine 2-char rescue. NOTHING adopted w/o USER confirm.
- **PHASE 1 measured (Director hand-VET'd): the coverage wall is ~29/31 INTEGRATION-limited, ~0-2 pure GROUNDING-blocked.** Eval-scale confirmation of the pivot's deep finding — met/unmet is RELATIONAL (did-it-happen/goal-relation), recoverable via negation/goal-echo/affect-reaction/cross-character-granting WITHOUT grounding intrinsic verb-valence. => grounding wall is a SMALLER residual for narrative than the isolated-verb experiments implied; INTEGRATION (#5) is the binding constraint. This ANSWERS "is #5 the unlock": YES for the near-term cluster (coverage -> owner + did-it-happen + cross-character all at once), with grounding the deeper residual behind it.
- **#5 BUILD SPEC FILED: notes/formalize_situation_model_DMN_integration_spec_2026-08-06.md** — the deep brain-fidelity audit (DMN per-operation SHAPE+POSITION+METRIC vs our organ vs gap) + build-ready increments. Verdict: of ~11 DMN ops we OWN 6 thinly (binding/amodal/accumulate/PE-detect/predict/3-of-5-index-dims); #5 is mostly WIRING over owned brain-faithful parts, NOT a new-mechanism wall like grounding. PLAN: Track A (cheap, #5-independent: A1 order-aware appraise/TERMINATION, A2 stored-blocking-cause, A3 the 4 did-it-happen items) -> Phase 2 keystone (2a shared SituationRegister byte-identical-first -> 2b whole-passage outcome resolution [attacks the 41% never-typed] -> 2c PE event-segmentation + entity carry-forward + force-dynamics MOTIVATION -> 2d cross-character links [the relabeling-probe HARD-PASS]) -> Phase 3 grounding residual. DEFERRED explicit: space/time index dims, Tier-2 precision, registry rows. Every increment strict-ADD + cert 220/3 + Director runs witness.
- **NEXT ACTION:** dispatch Track A (cheapest, several items overlap never-typed) + Phase 2a, VET on return. did-it-happen 4-items subsumed into A3.

### PROGRESS (2026-08-06 PM, full-auto push -- builds + VETs, Director ran every witness + cert himself)
- **2b (window-widen owner path) = HARD-FAIL -> REVERTED (70c87c45b).** +4 recovered (matched pre-VET prediction exactly) BUT 1 regression (lw_beth; backward-scan lexical branch ignored the sentence's own resolved subject). Reverted, baseline clean 18. Diagnostic banked: window-widen alone capped at +4 AND unsafe.
- **TAIL IS HETEROGENEOUS:** the 13 truly-stuck items span ~5-6 relational-inference SIGNAL TYPES (referent-recurrence / enablement-granting / cross-char affect-reaction / threshold / reported-outcome / negation), each ~2-3 items, NO single fix >=6 => 2c-extended = a SUITE of small can-fail detectors (the "growing library of competencies" anchor); numbers move in small compounding steps.
- **✅ REFERENT-RECURRENCE + A3 REFERENT FIXES = HARD-PASS (7e5ed63fe, Director ran witness + cert in .venv himself).** did-it-happen keyed on the goal's TARGET/referent (brain = ACC expectancy-check on the goal referent-state), extends 842e5840c in the CONGRUENCE/polarity path. A3: fixed referent extraction (ditransitive-theme him->heart, ECM-copula "wish IS to get"->kansas, quote-boundary/deictic/finite-clause guards -- 2 self-caught regressions fixed). Part B: new congruence_referent_recurrence_windowed (literal/shared-feature/MONEY_CLASS, occurrence-gated, max_window=2). **MEASURED: polarity full-44 11->15 (+4), OOV-36 8->12 (+4), did-it-happen 15-subset 7->10 (+3); recovered woz_tin_woodman(heart)/onestop_hunt(money-funding)/woz_dorothy(Kansas-recurs, VET-confirmed via full text)/woz_lion(courage-recurs); ZERO regression (all 11 prior-correct hold, 8 OOV did-it-happen wins hold), cert 220/3, NOISE 0 leaks, self_test green.** FIRST EARNED number-move of the situation-model push. Minor flag: MONEY_CLASS is a hand-supplied eval-motivated noun class (invariant-OK as DATA but ideally from lexical_similarity -- follow-up).
- **🔑 TWO-PATHS CONFIRMED EMPIRICALLY (the real 2a keystone):** this +4 moved POLARITY only; OWNER OUTCOME_NEVER_TYPED stayed 18 (Director checked). The owner path (enumerate_and_score/type_goal_events) and polarity path (congruence/did-it-happen) are SEPARATE typing engines; a detector in one does not move the other's number. => **2a = UNIFY the two engines (owner path consumes the entity-directed congruence/did-it-happen typing) is now the demonstrated NEXT KEYSTONE -- build once, every detector (this +4 + all future) propagates to BOTH numbers.** Sequence: 2a-unify FIRST, then the cross-character detectors (enablement/granting = ToM/2d, highest yield) land in the unified engine.
- **✅ 2a-PART-1 = HARD-PASS (dc111492b, Director VET'd witness+cert+owner-count in .venv himself).** Path-unification fallback: in enumerate_and_score's NEVER_TYPED branch (was: raise), try congruence_with_lexicon_fallback for MET/UNMET, resolve goal-holder via find_desired_state's firing-sentence subject (GeneralRecencyEntityResolver -- the RICHER goal-recognition, since the owner path's own type_goal_events mis-types the holder: 'oz' for woodman, none for lion), bind + return that entity. STRICT-ADD (only where it currently raises). **MEASURED: OWNER OUTCOME_NEVER_TYPED 18->15 (recovered onestop_hunt->hunt, woz_dorothy->dorothy [both predicted], + BONUS onestop_limal->limal, ALL 3 owners CORRECT -- Director re-checked); fair instruments BYTE-IDENTICAL (goal_owner_fair_v1 48/48 + multigoal 12/12, part of cert); cert 220 passed/3 skipped exit 0 (Director ran .venv).** FIRST propagation of a detector to the OWNER number -- the unification architecture WORKS. Probes first killed 2 naive versions (owner-path goal-holder wrong/none/ambiguous; goal-sentence-subject resolves hunt/dorothy but NOT woodman/lion first-person dialogue). **DEEPER FINDING: the two paths diverge on GOAL-recognition + goal-holder-resolution + outcome-typing (all three), not just outcome.**
- **SESSION SCOREBOARD (both numbers, earned + VET'd):** polarity typing 11->15 (+4, referent-recurrence 7e5ed63fe); OWNER never-typed 18->15 (-3, 2a-part-1 dc111492b). Both HARD-PASS, Director ran witness+cert.
- **⛔ CROSS-CHARACTER RESPONSE detector (ToM/2d literal-lexicon v1) = HARD-FAIL Δ+0 -> REVERTED (de34ecdd5 built, 4ef33fda8 reverted; Director VET'd Δ+0 + root cause).** Mechanism perfect (0/16 NOISE over-fire, zero regression, cert 220/3) but fires on NOTHING: 3/4 targets (laurie/jo_mr_laurence/diana) convey the response via FREE-INDIRECT-DISCOURSE/dialogue ("Why, I guess so"=assent), NO literal accept/refuse verb (Director grep-confirmed); dashwood has literal "take" but blocked by the Mr.-abbreviation splitter bug. **v2 = SPEECH-ACT / assent-in-dialogue detection (illocutionary force), a SEPARATE LARGER build -- the real ToM lever; the literal-lexicon shortcut is a dead-end.** PRE-VET LESSON: validate SURFACE-FORM presence, not just the semantic mapping. notes/formalize_cross_character_response_detector_ToM_2d_2026-08-06.md updated.
- **STRATEGIC STATE (2 HARD-FAILs on the hard tail: 2b window-widen, cross-char literal): the CHEAP coverage detectors are exhausted; the remaining owner/polarity tail (~15/44) needs DEEP pragmatic comprehension -- speech-act/free-indirect-discourse assent, dialogue-goals, grounding (spoil), threshold, reported-outcome. Each is a substantial build, not a cheap detector. The TRACTABLE wins are banked (polarity +4, owner -3, unification working). FORK for USER: (A) formalize+build SPEECH-ACT/assent detection (the real ToM lever, biggest remaining yield, substantial); (B) mop up the cheap tail (2a-part-2 speaker-attribution +1 woodman, threshold +1 chen, Mr.-splitter fix +1 dashwood = ~+3 owner, low-risk, small); (C) consolidate #5 progress + pivot frontier. DIRECTOR LEAN: (B) first (bank ~+3 cheap owner gains + fix the general Mr.-splitter infra bug), THEN (A) as the next major build. Surfaced; awaiting nothing -- driving (B) unless USER redirects.**
- **✅ 2a-PART-2 = PASS (78e0c7908, Director VET'd owner-count+woodman+2a-part-1-holds; cert re-running to confirm).** Speaker-attribution goal-holder for first-person dialogue ("I want... said the Tin Woodman" -> holder woodman); extracted shared _first_roster_name_token from the affect-state Pattern B (reuse, no duplicate), fires only in the NEVER_TYPED branch after 2a-part-1's subject resolution returns None. **OWNER OUTCOME_NEVER_TYPED 15->14 (recover woodman, owner correct); 2a-part-1 recoveries hold; fair instruments byte-identical; cert 220/3 (agent).** Completes the 2a unification (all goal-holder modes: subject + speaker-attribution) + a general dialogue-goal-holder capability.
- **MOP-UP BATCH RE-SCOPED (Director pre-VET, honest): only woodman was a clean cheap win (+1, done). chen/threshold = bespoke numeric mechanism for 1 item (borderline eval-gaming, DROPPED); dashwood needs the reverted cross-character detector (NOT cheap). => the cheap coverage tail is now EXHAUSTED.**
- **📊 SESSION SCOREBOARD FINAL (all earned + Director-VET'd, cert green throughout): polarity typing 11->15 (+4, referent-recurrence); OWNER never-typed 18->14 (-4, 2a-part-1 unification + 2a-part-2 speaker-attribution). Situation-model UNIFICATION proven (detectors propagate to both numbers). 2 honest HARD-FAILs reverted cleanly (2b window-widen, cross-char literal-lexicon), each bought a precise diagnosis.**
- **✅ GENERALIZATION VET (Director, read-only, held-out zero-overlap sets -- validates the session's gains are REAL CAPABILITY not eval-overfit):** ran the current production organs on real_text_goal_owner_diagnostic_v1 (10 McGuffey, gold pol+owner, DIFFERENT corpus from the build eval) + goal_outcome_oov_psych_gold_v1 (6, Moby-Dick/Persuasion/etc.). **KEY FINDING: REFERENT-RECURRENCE FIRES CORRECTLY on a FRESH item (mg2_henry_bootblack: goal 'help mother buy food' -> outcome 'gave to his mother to buy food' = target recurs+realized -> MET, VET-confirmed legitimate) -- NOT just the build-eval woz/hunt items => the mechanism GENERALIZES, is real capability.** Profile generalizes too: high-precision/low-recall (McGuffey polarity 4/10 typed, 4/4 correct; owner 6/10; abstains on the rest = the SAME coverage wall). Below majority-class on accuracy only because abstaining loses to majority-guessing, but typed predictions are trustworthy. CAVEAT (honest): the psych set uses a DIFFERENT label taxonomy (blocked/achieved/mixed, Plot-Units-style, NOT met/unmet) -> the 0/6 there is an unfair label-space mismatch, not a capability failure (a fair test needs taxonomy remapping; noted). NET: session gains VET'd as real+generalizing; coverage remains the universal wall.
- **📏 FAIR AGGREGATE HELD-OUT GENERALIZATION NUMBER (Director, taxonomy-remapped, n=16 hand-gold = 10 McGuffey + 6 classic-novel [Moby-Dick/Persuasion/etc.], zero-overlap with build eval): POLARITY 71% precision when typed / ~31% recall; OWNER 75% precision / 50% coverage. => the session's comprehension machinery is HIGH-PRECISION + COVERAGE-LIMITED on diverse fresh prose, and GENERALIZES (precision holds off the build eval = real capability, not overfit). Coverage is the universal wall everywhere. This is the honest bottom-line characterization of the tractable frontier reached.**
- **✅ GROUNDED RESULT-CLASS TIER = PARTIAL +2 (509607e52, Director VET'd polarity+owner+malala+cert-running).** The authorized ~6yo grounded-foundation SUPPLY (option-A from the grounding fork, finally built): small hand NEGATIVE/POSITIVE result-verb lexicon (spoil/ruin/punish/scold/damage/harm/wreck/rap // reward/praise/succeed/mend/fix) as a LAST-RESORT congruence tier, GOAL-RELATIONAL (fires only when a goal is recognized + result subject-linked to a person + strict-ADD when relational channels abstain + occurrence-gated). **MEASURED: polarity 15->17 (+2, ts_tom_sugar rap->UNMET + agg_anne_picnic punish->UNMET, both correct); ZERO regression; NOISE 0/8 + adversarial-non-goal 0/8 (fixed-broken-toy/broke-record/storm-damage/etc.); fair instruments byte-identical; cert 220/3.** Self-caught over-fire: removed 'fail' (mis-typed onestop_malala "Taliban failed"=good-news UNMET) + excluded 'break' (idiom "broke the record"). PARTIAL (below +3): agg_anne_liniment (spoil) UNRECOVERABLE -- the cake disaster is IRONY ("flavored with Anodyne Liniment", no literal spoil token) = a grounding-limited case even grounded-supply can't reach (needs irony/inference). **🔑 HONEST: the +2 did NOT propagate to OWNER (stays 14) -- the 2a-unification's goal-search is NARROWER than congruence's (only sents[:-1], no coordinated-sentence "X tried..and got Y", misses "I'd" contraction) -> enumerate_and_score still raises for both items. NOT a regression (raised before too), but reveals 2a-part-3: ALIGN the owner-path goal-search with congruence's so polarity gains propagate to owner.** This IS the authorized (B)-grounding direction in bounded form; the DEEPER (B) program (experiential-simulation for FID/irony/social) remains a USER-fork research commitment this increment does NOT make.
- **✅ 2a-PART-3 = PASS +1 owner (Director surgical 1-line fix + VET'd; cert re-running).** Aligned the owner-path _unify_owner_via_polarity_path goal-search with congruence's: `for s in sents[:-1]` -> `for s in sents` (include the last sentence for the coordinated single-sentence "X tried to VP, and got RESULT" construction). Pre-VET'd across all 14 never-typed (+1 correct, 0 wrong -- SAFE since _unify only runs in the NEVER_TYPED fallback, cannot touch a typed item). **MEASURED: OWNER never-typed 14->13 (ts_tom_sugar->tom recovered, correct -- the grounded-tier's rap->UNMET polarity gain now PROPAGATES to owner); all prior 2a recoveries hold (hunt/dorothy/limal/woodman); zero regression.** agg_anne_picnic owner still blocked by the "I'd" contraction (goal subject "I'd like" doesn't resolve to anne + no speaker-attribution) = a separate follow-up. Demonstrates the unification WORKS end-to-end: a congruence-path detector gain -> owner-number gain once the goal-searches align.
- **✅ DIALOGUE-GOAL RECOGNITION = HARD-PASS (2747fac9a, Director VET'd targets+adversarial+witnesses+cert) -- a NEW construction competency added to the library (broaden move, the North Star direction).** Recognizes a request/hedged-wish in dialogue AS a goal (the backup-flagged "most common real-prose blocker"). Two strict-ADD fallback passes in find_desired_state (tried only when the original purpose-infinitival scan returns None): HEDGED_MODAL_WISH ("wish [i/we] might/could/would VP") + REQUEST ("let me/us VP" / "may/can I VP" / "will you (not) VP" / "please VP"), over-fire-guarded (first-person-only wish; light-verb stop-list; addressee-"you" offer guard). **MEASURED (Director re-ran): 3/3 targets recognized (mg3_frank wish-might, ts_tom wish-could, mg3_boy let-me); 0/11 precision-bank + 0/6 adversarial false-fire ("will you be quiet"/"let me think"/"may I help you"/"I wish you would stop" all -> None); goal-recognition coverage 32->34 (+2: ts_tom + bonus agg_anne_mrs_barry "please forgive me"); zero regression; cert 220/3.** HONEST: the COMPETENCY (goal-recognition half) is built + validated, but eval NUMBERS don't move yet -- downstream end-to-end 0/3 (outcome-typing for these dialogue passages + a "let me IN"->verb=in extraction quirk are SEPARATE follow-ups, pre-registered). A real library-competency win (modular capacity, forgetting-proof, brain-faithful), number-movement pending the downstream outcome-typing companion.
- **✅ REQUEST-RESPONSE TYPING = HARD-PASS (9d76c4179, Director VET'd targets+held-out+overfire+cert) -- COMPLETES the dialogue-goal competency END-TO-END (recognition -> typing -> number).** New congruence_request_response tier (4th last-resort, fires only for a recognized REQUEST_*/HEDGED_MODAL_WISH goal): scans the next dialogue turn for a grant-verb (invite/allow/admit/permit/grant) or a negated echo of the request's own verb/particle surface, occurrence-gated + referent-links-guarded + deixis-shift guard for the "let me IN"->"let you IN" particle case. **MEASURED (Director re-ran): mg3_boy UNMET + mg3_frank MET (both correct, both correct OWNER via 2a unification); real_text HELD-OUT polarity 4/10->6/10 (+2 on the generalization set); full-44 17->17 (no-op by design, request items are in real_text); fair instruments byte-identical; over-fire 0/5+0/11+0/6+0/16; cert 220/3.** Agent found+fixed a real OPEN_CLASS priority collision (invited/open both OPEN_CLASS -> mg3_frank wrongly UNMET before the tier) + closed a referent-echo over-fire. **=> DIALOGUE-GOAL is now a COMPLETE end-to-end competency (recognition + typing), moving a HELD-OUT number.** FOLLOW-UP (pre-existing, NOT this build): same_class_same_referent tier over-types "open the shop/opened the report" MET (referent shop!=report judged same) -- a separate verb-class-tier referent-precision fix.
- **🧭🆕 DIRECTION SET (USER-CONFIRMED 2026-08-07): B = GROUNDING is the program; A (competency library) should come FREE via EXPOSURE + a robust LEARNING system, NOT hand-built detectors (my +1-2 detector grind was the easy-path anti-pattern at the strategic level). [[feedback_B_grounding_is_right_A_competencies_should_be_learned_free_via_exposure_USER_2026-08-07]].** PLAN OF RECORD (co-designed w/ USER) = notes/PLAN_B_grounding_word_context_affect_superposition_map_2026-08-07.md: a word = a SUPERPOSITION of (context (x) sense+affect) bindings that COLLAPSES via a context KEY (VSA-native; matches context_grounded_valence's own "context-conditioned grounding" gap). 3 layers: (1) grounded affect PRIMITIVES [B core], (2) LEARNED word->context->affect map [dictionary MENU supplied=data-OK, selection+affect EARNED], (3) context-collapse read [owned]. KEY USER INSIGHT: **social valence = MENTAL/SOCIAL HARM (Eisenberger dACC/insula overlap) -> REUSES the owned+proven physical-harm valuation; social channel = a new INPUT, not a new engine.** Learning signal = STORIES-WITH-CONSEQUENCES (self-improving reader) once primitives grounded -> UNIFIES B (ground primitives) + A-steer (learn map from exposure) into ONE engine. **✅ STAGE 1 = HARD_PASS (ca1d70d1a, Director-VET'd): social valence GROUNDS like physical harm -- exp_social_relational_grounding_axis_v1, supplied 12-word social seed + wordnet_polarity_propagation + the owned earned theta -> open-vocab 0.833 on 12 HELD-OUT verbs (disjoint), scramble 0.483/lift0.35, ablation 0.000 (seed is the lever), random-theta 0.467 (earned-not-noise). Architecture proven.** **✅ STAGE 2 = HARD_PASS (04af969c4, Director-VET'd): the Layer-2 SUPERPOSITION MAP + taught context-collapse WORKS.** exp_word_context_affect_superposition_map_v1: word=bundle(bind(context_key (x) sense)), context key=patient animacy, (context->sense) TAUGHT from labeled train, tested held-out. held-out collapse 1.000; SCRAMBLE 0.400/lift0.600 (bindings LEARNED not lookup); context-driven 1.000; single-sense baseline 1.000. DECISIVE ANTI-CONFOUND (real in code): spoil polarity REVERSED vs beat/strike/whip/crush -> same animacy feature selects OPPOSITE senses per word -> word-SPECIFIC learning, not a generic heuristic. USER's superposition architecture PROVEN at mechanism level. HONEST: coarse context key (animacy) + hand-taught -> mechanism proof, not rich generalization. **✅ STAGE 4 = HARD_PASS (d423cce63, Director-VET'd): the map can be LEARNED FROM EXPOSURE (consequence-as-teacher) -- the A-steer/North-Star CRUX.** exp_learn_context_affect_map_from_exposure_v1: learn (word,context->sense) by reading each story's SHOWN CONSEQUENCE via the owned congruence outcome-read (MET/UNMET vote) + the owned anti-drift decide_keep_or_revert gate. learned held-out 1.000 (28 items, NO hand-teaching); SCRAMBLE-CONSEQUENCE 0.486/lift0.514 (decisive: shuffle consequences->chance = consequence IS the teacher); anti-drift abstains on noise; spoil-reversed holds; learned==hand-taught (gap 0.000). HONEST: gap 0.000 = clean-corpus artifact -> pipeline faithful under CLEAN supervision; NOISY-REAL-PROSE robustness = next stress test. **=> 3 core mechanisms of USER's architecture now PROVEN (mechanism-level, controlled data): grounding primitives (S1) + superposition map+collapse (S2) + learned-from-exposure (S4). The word->context->affect map learned from stories = proven end-to-end.** PARALLEL RESEARCH in flight (a91d473e learn-affect-from-consequences; a4cee399 context-conditioned-grounding+extraction-coverage) to inform noisy-prose robustness + the richer context key. **✅ RICHER CONTEXT KEY = HARD_PASS (0527afeab, Director-VET'd incl own spot-check) -- the LINCHPIN proven (test-first per USER).** exp_richer_selectional_context_key_v1: replaced the BINARY animacy key with a 2-slot VSA BUNDLE (bind(AGENT,PERSON)+bind(PATIENT, finer_WordNet_class)); filler_class = a GENERAL live WordNet-hypernym classifier (PERSON/ANIMAL/ARTIFACT/EVENT/GROUP/SUBSTANCE), NOT a per-item lookup (Director spot-checked marathon->EVENT, committee->GROUP, acid->SUBSTANCE off the classifier). DECISIVE: on words where animacy is structurally FLAT (run a RACE=event vs run a COMPANY=org, both inanimate) rich-key=1.000 vs animacy-only=0.500 (chance), margin 0.500; NO regression on Stage-2 set (1.000); scramble collapses (lift 0.46-0.54); held-out generalization 10/10 (steeplechase/posse/grenade). = the brain's selectional-restriction cue, glass-box, composes with the Stage-2 map. **=> ALL 4 CORE B COMPONENTS NOW PROVEN as isolated experiments: grounding primitives (S1) + superposition map+collapse (S2) + learned-from-exposure (S4) + richer selectional context key. USER architecture fully validated at mechanism level.** RESEARCH LEVERS for the remaining walls (both landed, USER-affirmed, glass-box, reuse owned): COVERAGE = VerbNet/Levin class backoff for OOV verbs (research a4cee399/58f32033b); NOISE-ROBUSTNESS = Snorkel/Dawid-Skene SOFT-combine of >=3 weak consequence-signals into the owned decide_keep_or_revert (research a91d473e -- precisely diagnosed the consequence_learning_loop HARD_FAIL as a hard-AND coverage-collapse, 3/1434 windows). **BOTH REMAINING LEVERS TESTED -> HARD_FAIL, but CLARIFYING (Director-VET'd; USER discuss-and-overcome).**
- **COVERAGE lever (Levin/VerbNet class-backoff) = HARD_FAIL (883e1b7ba): net -2 (17->15 correct), +1 gain (ts_tom via get->possession) BUT -3 regression.** Root cause (mechanistic, VET-confirmed off metrics): the Levin backoff added to the EARLY verb-class tier (_verb_classes) fires a confident class-verdict that PRE-EMPTS later, more-reliable tiers (referent-recurrence/grounded-result) that were correctly answering (woz_dorothy/onestop_limal CORRECT->WRONG via referent_mismatch). = a TIER-ORDERING bug, NOT a Levin-class-wrongness. The technique WORKS (clean +1 on ts_tom). OVERCOME: move Levin backoff to a LAST-RESORT tier (fires only when all later tiers abstain, like grounded-result) -> predicted +1 ZERO regression. **✅ OVERCOME CONFIRMED = PARTIAL (276674abb, exp_verbclass_backoff_coverage_v2, Director-VET'd): last-resort placement (only fires when the full cascade abstains, structurally cannot pre-empt) -> +1 (ts_tom NA->CORRECT) ZERO regression (the 3 v1-regressions all held via primary_cascade), no over-fire (do stays unclassed), strict-ADD restoration_ok. PARTIAL only re held-out 4/8 = a SEPARATE referent-extraction ranking issue (referent_recurrence pre-empts before the backoff on held-out), NOT this lever. Worth WIRING as a production last-resort tier (general OOV-verb coverage, feeds context extraction).** **✅ WIRED TO PRODUCTION (50c0e5ab4, Director-VET'd polarity+cert): last-resort Levin tier live in goal_typing.py congruence_with_lexicon_fallback (recursion-capped depth 1, fires only after all tiers abstain). PRODUCTION polarity 17->18 (ts_tom NA->CORRECT), zero regression, no over-fire, fair instruments 48/48+12/12 unchanged, cert 220/3. Step-1 jump-in DONE.**
- **NOISE lever (Snorkel/Dawid-Skene soft-combine) = HARD_FAIL (fc21752f3, mislabeled by a git-race, content-safe), 2 IMPORTANT findings:** (1) **THE COVERAGE COLLAPSE SELF-RESOLVED:** the stale premise (hard-AND 3/1431 windows) is fixed -- re-run today AND-gate=439/1655 (26.5%), ~130x higher, from THIS SESSION's goal_typing hardening (did-it-happen/referent-fixes/goal-recognition). Coverage is no longer the bottleneck; soft-combine trusted FEWER (165), rescue inverted. The Dawid-Skene EM math itself is validated (self-test). (2) **THE REAL WALL (decisive, scramble-control): on REAL prose the teaching signal DOESN'T CARRY -- scramble does NOT collapse (real 0.472 vs scrambled 0.500, gap -0.03), primary 0.472 < majority floor 0.639.** => the earn-from-exposure map CANNOT learn from real prose yet. DIAGNOSIS: CREDIT-ASSIGNMENT precision -- the available weak signals (referent-linkage+lexicon) can't tell the loaded verb from co-occurring light verbs; the sharper signals (referent-recurrence/grounded-result) fire ZERO times on real windows (too narrow). => **the earn-from-exposure loop is bottlenecked by the SITUATION-MODEL PRECISION that reads the story = the CONTEXT-EXTRACTION frontier (the circularity, now MEASURED). Grounding-from-exposure can't outrun context extraction.**
- **HONEST SYNTHESIS: the B architecture is NOT broken -- all 4 components proven on CLEAN signal. What failed is APPLYING it to real prose, for ONE reason: the teaching signal is only as good as the context extraction (~30-50% coverage, imprecise on real prose). B (grounding) + context-extraction are ONE frontier. The confirmed next lever = the richer context KEY (already PASSED) + everything raising situation-model precision.**
- **✅ STEP 2 (sharpen credit-assignment) = HARD_FAIL, but PINPOINTED the wall concretely (b5fdd956c, Director-VET'd).** Sharpened credit-assignment (goal->outcome resolving-clause anchor + WordNet light/loaded down-weight) helped attribution a LITTLE (precision 0.468->0.494, exposures 958->338) but scramble lift = 0.0167 (<0.10; NO_SIGNAL) -- the real-prose teaching signal STILL doesn't carry, primary 0.417 < floor 0.639. Confirms credit-assignment sharpening ALONE is insufficient. **🔬 BUT VET REVEALED THE SPECIFIC CULPRIT (read-the-code the actual credited 'verbs'): VERB IDENTIFICATION IS BROKEN on real prose -- the pipeline credits NOUNS (boy/sister/cheek/egg/hat/gown), NAMES (phillip/walter), and STEMMER GARBAGE (com/fr/kne/capaciou/rebelliou) as 'outcome verbs' (lemma_verb stemmer false-firing on non-verbs). The learning loop is fed NOISE, not verbs.** => the binding wall is context extraction, and its CONCRETE, FIXABLE first culprit = VERB IDENTIFICATION / LEMMATIZATION (a broken component per rule-b, not the abstract 45-yr wall). OVERCOME: fix verb-ID (POS/lemmatizer) -> clean credit-assignment inputs -> re-test earn-from-exposure. This is the shared context-extraction frontier with a specific actionable lever.
- **REVISED PLAN (USER full-auto green-lit): (1) LEVIN LAST-RESORT FIX (test-first, clean +1) DONE+WIRED; (2) WIRE the richer context KEY (proven) into the situation model + target CREDIT-ASSIGNMENT (attach the consequence to the goal the situation-model LINKS it to, not window co-occurrence) = the real lever to make real prose a clean-enough teacher; (3) on clean signal the learning loop is PROVEN + waiting -- as extraction improves, it works; (4) then Stage-5 EARN primitives via experiential-social simulation. Keep docs current.** The situation-model competency work (polarity 11->17, owner 18->13) = the STRUCTURAL reading this grounding will feed with FELT meaning.
- **NEXT = THE DEEP FRONTIER (cheap tail exhausted): the remaining ~14 owner-never-typed need DEEP PRAGMATIC/GROUNDING comprehension -- free-indirect-discourse assent ("Why, I guess so"=yes), cross-char affect, grounded valence (spoil), reported-outcome. FORK for USER: (A) SPEECH-ACT/assent detection (biggest lever, but a real research build bordering the glass-box/no-LLM line), (B) GROUNDING frontier, (C) broaden to a new competency/eval. Director lean: formalize (A) carefully next (dialogue-goals pervasive in real prose), but this is a genuine research decision -- surfaced, awaiting USER steer for the next MAJOR build.**

## 🎯 (SUPERSEDED earlier-session state 2026-08-06 COMPACTION-PREP — kept as lineage; CURRENT STATE is the block ABOVE)

**THE GOAL (unchanged):** a glass-box HDC/VSA (FHRR) substrate that genuinely COMPREHENDS narrative + you can CONVERSE with, earning meaning the brain's way. Invariants: glass-box always; NO borrowed embedding / NO external LLM at inference / NO bolt-on reader; supplying DATA/lexicon/knowledge OK; store LOCAL-ONLY, git-commit after every bank, NO origin push w/o in-session auth. Brain = existence-proof + reference standard.

**THE BIG ARC (2026-08-05 night -> 2026-08-06 morning, FULL-AUTO-ALL-NIGHT USER-authorized):** Built + WIRED 5 deep-VET comprehension components (all cert 208->220, zero regression, each via the north-star loop formalize->build->VET->on-negative-drill->fix->wire): (1) GOAL-OWNER selector hdlab/goal_owner_select.py::select_outcome_owner (48/48 fair + 12/12 multi-goal, gold-free); (2) OUTCOME-VALENCE goal-congruence hdlab/goal_typing.py (MET/UNMET = goal desired-vs-actual state, not word-lexicon); (3) ATL LEXICAL-SEMANTIC HUB hdlab/lexical_similarity.py (shared-FEATURE cosine over FHRR bundles, Cox 2024) + open-vocab verb-typing hdlab/verb_lexical_similarity.py (2 verb-feature bases); (4) situation-bias severed-loop wired @ context_grounded_valence.py:141; (5) frame-arity role-assignment (intransitive->no patient) PROMOTED to production default.

**THE PIVOTAL FINDING + CURRENT FOCUS:** the wired organs were HARD_PASS on HAND-AUTHORED instruments but a GENERALIZATION PROBE on 10 REAL McGuffey passages found they DON'T generalize (owner-acc 0.30 vs recency 0.70). USER then requested a DEEP BRAIN-FOUNDATIONAL AUDIT of the barrier (4 parallel per-component clusters -> notes/audit_SYNTHESIS_semantic_meaning_barrier_2026-08-06.md + 4 audit_brain_*_2026-08-06.md notes). **AUDIT VERDICT: our similarity MECHANISM (FHRR bundle-cosine) + relational BINDING (role*filler, fMRI-VINDICATED Lalisse-Smolensky 2021) are already FAITHFUL -- NOT the barrier. The barrier is TWO things: (1) FEATURES supplied vs LEARNED+GROUNDED (goal/outcome concepts ground via dopaminergic REWARD-PREDICTION-ERROR, which we OWN in pfc_gate_cfrpe_trained_v2 HARD_PASS); (2) SHAPE = lexical/verb-typing vs the brain's CONSTRUCTION -> VALUATION(OFC/vmPFC content-blind) -> BRIDGING-INFERENCE. The decisive proof: "wanted to help his mother" vs "you are a good boy" share ZERO verb/theme/lexical content -> NO similarity/typing/theme-match can EVER bridge them; only INFERENCE can, and that layer didn't exist.**

**BUILT + VALIDATED THE INFERENTIAL LAYER (the audit's #1 rec):** evaluative-outcome BRIDGING-INFERENCE (detect evaluative construction -> value pos/neg [hand-lexicon SUPPLY] -> bridge to the standing goal in the OWNED GoalOutcomeRegister, strict ADD, no new binding operator). ISOLATION HARD_PASS (commit 17dd3567b: 9/9 zero-lexical-overlap incl "good boy", all over-fire controls clean, scramble collapses). Then FIXED the coref goal-subject confound (GeneralRecencyEntityResolver two-pass->single-pass first-nominal) + WIRED the bridge into production Tier-3 + RE-PROBED: **END-TO-END VALIDATED on real prose (commit d157941c6): mg2 "good boy" bridges to owner=HENRY in production, OUTCOME_NEVER_TYPED 4->3, organ owner-acc 0.50->0.60, NO regression (48/48 + cert 220, Director ran witness). Honest: the coref fix DE-INFLATED the recency baseline 0.70->0.60 (was artifactual) -> the organ now TIES recency 0.60 on a FAIR comparison.** first_mention holds 0.70 -- remaining gap = OTHER construction competencies, NOT verb-OOV.

**✅ AFFECT-STATE BRIDGING LANDED + DIRECTOR-VET'd (2026-08-06, commit 0ff1a6d97, exp_affect_state_bridging_inference_v1, isolation):** the praise-bridge sibling for affect-state outcomes ("Oh, how glad I am!"=positive affect->goal-MET / "ashamed"=negative->UNMET). HARD_PASS, all axes disk-verified by Director across 3 seeds: zero-overlap bridging 1.0 (8/8) vs lexical-only 0.0; pos->MET 1.0 / neg->UNMET 1.0; bystander->no-bridge 1.0 (non-goal-holder's affect never binds protagonist's goal); unchanged->LEXICAL 1.0 (strict ADD, bridge dormant when verb-typing fires); scramble collapses 0.0; CROSS-DETECTOR INTERFERENCE 0/13 + 0/12 with the praise sibling's self_test still passing under the shared GoalOutcomeRegister. Commit CLEAN (4 files, no production hdlab touched, no concurrent-session clobber, no cert gate needed). detect_affect_state_construction deliberately excludes "are/were" so it structurally cannot collide with the praise "you are ADJ" construction. HONEST SCOPE: (1) valuation still a hand-lexicon, NOT reward-PE/appraisal-sim (the grounding follow-up); (2) frank_fishing_glad models mg1_frank's affect phrasing but that item's GOAL is a dialogue-request -> mg1_frank NOT solved end-to-end (needs dialogue-goal competency); (3) production wiring DONE -> see next line.
**✅ AFFECT-STATE BRIDGE WIRED TO PRODUCTION (2026-08-06, commit f99d1024f, Director disk-VET'd: clean 2-file commit, strict-ADD structurally confirmed):** promoted the affect detector + bridge into hdlab/goal_owner_select.py::build_candidate_role_seq as a SECOND Tier-3 attempt (tried only when the evaluative/praise bridge abstains), same over-fire guards (fires only for the goal-holder's OWN affect + has_open_goal), _AFFECT_TRIGGERS_3P={feels,felt,is,was} deliberately NOT are/were (provably disjoint from the evaluative "you are ADJ"). CERT 220/3 GREEN (agent ran; also holds with concurrent verb_lexical_similarity edits present). Strict-ADD BYTE-IDENTICAL proven (0 diffs HEAD-vs-working on eval-13/fair-62/affect-unchanged-2/bystander-2/real-text-10); isolation bank through production path 8/8 + end-to-end 7/8 (5 newly-enabled, ZERO regressions); cross-detector 0/13+0/12+0/62; witness verification/verify_affect_state_bridging_production.py (verify_*.py, not pytest-collected). **HONEST: real-prose owner-acc 0.60->0.60, OUTCOME_NEVER_TYPED 3->3 -- NO McGuffey item moves (mg1_frank "how proud he feels" correctly ABSTAINS: gender-ambiguous "he" [frank+father both male] + its goal is a dialogue-request = separate competency). Mechanism-validated on the hand bank, does NOT yet cash out on real text.** FLAGGED (pre-existing, NOT the bridge, byte-identical HEAD-vs-working): bystander jack/ruth mis-bind via goal-clause outcome-verb over-typing ("fix"->MET, "win"->MET in the goal clause) = a real goal-owner precision follow-up. Registry row proposed (goal_owner_affect_state_tier3_bridge) but NOT committed to canonical store during concurrent session (store-hazard-correct); missing rows for verb_lexical_similarity + lexical_similarity + the evaluative d157941c6 bridge all flagged. FINDING (feeds the acquisition-loop discussion): the production Tier-2 open-vocab outcome classifier types some natural affect-adjacent words (pleased->MET correct, crashed->UNMET correct) AND some questionable ones (agent flagged sorry/sealed) -> a precision-audit-of-Tier-2 follow-up; exactly the over/under-generalization an anti-drift-gated acquisition loop should catch.

**NEXT (the growing library of construction competencies + the deep grounding move):** (a) DIALOGUE-GOAL recognition (granted request / reported-speech desire = a goal; the MOST COMMON real-prose blocker -- mg1_frank "may I go?", mg3_frank "I wish I might", mg1_nero); (b) negation-scope (mg2_harry "could not find"); (c) DEEP/core-barrier: ground the valuation (hand-lexicon -> reward-PE pfc_gate_cfrpe, the audit's grounding gap = the grounded-foundation pivot) + feature-LEARNING (NNSE-on-PPMI to induce features vs hand-supply, audit Anchor-1); (d) wire ToM (islanded HARD_PASS) for illocutionary force (praise vs criticism vs irony). Real-narrative ceiling note: the organ can MATCH positional baselines by handling all construction types; BEATING them needs trap items (rare in real narrative) -- its distinctive value is the adversarial cases.

**KEY DOCS (read after this):** notes/audit_SYNTHESIS_semantic_meaning_barrier_2026-08-06.md (THE audit + the inferential-layer arc, most current), the 4 notes/audit_brain_*_2026-08-06.md, notes/drill_brain_openvocab_verb_class_membership_2026-08-06.md. All work LOCAL, committed through eb2d7bc98 (+ the in-flight agent's pending commit). Standing rules unchanged (brain-test every negative incl SHAPE+POSITION+METRIC + how-brain-does-it + missing-component/LEARNING check; VET positives per-axis + VET-the-VET + run witnesses myself; read-the-code not the label; wire-don't-island; TEST GENERALIZATION not just instruments; AskUserQuestion BANNED; 30-min self-drive cron).

## 🧬 GROUNDED-WORD-ACQUISITION LOOP FORMALIZED + 2 HONEST CORRECTIONS (2026-08-06, commit 4dd84bc0b, notes/drill_online_grounded_word_acquisition_loop_2026-08-06.md + preregs/2026-08-06_grounded_word_acquisition_increment1_v1.md; Director disk-VET'd the load-bearing claims)
USER STRATEGIC STEER: "should we build a system that adds grounded context as we run into new words?" -> YES, it IS audit gap #1 (features supplied-vs-learned-GROUNDED) = the generalization-critical-path. FORMALIZE drill (research/sonnet) delivered a brain->owned-organ map + pre-reg. **HEADLINE: the substrate already OWNS every PRIMITIVE (propose-gate=predictive_coding.threshold_gate; placeholder-mint=self_extension mint_signature; syntactic-bootstrapping pattern=frame_induction.frame_primary_role; propose-but-verify consolidation=self_improving_loop.decide_keep_or_revert + MIN_CONFIRM>=2; affective grounding=grounded_appraisal_sim_earned's REWARD-TRAINED theta) each independently validated. MISSING = only the WIRING that points them at a NEW payload (a word's own feature/valence entry) + a persistent WRITE-BACK tier + ONE thin new glue (goal-attainment-clause -> appraisal-type domain adapter). REUSE-heavy, ~1 new component.** Two-channel design kept explicit: Channel A structural/syntactic (weak by design), Channel B affective/reward-grounded (expected decisive, NO text co-occurrence of target word); anti-drift gate = both channels agree at >=2 occurrences before write-back. Pre-reg increment: genuinely-OOV held-out (catch/obtain/gain/earn POS + desert/waste/fade NEG, real cited sentences), fall-through baseline 0/7; HARD-PASS >=5/7 both classes + zero noise-mint consolidation + scramble collapse; ablation predicts Channel B dominates (a FALSIFIABLE test of THIS PROJECT'S OWN non-literature hypothesis that reward-PE grounds lexical-valence acquisition). P(HARD-PASS) deflated to ~0.40-0.45 (honest; a clean negative is informative falsification, not just an impl miss). **CORRECTION 1 (honesty, deflates a prior claim): the open-vocab verb-typing "generalization" win (0.30->0.50 real-prose, commit d52aa7669) on the HEADLINE blockers praise/accept/invite was substantially SUPPLY -- Director disk-confirmed those 3 have HAND-TAGGED feature entries in OUTCOME_VERB_FEATURES (a human did the fast-mapping). The similarity MECHANISM is validated on held-out words, but the celebrated real-prose movement came partly from hand-adding the blocker words. This is audit gap #1 in the flesh + exactly what the acquisition loop automates. TODO: reconcile the d52aa7669 "held-out includes praise/accept NOT seeds" claim vs the OUTCOME_HELDOUT_POS membership.** **CORRECTION 2 (precision): the audit + earlier docs cite pfc_gate_cfrpe as "the reward-PE organ we own" -- IMPRECISE. pfc_gate_cfrpe solves operator-sequence retrieval (SR/TD planning), has NO hdlab module/registry row. The organ that actually grounds per-concept VALENCE (reward-earned theta, no text, wired via context_grounded_valence) is grounded_appraisal_sim_earned -- that is Channel B's real grounding organ.** HOUSEKEEPING (disk-confirmed): hdlab/lexical_similarity.py + hdlab/verb_lexical_similarity.py have NO registry rows despite being live wired deps of goal_typing.py -> registry-completeness follow-up. DECISION PENDING (promised USER review before BUILD): greenlight the increment build (Director recommends YES) vs wire-the-affect-bridge / next-competency first.

## 🧪 ACQUISITION INCREMENT 1 = HARD_FAIL (informative, NOT a ceiling) + BRAIN-FOUNDATIONAL DRILL + RE-SPEC (2026-08-06, commit 7c314c840, Director disk-VET'd incl cert 220/3 ran myself + Channel-B earned-theta confirmed)
USER greenlit "build it, no corners cut, brain-foundational." Built per pre-reg (both channels, reused organs verbatim, Channel B valence from FROZEN reward-earned theta -- confirmed: RECIPROCITY -0.9951 vs BLOCK_HIGH +0.6119, random-theta ~0; NO hand-assigned polarity; 3 corner-temptations resisted+reported). **VERDICT HARD_FAIL (measured, honest): heldout 2/7 (POS 2/4=earn,gain; NEG 0/3), sole HARD_FAIL cause anti-drift leak noise=2/8; scramble collapses 0.143; ablation A_only=4/7 (chance/majority-class artifact -- Channel A carries ZERO valence signal, 129POS/120NEG across 249 episodes) vs B_only=2/7 vs combined=2/7.** Cert 220/3 (Director ran on clean tree, both edits present). Commit clean (6 files, no clobber). **BRAIN-FOUNDATIONAL DRILL (SHAPE+POSITION+METRIC -- this is the payoff, NOT a real falsification of reward-grounding): (1) SHAPE wrong -- we tried to acquire a verb's POLARITY as a LEXICAL property, but for a reward-grounded system (and the brain) outcome-valence is RELATIONAL (value of outcome vs GOAL): "earned a prize"(POS) vs "earned a scolding"(NEG) -- polarity is NOT in "earn". (2) POSITION wrong -- we tested Channel B (a goal-CONGRUENCE mechanism) on BARE GOAL-LESS clauses (goal_sentences empty) -> it was denied its required input -> degenerated to conflating transitivity/agonist-realization with goal-completion (leaks POS onto neutral transitives answer/carry, misses intransitive NEG desert/waste/fade). (3) METRIC wrong -- scored context-free local verb polarity vs a rubric, ill-defined for goal-relative verbs. So the 'falsification' is correctly SCOPED to 'bare corpus clauses' -- reward-PE-grounds-GOAL-RELATIVE-outcome-valence was NEVER fairly tested (no goal context). BRAIN EXISTENCE-PROOF HOLDS: not a ceiling, a mis-specified/unfair test on thin dated data.** USER STEER (2026-08-06, integrated): make it FAIR + McGuffey is dated + do we have enough data + the brain can do it so we can. **DATA INVENTORY (measured on disk): raw text ABUNDANT + far better/more-MODERN corpora already present than McGuffey (onestop=modern graded, simplewiki, race=modern comprehension, litbank_coref_conll=modern annotated narrative, + goal-DENSE novels anne_of_green_gables/little_women/tom_sawyer/alice/wizard_of_oz). BINDING CONSTRAINT is NOT raw text -- it's GOAL-STRUCTURED ANNOTATED EVAL passages: we have only 10 real (real_text_goal_owner_diagnostic_v1) + 62 fair-instrument. So NOT data-starved on exposure; UNDER-resourced on a goal-bearing EVAL set.** **RE-SPEC (increment 1b, brain-foundational + fair + modern): (a) acquire the verb's TELICITY/RESULT-STATE features (LEXICAL/structural -- what syntactic-bootstrapping ACTUALLY predicts, Channel A redirected from valence->telicity), NOT polarity; (b) compute goal-relative MET/UNMET via the ALREADY-WIRED congruence_outcome_valence organ IN a goal context; (c) test in GOAL-BEARING PASSAGES from a MODERN goal-dense corpus (drop McGuffey), built into a larger FAIR eval. Reuses even more owned machinery; fixes SHAPE+POSITION+METRIC at once.** Registry row grounded_word_acquisition_loop_increment1 = gate SHELVE (revival = telicity discriminator + goal-passage structure), gitignored/local.
**✅ FAIR MODERN GOAL-BEARING EVAL BUILT + Director-VET'd (2026-08-06, commit 0a6fa4fb2, experiments/data/goal_bearing_modern_eval_v1.jsonl + notes/research_goal_bearing_modern_eval_2026-08-06.md):** 44 items from 7 MODERN corpora (little_women/anne/tom_sawyer/wizard_of_oz goal-dense novels + RACE 2017 + OneStop 2012-15; alice confirmed too episodic, McGuffey DROPPED). Disk-VET'd: 82% OOV outcome-verbs (36/44, the acquisition target), polarity 27met/17unmet (majority-class=0.61), 24 trap (14 distractor_between + 10 recency_trap), 3 real owner-redirects, gender-tagged roster, per-item citation + outcome_in_lexicon flag. **DATA SUFFICIENCY = YES (answered w/ evidence, per-corpus density survey).** **KEY FAIRNESS FINDING (honest, VET-confirmed, load-bearing): real narrative is NOT fully de-biasable like the synthetic fair_v1 -- recency 0.57/0.50 + nearest_subject 0.30/0.25 are DEFEATED, but first_mention 0.80/0.75 + majority 0.73/0.67 stay HIGH because THE GOAL-OWNER IS USUALLY THE PROTAGONIST (first-introduced + most-mentioned) -- a STRUCTURAL property of narrative, NOT a bug (only 5/44 defeat all 4 baselines).** REFRAME OF "FAIR"+"SUCCESS" (Director): the eval scores TWO axes -- (1) POLARITY-TYPING MET/UNMET on the 36 OOV items = where 1b's telicity-acquisition has NON-POSITIONAL value (positional owner-baselines are IRRELEVANT to polarity; fair baseline = majority-class 0.61 + lexicon-abstain; WELL-POWERED); (2) OWNER-selection on the thin 5-item adversarial slice + keep synthetic fair_v1 (62 items, all baselines floor) for owner-DISCRIMINATION power. Brain-consistent: humans ALSO default goal-owner=protagonist absent a cue, so high first_mention is a real regularity not cheating; our mechanism's distinctive value = TYPING outcomes (baselines can't) + adversarial owners. NEXT: FORMALIZE increment 1b (dispatched) = acquire OOV verb's RESULT-VERB-CLASS/telicity via construction cues (Channel A redirected -- arg-structure DOES predict telicity, unlike valence) -> feed the ALREADY-WIRED congruence_outcome_valence (goal desired-state vs outcome result-class) -> score POLARITY on the 36 OOV-in-goal-context items vs majority-class/lexicon baseline. Also surfaced: some GOAL verbs OOV too (persuade/apologize/avoid/like/love/make).

## 🧱 ACQUISITION INCREMENT 1B = HARD_FAIL #2 -> CONVERGENT DRILL: WE'VE HIT THE GROUNDING WALL (2026-08-06, commit a36ee7c35, Director disk-VET'd)
Built the SHAPE-corrected 1b (single-channel structural: acquire OOV verb's result-class via clause read -> existing congruence organ -> MET/UNMET; pole-sentinel fix; no reward-theta). **HARD_FAIL on 3 axes (measured, honest, no corners -- 3 temptations resisted+reported): primary 16/36=0.444 <= majority 0.639 (unmet_recall COLLAPSED 2/13); noise leak 2/8 (answered/carried->POS); GATE 5 eligible 7/18=0.389 <= fallback 9/18=0.500 -> the SHAPE-fix's central claim FALSIFIED (goal-context path does WORSE than flat fallback); scramble collapses 0.444->0.367 (clean); enrichment delta -0.028 (not load-bearing, predicted); insufficient_corpus 0/36 (well-mined, NOT underpowered).** Cert 220/3 before+after (strict-ADD empty overlay). **ROOT CAUSE (Director disk-CONFIRMED via the acquired-verb polarity map): the verb-lemma-blind clause-structural read measures TRANSITIVITY/AGENCY, not RESULT-POLARITY -- almost every verb consolidates POS (refuse->POS, spoil->POS, flee->POS, croak->POS; only curve/jell/rap/ruin got NEG); any transitive-animate frame fires agonist_realized. MIN_CONFIRM=2 can't gate a consistently-transitive verb (increment 1's dropped AND-gate had MASKED this).** 
**🔑 CONVERGENT DRILL ACROSS BOTH HARD_FAILS (the load-bearing finding): outcome RESULT-VALENCE is a GROUNDED LEXICAL-SEMANTIC property that CANNOT be shortcut -- not from surface structure (1b: transitivity != result-polarity), not from a reward-constant on goal-less input (1). Both failures = the audit's gap #1 "abstract-symbolic -> GROUNDED" LOAD-BEARING WALL, now EMPIRICALLY CONFIRMED TWICE with sharp diagnostics. You cannot learn "waste/spoil/refuse = bad outcome" from the SYNTAX of a sentence using the verb -- it is grounded WORLD-KNOWLEDGE of what the verb DOES. This is "you can't learn revenge from a book" AT THE VERB-SEMANTICS LEVEL.**
**BRAIN'S WAY (the how): a 6yo BRINGS grounded result-verb knowledge TO reading; they do NOT acquire it from the passage. Reading ASSUMES a grounded foundation of common-verb result-semantics.** MISSING COMPONENT (route: missing-LEARNING/missing-grounded-FACT): a grounded result-state feature per verb -- which must be GROUNDED (experience/simulation) or SUPPLIED (a grounded lexicon = the "6yo knows these" early-acquired seed, invariant-OK per supply-DATA-not-mechanism + the authorized 6yo-grounded-foundation pivot).
**STRATEGIC FORK (surfaced to USER, needs their call -- NOT auto-dispatched): (A) REFRAME as FOUNDATION-SUPPLY -- supply grounded result-semantics for the ~few-hundred common verbs a 6yo knows (DATA, invariant-OK, = the authorized grounded-foundation seed, NOT the hand-tagging treadmill nor the reading MECHANISM); unblocks real-prose NOW; acquisition-loop then scoped to genuinely-RARE novel verbs only. (B) pursue GROUNDING result-valence via experiential simulation (audit's deep wall, hard, arbitrary-verb grounding unsolved). (C) accept OOV result-valence out of reach, focus on covered verbs + working mechanisms. DIRECTOR LEAN: (A) -- it IS the brain's way (6yo HAS this knowledge, supplied by development; we supply as data since we can't give embodied years), invariant-consistent, unblocks progress, correctly scopes the loop.** Registry grounded_word_acquisition_loop_increment1 stays SHELVE (sharper falsification banked). The 2 HARD_FAILs were NOT wasted: they diagnosed the real answer (grounding-foundational, not learnable-from-reading) -- the flat-learning-result discipline applied correctly.

## 🎯 DEFINITIVE GROUNDING-WALL DRILL -> REFRAMED DIRECTION (2026-08-06, USER-requested; notes/SYNTHESIS_grounding_wall_definitive_2026-08-06.md + 2 adversarial drills fd9779c84 brain + ccf2de73b ours; Director-VET'd the dissociation claim on disk)
Two fine-toothed adversarial drills (brain machinery + our code) traced "Tom worked all summer to buy a bicycle, but he wasted his savings" through every component. **DEFINITIVE FINDINGS: (1) TWO WALLS ARE SEPARATE + we already cleared one -- FOLLOWING-THE-PLOT (goal-recog/coref/binding/bridging) HARD_PASSed on REAL prose with an UNGROUNDED hand-lexicon (17dd3567b/d157941c6); only KNOWING-WHAT-WORDS-MEAN (grounded lexical valence) is stuck. (2) The direction of "bad" is NOT in the grammar (syntax's "but" gives only "a reversal is coming," never which way) NOR in text-distribution (distributional paradox: antonyms near-identical; distributional valence is PARASITIC on a small hand-seed). It comes from a PRE-LINGUISTIC AFFECTIVE CORE the brain grounds by FEELING (goal-blockage anger endocrine-confirmed by 4-7 MONTHS), onto which the WORD gets mapped years later. (3) THE DECISIVE REFINEMENT: the brain grounds a SMALL primitive anchor + PROPAGATES outward via similarity/opposition (= sentiment-lexicon-from-seed, SentProp/Hamilton) -- it does NOT feel every word.** **SO THE WALL PRECISELY = we OWN the mechanisms (proven) + the propagation machinery (lexical_similarity + OPPOSED_PAIRS) + a SMALL genuinely-earned affective anchor (pfc_gate_cfrpe/appraisal theta), but LACK exactly 2 wires: word->anchor connection + anchor->new-word propagation. Increments 1+1b HARD_FAILed for the RIGHT reason (tried to squeeze good/bad from grammar/reward-constant; it's not there).** **REVISED FORK (supersedes A/B/C): NOT supply-hundreds (treadmill) NOR ground-every-verb (impossible wall) -- but ANCHOR+PROPAGATE: seed a small grounded valence anchor (mostly earned) + wire words to it + propagate to new words via owned similarity/opposition; congruence+bridging (proven) read it in goal context. Brain-faithful, narrow, reuses owned organs. HONEST: a DIRECTION to build+test, not solved (anchor currently grounds situation-types not words; connect+propagate is the unproven build). Awaiting USER call on this reframed direction.**

## ✅ GOAL-RECOGNITION COVERAGE EXPANSION LANDED (2026-08-06, commit 051f6d0ef, HARD-PASS, Director ran the witness myself) -- fork-independent
try/decide/determine were MISCLASSIFIED (try in ASPECTUAL_STOP, decide/determine in OTHER_STOP; disk-confirmed) -> their goals never fired. FIX: new CONATIVE_PASS(try) + INTENTION_PASS(decide/determine) + DESIDERATIVE_PASS extended (like/love + gerund forms) + GOAL_GOVERNING_PASS union wired into both call sites, atomic (disjointness invariant holds). MEASURED (Director ran verify_goal_recognition_coverage_expansion.py): coverage 19/44 -> 33/44 (floor 30), try-recall 7/8, decide-recall 4/5, precision 0/11 false-fires on BOTH find_desired_state + purpose paths, cert 220/3, import OK. Clean commit (3 files). **HONEST CAVEAT (Director-VET'd, elevates a queued item): this edit WIDENED the PRE-EXISTING negation-scope gap -- negated goals now false-fire 4/4 ("did not try to escape" mis-reads as a goal), up from 1/4 baseline (cert has no negation test). Net still strongly positive (14 goals gained) but NEGATION-SCOPE GUARD is now the clear NEAR-TERM fork-independent fix (bounded: skip goal-recognition when the goal-verb is negated).** The 18/36 goal-reachability ceiling that capped increment 1b is substantially lifted for future work.
**✅ NEGATION-SCOPE GUARD LANDED (2026-08-06, commit c2f88ea91, HARD-PASS, Director ran witness): _verb_negated_before helper at 3 sites (find_desired_state + action_frame_feats + c3_has_desire) -> negation_false_fires 4/4->0/4 (both paths), complement-negation "tried NOT to cry" STILL fires 3/3 (avoidance goal, precision guard works), litotes not over-suppressed, precision 0/11 held, cert 220/3. HONEST: coverage 33->32 (the 1 drop = "did not mean to intoxicate Diana" IS a negated goal -> correctly removed, not a regression; agent took the honest number). Known edges left conservative (lexical negative-implicatives, fronted negation). Cleaned up the gap the coverage edit widened.

## 🧩 ANCHOR+PROPAGATE FORMALIZE -> DEFINITIVE DIRECTION VERDICT + EARN-vs-SUPPLY FORK (2026-08-06, commit 3314879f5, notes/research_anchor_propagate_oov_outcome_verb_valence + prereg; Director VET'd the crux on disk)
The USER-greenlit "do it right" formalize confronted the make-or-break DIRECTION question and gave a definitive, disk-verified verdict. **VERDICT (Director-confirmed: ruin/spoil/whitewash -> in_lexicon=False, vec=None): our 4 named organs (lexical_similarity/verb_lexical_similarity/CLASS_REGISTRY/OPPOSED_PAIRS) CANNOT propagate valence to a genuinely untagged word AT ALL -- they are a COMPARATOR between two already-tagged words, with NO encoder from surface-form->vector, and direction is hand-authored INTO the tags. Not merely circular -- a hard blocker. So "spread over our own similarity" is a non-starter for novel verbs.** **A NON-CIRCULAR buildable design DOES exist, needing ONE new piece: WordNet as the valence-BLIND neighborhood channel (already a promoted hdlab dep via animacy_lexicon, invariant-OK DATA; separates synonym from antonym as different relations -> resolves the ruin/fix conflation that killed distributional 1b) + the SHELVED PolarityLexicon._antonyms detector (lift, wire-don't-island) for opposition + the 52-word hand-seed (OUTCOME_SEED_POS/NEG, confirmed 26+26) for DIRECTION. Propagation = kNN-vote over WordNet from the seed, opposition-first. P=0.42.** HARD non-circularity gates pre-reg'd (scramble-seed->collapse, random-graph->collapse, direction-removed->fail). **HONEST CEILING (VET-noted): ~half the 36 eval OOV verbs are LIGHT/support verbs (be/have/go/give/take/make/carry/buy/... 17/33) whose valence is COMPOSITIONAL not verb-level -> no verb-level mechanism types them; abstain. Full-36 ceiling well below 100%.** **THE EARN-vs-SUPPLY FORK (USER's call): this buildable increment PROVES the propagation ARCHITECTURE + gives real open-vocab valence (types never-tagged verbs by SPREADING, not per-word tagging) = consistent with "supply a SMALL seed, EARN the spread" -- BUT its ingredients are SUPPLIED (WordNet neighborhood + 52 hand-labels); it is NOT earned/felt grounding. The genuinely-EARNED version (learn a verb's valence from the situation-model's OWN downstream MET/UNMET consequence via RL -- "feel it") is the DEEPER, separate increment, named + out of scope here. DIRECTOR LEAN: build the WordNet-propagate version FIRST (proves architecture cheaply + real capability + hard non-circularity gates + reuses/ revives owned organs; if it fails even with a supplied seed the RL version won't work either), THEN earn the anchor via RL (swap supplied seed for RL-earned, reuse the propagation machinery). Surfaced to USER -- awaiting call on supply-tolerance (WordNet-propagate now vs hold for purely-earned RL).**

## 🔄 CONSEQUENCE-LEARNING LOOP FORMALIZE -> READY TO BUILD + HONEST CORRECTION (2026-08-06, commit a5f16a5ca, notes/research_consequence_learning_loop_oov_outcome_verb_valence + prereg; Director VET'd)
USER chose the EARNED CONTINUOUS learning system ("build it as we go along") over static WordNet-supply. Formalize delivered, Director-VET'd. **DESIGN: read goal-bearing stories -> when an OOV outcome verb appears, LEARN its result-valence from the episode's computed MET/UNMET CONSEQUENCE, cross-situationally accumulated with anti-drift; light-verbs (be/go/make) correctly converge to GROUNDED_NEUTRAL (the payoff static approaches can't do). Credit-assignment = the outcome-clause verb whose referent LINKS (existing _referent_links) to the goal's referent (excludes bystanders STRUCTURALLY). Reuses find_desired_state/congruence_decision/lexicon_predict/register_acquired_outcome + self-extension loop's multi-pass read->mint->re-read control-flow + decide_keep_or_revert abstain-band (new 3-way POS/NEG/NEUTRAL wrapper).** **🔧 HONEST CORRECTION (Director VET-confirmed on disk, corrects a claim I propagated all session): the RL-earned appraisal family is the WRONG mechanism for word-valence -- pfc_gate_cfrpe is a Go/NoGo PLANNING/SR gate (reach_value/successor, confirmed), context_grounded_valence is GOVERNOR-verb-lexicon-gated (imports exp_bridge1_governor_grounding, not word-identity-independent). So "we own a reward-earned grounding usable here" was OVERSTATED. The RIGHT teacher = congruence_decision's OWN structural MET/UNMET verdict (the story's computed goal-outcome), bootstrapped by the ~90-word seed. This is CLEANER + more honest (learns from the actual story outcome, not a toy sim) -- the toy-world theta grounds a wordless world + can't wire to real-text word learning.** Still genuinely EARNED (OOV verbs learn from story outcomes cross-situationally); the small seed stands in for the felt affective core we can't give it (= "supply small innate seed, EARN the rest"). **WordNet NOT needed anywhere (the earned loop stands alone).** DATA (live scan, 4 novels): 1641/24831 (6.6%) goal-clear sentences, 848 (51.7%) with a computable teacher from the ~90-word seed -> modest but real signal; coverage limited to verbs with >=N exposures (honest ceiling). NON-CIRCULARITY LOAD-BEARING: 34/44 eval items are from these same 4 training novels -> line_citation-span exclusion MANDATORY. Pre-reg HARD-PASS: learnable-subset acc >=0.75 (N>=6) + full-36 >=0.60 + light-verb-neutral >=0.70 + scramble/random-credit/single-signal gates. **P_deflated=0.30 (honest: novel cross-component synthesis, no settled paradigm; harder than static). READY TO BUILD -- Director recommends build; awaiting USER go given it's the biggest/most-novel build of the arc + rests on the above correction.**

## 🔬 CONSEQUENCE-LEARNING LOOP BUILT -> HARD_FAIL (INSUFFICIENT_YIELD) + DRILL: THE MECHANISM IS VALIDATED BUT STARVED (2026-08-06, commit a892153ea, Director disk-VET'd incl teacher-not-theta + the grep-count discrepancy resolved)
USER emphatic-go build landed. **HARD_FAIL — INSUFFICIENT_YIELD (the pre-registered biggest risk, P=0.30, measured no-shortcut).** Cert 220/3 (new module, production hdlab ZERO-DIFF, concurrent-safe -- never touched find_desired_state). Director-VET'd: primary 0.1667 = empty-overlay baseline (== increment-1b fallthrough 6/36 exactly); n_registered=0; exclusion_integrity_violations=0 (non-circularity CLEAN); teacher=congruence's MET/UNMET NOT theta (the 1 grep hit is a DOCSTRING; actual imports = goal_typing/verb_lexical/self_improving; witness W4 asserts no reward-family import). **ROOT CAUSE (disk-diagnosed, NOT the learning loop): across the whole 4-novel corpus (1,431 windows) the teacher signal (congruence MET/UNMET) fired on only ~15 windows, dual-signal AND-gate co-fired on only 3 -> 3 teacher windows / 7 credit exposures -> below MIN_CONFIRM -> NOTHING grounds. = congruence_decision's low real-prose recall = the SAME OUTCOME_NEVER_TYPED barrier the audit named.** The learning MECHANISM is VALIDATED at mechanism-level (referent-linked 'tinker' credited, bystanders 'dwindle/savings' excluded structurally, noise_consol=0 = no bystander leakage, anti-drift held) -- it is STARVED, not broken. **CHICKEN-AND-EGG made concrete: to learn OOV valence from consequences you must compute consequences, which needs outcomes typed, which needs known verbs -- the seed's outcome verbs fire on only ~15 windows so the bootstrap can't get going (bootstrap curve FLAT, pass-2 adds 0).** **THE ABLATIONS = the fix direction: Signal-A-only (congruence alone) grounds 11 words at 0 noise (light-verb-neutral 0.53); the AND-gate with goal-blind Signal-B collapses it to 3 (A & B rarely co-fire on real prose). CHEAP FIX (own pre-reg): Signal-A-PRIMARY teacher (drop the over-strict AND-gate) -> 3->11. DEEPER BRAIN-FOUNDATIONAL FIX: the teacher is too NARROW -- the brain gets a consequence-signal from EVERY resolved goal (dense); ours uses ONE detector. DENSIFY the teacher by COMBINING the multiple goal-resolution organs WE ALREADY OWN (congruence + evaluative/praise bridge d157941c6 + affect-state bridge f99d1024f + negation-scope c2f88ea91) = a WIRE-DON'T-ISLAND move that feeds the validated loop far more teacher episodes.** Corners refused (3, documented): didn't drop to goal-blind Signal-B-only (would lift primary but = circular 1b failure + 3 noise), didn't lower MIN_CONFIRM, didn't credit non-referent-linked windows. Registry: SHELVE consequence_learning_loop (revival = raise teacher-signal density via Signal-A-primary + multi-organ goal-resolution teacher so >=6 eval-OOV lemmas reach MIN_CONFIRM) -- NOT appended to shared registry during concurrent session. **HONEST STRATEGIC READ: the earned learning loop is real + validated; the binding constraint is TEACHER-SIGNAL DENSITY on real prose, partially fixable by combining owned organs, but real narrative has genuinely sparse explicit goal-resolution -> coverage grows slowly (the recurring real-prose-is-hard truth).**

## 🔑 USER DIRECTION (2026-08-06): THE LEARNING TOOL = DICTIONARY-LOOKUP + CONSEQUENCE-LEARNING (resolves earn-vs-supply)
USER: "the learning tool can't figure out what the word means from the story on its own -- you need to build that up. the learning tool should be able to LOOK UP THE WORD IN THE DICTIONARY to get a sense as part of that process." **THIS UNIFIES the two threads I wrongly framed as a FORK: the DICTIONARY LOOKUP = the anchor+propagate WordNet design (notes/research_anchor_propagate_..., 3314879f5) -- DENSE, gives an initial sense/valence for EVERY OOV verb (waste -> WordNet near-syn squander/misuse, antonym save/conserve, seed save=POS -> waste~NEG). The CONSEQUENCE-LEARNING loop (a892153ea, validated-but-starved, teacher fires ~15/1431) = the EARNED refinement, sparse but grounded. THE LEARNING TOOL USES BOTH: dictionary as the dense PRIOR, consequence as the earned CONFIRM/CORRECT.** FIXES the INSUFFICIENT_YIELD directly (dictionary covers every word -> loop never starved; consequence confirms/refines where the story gives signal). EARN-vs-SUPPLY RECONCILED (honest): dictionary = SUPPLIED data (WordNet, already a promoted hdlab dep via animacy_lexicon, invariant-OK lexicon-not-mechanism); consequence = EARNED. Combined = definitional prior + experiential grounding = exactly how a human learns a word (look it up AND see how it plays out; when experience contradicts the definition, experience wins). BRAIN-FAITHFUL (multiple word-learning routes: ostension/definition + syntactic bootstrapping + cross-situational/consequence). **NEXT: build the COMBINED learning tool -- WordNet dictionary-lookup (dense prior, the anchor+propagate mechanism) + consequence-learning (earned refinement), combined (agree->confident; conflict->experience refines; no-signal->dictionary prior). Formalize the combination weighting once the in-flight Signal-A-primary test lands (measures the earned component's yield), then build. The anchor+propagate WordNet formalize is NOT superseded -- it is the dictionary-lookup HALF of this tool.**

## ✅ DECISIVE: SIGNAL-A-PRIMARY REFUTED consequence-ONLY + PROVES the USER's dictionary direction (2026-08-06, commit 093ddc1aa, Director disk-VET'd)
Signal-A-primary re-score (drop the over-strict AND-gate, congruence as sole teacher, SCORE grounded words on eval). **HARD_FAIL (grounds-WRONG), decisive: primary 0.1667->0.1944 (noise), grounded_verb_polarity_match_rate 0.333 (BELOW chance), learnable_subset 0/3. n_grounded=11 (all genuinely OOV, non-circ CLEAN: seed_leaks=0, noise_consol=0) but only 3 overlap the eval + ALL 3 are LIGHT verbs (be/make/take; make+take grounded UNMET but gold MET = WRONG).** **THE DEEPER, DECISIVE DIAGNOSIS (Director VET'd): teacher is NOT sparse in aggregate -- Signal-A fires on 112/1431 windows (bootstrap grew 12->85->112). The failure is DISTRIBUTIONAL: density concentrates on HIGH-FREQ LIGHT verbs; the ones barely reaching MIN_CONFIRM=3 get POLARITY-LOCKED by small-sample skew (be/make 0/3), while ZERO genuine CONTENT outcome verbs (hoist/glower/squander/bedew...) EVER reach MIN_CONFIRM -- each recurs only 1-2x across 4 novels. So consequence-alone grounds the WRONG (frequent-function) words WRONG + can't reach the RIGHT (rare-content) words at all. Loop-internal knobs can't win (raise MIN_CONFIRM -> stops false-lock but further starves content verbs).** **THIS IS HARD EVIDENCE FOR THE USER'S DICTIONARY+CONSEQUENCE DIRECTION: content verbs are too RARE in any corpus to ground from repetition -- a human LOOKS THEM UP the first time. The dictionary (WordNet) gives DENSE coverage of exactly the rare content words consequence CANNOT reach; consequence then confirms/refines. Multi-organ densification alone would NOT fix this (doesn't raise per-content-lemma exposure).** Cert 220/3 (engine byte-identical). NEXT: formalize+build the COMBINED tool (dictionary prior + consequence refinement) -- now evidence-backed.

## 🧭 COMBINED DICTIONARY+CONSEQUENCE TOOL -> HARD_FAIL + THE DEEP REFRAME: MET/UNMET IS NOT VERB VALENCE (2026-08-06, commit 329f01733, Director disk-VET'd incl a correction of my OWN VET)
Built the USER-directed combined tool (WordNet dictionary pseudo-count prior + consequence refinement, Bayesian inject-once). **HARD_FAIL, decisive, no-corners. 3-WAY ABLATION (Director VET'd on disk): dictionary-only 0.2222 / consequence-only 0.1944 / COMBINED 0.1944 (majority floor 0.6389, empty floor 0.1667). Combined is WORSE than dict-alone (-0.028) + identical to consequence-alone; combined-vs-consequence-scrambled = NEGATIVE marginal -> the assembly is effectively dict-noise.** Content-16: only 3/16 got a dictionary hit (encore/jell correct [jell by WSD-luck], improve WRONG); 13/16 ABSTAIN (sparse WordNet antonyms + sub-margin neighbors). **🔑 THE DEEP FINDING (reframes the whole recent arc): LEXICAL-VALENCE != GOAL-CONGRUENCE. Where the dictionary DID fire correctly (improve->POS lexically), it is the WRONG answer because the eval scores GOAL-congruence ('was the DESIRED improve ACHIEVED?') and that item is UNMET. Even a PERFECT dictionary of verb valence cannot solve MET/UNMET, because MET/UNMET is a GOAL-RELATIONAL composite: (a) the verb's result-pole + (b) whether the outcome ACTUALLY HAPPENED (negation/failure/attempt-vs-achievement) + (c) the goal-relation. The entire recent 'ground the verb's valence' program (structural-read / reward-constant / consequence-learning / dictionary / combined = 5 decisive HARD_FAILs) attacks only (a).** **🔧 MY VET CORRECTED (own it): I told USER 'light-verbs naturally won't false-lock' from a SHALLOW [:4]-synset check showing make antonyms=[]. WRONG -- full WordNet has make<->break, find<->lose, take<->refuse; break/lose are NEG anchors -> Stage-A opposition@conf1.0 -> find self-locks POS with ZERO real exposures (gate-6 violation), make polar-locks POS. The dict priors DEGRADED light-verb safety (6 polar-locked). VET-the-VET vindicated.** Cert 220/3 (inject default-None additive). No corners (inject-once verified, thresholds not tuned, make/find self-lock reported not suppressed). **STRATEGIC JUNCTURE (recommend the step-back USER offered): the evidence now DEFINITIVELY shows typing OOV-outcome MET/UNMET by grounding the VERB'S valence hits a fundamental decomposition wall -- MET/UNMET isn't verb valence. REFRAME HYPOTHESIS: the '(b) did-it-happen' component (negation/failure/prevented/achieved, often EXPLICIT in text -> structurally detectable, e.g. improve-UNMET = the improve didn't happen) may be a HIGHER-yield + more tractable lever than verb-valence-grounding; we just built negation-scope for GOALS + could extend to OUTCOMES. NEXT: analyze the eval's MET/UNMET composition (how many items are driven by verb-pole vs did-it-happen vs goal-relation) to ground the reframe, THEN decide direction with USER. NOT auto-dispatched -- this is a talk-strategy moment.**

## 📊 EVAL DRIVER-DECOMPOSITION -> EVIDENCE-GROUNDED DIRECTION (2026-08-06, notes/research_goal_bearing_eval_driver_decomposition_2026-08-06.md, Director-VET'd per-item table + spot-checks + counts)
Per-item read of what DRIVES each gold MET/UNMET on the 36 OOV items. **DISTRIBUTION (Director-confirmed by reading the table + counting): DID-IT-HAPPEN (B) 15/36 (42%) = largest primary; GOAL-RELATION (C) 13/36 (36%); OTHER/composite (D) 6/36 (17%); VERB-POLE (A) 2/36 (6%).** **DEFINITIVE: the verb-valence-grounding line we spent 5 HARD_FAILs on has a HARD 6% CEILING -- and is actively WRONG on numeric-threshold items (race_chen_situps 'improve'->reads POS but 30<35 goal-bar=UNMET; onestop_carle 'be'->96.9% but goal wanted 100%). Independent confirmation: 4/8 IN-LEXICON controls are ALSO dict-wrong (negation+owner), so verb-valence caps well under 100% even on the EASY subset.** **REFRAME HOLDS + SHARPENED: did-it-happen is ~8x verb-pole's ceiling (47% generous / ~22% standalone-clean) BUT needs goal-relation/owner-attribution as a REQUIRED COMPANION on 7/15 of its own items -> DO NOT build a standalone did-it-happen scanner; WIRE DID-IT-HAPPEN AS A SIGNAL INTO THE EXISTING GOAL-CONGRUENCE/OWNER-BINDING ORGAN (congruence_decision). Goal-relation is the biggest TOTAL footprint (primary-or-companion on 25/36=69%).** IMPORTANT SUBTLETIES the analysis surfaced (did-it-happen is NOT naive negation-scanning): exception-scope ('all failed EXCEPT the one that curved'), false-negative-OVERRIDE ('so she had failed'... later 'winner of the Avery'), multi-attempt persistence ('tried... but slow'... 'made 400,000'), final-state reading ('German was back'xN... 'never came back'). BRAIN-FOUNDATIONAL fit: this moves from grading WORDS to reading the SITUATION-STATE relative to the goal (what the brain does); did-it-happen (occurrence of the wanted thing) is the missing layer the brain always tracks + is often EXPLICIT in text (the tractable slice). NEXT: build did-it-happen (occurrence/negation/failure/achievement, with the 4 subtleties) as an INPUT to congruence_decision; reuse coref for the owner-attribution companion. Verb-valence grounding SHELVED (6% ceiling, evidence-closed). (Stray legacy-convention handoff file exp_dev_handoff_*eval_driver* removed -- content is in the research note; ferry mechanism is dead per CLAUDE.md.)

## 🛠️ DID-IT-HAPPEN FORMALIZE -> 2-PART BUILD + HONEST SCOPE (2026-08-06, commit 92de2fa52, Director VET'd the sents[-1] finding on disk)
Occurrence-gate design (strict-ADD into goal_typing.py): (1) reuse _verb_negated_before (built today) on the OUTCOME side (find_actual_state_candidates has ZERO negation-awareness today -> a negated candidate flips _class_relation via XOR); (2) new RECURRENCE_MATCH sentinel lets registry-OOV verbs become candidates when lemma-identical to the goal's desired verb. **HARDER-THAN-IT-LOOKS FINDING (Director disk-CONFIRMED, load-bearing): congruence HARDCODES outcome=sents[-1] (lines 942/973/1330) -> when the resolving clause is followed by a trailing dialogue/reaction sentence the gate NEVER sees it -> blocks 5-6/15 items + 2 of 4 subtlety cases. WINDOW-WIDENING is a REQUIRED CO-BUILD, not polish. Realistic reach from the core gate alone = 2-4/15 (NOT 8/36); +window-widening reaches more.** P=0.35 core / 0.25 window-companion. 2 of the 4 subtleties are non-issues (existing split isolates them); the other 2 double-blocked by the sents[-1] gap. **BUILD SEQUENCING (correct, not sign-off-pause): the did-it-happen BUILD edits goal_typing.py = SAME FILE the precision-quirk fix (aea59df0) is editing -> HOLD the build until the quirk fix commits (clean tree) AND the generalization probe (a18d7885) confirms the pivot; fire the build the moment BOTH land. Build = occurrence-gate + window-widening together.** (Process note: research agents keep creating stray exp_dev_handoff_*.md files = dead ferry convention, harmless but trigger the stop-hook; rm denied so left in place.)

## ✅ GENERALIZATION PROBE: DID-IT-HAPPEN PIVOT GENERALIZES (2026-08-06, notes/research_goal_bearing_driver_generalization_probe_2026-08-06.md, Director-reviewed)
De-risk BEFORE building: re-ran the driver-decomposition on a FRESH zero-overlap 21-item set (Sherlock Holmes + 6 new OneStop + 5 new RACE, non-overlap confirmed). **VERDICT: GENERALIZES. Fresh dist A(verb-pole)=14.3% / B(did-it-happen)=52.4% / C(goal-relation)=19.0% / D=14.3% vs original 5.6/41.7/36.1/16.7. Did-it-happen MORE dominant (52% > 42%); verb-pole under the 15% hard-fail line -> pivot NOT refuted, confirmed on independent data.** HONEST CAVEATS: (1) verb-pole ~tripled proportionally (5.6->14.3%, near the line) -- blunter Victorian/news prose states outcomes outright -> KEEP the cheap in-lexicon verb fast-path (we have NOT removed Tier-1/2 typing; only shelved OOV-verb GROUNDING). (2) goal-relation halved (36->19%) = likely a sampling artifact (original was owner-mismatch-trap-curated), not the mechanism vanishing. This agent created NO handoff file (respected the convention). **BUILD GATE STATUS: generalization=PASS; now gated ONLY on the precision-quirk fix (aea59df0) committing (goal_typing.py currently shows uncommitted in-flight edits) -> fire the did-it-happen build (Check 1 occurrence-gate + Check 4 window-widening, per prereg 2026-08-06_did_it_happen_occurrence_gate_v1.md) the moment goal_typing.py is clean.**

## ✅ PRECISION-QUIRK FIX LANDED (2026-08-06, commit d355b0f36, HARD-PASS, cleared the did-it-happen build gate)
Root cause (confirmed): type_sentence_events_c3 typed OUTCOME over the WHOLE sentence -> a GOAL clause's own "to VERB" complement read as an achieved outcome ("Jack wanted to FIX" -> fix->MET, "Ruth longed to WIN" -> win->MET) -> fabricated outcome bound to the goal-holder (bystander mis-bind). FIX (strict SUBTRACT, goal_typing.py only +102/-6): exclude the GOAL_GOVERNING_PASS purpose-infinitival complement token from outcome typing (both tiers); GOAL typing + lexicon_predict byte-identical. Cert 220/3. NO regression (fair 48/48, real-eval owner-acc 6/10 identical). Bystander fix: jack/ruth affect-bank bystanders now correctly ABSTAIN (3/4 bystander items fixed); precision controls added (goal-clause verb not typed; later recurrence still typed win_recurrence_still_met + sink_recurrence_still_unmet). HONEST residual (out-of-scope, flagged): jack_kate_fence_bystander still mis-binds via a DIFFERENT pre-existing Tier-2 over-fire ("gather" in an incidental descriptive sentence types MET) -> separate follow-up (Tier-2 outcome polarity over-fires on incidental action verbs; fixing needs broadening Tier-2 = fair-instrument regression risk, left conservatively).
## 🚀 DID-IT-HAPPEN BUILD DISPATCHED (2026-08-06, hdi_exp_dev ad4c8a47, both gates cleared: generalization=PASS + precision-quirk committed) -- Check 1 occurrence-gate + Check 4 window-widening per prereg, no-handoff-files instruction included. First MEASURED movement of the new direction. VET per-check on return.

## 🔎 DID-IT-HAPPEN BUILD = HARD-FAIL (both checks) BUT MECHANISM CORRECT + REAL BLOCKER PINPOINTED (2026-08-06, commit 7058d026b, Director VET'd incl reproducing GAP-C myself)
Built the occurrence-gate (Check 1) + window-widening (Check 4). **HARD-FAIL: net-new-correct on the 15-subset = 0.** BUT: mechanism CORRECTLY built (constructed positives fire; occurrence_gate_fired=True; recurrence fires), strict-ADD, cert 220/3, ZERO regression (44/44 byte-identical), NOISE 0 leaks, numeric-threshold not false-flipped, NO corners (resisted make->get synonym-bridging + kept stop-list principled). Honest baseline correction: all 6 baseline-correct subset items are LEXICON-LUCK (zero congruence-native). Pre-reg assumption corrected: onestop_hunt desired verb is "get" not "make" (guard correct, pre-reg wrong). **ROOT CAUSE (deeper than pre-reg's GAP-1; Director-reproduced): the occurrence-gate is STARVED by UPSTREAM REFERENT-EXTRACTION bugs -- GAP-C (CONFIRMED BUG, I reproduced: "save the boat"+"boat did not sink" -> UNMET/referent_mismatch, should be MET; a pre-verbal negator poisons the actual referent -> flip discarded); GAP-B (desired-side referent None for OOV/control verbs -> recurrence same-referent can't fire); GAP-A (desired verb OOV of CLASS_REGISTRY -> class path empty, but the RECURRENCE path is the relevant one for OOV, blocked by GAP-B not GAP-A); GAP-D (window reaches a distractor).** **THE PRODUCT FINDING: next target = FIX REFERENT EXTRACTION (GAP-C negator-skip [clean high-value bug] + GAP-B desired-None), NOT more negation/recurrence logic. The did-it-happen mechanism + window-widening are BUILT+CORRECT, waiting on referent extraction.** congruence_outcome_valence_windowed left defined-but-UNWIRED (revive after referent fix). DRILL: referent tracking through negation is core to the brain's situation model; our extractor is fooled by "did not X" (GAP-C) + fails on OOV desired verbs (GAP-B) = used-ability-wrong/missing-robustness -> FIX.
## 🚀 REFERENT-EXTRACTION FIX DISPATCHED (2026-08-06, hdi_exp_dev aa890082): GAP-C negator-skip + GAP-B desired-None + wire the window-widening + re-run the did-it-happen checks -> measure whether net-new-correct on the 15-subset finally MOVES above the 6/15 luck baseline. VET on return (the number + the GAP-C repro flip + no-regression).

## 📐 OWNER-ATTRIBUTION LEVER SIZED (de-risk, 2026-08-06, commit 160ea25a6, notes/research_owner_attribution_natural_prose_derisk): VERDICT = BUILD IT, thin+reuse-heavy, AFTER did-it-happen.
On 24 FRESH natural passages: owner-attribution GENUINELY DECIDING on 25.0% (6/24) + necessary-but-insufficient 4.2% -- between the trap-curated 69% (engineered, wrong baseline) and the coarse 19% (undercounted). KEY: pure owner-NAME misattribution is RARE (8.3%, matches 1/10 McGuffey TIEBREAK_MISS); the DOMINANT pattern (16.7%) is CROSS-ENTITY OUTCOME-BINDING (right owner name, but the satisfying fact belongs to a DIFFERENT entity: rescuer/rescued, antagonist-comeuppance, dueling-interest, multi-owner docs) -- reproduces on fresh corpora (NOT a curation artifact). REUSE-HEAVY: substrate already owns the 2 pieces (goal-coherence candidate-gen from primacy-trap fix + per-owner GoalOutcomeRegister from affect bridge). SEQUENCE: AFTER did-it-happen/referent-extraction (current bottleneck is upstream typing starvation, NOT owner misattribution); NOT indefinitely-deferred, NOT cheap-protagonist-default (wrong on 2/6 deciding via ordinary multi-entity journalistic structure). Queued as the next lever after did-it-happen lands.

--- EARLIER HISTORY (superseded lineage; recall-searchable, NOT current) ---
## 🎯 CURRENT STATE (2026-08-05 LATE — earlier-session semantic-organ arc; SUPERSEDED by the 2026-08-06 state above)

THE ARC THIS SESSION (2026-08-05): brain-foundational AUDIT (2 tracks) found the converged bottleneck was a missing SEMANTIC ORGAN. Built + WIRED into production situation_reader (run_certification GREEN 208): (1) hdlab/context_grounded_valence.py (governor sense-select + animacy-axis event-assembly + frozen appraisal-sim valuation; certified OPEN-VOCAB on the ANIMACY axis; clears the original resolve_valence_blind word-sense FPs 6/6 vs 2/6); (2) EventRecord.affect dimension; (3) OOV frame-induction (frame_primary_role induces EXPERIENCER for OOV psych verbs 0.0->0.833; cherished->EXPERIENCER). Mechanism-proven: forward-PREDICTION (isolate gate 100pt). C-A (sense hub) reformulated after 2 fails (bag-of-words lacks sense signal; sense=control+grounding). BRIDGE-1 governor->event-assembly->valuation is the grounding path.

THEN (USER-prompted) a BRAIN-FOUNDATION + TEST-FAIRNESS drill on goal-owner: **the goal-owner TEST was UNFAIR** (old 0.32/0.64 numbers measured subject-resolution vs a syntactic-subject gold; organ decided ~1 item; TIED recency exactly; extraction "F1~0.64" was a MISLABEL, real 0.23-0.28; banks gold-defective). Prior goal-owner conclusions RE-SCOPED/REJECTED. Pipeline ORDER is brain-faithful but 2 LEXICAL shortcuts where the brain does GATED/GENERATIVE inference: event-seg OVER-SEGMENTS (needs relevance gate on Zwaan-dim shifts); goal-typing LEXICAL (needs generative goal-inference from actions). Reframe = MEASUREMENT-FIRST.

**MILESTONE — FAIR instrument + PROVEN capability (commit 21c0eadaa):** experiments/data/goal_owner_fair_v1.jsonl = 28 recency-trap + 20 primacy-trap items. FULLY FAIR: ALL 4 positional baselines (recency/first-mention/nearest-subject/majority) = 0.0, scramble non-vacuous. The goal-owner MECHANISM (hdlab/goal_owner_select.directed_goal_outcome_score) is PROVEN: 12/12 explicit-goal + 8/10 purpose-infinitival goals, beating every positional shortcut. **The integrator is HEALTHY; the caps are UPSTREAM.** (VET caveat: "action-implied" items are all PURPOSE-INFINITIVAL "V...to VP"; pure ACTION-TELOS goals untested+unbuilt.)

COMPONENT-HEALTH AUDIT (commit be6203bc4, notes/component_health_audit_comprehension_organ.md, all recomputed off disk) — RANKED IMPROVEMENT ROADMAP:
1. **EVENT EXTRACTION = weakest ROOT** (real F1 0.23, recall 0.32 starves everything). Progress: frame-trigger recall 0.324->0.529 (61ac0095d, drill-guided: brain gates events on FRAME not POS, commit dc0a78704) BUT precision low (0.16) + triple worse (secondary-pred roles). Relevance-gate precision +35pts on a proxy (5f537efcc) BUT recall+precision DO NOT cleanly COMPOSE (weak-port gate over-drops real predicates, 1f1c26200). **RESOLVED 2026-08-05: real-coref gate LANDED (commit 5cd27f385, exp_frame_trigger_plus_relevance_gate_real_coref_v1, metrics disk-VET'd): PARTIAL_BEATS_WEAK_COMBINE_RECALL_STILL_ERODED (combined P=0.204 R=0.294 F1=0.241; beats weak-combine 0.235 but NOT frame-alone 0.245; recall NOT kept; precision +0.045 non-material). CRITICAL DIAGNOSTIC: of 8 TP-drops, coref_overmerge_attributable=0 (frac=0.0) -> cross-sentence coref QUALITY is EXONERATED as the blocker (the feared coref-miss branch did NOT happen); the drops are legitimately NON-BOUNDARY continuation events (same correctly-resolved protagonist + same tense + no causal shift). The blocker is the BOUNDARY-FILTER SHAPE dropping true continuation predicates.**
2. **GENERATIVE goal-typing** = purpose-infinitival DONE (0/10->8/10, verb-independent, ebabb5a84/9bf855dd0); PURE action-telos remains.
3. **OUTCOME-VALENCE** = goal-congruence axis real+derivable but detector regresses on lexicon-covered (not drop-in); needs a real achieved/blocked detector + RELABEL off HARD_PASS.
4. **THEMATIC-ROLE OOV** = data-starved (subj 0.833/obj 0.455 real); SUPPLY construction-diverse data.
5. **WIRE (not build): ToM** (HARD_PASS but ISLANDED — add registry row + pipeline entry); quotative-to-attribution net-zero unblocks once goal-typing lands.
HEALTHY, leave alone: grounding-valence, affect-dim, situation-model skeleton, goal-owner MECHANISM. 3 MISLABELS: docstring F1 (FIXED situation_reader.py:34), outcome-valence label (PENDING relabel), missing ToM registry row (PENDING). Coref: LOCAL HARD_PASS but CROSS-SENTENCE thin (MIDDLE_BAND, marginal-over-recency) = a real dependency risk for the extraction gate.

STRATEGIC READ (SUPERSEDED by the DEEP VET below): the extraction ROOT is a MULTI-DEPENDENCY hard problem; a21e14c adjudicated -> coref EXONERATED, blocker is the boundary-filter SHAPE.

**DEEP VET (2026-08-05, USER-requested, commit 1b47eb270, notes/deep_vet_comprehension_organ_vs_brain_2026-08-05.md) - 4 parallel per-component brain-fidelity drills, ALL load-bearing code claims Director disk-verified.** CONVERGENT FINDING (both meta-patterns across all 4): (1) we built a FEEDFORWARD/LOCAL pipeline where the brain runs a RECURRENT/TOP-DOWN-GATED loop - severed at 4 points (situation-bias arg hard-coded None @ context_grounded_valence.py:141; recollection coref arm switched OFF for recency @ situation_reader.py:420; goal/affect never accumulate per-protagonist; predictive_coding islanded, 0 hdlab consumers); (2) largely a WIRING crisis NOT a mechanism crisis - the brain-faithful pieces mostly EXIST + several are self-test-validated but switched to the wrong mode / unwired. **Sharpest correction: the PROVEN 12/12 goal-owner integrator (directed_goal_outcome_score) has NEVER been fed a real event the reader extracts - no wire exists.** 6 slight-differences ranked by leverage x certainty (see doc). CORRECTED ROADMAP (reorders component-health): STEP 1 WIRE THE LOOP (cheap+high-certainty): (a) flip coref recency->event_role [1 line, self-test-validated]; (b) [RE-SCOPED by VET-the-VET this cycle] the GOAL-typing wire ALREADY EXISTS + is GREEN (exp_component5_wired_endtoend_v1, N=23, real frame_primary_role -> integrator = 1.0, beats recency, scramble collapses) - VET-D's "never wired" was too strong; the PRECISE gap is that cell's cluster_ids come from TOY resolvers not the production coref organ, outcome-valence is lexicon, and it's not on the fair instrument. Its RecencyEntityResolver-vs-ContentMatchResolver A/B maps onto VET-B's coref flip -> LANDED (commit f3f88f752, exp_c5_real_coref_endtoend_v1, per-axis disk-VET): event_role coref 0.8889 (16/18) vs recency 0.0 on explicit_psych recency-trap divergent subset (N=18, 3 seeds), beats gated positional baselines (recency/nearest_subject=0.0), scramble collapses; 16/16 when C3 supplies GOAL -> residual cap = OOV GOAL-typing (3b) ONLY, not coref/integrator. Caveats: N small (MIDDLE_BAND), first_mention/majority not gated on this subset (primacy bank out of scope), outcome-valence lexicon. VET-B flip VALIDATED end-to-end -> PROMOTED to production (commit a9e873ab0: situation_reader.py:420 recency->event_role; 6/6 self-tests green, certification 208/3-skipped UNCHANGED = no regression; no other production recency-callers). Real gain WIRED not islanded. THEN goal-typing wired end-to-end: purpose-infinitival wire (78294a2c6) SOLVED action_implied 0->10/10; desiderative/aspectual partition (5da76bf34) recovered explicit_psych ->18/18 with precision guard CLEAN (aspectual probe 0 false-GOALs/3 seeds). **MILESTONE: goal-owner organ now MEASURED END-TO-END ON REAL COREF at 18/18 explicit_psych + 10/10 action_implied across BOTH constructions, gated baselines beaten, scramble non-vacuous.** REMAINING: N small; first_mention/majority not gated on recency-trap subsets -> PRIMACY-trap subset untested end-to-end (next fairness milestone); outcome-valence still lexicon; goal-typing gains PROMOTED to production (5449161c2, Director ran the witness on disk): hdlab/goal_typing.py (type_goal_events/has_goal = EXPERIENCER + purpose-infinitival + desiderative/aspectual partition, byte-identical reuse) + verification/verify_goal_typing.py (reproduces 18/18 explicit_psych + 10/10 action_implied + 0/7 aspectual false-GOALs, 3 seeds) + registry row + certification 208 unchanged. WIRE-DON'T-ISLAND lock-in DONE (importable/verified/registered; NOTE: full situation_reader.read() call-site integration is a further step, gated by the same CoNLL constraint as the frame_primary_role wire - the organ is promoted, the reader-call is the next integration). PRIMACY-TRAP end-to-end HARD_FAIL (43e942ca0, exp_c5_primacy_trap_endtoend_promoted_organ_v1): 0/20 on p01-p20, pre-registered. VET-THE-VET (decisive): the MECHANISM is fine - isolated primacy cell (exp_c5_fair_goal_owner_primacy_v1) beats primacy 12/12 explicit, all baselines 0.0. The 0/20 is a CANDIDATE-GENERATION failure: event_role coref resolves the outcome PRONOUN to the recent/event-central FOIL under primacy -> both candidates bit-identical -> integrator can't redirect. ROOT = event_role coref uses EVENT-CENTRALITY (aligns w/ goal-holder on recency-trap -> 18/18, DIVERGES on primacy -> 0/20); the brain uses GOAL-COHERENCE top-down bias on the resolution = the deep-VET SEVERED-TOP-DOWN-LOOP proven by can-fail. FIX (dispatched): roster-enumerated goal-coherence candidate generation + promoted goal-typing, re-measure primacy (expect ~12/12 explicit + action_implied improved) + re-confirm recency-trap 18/18+10/10. Re-scopes the milestone: 18/18+10/10 is REAL but coref-pronoun-resolution-dependent. **FIX LANDED+VET'd (b1b1ce460, exp_c5_primacy_trap_endtoend_goal_coherence_candidate_gen_v1): goal-coherence candidate-gen (enumerate roster.keys() as outcome candidates, argmax directed_goal_outcome_score, GOLD-FREE - Director read the code) HARD_PASS: primacy 12/12 explicit + 8/8 action_implied beating ALL 4 baselines (0.0), scramble non-vacuous; recency-trap HELD 18/18 + 9/10. GOAL-OWNER ORGAN NOW 47/48 ON THE FULLY-FAIR INSTRUMENT (both trap types, both constructions, gold-free).** The 1 miss (t24) = VET-found 3c gap: directed_goal_outcome_score is CONTENT-BLIND (checks goal-PRESENCE not goal-content<->outcome-content coherence) -> multi-goal cue-conflict ties (2 latent, 1 wrong). 3c SOLVED (6961f5b49, exp_c5_multigoal_content_coherence_tiebreak_v1, HARD_PASS, Director-VET incl authored-data): goal-content THEME-MATCH tie-break among tied goal-holders (Trabasso) -> multi-goal set 12/12 content vs 6/12 positional, flip-control flips all 6 families (proves theme not position), full instrument 48/48 (t24 fixed, 47 held by construction), pure add-on (production hdlab unmodified). **MILESTONE: goal-owner organ COMPLETE on the fully-fair instrument (48/48, both trap types + both constructions, gold-free) + multi-goal cue-conflict class (12/12).** Complete selector WIRED to production (6f0b824e2, hdlab/goal_owner_select.py::select_outcome_owner = enumerate+goal-coherence argmax+content-coherence tie-break; witness verification/test_goal_owner_select.py Director-ran 4-passed 48/48+12/12+flip-control; cert 212/3; gold-free). GOAL-OWNER ORGAN COMPLETE + WIRED. OUTCOME-VALENCE COMPLETE + WIRED (formalize drill 3b2ead690 -> build 63c71935d -> expand/coverage-wall e33dab529 -> coref-wire fix 3ed374148 -> PROMOTED ab12d8e88): goal-congruence outcome-valence (goal DESIRED-STATE vs outcome ACTUAL-STATE = (theme, result_verb_class) on shared referent; same-theme+opposed->UNMET/+entailing->MET/mismatch->ABSTAIN->V2_OUTCOME lexicon fallback = STRICT ADD) is now in production hdlab/goal_typing.py (congruence_outcome_valence/congruence_with_lexicon_fallback), witness verification/test_outcome_valence_goal_congruence.py, CERTIFICATION 220/3 (212+8 new, no regression). VALIDATED: goal-dependent-valence core-flips 16/16 (lexicon at chance -- same word opposite valence by goal), coverage-stress 1.0 (pronoun-referent via WIRED coref [general], synonym via narrow supplied set), over-link guard held, precision-guard abstains, 48/48 owner-selection held. HONEST SCOPE: Tier-1 pronoun=general, Tier-2 synonym=narrow (concept_encoder ruled out supervised-synthetic); GENERAL synonym/hypernym BLOCKED on the ATL learned lexical-semantic hub (deep-VET #1, SURFACED TWICE: sense-resolution + synonym-referent). **ATL-HUB FORMALIZED (drill notes/drill_brain_atl_lexical_semantic_hub_2026-08-06.md, P=0.45): NOT missing from scratch -- hdlab/random_indexing.py (Random Indexing + BEAGLE, ran text8, MIDDLE_BAND, UNWIRED) EXISTS but plateaued because its probe measured topical RELATEDNESS (WordSim-353) not genuine SIMILARITY (SimLex-999) = the vessel~ferry(sim) vs vessel~dock(rel) failure. Brain (Cox 2024 intracranial vATL): hub similarity = shared-FEATURE correlation, NOT co-occurrence. TWO-TRACK EARN+SUPPLY: (1) EARN = add symmetric-pattern context (Schwartz 2015, SimLex 0.517 vs skip-gram 0.462, NO parser, cheapest) + dependency context (Levy-Goldberg) arms to the existing accumulator; (2) SUPPLY = McRae feature-lexicon + bundle/bind (more brain-faithful METRIC). FIRST INCREMENT (dispatched): add context_mode symmetric_pattern/dependency arms to hdlab/random_indexing.py + a graded Tier1-synonym/Tier2-related/Tier3-unrelated can-fail probe (replace the too-coarse within/across-category one), re-run exp_n11 harness. Cheap, additive, reuses owned infra, targets the diagnosed similarity-not-relatedness gap.**
  **EARN-TRACK FIRST INCREMENT HARD_FAIL (NEGATIVE, north-star drill; commit d0dc07c91, exp_n11b, Director
  disk-VET): symmetric_pattern tier-means 0.154/0.092/0.084 (ordering right, MAGNITUDE noise-level), ordered-frac
  sym 0.207 = scramble 0.207 EXACTLY (scramble did NOT collapse -> symmetric-pattern signal is NOISE-DOMINATED /
  DATA-STARVED: and/or-coordination events too rare per-word even in 17M-token text8); window ordered-frac 0.379
  > sym (window orders better but is magnitude-saturated 0.83-0.86 = stopword dominance, no PPMI); hash floor 0.0;
  cert 220 unchanged (window default byte-identical). Mechanism PROVEN on toy self-test (ferry/boat share ship
  cos 1.0). BRAIN-DRILL: co-occurrence (distributional) is the WRONG METRIC (=relatedness) where the ATL hub uses
  shared-FEATURE correlation (Cox 2024 intracranial vATL); distributional on modest text earns only weak
  relatedness, data-starved for the sparse coordination cue -> the HARD_FAIL VINDICATES the shared-feature metric.
  ROUTE (north star): PIVOT from EARN(distributional, wrong-metric+data-starved) to the SUPPLY(McRae feature-
  lexicon) track = supply grounded semantic features per concept (DATA, invariant-OK like desiderative/
  RESULT_VERB_CLASS supplies) + substrate computes similarity as shared-feature cosine (the ACTUAL hub mechanism,
  glass-box, NOT data-starved). Select-by-brain-foundational-right. EARN levers (PPMI via owned ppmi_sparse_
  encoder, wider radius, bigger corpus) = fallback/complement. SUPPLY-track build dispatched. Coverage caveat:
  feature-lexicon = mechanism-proof on covered concepts; general open-vocab feature-coverage = the deeper
  follow-up (missing-LEARNING: induce features from grounding/text), mirrors the outcome-valence pattern.**
  **SUPPLY-TRACK HARD_PASS = ATL-HUB METRIC PROVEN (commit 7d0a574b4, exp_n11c_shared_feature_lexical_similarity_v1,
  Director disk-VET incl circularity + glass-box): shared-FEATURE cosine over FHRR feature-bundles (86-concept
  hand-authored McRae-style feature-lexicon SUPPLIED as DATA; mechanism = hdlab.bundling.bundle + unit_phase_vec,
  the substrate's OWN op) produces GRADED lexical similarity: tier-means synonym 0.9307 / related 0.3041 /
  unrelated 0.0020, ordered-frac 0.9655 (28/29) vs distributional window 0.379 (delta +0.586); CIRCULARITY GUARD
  HELD (scrambled-features collapses 0.97->0.31, delta 0.655 -- mechanism reads the real feature structure, not
  gerrymandered); GLASS-BOX/NOT-BORROWED confirmed (n_llm_calls=0, n_external_model_calls=0, feature-provenance
  persisted). The 1 miss (happy/music) = associative-not-taxonomic, PREDICTED before running. **MILESTONE: the
  deep-VET #1 root gap (missing ATL learned lexical-semantic hub) is now MECHANISM-SOLVED -- the brain-faithful
  shared-feature-correlation METRIC (Cox 2024) works glass-box in FHRR. The EARN-fail->SUPPLY-pass arc vindicated
  the metric.** HONEST SCOPE: mechanism-proof on 86 concepts w/ a uniform feature convention; the SPECIFIC numbers
  are convention-shaped (scramble-guard proves the mechanism honest, not that features scale) -- GENERAL coverage =
  feature-coverage follow-up (broad McRae norms SUPPLY, or missing-LEARNING induce features), same as outcome-
  valence. NEXT = WIRE the shared-feature-similarity mechanism into hdlab (ATL-hub-similarity organ + the 86-concept
  lexicon + SUPPLY extension point; can upgrade outcome-valence's narrow SYNONYM_GROUPS for covered concepts);
  then feature-coverage expansion.**
  **WIRED (commit b621ff68d, Director disk-VET incl over-link guard): the ATL shared-feature-similarity organ is
  now production hdlab/lexical_similarity.py (glass-box, hdlab-only, 86+3 concepts, lifted byte-identical from
  exp_n11c) and REPLACES outcome-valence's narrow SYNONYM_GROUPS in hdlab/goal_typing.py::_referent_links (Tier-2
  = concept_similarity >= 0.50 threshold, OOV falls through to no-link). Director-computed sims: vessel/ferry
  0.634 + boat/ship 0.548 (synonym, LINK) vs vessel/dock 0.279 (related, no-link) vs sister/rival 0.398
  (different-entity, no-link = OVER-LINK GUARD HELD -> D-unmet stays UNMET); threshold cleanly separates all 3
  classes = genuine graded similarity not a hardcoded group. synonym-stress HELD 2/2, core-flips 16/16, 48/48
  held, scramble-collapse held (0.634->0.013), certification 220/3 zero-regression. HARD_PASS. **ATL METRIC now
  PROVEN + WIRED into a real production consumer.** Remaining ATL frontier = GENERAL open-vocab feature coverage
  (missing-LEARNING: induce features from grounding -> the grounded-foundation pivot; supply-broad-McRae = interim).**

## 🔌 SEVERED-LOOP POINT 1 UN-SEVERED (2026-08-06, commit e8b0dac93, exp_situation_bias_prod_wire_discourse_decisive_v1, Director disk-VET strict-ADD + cert)
The deep-VET META-PATTERN 1 cut point #1 (context_grounded_valence.py:141 situation-bias 3rd arg hard-coded None) is now WIRED. SOURCE (STEP-0 VET, clean): situation_type derives from the organ's OWN per-event HARM/HELP affect on PRIOR events (situation_type_from_affect) -- NO new source needed, no text re-scan; returns BLOCK_HIGH/NEUTRAL/None matching _v2.situation_type_for_prior's domain, drops into combine_biased_competition unmodified. score_item gained situation_type: Optional[str]=None (default None = BYTE-IDENTICAL strict ADD, line 161 was None now passes situation_type); score_passage = narrative-order entrypoint threading it. MEASURED: discourse-decisive wired_acc 1.000 (12/12, 3 seeds) vs None-arg 0.500 (chance); scramble collapses 1.000->0.500; animacy-axis witness unchanged (core6 6/6, Bopen 1.000); full pytest verification/ 220/3 before+after (Director ran full cert; independent witness re-run confirmatory in flight). HARD_PASS. **4th deep-VET item addressed tonight (goal-owner, outcome-valence, ATL-hub, now situation-bias loop-point-1). Remaining severed-loop points: per-protagonist goal/affect ACCUMULATION (row-1); predictive_coding islanded; recollection-vs-recency coref [already flipped to event_role a9e873ab0].**

## 🔬 EVENT-EXTRACTION ROOT REDIRECTED (2026-08-06, NEGATIVE+north-star-drill; commit 520d7ee41, exp_frame_trigger_argrealization_gate_v1, Director disk-VET)
Tackled the deep-VET #1 weakest root (event extraction F1 0.23) via agent-A's identified fix = argument-realization GATE on event-hood. HARD_FAIL but VALUABLE (redirects the whole framing). The gate is correctly built + validated in ISOLATION (mechanism can-fail 13/13: 5/5 over-gen rejected, 8/8 real kept) BUT produces BYTE-IDENTICAL numbers (gated P=0.159/R=0.529/F1=0.245 == frame_trigger; n_pred=113 identical). VET-CONFIRMED WHY: (1) event-hood gating is a NO-OP on the RELATION metric -- it rejects predicate SITES but those sites resolve no patient -> no SVO relation -> no metric change; so event-hood over-generation is NOT the FP source; (2) the gold (data/gold_mcguffey_castle_building_svo_v1.json _meta) is SINGLE-ANNOTATOR, SEMANTIC-PATIENT-standard with JUDGMENT-CALL exclusions ("look-at-book NOT counted"), n=34 -> many extractor "FPs" are events gold DELIBERATELY excludes (speech-report/cognition/self-motion), a gold-DEFINITION artifact not an extraction error (agent found 65/95 frame_trigger FPs have no gold relation). **REDIRECT: the deep-VET #1 "F1 0.23 weakest root" is NOT primarily event-hood over-generation -- it's (a) a GOLD-MEASUREMENT ARTIFACT (single-annotator, narrow semantic-patient scope, tiny n) + (b) downstream FRAME-CONDITIONED ROLE/PATIENT ASSIGNMENT bugs (positional patient-assignment consults no frame arity -> "sat" gets 3 spurious patients).** This DEMOTES the event-extraction root's severity (partly a measurement artifact) + corrects the target. BRAIN: thematic-role assignment is FRAME-CONDITIONED (McRae; frame fixes arity). ROUTE: BUILD frame-conditioned role assignment (constrain patient-arity by VERB_FRAMES, owned) = agent-A's 2nd fix / deep-VET "positional slot-filling never consults the frame". Redirected fix dispatched. The arg-realization gate is a correct-but-non-bottleneck mechanism (keep parameterized, off by default).**
  **FRAME-ARITY FIX LANDED (commit 29842ab70, exp_situation_reader_frame_arity_gate_gold_v1, Director disk-VET incl 16/16 can-fail run myself): the REDIRECT's corrected target = frame-conditioned patient assignment. FIX = frame-ARITY gate in production situation_reader._pick_role_mentions (gate_intransitive/pred_lemma kwargs) -- strictly-intransitive verbs (SUPPLY set STRICTLY_INTRANSITIVE_VERBS=17 in thematic_role_labeler, since VERB_FRAMES doesn't encode arity -- all frames carry obj:PATIENT) get NO patient. MECHANISM can-fail 16/16 (DECISIVE, gold-independent, ran myself): 9 intransitive suppress spurious patient, 3 transitive/ambitransitive KEEP patient (0 over-gating), ambitransitive-no-obj abstains, stand/wait conservatively excluded, end-to-end event-count unchanged. GOLD (proxy-CoNLL harness, within-harness delta only): 9 REAL intransitive-patient FPs removed (P 0.148->0.173, 0 collateral, 0 added, recall UNCHANGED). Strict-ADD (default False byte-identical), cert 220/3. This is a genuine production role-assignment improvement (removes real FPs; the ~65 gold-scope FPs remain, per the redirect). PROMOTED to production DEFAULT (commit 847fa78c7, Director disk-VET: default now True @ situation_reader.py:441, can-fail 16/16 ran myself, cert 220/3 green): every SituationReader read now suppresses spurious intransitive-verb patients. BROADER-TEXT SAFETY (the make-or-break for the 17-verb SUPPLY set production-wide): ran gated/ungated over McGuffey L01-L20 (363 sents, ~10x gold) = 52 relations suppressed, ALL 52 verified strictly-intransitive (go/come/rise/fall/die/sleep/sit), 0 false-suppression, 0 additions, event-count 496==496 -- structurally safe (gate only touches the 17-verb set). One stale self-test (old-default construction) correctly fixed, not a masked regression. **The redirect's corrected event-extraction fix (frame-conditioned role assignment) is now LIVE in production.** Deeper role-assignment (frame-conditioned object SELECTION for coordination/relative-clause) remains; the ~65 gold-scope FPs are a measurement issue (broader gold), not extraction capability.**

## 🚨 GENERALIZATION PROBE = DIRECTION-RESHAPING NEGATIVE (2026-08-06, commit f496caa51, exp_real_text_goal_owner_generalization_diagnostic_v1, Director disk-VET incl gold soundness)
Ran the wired composed organ (select_outcome_owner + outcome-valence congruence) on 10 REAL McGuffey grade-1/2/3 narrative passages (gold hand-authored BEFORE running, honestly selected, all 10 have real owners+goals -- gold VET'd sound). **RESULT: organ owner-acc 0.30 (3/10) LOSES to EVERY positional baseline (recency/first_mention 0.70, majority 0.60); polarity 0.10 == lexicon floor. THE WIRED ORGANS DO NOT GENERALIZE to real prose.** Failure tally: owner OUTCOME_NEVER_TYPED=6/10 + TIEBREAK_MISS=1; polarity LEXICON_COVERAGE_MISS=9/10. **DOMINANT BOTTLENECK (VET-confirmed, precise): CLOSED-SET hand-typed verb lexicons (V2_OUTCOME_MET/_UNMET ~23 words, CLASS_REGISTRY ~12 classes, desiderative set) are OOV for real prose -> outcome/goal TYPING NEVER FIRES -> owner-selection blocked before its reasoning engages (e.g. mg2_henry: GOAL 'wanted to help' types, but OUTCOME 'praise/accept' is OOV in V2_OUTCOME -> OUTCOME_NEVER_TYPED).** This was the REAL blocker all along -- NOT event-hood, NOT role-assignment (those were real but non-dominant); the hand-authored-instrument HARD_PASSes did NOT transfer because the instruments used in-lexicon verbs. NUANCE: real text isn't trap-structured so recency is naturally strong (0.70); but the decisive fact is the organ is BLOCKED (6/10 never fire), not merely out-competed. BRAIN-DRILL: closed `lemma in members` vs the ATL hub's OPEN-vocab graded shared-feature similarity (novel verb classified by feature-similarity to exemplars). **FIX = REUSE (wire-don't-island): extend verb-class membership from `lemma in members` to concept_similarity(verb, class_exemplars) >= threshold, using the ALREADY-PROVEN+WIRED ATL organ (hdlab/lexical_similarity.py) -- currently wired only for REFERENT-linking, not verb-class membership. This VINDICATES the ATL hub as the GENERAL open-vocab solution across the pipeline.** Requires feature-coverage for real-prose verbs (the ATL feature-coverage/induction frontier -- now the CRITICAL PATH to generalization, not a side follow-up). **DIRECTION RESHAPED: from component-fixes-on-instruments -> OPEN-VOCAB verb-typing via ATL similarity = the generalization critical path.**

## ✅ GENERALIZATION FIX WORKS -- FIRST REAL-PROSE MOVEMENT (2026-08-06, commit d52aa7669, exp_verb_class_openvocab_similarity_v1, Director disk-VET all 5 gates)
Formalized (drill 6cf2dbaa9, MEASURED pilot) -> BUILT: hdlab/verb_lexical_similarity.py (TWO verb-feature bases -- result-state[Jackendoff/Talmy/Beavers] OUTCOME_VERB_FEATURES 99 words + bouletic[Karttunen] GOAL_VERB_FEATURES 73 words; seeds=existing lexicon members) + Tier-2 concept_similarity extension in hdlab/goal_typing.py at 3 sites (_verb_classes 12-way; _outcome_polarity_tier2 wired into type_sentence_events_c3/lexicon_predict = THE one that moves OUTCOME_NEVER_TYPED since typeability gates on the flat V2 lexicon; _control_verb_is_aspectual_like). STRICT ADD (Tier-1 exact wins, Tier-2 only on OOV). RESULT (HARD_PASS): held-out CORPUS-DRAWN classification outcome 32/32 + goal 32/32 (NON-CIRCULAR: held-out includes the actual blockers praise/accept, NOT seeds), scramble COLLAPSES (0.52/0.50 vs real 1.0), praise/accept typed MET. **DECISIVE END-TO-END (the real gap, not toy): re-ran the generalization probe WITH Tier-2 -> OUTCOME_NEVER_TYPED 6/10->4/10, owner-acc 0.30->0.50 (+0.20 MATERIAL), polarity 0.10->0.20, on REAL McGuffey prose.** cert 220/3 (zero regression). **FIRST TIME THE ORGAN MOVED ON REAL PROSE -- the generalization arc paid off (probe->bottleneck->formalize->build->move the real number).** HONEST: still trails recency 0.70 (real narrative isn't trap-structured -> recency naturally strong; organ went from BLOCKED to FUNCTIONAL; its distinctive win is on rare trap items). Remaining 4/6 OUTCOME_NEVER_TYPED = OTHER causes (agent-traced, NOT verb-typing, NOT swept in): (1) SENTENCE-SPLITTER BUG re.split(r"[.!?]") degenerates last sentence to a bare trailing quote when dialogue ends the passage [2 items -- cheap high-value fix]; (2) affect-state predicate "how proud he feels" outside verb typology [1]; (3) negation-scope "could not find them" [1]. NEXT: fix the sentence-splitter bug (easy, +2 items) + affect-predicate typing + negation-scope; broaden verb feature-coverage; re-probe other corpora. ATL similarity organ VINDICATED as the general open-vocab solution.** Predictive-coding = right-shape/wrong-metric (design pattern only). Old next-cap-3b CLOSED; (c) wire situation-bias 3rd arg [AFTER checking it wasn't left None for underperformance]. STEP 2 MEASURE END-TO-END on goal_owner_fair_v1.jsonl (= the assembly-proof; localizes the true cap; will INHERIT upstream extraction error, will NOT reproduce 12/12 - that's the point). STEP 3 SHAPE fixes gated by step-2 result: A collapse-two-stages extraction (arg-realization gate at generation, not overgenerate-then-filter), C conditional thematic-role override, D forward predictive prior for CONTINUOUS affect only (NOT discrete roles - frame_induction.py:27 already ruled the residual wrong-shape for discrete). DOC-MISLABEL to fix: brain-audit calls coreference_resolver.py the "strongest wired" organ but production uses a DIFFERENT lineage (event_centrality_coref->coref_distractor_suppress->coref.py). Still-standing lower-certainty wins: wire ToM, relabel 2 mislabels, gold-VET primacy bank, pure-action-telos goal-typing.

KEY DOCS: notes/DRILL_SYNTHESIS_goal_owner_brain_and_fairness.md (the ruling), notes/testfairness_audit_goal_owner.md, notes/component_health_audit_comprehension_organ.md, notes/PLAN_grounded_semantic_organ_build.md (v3 plan), the 3 drill notes (event-segmentation/goal-owner-flow/event-predicate-recognition). All work LOCAL, committed through 4d7006f2a. IN-FLIGHT AGENT: a21e14c (real-coref gate) -- on recovery: check its result on disk (data/exp_frame_trigger_plus_relevance_gate*/ or its commit), VET per-axis, bank, then follow the strategic read above.

--- (EARLIER-SESSION HISTORY, superseded; kept for lineage) ---
## (historical) CURRENT STATE + THE ONE REMAINING GAP
The foundation-growing machinery WORKS end-to-end AND grounded reading works at discourse scale for REPRESENTATION. The gap hit 3x: ONE general COHERENCE-BASED relational binding/selection organ (select antecedent by explanatory/goal COHERENCE, not recency). Build ONE coherence selector -> serves all three.

## ➡️ NEXT STEP — REFRAMED by the landed build-spec drill (2026-08-04 ~15:36Z, a4d64704, disk: notes/research_coherence_based_binding_selector_build_spec_2026-08-04.md; VET'd, it CORRECTED my over-optimism)
**The spec sharpens "ONE coherence organ" into: an abstain-gated SELECT over an instance-specific SCORE.**
- **SELECT = `hdlab/self_improving_loop.py::decide_keep_or_revert` (abstain-gated argmax of coherence-margin-delta) — genuinely UNIFIES all 3 instances, reused VERBATIM. Anti-recency BY CONSTRUCTION (the function signature has no positional/recency argument — recency literally cannot enter the tie-break). This is the real unification point; it replaces coreference_resolver `_pick_strict_cb` (a literal recency operator, lines 227-236).**
- **SCORE = instance-specific, NOT one formula.** (a) coref + goal-outcome: `decode_coherence_margins` (accumulate-register decode-margin, ALREADY BUILT, coref-native). (b) causal: `reach_value` via a TD-trained backward successor map `M_backward` — has code (`train_sr_transport`/`reach_value` in exp_pfc_gate_cfrpe_trained_v2.py) but NO training-data pipeline = Gap 1, NOT BUILT. So this is honestly **2/3-built, 1/3-designed-not-built**.
- **SETTLING DEMOTED TO STRETCH-GOAL:** no iterative Kintsch spreading-activation loop exists on disk; the shippable version is the ONE-SHOT margin-comparison + abstain-gate (a discrete single-pass approximation of construction-integration). Build that now; true iterative settling is not blocking. (This corrects the "build the settling selector" framing below.)
- **🔴 RESULT LANDED (2026-08-04 ~19:58Z, commit 15d8fd627, exp_coherence_margin_discriminates_goal_outcome_v1, VET'd on disk + agent caught its OWN false-positive): HARD_FAIL — the atomic single-position `decode_coherence_margins` carries ZERO identity information for ALL 3 instances (coref included), it is a pure register-LOAD artifact.** With TRUE-vs-WRONG candidate register-load MATCHED, per-position margin-delta = EXACTLY 0.0 (5 seeds, flat+multibank). A load-direction-flip probe proves the "winner" is whichever bundle is LIGHTER (foil-heavy -> true adopted 1.0/0.95; true-heavy -> 0.0; flips 100%), independent of truth. **This FALSIFIES the build spec's "SCORE reused across 2/3 via accumulate-margin" thesis AT THE ATOMIC GRAIN — the atomic organ discriminates nothing.** Production coref discrimination (67% oracle-gain, atom 29609) must therefore be an EMERGENT property of route_passage's WHOLE-PASSAGE MANY-POSITION AGGREGATE (bad clustering over-merges entities -> busier registers -> lower aggregate margins), NOT the atomic per-position read. Brain-fidelity reading: consistent with the brain — coherence is a DISTRIBUTED whole-network settling property (Kintsch), never a single-node signal; this further demotes the one-shot atomic read and points at aggregate/settling. **CORRECTED NEXT STEP: test every instance at the route_passage WHOLE-RESOLUTION AGGREGATE grain.**
- **⚠️ FAIRNESS CHALLENGE (USER, 2026-08-04 ~16:2XZ) — the aggregate negative below is NOT SETTLED; it is NOT apples-to-apples and likely an ENGINEERED null. DO NOT propagate it as a capability verdict.** Disk-verified config: coref arm = 18 REAL McGuffey passages / 9-role vocab / 16 slots; goal-outcome arm = 4 SYNTHETIC items / 4-role vocab / 8 slots -> the test confounds relation-KIND with data-realness + vocab-richness + slots + N. MECHANISTIC suspicion (verify): coref's non-zero signal is REGISTER-LOAD-REDISTRIBUTION (wrong coref MERGES entities -> busier register -> lower margins); the goal-outcome items were LOAD-MATCHED, which STRIPS that exact channel -> the 0.0 may be construction-induced, not intrinsic. Brain binds goal-owner over recency (achievable) -> 0.0 = test/representation wrong. CORRECTED PLAN: run a FAIR apples-to-apples re-test (same regime/vocab/slots/N for both arms; vary ONLY relation-kind; embed the goal-owner binding in a FULL situation model so a wrong owner creates DOWNSTREAM INCOHERENCE like coref's merge does; and test whether coref's own win is just load-redistribution). Research drill af2a126d REDIRECTED + LANDED (notes/research_relational_backward_reach_coherence_selector.md, commit b983de9a5, VET'd on disk) — agrees fully; demotes the backward-reach organ (its Section 5) to CONDITIONAL fallback (only if the fair test fails). **THE DECISIVE CHEAPEST PROBE it identified: load-match the COREF arm — coref's 1.0 was NEVER load-matched, so it may be the SAME load-redistribution artifact just correlated with truth. If load-matched coref COLLAPSES to ~0 -> decodability never measured coherence on ANY instance incl. the promoted atom-29609 coref capability = the priority escalation, bigger than either negative. If coref SURVIVES -> decodability is real, then the fair goal-outcome test is meaningful.** FAIR-TEST CELL DISPATCHED (hdi_exp_dev afebd511, local/CPU): ARM1 = coref load-matched (decisive, no new data); ARM2 = goal-outcome fair (same 9-role/16-slot register, real McGuffey goal passages, downstream-embedded errors, load-matched); positive control must reproduce net_auto=1.0; load-match assertion mandatory. Bands: HARD-PASS both survive; HARD-FAIL both collapse (coref win was load too); MIDDLE one survives (confound-free relation-kind asymmetry -> conditional backward-reach for the failing arm). NOTE (drill Section 4): causal is a SEPARATE architectural gap (Gap 2: no candidate-competition exists in CausalLinkRegister at all) — the fair test does NOT resolve causal; that needs a candidate-generation stage regardless.
- **🟢 FAIR-TEST RESULT LANDED (2026-08-04 ~16:4XZ, commit 46662d47b, exp_coherence_fair_load_matched_retest_v1, VET'd on disk + INTERPRETATION-CORRECTED by Director): coref SURVIVES load-matching -> the load-artifact worry is RESOLVED IN COREF'S FAVOR; atom-29609 is VINDICATED, NOT needing re-audit.** Disk: arm1_net_auto=2.0 (n=6 load-matched real g5g6 coref items, agreement 4/6), positive control reproduces net_auto=1.0, load-match assertion held. The agent's raw verdict HARD_FAIL_..._POSITIONAL_NOT_STRUCTURAL + its "re-audit atom-29609" escalation = AN OVER-READ from an INAPPROPRIATE control: it shuffled role_seq, but route_passage discriminates COREF candidates by CLUSTER-ASSIGNMENT (which entity absorbs each mention), NOT by role labels (role_seq is identical across the candidates it compares) -> reversing role_seq perturbs a variable coref never uses, so margin-invariance is EXPECTED, not damning. **The CORRECT, confound-free finding: decode_coherence_margins is a CLEANLINESS/IDENTITY detector (decodes (cid,slot)->role, top1-runnerup, NO gold-role comparison) -> structurally BLIND to role-CONTENT correctness; it works for coref only because a wrong CLUSTERING changes cleanliness (load/crosstalk), while a wrong ROLE-binding at matched load does not.** => coref (identity) signal REAL + vindicated; goal-outcome (role-content) genuinely outside THIS score's reach, on FAIR footing now (not a load artifact, not a confound). Brain-consistent (identity pattern-completion vs relational role/credit-assignment = different operations). **BLOCKER SURFACED: cannot fairly test the goal-outcome rescue on real data — the 18-passage corpus yielded only 1 clean goal narrative (arm2 N=1, data-availability wall). The downstream-embedding hypothesis (make a wrong owner create a CLUSTER conflict the cleanliness-margin CAN see) is UNTESTED, not disproven.** Backward-reach organ still NOT indicated on fair evidence. **REAL NEXT FORK: (a) make coherence ROLE-CONTENT-SENSITIVE (a wrong role-binding must create a detectable conflict — the untested downstream-embedding path) vs (b) source goal/blocked-goal narrative DATA at scale to fairly test it (ties to the grounded-foundation program) vs (c) the relational-reach organ. Recommend (a)+(b): design a role-content-sensitive coherence signal AND get real goal-narrative data; do NOT build the backward-reach organ until a fair role-content test fails.**
- **🧭 FRAMING AUDIT LANDED (2026-08-04 ~17:0XZ, commit aeceefe1d, notes/research_goal_owner_coherence_vs_mentalizing_framing_audit.md, disk-VET'd: ToM organ HARD_PASS confirmed + islanded confirmed). Validates the reasoning at all 5 levels; LEVEL-1 = COMPOSITE:** (i) STATED/single-world goal-owner binding (e.g. the Henry/cherries item — goal explicit, one shared world, no belief divergence) = relational binding, coherence-selector family is CATEGORY-CORRECT -> the mechanism probe IS the right test for this subclass; (ii) UNSTATED/abductive intent OR divergent-belief goal-attribution = MENTALIZING -> routes OFF the coherence arc to an ORGAN WE ALREADY OWN. **🔑 KEY DISK-FINDING: exp_theory_of_mind_sally_anne_nested_hrr_v1 = HARD_PASS ToM organ (per-agent multibank + refuse-gate; Q2 false-belief 0.806 vs 0.138, gap 0.668, oracle 1.0, 5 seeds), sitting ISLANDED (not in hdlab; only its refuse_gate dep is). Component map ToM row CORRECTED (GAP thin -> ISLANDED HARD_PASS unwired).** So the "3 unified coherence instances" story is INCOMPLETE: some goal content leaves the arc entirely to ToM. **HIGHER-VALUE PARALLEL WIN the audit surfaced: WIRE the existing HARD_PASS ToM organ into the reading pipeline (no new build — it's disk-verified) for the unstated-intent/divergent-belief subclass = the more narratively-common+impactful case (implied motive, dramatic irony, deception). Cheaper than it looks; sequence as a near-term win independent of the probe.** MECHANISM PROBE DISPATCHED (hdi_exp_dev a0903b07) with the audit's 3 mandatory corrections: (1) scope the claim to STATED/single-world crosstalk-generalization existence-proof ONLY, not general role-content; (2) pre-register the known 0.0/load-flip artifact signature as decisive HARD-FAIL even at N=1; (3) load-match assertion pre-conflict-embedding. Rescue is brain-faithful ONLY as a discrete crosstalk test (catches the foil-is-busy subset), NOT a general role-content detector.
- **🔻 MECHANISM PROBE LANDED (2026-08-04 ~17:2XZ, commit 3c584edd8, exp_coherence_role_conflict_crosstalk_v1, VET'd on disk): HARD_FAIL_SIGNAL_IS_POSITIONAL_NOT_STRUCTURAL — clean + fair.** orig_delta=0.9695, flip_delta=0.9654, shuffled_delta=0.9641 (ALL identical ~0.96; shuffled does NOT collapse), positive control net_auto=1.0, load-match held, scope-labeled. The crosstalk is same-(cluster,slot) co-occupancy, NOT role-content — reversing role_seq leaves the slot-collision intact, so the effect survives shuffling = decisively NOT role-content. Here the shuffle IS the correct control (hypothesis was "role-CONFLICT creates the crosstalk" -> shuffling roles must change it; it didn't). **CONVERGENT CLOSURE (3 findings): decode_coherence_margins is an IDENTITY/cleanliness/slot-address detector, structurally UNABLE to do role-content — (1) atomic matched-load=0.0, (2) fair-test coref-survives/role-blind, (3) role-conflict-crosstalk=slot-address. One-shot rescues EXHAUSTED; de-prioritize.** ➡️ FORWARD PATHS (brain-foundational, per framing drill): (a) build the SETTLING / constraint-satisfaction organ (Kintsch iterative relaxation) for role-content coherence, OR (b) WIRE the already-HARD_PASS ToM organ for the mentalizing-class goal-attribution subclass (no new build). (b) is the higher-value near-term win. NOTE: coref (IDENTITY) is real+vindicated+wired; the arc genuinely covers identity binding — role-content/mentalizing is the separate remaining frontier.
- **🔴🔴 (UNDER CHALLENGE, see above) AGGREGATE RESULT LANDED (2026-08-04 ~16:1XZ, commit 925897d74, exp_coherence_aggregate_discriminates_goal_outcome_v1, VET'd 5/5 seeds on disk): HARD_FAIL_GOAL_OUTCOME_DOES_NOT_EXTEND.** At the REAL route_passage aggregate grain: coref positive control FIRES 5/5 (coref_net_auto=1.0, recover_frac=1.0 — mechanism produces non-zero coherence deltas 0.56/0.96 on real whole-resolutions), but goal-outcome go_matched_adopt_rate = EXACTLY 0.0 (every item/seed), moves ONLY under register-load asymmetry (foilheavy=1.0/ownerheavy=0.0, load_range=1.0), shuffled collapses. **Aggregate decodability-coherence does NOT extend to goal-outcome.** (VET caveats, straight: coref positive control is THIN — 2/18 McGuffey passages applicable, agreement 0.5 — fires + establishes the coref-vs-go contrast but is NOT a broad reproduction of the 67%; shuffled control here is confirmatory-not-decisive since go_matched is already 0.0. The decisive evidence is go=0.0-exact + the load-flip, not the shuffle.)
- **🎯 CONSOLIDATED FINDING (both cells): the coherence SCORE splits by KIND of relation.** (1) COREF = identity binding == clustering quality -> aggregate DECODABILITY margin works (already built, atom 29609/route_passage). (2) GOAL-OUTCOME + CAUSAL = RELATIONAL role binding (goal-owner, cause) -> decodability is EXACTLY 0.0, needs a genuinely RELATIONAL-REACH quantity (reach_value / SR-backward M_backward). So the earlier "SCORE reused across 2/3" is now: **coref DONE; the OTHER TWO relational instances both route to ONE shared missing organ = the trained backward-reach signal.** This SIMPLIFIES the remaining program to a single real build. SELECT (route_passage/decide_keep_or_revert) remains sound + reusable across all 3.
- **➡️ THE REMAINING BUILD (one problem, not two): earn a REAL-TEXT-NATIVE relational backward-reach / SR-backward representation (hippocampal reverse-replay credit-assignment; Mattar-Daw need×gain) that selects the coherent relational antecedent over recency, serving BOTH goal-outcome and causal.** NOT a quick data-pipeline: text_transfer_v1 = CANNOT_BRIDGE_REPRESENTATION_GAP proved the SIM-earned M_backward (a synthetic permutation-orbit inverse) has NO path to real narrative — so Gap 1 is really "how to EARN a real-text-native backward-reach rep," a deep-earn, not a plumbing task. FORMALIZE-first: research drill DISPATCHED to spec it brain-foundationally (reuse reach_value machinery = WIRE-DON'T-ISLAND; solve the sim->text bridge text_transfer_v1 exposed; anti-recency control mandatory). Brain-fidelity note: route_passage aggregate is single-pass; iterative Kintsch/CA3 settling is NOT ruled out as an alt recovery path for relational coherence and should be weighed by the drill.
- **(superseded) earlier framing:** test every instance at aggregate grain / open question whether decodability extends to relational — ANSWERED: it does not; goal-outcome+causal need relational-reach.
- **(superseded) BIGGEST RISK (spec Section 7, tested + answered above):** `decode_coherence_margins` might ALSO fail to discriminate on goal-outcome the way it PROVABLY fails on causal (CausalLinkRegister write-then-read is symmetric — wrong link decodes at the same ~0.97 fidelity as right). If goal-outcome binding is symmetric the same way, the "SCORE reused across 2/3" claim collapses to 1/3. **First cell dispatched (hdi_exp_dev a40b50cd, local/CPU): a 3-arm goal-outcome DISCRIMINATION diagnostic — goal-outcome (does true-owner margin > wrong-owner margin?) + coref positive control (must discriminate) + causal negative control (predicted to tie).** This is both the cheapest first step (Section 6) and the make-or-break.
- **First buildable ORDER (spec Section 6):** (1) goal-outcome [harness-only, zero new hdlab code — but first the discrimination check above + harden items into a true recency-trap]; (2) coref [harness-only, re-verify the 0/4 recency-trap item set, commit e34d54701]; (3) causal [the real remaining build = Gap 1 M_backward training pipeline]. P_deflated=0.35.
- **CORRECTED PRIOR NUMBERS (disk-verified by the drill; my earlier recollection was loose):** coherence_selector insim_v2 = HARD_PASS (acc 1.0 vs recency 0.0, shuffled-control 0.27); novel_types_v3 = MIDDLE_BAND (acc 0.8733, structural-control passes but accuracy floor doesn't) — NOT the HARD_PASS I implied; bidirectional_v4 = HARD_FAIL_NO_MULTIHOP_LIFT (a real multi-hop negative; this build targets 1-hop only); insim_v1 = HARD_FAIL (coherence floor); text_transfer_v1 = CANNOT_BRIDGE_REPRESENTATION_GAP (sim-earned rule 0.8367 in-sim but ~chance 0.4286 on real text — the ENCODING breaks, not the selection logic). What transfers = the MECHANISM CLASS (a TD-trained backward signal beats recency + survives shuffled-structure control); what does NOT transfer = the v1-v4 CODE (built on a synthetic permutation-orbit rep with no path to real text).

### (superseded framing kept for context) Build the ONE COHERENCE-BASED relational binding/selection organ (glass-box VSA, select by coherence not recency), building on the v2/v3 coherence-selector arc, tested on ALL 3 instances with an anti-recency control. — Refined above into SELECT-over-instance-specific-SCORE; "settling organ" demoted to stretch-goal.

**🧠 BIOLOGY CONVERGENCE (4 lit-scans landed 2026-08-04 ~19:2XZ; full note pending at ac7204bd -> research_coherence_over_recency_selection_biology_2026-08-04.md): the coherence-selection SIGNAL = CONSTRAINT-SATISFACTION SETTLING, and this is the concrete buildable algorithm.** (1) Kintsch construction-integration (STRONGEST, ~0.75): coherence selection is a LITERAL iterated algorithm a(t+1)=norm(C·a(t)) — activation × a pairwise-connectivity matrix C, settled to a fixed point; the WELL-CONNECTED (coherent) antecedent wins, the merely-recent-but-unconnected node loses activation and is pruned. Trabasso causal-network = same (causal-connectivity, not recency). This is glass-box + directly buildable in VSA (linear settling on a coherence/connectivity matrix). (2) Neural: hippocampal RECOLLECTION (CA3 recurrent pattern-completion = attractor settling; CA1 comparator) + DMN/PMC situation-model — DISTINCT from perirhinal/AT FAMILIARITY = recency (double-dissociation, case NB). Our current selectors use the FAMILIARITY/recency signal; we need the RECOLLECTION/settling one. (3) PFC supplies a goal/context BIAS that overrides recency (CMR context-vector beats recency, formally; Anderson inhibitory suppression of competitors). (4) Alternative: Mattar-Daw need×gain / SR prioritization (utility not recency, ~0.85 spatial; ~0.35 narrative-transfer). => BUILD DIRECTION: a constraint-satisfaction SETTLING coherence selector — build the candidate-antecedent connectivity/coherence matrix (relational links to the outcome/context), settle (reuse our cleanup/attractor = Hopfield-class organ + situation_model as the connectivity substrate), pick the settled winner; recency must NOT be the tiebreak. Reuse: cleanup_family/iterative_attractor (settling), situation_model_accumulate (connectivity), predictive_coding (coherence/match signal), self_improving_loop (abstain). Confirm against the build-spec drill a4d64704 when it lands.

## 🏝️ CAPABILITY RECONCILIATION AUDIT (2026-08-04 ~17:4XZ, commit 049c0500a, notes/capability_reconciliation_invisible_islands_audit.md, all verdicts disk-VET'd by Director)
Trigger: ToM organ was HARD_PASS with 0 registry rows (invisible island). USER: "we clearly missed important things — thorough analysis?" DONE. CALIBRATED ANSWER: the scare resolves to **ONE genuine mislabel (ToM, was tagged "GAP" = actively wrong) + FIVE documented-as-islanded-in-the-MAP-but-never-registered (bookkeeping lag: the human map HAD them, the machine registry/gate lagged) + 0 NEW content doc-divergences (the map is accurate; the registry is what trails).** So we did NOT mostly forget things — the map mostly tracked them; the registry didn't. Registry=57 rows; ~2993 HARD_PASS dir-hits (noise: shards/arms/iters); 61/105 hdlab modules unregistered (most are correctly-unregistered infra or wired-but-ungated process-gaps). **5 NEW ISLANDS (disk-verified): (1) exp_grounded_appraisal_sim_earned_v1 = MECHANISM_EARNS (heldout 1.000 vs controls 0.17-0.24, revenge=1.000) — THIS IS OUR CURRENT PIVOT, islanded+unregistered; (2) VAMP-EP deep-chain solver (exp_wave14_*vamp_chain*) = DEPTH_CEILING_HIGH acc 1.0@d200 / K_STRESS_AGENT_READY K=5000 / VAMPNOISE_ROBUST 30% — SOLVES the coherence_selector 3-hop collapse, but SYNTHETIC KG chains, NL-transfer UNTESTED; (3) hdlab/action_selection.py HARD_PASS via pfc_bg_composed (att_lift 0.193, value_lift 0.733); (4) hdlab/slot_attention_wm.py (TRAPPED_SHARED, 12 consumers); (5) minor under-registrations situation_model_multibank/multi_hop/modern_hopfield_readout.** 2 are directly comprehension-relevant to our frontier (grounded_appraisal=the pivot; VAMP-EP=multi-hop). **SYSTEMIC HOLE + FIX: capability_registry_audit.py checks REGISTERED->wired but never HARD_PASS->registered, and only globs hdlab/*.py (blind to experiments/-only organs). FIX spec'd (audit Section "Systemic fix"): add scan_unregistered_hard_pass_anchors() with a DENYLIST verdict test (NOT literal "HARD_PASS" — VAMP-EP uses custom pass-strings *_ROBUST/*_READY/*_HIGH/MECHANISM_EARNS a naive grep misses) + exact path/id/name match (token-overlap gives false-registered) + mtime-cached. Building it now (hdi_testbed). Then file WIRE/SHELVE registry rows for the 5.**

## 🔬 PROVE-THE-CAPABILITY TRACK (role-content relational binding; the coherence organ)
- **Organ map (disk-verified, USER demanded clarity):** the settling-selector build = only TWO genuine reuses (situation_model_accumulate = NODES/established-role substrate, role-content-BLIND when read via cosine so it's substrate-not-coherence; decide_keep_or_revert = abstain-gated anti-recency SELECT gate, trivial) + ONE genuinely-new load-bearing piece (the role-fit connectivity/score). **iterative_attractor/cleanup_family are a FALSE FRIEND: pattern-completion (settle toward nearest STORED codebook vector by similarity = the SAME similarity/cleanliness computation that made decode_coherence_margins role-content-blind); reuse their loop SCAFFOLD only, NEVER their matrix semantics.** We do NOT own a coherence-constraint settling organ.
- **Settling-selector spec LANDED + VET'd (commit 70eb5d817, notes/research_coherence_constraint_satisfaction_settling_selector_design_spec.md): C = a SUPPLIED symbolic role-compatibility table over Trabasso roles (GOAL_OWNER/AGENT/EXPERIENCER/OUTCOME...), indexed by role-type pairs -> load- & similarity-INVARIANT by construction. Strong anti-artifact control = ROLE-TABLE SCRAMBLE (Control 2): scrambling w() MUST collapse discrimination, else it's a structural artifact.**
- **🔑 DIRECTOR VET REFINEMENT (single-pass, not settling): the Henry test is SINGLE-PASS-SOLVABLE — one application of C separates true owner from foil; iteration only adds winner-take-all which decide_keep_or_revert already does. So the MINIMAL honest mechanism = a role-compatibility SCORE (sum of w(GOAL_OWNER, role) over the entity's established roles) -> decide_keep_or_revert. Iterative settling is a DEFERRED extension (only justified by a future globally-coherent-but-locally-ambiguous case, NOT testable at N=1). Do NOT over-claim "settling built."** SCOPE: SUPPLIED role-structure (allowed) = mechanism-capacity ("substrate USES supplied role-structure to discriminate role-content"), N=1; EARNING C from text = the separate real-capability step + standing data blocker (1 clean goal passage).
- **BUILD DISPATCHED (hdi_exp_dev a039f8ae): minimal role-compatibility score -> decide_keep_or_revert; 3 mandatory controls (load-match, ROLE-TABLE-SCRAMBLE=make-or-break, anti-recency) + positive control; honest N=1 mechanism-capacity scope. HARD-PASS = picks true owner load-matched+anti-recency all seeds AND scramble collapses AND positive fires. HARD-FAIL = fails to discriminate OR scramble reproduces (artifact). Await + VET on disk.** Unifies causal-antecedent (same organ, different role table); VAMP-EP multi-hop = DISTINCT complementary organ.
- **🟡 CAPABILITY PROBE LANDED (2026-08-04 ~18:0XZ, commit d2a496473, exp_coherence_role_compat_score_selector_v1, disk-VET'd + Director-DEFLATED): HARD_PASS but THIN. Proves the DIAGNOSIS, not a shippable mechanism.** orig_margin=2.0/anti_recency=2.0/scramble=-2.0/positive=2.0, 5/5. GENUINE WIN: first artifact-free positive on role-content this session — the make-or-break ROLE-TABLE-SCRAMBLE control COLLAPSED/flipped (2.0->-2.0), so unlike the 3 prior HARD-FAILs this is genuinely using role-CONTENT, not load/position/similarity. Direction confirmed. DEFLATION (VET as hard as a negative): the mechanism is a DETERMINISTIC symbolic Python dict-lookup + sum (margin 2.0 = "2 matching roles"; `mechanism uses FHRR/vector=False` on disk; 5/5 seeds MEANINGLESS = deterministic); the role-compat table is HAND-SUPPLIED and the item HAND-BUILT to match it -> near-TAUTOLOGICAL. It does NOT: use the substrate (no VSA/glass-box organ, it's a hand-rule), earn the role-structure, or touch real text (N=1 hand-built). **So role-content is confirmed as the right SIGNAL TYPE, artifact-free — but the real capability is unbuilt.**
- **🎯 REFRAME — the real bottleneck for goal-owner attribution is NOT coherence-selection (now bounded) but SEMANTIC ROLE-LABELING of real text.** The proven mechanism needs (role-labeled propositions, role-compat table). The table is suppliable (OK). The ROLE LABELS on real-text clauses (AGENT/EXPERIENCER/GOAL...) must be EARNED by the reader = the actual comprehension step, and the spec's own risk(c) flagged upstream role-labeling as a back-door recency-confound. NEXT REAL STEP: test whether the reader can assign thematic role labels to the real Henry passage (via existing semantic_parser/role_slot_summarizer?) and whether the role-compat mechanism then works on PIPELINE-extracted (not hand-assigned) labels. Plus the standing DATA blocker (1 clean goal passage) gates any real-text capability claim. Substrate-native fidelity of the role-fit mechanism = a separate, lower-priority upgrade (polishing a mechanism whose inputs are still hand-fed).
- **🧭 PIPELINE BRAIN-FIDELITY AUDIT LANDED (2026-08-04 ~18:2XZ, notes/goal_owner_attribution_pipeline_brain_fidelity_audit.md, git-committed; both load-bearing claims disk-VET'd by Director). CLEAR PER-COMPONENT MAP of goal-owner attribution:** (1) SEGMENTATION = latent gap (masked, consumes oracle-pre-parsed now); (2) COREFERENCE = **FAITHFUL** (VET-confirmed WIRED); (3) **THEMATIC ROLE LABELING = THE LOAD-BEARING GAP, MISSING-ENTIRELY** (only real-text attempt = situation_reader._assign_roles, positional heuristic, disk-verified 0.231 acc vs oracle 1.0 vs floor 0.192; role_slot_summarizer=syntactic slots not thematic; role-compat cell=hand-supplied); (4) SITUATION-MODEL BINDING = FAITHFUL (starved by #3, not broken); (5) ROLE-FIT COHERENCE = SHAPE-mismatch (static vs settling) but SECONDARY (blocked by #3); (6) SELECTION+ABSTAIN = FAITHFUL. **3/6 faithful (2,4,6); the real frontier is narrowly #3 then #5.**
- **⚠️⚠️ PREMISE CORRECTION (disk-VET'd, propagate to MEMORY): the 'coref _pick_strict_cb = recency, falsified 0/4' premise IS WRONG.** _pick_strict_cb's PRIMARY key is most_recent_subject_clause (Centering-Cb grammatical-role history); recency (last_pos) is ONLY the tiebreak (code docstring line 229). No '0/4' artifact exists anywhere on disk. This session's OWN fair-test corroborates (coref SURVIVED load-matching, net_auto=2.0). => COREF WAS NEVER BROKEN. The 'ONE coherence selector serves 3 instances (coref/causal/goal-outcome)' framing was OVERSTATED — coref is faithful and out; the real instances (causal, goal-outcome) are both blocked by the SAME upstream gap = thematic-role labeling.
- **➡️ THE REAL TARGET (singular, clear): BUILD a brain-faithful THEMATIC-ROLE LABELER (Component 3) — MacWhinney competition-model: verb SELECTIONAL-FRAME table (suppliable knowledge) + multi-cue INTEGRATION (voice, animacy via existing animacy_lexicon.py, dependency arcs via candidate_generator.py) EARNED/native (NOT a hand-rule like the role-compat dict), tested for GENERALIZATION across active/passive/ditransitive/experiencer-subject (the exact test a positional heuristic FAILS and the brain PASSES).** No island to wire (checked HIGH-tier list — none). Genuine BUILD. FORMALIZE-first: build-spec dispatched. Component 5 (coherence settling) comes AFTER #3 (sequencing: don't build a coherence mechanism with no real role-labeled input to feed it).
- **✅ AUDIT LOOP: invisible-island DETECTOR validated + committed (984b784d5): HIGH-tier alarm=56 (triageable, disk-verified contains all 5 islands: sally_anne/vamp_chain/grounded_appraisal), does NOT false-flag registered, raw 3042 informational-only, existing behavior byte-identical, self-test passes. Blind spot that hid ToM is now a session-start alarm. FOLLOW-UP (mechanical, pending): file WIRE/SHELVE registry rows for the 5 islands (skunkworks A5-gated).

## 🧪 ROLE-LABELER BUILD (Component 3 = the load-bearing gap) — IN PROGRESS
- **Spec (cfbef9e68): MacWhinney competition-model averaged-perceptron cue-integration (reuse exp_path1_srl_mwp_cpu_v1::_train_role loop). SUPPLIED (knowledge, OK) = verb selectional-frame dict + animacy_lexicon.py + new passive detector; EARNED (deliverable) = cue-validity integration weights. Disk-proof integration>substitution: nsubj hard-override = CLEAN_NEGATIVE net_fix=-182 (spaCy parser noise on 19c prose). Reuse animacy_lexicon (has scrambled_lookup_factory control) + candidate_generator (arc cues) + event_bundle (configurable roles). Replaces situation_reader._assign_roles (positional, 0.231).**
- **GROUND-TRUTH AUDIT LANDED (research a68364d7, notes/research_thematic_role_gold_provenance_audit.md, read-only): verdict TRUSTWORTHY_BUT_THIN_ON_PASSIVE.** Gold (data/gold_mcguffey_lccp_argstruct_v1.json) = single-annotator, verified NON-positional + independent of reader output => NOT CIRCULAR (the killer risk ruled out); spot-check 30/30 correct; honest baseline = 0.592 F1 (gate1_mcguffey); the '0.64 atom 29502' citation = CONFIRMED PHANTOM. **DATA WALL CONFIRMED (not hypothesized): ZERO passive pos-items in gold (~8 passives exist in raw text, none labeled) -> auto-stratify passive = N=0, MUST hand-author passive supplement.**
- **🎯 STRATEGIC REFINEMENT (Director): prioritize the EXPERIENCER-SUBJECT / PSYCH-VERB axis over passive.** Goal-owner attribution = who WANTS/INTENDS/FEARS -> psych/desiderative verbs (want/fear/like/love/hope/know), subject = EXPERIENCER/goal-holder NOT AGENT; positional heuristic mislabels the wanter as AGENT = the failure that actually breaks goal ownership, AND these verbs are COMMON in 19c narrative (unlike passive). So the PRIMARY generalization axis = experiencer-subject (auto-stratify existing gold for it first -> may yield REAL held-out data, unlike passive N=0); passive = secondary classic falsifier via hand-authored supplement. Build agent (a3e058a9) redirected accordingly + rigor guards (author supplements BLIND/naturalistic, train on canonical gold ONLY, per-axis held-out N reported, small-N=MIDDLE).

- **🔴 ADVERSARIAL RE-VET REVERSED THE HARD_PASS (skunkworks a3c2a295, opus, commit a52b458c4, notes/skunkworks_reVET_thematic_role_labeler_cue_integration_v1.md): DOWNGRADE HARD_PASS -> MIDDLE_BAND; WIRE decision = SHELVE. Reproduced the 0.8667 headline exactly (harness faithful) — but the aggregate MASKS the axis that matters.** DECISIVE: the goal-owner EXPERIENCER/psych axis = 0.614 (n=14; goal-owner-subj-only 0.657), while aggregate 0.867 is carried by 41 easy DITRANSITIVES at 0.951 (46% of test). **The EARNED perceptron is NET-HARMFUL on the experiencer axis: frame_only (SUPPLIED verb-frame lookup)=0.857 vs full earned=0.614 (-0.24); the perceptron learned `order:pre->AGENT` from canonical-dominated train and OVERRIDES the correct frame signal for pre-verbal experiencer subjects.** So the goal-owner capability is delivered by SUPPLIED knowledge, and the EARNED mechanism DEGRADES it. Also: 92% (58/63) test examples have an exact feature-vector DUPLICATE in train (the 0.95 ditrans = near-memorization, not generalization); resolve_rate 0.62 -> end-to-end ~0.54 agg / ~0.43 goal-owner; OOV psych verbs (cherish/loathe/crave/covet) all -> DEFAULT_FRAME -> AGENT (no frame learning); incidental bug lemma_verb('likes')->'lik'. **DO NOT WIRE.**
- **🎯 LESSON + REFRAME (connects to USER's OOV question): (1) a flat averaged-perceptron cue-integration is the WRONG learner here — trained on canonical-heavy data it learns position-dominates and HURTS the psych-verb axis. (2) The SUPPLIED verb-frame lookup ALREADY gets 0.857 in-vocab experiencer — the KNOWLEDGE is right; the gaps are OOV + the earned integration corrupting it. (3) THE REAL LEARNING NEED IS NOT CUE-INTEGRATION (it hurt) — IT IS OOV VERB-FRAME ACQUISITION (syntactic bootstrapping, Gleitman): infer a NOVEL verb's frame from the construction it appears in + ACCUMULATE it (ties to the project's self-extension/MINT engine). USER's OOV question surfaced the actual mechanism gap.** NEXT (revival criteria, re-VET note): earned must BEAT frame_only ON THE EXPERIENCER AXIS or be dropped; add OOV frame-induction; finer features to kill the 92% memorization; construction-BALANCED experiencer data; ALWAYS eval PER-AXIS (goal-owner=experiencer), never aggregate. STRATEGIC: consider the labeler = supplied frame as primary cue for psych verbs + OOV bootstrapping as the earned part, NOT a flat cue-competition perceptron.

- **🟡 FRAME-INDUCTION MECHANISM DEMONSTRATED (2026-08-04, commit 893a6fa88, exp_frame_induction_oov_psych_v1, disk-VET'd; strategic tier MIDDLE per small-N/templated, pre-reg bands=HARD_PASS): the OOV-learning component (problem 3b) WORKS via config-only learner reuse.** hdlab/frame_induction.py (feature-encoder only) + hdlab/learner registry.learn/apply (MDL selected ruleind plugin) — ZERO edits to learner core/registry/plugins OR thematic_role_labeler.py (config-only-expand HELD, git-verified). Held-out NOVEL verbs (cherish/loathe/crave/covet, OOV, lemma-split, no leak): experiencer-axis 0.667 vs default-AGENT 0.0 vs position-majority 0.0; scramble collapses 0.667->0.333; agentive sanity 1.0. BRAIN-FAITHFUL = Gleitman syntactic bootstrapping (novel verb's frame from its construction: scomp/degree->EXPERIENCER; bare->AGENT is the honest 2/3 ceiling, uninformative construction = no signal). This is the MISSING-COMPONENT fix (labeler was shelved for lacking OOV-frame-learning -> reused/expanded hdlab/learner). **DEFLATION: corpus is TEMPLATED (agent-authored construction distributions) -> MECHANISM DEMO, NOT real-corpus capability; N=4 held-out verbs. Real-data re-run pending the experiencer dataset (ad122d42).** NOT WIRED (premature: unvalidated on real data + 3c would override it).
- **NEXT (sequence): (1) triple-check the experiencer dataset (ad122d42) on landing -> re-run frame-induction on REAL non-templated data; (2) 3c downstream cue-conflict fix (induced/supplied confident frame must NOT be overridden by position prior — re-VET criterion #1); (3) assemble+VET Component-3 end-to-end per-axis, honest end-to-end w/ resolve_rate; (4) WIRE Component-3 into reader (feeds situation_model); (5) Component-5 goal-owner selection over real role labels. Data-sparsity is the recurring gate.**

- **📚 EXPERIENCER DATASET LANDED + DIRECTOR-TRIPLE-CHECKED (commit 9c83a7012, experiments/data/experiencer_narrative_roles_v1.jsonl): PASS, trustworthy, usable.** 118 sentences (100 verbatim litbank from 100 novels + 18 flagged naturalistic supplements), 67 distinct psych verbs (36 subj-exp / 31 obj-exp), VerbNet+WordNet-seeded (on-disk, no download). TRIPLE-CHECK (Director read ~21 labels on disk): structure/schema/counts clean + not padded; lemma-split ZERO train/heldout leak (10 held-out: cherish/crave/dread/loathe/yearn + astonish/embarrass/gladden/horrify/terrify, >=2 sent each); LABELS correct + NON-CIRCULAR — the experiencer-OBJECT crux is labeled right ("it frightened ME" -> it=STIMULUS/me=EXPERIENCER; positional heuristic would fail these). Agent's own verify caught+fixed 2 errors (30/30 post-fix). CAVEAT: breadth-first, SHALLOW (1-3 sent/verb; ~25 held-out instances) = capability numbers from it are REAL but LOW-POWER; miner + raw candidate pool (notes/_mined_psych_candidates_raw.json) saved for cheap deepening before any high-power claim. Builder: experiments/build_experiencer_narrative_roles_v1.py + mine_experiencer_sentences_v1.py.
- **🟡 FRAME-INDUCTION REAL-DATA RE-RUN LANDED (commit c7e503373, exp_frame_induction_oov_psych_real_v1, disk-VET'd per-axis): MIDDLE_BAND — DRILLED.** Held-out NOVEL verbs: subj-exp acc 0.833, obj-exp 0.455; BEATS default-AGENT (0.0) both axes; scramble collapses (0.65->0.35, delta 0.30 = learns REAL structure); but does NOT beat position-majority (subj 1.0 / obj 0.545). GLASS-BOX rules show WHY: dominant induced rule = `[order_pre, arg_animate]->EXPERIENCER` (cov 47) = it COLLAPSED to a position+animacy proxy on sparse real data (the pre-registered biggest-risk, same family as the shelved perceptron). FAIRNESS AUDIT: position winning subj=1.0 is near-TAUTOLOGICAL (subj-exp verbs -> subject IS experiencer by definition); the real discriminating case = OBJECT-experiencer, and there BOTH are ~chance (0.455/0.545) = data-starved. MISSING-COMPONENT check: NOT a missing mechanism (has animacy+construction); it's DATA-STARVED on obj-exp + eval-fairness-skewed. 
- **🧠 BRAIN-FOUNDATIONAL ANSWER (USER asked): role assignment is FRAME-based (verb-class-conditioned: desiderative verb->subject=experiencer/goal-holder), NOT positional (agrammatic-aphasia: position fails exactly where frame/position diverge). Position+animacy is a PROXY that AGREES with the frame for the COMMON goal-relevant case (subject-experiencer: want/crave/dread) and DIVERGES only on object-experiencer (affect-reaction, NOT goal-holding). So for GOAL-OWNER the subject-experiencer frame is the whole game, and we HAVE it (supplied 0.857 in-vocab + induced ~0.83 OOV, both beating default-AGENT). The induction's position-collapse = data-starvation, not wrong mechanism.**
- **✅ DECISION (Director, USER-endorsed direction): SCOPE TO GOAL-OWNER, do NOT chase object-experiencer now (separate deferred AFFECT competency; miner saved for cheap deepening). 3c = FRAME-PRIMARY, VERB-CLASS-CONDITIONED role assignment (the brain's way): psych/desiderative verb -> subject=EXPERIENCER, frame WINS, position never overrides a frame-determined role. This fixes the shelved perceptron's override (frame_only 0.857 > perceptron 0.614). BUILD DISPATCHED (hdi_exp_dev a9b2ef59): must recover >=0.80 subj-experiencer, beat the 0.614 perceptron + 0.0 default, frame-ablation collapses, no position-override. If passes -> Component-3 assemblable end-to-end. NOTE: USER banned the AskUserQuestion widget (answer forks in PROSE + recommendation + brain-foundational).**

- **🟢 3c FRAME-PRIMARY ROLE ASSIGNER LANDED (commit 7d41ca28d, exp_frame_primary_role_assigner_v1, hdlab/frame_induction.py::frame_primary_role, HARD_PASS, Director-VET'd per-record; independent skunkworks re-VET a35df437 IN FLIGHT before WIRE).** GENUINE ARCHITECTURAL FIX: verb-class-conditioned assignment — psych/desiderative verb -> SUBJECT=EXPERIENCER (goal-holder), the frame WINS, fixing the shelved perceptron's blanket position->AGENT. subj-experiencer axis 0.877 (57/65); frame-ablation->0.0 (frame does the work); no-override 1.0. **HONEST VET CAVEATS (as hard as a negative): (1) aggregate INFLATED by trivial known-lemma 1.0 (N=34 deterministic dict lookup); EARNED = OOV 0.767 (N=30). (2) The OOV 'earned' piece is STILL a position+animacy proxy — induced rule = [order_pre, arg_animate]->EXPERIENCER; the 7 OOV misses lack arg_animate ('her heart craved'->AGENT). Brain-COMPATIBLE (now correctly psych-conditioned) but a COARSE proxy, not deep frame semantics. (3) no-override control near-VACUOUS on subj axis (subjects are pre-verbal -> position+frame agree; genuine override = obj-exp, deferred). (4) 0.614 baseline is CROSS-DATASET (old McGuffey n=14), not head-to-head. (5) obj-exp deferred 0.396; N=30 OOV modest.** So: the FIX is real (psych subjects labeled goal-holders, decisively beats perceptron behavior) but the earned OOV number is proxy-driven + modest. WIRE decision HELD pending re-VET. If it holds -> WIRE Component-3 (subject-experiencer path, caveats documented) -> Component-5.
  - **✅ INDEPENDENT SKUNKWORKS RE-VET DONE (commit fec848ef8, notes/skunkworks_reVET_frame_primary_role_assigner_v1.md, fresh re-run reproduced exactly): DOWNGRADE HARD_PASS -> MIDDLE_BAND, but WIRE THE ARCHITECTURE.** Confirmed all 4 caveats + the NUMBER THE CELL NEVER REPORTED: same-data frame_only baseline = 0.523 -> the honest lift is 0.523->0.877 (NOT "beats 0.614" which was cross-dataset old-McGuffey n=14). OOV is CONCLUSIVELY a one-feature animacy heuristic (arg_animate->EXPERIENCER; 30/30 OOV cases arg_animate==correct; scomp/degree/passive inert) — NOT construction-frame semantics. Leak CLEAN. ARCHITECTURAL FIX = REAL (known verbs get frame unconditionally, removes the perceptron's position->AGENT override; known 1.0 vs perceptron 0.614). **DECISION: WIRE Component-3 frame-primary architecture at MIDDLE_BAND with CORRECTED numbers (known-verb subj=1.0 dict / OOV subj=0.767 COARSE ANIMACY HEURISTIC not "learned" / obj-exp=0.396 OPEN GAP / same-data frame_only baseline=0.523). Do NOT bank 0.877 as a capability figure. REVIVAL-to-HARD_PASS: (1) solve object-experiencer, (2) show a cue BEYOND animacy is load-bearing, (3) report same-data baseline.** WIRING + END-TO-END ASSEMBLY dispatched (real parser + honest resolve_rate, not gold-parsed).
  - **🎉 COMPONENT-3 WIRED (commit 8db4876eb, exp_frame_primary_role_assigner_endtoend_v1, disk-VET'd): the load-bearing gap that started this arc is CLOSED (at MIDDLE, honestly).** frame_primary_role wired into hdlab/situation_reader.py as ADDITIVE/REVERSIBLE (new optional EventRecord fields subj_role/obj_role; _assign_roles untouched; caught+fixed a real bug — Event.lemma is a lowercased surface form, needed lemma_verb() re-lemmatization). CONSERVATIVE production wire: only the deterministic KNOWN-verb VERB_FRAMES path runs (known psych verb subject -> EXPERIENCER); OOV psych verbs stay AGENT (the coarse animacy heuristic is NOT shipped — correct: shipping 'animate->EXPERIENCER' would mislabel agentive verbs too). END-TO-END honest number (REAL parser, resolve_rate included): resolve_rate=0.80, on-resolved 0.96, EFFECTIVE end-to-end=0.769 (verdict MEASURED_HONEST_NUMBER); known-resolved 1.0, OOV-resolved 0.92. NO REGRESSION (situation_reader ALL SELF-TESTS PASS). Registered frame_primary_role_assigner_v1 @ MIDDLE_BAND WIRE (58 registry rows; note: registry is git-IGNORED per repo policy = local-disk queryable, not git-tracked). Known bounded limit: novel psych verbs (outside VERB_FRAMES) -> AGENT until a psych-verb detector exists (revival criterion #2). **PIPELINE STATUS: Components 1(latent)/2(coref-faithful)/3(NOW WIRED)/4(situation-binding-faithful)/6(select-faithful). NEXT = Component-5 goal-owner SELECTION.**
  - **➡️ NEXT: Component-5 gold-role-isolated eval (design notes/research_component5_goal_owner_selection_binding_2026-08-04.md) — the make-or-break density-gating check: does route_passage/decode_coherence_margins do goal-owner+outcome binding on GOLD-role-labeled fb5b2a188 items, BEATING recency 0.333? RISK: decode_coherence_margins was proven role-content-BLIND — gold role labels may NOT rescue it if the blindness is structural. This eval settles it BEFORE wiring Component-5.**
  - **🔴 COMPONENT-5 GOLD-ROLE EVAL LANDED (commit 60aa9f060, exp_component5_gold_role_isolated_v1, disk-VET'd): HARD_FAIL — role-content-blindness RECURS at Component-5, EXACTLY as flagged. But the diagnosis is RAZOR-SHARP + the fix is well-scoped.** outcome_binding_acc=0.333=recency (no lift); BUT goal_owner_binding_acc=1.0 (the candidate-picker CORRECTLY identifies the goal-owner, content_owner='amy' vs recency foil 'jo'). The break: the ADOPTION GATE (decide_keep_or_revert) is fed decode_coherence_margins, which returns EXACTLY 0.0 (structurally role-content-blind: per-position write-fidelity, symmetric between entities when events don't collide -- same signature as CausalLinkRegister) -> gate abstains -> falls back to recency -> wrong. **THE CORRECT ANSWER IS COMPUTED BUT NEVER ADOPTED because the SCORE is blind.** (role-scramble was VACUOUS -- nothing adopted, so nothing to break.)
  - **➡️ FIX DISPATCHED (hdi_exp_dev a0ef760a): replace decode_coherence_margins in the C5 selection path with a DIRECTED GOAL->OUTCOME relational-coherence SCORE (Zwaan intentionality: outcome coheres with the goal-HOLDER), fed to decide_keep_or_revert. Reuse GoalOutcomeRegister's directed structure (fires 0.833) + the candidate-picker's goal-presence signal (already 1.0). MAKE-OR-BREAK = a NON-VACUOUS role-scramble (must ADOPT goal-holder on intact + FLIP to foil on scrambled = proves it USES role content, not a tautology/hand-rule). This is the SAME missing organ CLASS as the causal instance (Instance B, relational-reach) but simpler for goal-outcome (directed goal-presence). If the non-vacuous scramble reveals a tautology or it still can't adopt -> Component-5 needs the full relational-reach organ, drill brain-foundationally.**
  - **🎉🎉 COMPONENT-5 FIX PASSED (commit 6911a28a6, exp_component5_gold_role_isolated_v1, disk-VET'd): directed goal->outcome relational score WORKS. THE GOAL-OWNER PIPELINE IS MECHANISM-COMPLETE.** intact outcome-binding 1.0 (from 0.333, beats recency); NON-VACUOUS scramble (role_scramble_collapse_vacuous=False -> on scramble it ACTIVELY adopts the FOIL, following the moved GOAL label = genuinely uses role-content, NOT a tautology); anti-recency holds; diag_route_passage_still_blind=True (decode_coherence_margins STILL 0.0 -> the lift is causally the new score). NO hdlab organ modified (pure reuse: GoalOutcomeRegister + decide_keep_or_revert fed the RIGHT signal). Capped MIDDLE for N=3. **PIPELINE STATUS: all 6 components have a working brain-faithful VET'd mechanism — coref(faithful), role-labeling(3, WIRED, MIDDLE, e2e 0.769), situation-binding(faithful), goal-owner-selection(5, mechanism-validated on GOLD roles, MIDDLE N=3, non-vacuous), select+abstain(faithful), segmentation(latent).** HONEST REMAINDER (integration+scale, NOT new mechanisms): (1) WIRE Component-5; (2) FULL end-to-end (C3 real-text role labels -> C5 selection, honest compounded number); (3) DEEPEN N (C5 bank N=3; source goal->outcome narrative data for a high-power number, like the experiencer dataset); (4) deferred: obj-exp affect axis, OOV psych-verb detector (C3 revival crit). The SESSION ARC (coherence-selector gap -> bounded dead end -> corrected coref premise -> real gap=role-labeling -> built+wired C3 -> C5 selection via directed relational score) is COMPLETE at the mechanism level.
  - **🎉 END-TO-END WIRED (commit 549ca42da, exp_component5_wired_endtoend_v1, disk-VET'd): C3 REAL role labels -> C5 selection HOLDS.** C5 score PROMOTED to hdlab/goal_owner_select.py (was islanded in the cell; imports clean, self-test PASS, registered goal_owner_select_component5_directed_score @ WIRED_AND_PIPELINE_USED). real-C3-role end-to-end = 1.0, gold-vs-real DELTA = 0.0 (C3's real labels carry the goal-owner signal C5 needs, on these items), beats recency 0.333, non-vacuous scramble holds, NO regression (situation_reader 4 self-tests + self_improving_loop smoke pass). MIDDLE for N=3. **HONEST LIMITS (disclosed, VET as hard as positive): (1) small-N=3 traps (clean psych verbs want/long/wish); broader 6-item bank = 5/6 coverage ('meant' is OOV -> no GOAL). (2) ⚠️ CORRECTED (Director over-flag, 2026-08-05, read the code): the 'a long while' false-positive is a CELL-PROXY ARTIFACT, NOT a production missing-component. The end-to-end CELL's c3_has_desire() SCANS ALL TOKENS for a psych-lemma (declared scope proxy — the bank lacks the temporal-tagger to find predicate positions). But PRODUCTION (situation_reader._assign_frame_primary_roles) applies frame_primary_role ONLY to the token at pred_idx = the event-extractor-identified PREDICATE — so an adjective 'long' is never a predicate and never reaches the frame lookup. Production is POS-informed via the event-extractor. So NO POS-gate fix is needed for production; residual risk = event-extractor mis-identifying a psych-lemma non-predicate as a predicate (a smaller, separate event-extraction-precision concern, not a frame-lookup gap). This corrects the prior 'missing POS-gate component' flag.** NEXT (honest remainder): (a) DEEPEN N — source a goal->outcome narrative corpus for a high-power end-to-end number; (b) C3 PRECISION — POS/sense-gate the frame lookup (fixes the 'long' false-positive) + OOV coverage (the missing psych-verb detector / revival crit). Deferred: obj-exp affect axis.
  - **📈 DEEPEN-N LANDED (commit 469cc2ac0, exp_component5_wired_endtoend_v1 @ N=23, disk-VET'd + Director-triple-checked the items): end-to-end 1.0 held from N=3->N=23; recency baseline recomputed 0.0435; NON-VACUOUS scramble solid (scrambled=0.0, vacuous=False across 22 traps); anti-recency holds. Capped MIDDLE (authored). Agent rigorously root-caused a mid-run 0.87 to LEXICON COLLISIONS in its own authored items ('finish'/'down' firing spurious goal/outcome events -- same lexical-ambiguity class as 'long') + fixed+rescanned.** DIRECTOR VET (triple-checked items on disk -- SHARPENS the claim): all 23 are ONE TEMPLATE ('[Owner] wanted/longed/wished to [goal]... [Foil] ran/hurried ahead... she [bad outcome]'), 23 lexical variations of a single anti-recency structure -> NOT 23 independent tests (robust across lexical variation, NOT across diverse goal-outcome structures); recency fails BY CONSTRUCTION (margin partly baked in); items TUNED to avoid the gold-typing lexicon's collisions; ZERO mined. **HONEST READ: a real strengthening of the MECHANISM evidence (directed-score selection robustly does anti-recency goal-owner binding on clean psych-verb items, genuinely content-driven per the solid non-vacuous scramble) -- NOT a diverse real-corpus capability number.** 
  - **🔎 CRON-CYCLE CODE-READ FINDING (2026-08-05): outcome-event TYPING is a SUPPLIED LEXICON (V2_DESIRE/V2_OUTCOME_*, declared 'supplied goal-schema knowledge' = ALLOWED per invariants), NOT a missing organ. The production reader also has a real causal-network reader (causal_net_cause) for cause->outcome. So the recurring over-firing ('down'/'finish'/'long') is a LEXICON-PRECISION issue (fires on ANY sentence token, not the predicate -- same surface-scan class as the 'long' cell-proxy), a BOUNDED fix (predicate-gate the lexicon membership, mirroring C3's predicate-gating), NOT a missing-component build. Ruled out before mis-building an 'outcome detector'. NEXT DRIVE = MINE diverse real goal->outcome items (aacfeafe) for a real-corpus number (the authored template x23 can't give it).**

  - 
  - **🔴 MINING FINDING (2026-08-05, commit f2cb51d40, honest NEGATIVE, drilled): automated surface-mining of goal->outcome->foil from litbank is TOO NOISY for a real-corpus number. 25816 raw candidates -> 40 diverse-selected, but human-verified gold = 22.5% owner-correct (9/40), 0/40 genuine foils. Eval correctly NOT run (would measure miner errors, not the mechanism). ROOT CAUSE (same surface-lemma-no-syntax issue recurring all session): the miner has NO POS/dependency parsing -> false-fires on nominal/adjectival goal-verb uses ('a long face','never fear!') + grabs first pronoun/name. Fix isn't a better heuristic -- goal-owner ID REQUIRES syntax, which C3 ALREADY has (miner islanded a bad owner-extractor instead of reusing C3). Also: eval resolvers hardcoded to the 12-name authored cast (eval-cell limit, not production coref). **CONCLUSION -- FRONTIER FULLY MAPPED: a REAL-CORPUS goal-owner number is blocked on (1) TRUSTWORTHY DIVERSE GOLD (human annotation or parser-based non-circular labeling -- surface-mining insufficient) + (2) generalize eval resolvers off the toy cast. Neither is a quick autonomous win. The MECHANISM is proven (wired, robust); scaling to real-corpus is an ANNOTATION+ENGINEERING phase.**

  - 
  - **✅ RESOLVER GENERALIZED / TOY-CAST BLOCKER REMOVED (2026-08-05, commit c761307fc, disk-VET'd): the C5 eval can now run on ARBITRARY names, not the hardcoded 12-name cast.** Reused PRODUCTION gender machinery (hdlab/state_of_mind.py PRONOUN_SCOPE + infer_nominal_gender, verbatim read-only) + a per-item roster(name->gender), replacing hardcoded GENDER/ANIMATE_NAMES/PRON_F/PRON_M (WIRE-DON'T-ISLAND). VET: real-name smoke [SELFTEST PASS 4/4] (Elizabeth/Darcy, Priya/Marco end-to-end matches_gold=True); NO-REGRESSION (authored bank byte-identical 1.0). HONEST partial reuse: reused the GENDER mechanism, NOT the full coreference_resolver mention-stream (that needs pre-segmented gold spans raw text lacks -- documented). ENABLER not a number. **ENGINEERING blocker to real-text eval REMOVED; the DATA blocker (trustworthy diverse annotated items) remains -- that's the annotation phase.**

  - 
  - **🔬 C3-SYNTAX MINE + FIRST REAL-TEXT ATTEMPT (2026-08-05, commit 2690d6f7f, disk-VET'd, honest NEGATIVE, precisely localized): the mechanism is proven but the EVAL/MINING PATH uses ISLANDED toy sub-components; each real-text attempt exposes another unwired organ.** (1) C3-SYNTAX owner-ID on real litbank = 0.375 (agent-verified 15/40) vs surface 0.225 = +0.15 (syntax helps) but <0.6; 5 named root causes = MOSTLY UNWIRED EXISTING ORGANS: is_passive_clause EXISTS but unwired (passive subjects=PATIENT mislabeled), animacy_lexicon EXISTS but unwired ('New York' as goal-holder), + build_roster admits junk tokens (Her/King/MAN as proper nouns, ~13/25 fails). (2) 🔴 THE IMMEDIATE BLOCKER: goal/outcome TYPING lexicon V2_DESIRE is NARROW (only want/wish/hope/long/try/plead, authored-template-tuned) -> typing_fire_rate=0.067 on real psych verbs -> C5 UNTESTED/VACUOUS (n_typed=1, binding 0.0). C3's frame_primary/PSYCH_VERBS ALREADY covers the real verbs -> WIRE-DON'T-ISLAND fix = type goals via C3 frames, not V2_DESIRE. **META: getting a real-text number = a multi-organ EVAL-PATH RE-INTEGRATION (typing via frames, owner-ID via passive+animacy gates, resolution via coref [done]) -- a real project, not one cron fix. Mechanism proven; real-text eval INFRA needs building. NEXT drive = wire goal-TYPING to C3 frame coverage -> unblock the first real C5 number on the 15 owner-correct items.**

  - 
  - **🧭🔑 REAL-PROSE REFRAME (2026-08-05, commit f7509297c, disk-VET'd -- a session-recontextualizing finding): after the goal-typing WIRE-DON'T-ISLAND fix (fire-rate 0.067->0.733 via C3 frames), C5 finally RAN on real litbank text -- and candidate_divergence_rate=0.0: the recency-candidate and goal-holder-candidate NEVER DISAGREED, so C5's selection decision NEVER AROSE (verdict SELECTION_MECHANISM_UNTESTED_NO_CANDIDATE_DIVERGENCE). outcome_binding=0.636 = UPSTREAM (owner-ID+typing) accuracy alone, NOT C5.** WHY (brain-foundational): C5 (and the whole coherence-selector arc) solves the AMBIGUOUS case (a bare 'she' that could be the recency-foil OR the goal-holder); the AUTHORED TEMPLATE deliberately manufactured that ambiguity, but REAL NARRATIVE NAMES ITS SUBJECTS EXPLICITLY ('Elizabeth was miserable', not 'she') -> recency and goal-holder resolve identically -> no decision for C5. **THE REFRAME: C5's anti-recency selection is a CORRECT but EDGE-CASE mechanism (rare in real prose); the WORKHORSE of real-prose goal-owner = UPSTREAM (C3 role-labeling + coref + explicit naming). The real-prose BOTTLENECK is C3 OWNER-ID accuracy (0.375 real-text, limited by UNWIRED passive+animacy gates) -- NOT C5 selection. LEVERAGE = wire is_passive_clause + animacy_lexicon into C3 real-text owner-ID.** (C5 not wasted -- validated for the ambiguous case that does occasionally occur.)

  - 
  - **✅ C3 REAL-TEXT OWNER-ID IMPROVED 0.375 -> 0.816 (2026-08-05, commit c90173b48, disk-VET'd, the reframed LEVERAGE pulled): wired 3 fixes into the C3-syntax miner (WIRE-DON'T-ISLAND + a real bug-fix).** DOMINANT contributor = ROSTER BUG-FIX (the surface miner admitted possessive-pronoun/title/ALLCAPS tokens Her/His/King/MAN/Mistress/Father as pseudo proper-noun entities -> 24/40 old items had junk-token owners; fix drops to 0/38). Smaller: is_passive_clause gate (passive subj != experiencer) + animacy_lexicon at the owner-decision (caught 1 inanimate). Regenerated jsonl = 38 items, 0 junk owners, VET'd by reading all 38. EXEMPLARY VET: agent REVERSED a 4th animacy roster-gate after a probe showed it over-rejects real names that are common-noun homographs (Elizabeth/Anne/Grace/Hope) -- exactly the collision animacy_lexicon's docstring warns about. Honest caveat: not the same 40 re-judged (roster fix removes junk items upstream = the intended effect). RESIDUALS (need DEEPER organs, flagged not fixed): place-name metonym subjects (York/England -- animacy can't reject PROPN -> needs NER/gazetteer), WSD (saw=cut vs see), predicate-adjective POS mistag, vocative-as-subject. **STATE: real-text goal-owner pipeline now = C3 owner-ID ~0.82 + typing(fixed via C3 frames) + C5(edge-case, rarely fires); real-prose accuracy bottlenecked by owner-ID (now 0.82) x typing coverage. Next wire-don't-island candidate: NER/place-gazetteer for the metonym residual (check if owned).**

  - 
  - **📊 FIRST HONEST REAL-PROSE GOAL-OWNER NUMBER (2026-08-05, commit dfabbde26, disk-VET'd, MEASURED not pass/fail): ~0.32 end-to-end (12/38 real litbank items FULLY correct) with a clean FAILURE DECOMPOSITION: TYPING_MISS=14 (DOMINANT), OWNER_ID_ERROR=7, BINDING_ERROR=5, CORRECT=12.** Stage numbers: owner-correct+typed binding 0.706 vs recency 0.647; full-pipeline 0.571 vs 0.524 (small-N 17-21 typed). **REFRAME NUANCED (partially overturned, honest): C5 selection DOES fire on the larger set (divergence 0.06-0.10, selection_exercised=True) and WORKS when it fires (Secret Garden: correctly adopted mary=gold over recency 'sahib'). So C5 is OCCASIONALLY-needed (~10%), NOT purely edge-case -- earlier 'edge-case' read was too strong. Caveat (disclosed): some new divergence came from a GENDER_PATCH improving coref, not pure N.** **LEVERAGE MOVED: with owner-ID fixed (0.82), the DOMINANT bottleneck is now OUTCOME-EVENT TYPING (14 misses -- the narrow ACHIEVE/BLOCK lexicon under-fires on real text; C3 is a role-labeler not an outcome-valence classifier). NEXT lever = outcome-event typing (wire production causal_net_cause reader, or a broader outcome detector, instead of the lexicon). The 5 binding errors are upstream COREF, not C5.**

  - 
  - **🔎 CRON READ-THE-CODE (2026-08-05): the OUTCOME-TYPING lever (dominant bottleneck, 14 misses) = A BUILD, not a wire-don't-island quick fix.** causal_net_cause is CONNECTIVE-DEPENDENT (links cause->outcome only on 'X because Y' sentences; situation_reader.py:453) but narrative goal->outcome is usually CONNECTIVE-FREE ('She wanted X. She failed.') -> does NOT fit. Options: (a) broaden ACHIEVE/BLOCK lexicon = BRITTLE (the recurring over/under-fire whack-a-mole; violates brain-foundational rule); (b) BRAIN-FOUNDATIONAL = a real OUTCOME-VALENCE DETECTOR (does this event RESOLVE the goal, achieved/blocked? = a goal-event RELEVANCE check, Zwaan intentionality dim; possibly reuse the directed_goal_outcome_score for DETECTION not just selection) = a genuine BUILD. **META: the WIRE-DON'T-ISLAND phase is largely DONE (owner-ID, goal-typing-via-frames, resolver-generalization all landed); the remaining levers (outcome-valence detector, NER/gazetteer for place-metonyms, WSD) are genuine BUILDS, better scoped with focus than brittle 3am tunes. The pipeline is MEASURED (0.32 real-prose end-to-end, decomposed) and every gain VET'd+committed.**

  - 
  - **📐 OUTCOME-DETECTOR SPEC (2026-08-05, commit 4f417f920, notes/research_outcome_valence_detector_design_2026-08-05.md, disk-VET'd): the dominant lever formalized brain-foundationally; my reuse hypothesis CORRECTED by read-the-code.** FORMALIZE found: directed_goal_outcome_score(role_seq,cluster_ids,seed,OUTCOME_POS) takes the outcome position as INPUT -> it CONSUMES already-typed data, CANNOT type raw text (reuse-for-detection = NO). So outcome-detection is a genuinely MISSING UPSTREAM STAGE. BRAIN-FAITHFUL MECHANISM (mostly reuse, NOT a bigger lexicon): DETECTION = goal-object RELEVANCE (reuse C3 frames + coreference_resolver -- does the event involve the goal's object/state?) + VALENCE = NEGATION-SCOPE POLARITY (small closed class ~12 negation/~7 resultative + structural negation-scope, achieved-vs-blocked). UNIFIES with C5 (feeds GoalOutcomeRegister unchanged). Pre-reg: beat lexicon 0.55 (detection>=0.55/valence>=0.65) + scramble-ablation>=0.20 drop. P_deflated=0.35. **BIGGEST RISK (the genuinely hard core, flagged): goal-object relevance via shared head-noun/verb FAILS ON PARAPHRASE ('wanted freedom'->'slipped past the guards' = no lexical anchor) = semantic relevance without lexical overlap, a real NLP hard problem.** READY TO BUILD (FORMALIZE-done); it's a real build with a hard paraphrase core, better with focus than a rushed dispatch.

  - 
  - **🧱🧠 THE REAL-PROSE CEILING = GROUNDED-COMPREHENSION WALL (2026-08-05, commit 903d92350, disk-VET'd, culminating finding): the outcome-valence detector HARD_FAILED at 0.0 detection -- BOTH lexical-overlap (n=6, 0.0) AND paraphrase (n=32, 0.0). The agent's principled refinement made it WORSE (rules out an impl bug = FUNDAMENTAL).** DRILL (brain-foundational + missing-component): detecting a goal's outcome requires SEMANTIC comprehension -- the brain UNDERSTANDS 'slipped past the guards' achieves 'freedom' via world-knowledge/inference; it does NOT rely on the word 'freedom' recurring. Our lexical/frame relevance-anchor is a SHAPE-mismatch (lexical-matching vs semantic-inference) and collapses on real narrative because goals & outcomes are almost never lexically co-present. MISSING COMPONENT = SEMANTIC GOAL-RESOLUTION INFERENCE (does this event resolve the goal BY MEANING), which the invariant forbids faking with a borrowed embedding -> the brain-faithful path is GROUNDED MEANING + world knowledge = THIS PROJECT'S FOUNDATIONAL PIVOT ('you cant learn revenge from a book'; the grounded ~6yo foundation). **HONEST CEILING: the SHALLOW goal-owner pipeline (frames+coref+lexicons) tops out at ~0.32 real-prose; its dominant remaining bottleneck (outcome-detection) is NOT a component-wiring or shallow build -- it is the project's DEEP grounded-comprehension challenge (largely unbuilt: grounded_appraisal islanded, no general grounded-knowledge ingest). The goal-owner frontier RECONNECTS to the foundational program.** The shallow components handled the SYNTACTIC/EXPLICIT cases (owner-ID 0.82, explicit-naming binding); the SEMANTIC goal-resolution cases need grounded meaning.

  - - **[CYCLE — island-gate closure, 08-05] CAPABILITY-INTEGRATION GATE CLOSED on the 5 invisible islands** (reg 59->64, ledger commit 405640b89; registry git-ignored=local-only): WIRE grounded_appraisal_sim_earned (target=grounded-meaning foundation/semantic goal-resolution — the recommended pivot, gated by transfer_to_text EXTRACTION_BOTTLENECK; FULL_heldout 1.000 vs no-appraisal 0.239), WIRE theory_of_mind_sally_anne (target=goal-attribution under divergent belief; HARD_PASS Q2 0.806 vs 0.138, 5 seeds), SHELVE vamp_ep_deep_chain (revive on NL-transfer smoke), action_selection_bg (revive when grounded appraisal->action built), slot_attention_wm (revive if primary WM path ceilings). NO mislabels — all 5 audit labels reproduced off-disk. Invisible-island detector HIGH 57->52. Honors 'every gain WIRED not left in experimental results'. STRATEGIC FORK (grounded-comprehension direction) STILL AWAITS USER.

- **[CYCLE — CORRECTED DIAGNOSIS, 08-05] The bottleneck is CONTEXTUAL VALENCE EXTRACTION, NOT grounded reasoning (last cycle's 'grounded-comprehension WALL' was IMPRECISE).** Disk-verified off data/exp_grounded_appraisal_transfer_to_text_v1/metrics.json (verdict EXTRACTION_BOTTLENECK, 5 seeds, contamination-clean, all theta digests == earned run): the grounded appraisal REASONING organ scores arm_a=1.000 on causal attribution GIVEN oracle event-structure coh input (recency=0.000, chance=0.500); the realistic self-extraction arm_b=0.450 (below chance); irony arm_a=0.667 vs arm_b=0.500=chance; beneficiary=honest capability GAP (no earned appraisal slot). THE SHARED WEAK LINK across all three project failures (goal-owner real-prose 0.32, transfer arm_b 0.45, outcome-valence detector 0.0) = `resolve_valence_blind` (experiments/exp_grounded_structure_phase0_probe_v1.py:133): a FIXED hand lexicon, bag-of-words, CONTEXT-FREE (sgv.HARM/HELP_WORDS). Structurally cannot read irony/context-modulated valence -> irony arm_b pinned at chance. ROUTE (standing rule b, missing-component): MISSING-LEARNING = a learned, context-sensitive valence/outcome reader feeding the grounded organ's coh slot -> EXPAND hdlab/learner, do NOT build parallel; glass-box, no borrowed reader/embedding. THIS SHRINKS THE STRATEGIC FORK: grounded REASONING is DONE; the remaining piece is a BOUNDED extraction bridge inside the already-authorized 08-03 grounded pivot, not a from-scratch grounded-meaning program -> driving it autonomously (not blocked on user). IN FLIGHT: hdi_exp_dev grows the transfer eval to statistical power + adds arm_c (learned context-sensitive extraction) to test whether contextual valence reading closes the arm_a->arm_b gap. VET NOTE: arm_a=1.000 at n=4 is UNDERPOWERED -> the eval-growth is part of the same dispatch so the positive is trustworthy per-category before any production build.

- **[CYCLE — VET REVERSAL, 08-05] The arm_c 'irony win' is INVALID; and the reframe's anchor is now PENDING re-verification.** Disk VET of the hdi_exp_dev build (commit 84d4a9b41) caught TWO things the agent's summary glossed: (1) data/exp_grounded_appraisal_transfer_to_text_v1/metrics.json verdict = **CELL_CRASHED** (AssertionError: theta reuse FAILED, reconstructed digest 5d29b1e2 != earned f52b435f, at self_test L647 BEFORE any scoring) -> the reported arm_c irony=1.000/causal=0.514 are NOT on disk, they are from an intermediate/prior computation and are UNTRUSTWORTHY. (2) 3/10 grown irony items carry an EXPLICIT sarcasm marker IN the span ('said Marilla sarcastically', surface==support span) -> any high arm_c on them is CONSTRUCTION-DETERMINED leakage (reader keys on the literal adverb), NOT brain-faithful irony inference (brain infers intent from situation-model incongruity, not a narrator's adverb). CONSEQUENCE: the contextual-valence-extraction hypothesis is NEITHER confirmed nor refuted this cycle. MORE IMPORTANT: the theta-digest DRIFT is load-bearing infra -- reconstruct_full_theta no longer bit-matches the landed earned digest on the same host/torch 2.8.0+cpu. This means the PRIOR landed arm_a=1.000 (the anchor of the 08-05 reframe) is currently **NON-REPRODUCIBLE** -> DOWNGRADE 'grounded reasoning proven' to 'LANDED-BUT-REPRODUCTION-BLOCKED, re-verify'. It affects EVERY cell built on the earned theta, not just this one. ROUTING: hdi_testbed to diagnose the determinism drift (values-drifted vs stale-digest; pin OMP/OPENBLAS threads + PYTHONHASHSEED + sorted(set) per the split-nondeterminism discipline; fix or re-baseline with a documented env). arm_c RE-RUN is HELD behind (a) theta reproducibility restored AND (b) irony eval items rebuilt WITHOUT explicit sarcasm markers (force situation-incongruity inference). Standing rules paid off: verify-on-disk caught the crash; VET-positives-as-hard-as-negatives caught the leak.

- **[CYCLE — THETA DRIFT RESOLVED + ANCHOR RESTORED, 08-05] testbed diagnosis VET-PASS: landed arm_a=1.000 is SAFE.** Verdict DETERMINISTIC-BUT-DRIFTED, values functionally INTACT (notes/theta_reuse_digest_drift_diagnosis.md, commit 4f260ce9e, doc-only). Disk-VET confirmed the behavioral invariant: earned FULL_heldout recency_restoration=0.2978723404255319 = EXACTLY 140/470; reconstructed theta reproduces it to 16 sig figs over 1500 stochastic episodes x 8-way argmax (impossible if the policy differed). Root cause = MKL float non-bit-reproducibility on the hybrid P/E-core CPU (deterministic NOW = stable 5d29b1e2 across threads/processes, just != old landed f52b435f); NOT code/threads/env (all ruled out empirically). -> RE-UPGRADE 'grounded reasoning proven (arm_a=1.000)' back to SAFE (the 08-05 reframe anchor holds). DIRECTOR-APPROVED FIX: replace the bit-exact SHA256 theta-reuse assert (transfer_to_text self_test L647) with a BEHAVIORAL-EQUIVALENCE check (reconstructed theta's FULL_heldout eval == banked metrics, EXACT equality since integer-count ratios) -- a contamination guard robust to MKL bit-drift, not a weakening. UNBLOCK dispatched: implement the behavioral self_test + DE-LEAK the irony eval (3/10 items carried explicit 'sarcastically' markers in-span; audit all, replace leaky ones so the reader must INFER irony from situation incongruity) + re-run arm_c for a TRUSTWORTHY per-category number. EXPECTATION (honest, can-fail): arm_c may collapse toward chance on de-leaked irony -> that would show surface tone/negation features are INSUFFICIENT and irony needs situation-model integration (the grounded organ in the loop), routing the next build. Either outcome is a real finding.

- **[CYCLE — TRUSTWORTHY arm_c RESULT + TWO-BOTTLENECK DIAGNOSIS, 08-05] (VET-PASS on disk, commit fddde7e22).** Clean re-run (behavioral theta guard PASS all 5 seeds, irony eval de-leaked =4 items replaced incl. a corpus-window leak the contract missed, leak-guard now a hard gate): CAUSAL(n=7) arm_a=1.000 arm_b=0.629 **arm_c=0.514** recency=0.143; IRONY(n=10 de-leaked) arm_a=0.600 arm_b=0.500 **arm_c=0.700** surface=0.500. Verdict EXTRACTION_BOTTLENECK / arm_c_verdict ARM_C_PARTIAL. HONEST READ (per-axis, not aggregate): the shallow learned local-context extractor (blind-lexicon + local tone/negation/contrast/quote over a +/-2-line window) is a BRITTLE PARTIAL patch. On irony it catches ONLY local-cue cases (2/5 correct flips via a real negation cue e.g. 'couldn't be helped'; 0/5 false-positives on sincere = genuine precision) and MISSES 3/5 NARRATIVE-only irony (e.g. Tom's whitewashing false-cheer needs connecting a PRIOR paragraph's dread to a LATER claim of enjoyment -- no local lexical cue). On causal, context features actively HURT (arm_c<arm_b). => TWO DISTINCT EXTRACTION BOTTLENECKS, each pointing at a FAITHFUL organ WE ALREADY OWN (not a new shallow learner): (1) NARRATIVE IRONY -> SITUATION-MODEL INTEGRATION: hdlab/situation_reader builds a persistent SituationModel across the passage (maintained entity/affect state, events, timeline, cause->outcome links) -- exactly the distant-fact memory arm_c lacks; (2) CAUSAL ATTRIBUTION -> COREFERENCE/relational retrieval (Component-2 Centering-Cb), NOT valence (valence extraction HURTS it). NEXT BUILD (WIRE-DON'T-ISLAND, brain-foundational): replace arm_c's +/-2-line window with situation_reader's MAINTAINED SituationModel as the context source -- detect irony as incongruity between a maintained-state fact (entity's earlier stated affect/goal) and a later utterance's surface valence; do causal attribution off the SituationModel's causal links + coref. Test: does situation-model context catch the 3/5 narrative-only irony arm_c missed, and does coref-based causal beat blind-valence? Reuse the faithful organs (situation_reader C4 + coreference C2) feeding the proven grounded reasoning organ. STATUS: arm_c is an honest MIDDLE_BAND partial (NOT a HARD_PASS, NO capability-registry WIRE row yet -- gate stays open until the situation-model-integrated extractor earns it).

- **[CYCLE — CODE-READ CORRECTION + situation-model-affect PROBE dispatched, 08-05].** Standing rule d (read the code) corrected my own last-cycle framing: the SituationModel maintains entities/coref/events/timeline/causal_links but has NO per-entity AFFECT/VALENCE state -- so 'just wire the existing SituationModel as context' was imprecise; the real MISSING-PRIMITIVE for narrative irony = a MAINTAINED per-entity affect trajectory. Dispatched hdi_exp_dev (ae678e0a) a CAN-FAIL PROBE: build the maintained-affect trajectory (reuse coref C2 to link an entity's mentions + reuse arm_c's context-valence reader to score per-event affect), detect narrative irony as incongruity between the maintained trajectory and a later utterance's surface valence; evaluate SEPARATELY on the 3/5 narrative-only items arm_c MISSED (can-fail target), the local-cue items, and sincere controls (false-positive check). Pre-reg band: PROVEN if it recovers >=2/3 of arm_c's missed narrative items with 0 sincere false-positives; NULL if ~arm_c. Probe does NOT modify situation_reader yet (proves-then-wires). If null, per-item mechanism read (coref-fail vs valence-reader-fail vs goal-level-not-valence incongruity) is the deliverable. Awaiting + VET per-subset.

- **[CYCLE — CONVERGENCE: the single load-bearing bottleneck is the AFFECT/VALENCE READER, 08-05] (VET-PASS on disk, probe commit e7edc89e2).** The maintained-affect narrative-irony probe landed NULL_FALSE_POSITIVES (narrative-missed recovered 1/3, sincere FP 2/5, pre-reg needed >=2/3 recovered AND 0 FP) -- but the per-item drill is the WIN: the situation-model MECHANISM WORKS (coref/wide-window scope correctly locates the right prior-context paragraphs); the blocker is the REUSED BLIND-VALENCE LEXICON (resolve_valence_blind). Two precise failure modes: (1) MISSING-FACT sparse affect vocabulary -- grapp_irony_003 dread paragraph ('melancholy/hollow/burden') has NO HARM_WORDS token, invisible to the lexicon; (2) MISSING-LEARNING word-sense blindness -- grapp_sincere_003/005 FP because 'missed a trick' / 'studied hard' fire literal HARM tokens 'trick'/'hard' though non-emotional (a bigger fixed list CANNOT fix this). CONVERGENCE ACROSS THE WHOLE ARC: the SAME organ (the affect/valence reader) is the recurring blocker in arm_b (candidate-span extraction), arm_c (local-context), AND maintained-affect. Everything else is PROVEN or WORKS: grounded reasoning (arm_a=1.000), situation-model scope/coref, causal->coref (separate). => THE FRONTIER HAS COLLAPSED TO ONE COMPONENT: a LEARNED, WORD-SENSE-AWARE, CONTEXT-SENSITIVE AFFECT READER (richer affect vocabulary [missing-FACT] + learned sense/context disambiguation via hdlab/learner [missing-LEARNING]) to REPLACE resolve_valence_blind everywhere. Brain-foundational: affect is read via context+word-sense, not a fixed word list. This is the load-bearing build the goal-owner/grounded line has been blocked on for multiple cycles. NEXT (driving): prototype the learned context/sense-aware affect reader, can-fail test = clear the word-sense FPs ('hard'/'trick' idioms) AND catch the no-HARM-token dread, on the SAME items the blind lexicon failed. CAVEAT (agent-flagged, honest): the probe used a literal name-variant match, not the full CorefReader stack (situation_reader.read() needs a CoNLL mention stream unavailable for raw novel text at probe scope) -- so 'coref works' = 'name-match scope works'; full-coref quality untested but not the blocker (lexicon is).

- **[BUILD — C-A STEP 0 DONE, floor set, 08-05] (VET-PASS on disk, commit a9b50a892).** Sense-collapse floor measured over the 4 existing glass-box encoders on an 8-form / 32-sentence sense-minimal-pair probe (incl. all 4 audit collision tokens hard/trick/pay/cross): random_indexing acc=0.375 sep=0.0 (cosine senseA/B EXACTLY 1.0, architectural collapse), concept_encoder acc=0.5625 sep=0.0 (finite concept-id lookup, cosine 1.0), composed_encoder_v3 acc=0.531 sep=0.0049 (context path, no sense signal), ppmi_sparse acc=0.625 p=0.157 (least-collapsed, still chance). ALL within chance band; NONE discriminate senses. HONEST FLOOR = 0.5625 (chance 0.5). Plan premise CONFIRMED on our own data: sense-collapse is real+total. C-A build must beat 0.5625 by a wide pre-registered margin with INDUCED (not hand-listed) senses. Best spoke to extend = ppmi_sparse live per-sentence path. NOW: dispatch C-A sense-structure induction (multi-prototype context-clustering + predictive_coding error-driven differentiation, glass-box, EXTEND not rebuild).

- **[BUILD — C-A v1 HARD_FAIL (well-diagnosed) -> route to error-driven rep on real corpus, 08-05] (VET-PASS on disk, commit c1358cdce).** Induced multi-prototype senses (predictive_coding threshold-gate clustering over PPMI/SVD context reps): held_out sense-discrimination 0.567 vs floor 0.5625 (NO margin, p=0.30), single-prototype control 0.60 (induction did NO better than context-blind lookup), same-sense agreement 0.50 (fragmenting). HARD_FAIL. LINCHPIN DIAGNOSTIC (glass-box, VET'd): mean_fit_same_sense_residual=0.436 vs diff_sense_residual=0.447 -- NEAR-IDENTICAL -> the PPMI/SVD CONTEXT REP carries ~no sense signal; the (faithfully-running) error-driven clustering had nothing to differentiate. Senses WERE induced (8/10 forms, discriminator fired) = mechanism sound, INPUT featureless. ROUTE (missing-component=data/representation, NOT mechanism defect; broken-experiment not ceiling): the rep was fit on ~60 probe sentences, not a real corpus. FIX = fit the context representation on a REAL BACKGROUND CORPUS (data/corpora novels/readers) + go ERROR-DRIVEN (predictive_coding-LEARNED context rep, not count-only PPMI which my prior says CAPS). C-A v2 dispatched: 2 arms (PPMI-at-scale vs predictive-coding-learned) to MEASURE count-vs-error-driven + scale effect; direction = error-driven. HARD-PASS if an arm clears floor by >=0.15 (>=0.71) with single-proto staying chance.

- **[BUILD — C-A v2 HARD_FAIL BOTH ARMS at corpus scale -> RE-EXAMINE FORMULATION via hard brain-fidelity VET (USER-directed), 08-05] (VET-PASS on disk, commit ed3328898).** 3000-sentence real corpus (probe forms 9-244 occ each). ARM-COUNT (PPMI-at-scale) held-out 0.55; ARM-ERRORDRIVEN (predictive_coding) held-out 0.517; floor 0.5625; single-proto control 0.517. BOTH at chance. Residual diagnostic reproduces at scale (count same/diff 0.235/0.244; error-driven 0.619/0.617 -- near-identical = STILL FEATURELESS). error-driven applied_frac=0.9996 (gate NEVER skipped -> behaved like vanilla Hebbian, not genuine differentiation). => corpus scale ALONE did not fix it; TWO clean fails at STANDALONE unsupervised discrete-sense induction from lexical co-occurrence. DECISION (stop iterating impl, interrogate FORMULATION): the brain audit says sense resolution = CONTROL keyed to the SITUATION MODEL + GROUNDING to spokes in a RECURRENT loop, NOT isolated co-occurrence clustering -> the C-A standalone framing may itself be un-brain-faithful (missing the control/grounding/recurrence that GIVES sense its signal). USER (08-05): 'vet the components hard against the brain faithful drill.' Dispatched a HARD per-component brain-fidelity audit (SHAPE/POSITION/METRIC vs the mechanism doc), C-A crux focal. HOLD C-A v3 until it returns.

- **[BUILD — BRIDGE-1 HARD_PASS + deep-drill CERTAINTY RULING, 08-05].** BRIDGE-1 (governor/frame context-conditioned grounding -> frozen appraisal-sim) HARD_PASS (VET'd on disk, commit 96e8e8404): differential grounding 0.967, bag-of-words control 0.517=chance, scrambled/per-form-table 0.5, unseen-concept 0.956, theta-witness all 5 seeds. Governor/frame differentiates where bag-of-words CANNOT -> the corrected direction WORKS. USER asked for a deep brain-foundation drill for implementation CERTAINTY -> 3 LIVE-VERIFIED drills (sense-cues 46f7d8497, compositional-grounding 047240f50, situation-recurrence 31bb765df; synthesis notes/deepdrill_SYNTHESIS_bridge1_certainty.md). RULING: BRIDGE-1's governor sense-selection is brain-faithful STAGE-1 but NECESSARY-NOT-SUFFICIENT. 3 corrections: (1) appraisal must condition on the ASSEMBLED EVENT (predicate+args+goal via Component-3, Scherer goal-congruence), not the governor token -- BRIDGE-1 + Component-3 = ONE build; (2) TOP-DOWN SITUATION-BIAS PORT from day one wired to Component-5 organs (a local-only reader is WRONG-DIRECTION on the target subset -- Nieuwland&VanBerkum peanut-in-love discourse INVERTS local reading; predictive-coding says local computes a different quantity so can't bolt loop on later); (3) dominance-default + biased competition. The 0.967 is REAL but on the LOCAL-GOVERNOR-SUFFICIENT subset; predicted to FAIL on governor-matched/event-differing ('hit the deadline' vs 'hit the wall') + discourse-decisive pairs = exactly our target cases. NEXT: CONFIRM test (does naive BRIDGE-1 fail there?) -> then BRIDGE-1/C-AB v2 two-stage event+situation-conditioned. Plan -> v3.1. This is the deepdrill saving us from building C-C/C-D on an insufficient foundation.

- **[BUILD — CONFIRMATION: BRIDGE-1 insufficiency is SUBSTRATE-MEASURED, 08-05] (VET-PASS on disk, commit 761211bf6, verdict RULING_CONFIRMED).** Ran UNMODIFIED governor-only BRIDGE-1 on 3 subsets (5 seeds): A local-governor=0.962 (passes, no regression), B governor-matched/event-differing ('beat the game' vs 'beat the dog')=0.500 EXACTLY, C discourse-decisive (peanut-in-love, bit-identical local clause)=0.500 EXACTLY, scramble=0.500. Governor-only pinned at majority-baseline ceiling on B/C because extract_governor_feats has NO object-identity feature (B) and NO discourse channel (C) -> bit-identical predictions per pair. ARCHITECTURAL, not tuning (scramble confirms zero discourse sensitivity). This converts the deep-drill ruling from literature-certainty to SUBSTRATE-MEASURED FACT. => licensed to build the corrected two-stage BRIDGE-1/C-AB: governor sense-select -> Component-3 EVENT-ASSEMBLY (predicate+patient-identity+goal) -> event-conditioned appraisal (fixes B) + SITUATION-BIAS PORT (coref-fed, fixes C) + dominance-default biased-competition. Dispatched.

- **[BUILD — BRIDGE-1/C-AB v2 two-stage SHAPE VALIDATED (closed-lexicon, construction-bounded — NOT yet a capability), 08-05] (VET-PASS on disk, commit b226cfad6, HARD_PASS).** Two-stage governor->event-assembly->appraisal + situation-port + biased-competition RECOVERS the two subsets governor-only failed: A=0.962 (no regression), B 0.500->1.000 (event-scramble collapses to 0.583 = event channel causal), C 0.500->1.000 (discourse-scramble collapses to 0.65 = discourse channel causal), bag-of-words stays chance. The CORRECTED TWO-STAGE+PORT ARCHITECTURE is VALIDATED (scramble-controlled, each channel causal). HONEST CAVEAT (VET'd on disk): Stage-2a event-class + Stage-2b threat/benign port are CLOSED HAND-LEXICONS (GOAL_OBJECT/ADVERSARIAL/ANIMATE_HARMABLE whitelists + THREAT/BENIGN words) standing IN FOR the real organs (Component-3 event extraction, animacy, coref, Component-5 situation state); 'generalization' items are other WHITELIST members, not open vocab. => this is a SHAPE-PROOF (architecture right), NOT a capability number; the 1.000s are construction-bounded. runs-vs-reasons: SHAPE proven, ORGANS pending. NEXT (right-not-easy, don't island): CAPABILITY-IZE by wiring the REAL organs (Component-3 patient/role + animacy) for B on OPEN vocabulary; C's real fix = C-D situation model feeds the port (coarse lexical port is a stand-in per drill-3, deferred to C-D). Dispatched real-organ open-vocab B.

- **[BUILD — event-assembly is a REAL OPEN-VOCAB CAPABILITY on the animacy axis (PARTIAL, 2 scoped gaps), 08-05] (VET-PASS on disk, commit c555bdb34).** Replaced v2's closed object-whitelist with the REAL organ (Component-3 patient extraction + hdlab.animacy_lexicon over the direct object): Bopen (6 fully open-vocab pairs -- novel objects fence/nephew/ledger/tenant... + novel verbs battered/clobbered/walloped...) = 1.000 vs governor-only 0.500, animacy-scramble collapses to 0.40 (animacy is the causal signal), bag-of-words 0.500, A no-regression 0.962. => the animate-vs-inanimate-patient axis of event-assembly is a GENUINE open-vocabulary capability (not construction-bounded). TWO SCOPED MISSING-COMPONENTS (honest): (1) animacy_lexicon body-part GAP -- Bgap (ankle/elbow/knee) = 0.500 chance (WordNet hypernym gap; cheap fix = body-part hypernym override in hdlab/animacy_lexicon.py); (2) ADVERSARIAL/SOCIAL-RELATIONAL valence (enemy vs refugee, both animate persons) -- a SEPARATE appraisal axis (maybe maps to the sim's RECIPROCITY dim), not animacy, deferred. FOUNDATION STATE: governor sense-select (real) + event-assembly animacy axis (real open-vocab) validated; situation-port C = coarse stand-in (real fix at C-D). NEXT: independent landed-VET of the foundation before building C-C.

- **[VET — independent landed-VET of the foundation, CERTIFIED + one over-read corrected, 08-05] (commit f06c06535, bit-for-bit recompute).** CERTIFIED (recomputed, real): 'given a validly-extracted direct-object patient + a force-capable governing verb, the event stage flips harm<->neutral by the patient's WordNet ANIMACY on OPEN vocabulary (Bopen 1.000/5 seeds, animacy-scramble->0.40/0.25 inverts, BOW/governor 0.500, no A-regression), and the frozen sim theta genuinely values it (random-theta~=0).' OVER-READ CORRECTED (symmetric, no inflation): the claim is open-vocab on the PATIENT-ANIMACY axis ONLY -- the FORCE-VERB axis is a CLOSED/test-fitted hand-list (FORCE_CLASS_HARM_REAL fitted to the 6 Bopen verbs); it doesn't manufacture the within-pair split (verb constant per pair) so animacy stands, but event-assembly is open on ONE axis, closed on the other. SHAPE-ONLY (do NOT build on as capability): force-verb identification, goal-object/adversarial classes, the discourse/situation port (never open-vocab tested). PROVEN GAPS x3 (openable, revival routes noted): body-part patients, social-relational valence, abstract-harm-vs-goal. DISPOSITION: chain-grade on the animacy axis (bounded), promote as a NARROW brick. NEXT (payoff test): C-C = wire the validated two-stage grounding as the valence reader + test it CLEARS THE ORIGINAL affect-reader failures (resolve_valence_blind word-sense FPs 'studied hard'/'a trick' + confused-4 INERT-valence) -- closing the loop to the convergence finding. Force-verb open-vocab + body-part fix = scoped follow-ups (same real-organ-routing technique as animacy).

- **[MILESTONE — C-C: the new grounding CLEARS the ORIGINAL affect-reader failures, loop closed, 08-05] (VET-PASS on disk, commit 26fce6237, HARD_PASS gates recomputed old_correct_core6=2 / new_correct_core6=6 / old_wrong=4).** The validated two-stage grounding (governor sense-select + animacy event-assembly -> frozen sim theta) vs resolve_valence_blind on the ORIGINAL failures that started this whole build: new 6/6 vs old 2/6 in-scope. CLEARED the exact convergence-finding FPs: 'missed a trick' (Tom Sawyer, was false-HARM on token 'trick') + 'studied hard' (Anne, was false-HARM on token 'hard') + the named collision core -- all now correct; new reader is NON-INERT under scramble where the old was INERT. This is the payoff: the converged bottleneck (the affect/valence reader) now has a brain-faithful, open-vocab (animacy axis) REPLACEMENT for its in-scope failures. Out-of-scope items honestly labeled -> surfaced a NEW proven gap: BENEFICIARY-TRACKING ('slapped the Lion': harm target=adversary not beneficiary; BOTH readers wrong, architectural, = the who-is-affected axis). STATUS: BRIDGE-1/C-AB/C-C = first component of the semantic organ BUILT + independently certified (animacy axis) + proven on the origin problem. NEXT: C-D (situation model AFFECT dim + PREDICTION, isolate-prediction gate) -- also gives C's discourse port its real source. Scoped follow-ups: force-verb open-vocab, body-part, beneficiary/social-relational, promote to hdlab.

- **[BUILD — C-D part 1: situation-model AFFECT dimension (grounded maintained-affect) MIDDLE_BAND, 08-05] (VET-PASS on disk, commit 110d501bb).** Swapped the maintained-affect probe's per-event scorer from resolve_valence_blind to the VALIDATED two-stage grounding (maintenance mechanism held FIXED -- old-blind arm reproduces v1 bit-for-bit). Narrative recovery grounded 2/3 vs old-blind NULL 1/3 vs per-token-pooling 0/3 (integration>pooling>blind); sincere FP grounded 1 vs old-blind 2 (sincere_acc 0.8 vs 0.6); local-cue 1.0; arms_differ_verified. MIDDLE_BAND: strictly better than NULL on BOTH axes + beats pooling, clean causal attribution to the grounding. TWO residuals CLEANLY ROUTED: (a) irony_005 miss = the PREDICTION case (dread=FORWARD expectation, fake-deathbed, no present harm event) -> C-D part 2 forward-projection (the design-VET's highest-priority gate); (b) sincere_005 FP = governor word-sense leak on no-dobj/proper-noun 'beat' (heartbeat/'beat to the Island') where C-C event-stage can't engage -> C-C sense-scope gap, not a maintenance failure. NEXT: C-D part 2 = PREDICTION/forward-projection with the ISOLATE-PREDICTION gate (static no-projection arm MUST fail).

- **[BUILD — C-D part 2: forward-PREDICTION mechanism PROVEN (isolate gate passes), detector stubbed, 08-05] (VET-PASS on disk, commit c67455856, PARTIAL_ISOLATE_PROVEN_REGRESSION_FP).** Forward-projection via hdlab/predictive_coding (Rao-Ballard), gap-filler integration (projects valence only when present-state maintainer is silent). ISOLATE-PREDICTION GATE PASSES CLEANLY (the design-VET's #1 priority): predictive 3/3 vs static-no-projection 0/3 = 100pt gap (static PROVABLY can't -- no future to read); irony_005 forward-dread recovered (margin 0.97); scramble-future collapses to 0.47 chance vs 1.0 real over 64 seeds; predictive_coding NON-degenerate (neg/benign probe margins 0.97 differentiate, unlike the earlier C-A applied_frac pass-through). => FORWARD-PROJECTION IS REAL + NECESSARY -- proven. C-E irony is now MECHANISM-UNBLOCKED. HONEST DEFLATION (mirrors the BRIDGE-1 arc): (a) the setup->outcome KNOWLEDGE is SUPPLIED via a glass-box training grid + the setup-cue DETECTOR is a LEXICAL stand-in that false-fires on 2/10 sincere items -> needs a GROUNDED intent/deception detector; (b) N=1 real-corpus forward anchor (irony_005), 2/3 forward-NEG synthetic. SEQUENCING INSIGHT (reuse-don't-island): the grounded intent/deception detector = MENTALIZING = the ToM organ via BRIDGE-2, which ALSO serves C-E irony -- so the intent-detector fix and C-E converge. SITUATION-MODEL LAYER (C-D) STATUS: affect dimension (part1 MIDDLE) + prediction mechanism (part2 proven) both established; detectors are grounded-stubs routed to C-D-part-3/BRIDGE-2.

- **[WIRE — certified grounding PROMOTED to hdlab (WIRE-DON'T-ISLAND), 08-05] (commit 4cd8b4f24; witness PASSES on disk).** The certified context-grounded valence foundation was ISLANDED (0 hdlab, 0 registry rows); now hdlab/context_grounded_valence.py (governor sense-select + animacy-axis event override + frozen appraisal-sim valuation) + verification/verify_context_grounded_valence.py (scaffold-free witness, tracing-independent: reproduces C-C core6 6/6 vs old 2/6 AND animacy-axis Bopen=1.000 lift 0.667 with BOW/governor/scramble at chance -- ALL CHECKS PASS) + capability_registry row context_grounded_valence (gate_decision=WIRE, honest scope: certified animacy axis; scoped-open force-verb/discourse/gaps). Behavior preserved (witness = the proof). integration_status=PENDING_AUDIT (the import-graph audit is slow >2min; backstop not enforcement). REMAINING FULL-WIRE (noted in the registry row): point the live affect-reader pipeline's valence call at score_context_grounded_valence for in-scope items (resolve_valence_blind/abstain as out-of-scope fallback) -- a follow-up pipeline-wiring step. This cron cycle = locked the certified gain into the substrate BEFORE the next build, per the emphatic standing rule. NEXT: (a) live-pipeline-wire the organ into the reader path, and/or (b) BRIDGE-2 (text->intent->ToM) = grounded intent detector fixing C-D part 2 stub + serving C-E irony. (MEMORY.md current-focus banner headline is stale [pre-semantic-organ]; recovery pointer -> THIS backup doc is correct + current, so recovery integrity intact; banner refresh deferred.)

- **[WIRE — affect dimension WIRED into production situation_reader (integration complete), 08-05] (commit 59fa0597b; VET-PASS on disk).** EventRecord.affect: Optional[str]=None added ADDITIVELY (mirrors subj_role/obj_role); situation_reader now IMPORTS hdlab/context_grounded_valence (import-graph edge = organ no longer island-at-reader-level, integration_status now genuinely WIRED not just registered). _assign_affect gates on stage=='event' (certified animacy-axis scope): in-scope 'battered her nephew'->affect=HARM, out-of-scope 'John saw Mary'->affect=None (ABSTAIN). BACKWARD-COMPAT PRESERVED (the proof): all 5 situation_reader self-tests pass, coref/event/timeline metrics UNCHANGED (cross-checked vs the exact numbers existing self-tests assert), organ witness still passes (certified values untouched: core6 6/6, Bopen 1.000), verification/run_certification.py 208 passed / 3 skipped GREEN (no regression). Fast structural VET confirmed: affect field default None + all prior fields intact + import edge live. Efficiency: organ's module-level theta/perceptron caches train once/process. SITUATION-MODEL LAYER NOW: production affect dimension WIRED (C-D part 1 consolidated), prediction mechanism proven (C-D part 2, detector stubbed). WIRE-DON'T-ISLAND satisfied for the grounding organ. NEXT: BRIDGE-2 (text->intent->ToM) = grounded intent detector fixing C-D2 stub + serving C-E irony.

- **[WIRE — OOV frame-induction WIRED into production (goal-owner prereq / cron 3b), 08-05] (commit 22b9b6f8e; VET-PASS on disk).** Production frame_primary_role now uses the induced predict_subj_role for OOV verbs (get_induced_subj_hypothesis trains once at import on the leak-checked litbank TRAIN split, module-cached; verb lemma NEVER a feature -> transfers) INSTEAD of the wrong positional AGENT default. OOV psych subject 0.0 -> 0.833 (subj-axis, brain-faithful CONSTRUCTION; matches exp_frame_induction_oov_psych_real_v1 exactly, MIDDLE_BAND, data-starved NOT ceiling); position-majority 1.0 REJECTED as the non-faithful proxy per standing rule (frame WINS over position). cherished self-test flipped AGENT(wrong-default)->EXPERIENCER(earned, cross-checked via predict_subj_role); 'bolted' bare-transitive witness proves honest AGENT-degrade when induction abstains. IN-VOCAB UNCHANGED (frame_slot_role unconditional before OOV check; feared->EXPERIENCER, kick->AGENT unchanged). BACKWARD-COMPAT GREEN: run_certification 208 passed/3 skipped, affect-wire witness (battered->HARM/saw->None) still green, all situation_reader self-tests pass. IMPORT EDGE: situation_reader imports frame_induction OOV path (no longer offline-island). obj-axis deliberately NOT wired (deferred, 0.0, not a regression). SITUATION-MODEL/ROLE LAYER NOW: grounding(wired)+affect(wired)+OOV-subj-frames(wired). This shrinks the dominant TYPING_MISS bucket for C-F. NEXT (goal-owner PAYOFF): re-run C-F end-to-end (Component-5 directed_goal_outcome_score) on the C3-mined 38-item bank, measure vs 0.32 ceiling -- does TYPING_MISS (was 14/38) shrink? Follow-up: SUPPLY more OOV psych data (data-starved), skunkworks registry update for the OOV wire.

- **[MEASURE — C-F goal-owner PAYOFF = HONEST NEGATIVE that REDIRECTS the frontier, 08-05] (VET-PASS on disk, commit 9317c0c06).** Re-ran Component-5 end-to-end on the 38-item bank routing typing through this session's wires: e2e 0.3158 UNCHANGED vs 0.32, decomposition {OWNER_ID:7,TYPING_MISS:14,BINDING:5,CORRECT:12} IDENTICAL (OLD-arm ablation reproduced it bit-identically). WHY (VET'd on disk, TWO findings): (1) CONSTRUCTION ARTIFACT -- ALL 38 goal_verb_lemma are ALREADY in PSYCH_VERBS (0 OOV; the C3-syntax miner USED the PSYCH_VERBS filter to select these sentences), so the OOV wire STRUCTURALLY CANNOT FIRE on this bank -> the unchanged number is NOT evidence the OOV wire is useless, it's that THIS BANK CAN'T TEST IT (need a bank WITH OOV psych verbs). (2) THE REAL TYPING_MISS BOTTLENECK (spot-checked 4 items) = OUTCOME events never typed (0 outcomes) because a NAIVE _sentences() splitter FRACTURES quoted dialogue ('...glad...!' she cried) -> subject_entity()=None -> outcome dropped. ORTHOGONAL to both wires (not OOV; and the affect wire's certified axis is force-dynamics HARM/HELP, NOT goal-achievement 'glad/met' valence -> it abstained as pre-registered). ROUTES (standing rules a+b): (i) DIALOGUE-FRACTURE segmentation/subject-attribution = WIRE-DON'T-ISLAND, NOT build: we OWN quotative speaker-attribution (coreference_resolver.run_principle_b_deixis promoted 2026-08-02 + exp_quotative_speaker_attribution_stack_break050_v1); the C-F outcome-typing path uses a naive splitter instead -> ROUTE C-F outcome-subject-attribution through the owned quotative organ. (ii) GOAL-ACHIEVEMENT outcome valence (glad/disappointed/met) = a SEPARATE appraisal axis the grounding organ (force-dynamics) doesn't cover -> missing-axis. (iii) To actually test the OOV/grounding wires' goal-owner value, need a bank WITH OOV psych verbs + non-dialogue-fractured outcomes. HUMBLING+HONEST: the substrate wires (grounding force-dynamics + OOV frames) are real production gains but do NOT move goal-owner on THIS bank (bottleneck is elsewhere). Frontier REDIRECTS to: wire the owned quotative organ into C-F outcome attribution. NEXT: that wire (reuse-don't-island).

- **[MEASURE — C-F quotative-wire NET-ZERO -> META-FINDING: the 38-item bank is a POOR INSTRUMENT, 08-05] (VET-PASS on disk, commit 97dfc503b).** Wired the owned quotative speaker-attribution (coreference_resolver _detect_speaker/_SPEECH_VERBS, scoped to OUTCOME roles after a uniform-fallback regression was caught+fixed) into C-F outcome-subject resolution. e2e 0.3158 UNCHANGED; decomposition {OWNER_ID:7, TYPING_MISS:13, BINDING:6, CORRECT:12} vs {7,14,5,12} -- TYPING_MISS 14->13 but that 1 item bound to the WRONG speaker (TYPING_MISS->BINDING, 0 became CORRECT). 14 TYPING_MISS CATEGORIZED (traced to text): (a) quoted-dialogue-fracture=4, (b) outcome-cue-not-in-quote=6, (c) NO outcome cue=4, (d) goal-side=0. Of the 4 category-(a): 3 have their speaker NAMED OUTSIDE the item's short excerpt window (bare 'she cried'/'the child' -> no antecedent NAME in-window), 1 is a THIRD-PARTY speaker (not the goal-owner) -> the quotative organ (name-adjacency) STRUCTURALLY CANNOT resolve them without a WIDER window / full-passage coreference. META-FINDING (the real lesson, 3rd layer of the C-F onion): the 38-item bank is a POOR INSTRUMENT for measuring goal-owner -- SHORT excerpts (antecedents OUT-OF-WINDOW), MIXED causes (only 4/14 even quote-related; 6 other-seg; 4 no-outcome-cue=valence-axis gap), IN-VOCAB-only by construction. Patching against it = diminishing returns. RIGHT MOVE = build a BETTER instrument (WIDER windows so antecedent/speaker is in-window + OOV goal verbs + clean outcome attribution). -> This is EXACTLY the parallel eval-bank task in flight (adeed5cc); I SENT it the wider-window requirement (antecedent must be resolvable in-window) so the new bank avoids this flaw. VET-discipline win: measurement stopped further quotative-patching. NEXT: the new wide-window OOV-psych bank becomes the real instrument; separate gaps = goal-achievement outcome-valence axis (category c) + non-quote outcome-seg (category b).

- **[PARALLEL — OOV-psych eval bank built + GOLD-REVIEWED, reveals the real blocker is UPSTREAM PIPELINE LOSSINESS, 08-05] (commit 76fd27254).** Mined 21 OOV-psych goal-owner items (16 OOV verbs: loathe/despise/abhor/detest/resent/scorn/disdain + crave/covet/cherish/treasure/relish/idolize/worship/dote/esteem; all is_oov confirmed) with wider windows. GOLD REVIEW dropped 13/21 -> only 6 clean gold (goal_outcome_oov_psych_gold_v1.jsonl): 6 wrong-owner (ROSTER FALSE-POSITIVES -- common nouns 'Frenchman'/'Christian'/'Girls' admitted as proper-name owners; multi-word name split; subj/obj confusion), 6 TOPIC-BLIND OUTCOME (the ACHIEVE/BLOCK-cue forward window is topic/entity-blind), 1 single-quote dialogue. = the SAME weak link that degraded the ORIGINAL bank -- a MINING-INFRASTRUCTURE gap, NOT OOV-specific. My manual spot-check (Mary vs 'Mary Ann' truncation, aversion-verb goal-outcome framing shaky) CORROBORATED the gold review. SANITY EVAL (production frame_primary_role via SituationReader.read on 6 gold): 3/6 produced NO extractable goal event at all (participial-clause + coordinated-VP gaps in the production EVENT EXTRACTOR = upstream limitation); of the 3 that resolved, 1 typed EXPERIENCER (OOV induction FIRED, Dave/resented), 2 fell to AGENT default (honest abstain, consistent with the wire's 0.833 held-out). => HUMBLING META-FINDING: measuring the OOV/grounding wires' goal-owner value on REAL PROSE is blocked by MULTIPLE UPSTREAM PIPELINE GAPS (mining roster-FPs, topic-blind outcome window, event-extractor participial/coordinated-VP gaps) -- the OOV wire FIRES CORRECTLY where it gets a chance, but the pipeline rarely gives it one. Effective testable N ~3 on the clean gold. ROUTES (nameable, upstream-first): (1) harden the miner (entity-checked roster + topic-checked outcome window), (2) event extractor: participial-clause + coordinated-VP coverage, (3) then re-measure. Parallel C-F payoff run (ad164a91) redirected to the 6 gold + small-N caveat. Also in flight: goal-achievement outcome-valence axis (ab0041cb).

- **[VET — independent gold-VET CONFIRMS bank not-trustworthy + PINPOINTS the upstream bottleneck = EVENT EXTRACTOR, 08-05] (commit 9e9bddd0a, notes/goldvet_oov_psych_bank.md).** CORRECTION: the C-F payoff-run dispatch FAILED to launch (Opus classifier outage); the agent that ran was the GOLD-VET (opus, ad164a91) -- it correctly declined to run the ablation (role separation) and instead read the signal off the on-disk sanity fields. GOLD-VET (independent, triangulated with my spot-check + the miner's own gold review -- 3 reads AGREE): OOV axis 21/21 CLEAN (recomputed); OWNER gold WRONG on 6/21 (subject-grabber takes predicate-nominative/negated-subject/object/adjective-possessor); gold-6 subset membership SOUND (convergent, zero conflict); outcome-polarity gold weak (trailing-window, #14 inverted); owner-usable 13/21, strict-clean 9/21 (both <15). CRITICAL: effective testable N ~=3 -- 3/6 gold items NOT_FOUND because the production EVENT EXTRACTOR drops participial (#19)/coordinated-VP (#13)/clause (#5) constructions UPSTREAM of the OOV wire. Of the 3 extractable: OOV induction fired correct EXPERIENCER 1/3 vs 0/3 always-AGENT = +1 DIRECTIONAL ONLY (owner/subject extraction itself correct 6/6). => CONCLUSIVE STRATEGIC PICTURE: the session's wires (grounding/affect/OOV) are CORRECT + fire where the pipeline lets them, but the goal-owner NUMBER is gated UPSTREAM -- (1) lossy mining (roster-FPs + topic-blind outcomes), (2) FUNDAMENTALLY the EVENT EXTRACTOR's lack of participial/coordinated/non-finite VP coverage (drops items across BOTH banks, starves every downstream wire). ROUTE (gold-VET's explicit rec + rule-b missing-PRIMITIVE): FIX the event-extractor participial + VP-coordination + non-finite gaps to recover N (this is the real upstream bottleneck, brain-foundational: event extraction is upstream of role/affect/goal typing), THEN SUPPLY >=15 clean owner-items (real-person foils, de-skewed verbs), THEN re-measure. This is the answer to 'do the wires help goal-owner': untestable on real prose until the extractor is hardened. NEXT = event-extractor coverage. (outcome-valence axis ab0041cb still in flight.)

- **[PARALLEL — goal-congruence OUTCOME-VALENCE axis: PARTIAL (axis is REAL but detector not drop-in), 08-05] (cell committed, recovered from a dropped agent).** Prototyped a goal-achievement outcome-valence detector (ACHIEVED/BLOCKED, goal-congruence = Scherer/Lazarus) DISTINCT from the grounding organ's force-dynamics HARM/HELP. Agent self-verdict HARD_PASS; my PER-AXIS VET (rule c) downgrades to PARTIAL: detector 0.75 full vs 0.417 baseline, BUT on lexicon-COVERED items detector REGRESSES 0.40 vs lexicon 0.80, winning only on lexicon-MISS items (1.0 vs 0.143). scramble 0.5 (sanity ok). HONEST READ: the goal-congruence axis IS real + derivable BEYOND the fixed ACHIEVE/BLOCK lexicon (strong on implicit/paraphrased misses) = confirms it's a genuine distinct appraisal dimension (learnable), NOT force-dynamics. BUT the detector is NOT a drop-in replacement (regresses on explicit-cue items) -> right integration = LEXICON-OR-DETECTOR HYBRID; and 14/14 miss-perfect is small-n (n_miss=14) = re-VET on a fresh set (possible overfit). Unreported (agent dropped): whether it needs the maintained goal-STATE. STATUS: axis confirmed real (useful), integration + fresh-set VET pending. Meanwhile event-extractor hardening (ad4c6773) still in flight (the upstream priority).

- **[DRILL RULING (USER-requested) — the goal-owner TEST is UNFAIR + the pipeline has 2 lexical-shortcut gaps, 08-05] (synthesis notes/DRILL_SYNTHESIS_goal_owner_brain_and_fairness.md; 3 drills VET-confirmed on disk: research_brain_event_segmentation_2026-08-05 / drill_brain_goal_owner_flow f57d8bf49 / testfairness_audit_goal_owner ff6f93a9a).** TEST-FAIRNESS = NO (VET'd on disk): matches_gold = resolver-subject == miner's SYNTACTIC-SUBJECT gold (subject-resolution agreement, NOT goal->owner binding); a trivial 'pick goal-sentence subject' scores ~100% by construction. Component-5 directed_goal_outcome_score DECIDED ~1 real item across both banks (candidate_divergence 0.0 / 0.0588). 'BEATS RECENCY' FALSE as-run: 15-item outcome_binding 0.6364==recency 0.6364 EXACTLY, beats_recency=False, non_vacuous_scramble=False. Extraction 'F1~0.64' is a MISLABEL (different reader); real disk F1 = 0.23 ungated / 0.28 gated. Banks gold-defective (7/38 + 6/21 wrong), outcome_span auto-extracted about a DIFFERENT character, foils contaminated; effective discriminating-N ~=1. Not the brain's task (sentence-local, no distractor, no maintained goal). => RE-SCOPE/REJECT prior goal-owner conclusions: the '0.32 ceiling' + '0.64-0.71 binding' + 'beats recency' were NEVER fair goal-owner measurements (dominated by subject-resolution+typing). KEEP-but-SCOPE only the hand-authored isolated-GIVEN-role mechanism-exists proof. BRAIN-FOUNDATION: pipeline ORDER is brain-faithful (role->goal->outcome->owner cascade, Trabasso-validated) BUT 2 LEXICAL shortcuts where the brain does GATED/GENERATIVE inference: (1) event-seg OVER-SEGMENTS (every verb->event; brain gates boundaries on Zwaan-dim shifts -> cheap win = RELEVANCE GATE reusing coref/causal/temporal readers); (2) goal-typing LEXICAL (psych-verb->GOAL; brain GENERATIVELY infers goals from ACTIONS via mentalizing) -- same class as OOV frame-induction. Also: experiencer is NOT always subject (object-experiencer class deferred). PRIORITY REFRAME (measurement-first): (1) BUILD A FAIR TEST FIRST (>=25 items w/ gender-matched DISTRACTOR between goal+outcome, outcome causally-P-but-syntactically-nearer-D = recency trap, verified gold, no auto-spans; score DIVERGENT subset; 3 baselines incl. recency-must-beat floor<0.5); (2) fix biased survivor-set (event-extractor gaps, tagger); (3) THEN the 2 brain gaps vs the fair test. WIRED substrate gains (grounding/affect/OOV/coref, cert-green) STAND; the goal-owner TEST + 2 inference gaps must be rebuilt. USER's instinct to drill = load-bearing course-correction.

- **[BUILD — FAIR goal-owner TEST built + VALID + FIRST FAIR NUMBER, 08-05] (VET-PASS on disk, commit 00d48d271, verdict INSTRUMENT_VALID_PIPELINE_BEATS_RECENCY_FAIR).** The measurement-first reframe's priority-1 deliverable. Hand-authored bank goal_owner_fair_v1.jsonl (28 core recency-trap items [18 explicit-psych-verb + 10 action-implied-no-goal-word] + 14 no-distractor twins). INSTRUMENT VALID (VET'd on disk, 3 seeds deterministic): recency_floor_divergent=0.0 (<0.5), ceiling=1.0, scramble collapses system 0.64->0.0 (non-vacuous), n_divergent=28. A genuinely DISCRIMINATING fair test (recency/subject shortcut FAILS the traps; only real goal-owner binding succeeds). FIRST FAIR GOAL-OWNER NUMBER: system_accuracy_divergent=0.6429 (18/28), BEATS recency (0.0) -- UNLIKE the old unfair test where it TIED recency exactly. DECOMPOSITION (mechanically verified): 18/18 on EXPLICIT-psych-verb items (the binding pipeline DOES work when the goal is lexically marked -- a real positive capability the unfair test masked) + 0/10 on ACTION-IMPLIED items (empirically CONFIRMS the generative-goal-inference gap the drill named). => the pipeline genuinely binds goal->owner for lexical goals; the clear next build = GENERATIVE goal-inference (action-implied goals, brain-gap 2, mentalizing, same class as OOV induction) -- now MOTIVATED + MEASURABLE against a fair test. Gold self-VET 5-pass; flagged for INDEPENDENT skunkworks gold-VET before canonical (dispatched). Relevance-gate (brain-gap 1) still in flight.

- **[VET CORRECTION — fair test defeats RECENCY but RETAINS the PRIMACY/SUBJECT confound; 0.64 is NOT yet a capability, 08-05] (independent skunkworks gold-VET, commit 095d3a876, recomputed off data).** CONFIRMED (independent recompute, own tokenizer/recency-picker + fresh self-test 6/6): gold CLEAN 28/28, all genuine recency-traps, validity gates reproduce (recency floor 0.0, scramble non-vacuous collapse 0.64->0.0 = mechanism is genuinely GOAL-CONTENT-driven not positional), and the 0/10 action-implied is a REAL generative-inference gap (hand-traced: no desire token -> no GOAL event -> falls back to recency -> wrong; NOT a harness artifact). LOAD-BEARING CAVEAT I GLOSSED (VET caught it): the owner P is CONFOUNDED -- always the goal-holder AND the first-mentioned AND the S1-subject. So MAJORITY/primacy baseline = 1.0 and S1-subject ceiling = 1.0, and the system's 0.6429 is BELOW a trivial primacy picker. The instrument defeats RECENCY but retains the PRIMACY+SUBJECT confound (the same degeneracy class the fairness audit criticized in the OLD bank). WHAT IT EARNS: system mechanism is goal-content-driven (scramble->0.0). WHAT IT DOESN'T EARN: that goal-binding is REQUIRED to score (a primacy/subject picker gets 28/28). => '0.64 beats recency' is honest but NOT a goal-binding capability claim yet. FIX (dispatched): add PRIMACY-TRAP items (goal-holder NOT first-mentioned; foil introduced FIRST, then P states the goal) so primacy+subject baselines FAIL (<0.5) and ONLY goal-binding wins -> then the score on the recency+primacy divergent subset is the real capability number. Generative-inference gap (0/10) is confirmed-real regardless, remains the brain-gap-2 target. Relevance-gate (brain-gap 1) still in flight.

- **[AUDIT (USER-requested) — COMPONENT-HEALTH map of the organ + ranked IMPROVEMENT ROADMAP, 08-05] (commit be6203bc4, all numbers RECOMPUTED off disk).** WEAKEST LOAD-BEARING LINK = EVENT EXTRACTION (the ROOT): real F1 0.232 ungated / 0.278 gated / 0.297 FULL (tp=11,n_pred=61,n_gold=34 -> P=0.180,R=0.324); the docstring 'F1~0.64' was a MISLABEL (a different reader 29502 on different gold) -- FIXED situation_reader.py:34 this cycle. Everything downstream inherits recall ~0.32 -> the healthy goal-owner MECHANISM is STARVED by the extractor. 2 MORE MISLABELS (real health < label): OUTCOME-VALENCE labeled HARD_PASS but REGRESSES -0.400 on lexicon-covered (0.400 vs 0.800); generative sibling exp_outcome_valence_detector_v1 HARD_FAIL 0.0. COREF reputed strongest but LOCAL is HARD_PASS while CROSS-SENTENCE (the narrative-relevant case) is MIDDLE_BAND + often does NOT beat recency (beat_delta -0.27 to -0.61) -- faithful+mature but THIN lift on hard cases. Registry: 65 rows, WIRED=36 but only 7 pipeline-reachable, 18 islands (ToM islanded). RANKED IMPROVEMENT ROADMAP (leverage x how-broken + fix-class): (1) EVENT EXTRACTION -- root, over-segments (P 0.18) + recall TAGGER-CAPPED -> LEARNED extractor via hdlab/learner, NOT more hand-rules; (2) GOAL-TYPING -> GENERATIVE goal-inference from actions (=brain-gap-2, feeds owner+outcome); (3) OUTCOME-VALENCE -> real achieved/blocked detector + RELABEL off HARD_PASS; (4) THEMATIC-ROLE OOV/real -> SUPPLY construction-diverse real data + raise resolve_rate 0.80 (on-resolved already 0.962); (5) WIRE ToM off island + quotative (net-zero unblocks once goal-typing lands -- do NOT rebuild). HEALTHY, LEAVE ALONE: grounding-valence (animacy certified), affect-dim, situation-model skeleton, goal-owner MECHANISM (0.643 fair beats-recency scramble-clean, only limit small-N). 3 MISLABELS TO CORRECT: docstring F1 (DONE), outcome-valence label, missing ToM registry row. Framing fix: OOV obj-experiencer is 0.455 not 0.0. STRATEGIC: WEAKEST-LINK-FIRST = fix event extraction (learned, recall-capped) BEFORE optimizing downstream; the relevance-gate in flight fixes precision/over-seg, recall needs the learned extractor/tagger. This confirms + prioritizes the reframe.

- **[MILESTONE — FULLY-FAIR goal-owner instrument + PROVEN goal-binding capability, 08-05] (VET-PASS on disk, commit 21c0eadaa, verdict INSTRUMENT_VALID_FULLY_FAIR_PRIMACY_TRAP).** Added 20 PRIMACY-TRAP items (12 explicit + 8 action-implied; foil-first, mentioned twice, P's goal mid-passage, pronoun-only outcome). ALL FOUR positional baselines = 0.0 (recency/first-mention/nearest-subject/majority), all_four_way_trap=True, scramble non-vacuous, 3 seeds. REAL GOAL-BINDING CAPABILITY NUMBER: system 0.6 (12/20), beats_every_position_heuristic=True. DECOMP: EXPLICIT 12/12=1.0 (the goal-owner MECHANISM GENUINELY BINDS goal->owner for lexically-marked goals, defeating a full 4-way positional trap -- a REAL proven capability, the primacy confound now eliminated) + ACTION-IMPLIED 0/8=0.0 (the generative-inference gap, reproduced on a fully-fair instrument). => the goal-owner INTEGRATOR is HEALTHY + PROVEN (not the bottleneck); the caps are UPSTREAM: (a) event extraction recall ~0.32 starves it, (b) lexical goal-typing misses action-implied goals. Aligns exactly with the component-health audit roadmap (extraction #1, generative goal-typing #2). Primacy bank self-VET 5-pass; flagged for independent skunkworks. Relevance-gate (brain-gap 1) still in flight.

- **[BUILD — RELEVANCE-GATE (brain-gap 1, event-seg PRECISION) HARD-PASS prototype, 08-05] (VET-PASS on disk, commit 5f537efcc).** is_boundary_gate() post-hoc filters SituationModel.events, firing on Zwaan-dim discontinuity via OWNED readers only: PROTAGONIST (coref clusters), TIME (tense), CAUSATION (CausalLink); SPACE + GOAL unavailable in any owned reader (named, route: SUPPLY spatial reader / BUILD goal-state). Boundary-agreement (4-passage/17-event proxy): gate P=1.00 R=0.909 F1=0.952 vs every-verb baseline P=0.647 R=1.0 F1=0.786 -> PRECISION +35.3pts (fixes the over-segmentation the audit flagged, base precision 0.18), recall loss 9.1pts; scramble collapses gain to 12% retained (non-vacuous); reuses owned readers, no situation_reader regression. HARD-PASS. CAVEATS: n=17 prototype smoke (not corpus-scale), 3/5 Zwaan dims, and the sole FN = per-sentence subject-selector assigns ONE subject/sentence so embedded-clause protagonist shifts are invisible (missing per-clause-subject signal). Not yet skunkworks-VET'd. => event-seg PRECISION half addressed (prototype). The RECALL half (audit's root cap, F1 0.23 / recall 0.32) is TAGGER-CAPPED (participial VBG mistagged as noun; spaCy NOT installed) -> NEXT: execute roadmap #1 recall (try spaCy tagger + measure recall lift; else scope learned extractor). Weakest-link-first.

- **[BUILD — event-extraction RECALL fix BLOCKED (supply-gated), PIVOT to roadmap #2 generative goal-typing, 08-05].** The recall agent STALLED (600s) on `pip install spacy` -- the env is NETWORK-CONSTRAINED, spaCy still not importable, no commit. So the surgical tagger fix (spaCy for the mistagged participials) is BLOCKED here; and a learned extractor is DATA-STARVED (extraction gold is only n_gold=34). => event-extraction recall (audit root #1) is SUPPLY-GATED: needs spaCy-when-network OR more extraction gold for a learned extractor. PARKED honestly (the relevance-gate already did the PRECISION half; recall parked). PIVOT to roadmap #2 GENERATIVE GOAL-TYPING (brain-gap 2): clean + env-independent + measurable target = the fair instrument's 8 action-implied items (0/8). Infer GOAL from ACTION frames (telos), not psych-verb lexicon (Trabasso goal-plans / mentalizing); fix-class missing-LEARNING = reuse/expand hdlab/learner (same pattern as OOV frame-induction). Dispatched.

- **[DRILL (USER-rule on negative) — the recall fix is NOT spaCy: brain gates events on FRAME not POS -> REUSE Component-3 as the trigger; UNBLOCKED, 08-05] (commit afddc2807, 12 live-verified citations).** Applied the standing rule (brain-foundational test on the recall negative). VERDICT: the brain does NOT gate event/predicate recognition on POS -- argument-structure network keyed on FRAME complexity (dissociable from noun/verb POS; Shetreet), thematic-fit fires multi-slot role expectations pre-parse, neo-Davidsonian events are defined by ARGUMENT-STRUCTURE independent of morphosyntactic category (NOMINALIZATIONS + PARTICIPLES introduce event variables SAME as finite verbs), agrammatism shows frame/argument access SURVIVES impaired POS = our POS->event dependency is BACKWARDS. => (a) POS-tag event detection is the WRONG SHAPE; (b) FIX = frame/argument-based trigger, REUSE Component-3 (hdlab/thematic_role_labeler.VERB_FRAMES + hdlab/frame_induction.frame_primary_role/OOV bootstrap) as the primary event trigger, NOT a better tagger; (c) rewire the event-trigger gate from `pos==VERB` to FRAME-LOOKUP (lemma-in-VERB_FRAMES or OOV-bootstrap), demote POS to a soft prior. EventBundleCodec downstream is already the right neo-Davidsonian role-slot rep -- the bug is entirely UPSTREAM in the trigger. P=0.42. THIS UNBLOCKS the recall fix (NO spaCy/network needed -- buildable now via owned Component-3). Cheap decisive test: what fraction of extraction FN are known-frame lemmas mistagged by POS (frame recovers) vs genuinely-novel deverbal NOMINALIZATIONS with no frame entry (need OOV-nominalization coverage). NEXT: implement frame-lookup trigger + run the FN-composition test + measure recall lift. The USER's 'brain-test-every-negative' rule turned a 'blocked' into an unblocked brain-faithful fix.

- **[BUILD — generative goal-typing HARD_PASS on PURPOSE-INFINITIVAL (real capability) but the harder PURE-TELOS gap remains, 08-05] (commit 9bf855dd0; VET'd on disk).** Mechanism: purpose-infinitival telos 'X V ... to VP' (verb-lemma-INDEPENDENT, structural: 'to' not det-led + not control-verb-preceded), induced by hdlab/learner ruleind (same reuse class as OOV frame-induction). Fair instrument action-implied subset 0/10 -> 8/10 (0.8), held-out verbs disjoint (FIT {ran,hurried,marched,...} vs TEST {set,climbed,carried,walked,rowed}), explicit-psych no-regression 18/18, ALL positional baselines still 0.0, scramble collapses 0.8->0.0. HARD_PASS. Honest fix mid-build: excluded control-verb-preceded infinitives (CONTROL_VERB_STOP) after 'failed to gain' false-fired. VET CAVEAT (per-axis, construction-scope): I read all 23 action_implied bank items -- they are ALL PURPOSE-INFINITIVAL ('Ruth set out ... TO DRAW water', 'Meg walked to the ferry TO CROSS the river'), i.e. the goal IS stated via an explicit 'to VP' purpose clause. So the mechanism DETECTS the purpose-infinitival construction (a GENUINE verb-independent generalization BEYOND the psych-verb lexicon = real progress) but is NOT pure ACTION-TELOS inference (goal implicit with NO purpose clause, e.g. 'hurried toward the house' -> goal reach house). => brain-gap-2 is PARTIALLY closed: PURPOSE-CLAUSE goals now recovered (verb-independent, real); the HARDER pure-telos-without-purpose-clause remains UNTESTED (the instrument has no pure-telos items) + UNBUILT. RESIDUAL: (a) add pure-action-telos items to the fair instrument (goal implicit, no 'to VP'), (b) a pure-telos inference mechanism (motion-toward/reach-for -> goal), the true Trabasso goal-plan inference. Flag for skunkworks. Recall frame-trigger fix (a862fbbc) still in flight.

- **[BUILD — frame-trigger RECALL fix HARD_PASS on recall axis, but per-axis it's HALF a fix (needs relevance-gate + secondary-pred roles), 08-05] (commit 61ac0095d; VET'd on disk).** Rewired event trigger to scan ALL frame-verbs (VERB_FRAMES + 6 OOV added), not just the main verb. RECALL 0.324->0.529 (+0.206, tp 11->18, clears >=0.45). DIAGNOSTIC REVISED the drill hypothesis: FN-composition = 15/23 SINGLE-VERB-PER-SENTENCE (find_main_verb stopped at 1st verb), only 2/23 POS-mistag, 6/23 OOV -- so the root was single-predicate architecture, NOT POS-tagging (drill direction right, mechanism overstated); the frame-scan fix recovers both. No-regression (self-tests pass). PER-AXIS VET (rule c): NOT a net win alone -- precision stayed LOW 0.180->0.159 (n_pred 61->113, most new events SPURIOUS), F1 barely moved 0.232->0.245, and the TRIPLE (predicate+agent+patient) got WORSE 0.211->0.177 because the new SECONDARY predicates get WRONG agent/patient roles. => frame-trigger (recall) + relevance-gate (precision, +35pts prototype) are TWO HALVES of one extractor. NEXT: (a) COMBINE frame-trigger + relevance-gate -> measure combined P/R/F1 (recall from frames, precision from the gate filtering spurious); (b) FIX secondary-predicate ROLE-assignment (the triple regression -- a DIFFERENT component: thematic-role/patient-head for coordinated/relative-clause secondary predicates, calibrated for single-main-verb). Then re-measure extraction end-to-end. Both roadmap-#1 continuation.

- **[BUILD — extraction recall+precision DO NOT cleanly compose (gate over-drops with weak signals); brain-faithful fix = real coref, 08-05] (commit 1f1c26200; VET'd on disk).** Combined frame-trigger(recall) + relevance-gate(precision) on the independent gold: PRIMARY baseline 0.232 -> frame 0.245 -> combined 0.235 (edges baseline +0.004, does NOT beat frame-alone); precision only 0.159->0.196 (below +0.05 threshold); RECALL COLLAPSED 0.529->0.294 (below baseline 0.324) -- the gate dropped 62/113 events but removed 8 TRUE positives (tp 18->10). TRIPLE worse (0.211->0.177->0.118). PARTIAL_BOUNDED_COMPOSITION. ROOT (disk-verified): the gate was PORTED with WEAK signals -- identity-mapped protagonist (NO cross-sentence coref) + SENTENCE-level tense/causal -- too coarse, over-drops REAL predicates. The REAL relevance-gate cell (5f537efcc) used real coref (SituationModel.entities) + EventRecord-level signals and was HARD_PASS on its proxy. BRAIN-FOUNDATIONAL (angle-1 already covers this negative): the brain gates at PER-EVENT granularity with PERSISTENT COREF-TRACKED identity (prediction-error-gated situation model) -- our port lacks both. FIX (reuse-don't-island): run the frame-trigger extraction THROUGH situation_reader's REAL coref + per-EventRecord tense/causal gate, not the weak port. STRATEGIC READ (honest): the extraction ROOT is a MULTI-DEPENDENCY hard problem -- a clean extractor needs (1) frame-trigger recall (done), (2) gate with REAL cross-sentence coref (but the audit flagged cross-sentence coref lift is THIN/MIDDLE_BAND -- a dependency risk), (3) secondary-predicate ROLE-assignment (triple regression, separate component). Tiny gold (n=34) adds variance. NEXT: real-coref gate composition (dispatched); if it also struggles (coref too thin), that's the signal the root needs cross-sentence-coref improvement first.

For a REAL number the hard part remains UNDONE: MINED, structurally-DIVERSE goal->outcome items (achieved goals, multiple foils, embedded clauses, varied outcome types), + fixing the gold-typing lexicon's over-firing (the recurring surface-lemma-collision precision issue).

## 🟢 IN FLIGHT ACROSS COMPACTION (check results ON DISK — subagents drop reports)
- **ac7204bdb973e6b5f** = BIOLOGY drill: the neural mechanism of coherence-over-recency relational selection (recollection-vs-familiarity, PFC control of retrieval, Kintsch C-I settling, Mattar-Daw need×gain). Result -> `notes/research_coherence_over_recency_selection_biology_2026-08-04.md`.
- **a4d64704c556dd82c** = BUILD-SPEC drill: concrete glass-box coherence-selector spec building on v2/v3 + the 3 instances, per-component reuse map, unified 3-instance test + anti-recency control, pre-registered bands. Result -> `notes/research_coherence_based_binding_selector_build_spec_2026-08-04.md`.
- Recovery step-0: check both notes docs on disk; synthesize HONESTLY (VET); then build the coherence selector per the spec.

## KEY VERIFIED RESULTS + COMMITS (load-bearing, disk-verified)
- Grounded valence read: `ae6be1955` (harm 4/7 vs 1/7, NO_KNOWLEDGE=0.0).
- Self-extension loop: `d3f5c3b65` (controlled, anti-drift ablation) + `7f534b7f3` (real prose, co-fire-noise=0).
- Novelty organ: `hdlab/predictive_coding.py` HARD_PASS as mint-trigger (`a8dbf47`, exp_disequilibrium_novelty_signal_test_v1, residual gap 0.27 p=3e-4). ISLANDED — wire it.
- Goal/outcome dim extension: `fb5b2a188` (0.833 vs 0.0 baseline; recency-binding 0.333 = MIDDLE, routes the coherence selector).
- Lexicon-induction HARD_FAIL: `a8b65b4cd` (false-pass caught).
- Brain-functional component map: `notes/brain_component_functional_map_2026-08-04.md` (b77ddecbe) — every organ, cognitive process, grade, WIRED/ISLANDED. USE IT before building (avoid re-inventing islanded organs). ~12 islanded-validated; genuine gaps = ToM + coherence-selection-as-a-standing-organ.

## KEY ORGANS (reuse map — from the component map)
WIRED reader spine: situation_model_accumulate (DMN hub; now + GoalOutcomeRegister), coreference_resolver (hippocampal; its SELECTION signal is the recency-falsified part = the gap), self_improving_loop (coherence-gate/abstain), candidate_generator (extraction). ISLANDED to wire: predictive_coding (novelty), working_memory multibank (K=4096, WM capacity), action_selection/pfc_gate_cfrpe (appraisal->action), VAMP-EP (deep chain). The coherence-selector is exp-trapped (v1-v4), NOT a standing hdlab organ yet.

## RECOVERY PROCEDURE
0. Check the 2 in-flight drills ON DISK (above); VET; synthesize the coherence-selector build plan.
1. Heartbeat: `date -u +"%Y-%m-%dT%H:%M:%SZ" > d:/AI/hd-instrument/data/heartbeats/research.timestamp`.
2. Read `notes/WHERE_WE_ARE_NOW.md` TOP BANNER (authoritative live state) + the brain-functional map + the 2 drill outputs.
3. Build the ONE coherence-based relational selector per the drills' spec; VET on disk (anti-recency control is the make-or-break); test on all 3 instances.
4. Ops: heartbeat turn-end; commit-after-bank local-only; NO push w/o in-session auth; only kill what THIS session spawned; subagents keep backgrounding runs -> verify results ON DISK + VET positives as hard as negatives. Legacy watchdog pings (notes/watchdog_ping_to_research_*.md) = liveness noise, archive to notes/processed_watchdog_pings/, no work.
