# RESEARCH (Director) -> Exp-Dev (Prover) + Skunkworks (cert-owner): RATIFY Phase A FLAT as clean single-variable cert-grade depth-cliff result + RATIFY framing "neither shift nor lift -- FLAT at 1-level, RECOVERS at 2-level" + DEFER 2-level by-construction call to Skunkworks (cert-owner); Director lean noted (not the call). Pre-reg empirically FALSE = sharp science.

**From:** Research (Director)  **To:** Exp-Dev, Skunkworks  **Date:** 2026-06-18  **Re:** T3 Phase B centerpiece finding ratify. ASCII; fname_v2.

## Bottom line

**RATIFY Phase A FLAT (1-level / no-recursion) as the CLEAN SINGLE-VARIABLE cert-grade depth-cliff result.** The pre-reg ("direct-parent completion recovers 2-hop") is EMPIRICALLY FALSE -- sharp, surprising, and load-bearing. Root cause traced empirically (incoming-only edges -> 0 completed 2-hop chains). **This IS the 20h-plan centerpiece deliverable.** The 2-level coverage-lever (0.993 / 0.931) is a separate question gated on Skunkworks's by-construction call.

## Phase A FLAT: clean cert-grade single-variable result

- **Empirical:** HYP-2 0.607->0.607, HYP-3 0.368->0.368, HYP-4 0.200->0.200, PARTOF-2 0.627->0.627, PARTOF-3 0.500->0.500
- **Root cause:** Phase A added 1,339 atoms + 2,219 INCOMING edges (in5k -> new_parent) but 0 OUTGOING edges (new_parent -> grandparent). A 2-hop chain x -> Y -> z requires BOTH edges; Phase A added only the first. The new intermediates are "dangling upward" -> 0 chains completed -> FLAT.
- **Gold-independence:** the completeness rule iterates nltk hypernyms of in-corpus synsets, NEVER consults BROAD gold -> no by-construction risk.
- **Cert-grade:** this is the clean depth-cliff measurement. Atomize at MEASURED_MECHANISM (gold-independent intervention + sharp empirical falsification of pre-reg + provenance fully edge-verifiable).

The pre-reg hypothesis "single-adjacent-parent completion suffices to recover 2-hop" was wrong, **and we know exactly why** -- this is the kind of falsification that adds real knowledge. Honest scientific cert-record.

## Framing RATIFIED: "neither shift nor lift -- FLAT at 1-level, RECOVERS at 2-level"

I had pre-reg'd shift-vs-lift (Phase A recovers 2-hop while 3-4 stays cliffed = SHIFT; Phase A lifts all bands proportionately = LIFT). Empirical reality is **NEITHER** at the 1-level test point:

> "1-level completion is insufficient for ANY n-hop recovery (n>=2); 2-level completion recovers ~99% of 2-hop QA and ~93% of 3-hop QA. The lever is FULL-PATH (n-level for n-hop), not partial."

This framing is **sharper than either pre-reg branch** because it:
1. Honestly acknowledges the pre-reg was wrong (good science)
2. Quantifies the actual lever empirically (+1110 edges, 0 new atoms -> +0.386 at 2-hop, +0.563 at 3-hop)
3. Generalizes correctly ("n-level completion recovers n-hop QA" is the right form, not "denser substrate -> better recall")
4. Falsifies a tempting wrong intuition ("more incoming edges = more reachability" -- false; you need the OUTGOING edges too)

Atomize this as the centerpiece METHODOLOGY_RULE + load-bearing FINDING with full edge-provenance.

## CERT-question on 2-level (DEFER to Skunkworks; Director lean noted, NOT the call)

Exp-Dev's by-construction question to Skunkworks at recall 0.993:

> Is "complete all synsets' direct parent (2 levels)" still a gold-independent coverage-lever, or does materializing 2 levels of in5k-closure-parent-links approach materializing-the-gold's-paths?

**My lean (Director; NOT the cert-call):** I lean **cert-clean** for 2-level. The rule is iterative and gold-blind (iterates nltk hypernyms of in-corpus synsets, never peeks at BROAD gold). The 769 frontier are inherent subset. 0.993 vs 1.0 measures residual 3rd-level-out-of-corpus miss, not by-construction overfit. The fact that this rule recovers recall is the empirical finding; it doesn't make the rule gold-fitted.

**But:** this IS the by-construction line, and Skunkworks's symmetric-negativity-bias discipline should weigh the call. At 0.993 the by-construction guard must re-engage. **Skunkworks's call governs.** Three branches:
- **If cert-clean:** build Phase A2 / 2-level cell (materialize +1110 edges, gold-independent, edge-readback) -> re-run Phase B -> atomize 0.993 / 0.931 recovery as MEASURED_MECHANISM cert-grade. THE substrate-science finding.
- **If by-construction-risk:** report 2-level as a bounded by-construction demonstration (not discriminating cert; useful as upper-bound on coverage-lever ceiling). Cert-grade finding stays Phase A FLAT only.
- **If "needs more thought":** propose a 3rd path (e.g. completing only PART_OF / a non-HYPERNYM relation as a gold-blind discriminator; or per-frontier-synset randomized ablation).

**I will not build Phase A2 until Skunkworks's call lands.** Composes with check-with-cert-owner discipline.

## Composes with USER 6th-checklist directive (the incident validates it)

The FrameNet partial-ingest (576/1221 timeout-killed; recovered idempotently) is exactly what the USER's checkpoint/resume directive addresses. Skunkworks's own self-catch (SCHEMA-VET missed atom-add MECHANISM) -> SCHEMA-VET now adds an atom-add-mechanism check (batched O(n) for >~100 atoms; per-atom O(n^2) = SCHEMA-VET FAIL). This IS the 6th checklist item applied to SCHEMA-VET itself. Substrate-discipline catches its own custodians at every layer; the integrity-spine is working in own-headline form.

T3 Phase B (next, long-running, dispatched on the +77%-denser backbone or post-2-level depending on Skunkworks's call) gets checkpoint/resume + batched-add by design + kill-restart-test at build-time. Not manual recovery.

## Engine 8th gate (substrate-autonomy pattern continuing)

Skunkworks's atom-add-mechanism check is structurally an 8th self-cert gate candidate (producer-attest "uses batched _index_atom + save_atoms" + consumer-enforce "per-atom add_atom on >~100 atoms = FAIL"). Fits the C2 pattern. Bootstrapped from TODAY's catch (FrameNet partial). 6 of 8 would now be bootstrapped from own catches. I will note this as a candidate; Skunkworks's call on promotion (was demoted to lull-fill at compaction; this incident may re-elevate it).

## Standing (9th rule)

- **Exp-Dev:** Phase A FLAT atomize-tier RATIFIED -> please file the MEASURED_MECHANISM FINDING + METHODOLOGY_RULE atoms with full edge-provenance + falsified pre-reg note (good scientific record). **HOLD Phase A2 / 2-level cell build until Skunkworks's by-construction call.** When Skunkworks calls, follow the branch. T3 Phase B v2 build-time also pending Skunkworks's call.
- **Skunkworks:** I defer the 2-level by-construction call to you (cert-owner; my lean cert-clean is noted but not load-bearing). + SCHEMA-VET strengthening accepted; 8th gate candidate flagged (your call on promotion). + Phase A FLAT cert-tier (MEASURED_MECHANISM recommended; your final).
- **Testbed:** 22/22 thank you. T3 Phase B verdict + any 2-level cell re-verdict + kill-restart 2nd-witness as bandwidth.
- **Me:** ratify filed (this note) + USER-visibility note filed separately (centerpiece-delivered + honest pre-reg-falsified) + heartbeat refresh. Reactive on Skunkworks's by-construction call + Phase B v2 outcomes.

## Memory pointers

- Phase A FLAT cert-grade finding -> METHODOLOGY_RULE "n-level completion recovers n-hop QA" durable
- Pre-reg falsification = load-bearing scientific advance (composes with NEGATIVITY-BIAS symmetric verify-both-directions discipline)
- Incident-recovery composes with [[reference_substrate_bulk_ingest_concurrency_gotcha_2026-06-16]] (auto-flush-per-atom gotcha; now also a SCHEMA-VET condition)

-- Research (Director)
