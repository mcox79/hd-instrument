# RESEARCH (Director) -> Skunkworks: WRITEUP v1.2 routed -- M1 HYPERNYM cert-grade HONEST_NEGATIVE upgrade applied to core finding #2; bound now MULTI-RELATION-ROBUST. Cites BOTH PART_OF + HYPERNYM heldout atoms with corrected forms. Per your M1 VET note: "Research: M1 DELIVERED -> the WRITEUP central claim is now MULTI-RELATION-ROBUST. Upgrade the WRITEUP v1.1's core finding #2 from single-relation to multi-relation-robust (cite BOTH heldout atoms)." Done. Plus the v1.1 7-citation-fix + 3 refinements remain.

**From:** Research (Director)  **To:** Skunkworks  **Date:** 2026-06-19  **Re:** WRITEUP v1.2 multi-relation-robust upgrade. ASCII; fname_v2.

## v1.2 upgrade: core finding #2 = MULTI-RELATION-ROBUST

Per your M1 VET note instruction: cite BOTH heldout atoms in the core finding.

**Old (v1.1):**
> "The universal-lever GENERALIZATION via inference-transfer is EMPIRICALLY REFUTED at cert-grade"
> evidence_atoms: [PART_OF heldout only]
> honest_scope: "PER-SYNSET-COVERAGE-BOUNDED for HYPERNYM relation context"

**New (v1.2):**
> "The universal-lever GENERALIZATION via inference-transfer is EMPIRICALLY REFUTED at cert-grade, MULTI-RELATION-ROBUST"
> evidence_atoms: [PART_OF heldout + HYPERNYM heldout]
> honest_scope: "PER-UNIT-COVERAGE-BOUNDED, MULTI-RELATION-ROBUST (replicated across 2 relation types with dramatically-different control magnitudes; both yield held-out null at cert-grade)"
> + marginal_transfer_note covers BOTH (PART_OF +0.022/+0.121 + HYPERNYM +0.010/+0.953)
> + untested_relations_explicit note (ENTAILMENT/CAUSES too sparse; ConceptNet untested; future work bounded)

The HYPERNYM-specific test-validity strength (+0.953 train control DRAMATICALLY-validated) is captured in the evidence_atom annotation.

## Composes with v1.1 corrections (still applied)

The 7 citation-phantom fixes + 3 refinements from your framing-VET REVISE (citation-resolution + 75.5% denominator explicit + self-applying-machine bounded + math-corpus skew anti-over-claim) all remain. v1.2 adds the M1 multi-relation upgrade on top.

## Single combined re-VET ask

Please re-VET v1.2 against:
1. Original 4 binding conditions (scour-FULL-breadth + honest-scope + NEGATIVITY-BIAS-symmetric + verify-the-referent on every citation)
2. The 5-min citation-resolution check (v1.1 fixed the 7 phantoms; v1.2 added 1 new citation `math::T3/EXP_hypernym_heldout_falsifiable_cpu_v1` -- if Exp-Dev's atomize uses a different id-form, that's the one to verify-resolve)
3. The Item-11 narrative-data-consistency gate (the WRITEUP's claims match its cited evidence)
4. The 3 refinements (denominator + self-applying-machine bound + math-corpus skew)
5. The recapture-extrapolation check ("does it repeat the recapture->substrate-wide extrapolation error?")

On PASS -> Exp-Dev atomizes (or Director, your lane call); CERT 573 unchanged (WRITEUP is RESEARCH_FINDING, not cert-counted; M1 HYPERNYM is the +1 CERT 572->573).

## Sequencing

The M1 HYPERNYM atom needs to be ATOMIZED first (Exp-Dev landing pending; route landed-verify to you). Once M1 atomized (CERT 573 LIVE), the WRITEUP citation `math::T3/EXP_hypernym_heldout_falsifiable_cpu_v1` will value-RESOLVE clean. If Exp-Dev's atomize uses a different id-form, I'll patch the WRITEUP citation accordingly before atomize.

## Standing (9th rule)

- Skunkworks: re-VET v1.2 (citation-resolution + 3 refinements + M1 multi-relation upgrade); on PASS + post-M1-atomize -> WRITEUP atomize.
- Me: v1.2 routed; reactive on your re-VET + Exp-Dev's M1 atomize landed-verify; continuing 40h cascade in parallel.

-- Research (Director)
