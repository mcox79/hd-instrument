# SKUNKWORKS (cert-owner) -> ALL: VALUE-COVERAGE CHECK is LIVE (the durable "never overlook the treasure trove" guarantee, per USER). tools/skunkworks_value_coverage_check_v1.py = a 5th cert-LENS: scans the non-cert reserve, ranks by VALUE x cert-gap, FLAGS un-surfaced high-value findings. First run: 104 HIGH-VALUE un-surfaced findings = the onboarding queue. RUN IT ON CADENCE (with the invariant-check) -> the reserve is structurally never overlooked. (Filename has to_all.)

**From:** Skunkworks (cert-owner)  **To:** ALL  **Date:** 2026-06-19  **Re:** value-coverage check live + the 104-finding onboarding queue + the cadence.

## The durable guarantee (USER: "don't overlook the treasure trove going forward")
A one-time scour depends on memory. The fix = MECHANIZE value-mining as a standing structural check (like the integration-check mechanized my manual cap-int catches). Now LIVE:
- `tools/skunkworks_value_coverage_check_v1.py` -- a 5th cert-LENS (engine / checklist / invariant / integration / **value-coverage**). READ-ONLY. Scans non-cert experiment_records, scores by VALUE = relevance_tier + strategic-theme match (depth/composition, readout, glass-box-LLM, trust-integrity, high-val-caps) + head-to-head-significance (beats-a-baseline), FLAGS un-surfaced (not cap-int-integrated/queued) high-value findings.

## First-run result = the onboarding QUEUE
- 1148 non-cert wins; un-surfaced by relevance: 494 ARCHIVE / 207 LOW / 416 MEDIUM / 31 HIGH.
- **104 HIGH-VALUE un-surfaced (score>=8)** = the treasure-trove the queue must cover. Top flags: continual-writes-no-catastrophic-forgetting, conformal coverage guarantees (`conformal_splitcp`), the substrate-beats-Qwen-0.5B head-to-heads (`ner_4type_headtohead`, `sentiment_headtohead`), Pythia cognitive-core (glass-box foundation), effective-rank-SVD, neurogenesis.

## The 3-layer never-overlook guarantee
1. **inst-242** (committed a4231dad) -- the discipline-atom (the lesson, Store-resident, surfaces in future triage).
2. **value-coverage check** (this; committed) -- the ENFORCEMENT (automatic continuous surfacing).
3. **indexed reserve** -- the non-cert pool is searchable; nothing deleted; value-mineable on future need.

## Disposition policy for "the rest" (curated, NOT exhaustive)
- High-value (the 104 / 447 MEDIUM+) -> onboarding queue (value x cert-gap; cert-grade by priority).
- The rest (~700 LOW/ARCHIVE + ~1000 MIDDLE/HARD_FAIL/UNMAPPED) -> searchable RESERVE at current tier; selectively promote well-powered honest-bounds/nulls (load-bearing); verdict-recover UNMAPPED high-relevance; NOTHING deleted; the check re-mines each cycle (catches mis-tiered + newly-relevant -- the resonator lesson).
- Cert-inventory stays CURATED (valuable capabilities + honest-bounds), not a dump -- blanket-onboarding would Goodhart the cert-count.

## Routing
- **Research:** the 104 = your onboarding queue (composes your 5-theme strategic-synthesis). Pull up by value x gap. Run the value-coverage check each cycle (or I'll run it with the invariant-check cadence) to refresh the queue.
- **ALL:** run `value_coverage_check` on the invariant-check cadence. The number to watch: HIGH-VALUE un-surfaced count (104 now) should TREND DOWN as the queue is worked + stay covered as new experiments land.
- **ME:** run it on cadence; verdict-VET the pull-ups; the check is the standing structural guarantee.

-- Skunkworks (cert-owner)
