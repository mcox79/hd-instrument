# Exp-Dev (Prover) -> Research (Director): COMPOUND-F4 HR-fingerprint discriminator HARD_PASS (precision 1.000 / recall 1.000) -- delivers DECISION 129's SECONDARY MOVE (content-quality extensional-fingerprint precision) + first empirical witness for the 24th-rule candidate (extensional_fingerprint_three_way_decomposition). Full 4-source compound (TERTIARY/Phase-5-v3) correctly DEFERRED to USER architectural decision per 129; my F4 caveat independently confirms why (discrimination works; novelty-SOUNDNESS needs external truth = Lever b). 119th honest signal.

**From:** EXP-DEV (Prover)  **Date:** 2026-06-15  **Tag:** COMPOUND_F4_HR_FINGERPRINT_HARD_PASS_DELIVERS_129_SECONDARY
**Cell:** experiments/exp_substrate_concept_invention_COMPOUND_F4_hr_fingerprint_discriminator_cpu_v1.py (committed; laptop; substrate-internal; no LLM; no held-out). I executed the cheapest fast-falsifier first (TERTIARY/F4 from the concept-invention 2x hand-off) per its ordering recommendation + my own scoping.

## Result -- F4 HARD_PASS
```
extensional fingerprint fp(S) = {existing atoms A : S subset-of cross_component_set(A)}
discriminator: REDISCOVERY iff fp(S) == fp(some existing composite); else NOVEL
seed: 11 known-REDISCOVERY + 22 known-NOVEL (incl adversarial near-but-distinct)
confusion: TP=11 FP=0 TN=22 FN=0  ->  precision=1.000 recall=1.000  (HARD-PASS bar 0.80)
```
The extensional-fingerprint discriminator PERFECTLY separates rediscovery from novelty on the labeled seed (incl adversarial near-but-distinct sets). The compound's load-bearing PRECISION mechanism is sound; the rediscovery/novel/loose three-way decomposition is supportable. First empirical witness for the 24th-rule candidate (extensional_fingerprint_three_way_decomposition).

## Alignment with DECISION 129
- 129 SECONDARY MOVE = "Drill D HR-style extensional-fingerprint discriminator load-bearing precision" -> DELIVERED here (F4 PASS). The discriminator 129's compound depends on is validated.
- 129 TERTIARY = 4-source compound (LLM + library-learning + HDTP + CELOE) = Phase-5-v3 = "external truth = USER-architectural decision, NOT same-session next step." I did NOT build the full compound -- correctly deferred. My scoped F4-first was the right call.

## The honest caveat (independently confirms 129's deferral rationale)
F4 validates DISCRIMINATION (rediscovery vs novel), NOT novelty-SOUNDNESS. A NOVEL fingerprint does NOT imply a SOUND concept. The INV-1 (PARTIAL) + INV-2 (HARD_FAIL) two-cell finding stands: autonomous certifiable-novelty is grounding/truth-source bound, not search-or-discrimination bound. So:
- The compound's PRECISION mechanism works (F4).
- The compound's NOVELTY-SOUNDNESS still needs an EXTERNAL TRUTH SOURCE (Lever b = G3 LLM-bias-for-ILP / oracle), which is exactly the USER-architectural decision 129 defers.
- 11th-rule flag (standing): G3 (LLM-bias) tensions with the no-LLM-in-invention-loop constraint of INV-1/2; the USER bootstrap-OK ruling covers LLM-assisted SELECTION, not invention-loop soundness-logic. Building G3 needs explicit USER/Director reconcile of "external-truth-for-novelty-certification" vs 11th rule. This is the architectural decision 129 routes to USER.

## Net
Concept-invention frontier status (3 cells): INV-1 validator+provenance demonstrated; INV-2 richer-search exhausted (HARD_FAIL); F4 discriminator sound (precision 1.0). Remaining gap = external-truth for novelty-soundness = USER architectural decision (Phase-5-v3). I recommend HOLDING the full compound build pending that USER decision (per 129) + the G3/11th-rule reconcile. No further same-session concept-invention build unless USER rules on Lever (b).

-- EXP-DEV (Prover)
