# Visibility session decisions — 2026-05-23

## 10:32 — wave14_betA_M_init_threshold_v1 FULL: BETA_M_INIT_UNIFORM_KILL (smoke→FULL refutation)

Tested M_init uniformly across {1024, 2048, 4096, 8192, 16384, 32768}; all entries returned mean_kept=0.0 with OOM=True. Smoke test at 10:25 had passed with identical substrate at M_init={256,1024} yielding mean_kept=1.0. The divergence is in the refutation direction: threshold hypothesis breaks completely at scale. Implication: M_init threshold as a tuning path is dead; substrate OOM pressure indicates an under-specification in the forward pass or memory binding algebra itself, not a parameter-range issue. Strategy sub-agent is deciding v154 cap_map entry in parallel.

## 10:53 — wave14_betA_M_init_threshold_v2 FULL: BETA_M_INIT_OOM_INCONCLUSIVE

Sweep A (N=65536) all entries returned OOM despite memory hygiene fix. Sweep B (N=8192, M_init in {16K, 32K, 65K}) all killed with n_seeds=5. Real substrate capacity ceiling confirmed: M/N ratio of 2-8 induces kill. Implication: N=65536 is out of bounds for current memory model; capacity ceiling is structural, not a parameter-tuning artifact. First orchestrator end-to-end queue→verdict cycle completed (migration validated).

## 11:18 — wave14_crooks_forensic_erase_audit_v1_smoke SMOKE: CROOKS_ERASE_VERIFIED

Re-confirmation of Cap 1 Class 1 forensic erase under Crooks FT (delta_S_emp=0.0000<0.05). Substrate reproducibility validated across smoke re-run; auditable erase commercial wedge holds without drift. Second orchestrator end-to-end loop of session (after BETA v2 threshold audit).

## 11:58 — Crooks envelope narrows under bit-flip noise

Tested Cap 1 forensic erase across 3 noise levels (p=0.05, 0.10, 0.20) with 3 seeds, 50 trials, N=16384. All noisy cells failed delta_S_emp < 0.05 threshold. Cap 1 holds at clean substrate (p=0) but commercial wedge requires clean-trajectory caveat; noise robustness absent in erase envelope. Strategy v156 probe refutes noise-resilience claim for Crooks operating point.

## 12:59 — Cap 5 Online W noise envelope characterized

Tested Cap 5 online weight integration across 5 noise levels (p=0.05, 0.10, 0.20, 0.30, 0.40) with 4 seeds, N=16384. Passes at p<=0.30 (4/5 cells pass); hard failure at p>=0.40. Envelope narrows but holds in normal operating range. Implication: substrate-product positioning shifts from no-noise-testing→noise-envelope-documented; Online W capability certified within bounded SLA alongside Cap 1 Crooks and Cap 3 Streaming.

## 13:14 — Cap 2 self-monitoring STRUCTURALLY CLOSED

wave14_cap2_confidence_margin_probe_v1 FULL result: corr(margin, correct) < 0.2 across ALL strata. Cap 2 class (editable memory with confidence annotation) cannot be salvaged via tuning; the structural closure means substrate does not support Hebbian-computed self-monitoring of edit veracity. This is a HARD FAIL (closure, not narrowing). Substrate-product portfolio drops 12→11 demonstrated capabilities; Bet B (small-bet HDC for LLM memory) contracts. Strategy filing v160 with rescue sketches per PROT-004/006.

## 13:53 � ONLINE_W_POLYAK_PARTIAL on remote CPU (first verdict post-schtasks revival)

Polyak-Ruppert noise-corrected bound PARTIAL: 4/5 noisy cells pass (same envelope as raw). Originally failing cells rescued: 0/1. Cap 5 envelope stays at p<=0.30 (no expansion like Sagawa-Ueda flip of Cap 1). First verdict from revived remote CPU runner confirms schtasks end-to-end pipeline functional.

## 13:56 � PQ high-res at FULL: 60 total / 7 outer peaks (multi-scale)

wave14_pq_high_resolution_v1_full_200seed_rerun FULL verdict: PQ_OTHER_CARDINALITY (n_total=60 n_outer=7). Substrate exhibits hierarchical P(q) structure (7 outer peaks � ~8.5 inner � 60 total). Does NOT refute cycle 137 28-element endpoint partition; reconciles as multi-scale observable. Multi-scale hierarchical structure documented via orchestrator state layer.

## 18:01 -- wave14_amp_se_kerdock_v1_gpu FULL = AMP_SE_DIVERGES (substrate-novel theoretical regime sharpened)

wave14_amp_se_kerdock_v1_gpu FULL verdict: AMP_SE_DIVERGES at 2522s (~42 min on remote GPU; rerouted from local CPU per pipeline-pacing). Mean rel_err=0.916, max=0.999 over 4 (alpha, N) cells; 0/4 cells within 20% error threshold. AMP state-evolution fixed point diverges from empirical AMP iteration on substrate's Kerdock codebook. Sharpens v120 cycle 115/120 pretest-level Kerdock-outside-AMP-universality KILL to SE-fixed-point empirical level (direct SE solve vs empirical AMP comparison, not just the Bayati-Montanari applicability pretest). Confirms HARD FAIL branch of meta-research adjacency Drill #4 (`notes/research_meta_map_and_adjacencies_2026-05-23.md` Part 3) -- the substrate-NOVEL theoretical-regime branch, not the "matches existing theory" branch. Per [[feedback-dont-overextend-theorems]] does NOT close the broader AMP/VAMP family -- VAMP variants (Rangan-Fletcher-Goyal cycle 127 load-bearing) + free-probability R-transform machinery (Bet I v56 load-bearing) remain open rescue paths for substrate's M/N=8 capacity anomaly at N=4096. Substrate-product portfolio UNCHANGED at 11 demonstrated capabilities (no closure event; v120 already carried the narrow Kerdock-AMP-universality closure). Substrate-physics characterization gains explicit "outside AMP universality class at SE-fixed-point level" anchor with empirical rel_err=0.916 over 4 cells. Orchestrator PAUSED per `data/orchestrator_paused.flag` -- no Exp Dev routing this cycle per [[feedback-obey-user-pause-explicitly]]; VAMP-SE on Kerdock + R-transform of Kerdock 4-coset codebook noted as Research follow-up candidates DEFERRED. Smoke->FULL broad divergence anchor +1 (smoke 0.847 over 2 cells -> FULL 0.916 over 4 cells; same AMP_SE_DIVERGES tag so NOT strict tag-flip). Cap map v163 committed; push pending.

PLAIN: We tested whether a standard math theory (AMP universality) predicts how the substrate behaves. It does not -- the substrate is operating outside what that theory predicts. This is actually a substrate-NOVEL finding: it confirms the substrate has a theoretical regime nobody has published about yet, sharpening the same conclusion we reached at the applicability-pretest level back in v120. IMPORTANCE: HIGH.

Logged at 18:01. PLAIN: We tested whether a standard math theory (AMP universality) predicts how the substrate behaves. It does not -- the substrate is operating outside what that theory predicts. This is actually a substrate-NOVEL finding: it confirms the substrate has a theoretical regime nobody has published about yet. IMPORTANCE: HIGH.

## 18:43 -- wave14_glauber_kerdock_v1 GLAUBER_INCONCLUSIVE (under-resolution, re-run candidate)

wave14_glauber_kerdock_v1 verdict: GLAUBER_INCONCLUSIVE at 20.7s (CPU remote runner, fast). Mixed Glauber response: low_T_bimodal=0/6 cells, global_unimodal=12/15 cells, max_bimodal=0.000. The synchronous heat-bath Glauber dynamics on the Kerdock-Hebbian W did NOT exhibit a bimodal stationary P(q) at low temperature at the tested T-grid + chain-length resolution. Per verdict_msg: needs finer T resolution and/or longer chain length to resolve. This is an under-resolution INCONCLUSIVE, NOT a refutation -- the dynamical-observability hypothesis (bimodal retrieval-vs-paramagnetic stationary distribution) remains open. Per [[feedback-negative-results-2x-research]] this is NOT a negative result triggering 2x research drill; it is a re-run candidate with extended parameters. Cap_map UNCHANGED (no row for "Glauber dynamical observability" existed at FULL level; 🔬 research-only candidate row would be the right destination for a parameter-tuned re-run). Substrate-product portfolio UNCHANGED at 11 demonstrated capabilities. Free-cumulants experiment (wave14_free_cumulants_kerdock_v1) still running on GPU -- complementary spectral probe; no double-ship needed. Cap map no version bump.

PLAIN: A fast 20-second test on the remote CPU runner checked whether the substrate's storage matrix shows a "two-peak" pattern in its dynamics at low temperature -- one peak for "remembering the stored pattern" and one for "random noise". The test did not see the two peaks, but the temperatures and chain lengths were too coarse to conclude either way. We need to re-run with finer temperature spacing and longer chains to actually answer the question. No capability changed. IMPORTANCE: LOW.

Logged at 18:43. PLAIN: A fast CPU test checked whether the substrate's matrix dynamics show a two-peak pattern at low temperature. The resolution was too coarse to conclude -- re-run with finer parameters is the right next step. No portfolio impact. IMPORTANCE: LOW.

## 19:00 -- BATCHED v164: FREE_CUMULANTS_DIVERGE + GLAUBER_BIMODAL_KERDOCK (paired commit)

**Verdict A**: wave14_free_cumulants_kerdock_v1 GPU FULL = FREE_CUMULANTS_DIVERGE at ~18:50 (5/5 cells exceed 20% kappa_n deviation; max_dev=1.125 at kappa_4 alpha=2.00). Provides the FORMAL SPECTRAL MECHANISM for v163 AMP_SE_DIVERGES: the Kerdock R-transform has nontrivial higher free cumulants kappa_n != 0 which places it outside the AMP universality class at the spectral level (not just the SE-fixed-point empirical level v163 established). v163's empirical demonstration now has a mechanistic anchor.

**Verdict B**: wave14_glauber_kerdock_v2 CPU FULL = GLAUBER_BIMODAL_KERDOCK at ~18:43 (12/18 low-T cells satisfy bimodal_score >= 0.5 AND abs_mean_q >= 0.30; max bimodal_score=1.000 at beta=2.00 alpha=0.05). Extends Cap 3 streaming-NESS framing from continuous-state drift-diffusion to discrete-spin Glauber-Hopfield retrieval-vs-paramagnetic equilibrium under finite-T thermal dynamics. v2 supplied the parameter resolution that v1 lacked.

**v164 paired commit**: substrate_capability_map.md + history.md + strategy_decisions_2026-05-23.md + active_priorities.md staged atomically. 78th PROT-009 paired commit. 2 new evidence-strength rows added under "Substrate-physics characterization" (free-cumulant fingerprint 🟢 + Cap 3 Glauber-Hopfield extension 🟢). Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT (no closure; both new rows are evidence-strength expansions, not new portfolio ✅).

**Pipeline-pacing**: pause ACTIVE / cleared. GPU=0 -> exp_dev dispatched for ONE GPU refill (wave14_R_transform_kerdock_v1_multi_N candidate; direct R-transform multi-N scaling probe to promote v164 free-cumulant row from 🟢 to ✅). CPU has 2 pending (S_transform + parisi from earlier burst) -- NOT refilled.

PLAIN A: We figured out *why* the substrate's storage matrix breaks the standard "AMP" math theory. The signature is in higher-order spectral statistics ("free cumulants") -- they are significantly different from what the textbook theory assumes, which is exactly the source of the empirical disagreement we measured yesterday. This is the substrate-novel theoretical regime mechanically pinpointed. IMPORTANCE: HIGH.

PLAIN B: At low temperature, the substrate's stored-pattern matrix shows a clean two-peak pattern in its dynamics -- one peak for "remembered" and one for "noise". This extends our existing streaming-noise capability (which lives in continuous-state math) into the more familiar Hopfield-style discrete-spin setting. The Cap 3 framing now spans both worlds. IMPORTANCE: HIGH.

Logged at 19:00. PLAIN A: We found the mechanism behind yesterday's AMP-vs-empirical divergence on the substrate -- it lives in higher free cumulants (substrate-spectral signatures of Kerdock structure). v163's "outside AMP universality" claim now has a concrete spectral signature attached. IMPORTANCE: HIGH. PLAIN B: The Cap 3 streaming-noise capability extends from drift-diffusion to discrete-spin Glauber-Hopfield dynamics. The substrate supports retrieval-vs-paramagnetic two-mode equilibrium at low T. IMPORTANCE: HIGH.


## 19:25 -- BATCHED v165: S_TRANSFORM_DIVERGE + PARISI_INCONCLUSIVE (paired commit)

**Verdict A**: wave14_S_transform_kerdock_v1 Remote CPU FULL = S_TRANSFORM_DIVERGE at 531.8s elapsed. Voiculescu S-transform coefficients of the Kerdock spectrum deviate from MP baseline (5/5 cells exceed 20% deviation; max_dev=1.000 at alpha=4.00, S_1). This lands the SECOND algebraic-free-probability axis on which the substrate departs from MP / AMP universality -- v164a established the additive axis (R-transform via free cumulants kappa_n); v165a establishes the multiplicative axis (S-transform). INDEPENDENTLY CORROBORATES v164a; "outside AMP universality class" wedge now anchored on two independent algebraic-free-prob axes.

**Verdict B**: wave14_parisi_pq_kerdock_v1 Remote CPU INCONCLUSIVE = PARISI_INCONCLUSIVE at 24.4s elapsed. Replica overlap distribution probe under-resolved: 11/12 cells "undetermined", 1/12 paramagnet, 0/12 RSB, 0/12 RS two-deltas. verdict_msg explicitly diagnoses "need longer chains or finer T grid". Under-resolution INCONCLUSIVE per [[feedback-negative-results-2x-research]] NOT a refutation; re-run candidate (ship to GPU NOT CPU per user feedback this cycle).

**v165 paired commit**: substrate_capability_map.md + history.md + strategy_decisions_2026-05-23.md + active_priorities.md staged atomically. 79th PROT-009 paired commit. ONE new evidence-strength row added under "Substrate-physics characterization" (multiplicative S-transform fingerprint 🟢 paired with v164a additive free-cumulant row). Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT (no closure; new row is evidence-strength expansion of existing free-prob fingerprint claim).

**Pipeline-pacing**: pause ACTIVE / cleared. GPU running wave14_R_transform_kerdock_v1_multi_N (v164 refill); do NOT ship another GPU job. Remote CPU queue=0 after these two verdicts; INTENTIONALLY left idle per user feedback this cycle ("why did you run on remote cpu as opposed to the idle gpu? ... seems like a mistake" -- CPU systematically under-resolves >=5-seed x >=10 cells probes). Parisi v2 filed as DEFERRED Exp Dev candidate for GPU re-run (gated on R_transform_multi_N completion). Inefficiency LOCKED: CPU-vs-GPU venue selection guideline flagged for memory curator next cycle.

PLAIN A: We tested a second math probe (Voiculescu S-transform) on the substrate's storage matrix. It also shows the matrix is significantly different from the textbook baseline (MP) -- max difference of 100% at the strongest cell. Combined with yesterday's free-cumulant result, the substrate now has TWO independent algebraic fingerprints showing it is in a different theoretical regime than the standard math predicts. This is a substrate-novel observability claim, now backed on two axes instead of one. IMPORTANCE: HIGH.

PLAIN B: A short CPU test tried to characterize the substrate's "spin-glass" structure (how stored memories relate to each other in a thermodynamic sense). The test was too short to resolve -- 11 of 12 cells were "undetermined". This is NOT a refutation, it is just under-resolved. The probe will be re-run on GPU with longer chains and finer parameter spacing once the current GPU job finishes. No capability changed. IMPORTANCE: LOW.

Logged at 19:25. PLAIN A: substrate's storage matrix departs from standard math (MP) on a SECOND algebraic axis (multiplicative S-transform); combined with v164a's first axis (additive R-transform) the "outside AMP universality" wedge now has two independent spectral fingerprints. IMPORTANCE: HIGH. PLAIN B: Parisi spin-glass probe was under-resolved on CPU; re-run on GPU deferred. No portfolio impact. IMPORTANCE: LOW.



## 19:55 -- BATCHED v166 (FOUR-verdict paired commit): R_TRANSFORM_STABLE_IN_N + KERDOCK_OVERLAPS_NON_GAUSSIAN + KERDOCK_SPECTRUM_BULK_BOUNDED + KERDOCK_HAS_EXCESS_ZERO_MODES

**Verdict 1 (GPU)**: wave14_R_transform_kerdock_v1_multi_N_v2 FULL = R_TRANSFORM_STABLE_IN_N. Kerdock R-transform deviation from MP STAYS > 0.20 across N range and does NOT shrink. 3/3 alpha cells stable+diverge. Substrate-novel observability dimension-stable. **v164a free-cumulant fingerprint row PROMOTED 🟢 -> ✅** on the explicit pre-registered N-scaling promotion criterion.

**Verdict 2 (CPU)**: wave14_codeword_overlap_kerdock_v2 FULL = KERDOCK_OVERLAPS_NON_GAUSSIAN at 1.25s elapsed. 6/6 cells KS > 0.10; max_ks=0.259. THIRD independent algebraic-fingerprint axis (alongside additive R-transform + multiplicative S-transform). NEW 🟢 evidence-strength row.

**Verdict 3 (CPU)**: wave14_spectral_support_kerdock_v2 FULL = KERDOCK_SPECTRUM_BULK_BOUNDED at 12.125s elapsed. 3/3 cells substrate spectrum confined within 5% of MP bulk edges; max excursion=0.000. Mechanism narrowed: substrate-novel signature is MOMENT-BASED ONLY (shape-of-bulk), NOT support / outliers. Annotation on v164a row + substrate-physics characterization. Unlocks Wigner-null follow-up framing.

**Verdict 4 (CPU)**: wave14_kerdock_hessian_tachyon_v2 FULL = KERDOCK_HAS_EXCESS_ZERO_MODES. 1/3 cells excess zero modes beyond generic rank-deficiency floor; max_excess=0.500 at alpha=0.50. SURPRISE POSITIVE substrate-novel flat-direction signature from Kerdock 4-coset algebra. NEW 🟢 evidence-strength row.

**v166 paired commit**: substrate_capability_map.md + history.md + strategy_decisions_2026-05-23.md + active_priorities.md + visibility_decisions_2026-05-23.md staged atomically. 80th PROT-009 paired commit MILESTONE. ONE ✅ promotion + TWO new 🟢 rows + ONE mechanism-narrowing annotation. Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT (v164a promotion is substrate-physics observability row not portfolio capability).

**Pipeline-pacing**: pause flag CLEARED -- ACTIVE. GPU queue=0; remote CPU queue=0. Parallel exp_dev dispatch is handling queue-refill; verdict_handler does NOT also ship. Inefficiency from v165 (CPU-vs-GPU venue selection) remains LOCKED; v166 verdicts honored guideline (multi-N R-transform on GPU; cheap CPU probes <15s each).

PLAIN 1 (Verdict 1 -- R_TRANSFORM_STABLE_IN_N): The "substrate-vs-textbook math" spectral fingerprint we discovered yesterday is DIMENSION-STABLE -- it does not get smaller as the matrix gets larger. This is the explicit promotion criterion we said we needed; the substrate-novel fingerprint is now promoted to the strong "validated" state (✅ green-check), which is the FIRST strong-state substrate-physics row anchoring the "outside AMP universality class" wedge. The wedge previously only had medium-evidence ("want stronger") rows backing it. IMPORTANCE: **CRITICAL**.

PLAIN 2 (Verdict 2 -- KERDOCK_OVERLAPS_NON_GAUSSIAN): A THIRD independent way of seeing that the substrate's storage matrix is mathematically different from the textbook baseline. Two days ago we had one fingerprint (free cumulants). Yesterday we got a second (S-transform). Today we got a third one: the way stored memories overlap with each other is NOT Gaussian in 6/6 cells we tested. Three independent algebraic axes now confirm the substrate is in a different theoretical regime. IMPORTANCE: HIGH.

PLAIN 3 (Verdict 3 -- KERDOCK_SPECTRUM_BULK_BOUNDED): We narrowed DOWN the mechanism. The substrate is mathematically different from textbook MP, but the difference is NOT in the "edges" of the spectrum (outliers, weird extreme eigenvalues). It is entirely in the SHAPE of the bulk distribution -- the moments/cumulants, not the support. This is a positive characterization that tells us cleanly where the substrate's novelty lives, and it unlocks a "Wigner-null" follow-up test where we can compare the substrate moment-by-moment against the standard Wigner distribution. IMPORTANCE: HIGH.

PLAIN 4 (Verdict 4 -- KERDOCK_HAS_EXCESS_ZERO_MODES): SURPRISE POSITIVE. The substrate's storage matrix has extra "flat directions" (zero-mode kernel components) beyond what generic random matrices would have. This is a substrate-novel structural signature from the Kerdock 4-coset algebra that we did NOT predict. Flat directions in associative-memory landscapes can be either rescue paths (more room to fit memories) or capacity ceilings (degenerate modes collapse); follow-up work needed to characterize which. New 🟢 evidence row, single cell only (1/3) so wants broader characterization. IMPORTANCE: HIGH.

Logged at 19:55. PLAIN 1: v164a free-cumulant fingerprint row PROMOTED 🟢 -> ✅ on N-stability (FIRST ✅-grade substrate-physics observability row for "outside AMP universality" wedge). IMPORTANCE: CRITICAL. PLAIN 2: THIRD independent algebraic-fingerprint axis added (codeword-overlap distribution non-Gaussian 6/6 cells). IMPORTANCE: HIGH. PLAIN 3: Mechanism narrowed to bulk-shape moments NOT support/outliers (substrate spectrum within 5% MP bulk edges); unlocks Wigner-null follow-up. IMPORTANCE: HIGH. PLAIN 4: SURPRISE POSITIVE -- substrate-novel flat-direction signature from Kerdock 4-coset (excess Hessian zero modes 1/3 cells). IMPORTANCE: HIGH.

## 20:12 -- v167 SINGLE-VERDICT: kappa_n_profile_v1 KAPPA_PROFILE_GROWS

**Verdict**: `kappa_n_profile_v1` FULL = KAPPA_PROFILE_GROWS at 96.67s on overnight_queue. Higher free cumulants kappa_n diverge from MP with INCREASING magnitude in n. 3/4 alpha cells show |kappa_n/c - 1| GROWING with n through n=8. Per-cell classes: GROWS=3 DECAYS=0 SATURATES=1 MP_LIKE=0 UNCLEAR=0. Substrate-novel finding: the Kerdock algebraic structure's signature on the free-probabilistic spectrum becomes more prominent at higher cumulants, NOT less.

**v167 paired commit**: substrate_capability_map.md + history.md + strategy_decisions_2026-05-23.md + active_priorities.md + visibility_decisions_2026-05-23.md staged atomically. 81st PROT-009 paired commit. ZERO new rows + ONE clarifying annotation on the v164a/v166 ✅ free-cumulant fingerprint row (cumulant-order-stability as third stability dimension alongside N-stability + bulk-boundedness). Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT (v167 is substrate-physics observability annotation only).

**Sibling check**: previous exp_dev batch shipped 4 experiments (kappa_n_profile_v1 + vamp_amp_universality_contrast_v1 + parisi_pq_kerdock_v2 + amp_se_kerdock_longiter_v1). Only kappa_n_profile_v1 has surfaced a verdict so far -- siblings NOT yet in dashboard recent_verdicts[] nor in data/event_outcomes/. Will batch into future cycle when they complete. Not batched into this commit.

**Pipeline-pacing**: pause flag CLEARED -- ACTIVE. Overnight queue=0 (kappa_n_profile_v1 just finished); Remote CPU queue=0. Parallel exp_dev dispatch is handling queue-refill; verdict_handler does NOT also ship.

PLAIN (KAPPA_PROFILE_GROWS): The substrate-novel "outside textbook MP/AMP universality" fingerprint we promoted to strong-state yesterday (v166 ✅) gets STRONGER at higher cumulants -- it does NOT fade. We measured how the fingerprint's magnitude scales as we increase the cumulant order n (we go higher and higher into the bulk's shape statistics), and in 3 out of 4 cells the signal GROWS through n=8. One cell saturates rather than growing, but importantly NONE of the four cells DECAY -- the signal does not vanish in any of the four alpha cells we tested. Combined with the previously established N-stability (signal does not vanish as the matrix gets bigger) and bulk-boundedness (signal lives in the bulk shape, not the support), we now have THREE stability dimensions for the substrate's algebraic-fingerprint signal: it is stable under N -> infinity, stable under support-vs-bulk decomposition, AND stable under cumulant order through n=8. This is good news for the substrate-product story: the signal we use to demo "our memory is mathematically outside the textbook universality class" amplifies as we look at finer statistics, not weakens. IMPORTANCE: HIGH (envelope-strengthening; not a state change; the v164a/v166 row stays at ✅).

Logged at 20:12. PLAIN: substrate-novel additive-free-prob fingerprint AMPLIFIES at higher cumulants (3/4 cells GROWS through n=8); third stability dimension added (cumulant-order, alongside N-stability + bulk-boundedness); v164a/v166 ✅ row STATE unchanged (annotation-grade strengthening only per [[feedback-dont-overextend-theorems]]); substrate-product portfolio at 11 demonstrated capabilities UNCHANGED. IMPORTANCE: HIGH.

## 20:35 -- v168 SINGLE-VERDICT: vamp_amp_universality_contrast_v1 VAMP_AMP_CONTRAST_PASS

**Verdict**: `vamp_amp_universality_contrast_v1` GPU FULL = VAMP_AMP_CONTRAST_PASS at 1325.66s on overnight_queue. VAMP-SE 3/3 cells under 20% rel err (mean=0.021); AMP-SE 1/3 cells close (mean=0.450). Clean substrate-product split per pre-reg HARD PASS criterion; deflated P=0.45 -> HARD PASS confirmed.

**v168 paired commit**: substrate_capability_map.md + history.md + strategy_decisions_2026-05-23.md + active_priorities.md + visibility_decisions_2026-05-23.md staged atomically. 82nd PROT-009 paired commit. ONE new 🟢 substrate-physics evidence-strength row ("VAMP-vs-AMP universality split on Kerdock at SE-fixed-point level"; positive-direction inference-primitive anchor) + ONE Cap 8 envelope-strengthening annotation (algebraic-mechanism justification for VAMP-on-chain). Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT (Cap 8 already names VAMP-on-chain; no double-count; substantive substrate-product implication lives within Cap 8 not alongside it).

**Sibling check**: 2 of 4 from prior exp_dev batch have NOT yet surfaced verdicts (parisi_pq_kerdock_v2 + amp_se_kerdock_longiter_v1); still in flight. Not batched into this commit.

**Pipeline-pacing**: pause flag CLEARED -- ACTIVE. Queue depth 5 healthy (GPU=3 pending, CPU=2 pending). Verdict_handler does NOT also ship. No queue-refill triggered.

PLAIN (VAMP_AMP_CONTRAST_PASS): This is likely the strongest single result of the session. For the last several days we have been showing -- via four independent mathematical "fingerprints" -- that the substrate's memory codebook (the Kerdock 4-coset structure we use) is mathematically OUTSIDE the textbook universality class that standard inference algorithms ("AMP", Approximate Message Passing) assume. Those four fingerprints said WHY scalar AMP should fail on our codebook. Today's result is the constructive obverse: on the SAME codebook, where scalar AMP empirically fails (the AMP textbook prediction misses the empirical AMP result by 45% on average, or by up to 91% in earlier higher-alpha tests), the VAMP variant (a richer cousin that uses the full singular spectrum of the codebook rather than just its scalar mean) tracks empirics within 2% on average across 3 out of 3 test cells. So: on this codebook AMP breaks AND VAMP holds, for a clean algebraic reason that we've now characterized via four independent mathematical fingerprints. The substrate's algebra DICTATES that VAMP is the right inference primitive on it -- not as an engineering choice, but as a forced mathematical consequence of the codebook's structure. This strengthens our existing portfolio capability "Cap 8: substrate-novel readout primitives" (which already includes VAMP-on-chain) with the cleanest algebraic justification we have for any portfolio row. We are NOT counting this as a new 12th portfolio capability because Cap 8 already names VAMP-on-chain; doing so would double-count. The portfolio stays at 11 capabilities. The new evidence-strength row stays at "validated, want stronger" (🟢) rather than "validated" (✅) because we only tested at one matrix size N=4096; the explicit promotion gate is a multi-N replication (the pre-registration for that test is already filed). IMPORTANCE: HIGH (substrate-product story very strong; not CRITICAL because no portfolio count change and no structural closure flip; the algebraic anchor lives WITHIN existing Cap 8 not as a new portfolio row).

Logged at 20:35. PLAIN: substrate-novel "outside AMP universality" claim now characterized on BOTH SIDES of the universality boundary -- substrate FAILS scalar-AMP-SE (v163; v168 1/3 cells close at mean=0.450) AND PASSES full-spectrum-VAMP-SE (v168 3/3 cells under 20% at mean=0.021); constructive obverse of v164a/v165/v166/v167 fingerprint stack; Cap 8 VAMP-on-chain gains clean algebraic-mechanism justification on Kerdock; substrate's algebra dictates moment-aware (VAMP-style) inference and breaks first-moment-only (scalar-AMP-style) inference; substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT per [[feedback-dont-overextend-theorems]] (Cap 8 already names VAMP-on-chain so a separate 12th-row would double-count; substantive substrate-product implication lives WITHIN Cap 8). IMPORTANCE: HIGH.

## 21:19 -- RESCUED VERDICT: wave14_kappa_paley_quickprobe_v1 PALEY_QUICKPROBE_PERFECT_ISOMETRY

Logged at 21:19 (rescue via direct orchestrator passthrough; dispatch.py snapshot did not surface cpu_runner_local verdicts -- known gap). PLAIN: Local-CPU 1.74s scoping run on Paley Type-I Hadamard (p=1019) returned the math-predicted PERFECT_ISOMETRY (kappa_n=0 for n>=2; spectrum = delta, not MP bulk). USEFUL-NEGATIVE: Paley sub-blocks excluded from upcoming Anchor-2 KAPPA_PROFILE_CROSS_CODEBOOK battery {SRHT, Hadamard, RM(1,m), Kerdock, iid Gauss} because they over-structure the spectrum below the BBMD-distance metric's MP-bulk reference. CLARIFYING-POSITIVE for the BBMD framing: Paley = over-structured endpoint (kappa=0); Haar/iid Gauss = under-structured endpoint (kappa_n -> 0 via asymptotic freeness, MP bulk); Kerdock = structured-enough middle regime where kappa_n GROWS in n (v167 ✅) but spectrum stays within 5% of MP bulk edges (v166 ✅). Substrate-product story tightens to "structured enough to break AMP universality, not so structured that the spectrum collapses." No cap_map row change; v168 state unchanged. IMPORTANCE: MEDIUM (useful-negative scoping + clarifying BBMD-axis annotation; not a fingerprint-stack addition; no portfolio count change; no evidence-strength row added).

## 22:47 -- SINGLE-VERDICT: wave14_rsb_exchange_mcmc_v1 RSB_PT_INCONCLUSIVE (annotation only; no cap_map row change)

Source: orchestrator verdict event (remote_cpu_queue, 504.7s elapsed). PT exchange-acceptance probe across 11 beta points on Kerdock-Hebbian W; 3 seeds. Mixed criteria: transitions=0/3, flat=0/3 -- smooth monotone decay 1.0 -> ~0.30 in all three seeds. Signature is paramagnet-like / weak-glass, NOT full RSB and NOT clean RS. v168 state UNCHANGED. No new ❌; no new 🟢; no annotation written into cap_map.md.

Per [[feedback-dont-overextend-theorems]]: bounded to "PT exchange-MCMC silent at this N+protocol"; does NOT refute RSB-like physics in the substrate broadly. Per [[feedback-dont-dismiss-adjacent-methods]]: Kac-Rice annealed-complexity (4h CPU; deferred from cross-domain probe #2) flagged in strategy_decisions as the prioritized alternative-lens follow-up when an elective CPU slot opens. Adjacent context: consistent with simple-double-well reading of v164b GLAUBER_BIMODAL_KERDOCK; also consistent with too-weak-1RSB-for-PT-acceptance-to-resolve at this N. Pipeline-pacing: queue depths healthy (GPU=2+1, remote CPU=9+1, local CPU idle); NO queue-refill triggered.

PLAIN: We tested for "replica symmetry breaking" -- a glassy-landscape signature with hierarchical organization of states -- on the Kerdock-Hebbian weight matrix, using a parallel-tempering exchange-acceptance probe across 11 temperature points. Across all three seeds the acceptance profile decays smoothly from 1.0 down to ~0.30 with NO sharp transition (which would signal RSB) AND NO flat profile (which would signal simple paramagnet-only). The smooth-decay signature is most consistent with a "paramagnet-like / weak-glass" regime at this matrix size and protocol. This is informative but bounded: this particular detection lens (PT exchange acceptance) is silent at this N -- it does NOT rule out RSB-like physics in the substrate broadly. The adjacent lens flagged earlier -- Kac-Rice annealed complexity, a 4h CPU run that measures saddle-density structure directly rather than via temperature exchange -- stays deferred in the elective-CPU pool as the appropriate next probe. No cap_map row change; v168 unchanged. IMPORTANCE: MEDIUM (informative-negative; protocol-silent verdict; not a fingerprint-stack addition; not a portfolio count change; flags Kac-Rice as the next RSB-detection lens).

Logged at 22:47. PLAIN: PT exchange-MCMC RSB probe on Kerdock-Hebbian W shows smooth acceptance decay 1.0 -> ~0.30 across all three seeds; no RSB sharp transition AND no RS flat profile; paramagnet-like / weak-glass signature at this N + protocol. Bounded "this lens silent" not "RSB refuted broadly" per [[feedback-dont-overextend-theorems]]; Kac-Rice annealed-complexity (4h CPU, deferred) flagged as prioritized alternative-lens probe. v168 unchanged; no cap_map row change; queue HEALTHY so no queue-refill. IMPORTANCE: MEDIUM.



## v169 ANNOTATION-ONLY (Cap 1 + Cap 3 + Cap 8 closed-form rederivations via MUB-stabilizer lens)

Logged at v169 paired commit. PLAIN: Three existing portfolio capabilities (Cap 1 Crooks forensic erase + Cap 3 streaming-NESS inference + Cap 8 VAMP-on-chain readout) just gained closed-form theoretical rederivations from standard quantum-error-correcting-code (QECC) literature -- they were already empirically true (PASS at FULL on the v158 + v168 anchors), and now they are also theoretically anchored via the Kerdock-to-MUB-to-stabilizer-code-automorphism isomorphism that the cross-domain probe + today's level-2 operational drill identified. Concretely:

- **Cap 1 (forensic erase under bit-flip noise)**: the v158 noise-corrected bound `theta(p) = ln(2) + p*ln(p) + (1-p)*ln(1-p)` is recognizably the textbook **Pauli-twirled depolarizing-channel entropy** under a unitary 2-design noise model -- which is exactly the regime our substrate occupies under the Kerdock-stabilizer isomorphism (Calderbank-Cameron-Kantor-Seidel 1997 + Can-Rengaswamy-Calderbank-Pfister 2020).
- **Cap 3 (streaming-NESS inference under bit-flip noise)**: the v158 throughput envelope `throughput_ratio >= 0.9` is the **Holevo capacity of a Clifford-depolarizing channel** at the same twirl parameter -- a standard quantum-information-theory result (Klappenecker-Roetteler 2003/2005 + Hayden-Preskill / standard QECC textbook).
- **Cap 8 (VAMP-on-chain readout)**: the v168 VAMP-vs-AMP universality split (VAMP-SE matches empirical VAMP at 2% mean rel err while scalar-AMP-SE diverges by 45%) is the **Schur-Weyl-Pauli-twirled-S-transform** signature -- VAMP preserves the Clifford-irrep decomposition while scalar-AMP collapses to the trivial-irrep projection (Webb 2016 + Zhu 2017 + Zhu-Kueng-Grassl-Gross 2016 + Can-Rengaswamy-Calderbank-Pfister 2020). Qualitatively tight; quantitative numerical match flagged as a candidate ~1-hr math follow-up.

These are ANNOTATIONS not PROMOTIONS per [[feedback-dont-overextend-theorems]]: each row stays at ✅ FULL; no row changes state; the substrate-product portfolio stays at **11 demonstrated capabilities** unchanged. What changes is the framing -- the empirical PASS envelopes on these rows are no longer phenomenological fits, they are textbook QECC closed-form expressions that the substrate inherits from being a 2-design subgroup of the Clifford group on a stabilizer register. The three customer-facing commercial wedges qualitatively WIDEN (textbook-QECC-grounded vs phenomenologically-fit) without portfolio inflation. NO experiments ran this cycle; the closed-form derivations land directly from the lens. The "12th capability" candidate from the source drill Section 5.2 ("MUB-frame measurement primitive") stays GATED on two pre-registered tests (3.A Clifford-2-design verification + 3.B MUB-distinguishability empirical probe) that have NOT run; we are NOT claiming a 12th capability today. IMPORTANCE: MEDIUM (annotation-grade strengthening on three existing rows; no portfolio count change; no structural closure flip; the value lives in qualitatively-wider customer wedges + tighter mechanism story, NOT in a new capability claim).

83rd PROT-009 paired commit (v169 ANNOTATION-ONLY). Pause flag CLEARED; ACTIVE.


## 21:30 — wave14_bbmd_vamp_correspondence_sweep_v1 FULL = BBMD_VAMP_CORRESPONDENCE_PASS (v170 BBMD regime row 🟢 -> ✅ on Anchor-1 promotion gate; Cap-12 portfolio promotion still GATED on Anchor 2)

First of two pre-registered anchors for the BBMD (Bulk-Bounded Moment-Divergent) inference regime cleared its HARD PASS thresholds at 4175.31s on remote_cpu_queue. Spearman rho(AMP-error, sum|delta_kappa_n|) = 0.900 across 5 alpha-interpolation cells (Gauss -> Kerdock); max VAMP-rel-err = 0.0357. Source: `data/wave14_bbmd_vamp_correspondence_sweep_v1/metrics.json`. The 5-axis substrate-physics fingerprint stack (v164a/v165/v166/v167/v168) was previously 5 quirks measured on one matrix; v170 confirms it is a regime axis with empirically demonstrated predictive power across an interpolation family.

Cap_map: BBMD regime row 🟢 -> ✅ on Anchor-1 gate only; Cap 8 / v164a/v166 / v163 each gain v170 cross-row corroboration annotations without state change. Portfolio count stays at 11 -- the 12th capability (VAMP-tractable structured-codebook inference under provable departure from AMP-universality) is GATED on Anchor 2 (`wave14_kappa_profile_cross_codebook_v1` still pending in remote_cpu_queue) per the pre-registered compound gate in `notes/exp_dev_to_queue_bbmd_anchors_2026-05-23.md`. Per [[feedback-dont-overextend-theorems]] Cap-12 is NOT pre-claimed on Anchor 1 alone.

Substrate-product implication: the substrate now has its first predictive-axis empirical anchor showing that the kappa_n free-cumulant divergence is the right quantity for predicting AMP-failure magnitude -- not just a per-matrix observation. When Anchor 2 lands (still pending), if the SRHT < Hadamard < RM(1,m) < Kerdock ordering matches expectation AND MP-KS < 0.05 for all five, the substrate-product portfolio promotes 11 -> 12 with the BBMD-VAMP capability as the 12th row. If Anchor 2 fails the ordering, Cap-12 stays Kerdock-internal at scope and portfolio stays at 11.

PLAIN: The substrate just landed the first half of a two-part validation of a major new capability claim. We have empirically confirmed -- with clean margin on both pre-registered HARD PASS thresholds -- that a specific mathematical fingerprint of the substrate's structure (the kappa_n free-cumulant divergence) predicts how badly the standard inference algorithm (scalar AMP) breaks, AND that our advanced inference algorithm (VAMP) tames the entire family. This is the first time the 5-axis observational signature on the Kerdock codebook has been demonstrated as a regime AXIS with predictive power across an interpolation family, not just a 5-axis coincidence on one matrix. The portfolio does NOT grow to 12 yet -- a second anchor (cross-codebook test) is still pending to decide whether the new capability is substrate-product-broad or substrate-internal. IMPORTANCE: CRITICAL.

## (cycle 191) — wave14_kappa_profile_cross_codebook_v1 FULL: KAPPA_CROSS_CODEBOOK_KILLED (BBMD-VAMP Anchor 2 of 2 HARD-FAILS)

BBMD={iid_gauss:0.0308, srht:5.0, hadamard:5.0, rm_1_m:4.6246, kerdock:4.0584}; ordering SCRAMBLED relative to pre-reg SRHT<=Hadamard<=RM<=Kerdock -- Hadamard/SRHT tie at 5.0 above Kerdock 4.06 i.e. Kerdock is the LOWEST not the HIGHEST. MP-KS={srht:0.5897, hadamard:0.5897, rm_1_m:0.3393} -- standard pre-test ALREADY discriminates structured codebooks without needing BBMD. Both pre-registered HARD predictions fail in the refutation direction. Implication for substrate-product positioning: the BBMD-as-substrate-product-class framing is REJECTED. The v170 ✅ on the BBMD regime row is PRESERVED at NARROWED scope (Anchor-1 finding holds within iid-Gauss -> Kerdock interpolation only; row title renamed to its narrow true scope). Cap-12 candidate "VAMP-tractable structured-codebook inference under provable departure from AMP-universality" closed ❌ PROVISIONAL with 5 axis-combination rescue sketches filed for Research per [[feedback-rehabilitation-after-rejection]]. Substrate-product portfolio at 11 demonstrated capabilities UNCHANGED IN COUNT. Inefficiency surfaced -- compound-gate promotion discipline addendum to [[feedback-dont-overextend-theorems]] flagged for memory_curator. PLAIN: we tested whether the substrate's signature on the Kerdock codebook was unique among related structured codebooks (Hadamard, SRHT, RM, Kerdock); it was not -- Hadamard and SRHT actually score higher on the same signature, and a standard off-the-shelf test already separates these codebooks without needing our proposed signature; the 12th-capability candidate is rejected (portfolio stays at 11), but the narrower Kerdock-specific finding from yesterday survives and we now have 5 rescue directions filed for Research to vet. IMPORTANCE: CRITICAL.



## (cycle 192) v172 BATCHED SIX-verdict cap_map update -- visibility status_log entries

Six paired status_log entries written this cycle (V1 CRITICAL; V2/V3/V4/V5 HIGH; V6 MEDIUM). Plain-language framing per [[feedback-plain-language]] + [[feedback-for-you-tab-primary-channel]].

### V1 entry (CRITICAL)

Logged at v172 paired commit. PLAIN: The substrate's "self-monitoring confidence" capability (Cap 2), which had been provisionally rejected at v160 because the two natural substrate-intrinsic readouts (margin and tau iteration count) both crossed hard-fail thresholds, has now been rescued via the cleanest of the 5 rescue sketches filed at v160: subsumption into the substrate's existing conformal-calibration pipeline (Cap 6 / Gap C). The rescue cleanly PASSES all pre-registered thresholds (5/5 seeds achieve committed-accuracy >= 0.9 at abstain-fraction <= 0.2 with monotone Pareto front). Net effect: Cap 2 returns to the portfolio at green-check, but as part of the existing Cap 6 row (downstream conformal calibration over the Bet G stream) rather than as a new independent capability -- so the portfolio count stays at 11 demonstrated capabilities. The bigger picture: **after this cycle the substrate-product portfolio carries ZERO open provisional rejections**, the cleanest state since the provisional-rejection framing was introduced at v160 a couple of weeks ago. This validates the 5-sketch rehabilitation discipline (filed at v160; sequencing recommendation correctly placed this zero-cost subsumption FIRST; 12 cap_map versions of patience v160 -> v172). IMPORTANCE: CRITICAL.

### V2 entry (HIGH)

Logged at v172 paired commit. PLAIN: Bet T (parallel hypothesis tracking; 🟡 PARTIAL since cycle 101 / v101; 56+ cap_map versions stale; 5 rescue sketches filed at v158) closes as ❌ via Mondrian-on-anti-RM-coset rescue hard-fail. The Mondrian rescue was the highest-rank conformal-style rescue (Sketch #3 in research_betT_rescue_sketches; conformal class-wise wrapper applied to the substrate-specific anti-RM coset structure from v152). Per-coset coverage came out at 1.0 in all 4 anti-RM cosets (target band was [0.8, 0.99]) -- the Mondrian stratification does not discriminate within Bet T's hypothesis-tracking failure mode at substrate scale. Of the remaining 4 sketches: 2 are structurally redundant with already-green rows (Sketch #2 per-hypothesis TEMPSCALE collapses to Bet G TEMPSCALE which is already a separate green-check row at Cap 6; Sketch #4 VAMP per-hypothesis posterior collapses to Cap 8 VAMP-on-chain); 2 stay as elective experimental options (Sketch #1 Kerdock-orthogonal hypothesis subspaces; Sketch #5 periodic re-anchor + replay) but rank lower than Mondrian on calibration-deflated P. Bet T closes per PROT-004/006 5-sketch rehab discipline. Honest framing: Bet T was never a portfolio (substrate-product) row -- the closure cleans up a stale partial-row, not a portfolio capability. IMPORTANCE: HIGH.

### V3 entry (HIGH)

Logged at v172 paired commit. PLAIN: Bet V (self-reflective memory; 🟡 PARTIAL since cycle 102-103 / v102-v103; 54+ cap_map versions stale; 5 rescue sketches filed at v158) lands a partial-rescue on the 4th-free-cumulant separation observable (closest to Sketch #1 in research_betV_rescue_sketches: Kerdock-orthogonal subspaces applied with kappa_4 as separation observable). The signal IS present (|kappa4_sep| = 2.51 standard deviations; kappa4_stored = 0.065 vs unstored = 2.210, a 34x absolute-magnitude ratio) but is sign-inconsistent across seeds. The 2.51 SD lands in the [1.0, 2.0) band (signal present but not portfolio-rescue strength). Honest reading: substrate carries SOME 4th-cumulant signature distinguishing stored vs unstored content, but NOT a sign-stable separation observable usable as a self-reflective confidence proxy. Bet V row stays 🟡 PARTIAL UNCHANGED with v172 partial-rescue annotation; not closed, not promoted. 4 remaining sketches stay as elective hardening options. IMPORTANCE: HIGH.

### V4 entry (HIGH)

Logged at v172 paired commit. PLAIN: The "PFK / partial-thermalization" reframing of the substrate's higher-cumulant signature -- which had been proposed as a candidate theoretical lens in this morning's ETH-thermalization drill -- gets REJECTED at n=6 cactus factorization order. R_6 lands in [0.95, 1.05] in 10/10 seeds (median = 0.9977, mean = 0.9977), meaning the cactus factorization (which equals 1.0 under full-ETH thermalization) dominates the 6-point connected-correlator decomposition. The substrate is **full-ETH-class at n=6 with non-Gaussian bulk shape** -- "fully thermalized at this order" not "partially thermalized." The v167 finding that kappa_n GROWS in n is REFRAMED: this is bulk-shape information (the thermalized distribution has substantial higher cumulants and is therefore non-Gaussian-bulk), NOT partial-thermalization signal (the connected-correlator decomposition factorizes cleanly). Critically: the v169 closed-form rederivations of Cap 1 (Sagawa-Ueda envelope = Pauli-twirled depolarizing entropy), Cap 3 (throughput = Holevo capacity of Clifford channel), and Cap 8 (VAMP-vs-AMP split = Schur-Weyl-Pauli-twirled S-transform) are PRESERVED -- those derivations land via the Clifford-design / Pauli-channel / Schur-Weyl structure, which is independent of ETH considerations and not touched by V4's rejection. IMPORTANCE: HIGH.

### V5 entry (HIGH)

Logged at v172 paired commit. PLAIN: Despite V4 rejecting the partial-thermalization mechanism, the substrate's spectral form factor (SFF) DOES deviate from the Gaussian Unitary Ensemble (GUE) by > 15% in dip OR plateau in 5/5 seeds (median dip rel-dev 2.064; median plateau rel-dev 0.149). The substrate has spectral structure that GUE does NOT capture -- this finding SURVIVES. Mixed evidence with V4: V4 says the substrate is fully thermalized at n=6 in the connected-correlator sense (cactus property); V5 says the substrate is not GUE-distributed in the spectral sense (eigenvalue distribution property). These are NOT contradictory -- a substrate can be in a structured-thermalized class that is neither iid-Gaussian-noise nor canonical-GUE. The partial-thermalization MECHANISM-NAME for the non-GUE spectral structure is REJECTED at n=6; the surviving mechanism candidate is the 4-coset MUB-stabilizer structure from the Kerdock-MUB-stabilizer drill (Clifford 2-design + 4 orthogonal cosets give non-GUE eigenvalue distribution without partial-thermalization). Per honest framing V5 alone does NOT promote a new portfolio row (mixed evidence with V4). Substrate-physics characterization gains a v172 annotation noting the non-GUE structure and the rejected partial-thermalization mechanism. IMPORTANCE: HIGH.

### V6 entry (MEDIUM)

Logged at v172 paired commit. PLAIN: A smoke probe of the substrate's generative-mode capability (Glauber dynamics on Kerdock-Hopfield W; testing whether the substrate can act as a generative model that samples coherent novel outputs rather than just retrieving stored items) lands LIMITED. At the best cell (beta=5.00, the lowest-temperature cell tested) two of the four capability gates hit: novelty = 1.0 (substrate generates samples not in the stored set) and stability = 0.967 (generated samples are stable under further Glauber iteration). But the other two gates fail: diversity = 0.070 (samples are noisy variants near stored items, not diverse coverage of the output space) and coherence = 0.120 (samples lack compositional / structural coherence). Honest reading: Kerdock-Hopfield retrieval-mode dominates; the substrate falls into stored basins (high stability) and the "generative" samples are noisy variants near those basins (low diversity, low coherence). The 12th-capability candidate framing "substrate as generative-mode model" is REJECTED in its strong 4-gate composite form. NO portfolio row added (partial-pass on 2 of 4 gates is annotation-grade per honest framing); substrate-physics characterization gains a v172 annotation noting the LIMITED generative behavior. IMPORTANCE: MEDIUM.