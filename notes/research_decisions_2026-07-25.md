# Research decisions log — 2026-07-25

- Holistic (set-level) evidence selection for the ARC-Challenge selection wall (biology + concrete build design, serving atom 29548): `notes/research_holistic_set_selection_2026-07-25.md`. Headline: converges on 3 brain mechanisms (population-vector readout = what the existing bundle combiner already is, unchanged; Kintsch CI over-generate-then-settle = concrete greedy set-construction algorithm shape, primary-source detail; Usher-McClelland/Wang/Albantakis-Deco N-alternative competitive dynamics = the confirmation-trap answer, track all answer-choice hypotheses simultaneously via margin not absolute score). Build design: greedy holistic set construction scored by MARGINAL cross-answer-choice discrimination margin (not any per-fact intrinsic score), reusing the existing unmodified bundle combiner as readout; must-fail control = random-same-size subset; backtracking pass as confirmation-trap guard. SHAPE (per-item scalar -> set-valued construction) and METRIC (intrinsic per-fact score -> group-marginal contribution) both change; PLACE in the pipeline does not. P_deflated=0.38-0.40 for full design; P=0.55 for the cheap decisive test (gold-in-random-order margin separation) that should run BEFORE full build.
- Verification-by-derivation reasoning pivot (atom 29549: retrieve->select->combine is similarity all the way down, never reasons; USER-directed "stop matching, start reasoning"): `notes/research_verification_by_derivation_reasoning_pivot_2026-07-25.md`. Headline: biology converges on backward-chaining-from-candidate (Tanji-Hoshi PFC, Rips PSYCOP natural-deduction proof search) MEETING forward-chaining-from-givens (reuses the ALREADY-PROVEN substrate bidirectional meet-in-middle search shape, 0.620 chain-grade at depth-5), over a TYPED/DIRECTIONAL graph built from WorldTree's already-parsed typed relations (`parse_tablestore_typed()`, reused as-is) restricted to LICENSED causal/conditional types (CAUSE/IFTHEN/REQUIRES/COUPLEDRELATIONSHIP/SOURCEOF/USEDFOR); "why not everything associates with everything" answered by 4 converging literatures (LISA directional binding, Gentner systematicity, PFC relational-integration capacity limits, ACT-R basal-ganglia serial gated rule-firing) all requiring typed/directional/serial structure, not symmetric similarity. Chain-consistency check reuses the existing polarity/CI settle (Johnson-Laird counterexample-search / Kintsch situation-model equivalence, flagged as our synthesis). Must-fail controls: SHUFFLE_DIRECTION (scramble arg0/arg1) and UNTYPED_SIMILARITY_NULL (identical search over a generic cosine-threshold graph, ignoring type/direction) — the second directly operationalizes similarity-vs-entailment as a run-able null model. Cheap decisive test = graph-connectivity-only coverage+selectivity check (no scoring yet) before full build; pre-registered GREEN/YELLOW/RED bands. Honest coverage flag: only ~1,700-2,000 causal/conditional rows total in WorldTree across all domains vs ~2,200 questions -- coverage, not search quality, is likely the dominant limiter; recommend building/reporting on the coverage-subset first. Joint P_deflated ~0.13-0.15 (cheap-test-GREEN ~0.35-0.40 x full-build-HARD-PASS-given-green ~0.35-0.40), novel-synthesis cap honored.
- Earn-it differentiation loop over REAL WorldTree definitional structure (build B, pre-designed and gated on build A greenlight): `notes/research_earn_it_differentiation_loop_definitional_worldtree_2026-07-25.md`. Headline: build A (`exp_intrinsic_foundation_loop_tie_gaps_defmatch_v1.py`) confirmed actively running (smoke completed 2026-07-26T00:08Z), its own docstring names it the bootstrap for build B and its `feed_b` metrics field already emits per-concept role-slot profiles + a held-out-concept split. Build B design = retarget the already-twice-landed composed differentiation loop (v1 HARD_FAIL memorize on frequency-saturated slice, commit 79083ef02; v2 MIDDLE 0.24 held-out / compression_ratio 1.17, commit 76471541d) onto build A's own concept set, own held-out split, and the SPECIFIC tie-discriminating role-filler slot (not a generic property). Biology: Rogers-McClelland/Saxe-McClelland-Ganguli/Lambon-Ralph carried forward from atom 29556's note; NEW grounding this session (WebSearch-verified) = Complementary Learning Systems updated (Kumaran-Hassabis-McClelland 2016) mapping directly onto the existing MDL KEEP_EPISODIC-vs-compression gate, and Words-As-social-Tools theory (Borghi-Binkofski) predicting abstract/propositional concepts lean on relational-linguistic structure over perceptual grounding -- convergent with Binder direct-supply already closing structurally at this exact seam (atom 29571). Explicit hard gate: if build A's FULL run (not the noisy n=15 smoke) HONEST_NEGs, build B should NOT be dispatched (B can only approximate an oracle profile that would already be proven insufficient). Pre-registered HARD-PASS/HARD-FAIL bands, shuffled-relation AND shuffled-target must-fail controls, oracle-substitution transfer test. P_deflated: P(A greenlights)=0.30, P(B HARD-PASS | A greenlights)=0.25, joint~0.10; P(B lands MIDDLE again)=0.40.
- Definitional/propositional meaning-foundation SCALE audit (full ARC-Challenge+Easy vocabulary vs. WorldTree v2.1, extending a642b513's 128-tie/materials-micro-benchmark characterization): `notes/research_definitional_meaning_foundation_scale_audit_2026-07-25.md`. Headline: two-tier verdict, not single yes/no. BREADTH is close to solved and near-free -- WorldTree def/prop tables alone cover 69.0% of ARC content-word tokens (24.8% of types); stacking already-on-disk WordNet glosses (via `nltk`, not previously named as a supply source in this arc) pushes "has some clean definition" to 92.3% types / 98.5% tokens, at zero new acquisition cost -- the remaining work is sense-disambiguation (mean 5.1 senses/word), not data acquisition. DEPTH is genuinely DATA-BOUND: full-ARC-vocab PROP-*-only measurement gives mean 1.36 attrs/concept, frac_ge2=0.255 -- independently cross-validating a642b513's own cited "v2's 0.24" frac_ge2 figure and reproducing the 128-tie's shallow shape (not a sampling artifact of an unusually thin subset). ConceptNet promiscuity now quantified: 98.3% lexical presence for WorldTree-gap words but 52-53% of all edges are the generic RelatedTo relation regardless of slice; clean WorldTree-analog relation types (IsA/HasProperty/PartOf/UsedFor) are a ~12-15% minority -- directly explains the substrate's own prior ConceptNet HARD_FAIL (Hits@10 0.451 < frozen-bge 0.502 < closure 1.0). Binder norms (a642b513's option B) independently confirmed thin at full-ARC scale (3.05% type / 9.1% token coverage, 534 general-purpose concepts, not science-curated). GenericsKB/CSLB/other-subject OpenStax textbooks: confirmed absent from disk. Ranked sourcing rec: (1) wire WordNet gloss + hypernym-chain sense-disambiguation FIRST (near-zero cost, highest leverage); (2) deepen WorldTree's own thin-domain PROP/SOURCEOF/REQUIRES/CAUSE tables (physics/chem/earth-science); (3) acquire additional OpenStax textbook glossaries via the already-proven `clean_cnxml.py` pipeline; (4) GenericsKB/CSLB only if 1-3 insufficient; (5) ConceptNet only as a heavily-filtered last resort. P_deflated=0.40 (WordNet-disambiguation viability) / 0.35 (OpenStax-scaling assumption), both under the 0.50 novel-synthesis cap. Read-only disk audit; no build, no dispatch.
- sr_chaining_arc_reasoning_design: pinned TIE-bucket (not depth-coverage) as the honest ARC target for SR-routing+gated-fusion transplant; designed (not built) exp_arc_reasoner_sr_gated_tiebreak_v1, P_deflated=0.22 -> notes/research_sr_chaining_arc_reasoning_design_2026-07-26.md
