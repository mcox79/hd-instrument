# STATUS -- THE RECOVERY ENTRY POINT. READ THIS, THEN THE PLAN.

**07:33 (09-14): STATE DROP EXPLAINED + MERGE COMMITTED: 0.8148 was the supervised parser's number (reproduced under HDLAB_HEADS_SOURCE=arceager); under the BF arm the state read is 0.73-0.75 and the merged table is the best (0.7434 board). Board baseline is now 0.6344 on the honest BF basis. Merge committed (hdlab + tools + asset); INTEGRATED marks 94 + 105; 0.8148 retired (memory reference file); scorecard corrected. Consumer repair item: the copular state read under arm heads (subject of a copular predicate right 0.50) -> pri 108 chain. pri 108 LAUNCHED. Running: pri 107 phase 7.** **10:09 (09-14): DESKTOP: repo PLACED at C:/AI/hd-instrument (276,802 files, 192 GB, HEAD 937232f6f = the bundle head); venv build running over ssh (Python 3.14; `build_venv.log`); then board self-test + witnesses there; final delta + state resync before the switch. pri 108 COMMITTED (board 0.6351). pri 109 APPLIED in the tree (19 files); witnesses + board republish running. Solvers: 110, 106.** **09:47 (09-14): pri 108 DONE + APPLIED in the tree (nmod class; v4 table live; OBJ_DEPS consumer fix; witnesses 7/7 with the affected-entity constant re-pinned UP 0.4632 -> 0.4676) -- board running (board_pri108.log); pri 109 DONE (reverified), its two diffs land after that board. Solvers launched: pri 110 (sole-AUX predicates), pri 106 (role margin to consumers). Desktop: repo still being placed (robocopy); venv + self-test next; FINAL delta + state resync right before the early-afternoon switch.** **09:23 (09-14): OWNER'S LIVE DOC = `notes/HOW_WE_ARE_DOING.md` (GUI tab 1 + `tools/how_we_are_doing_web.py --bind <tailscale ip>` render it; update at EVERY landing: the stage row, one dated WHAT MOVED line, WHAT IS RUNNING; `tools/scorecard_gui.py --parser-smoke` before commit). Desktop: repo copy at C:/AI/hd-instrument still being placed (robocopy); venv script shipped (`build_venv_desktop.ps1`).** **09:11 (09-14): pri 104 INTEGRATED + COMMITTED (board byte-identical). Desktop: robocopy placing the repo at C:/AI/hd-instrument still running; then venv (3.14) + self-test.** **08:59 (09-14): OWNER: the desktop switch is EARLY AFTERNOON -> making the desktop current: delta bundle + memory/sessions + hook_state + assets shipped; the copy at C:/AI/hd-instrument_desktop_2026-09-12/hd-instrument RESET to the bundle head 937232f6f (LFS smudge skipped; the 2 LFS files shipped raw); desktop Python is 3.14 (no 3.12): core packages install under 3.14 (numpy 2.5.3, scipy 1.18.1, nltk) -> venv from a pin-loosened requirements file; robocopy of the copy to C:/AI/hd-instrument on the desktop launched; then venv + board self-test + witnesses over ssh; a FINAL delta + state resync right before the switch. OWNER: GUI 'how we're doing' tab redesign (informative, live doc, tailscale-remote compatible) -> dedicated agent. pri 108 + 109 first reports SOLVED (phase 7 running): 109 found FIVE gold columns + 18 redacted GUM docs; gold-free the pronoun-coref margin holds (0.4172 vs floor 0.3202), the common-noun win was the gold columns; the reader's entity QA repaired (0 -> 200 questions); a head-domain defect in pri 104's landed wire (one-line fix).** **08:04 (09-14): pri 107 witnesses GREEN (predicate_recall now green = the organ is live; tense-agnostic 96 -> 238 events after a stale-default repair; extraction 11/11; shared-cache 4/4); board no-regress running (board_pri107.log vs 0.6344) -> then the integration commit. pri 109 launched (gold-free entity instruments); pri 108 running.** **08:32 (09-14): pri 104's ENTITY PRIOR LANDED in the tree (lexical_categories arm + the reader's new_document() wire + the merged category asset live; probe: UPOS 0.9312 -> 0.9313, confusions 378 -> 373; lexical witness green); board no-regress running (board_pri104.log) -> then commit + INTEGRATED mark. Running: pri 108, pri 109.** **08:17 (09-14): pri 107 INTEGRATED + COMMITTED (board byte-identical 0.6344; a board-invisible win: reader event recall 0.919 -> 0.950). Running: pri 108 (nmod role class), pri 109 (gold-free entity instruments). Next launch when a slot frees: pri 110, then 106, 101, 102, 96; pri 104's entity-prior diff after pri 109 (same organ family).** **07:51 (09-14): pri 107 CLOSED (SOLVED, exhausted; reverified) -> DONE; its diff APPLIED (predicate_detector + the reader's rescue branch + witness; 417+/20-) and the asset moved live (predicate_detector_bf_counts_v1.json); consolidating the rescue's posterior read onto the reader's cache, then witnesses (predicate_recall must go green; tense-agnostic events; extraction recall) + a board no-regress run vs 0.6344. Sole-AUX arm selectable (default OFF) -> brief pri 110 with the upstream VERB-as-AUX confusion. Running: pri 108.** **07:29 (09-14, by `date`): BOARD under the merged arm: AGG 0.6369 -> 0.6344, ONLY the state dimension moved (0.8148 -> 0.7434); UD-EWT copular arcs IMPROVED under the new table (cop 0.511 -> 0.608), so the loss is in the READER's graded route (tag posterior -> frontend Parser -> entity-state read), not in arc accuracy -> item-level A/B of the state read + graded-path UAS under prev vs new tables running (`state_ab.log`). Merge NOT committed; new asset live (prev kept) until decided. pri 107 SOLVED first report (event recall 0.919 -> 0.950; phase 7 running).** **08:20 (09-14): MERGED ARM TABLE LIVE (after a located defect: the two-sided teacher's association was never passed at the builder's call site -> strategy wired it; the first rebuild had collapsed nmod to 0.236): heads UAS +0.008 gold / +0.010 organ, nmod 0.436 -> 0.537, obl +0.02, nothing down but xcomp -0.007. BOARD A/B RUNNING (`board_merge94_105.log`, vs 0.6369). On a non-regressing board: commit hdlab/attachment_arm.py + tools/build_attachment_validities.py + the asset (git add -f), INTEGRATED marks for 94 + 105, scorecard line; then pri 104's entity-prior diff (after pri 107 closes), then launch 108. Running: pri 107.** **05:30 (09-14): ATTACHMENT-ARM MERGE IN THE TREE (uncommitted): pri 94 main + pri 105 + pri 94 two-sided teacher landed in hdlab/attachment_arm.py + tools/build_attachment_validities.py (752+/37-); arm witnesses 17/17 + fastpath green; both cells' self-tests invert by construction post-landing (noted in their SOLVED.md). pri 94 DONE (reverified 40/40). REBUILD running: cap 6000, beta 10, --pp-assoc (candidate `data/hook_state/attachment_validities_v1_merge94_105_cap6000.json`, log build_merge_cap6000.log); then `data/hook_state/heads_probe_merge.py <candidate>` (live vs candidate, gold + organ tags) -> swap (backup prev) -> board A/B vs 0.6369 -> commit. Forward wire landed in coref.py (capability; pri 109 wires it). Coref witnesses green. Running: pri 107 solver.** **03:45 (09-14): pri 104 CLOSED (PARTIAL, exhausted; reverified 8/8) -> DONE; its two diffs (entity prior + forward name-vs-common wire) queue behind the attachment-arm merge. pri 109 brief FILED (make the entity instruments gold-free: the board's coref/common-noun/salience rows read the gold UPOS column at gum_coref.py:200/107-118/236-240; the reader's entity QA finds no nameable person cluster). pri 107 LAUNCHED 03:55 into the freed slot. Running: pri 94b round 3, pri 107, state_qa witness (2 h budget). MERGE PREPARED in scratch (pri 94 + 105 attachment_arm; hunk-limited); integration sequence in the plan.** **03:10 (09-14): coref-QA red LOCATED (pre-existing since the 09-09..09-11 entity-layer changes: person clusters hold only pronouns -> no nameable cluster -> 0 questions; ledger 03:05) and the board's coref split found to PEEK AT GOLD mention types (pri 104) -> both go into one entity-layer/instrument brief (pri 109) once pri 104 hands the evidence. Solvers: pri 94b round 3 (BETA_NOM sweep at cap 6000; the bare-nominal construction), pri 104 round 2 (online precision; graded name belief; name lexicon). Next launches when a slot frees: 107, 108, 106, 101, 102, 96. Then: merge pri 94 + 105 diffs, one cap-6000 rebuild, one board A/B; pri 104's two diffs after its close.** **02:05 (09-14): ALL WITNESS REPAIRS LANDED under the one-Parser reader (shared-cache 4/4, copular 6/6, extraction_recall 11/11, role_route 4/4, precision_defer 4/4, arceager_route 4/4, precwt re-pinned green, double_parse 8/8, causation landing green, zero-spaCy 3/3; predicate_recall RED by design = pri 107). Causation typing reads the reader's cached parse (one parse; byte-identical to its standalone route). BRIEFS FILED: pri 107 (dormant predicate rescue), pri 108 (no NMOD class in the role competition + every label consumer on one structure; from pri 94's section 17 and the 'storm -> dep' finding). OPEN: situation_model_qa's coref check scored 0.0 on every floor in the 1 h run (isolating, pytest -k coref_which_entity running); state_qa witness runs the whole board in W6 (overnight-only). Solvers: pri 94b phase-7 round 2 (second diff = two-sided teacher written), pri 104 running; next launches 107, 108, 106, 101, 102, 96 (two at a time). Then: merge pri 94 + pri 105 diffs into attachment_arm, one cap-6000 rebuild, one board A/B.** **00:20 (09-14, clock-checked): ONE-PARSER READER in the working tree (uncommitted; shared-cache witness re-running on a saturated box: 8 witness processes + two solvers); 6 stale witnesses repaired to the one-Parser structure (uncommitted, runs pending); DORMANT ORGAN found: the predicate rescue never fires under the category organ -> pri 107 brief filed (its witness stays RED on the live check on purpose). pri 105 DONE (reverified). Solvers: 94 phase 7, 104 running. Next launches: 107, 106, 101, 102, 96.** **22:05 UPDATE: THE WAVE IS COMMITTED (9c1cea1d0: organ patches 97/98/99/100/103 + live assets + the one-default reader fix + ledger/scorecard/INTEGRATED marks) -- items (1)-(4) of the 20:49 block are DONE. OPEN NOW: (i) reader witnesses under the BF heads default: 9 green, 10 reds classified in the ledger (2 stale API drift, 5 byte-identity vs the supervised parse or a frozen supervised constant, 1 inverted premise = a win, 1 stale since 08:20, 2 timeouts rerunning with 1 h budget) -- the control sweep under HDLAB_HEADS_SOURCE=arceager (`witness_sweep_ctrl.log`) decides pre-existing vs caused; then REPAIR each witness to assert the invariant on the frontend's parse (one structure), never by reverting the default; (ii) solvers running: pri 105 phase 7 (first report PARTIAL: clause-membership cue, core arcs +0.018 CI-sep, board patient 0.7724 -> 0.8069, constituency half refuted by counting; probe sent 21:55), pri 94 phase 7 continuation; next launches 104, 106, 101, 102, 96 (two at a time); (iii) pri 105's and 94's diffs both touch attachment_arm (CUES tuple, SentenceCues.cues, arc_scores) -- merge per pri 105 SOLVED.md section 13, one rebuild at cap 6000, board A/B; (iv) light desktop resync launched 22:05.**

**12:10 UPDATE (post-compaction, autoloop):** (1) pri 109 COMMITTED e231a91c3 (gold-free entity rows; AGG 0.5930 = honest baseline; the two new board arms now use get_output_dir per Q115 -- the hook refused the first attempt). Commit dd0686d37 titled 'test' IS the repaired referent_per_np witness (an accidental message; the amend was refused by the hook because the index held the unpatched arms; left as is). (2) LOADER: gum_coref.load_docs now READS exclude_scrubbed (skips a doc whose FORM column is redacted on >50% of tokens): 275 -> 257 docs; board republish RUNNING (HDLAB_EXP_NAME=situation_model_qa_modern_v1_pri109c, log data/hook_state/board_pri109c.log) -- expected common-noun 0.5417 / n=3024. (3) pri 106 PHASE 7 DONE, agent says EXHAUSTED at this rung: the reader's _read_affected_entity fed the resolver a WORD-ORDER PROXY (rank2dep), the role organ never reached it; agent-measured on GUM test n=903: proxy 0.3522 -> organ label 0.3832 (+0.0310 CI-sep) -> organ DECISION (ppc cue) 0.4385 (+0.0864 CI[+0.0576,+0.1163]); its diff (role_margin_consumers_patch.diff: graded_role_assigner + affected_entity_resolver + situation_reader; default HDLAB_AER_ROLE_CUE=hard = byte-identical) is APPLIED UNCOMMITTED in the working tree; witnesses under the default running (b315q2c1f); board A/B with HDLAB_AER_ROLE_CUE=ppc RUNNING (pri106a, log data/hook_state/board_pri106a.log). Plan: flip the default to ppc if the board holds (no default-off), commit, re-pin the affected-entity witness. (4) pri 110 FIRST REPORT: SOLVED -- a graded predicate-slot occupancy read off the category posterior (sole-AUX be/have clauses): governor UAS 0.6245 -> 0.6330 (+0.0085 CI-sep), sole-AUX clauses +0.0503, reader blind clauses 33 -> 9 of 1240, 83% of the tag-to-head loss gone, GUM/QA-SRL out-of-supply both CI-sep up, twins below floor, board 0.5930 -> 0.5929 (state -3 items of 378); diff predicate_slot_patch.diff (attachment_arm + lexical_categories + predicate_detector doc). PHASE-7 PROBE SENT (17 residual flips; repair the copular state reader for the -3; build 7A = rebuild attachment validities under the revised hand-off as a NEW asset; file 7D with a convention-free precision instrument; count what each VERB-gated consumer still misses). The agent reported ONE DENIAL, verbatim: `Permission to use Bash with command rm -f "C:/Users/marsh/AppData/Local/Temp/claude/c--AI/95c182ee-4222-401b-92f4-de5c381c5ab4/scratchpad/make_patch.py" && echo removed has been denied.` -- it did not retry and used a different filename. (5) pri 111 FILED + COMMITTED 071b7326d (the passive cue fires on the whole sentence, not the clause: 33/43 licensed agent overrides gold-active; from pri 106 section 7.4) and LAUNCHED as an opus solver agent (organ: thematic_role_labeler.is_passive_clause + graded_role_assigner call sites). (6) DESKTOP: delta_sync.sh running (bp9onsl2a); the desktop-side fetch/reset + LFS tar + memory copy still to do after the pri 106 commit.

**13:03 UPDATE:** pri 110 phase 7 EXHAUSTED (state 0 items, cop +0.0000, UAS +0.0086 sep, tag-to-head loss 102->15; 7A assets in hook_state; 10e: 167/762 non-verbal predicates) -- diff applies, landing QUEUED until the two boards finish (a late import must not contaminate the A/B). pri 113 + 114 filed. pri 112 (passage register) filed + LAUNCHED as an opus agent (organ: lexical_categories register + reader memo + frontend callers). Witness re-pins committed: affected-entity (gold source explicit, 0.4721), coref-stack (organ population 0.4827/0.5574). GUM witness batch running (reds so far: crosstype_live_wire exit 124 timeout under load; crosstype_fullread_endtoend exit 1 -- rerun individually after the boards). Desktop: bundle delta_20260914T160310Z applied (HEAD 1bbe54677); final catch-up after the pri 106/110 commits.

**13:12 BOARDS READ + TWO LANDINGS:** pri109c (redacted docs excluded): common-noun 0.5470 (floor 0.5397, not sep), salience 0.2734, coref 0.4172, AGG 0.6150 = HONEST BASELINE (supersedes 0.5930). pri106a (HDLAB_AER_ROLE_CUE=ppc): IDENTICAL 7/7 -> board-invisible by construction; pri 106 COMMITTED 8b6dede80 with default 'hard' (byte-identical); flip waits on the cell's own --wire-price arm + a live-reader form (asked the agent). pri 110's diff APPLIED (uncommitted): witnesses (b68i9dwob) + board A/B pri110a (data/hook_state/board_pri110a.log) RUNNING; commit on green + no-regress. Agents running: pri 111, pri 112, pri 106 (instrument arm).

**14:31 pri 110 LANDED 8dafb6c9b** (board 6/7 identical, patient +2, AGG 0.6152; Penn-arm guard added at landing). pri 106 'label' mode committed (default 'hard'; flip after the agent's 40-doc live-reader A/B). pri 111 phase 7: board agent row 0.8271 -> 0.8552 CI-sep above its floor (the board arm's duplicate hybrid retired in the diff); phase 7b running (fold D2/D4, delete D5). pri 113 HEAD-TO-HEAD: owner in notes/problems/<slug>/, agent in notes/comparisons/pri113_agent/ (blind). Witness triage agent (10 GUM witnesses) running. Desktop: final delta sync running now.

**16:04 UPDATE:** pri 106 INTEGRATED + EXHAUSTED (30efcbe3b; the label wire confirmed live +0.0509 CI-sep; default 'label' since a38f0ecd1). pri 111: 5-file diff APPLIED (uncommitted) + the D4 repair (the participle gate fold had dropped 7 reduced passives 'mass divided by volume'; repaired to a strict superset; board agent row 0.8552 CI-sep above its floor either way); waiting on the twin-copy hunk + the D2 construction check; board A/B pri111a running (pre-repair tree; the repair touches only the byhead gate, agent-measured board-identical); witness batch: coarse_role 37/37, graded_role_assigner green, byhead 14/15 (the byte-faith twin, one item, organ right), cmrole_agent_landing TIMEOUT under load (rerun alone later). pri 112 FIRST REPORT: bar met (12/12 repeatability; the register read the FUTURE: 100% of first mentions leaked; gum_coref.organ_tags never calls new_document -> pri 104's arm inert on the board rows) -> phase-7 probe sent; diff queued behind the pri111a board. pri 113 HEAD-TO-HEAD: agent side PARTIAL in 1h22 (participant instrument built; event detector recall on the 167 non-verbal clauses 0.1856 -> 0.8084, +0.1365 CI-sep; precision flat; capped board 7/7 zero; full board A/B in flight) -> probe sent; OWNER side in progress in notes/problems/<slug>/ (strategy reads neither until both are in). Filed: pri 115 (by-NP head from the attachment arm), pri 116 (the reader lowercases at its sentence source: -0.4076 PROPN F1). Desktop at b3c5c5517; final light resync after the pri 111/112 commits.

**18:37 DESKTOP at 10231db1b (pri 111 included; final delta after pri 112). **17:19 pri 112 APPLIED (uncommitted): witnesses + board pri112a running; expect W3a green + cmrole B1 attributed. **16:43 pri 111 LANDED f4f051170** (clause-local voice cue; six detectors -> one organ; board agent row 0.8271 -> 0.8559 CI-sep above its floor; AGG 0.6191 = honest baseline). pri 112 phase 7 in: three-file diff ready (both new defaults ON, board loader wired, 12/12 repeatability); GENTLE control for arm B requested, then strategy applies + witnesses + full board A/B. pri 113: agent side PARTIAL with state +0.0370 CI-sep (one structure per clause) and the arc cue justified on two populations; OWNER side still in progress (strategy reads neither folder until both are in). Remaining pri 111 witnesses (cmrole_agent_*, arc_labeler) running under load; light desktop resync running.

**19:31 UPDATE:** pri 113 head-to-head compared + committed (4d3e7d036); owner_verdict DONE on the owner side; integration = merged (section 5 of the comparison) AFTER pri 112 lands (both touch situation_reader). pri 112: lexical_categories 7/7, cache_shared 4/4, coref_graded_pick W1/W2/W4 green (W3a repeatability rerunning uncapped), cmrole_agent_landing timed out twice under load (B1 hypothesis untested; rerun uncapped when the load drops), board pri112a running since 17:19. OPUS WEEKLY LIMIT HIT at ~19:20 (resets Thu 18 Sep 8pm): the pri 111 agent died mid-attribution; no new opus solvers until then (sonnet/haiku only).

**19:53 pri 113 MERGED APPLIED (agent diff + owner is_state marker; uncommitted): witnesses (b4uxai8ry) + full board pri113a running. **19:50 pri 112 LANDED 4ea4980ea** (repeatability 12/12; board null, AGG 0.6190). NEXT: the merged pri 113 integration (owner core + agent state consolidation + owner typed STATE; arc cue off) with a full one-process board; then the final desktop delta. Account switched at ~19:45 (usage limit lifted for now).

**22:29 pri 113 LANDED 25efc2e53 (merged head-to-head): state 0.7487 -> 0.7910 CI-sep, AGG 0.6205 = HONEST BASELINE; desktop at 4f41d32f6 (= laptop). Agents 116 + 117 resumed after a network error. Remaining today: the 2h temporal QA witness rerun (re-pinned), the pri 113 batch's last witness, agent returns.

**2026-09-15 08:25:** pri 116 SOLVED (agent) -> diff APPLIED (6 hdlab files) + sentence-source default flipped to CASED (uncommitted): witnesses (bm4aklxl1) + full board pri116a running. Paraphrase-QA instrument repaired (gold names + reader file names; the pri 109 leftover) -> test rerunning. pri 117 finishing. **2026-09-15 08:07 MORNING:** the laptop slept overnight; agents pri 117 + 116 resumed at 08:06 (both alive, near their final reports); the 2h QA witness rerun still running (33 CPU-min done); desktop repo = laptop f79d5e9ef, assets re-streaming. The STOPPING POINT block below is still the handoff.

**2026-09-15 09:37:** briefs 122 (board rows on the reader), 123 (participle/adjective boundary graded), 124 (the last voice detector) FILED; agents pri 117 (phase 7) + pri 118 running. **2026-09-15 09:23:** pri 116 LANDED 7a4c6f15a (the case goes through; board byte-identical by construction; reader instruments up); pri 117 FIRST REPORT in (copular subject 0.5385 -> 0.6568 CI-sep, learned online; phase-7 probe sent); desktop = laptop at b1df26fec; the Updates tab now carries the substrate advances (two posts; rule: every landing gets one). NEXT: pri 117 return -> integrate; dispatch pri 118 (typing hand-off); file the board-rows-on-the-reader instrument brief.

**13:26:** E05 audit done -- an import probe on a default text-only read shows the NOT_BF arc_labeler + parse_confidence RUNNING (patient defer, goal purpose filter, copular label path); pri 129 FILED = TOP OF THE QUEUE after 125/122 (a blocking-class defect at the labels rung). Owner window (scorecard_gui) launched at the owner's request. **14:58 REPORTS IN:** pri 125 (PARTIAL, strong): the reader discovers its own pronouns; text-only agents 0.19 -> 0.75 CI-sep, annotation invariance proven; anaphora pick n.s. (phi cue inert) -> phase-7 probe running (phi-weighted retrieval + Principle B; agent_hybrid flip on the full board; positional validity re-accrued; space_reader hunk). pri 122 (PARTIAL): all seven rows reader-driven in three provenance columns; raw-text agent 0.1510 vs 0.8552 rebuilt (0/948 pronoun agents), patient 0.6869 vs 0.8143, state/WiC agree, coref n=0 on text for two gold gates (pri 125 removes both) -> phase-7 probe running (headline = reader text-only; both aggregates). Q140 FILED (headline switch; proceeding on 'yes'). pri 128 (sonnet) still running. INTEGRATION ORDER when the probes return: apply pri 122's board diff (self-test 15/15), then pri 125's diff -> its witness set + the reader-driven rows text-only + the full rebuilt board (no-regress), commit, records, Updates post; then dispatch 129 + 127. **13:35 PREP:** pri 125 landing witness set (run in one batch when its diff is applied): test_referent_per_np_organ, test_referent_per_np_source_landing_organ, test_referent_coref_linking_organ, test_unified_referent_landing, test_coref_stack_landing, test_coref_coherence_prior_on_chain_bucket, test_commonnoun_resolution_wire, test_entity_resolver_typed_commonnoun, test_entity_maintenance_chaining, test_cmrole_agent_struct_organ, test_structural_patient_landing_organ, test_crosstype_live_wire, test_space_ground_binding, test_state_register, test_graded_pos_consumption_located_negative, test_tagger_cache_unification_sweep, test_situation_model_qa (2h) + the pri 118 set (coref_graded_pick, entity_resolver_unified, route_unified_to_consumers, affected_entity_forward_half); then the FULL board both arms one process. Agents running: solver_pri125, solver_pri122 (opus), solver_pri128 (sonnet, witness wrappers; its diff is applied AFTER 125/122 land). **13:20 UPDATE:** route_unified R2 re-pinned and GREEN 16/16; the temporal tagger switch guarded like the frontend (witness 7/7); the health tool now reads the newest full-size board (0.6200); NLTK at inference ENUMERATED: 78 sites in 37 hdlab modules (lexica; two model stand-ins off the live default) -> brief pri 127 (freeze into one lexicon organ) FILED; brief pri 128 (447 witnesses invisible to pytest; wrappers + tiers + manifest; a sonnet job after the solvers land) FILED. All 13 evaluation findings are now either repaired (E06, E09, E10), filed (E01=122, E02/E07=125, E08=128, E12=127, E13=126) or answered in the ledger (E03/E04/E05/E11: folded into 122/125/127's bars). Solvers 125 + 122 still running. **2026-09-15 13:11 (AUTOLOOP RE-ARMED by the owner; full speed): EXTERNAL EVALUATION ACTED ON.** The owner had another model evaluate the repo read-only (`notes/SUBSTRATE_EVALUATION.md`, 13 findings); strategy verified its four load-bearing claims in code and the owner said 'do all'. DONE: hygiene commit 0308e1603 (frontend refuses unknown tag/heads source names; the aggregate keeps a missing floor/twin missing; witness 6/6; published number unchanged), pri 118 post-hoc board recorded (AGG 0.6200 = HONEST BASELINE; coref 0.4217, common-noun 0.5383 population artefact, five rows identical), the retired-claims file now exists in the repo at the path the briefs name, stop hooks restored with C: paths (97b2d8ee7), briefs pri 125 (4d2034096) + pri 126 (afee2cba4) filed, the route_unified R2 witness re-pinned (exact-zero pin -> tolerance form; rerun in data/hook_state/route_unified_repin.log), Updates-tab post made, desktop = laptop at afee2cba4. THE FINDING THAT MATTERS (E02/E07, verified): on annotation-free text the live reader has NO pronoun mentions -- `referent_per_np` excludes PRON, every pronoun comes from the coref column, `build_pronoun_targets` needs a prior same-gold-cluster mention, `_cm_agent_candidates` is None on an empty stream -> the product on raw text answers agents 9/52 vs 42/52 with our own category organ's spans (evaluation's 5-doc probe). The board's 0.62 is a COMPONENT number given gold mentions; never quote it as the product's accuracy. RUNNING NOW: opus solver agents `solver_pri125` (pronoun discovery as an arm of the introduction organ; diffs against referent_per_np / situation_reader / coref; annotation invariance is the bar) and `solver_pri122` (the seven board rows scored on the live reader, text-only AND annotated, with provenance fields; diff against the board file only); the two touch disjoint files. When either reports: read its SOLVED.md, send the phase-7 probe (understanding + improvement) until EXHAUSTED, then integrate: strategy reverify + witnesses + FULL board both arms one process + ledger/scorecard/live doc + an Updates-tab post. QUEUE after those: 126 (sealed holdout; needs 122 + 125 first), 114, 120, 119, 123, 124, 115; leads: freeze the WordNet lexname table used lazily by the sense reader (E12), the NP span in the reader stream (118), csubj slot + beam width (117), the clustering organ's name-vs-common route (118), scene_segment.py:452 typing, 'a code not a string' (116), the PRED disagreement (113). NLTK: lazy, off the default read, a static lexicon -- a lead, not a blocker.

**COMPACTION HANDOFF 2026-09-15 11:09 local (owner asked; AUTOLOOP DISARMED, no crons; supersedes every block below):**
LANDED 2026-09-15 (all committed): pri 116 (the case goes through the reader: PROPN F1 on the live path 0.45 -> 0.87; commit 7a4c6f15a), pri 117 (the copular subject attaches by a learned online cue 0.54 -> 0.65; 8f191bf77), pri 118 (the mention typer decides at the TYPE level + a span-level name-run cue; typing 0.9506 -> 0.9608; 290d7bdd3), the pri 121 row registered, the paraphrase-QA instrument repair. LANDED 2026-09-14: 109, the loader exclusion, 110, 106, 111, 112, 113 (merged head-to-head). HONEST BOARD (strategy's separate-process boards): AGG 0.6204 after pri 117 (coref 0.4178 / common-noun 0.5461 / salience 0.2656 / agent 0.8552 / patient 0.8143 / state 0.7910 / wic 0.7493); pri 118's own one-process board: coref 0.4220 (up, CI-sep), common-noun 0.5375 (down by a counted population artefact), others unchanged -- strategy's board pri118a was RUNNING at compaction (data/exp_situation_model_qa_modern_v1_pri118a/metrics.json when it lands; read it, record it in the ledger, retire the superseded figures). THE BOARD'S SEVEN ROWS NEVER RUN THE READER (pri 116 finding; pri 122 filed): reader landings are gauged on their own instruments; the board is the no-regress check only.
WITNESS RESULT (after the handoff was written): pri 118 second half -- coref_graded_pick 7/7, entity_resolver green, affected_entity PASS, route_unified 15/16 (one red, named in the ledger 11:28; not investigated -- first item for the next session). IN FLIGHT AT COMPACTION (background, harmless): strategy's board pri118a; the pri 118 witness set second half (coref_graded_pick, entity_resolver_unified, route_unified, affected_entity) -> data/hook_state/pri118_witnesses_2.log (first half all green). No agents running. Desktop = laptop at the last delta (see the bundle stamp in /c/AI/hd-instrument_desktop_2026-09-12/).
QUEUE (top of the chain first): 122 (board rows on the reader; strategy patches), 114 (organ-tagged attachment acquisition), 120 (construction memberships from counts), 119 (typed attribute register), 123 (participle/adjective boundary), 124 (the last voice detector), 115 (by-NP head); leads not yet briefed: keep the NP span in referent_per_np (pri 118), the csubj subject slot + the beam-width default (pri 117), the clustering organ's name-vs-common route (pri 118), scene_segment.py:452 capitalisation typing (pri 118), 'a code not a string' from the sentence source (pri 116), the role-competition PRED disagreement (owner vs agent; run the owner's selective form under the agent's control before any table swap).
PROGRAM RULES CONFIRMED BY THE OWNER 2026-09-15: run a solver per brief (build the BF component; trace the chain for signal loss and repair cheap upstream, log the rest; optimise the target on the signal it has), then dispatch the upstream briefs TOP-DOWN BY POSITION; agents by default with the owner's push script (phase 5b of tools/solver_agent_prompt.py); the owner runs one blind head-to-head a week + architectural briefs; every landing gets an Updates-tab post (tools/owner_updates.py) and a line in chat; a default flip needs the full board, both arms in ONE process. Witness/board cost on this laptop: 5-40 min per witness, 70-90 min per full board (2-3x faster on the desktop; parallel witnesses = an infra item).
DENIALS (verbatim, agents' own, none retried): pri 118: a compound `rm -f data/exp_graded_mention_typing_v1/metrics_tmp_kc*.json ...; for kc in 1.5 2.0 2.5; do ... done` (the kappa sweep never ran; kappa_caps 1.0 shipped); pri 116: `rm -f "$SL/SOLVED.md"`; pri 110: `rm -f .../scratchpad/make_patch.py`.

**STOPPING POINT / HANDOFF 2026-09-15 10:10 local (owner asked; AUTOLOOP DISARMED; supersedes the 2026-09-14 block below):**
LANDED 2026-09-15: pri 116 (the case goes through the reader; PROPN F1 on the live path 0.45 -> 0.87; board byte-identical because the rows never run the reader), the paraphrase-QA instrument repair, the pri 121 row registered (informational). Briefs filed today: 122 (board rows on the reader), 123 (participle/adjective boundary graded), 124 (the last voice detector). HONEST BOARD: AGG 0.6205 (state 0.7910). Desktop = laptop at the last delta (48e8a2a56 + the commits since are small docs; re-ship at the next session).
IN THE TREE, UNCOMMITTED: pri 117's diff (hdlab/attachment_arm.py + tools/build_attachment_validities.py; the learned copular-subject arc feature + predicate-arrival reanalysis + hyphen crossing; 5 switches default ON; the None-head guard) -- witnesses green so far (cell 19/19, attachment_arm 17/17, fastpath, coordination 6/6, cache_shared 4/4, copular 6/6; state_qa / double_parse / coarse_role / predicate_recall / role_route were still running at 10:10: see the task output referenced in the ledger), FULL BOARD pri117a RUNNING since 09:48 (data/exp_situation_model_qa_modern_v1_pri117a/metrics.json when done; A/B against pri116a; the agent's own full board showed patient -2 items pre-hyphen-fix, -1 after). NEXT SESSION: if the board holds (patient within 1-2 items, nothing else down), COMMIT pri 117 path-limited (the two files + INTEGRATED mark in its SOLVED.md + ledger/scorecard/live doc + an Updates-tab post), else repair the patient consumer per the 09-12 rule; then ship the desktop delta.
AGENT STILL RUNNING: pri 118 (graded mention typing; opus) -- it commits its own files path-limited; read notes/problems/<slug>/SOLVED.md when it reports, send the phase-7 probe, integrate with a full board.
QUEUE (top of the chain first): 114 (organ-tagged acquisition), 120 (construction memberships from counts), 119 (typed attribute register), 122 (board rows on the reader -- strategy patches), 123, 124, 115; leads from pri 117: the csubj subject slot, the beam-width default (own board), the role-cue re-run, the representation past ~0.72.
RULES THIS SESSION (memory): post every substrate advance to the Updates tab + chat; the owner's push script is phase 5b of the solver prompt; capped-board zeros are underpowered; fold detectors as strict supersets checked on their construction's population; a GUM-reading instrument asks for gold explicitly. DENIALS (verbatim, agents' own, none retried): pri 116's `rm -f "$SL/SOLVED.md"`; pri 110's `rm -f .../scratchpad/make_patch.py`.

**STOPPING POINT / HANDOFF 2026-09-14 23:04 local (owner asked for a good stopping point; supersedes every block below):**
LANDED TODAY (all committed; desktop = laptop at the last delta): pri 109 (gold-free entity rows), the GUM loader's redacted-doc exclusion, pri 110 (predicate slot), pri 106 (affected-entity reads the role organ's label), pri 111 (clause-local voice cue; agent row above its floor), pri 112 (passage register fed once in order; repeatability 12/12), pri 113 MERGED (owner + agent head-to-head; event detector on the predicate slot; one typed STATE per clause; state 0.7487 -> 0.7910 CI-sep), pri 121 row registered (informational). HONEST BOARD: AGG 0.6205 (coref 0.4178 / common-noun 0.5461 / salience 0.2656 / agent 0.8552 / patient 0.8151 / state 0.7910 / wic 0.7493). Defaults flipped today: HDLAB_GUM_DECISION=organ, HDLAB_LC_PREDICATE_SLOT=1, HDLAB_AER_ROLE_CUE=label, HDLAB_NONVERBAL_PREDICATION=1, HDLAB_STATE_FROM_PREDICATE_SLOT=1, ENT_REG_FILE_ALL / ENT_REG_LIVE_DOC on; the arc cue off (tau 0).
RUNNING WHEN THIS WAS WRITTEN: opus solver agents pri 117 (copular subject heads; the owner's prototype 0.54 -> 0.65 as the floor) and pri 116 (case at the sentence source) -- they commit their own files path-limited; if the session ended before their reports, read notes/problems/<slug>/SOLVED.md for each and re-dispatch phase 7 via tools/solver_agent_prompt.py (phase 5b = the owner's push script). Witness reruns in flight: test_situation_model_qa (2h, re-pinned keystone contrast) -> data/hook_state/situation_model_qa_repin.log; test_event_detection_crossarm_organ -> data/hook_state/event_detection_crossarm_pri113.log (timed out twice under load).
OPEN / NEXT (top of the chain first, per the owner 22:35): (1) pri 117 + 116 returns -> probe -> integrate with full one-process boards; (2) dispatch pri 118 (graded mention typing), 114 (organ-tagged acquisition), 120 (construction memberships), 119 (typed attribute register); (3) promote the pri 121 row after pri 117; (4) the owner/agent disagreement on the role PRED keying (obliques 0.53 -> ~0.83 located vs -0.026 matched control): run the owner's selective form under the agent's control before any table swap; (5) leads still to file: participle/adjective boundary (pri 111 sections 17.1/20.2), D3 voice_cues/robust_passive, role asset drift (22 percent). RULES LEARNED TODAY (in memory): fold a detector onto one organ -> check the construction's own population, strict superset; a capped board's exact zeros are underpowered -> default flips need the full board in one process; a GUM-reading INSTRUMENT asks for gold explicitly, a LANDING witness reads the organ population; reference twins must be re-derived at every fast-path landing. DENIALS TODAY (verbatim, none of strategy's this afternoon): the pri 110 agent's `rm -f .../scratchpad/make_patch.py` was denied and not retried.

**COMPACTION HANDOFF 2026-09-14 10:36 local (owner asked; supersedes every block below):**
**11:33 SOLVERS:** pri 106 first report PARTIAL (the board's agent dimension explained: an unconditional marked-cue override, 17 fixes / 45 breaks; confidence-gated it lifts the board arm 0.8271 -> 0.8517, twin = floor; the live reader fed a positional rank as the role -> its diff wires the competition's decision; C1 weighting form refuted with mechanism, log-posterior hand-off +0.055) -> phase 7 sent; integrate with a board A/B after pri 109. pri 110 running (an interim commit reports 77 percent of the tag-to-head loss gone under the revision). The gold-free board republish (board_pri109b.log) still running.
**11:22 DESKTOP CAUGHT UP:** the placed repo C:/AI/hd-instrument is at 2ea8dff53 (bundle delta_20260914T144805Z; includes a pri 110 interim commit), the 2 LFS files shipped raw, memory/sessions/hook_state re-synced. Laptop HEAD keeps moving (solvers); the FINAL cycle before the switch = the same three steps (delta_sync.sh; on the desktop `GIT_LFS_SKIP_SMUDGE=1 git fetch <bundle> HEAD:refs/heads/delta_<stamp>; git reset --hard delta_<stamp>`; tar the LFS files) + sync_assets.sh + copy `_claude_projects\c--AI` into %USERPROFILE%\.claude\projects\ on the desktop.
**12:53 W3a ATTRIBUTED -> pri 112 FILED (the passage register is written out of order by every consumer; see ledger 12:53); the 11:15 bisect is moot. 11:15 DENIAL (kept for the record):** the bisect of the W3a first-read effect against the committed reader (placing a copy of the pre-109 reader beside the package) was DENIED and is left undone; the finding stands as: a FRESH reader per read differs on 2/12 docs while the same reader read twice is identical, i.e. process-level state set by the FIRST read (candidates: the thematic labeler's T.observe accrual at situation_reader ~3371, a lazily initialised organ); whether it predates pri 109 is NOT established. Next session: bisect by running the two-fresh-reads probe under `git stash`-free means (e.g. a second checkout the owner authorises) or by instrumenting which organ's state changes after the first read. The gold-free board republish (board_pri109b.log) and the desktop delta cycle (bjfa8clc8) were still running at this point.
**11:03 UPDATE TO THE HANDOFF:** pri 109 open item 1: `HDLAB_GUM_DECISION` default flipped to 'organ' in experiments/gum_coref.py (uncommitted, with the rest of pri 109), gold-free board republish RUNNING (board_pri109b.log; expect coref ~0.4172 / common-noun ~0.5417 / salience ~0.2993). Open item 2 DIAGNOSED IN PART: `test_coref_graded_pick_landing` W3a -- the SAME reader reading the same document twice differs on 2/12 docs (bleak_house, masque) = an organ keeps state across reads; the reader itself makes no new online call (only the pre-existing thematic T.observe), so it is either a reader-instance organ (the online entity cluster, default-on since 09-09, touched by pri 109) or module state in the category organ; a fresh-reader-per-read bisect is running (diag_w3a2.log). Repair the WITNESS by pinning state (fresh reader per read, or snapshot/restore), never by freezing an organ. Desktop: fresh delta bundle + reset of the PLACED repo to the current HEAD running (task bjfa8clc8); re-run right before the switch. GUI: relaunched for the owner with pythonw. Solvers 110 + 106 running.
OWNER TODAY: (a) 'make sure the remote desktop is up to date -- early afternoon we switch to that unit' -> DONE SO FAR: desktop repo PLACED at C:/AI/hd-instrument (276,802 files, 192 GB) at bundle head 937232f6f; venv built there under Python 3.14 (all packages but gensim; numpy 2.5.3, scipy 1.18.1, nltk; WordNet data present); BF-tag witness all PASS there (board self-test + coarse-role witness output in the task file b00mhci1t -- re-run on the desktop if unsure); .claude settings shipped. STILL TO DO RIGHT BEFORE THE SWITCH: `bash /c/AI/hd-instrument_desktop_2026-09-12/delta_sync.sh` (fresh bundle to the current HEAD + memory/sessions + hook_state) then on the desktop, from C:/AI/hd-instrument: `git bundle verify <bundle>`; `$env:GIT_LFS_SKIP_SMUDGE='1'; git fetch <bundle> HEAD:refs/heads/delta_<stamp>; git reset --hard delta_<stamp>` (the copy has stale snapshot edits, ff-merge aborts; reset is the correct move for a transfer copy), then tar the LFS files changed since 358d86d4a raw (2 today: data/exp_semantic_hub_convergence_v1/hub.npz, data/frontend_assets/semantic_hub_vectors_v1.npz -- `git diff --name-only <base>..HEAD | git check-attr --stdin filter | grep lfs`), then `bash /c/AI/hd-instrument_desktop_2026-09-12/sync_assets.sh` (frontend assets + stores) and copy `_claude_projects\c--AI` to %%USERPROFILE%%\.claude\projects\ on the desktop; finally `tools/board.py self-test` there. (b) GUI 'how we're doing' tab: DONE and committed -- `notes/HOW_WE_ARE_DOING.md` is the LIVE DOC (update at every landing: stage row + dated WHAT MOVED line + WHAT IS RUNNING; `tools/scorecard_gui.py --parser-smoke` before commit), tab 1 renders it, `tools/how_we_are_doing_web.py --bind <tailscale ip>` serves it remotely (port 8787), design note `notes/GUI_HOW_WE_ARE_DOING_design_2026-09-14.md`.
INTEGRATION STATE: COMMITTED today -- the wave (97/98/99/100/103), one-default + one-Parser reader, ten witness repairs, attachment-arm merge (94+105+two-sided teacher; board 0.6344), pri 107 (rescue rebuilt; board identical), pri 104 (entity prior; board identical), pri 108 (nmod class + v4 table + OBJ_DEPS fix; board 0.6351 = CURRENT BASELINE). UNCOMMITTED IN THE TREE (pri 109, DONE, reverified, applied): the 19 files in data/hook_state/pri109_files.txt + verification/test_referent_per_np_source_landing_organ.py (repaired: reflexives are a deliberate superset; 9/9). TWO OPEN ITEMS BEFORE COMMITTING IT: (1) the gold-free board scorer is behind `HDLAB_GUM_DECISION` in experiments/gum_coref.py with DEFAULT 'gold' -> the board republish (board_pri109.log) shows coref/common-noun/salience UNCHANGED (0.4681/0.5671/0.2555); the discipline says no default-off for an instrument fix -> flip the default to 'organ', run the board (`HDLAB_EXP_NAME=situation_model_qa_modern_v1_pri109b`), expect coref ~0.4172 (floor 0.3202, CI-sep), common-noun ~0.5417 (floor 0.5397, not sep), salience ~0.2993 per the solver, then retire 0.4681/0.5671 in SCORECARD/HOW_WE_ARE_DOING (already in the memory retired file). (2) verification/test_coref_graded_pick_landing.py W3a red: coref-independent dims (events/causal/timeline/entity_states) not byte-identical ON vs OFF on 2/12 docs after the entity-layer diff -> diagnose (the reflexive mentions or the case restore in scene_segment?) and repair or revert that hunk. Other pri 109 witnesses green (coref stack, entity resolver, shared cache, copular, common-noun, unified referent, situation_model_qa coref check GREEN again = 200 questions).
SOLVERS RUNNING: pri 110 (sole-AUX predicates; one convention two losses) and pri 106 (role margin to consumers) -- both need the phase-7 probe on their first report, DONE by strategy after first-hand reverify, integration with a board A/B. QUEUE: 101, 102, 96; leads to file: inverted-copular construction (108), appos/cop labeller for the entity bridge (108+109), replay consolidation of the perceived-heads rung (108), the `lower=False` flip in scene_segment behind a board A/B (109), a reader-side event-recall board arm (107).
RULES TODAY: never junction live data into a scratch tree / never recursive-delete a tree you did not create (the 21:05 incident); a solver diff must be complete on the shipped tools path (the builder call-site lesson); stamp docs from `date` (my stamps ran ahead of the clock earlier).**

**POST-COMPACTION 2026-09-13 21:35 local -- READ THIS PARAGRAPH BEFORE THE 20:49 BLOCK (it corrects it):** (a) THE SHARED-CACHE RED IS EXPLAINED AND FIXED: `hdlab/situation_reader.py` re-read HDLAB_HEADS_SOURCE with its OWN default 'arceager', so after the frontend's default flipped to the attachment arm (66edc3ad6, 11:05) the READER's shared per-read parse silently stayed on the supervised parser whenever the env was unset; the flags-off reader then parsed with the batch parser and the capable reader with the arc-eager one (45/46 vs 49 copular states). FIX (uncommitted, in the wave): the reader imports HEADS_SOURCE/TAG_SOURCE from `hdlab.frontend` (ONE default); both routes now give 50 states; witness 4/4 green. CONSEQUENCE TO RECORD HONESTLY: every reader-internal read (103 reader witnesses, the FD live cross-check, any experiment constructing SituationReader without the env) ran on the SUPERVISED parser between 11:05 and 21:20 unless HDLAB_HEADS_SOURCE was set; the board's 7 dimensions route through the frontend and were on the arm. A witness sweep under the fixed default is running (`data/hook_state/witness_sweep_fix.sh`, ws_*.log). (b) INCIDENT 21:05: `git worktree remove --force /c/AI/hd_wt_head` (the step the 20:49 block recommended) deleted THROUGH two junctions the worktree runner had created into the main data folder -> data/lexicons (gazetteer) and data/litbank (1557 files) were EMPTIED; both RESTORED from the desktop host (tar over ssh; counts + timestamps match). Memory rule written (never junction live data into a scratch tree; check reparse points before any recursive delete). /c/AI/hd_wt_head holds only a dangling `data$d` link; leave it. (c) WAVE BOARD DONE: 0.6369 vs 0.6352 (patient 0.7920 -> 0.8072; every other dimension identical; pri 103's board was 0.6366) -> the wave may commit once the sweep is green/explained. (d) Both solver agents (94 phase 7, 105) were stopped by the compaction -> RELAUNCHED 21:40 with the hazard rule in their prompts; pri 105 had written nothing yet; pri 94's SOLVED.md + diff (20:38/20:44) stand.**

**COMPACTION HANDOFF 2026-09-13 20:49 local (supersedes the 20:40 block; owner asked for immediate compaction prep):**
THE STATE OF THE INTEGRATION WAVE (six agent solutions: 97, 98, 99, 100, 103 DONE by strategy; 94 SOLVED first report, phase 7 running):
(1) ALL PATCHES APPLIED TO THE WORKING TREE, UNCOMMITTED: hdlab/{attachment_arm (pri 97 + ROOT_PICK default restored to 'score'), force_dynamics_valence (pri 98 + the hand-written pri 100 consumer hook: sense_posterior_in_context, PREVENT decided-neutral fix, SENSE_POSTERIOR switch), lexical_categories (pri 99; the refuted frame cue stays inert), affect_lexicon (pri 100), graded_role_assigner (pri 103 cue set v3)}.py and tools/{build_attachment_validities, build_coarse_role_validities}.py. `git status` shows them modified. pri 94's patch (notes/problems/pp_attachment_.../attachment_arm_pp_patch.diff, 537 lines) is NOT applied yet (its phase 7 is composing at cap 6000).
(2) LIVE ASSETS SWAPPED (owner granted the cp permission; backups under data/hook_state/*_prev*.json): role table = pri 103 v3 (board 0.6366: patient 0.802, agent 0.8285, nothing down); attachment validities = pri 97 cap 6000 (own-cat UAS 0.6002 -> 0.6134); category counts UPOS + Penn = pri 99 module rebuilds; manner + sense assets present.
(3) WITNESSES: all green under the stack EXCEPT -- (a) verification/test_frontend_role_who_did_what.py: FIXED and committed (null = p95 of 200 draws; the learned class sits at the 99th percentile with pri 99's cues ON; a runner was confirming green); (b) verification/test_reader_frontend_cache_shared.py [1]: entity states differ between the positional/lazy reader (45-46 states) and the wired reader (49) -- PROVEN NOT order-dependent (experiments/_diag_cache_shared_order.py: fresh-process reads differ identically); it is a ROUTE difference. Leading hypothesis (not yet confirmed): `situation_reader._cached_parse_conf` hands the category POSTERIOR to `AA.head_posterior_graded` when `_cached_tag_posterior` is available and the HARD heads otherwise; pri 97's whole-grid graded read made the graded/hard difference large enough to change copular states, and the positional reader's route yields no posterior (check `_cached_tag_posterior` / `all_capabilities_off`). The principled fix = hand the graded read to BOTH routes (one structure), then the invariant holds; else document the mechanism and update the witness. DO NOT COMMIT THE WAVE until this is explained.
(4) WAVE BOARD running (`data/hook_state/board_wave.log`, HDLAB_EXP_NAME=situation_model_qa_modern_v1_wave) = the no-regress check vs 0.6352 (pri 103 board gave 0.6366 with old 97/99 assets). Then: commit the wave path-limited (hdlab + tools + verification + notes; `git add -f` for assets under data/frontend_assets and hook_state), ledger rows, scorecard line, INTEGRATED marks in the six SOLVED.md files, light desktop resync.
(5) RUNNING AGENTS: solver-pri94 (phase 7: compose at cap 6000 with --pp-assoc data/hook_state/attachment_pp_assoc_v2_simplewiki100k_candidate.json; live chain; heads->labels trace), solver-pri105 (governor core arcs: clause membership + constituency; requirement 0.95), board-wave-runner, witness-wdw-runner. A HEAD worktree exists at /c/AI/hd_wt_head (its asset junction step was DENIED; remove with `git worktree remove --force /c/AI/hd_wt_head` when convenient).
(6) QUEUE: 104 (entity -> category prior), 106 (role margin to consumers), 101 (SCONJ class), 102 (online validities), 96; launch via tools/solver_agent_prompt.py; two at a time; phase 7 until exhausted; FAILED/PARTIAL -> 'how to convert to a full pass' + research + chase leads; yield table in notes/SOLVER_AGENT_PILOT_2026-09-13.md.
OWNER RULES TODAY: the goal is a fully functional glass-box AI; strategy runs solvers and marks DONE itself; integration gates on strategy's reverify + board A/B; push tailored by problem type; documents kept current for compaction.  **FOCUS MAP (2026-09-13 20:20, owner asked for clarity):** the STRATEGY in one sentence -- make every organ on the live reading chain compute what the brain computes (100% BF), top-down by position, then raise each organ where the measurements say the signal is lost, because the reasoners already work on gold and fail only where the chain hands them bad structure. CHAIN STATUS: word kinds (count organ, in-order) BF 0.9312 | word forms (dual-route) BF 0.983 | heads (Competition-Model arm, incremental) BF, UAS 0.6239 / root 0.777 at the landed cap with pri 97 | roles (cue competition, own-perception validities) BF, subjects 0.850 live with the weighted table | valuation (result state / manner / sense-keyed affect) BF, prose 0.667 | ENTITY LAYER (coref 0.47, common-noun 0.57, salience 0.26) built in the brain's spirit but NOT yet re-derived -- the next front. ISLANDS: the board's agent instrument (bypasses the chain; chain 0.80 vs 0.84, causes counted). LOSSES LOCATED with numbers: the governor misattaches 1 core argument in 4 (wrong clause / swallowed into an NP) -> pri 105; subordinator tag 0.73 -> pri 101; consumers ignore the role margin -> pri 106; names vs common nouns need the entity layer -> pri 104. HOUR-TO-HOUR: integrate the five agent solutions (patches applied; assets as candidates; blocked at the copy step by Q139), supervise the running agents, write the next briefs from located losses.  **THE GOAL (owner, restated 2026-09-13 20:00): a FULLY FUNCTIONAL GLASS-BOX AI -- a functional brain recreated in software, every organ computing the brain's computation, inspectable end to end, nothing opaque or off-the-shelf at inference. Reading comprehension is the current FRONT (where organs can be measured against human gold); the situation model, the reasoners, the generative world model and online learning are parts of the one system. The board is an instrument, not the objective.**  **POSITION 20:15 local 13 Sep:** FIVE solutions DONE by strategy (97, 98, 99, 100, 103; all reverified or reverify running); patches 97+98+99+103 APPLIED to hdlab (uncommitted until the witness suite reports); candidate assets under data/hook_state (category UPOS/Penn built; attachment validities cap 6000 building); the live swap of every asset waits on Q139 (frontend_assets write permission). Running: pri 94 (PP attachment). Next briefs: pri 105 (governor core-arc accuracy: constituency + clause membership, requirement 0.95), role-margin reliability to consumers.  **POSITION 19:40 local 13 Sep:** OWNER (Q135): 'I should not be in the loop on those unless there's something very intractable' -> strategy integrates on its own reverify + board A/B. INTEGRATION WAVE IN PROGRESS: patches 97 + 98 + 99 applied to hdlab; weighted role table live (prev at data/hook_state/coarse_role_validities_ud_ewt_perceived_unweighted_prev.json); pri 103 diff written (Q138). Next: asset rebuilds (attachment validities cap 6000; both category count assets; manner asset), witnesses, board A/B; then pri 100 (affect_lexicon + consumer hook); then the chunk-cue candidate (Q136). Solvers running: pri 94, pri 103 (phase 7).  **POSITION 19:15 local 13 Sep:** four agent solutions REVERIFIED (97 SOLVED, 98 SOLVED, 99 PARTIAL-by-bar, 100 PARTIAL-by-twin-clause) with patches that apply together; pri 103 (labels rung, PARTIAL; its diff write was denied -> Q138) in phase 7; pri 94 (PP attachment) running since 18:53. Awaiting the owner: DONE x4, Q135 (reverify as the gate?), Q136, Q137, Q138.  **POSITION 18:35 local 13 Sep:** OWNER DECISION: solver sessions are run by strategy as opus agents from now on (the owner no longer runs them); the phase-7 push is tailored by problem type (supervisor plan). Five solutions so far (97, 98, 99 reverified; 100, 103 in their probe rounds). Still the owner's: DONE marks (or Q135 = let reverify gate), Q136/Q137/Q138 (blocked writes).  **POSITION 17:30 local 13 Sep:** all three pilot solutions REVERIFIED first-hand (97: 18/18 + cap-6000 metrics; 98: every number; 99: shipped config exact) and the three patches apply together cleanly -> integration is one apply + asset rebuilds + witnesses ON THE OWNER'S DONE (Q135 asks whether reverify may gate instead). Running: pri 100 (sense-keyed norm), pri 103 (labels rung), the agent-dimension live-chain probe (the board's agent read is an island). Open for the owner: DONE x3; Q135, Q136, Q137.  **POSITION 17:15 local 13 Sep:** three agent solver sessions complete and closed after probe rounds -- pri 98 SOLVED, pri 97 SOLVED (landed-cap in-order UAS 0.6125 -> 0.6239, root 0.721 -> 0.777; live-chain root 0.699 -> 0.769; reverified), pri 99 PARTIAL-by-bar (unseen 0.752 -> 0.800 CI-sep; reverify running). ALL AWAIT THE OWNER'S DONE in the tab (Q135/Q136/Q137 open). Next agent: pri 100 (sense-keyed affect norm). Briefs 101/102 written. Integration order on DONE: pri 97 (attachment_arm + validities rebuild, board A/B), pri 98 (force_dynamics_valence + asset; live gold + real-prose check), pri 99 (lexical_categories + both count assets; board A/B), then the weighted role table (Q137).  **POSITION 17:08 local 13 Sep:** weighted role table: board 0.6354 (vs 0.6352; patient +0.002; agent identical 0.8271 -> that board read bypasses the table); the live swap was DENIED by the harness -> Q137 (owner). Pilot: pri 98 CLOSED (phase 7 run; exhausted within remit; 6 alternate paths; awaits DONE); pri 97 in round 3 (live-chain root 0.697 -> 0.753, UAS 0.603 -> 0.615; a broken copula detector found); pri 99 round 1 (PARTIAL; unseen 0.752 -> 0.800). Open for the owner: DONE on pri 98; Q135, Q136, Q137.  **POSITION 16:05 local 13 Sep:** OWNER CORRECTIONS applied to the solver program: phase 7 probe (understand? research; improve?) mandatory after every report (pri 98 reopened on it); every solver traces the signal loss chain by chain, confirms upstream BF per signal, researches the brain's math per chain, understands every negative, and says why its wins won -- in the kick-off prompt (phases 3/5/6) and sent to 97/98/99. Weighted role table: live matrix nsubj 0.714 -> 0.776 with objects kept; its board RUNNING (slow under load, ~16:30). Runners: 3 solver agents (97 at 115 min, 98 phase 7, 99 at 45 min).  **POSITION 15:20 local 13 Sep:** PILOT SESSION 1 COMPLETE: pri 98 (manner intensity) SOLVED by an opus agent, 57 min, 0 nudges (manner slice 6/6, CF precision 1.00, adverb-in-prose gold 0.444 -> 0.639 CI-sep); strategy reverify RUNNING; integration plan written in the day plan; waits for the owner's DONE in the tab. pri 97 (main assertion) RUNNING (65 min). Weighted role-table chain RUNNING (second launch after an audit ZeroDivision fix). New briefs in the tab: pri 99 (unseen words by form + position), pri 100 (sense-keyed affect norm). Next agent slot: pri 99 (lexical_categories; no collision) once the pri 98 reverify passes.  **POSITION 14:55 local 13 Sep:** board 0.6352 recorded; light shipment done. LABELS RUNG: the perceived-heads role table traded subjects for objects (matrix nsubj at gold heads 0.873 gold-table vs 0.748 live table) = agent below floor; confidence-weighted rebuild (`--perceived --weight`) + clause diag + board A/B RUNNING (`board_roles_w.log`). Copular subjects = heads problem (0.46 attached right). pri 99 (unseen words) written. Pilot solvers (97, 98) RUNNING. Parked on the board: Q135 (integration gate), Q136 (chunk patch run, denied).  **POSITION 14:45 local 13 Sep:** FINAL BOARD RECORDED 0.6352 (pri 93/15 in; patient 0.792, state 0.815; no dimension down). The frame cue's blanket form is REFUTED (full test 0.9152/0.9283 vs 0.9278); the stored-chunk form is drafted (scratchpad patch) but its run was DENIED -> autoloop stopped per rule; hdlab carries the refuted form INACTIVE (live asset has no frame counts). RUNNING: the two opus pilot solvers (pri 97, 98). NEXT (owner's call): approve the chunk patch run or drop it; light desktop shipment; pilot scoring on completion.  **POSITION 14:35 local 13 Sep:** gap work while the pilot runs: the heads rung's loss under the organ's own categories is ATTRIBUTED (1.56 UAS points; 58% from main-verb be/have tagged AUX: 'there is', 'have to', 'has an essay') -> the FREQUENT-FRAME cue (Mintz 2003; left/right neighbour word as two plastic count channels) is BUILT in `lexical_categories` (switch `HDLAB_LC_FRAME`, inactive until an asset carries frame counts); smoke repairs exactly those cases; full-test sweep + heads probe running (`diag_frame_cue.log`). Brain-math rows updated for today's landings.  **POSITION 14:15 local 13 Sep (post-compaction):** final board (`board_final2.log`) DIED with its stopped watcher -> relaunched as `board_final3.log` (haiku runner, ~70 min). THE PILOT IS RUNNING: two opus solver agents on pri 97 (main assertion) + pri 98 (manner intensity), strategy = supervisor (nudge on early stop; 'what would it take / path A' on PARTIAL); measuring wall time / turns / nudges / quality vs the owner-run sessions. Light desktop shipment after the board is recorded. Hand-outs open for the owner: pri 94, 96.  **COMPACTION 14:22 local 13 Sep (owner asked):** everything committed path-limited; desktop shipment done 14:02 (last light shipment to run after the final board). RUNNING: `board_final2.log` (watcher `board-final2-runner`; expected ~0.635). NEXT AFTER COMPACTION: record that board; light shipment; then the OWNER-AUTHORISED PILOT = two opus solver agents on pri 97 (main assertion) + pri 98 (manner intensity) per `notes/SOLVER_SUPERVISOR_PLAN_2026-09-13.md`, strategy as supervisor (the 4-step script baked into the prompt as phases; nudge on early stop; 'what would it take / path A' on PARTIAL); measure vs the owner-run sessions. Live defaults: heads attachment_arm; decode incr (beam 8, decay 0.8, clause wrap-up); categories lag 2 + stem reanalysis + v2 induced cue; lemma dual-route + collision rule; temporal Penn arm; role validities perceived-heads (occupancy OFF).  **POSITION 14:05 local 13 Sep:** pri 93 + pri-15 DONE and INTEGRATED (induced-categories v2 organ feeds the count tagger: 0.9278 / Penn 0.9176; slot occupancy landed selectable, default OFF by measurement -- patient 0.7954 off vs 0.7781 on; re-learned role table live). RUNNING: final board (`board_final2.log`); desktop shipment (assets + memory + bundle); solver-session analysis for the owner's evaluation. Then the owner moves to the desktop (MIGRATION_README steps; bundle fetch + ff-merge).  **HOLD 13:27 local 13 Sep:** two solutions await the owner's verdict in the tab (pri 93 roles object slot, PARTIAL; pri-15 reading-induced categories v2, SOLVED) -- integrate on DONE only. Hand-outs open: 94, 96, 97, 98. Nothing running.  **POSITION 13:22 local 13 Sep:** categories rung: CONFLICT-TRIGGERED STEM REANALYSIS landed (default on; UPOS 0.9264 -> 0.9271; rare slice +0.6; self-test PASS 0.6311 vs 0.6251). Live-chain heads probe running. Hand-outs: pri 93, 94, 96, 97.  **HEALTH 13:12 local 13 Sep:** eleven live witnesses GREEN under the final defaults (arm 17/17, fast path, coordination 6/6, roles 23/23, categories 7/7, morphology, harm/help 22/22 + result-state + landing, supersense, affect board 17/17 with a one-item tolerance for the filed 'wounded' tag miss). Desktop light resync done. Nothing running. Hand-outs: pri 93, 94, 96, 97.  **POSITION 13:05 local 13 Sep:** FINAL BOARD 0.6346 (stand-in 0.6378; patient 0.7888 the last gap; state 0.8095; coref/wic identical). Nothing running. Owner hand-outs live: pri 93 (roles-rung object slot), pri 94 (PP seesaw), pri 96 (far coordination = meaning). Next levers (filed): stem-informed category prior (rare forms like 'wounded'); governor scorer knowledge for non-verbal predicates / subordinate-first clauses; manner-intensity (grounded channel); pri 92 superseded.  **POSITION 12:45 local 13 Sep:** BOARD 0.6345 with the perceived role table (patient 0.788; stand-in 0.6378 -> gap 0.0033). Pri 95 COORDINATION INTEGRATED (conj 0.30 -> 0.38 incr; asset + re-keyed plausibility live). Final board RUNNING (`board_final.log`). New brief pri 96 (far coordination = meaning channel) in the tab. Live defaults: heads attachment_arm, decode incr (beam 8, decay 0.8, clause wrap-up), categories lag 2, lemma dual-route + collision rule, temporal Penn arm, role validities from perceived heads.  **POSITION 12:10 local 13 Sep:** post-landing board 0.6277 (neutral). ROLE VALIDITIES now learned from the governor's own PERCEIVED heads (live; patient 0.749 -> 0.790 A/B; witness 23/23; gold table kept); board with it RUNNING (`board_percval.log`). Then: typed-SP trio rebuild on dual-route keys with a UAS check (separate path first); governor main-word residual; stem-informed category prior (filed).  **POSITION 11:55 local 13 Sep:** pri 14 harm/help INTEGRATED + marked (all upstream joins; live gold 0.972; reverify green). Incr-default board 0.6278 (state recovered 0.8095; patient 0.7275 = the one gap). Landed since: copular predicate-head rule (binder 0.44 -> 0.60; UAS 0.612), lemma collision rule (wound stays wound), predicate-recall posterior fix, SEQ/GEK stores rebuilt+swapped. RUNNING: post-landing board (`board_post.log`); role validities from PERCEIVED heads (-> measure patient). Negatives: state head-posterior hand-off null; sense-in-context as override (regressed live gold); wider rare-word mixing. Owner update posted 11:45.  **POSITION 11:25 local 13 Sep:** owner-DONE pri 14 (harm/help result-state) INTEGRATED with all upstream joins (commit a1b9e1f2f; witness 22/22; reverify chain on a runner); labels-rung trace done -> state read now gets the graded head belief (commit 04bf8cbbc; A/B on a runner); incr-default board still running; SEQ swap + GEK rebuild after it.  **POSITION 11:02 local 13 Sep (after a session restart):** DEFAULTS NOW: heads = attachment_arm, decode = incr (beam 8, decay 0.8; 0.6080 vs search 0.6034), categories lag 2, lemma dual-route, temporal tagger Penn arm. RUNNING: full board under these defaults (`board_incr.log`, PID 14848, ~1 h from 10:58). NEXT after it: swap the SEQ store to `seq_store_v2_dualroute` + rebuild the GEK asset + self-test; labels-rung repair (patient/state) from the trace. Solvers: pri 93 + 95 assigned (owner), pri-15 running.  **POSITION 10:58 local 13 Sep:** BF-HEADS BOARD DONE: 0.6223 vs 0.6378 (coref/salience/cn/wic identical; patient -0.087, state -0.140 = labels-rung reads) -> HEADS DEFAULT FLIPPED to `attachment_arm` (commit 66edc3ad6; Q134 resolved by evidence; arceager selectable). Self-test + witnesses under the new default on a runner. Repair target = the ROLES rung (pri 93 object slot; copular-state read). Incremental arm: decay sweep running (120-sentence tie at decay 0.8). Category-work board `board_route2.log` still running.  **POSITION ~10:50 local 13 Sep (owner awake; autoloop armed):** owner direction 10:05 'organs take data in, in order' -> (a) CATEGORY ORGAN DEFAULT = two-word revision window (`LAG=2`, fixed-lag backward pass, byte-identical to prefix recomputation, 0.9264 = whole-sentence; witness 7/7; commit 74fd4d19f); (b) INCREMENTAL ARM of the attachment organ landed (`HDLAB_ARM_DECODE=incr`, learned hold expectation asset `attachment_hold_expect_v1.json`; 0.5972 best vs search 0.6034 on UD-EWT test 700; obj/nmod better, root/ccomp worse; 22-33% wrap-up words; richer by-left-candidate expectation building) -- default still map1. Boards RUNNING: `board_route2.log` (all category work) + `board_arm.log` (BF heads = Q134) under one watcher. Owner assigned solvers: pri 93 (re-scoped to roles rung) + pri 95 (coordination). Pri 92 superseded / pri 94 largely folded -> refresh/close. Earlier header follows.  AS OF: 2026-09-13 ~05:50 local (OVERNIGHT, Fable 5.1 session; stop hook ARMED; see POSITION ~05:50 and the overnight plan log) — earlier 09-12 header kept below: 🌙 overnight program COMPLETE (`notes/OVERNIGHT_PLAN_2026-09-12.md`, all steps ticked; fold-in backlog 0; board Q132 RESOLVED by strategy) **+ the pri-14 wall OVERCOME by strategy research: the RESULT-STATE arm of `force_dynamics_valence` is LIVE** (`notes/RESEARCH_result_state_valuation_2026-09-12.md`; witness `test_fd_result_state_arm` 15/15; full board pooled AGG 0.6408 -> 0.6408 (every dimension identical); helped-or-harmed 35/36 -> 36/36 (0.972 -> 1.000, twin 0.694); who-was-affected 0.387 unchanged; no regression). HOLD state: watch for owner DONE marks / solver landings (pri-1, 8–14); SCORECARD + UPDATES current.** Earlier today: ✅ **BOTH remaining owner-DONE solutions LANDED (fold-in backlog → 0): pri-5 meaning-fusion = the reading-grounding loop's sense-assignment read is now the FUSED convergent-cue read (grounded-distinctive + reading-grown SEQ + visual referent, SDT accept criterion; LIVE by default; grown 1M-line SEQ store shipped + merged into every fresh Substrate) — measured on its NEW coverage-QUALITY board arm: MRR@0.5 0.375 vs incumbent 0.014, twin loses, grounded-only 0.175 (n=87 smoke); context-gated sense = board WiC arm re-pointed at the LIVE wire + SCWS graded arm; NO discrete consumer gated (durable DO-NOT). Witness `test_fused_sense_ranker_live.py` 8/8; pri-5 witness 38/38 after; board self-test AGG 0.6313 (was 0.6294; WiC instrument change). FULL BOARD RUN DONE: AGG 0.6253 → 0.6409 (5/7 CI-sep; WiC instrument change), grounding_coverage_quality n=247 FUSED 0.3823 vs grounded-only 0.1586 vs incumbent 0.027 (CI-sep, twin loses), scws n=1260 +0.0156 CI-sep. Posted pri-12 glass-box morphology; queue 6 assignable. Everything committed path-limited, NOTHING pushed. Recovery: `project_state.md` + `substrate_health.py` + this header.**
   RECENT: owner raised AGGREGATE brain-foundationality ("organs left and right — same structure, different arms"). Ran the **brain-structure consolidation audit** (`notes/BRAIN_STRUCTURE_CONSOLIDATION_AUDIT.md`): 83 organs → ~25 structures; CONSOLIDATE coref 6→1 / meaning-channels / temporal / thematic(split) / force; KEEP causal-Pearl-rungs + predictive-stages + situation-model-dimensions (verified). Landed the **anti-fragmentation GATE** (one-structure-one-organ, in the brief standing-rules). Owner steer (Q126 RESOLVED): start it, deep merges → FLEET problems. **POSTED the flagship `consolidate_the_six_coreference_organs_into_one_cue_based_entity_resolver` (pri-6, assignable);** staggering temporal/thematic/force; trivial `sensorimotor_spoke` fold = mine-side quick-win on request. Board **2 open (Q124 ready-wave, Q125 parser-reconciliation)**. Also: pos_tagger located-negative reverified 6/6 (converges w/ pri-3 on the arc-scorer wall) + tracked; core-cap discipline re-applied (OMP=4). ⬇️ prior CONT-126:
   THIS TURN (CONT-126): INTEGRATED the owner-DONE **pri-3 parser** solution (reverified 66/66). It is a rigorous LOCATED NEGATIVE — the parser's wall is the SCORER (~99% of head error), not the decode (0.2 UAS pts); text-only acquisition ceilings ~0.46; attachment is POS-structural (semantics refuted 4×). Landed its CORE VALUE (characterization → §2b; SOLVED marked integrated; BF ledger) + enriched the follow-on (pri-2 scorer-acquisition, with pri-3's proven 0.463/DMV-refuted/structure-lever results). **RECONCILIATION (Q125): items 1a/1b/2 target the arc-FACTORED parser but the live head path is arc-EAGER (owner-DONE consolidation) — did NOT reverse it for a +0.002 micro-win; route-through = measure-first follow-on. NO live code touched → board byte-identical (self-test env-flaked on a torch DLL load; clean 0.6294 earlier this session, guaranteed no-regress since zero code changed).** BF NOT_BF count unchanged (parser organs keep NOT_BF, sharper residual noted; the scorer acquisition is the deep gap = pri-2). Committed path-limited, NOTHING pushed. Q124 (pri-4/pri-7 ready wave) + Q125 open.
   >> PROCESS + ROADMAP (owner 2026-09-10 'own this project'): the front door is now **`python tools/substrate_health.py`** (the single DERIVED health view: BF status, integration backlog, gates, top-issues) + **`notes/SUBSTRATE_PROCESS_AND_ROADMAP.md`** (the maintenance cadence + prioritized roadmap + doc map). Run substrate_health FIRST each cycle; act on TOP ISSUES worst-first.
   🗺️ **DERIVED VIEWS -- DO NOT RESTATE THEM HERE, THEY ROT. `python tools/substrate_map.py`**
   joins the `38` organs, `10` pipeline stages, `22` briefs and the registry and rebuilds on every
   run: `--gaps` worst-first, `--organ B3`, `--brief <slug>`, `--progress`. **Solvers may use it.**
   ✅ **CLOSED 08-22, one line each, full text at the named note:** grounding quality is
   `3/100 MEANINGFUL, 19 RELATED, 78 NOISE` blind (`THE_GROUNDING_ANSWER_...`); the best-evidenced
   grounding result is real but MISNAMED -- it relieves a d=256 CAPACITY bottleneck, not an ability
   (`THE_BEST_EVIDENCED_GROUNDING_RESULT_IS_MISNAMED_...`); 156 smoke rows carry a HARD_PASS their
   FULL run does not, hazard did NOT bite (`156_SMOKE_RUNS_...`); the certification gate ran ZERO of
   456 tests for two days behind a false `RESULT: PASS`, now fixed
   (`THE_CERTIFICATION_GATE_HAS_RUN_ZERO_...`); Q103's "only 40 usable pairs" was my own 60k-cap
   artifact, WITHDRAWN. **ONE STRUCTURAL FINDING spans three of those dead ends: one representation
   does two jobs needing OPPOSITE things -- grounding must delete the word, identification needs it
   present (word alone `0.9687` vs `0.6423` word+sentence; CONTEXT DILUTES identification, it is a
   LOOKUP). The form organs are already built and unwired.**
   **READ `notes/BUILD_PLAN_post_audit_2026-08-19.md` FIRST BLOCK -- THEN `## POSITION` BELOW.**

Rules: `STATUS_SPEC.md`; stubs resolve in `STATUS_LESSONS.md` (uncapped). FOUR literals
MACHINE-PARSED, never reword: `AS OF:`, "POSITION", "TOP ITEM" and "WHAT IS RUNNING"
(`session_start_hook.py`, `board.py`). **Inside a section use `###`, never `##`.**
*NEVER let a line BEGIN with one of those literals even when merely NAMING it -- which is why the
four names above are in DOUBLE QUOTES, not backticks. **AND THE OPPOSITE FAILURE HAPPENED HERE ON
2026-08-21: quoting them in BACKTICKS that were never closed swallowed the two REAL headings into
this paragraph** -- one before `### 2026-08-21` and one before the TOP ITEM heading -- so
`board.py` wrote MISSING REQUIRED LITERAL for BOTH, into the owner-facing board, and the file
still LOOKED complete because every section BODY was present. **A heading is not the same object as
its section: grep the four literals, never eyeball the content.** Restored the same day.
**RUN `python tools/board.py self-test` AFTER ANY EDIT TO THIS HEADER.***

## POSITION

### 2026-09-12 ~23:45 — OVERNIGHT (log times in the plan are local clock) (owner asleep; autoloop ARMED; plan = `notes/OVERNIGHT_PLAN_2026-09-12.md`, log at its end)
  - **THE LIVE CHAIN IS NOW ORGAN-BY-ORGAN BF-OR-BF_SPIRIT from tokens to roles, behind ONE shared frontend (`hdlab/frontend.py`):**
    tokens (corpus / `frontend.tokenize`) -> categories `hdlab/lexical_categories.py` (count-based generative model + forward-backward
    posterior; LIVE DEFAULT, 0.912 vs perceptron 0.945; reading-acquisition arm `tools/build_lexical_categories_from_reading.py`)
    -> lemma `hdlab/morphology.py` (glass-box morphy, BF) -> heads `hdlab/attachment_arm.py` (semantic bootstrapping; self-grown
    plausibility store; graded category hand-off; copular convention; Hindle-Rooth PP cue; `HDLAB_HEADS_SOURCE=attachment_arm`) ->
    roles (reads P(head)). The board arms (patient/agent/state/causation) now read the SAME frontend, so both switches reach the board.
  - **Numbers (UD-EWT test 700, gold categories):** attachment arm 0.5583 (root 0.796 nsubj 0.724 obj 0.688 obl 0.451 nmod 0.185
    xcomp 0.686 ccomp 0.414); under the category organ's tags 0.5362 hard / 0.5400 graded; fully reading-learned chain (induced
    categories) 0.4649 (pri-15's target). STATE dim under BF heads 0.527 -> 0.6587 (CI-sep) after the convention-agnostic copula read
    + copular convention. Boards: baseline 0.6395; BF heads (reader only) 0.6294 (only STATE moved); counts tagger (reader only)
    0.6385 (only STATE moved, 0.799). Post-routing boards running.
  - **INTEGRATED tonight (owner-DONE):** pri-12 morphology (BF, 13 repoints); pri-9 grounding-quality instrument; pri-13 semantic hub
    (PARTIAL: `hdlab/semantic_hub.py` representation organ, fusion read kept); pri-10 causal store (REFUTED -> durable negative, +0.139
    retired). Heads-rung reconciliation: the arm subsumes the pri-2 reading-learned scorer; its Hindle-Rooth PP cue folded.
  - **Fold queue:** `notes/UPSTREAM_FOLD_QUEUE_2026-09-13.md` (13 proven-but-unfolded upstream items). Transfer to the desktop
    COMPLETE; delta bundle `delta_20260913T032825Z.bundle` sent (see MIGRATION_README step 5b).
  - **Boards (post-routing):** tagger everywhere 0.6358; fully BF chain 0.6219 (patient 0.728, state 0.653, agent 0.821 -> repaired to
    0.836 by the graded candidate read; coref/common-noun/salience/wic identical). PP cue: heads UAS 0.5706 (nmod 0.185 -> 0.346), promoted.
    Reading-acquired category organ 0.7242; fully reading-learned chain 0.4922. Order-aware teacher: refuted as built (ccomp collapse).
    Phase 2: agent repaired; patient/state losses are UPSTREAM (heads obj attachment -- 48% of object misses go to the NEXT verb; tagger on
    copular predicates). OWNER QUESTION FILED: flip the heads default now (slow boards ~3 h; patient/state dips) or vectorise first.
  - **~05:50 heads rung, teacher v2 PROMOTED** (order-aware + root = subject fit + object fit + pronoun participants by the verb's
    pronoun-filler rate): obj 0.685 -> 0.700, nsubj 0.762, root 0.813, obl 0.468, ccomp held 0.457, UAS 0.5694 (noise), nmod 0.346 ->
    0.311. Previous asset kept at `data/hook_state/attachment_validities_pp_prev.json`. **FINDING: only 1.7% of head decisions differ
    between the two builds -- the arm was MEANING-BLIND at read time (meaning only shaped the acquisition posterior); the patient
    dimension under the new asset is 0.7283 = unchanged.** Two flagged builds in A/B (runners): case-marking rules
    (`HDLAB_SBT_PP_NOSUBJ`: pre-verbal PP object is not a subject; possessive pronoun is not a participant -- from the nmod anatomy)
    and meaning as a READ-TIME cue (`HDLAB_ARM_PLAUS_CUE`, Competition Model plausibility cue with learned validity). Lemma-rung fold
    items (dual-route morphology; POS into morphology, now unblocked by the BF category organ) under audit.
  - **~05:45 local (09:45Z) -- three more upstream landings:** (1) the attachment arm's readout VECTORISED (identical to 1.8e-15,
    scorer x4.3; `arc_scores_reference` kept; witness `test_attachment_arm_fastpath.py`); (2) MEANING AS A READ-TIME CUE (`plaus`,
    flag `HDLAB_ARM_PLAUS_CUE`): v1 nmod 0.311 -> 0.375, ccomp 0.500, obl 0.468 -> 0.379 (fired on PP objects); v2 core-slots-only
    rebuilding; noun typing now reads an offline supersense asset (0 mismatches on 1,347 nouns; 27x faster; no WordNet call at read
    time); (3) the live reader's LAST off-the-shelf tagger (nltk PerceptronTagger in `temporal_model`, 5 reader sites) replaced by the
    category organ's PENN ARM (`lexical_categories_counts_penn_v1.json`; 0.9075 vs 0.8508; VBD/VBN/VBZ recall equal or better) --
    default flipped, board no-regress run queued. Dual-route lemma arm landed (`HDLAB_MORPH_MODE`, default morphy; board A/B running).
    Fold queue item: ~40 hdlab modules still read WordNet at inference (foundation lookups, allowed) -- export pattern available.
  - **~06:40 local -- HEADS RUNG 0.6034 (UD-EWT test, gold categories; 0.4826 at landing):** meaning-cue v2 asset promoted (0.5726);
    PUNCTUATION CONVENTION (+2.3; formatting, board-invisible); SINGLE-ROOT decode (`HDLAB_ARM_DECODE=map1`): the old MAP was multi-
    rooted (root recall inflated; consumers got disconnected clauses) -> ccomp 0.672, advcl 0.313, xcomp 0.730, obl 0.463, obj 0.725,
    nsubj 0.767, root 0.744 honest. **`frontend.Parser` served a NON-TREE (argmax of marginals, 87/300 sentences not a tree) to every
    consumer until now; it serves the decoded tree + marginals.** MBR tree refuted as point estimate (0.5855). Category switch TOTAL
    (`PosTagger.load` -> frontend). Boards in flight: dual-route lemma, Penn tagger, total routing, then the board under the BF heads.
  - **~07:25 local -- CATEGORY ORGAN ARM v2 live** (same count model + shape emission factor + rare-word mixing + second-order
    transitions with Dirichlet back-off + the READING-ACQUIRED cluster cue for rare/unknown words): UD-EWT test 0.912 -> 0.9264 (unknown words 0.637 -> 0.749); Penn arm 0.9075 -> 0.9160
    (first order + shape; second order too slow for read time at 49 tags). Live-chain heads (count organ's own tags) 0.5761 -> 0.5892
    (gold-category ceiling 0.6034). Occupancy-at-decode REFUTED (belongs to the labels rung); coordination convention measured too
    weak to land (0.59 on gold trees).

### 2026-09-12 ~14:30 local — DEVELOPMENT RESUMED ON THE LAPTOP (owner) while the transfer streams to the desktop over SSH/Tailscale (~0.3–0.5 GB/min; a delta sync follows)
  - **HEADS RUNG, built to a spec:** `notes/RESEARCH_attachment_organ_spec_2026-09-12.md` (what the organ computes, how the brain separates signals optimally, every consumer's signal requirement). First build = probe v18, attachment as configuration-conditioned CUE COMPETITION with strengths learned from soft tree-posterior counts (no treebank, no hand-authored prior): **UD-EWT test UAS 0.4715 (r0) → 0.4801 (anchored self-teaching)** vs the cached reading-learned scorer 0.4626 and its decode-time-constrained 0.4755; twin 0.03; unconditioned cues drift. Heads→roles hand-off +0.0016 (v19). Lexical cue too sparse at 1.5k sentences. **Owner: the brain is 0.9+; we are far behind** — gap = experience volume (probe v20: 20k/60k Simple-Wiki sentences, running), a learned argument-structure lexicon, meaning feeding structure, incremental prediction.
  - **HEADS RUNG, evening:** two research drills kept (`RESEARCH_syntax_acquisition_brain_and_labelfree_parsers_2026-09-12.md`, `RESEARCH_sota_parser_anatomy_and_glassbox_recovery_2026-09-12.md`): the label-free field ceiling is ~0.65–0.68, not 0.9; labels buy CONVENTION (~100 gold trees → 75–80 UAS). Our ERROR ANATOMY vs the supervised parser: a third of the 0.30 gap is function-word convention (punct/case/cop/det/cc/flat), comprehension-relevant gap ≈ 0.10. Built: constructions as coalition cues (full 0.4626 → 0.5303, prior-informed), function-word frames, a CONVENTION layer at decode (smoke 0.505 → 0.541), the landed organ `hdlab/attachment_arm.py` (+ learner, witness 13/13). Knowledge-free bootstrap: 0.272 → 0.4495 (5 rounds). Negatives recorded: volume (closed), the learning-time mechanism bundle (−0.04), occupancy (second-order mean-field) cue, induced constructions (two forms), meaning teacher (locality artefact). DECISIVE run in flight: knowledge-free + constructions + convention layer.
  - **HEADS RUNG, late evening — THE JUMP:** the 0.4826 landing hid a COLLAPSED CORE (obj 0.19, obl 0.12, root 0.41: the knowledge-free teacher never learns that the verb heads its arguments). SEMANTIC BOOTSTRAPPING (the event predicate heads its plausible participants; `attachment_arm.SemanticBootstrapTeacher`, β swept 2/5/10, hard gate refuted) joined to the co-occurrence teacher as one posterior: **asset rebuilt 21:03 — UAS 0.4792 → 0.5573, root 0.79, nsubj 0.71, obj 0.71, obl 0.45, xcomp 0.59** (witness 15/15; above the prior-informed 0.5303). Caveat: the plausibility store is foundation-informed (UD-shaped parse of simplewiki, offline). Three owner-run solver briefs written (pri 15 closed-class categories, 16 second-order sibling, 17 PP mining) — scorecard tab 4 'PROBLEMS TO HAND OUT' with a copy-prompt button; pri-8/11 marked SUPERSEDED, pri-1 strategy-owned. GRADED HAND-OFF LANDED (coarse_roles reads P(head); reader passes it via _cached_head_posterior). Heads→roles on BF heads: 0.726 marginalised vs 0.809 on supervised heads (expected dip; graded read +0.013). BOARD A/B (HDLAB_HEADS_SOURCE=attachment_arm): the process EXITS 1 SILENTLY after the causal-sign arm (no traceback; the spatial arm passes alone) — diagnostic + both board runs on haiku RUNNER agents (never wait in the main thread — owner, twice). OWNER 21:50: no problems downstream of an unfixed element → pri-16/17 PARKED (priority 93/94, banner) until pri-15 (categories) lands; pri-15's exact target = SIGNAL_FLOW_MAP §2e; its hand-off NUMBER (22:20): induced categories → heads UAS 0.4649 vs 0.5640 with UPOS (same 1.5k slice; conj 0.09 vs 0.24, root 0.61 vs 0.79, obj 0.50 vs 0.74) = the first fully reading-learned chain, 0.465. Autoloop ARMED. LATE: pri-12 glass-box morphology INTEGRATED (hdlab/morphology.py; byte-identical); SELF-GROWN plausibility store is the heads teacher's default (0.5642 = parser store 0.5640); board A/B with BF heads: only STATE moved (0.828->0.527, copular SHAPE) -> copular_binding repaired (convention-agnostic holder), A/B rerunning; who-did-what/coref board arms bypass the reader's parse (own parser) -> next wiring item. STATE under BF heads repaired 0.527->0.656 (direction-agnostic copula). 23:15: CATEGORY ORGAN `hdlab/lexical_categories.py` (count-based generative model + forward-backward posterior; from the pri-12 solver's prototype) is the reader's DEFAULT tagger (0.912 vs perceptron 0.945; HDLAB_TAG_SOURCE=perceptron restores); board A/B running (counts; counts+BF heads = fully BF inference chain).
  - Phone app `tools/owner_phone_app.py` (scorecard / questions / updates / account switch + NOW card + pairing link) in the transfer set.

### 2026-09-12 ~13:45 local — DEVELOPMENT PAUSED BY OWNER FOR MIGRATION TO THE DESKTOP (superseded by the resume above; the transfer set is still the handover)
  - Transfer set: `C:\AI\hd-instrument_desktop_2026-09-12\` (repo minus .venv 191.9 GB / 276,169 files, 0 failures; git bundle; frozen requirements; Claude memory + session folders c--AI/d--AI; `MIGRATION_README.md` = the ordered next steps + verification + re-sync commands). Autoloop DISARMED. HEAD at pause: see `HEAD_commit.txt` in the set. Resume on the desktop: `notes/STATUS.md` → plan top block → `SIGNAL_LOSS_LEDGER_affected_entity_chain.md`; top unblocked item = the heads rung (attachment LEARNER is the bottleneck; pri-11 + the two label-free constraints).
  - Standing rules added today (memory): narrate focus; start at the top by position; plastic/never frozen + phase diagram; downstream regression ≠ failure; trace the consumer's signal requirement; route agents to cheaper models (Fable capped at 50%).

### 2026-09-12 (LATE NIGHT, post-compaction — UPSTREAM MATH-BF PASS: the Competition-Model ROLE LABELER landed)
  - **Owner direction at compaction:** "+4 is fine, not great → a dedicated focus on establishing mathematical brain-foundational operation to all upstream components; if items are clearly BF they correctly separate and send signals downstream, essentially lossless." Executed on rung 5 of the affected-entity chain (`notes/SIGNAL_LOSS_LEDGER_affected_entity_chain.md`).
  - **✅ BUILT + LIVE: the Competition-Model coarse ROLE LABELER** (`graded_role_assigner.coarse_role_cues/coarse_roles`; cue validities LEARNED on UD-EWT train → `data/frontend_assets/coarse_role_validities_ud_ewt.json`; wired as `arc_labeler.COMPETITION_ROLES=True` over the one `label()` call every live consumer reads — argument roles the competition's, fine relations kept). Works ONLY in the configuration-conditioned contrast form; v1 (one role per cue) and v2 (flat additive table) FAILED with numbers — recorded so nobody re-treads.
  - **Measured:** held-out UD-EWT test 0.917 (BY_AGENT .944, OBJ .953, SUBJ .926); **the 596-item pronoun-undergoer decision on the deployment parse 0.4698 → 0.4933 = +0.0235 CI95 [+0.0033, +0.0436]** (a third of the −0.0705 parse loss recovered); witness `verification/test_coarse_role_competition.py` 18/18; shuffled-strength twin breaks it. Decomposition: gold structure 0.845 → predicted POS 0.781 → predicted heads 0.698 on GUM — the TAGGER and HEADS are now the lossy rungs above a clean labeler.
  - **BOARD with the labels LIVE (16:31Z): 0.6408 → 0.6377** — affected_entity 0.387 → 0.397, forward half 0.479 → 0.501 UP; who_did_what_patient 0.830 → 0.809 and state 0.833 → 0.815 DOWN. **Owner ruling: a downstream regression after a more-BF upstream is NOT failure — repair the consumers to receive the richer signal.** Consumer signal trace (instrument mirror, n=1255): 23/37 lost items = the competition labelled the object AND a second post-verbal nominal `obj` (iobj folded into OBJ) → IOBJ class + animacy + verb-frame (per-lemma recipient propensity) + ONE post-verbal slot coalition cue (two redundant cues had double-counted). Standalone: patient 0.8088 → 0.8207, state 0.8148 → 0.8280; held-out UD-EWT 0.9235; witness 23/23 (incl. PLASTIC validities: counts carried, online observe/update, save/load). **FULL BOARD 17:15Z: AGG 0.6377 → 0.6395** (pre-switch 0.6408; patient 0.8207, state 0.8280, patient_defer 0.9611, forward-half row 0.5036, every other arm identical; per-dim gate OK). **596-item decision with the final labeler +0.0268 CI95 [+0.007, +0.049].** Residual vs pre-switch (patient −0.010, state −0.005) traces UP to tagger/heads.
  - **HAND-OFF categories → heads measured (probes v15/v16):** the reading-learned attachment scorer over 8k sentences cannot use the 70-way inventory (UAS 0.012); the same categories collapsed by gold-majority NAMES reach 0.208 (gold POS 0.276) but the label-free 17-cluster coarsening scores only at its twin (0.126 vs 0.131) → the attachment rung needs SPECIFIC distinctions (not just fewer classes); the scorer learning from the same reading at 20k/50k lines is running.
  - **HEADS RUNG REFRAMED (probes v15–v17 + two learner constraints):** the fine-inventory collapse was the attachment learner's frequency-driven root prior crowning PUNCTUATION (the form-class constraint fixes it: marks never head/root); punctuation = written PROSODY → a boundary cue on arcs (gold POS 0.296 → 0.315; floor 0.290); coarsening the categories by attachment behaviour = twin (refuted). **With gold categories the learner reaches only 0.315 → the attachment LEARNER is the heads rung's bottleneck, not the categories**; the two label-free constraints + the graded category posterior go into pri-11's full-scale scorer next.
  - **TOP RUNG (owner: start at the top, by position):** `exp_reading_induced_categories_v1` — lexical categories induced from reading Simple-Wikipedia (Harris/Mintz/Redington substitution classes; exposure-weighted competitive learning = Rumelhart-Zipser; orthographic form classes; learned form cue for unknown words; gold UPOS for EVAL only): **0.745 type-level / 0.722 token-level @1M lines, k=68** (prior attempt 0.323; supervised tagger 0.944); plastic ONLINE form 0.67–0.69 @20k; Ward hierarchical REFUTED; raw induced category as a role cue REFUTED-AS-BUILT (too fine). Closed classes (ADV/CCONJ/SCONJ) still ≈0 = the next fidelity step.
  - **Top-of-chain audit:** board instruments consume GOLD tokens/sentences (rung 0 = 0 loss here); WordNet-morphy at read time = pri-12; the nltk tokenizer+tagger tools removed from `context_grounded_valence` (glass-box tokenizer + our tagger; witness PASS).
  - **Memory rules saved (owner, today):** narrate focus at every step; start at the top by position; plastic/never frozen + move on the phase diagram; downstream regression ≠ failure; trace the end consumer's signal requirement chain by chain; "train" = a corpus split name only, nothing frozen.

### 2026-09-12 (DAY — strategy took pri-1 itself; FIRST ARM LANDED)
  - **Owner directives this morning:** wall-push protocol saved (`notes/WALL_PUSH_PROTOCOL_owner_motivation_messages.md`) + evidence ledger (`notes/HOW_WALLS_WERE_BROKEN_2026-09-12.md`); build from math-BF organs (no model replication); keep `notes/BRAIN_MATH_REFERENCE.md` as THE key experimental document (per brain computation: math | status | our numbers | organ).
  - **✅ pri-1 FIRST ARM LANDED (forward half of pronoun-undergoer resolution):** `affected_entity_resolver.EntityTokens` (every resolved pronoun ACCRUES to its entity token; FOREGROUND window; Principle A for reflexives), reader walks mentions incrementally, `referent_per_np` keeps reflexives as pronouns, board row `affected_entity_forward_half`. THIRD-person GUM undergoers 0.4966 -> 0.5403 gold (+0.0436 CI-sep) / 0.4418 -> 0.4789 predicted (+0.0371 CI-sep); twins 0.445/0.223; witness `test_affected_entity_forward_half` 12/12; landed resolver path byte-identical. **Full board pooled AGG 0.6408 -> 0.6408 (every dimension identical); who-was-affected row 0.3874 -> 0.3874; forward-half row 0.4789 on 593 third-person items; no regression.**
  - **Findings with numbers (research note `RESEARCH_generative_entity_state_pri1_2026-09-12.md`):** the GUM undergoer slice mixes THIRD/DEICTIC/DEMONSTR/OTHER (596/263/115/168); only THIRD responds to entity mechanisms; in-focus ceiling top-3 0.753; generic knowledge marginals fused uniformly HURT (REJECTED); individuation prize +0.091 is circular (coref line); `state_register` is surface-keyed with no GUM coverage (starved upstream).
  - **Arm-2 probes (v8/v9, committed):** hub-relatedness event coherence REFUTED-AS-BUILT inside the focus; residual anatomy: the pick is the ESTABLISHED entity (6+ mentions 57 vs 29; current subject 44 vs 17), gold the NEW low-prominence referent; Centering continuity terms HURT. v10 (role-prominence / history-depth / decay sweep) written, not run.
  - **OWNER AT COMPACTION (2026-09-12 ~15:50Z): '+4 is fine, not great' -> NEXT = a DEDICATED UPSTREAM MATH-BF PASS** (top-down along the affected-entity chain: parse spine [NOT_BF, costs 0.540->0.479], entity tokens [oracle +0.091], state register keying, fusion rule), making each rung mathematically BF so signals pass losslessly, BEFORE adding downstream terms. Key docs: `BRAIN_MATH_REFERENCE.md`, `SIGNAL_FLOW_MAP.md` s9, `RESEARCH_generative_entity_state_pri1_2026-09-12.md`.

### 2026-09-12 (NIGHT, later — the pri-14 wall overcome by strategy research; result-state arm LIVE)
  - **✅ `force_dynamics_valence` RESULT-STATE ARM landed (strategy, not a solver; pri-14 brief §9 records it so no solver rebuilds it).** `endstate_valence_sign` now reads the patient's RESULT STATE first: WordNet-SENSE-keyed VerbNet result/end-state predicates over the Patient (`harmed`/`suffocate`/`degradation_material_integrity`/`!alive`/`manner(forceful)&contact(end(E))`) over the verb's affecting ANIMATE-OBJECT senses, valued by the affect lexicon or the innate nociceptive sign; the word-level Warriner norm is the SECOND read; else abstain. Offline asset `data/frontend_assets/verbnet_result_state_v1.json` (233 sense keys; `tools/build_verbnet_result_state_asset.py`).
  - **Measured:** recovery 9/9 (batter, bludgeon, pummel, club, throttle + clobber/wallop/thrash/whack); wrench/maul honest abstention; harm-frame 10/10; social-harm/help = word-only read (no regress); NO neutral decided; consistency with the word sign 0.84–0.87 where both exist (all disagreements = weapon/assault senses read correctly for a person); 31 newly decided (all HARM; VerbNet has no positive result-state predicate); twin (states on random sense keys) 0/5 vs 5/5; board gold 24/24; 5 prior witnesses + tag gate green; smoke board 0.6319 unchanged; **full board pooled AGG 0.6408 -> 0.6408 (every dimension identical); helped-or-harmed 35/36 -> 36/36 (0.972 -> 1.000, twin 0.694); who-was-affected 0.387 unchanged; no regression**.
  - **REJECTED with numbers (do not redo):** hypernym inheritance of result states 0.65–0.75 (rare-sense routes: fry=electrocute), synset-mate class reads 0.75, gloss-first (breaks punch/slap/beat/choke); gloss-as-fallback 0.81 measured, NOT landed. Research note `RESEARCH_result_state_valuation_2026-09-12.md`; §2b entry; registry corrections; SIGNAL_FLOW_MAP §8; asset register #9.

### 2026-09-11 (EVENING — Fable 5.1 session: the MEANING-CHANNEL PAIR integrated; fold-in backlog 0)
  - **✅ pri-5 `measure_end_to_end_whether_the_meaning_fusion_lifts_live_grounding_coverage` INTEGRATED (owner-DONE; reverified 38/38 BEFORE+AFTER).** Behavior-changing, LIVE by default: `hdlab.reading_grounding_loop.FusedSenseRanker` at the grounding gate — grounded-DISTINCTIVE ATL (+) reading-grown DIRECTION-typed SEQ (+) visual referent (new `sensorimotor_spoke` arm; DINOv2/THINGS centroids = FOUNDATION asset at ingest, labelled stricter than the solver), each cosine → z over the field (divisive norm) → log-softmax → earned-gain (log1p, Ma-Pouget) weighted sum → SDT z_top criterion on the iid null (FA 0.05 swept) replaces the fixed 0.45 cosine. Recall path byte-identical (W2). The loop accrues directional counts for every content lemma; `tools/grow_seq_store.py` grows it on 1M modern Simple-Wiki lines (~1 min) → `data/foundation/seq_store_v1` (gitignored; regenerate on a new machine) → `Substrate()` merges it. **MEASURED (new board arm `grounding_coverage_quality`, the loop's OWN live decision, n=87 smoke): incumbent 0.014 → FUSED 0.164 (curriculum) → 0.324 (250k) → 0.375 (1M), all CI-sep, twin loses; grounded-only 0.175 → grown SEQ+referent add +0.200 CI-sep.** The count metric stays quality-blind (fewer, better links). NOT wired (solver's own W21–W23 negatives): valence axis, confidence reweighting. Residual recorded: WordNet-morphy at runtime in the lemma rung (computation BF, impl external) = the next glass-box fix; PBV per-encounter path still incumbent (live substrate uses the consolidation gate).
  - **✅ `wire_the_context_gated_sense_read_into_the_live_meaning_path_and_measure` INTEGRATED (owner-DONE; reverified PASS+PASS).** Board WiC dim RE-POINTED at the live wire (`board_wic_via_live_wire_dimension`; old CO._pick stand-in informational) → self-test AGG 0.6294→0.6313 (instrument change, reader untouched); NEW arm `board_scws_graded_dimension` (settled vector vs context-free, SCWS; rho). NO discrete consumer gated — the ONE principle: sense pays only in a graded DISCRIMINATIVE consumer (the pri-1 world-model), never OR/licensing (coref type-license null; natural-logic is-a −0.48).
  - **✅ FULL BOARD RUN DONE (1013 s):** FULL BOARD `--run` (2026-09-11, 1013 s, n=11218 pooled): AGG 0.6253 → **0.6409** (5/7 dims CI-sep; the rise is the WiC instrument change — live wire 0.7493 vs the old CO._pick stand-in 0.6639 on the SAME n=2038 — the reader path is untouched); `grounding_coverage_quality` FULL population n=247 (4640 curriculum sentences + the 1M-line grown store): FUSED MRR@0.5 **0.3823** vs grounded-only 0.1586 (+0.2237 CI [0.152, 0.304]) vs incumbent AUF-MRR 0.027, twin 0.027 (+0.3557 CI [0.294, 0.432]); 0 fallbacks to the incumbent; `scws_graded_meaning` FULL n=1260: rho 0.3942 vs context-free 0.3785 (+0.0156 CI [0.0089, 0.0229]) and vs twin +0.013 CI-sep. Folded into §2b / INTEGRATION_LEDGER / project_state.
  - **🧩🔁 SALIENCE EQUATION → ONE IMPLEMENTATION (late 09-11):** `graded_coref_pick` re-implemented ACT-R base-level activation; it now imports `salience_binder.actr_activation` (as entity_resolver/online_entity_cluster already did). Witness `test_salience_equation_single_source` 3/3 (1-ulp compensated-sum note); 5 coref witnesses + self-test rows identical (AGG 0.6313). Consolidation audit Cluster 13 closed by composition.
  - **🧩⏱️ TEMPORAL TRIO → ONE ORGAN (late 09-11, delegated byte-identity refactor, verified first-hand):** `hdlab/temporal_model.py` replaces `temporal_ordering`/`_multiframe`/`temporal_order_register` (shims kept; 5 importers repointed incl. situation_reader); byte-identical on module self-tests, the 4 board temporal rows and a 127 KB public-surface probe; self-test AGG 0.6313 unchanged; witness `test_temporal_model_consolidation` PASS; 87 organs tagged. Consolidation audit #3 closed; next: salience pair, force, appraisal (one at a time).
  - **✅ 2026-09-12 READY WAVE INTEGRATED TOP-DOWN (owner Q131):** pri-7 harm/help landed in 4 steps along `SIGNAL_FLOW_MAP.md` (3a Bayesian DP-head POS correction; 3b voice post-correction; 3c force-dynamic arithmetic, `force_dynamics_valence` NOT_BF→BF_SPIRIT, frame list + read-path FrameNet gone; 3d the reader passes its bound predicate `gov_idx`); board affect arm re-pointed at the LIVE organ (0.972 vs 0.333, twin 0.583 — it had scored a stand-in copy stuck at 0.778); smoke AGG 0.6313→0.6319; 8 landing-era witnesses re-based with reasons; recorded boundary: word-level-valence-uninformative assault verbs abstain (synonym backoff tried + WITHDRAWN). pos-tagger = located negative folded (CRF not flipped, −0.010 alone; joint-decode note in pri-11). Typed selectional organ PROMOTED (`hdlab/typed_selectional_preference.py`, 6,450 verbs, LATENT → pri-1). Fold-in backlog 0; 85 organs: 7 BF / 72 BF_SPIRIT / 6 NOT_BF. Consolidation audit: force = composition, appraisal = distinct checks (both closed by code check). POSTED pri-14 (resulting-state valence for the harm/help read — the recorded boundary; board Q132 asks the owner's intuition). Queue 8 assignable. Full board `--run` DONE 03:11Z: AGG 0.6408 (5/7 CI-sep), affect_harm_help 0.972, affected_entity 0.387 (+0.014), no per-dimension regression (trend gate, 2 rows).
  - **🖥️ OWNER WINDOW SIMPLIFIED (owner request): `tools/scorecard_gui.py`** — two tabs: HOW WE'RE DOING (plain-language scorecard: every ability's score vs a simple rule, trend since the previous full check, brain-faithful or stand-in; the short version / what changed / what we're working on / what isn't brain-faithful yet) and QUESTIONS FOR YOU (board answers, a note box, Mark-as-DONE for finished work). Source of truth = `notes/SCORECARD.md` (hand sections I keep updated after every landing) + `data/scorecard.json` built by `tools/scorecard.py` from BOARD_TREND.jsonl / the BF registry / notes/problems / BOARD.md. **Discipline: update SCORECARD.md's hand sections at every landing and every full check.** Old `tools/status_gui.py` left in place.
  - **🧠🔬 RESEARCH FOLD + pri-13 POSTED (owner: "do the requisite research and KEEP it; the brain reuses one structure for many functions — no islanded copies").** `notes/RESEARCH_one_semantic_hub_many_readouts_2026-09-11.md` (14 verified sources): ONE graded vATL hub read by every task (PINNED); built by error-driven CONVERGENCE of spokes `h=f(Σ W_i s_i)` (Rogers-McClelland; Jackson 2021), task control = input gain on the spoke layers (Hoffman 2018), CLS consolidation folds reading-grown knowledge in. **Corrects two labels:** the precision-weighted convergent-cue fusion (FusedSenseRanker, convergent_cue_reader, pri-5 W38-41) is a normative ANALOGY never fit to ATL → its RULE is OPEN in the registry; W40's "separate pools is PINNED" over-reached (the dissociation is episodic-vs-semantic, not spokes). **Disk confirms the islanding risk:** ≥6 graded lexical-semantic representations, 2 with zero importers. → **pri-13 `consolidate_the_graded_lexical_semantic_stores_into_one_learned_convergence_hub_with_task_readouts`** (cert 8/8): ONE learned convergence hub + task readouts, decided by a pre-registered HARD-PASS/HARD-FAIL vs each store and vs the landed fusion on the live coverage-quality instrument. Queue: 7 assignable. **HONEST LIVE-BF STATE:** 86 tagged organs = 7 BF (pinned equation) / 72 BF_SPIRIT (defensible computational-level, implementation unpinned) / 7 NOT_BF (parser cluster 5 + force_dynamics_valence + commonnoun_binder — all on the live read path, all with fleet solutions posted or in review) + WordNet-morphy at runtime (pri-12) → the live substrate is NOT yet 100% mathematically BF; the path to it is the posted queue.
  - **✅ POSTED pri-12 `the_lemmatizer_is_wordnet_morphy_at_inference_not_a_glass_box_morphological_decomposition`** (cert 8/8; the last external-tool rung on the meaning chain; ~10 organs call `wn.morphy` at read-time) + NARROWED pri-9 (its sense-assignment half now exists as the landed instrument; remaining = referent-link half, flat-on-count demo, power). Queue: 6 assignable.
  - **NEXT (in order):** (1) glass-box morphology to replace WordNet-morphy at runtime (the deepest meaning-chain residual; post as a fleet problem); (2) pri-1 generative world-model = the graded consumer of the settled vector (the forward half); (3) descend the `substrate_health` top-issues (7 NOT_BF: parser cluster 5 + force_dynamics_valence [pri-7 SOLVED awaiting owner] + commonnoun_binder).


### 2026-09-11 (OVERNIGHT — OWNER-DONE INTEGRATION WAVE, owner marked several solutions DONE)
  - **✅ pri-2 `scale_the_reading_learned_arc_scorer` INTEGRATED (owner-DONE, reverified 94/94).** The parser SCORER-acquisition deep lever. Core value folded like pri-3: **the capability WIN** (reading-learned scorer read as a graded distribution MATCHES supervised in-domain + BEATS OOD, GUM +0.0235 CI-sep, no treebank = the register-general SECOND-TRACK reframe) + **the numbered located negative** (text-only UAS caps 0.478; scale/EM/online all NOT the lever; residual = disambiguation signal text lacks) + the readout-lever oracle + the fully-BF-acquisition chain (78.9% of gold-POS; unlock = functional naming) + the NP-modifier construction + MBR decode land-ready. **NO LIVE FLIPS** (targets graded_parser/arc_parser, not the live arceager head path → byte-identical, mirrors pri-3). §2b + INTEGRATED marker + BF ledger done. **FILED pri-11** `wire_the_reading_learned_arc_scorer_as_a_register_general_second_track_and_measure` (the #1 next-step + queue refill).
  - **✅ coref-consolidation `consolidate_the_six_coreference_organs...` FULLY INTEGRATED (owner-DONE, reverified first-hand 12/12+2/2+3/3, board 0.6294 byte-identical).** 6 coref organs → ONE `hdlab/entity_resolver.py` (ACT-R retrieval core + Ariel routed cue-arms) + BF `hdlab/lexical_utils.py` split out of commonnoun_binder; 4 reader call sites delegate; `situation_predict` retired; dormant KB arm folded. Commits `859c02026`/`ff0643285`/`8fbc3d8f0`; marker + §2b + registry (84→86) + BF ledger done. Landed via a delegated fresh-context agent, strategy-verified. FOLLOW-ONS filed/open: the BF PRIZE (graded L&V `retrieve()`, separate behavior-changing problem) + clearing `entity_world_model_resolver`'s dormant experiments-import debt.
  - **🆕 HANDOFF PREPARED (owner migrating to a fresh Fable 5.1 session):** `project_state.md` / `project_graveyard.md` / `project_nuance.md` (repo root) + `SESSION_STARTUP_PROMPT.md` synthesize the full mental model for the new session. The meaning-channel pair below is staged as the new session's Active Vector.
  - **⏳ pri-5 `measure_end_to_end...meaning_fusion` (owner-DONE — Q129 answered) — LAND NEXT (serialized after coref; both edit situation_reader).** The meaning-fusion flip: fold W19/W20 to §2b + flip pri-2's 3 latent meaning pieces LIVE (directional SEQ channel / valence_polarity / intrinsic_gain) + land the hdlab wire spec (SOLVED §"hdlab proposal" L933/L1036) + persist the grown SEQ store + re-measure on the board. Response already measured POSITIVE first-hand (+0.16@250k→+0.22@500k CI-sep, twin losing). Behavior-CHANGING (no byte-identity gate) → careful measured pass.
  - **⏳ `wire_the_context_gated_sense_read_into_the_live_meaning_path_and_measure` (owner-DONE, the Q127 problem) — LAND with pri-5 (both are the MEANING PATH / `situation_reader`).** In-context/token-level sense read into the live meaning path + measure. **COORDINATE with pri-5** (the fusion-flip) as ONE meaning-channel landing to avoid conflicting situation_reader edits — reverify its witnesses first, read its SOLVED wire spec, land + measure together, board no-regress.
  - **⚠️ SERIALIZATION: all 3 remaining owner-DONE (coref-consolidation, pri-5, context-gated-sense) touch `situation_reader` → land ONE AT A TIME.** Order: (1) coref-consolidation (agent `a03d06addc363ce4f` running — verify its byte-identical commits first), (2) the meaning-channel pair (pri-5 + context-gated-sense, together, behavior-changing measured pass). Integration backlog is piling faster than one session's context can safely land — best processed across fresh continuations, one situation_reader landing each.
  - **QUEUE now 5 assignable** (pri-1 generative_entity_state, pri-8 reading_learned_pos, pri-9 grounding_coverage_quality, pri-10 wire_directed_causal_store, pri-11 wire_reading_learned_scorer_2nd_track). Board Q128 (brief-format) open; Q129 resolved.

### 2026-09-11 (OVERNIGHT — GROWN-KNOWLEDGE INTEGRATION, owner-directed "integrate the grown knowledge + see how the substrate responds + optimize")
  - **MEANING-FUSION grown knowledge = MEASURED-POSITIVE first-hand** (ran pri-5's `exp_meaning_fusion_grow_seq_live_v1.py --read-cap 500000`): growing the parser-free SEQ meaning channel by reading (through the loop's REAL taps) lifts live grounding-coverage quality **+0.16@250k → +0.22@500k lines, CI-sep, twin losing, monotone** (→ +0.26@1M per pri-5). So the grown meaning knowledge IS a real live lever. The PERMANENT landing is pri-5's Q111 strategy-wire (flip the channel + persist the store into the live sense decision) — a substantial wire on a 42KB PARTIAL. **Filed Q129 (go/no-go on landing pri-5's wire now vs review-first); NOT front-running the owner-DONE gate.** (Enabling the pri-2 `track_directional_context_counts` flag alone only GROWS the store; the live sense-decision wire is pri-5's.)
  - **DIRECTED CAUSAL STORE (stranded grown knowledge, +0.139 measured, no live consumer) → POSTED pri-10** `wire_the_mined_directed_causal_store_into_the_live_causal_reasoner_and_measure` (integrate into causal_reasoner._graph() + measure live transfer on the necessity instrument + optimize coverage; causal machinery is board-invisible). Queue now 4 assignable.
  - **KNOWLEDGE_ASSET_REGISTER re-audited** (disk-verified): LIVE + pushing perf = causal_sign (modest — beats falsifier, ties co-occurrence), C8 entity-type KB (DBpedia P31, live in _resolve_commonnouns → the 0.5818 dim), curated meaning_foundation (sm.select_sense + board WiC). LATENT = the meaning channel (gated on pri-5/Q129), who_is_who (world-model-gated), the causal store (now pri-10). **Board: Q128 (brief-format) + Q129 (land pri-5) OPEN.**

### 2026-09-10 (CONT-101, POST-COMPACTION) — ✅ PRE-STAGED THE READY WAVE: reverified the 2 clean submissions first-hand + board no-regress; HOLDING on Q124
  - **RECOVERED cleanly** (substrate_health + git + board all agree with the CONT-100 anchor — nothing lost in compaction). Health: 83 organs (BF=7, BF_SPIRIT=68, **NOT_BF=8**), all 4 gates OK, fold-in backlog 0, 4 in-flight, 2 assignable, 200 integrated lifetime.
  - **BOARD: self-test re-run this turn AGG 0.6294 (3/7 dims CI-sep) NO-REGRESS; authoritative FULL-POPULATION aggregate on disk = 0.6253 (n=11218, 5/7 dims CI-sep, `data/exp_situation_model_qa_modern_v1/metrics.json`, 0.9d old).** FIXED a real bug in `substrate_health._board_aggregate` (`a927a6dc3`): it read `agg.get("model")` but the board writes `model_acc`, so the derived health view was FALSELY printing "no metrics.json on disk" — it now shows the real 0.6253 + age. (My CONT-101 first-pass diagnosis "self-test doesn't persist" was WRONG; it was the tool key.)
  - **PRE-STAGED the 2 CLEAN fleet-SOLVED submissions (reverified FIRST-HAND, integration scope mapped — this READS, does NOT land; owner-DONE gate respected):**
    - **pri-7 `harm_help_valence...force_dynamic_arithmetic` (SOLVED):** witnesses `test_fd_harm_help_arithmetic` **9/9** + `test_pos_nominal_head_correction` **6/6** pass. Wolff force-structure × grounded-Warriner endstate valence + affectedness gate, retiring the verb-LIST + the read-path FrameNet enumeration. LIVE 0.778→**0.944** (paired +0.167 CI[+0.056,+0.278], 6 gains/0 losses), info-free twin loses, non-affect dims byte-identical. **CHAIN-FIX pulled in:** an upstream `nominal_head_correction` for `pos_tagger` (rare role-nouns medic/intern mistagged ADJ) — resolves part of the parser cluster's POS defect. Exact hdlab diff in SOLVED §"THE EXACT hdlab DIFF". → `force_dynamics_valence` NOT_BF→BF_SPIRIT on land.
    - **pri-4 `the_common_noun_binder...typed_coref` (SOLVED):** witness `test_cn_conceptkey_binding` **7/7** pass. One-line KEY swap: crude `head_lemma` regex → brain-faithful `concept_lemma` (WordNet morphy, non-alpha-safe) in the RESOLUTION consumer; 0.5254 honest-de-leaked floor → **0.5580** (+0.0326 CI[+0.0205,+0.0450] CI-sep), twin loses, additive/no-regress by construction (clustering consumer flat -0.0001). **CHAIN-FIXES pulled in:** a de-leaked-floor measurement-honesty fix (the board's 0.5412 `common_noun` floor embeds a +0.0158 redaction see-through leak) + a prototyped `boundary_nphead` raw-text span-head selector (recovers the -0.049 parser head-selection wall to -0.015). Exact diff in SOLVED §6. Marks `commonnoun_binder.head_lemma` NOT_BF, adds BF `concept_lemma`.
  - **Q124 recommendation (strengthened):** both reverify CLEAN first-hand and are additive/no-regress → **front-running them is SAFE**. Still HOLDING by default (the owner-DONE gate is the owner's vetting; I do NOT front-run WIP). If owner replies "front-run the SOLVED ones" or marks pri-4/pri-7 DONE, I land both in one careful pass (SERIALIZE — both edit `situation_reader.py`; pri-7 also edits `pos_tagger.py`) → NOT_BF 8→6, fire propagations, full board trend point.
  - **pri-1 DE-RISKED (CONT-115, `976dce908`):** ran a BF research drill on pri-1's central open question → `RESEARCH_bf_isa_acquisition.md` + folded the first-build step into PROBLEM.md §4b (brief still certs 8/8). KEY: the prototype's 0.26 is missing DIRECTED read is-a/genus EDGES (not volume); BF fix = extract copular/appositive/set-inclusion predication from the in-substrate parse → graded reliability-weighted accumulation → run the EXISTING ordered SVD on the is-a-edge matrix (Roller-Kiela-Nickel precedent BLESS 0.45→0.76). PROCESS LESSON: the research agent fanned out to 3 lit-scan children that couldn't route back to it → it stalled on synthesis twice; I stopped it and synthesized from the 2 recovered scans + domain lit. (Future: tell a dispatched research agent NOT to fan out to its own children, or plan to take over synthesis.)
  - **STATE:** everything committed path-limited, NOTHING pushed. Autoloop ARMED. pri-3 parser (5 NOT_BF) + pri-5 measure remain PARTIAL/in-flight (owner's move). Fleet has 2 assignable (pri-1 is-a [now de-risked], pri-2 reading-parser).

### 2026-09-10 (CONT-100 — COMPACTION PREP) — 🧭 OWNED THE PROJECT: robust process built + 4 owner-DONE integrated + grown knowledge LIVE + abduction landed
  - **FRONT DOOR (new, authoritative):** `python tools/substrate_health.py` = the SINGLE derived health view (BF status + NOT_BF resolution, integration backlog, in-flight, assignable, board age, 4 CONSISTENCY GATES, top-issues worst-first; `--gaps`/`--check`). `notes/SUBSTRATE_PROCESS_AND_ROADMAP.md` = the process cadence + governance + PRIORITIZED ROADMAP + DOC MAP (ends the ~15-plan-doc sprawl). STATUS/ledger/BF-registry/cross-solution-map/knowledge-register are the detail these two point to.
  - **HEALTH NOW (derived):** 83 organs (BF=7, BF_SPIRIT=68, **NOT_BF=8**). ALL 4 gates OK. owner-DONE fold-in backlog **0**. in-flight awaiting OWNER review **4**: pri-3 parser PARTIAL, pri-4 commonnoun_binder SOLVED, pri-5 measure PARTIAL, pri-7 force_dynamics SOLVED (the fleet solved my 3 BF-upchain posts + pri-5). assignable OPEN **2**: pri-1 learned-is-a channel, pri-2 reading-learned parser.
  - **INTEGRATED THIS SESSION (4 owner-DONE, reverified first-hand, committed):** name-bridge world-knowledge (safe_kb_gate BF + who_is_who_lexicon grown asset) · meaning-representation point-vector (3 BF pieces default-off/latent: directional ROUTE-B channel, valence_polarity_channel, convergent_cue_reader intrinsic gain) · **grow_the_causal_mechanism** (RUNG correction + **GROWN CAUSAL KNOWLEDGE INGESTED LIVE = `hdlab/causal_sign_channel.py` wired as `sm.causal_sign`** + rung-3 abduction primitive `causal_reasoner.is_necessary_abductive` landed CONT-97) · reward-cluster audit (faithful-but-dormant, NO live stand-in). Board self-test AGG **0.6294** no-regress throughout.
  - **GROWN-KNOWLEDGE (owner directive, LIVE-audited):** `notes/KNOWLEDGE_ASSET_REGISTER.md` (5 assets). causal_sign is the one wired LIVE; who_is_who_lexicon/Warriner VAD/directional-store/directed-causal-store are correctly LATENT (consumers not live yet — verified, not mislabeled).
  - **THE 8 NOT_BF (the last defects):** parser cluster (5) → pri-3 PARTIAL; commonnoun_binder → pri-4 SOLVED; force_dynamics_valence → pri-7 SOLVED; scene_segment = dead-in-default. **7/8 have fleet solutions in the OWNER REVIEW QUEUE.** Resolution = owner marks them DONE → I integrate. Board **Q124** asks: integrate the fleet-SOLVED ones NOW vs. HOLD (rec: hold = the owner-DONE gate is the owner's VETTING; I do NOT front-run).
  - **#1 GO-FORWARD (the unblock):** OWNER reviews the 4-submission wave (or answers Q124). Then integrate promptly (reverify→land+upstream fixes→ingest grown knowledge LIVE→ledger/§2b/propagation→commit path-limited, NOTHING pushed) → NOT_BF → ~1, + fire the queued cross-solution propagations (parser→~16 consumers) + a full board trend point.
  - **ROADMAP (prioritized, in SUBSTRATE_PROCESS_AND_ROADMAP.md §4):** NOW=close the NOT_BF wave (owner review). NEXT=pri-1 learned-is-a (dominant meaning lever, +0.488) + pri-2 reading-learned parser (extraction wall) — POSTED, fleet-assignable. MAIN EVENT=the generative world-model program (in-flight, ~9 solvers; the everyday-causal-sign tail is a downstream read over it, do NOT post separately). STANDING (mine): the causal machinery is BOARD-INVISIBLE (no smoke-viable causal gold) → the lever is a causal board INSTRUMENT or FLEET problems, NOT mine-side grinds (directed-store/Rhea/abduction-from-evidence).
  - **TRIANGULATED (3 derived tools agree):** substrate_health + substrate_map --gaps (top gaps=meaning+extraction=pri-1/pri-2) + wiring_debt (activation leverage routes through the north-star problems) → the roadmap targets the real levers; NO neglected mine-side work.
  - **STATE:** everything committed path-limited, NOTHING pushed. Autoloop ARMED (`auto_d9672062a6`, unlimited). FLEET is actively updating the pri-3 parser submission (uncommitted WIP in its folder — theirs, do NOT touch). hd_metrics_sync commits notes/ concurrently (races — my commits are separate + all in `git log`).

### 2026-09-10 (CONT-30, post-compaction — LATEST) — 🧠✅ **INTEGRATED BOTH owner-DONE solutions FULL-CHAIN + POSTED 3 BF-UPCHAIN problems (owner: '100% BF upchain first') + INCORPORATED GROWN KNOWLEDGE. Everything committed path-limited, NOTHING pushed. Autoloop ARMED (unlimited, `auto_d9672062a6`).**
  - **IN FLIGHT (CONT-71, background `research` agent):** a brain-foundational syntax-ACQUISITION drill for the pri-3 parser problem (how the brain acquires syntax WITHOUT a treebank; builds on the pri-2 directional grow-by-reading channel) -> writes `notes/problems/the_parser_is_a_frozen_supervised_hard_decode_not_the_brains_graded_probabilistic_parse/RESEARCH_bf_acquisition.md`. On completion: review + fold its recommended first build-step into the parser brief. NOTHING else running; owner-DONE gate EMPTY; frontier fleet-side.
  - **name-bridge `world_knowledge_common_noun_to_name_bridge_the_81_percent_residual`** (STRONG, reverified 11/11+9/9) INTEGRATED (`9717db167`): residual = document-local COVERAGE (oracle 0.924, selection brain-optimal); `safe_kb_gate` (BF safety theorem) landed; grown who-is-who lexicon → durable `hdlab/who_is_who_lexicon.py` (witness 8/8); the generative WIN handed to the world-model program (pri-1). 
  - **meaning-representation `the_meaning_representation_is_a_point_vector...`** (STRONG, reverified 57/57) INTEGRATED (`af3e16e97`/`576de44e6`/`ee7f81ab6`/`df39a76e3`): 3 all-BF pieces landed default-off/latent (recall byte-identical) — (1) directional ROUTE-B parser-free identity channel (removes NOT_BF pos_tagger/arceager, grows by reading), (2) `valence_polarity_channel` (un-blinds antonymy, FUSED 0.878 vs 0.636 floor CI-sep, no WordNet), (3) `convergent_cue_reader.intrinsic_gain_w` (retires fitted DEFAULT_W). LIVE fusion-flip gated on pri-5. 
  - **GROWN-KNOWLEDGE (owner directive)**: NEW `notes/KNOWLEDGE_ASSET_REGISTER.md` — who_is_who_lexicon (#1), safe_kb_gate (#2), Warriner VAD (#3), directional grow-by-reading store (#4). The cross-solution reverse-index (`CROSS_SOLUTION_IMPROVEMENT_MAP.md`) maintained. 
  - **3 BF-UPCHAIN PROBLEMS POSTED** (`c6d2e96e8`, idle fleet, cert 8/8): **pri-3 the parser** (5 of 8 NOT_BF organs; route through graded_parser Matrix-Tree marginals + acquisition question — the dominant upchain + the shared extraction wall), **pri-4 commonnoun_binder→typed_coref** (string-identity loses to floor, route through the landed typed content-addressable binding), **pri-7 force_dynamics_valence** (verb-list → Wolff force-dynamic arithmetic). 8th NOT_BF (scene_segment) = at-land prune. BF registry now **82 organs** (BF=7, BF_SPIRIT=67, NOT_BF=8). 
  - **#1 GO-FORWARD:** the parser (pri-3) is the highest-leverage assignable problem (upchain + real-prose extraction wall). Owner-DONE gate EMPTY (both folded in). Awaiting owner review: pri-5 measure_end_to_end (gates the meaning fusion-flip) + pri-6 reward-cluster audit.
### 2026-09-10 (CONT-29 CONTINUATION — COMPACTION PREP) — 🧠✅ **BF-CERTIFICATION COMPLETE + CROSS-SOLUTION REVERSE-INDEX + board_tom. #1 GO-FORWARD: INTEGRATE pri-2 (owner-DONE). Everything committed, NOTHING pushed. Autoloop ARMED (unlimited, `auto_d9672062a6`).**
  - **🧭 RECOVERY (read this + `git log` + `notes/CROSS_SOLUTION_IMPROVEMENT_MAP.md` + `notes/bf_status_registry.jsonl` + `BRAIN_FOUNDATIONAL_AUDIT.md` §2b [newest]; they OUTRANK recollection):**
    1. **🔴🔴 #1 GO-FORWARD — INTEGRATE the newly owner-DONE `the_meaning_representation_is_a_point_vector_not_a_probabilistic_population_code`** (pri-2, owner-DONE 20:49, PARTIAL, NOT yet processed). It is the MEANING-REPRESENTATION mega-cluster (~15 solutions converge here — Target 2 of the reverse-index). **Reverify FIRST-HAND `verification/test_ppc_meaning_representation.py` (57/57)**; read SOLVED.md "WHAT TO INTEGRATE" + `HDLAB_INTEGRATION_SPEC.md` + `COMPONENT_REGISTER.md` + `BF_AUDIT_UPDATE.md`. **Bar result:** item 1 PASS (intrinsic precision = accumulated-evidence GAIN, Ma/Pouget, tracks correctness CI-sep on SimLex+SimVerb), item 3 PASS (recall byte-identical), item 2 LOCATED NEGATIVE (precision-weighted fusion CANNOT beat equal-weight Bayes — the reweighting lever does not exist; thoroughly closed). **3 LAND-NOW BF pieces (all reuse, recall byte-identical, NO WordNet):** (1) PARSER-FREE learned channel (ConceptSpace ROUTE-B + directional typing + PPMI — **removes the NOT_BF pos_tagger/arceager at no accuracy cost**, grows by reading; a direct hit on the parser mega-cluster); (2) **AFFECTIVE-VALENCE dimension** (reuse affect_lexicon+Warriner — un-blinds meaning to ANTONYMY: syn-vs-antonym AUC 0.535→0.75, CI-sep, twin collapses; grounded_similarity omits this); (3) `convergent_cue_reader.w` (fitted OUR-INVENTION=NOT_BF) → **intrinsic per-query GAIN RATIO + population-consensus reliability→deferral** (CI-sep +0.049@N=30). DEEP FINDING: the loss is REPRESENTATIONAL DEPTH ("which words pattern alike" ≠ "what a word means"); the transfer-converse (buy/sell) polarity needs argument-role parsing + FHRR binding = the generative-world-model layer (→ pri-1). Integrate FULL-CHAIN per [[implement-all-upstream-chain-fixes-in-incoming-solutions]] (each LAND-NOW piece is all-BF + reuse → land + verify no-regress + §2b + reverse-index + marker + commit path-limited, NOTHING pushed). **This is a real board mover** (meaning channel + removes 2 live NOT_BF parser deps from the learned channel).
    2. **✅ THIS SESSION (CONT-29 CONTINUATION, the BF-cert + correlation arc):** (a) **BF-CERTIFICATION OF THE LIVE SET 100% COMPLETE** — every organ in `situation_reader.py`'s closure (51/51) carries a machine-checked `__bf_status__` + operation/math verdict + residual; `notes/bf_status_registry.jsonl` registry **25→79** (BF=7 BF_SPIRIT=64 NOT_BF=8, ZERO unverified); enforced by `verification/test_bf_status_tags.py` + the pre-commit gate. The 8 LIVE NOT_BF stand-ins each fleet-gated (parser cluster→pri-1; commonnoun_binder→pri-6; force_dynamics_valence/parse_confidence/scene_segment→named). (b) **ANTI-MISLABEL:** `predictive_world_model` relabelled counterfactual→rung-1 predictive-relevance (verified first-hand); `causal_reasoner` verified the genuine rung-2/3 do-surgery reasoner. (c) **C6 governor perceptron (trained-at-inference) REMOVED from the live affect path** (byte-identical, decision-dead verified, `e6b8dcd8c`). (d) **THE CROSS-SOLUTION IMPROVEMENT REVERSE-INDEX** `notes/CROSS_SOLUTION_IMPROVEMENT_MAP.md` (owner idea) — improve X → find every solution that consumes/flagged X; **4 mega-clusters: parser (~16), meaning representation (~15), coref/entity (~14), generative world-model (~9)**; wired into ledger+README with a maintenance rule + a DISK-VERIFY caveat (the "C8 asset unbuilt" claim was stale — it's built+live). (e) **`board_tom` arm built** (`07fc00461`) — the proven ToM capability is board-visible (FB-subset chain 0.669 vs belief-blind floor 0.237 = +0.4317 CI-sep, twin loses; board self-test no-regress AGG 0.6294). (f) EARLIER in the session (below): integrated C7 (STRONG) + name-bridge (STRONG, safe-KB gate landed, FIX A measured+refuted on the live path) + grounding-audit (STRONG); posted pri-2/pri-5/pri-6; 2 C4 repoints.
    3. **OWNER-DONE GATE after pri-2 = the name-bridge INTEGRATING (FIXES A–E measured-refuted for the live experiencer; generative route world-model-gated on pri-1) is the only other open thread.** REMAINING MINE-SIDE (all lower priority than pri-2): more Target-6 board arms (board_bridging next — bridging_inference measurement in `verification/test_bridging_inference.py`); the fidelity-sensitive careful units (grounded_similarity .SD [now SUBSUMED by pri-2's affective-valence + gain-ratio pieces], context_grounded_valence C4 port). **Note: pri-2's LAND-NOW pieces SUPERSEDE the grounded_similarity .SD follow-on** (they build the intrinsic-gain + antonymy dimensions directly).
    4. **📚🆕 OWNER DIRECTIVE (2026-09-10): INCORPORATE THE GROWN KNOWLEDGE DATABASES, not just the code.** Many incoming solutions GREW significant knowledge DBs that resolved their problem's issues (e.g. pri-1 causal-testimony 1.29M edges; ROCStories 454k verb-pair orders; DBpedia P31 types 548MB; curated meaning-foundation 117k sense-signatures; pri-2's parser-free learned channel that grows-by-reading SEQ 0.07→0.16). Q111 integration has been landing the ORGAN/mechanism but NOT systematically the grown KNOWLEDGE asset. **THE MECHANISM (build it): a KNOWLEDGE-ASSET REGISTER** (mirror of `bf_status_registry`) — one row per grown DB: `{asset_path, build/grow provenance (rebuildable script vs read-grown), coverage/size, the live consumer organ, LIVE vs LATENT (loader finds it + wired), the problem that grew it}`. **AT INTEGRATION, incorporate the grown knowledge:** (a) PERSIST the asset (data/ is gitignored → ensure the build script + raw source are tracked/rebuildable, OR the read-grown asset is checkpointed) so a fresh checkout can reconstruct it; (b) WIRE it into the live consumer (many are LATENT — landed but no live reader consumer, the "landed≠live" trap); (c) REGISTER it. **FIRST APPLICATION = pri-2 (the #1 go-forward): its parser-free learned channel IS a grow-by-reading knowledge asset — incorporate its grown store, not just the channel code.** Cross-links the reverse-index Target 5 (world-knowledge KB) + Target 10 (learner/grow-by-reading). This is the [[knowledge-lever-and-sequenced-learner-strategy-owner-confirmed]] made operational.
    5. **⚙️ OPS:** Autoloop ARMED (unlimited, scoped `auto_d9672062a6`; disarm `python tools/autoloop.py disarm`). All work committed path-limited on branch `dataprep/mcguffey-graded-corpus`, NOTHING pushed. Board self-test healthy (AGG 0.6294). Q122 still OPEN on the board (loop-vs-fresh; non-blocking).

### 2026-09-09 (CONT-29 CONTINUATION) -- 🧹 **DE-LEAK CROWN JEWEL COMPLETE + board-regression FIXED + BF-status convention IMPLEMENTED/ENFORCED + authoritative full-pop board CONFIRMED + audit-CATALOG integrated + idle-solver problems posted. SESSION CONT (owner: "new submission done + 1 idle solver"): INTEGRATED C7 attractor-solver (STRONG) + POSTED pri-2 population-code problem + 2 C4 repoints. HEAD `70a58cca6`, NOTHING pushed. Autoloop ARMED.**
  - **🔟 SESSION-CONT DELTAS (2026-09-09, this turn — read alongside points 1-8 below):** (a) ✅ INTEGRATED owner-DONE **C7 `replace_the_attractor_as_ranker…`** (reverified 23/23, STRONG, `1c008f25b`): a LOCATED NEGATIVE (the ranking is ALREADY a graded population read; attractor confined to the recognition gate, swap = −1.4e-5 CI incl 0) + a real finding (all-BF equal-weight composition beats the mixed chain +0.028 CI-sep — confirms the all-BF thesis; relational is-a identity = the ~214× knowledge lever). **C7 RE-SCORED** in §2b (attractor-as-ranker NOT active harm; iterative_attractor/cleanup_family=BF for CA3 recall, canonicalize_fast=BF). **6 organs BF-tagged → registry now 24** (BF=4 BF_SPIRIT=14 NOT_BF=4 BF_UNVERIFIED=2). **FOLLOW-ON (mine, NOT landed — honest bounds):** the fusion-wire into `canonicalize_fast` is gated on the END-TO-END grounding-COVERAGE measurement the solver flagged not-yet-done (win proven on the sense-assignment RANKING proxy, not the live loop) → measure-impact-first before flipping. (b) ✅ POSTED **pri-2 `the_meaning_representation_is_a_point_vector_not_a_probabilistic_population_code`** (cert 8/8, `70a58cca6`) for the idle solver — the C7 solver's named "largest architectural lever"; dedup'd vs the crowded learner/meaning space. (c) ✅ **C4 self-containment:** `temporal_reasoner` (`cbf58ace7`) + `temporal_script_schema` (`956a18042`) repointed off experiments/ to promoted hdlab twins, both PROVEN byte-identical, ZERO experiments imports. (d) OWNER-DONE GATE cleared C7; then a NEW owner-DONE arrived: **`world_knowledge_common_noun_to_name_bridge_the_81_percent_residual`** (STRONG, reverified 11/11+9/9 first-hand) — a LARGE PARTIAL, INTEGRATING carefully (NOT rushed at loop-cap). Its headline generative who-is-who route is **WORLD-MODEL-GATED (coordinate with pri-1 `grow_the_causal_mechanism`, still in a solver)**; the LAND-NOW BF pieces are the safe-KB gate (✅ LANDED `b8e552b12` → `hdlab/safe_kb_gate.py`, additive/self-contained, registry now 25) + abstention + FIXES A–E into the C8/crosstype_bridge SELECTION math (**REQUIRED board no-regress on crosstype_experiencer+namebridge** — the next careful unit). Stays on the gate as INTEGRATING until those verify. (e) **POSTED 2 idle-solver problems this session** (dedup-scout-verified, cert 8/8): **pri-2 `the_meaning_representation_is_a_point_vector_not_a_probabilistic_population_code`** + **pri-5 `measure_end_to_end_whether_the_meaning_fusion_lifts_live_grounding_coverage`**; the space is saturated (~150 briefs) so these are the genuine unfiled gaps — also the already-OPEN pri-4 grounding-audit is assignable. (f) AUTOLOOP hit cap 60/60 this session → will stop; owner re-arms via `python tools/autoloop.py arm --session auto_d9672062a6`.
  - **🧭 RECOVERY (read this + `git log` + `INTEGRATION_LEDGER.md` "CONT-29 CONTINUATION" + `BRAIN_FOUNDATIONAL_AUDIT.md` §2b [newest]; they OUTRANK recollection):**
    1. **AUDIT-CATALOG INTEGRATED** (`audit_the_live_substrate_for_non_brain_faithful_stand_ins_and_name_replacements`, owner-DONE, reverified 30/30 pure-disk; `c4ab52f60`): a MAP (NO hdlab writes) → its `CATALOG.md` is now the authoritative breadth reference (supersedes the CONT-28 §2b defect list); its #1 blast = the C1 gold-coref leak = the de-leak; 5 disk-corrections adopted (spaCy purged, force-verb retired, state/location WordNet fallbacks, C7/C8 dormant-wrt-reader, coref name-Jaccard misattributed); pri-2 dropped.
    2. **POSTED 2 idle-solver problems** (`de47d4fb3`, cert 8/8) from the audit's grounding-subsystem follow-on (the 2nd live entry point it excluded): **pri-4** `audit_the_grounding_acquisition_subsystem_for_non_brain_faithful_stand_ins` (breadth catalog of `reading_grounding_loop`/`substrate`/`gap_driven_reader`) + **pri-5** `replace_the_attractor_as_ranker_with_a_graded_population_read_in_the_grounding_loop` (C7, disk-located, parallelizable). **DEDUP (Explore agent, first-hand):** the generative-world-model/Wall-2, bridging, C3-parser, and `world_knowledge_81%` follow-ons are ALL already filed/solved/exhausted → these 2 grounding-subsystem problems are the genuine unfiled frontier. **pri-1 `grow_the_causal_mechanism…` remains the open main-event successor** (the completing lever of the generative-world-model line).
    3. **✅ DE-LEAK COMPLETE (`replace_the_entity_gate_gold_coref_inheritance…`, owner-DONE) — THE CROWN JEWEL LANDED + FLIPPED DEFAULT-ON.** PART 1 `hdlab/online_entity_cluster.py` (online ACT-R cue-clustering, `6d1330da3`) + PART 2 `hdlab/crosstype_live_adapter.py` (NEW, self-contained, gold-free crosstype definite→name bridge adapter: Doc from the reader's OWN live parse, mention `.eid` = the ONLINE label not gold, `crosstype_bridge_links(conf_thr=-3.0)`, merge into online labels) WIRED into `read()` (sub-flag `online_entity_cluster_bridge`) + `online_entity_cluster` FLIPPED DEFAULT-ON. VERIFIED `verification/test_deleak_crosstype_live_adapter.py` PASS (self-contained; gold-free Doc; LIVE read() full-de-leak-ON no crash + entities built; pronoun/coref BYTE-IDENTICAL floor/part1/full; +0.0838 CI-sep n=549 reproduced from disk). BOARD NO-REGRESS (smoke ON: core agg 0.6294 vs floor 0.5882, coref FLAT 0.504 vs 0.506, no crash; full `--run` heavy arms crash-free, full-population aggregate table = a follow-on due to a `--run` harness capture quirk). REALIZES the +0.0838 CI-sep experiencer gain the peek was faking. Keystone `test_coref_which_entity` all-zero failure is PRE-EXISTING (byte-identical coref flag-on/off), not this flip. INTEGRATED marker + pri6 dropped. FOLLOW-ON: C4 self-containment (`context_grounded_valence`+`temporal_reasoner` off experiments/); point the `board_crosstype_experiencer_dimension` arm at the live flipped path for true "live==scored".
    4. **✅ DE-LEAK PART 2 COMPLETE + FLIPPED ON (`54c07df20`):** `hdlab/crosstype_live_adapter.py` (gold-free) wired into `read()`, `online_entity_cluster` DEFAULT-ON; verified (witness PASS; pronoun byte-identical; no crash); +0.0838 experiencer realized. BLAST-RADIUS ENUMERATED: the flip breaks ONLY the legacy-19c LitBank eval join (sm-based `_named_clusters`); the modern board + live inference use the mention-based `_named_clusters` → UNAFFECTED (corrected my earlier "pre-existing" mis-call; recorded in ledger).
    5. **✅ BOARD-REGRESSION FIXED (`146e42688`, owner-surfaced):** `experiments/_hashseed_guard.py` `os.execv` re-exec-on-import (added by 2026-09-07 board arms) killed the full `--run` mid-run; neutralized by pinning `PYTHONHASHSEED=0` at the board top. Instrument HEALTHY: full uncapped board runs end-to-end (~40min, 21 arms) via run_in_background post-fix.
    6. **✅ AUTHORITATIVE FULL-POPULATION BOARD CONFIRMED + REPRODUCIBLE (de-leak ON):** AGG **0.6253**, **5/7 core dims CI-sep**, 21 arms, de-leak `crosstype_experiencer` n=303 CI-sep; my 17:55 run == the 16:05 run. Canonical `metrics.json` refreshed.
    7. **✅ BF-STATUS TAGGING CONVENTION IMPLEMENTED + ENFORCED + RECORDED (owner "do it"; `08e70cf2c`+`4be45f72d`+`2c41fbe44`):** canonical `notes/bf_status_registry.jsonl` (18 organs) + `__bf_status__` tags + registry-driven AST checker `verification/test_bf_status_tags.py` + a THIRD pre-commit guard (installer, gate LIVE-VALIDATED). Taxonomy BF|BF_SPIRIT|NOT_BF|BF_UNPINNED|BF_UNVERIFIED. Counts BF=2 BF_SPIRIT=11 NOT_BF=3 BF_UNVERIFIED=2. RULE (in `BRAIN_FOUNDATIONAL_AUDIT.md` §2b): a status-raising fix bumps tag+registry+`__bf_corrections__` in the same commit; new/audited organs get a row+tag (never guess). ROLLOUT ONGOING: remaining ~162 organs tagged as audited; the 2 BF_UNVERIFIED (`causal_reasoner`,`quality_relation`) are the WIP-pri1 solve's AUDIT-NEXT.
    8. **🔴 HOLD + #1 GO-FORWARD:** all 3 posted problems (pri-1 causal-knowledge, pri-4 grounding-audit, pri-5 C7-attractor-fix) now have SOLVED.md but are WIP (owner_verdict DONE count = 0) → integrate each ONLY on owner-DONE (do NOT front-run). Everything committed path-limited, NOTHING pushed.

### 2026-09-09 (CONT-29) -- 🔗 **INTEGRATED TWO owner-DONE PARTIALs FULL-CHAIN (obl-spatial pri5 + causal world-model pri4) with per-change RECONCILIATION against current-live. HEAD `1d3d3bbf2`, NOTHING pushed. Autoloop ARMED.**
  - **🧭 RECOVERY (read this + `git log` + `INTEGRATION_LEDGER.md` CONT-29 + `BRAIN_FOUNDATIONAL_AUDIT.md` §2b CONT-29; they OUTRANK recollection):**
    1. **OWNER DIRECTIVE (2026-09-09):** both incoming solutions had the whole upstream chain vetted brain-foundational → implement the upstream changes, ACCEPT the blast radius, upgrade downstream. CAUTION: the solvers built in `experiments/` against upstream snapshots that MAY NOT be the current live hdlab, and each optimized only its OWN downchain → for every change: (a) reconcile current-live-vs-solver-assumed, (b) enumerate ALL live consumers, upgrade each. The caution proved decisive on obl-spatial (below).
    2. **obl-spatial (pri5, owner-DONE, PARTIAL, `25b5cc8b5`):** OPT-A's headline +0.0144 was vs a STALE greedy decode; the live front-end already uses the P4 exact-MAP. MEASURED the true live headroom: the marginal-argmax COMMIT still beats the live exact-MAP +0.0102-0.0133 CI-sep (disagree on 1.9% of obl edges) → LANDED OPT-A `commit_spatial_ground_heads` + OPT-C one-marginal-reuse in `hdlab/joint_relation_frontend.py`+`situation_reader._read_spatial_reasoning` (default-on `spatial_obl_commit`, TEMPORAL BYTE-IDENTICAL). OPT-B moot (`parse_confidence.obl_confidence` DORMANT). Witness `verification/test_obl_spatial_commit_landed.py` 10/10.
    3. **causal world-model (pri4, THE NORTH-STAR, owner-DONE, PARTIAL, `1d3d3bbf2`):** NEW self-contained organ `hdlab/predictive_world_model.py` — the ONLINE predictive-coding FORWARD event-transition world-model (Rao-Ballard + Rescorla-Wagner, glass-box `pos_tagger`+WN, NO LLM/gold/training) + counterfactual-NECESSITY causal reader + persisted foundation asset (simplewiki, gitignored/rebuildable via `python -m hdlab.predictive_world_model --build`). Self-test 3/3; reproduces the solver EXACTLY (PC beats bigram +0.2543 CI-sep; necessity reader-vs-random +0.3084 CI-sep). WIRED additive `sm.causal_antecedent`/`sm.predictive_necessity`/`sm.predictive_world_model` (default-on `track_predictive_causal`, BYTE-IDENTICAL off); witness `verification/test_predictive_causal_wire.py` 7/7. RECONCILED genuinely NEW (the FORWARD level — predictive_reader=feature/static, n400=backward, sm.causal_reasoner=graph-necessity) → additive, NO downchain regression.
    4. **Both board-INVISIBLE by design** (obl: construction-coverage-dominated 35% ≫ attachment 10%, SpaceEval saturated; causal: external golds are POSITION-ARTIFACT TRAPS → value is INTRINSIC surprisal necessity). Real brain-foundational upstream/foundation gains, not headline movers.
    5. **GO-FORWARD (documented follow-ons, NOT front-run):** (a) ✅ DONE (`71f6e3c29`): the intrinsic-necessity board arm `board_predictive_causal_necessity_dimension` (HELD-OUT simplewiki n=6729: reader necessity 0.3602 vs random 0.038 +0.3223 CI-sep, vs nearest-position 0.1124 +0.2478 CI-sep — escapes position); (b) the RICH situation-event key (finding 23: verb+agent+patient+dense-valence+goal); (c) event_type MFS→WSD; (d) `AtLocationClassFit` THEM organ on who-did-what (obl follow-on, measure first); (e) **THE loop-closure — N400 error against this forward model = the pri-1 generative world-model (owner-review-pending, the substrate's #1 north-star).** FLEET UNPICKED (flag for routing): de-leak pri6 (#1 blast, masks coref/name-bridge/experiencer gains), audit-solver pri2. DEFERRED MINE: attractor-as-ranker gate-FAIL, self-containment chain.

### 2026-09-08 (CONT-28) -- 🧠💯 **THE 100%-BRAIN-FOUNDATIONAL SPINE: full 5-tier audit + ~7 gate-fixes + FULL hdlab spaCy purge + self-containment promotion #1. HEAD `9a59894a0`, NOTHING pushed.**
  - **🧭 RECOVERY (read this + `git log` + `BRAIN_FOUNDATIONAL_AUDIT.md` §2b [CONT-28, newest] + ledger CONT-28; they OUTRANK recollection):**
    1. **OWNER GATE (load-bearing):** every component must be 100% brain-foundational; spaCy/GLUCOSE/MAVEN + any off-the-shelf parser/dataset/model = NOT brain-foundational; NEVER reach for a convenient/easy tool without careful consideration. Baked into `notes/problems/README.md` standing-rules + memory [[every-component-must-be-100-percent-brain-foundational]]. The brain-foundational AUDIT is the SPINE; board-movement is downstream of the gate.
    2. **hdlab is SPACY-FREE** (`grep -rn "import spacy|spacy.load|nlp(" hdlab/*.py` returns NOTHING). The PARSER = the substrate's own PosTagger/ArcParser/arceager/arc_labeler (NO spaCy at inference); causation_typing + perceptual_access_ledger were re-expressed over the in-substrate parse.
    3. **LANDED (each verified no-regress + self-contained, NOTHING pushed):** state_register→WordNet `fee9337a1`; context_grounded_valence→force-dynamics `406fa9dd7`; PAL spaCy→in-substrate `519ddd3e1`; location_register→WordNet `c1c449bc1`; board arms `f8d061b53`; FULL spaCy purge `8fb805163`; temporal_ordering promotion `9a59894a0`. **None moves the HEADLINE aggregate (0.6253, unchanged) — they raise BRAIN-FOUNDATIONALITY; 2 are board-visible wins on new constructed/self-authored arms, 1 parity. The headline movers are FLEET.**
    4. **GO-FORWARD:** (a) self-containment promotion chain (11-cell debt map from the `promote-selfcontain` result): promotions #1-#3 DONE (temporal_ordering/causal_network/temporal_ordering_multiframe + aspect_interval; `situation_reader` now has ZERO module-level experiments imports). NEXT = the HIGH cells (LAZY in-function imports: `_belief_reader`/`_space_reader`/`_temporal_order_register`/`_forward_prediction_live` + the bridge1 web `_ea`/`_gov`/`_sim`/`_v2`, which trains a perceptron at inference); (b) the dedicated brain-faithful-audit SOLVER (pri 2, `045e29fb3`) catalogs the remaining stand-ins; (c) FLEET headline movers = de-leak pri6 (UNPICKED, #1 blast) + world-model pri1 — flag for routing, do NOT front-run. Continue gate-fixes ONE unit at a time, verify no-regress, NOTHING pushed.

### 2026-09-08 (CONT-27) -- 🧩 **REALIZATION+INTEGRATION session complete; GO-FORWARD PLAN recorded; git-incident recovered. HEAD `e776cd522` (~11 strategy commits), NOTHING pushed by me; board + brief cert green.**
  - **🧭 COMPACTION-RECOVERY #1 (read this + the ledger "GO-FORWARD PLAN" block + `git log` + `python tools/wiring_debt.py`; they OUTRANK recollection):**
    1. **NO recent owner-DONE integration pending.** The recent owner-DONE (typed_coref, causal-store, crosstype [EXCELLENT PARTIAL], extract_spatial CLOSED) are INTEGRATED (INTEGRATED_BY_STRATEGY markers appended; `problem_ledger`'s "31 awaiting" = HISTORICAL missing-marker bookkeeping, NOT lost integrations — verify-not-panic). Still CHECK the gate (a RECENT OWNER_NOTES `owner_verdict: DONE` with no SOLVER REVIEW block) + integrate any NEW promptly FULL-CHAIN per [[implement-all-upstream-chain-fixes-in-incoming-solutions]].
    2. **THE SUBSTANTIVE BACKLOG (ledger GO-FORWARD PLAN — do FRESH, one unit at a time, gate every flip on net-positive + no-regress):** (A) **DORMANT-ORGAN FLIP-TREATMENT BURN-DOWN** — ~137 island-only capabilities; live reader imports ~52/182 hdlab modules; MANY latent-by-design (flip-tested net-neutral/negative — do NOT re-flip); per organ measure LIVE board impact → turn on IFF net-positive. (B) **LEDGER-MARKER + CASUALTY AUDIT** (31 historical missing markers). (C) **P1 one-parse read() restructure** (deferred byte-identical optimization).
    3. **SOLVER-SIDE (integrate PROMPTLY on owner-DONE):** the de-leak `replace_the_entity_gate_gold_coref_inheritance_with_online_cue_based_clustering` (pri 6) is the SHARED unblocker — turns crosstype (+0.0528) + typed_coref honest-floor wins into LIVE board gains. AWAITING owner review (SOLVED, NOT owner-DONE → NOT mine): world-model (pri 1), chain_multi_step (pri 8), generate_dont_retrieve_causal_edges (pri 4).
    4. **⚠️ PROCESS (USER 09-08, EMPHATIC):** never delete a `notes/problems` folder with a SOLVED.md/analysis (it's ACTIVE — a solver may have adopted it — NOT a stray); commit strategy PROBLEM.md's to git IMMEDIATELY. A concurrent git op + a mistaken stray-delete lost 3 untracked briefs this session (all restored + now tracked). [[be-careful-with-projects-in-process]]
  - **✅ THIS SESSION (CONT-27): LANDED** P2 spatial semantic-typing live+scored (`891d13861`,`4c8413cab`); typed_coref common-noun RESOLUTION consumer + C8 encyclopedic route (`0349d0dd1`,`27b3dc64e`; `sm.commonnoun_resolution` 0.5482); broader causal-order store LIVE in `temporal_reasoner` (`ec3d26ace`); 4 board arms. **INTEGRATED + FULL-INVENTORY tracked** the 4 owner-DONE (ledger CONT-26 + audit §2b). **LOCATED NEGATIVE:** location_register spatial enrichment (closed). **RECOVERED** the git incident (3 briefs restored+tracked). Board self-test PASS; brief cert 8/8; NOTHING pushed by me.

### 2026-09-08 (CONT-25) -- 🌍 **extract_spatial_and_causal owner-DONE: P4 LANDED+VERIFIED + P31 now fully INTEGRATED + fresh board baseline persisted + BOTH session arms validated full-board. AUTOLOOP DISARMED at the careful-work limit. Committed path-limited (HEAD `d0f928136`), NOTHING pushed.**
  - **🧭 COMPACTION-RECOVERY #1 (read this + ledger CONT-13→25 + `git log` + the persisted board baseline; the LEDGER OUTRANKS recollection):**
    1. **CHECK THE OWNER-DONE GATE FIRST** — scan `notes/problems/*/OWNER_NOTES.md` for `owner_verdict: DONE` with `PROBLEM.md status != INTEGRATED` (reliable detector = a RECENT OWNER_NOTES mtime; the owner submits in bursts). Integrate any PROMPTLY **FULL-CHAIN** per [[implement-all-upstream-chain-fixes-in-incoming-solutions]] (read SOLVED in full → reverify first-hand → land ALL upstream fixes, accept+fix breakage → §2b+ledger+status:INTEGRATED → commit path-limited, SERIALIZE shared-file landings on situation_reader.py/typed_spokes.py/joint_relation_frontend.py/the board file, NOTHING pushed).
    2. **THEN land the spatial+causal REMAINING CHAIN (ledger CONT-25):** P2 (port `experiments/_joint_spatial_frontend.spatial_edges_ext(use_thematic)` + the mined construction inventory → `hdlab/joint_relation_frontend`; ~200-line port, measure on the board `spatial_relational` arm — baseline **0.2835** — NOT the reader's separate LocationRegister dim) / P3 (nominal-event flip for the CAUSAL channel — scope carefully vs the already-ON `joint_nominal_events`) / P5 (coref→causal generative typer, `exp_joint_causal_survival_v1.gen_causal_type`; first FIX the degraded `causal_multihop` board dim). P1 (one-parse read() restructure) = deeper. Each verified vs the persisted baseline (2026-09-08 02:32) before commit.
    3. **THE PERSISTED BASELINE (2026-09-08 02:32, canonical `data/exp_situation_model_qa_modern_v1/metrics.json`):** aggregate_19c_free 0.6253; per-dim reference — coref 0.4681 / common_noun_coref 0.5671 / who_did_what_patient 0.8311 / state 0.8333 / wic 0.6639 / spatial_relational 0.2835. New arms all present incl. this session's **crosstype_experiencer 0.2168 vs 0.1311 (CI-sep floor+twin, n=549)** + **namebridge 0.5815 vs 0.4719 (CI-sep floor+twin, n=356)** — both VALIDATED full-board.
  - **✅ THIS SESSION (CONT-25): `extract_spatial_and_causal_relations...` owner-DONE reverified FIRST-HAND 11/11** (SPATIAL = brain-foundational WIN: semantic Figure-Ground typer beats incumbent +0.0533 CI-sep on TYPE-PRECISION + a density floor +0.3864; recall-survival is DENSITY-CONFOUNDED → score on precision. CAUSAL = blessed LOCATED NEGATIVE: unmarked 65.6% → generative simulator wins binding precision +0.339 but coverage-capped ~5%, coref lifts 3.29×. KEYSTONE: the meaning/entity channel is built but never CONSUMED in read()). **P4 LANDED+VERIFIED** (`ff8008131`: `hdlab.joint_relation_frontend.parse_sentence` → exact-MAP decode; byte-identical temporal 0/230 event diffs first-hand; reader's only parse_sentence consumer is joint_event_ranks). Board arms capped (`64daac408`). Baseline persisted (`d0f928136`). **P31 `acquire_wikidata_p31` now fully INTEGRATED** (C8 spoke `664139141` + board_namebridge `0e1fa55ba` + compact swap `47ab3aec4` + C6→bridging live flip `a057a63bc` + status `7651a1516`; live-wire = tracked follow-on).
  - **⚙️ OPS STATE:** AUTOLOOP **DISARMED** (I armed it this session, then disarmed at the careful-work limit after 6 continuations — the remaining P2/P3/P5 are substantial deep ports best done fresh, thoroughly scoped in ledger CONT-25, NOT botched fatigued). **Re-arm: `python tools/autoloop.py arm --session auto_d9672062a6`.** **Q122 OPEN on the board** (continue-on-loop vs resume-fresh — owner to decide). Branch `dataprep/mcguffey-graded-corpus`, HEAD `d0f928136`, NOTHING pushed all session.

## TOP ITEM -- **THE THREE REVIEWS RE-AIMED THE WORK. TWO ORGANS, TWO OPPOSITE FAULTS.**

### 🥇 **08-23 (CURRENT): WHAT THE REVIEWS CHANGED, AND WHAT IS NOW THE BUILD**
🔻 **THE SIXTEEN-LOSSES PREMISE BELOW IS REFUTED -- read the correction before the block.** P2 built
the STRONG version of learning-from-reading and it clears the strongest floor CI-separated on all
three banks, beating the spelling floor `15-40x`, **with the curve STILL CLIMBING at `38.09M`
tokens.** *The losses tested a WEAK implementation. The route is CORPUS-LIMITED, not exhausted.*
➡️ **NEXT MECHANISM, AND IT IS BRAIN-PINNED: RELIABILITY-WEIGHTED CUE COMBINATION.** P1 showed our
fixed-weight mix lets a strong frequency prior SWAMP a weaker-but-correct grounded channel -- the
channel alone beats chance on subordinate senses (`0.4811` vs `0.3854`), the mix lands BELOW chance
(`0.1415`). **The brain does not combine cues at fixed weights; it weights each by its reliability
(Ernst & Banks 2002 is the canonical measurement). Our rule cannot express that, so it can only ever
reproduce whichever cue is louder.** 🚫 **BUT NOT ON THE BUNDLING ORGAN:** P3 says the fix THERE is
MORE INDEPENDENT SOURCE DIMENSIONS (an 11-dim signal cannot fill a 256-dim code), and a count table
with no rank ceiling is the arm that wins. **TWO ORGANS, OPPOSITE FAULTS -- DO NOT MERGE THEM.**
🚨 **AND THE BAR ITSELF IS IN QUESTION: Q117 (spelling floor `~78%` morphological leakage).**

🔴 **A DIRECTION I ALMOST SET TONIGHT AND THEN REFUTED WITH OUR OWN DATA -- READ THIS BEFORE
PROPOSING "READ MORE".** Two measurements looked like they converged on *reading volume is the
binding constraint*: P2 is still climbing at `38.09M` tokens, and the substrate's vocabulary is
still climbing at `5,200` sentences (`2,270` lemmas at `800` -> `7,334` at `5,200`). **Both are
true and the conclusion does NOT follow. Vocabulary size is an INTERNAL STATISTIC; the standing
rule is that a statistic may DIAGNOSE and never DECIDE.** The OUTCOME is measured in
`data/exp_substrate_resume_helps_v1`, and it is at the floor **in `12` of `12` arms across `3`
seeds**: SUBSTRATE grounding precision `0.0199` (**`3` hits / `151`**) against a `RANDOM_ANCHOR`
floor, paired permutation **`p = 0.2634`** -- *not separated.* **THE SUBSTRATE GROUNDS `168`
MEANINGS AND THREE ARE RIGHT.**
🔴 **AND THEN I CAUGHT MYSELF RUNNING PAST A PRE-REGISTRATION, WITHIN THE HOUR.** The cell that
DEFINES this measurement (`exp_grounding_precision_gold_v1`) pre-committed, before any number
existed: ***"(iv) fewer than ~300 scorable items -> UNDERPOWERED ... do NOT issue a verdict."***
**`0` of `12` arms reach it; max n = `151`.** ➡️ **SO "GROUNDING IS AT CHANCE" IS NOT AN AVAILABLE
CONCLUSION -- NOT MINE, AND NOT THE SUBMISSION'S "sits at the RANDOM_ANCHOR floor in every arm".**
*A pre-registration I did not write is not one I may quietly outgrow.*
🔴🔴 **AND THEN A SECOND CORRECTION THAT RETIRES THE FIRST: THE POWERED MEASUREMENT ALREADY EXISTS
AND I HAD NOT OPENED IT.** *I quoted `exp_grounding_precision_gold_v1`'s power RULE without reading
its RESULTS.* It ran **3 seeds at ~`40,000`-sentence reads**, `UNDERPOWERED: False`, `n_scorable`
**`441`/`441`/`398`**. **THE ANSWER HAS BEEN ON DISK ALL ALONG:**

| seed | n | SUBSTRATE | RANDOM (paired p) | 🔻 **TOP_COOCCURRENT** |
|---|---|---|---|---|
| `101` | `441` | `0.0272` | `0.0045` (**`0.0110`**) | **`0.0590`** |
| `20260819` | `441` | `0.0159` | `0.0023` (`0.0695`) | **`0.0476`** |
| `7` | `398` | `0.0302` | `0.0025` (**`0.0050`**) | **`0.0653`** |

## ✅✅ **08-24: THE FIRST GENUINE BRAIN-FAITHFUL WIN OF THE STRETCH -- COMBINE, DON'T SUBSTITUTE, CONFIRMED ON FAIR GOLD**
**`exp_c3_grounded_fusion_v1` (`run_mode: full`, n=`4,000` over `5,491` anchors, 5,000x paired
bootstrap). OWNER MARKED BOTH REMAINING PROBLEMS `DONE` 08-23 23:55/23:58.**
🔑 **READ THE MORPHOLOGY-STRIPPED (FAIR) GOLD, NOT THE LEAKY ONE -- THE RANKING INVERTS:**

| arm, FAIR gold | hit@1 | |
|---|---|---|
| ✅ **`FUSE_BASE_GROUNDED` -- distributional **+** grounded spoke, NO spelling** | **`0.0790`** `[0.0707,0.0875]` | **BEST ARM** |
| `GROUNDED` alone | `0.0607` `[0.0534,0.0682]` | *fusion beats it, CI-separated* |
| `A1_BASE` (flat bag alone) | `0.0459` `[0.0396,0.0524]` | *fusion beats it, CI-separated* |
| 🔻 `FUSE_BASE_GROUNDED_STRING` (**+ spelling**) | 🔻 `0.0431` | **WORSE THAN THE BAG ALONE** |
| `A5_STRINGCTRL` (honest spelling floor) | `0.0193` | *fusion beats it ~`4x`* |

➡️ **THE HUB-AND-SPOKE FUSION BEATS BOTH OF ITS OWN COMPONENTS CI-SEPARATED, AND BEATS THE HONEST
FLOOR BY ~`4x`.** *Control binds: `FUSE_RANDOM_GROUNDED` `0.0291` -- it is not "any second channel".*
⚠️ **AND THE `0.1125` "clears the floor's upper bound" HEADLINE IS THE **WEAKER** CLAIM, NOT THE
STRONGER ONE.** It uses the SPELLING channel scored on LEAKY gold -- morphology on both sides. **On
fair gold that same arm COLLAPSES to `0.0431`, below the bag alone: once the leakage is stripped the
spelling channel is ACTIVELY HARMFUL.** *The cell's own verdict is the conservative
`COMBINE_BEATS_EITHER_BUT_NOT_THE_FLOOR`, and the brain-faithful arm indeed does not clear the LEAKY
floor (`-0.0033`, CI includes 0). Quote the FAIR-gold row.*
🧠 **AND IT FITS THE NIGHT'S ONE FINDING RATHER THAN CONTRADICTING IT: fusion WORKS when the two
channels are COMPARABLE (distributional + grounded); it FAILS when one is a DOMINATING PRIOR (the
frequency prior swamps the same grounded channel, `0.4811` -> `0.1415`).** *Same missing organ: a
control that weights a source by how much it should be trusted HERE.*
🎯 **THIS IS `reader_meaning_channel`'s MISSING ADAPTER, MEASURED: z-score-fuse the distributional
cosine with the grounded sensorimotor cosine at read-out.** 🚫 **Do NOT add the spelling channel, and
score future c3-style gates against MORPHOLOGY-STRIPPED gold.**

🛑🛑 **AND A PRE-REGISTERED `STOP_IF` HAS ALREADY FIRED ON THIS WHOLE ROUTE -- `exp_corpus_capacity_
ppmi_svd_ceiling_v1` (08-18). READ THIS BEFORE PROPOSING ANY WRITE-RULE FIX.**
**`CORPUS_CAPACITY_CEILING__STOP_IF_iii_INFO_PRESENT_NO_UNSUPERVISED_FIRST_ORDER_TRANSFORM_REACHES_IT`**
*Every arm below is scored on the SAME dissociation instrument and the SAME 242-pair population:*

| arm | AUC | |
|---|---|---|
| **`C1_FITTED_ORACLE` (HELD-OUT CV, not in-sample)** | **`0.9606`** | ✅ **THE INFORMATION IS IN THE CORPUS** |
| `K1_KNOWN_ANSWER_WORDNET` | `0.9599` | *instrument works* |
| `N0_RANDOM_VECTOR_STORE` | `0.4862` | *chance* |
| **`A0_INCUMBENT` -- OUR write rule** | **`0.0710`** | `BELOW_0.5_COOCCURRENCE` |
| `B3_SECOND_ORDER_COSINE` | `0.0510` | below |
| `B2_PPMI_SVD` k=`50`/`100`/`300`/`500` | `0.0519`/`0.0285`/`0.0230`/`0.0278` | below |
| `B1_PPMI` | `0.0249` | below |

🔑 **THE INFORMATION IS PRESENT (a FITTED model reaches `0.9606` HELD-OUT) AND NO UNSUPERVISED
FIRST-ORDER TRANSFORM REACHES IT.** *PPMI, PPMI+SVD at four dimensionalities, second-order cosine,
and our own write rule ALL land in `0.02`-`0.07`, deep in the co-occurrence band.*
🔴 **THIS CORRECTS MY OWN FRAMING FROM EARLIER TONIGHT.** I wrote *"a plain distributional model over
this corpus extracts it CI-separated while ours does not."* **ON THIS INSTRUMENT NEITHER DOES -- and
OURS (`0.0710`) IS AHEAD OF `PPMI_SVD` (`0.0285`).** *P2's distributional win was on word-pair
SIMILARITY RATINGS, a different task and scorer, exactly the comparison I had already flagged as
non-transferable. Here is the direct evidence that it does not transfer.*
➡️ **SO THE PROBLEM IS NOT "OUR MECHANISM IS UNIQUELY BAD". IT IS THAT FIRST-ORDER CO-OCCURRENCE
TRANSFORMS AS A CLASS CANNOT PRODUCE SUBSTITUTABILITY FROM THIS CORPUS, WHILE THE INFORMATION IS
DEMONSTRABLY THERE.** 🚫 **DO NOT propose another unsupervised first-order transform of the
co-occurrence matrix -- that is the class the STOP_IF closed.**

🛑 **A SECOND `STOP_IF` CLOSED THE "TUNE IT HARDER" ESCAPE (`exp_tuned_count_unsupervised_
dissociation_v1`):** `TUNING_IMPROVES_ON_VANILLA_BUT_STAYS_BELOW_0.5__SUPERVISION_CONCLUSION_
SURVIVES_A_FAIRER_TEST`. *Shifted PPMI lifts `0.0519` -> `0.1144` on held-out selection and is still
nowhere near chance.* **Four tuning families tried; the ordering does not change.**

🧠 **AND THE GROUNDED CHANNEL WAS ALREADY SCORED ON THIS EXACT INSTRUMENT TOO
(`exp_sensorimotor_channel_discrimination_v1`) -- IT IS THE ONLY THING ON THE RIGHT SIDE OF CHANCE,
AND IT STILL DOES NOT CLEAR ITS OWN FLOOR:**

| arm | AUC | |
|---|---|---|
| 🔻 **`F_CONSTANT_PROTOTYPE__SM11` -- ONE VECTOR FOR EVERY WORD, ZERO word-specific information** | 🔻 **`0.6195`** | **its own floor, and the HIGHEST arm** |
| `SM11_RAW_NEG_EUCLID` | `0.6019` | above chance, **BELOW that floor** |
| `SM11_RAW_COSINE` | `0.5990` | above chance, **BELOW that floor** |
| `F_SCRAMBLE__SM11_*` | `0.4669` / `0.5000` | *chance -- controls bind* |
| `SM11_Z_COSINE` | `0.5358` | not separated |

**VERDICT `SENSORIMOTOR_DISCRIMINATION__B_AT_OR_NEAR_CONSTANT_PROTOTYPE_FLOOR`.** *A vector carrying
NO word identity beats the real grounded arms.* ➡️ **So the channel's above-chance reading is a
GENERAL property (a constant offset -- most plausibly concreteness), NOT word-specific meaning.**

## 🧱 **THE WALL -- 🔴 BROKEN 2026-08-24. READ THIS BANNER BEFORE THE SECTION BELOW IT.**
> **THE CLAIM BELOW ("NOTHING UNSUPERVISED CLEARS ITS OWN FLOOR") WAS TRUE WHEN WRITTEN AND IS NOW
> FALSE.** `where_does_a_meaning_signal_come_from_without_labels` came back SOLVED and re-verified:
> **cross-modal distillation reads `0.8388` CI `[0.8031,0.8720]`** on this exact instrument, beating
> its info-free twin's **MAXIMUM** over 200 draws (`0.7047`), with **no gold anywhere** -- the
> grounded hub TEACHES a direction over the distributional model, on `8,000` pairs whose vocabulary
> is disjoint from the instrument.
> ➡️ **SO THE SECTION BELOW IS THE STATE OF THE PROBLEM *BEFORE* THAT RESULT.** It is kept because
> every number in it still stands and is what the new arm had to beat -- but **do not quote its
> headline.**
> ⚠️ **AND THE LIMITS TRAVEL WITH THE FIX: LABEL-free but NOT RESOURCE-free (the teacher is the
> supplied Lancaster table), and TRANSDUCTIVE (orientation reads the candidate pairs' inputs).**
> 🚫 **THE SUBSTRATE ITSELF IS UNCHANGED -- stage 2 is still `BROKEN` and its write rule still reads
> `0.051`. This is a research result, not a capability, until it is wired.**

**As it stood BEFORE 2026-08-24 -- on one licensed instrument, one 242-pair population, chance
`0.4862`, known-answer `0.9599`:** **nothing unsupervised cleared its own floor.** Text transforms land at `0.02`-`0.13` (confidently
INVERTED -- they measure co-occurrence); the grounded channel lands at `0.599`-`0.602` but under a
`0.6195` no-information floor; **and the ONLY arms above `0.5` on merit are a FITTED SUPERVISED model
(`0.9606` held-out) and WordNet itself.** 🔑 **THE INFORMATION IS THERE AND EVERY UNSUPERVISED ROUTE
WE HAVE TRIED MISSES IT.** ⚠️ **THE NEXT MOVE IS NOT ANOTHER ARM ON THIS INSTRUMENT.** *Per the
owner's standing rule -- when the wheels spin, go back to brain foundationality and ask what
FUNCTION is missing -- the question is: **the brain acquires substitutability without a labelled
oracle. What does it use that a corpus does not contain?** That is a research question, not a
sweep.*

🧠🎯 **AND THE MECHANISTIC ANSWER WAS ALSO ALREADY ON DISK -- `exp_writerule_step_ladder_v1` (08-17)
AND `exp_writerule_maxpool_occurrence_v1` (08-18). THIS IS THE SYNTHESIS OF THE WHOLE NIGHT:**
**THE WRITE RULE BUILDS A CO-OCCURRENCE DETECTOR WHEN MEANING NEEDS SUBSTITUTABILITY.** On the
dissociation AUC (paradigmatic vs syntagmatic), where the WordNet known-answer arm reads `0.9599`
and every info-free floor sits at chance `~0.50`:

| arm | AUC | |
|---|---|---|
| `KNOWN_ANSWER_WORDNET_PATH_SIM` | `0.9599` | *the instrument works* |
| info-free floors (scramble / frequency / orthographic / random store) | `0.46`-`0.54` | *chance* |
| `S1_SINGLE_OCC` | `0.4173` | below |
| 🔻 **`A0_SUM` -- THE SUBSTRATE'S ACTUAL WRITE RULE** | 🔻 **`0.0510`** | **`BELOW_0.5_COOCCURRENCE`** |
| 🔻 `M1_MAXPOOL` (keep occurrences separate, best match) | `0.0299` | **WORSE** |
| 🔻 `M2_TOPK_MEAN` k=2/3/5 | `0.0264`/`0.0240`/`0.0217` | **worse still** |

🔑 **`AUC 0.051` IS NOT "AT CHANCE" -- CHANCE IS `0.50`. IT IS NEARLY PERFECTLY INVERTED.** *The
summed profile CONFIDENTLY ranks words that appear TOGETHER as similar, when the task asks which
words are INTERCHANGEABLE.* **It is not failing to learn; it is successfully learning the wrong
relation.** ➡️ **THAT EXPLAINS `TOP_COOCCURRENT` BEATING IT: the substrate is a WORSE
CO-OCCURRENCE DETECTOR THAN COUNTING CO-OCCURRENCE.**
⚠️ **AND THE OBVIOUS FIX IS ALREADY REFUTED TWICE: max-pool and top-k mean are WORSE than summing.**
🔻 **BUT THE INFORMATION IS THERE:** the ladder's decisive arm reads `BEST_SINGLE_ORACLE hit@1
0.3033` against `SUM_ALL 0.0100` and `RANDOM_SINGLE 0.0367` -- **summing is worse than picking ONE
occurrence AT RANDOM, and `30x` worse than picking the RIGHT one.** *So the gap is SELECTION, not
capacity -- and max-pool failing says we cannot yet select by best-match.*

✅ **GROUNDING IS NOT NOISE -- it beats the random floor on `2` of `3` seeds** (so my "at chance" was
wrong in that direction too). 🔻 **BUT A TRIVIAL "MOST CO-OCCURRING WORD" COUNT BEATS IT `2-3x` ON
`3` OF `3` SEEDS.** *The cell PRE-COMMITTED this reading: "(iii) ... ties TOP_COOCCURRENT -> what it
has learned is co-occurrence, this project's standing diagnosis arriving on a third instrument."*
**The observed case is WORSE than the tie it anticipated.** 🎯 **THE MECHANISM IS BEATEN, ON ITS OWN
TASK AND ITS OWN GOLD, BY THE SIMPLEST SUMMARY OF THE SAME TEXT -- and "read more" does not follow,
because these are already `10x` the reading volume of the underpowered arm.**
⚠️ **AND A LIMIT I CHECKED ON MYSELF: the distributional result is Spearman rho on word-pair
similarity; ours is anchor-assignment precision against ConceptNet. DIFFERENT TASKS, DIFFERENT
SCORERS -- the numbers may NOT be compared.** *Each is judged only against ITS OWN floor. I withdrew
"the gap IS the extraction mechanism" for resting on the forbidden verdict.*
*Witness: `test_grounding_correctness_has_never_been_measured_at_power.py`.*

### 🗄️ **08-22 (SUPERSEDED IN ITS PREMISE, KEPT FOR ITS SCOPING CORRECTION)**
🔻 **THE SCOPING CORRECTION (08-22): "a 1970s baseline beats us EVERYWHERE" over 16 measures was
wrong -- ALL SIXTEEN ARE THE WORD-SIMILARITY CHANNEL**, which `SUBSTRATE_CHARTER` (08-05) and
`PLAN_grounded_semantic_organ_build` had already ruled out and MEASURED (bag-of-words scores
`0.5167` = chance, gold sense handed to the classifier). ✅ On the GROUNDING organ the picture
inverts: `0.962-1.000` vs bow's `0.500-0.517` on the same items. The 16 measurements stand; only
"everywhere" was wrong.

🧭 **THE DIRECTION HAS A NAME, SET 08-06/08-07, NOT BY ME: ANCHOR + PROPAGATE** -- ground a small
affective anchor and reason outward (antonyms are distributional twins, so good/bad is in neither
grammar nor text statistics). 🚨 The build plan cited `PLAN_B` zero times (grep) -- two plans, only
one read; that is why the night went where it did.

✅ **WHAT IS BUILT AND PASSING:** *`bridge1_governor_grounding` HARD_PASS `0.967`; `confirmation_test`
RULING_CONFIRMED (the PREDICTED failure at `0.500`); `twostage_event_situation_v2` HARD_PASS B `1.000`
C `1.000`; a 12-WORD seed via `wordnet_polarity_propagation` -> `0.833` held-out, seed-ablation `0.000`.*
🎯 **DOUBLE DISSOCIATION: each subset's MATCHED scramble degrades, the UNMATCHED one does not.**
🔻 **SCALE: n = 21/12/12/8 and one `Cgen 1.000` IS n=2 -- that number carries NOTHING (p=0.25).**
⚠️ **VERIFICATION: those numbers are READ, NOT REPRODUCED. Re-running gives `elapsed 0.0s` -- checkpoints
REPLAY and the no-op is INDISTINGUISHABLE FROM A PASS.** *`--self-test` DOES recompute and reproduced the
pattern. Scope measured: `399` of `7,868` landed cells, 5.1%.*

✅ **OPEN VOCAB FULLY DIAGNOSED, NOTHING REPAIRED:** `B` 10/12=`0.8333`, `Bgen` 6/8=`0.750`, and ALL
FOUR ERRORS HAVE AN ADVERSARIAL PATIENT (`enemy` x2, `rival`, `thief`). **THE ANIMACY MAP CANNOT
EXPRESS `ADVERSARIAL`** -- an expressiveness gap, not a lookup bug (WordNet supplies it for 7/8). It
misses `rival` correctly: WordNet files it under `contestant`, hostility is in the gloss not the
taxonomy -- exactly the part both plans say needs SITUATION context. The `== "UNK"` guard is a
deliberate hand-off, not a defect -- DO NOT TOUCH IT. 🔻 Three retractions getting there (root
fault: compared `v2.lookup_animacy` vs the real lookup, and the closed arm does not consume it) --
the answer was in the cell's docstring at line 52, read past twice over four turns.

➡️ **FRONTIER: REAL PROSE / CREDIT-ASSIGNMENT -- MEASURED TO EXHAUSTION THIS SESSION.** The WALL
REPRODUCES: both cells landed 08-07, their lemmatizer was repaired 08-13 (a real confound I
identified) -- re-ran today, `primary 0.4722` IDENTICAL to four digits, same `HARD_FAIL`; confound
hypothesis REFUTED, wall is STRONGER (`_is_verblike` fires on the SURFACE form, so the repair
changed each mistake's NAME, not its DECISION). **The precision problem is LIGHT VERBS, not junk:**
53.2% of credited exposures are not loaded; of 173 error types, 46 are genuine verbs carrying no
outcome information. My morphology thread addressed only `5.4%` of credited tokens (measured 3x,
progressively smaller) -- I RETRACT calling it "the cheapest high-value fix". Local-window credit is
measured out: coverage is NOT the cause, precision ~47% dominated by light verbs. RETURNS TO WHAT
BOTH DOCUMENTS SAID: REPLACE THE WINDOW -- but that build's causal component is reducible to
connective-else-most-recent by its own VET, and needs mention-annotated CoNLL the loop does not
produce.
`I_RE_RAN_THE_WALL_...` `THE_OPERATIVE_NUMBER_IS_5_4_PERCENT_...` `SCOPING_THE_PRESCRIBED_BUILD_...`

## 🌙 THE NIGHT OF 2026-08-21 -- **COLLAPSED. NOT THE ONLY COPY: every row lives in its named note
AND in the plan's consolidated top block. Do NOT re-expand.**

| # | the number that carries it |
|---|---|
| **H2/E3/B4** | Foraging read its way to `textbook_biology 0.63245` (register headline WITHDRAWN). Coref "salience" is PROVABLY a mention-count (`argmax_count_fraction 1.0`, n=89) and that HARD_FAIL is UNDERPOWERED. Spelling beats the meaning read-out at rank 1 (`0.0767` vs `0.0480`) but the substrate WINS the full ranking (`37.0` vs `54.0`) -- **the defect is PRECISION AT THE TOP.** [`T1_`,`T2b_`,`T2c_`,`T5b_`] |
| ⭐ **RATE** | **WRITE LESS: `0.0710 -> 0.3079` at p90, 4.3x -- BUT A RATE-MATCHED RANDOM GATE MATCHES IT.** *Prediction-error gating REFUTED; the gain is RATE.* ⛔ every arm stays BELOW co-occurrence. [`THE_RATE_SWEEP_...`] |
| **NORMS12** | 829 identical pairs, one scorer: `SUPPLIED euclid 0.2876 | cosine 0.2176 | LEARNED d=1024 0.1071 | d=256 0.0944`. **EUCLID BEATS COSINE BY `+0.0700`, MORE THAN OUR WHOLE ARM CLEARS ITS NULL (`0.0440`).** ✅ `GROUNDED_CAP=0.45` is a MEASURED SAFETY PROPERTY. 🚫 SUPPLY, NOT LEARNING. [`SUPPLIED_BEATS_LEARNED_2_69x_`,`THE_CAP_IS_PRINCIPLED_`] |
| **CHANCE?** | **CORRECTED, I OVER-REACHED:** the trained substrate is **+16.3 pp REPLICATED** over an untrained codebook. **But `SUBSTRATE - COUNTING = -0.142` CI `[-0.203,-0.082]` SEPARATED**, cross-task: `counting RAW 0.0885 | OURS 0.1071 | counting+IDF 0.1835 | supplied 0.2876`, **`IDF - OURS = +0.0764` CI `[+0.0263,+0.1278]`.** 🚫 **ANY CLAIM HERE MUST CLEAR `0.1835`.** ⚠️ *Word-similarity channel ONLY -- see TOP ITEM.* [`COUNTING_WITH_ONE_STANDARD_WEIGHTING_`] |

## WHAT IS RUNNING

- **2026-09-13 ~08:05 local (overnight session, stop hook ARMED):** RUNNING = the all-category-work board (total routing + category
  organ v2 + dual-route + Penn defaults; `data/hook_state/board_route.log`), then chained behind it the board with the BRAIN-
  FOUNDATIONAL HEADS on (`HDLAB_HEADS_SOURCE=attachment_arm`; `board_arm.log`) -- that number answers Q134. Finished this morning:
  dual-route board 0.6378 (neutral), Penn-tagger board 0.6378 (neutral). Owner-DONE fold-in gate EMPTY; pri-15 solver folder untouched.
  Open owner question: Q134 (flip the heads default). Everything committed path-limited; NOTHING pushed.
  09:40 local: desktop bundle (197 commits) LANDED; the 15 GB session-folder copy was cut by a connection reset -- rerun the light
  `resync_claude_state.sh` before moving; owner is awake (chat update given 09:31). Two boards still pending (route, arm).

- **OPERATING MODEL (owner 08-22): STRATEGY SESSION (this) + SOLVER SESSIONS.** This session keeps the 10k view, writes/verifies briefs, and INTEGRATES owner-DONE; solvers each solve one bounded problem.
- **RESEARCH DELIVERED + FOLDED (CONT-73):** the pri-3 parser BF syntax-acquisition drill completed + vetted first-hand -> `RESEARCH_bf_acquisition.md`. Key: the 'frozen supervised hard-decode' = TWO orthogonal defects (graded-decode route-through NOW + the acquisition question); category-induction-from-prediction is a verified in-substrate HARD_PASS, structure-induction hit an UNDER-POWERED located ceiling (3k/12.5k sents, 0 EM). Cheap first build folded into the brief (re-run exp_predictive_selfsup_parser_v1 full-scale + EM, composing the pri-2 directional channel + graded Matrix-Tree decode). NOTHING now running.
- **FRONTIER = FLEET-SIDE, PRODUCTIVE (CONT-81):** all 3 BF-upchain problems I posted + pri-5 now have SOLVER SUBMISSIONS awaiting OWNER REVIEW (owner-DONE gate EMPTY for me): **pri-3 the_parser PARTIAL**, **pri-4 commonnoun_binder->typed_coref SOLVED**, **pri-5 measure_end_to_end PARTIAL**, **pri-7 force_dynamics_valence->Wolff SOLVED**. The problem-posting + de-risking (scouting maps, verified premises, folded parser research) bore fruit. When owner marks any DONE, integrate promptly (reverify->grade->land-> ledger/§2b->commit path-limited, NOTHING pushed).
- **AWAITING OWNER REVIEW (not my move):** pri-5 `measure_end_to_end` (gates the pri-2 meaning fusion-flip), pri-6 reward-cluster audit; board Q123 (STATUS trim -- DONE by me, this file, CONT-73), Q122 (stale/obsolete -- its referenced landings are all integrated).
- **NOTHING else running.** Everything committed path-limited, NOTHING pushed. Owner-DONE fold-in gate EMPTY.

## DO NOT REDO -- NEVER-TRIM -- stubs; detail in LESSONS
All CLOSED. `*` = revival criterion. 1 intersection-over-argmax; 2 the "40% ceiling"; 3 syntactic
bootstrapping*; 4 F2 freq-corrected pool*; 5 same-sentence cosine/PMI; 6 FHRR superposition; 7 PBV;
8 read-out vs v5's 64%; 9 F1+F3 stabilisation; 10 news->textbook swap; 11 norms as FILTER*; 12 sense
selection v2; 13 minimum-grounded-basis; 14 `genuine_cross_source_corroboration_v1`*; 15 combined
dictionary v1; 16 "context vector is noise"; 17 co-occurrence as the explanation; 18 role-bound
structure alone -- STRUCTURE_HURTS*; 19 frontmatter `isolation:`/`background:`; 20 the voting
mechanism*; 21 hand-scored delta at 1-3%; 22 the 2-hop bridges; 23 extraction as DIRECT-BANK*;
24 distinctiveness as log-IDF*; 25 differentia/genus + supply; 26 `sign()` vs forgetting kernel;
27 rank-1 common-mode removal*; 28 FORAGE_REFUSAL; 29 five-stage read-out chain; 30 near-duplicate
anchors; 31 supply as an ADDITIVE CHANNEL -- NARROWED*; 32 DG/pattern-separation for grounding;
33 crowding as a gate criterion; 34 the GRADED SWITCH*; 35 +0.0602 as a C3 number; 36 `k_eff~=50` as
a MEASURED limit*; 37 "right neighbourhood, wrong member"*; 38 bridging WITH the THEMATIC hub --
MEASURED NULL*; 39 sparsifying the READING anchor -- dies on the real task*; 40 quoting +0.2285 as
the bridging margin; 41 quoting a "0.073 lift gap"; 42 `grounded_similarity()` AS A SCORER --
76.18% of SimLex on two values, NO revival; 43 SELECTIONAL-CONSTRAINT bridging -- CI-separated
BELOW the neighbour-copy incumbent*; 44 sparsifying the STORED KEY under a partial cue -- below the
flat store with oracle*; 45 the BASIN EXPLANATION for cleanup nulls -- opposite to prediction*;
46 CUE-SIDE ENGINEERING as a read-out fix -- does not transfer to hit@1*.
CAVEATS: D1 near-vs-far; D2 encoder-swap; D3/D4 foraging reversals; D5 sharpening SMOKE-only;
CT1 consistent!=good; CT2 run_mode is an ingestion constant.
CORRECTIONS: C1 availability-binds-first; C2 CLIP-at-INGEST; C3 the 94% has NO floor;
C4 DGProj=interference; C5 an encoder EXISTS; C6 wrong checkpoint; C7 opp-map #5/#6; C8 comparator
was a LOOKUP TABLE; C9 results ARE searchable; C10 tautology=eligibility bug; C11 "58% common mode";
C12 doc date; C13 the FULL DID report; C14 whiten+pinv IS tested; C15 chain self-contradiction;
C16+C22 `A5_STRINGCTRL` not zero-meaning; C17 scramble is DONOR-RULE dependent; C18 conjunctive lean
QUALIFIED; C19 k_eff correction-of-a-correction; C20 "0.90 precision" UNSOURCED; C21 "0.95"=parse
coverage; C23 121.1M-token encoder/237.7M corpus; C24 norms stale BOTH ways; C25 shortlist-hit out
of scope; C26 FHRR 0.956 bare threshold; C27 VET residue; C28 +0.2285 was a NEIGHBOUR-CHOICE
diagnostic; C29 the "0.073 lift loss" is 0.0034, populations mixed; C30 "retrieval fine / we tie
spelling" is EXACT-KEY + OPTIMISTIC-TIE ONLY; C31 the checker's false pass was THREE defects;
C32 "0 of 7,769 meet the bar" -> 1 of 7,789, and that survivor is itself rejected; C33 "our
instrument cannot resolve verbs even when handed the answer" -- SUSPENDED at n=86 became MEASURED
NEGATIVE at n=222 (rho 0.2607, margin +0.1452 NOT_SEPARATED, permutation p=0.001) -- cite this,
never the retired n=86 number; C34 "the constant floor is the binding one" FALSE in general -- it
is -0.1959/-0.2253 on the bridging/selectional strata, the WEAKEST of the four; C35 "binding-operator
choice is EMPIRICALLY NULL across two cells/six operators" PART-WRONG THREE WAYS -- a 3-bin
instrument is not a null, wrong cell named, two operators COLLAPSE; never varied on any job this
programme runs on; C36 "d 256->8192 moves partial-cue addressing" MIXES READ REGIMES -- matched, the
conclusion (dimensionality does nothing for addressing) STRENGTHENS; C37 "B1 IS A CLIFF" WITHDRAWN/
INVERTED -- those are cos_syn/rel/unrel, not vocabulary strata; OUR 0.002 is correct and counting's
0.830 is the defect. Full text of C33-C37: `STATUS_LESSONS.md`.


## STANDING DISCIPLINES -- NEVER-TRIM -- LESSONS
1 NO HAND-SCORED MEANINGFUL-DELTA GATE WHILE THE GENERATOR SITS AT 1-3% -- cost TWO experiments, the
2nd claiming to have FIXED the 1st; gate on KNOWN-ANSWER RECALL. 2 SERIALIZE MEASUREMENT vs CODE
CHANGE (2x). 3 A CHECKER SHARING A FLAW WITH WHAT IT CHECKS HIDES IT (4x in one night; C31 = 5th;
**6th 08-21, TWO NEW FORMS: (a) A CHECKER THAT CHECKS A *SUBSET* REPORTS GREEN ON A BROKEN FILE --
the session hook's own positive control asserted "the real STATUS.md parses clean" and PASSED while
2 of its 4 required headings were GONE, because it guarded only the 2 IT consumes. A POSITIVE
CONTROL IS ONLY AS BROAD AS ITS ASSERTION. (b) A CONTROL THAT FIRES INTO A FILE NOBODY RE-READS IS
NOT YET A CONTROL -- `board.py` DID fail loud, into `BOARD.md`, and it sat. Fixed: all 4 literals
guarded at session start, PROVEN by firing on the real pre-fix file, not only on fixtures**).
4 ESTABLISH THE FINAL LANDED VERSION BEFORE EVALUATING A SUBSYSTEM (6x); AN ABSENCE CLAIM REQUIRES
AN ENUMERATION, NOT A SEARCH -- state HOW. 5 BEFORE GATING ON A BENCHMARK, CHECK THE BRAIN PERFORMS
THE OPERATION IT SCORES. 6 RUN A POSITIVE / KNOWN-ANSWER ARM (2x): a FLOOR says whether the EFFECT
is real, a KNOWN-ANSWER arm whether the INSTRUMENT is -- run both. 7 NO DEMOTION WITHOUT A FRESH
ON-DISK RE-CHECK -- ~11 wrongly demoted, 17 corrections-of-a-correction in 48h; keep
EXISTS/IS-REACHED/IS-GOOD separate. **C35 is the 18th: a correction said a claim "does not
reproduce" when the cell it reproduces in was never opened.** 8 A GATE IS A CI-SEPARATED MARGIN
ABOVE max(ORTHOGRAPHIC, FREQUENCY, SCRAMBLE, CONSTANT) on the IDENTICAL scorer/n/pool/gold -- never
a bare number, baseline STANDALONE, every floor recomputed on the item's OWN population **AND ITS OWN
REPRESENTATION (widened 08-18 -- see 16; "population" alone did NOT catch the 0.5431 import).**
9 DETECTORS FIRE ON HONESTY (49/49 flagged were false positives). 10 SILENT JOINS FABRICATE GREEN
AND RED -- ASSERT+COUNT joined rows. 11 A NUMBER MAY NOT BE CARRIED BETWEEN SCORERS OR POPULATIONS
-- cost 3x in one night (C28/C29/C30); name the scorer, n, pool and gold for BOTH sides or you have
no comparison. 12 A CLAIM MEASURED AT THE EXACT-KEY OPERATING POINT DOES NOT TRANSFER TO THE
PARTIAL-CUE REGIME, WHICH IS THE REAL ONE -- top-50 0.5566 exact vs 0.3758 partial; state the cue
regime beside every retrieval number. 13 REPORT TIE CONVENTIONS BOTH WAYS, NEVER SILENTLY PICK THE
FLATTERING ONE -- +0.0105 NOT_SEP flips to +0.0641 ABOVE on tie mass alone. 14 REPORT THE CI
HALF-WIDTH + NULL p95 BESIDE EVERY MARGIN -- A WIDTH IS NOT AN EFFECT (cost 3x, C32-C34). 15 A
GRID'S RESOLUTION IS PART OF ITS VERDICT -- a 3-value-grid equality is a BIN not a measurement
(C35); state swept values + queries/point. 16 A FLOOR IS SPECIFIC TO ITS REPRESENTATION, NOT ONLY
ITS POPULATION -- 0.5431 (bag-of-words) was quoted as THE bar for 2 days then misapplied to
arc-built arms (real floor 0.6317); rebuild the floor whenever the representation changes. 17
EVERY NEGATIVE GETS A BRAIN-FIDELITY DRILL, EVERY TIME (owner 08-18) -- ask WHICH BRAIN STRUCTURE,
replicating vs substituting, what closes the gap; FIRST ask if the negative is even real (4 of
08-18's were measurement defects, not results). SAFETY: brain-drill queries use general biology
terms only, never our architecture/organs/operators/dims/results. 18 GATE ON THE FLOOR'S UPPER
BOUND, NOT ITS POINT VALUE -- CREDIBLE BAR = floor + its own 95% half-width; if no achievable
score could clear it, the point is UNTESTABLE, not negative.
Full text of 14-18 (verbatim, nothing shortened in substance): `STATUS_LESSONS.md`
"STANDING DISCIPLINES 14-18 -- FULL TEXT MOVED FROM STATUS.md 2026-08-22".
