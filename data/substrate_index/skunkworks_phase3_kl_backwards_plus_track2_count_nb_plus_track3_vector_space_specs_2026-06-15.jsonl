{"_meta": "SKUNKWORKS Phase 3 specs unblocked by Sub-batch 2 landing (2026-06-15). THREE sections: (A) kl-canonical backwards-edge review (113b); (B) Track 2 count_nb materialized re-type (117b/118d); (C) Track 3 vector_space->group_type secondary materialized sweep (117b/118d). All are materialized relation-store edges (verified). SPEC/VET ONLY -- Testbed executes; Exp-Dev pre-checks; I vet. Standard pre-check stack per section. ASCII only.", "date": "2026-06-15", "author": "skunkworks"}
{"_section": "A -- kl-canonical backwards-edge review (DECISION 113b)", "canonical": "math::T1/kullback_leibler_divergence", "finding": "the canonical (post-Sub-batch-2 merge) has 3 backwards DEPENDS_ON to its own CONSUMERS: kl -DEPENDS_ON-> {bocpd_changepoint, em_algorithm, mp_bulk_kl}. These atoms USE kl (em E-step convergence; bocpd changepoint scoring; mp_bulk_kl IS a kl-based observer). Foundational KL should NOT depend on its consumers. Same disease as integral/lebesgue (101c) + matrix_decomposition (109b)."}
{"section": "A", "op": "REMOVE_backwards", "edges": [{"src": "math::T1/kullback_leibler_divergence", "rel": "DEPENDS_ON", "tgt": "math::T3/bocpd_changepoint", "why": "bocpd USES kl, not the reverse"}, {"src": "math::T1/kullback_leibler_divergence", "rel": "DEPENDS_ON", "tgt": "math::T3/em_algorithm", "why": "em_algorithm uses kl for convergence, not the reverse"}, {"src": "math::T1/kullback_leibler_divergence", "rel": "DEPENDS_ON", "tgt": "math::T3/mp_bulk_kl", "why": "mp_bulk_kl is a kl-based observer (consumer)"}], "verify_upward_edges": "for each of bocpd/em_algorithm/mp_bulk_kl, confirm the CORRECT consumer->kl edge exists (USES or DEPENDS_ON kl); if missing, ADD it so the relationship is preserved in the correct direction. (Earlier dump: mp_bulk_kl USES kl + RELATES kl; bocpd RELATES kl -- verify these suffice or add USES.)", "leaf_strand": "SAFE: kl retains DEPENDS_ON integral + metric_space (axiom path); removing 3 backwards consumer-edges does not strand it.", "tier_note": "kl is T1, consumers are T3 -> removing kl(T1)->consumer(T3) DEPENDS_ON also fixes a tier-monotone inversion (T1 should not depend on T3)."}
{"_section": "B -- Track 2 count_nb materialized re-type (Naive Bayes is GENERATIVE)", "atom": "math::T3/count_nb", "finding": "count_nb -SPECIALIZES-> math::T2_FAM/discriminative_classification is MATERIALIZED and WRONG: Naive Bayes is a GENERATIVE classifier (models p(features|class)*p(class) via bayes_rule), not discriminative. count_nb already DEPENDS_ON+USES bayes_rule (confirms generative)."}
{"section": "B", "op": "REMOVE_wrong_SPECIALIZES", "edge": {"src": "math::T3/count_nb", "rel": "SPECIALIZES", "tgt": "math::T2_FAM/discriminative_classification"}, "blocker": "the correct family atom math::T2_FAM/generative_classification DOES NOT EXIST (verified NOT FOUND).", "recommendation": "TWO-STEP: (1) REMOVE the wrong count_nb -SPECIALIZES-> discriminative_classification NOW (correctness; count_nb retains DEPENDS_ON bayes_rule/labeled_example/probability_vector/tier2_schema -> leaf-strand SAFE). (2) Author a math::T2_FAM/generative_classification family atom (small additive op; parallels discriminative_classification family_tag) and ADD count_nb -SPECIALIZES-> generative_classification. Until (2), count_nb has no classification-family link (acceptable interim; it is still grounded via bayes_rule). ALTERNATIVE if family-authoring deferred: count_nb -RELATES-> discriminative_classification (contrast: generative-vs-discriminative siblings).", "signature_fix": "also correct the count_nb self-model signature pointer (specializes:discriminative_classification -> instance_of/specializes:generative_classification).", "confidence": "high on REMOVE (NB is unambiguously generative); family-authoring is the clean completion"}
{"_section": "C -- Track 3 vector_space->group_type secondary materialized sweep", "atom": "math::T1/vector_space", "finding": "vector_space -SPECIALIZES-> group_type is MATERIALIZED. Borderline: a vector space IS an abelian group under addition (so SPECIALIZES group_type is partially defensible), BUT a vector space is MORE -- it adds scalar multiplication over a field. vector_space already DEPENDS_ON field_axioms + group_axioms (the structural components are present)."}
{"section": "C", "op": "RE_TYPE_or_KEEP", "edge": {"src": "math::T1/vector_space", "rel": "SPECIALIZES", "tgt": "math::T1/group_type"}, "recommendation": "RE-TYPE SPECIALIZES group_type -> composed_of:[group_type (abelian, additive), scalar_action, field] -- because a vector space is COMPOSED OF an abelian group plus a scalar action over a field, not merely a SPECIALIZATION of group. This matches the Track-1 vector_space signature fix (defined_over:field + composed_of:[abelian_group, scalar_action]) so signature and materialized edge agree.", "alternative": "KEEP SPECIALIZES group_type as defensible (VS is-a additive group) AND add the scalar structure separately. Lower-confidence than count_nb; flagged for Director ruling.", "leaf_strand": "SAFE: vector_space retains DEPENDS_ON field_axioms + group_axioms either way.", "confidence": "medium (defensible to keep; composed_of is the more precise model)"}
{"_summary": {"section_A_kl_backwards": "remove 3 backwards consumer DEPENDS_ON (+ tier-monotone fix); verify upward edges", "section_B_count_nb": "remove wrong discriminative SPECIALIZES (high conf) + author generative_classification family + signature fix", "section_C_vector_space": "re-type SPECIALIZES group_type -> composed_of (medium conf; or keep)", "all_materialized": true, "leaf_strand_all": "SAFE", "pre_check": "standard stack per section; Exp-Dev forward-walk + tier-monotone + axiom-term + dangling"}}
{"_execution_notes": "All three are real relation-store edges (verified materialized; distinct from Track 1 which is signature-metadata-only). Section A also corrects a T1->T3 tier-monotone inversion. Testbed: ratify per section (independent); atomic each; R3 verify. Skunkworks vets each post-op. count_nb generative_classification family authoring is a small additive op (could be its own micro-batch). NOT executed here."}
