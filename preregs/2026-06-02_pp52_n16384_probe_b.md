# Prereg: PP-52 Probe B -- exact rollback + one-shot addition at N=16384

**Date:** 2026-06-02
**Anchors:**
- pp52_exact_rollback_n16384_v1
- pp52_one_shot_addition_n16384_v1

**Capability question:** Does PP-52 exact-rollback + one-shot-addition hold at N=16384?

**Context:** PP-52 BAND-LIFT (0.60-0.75) cycle 12. N=4096+N=8192 both HARD_PASS. N=16384 is next cross-N rung for flagship-class production claim.

## Pre-registered bands (v342 routing note, Probe B)

### Exact rollback (pp52_exact_rollback_n16384_v1)
- HARD-PASS: rel_err < 1e-6 (HP1) in >= 4/5 seeds; acc_after >= 0.95 (HP2) >= 4/5; rollback_time < 2.0s (HP3) all seeds.
- HARD-FAIL: rel_err > 1e-3 OR acc_drop > 5pp.
- MIDDLE: rel_err in [1e-6, 1e-3] OR acc_drop 1-5pp.

### One-shot addition (pp52_one_shot_addition_n16384_v1)
- HARD-PASS: cos_new >= 0.90 (HP1) >= 4/5 seeds; acc_retain >= 0.95 (HP2) >= 4/5; write_time < 2.0s (HP3) all seeds.
- HARD-FAIL: cos_new < 0.70 OR acc_drop > 10pp.
- MIDDLE: cos_new in [0.70, 0.90) OR acc_drop 5-10pp.

### acc_drop = 0 pre-registered as expected (algebraically guaranteed)

## Smoke results
- Rollback smoke (N_smoke=2048, 2 seeds): rel_err=0.00, acc_drop=0.00, rollback_s=0.003. All HP gates clear. MIDDLE_BAND verdict (seed-count artifact: requires 4/5 threshold at full run).
- One-shot smoke (N_smoke=2048, 2 seeds): cos_new=1.0, acc_drop=0.00, write_s=0.001. HARD_PASS.
- OOM check: peak_est=0.03-0.05GB vs total=8.59GB. Safe.
- GPU util: peak_gpu=0.209GB.

## Timeout estimate

### Rollback N=16384
- smoke_wall_s=0.11, FULL_N/smoke_N=8, FULL_seeds/smoke_seeds=2.5, scaling_exp=2.0 (matrix outer product)
- timeout_s = ceil(1.5 * 0.11 * 8^2.0 * 2.5) = ceil(26.4) -> 1800s (conservative floor)

### One-shot N=16384
- smoke_wall_s=0.04, same parameters
- timeout_s = ceil(1.5 * 0.04 * 64 * 2.5) = ceil(9.6) -> 1800s

## PROT compliance
- PROT-018: _n16384 suffix; N=16384 in FULL config. Verified via grep.
- OOM pre-check: 3 matrices * 1073MB = 3.2GB << 8.59GB. Safe.
- GPU template enforced.
