# Prereg: Q-A3 L=30 cross-layer composition N=8192

Date: 2026-06-03
Cycle: v369 refill (Cycle 38)
Anchor: q_a3_l30_cross_layer_composition_v1_n8192

## Context

N=8192 cross-N depth ladder: {L=19, L=22..L=29} all HARD_PASS EXACT-1.0000 (L=29 confirmed v369). This adds rung 10 (L=30) in the N=8192 series.

N=4096 series has confirmed L=30 (within L=2..L=35 series at N=4096, see v360 cycle). This rung establishes 2-N cross-N {N=4096+N=8192} confirmation at L=30, extending N-independence evidence one rung.

## Scientific question

Does the EXACT-1.0000 fidelity property hold at L=30 N=8192? Does N-independence of the unlimited-depth property extend to L=30?

## Pre-registered bands

HARD-PASS: all 30 level fidelities >= 0.9999 unanimous (5/5 seeds) AND l30_acc >= 0.5.
MIDDLE-BAND: any L_fid in [0.85, 0.9999) OR graceful degradation.
HARD-FAIL: any L_fid < 0.85 OR l30_acc < 0.5.

## Timeout estimate

L=29 N=8192 FULL elapsed 3.0s (5-seed from v369 remote metrics). L=30 near-linear.
Formula: ceil(1.5 * 3.0 * 1.0 * 5) = ceil(22.5) -> 300s (floor applied).

## N-suffix binding (PROT-018)

Anchor has _n8192; production N = 8192 in script. Verified: grep N=8192 returns match; _N_SUFFIX = 8192; assert N == _N_SUFFIX present.

## PROT-021

Seed checkpoints keyed with run_mode + L. Smoke uses N_ACTIVE (smaller), FULL uses N=8192.

## Memory estimate

On-demand W: 8192 x 8192 x 4 bytes = 268 MB per layer. Single W at a time. Well within 8 GB GPU (peak observed ~0.56 GB at L=29 N=8192).

## Formula self-tests (PROT-022)

1. L=30 chain: 29-ctx Hadamard roundtrip recovers xi_L1. Verified at module scope.
2. All alphas < alpha_c=0.138 at N=8192. M_INNER=100 -> alpha=0.0122. All M_MID entries <= 50 -> alpha <= 0.0061. All < 0.138.
3. GPU memory > 0 after W build.

## Middle-band outcome plan

If MIDDLE-BAND: N=8192 depth ceiling below L=30. Compare to N=4096 (all PASS to L=35) and N=16384 (all PASS to L=51). If N=8192 shows ceiling at L=30 while N=4096 and N=16384 don't: N-dependent ceiling effect; file to Strategy.

## Smoke policy

GPU script; local CPU has no CUDA. Using --skip-smoke path per exp_dev policy: anchor L=29 N=8192 (elapsed_s=3.0s from v369) as timing anchor. Script structurally identical to L=29 N=8192 which was FULL HARD_PASS.

## Queue

overnight_queue (GPU). Short run (~3s FULL); included for cross-N coverage. Timeout 300s.
