# Prereg: substrate_continual_learning_distshift_v1
## Anchor
substrate_continual_learning_distshift_v1
## Routing
HP-11: continual learning under distribution shift (stream A then contradicting stream B; delete-and-replace; no silent contradictions). CPU $0.
## Bands
HARD-PASS current-state>=0.95 AND updated-returns-B>=0.95 AND old-valid>=0.95 AND silent-contradiction<=0.02. MIDDLE >=0.85. HARD-FAIL silent-contra>10