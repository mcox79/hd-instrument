# Prereg: Q-B1 chain depth-400 at N=16384

**Date:** 2026-06-03
**Anchor:** q_b1_chain_depth_400_v1_n16384
**Queue:** overnight_queue (GPU)

## Hypothesis
Heteroassociative chain fidelity at N=16384 remains flat (near-zero per-hop decay) to depth-400.
Five prior N=16384 flat-profile confirmations: {d80, d100, d150, d200, d300}. All showed ~0.969-0.990
fidelity with per-hop lambda~0.00015/hop (noise floor). Depth-400 extends by 100 hops.

## Pre-registered bands
**HARD-PASS:** d5 >= 0.90 AND d20 >= 0.75 AND d50 >= 0.50 AND d100 >= 0.20 AND d300 >= 0.02 AND d400 >= 0.02
**MIDDLE:** d400 in [0.005, 0.02) while earlier depths meet HP
**HARD-FAIL:** d5 < 0.80 OR d20 < 0.50 OR d400 < 0.005

## Formula self-tests (PROT-022)
1. depth-400 chain retrieval non-NaN at N=128
   [INPUT: N=128, CHAIN_DEPTH=400] [EXPECTED: cos non-NaN in [-1,1]]
2. Cosine of stored pattern with itself = 1.0
   [INPUT: xi vs xi] [EXPECTED: cos_self = 1.0]
3. GPU memory > 100 MB after H build
4. Per-hop lambda extrapolation d400: 0.969 * (1-0.00015)^100 > 0.90
   [INPUT: d300_cos=0.969, lambda=0.00015, extra_hops=100] [EXPECTED: 0.954 > 0.90]

## Timeout estimate
d-300 estimated wall ~1000s FULL 5-seed GPU (linear scaling: d200=470s -> d300 ~705s -> d400 ~940s).
ceil(1.5 * 1000 * (400/300) * 1.0) = ceil(2000) = 2100s. Well within 14400s cap.

## Band rationale
Calibration: all 5 prior N=16384 flat-profile confirmations in {d80..d300} passed by 18-48x margin.
d400 bands (HP >= 0.02, HF < 0.005) apply +-50% calibration policy to the new depth point.
Expected per flat-profile continuation: d400 ~ 0.954 >> 0.02 HP.

## N-suffix section
Anchor has _n16384; N = 16384 in script. PROT-018 verified.
