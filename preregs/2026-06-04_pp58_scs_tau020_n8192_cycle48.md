# Prereg: PP-58 SCS tau sweep d=8 tau=0.20 N=8192 cycle 48

## Anchor
pp58_scs_tau_sweep_d8_tau020_v1_n8192

## Context
Continuing tau sweep past tau=0.15 (completed, cycle 47).
tau=0.05: HARD_FAIL (d_estimate < 1.5). tau=0.10: completed. tau=0.15: completed.
tau=0.20 continues the sweep to map tau-vs-spike relationship at d=8.
W = (1-0.20)*W_sym + 0.20*W_asym where W_sym = Xi^T Xi / N, W_asym = antisymmetric random.

## SCS Formula
gamma = (d + tau/d) / (1 + tau)
At d=8, tau=0.20: gamma = (8 + 0.025) / 1.20 = 8.025 / 1.20 = 6.6875

## Pre-registered thresholds
- HARD-PASS:
  - mean d_estimate >= 1.5 (spike condition met)
  - gamma_SCS within 30% of gamma_emp at 4/5 seeds AND mean ratio in [0.7, 1.3]
  - tau_actual in [0.13, 0.28] (within 40% of tau_target=0.20)
- MIDDLE:
  - d_estimate in [1.2, 1.5) OR ratio in [0.5, 1.5)
  - tau_actual outside [0.13, 0.28] but d_estimate >= 1.2
- HARD-FAIL:
  - mean d_estimate < 1.2 (no spike)
  - OR mean ratio < 0.5 or > 2.0 (SCS off by >2x)
  - OR tau_actual < 0.09 (asymmetry injection failed)

## Timeout estimate
- Smoke wall (tau=0.15 precedent): ~1200s per seed
- FULL formula: ceil(1.5 * 1200 * 5) = 9000s
- Applied floor: 21600s (6 hours)
