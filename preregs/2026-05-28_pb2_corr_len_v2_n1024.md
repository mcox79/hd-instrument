# Pre-registration: pb2_corr_len_v2_n1024

Date: 2026-05-28
Queue: remote_cpu_queue
Script: experiments/exp_pb2_corr_len_v2_n1024.py
N: 1024
Seeds: [7, 17, 23, 31, 41]
M_fracs: [0.1, 0.25, 0.5, 1.0, 2.0, 5.0]

## Hypothesis
Edit-propagation correlation length (xi) increases with M_frac (more stored memories = broader interference coupling). At low M_frac, xi_normalized < 0.5; at high M_frac, xi_normalized > 0.5.

## Thresholds (pre-registered)

HARD_PASS: monotone increasing xi_normalized vs M_frac (Spearman_rho >= 0.80), across >= 3/5 seeds; xi_normalized[M_frac=5.0] >= xi_normalized[M_frac=0.1] * 1.5
HARD_FAIL: xi_normalized is flat or decreasing (Spearman_rho < 0.30) for majority seeds
MIDDLE_BAND: all other outcomes

## Calibration basis
Smoke result: xi_normalized=0.395 at M_frac=1.0 (single point). HARD_PASS requires demonstrating M-frac trend across 6 points.
First correlation-length measurement (calibration probe). Thresholds set with +-50% envelope per policy.

## Timeout
1800s (remote CPU; N=1024 is small; 6 M_fracs x 5 seeds = 30 cells; ~30min budget)
