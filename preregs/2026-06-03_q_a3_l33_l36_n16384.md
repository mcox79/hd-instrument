# Prereg: Q-A3/PP-12 L=33..L=36 cross-layer composition N=16384

**Date:** 2026-06-03
**Anchors:**
  - q_a3_l33_cross_layer_composition_v1_n16384
  - q_a3_l34_cross_layer_composition_v1_n16384
  - q_a3_l35_cross_layer_composition_v1_n16384
  - q_a3_l36_cross_layer_composition_v1_n16384
**Queue:** overnight_queue (GPU)
**Cap map row:** PP-12 / Q-A3 (cross-layer composition, ECC unlimited-depth ladder)

## Context

N=4096 series: L=2..L=35 all EXACT-1.0000 unanimous 5/5 seeds (21 consecutive extensions, last confirmed v362).
N=16384 series: L=20..L=32 all HARD_PASS via v361/v362 cycles (12 consecutive rungs).
These anchors continue the N=16384 depth ladder to L=33, L=34, L=35, L=36.
On-demand W: each layer's W matrix is built and released one at a time. Single-W VRAM = 16384^2 * 4 / 1e9 = 1.073 GB -- safe on 8 GB GPU.

## Hypothesis

Cross-layer composition fidelity remains EXACT-1.0000 at all levels through L=33..L=36 at N=16384, continuing the empirically unbounded-depth ECC pattern. Prediction: HARD_PASS (all 4 anchors) consistent with all prior N=16384 rungs.

## Pre-registered bands

### All rungs L=33..L=36 (same threshold):
- HARD-PASS: all L level fidelities >= 0.9999 unanimous (5/5 seeds) AND l{L}_acc >= 0.5
- MIDDLE: any L_fid in [0.85, 0.9999) OR graceful degradation
- HARD-FAIL: any L_fid < 0.85 OR l{L}_acc < 0.5

## Middle-band outcome plan

If MIDDLE: indicates ceiling beginning to approach at some L in [33..36]. File strategy note
to investigate capacity limits. Do not close PP-12 row; annotate "ceiling approaching near L=X N=16384."
Run bisection to narrow ceiling depth.

## Walk-back gate

Effect size: N=16384 streak L=20..L=32 all fid=1.0000 exact. d >> 1.0. No walk-back needed.

## Timeout estimate

- smoke_wall_s: ~15s estimate (L=32 elapsed ~15s FULL 5-seed GPU; L=33..36 linear scaling)
- FULL_N/smoke_N: 1.0 (N=16384 both)
- FULL_seeds/smoke_seeds: 1.0 (5 seeds both)
- scaling_exp: 1.0 (linear with L; on-demand W build dominates, not N^2)
- ceil(1.5 * 15 * 1.0 * 5) = ceil(113) = 300s
- PROT-019 floor applies for N >= 4096: timeout_s = 14400s each

## PROT-018 N-suffix binding

All 4 anchors have `_n16384` suffix. Each script has `N = 16384` at line 64.
Assert: `N == _N_SUFFIX = 16384` at module scope in each script.

## PROT-021 checkpoint key

Keyed with run_mode + L in get_output_dir(ANCHOR_NAME). Smoke at N_active=512 does not contaminate N=16384 full-run keys because anchor name includes L (distinct key per L).

## Formula self-tests (PROT-022)

Each script contains _selftest_l{L}_chain():
  - L-ctx Hadamard roundtrip recovers xi_L1 exactly (32..35 context ops respectively)
  - Capacity check: M_INNER/N_ACTIVE < ALPHA_C=0.138 for all layers
  - GPU memory > 0 after dummy allocation

## Outcome map

All 4 HARD_PASS -> N=16384 ladder continues; file L=37+ for next cycle.
Any MIDDLE -> ceiling near that L; bisect.
Any HARD_FAIL -> ceiling hit; close N=16384 ladder at that L; report to Strategy.
