# Prereg: Q-B1 heteroassoc chain depth-35 and depth-45 at N=8192

**Date:** 2026-06-02
**Anchors:**
- q_b1_chain_depth_35_v1_n8192
- q_b1_chain_depth_45_v1_n8192

**Capability question:** Does heteroassociative chain retention hold at depths 35 and 45 at N=8192?

**Context:** depth-30 HARD_PASS (cycle 12). depth-40 completed (cycle 12). Depth-35 fills in the gap; depth-45 pushes ceiling.

## Pre-registered bands

### depth-35
- HARD-PASS: depth-5 >= 0.95 AND depth-10 >= 0.88 AND depth-20 >= 0.70 AND depth-30 >= 0.55 AND depth-35 >= 0.50.
- HARD-FAIL: depth-5 < 0.80 OR depth-10 < 0.65 OR depth-20 < 0.40 OR depth-35 < 0.25.
- MIDDLE: depth-35 in [0.35, 0.50) while earlier depths HP.

### depth-45
- HARD-PASS: depth-5 >= 0.95 AND depth-10 >= 0.88 AND depth-20 >= 0.70 AND depth-30 >= 0.55 AND depth-45 >= 0.40.
- HARD-FAIL: depth-5 < 0.80 OR depth-10 < 0.65 OR depth-20 < 0.40 OR depth-45 < 0.20.
- MIDDLE: depth-45 in [0.30, 0.40) while earlier depths HP.

## Smoke results
- depth-35 smoke (N_smoke=1024, 2 seeds): all depths >= HP. d35=0.970. HARD_PASS.
- depth-45 smoke (N_smoke=1024, 2 seeds): d5=0.929 (< HP_D5=0.95 at smoke-N; scale artifact), d45=0.918 >> HP_D45=0.40. MIDDLE_BAND (seed-count + scale artifact). All values >> HF thresholds.
  - Walk-back note: d5 borderline at smoke (0.93 vs 0.95 HP). At full N=8192 the low-depth performance typically improves. FULL run design unchanged. MIDDLE_BAND at smoke does NOT indicate HARD_FAIL at full N.
- GPU util: peak_gpu=0.022GB.

## Timeout estimate

### depth-35
- smoke_wall_s=0.04, FULL_N/smoke_N=8, FULL_seeds/smoke_seeds=2.5, scaling_exp=2.0 (H matrix matmul per step)
- timeout_s = ceil(1.5 * 0.04 * 64 * 2.5) = ceil(9.6) -> 3600s (conservative; 15 chains * 35 steps * N^2 matmul at N=8192)

### depth-45
- smoke_wall_s=0.05, same parameters
- timeout_s = ceil(1.5 * 0.05 * 64 * 2.5) = ceil(12) -> 3600s

## PROT compliance
- PROT-018: _n8192 suffix; N=8192 in FULL config.
- PROT-021: seed checkpoints keyed with run_mode + chain_depth.
- GPU template enforced.
