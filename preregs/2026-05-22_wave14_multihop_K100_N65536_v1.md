# Pre-reg: Wave 14 Multi-hop K=100 at N=65536 v1

**Filed:** 2026-05-22
**Source:** `strategy_request_to_exp_dev_BetY_V2D_mechanism_revision_2026-05-22.md` (Strategy 13:15) — V2.D Phase 1 sub-test #4.
**Predecessor:** Cycle 96 multi-hop K=100 acc_50hop=0.767 NEW HIGH at N=4096.

## Question

At N=65536, K=100 stored facts in BSC bundle M, does the substrate's 50-hop chained-query accuracy match the cycle 96 baseline of 0.767?

## Hypothesis

H_scale: acc_50hop ≥ 0.767. Substrate's bundle-based multi-hop chains scale with N (per-hop cleanup margin grows as √(N/F)).

H_null: acc_50hop < 0.40 — N-scaling fails for multi-hop.

## Pre-declared verdicts

- `MULTIHOP_N65K_PASS` — acc_50hop ≥ 0.767 (matches cycle 96).
- `MULTIHOP_N65K_PARTIAL` — 0.40 ≤ acc_50hop < 0.767.
- `MULTIHOP_N65K_KILLED` — acc_50hop < 0.40.
- `MULTIHOP_N65K_INCONCLUSIVE` — metric collection error.

## Method

Reuses `mh.run_one_seed` from validated `exp_wave14r_multihop_K100.py`:
- 200 entity atoms, 20 relation atoms, 100 facts per fact-base.
- hop_depths = [1, 5, 10, 25, 50].
- 30 trials per depth.
- 2 seeds: [17, 23] full.

Memory: bundle M = N bits = 8 KB; codebooks ~ 16 MB. No W matrix. Fits in 16GB VRAM easily.

## Acceptance thresholds

- 0.767 PASS matches cycle 96 NEW HIGH at N=4096 — substrate must MAINTAIN, not necessarily improve.
- 0.40 lower bound = "substantial but degraded".

## Config

- N=8192 smoke, 65536 full.
- num_entities=200, num_relations=20, num_facts=100.
- hop_depths full: [1, 5, 10, 25, 50].
- n_trials=30, seeds=[17, 23] full.

## Pre-declared interpretation

- **PASS**: V2.D Phase 1 sub-test #4 PASS. Multi-hop scales to N=65536 substrate. 3 of 5 sub-tests now PASS (Bet S + Bet V + multi-hop).
- **PARTIAL**: Partial extension. May indicate hop-depth d=25 cliff is N-sensitive (substrate-physics question).
- **KILLED**: N-scaling fails for multi-hop. Update V2.D Phase 1 to investigate.

## Not in scope

- K > 100 (separate experiment).
- N > 65536.
- Modern dense AM cleanup (V2.D revision explicitly dropped this).
- Per-hop retention (covered in multi-hop_K100 baseline).
