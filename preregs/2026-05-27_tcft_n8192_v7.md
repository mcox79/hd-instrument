# Pre-registration: tcft_n8192_v7 (FULL N=8192 TCFT deletion-certificate)

**Date filed:** 2026-05-27
**Anchor:** tcft_n8192_v7
**Script:** experiments/exp_tcft_n8192_v7.py
**Queue:** remote_cpu_queue
**Parent chain:** tcft_fresh_erase_v4 (HP N=4096) -> tcft_n8192_v5 (timeout 4/5 seeds HP) -> tcft_n8192_v6 (smoke only)

## Hypothesis

TCFT conditioning (thermal-conditioning-Fluctuation-theorem) suppresses free-energy variance
by a factor > 10x (var_ratio < 0.10) relative to unconditioned dynamics at N=8192.

## Experiment design

- N = 8192 (FULL config)
- Seeds = [7, 17, 23, 31, 41] (5 seeds)
- M = 64 stored patterns per seed
- Metric: tcft_variance_ratio = tcft_variance / vanilla_variance

## Pre-registered thresholds

HARD-PASS:
- var_ratio < 0.10 in >= 3/5 seeds at N=8192.
- (Expected 5/5 based on v5 4/5 HP pattern at var_ratio=0.0000)

HARD-FAIL:
- var_ratio >= 1.0 in ALL 5 seeds.
- (Would directly contradict v5 evidence; extraordinary finding)

MIDDLE-BAND:
- var_ratio < 0.10 in exactly 1-2/5 seeds.
- Action: investigate seed-to-seed variance; possible initialization sensitivity.

## Prior anchor

tcft_n8192_v5: 4/5 seeds completed with var_ratio=0.0000 before timeout killed seed=41.
tcft_n8192_v6: smoke only at N=512, 1 seed, var_ratio=0.015486 (HP signal present).

Both v5 and v6 confirm the HP signal is present; v7 completes the FULL run.

## Timeout estimate

- v5 actual: 4 seeds completed in ~1800s => ~450s per seed at N=8192.
- Formula: ceil(1.5 * 450 * 5 seeds) = ceil(3375) = 3600s.
- Safety margin: 5400s (50% above estimate; lessons from v5 timeout failure).
- 5400s < 14400s threshold. Remote-CPU usage is appropriate.

## N-suffix

No _nN suffix on anchor name. Production N = 8192 (N_FULL = 8192 in script config).

## Self-tests verified (pre-ship)

1. run_one_seed(N=512, seed=17) -> var_ratio=0.0155 < 0.10 (HP signal)
2. Multi-scale: run_one_seed(N=2048, seed=17) -> tcft_valid=True, finite var_ratio
3. OOM check: N=8192 float64 matrix = 512MB < 6GB
4. Selftest PASSED printed to stdout before sweep begins
