# Prereg: PP-58 SCS tau sweep d=8 tau_target=0.10 (cycle 46)

**Date:** 2026-06-04
**Anchor:** pp58_scs_tau_sweep_d8_tau010_v1_n8192
**Queue:** remote_cpu_queue (CPU; pure numpy; ~30-60 min total)
**Context:** v376. tau=0.05 HARD_FAILed (d_estimate=1.487 < 1.5; spike absent in purely random Xi).
Strategy rescue R3: tau sweep at d=8 to map tau-vs-spike. This is tau=0.10 (second point in sweep).

## Scientific question

With controlled asymmetry (tau_target=0.10 injected into W via W = (1-tau)*W_sym + tau*W_asym),
does the spike condition d > 1.5 become satisfied, and does SCS formula match empirical gamma?

## Design change from tau=0.05

tau=0.05 test measured tau empirically from random Xi: tau~0.000 (pure symmetric W).
This test CONSTRUCTS W with explicit asymmetry mixing at tau_target=0.10.
This is the correct approach to probe the non-Hermitian regime of SCS theory.

## SCS formula self-test

gamma_SCS(d=8, tau=0.10) = (8 + 0.10/8) / (1 + 0.10) = 8.0125 / 1.10 = 7.2841.

## Pre-registered bands

**HARD-PASS:**
- mean d_estimate >= 1.5 (spike condition met with asymmetric W)
- gamma_SCS within 30% of gamma_emp at 4/5 seeds AND mean ratio in [0.7, 1.3]
- tau_actual in [0.07, 0.14] (within 40% of tau_target=0.10)

**MIDDLE_BAND:**
- d_estimate in [1.2, 1.5) (marginal spike) OR ratio in [0.5, 1.5)
- OR tau_actual outside [0.07, 0.14] but d_estimate >= 1.2

**HARD-FAIL:**
- mean d_estimate < 1.2 (no spike even with asymmetric W)
- OR mean ratio < 0.5 or > 2.0 (SCS formula off by >2x)
- OR tau_actual < 0.05 (asymmetry injection failed)

## Timeout estimate

tau=0.05 test: per-seed ~10-20 min (eigvalsh at N=8192 dominates). 5 seeds = 50-100 min.
This test adds eigvalsh instead of SVD. Similar cost.
Formula: ceil(1.5 * 1200 * 5) = 9000s. Use PROT-019 floor: 21600s.

## PROT-018 binding

anchor _n8192; script sets N = 8192. Verified pre-ship.

## Prior empirical anchor

tau=0.05 test HARD_FAILed (d=1.487 < 1.5). No prior anchor in controlled-asymmetry regime.
Calibration probe: bands set per +-50% policy around SCS prediction.
  gamma_SCS(d=8, tau=0.10) = 7.28; +-50% gives HP ratio [0.7, 1.3].
  No prior empirical anchor for controlled-asymmetry regime.
  Note: bands widened to +-50% per calibration-probe policy (no prior empirical anchor in this regime).

## Tau sweep context

Full planned sweep: tau={0.05 (done, HF), 0.10 (this), 0.15 (deferred), 0.20 (deferred), 0.30 (deferred)}.
tau=0.05 result: d=1.487 (just below threshold). Hypothesis: higher tau may push d above 1.5.
