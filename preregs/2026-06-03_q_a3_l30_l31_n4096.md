# Prereg: Q-A3 L=30 and L=31 cross-layer composition at N=4096

**Date:** 2026-06-03
**Anchors:** q_a3_l30_cross_layer_composition_v1_n4096, q_a3_l31_cross_layer_composition_v1_n4096
**Queue:** overnight_queue (GPU)

## Hypothesis

Substrate cross-layer composition depth ceiling has not been found through L=29 (cycle 27). Both L=30 and L=31 are expected to show EXACT-1.0 fidelity at all levels, continuing the unbroken series from L=2 to L=29.

## Pre-registered bands

**HARD-PASS:** all L fidelities >= 0.9999 unanimous 5/5 seeds AND l_acc >= 0.5
**MIDDLE:** any L_fid in [0.85, 0.9999) OR graceful degradation
**HARD-FAIL:** any L_fid < 0.85 OR l_acc < 0.5 (ceiling found)

## Formula self-tests (PROT-022)

1. L=30: 29-ctx Hadamard roundtrip recovers xi_L1 exactly.
   [INPUT: +-1 vectors, 29 context ops] [EXPECTED: decode = xi_L1 exactly, atol=1e-6]
2. L=31: 30-ctx Hadamard roundtrip recovers xi_L1 exactly.
   [INPUT: +-1 vectors, 30 context ops] [EXPECTED: decode = xi_L1 exactly, atol=1e-6]
3. All alphas < alpha_c=0.138 at N=4096 (M_INNER=100 -> alpha=0.0244)
4. GPU memory: 30 * 4096^2 * 4 / 1e9 = 2.013 GB < 2.2 GB (L=30); 31 * 4096^2 * 4 / 1e9 = 2.080 GB < 2.2 GB (L=31)

## Timeout estimate

L=29 elapsed ~0.8s FULL 5-seed GPU (similar to L=27 ~0.71s, L=28 transient crash).
L=30: ceil(1.5 * 0.8 * (30/29) * 1.0) = 300s (GPU queue overhead floor).
L=31: ceil(1.5 * 0.8 * (31/29) * 1.0) = 300s (GPU queue overhead floor).

## Band rationale

Prior: L=2..L=29 all EXACT-1.0000 unanimous (unbroken series). Isochoric M-halving ensures
all mid-stages saturate at M=2 from L=7+. Alpha < alpha_c at all stages. Expectation = HP.
Bands follow EXACT-1.0 criterion from established series.

## N-suffix section

Both anchors have _n4096; N = 4096 in both scripts. PROT-018 verified.
