# Prereg: asdiv_pp375_wk_cpu_v1
**Date:** 2026-06-12  **Lane:** CPU  **Routing:** synthesize cycle's two wins -- PP-375 mechanism (best, 0.378) + WK constants (oracle +0.114).
PP-375 +search variant + question-guided WK constant injection (X_per_Y fires when target~X and Y present); the search variant CAN
use injected WK constants for world-knowledge items. A/B: +search vs +search+WK. ASDiv-1op.
HARD-PASS 1op>=0.45 (wins compound). MIDDLE WK-lift>=0.02 OR 1op>=0.40. HARD-FAIL WK adds nothing.
Smoke (easier first-200): +search 0.4286 -> +search+WK 0.4870 (WK lift +0.058)! Full is the honest number.
