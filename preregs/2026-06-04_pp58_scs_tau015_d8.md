# Prereg: PP-58 SCS tau sweep tau=0.15 at d=8 (cycle 47 all-night burst)

**Date:** 2026-06-04
**Anchor:** pp58_scs_tau_sweep_d8_tau015_v1_n8192
**Queue:** remote_cpu_queue (pure numpy; eigvalsh + kappa3)

## Context

tau=0.05 HARD_FAILed (d_estimate < 1.5, spike absent). tau=0.10 queued (cycle 46).
This continues the tau sweep at tau=0.15 to map tau-vs-spike relationship.

Method: W = (1-tau)*W_sym + tau*W_asym where W_sym is standard Hopfield and W_asym
is a normalized skew-symmetric random matrix. This SETS tau rather than measuring it.

SCS formula: gamma = (d + tau/d) / (1+tau).
At d=8, tau=0.15: gamma = (8 + 0.15/8) / 1.15 = 8.01875/1.15 = 6.9728.

## Pre-registered bands

**HARD-PASS:**
- mean d_estimate >= 1.5 (spike condition met with asymmetric W).
- gamma_SCS within 30% of gamma_emp at 4/5 seeds AND mean ratio in [0.7, 1.3].
- tau_actual in [0.10, 0.21] (within 40% of tau_target=0.15).

**MIDDLE:**
- d_estimate in [1.2, 1.5) (marginal spike) OR ratio in [0.5, 1.5).
- tau_actual outside [0.10, 0.21] but d_estimate >= 1.2.

**HARD-FAIL:**
- mean d_estimate < 1.2 (no spike).
- OR mean ratio < 0.5 or > 2.0 (formula off by >2x).
- OR tau_actual < 0.07 (asymmetry injection failed).

## Formula self-tests (PROT-022)

1. SCS gamma(d=8, tau=0.15) = 6.9728. [VERIFIED at write time: 6.9728 OK]
2. SCS gamma(d=8, tau=0) = 8.0. [VERIFIED: 8.0 OK]
3. SCS gamma(d=1, tau=0) = 1.0. [VERIFIED: 1.0 OK]
4. M = int(0.05 * 8192) = 409. [VERIFIED: 409 OK]

## PROT-018 binding

Anchor _n8192; N=8192 in script. Verified via grep.

## Smoke behavior note

Smoke at N=256 produces HARD_FAIL from ratio >> 2.0. This is a KNOWN SCALE ARTIFACT:
- eigvalsh d estimator is unstable at M=12 (N=256, alpha=0.05).
- self-test PASSES: tau_actual > 0, d > 0, gamma_emp > 0 (all non-zero, non-null).
- tau_actual=0.174 at smoke scale (near target=0.15, asymmetry injection working).
- The ratio failure is from d=18-20 at N=256 vs expected d~1.5-8 at N=8192.
- Precedent: tau=0.10 (cycle 46) shipped with same smoke behavior.
- Verdict bands designed for N=8192 FULL scale only.

## Timeout estimate

tau=0.10 precedent: ~20-30 min estimated per seed (eigvalsh at N=8192 + kappa3).
formula: ceil(1.5 * 1200 * 1.0 * 5) = 9000s estimated.
Use floor: **21600s** (6h).
