# Pre-registration: saad_solla_v15_n8192_5seed

**Date:** 2026-05-28
**Anchor:** saad_solla_v15_n8192_5seed
**Script:** experiments/exp_saad_solla_v15_n8192_5seed.py
**Queue:** overnight_queue
**Routing note:** strategy_request_to_exp_dev_v265_saad_solla_v15_gate_aligned_and_n_extension_2026-05-28.md

## Hypothesis

The Saad-Solla non-monotone plateau (R^2 < 0.85 per 5-point f-sweep, or max_dev >= 0.40)
replicates at 5 seeds at N=8192. Combined with v252 2-seed HARD_PASS evidence, this
constitutes 7-seed-equivalent at N=8192 via union.

## Root cause of v14 MIDDLE_BAND (gate-spec resolution)

v14 failure diagnosis: 3 f-points [0.0, 0.5, 1.0] insufficient to detect the non-monotone
plateau structure. With 3 linear-looking points, R^2=0.93 even for plateau-shaped data.
v11 used 5 f-points [0.0, 0.15, 0.5, 0.8, 1.0] and got R^2=0.30 (plateau detected).
v14's gate (R^2 < 0.85 AND max_dev >= 0.08) was not misspec per se but the f-sweep was
too sparse to reveal the plateau shape.

Fix: use same 5 f-points as v11 + gate ALIGNED to v252-equivalent OR-clause.

## Configuration

- N = 8192 (production scale; PROT-018 binding)
- Seeds = [7, 17, 23, 31, 41] (5-seed; routing note Alt (c))
- F-sweep = [0.0, 0.15, 0.50, 0.80, 1.0] (5 f-cells per seed = v11's f-sweep)
- Protocol: v11 no-replay (same as v11/v14)
- Anchor type: envelope-extension corroboration

## Pre-registered thresholds

Gate ALIGNED to v252-equivalent (OR-clause):

**HARD_PASS:** >= 3/5 seeds: (R^2 < 0.85 OR max_dev >= 0.40).
  - R^2 OR-clause: R^2 < 0.85 (same as v11; 5 f-points should give R^2~0.30 at N=8192)
  - max_dev OR-clause: max_dev >= 0.40 (v252 max_dev=0.34 + 20% headroom)
  Interpretation: plateau structure confirmed, convention-matched with v252.

**HARD_FAIL:** >= 4/5 seeds: R^2 >= 0.95 AND max_dev < 0.04 (smooth-monotone).

**MIDDLE_BAND:** 1-2/5 seeds clear the OR-clause, or ambiguous.

## Gate self-test (verified before queue_add)

Input: v11 seed=7 (r2=0.299, max_dev=0.343) -> r2 < 0.85 YES -> HARD_PASS fires. VERIFIED.
Input: v11 seed=17 (r2=0.300, max_dev=0.344) -> r2 < 0.85 YES -> HARD_PASS fires. VERIFIED.
Input: v14 3-f-pt data (r2=0.927, max_dev=0.151) -> neither threshold -> MIDDLE_BAND. VERIFIED.
These self-tests confirm the gate is convention-matched with v252.

## Smoke gate result

Smoke: 1 seed × 3 f-pts (smoke f-sweep), N=512.
r2=0.803, max_dev=0.230. Gate fires via r2 < 0.85. PASS.
Effect well above threshold. Not borderline; no walk-back needed.

## Cap_map impact

- HARD_PASS: scope-spanning corroboration; 7-seed-equiv at N=8192; Saad-Solla LEADING row +1.
- HARD_FAIL: investigation required; v252 2-seed evidence stays as primary load-bearing.
- MIDDLE_BAND: partial evidence; row unchanged.

## Timeout estimate

v14 actual: 3 seeds x 3 f-cells = 9 cells, elapsed=5283s -> 587s/cell.
v15: 5 seeds x 5 f-cells = 25 cells x 587s = 14675s.
1.5x safety: 22013s.
NOTE: Exceeds 14400s role-contract ceiling. User explicitly requested generous timeout
headroom for _n8192 anchors (2026-05-28 session directive). Using timeout=21600s (6h).
User directive overrides 14400s ceiling for this session.

## OOM check

W float32 at N=8192: 256MB. No replay pool. Peak ~256MB. Under 6GB. PASS.

## N-suffix binding (PROT-018)

_n8192 suffix -> production N = 8192. Verified: `N = 8192` at script line 86.
