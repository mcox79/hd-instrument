# Prereg: Q-A3 L=52, L=53 cross-layer composition N=16384

Date: 2026-06-03
Cycle: v369 refill (Cycle 38)
Anchors: q_a3_l52_cross_layer_composition_v1_n16384, q_a3_l53_cross_layer_composition_v1_n16384

## Context

N=16384 depth ladder series: {L=20..L=51} all HARD_PASS EXACT-1.0000 (32 rungs; deepest L=51 confirmed v369). PP-12/Q-A3 band 0.84-0.97 (v369 band-lift). This batch adds rungs 33 and 34.

## Scientific question

Does the EXACT-1.0000 fidelity property hold at L=52 and L=53 at N=16384? Is the unlimited-depth ECC property confirmed through 2 additional rungs beyond current record?

## Pre-registered bands (identical structure; L varies)

For each of L=52, L=53:
- HARD-PASS: all L level fidelities >= 0.9999 unanimous (5/5 seeds) AND lL_acc >= 0.5
- MIDDLE-BAND: any L_fid in [0.85, 0.9999) OR graceful degradation
- HARD-FAIL: any L_fid < 0.85 OR lL_acc < 0.5

## Timeout estimate

Prior anchor L=51 FULL elapsed 25.3s (5-seed). L=52/L=53 near-linear scale (+1 rung, <5% overhead).
Formula: ceil(1.5 * 35 * 1.0 * 5) = ceil(262) -> 300s per anchor (floor applied).
Source anchor: L=51 elapsed_s=25.3s from v369 remote metrics.

## N-suffix binding (PROT-018)

Both anchors have _n16384; production N = 16384 in each script. Verified: grep N=16384 returns match; _N_SUFFIX = 16384; assert N == _N_SUFFIX present.

## PROT-021

Seed checkpoints keyed with run_mode + L. Smoke uses N_ACTIVE=512, FULL uses N=16384.

## Memory estimate

On-demand W: 16384 x 16384 x 4 bytes = 1073 MB per layer. Single W at a time. Fits in 8 GB GPU with margin (peak observed ~2.19 GB at L=51).

## Formula self-tests (PROT-022)

1. L_DEPTH chain: (L-1)-ctx Hadamard roundtrip recovers xi_L1. Verified at module scope.
2. All alphas < alpha_c=0.138. M_INNER=100 -> alpha=0.0061 at N=16384. M_MID max=50 -> alpha=0.0031.
3. GPU memory > 0 after W build.
4. len(M_MID) == L_DEPTH - 2 assertion verified at module scope.

## Middle-band outcome plan

If any rung hits MIDDLE-BAND: depth ceiling detected at that L. File rescue: identify first L where fid < 0.9999. Investigate M_MID schedule or alpha near-capacity effects. Do NOT band-lift on incomplete sequence.

## Smoke policy

GPU scripts; local CPU has no CUDA. Using --skip-smoke path per exp_dev policy: anchor closest to L=51 (elapsed_s=25.3s from v369) used as timing anchor. Scripts structurally identical to L=49/50/51 which passed FULL 5-seed. Suspicious-result gate: any deviation from EXACT-1.0000 pattern would be anomalous given 32-rung streak.

## Queue

overnight_queue (GPU). Both are depth probes (N=16384 compute-heavy). Timeout 300s each.
