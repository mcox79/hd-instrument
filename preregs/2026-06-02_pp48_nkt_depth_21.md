# Pre-registration: pp48_nkt_depth_21_v1_n4096

**Filed:** 2026-06-02  
**Anchor:** pp48_nkt_depth_21_v1_n4096  
**Queue:** overnight_queue  
**Script:** experiments/exp_pp48_nkt_depth_21_v1_n4096.py

## Hypothesis
Negative-knowledge tree (NKT) substrate repels depth-21 leaf patterns via anti-Hebbian inhibition at N=4096.
Depth-19 HARD_PASS this cycle. Depth-21 ceiling push.

## Pre-registered threshold bands

**HARD-PASS:** pos_retrieval_rate >= 0.75 AND nkt_repulsion_rate >= 0.65.  
**HARD-FAIL:** pos_retrieval_rate < 0.40 OR nkt_repulsion_rate < 0.30.  
**MIDDLE:** 1/2 conditions met.

Calibration: depth-13, depth-17, depth-19 all HARD_PASS with pos=nkt=1.0. Bands unchanged; the NKT property depends on binding depth (not tree enumeration), and capacity is fixed (alpha=0.027) regardless of NKT_DEPTH. Bands stable across depth extension.

## Smoke results

seed=7: pos_rate=1.0000, nkt_rep_rate=1.0000, elapsed=0.27s, peak_gpu=0.009GB  
seed=17: pos_rate=1.0000, nkt_rep_rate=1.0000, elapsed=0.14s, peak_gpu=0.009GB  
**VERDICT: HARD_PASS** (smoke, N=512)

GPU util assertion lowered to 0.001 GB (1 MB) threshold: correct for this experiment class where Xi tensors are K x N float32 (K=20, N=512) = 0.04 MB each.

Effect size at smoke: pos=nkt=1.0 >> all thresholds. No walk-back needed.

## Timeout estimate

smoke_wall_s = 0.27s at N=512, 2 seeds  
FULL_N = 4096, smoke_N = 512, ratio = 8  
FULL_seeds = 5, smoke_seeds = 2  
scaling_exp = 1.5 (Xi @ state vector ops; no N x N matrix stored)  
timeout_s = ceil(1.5 * 0.27 * 8^1.5 * (5/2)) = ceil(1.5 * 0.27 * 22.6 * 2.5) = ceil(22.9) = 300s  
Corrected timeout (2026-06-02 pre-ship fix): depth-17 at N=4096 took ~89s for 5 seeds. depth-21: ceil(1.5 * 89 * (21/17) * 1.0) = ceil(165.6) = **timeout_s = 300s**

## PROT-018 check

grep N=4096 binding: `N = 4096` and `_N_SUFFIX = 4096` present. PASS.

## N-suffix section

_n4096 suffix: production N = 4096. Smoke uses N_ACTIVE = 512.

## TOTAL_NKT formula self-test

BRANCH=2, D=21: sum(2^d for d in range(21)) = 2^21 - 1 = 2097151.  
Script asserts at module scope: `TOTAL_NKT_FULL == 2097151`. Passed in smoke run.
