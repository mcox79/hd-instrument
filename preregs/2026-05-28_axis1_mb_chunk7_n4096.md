# Pre-registration: axis1_mb_chunk7_n4096

**Date:** 2026-05-28
**Anchor:** axis1_mb_chunk7_n4096
**Script:** experiments/exp_axis1_mb_chunk7_n4096.py
**Queue:** overnight_queue
**Parent:** axis1_mb_chunk6_n4096 (FULL transition surface; this extends to tail regime)

## Hypothesis

In the far over-capacity tail (M/N in {16, 20}), the substrate retains measurable softmax
confidence (>= 0.10) at medium-high beta values (32-64), consistent with partial pattern
retrieval observed in chunk2 (argmax_ret=0.238 at M/N=16).

## Configuration

- N = 4096 (PROT-018 binding via _n4096 suffix)
- M_fracs = [16.0, 20.0] (tail regime; scope-limited to avoid OOM and wall-time ceiling)
- Beta = 10-point grid [0.5..256]
- Seeds = [7, 17, 23] (3-seed)
- Total = 60 cells

## Design note

Bug fix from smoke iteration: initial chunk7 design probed random keys NOT stored in W.
Fixed to use store_facts_batched() and probe the actual stored pattern set.
Empirical probe at N=4096, M/N=16, beta=32: conf=0.23 (confirms real signal).

## Pre-registered thresholds

Calibration probe (first softmax confidence measurement at M/N>12).
Prior anchor: argmax_ret=0.238 at M/N=16 (chunk2 N=4096; directional prior exists).
Bands NOT widened to +-50% because we have a directional prior.

**HARD_PASS:** mean softmax_conf >= 0.10 at M/N=16 and beta in {32, 64}
  AND M/N=20 conf <= M/N=16 conf (monotone in M).

**HARD_FAIL:** softmax_conf < 0.001 at M/N=16 for ALL beta values.
  Would contradict chunk2 argmax result.

**MIDDLE_BAND:** softmax_conf in [0.001, 0.10) at M/N=16 best-beta.

## Cap_map impact

HARD_PASS: Axis-1 phase diagram tail characterized; annotation noting partial signal
retention at M/N=16 with high beta.
HARD_FAIL: requires investigation of why softmax doesn't track argmax.

## Timeout estimate

- Per-cell calibration: store_facts_batched at M=65536 ~14s/cell at N=4096.
- M/N=20: ~17.5s/cell (linear in M).
- 2 M_fracs x 10 betas x 3 seeds: but W built once per (M_frac, seed), not per beta.
- Actually W built per (M_frac, seed, beta) since run_one_cell_chunk7 builds W each call.
- Total: (14+17.5)s/cell_pair x 10 betas x 3 seeds = 945s. Safety 1.5x: 1418s.
- PROT-019 floor for N=4096: 3600s. timeout_s = 3600.

## OOM check

M/N=20 at N=4096: M=81920. Peak (keys + vals + W + CB) = 3.02GB. Under 6GB. PASS.

## Smoke gate

Passed: N=1024 1-seed 3 betas at M/N=16. conf={0.179, 0.247, 0.223} at beta={8, 32, 256}.
HARD_PASS: M16_midhibeta=0.247 >= 0.10. elapsed=0.6s.
