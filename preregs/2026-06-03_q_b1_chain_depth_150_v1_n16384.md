# Pre-registration: Q-B1 heteroassoc chain depth-150 at N=16384

**Date:** 2026-06-03
**Anchor:** `q_b1_chain_depth_150_v1_n16384`
**Queue:** overnight_queue
**Trigger:** Q-B1 depth-100 HARD_PASS at N=16384 (anomalously flat profile d5-d100 ~0.96). BAND-LIFT 0.80-0.95 eligible. Pushing to d-150 to characterize full extent.
**Priority:** Q-B1 depth ceiling characterization + v343 BAND-LIFT eligibility confirmation.

## Capability question

Does substrate Q-B1 heteroassociative chain maintain usable fidelity at depth-150 at N=16384?
If d-150 meets the HP threshold (d150 >= 0.05), this confirms the extremely flat fidelity
profile is not an artifact and supports the BAND-LIFT from 0.65-0.80 to 0.80-0.95.

## Prior results

| Anchor | depth | N | verdict | d100 value |
|---|---|---|---|---|
| q_b1_chain_depth_100_v1_n16384 | 100 | 16384 | HARD_PASS | near-flat ~0.96 |
| q_b1_chain_depth_80_v1_n16384 | 80 | 16384 | HARD_PASS | flat profile |

NOTE: Smoke at N=1024 shows rapid decay (d5~0.2) -- this is a SCALE ARTIFACT. Prior d-100
at N=16384 also fails smoke at N=1024 but HARD_PASSES at full N=16384. Smoke instrumentation
is valid (non-null metrics, valid cosines); only the verdict bands are calibrated for N=16384.

## Pre-registered bands

Calibration note: no prior empirical anchor at d-150; bands derived from flat-profile prior
(d-100 ~0.96 at N=16384). Bands set at +-50% of extrapolated theory per calibration-probe policy.

### HARD-PASS
d5 >= 0.90 AND d20 >= 0.75 AND d50 >= 0.50 AND d100 >= 0.20 AND d150 >= 0.05.
BAND-LIFT 0.80-0.95 eligible upon HARD_PASS.

### MIDDLE
d150 in [0.02, 0.05) while earlier depths meet HP.

### HARD-FAIL
d5 < 0.80 OR d20 < 0.50 OR d150 < 0.02.

## Formula self-tests (PROT-022)

1. depth-150 chain retrieval non-NaN at N=128.
   [INPUT: N=128, CHAIN_DEPTH=150] [EXPECTED: cos non-NaN in [-1,1]]
2. Cosine of stored pattern with itself = 1.0.
   [INPUT: xi vs xi] [EXPECTED: cos_self = 1.0]
3. GPU memory > 0 after H build.

## Smoke result

N_ACTIVE=1024, 2 seeds, wall=0.58s:
HARD_FAIL at N=1024 (scale artifact; same as prior d-100 which also fails at N=1024).
Self-test PASSED. Metrics non-null (d1=0.75, d3=0.42 -- valid, not all-zero).
Decision: ship based on empirical precedent (d-100 N=16384 showed same smoke pattern).

## Timeout estimate

Prior d-100 at N=16384: FULL runtime approximately 60-120s per seed (H matrix build 1073 MB).
Scaling d-150/d-100 = 1.5x chains: timeout = ceil(1.5 * 90 * 1.5^1.0 * 1.0) = ceil(202.5) = 300s.
Conservative: 1200s (accounting for 150 chain steps vs 100, 15 chains each).
