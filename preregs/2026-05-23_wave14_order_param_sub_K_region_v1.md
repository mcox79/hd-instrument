# Pre-reg: Sub-K-region order parameter (Strategy 09:35 P4)

Follow-up to Gap 2 FULL ORDER_PARAM_NONE refutation. Test q_overlap stability within sub-K regions: K_RESONANCE_BROAD band (900-1500), normal cycles (100-500), longer cycles (2000+). 5 seeds at N=16384.

## Verdicts
- `ORDER_PARAM_SUB_REGION_STABLE` — any region seed-consistency >= 0.85.
- `ORDER_PARAM_HIERARCHICAL` — q means cluster with >=3 plateaus across regions.
- `ORDER_PARAM_GLOBAL_NONE_CONFIRMED` — all regions unstable.
