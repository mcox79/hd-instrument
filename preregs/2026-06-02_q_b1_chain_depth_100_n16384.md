# Pre-registration: Q-B1 chain depth-100 at N=16384

**Date:** 2026-06-02
**Anchor:** `q_b1_chain_depth_100_v1_n16384`
**Queue:** overnight_queue (GPU)
**Trigger:** Candidate E from v349 REFILL (Q-B1 cross-N N=16384 d-100 deeper).
**Prior:** Q-B1 d=80 N=16384 completed (HARD_PASS); Q-B1 d=100 N=8192 HARD_PASS (d5-d100 all ~0.96, flat profile confirmed).

## Capability question

Does substrate Q-B1 heteroassociative chain flat profile (d5-d100 all ~0.96) replicate at N=16384, confirming the flat profile is N-independent?

## Prior results

| Anchor | N | d=80 | d=100 | Verdict |
|---|---|---|---|---|
| q_b1_chain_depth_100_v1_n8192 | 8192 | 0.9820 | 0.9624 | HARD_PASS |
| q_b1_chain_depth_80_v1_n16384 | 16384 | (apex) | - | HARD_PASS |

At N=8192, d5-d100 flat profile: d5=0.9621, d10=0.9620, d20=0.9618, d30=0.9615, d45=0.9628, d100=0.9624.
This is highly anomalous (expected super-exponential decay at depth) -- the substrate maintains near-perfect
retrieval across the entire depth range tested.

## Pre-registered bands

### HARD-PASS
depth-5 >= 0.95 AND depth-10 >= 0.88 AND depth-20 >= 0.70
AND depth-30 >= 0.55 AND depth-45 >= 0.40 AND depth-100 >= 0.055.

Calibration: if flat profile holds at N=16384 as at N=8192, d100 > 0.95.
HP threshold 0.055 is extremely conservative (accounts for N-scaling regression).
HF threshold 0.05 is 2.5x below HP.

### MIDDLE
depth-100 in [0.05, 0.06) while earlier depths meet HP.
Indicates some decay but not catastrophic.

### HARD-FAIL
depth-5 < 0.80 OR depth-10 < 0.65 OR depth-20 < 0.40 OR depth-100 < 0.05.

## N-suffix binding (PROT-018)

Script: `N = 16384`, `_N_SUFFIX = 16384`. Anchor name has `_n16384`. Matches.
stamp_anchor.py generated script; PROT-018 assertion in script header.

## GPU memory check

H matrix at N=16384: 16384^2 * 4 bytes = 1073 MB. Fits in 8 GB GPU with ~7 GB headroom.
Peak at N=16384 d=100: H (1073 MB) + probe vectors (minimal) ~ 1.1 GB. No OOM risk.

## Timeout estimate

stamp_anchor.py records prior depth-80 at N=16384.
Use Q-B1 d=100 N=8192 elapsed_s=162s as anchor.
Scale: (N=16384 / N=8192)^2.0 (matrix-multiply dominant) = 4x. Seeds same (5).
timeout_s = ceil(1.5 * 162 * 4 * 1.0) = ceil(972) = 972s -> round to **1200s**.
Well under 14400s.

## Dependency verification

No data dependencies. Script is self-contained.
Import: `experiments._seed_checkpoint` present in repo.
