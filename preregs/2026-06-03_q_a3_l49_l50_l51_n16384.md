# Prereg: Q-A3 L=49, L=50, L=51 cross-layer composition N=16384

Date: 2026-06-03
Cycle: v367 refill
Anchors: q_a3_l49_cross_layer_composition_v1_n16384, q_a3_l50_cross_layer_composition_v1_n16384, q_a3_l51_cross_layer_composition_v1_n16384

## Context

N=16384 depth ladder series: {L=20..L=48} all HARD_PASS EXACT-1.0000 (29 rungs; deepest L=48 completed v367). PP-12/Q-A3 band 0.82-0.95 (v366 band-lift). This batch adds rungs 30, 31, 32.

## Scientific question

Does the EXACT-1.0000 fidelity property hold at L=49, L=50, L=51 at N=16384? Is the unlimited-depth ECC property confirmed through 3 additional rungs beyond current record?

## Pre-registered bands (identical structure; L varies)

For each of L=49, L=50, L=51:
- HARD-PASS: all L level fidelities >= 0.9999 unanimous (5/5 seeds) AND lL_acc >= 0.5
- MIDDLE-BAND: any L_fid in [0.85, 0.9999) OR graceful degradation
- HARD-FAIL: any L_fid < 0.85 OR lL_acc < 0.5

## Timeout estimate

Prior anchors L=47/L=48 ran in ~21-25s (5-seed FULL). L=49-51 near-linear in L.
Formula: ceil(1.5 * 30 * 1.0 * 5) = 225 -> 300s per anchor.

## N-suffix binding (PROT-018)

All 3 anchors have _n16384; production N = 16384 in each script. Verified.

## PROT-021

Seed checkpoints keyed with run_mode + L. Smoke uses N_ACTIVE=512, FULL uses N=16384.

## Memory estimate

On-demand W: 16384 x 16384 x 4 bytes = 1073 MB per layer. Single W at a time. Fits in 8 GB GPU.

## Formula self-tests

1. Lk chain: (L-1)-ctx Hadamard roundtrip recovers xi_L1. Verified at module scope for each script.
2. All alphas < alpha_c=0.138. M_INNER=100 -> alpha=0.0061 at N=16384. All M_MID entries <= 50 -> alpha <= 0.0031.
3. GPU memory > 0 after W build.

## Middle-band outcome plan

If any rung hits MIDDLE-BAND: depth ceiling detected. File rescue: identify first L where fid < 0.9999, investigate M_MID schedule or alpha near-capacity effects. Do NOT band-lift on incomplete rung sequence.

## Queue

overnight_queue (GPU). All 3 are depth probes (N=16384 compute-heavy).
