# Prereg: PP-58 SCS tau sweep d=8 tau=0.30 N=8192 cycle 48

## Anchor
pp58_scs_tau_sweep_d8_tau030_v1_n8192

## Context
Continuing tau sweep past tau=0.20 (queued, cycle 48).
tau=0.05: HARD_FAIL. tau=0.10-0.20: completed/queued. tau=0.30 maps higher asymmetry.
W = (1-0.30)*W_sym + 0.30*W_asym where W_sym = Xi^T Xi / N, W_asym = antisymmetric random.

## SCS Formula
gamma = (d + tau/d) / (1 + tau)
At d=8, tau=0.30: gamma = (8 + 0.0375) / 1.30 = 8.0375 / 1.30 = 6.1827

## Pre-registered thresholds
- HARD-PASS:
  - mean d_estimate >= 1.5 (spike condition met)
  - gamma_SCS within 30% of gamma_emp at 4/5 seeds AND mean ratio in [0.7, 1.3]
  - tau_actual in [0.20, 0.42] (within 40% of tau_target=0.30)
- MIDDLE:
  - d_estimate in [1.2, 1.5) OR ratio in [0.5, 1.5)
  - tau_actual outside [0.20, 0.42] but d_estimate >= 1.2
- HARD-FAIL:
  - mean d_estimate < 1.2 (no spike)
  - OR mean ratio < 0.5 or > 2.0 (SCS off by >2x)
  - OR tau_actual < 0.14 (asymmetry injection failed)

## Timeout estimate
- Smoke wall (tau=0.15 precedent): ~1200s per seed
- FULL formula: ceil(1.5 * 1200 * 5) = 9000s
- Applied floor: 21600s (6 hours)
