# Prereg: tier5_self_discovery_rule_extraction_cpu_v1
**Date:** 2026-06-12  **Lane:** CPU  **Routing:** Research Cycle 46/47 Tier-5 self-discovery scope.
Substrate mines its OWN structured solution_history (atom.solution_history: capability->mechanism->metric replacement chains) to PROPOSE
a methodology rule NOT in its 18 RULE atoms. Patterns: recurring replacement (old->new across >=2 caps, +lift) + universal lever (mech
current-best >=3 caps). Novelty = mechanism-pair not already in any rule. Substrate-only, no LLM-judge.
HARD-PASS NOVEL rule >+0.05 lift >=2 caps. MIDDLE re-derives existing rules (miner validated) OR <0.05 lift. HARD-FAIL no pattern.
